---
milestone: v2.13
audited: 2026-07-18T13:00:00.000Z
status: tech_debt
scores:
  requirements: 33/33
  phases: 10/10
  integration: 31/33
  flows: 3/3
gaps: []
tech_debt:
  - phase: 107-m-dulos-documentos-financeiro
    items:
      - "DOF-01 scope gap (found by this audit, not a Phase 107 defect): a 4th document-upload progress bar exists in web/src/app/(dashboard)/pareceres/[id]/page.tsx (NovaVersaoForm anexo-upload flow, ~lines 361-450) still using a hand-rolled div instead of the official Progress component. Phase 107's own locked scope (107-CONTEXT.md) only ever named 3 sites; Phase 108's locked scope (108-CONTEXT.md) never named Progress at all. Flagged as background task task_af5aaa48 — small, well-scoped fix for a future session, not blocking this milestone's close."
  - phase: 109-notifica-es-settings-setup-wizard
    items:
      - "RESOLVED DURING THIS AUDIT: notificacoes/page.tsx was the one remaining screen using the pre-fix `!permissions.isLoading` RBAC guard instead of the app-wide `permissions.isFetched` pattern Phases 103/105/106/107/108 all adopted. Fixed directly, commit 7a8f28e."
  - phase: 108-m-dulo-pareceres
    items:
      - "useAdminUsers() called unconditionally (no enabled guard) in all 3 Pareceres files; the backend endpoint it calls is ADMIN-only, so any non-ADMIN role gets a spurious 500 toast and the Advogado picker never populates. Pre-existing (confirmed present at pre-Phase-108 base commit), unrelated to Phase 108's Select/NativeSelect/Tooltip/Accordion migration. See 108-m-dulo-pareceres/deferred-items.md."
      - "Pareceres 'Prioridade' NativeSelect defaulted to ALTA instead of the documented MEDIA on unedited submission (pre-existing, confirmed via git show against the pre-Phase-108 base). An independent fix session already completed this (branch claude/mystifying-leavitt-6657b5) but is not yet merged into master — merging is the user's call."
  - phase: 107-m-dulos-documentos-financeiro
    items:
      - "Document upload crashes 100% of the time on every NEW upload (Hibernate StaleObjectStateException from a manually-pre-assigned @Version field routing save() through merge() instead of persist()). Pre-existing, backend-only, unrelated to Phase 107's frontend-only scope. An independent fix session already completed this (Persistable<UUID> pattern, branch claude/jovial-lederberg-d017b2) but is not yet merged into master."
  - phase: 103-m-dulo-dashboard
    items:
      - "AtividadeRecenteCard's Empty-state branch is unreachable dead code (backed by a hardcoded 3-entry array). Phase 103's own VERIFICATION.md already documented and explicitly accepted this as intentional/out-of-scope; re-confirmed unchanged by this audit's integration check."
      - "A separate, genuine hydration-mismatch bug in this same component (unrelated to the dead Empty-state branch) was root-caused during Phase 109's live UAT this session and already independently fixed (branch claude/sad-fermat-78bc39), not yet merged into master."
---

# Milestone v2.13 Audit: Refactor UI/UX (shadcn/ui)

**Audited:** 2026-07-18
**Scope:** Phases 101–110 (10 phases, 42 plans)
**Status:** ⚡ tech_debt — all requirements satisfied, no blockers, minor accumulated debt to review

## Summary

Milestone v2.13 replaced ~15 categories of hand-rolled UI (toggle-button tabs, native `<select>`s, ad hoc progress bars, manual dropdowns, custom tooltips, a one-off notification badge, a static setup checklist, and a fully absent mobile nav on the public landing page) with shadcn/ui primitives across both `web/` (the authenticated app) and `webpage/` (the public landing). All 10 phases executed, all 33 requirements (FND-01 through LDG-18) satisfied, full quality pipeline (code-review, goal-backward verification, 6-pillar UI audit) closed on every phase.

This audit found **zero unsatisfied requirements** and **zero broken cross-phase wiring that would block completion**. It found one genuine, narrow completeness gap (a 4th upload-progress site missed by Phase 107's scoping) and one stale RBAC-timing guard (missed by every phase that touched adjacent files) — the RBAC guard was fixed directly during this audit; the upload-progress gap is flagged as tech debt for a follow-up session.

## Requirements Coverage (3-Source Cross-Reference)

All 33 v1 requirements cross-referenced against REQUIREMENTS.md's traceability table, each phase's SUMMARY.md `requirements-completed` frontmatter, and each phase's VERIFICATION.md status/score:

| Phase | VERIFICATION Status | Score | REQ-IDs | Final Status |
|-------|---------------------|-------|---------|---------------|
| 101 — Fundação | passed | 8/8 | FND-01..08 | ✓ satisfied |
| 102 — Reconciliação | passed | 15/15 | DSR-01..03 | ✓ satisfied |
| 103 — Dashboard | human_needed | 5/5 | DASH-01/02 | ✓ satisfied (human item is a visual-only residual, not a functional gap — see Phase 103's own VERIFICATION.md) |
| 104 — DataTable | passed | 8/8 | DTB-01..03 | ✓ satisfied |
| 105 — Clientes+Processos | human_needed | 8/9 | CLP-01..05 | ✓ satisfied (same pattern as 103/108/109 — residual live-checkpoint gap, not a functional defect) |
| 106 — Agenda | passed | 5/5 | AGD-36/37 | ✓ satisfied |
| 107 — Documentos+Financeiro | passed | 7/7 | DOF-01/02 | ✓ satisfied (see Tech Debt: DOF-01 scope-completeness gap) |
| 108 — Pareceres | human_needed | 10/11 | PARC-18..20 | ✓ satisfied (residual live-checkpoint gap) |
| 109 — Notificações/Settings/Setup | human_needed | 13/14 | NTF-28..30 | ✓ satisfied (residual live-checkpoint gap) |
| 110 — Landing webpage/ | passed | 15/15 | LDG-17/18 | ✓ satisfied |

**Score: 33/33 requirements satisfied. Zero orphans (all requirements present in REQUIREMENTS.md traceability, all phase VERIFICATION.md files exist, all SUMMARY.md frontmatter lists match).**

The 5 phases routed `human_needed` (not `passed`) each carry a residual live-checkpoint item substituted with source/pattern analysis after a Browser-pane environment issue during that phase's own UAT (root-caused this session, see Phase 103/109 decision history in STATE.md) — none indicate an unresolved functional defect; each phase's own VERIFICATION.md documents the specific substitution and rationale. This matches the established precedent from milestones v2.10/v2.11 of not blocking a milestone close over a purely-visual residual gap when the underlying code has already been verified by other means.

## Cross-Phase Integration

Spawned `gsd-integration-checker` to trace wiring across all 10 phases, focused specifically on this milestone's real integration risk profile: whether Phase 104's shared DataTable pattern was actually reused (not reimplemented per-module), whether Phase 102's reconciled primitives are what every later phase actually imports, and whether `webpage/`'s isolation from `web/` held.

**Confirmed wired, no gaps:**
- DataTable pattern (Phase 104) reused by all 5 declared list screens (Clientes, Processos, Pareceres, Financeiro, Documentos) — not reimplemented ad hoc.
- `UserMenu`/`SidebarNav` (Phase 109) wired at all 5 call sites, no leftover duplicated markup.
- Phase 102's reconciled `Button`/`Card`/`Badge`/etc. are the actual versions imported everywhere in Phases 103–109 — zero local hand-rolled re-declarations found.
- Breadcrumb (Phase 105) wired at all 6 declared pages; Cliente/Processo ficha Tabs styling consistent.
- Dashboard KPI/Skeleton/Empty flow genuinely wired to real query data (Phase 103).
- `webpage/` (Phase 110) maintains full architectural isolation from `web/` — zero cross-imports; `Sheet`/`Badge` copies confirmed byte-identical to `web/`'s Phase-102-reconciled versions.

**2 gaps found (both narrow, neither blocking):**
1. **DOF-01 scope-completeness gap** — a 4th upload-progress site (Pareceres' `NovaVersaoForm`) was never migrated, because it fell outside both Phase 107's and Phase 108's declared scopes. Real, but narrow (cosmetic/consistency, not broken functionality — the underlying upload itself works). Flagged as tech debt (background task `task_af5aaa48`), not fixed during this audit (fix requires a small scoped change, better suited to its own short session than an ad hoc audit-time patch).
2. **Stale RBAC guard on `/notificacoes`** — the sole remaining screen using the pre-fix `!permissions.isLoading` pattern instead of `permissions.isFetched` (Phases 103/105/106/107/108 all fixed this exact race in their own declared files, but none of their scopes included this file). **Fixed directly during this audit** (commit `7a8f28e`) — trivial, one-line, exactly matches the established pattern at 31 other call sites, negligible regression risk.

**Also found (process note, not a wiring gap):** Phase 107's `107-VERIFICATION.md` existed on disk since 2026-07-17 but was never committed to git — an oversight in that phase's close-out sequence. **Fixed during this audit** (commit `220fc17`).

## Tech Debt Carried Forward

None of the following block this milestone's close — all are pre-existing (predate v2.13), independently fixed-but-unmerged, or newly-flagged-but-narrow-scope items:

1. **3 pre-existing backend bugs, each with a completed independent fix not yet merged to `master`** (all predate v2.13, unrelated to any shadcn/ui migration work): document-upload Hibernate optimistic-lock crash (`claude/jovial-lederberg-d017b2`), Pareceres Prioridade defaulting to ALTA instead of MEDIA (`claude/mystifying-leavitt-6657b5`), Dashboard `AtividadeRecenteCard` hydration mismatch (`claude/sad-fermat-78bc39`). Merging is the user's call — these were started outside this GSD orchestration.
2. **Pareceres `useAdminUsers()` unconditional call** — spurious 500 + unpopulated Advogado picker for non-ADMIN roles. Pre-existing, deferred, not yet independently fixed.
3. **DOF-01 4th upload-progress site** (see Cross-Phase Integration above) — flagged as background task `task_af5aaa48`.
4. **2 standalone verification_gaps predating v2.13** (Phase 86/87 items, carried forward since v2.11's own milestone audit — see `.planning/STATE.md` Deferred Items) — out of this milestone's scope entirely, listed here only for continuity of the project's single running debt ledger.

## Nyquist Compliance

`workflow.nyquist_validation` not explicitly disabled; no `*-VALIDATION.md` files exist for any of the 10 phases in this milestone (this milestone's own workflow config never enabled Nyquist-style validation gates — consistent across all 10 phases, not a per-phase omission).

| Status | Phases |
|--------|--------|
| MISSING (no VALIDATION.md) | 101, 102, 103, 104, 105, 106, 107, 108, 109, 110 (all 10) |

No action recommended — this milestone never used the Nyquist validation gate; introducing it retroactively is out of scope for a close-out audit.

## Conclusion

Milestone v2.13 achieved its definition of done: 33/33 requirements satisfied, 10/10 phases complete with full quality pipelines closed, cross-phase integration verified with only 2 narrow gaps found (1 fixed during this audit, 1 flagged as small follow-up tech debt). Recommended: proceed to `gsd-complete-milestone`.
