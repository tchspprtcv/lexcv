---
milestone: v2.8
audited: 2026-07-06T16:13:00Z
status: tech_debt
scores:
  requirements: 20/20
  phases: 6/6
  integration: 17/17
  flows: 8/8
gaps: {}
tech_debt:
  - phase: 74-enum-documento-tipo-bi-nif-restri-o-por-tipo
    items:
      - "IN-01/IN-02 (carried, non-blocking): DocumentoTipo.java missing doc-comment on NIF removal rationale; magic-string tipo comparisons in ResourceController not fully tied to TipoCliente enum (partially addressed during gap closure)"
      - "backend/migrations/74-cleanup-nif-documento-tipo.sql is a standalone manual-execution script — no migration runner exists in this repo (ddl-auto=update only); must be run manually against the database before/alongside deploy"
  - phase: 75-componente-nico-view-edit
    items:
      - "75-HUMAN-UAT.md: live browser verification pending (visual parity, save round-trip, cancel-discard, sub-component toggle, cross-tenant/access-denied paths) — deferred, static verification passed 9/9"
  - phase: 76-separadores-—-dados-contactos-e-notas
    items:
      - "76-HUMAN-UAT.md: live browser verification pending (7-tab click-through, cross-tab validation error surfacing, intake dialog reset on tab round-trip, mobile horizontal scroll) — deferred, static verification passed 7/7"
  - phase: 77-separadores-—-processos-e-pareceres
    items:
      - "IN-01/IN-02 (non-blocking): minor code duplication of statusVariant/formatDate helpers copied locally instead of shared; a redundant type cast"
  - phase: 78-separadores-—-documentos-a-tratar-e-deslocações
    items:
      - "IN-01/IN-02 (non-blocking): stale explanatory comment above the widened dialog-reset useEffect; pre-existing array-index-as-React-key pattern in the two relocated lists (not introduced by this phase)"
  - phase: 79-documentos-entregues-—-upload-real
    items:
      - "79-HUMAN-UAT.md: live browser verification pending (real upload flow with progress bar, list/download/delete round trip, read-mode vs edit-mode gating) — deferred, static verification passed 9/9"
      - "Info (non-blocking): uploadDocumento's replaceId branch validates clienteId/processoId but never applies them to the replaced Documento — intentional (replace = new version of same file), no current call site exercises reassignment"
      - "GET /documentos (generic, standalone /documentos page) still ignores cliente_id/processo_id query params server-side — pre-existing gap, confirmed unreachable from this milestone's new cliente-scoped endpoint, out of scope to fix here"
  - phase: cross-milestone
    items:
      - "Cliente.documentosEntregues (backend field/column) and DocumentoEntregue (frontend type) are now orphaned by design (CLI-29 'corte limpo') — no longer read/written by the UI, not migrated, matching the same pattern used for dados_tipo in v2.7"
nyquist:
  compliant_phases: 0
  partial_phases: 0
  missing_phases: 6
  overall: "missing (expected — research_enabled=false for this milestone, no RESEARCH.md/VALIDATION.md generated for any phase by deliberate project default, not a gap)"
---

# Milestone v2.8 Audit — Refatoração Ficha de Cliente

**Audited:** 2026-07-06
**Status:** ⚡ Tech Debt (no blockers — accumulated deferred items need review)

## Summary

All 6 phases (74–79) complete. All 20 requirements (CLI-12 through CLI-31) satisfied and cross-referenced across REQUIREMENTS.md traceability, phase VERIFICATION.md reports, and SUMMARY.md frontmatter — no unsatisfied, partial, or orphaned requirements. Cross-phase integration checker found the entire 7-tab ficha (5 phases sequentially modifying the same file: 75→76→77/78→79) fully wired end-to-end, with zero broken flows, zero unprotected routes, zero orphaned exports (one deliberately orphaned backend column, matching spec).

The only outstanding items are **non-blocking**: 3 phases (75, 76, 79) have pending live-browser UAT (deferred because this environment has no running backend/DB to exercise), plus a handful of carried-forward code-quality Info items from code review, all explicitly out of critical/warning fix scope.

## Requirements Coverage (3-Source Cross-Reference)

| REQ-ID | Phase | REQUIREMENTS.md | VERIFICATION.md | SUMMARY frontmatter | Final Status |
|--------|-------|-----------------|------------------|----------------------|---------------|
| CLI-12–14 | 75 | `[x]` | human_needed, 9/9 | listed | **satisfied** |
| CLI-15,18,19 | 76 | `[x]` | human_needed, 7/7 | listed | **satisfied** |
| CLI-16,17 | 77 | `[x]` | passed, 5/5 | listed | **satisfied** |
| CLI-20–24 | 74 | `[x]` | passed, 10/10 (after 2 gap closures) | listed | **satisfied** |
| CLI-25–29 | 79 | `[x]` | human_needed, 9/9 | listed | **satisfied** |
| CLI-30,31 | 78 | `[x]` | passed, 7/7 | listed | **satisfied** |

**Score: 20/20 requirements satisfied.** No orphans (every REQUIREMENTS.md entry maps to a phase VERIFICATION.md that addresses it).

Note: `human_needed` phases (75, 76, 79) all scored 100% on their automated must_haves — the status reflects that live-browser interaction testing wasn't possible in this environment (no backend/DB configured), not a code deficiency. Each has an itemized `*-HUMAN-UAT.md` file tracking the specific manual checks needed.

## Phase Verification Summary

| Phase | Goal | Verification | Score | Notes |
|-------|------|--------------|-------|-------|
| 74 | Enum documento_tipo restriction | passed | 10/10 | 2 gap-closure rounds (frontend legacy-value preserve, backend unchanged-value exemption) |
| 75 | Componente Único View/Edit | human_needed | 9/9 | Live UAT deferred |
| 76 | Separadores — Dados/Contactos/Notas | human_needed | 7/7 | Live UAT deferred |
| 77 | Separadores — Processos/Pareceres | passed | 5/5 | Clean, no gap closure needed |
| 78 | Separadores — Documentos a Tratar/Deslocações | passed | 7/7 | UI-SPEC/CONTEXT correction caught by plan-checker before execution |
| 79 | Documentos Entregues — Upload Real | human_needed | 9/9 | 2 code-review fix rounds closed 3 critical + 4 warning findings (FormData key mismatch, wrong download link, missing tenant validation, swallowed error messages) |

## Cross-Phase Integration

**Integration checker verdict: fully wired, no blockers.**

- 17/17 cross-phase exports/consumers connected (hooks, endpoints, shared modules).
- 8/8 E2E flow steps verified (open ficha → 7 tabs → edit → tab-switch persistence → Processos/Pareceres read-only → Documentos Entregues upload → Documentos a Tratar/Deslocações → single Guardar commit).
- 0 broken flows, 0 unprotected routes, 0 unexpected orphaned exports.
- Confirmed the Phase 74 legacy-`documento_tipo` exemption chain (frontend Zod + backend `documentoTipoUnchanged`) survived Phase 76 relocating the fields into the "Dados" tab — the chain depends on component state, not DOM position.
- Confirmed `PlaceholderEmBreve` has zero references anywhere in `web/src/` — all 7 tabs now render real content, the placeholder scaffolding introduced in Phase 76 was fully retired by the end of Phase 79.

## Tech Debt

See YAML frontmatter `tech_debt` block above for the full itemized list by phase. Summary:

- **3 pending human-UAT sessions** (Phases 75, 76, 79) — live browser verification of interactions that static analysis can't fully cover. Recommended: run these before considering the milestone fully done in a live environment.
- **1 manual migration script** (Phase 74) — `backend/migrations/74-cleanup-nif-documento-tipo.sql` must be run manually (no migration runner in this repo).
- **Minor code-quality carryovers** (Phases 74, 77, 78, 79) — all Info-tier, explicitly out of critical/warning fix scope, documented in each phase's REVIEW.md.
- **1 deliberately orphaned backend column** (`Cliente.documentosEntregues`) — matches the explicit CLI-29 "corte limpo" decision, same pattern as `dados_tipo` in v2.7.

## Nyquist Compliance

No VALIDATION.md files exist for any of the 6 phases — expected, since `research_enabled=false` was the deliberate project default for this milestone (confirmed at milestone kickoff: this is a UI refactor of existing patterns, not new architecture requiring research). This is a discovery-only finding, not a gap requiring `/gsd:validate-phase`.

## Recommendation

**Option A (recommended):** Complete the milestone now — accept the tech debt above, track the 3 pending HUMAN-UAT sessions and the manual SQL script in the backlog for the next time a live dev/staging environment is available.

**Option B:** Insert a cleanup phase to address the minor code-quality Info items before completing (not recommended — none are blocking, and bundling them risks scope creep on a milestone that's otherwise cleanly done).
