---
phase: "85"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/main/java/com/lexcv/services/RiscoPrazoService.java
  - backend/src/test/java/com/lexcv/services/RiscoPrazoServiceTest.java
  - backend/src/main/java/com/lexcv/controllers/ResourceController.java
autonomous: true
requirements: [NOTF-22]

must_haves:
  truths:
    - "Um único bean @Service injetável (RiscoPrazoService) calcula o risco (ok/proximo/vencido) para Prazo (LocalDate) e Evento (LocalDateTime) usando a MESMA tabela de limiares 7d-ALTA / 3d-outros"
    - "O método privado computeRisco já NÃO existe em ResourceController — nenhuma cópia duplicada da lógica de limiares permanece no controller"
    - "Os 5 call sites baseados em Prazo (listProcessos, listPrazos, listAllPrazos, createPrazo, togglePrazoConcluido) devolvem veredictos byte-idênticos aos de antes — zero regressão, garantida pelo overload de 2 args que faz default hoje=LocalDate.now()"
    - "Os 3 call sites baseados em Evento (agendaUrgentesCount, getProcessosDashboard prazosCriticosCount, getUpcomingEventos) passam a usar riscoPrazoService.computeRiscoEvento — mudança de comportamento INTENCIONAL (ROADMAP critério 3), não regressão"
    - "Um teste de caracterização JUnit 5 puro (sem Spring context) fixa a tabela de limiares: dia-limiar exato (0/3/7/8 dias), caso null, insensibilidade a maiúsculas de ALTA e a conversão LocalDateTime->LocalDate"
  artifacts:
    - path: "backend/src/main/java/com/lexcv/services/RiscoPrazoService.java"
      provides: "computeRisco(LocalDate,String) + overload de 3 args (hoje); computeRiscoEvento(LocalDateTime,String) + overload de 3 args (hoje)"
      contains: "@Service"
    - path: "backend/src/test/java/com/lexcv/services/RiscoPrazoServiceTest.java"
      provides: "teste de caracterização da tabela de limiares (vencido/proximo/ok, dia-limiar, null, LocalDateTime->LocalDate) — primeiro teste do backend"
      contains: "new RiscoPrazoService()"
    - path: "backend/src/main/java/com/lexcv/controllers/ResourceController.java"
      provides: "8 call sites de Prazo repontados ao serviço injetado; método privado computeRisco removido; 3 blocos de Evento a delegar em computeRiscoEvento"
      contains: "riscoPrazoService.computeRisco"
  key_links:
    - from: "backend/src/main/java/com/lexcv/controllers/ResourceController.java"
      to: "RiscoPrazoService.computeRisco"
      via: "campo injetado @RequiredArgsConstructor riscoPrazoService"
      pattern: "riscoPrazoService\\.computeRisco\\("
    - from: "backend/src/main/java/com/lexcv/controllers/ResourceController.java"
      to: "RiscoPrazoService.computeRiscoEvento"
      via: "3 call sites de Evento (agendaUrgentesCount, prazosCriticosCount, getUpcomingEventos)"
      pattern: "riscoPrazoService\\.computeRiscoEvento\\("
---

<objective>
Consolidar as 5 implementações do veredito de "prazo crítico" — 1 método privado `computeRisco` baseado em `Prazo` (usado em 5 sites) + 3 blocos ad-hoc baseados em `Evento`, todos hoje inline em `ResourceController` — numa única fonte de verdade: um novo `@Service` injetável `RiscoPrazoService`. É um refactor puro de backend, sem endpoint novo e sem input novo. A única mudança de comportamento é intencional e limitada aos 3 sites de `Evento`, que passam a usar a mesma tabela de limiares 7d-ALTA / 3d-outros já usada por `Prazo`, em vez das 3 janelas fixas/inconsistentes de hoje (ROADMAP Phase 85, critério 3).

Purpose: Esta extração é o pré-requisito duro da Phase 88 (job diário `@Scheduled`), cujo propósito explícito é REUTILIZAR esta lógica consolidada em vez de introduzir uma 5ª cópia divergente. Um `@Service` injetável (não um utilitário estático — sem precedente neste código-base e não-mockável com a stack de testes atual) torna a lógica injetável com data determinística: daí o overload de 3 args que aceita `hoje`, para os testes futuros da Phase 88 poderem fixar a data de referência.

Output: `RiscoPrazoService.java` (novo) com 4 métodos públicos; `RiscoPrazoServiceTest.java` (novo — o primeiro teste do backend) a fixar a tabela de limiares; `ResourceController.java` com os 8 call sites de `Prazo` repontados, o método privado apagado, e os 3 blocos de `Evento` a delegar em `computeRiscoEvento`.

Endereça: NOTF-22.
</objective>

<execution_context>
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/LEXCV-85-consolida-o-da-l-gica-de-prazo-cr-tico/85-PATTERNS.md
@.planning/phases/LEXCV-85-consolida-o-da-l-gica-de-prazo-cr-tico/85-CONTEXT.md
@.planning/research/ARCHITECTURE.md

# Ficheiros-fonte (ler os intervalos indicados em cada task):
@backend/src/main/java/com/lexcv/controllers/ResourceController.java
@backend/src/main/java/com/lexcv/services/SetupService.java
@backend/src/main/java/com/lexcv/models/Prazo.java
@backend/src/main/java/com/lexcv/models/Evento.java

<interfaces>
<!-- Contratos exatos que o executor precisa. Código nos blocos abaixo é a-copiar/alvo — não explorar o código-base para os reconstruir. -->

LÓGICA A MOVER VERBATIM — origem `ResourceController.java` linhas 1397-1404 (payload da extração):
```java
private String computeRisco(LocalDate dataLimite, String prioridade) {
    if (dataLimite == null) return "ok";
    LocalDate hoje = LocalDate.now();
    if (dataLimite.isBefore(hoje)) return "vencido";
    long diasRestantes = ChronoUnit.DAYS.between(hoje, dataLimite);
    int limiarProximo = "ALTA".equalsIgnoreCase(prioridade) ? 7 : 3;
    return diasRestantes <= limiarProximo ? "proximo" : "ok";
}
```

SHAPE-ALVO do novo serviço (Task 1 — criar exatamente assim; 4 métodos públicos, ZERO dependências injetadas):
```java
package com.lexcv.services;

import lombok.RequiredArgsConstructor;
import org.springframework.stereotype.Service;

import java.time.LocalDate;
import java.time.LocalDateTime;
import java.time.temporal.ChronoUnit;

@Service
@RequiredArgsConstructor
public class RiscoPrazoService {

    // 3-arg: `hoje` injetável — usado pela Phase 88 para determinismo em testes.
    // É esta a implementação "real"; os wrappers de 2 args delegam nela.
    public String computeRisco(LocalDate dataLimite, String prioridade, LocalDate hoje) {
        if (dataLimite == null) return "ok";
        if (dataLimite.isBefore(hoje)) return "vencido";
        long diasRestantes = ChronoUnit.DAYS.between(hoje, dataLimite);
        int limiarProximo = "ALTA".equalsIgnoreCase(prioridade) ? 7 : 3;
        return diasRestantes <= limiarProximo ? "proximo" : "ok";
    }

    // 2-arg: wrapper de conveniência — comportamento byte-idêntico ao atual (default hoje = now).
    public String computeRisco(LocalDate dataLimite, String prioridade) {
        return computeRisco(dataLimite, prioridade, LocalDate.now());
    }

    // Evento aceita LocalDateTime — converte para LocalDate e reutiliza a MESMA tabela de limiares.
    public String computeRiscoEvento(LocalDateTime dataEvento, String prioridade, LocalDate hoje) {
        return computeRisco(dataEvento == null ? null : dataEvento.toLocalDate(), prioridade, hoje);
    }

    public String computeRiscoEvento(LocalDateTime dataEvento, String prioridade) {
        return computeRiscoEvento(dataEvento, prioridade, LocalDate.now());
    }
}
```

INVENTÁRIO DE CALL SITES em `ResourceController.java` (verificado — linhas atuais):

| # | Site | Linhas | Base | Ação |
|---|------|--------|------|------|
| 1 | `listProcessos` (risco_mais_critico) | 944 | Prazo | prefixar `riscoPrazoService.` |
| 2 | `listPrazos` | 1417 | Prazo | prefixar `riscoPrazoService.` |
| 3 | `listAllPrazos` | 1439 | Prazo | prefixar `riscoPrazoService.` |
| 4 | `createPrazo` | 1483, 1503 | Prazo | prefixar (2 chamadas) |
| 5 | `togglePrazoConcluido` | 1530, 1531, 1543 | Prazo | prefixar (3 chamadas — 1530/1531 são redundantes por design; PRESERVAR a redundância) |
| — | método privado | 1397-1404 | — | APAGAR após repontar 1-5 |
| 6 | `agendaUrgentesCount` | 2748-2753 | Evento | reescrever com computeRiscoEvento (mudança intencional) |
| 7 | `getProcessosDashboard` prazosCriticosCount | 2855-2867 | Evento | reescrever com computeRiscoEvento (mudança intencional) |
| 8 | `getUpcomingEventos` | 2246-2270 | Evento | ENRIQUECER com campo `risco`, filtro `days` INALTERADO (opção b) |

CONVENÇÃO DE INJEÇÃO (precedente vivo — `ResourceController.java:66`):
```java
private final StorageService storageService;
```
→ adicionar `private final RiscoPrazoService riscoPrazoService;` ao mesmo bloco (linhas 49-71); Lombok `@RequiredArgsConstructor` (linha 46) gera o construtor. Import `com.lexcv.services.RiscoPrazoService` junto do `import com.lexcv.services.StorageService;` existente (linha 6).

TIPOS DE CAMPO (confirmados): `Prazo.dataLimite` = `LocalDate`; `Prazo.prioridade` = `String`. `Evento.dataInicio` / `Evento.dataFim` = `LocalDateTime` (ambos anuláveis); `Evento.prioridade` = `String` (anulável — `"ALTA".equalsIgnoreCase(null)` é false-safe, trata null como não-ALTA/limiar 3).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Criar RiscoPrazoService (@Service) + teste de caracterização da tabela de limiares</name>
  <files>
    backend/src/main/java/com/lexcv/services/RiscoPrazoService.java
    backend/src/test/java/com/lexcv/services/RiscoPrazoServiceTest.java
  </files>
  <read_first>
    - backend/src/main/java/com/lexcv/controllers/ResourceController.java linhas 1390-1410 (o método privado computeRisco — payload verbatim)
    - backend/src/main/java/com/lexcv/services/SetupService.java linhas 1-36 (shape de @Service + @RequiredArgsConstructor, package com.lexcv.services — o precedente a igualar)
    - .planning/phases/LEXCV-85-consolida-o-da-l-gica-de-prazo-cr-tico/85-PATTERNS.md secção "RiscoPrazoService.java (NEW)" — inclui a nota "Important deviation": zero dependências injetadas; @RequiredArgsConstructor numa classe sem campos final gera construtor no-arg (correto e suficiente), NÃO inventar dependência artificial
    - .planning/phases/LEXCV-85-consolida-o-da-l-gica-de-prazo-cr-tico/85-PATTERNS.md secção "No Analog Found" — backend/src/test/ não existe; teste é JUnit 5 puro, sem @SpringBootTest, apenas new RiscoPrazoService()
    - .planning/research/ARCHITECTURE.md Pattern 3 (racional do overload de 3 args e do @Service vs. estático)
    - backend/src/main/java/com/lexcv/models/{Prazo,Evento}.java (tipos de campo — já resumidos no bloco <interfaces>)
  </read_first>
  <behavior>
    Teste de caracterização (usar SEMPRE o overload de 3 args com `hoje` fixo = LocalDate.of(2026,1,15) para determinismo; ALTA → limiar 7, não-ALTA → limiar 3; `<=` é inclusivo):

    computeRisco (Prazo, LocalDate):
    - computeRisco(null, "ALTA", hoje) -> "ok"                              (data null)
    - computeRisco(2026-01-14, "ALTA", hoje) -> "vencido"                   (ontem, antes de hoje)
    - computeRisco(2026-01-14, "MEDIA", hoje) -> "vencido"                  (vencido ignora prioridade)
    - computeRisco(2026-01-15, "ALTA", hoje) -> "proximo"                   (HOJE: diasRestantes=0, 0<=7)
    - computeRisco(2026-01-15, "MEDIA", hoje) -> "proximo"                  (HOJE: 0<=3)
    - computeRisco(2026-01-22, "ALTA", hoje) -> "proximo"                   (dia-limiar ALTA: 7 dias, 7<=7)
    - computeRisco(2026-01-23, "ALTA", hoje) -> "ok"                        (8 dias > 7)
    - computeRisco(2026-01-18, "MEDIA", hoje) -> "proximo"                  (dia-limiar não-ALTA: 3, 3<=3)
    - computeRisco(2026-01-19, "MEDIA", hoje) -> "ok"                       (4 > 3)
    - computeRisco(2026-01-20, "alta", hoje) -> "proximo"                   (5 dias: ALTA(<=7)="proximo"; prova equalsIgnoreCase, pois como não-ALTA seria "ok")
    - computeRisco(2026-01-20, null, hoje) -> "ok"                          (prioridade null tratada como limiar 3: 5>3)

    computeRisco 2-arg (delegação para hoje=now — casos estáveis independentes da data corrente):
    - computeRisco(null, "ALTA") -> "ok"
    - computeRisco(LocalDate.now().minusDays(1), "MEDIA") -> "vencido"

    computeRiscoEvento (Evento, LocalDateTime — .toLocalDate() strip da hora):
    - computeRiscoEvento(null, "ALTA", hoje) -> "ok"
    - computeRiscoEvento(2026-01-20T09:00, "alta", hoje) -> "proximo"       (=computeRisco(2026-01-20, ALTA): 5<=7)
    - computeRiscoEvento(2026-01-14T23:59, "MEDIA", hoje) -> "vencido"      (data passada apesar da hora tardia)
    - computeRiscoEvento(2026-01-15T23:59, "MEDIA", hoje) -> "proximo"      (evento MAIS TARDE HOJE: same-day nunca é "vencido"; diasRestantes=0, 0<=3)
  </behavior>
  <action>
    1. Criar `backend/src/main/java/com/lexcv/services/RiscoPrazoService.java` exatamente como o "SHAPE-ALVO do novo serviço" no bloco `<interfaces>`: package `com.lexcv.services`; anotações `@Service` + `@RequiredArgsConstructor`; imports `org.springframework.stereotype.Service`, `lombok.RequiredArgsConstructor`, `java.time.LocalDate`, `java.time.LocalDateTime`, `java.time.temporal.ChronoUnit`; os 4 métodos públicos: `computeRisco(LocalDate,String,LocalDate)` (a implementação real, usando o `hoje` recebido em vez de `LocalDate.now()`), `computeRisco(LocalDate,String)` (delega com `LocalDate.now()`), `computeRiscoEvento(LocalDateTime,String,LocalDate)` (converte via `.toLocalDate()` com guarda de null e delega ao computeRisco de 3 args), `computeRiscoEvento(LocalDateTime,String)` (delega com `LocalDate.now()`). Nenhum campo `final`, nenhuma dependência injetada, nenhum `getTenantId()`/repositório/try-catch — é uma função pura.

    2. Criar `backend/src/test/java/com/lexcv/services/RiscoPrazoServiceTest.java` (package `com.lexcv.services`) como teste JUnit 5 PURO: importar `org.junit.jupiter.api.Test` e `org.junit.jupiter.api.Assertions.assertEquals` (static), instanciar `RiscoPrazoService service = new RiscoPrazoService();` diretamente. NÃO usar `@SpringBootTest`, `@Autowired`, `@ExtendWith`, nem qualquer anotação de contexto Spring — o serviço não tem dependências, logo não precisa de contexto. Escrever um método `@Test` (ou vários) que asserta cada par input->output listado no bloco `<behavior>`, usando `LocalDate hoje = LocalDate.of(2026, 1, 15);` como referência fixa e `LocalDate.of(...)` / `LocalDateTime.of(...)` para os argumentos. Incluir todos os casos: null, vencido, dia-limiar 0/3/7/8, equalsIgnoreCase ("alta"), prioridade null, delegação 2-arg estável, e os 4 casos de computeRiscoEvento (com destaque para o par "vencido ontem à noite" vs. "proximo mais tarde hoje" que fixa a semântica .toLocalDate()).

    3. NÃO tocar em `ResourceController.java` nesta task — o método privado computeRisco continua a existir aqui; é a Task 2 que o apaga. Ambas as cópias coexistem temporariamente APENAS entre a Task 1 e a Task 2.
  </action>
  <acceptance_criteria>
    - `grep -c "@Service" backend/src/main/java/com/lexcv/services/RiscoPrazoService.java` retorna 1
    - `grep -c "public String computeRisco\b\|public String computeRiscoEvento\b" backend/src/main/java/com/lexcv/services/RiscoPrazoService.java` retorna 4 (os 4 métodos públicos)
    - `grep -c "@SpringBootTest\|@Autowired\|@ExtendWith" backend/src/test/java/com/lexcv/services/RiscoPrazoServiceTest.java` retorna 0 (teste puro, sem contexto Spring)
    - `grep -c "new RiscoPrazoService()" backend/src/test/java/com/lexcv/services/RiscoPrazoServiceTest.java` >= 1
    - `cd backend && mvn test -Dtest=RiscoPrazoServiceTest` termina em BUILD SUCCESS com todos os asserts a passar (compila src/main + src/test, corre só este teste, sem DB/.env porque não há contexto Spring)
  </acceptance_criteria>
  <verify>
    <automated>cd backend && mvn test -Dtest=RiscoPrazoServiceTest</automated>
  </verify>
  <done>RiscoPrazoService.java existe como @Service injetável com os 4 métodos públicos (computeRisco x2, computeRiscoEvento x2) e zero dependências; RiscoPrazoServiceTest.java fixa a tabela de limiares (null, vencido, dia-limiar 0/3/7/8, equalsIgnoreCase, prioridade null, semântica LocalDateTime->LocalDate) e passa; ResourceController ainda intacto.</done>
</task>

<task type="auto">
  <name>Task 2: Repontar os 8 call sites de Prazo + 3 blocos de Evento em ResourceController e apagar o método privado</name>
  <files>
    backend/src/main/java/com/lexcv/controllers/ResourceController.java
  </files>
  <read_first>
    - backend/src/main/java/com/lexcv/services/RiscoPrazoService.java (criado na Task 1 — as 4 assinaturas a chamar)
    - backend/src/main/java/com/lexcv/controllers/ResourceController.java linhas 1-71 (imports + bloco de campos do construtor: `import com.lexcv.services.StorageService;` na linha 6, `private final StorageService storageService;` na linha 66)
    - backend/src/main/java/com/lexcv/controllers/ResourceController.java linhas 940-950 (site 1), 1397-1404 (método privado a apagar), 1408-1445 (sites 2-3), 1478-1546 (sites 4-5: createPrazo + togglePrazoConcluido)
    - backend/src/main/java/com/lexcv/controllers/ResourceController.java linhas 2246-2270 (site 8: getUpcomingEventos), 2748-2753 (site 6: agendaUrgentesCount), 2851-2870 (site 7: getProcessosDashboard prazosCriticosCount)
    - .planning/phases/LEXCV-85-consolida-o-da-l-gica-de-prazo-cr-tico/85-PATTERNS.md secção "ResourceController.java (MODIFIED)" — os 8 pontos de modificação com o excerto exato de cada call site; ler em especial a "Flag for planning" do call site 8 (getUpcomingEventos) que confirma a opção (b) escolhida abaixo
  </read_first>
  <action>
    Aplicar as 3 mudanças estruturais + os repontamentos, tudo em `ResourceController.java`:

    1. IMPORT + CAMPO: adicionar `import com.lexcv.services.RiscoPrazoService;` imediatamente a seguir ao `import com.lexcv.services.StorageService;` (linha 6). Adicionar `private final RiscoPrazoService riscoPrazoService;` ao bloco de campos `private final` do construtor (junto de `private final StorageService storageService;`, ~linha 66). Não escrever construtor manual — Lombok `@RequiredArgsConstructor` gera-o.

    2. REPONTAR OS 5 SITES DE PRAZO (mudança mecânica de 1 linha cada, corpo circundante INALTERADO — comportamento byte-idêntico): em cada uma das chamadas a `computeRisco(...)` nas linhas 944, 1417, 1439, 1483, 1503, 1530, 1531 e 1543, prefixar a chamada com `riscoPrazoService.` (ex.: `computeRisco(pr.getDataLimite(), pr.getPrioridade())` -> `riscoPrazoService.computeRisco(pr.getDataLimite(), pr.getPrioridade())`). As linhas 1530 e 1531 chamam computeRisco duas vezes com argumentos idênticos por design — PRESERVAR essa redundância (fora do âmbito otimizar), apenas prefixar ambas.

    3. APAGAR O MÉTODO PRIVADO: remover por completo o método `private String computeRisco(LocalDate dataLimite, String prioridade)` (linhas 1397-1404). CONTEXT.md é explícito: "não deixar as duas versões coexistirem" — nenhum wrapper pass-through deixado para trás.

    4. SITE 6 — agendaUrgentesCount (2748-2753), MUDANÇA INTENCIONAL: reescrever o corpo para contar eventos pendentes cujo risco de Evento é crítico, em vez de contar todos os pendentes com prioridade ALTA. Manter o fetch `eventoRepository.findByTenantIdAndConcluido(tenantId, false)`, e substituir o `.filter(e -> "ALTA".equalsIgnoreCase(e.getPrioridade()))` por um filtro que retém os eventos onde `riscoPrazoService.computeRiscoEvento(e.getDataFim(), e.getPrioridade())` devolve `"proximo"` ou `"vencido"`, depois `.count()`. Usar o campo `dataFim` (alinha esta KPI com o site 7, que também usa dataFim — decisão de discricionariedade documentada; agendaUrgentesCount hoje não referencia nenhuma data, logo é preciso escolher, e dataFim dá às duas KPIs de dashboard a mesma base). Manter o retorno `long` e a assinatura do método.

    5. SITE 7 — getProcessosDashboard prazosCriticosCount (2855-2867), MUDANÇA INTENCIONAL: substituir o `if` de janela fixa de 7 dias por uma contagem baseada no serviço. Manter `List<Evento> eventos = eventoRepository.findByTenantIdAndConcluido(tenantId, false);` e o loop; dentro do loop, contar `prazosCriticosCount++` quando `riscoPrazoService.computeRiscoEvento(e.getDataFim(), e.getPrioridade())` for `"proximo"` ou `"vencido"` (isto passa a incluir eventos vencidos com dataFim no passado, que a janela fixa antiga excluía — é o efeito pretendido do critério 3). Se as variáveis locais `today`/`sevenDays` (2856-2857) ficarem sem qualquer outra utilização no método após esta mudança, removê-las; se forem usadas mais abaixo, deixá-las.

    6. SITE 8 — getUpcomingEventos (2246-2270), OPÇÃO (b) — ENRIQUECER SEM MUDAR O FILTRO: NÃO colapsar o parâmetro `days` na tabela de limiares. O filtro de inclusão por janela `days` (linhas 2253-2259: `effectiveDays`, `removeIf` de concluído e de fora de `[now, now+days]`) e a ordenação/slice (2260-2261) ficam EXATAMENTE como estão — o contrato do parâmetro `days` (janela configurável do dropdown do sino, default 7, cap 30) é preservado, garantindo zero regressão observável. A única alteração é adicionar uma 6ª entrada ao `Map.of(...)` de cada item (2262-2268): `"risco", riscoPrazoService.computeRiscoEvento(e.getDataInicio(), e.getPrioridade())`. Assim o endpoint passa a delegar o veredito de risco no serviço partilhado (repontado, per CONTEXT.md), sem tocar na semântica de listagem. (Map.of com 6 pares = 12 args, dentro do limite.)

    Não alterar mais nada. Nenhum endpoint novo, nenhum scoping de tenant movido — cada site mantém o seu `getTenantId()`/filtro `findByTenantId...` pré-existente.
  </action>
  <acceptance_criteria>
    - `grep -n "private String computeRisco(" backend/src/main/java/com/lexcv/controllers/ResourceController.java` não devolve NADA (método privado apagado)
    - `grep -c "riscoPrazoService.computeRisco(" backend/src/main/java/com/lexcv/controllers/ResourceController.java` retorna 8 (os 8 call expressions de Prazo repontados; nota: `computeRisco(` não conta os `computeRiscoEvento(` porque exige o parêntesis logo a seguir a "computeRisco")
    - `grep -c "riscoPrazoService.computeRiscoEvento(" backend/src/main/java/com/lexcv/controllers/ResourceController.java` retorna 3 (sites 6, 7, 8)
    - `grep -c "import com.lexcv.services.RiscoPrazoService;" backend/src/main/java/com/lexcv/controllers/ResourceController.java` retorna 1
    - `grep -c "private final RiscoPrazoService riscoPrazoService;" backend/src/main/java/com/lexcv/controllers/ResourceController.java` retorna 1
    - `cd backend && mvn test -Dtest=RiscoPrazoServiceTest` termina em BUILD SUCCESS — prova lógica-chave: se o método privado foi apagado E o ficheiro compila, então NENHUMA chamada `computeRisco(...)` sem qualificação pode ter sobrado (uma chamada nua daria "cannot find symbol"), logo os 8 sites estão repontados; o teste de caracterização a passar prova que a lógica movida não sofreu drift
  </acceptance_criteria>
  <verify>
    <automated>cd backend && mvn test -Dtest=RiscoPrazoServiceTest && ! grep -q "private String computeRisco(" src/main/java/com/lexcv/controllers/ResourceController.java</automated>
  </verify>
  <done>ResourceController.java: import + campo riscoPrazoService adicionados; os 8 call expressions de Prazo prefixados com riscoPrazoService.; método privado computeRisco removido; sites 6 e 7 (Evento) reescritos com computeRiscoEvento(dataFim); site 8 (getUpcomingEventos) enriquecido com campo risco via computeRiscoEvento(dataInicio) mantendo o filtro `days` intacto; compila e o teste de caracterização passa.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser -> REST API | Já existente e INALTERADO. Todos os endpoints tocados (`/processos`, `/processos/{id}/prazos`, `/eventos/upcoming`, `/dashboard`, `/processos/dashboard`) continuam atrás de `JwtAuthenticationFilter` (cookie httpOnly) + `@PreAuthorize`. Nenhum endpoint novo, nenhum parâmetro de input novo. |
| Controller -> RiscoPrazoService | Chamada de método in-process, mesma thread e transação. O serviço é uma função pura (data + prioridade -> string): não acede a repositório, `tenantId`, nem `SecurityContext`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-85-01 | Information Disclosure / Tampering | Os 8 call sites de Prazo + 3 de Evento repontados em ResourceController | mitigate | A extração NÃO move nenhum scoping de tenant para o serviço — `computeRisco`/`computeRiscoEvento` recebem apenas (data, prioridade), tal como o método privado nunca recebeu `tenantId`. Cada call site conserva o seu `getTenantId()` / filtro pré-existente (`findByTenantIdAndConcluido`, `findByTenantId`) intacto. Os greps de aceitação confirmam que só a chamada de risco muda; o gate de compilação garante que o scoping envolvente não foi tocado. |
| T-85-02 | Tampering | RiscoPrazoService (lógica de limiares movida verbatim) | mitigate | Um erro na cópia (off-by-one em `<=`, troca de 7/3, perda do caso null, ou `isBefore` vs `isAfter`) alteraria silenciosamente o veredito mostrado no dashboard/prazos e, mais tarde, nas notificações. Mitigado pelo teste de caracterização JUnit que fixa vencido/proximo/ok no dia-limiar exato (0/3/7/8 dias), o caso `null`, a insensibilidade a maiúsculas de "ALTA", e a semântica LocalDateTime->LocalDate (evento mais tarde HOJE é "proximo", não "vencido"). O overload de 2 args faz default `hoje=LocalDate.now()`, preservando byte-a-byte o comportamento dos 5 sites de Prazo. |
| T-85-SC | Supply Chain / Tampering | Instalação de dependências (maven/npm/pip/cargo) | accept | NENHUMA dependência nova adicionada nesta fase. `@Service`/`@RequiredArgsConstructor` já em uso no código-base; JUnit 5 já fornecido pelo `spring-boot-starter-test` presente em `backend/pom.xml`. Não existe tarefa de instalação de pacote -> nenhum checkpoint de legitimidade de pacote necessário. |

**Resumo:** Sem nova superfície de ataque — extração que preserva o comportamento de lógica já autenticada e scoped por tenant. Sem endpoints novos, sem input novo, sem dependências novas.
</threat_model>

<verification>
- `cd backend && mvn test -Dtest=RiscoPrazoServiceTest` passa (compila todo o `src/main` — incluindo o `ResourceController` modificado — e todo o `src/test`, depois corre o teste de caracterização; sem DB/.env porque o teste não carrega contexto Spring).
- `grep -n "private String computeRisco(" backend/src/main/java/com/lexcv/controllers/ResourceController.java` não devolve nada (método privado apagado).
- `grep -c "riscoPrazoService.computeRisco(" backend/src/main/java/com/lexcv/controllers/ResourceController.java` = 8 (sites de Prazo).
- `grep -c "riscoPrazoService.computeRiscoEvento(" backend/src/main/java/com/lexcv/controllers/ResourceController.java` = 3 (sites de Evento).
- Prova lógica: método privado apagado + compilação bem-sucedida => nenhuma chamada `computeRisco(...)` sem qualificação sobrou (seria "cannot find symbol").
</verification>

<success_criteria>
- `RiscoPrazoService` é um `@Service` injetável com 4 métodos públicos (computeRisco x2, computeRiscoEvento x2), zero dependências injetadas, zero acesso a tenant/repositório.
- Os 5 call sites de Prazo devolvem veredictos idênticos aos anteriores (garantido pelo default hoje=now do overload de 2 args + teste de caracterização) — zero regressão (ROADMAP critério 2).
- Os 3 sites de Evento usam a tabela de limiares partilhada 7d-ALTA/3d-outros via `computeRiscoEvento` (mudança intencional — ROADMAP critério 3).
- `getUpcomingEventos` conserva o contrato do parâmetro `days` (filtro de janela inalterado) e apenas ENRIQUECE cada item com um campo `risco` — zero regressão observável ao filtro/listagem.
- O método privado `computeRisco` não existe mais em `ResourceController`; não há cópia duplicada da lógica.
- NOTF-22 satisfeito: fonte única partilhada substitui as 4 implementações inconsistentes.
</success_criteria>

<output>
Create `.planning/phases/LEXCV-85-consolida-o-da-l-gica-de-prazo-cr-tico/85-01-SUMMARY.md` when done.
</output>
