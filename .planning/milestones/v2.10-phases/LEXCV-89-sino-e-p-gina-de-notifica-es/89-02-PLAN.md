---
phase: 89-sino-e-p-gina-de-notifica-es
plan: 02
type: execute
wave: 2
depends_on: [89-01]
files_modified:
  - web/src/components/shared/notification-bell.tsx
autonomous: true
requirements: [NOTF-08, NOTF-09, NOTF-10, NOTF-11]

must_haves:
  truths:
    - "O sino no topo mostra um contador de não lidas (cap '9+') que se atualiza a cada 30s E imediatamente ao voltar a focar a aba do browser"
    - "Abrir o sino mostra até 10 notificações recentes (qualquer estado de leitura), cada uma com link direto para linkUrl quando presente"
    - "Clicar numa notificação no dropdown marca-a como lida (fire-and-forget) E navega para linkUrl na MESMA ação, sem confirmação; o contador atualiza de imediato"
    - "Uma notificação com linkUrl null renderiza como texto não-navegável mas ainda permite marcar como lida via afordância inline"
    - "O botão 'Marcar todas como lidas' no dropdown funciona (com toast.success) e fica desativado quando não há não-lidas; o link 'Ver todas as notificações' navega para /notificacoes"
  artifacts:
    - path: "web/src/components/shared/notification-bell.tsx"
      provides: "Sino reescrito consumindo Notificacao persistida (badge + dropdown de 10 + marcar-lida-e-navegar + marcar-todas + footer)"
      contains: "useNotificacoesUnreadCount"
  key_links:
    - from: "notification-bell.tsx (badge)"
      to: "useNotificacoesUnreadCount"
      via: "count = data?.count"
      pattern: "useNotificacoesUnreadCount"
    - from: "notification-bell.tsx (dropdown body)"
      to: "useNotificacoes({ size: 10 }, { poll: true })"
      via: "data.content.map"
      pattern: "poll: true"
    - from: "clique na linha do dropdown"
      to: "useMarcarNotificacaoLida.mutate(id) + <Link href={linkUrl}>"
      via: "onClick fire-and-forget + navegação"
      pattern: "\\.mutate\\("
---

<objective>
Reescrever os internos de `web/src/components/shared/notification-bell.tsx` para consumir `Notificacao` persistidas (via os hooks do Plan 89-01) em vez da vista calculada `useUpcomingEventos` da v2.1. O nome exportado (`NotificationBell`) e o seu call site (`dashboard-shell.tsx:281`) permanecem inalterados — só a fonte de dados e o corpo do dropdown mudam. Cobre NOTF-08 (contador com polling), NOTF-09 (lista com links), NOTF-10 (marcar-uma ao clicar) e NOTF-11 (marcar-todas no dropdown).

Purpose: Dar ao utilizador o consumo rápido de notificações no topo de qualquer página, substituindo o sino de eventos por um sino de notificações genéricas. A Agenda mantém a sua própria página; o sino deixa de ser sobre eventos (decisão travada em CONTEXT.md). É o primeiro consumidor do override de polling e da invalidação de prefixo definidos no Plan 89-01.
Output: `web/src/components/shared/notification-bell.tsx` reescrito (in-place).
</objective>

<execution_context>
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/LEXCV-89-sino-e-p-gina-de-notifica-es/89-UI-SPEC.md
@.planning/phases/LEXCV-89-sino-e-p-gina-de-notifica-es/89-PATTERNS.md
@.planning/phases/LEXCV-89-sino-e-p-gina-de-notifica-es/89-CONTEXT.md
@web/src/components/shared/notification-bell.tsx
@web/src/hooks/use-notificacoes.ts
@web/src/lib/notificacao-categoria.ts
@web/src/types/notificacoes.ts

<interfaces>
<!-- Contratos do Plan 89-01 (Wave 1) já disponíveis. NÃO redefinir; importar e consumir. -->

De @/hooks/use-notificacoes:
- useNotificacoesUnreadCount() -> useQuery<{ count: number }> com polling 30s + refetchOnWindowFocus true
- useNotificacoes(filters, opts?) -> useQuery<NotificacoesPageResponse>; para o dropdown chamar useNotificacoes({ size: 10 }, { poll: true }); aceder a data.content
- useMarcarNotificacaoLida() -> mutation, .mutate(id: string), invalida ["notificacoes"]
- useMarcarTodasNotificacoesLidas() -> mutation, .mutate() / .mutateAsync(), invalida ["notificacoes"]

De @/lib/notificacao-categoria: categoriaToLabel(categoria), categoriaToBadgeVariant(categoria)
De @/types/notificacoes: Notificacao, NotificacaoCategoria

Toast (para o call site de marcar-todas): `import { toast } from "@/hooks/use-toast";` (mesmo import de documentos/page.tsx:15). Copy verbatim do 89-UI-SPEC: toast.success("Todas as notificações foram marcadas como lidas.")

MANTER VERBATIM do ficheiro atual (notification-bell.tsx, 77 linhas — ver @ acima):
- O shell Popover/PopoverTrigger/PopoverContent com `className="w-80 p-0" align="end"`.
- A markup do botão do sino + círculo do badge: `Button variant="ghost"` circular `h-9 w-9 p-0 rounded-full ...`, `Bell className="h-[1.1rem] w-[1.1rem]"`, e o círculo `absolute -top-0.5 -right-0.5 h-4 w-4 rounded-full bg-red-500 text-[10px] font-bold text-white ...` com o cap `count > 9 ? "9+" : count`.
- A estrutura de bordas header/body/footer: header `px-4 py-3 border-b`, lista `divide-y divide-slate-100 dark:divide-slate-800 max-h-72 overflow-y-auto`, cada linha `px-4 py-3 hover:bg-slate-50 dark:hover:bg-slate-900 transition-colors`, footer `px-4 py-2 border-t`.
- O padrão de fallback de link condicional (linhas 56-62 do atual): `ev.processoId ? <Link href=...>...</Link> : <NotificationItem/>` — replicar 1:1 trocando `ev.processoId` por `n.linkUrl`.

Classname do link do footer (manter): `text-xs text-blue-600 dark:text-blue-400 hover:underline font-medium`
Formato de data compacto do dropdown (89-UI-SPEC): `new Date(n.createdAt).toLocaleDateString("pt-CV", { day: "2-digit", month: "short" })` — nota: o ficheiro atual usa "pt-PT" na linha 16; padronizar em "pt-CV" (fix de 1 string, alinha com o resto da app).
Ícones lucide-react adicionais (mesmo package do Bell atual): CheckCheck (marcar-todas), Check (marcar-uma na linha de linkUrl null).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Reescrever notification-bell.tsx para consumir Notificacao persistida</name>
  <files>web/src/components/shared/notification-bell.tsx</files>
  <read_first>
    - web/src/components/shared/notification-bell.tsx (ficheiro atual inteiro, 77 linhas — o que se mantém verbatim e o que muda)
    - web/src/hooks/use-notificacoes.ts (os 4 hooks criados no Plan 89-01 — assinaturas exatas)
    - web/src/lib/notificacao-categoria.ts (categoriaToLabel, categoriaToBadgeVariant)
    - .planning/phases/LEXCV-89-sino-e-p-gina-de-notifica-es/89-UI-SPEC.md (Interaction Flow "Sino (bell)", Copywriting Contract, Categoria Badge Color Map, Reuse map)
    - web/src/app/(dashboard)/documentos/page.tsx (linha 15 — o import exato de `toast` a copiar)
  </read_first>
  <action>
    Reescrever o corpo do componente `NotificationBell` no MESMO ficheiro, preservando o export nomeado `NotificationBell` e o `export default NotificationBell` (o call site em dashboard-shell.tsx:281 NÃO muda — não tocar noutros ficheiros). Manter verbatim o shell Popover, a markup do botão+badge e a estrutura header/body/footer identificados no bloco &lt;interfaces&gt;.

    Imports: remover `useUpcomingEventos` e `UpcomingEvento`. Adicionar `useNotificacoesUnreadCount, useNotificacoes, useMarcarNotificacaoLida, useMarcarTodasNotificacoesLidas` de `@/hooks/use-notificacoes`; `categoriaToLabel, categoriaToBadgeVariant` de `@/lib/notificacao-categoria`; `Notificacao` de `@/types/notificacoes`; `Badge` de `@/components/ui/badge`; `toast` de `@/hooks/use-toast`; e adicionar `Check, CheckCheck` ao import existente de `lucide-react` (que já traz `Bell`). Manter `Link` de `next/link` e `Button`/`Popover*`.

    Dados: `const unread = useNotificacoesUnreadCount();` para o badge — `const count = unread.data?.count ?? 0; const showBadge = !unread.isLoading && count > 0;` (mesma forma do `showBadge` atual). `const list = useNotificacoes({ size: 10 }, { poll: true });` para o corpo — os itens são `list.data?.content ?? []` (aceder a `.content`, NÃO tratar como array; é a única diferença estrutural face aos outros hooks list). `const marcarLida = useMarcarNotificacaoLida(); const marcarTodas = useMarcarTodasNotificacoesLidas();`.

    Header do dropdown: trocar o texto `"Próximos eventos"` por `"Notificações"`. Ao lado do título, adicionar um botão `Button variant="ghost" size="sm"` com o texto "Marcar todas como lidas" (label pendente "A marcar..." enquanto `marcarTodas.isPending`), `disabled={count === 0 || marcarTodas.isPending}`; onClick chama `marcarTodas.mutateAsync()` num try e, em sucesso, `toast.success("Todas as notificações foram marcadas como lidas.")` (a falha já é coberta pelo toast automático do apiFetch; não é preciso try/catch bespoke além de evitar unhandled rejection — usar `void marcarTodas.mutateAsync().then(() => toast.success(...))` OU um handler async com try/catch, à escolha, desde que o toast.success só dispare em sucesso).

    Corpo (lista): substituir o mapeamento de eventos. Estados: quando `list.isPending` mostrar o texto de loading; quando os itens estiverem vazios mostrar a copy de empty "Sem notificações por agora." (substitui "Sem eventos nos próximos 7 dias"), no mesmo `px-4 py-6 text-sm ... text-center`. Caso contrário, `<ul>` com a mesma className de divisão, mapeando `list.data!.content.slice(0, 10)` (o backend já limita a 10 via size, o slice é defesa). Cada `<li>` mantém a className de linha atual. Dentro de cada linha renderizar um bloco de conteúdo com: o `titulo` (`text-sm font-medium ...`, a bold quando `!n.lida` para sinalizar não-lida), um `Badge variant={categoriaToBadgeVariant(n.categoria)}` com `categoriaToLabel(n.categoria)` (texto pequeno), a `mensagem` (truncada, `text-xs text-slate-500`), e a meta de data `new Date(n.createdAt).toLocaleDateString("pt-CV", { day: "2-digit", month: "short" })`.

    Comportamento de clique da linha (decisão travada CONTEXT.md — ação única sem confirmação): replicar o padrão de fallback condicional do ficheiro atual, trocando `ev.processoId` por `n.linkUrl`:
    - Quando `n.linkUrl` é um path relativo (começa por "/"): envolver o conteúdo num `<Link href={n.linkUrl} className="block">` cujo `onClick` chama `marcarLida.mutate(n.id)` (fire-and-forget, NÃO await — a navegação acontece no mesmo clique). Guarda de segurança: só tratar como link quando `n.linkUrl` começa por "/" (relativo same-origin); qualquer outro valor cai no ramo de texto simples (evita open-redirect / esquemas perigosos caso um valor não-relativo surja).
    - Quando `n.linkUrl` é null (ou não começa por "/"): renderizar o conteúdo como texto simples não-navegável, MAIS uma pequena afordância inline `Button variant="ghost" size="sm"` (ícone `Check`, `aria-label="Marcar como lida"`) que chama `marcarLida.mutate(n.id)` — para que uma linha sem alvo de clique ainda possa ser marcada como lida (novo comportamento face ao analog, exigido pelo 89-UI-SPEC). Mostrar este `Check` apenas quando `!n.lida`.

    Footer: trocar o link "Ver agenda" (`href="/agenda"`) por "Ver todas as notificações" (`href="/notificacoes"`), MANTENDO a mesma className `text-xs text-blue-600 dark:text-blue-400 hover:underline font-medium`.

    NÃO adicionar item à sidebar nem ao bottom-nav (locked CONTEXT.md/89-UI-SPEC) — este plano toca APENAS notification-bell.tsx. NÃO introduzir helper de tempo relativo ("há 2 horas") — usar apenas data absoluta.
  </action>
  <verify>
    <automated>pnpm --dir web build</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --dir web build` compila com sucesso
    - grep confirma que `useUpcomingEventos` e `UpcomingEvento` já NÃO aparecem no ficheiro
    - grep confirma `useNotificacoesUnreadCount`, `useNotificacoes(`, `useMarcarNotificacaoLida`, `useMarcarTodasNotificacoesLidas` presentes
    - O export nomeado `NotificationBell` e o `export default` mantêm-se (o call site dashboard-shell.tsx:281 não é alterado; nenhum outro ficheiro em files_modified)
    - Texto "Notificações" no header, "Marcar todas como lidas" no botão do header, "Ver todas as notificações" com `href="/notificacoes"` no footer, empty "Sem notificações por agora."
    - O clique da linha com linkUrl relativo chama `marcarLida.mutate` E navega via `<Link>`; linkUrl null renderiza texto simples + `Check` de marcar-lida
    - grep NÃO encontra `bg-destructive` nem qualquer novo item adicionado a NAV/BOTTOM_NAV (ficheiro dashboard-shell/bottom-nav não modificados)
    - marcar-todas dispara `toast.success` só em sucesso e está `disabled` quando `count === 0`
  </acceptance_criteria>
  <done>O sino consome notificações persistidas: badge com polling+refocus, dropdown de até 10 com badges de categoria e links, clique = marcar-lida+navegar, marcar-todas com toast, e footer para /notificacoes — tudo dentro do shell Popover preservado.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser→API (same-origin, cookie JWT) | O sino lê o contador e a lista do próprio destinatário; o scoping é server-side (Phase 86) |
| linkUrl renderizado como href | Valor autoria-backend, contratualmente same-origin relativo; tratado como não-confiável por defesa |
| gate implícito (autenticado) | O sino vive no DashboardShell, só visível a utilizadores autenticados |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-89-02-01 | Tampering | `n.linkUrl` renderizado como `<Link href>` | mitigate | `linkUrl` é autoria-backend e contratualmente same-origin relativo (89-UI-SPEC); a UI renderiza como link APENAS quando `linkUrl` começa por "/" (relativo), caso contrário texto simples — bloqueia open-redirect e esquemas `javascript:`/`data:` caso um valor não-relativo surja no futuro. Null já cai no ramo de texto simples |
| T-89-02-02 | Information Disclosure | contador + lista de 10 | mitigate | Endpoints dual-scoped (tenant+destinatário) server-side (Phase 86); o sino não envia parâmetro de destinatário; o backend deriva do JWT |
| T-89-02-03 | Tampering | id em marcarLida.mutate | mitigate | O id vem só da lista recebida pelo próprio utilizador; o backend revalida posse (Phase 86); `encodeURIComponent` aplicado no hook (Plan 89-01) |
| T-89-02-SC | Tampering (supply chain) | dependências | accept | Sem novos packages; `Check`/`CheckCheck` são exports do `lucide-react` já presente (fonte do `Bell` atual); sem alteração de `pnpm-lock.yaml` |
</threat_model>

<verification>
- `pnpm --dir web build` passa
- grep confirma a troca de fonte de dados (`useUpcomingEventos` ausente; hooks de notificações presentes), o header "Notificações", o footer `/notificacoes`, e a guarda de linkUrl relativo
- grep confirma que dashboard-shell.tsx e bottom-nav.tsx NÃO estão em files_modified (nenhum item de navegação novo)
- Verificação end-to-end (contador a incrementar, clique a marcar+navegar, marcar-todas) é feita no checkpoint do Plan 89-04
</verification>

<success_criteria>
O sino no topo mostra o contador de não lidas atualizado por polling (30s) e ao refocar a aba; abrir mostra até 10 notificações recentes com links diretos; clicar marca-lida-e-navega numa só ação; marcar-todas funciona com toast; e "Ver todas as notificações" leva a /notificacoes. Sem alteração ao call site nem à navegação lateral.
</success_criteria>

<output>
Create `.planning/phases/LEXCV-89-sino-e-p-gina-de-notifica-es/89-02-SUMMARY.md` when done
</output>
