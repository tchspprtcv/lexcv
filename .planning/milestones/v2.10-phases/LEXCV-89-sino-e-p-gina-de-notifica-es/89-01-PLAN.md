---
phase: 89-sino-e-p-gina-de-notifica-es
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/types/notificacoes.ts
  - web/src/lib/notificacao-categoria.ts
  - web/src/hooks/use-notificacoes.ts
autonomous: true
requirements: [NOTF-08, NOTF-10, NOTF-11, NOTF-13]

must_haves:
  truths:
    - "useNotificacoesUnreadCount() faz polling a cada 30s (refetchInterval: 30_000) E revalida ao voltar a focar a aba (refetchOnWindowFocus: true), fazendo override explícito ao default global false de providers.tsx"
    - "useNotificacoes(filters, { poll }) constrói query params opcionais categoria/lida/page/size (lida com guard !== undefined), devolve NotificacoesPageResponse (objeto com content, não um array), e só ativa o polling quando poll === true"
    - "useMarcarNotificacaoLida() e useMarcarTodasNotificacoesLidas() invalidam o prefixo top-level ['notificacoes'] no onSuccess (apanha unread-count E as duas queries list de uma vez), sem escritas otimistas manuais"
    - "categoriaToLabel e categoriaToBadgeVariant cobrem exatamente os 9 valores de NotificacaoCategoria com fallback seguro; NOTIFICACAO_CATEGORIA_OPTIONS deriva a lista {value,label}[] dos mesmos 9 valores"
  artifacts:
    - path: "web/src/types/notificacoes.ts"
      provides: "Notificacao, NotificacaoCategoria, NotificacoesListFilters, NotificacoesPageResponse"
      contains: "NotificacoesPageResponse"
    - path: "web/src/lib/notificacao-categoria.ts"
      provides: "categoriaToLabel, categoriaToBadgeVariant, NOTIFICACAO_CATEGORIA_OPTIONS"
      contains: "NOTIFICACAO_CATEGORIA_OPTIONS"
    - path: "web/src/hooks/use-notificacoes.ts"
      provides: "useNotificacoes, useNotificacoesUnreadCount, useMarcarNotificacaoLida, useMarcarTodasNotificacoesLidas"
      contains: "useNotificacoesUnreadCount"
  key_links:
    - from: "useNotificacoes"
      to: "GET /notificacoes?categoria=&lida=&page=&size="
      via: "apiFetch com buildNotificacoesSearch"
      pattern: "/notificacoes"
    - from: "useNotificacoesUnreadCount"
      to: "GET /notificacoes/unread-count (polling 30s)"
      via: "apiFetch + refetchInterval"
      pattern: "unread-count"
    - from: "useMarcarNotificacaoLida.onSuccess / useMarcarTodasNotificacoesLidas.onSuccess"
      to: "invalidação do prefixo ['notificacoes']"
      via: "queryClient.invalidateQueries"
      pattern: "\\[\"notificacoes\"\\]"
---

<objective>
Construir a camada de dados 100%-frontend da Phase 89: os tipos, o mapa de categoria→label/variant, e os 4 hooks TanStack Query que consomem a API `/notificacoes` já entregue na Phase 86. Nenhuma superfície visual — este plano é o contrato que os Plans 89-02 (sino) e 89-03 (página) consomem em paralelo na Wave 2.

Purpose: Fixar, num só sítio revisto, as decisões técnicas mais fáceis de sub-especificar desta fase — o override de polling (`refetchInterval` + `refetchOnWindowFocus: true`, primeiro uso no projeto), a forma paginada `NotificacoesPageResponse` (sem analog no codebase), e a invalidação de prefixo top-level `["notificacoes"]` que faz o contador do sino atualizar de imediato após marcar lida. Interface-first: definir os contratos antes de qualquer consumidor os usar.
Output: `web/src/types/notificacoes.ts`, `web/src/lib/notificacao-categoria.ts`, `web/src/hooks/use-notificacoes.ts`.
</objective>

<execution_context>
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/LEXCV-89-sino-e-p-gina-de-notifica-es/89-UI-SPEC.md
@.planning/phases/LEXCV-89-sino-e-p-gina-de-notifica-es/89-PATTERNS.md
@.planning/phases/LEXCV-89-sino-e-p-gina-de-notifica-es/89-CONTEXT.md
@web/src/types/eventos.ts
@web/src/lib/prazos.ts
@web/src/lib/tipo-decisao.ts
@web/src/hooks/use-pareceres.ts
@web/src/hooks/use-dashboard-kpis.ts
@web/src/hooks/use-eventos.ts

<interfaces>
<!-- Contratos verbatim (verificados no 89-UI-SPEC.md contra Notificacao.java). Copiar as formas, não re-derivar. -->

Contrato da API (Phase 86, já a correr — não criar nem alterar endpoints):
GET   /notificacoes?categoria=&lida=&page=0&size=20  -> { content: Notificacao[], totalElements: number, totalPages: number, page: number, size: number }
GET   /notificacoes/unread-count                     -> { count: number }
PATCH /notificacoes/{id}/lida                         -> Notificacao (a linha atualizada; SEM corpo no pedido)
POST  /notificacoes/ler-todas                         -> { marcadas: number } (SEM corpo no pedido)

Forma alvo de web/src/types/notificacoes.ts (camelCase puro — SEM normalize*/toApiPayload; as chaves JSON do backend são idênticas):
```typescript
export type NotificacaoCategoria =
  | "FASE_ENTRADA" | "DOCUMENTO_NOVO" | "PROCESSO_ATRIBUIDO" | "PARECER_ATRIBUIDO"
  | "PRAZO_PROXIMO" | "PRAZO_VENCIDO" | "EVENTO_PROXIMO" | "EVENTO_VENCIDO" | "HONORARIO_ATRASADO";

export interface Notificacao {
  id: string;
  categoria: NotificacaoCategoria;
  entidadeTipo: string;
  entidadeId: string;
  titulo: string;
  mensagem: string;
  linkUrl: string | null;
  lida: boolean;
  createdAt: string;
}

export type NotificacoesListFilters = {
  categoria?: NotificacaoCategoria;
  lida?: boolean;
  page?: number;
  size?: number;
};

export interface NotificacoesPageResponse {
  content: Notificacao[];
  totalElements: number;
  totalPages: number;
  page: number;
  size: number;
}
```

Mapa Categoria -> Badge variant (verbatim do 89-UI-SPEC "Categoria Badge Color Map"):
FASE_ENTRADA=blue, PROCESSO_ATRIBUIDO=blue, PARECER_ATRIBUIDO=blue, DOCUMENTO_NOVO=purple,
PRAZO_PROXIMO=amber, EVENTO_PROXIMO=amber, PRAZO_VENCIDO=red, EVENTO_VENCIDO=red, HONORARIO_ATRASADO=red

Mapa Categoria -> Label PT (verbatim do 89-UI-SPEC "Categoria -> Label map"):
FASE_ENTRADA="Nova fase", DOCUMENTO_NOVO="Novo documento", PROCESSO_ATRIBUIDO="Processo atribuído",
PARECER_ATRIBUIDO="Parecer atribuído", PRAZO_PROXIMO="Prazo a vencer", PRAZO_VENCIDO="Prazo vencido",
EVENTO_PROXIMO="Evento a aproximar-se", EVENTO_VENCIDO="Evento em atraso", HONORARIO_ATRASADO="Honorário em atraso"

Padrão de mapa enum->label a copiar (web/src/lib/prazos.ts / web/src/lib/tipo-decisao.ts): duas funções puras irmãs sobre um `Record<Enum, T>` literal com fallback `??`. Precedente do retorno de variant como união literal explícita (NÃO importar um BadgeProps inexistente): prazosRiscoToVariant devolve `"green" | "amber" | "red"`.

Guard de filtro booleano opcional a copiar (web/src/hooks/use-eventos.ts:30):
```typescript
if (filters.concluido !== undefined) params.set("concluido", String(filters.concluido));
```

Padrão de hook de contagem simples a copiar (web/src/hooks/use-dashboard-kpis.ts, ficheiro inteiro): useQuery com enabled `typeof window !== "undefined"`, staleTime 30_000. useNotificacoesUnreadCount copia esta forma MAIS `refetchInterval: 30_000` e `refetchOnWindowFocus: true`.

Padrão de query list com filtros a copiar (web/src/hooks/use-pareceres.ts:1-37): buildXxxSearch(filters) -> URLSearchParams -> queryKey com os filtros -> apiFetch. Divergência: o tipo de retorno é NotificacoesPageResponse (objeto), não um array.

Default global a fazer override (web/src/app/providers.tsx:14): `refetchOnWindowFocus: false`. Sem o override por-query, o contador não atualiza ao refocar a aba.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tipos em web/src/types/notificacoes.ts</name>
  <files>web/src/types/notificacoes.ts</files>
  <read_first>
    - .planning/phases/LEXCV-89-sino-e-p-gina-de-notifica-es/89-UI-SPEC.md (secção "API contract" e "Recommended new files" — a forma verificada de Notificacao)
    - web/src/types/eventos.ts (convenção de tipo-entidade camelCase + união de string-literal para o enum; ficheiro inteiro, 53 linhas)
  </read_first>
  <action>
    Criar o ficheiro novo com quatro exports, copiando a forma alvo do bloco &lt;interfaces&gt; verbatim: a união `NotificacaoCategoria` (9 valores), a interface `Notificacao` (todos os campos camelCase, `linkUrl: string | null`), o tipo `NotificacoesListFilters` (`{ categoria?, lida?, page?, size? }`) e a interface `NotificacoesPageResponse` (`{ content, totalElements, totalPages, page, size }`). NÃO adicionar função `normalize*` nem `toApiPayload` — o 89-UI-SPEC verificou contra `Notificacao.java` que as chaves JSON já são camelCase idênticas (esta é a primeira entidade do projeto que não precisa de camada de tradução; não reintroduzir o padrão snake_case dos tipos Processo/Cliente aqui). Sem imports (ficheiro puro de tipos).
  </action>
  <verify>
    <automated>pnpm --dir web lint</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --dir web lint` passa sem novos erros
    - grep encontra os 4 exports: `NotificacaoCategoria`, `Notificacao`, `NotificacoesListFilters`, `NotificacoesPageResponse`
    - A união `NotificacaoCategoria` contém exatamente os 9 valores do contrato (nem mais, nem menos)
    - `linkUrl` é tipado `string | null`; NÃO existe nenhuma função `normalize`/`toApiPayload` no ficheiro
  </acceptance_criteria>
  <done>O ficheiro de tipos existe, com a entidade Notificacao, a união de categoria de 9 valores, o tipo de filtros e o wrapper paginado — todos camelCase, sem camada de mapeamento.</done>
</task>

<task type="auto">
  <name>Task 2: Mapa de categoria em web/src/lib/notificacao-categoria.ts</name>
  <files>web/src/lib/notificacao-categoria.ts</files>
  <read_first>
    - web/src/lib/prazos.ts (analog exato — ficheiro inteiro, 29 linhas: duas funções irmãs Record+fallback, e o retorno de variant como união literal)
    - web/src/lib/tipo-decisao.ts (segundo analog do padrão label — ficheiro inteiro)
    - .planning/phases/LEXCV-89-sino-e-p-gina-de-notifica-es/89-UI-SPEC.md (tabelas "Categoria Badge Color Map" e "Categoria -> Label map" — os valores verbatim, já verificados contra o titulo do backend)
  </read_first>
  <action>
    Criar o ficheiro novo importando o tipo `NotificacaoCategoria` de `@/types/notificacoes`. Exportar três símbolos, seguindo o padrão prazos.ts:
    - `categoriaToLabel(categoria: NotificacaoCategoria): string` — um `Record<NotificacaoCategoria, string>` com os 9 pares label do bloco &lt;interfaces&gt; verbatim, com fallback `?? "Notificação"`.
    - `categoriaToBadgeVariant(categoria: NotificacaoCategoria): "blue" | "purple" | "amber" | "red"` — um `Record<NotificacaoCategoria, "blue" | "purple" | "amber" | "red">` com o mapa de variant do bloco &lt;interfaces&gt; verbatim (blue para os 3 informacionais, purple para DOCUMENTO_NOVO, amber para os 2 *_PROXIMO, red para os 3 vencido/atrasado), com fallback `?? "blue"`. Usar a união literal explícita como tipo de retorno — NÃO importar `BadgeProps` (não existe export desse tipo em badge.tsx; o precedente prazos.ts também usa união literal).
    - `NOTIFICACAO_CATEGORIA_OPTIONS: { value: NotificacaoCategoria; label: string }[]` — derivar mapeando um array literal dos 9 valores de `NotificacaoCategoria` através de `categoriaToLabel`, para alimentar o `<select>` de filtro do Plan 89-03. Um array plano, sem branching por segunda dimensão.
    Documentar cada função com um comentário curto "Fonte única de verdade..." no estilo prazos.ts.
  </action>
  <verify>
    <automated>pnpm --dir web lint</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --dir web lint` passa sem novos erros
    - grep encontra `categoriaToLabel`, `categoriaToBadgeVariant`, `NOTIFICACAO_CATEGORIA_OPTIONS`
    - Ambos os `Record` cobrem os 9 valores (o TypeScript recusa um Record incompleto sobre a união; a compilação prova a exaustividade)
    - `categoriaToBadgeVariant` devolve apenas variantes do conjunto {blue, purple, amber, red} e NÃO importa `BadgeProps`
    - `NOTIFICACAO_CATEGORIA_OPTIONS` tem 9 entradas `{value,label}` derivadas de categoriaToLabel
  </acceptance_criteria>
  <done>Existe a fonte única de verdade categoria->label/variant, mais o array de opções para o filtro, espelhando exatamente o padrão prazos.ts.</done>
</task>

<task type="auto">
  <name>Task 3: Hooks TanStack Query em web/src/hooks/use-notificacoes.ts</name>
  <files>web/src/hooks/use-notificacoes.ts</files>
  <read_first>
    - web/src/hooks/use-pareceres.ts (buildParecerSearch + query list com filtros; linhas 1-37)
    - web/src/hooks/use-dashboard-kpis.ts (query de contagem simples; ficheiro inteiro)
    - web/src/hooks/use-eventos.ts (guard `filters.concluido !== undefined` na linha 30 — copiar para `lida`; convenção de imports no topo)
    - web/src/hooks/use-processos.ts (useTogglePrazoConcluido, ~759-783 — forma da mutação PATCH com onSuccess/invalidateQueries)
    - web/src/app/providers.tsx (linha 14 — o default global `refetchOnWindowFocus: false` a fazer override)
    - .planning/phases/LEXCV-89-sino-e-p-gina-de-notifica-es/89-UI-SPEC.md (tabela "Query behavior contract" e "Cache invalidation" — a parte mais fácil de sub-especificar)
  </read_first>
  <action>
    Criar o ficheiro novo. Imports no topo (estilo verbatim dos outros hooks): `import { useMutation, useQuery, useQueryClient } from "@tanstack/react-query";`, `import { apiFetch } from "@/lib/api";`, e os tipos `Notificacao, NotificacoesListFilters, NotificacoesPageResponse` de `@/types/notificacoes`.

    Função privada `buildNotificacoesSearch(filters: NotificacoesListFilters): string` — cria um `URLSearchParams`, adiciona `categoria` se presente, `lida` com o guard `if (filters.lida !== undefined) params.set("lida", String(filters.lida))` (copiar o estilo de use-eventos.ts:30), `page` com `if (filters.page !== undefined) params.set("page", String(filters.page))`, `size` com o mesmo guard; devolve `qs ? \`?${qs}\` : ""`.

    Exportar quatro hooks:
    - `useNotificacoes(filters: NotificacoesListFilters = {}, opts: { poll?: boolean } = {})` — `useQuery` que devolve `NotificacoesPageResponse`. queryKey `["notificacoes", "list", filters.categoria ?? "", filters.lida ?? null, filters.page ?? 0, filters.size ?? 20]`. queryFn `apiFetch<NotificacoesPageResponse>(\`/notificacoes${buildNotificacoesSearch(filters)}\`)`. `enabled: typeof window !== "undefined"`, `staleTime: 30_000`. Quando `opts.poll === true`, adicionar `refetchInterval: 30_000` e `refetchOnWindowFocus: true`; caso contrário NÃO definir nenhuma das duas (a página herda o default sem polling). Este segundo parâmetro `opts` é o mecanismo que permite ao mesmo hook servir o dropdown do sino (com polling) e a página (sem polling) — decisão do 89-UI-SPEC "Query behavior contract".
    - `useNotificacoesUnreadCount()` — copiar use-dashboard-kpis.ts 1:1: queryKey `["notificacoes", "unread-count"]`, queryFn `apiFetch<{ count: number }>("/notificacoes/unread-count")`, `enabled: typeof window !== "undefined"`, `staleTime: 30_000`, MAIS `refetchInterval: 30_000` e `refetchOnWindowFocus: true` sempre (este hook só serve o badge do sino, que tem de fazer polling e refrescar ao focar a aba — override obrigatório ao default global false).
    - `useMarcarNotificacaoLida()` — `useQueryClient()` + `useMutation`. mutationFn `(id: string) => apiFetch<Notificacao>(\`/notificacoes/${encodeURIComponent(id)}/lida\`, { method: "PATCH" })` (SEM corpo — o endpoint não recebe body, ao contrário do exemplo useTogglePrazoConcluido que envia `{ concluido }`). onSuccess `async () => { await queryClient.invalidateQueries({ queryKey: ["notificacoes"] }); }` — invalidação do PREFIXO top-level `["notificacoes"]` (uma única segment key), que apanha `["notificacoes","unread-count"]` e ambas as queries `["notificacoes","list",...]` de uma só vez, satisfazendo "contador do sino atualiza-se de imediato" do CONTEXT.md. NÃO fazer `setQueryData`/escrita otimista manual (o 89-UI-SPEC recomenda explicitamente invalidação simples; sem escrita otimista, não há rollback a fazer em caso de falha — a UI só muda após o refetch bem-sucedido). NÃO disparar toast aqui (marcar-uma é silenciosa por decisão do 89-UI-SPEC).
    - `useMarcarTodasNotificacoesLidas()` — mesma forma: mutationFn `() => apiFetch<{ marcadas: number }>("/notificacoes/ler-todas", { method: "POST" })` (SEM corpo), onSuccess com a MESMA invalidação de prefixo `["notificacoes"]`. NÃO disparar toast aqui — o toast.success de sucesso é adicionado nos call sites (Plans 89-02 e 89-03), que já importam `toast`; manter os hooks livres de toast, como useTogglePrazoConcluido.
  </action>
  <verify>
    <automated>pnpm --dir web build</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --dir web build` compila com sucesso (typecheck dos hooks contra os novos tipos + TanStack Query)
    - grep encontra os 4 exports: `useNotificacoes`, `useNotificacoesUnreadCount`, `useMarcarNotificacaoLida`, `useMarcarTodasNotificacoesLidas`
    - `useNotificacoesUnreadCount` contém `refetchInterval: 30_000` E `refetchOnWindowFocus: true`
    - `useNotificacoes` ativa `refetchInterval`/`refetchOnWindowFocus` apenas dentro do ramo `opts.poll`
    - Ambas as mutações fazem `invalidateQueries({ queryKey: ["notificacoes"] })` (prefixo top-level, um só segmento) e NENHUMA envia corpo no pedido nem faz escrita otimista
    - `buildNotificacoesSearch` usa o guard `!== undefined` para `lida`, `page`, `size`
  </acceptance_criteria>
  <done>Existem os 4 hooks: list paginada com polling opt-in, contador com polling+refocus obrigatórios, e as duas mutações de marcar-lida com invalidação de prefixo — o contrato de dados completo para os consumidores da Wave 2.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser→API (same-origin via rewrite next.config, cookie JWT) | Os hooks leem/marcam apenas as notificações do próprio destinatário; o scoping (tenant + destinatário) é server-side (Phase 86) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-89-01-01 | Information Disclosure | useNotificacoes / useNotificacoesUnreadCount (leitura) | mitigate | Os endpoints são dual-scoped (tenant + destinatário) server-side na Phase 86; os hooks NÃO enviam qualquer parâmetro de destinatário/tenant — o backend deriva-o do JWT do cookie. Os únicos query params (categoria/lida/page/size) são de filtragem/paginação, não de autorização |
| T-89-01-02 | Tampering | id em useMarcarNotificacaoLida | mitigate | O `id` provém exclusivamente da lista que o próprio utilizador recebeu (nunca de input livre); `PATCH /notificacoes/{id}/lida` é revalidado server-side contra tenant+destinatário (Phase 86) — um id forjado de outro destinatário devolve 404/403; o frontend não é fonte de autorização. `encodeURIComponent` no id previne injeção de path |
| T-89-01-SC | Tampering (supply chain) | dependências | accept | Sem novos packages; `@tanstack/react-query` e `apiFetch` já presentes e usados em todos os hooks; sem alteração de `pnpm-lock.yaml` |
</threat_model>

<verification>
- `pnpm --dir web lint` e `pnpm --dir web build` passam
- grep confirma os 12 exports (4 tipos, 3 do lib, 4 hooks... nota: NotificacaoCategoria é partilhado)
- grep confirma `refetchInterval: 30_000` e `refetchOnWindowFocus: true` em use-notificacoes.ts (o override de polling, primeiro uso no projeto)
- grep confirma `invalidateQueries({ queryKey: ["notificacoes"] })` nas duas mutações (prefixo top-level)
</verification>

<success_criteria>
A camada de dados de notificações está completa e tipada: tipos camelCase sem mapeamento, mapa categoria->label/variant single-source, e 4 hooks (list com polling opt-in, contador com polling obrigatório, marcar-uma e marcar-todas com invalidação de prefixo). O projeto compila. Nenhuma superfície visual ainda — os Plans 89-02 e 89-03 consomem estes contratos.
</success_criteria>

<output>
Create `.planning/phases/LEXCV-89-sino-e-p-gina-de-notifica-es/89-01-SUMMARY.md` when done
</output>
