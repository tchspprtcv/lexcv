---
phase: 71-frontend-types-schema-api-integration
plan: 02
type: execute
wave: 2
depends_on: [71-01]
files_modified:
  - web/src/app/(dashboard)/clientes/novo/page.tsx
  - web/src/app/(dashboard)/clientes/[id]/editar/page.tsx
  - web/src/app/(dashboard)/clientes/[id]/ficha/page.tsx
autonomous: true
requirements: [CLI-06]

must_haves:
  truths:
    - "The frontend compiles cleanly (tsc --noEmit) after the client types are flattened"
    - "No consumer file imports or reads DadosTipoParticular / DadosTipoEmpresa"
    - "No payload assigns dados_tipo to a ClienteCreateRequest or ClienteUpdateRequest"
    - "The novo page's NIF-sync assignment is string-safe (no string|undefined into required nif)"
  artifacts:
    - path: "web/src/app/(dashboard)/clientes/novo/page.tsx"
      provides: "Create-client page that builds a dados_tipo-free create payload"
    - path: "web/src/app/(dashboard)/clientes/[id]/editar/page.tsx"
      provides: "Edit-client page that builds a dados_tipo-free update payload"
    - path: "web/src/app/(dashboard)/clientes/[id]/ficha/page.tsx"
      provides: "Ficha page reading flat client fields (no DadosTipo imports)"
  key_links:
    - from: "web/src/app/(dashboard)/clientes/novo/page.tsx"
      to: "ClienteCreateRequest"
      via: "payload build without dados_tipo"
      pattern: "ClienteCreateRequest"
---

<objective>
Make the minimal, compile-safe adjustments to the three client consumer pages that
reference the now-removed `dados_tipo` / `DadosTipoParticular` / `DadosTipoEmpresa`
types, so `pnpm build` (and `tsc --noEmit`) succeeds after plan 71-01 flattens the
types. This is a build-restoration change ONLY — no form/detail UX redesign, which
the roadmap explicitly reserves for Phase 72 (create/edit forms) and Phase 73
(detail/ficha).

Purpose: Keep the frontend building and running while types are flattened (CLI-06),
deferring the real form/detail rework to the next two phases as scoped.
Output: Three consumer pages that compile against the flattened types.
</objective>

<execution_context>
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/LEXCV-71-frontend-types-schema-api-integration/71-CONTEXT.md
@.planning/phases/LEXCV-71-frontend-types-schema-api-integration/71-PATTERNS.md

<interfaces>
<!-- Result of plan 71-01. ClienteCreateRequest / ClienteUpdateRequest / Cliente
     no longer have a `dados_tipo` field. DadosTipoParticular / DadosTipoEmpresa
     no longer exist. Cliente keeps flat idade?/sexo?/nacionalidade?. -->

Scope note: the Zod `clienteFormSchema` (plan 71-01) NO LONGER has a `dados_tipo`
object. Therefore `ClienteFormValues` has no `values.dados_tipo`, and any
`form.register("dados_tipo.*")` / `values.dados_tipo?.*` reads in the forms will ALSO
fail to typecheck — those form-UI blocks must be neutralised here (not redesigned).
The full flat-field form rebuild is Phase 72.

Type note (NIF): after 71-01, `ClienteCreateRequest.nif` is REQUIRED (`nif: string`,
no longer optional) and the Zod `nif` field is required. However
`values.documento_numero` remains `string | undefined` (schema field
`documento_numero: optionalTrimmedString`, untouched by either plan). Zod's
`.superRefine` pairing does NOT narrow the static TS type, so the NIF-sync assignment
`payload.nif = values.documento_numero` on `novo/page.tsx` becomes a compile error
(`Type 'string | undefined' is not assignable to type 'string'`) once 71-01 lands. It
MUST be guarded here. The `editar/page.tsx` counterpart is unaffected because
`ClienteUpdateRequest.nif` stays optional.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Strip dados_tipo from the create-client page (novo)</name>
  <files>web/src/app/(dashboard)/clientes/novo/page.tsx</files>
  <read_first>
    - web/src/app/(dashboard)/clientes/novo/page.tsx (full file)
    - web/src/types/clientes.ts (post-71-01 shape — confirm no dados_tipo; confirm ClienteCreateRequest.nif is now required `string`)
    - web/src/schemas/clientes.ts (post-71-01 — confirm ClienteFormValues has no dados_tipo; confirm documento_numero is still optional `string | undefined`)
  </read_first>
  <action>
Make the create page compile against the flattened types with the smallest change set.
Do NOT redesign the form layout (that is Phase 72). Concretely:

1. In `defaultValues` (line ~59), remove the `dados_tipo: {},` entry.

2. Remove the tipo-change effect writes that call `form.setValue("dados_tipo.*", ...)`
   (lines ~77-84). Delete those `setValue` lines. If removing them empties an effect
   body, keep a valid no-op or delete the now-empty effect entirely — do not leave a
   dangling reference.

3. In `onSubmit` (lines ~97-122): DELETE the entire `const dados_tipo = ...` derivation
   (lines ~97-111) and REMOVE the `dados_tipo,` property from the `payload:
   ClienteCreateRequest` object literal (line ~117). Leave the rest of the payload
   (spread `...values`, tipo, avencado, documentoTipo/documentoNumero/ramoAtividade/
   detalhesAdicionais) intact — but the NIF sync in step 5 must be adjusted, NOT left
   as-is.

4. Remove the `const dadosTipoErrors = form.formState.errors.dados_tipo as ...`
   declaration (lines ~148-149) and delete the JSX field blocks that register against
   `dados_tipo.*` and read `dadosTipoErrors?.*` (the Particular idade/sexo/nacionalidade
   inputs at lines ~205-235 and the Empresa nome_comercial/sede/representante_legal/cargo
   inputs at lines ~249-289). Removing these input blocks is acceptable here because the
   full flat-field form is rebuilt in Phase 72 — leaving a compile-safe page is the goal.

5. GUARD THE NIF SYNC (required — this is the compile-blocker fix). The existing block
   at lines ~124-127 is:
   `if (values.documento_tipo === "NIF") { payload.nif = values.documento_numero; }`
   After 71-01, `payload.nif` is required `string` but `values.documento_numero` is
   `string | undefined`, so this assignment fails `tsc --noEmit`. Change the condition
   so the assignment only runs when `documento_numero` is a defined string, i.e. make it:
   `if (values.documento_tipo === "NIF" && values.documento_numero) { payload.nif = values.documento_numero; }`
   This narrows `values.documento_numero` to `string` inside the block, so the assignment
   is type-safe. Do NOT introduce a non-null assertion (`!`) or cast; use the truthiness
   guard so an empty/undefined document number simply leaves `payload.nif` unchanged.

6. Scan the rest of this file for any OTHER assignment of a `string | undefined` value
   into a now-required field on `payload` (i.e. into `ClienteCreateRequest`). There
   should be none beyond the NIF sync, but if one exists, guard it the same way. Do not
   leave any unguarded `string | undefined` -> required-string assignment.

After edits, `grep dados_tipo` on this file MUST return zero matches, and the NIF sync
MUST be guarded by a defined-`documento_numero` check.
  </action>
  <verify>
    <automated>cd web && node -e "const s=require('fs').readFileSync('src/app/(dashboard)/clientes/novo/page.tsx','utf8'); if(/dados_tipo|DadosTipo/.test(s)){console.error('FAIL: dados_tipo remnant');process.exit(1)} if(/payload\.nif\s*=\s*values\.documento_numero/.test(s) && !/documento_tipo === \"NIF\" && values\.documento_numero/.test(s)){console.error('FAIL: unguarded NIF sync');process.exit(1)} console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E "dados_tipo|DadosTipo" web/src/app/(dashboard)/clientes/novo/page.tsx` returns zero matches.
    - The `payload: ClienteCreateRequest` object literal contains no `dados_tipo` property.
    - No `form.setValue("dados_tipo...` and no `form.register("dados_tipo...` calls remain.
    - The NIF sync assigns `payload.nif` only inside a guard that narrows `values.documento_numero` to a defined string (e.g. `if (values.documento_tipo === "NIF" && values.documento_numero)`), with no `!` assertion or cast — so `values.documento_numero` (string | undefined) is never assigned to the required `payload.nif` unguarded.
  </acceptance_criteria>
  <done>The create page builds a dados_tipo-free ClienteCreateRequest, guards the NIF sync so the required `nif` never receives `string | undefined`, and contains no reference to the removed types.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Strip dados_tipo from the edit-client page (editar)</name>
  <files>web/src/app/(dashboard)/clientes/[id]/editar/page.tsx</files>
  <read_first>
    - web/src/app/(dashboard)/clientes/[id]/editar/page.tsx (full file)
    - web/src/types/clientes.ts (post-71-01)
  </read_first>
  <action>
Apply the same minimal strip as the create page. Do NOT redesign the form (Phase 72).
Concretely:

1. In `defaultValues` (line ~79), remove `dados_tipo: {},`.

2. Remove the tipo-change `(form.setValue as ...)("dados_tipo.*", ...)` writes
   (lines ~104-111). Delete those lines; if that empties an effect, remove the empty
   effect or leave a valid no-op.

3. In the reset/populate path (line ~150), remove `dados_tipo: cliente.data.dados_tipo ?? {},`
   from the object passed to `form.reset(...)`.

4. In `onSubmit` (lines ~184-207): DELETE the `const dados_tipo = ...` derivation
   (lines ~187-201) and REMOVE the `dados_tipo,` property from the `payload:
   ClienteUpdateRequest` object literal (line ~207). Keep the rest of the payload intact.

   NIF sync note: on this page `ClienteUpdateRequest.nif` stays OPTIONAL after 71-01, so
   the counterpart NIF assignment (line ~221) is type-safe as written and must be left
   unchanged. If — and only if — this page has any assignment of a `string | undefined`
   value into a field that 71-01 made required on `ClienteUpdateRequest`, guard it with a
   truthiness check as in Task 1. (No such required field is expected here.)

5. Delete the JSX input blocks that register against `dados_tipo.*` and read
   `form.formState.errors.dados_tipo` (the Particular block lines ~321-330 and the
   Empresa block lines ~340-359, including the two `(form.formState.errors.dados_tipo as
   Record<string, { message?: string }>)` error reads). As with the create page,
   removing these inputs is acceptable — the flat-field form is rebuilt in Phase 72.

After edits, `grep dados_tipo` on this file MUST return zero matches.
  </action>
  <verify>
    <automated>cd web && node -e "const s=require('fs').readFileSync('src/app/(dashboard)/clientes/[id]/editar/page.tsx','utf8'); if(/dados_tipo|DadosTipo/.test(s)){console.error('FAIL: dados_tipo remnant');process.exit(1)} console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E "dados_tipo|DadosTipo" web/src/app/(dashboard)/clientes/[id]/editar/page.tsx` returns zero matches.
    - The `payload: ClienteUpdateRequest` object literal contains no `dados_tipo` property.
    - `form.reset(...)` and `defaultValues` no longer set `dados_tipo`.
  </acceptance_criteria>
  <done>The edit page builds a dados_tipo-free ClienteUpdateRequest and contains no reference to the removed types.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Fix the ficha page reads/imports and confirm full frontend build</name>
  <files>web/src/app/(dashboard)/clientes/[id]/ficha/page.tsx</files>
  <read_first>
    - web/src/app/(dashboard)/clientes/[id]/ficha/page.tsx (full file)
    - web/src/types/clientes.ts (post-71-01 — Cliente keeps flat idade/sexo/nacionalidade)
  </read_first>
  <action>
Make the ficha (printable detail) page compile against the flattened Cliente. Do NOT
redesign the ficha layout (that is Phase 73). Concretely:

1. Fix the import on line 16: change
   `import type { Cliente, DadosTipoEmpresa, DadosTipoParticular } from "@/types/clientes";`
   to `import type { Cliente } from "@/types/clientes";` (drop the two removed types).

2. DELETE the `isDadosTipoParticular` type-guard function (lines ~44-49).

3. DELETE the `dadosParticular` / `dadosEmpresa` derivations (lines ~156-157).

4. Replace the Particular reads (lines ~159-161) to use the flat Cliente fields directly:
   `const idade = cliente.idade;`
   `const sexo = cliente.sexo;`
   `const nacionalidade = cliente.nacionalidade;`
   (Cliente still carries flat `idade?`, `sexo?`, `nacionalidade?` after 71-01.)

5. For the Empresa `<Field>` reads (lines ~197-200) that reference
   `dadosEmpresa?.nome_comercial` / `.sede` / `.representante_legal` / `.cargo`: those
   sub-fields no longer exist on Cliente. Replace each value with the blank placeholder
   the file already uses — pass `value={fmt(undefined)}` (which resolves to the file's
   BLANK constant) so the fields render as "—". Do NOT invent new Cliente fields; the
   real Empresa ficha rendering is Phase 73. Keep the Field labels as-is.

After edits, `grep -E "dados_tipo|DadosTipo|dadosEmpresa|dadosParticular"` on this file
MUST return zero matches.

6. FINAL BUILD GATE: run `cd web && pnpm exec tsc --noEmit`. It MUST exit 0 with no
   errors across the whole web app (this is the phase's build-restoration proof —
   plans 71-01 and 71-02 together must leave the frontend compiling). This gate directly
   depends on Task 1's guarded NIF sync: if `novo/page.tsx` still assigns
   `string | undefined` into the required `nif`, this gate will fail — confirm Task 1's
   guard is in place before treating a failure as a ficha-page issue.
  </action>
  <verify>
    <automated>cd web && node -e "const s=require('fs').readFileSync('src/app/(dashboard)/clientes/[id]/ficha/page.tsx','utf8'); if(/dados_tipo|DadosTipo|dadosEmpresa|dadosParticular/.test(s)){console.error('FAIL: remnant');process.exit(1)} console.log('OK')" && pnpm exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E "dados_tipo|DadosTipo|dadosEmpresa|dadosParticular" web/src/app/(dashboard)/clientes/[id]/ficha/page.tsx` returns zero matches.
    - The import line imports only `Cliente` from `@/types/clientes`.
    - `cd web && pnpm exec tsc --noEmit` exits 0 (whole-app build is green, including the guarded NIF sync in novo/page.tsx).
  </acceptance_criteria>
  <done>The ficha page reads flat Cliente fields, imports no removed types, and `tsc --noEmit` passes across the whole web app.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser form -> API payload | Create/edit pages assemble `ClienteCreateRequest`/`ClienteUpdateRequest` and POST/PUT them |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-71-03 | Tampering | Create/edit payload assembly after removing `dados_tipo` | accept | Removing the `dados_tipo` payload branch drops fields the backend no longer accepts (Phase 70 flattened the model). No new untrusted field is introduced; the change strictly reduces the outbound surface. |
| T-71-04 | Information Disclosure | Ficha page rendering blank Empresa fields as "—" | accept | Replacing removed sub-field reads with the existing BLANK placeholder shows no data that was not already user-visible; it renders nothing where data no longer exists. Real Empresa ficha rendering is Phase 73. |
| T-71-05 | Tampering | NIF sync guard on novo/page.tsx | mitigate | Guarding the NIF assignment with a defined-`documento_numero` truthiness check (no `!`/cast) means the required `nif` is only ever set from a validated non-empty string; an empty/undefined document number leaves `nif` at its schema-validated value rather than silently coercing `undefined`. |
| T-71-SC | Tampering | npm/pnpm installs | mitigate | This plan adds NO new dependencies (edits three existing page files only). No install step, so the package-legitimacy checkpoint is not applicable. |
</threat_model>

<verification>
- `cd web && pnpm exec tsc --noEmit` exits 0 across the whole app (final gate in Task 3).
- No `dados_tipo` / `DadosTipo` remnants in any of the three edited pages (per-task node checks).
- The novo/page.tsx NIF sync is guarded so the required `nif` never receives `string | undefined`.
- The two read-only consumer pages NOT edited here — `web/src/app/(dashboard)/clientes/[id]/page.tsx`
  and `web/src/app/(dashboard)/clientes/page.tsx` — only read `documento_tipo`/`documentoTipo`,
  which survive 71-01 (retyped to `DocumentoTipo`), so they continue to compile without edits.
</verification>

<success_criteria>
- The three edited consumer pages contain zero references to `dados_tipo`,
  `DadosTipoParticular`, or `DadosTipoEmpresa`.
- Create/edit payloads no longer include a `dados_tipo` property.
- The novo/page.tsx NIF sync is guarded and type-safe against the now-required `nif`.
- `pnpm exec tsc --noEmit` passes for the entire web app.
- No form/detail UX redesign was performed (that work is deferred to Phase 72 / 73).
</success_criteria>

<output>
Create `.planning/phases/LEXCV-71-frontend-types-schema-api-integration/71-02-SUMMARY.md` when done.
</output>
</content>
</invoke>
