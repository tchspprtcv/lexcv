---
phase: 72-form-refactoring-create-edit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/app/(dashboard)/clientes/novo/page.tsx
  - web/src/app/(dashboard)/clientes/[id]/editar/page.tsx
autonomous: true
requirements: [CLI-05, CLI-07, CLI-08, CLI-09, CLI-10]

must_haves:
  truths:
    - "Ao selecionar tipo=EMPRESA no formulário de criação, o rótulo do campo nome muda para 'Nome Comercial' e o do campo morada para 'Sede' ao vivo"
    - "Ao selecionar tipo=PARTICULAR (ou por defeito), o rótulo do campo nome é 'Nome' e o do campo morada é 'Morada'"
    - "O mesmo comportamento dinâmico de rótulo está presente no formulário de edição"
    - "O campo NIF é rotulado apenas 'NIF' (sem sufixo '(Legado)') em ambos os formulários"
    - "O select documento_tipo oferece a opção 'Registo Comercial' (REG_COMERCIAL) em ambos os formulários"
  artifacts:
    - path: "web/src/app/(dashboard)/clientes/novo/page.tsx"
      provides: "Formulário de criação com rótulos dinâmicos nome/morada e NIF renomeado"
      contains: "form.watch(\"tipo\")"
    - path: "web/src/app/(dashboard)/clientes/[id]/editar/page.tsx"
      provides: "Formulário de edição com rótulos dinâmicos nome/morada e NIF renomeado"
      contains: "form.watch(\"tipo\")"
  key_links:
    - from: "web/src/app/(dashboard)/clientes/novo/page.tsx"
      to: "react-hook-form tipo field"
      via: "form.watch(\"tipo\") drives nomeLabel/moradaLabel"
      pattern: "nomeLabel|moradaLabel"
    - from: "web/src/app/(dashboard)/clientes/[id]/editar/page.tsx"
      to: "react-hook-form tipo field"
      via: "form.watch(\"tipo\") drives nomeLabel/moradaLabel"
      pattern: "nomeLabel|moradaLabel"
---

<objective>
Aplicar rótulos dinâmicos condicionados ao tipo de cliente (Nome/Nome Comercial, Morada/Sede) e renomear o rótulo do campo NIF de "NIF (Legado)" para "NIF" nos dois formulários de cliente (criação e edição). Confirmar que a opção REG_COMERCIAL já está presente no select documento_tipo de ambos os formulários (entregue na correção de review da Phase 71).

Purpose: Fechar CLI-07/CLI-08 (rótulos dinâmicos por tipo), CLI-05 (NIF é agora a identificação obrigatória, deixa de ser "legado") e verificar CLI-09/CLI-10 (REG_COMERCIAL selecionável). O modelo de dados já foi aplanado nas Phases 70/71; esta fase é puramente alteração de texto/UX no lado cliente.
Output: Dois ficheiros de formulário atualizados, com rótulos reativos e nomenclatura consolidada do NIF.
</objective>

<execution_context>
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/LEXCV-72-form-refactoring-create-edit/72-CONTEXT.md
@.planning/phases/LEXCV-72-form-refactoring-create-edit/72-PATTERNS.md

<interfaces>
<!-- Padrão exato a aplicar (extraído de 72-PATTERNS.md). Não é preciso explorar o codebase. -->
<!-- O campo `tipo` vive exclusivamente no estado do react-hook-form (Controller + form.setValue),
     sem espelho local de useState, pelo que form.watch("tipo") é a leitura live-reactive correta. -->

Bloco a inserir no corpo do componente, ANTES do return JSX (idêntico nos dois ficheiros):

```tsx
const tipoValue = form.watch("tipo");
const nomeLabel = tipoValue === "EMPRESA" ? "Nome Comercial" : "Nome";
const moradaLabel = tipoValue === "EMPRESA" ? "Sede" : "Morada";
```

Estado atual dos rótulos (verbatim, ambos os ficheiros):
- Nome:   `<Label htmlFor="nome">Nome</Label>`   → passa a `<Label htmlFor="nome">{nomeLabel}</Label>`
- NIF:    `<Label htmlFor="nif">NIF (Legado)</Label>` → passa a `<Label htmlFor="nif">NIF</Label>`
- Morada: `<Label htmlFor="morada">Morada</Label>` → passa a `<Label htmlFor="morada">{moradaLabel}</Label>`

REG_COMERCIAL — já presente em ambos os selects (confirmar, não adicionar):
`<option value="REG_COMERCIAL">Registo Comercial</option>`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rótulos dinâmicos + rename do NIF em novo/page.tsx</name>
  <files>web/src/app/(dashboard)/clientes/novo/page.tsx</files>
  <read_first>
    - web/src/app/(dashboard)/clientes/novo/page.tsx (bloco de campos Nome/NIF ~linhas 173-190, Morada ~linhas 217-223, select documento_tipo ~linhas 236-247)
    - .planning/phases/LEXCV-72-form-refactoring-create-edit/72-PATTERNS.md (secção "novo/page.tsx" e "Shared Patterns")
  </read_first>
  <action>
    No corpo do componente (o mesmo componente que define o form via useForm/zodResolver), imediatamente antes do `return (` do JSX, inserir as três constantes derivadas: `const tipoValue = form.watch("tipo");`, `const nomeLabel = tipoValue === "EMPRESA" ? "Nome Comercial" : "Nome";`, `const moradaLabel = tipoValue === "EMPRESA" ? "Sede" : "Morada";`. Por CLI-07, substituir o rótulo estático do campo nome `<Label htmlFor="nome">Nome</Label>` por `<Label htmlFor="nome">{nomeLabel}</Label>`. Por CLI-08, substituir o rótulo estático do campo morada `<Label htmlFor="morada">Morada</Label>` por `<Label htmlFor="morada">{moradaLabel}</Label>`. Por CLI-05, substituir o rótulo `<Label htmlFor="nif">NIF (Legado)</Label>` por `<Label htmlFor="nif">NIF</Label>`. NÃO reordenar campos (o NIF já está diretamente sob o Nome), NÃO tocar em onTipoChange/confirmTipoChange (form.watch já reage a form.setValue), NÃO alterar o schema. Por CLI-09/CLI-10, verificar (sem alterar) que o select documento_tipo já contém `<option value="REG_COMERCIAL">Registo Comercial</option>` — se por algum motivo estiver ausente, adicioná-la como última opção após Passaporte.
  </action>
  <verify>
    <automated>cd web && node -e "const s=require('fs').readFileSync('src/app/(dashboard)/clientes/novo/page.tsx','utf8'); const ok = s.includes('const nomeLabel') && s.includes('const moradaLabel') && s.includes('form.watch(\"tipo\")') && s.includes('{nomeLabel}') && s.includes('{moradaLabel}') && !s.includes('NIF (Legado)') && s.includes('>NIF<') && s.includes('REG_COMERCIAL'); if(!ok){console.error('gate failed');process.exit(1)} console.log('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `form.watch("tipo")` é lido e alimenta nomeLabel/moradaLabel
    - O rótulo do campo nome renderiza `{nomeLabel}` (não a string literal "Nome")
    - O rótulo do campo morada renderiza `{moradaLabel}` (não a string literal "Morada")
    - A string "NIF (Legado)" já não existe no ficheiro; o rótulo do NIF é exatamente "NIF"
    - O select documento_tipo contém a opção REG_COMERCIAL
  </acceptance_criteria>
  <done>Rótulos nome/morada dinâmicos, NIF renomeado, REG_COMERCIAL confirmado em novo/page.tsx.</done>
</task>

<task type="auto">
  <name>Task 2: Rótulos dinâmicos + rename do NIF em editar/page.tsx</name>
  <files>web/src/app/(dashboard)/clientes/[id]/editar/page.tsx</files>
  <read_first>
    - web/src/app/(dashboard)/clientes/[id]/editar/page.tsx (bloco Nome/NIF/Tipo ~linhas 249-294, Morada ~linhas 321-327, select documento_tipo ~linhas 341-351)
    - .planning/phases/LEXCV-72-form-refactoring-create-edit/72-PATTERNS.md (secção "editar/page.tsx")
  </read_first>
  <action>
    No componente ClienteEditContent, imediatamente antes do `return (` do JSX, inserir as mesmas três constantes: `const tipoValue = form.watch("tipo");`, `const nomeLabel = tipoValue === "EMPRESA" ? "Nome Comercial" : "Nome";`, `const moradaLabel = tipoValue === "EMPRESA" ? "Sede" : "Morada";`. Por CLI-07, substituir `<Label htmlFor="nome">Nome</Label>` por `<Label htmlFor="nome">{nomeLabel}</Label>`. Por CLI-08, substituir `<Label htmlFor="morada">Morada</Label>` por `<Label htmlFor="morada">{moradaLabel}</Label>`. Por CLI-05, substituir `<Label htmlFor="nif">NIF (Legado)</Label>` por `<Label htmlFor="nif">NIF</Label>`. NÃO reordenar campos (a ordem Nome→NIF→Tipo já é a atual e é aceitável por decisão de discricionariedade da CONTEXT.md), NÃO tocar em onTipoChange/confirmTipoChange nem no form.reset async do useEffect (form.watch reflete o valor após o reset carregar cliente.data). Por CLI-09/CLI-10, verificar (sem alterar) que o select documento_tipo já contém `<option value="REG_COMERCIAL">Registo Comercial</option>`; adicioná-la só se ausente.
  </action>
  <verify>
    <automated>cd web && node -e "const s=require('fs').readFileSync('src/app/(dashboard)/clientes/[id]/editar/page.tsx','utf8'); const ok = s.includes('const nomeLabel') && s.includes('const moradaLabel') && s.includes('form.watch(\"tipo\")') && s.includes('{nomeLabel}') && s.includes('{moradaLabel}') && !s.includes('NIF (Legado)') && s.includes('>NIF<') && s.includes('REG_COMERCIAL'); if(!ok){console.error('gate failed');process.exit(1)} console.log('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `form.watch("tipo")` é lido e alimenta nomeLabel/moradaLabel
    - O rótulo do campo nome renderiza `{nomeLabel}`
    - O rótulo do campo morada renderiza `{moradaLabel}`
    - A string "NIF (Legado)" já não existe no ficheiro; o rótulo do NIF é exatamente "NIF"
    - O select documento_tipo contém a opção REG_COMERCIAL
  </acceptance_criteria>
  <done>Rótulos nome/morada dinâmicos, NIF renomeado, REG_COMERCIAL confirmado em editar/page.tsx.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser (React) | Alterações puramente de apresentação (texto de rótulo). Nenhum novo input, endpoint ou fluxo de dados atravessa uma fronteira de confiança. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-72-01 | Information Disclosure | rótulos de formulário nome/morada/NIF | accept | Apenas texto estático/derivado de enum client-side; nenhum dado sensível novo é exposto. Rótulos não alteram o payload enviado ao backend. |
| T-72-02 | Tampering | validação NIF/documento_tipo | accept | A validação obrigatória do NIF e a whitelist de documento_tipo permanecem no Zod schema (Phase 71) e no backend @PreAuthorize/entidade (Phase 70). Renomear o rótulo não afeta validação server-side; o backend continua a ser a autoridade. |
| T-72-SC | Tampering | npm/pip/cargo installs | mitigate | Nenhuma instalação de package nesta fase — sem novas dependências. Gate não aplicável. |
</threat_model>

<verification>
- `cd web && pnpm lint` sem novos erros nos dois ficheiros alterados
- `cd web && pnpm build` compila com sucesso (TypeScript sem erros nos ficheiros de formulário)
- Grep confirma ausência total de "NIF (Legado)" no diretório clientes: `grep -rn "NIF (Legado)" web/src/app/(dashboard)/clientes` retorna vazio
</verification>

<success_criteria>
- CLI-07: rótulo do nome alterna Nome/Nome Comercial conforme tipo, ao vivo, em ambos os formulários
- CLI-08: rótulo da morada alterna Morada/Sede conforme tipo, ao vivo, em ambos os formulários
- CLI-05: rótulo do NIF é "NIF" (sem "(Legado)") em ambos os formulários
- CLI-09/CLI-10: opção REG_COMERCIAL presente no select documento_tipo de ambos os formulários
- Nenhuma regressão de compilação/lint
</success_criteria>

<output>
Create `.planning/phases/LEXCV-72-form-refactoring-create-edit/72-01-SUMMARY.md` when done
</output>
