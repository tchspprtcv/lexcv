---
phase: 73-detail-page-printable-ficha-update
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/app/(dashboard)/clientes/[id]/page.tsx
  - web/src/app/(dashboard)/clientes/[id]/ficha/page.tsx
autonomous: true
requirements: [CLI-11]

must_haves:
  truths:
    - "On the client detail page, the address row label reads 'Sede' when the client is an Empresa and 'Morada' when Particular"
    - "On the printable ficha, the Contactos address row label reads 'Sede' for Empresa and 'Morada' for Particular"
    - "The printable ficha Identificação block for an Empresa no longer shows the always-blank 'Nome Comercial', 'Representante Legal', 'Cargo', or duplicate 'Sede' fields"
  artifacts:
    - path: "web/src/app/(dashboard)/clientes/[id]/page.tsx"
      provides: "Detail view with dynamic Morada/Sede label"
      contains: "cliente.data.tipo === \"EMPRESA\" ? \"Sede\" : \"Morada\""
    - path: "web/src/app/(dashboard)/clientes/[id]/ficha/page.tsx"
      provides: "Printable ficha with trimmed Empresa Identificação block and dynamic Contactos address label"
      contains: "label={isEmpresa ? \"Sede\" : \"Morada\"}"
  key_links:
    - from: "web/src/app/(dashboard)/clientes/[id]/page.tsx"
      to: "cliente.data.tipo"
      via: "ternary in <dt> label"
      pattern: "tipo === \"EMPRESA\" \\? \"Sede\" : \"Morada\""
    - from: "web/src/app/(dashboard)/clientes/[id]/ficha/page.tsx"
      to: "isEmpresa"
      via: "ternary in Field label prop"
      pattern: "isEmpresa \\? \"Sede\" : \"Morada\""
---

<objective>
Update the client detail page and the printable ficha to present only the simplified, flattened client data structure (CLI-11), consistent with the Phase 72 form refactoring:

1. Detail page (`[id]/page.tsx`): make the static "Morada" `<dt>` label dynamic — "Sede" for Empresa, "Morada" for Particular.
2. Printable ficha (`ficha/page.tsx`): remove the always-blank discontinued Empresa fields ("Nome Comercial", "Representante Legal", "Cargo") and the duplicate "Sede" field from the Identificação block; make the Contactos "Morada" label dynamic using the existing `isEmpresa` const.

Purpose: Finish the v2.7 client-management simplification by removing the last visual traces of the discontinued/duplicated fields and aligning read/print views with the dynamic-label convention already used in the forms.

Output: Two modified presentation files. No new files, no type/schema changes, no data-fetch changes.
</objective>

<execution_context>
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/LEXCV-73-detail-page-printable-ficha-update/73-CONTEXT.md
@.planning/phases/LEXCV-73-detail-page-printable-ficha-update/73-PATTERNS.md

<interfaces>
<!-- Exact current-state excerpts from PATTERNS.md. Executor edits these verbatim — no codebase exploration needed. -->

From web/src/app/(dashboard)/clientes/[id]/page.tsx — the address row (lines 170-171), rendered inside a `<dl>` grid of `<dt>`/`<dd>` pairs:
```tsx
                  <dt className="text-neutral-500 dark:text-neutral-400">Morada</dt>
                  <dd className="col-span-2">{cliente.data.morada ?? "—"}</dd>
```
`cliente.data.tipo` is already read at line 159 (`{cliente.data.tipo ?? "—"}`), confirming the field is available on the loaded object. This is a plain data read — no `form.watch`, no controlled input.

From web/src/app/(dashboard)/clientes/[id]/ficha/page.tsx — the `Field` helper (lines 125-133, unchanged by this plan):
```tsx
function Field({ label, value }: { label: string; value: string }) {
  const isBlank = value === BLANK;
  return (
    <div className="grid grid-cols-2 gap-2 py-1 text-sm">
      <span className="text-gray-600">{label}</span>
      <span className={isBlank ? "font-mono underline" : ""}>{value}</span>
    </div>
  );
}
```

From web/src/app/(dashboard)/clientes/[id]/ficha/page.tsx — the `isEmpresa` flag (line 148, reused as-is):
```tsx
  const isEmpresa = cliente.tipo === "EMPRESA";
```

From web/src/app/(dashboard)/clientes/[id]/ficha/page.tsx — the Identificação `isEmpresa` branch (lines 185-199):
```tsx
      {isEmpresa ? (
        <>
          <Field label="NIF" value={fmt(cliente.nif)} />
          <Field label="Nome Comercial" value={fmt(undefined)} />
          <Field label="Sede" value={fmt(undefined)} />
          <Field label="Representante Legal" value={fmt(undefined)} />
          <Field label="Cargo" value={fmt(undefined)} />
        </>
      ) : (
        <>
          <Field label="Idade" value={fmt(idade)} />
          <Field label="Sexo" value={fmt(sexo)} />
          <Field label="Nacionalidade" value={fmt(nacionalidade)} />
        </>
      )}
```

From web/src/app/(dashboard)/clientes/[id]/ficha/page.tsx — the Contactos section (lines 201-205):
```tsx
      <SectionTitle>Contactos</SectionTitle>
      <Field label="Morada" value={fmt(cliente.morada)} />
      <Field label="Localidade" value={fmt(cliente.localidade)} />
      <Field label="Telefone" value={fmt(cliente.telefone)} />
      <Field label="Email" value={fmt(cliente.email)} />
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dynamic Morada/Sede label on the client detail page</name>
  <files>web/src/app/(dashboard)/clientes/[id]/page.tsx</files>
  <read_first>
    - web/src/app/(dashboard)/clientes/[id]/page.tsx lines 158-171 (the `<dl>` address row and surrounding `<dt>`/`<dd>` grid). See the `<interfaces>` block above for the exact current excerpt.
  </read_first>
  <action>
    Replace the static "Morada" text node in the `<dt>` at line 170 with a ternary that reads the already-loaded `cliente.data.tipo`, per CONTEXT.md decision (Dynamic Labels) and CLI-11. The label must read "Sede" when `cliente.data.tipo === "EMPRESA"` and "Morada" otherwise.

    Change only the `<dt>` label content. Do NOT touch the `<dd>` value expression — keep `{cliente.data.morada ?? "—"}` exactly as-is. Preserve the existing `className="text-neutral-500 dark:text-neutral-400"` on the `<dt>` and `className="col-span-2"` on the `<dd>`.

    Target result:
    - `<dt className="text-neutral-500 dark:text-neutral-400">` containing `{cliente.data.tipo === "EMPRESA" ? "Sede" : "Morada"}`
    - `<dd className="col-span-2">{cliente.data.morada ?? "—"}</dd>` unchanged

    Use `cliente.data.tipo` directly (this is a read view; there is no form or `watch` here — CONTEXT.md explicitly notes this simplification vs. the Phase 72 form pattern). Do NOT introduce a new state variable or helper.
  </action>
  <verify>
    <automated>cd web && node -e "const s=require('fs').readFileSync('src/app/(dashboard)/clientes/[id]/page.tsx','utf8'); const dyn=/cliente\.data\.tipo === \"EMPRESA\" \? \"Sede\" : \"Morada\"/.test(s); const noStatic=!/>Morada<\/dt>/.test(s); const ddOk=/\{cliente\.data\.morada \?\? \"—\"\}/.test(s); if(!dyn) throw new Error('dynamic Sede/Morada dt label missing'); if(!noStatic) throw new Error('static >Morada</dt> label still present'); if(!ddOk) throw new Error('morada dd value expression changed'); console.log('OK');"</automated>
  </verify>
  <acceptance_criteria>
    - The `<dt>` label expression `cliente.data.tipo === "EMPRESA" ? "Sede" : "Morada"` is present in the file.
    - No literal `>Morada</dt>` static label remains.
    - The `<dd>` still renders `{cliente.data.morada ?? "—"}` (value unchanged).
    - `pnpm build` compiles (verified in Task 2 phase-wide check / verification section).
  </acceptance_criteria>
  <done>The detail page address row shows "Sede" for Empresa clients and "Morada" for Particular clients, with the address value rendering unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Trim discontinued Empresa fields and add dynamic Contactos label on the printable ficha</name>
  <files>web/src/app/(dashboard)/clientes/[id]/ficha/page.tsx</files>
  <read_first>
    - web/src/app/(dashboard)/clientes/[id]/ficha/page.tsx lines 125-133 (`Field` helper), line 148 (`isEmpresa` const), lines 185-199 (Identificação `isEmpresa` branch), lines 201-205 (Contactos section). See the `<interfaces>` block above for the exact current excerpts.
  </read_first>
  <action>
    Two edits in this file, both presentation-only, per CONTEXT.md decisions ("Ficha Imprimível — remover campos de Empresa descontinuados" and "Dynamic Labels") and REQUIREMENTS.md "Out of Scope" (representante legal / cargo explicitly discarded):

    Edit A — Trim the Identificação `isEmpresa` branch (lines 185-199). Remove exactly four `<Field>` lines from the `isEmpresa ? (...)` branch: `Field label="Nome Comercial" value={fmt(undefined)}`, `Field label="Sede" value={fmt(undefined)}`, `Field label="Representante Legal" value={fmt(undefined)}`, and `Field label="Cargo" value={fmt(undefined)}`. Keep only `<Field label="NIF" value={fmt(cliente.nif)} />` in that branch. The Particular branch (`Idade`, `Sexo`, `Nacionalidade`) is untouched. Rationale: those four fields always render `fmt(undefined)` → `BLANK` today (dead visual clutter); "Nome Comercial" is already covered by the shared `Nome` field (CLI-07 unified nome/nome comercial), and "Sede" must not be duplicated — it moves to the Contactos section with a dynamic label (Edit B).

    Edit B — Make the Contactos address label dynamic (line 202). Change `<Field label="Morada" value={fmt(cliente.morada)} />` to use `label={isEmpresa ? "Sede" : "Morada"}`, reusing the pre-existing `isEmpresa` const (line 148). Keep `value={fmt(cliente.morada)}` unchanged. Leave the `Localidade`, `Telefone`, and `Email` fields exactly as-is.

    Do NOT modify the `Field` helper (lines 125-133), the `fmt()` formatter, or the `isEmpresa` derivation. Do NOT re-derive `tipo` — use the existing `isEmpresa` boolean.
  </action>
  <verify>
    <automated>cd web && node -e "const s=require('fs').readFileSync('src/app/(dashboard)/clientes/[id]/ficha/page.tsx','utf8'); if(/label=\"Nome Comercial\"/.test(s)) throw new Error('Nome Comercial field still present'); if(/label=\"Representante Legal\"/.test(s)) throw new Error('Representante Legal field still present'); if(/label=\"Cargo\"/.test(s)) throw new Error('Cargo field still present'); if(/label=\"Sede\" value=\{fmt\(undefined\)\}/.test(s)) throw new Error('duplicate blank Sede field still present'); if(!/label=\{isEmpresa \? \"Sede\" : \"Morada\"\} value=\{fmt\(cliente\.morada\)\}/.test(s)) throw new Error('dynamic Contactos Sede/Morada label missing'); if(!/label=\"NIF\" value=\{fmt\(cliente\.nif\)\}/.test(s)) throw new Error('NIF field removed by mistake'); console.log('OK');"</automated>
  </verify>
  <acceptance_criteria>
    - No `label="Nome Comercial"`, `label="Representante Legal"`, or `label="Cargo"` remain anywhere in the file.
    - No `label="Sede" value={fmt(undefined)}` duplicate blank field remains.
    - The Empresa Identificação branch retains `<Field label="NIF" value={fmt(cliente.nif)} />`.
    - The Contactos section contains `label={isEmpresa ? "Sede" : "Morada"} value={fmt(cliente.morada)}`.
    - The `Field` helper, `fmt()`, and `isEmpresa` const are unchanged.
  </acceptance_criteria>
  <done>The printable ficha's Empresa Identificação block shows only NIF (no blank Nome Comercial/Representante Legal/Cargo/Sede lines), and the Contactos address label reads "Sede" for Empresa and "Morada" for Particular.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server→browser render | Already-fetched, tenant-scoped `cliente` data is rendered client-side. No new input crosses any boundary in this phase. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-73-01 | Information Disclosure | `[id]/page.tsx`, `ficha/page.tsx` label/field rendering | accept | Change is label-string and dead-field removal only on data already fetched via existing tenant-scoped `useCliente` hook. No new fields exposed; removing always-blank fields reduces surface. No PII newly surfaced. |
| T-73-02 | Tampering | `cliente.data.tipo` / `isEmpresa` ternary | accept | Value is read-only display data from the trusted API response; the ternary only selects a static label string ("Sede"/"Morada") and cannot alter data or execute logic. |
| T-73-SC | Tampering | npm/pnpm installs | accept | No new dependencies added or installed in this phase. No `[ASSUMED]`/`[SUS]`/`[SLOP]` packages introduced; Package Legitimacy Gate not triggered. |
</threat_model>

<verification>
Phase-wide checks after both tasks:

1. Both automated task verifiers (Task 1 and Task 2) pass.
2. Frontend compiles and lints:
   - `cd web && pnpm lint`
   - `cd web && pnpm build`
3. Grep confirms no discontinued Empresa field labels remain in the ficha:
   - `grep -n 'Nome Comercial\|Representante Legal\|label="Cargo"' 'web/src/app/(dashboard)/clientes/[id]/ficha/page.tsx'` returns no matches.
4. Grep confirms dynamic labels present in both files:
   - Detail page contains `tipo === "EMPRESA" ? "Sede" : "Morada"`.
   - Ficha contains `isEmpresa ? "Sede" : "Morada"`.
</verification>

<success_criteria>
- CLI-11 satisfied: detail page and printable ficha present only the simplified, flattened client structure.
- Detail page address label is dynamic ("Sede"/"Morada" by tipo); address value unchanged.
- Ficha Empresa Identificação block no longer renders the always-blank "Nome Comercial", "Representante Legal", "Cargo", or duplicate "Sede" fields; retains NIF.
- Ficha Contactos address label is dynamic via the existing `isEmpresa` const.
- `pnpm lint` and `pnpm build` succeed with no new errors.
- No type, schema, or data-fetch changes introduced.
</success_criteria>

<output>
Create `.planning/phases/LEXCV-73-detail-page-printable-ficha-update/73-01-SUMMARY.md` when done.
</output>
