---
phase: 96-notf-26-snooze-de-lembrete-de-prazo
plan: 04
type: execute
wave: 3
depends_on: [96-03]
files_modified: []
autonomous: false
requirements: [NOTF-26]

must_haves:
  truths:
    - "A human confirms the snooze control appears (1/3/7 presets), snoozing drops the bell badge and removes the item from the bell preview"
    - "A human confirms the snoozed item stays visible on /notificacoes with an 'Adiado até DD/MM' badge"
    - "A human confirms PRAZO_VENCIDO notifications offer no snooze control and a direct snooze attempt is rejected"
  artifacts: []
  key_links: []
---

<objective>
End-to-end human verification of NOTF-26 snooze across the full stack (backend endpoint + read-side
filter + UI). Automated coverage already proves the write contract (96-01 Mockito) and the read-side
visibility predicate against real Postgres (96-02 Testcontainers); this checkpoint confirms the
integrated behavior a human can see in the running app — the bell/history split and the PRAZO_VENCIDO
block — which no unit or integration test observes.

Purpose: NOTF-26 success criteria 1, 2, and the PRAZO_VENCIDO block, verified live.
Output: human approval (or a list of issues to feed gap closure).
</objective>

<execution_context>
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/LEXCV-96-notf-26-snooze-de-lembrete-de-prazo/96-CONTEXT.md
@.planning/phases/LEXCV-96-notf-26-snooze-de-lembrete-de-prazo/96-01-SUMMARY.md
@.planning/phases/LEXCV-96-notf-26-snooze-de-lembrete-de-prazo/96-03-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Verify snooze end-to-end in the running app</name>
  <files>web/src/components/shared/notification-bell.tsx, web/src/app/(dashboard)/notificacoes/page.tsx</files>
  <read_first>
    - .planning/phases/LEXCV-96-notf-26-snooze-de-lembrete-de-prazo/96-01-SUMMARY.md (endpoint + migration shipped)
    - .planning/phases/LEXCV-96-notf-26-snooze-de-lembrete-de-prazo/96-03-SUMMARY.md (the two wired surfaces + snooze control)
  </read_first>
  <action>
    Human checkpoint (no code written): 96-01/96-02/96-03 already shipped the endpoint, the read-side
    filter, the IT, and the UI. This task pauses for real-browser verification before closing the phase —
    the runtime behavior automated tests do not cover (a full-context controller test would hit the
    MINIO_ENDPOINT blocker, Pitfall 5). Run the `<how-to-verify>` steps and wait for the resume signal.
    Do not implement anything in this task.
  </action>
  <what-built>
    NOTF-26 snooze: a nullable Notificacao.snoozedUntil column (manual migration
    96-add-notificacao-snoozed-until.sql), a snooze-visibility predicate on the bell badge-count and
    mark-all-read queries, a PATCH /notificacoes/{id}/snooze endpoint (1/3/7-day presets, PRAZO_VENCIDO
    blocked, dual tenant+destinatario scoped), and a reusable Popover+RadioGroup snooze control wired
    into the notification bell and the /notificacoes history page.
  </what-built>
  <how-to-verify>
    Preconditions:
    1. Run the manual migration once against your dev database:
       `psql "$DATABASE_URL" -f backend/migrations/96-add-notificacao-snoozed-until.sql`
       (or run dev with ddl-auto=update, which auto-adds the column — but confirm the SQL is valid).
    2. Start the backend (`cd backend && mvn spring-boot:run`, needs backend/.env + Postgres) and the
       frontend (`cd web && pnpm dev`). Log in (default admin admin@lexcv.cv / Pa$$w0rd). Ensure at least
       one non-PRAZO_VENCIDO notification and one PRAZO_VENCIDO notification exist for the logged-in user
       (trigger a fase entrada / document upload for a non-fatal one; a PRAZO_VENCIDO can be produced via
       the daily job on an overdue prazo, or inserted directly for the test).

    Checks:
    1. Snooze control present: open the bell dropdown. A non-PRAZO_VENCIDO notification shows an "Adiar"
       (clock) control. Click it — a Popover opens with exactly three options: 1 dia, 3 dias, 7 dias, and
       an "Adiar" confirm button. No free-form date picker.
    2. Disappears from the bell + badge drops: pick "3 dias" and confirm. The unread badge count decreases
       by one immediately (no wait for polling), and the item disappears from the bell dropdown preview.
    3. Stays in history, marked: navigate to /notificacoes. The same notification is still listed
       (searchable/filterable), now showing an "Adiado até DD/MM" badge. The snooze control is still
       available on that row.
    4. PRAZO_VENCIDO cannot be snoozed: locate a PRAZO_VENCIDO notification (bell and/or /notificacoes).
       It shows NO snooze/"Adiar" control. (Optional API check: `curl -X PATCH
       "$ORIGIN/api/v1/notificacoes/{prazoVencidoId}/snooze" -H "Content-Type: application/json"
       --data '{"dias":3}' --cookie "$AUTH"` returns 400.)
    5. Ownership (optional): a PATCH snooze against a notification id belonging to a different user returns
       404, not 403 (no existence leak).
  </how-to-verify>
  <verify>
    <human-check>Snooze control shows 1/3/7 presets on non-fatal notifications; snoozing drops the bell badge and removes the item from the bell preview while it stays on /notificacoes with an "Adiado até" badge; PRAZO_VENCIDO shows no snooze control (and API returns 400 if tested).</human-check>
  </verify>
  <acceptance_criteria>
    - Snooze control offers exactly 1/3/7-day presets (no date picker) on non-PRAZO_VENCIDO rows.
    - Snoozing drops the bell badge immediately and removes the item from the bell dropdown preview.
    - The snoozed item stays visible on /notificacoes, marked "Adiado até DD/MM".
    - PRAZO_VENCIDO shows no snooze control; (if tested) a direct PATCH snooze returns 400.
  </acceptance_criteria>
  <resume-signal>Type "approved" if all checks pass, or describe which check failed (surface, expected vs. actual) so gap closure can address it.</resume-signal>
  <done>Human verification confirms the bell/history split, the badge drop, the "Adiado até" marking, and the PRAZO_VENCIDO block behave as specified.</done>
</task>

</tasks>

<verification>
- Human confirms the bell/history split, the badge drop, the "Adiado até" marking, and the PRAZO_VENCIDO block behave as specified.
</verification>

<success_criteria>
- All live checks pass, or failures are captured precisely enough to drive `/gsd:plan-phase 96 --gaps`.
</success_criteria>

<output>
Create `.planning/phases/LEXCV-96-notf-26-snooze-de-lembrete-de-prazo/96-04-SUMMARY.md` when done
</output>
