---
phase: 95-notf-25-notificar-toda-a-equipa-do-processo
plan: 02
type: execute
wave: 2
depends_on: [95-01]
files_modified:
  - backend/src/main/java/com/lexcv/controllers/ResourceController.java
  - .planning/PROJECT.md
autonomous: true
requirements: [NOTF-25]

must_haves:
  truths:
    - "createProcesso and atribuirResponsavel pass the processo's clienteId so the assignment notification reaches the team, with the responsavel in 2nd person"
    - "createProcessoFase passes the processo's clienteId so the fase-entrada notification reaches the team"
    - "uploadDocumento's processo branch resolves the full cliente team (via resolverEquipaCliente) plus the responsavel, not just the responsavel"
    - "The daily-job-team-expansion-out-of-scope and parecer-stays-individual decisions are recorded in PROJECT.md Key Decisions"
  artifacts:
    - path: "backend/src/main/java/com/lexcv/controllers/ResourceController.java"
      provides: "4 call sites wired for team fan-out (clienteId passed / team resolved)"
      contains: "resolverEquipaCliente"
    - path: ".planning/PROJECT.md"
      provides: "recorded NOTF-25 scope decisions (criteria 4 and 5)"
      contains: "AlertasDiariosJob"
  key_links:
    - from: "ResourceController.uploadDocumento (processo branch)"
      to: "NotificacaoService.resolverEquipaCliente"
      via: "shared team-resolution helper (no second inline implementation)"
      pattern: "resolverEquipaCliente"
    - from: "ResourceController triggers"
      to: "notificarFaseEntrada / notificarProcessoAtribuido"
      via: "clienteId argument"
      pattern: "getClienteId\\(\\)"
---

<objective>
Wire the four ResourceController call sites to the team-expanded NotificacaoService API from plan
95-01, and record the two explicit NOTF-25 scope decisions.

This plan delivers the controller-integration half of NOTF-25: the three process-event triggers
(fase entrada, processo atribuído x2, novo documento processo-branch) now feed the full client team
into the notification path. It also closes NOTF-25 success criteria 4 and 5 by recording — in the
durable decision log — that the daily job stays responsavelId-only and that PARECER_ATRIBUIDO stays
individual (both already locked in CONTEXT.md and REQUIREMENTS.md Out of Scope).

Purpose: NOTF-25 success criteria 1, 2 (delivery side), 4, 5.
Output: modified `ResourceController.java` + a new row in PROJECT.md Key Decisions.
</objective>

<execution_context>
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/LEXCV-95-notf-25-notificar-toda-a-equipa-do-processo/95-CONTEXT.md
@.planning/phases/LEXCV-95-notf-25-notificar-toda-a-equipa-do-processo/95-01-SUMMARY.md

<interfaces>
<!-- Post-95-01 NotificacaoService API the call sites must match. -->
notificarFaseEntrada(UUID tenantId, UUID processoId, UUID clienteId, UUID responsavelId, String numeroProcesso, String nomeFase, String linkUrl)
notificarProcessoAtribuido(UUID tenantId, UUID processoId, UUID clienteId, UUID responsavelId, String numeroProcesso, String linkUrl)
public Set<UUID> resolverEquipaCliente(UUID tenantId, UUID clienteId)
notificarDocumentoNovo(UUID tenantId, String documentoId, Collection<UUID> destinatarios, String nomeDocumento, String linkUrl, UUID atorId)  // signature UNCHANGED

<!-- Current ResourceController call sites (line numbers approximate — locate by method). -->
createProcesso (~1085): notificarProcessoAtribuido(tenantId, saved.getId(), saved.getResponsavelId(), saved.getNumeroProcesso(), "/processos/" + saved.getId())
atribuirResponsavel (~1150): notificarProcessoAtribuido(tenantId, saved.getId(), saved.getResponsavelId(), saved.getNumeroProcesso(), "/processos/" + saved.getId())
createProcessoFase (~1847): notificarFaseEntrada(processo.getTenantId(), id, processo.getResponsavelId(), processo.getNumeroProcesso(), faseNome, "/processos/" + id + "?tab=fases")
uploadDocumento processo branch (~2763-2773):
    Processo proc = processoRepository.findById(saved.getProcessoId()).orElse(null);
    UUID resp = proc != null ? proc.getResponsavelId() : null;
    List<UUID> dests = resp != null ? List.of(resp) : List.of();
  (the cliente branch at ~2774-2787 already resolves the full team and MUST stay as-is)

<!-- Processo exposes getClienteId() (non-null column) and getResponsavelId() (nullable). -->
<!-- ResourceController already injects clienteAdvogadoRepository + clienteAdministrativoRepository (fields ~75-76) and notificacaoService. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire the four ResourceController call sites for team fan-out</name>
  <files>backend/src/main/java/com/lexcv/controllers/ResourceController.java</files>
  <read_first>
    - backend/src/main/java/com/lexcv/controllers/ResourceController.java (the four methods: createProcesso ~1061, atribuirResponsavel ~1091, createProcessoFase ~1823, and uploadDocumento's notification block ~2758-2788 — read the existing cliente branch there as the pattern to mirror)
    - backend/src/main/java/com/lexcv/models/Processo.java (getClienteId / getResponsavelId)
    - .planning/phases/LEXCV-95-notf-25-notificar-toda-a-equipa-do-processo/95-01-SUMMARY.md (confirm the exact new signatures shipped)
  </read_first>
  <action>
    In `createProcesso` (~1085) and `atribuirResponsavel` (~1150): insert `saved.getClienteId()` as
    the 3rd argument to `notificarProcessoAtribuido(...)`, keeping every other argument identical.

    In `createProcessoFase` (~1847): insert `processo.getClienteId()` as the 3rd argument to
    `notificarFaseEntrada(...)`. The enclosing try/catch(IllegalArgumentException) around the call
    stays as-is.

    In `uploadDocumento`'s processo branch (~2763-2766): replace the responsavel-only recipient
    build with a team resolution that mirrors the cliente branch. Keep the `proc` null-guard, then
    build `List<UUID> dests = new ArrayList<>(notificacaoService.resolverEquipaCliente(tenantId, proc.getClienteId()));`
    and, if `proc.getResponsavelId() != null`, `dests.add(proc.getResponsavelId());`. Pass `dests`
    into the unchanged `notificarDocumentoNovo(...)` call. Do NOT modify the cliente branch
    (~2774-2787) — CONTEXT locks it as-is. Reuse `resolverEquipaCliente` rather than inlining a
    second junction lookup, so there is exactly one team-resolution implementation (Pitfall 3).

    Do not touch AlertasDiariosJob or the ParecerController PARECER_ATRIBUIDO trigger.
  </action>
  <verify>
    <automated>mvn -f backend/pom.xml -DskipTests package && mvn -f backend/pom.xml test</automated>
  </verify>
  <acceptance_criteria>
    - Source: `notificarProcessoAtribuido(tenantId, saved.getId(), saved.getClienteId(), ...)` appears at both createProcesso and atribuirResponsavel call sites (grep for `getClienteId()` inside those calls).
    - Source: `notificarFaseEntrada(processo.getTenantId(), id, processo.getClienteId(), ...)` at createProcessoFase.
    - Source: the uploadDocumento processo branch references `notificacaoService.resolverEquipaCliente(` and the cliente branch is byte-for-byte unchanged.
    - Behavior: `mvn -f backend/pom.xml -DskipTests package` compiles and `mvn -f backend/pom.xml test` passes the full suite (controller-level integration is not directly testable — the MINIO_ENDPOINT blocker prevents full-context controller tests, owned by Phase 97 AUD-04; the fan-out logic is proven at the service layer in 95-01).
  </acceptance_criteria>
  <done>All three process-event triggers pass the processo's clienteId (or resolved team) so notifications reach the whole client team; the backend compiles and the whole test suite is green.</done>
</task>

<task type="auto">
  <name>Task 2: Record the NOTF-25 scope decisions in PROJECT.md</name>
  <files>.planning/PROJECT.md</files>
  <read_first>
    - .planning/PROJECT.md (the `## Key Decisions` markdown table at ~line 121 — append rows in the existing `| Decision | Rationale | Outcome |` format)
    - .planning/phases/LEXCV-95-notf-25-notificar-toda-a-equipa-do-processo/95-CONTEXT.md (the locked "Job diário — fora de âmbito" and "PARECER_ATRIBUIDO — mantém-se individual" decisions)
    - .planning/REQUIREMENTS.md (Out of Scope table already lists the daily-job extension as deferred pending this phase's explicit decision)
  </read_first>
  <action>
    Append two rows to the `## Key Decisions` table in .planning/PROJECT.md (satisfying NOTF-25
    success criteria 4 and 5, which require these decisions to be explicitly recorded, not just
    implemented):

    Row 1 (criterion 4): Decision = "NOTF-25: expansão de fan-out de equipa cobre apenas os 3
    gatilhos de evento (FASE_ENTRADA, DOCUMENTO_NOVO, PROCESSO_ATRIBUIDO); AlertasDiariosJob
    (PRAZO_*, EVENTO_*, HONORARIO_ATRASADO) mantém-se responsavelId-only". Rationale = deliberately
    out of scope for v2.11 per CONTEXT.md and REQUIREMENTS.md Out of Scope; registered as a future
    scope candidate, not a confirmed requirement. Outcome column = a short marker such as
    "Deferred (job team-expansion)".

    Row 2 (criterion 5): Decision = "NOTF-25: PARECER_ATRIBUIDO mantém-se individual (só o advogado
    atribuído + fan-out ADMIN), NÃO alarga à equipa do cliente". Rationale = atribuição de parecer é
    semanticamente a decisão de um advogado específico assumir o trabalho; alargar diluiria o sinal.
    Outcome column = "By design".

    Keep the table well-formed (three pipe-delimited columns per row).
  </action>
  <verify>
    <automated>grep -v '^#' .planning/PROJECT.md | grep -c 'AlertasDiariosJob' | grep -qv '^0$' && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Source: PROJECT.md Key Decisions table contains a row mentioning `AlertasDiariosJob` and the responsavelId-only scope for the daily job.
    - Source: PROJECT.md Key Decisions table contains a row stating PARECER_ATRIBUIDO stays individual.
    - Both rows are valid 3-column markdown table rows within the existing `## Key Decisions` section.
  </acceptance_criteria>
  <done>The two NOTF-25 scope decisions (daily job out of scope; parecer stays individual) are durably recorded in the canonical decision log, closing success criteria 4 and 5.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → ResourceController endpoints | Existing @PreAuthorize-gated endpoints (processos:manage / processos:edit / documentos:*); this plan adds no new endpoint and no new user-supplied input — clienteId flows from server-side Processo entities |
| ResourceController → NotificacaoService.resolverEquipaCliente | Crosses into tenant-scoped junction data; the helper's dual-scoped query is the isolation boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-95-04 | Information Disclosure | uploadDocumento processo branch | mitigate | `resolverEquipaCliente(tenantId, proc.getClienteId())` is dual-scoped by tenant; even a cross-tenant proc (not reachable here — documento is tenant-scoped) would yield an empty team, never another tenant's users (Pitfall 10). The pre-verified processo call sites (createProcessoFase/atribuirResponsavel) already load a tenant-checked Processo before passing clienteId |
| T-95-05 | Spoofing | team members learn of process events | accept | Team members are already authorized on the cliente via existing RBAC/junction membership; notifying them of their own processes is the intended behavior, not a disclosure |
| T-95-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase (Java source edit + planning-doc edit only); Package Legitimacy Gate not applicable |
</threat_model>

<verification>
- `mvn -f backend/pom.xml -DskipTests package` compiles with the wired call sites.
- `mvn -f backend/pom.xml test` full suite is green.
- Grep confirms the three triggers pass `getClienteId()` and the documento processo branch uses `resolverEquipaCliente`.
- Grep confirms PROJECT.md records the two scope decisions.
</verification>

<success_criteria>
- All three process-event triggers deliver to the whole client team (criteria 1, 2 delivery side).
- The daily job and PARECER_ATRIBUIDO scope decisions are recorded in PROJECT.md (criteria 4, 5).
- No regression: full backend test suite passes; AlertasDiariosJob and PARECER_ATRIBUIDO untouched.
</success_criteria>

<output>
Create `.planning/phases/LEXCV-95-notf-25-notificar-toda-a-equipa-do-processo/95-02-SUMMARY.md` when done
</output>
