---
phase: 93-notf-24-prefer-ncias-de-notifica-o-por-utilizador
plan: 04
type: execute
wave: 4
depends_on: [93-03]
files_modified:
  - web/src/types/notificacoes.ts
  - web/src/hooks/use-notificacao-preferencias.ts
  - web/src/app/(dashboard)/settings/page.tsx
autonomous: false
requirements: [NOTF-24]

must_haves:
  truths:
    - "A página /settings ganha uma secção/tab de preferências de notificação com um toggle por categoria silenciável (8 categorias — PRAZO_VENCIDO omitido)"
    - "Desligar um toggle chama PUT (silenciar); ligar chama DELETE (reativar); o estado inicial vem de GET /notificacoes/preferencias"
    - "A lista de toggles deriva de NOTIFICACAO_CATEGORIA_OPTIONS (sem duplicar a enumeração), com PRAZO_VENCIDO filtrado"
    - "O silenciamento persiste entre recarregamentos e é isolado por utilizador"
  artifacts:
    - path: "web/src/hooks/use-notificacao-preferencias.ts"
      provides: "Hooks TanStack Query: ler preferências + silenciar + reativar"
      contains: "notificacao-preferencias"
    - path: "web/src/app/(dashboard)/settings/page.tsx"
      provides: "Nova tab de preferências de notificação reutilizando NOTIFICACAO_CATEGORIA_OPTIONS"
      contains: "NOTIFICACAO_CATEGORIA_OPTIONS"
  key_links:
    - from: "settings/page.tsx (tab notificações)"
      to: "use-notificacao-preferencias.ts"
      via: "leitura do estado + mutações silenciar/reativar"
      pattern: "useNotificacaoPreferencias|useSilenciarCategoria|useReativarCategoria"
    - from: "tab de preferências"
      to: "NOTIFICACAO_CATEGORIA_OPTIONS (web/src/lib/notificacao-categoria.ts)"
      via: "filter(o => o.value !== 'PRAZO_VENCIDO')"
      pattern: "PRAZO_VENCIDO"
---

<objective>
Adicionar, DENTRO da página `/settings` já existente (sem criar rota nova — decisão travada), uma secção de preferências de notificação: um toggle por categoria silenciável, reutilizando `NOTIFICACAO_CATEGORIA_OPTIONS` (fonte de verdade única de labels PT) com `PRAZO_VENCIDO` omitido. Mais o hook TanStack Query que liga aos endpoints do Plan 93-03.

Purpose: fechar o loop self-service da NOTF-24 na UI (Critérios de Sucesso 1, 2 e 4) — o utilizador silencia/reativa categorias para si próprio, e `PRAZO_VENCIDO` simplesmente não aparece como opção.
Output: hook novo, tipo novo, e a tab de preferências na settings page; validado por checkpoint humano.
</objective>

<execution_context>
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/LEXCV-93-notf-24-prefer-ncias-de-notifica-o-por-utilizador/93-CONTEXT.md
@.planning/phases/LEXCV-93-notf-24-prefer-ncias-de-notifica-o-por-utilizador/93-03-SUMMARY.md

<interfaces>
<!-- Endpoints do Plan 93-03 (via apiFetch; base /api/v1 já aplicada pelo wrapper) -->
<!-- GET    /notificacoes/preferencias            -> { "silenciadas": NotificacaoCategoria[] } -->
<!-- PUT    /notificacoes/preferencias/{categoria} -> 200 { categoria, silenciada:true } | 400 { message } (PRAZO_VENCIDO/desconhecida) -->
<!-- DELETE /notificacoes/preferencias/{categoria} -> 200 { categoria, silenciada:false } -->

<!-- Padrão de hook a copiar (web/src/hooks/use-notificacoes.ts) -->
```ts
export function useMarcarNotificacaoLida() {
  const queryClient = useQueryClient();
  return useMutation({
    mutationFn: (id: string) => apiFetch<Notificacao>(`/notificacoes/${encodeURIComponent(id)}/lida`, { method: "PATCH" }),
    onSuccess: async () => { await queryClient.invalidateQueries({ queryKey: ["notificacoes"] }); },
  });
}
```

<!-- Fonte de labels/categorias (web/src/lib/notificacao-categoria.ts) -->
<!-- export const NOTIFICACAO_CATEGORIA_OPTIONS: { value: NotificacaoCategoria; label: string }[] -->
<!-- (deriva de CATEGORIA_LABEL_MAP, exaustivo). PRAZO_VENCIDO label = "Prazo vencido". -->

<!-- settings/page.tsx: TabId = "profile"|"security"|"users"|"rbac"; tabs são <button onClick={()=>setActiveTab(...)}>; -->
<!-- painéis: {activeTab === "..." && (<div>...</div>)}. usePermissions() dá { data: me, can }. -->
<!-- Gate self-service consistente com a página /notificacoes: can.view("notificacoes"). -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Tipo + hook use-notificacao-preferencias</name>
  <files>web/src/types/notificacoes.ts, web/src/hooks/use-notificacao-preferencias.ts</files>
  <read_first>
    - web/src/hooks/use-notificacoes.ts (ficheiro inteiro — padrão useQuery/useMutation, apiFetch, invalidateQueries)
    - web/src/types/notificacoes.ts (ficheiro inteiro — onde vive NotificacaoCategoria e os tipos de resposta)
    - web/src/lib/api.ts (assinatura de apiFetch — só para confirmar; não alterar)
  </read_first>
  <action>
    (NOTF-24) Criar a camada de dados frontend.

    1. Em `web/src/types/notificacoes.ts`, adicionar `export interface NotificacaoPreferenciasResponse { silenciadas: NotificacaoCategoria[]; }` (o shape de `GET /notificacoes/preferencias`). Reutilizar o `NotificacaoCategoria` já existente no ficheiro.

    2. Criar `web/src/hooks/use-notificacao-preferencias.ts` copiando o estilo de `use-notificacoes.ts` (`"use client"` não é necessário em hooks; seguir o ficheiro existente que não o tem). Três exports:
    - `useNotificacaoPreferencias()`: `useQuery` com `queryKey: ["notificacao-preferencias"]`, `queryFn: () => apiFetch<NotificacaoPreferenciasResponse>("/notificacoes/preferencias")`, `enabled: typeof window !== "undefined"`, `staleTime: 30_000`. (Prefixo de key DISTINTO de `["notificacoes"]` — silenciar afeta entregas futuras, não invalida a lista/contador atuais.)
    - `useSilenciarCategoria()`: `useMutation` com `mutationFn: (categoria: NotificacaoCategoria) => apiFetch(\`/notificacoes/preferencias/${encodeURIComponent(categoria)}\`, { method: "PUT" })`, e `onSuccess` que faz `queryClient.invalidateQueries({ queryKey: ["notificacao-preferencias"] })`.
    - `useReativarCategoria()`: igual mas `method: "DELETE"`.
    - Imports: `useMutation, useQuery, useQueryClient` de `@tanstack/react-query`; `apiFetch` de `@/lib/api`; `NotificacaoCategoria, NotificacaoPreferenciasResponse` de `@/types/notificacoes`.

    NÃO tocar em `use-notificacoes.ts` nem no mapa `notificacao-categoria.ts` (só serão lidos). NÃO duplicar a enumeração de categorias no hook.
  </action>
  <verify>
    <automated>cd web && node -e "const fs=require('fs'); const t=fs.readFileSync('src/types/notificacoes.ts','utf8'); const h=fs.readFileSync('src/hooks/use-notificacao-preferencias.ts','utf8'); const ok = /NotificacaoPreferenciasResponse/.test(t) && /silenciadas:\s*NotificacaoCategoria\[\]/.test(t) && /queryKey:\s*\[\"notificacao-preferencias\"\]/.test(h) && /\/notificacoes\/preferencias/.test(h) && /method:\s*\"PUT\"/.test(h) && /method:\s*\"DELETE\"/.test(h) && /useNotificacaoPreferencias/.test(h) && /useSilenciarCategoria/.test(h) && /useReativarCategoria/.test(h); process.exit(ok?0:1)"</automated>
    <automated>cd web && pnpm exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `NotificacaoPreferenciasResponse` (com `silenciadas: NotificacaoCategoria[]`) existe em types/notificacoes.ts
    - `use-notificacao-preferencias.ts` exporta os 3 hooks; a query usa key `["notificacao-preferencias"]`; PUT/DELETE apontam para `/notificacoes/preferencias/{categoria}`
    - `cd web && pnpm exec tsc --noEmit` sem erros de tipo
  </acceptance_criteria>
  <done>Camada de dados frontend pronta (tipo + 3 hooks) ligada aos endpoints do Plan 93-03.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Tab de preferências de notificação na página /settings</name>
  <files>web/src/app/(dashboard)/settings/page.tsx</files>
  <read_first>
    - web/src/app/(dashboard)/settings/page.tsx (ficheiro inteiro — TabId union, os botões de tab, os painéis condicionais, usePermissions, o padrão de toggle inline nas linhas ~546-551)
    - web/src/hooks/use-notificacao-preferencias.ts (os 3 hooks criados na Task 1)
    - web/src/lib/notificacao-categoria.ts (NOTIFICACAO_CATEGORIA_OPTIONS — a lista/labels a reutilizar)
    - web/src/app/(dashboard)/notificacoes/page.tsx linhas 33-47 (padrão can.view("notificacoes") + AccessDeniedState — para consistência)
  </read_first>
  <action>
    (NOTF-24, decisões travadas: UI dentro de /settings, reutilizar NOTIFICACAO_CATEGORIA_OPTIONS, omitir PRAZO_VENCIDO) Editar `settings/page.tsx`:

    1. Estender `type TabId` de `"profile" | "security" | "users" | "rbac"` para incluir `"notificacoes"`.

    2. Adicionar um botão de tab (a seguir aos existentes, no mesmo estilo `<button onClick={() => setActiveTab("notificacoes")} className={...}>`), com um ícone lucide adequado (ex.: `Bell` — adicionar ao import de `lucide-react`) e label "Notificações". Mostrar a tab a qualquer utilizador com `can.view("notificacoes")` (self-service; o scope está semeado aos 4 perfis) — gate com o `can` já obtido de `usePermissions()` no topo do componente.

    3. Adicionar o painel condicional `{activeTab === "notificacoes" && can.view("notificacoes") && (<div className="animate-in fade-in duration-200"><NotificationPreferencesTab /></div>)}`.

    4. Criar a sub-componente `NotificationPreferencesTab` (no mesmo ficheiro, padrão das outras sub-componentes tipo `RbacTab`):
    - `const { data, isLoading } = useNotificacaoPreferencias();` `const silenciar = useSilenciarCategoria();` `const reativar = useReativarCategoria();`
    - `const silenciadas = data?.silenciadas ?? [];`
    - Renderizar dentro de um `<Card>` (mesmo estilo visual das outras tabs) a lista `NOTIFICACAO_CATEGORIA_OPTIONS.filter((o) => o.value !== "PRAZO_VENCIDO")` — 8 linhas. Cada linha: o `label` PT + um toggle. O toggle representa ENTREGA (ligado = entregar; desligado = silenciado): `checked = !silenciadas.includes(o.value)`.
    - `onChange`: se estava entregue (checked→passa a silenciado) chamar `silenciar.mutate(o.value)`; se estava silenciado (unchecked→passa a entregue) chamar `reativar.mutate(o.value)`. Usar `toast.success(...)` no sucesso e `toast.error(...)` no erro (importar `toast` de `@/hooks/use-toast`, já usado no ficheiro). Desativar o toggle enquanto `silenciar.isPending || reativar.isPending` para essa linha, evitando cliques em corrida.
    - Estado de loading: `if (isLoading) { return <Loader2 .../> }` (Loader2 já importado).
    - Incluir um texto curto de ajuda: silenciar uma categoria só afeta o próprio utilizador; a categoria crítica "Prazo vencido" é sempre entregue e por isso não aparece na lista.

    NÃO criar rota/página nova. NÃO duplicar a enumeração de categorias (derivar sempre de NOTIFICACAO_CATEGORIA_OPTIONS). NÃO incluir PRAZO_VENCIDO na lista renderizada. NÃO alterar as tabs/sub-componentes existentes além de acrescentar a nova.
  </action>
  <verify>
    <automated>cd web && node -e "const fs=require('fs'); const s=fs.readFileSync('src/app/(dashboard)/settings/page.tsx','utf8'); const ok = /\"profile\"\s*\|\s*\"security\"\s*\|\s*\"users\"\s*\|\s*\"rbac\"\s*\|\s*\"notificacoes\"/.test(s) && /NotificationPreferencesTab/.test(s) && /useNotificacaoPreferencias/.test(s) && /useSilenciarCategoria/.test(s) && /useReativarCategoria/.test(s) && /NOTIFICACAO_CATEGORIA_OPTIONS/.test(s) && /o\.value\s*!==\s*\"PRAZO_VENCIDO\"/.test(s) && /can\.view\(\"notificacoes\"\)/.test(s); process.exit(ok?0:1)"</automated>
    <automated>cd web && pnpm exec tsc --noEmit</automated>
    <automated>cd web && pnpm lint</automated>
  </verify>
  <acceptance_criteria>
    - `TabId` inclui `"notificacoes"`; existe um botão de tab e um painel gated por `can.view("notificacoes")`
    - `NotificationPreferencesTab` renderiza `NOTIFICACAO_CATEGORIA_OPTIONS.filter(o => o.value !== "PRAZO_VENCIDO")` (8 toggles), com checked = não-silenciado
    - Desligar chama `useSilenciarCategoria`, ligar chama `useReativarCategoria`; PRAZO_VENCIDO nunca renderizado
    - `cd web && pnpm exec tsc --noEmit` e `cd web && pnpm lint` passam
  </acceptance_criteria>
  <done>Secção de preferências operacional em /settings, reutilizando a lista de categorias e omitindo PRAZO_VENCIDO.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verificação humana end-to-end das preferências</name>
  <files>web/src/app/(dashboard)/settings/page.tsx</files>
  <read_first>
    - web/src/app/(dashboard)/settings/page.tsx (a tab "Notificações" construída na Task 2)
  </read_first>
  <action>
    Checkpoint humano (sem escrita de código): as Tasks 1 e 2 já entregaram o hook e a tab; esta tarefa pausa para verificação em browser real antes de fechar a fase — a verificação runtime que os testes automáticos não cobrem (um teste full-context esbarraria no bloqueador MINIO_ENDPOINT, Pitfall 5). Executar os passos de `<how-to-verify>` e aguardar o sinal de retoma. Não implementar nada nesta tarefa.
  </action>
  <what-built>
    Uma tab "Notificações" na página `/settings` com 8 toggles (um por categoria silenciável), ligada aos endpoints `GET/PUT/DELETE /notificacoes/preferencias`. Desligar um toggle silencia a categoria só para o utilizador atual; `PRAZO_VENCIDO` ("Prazo vencido") não aparece na lista e é sempre entregue.
  </what-built>
  <how-to-verify>
    Pré-requisito: backend a correr com a migração `93-create-notificacao-preferencia-table.sql` aplicada (ou dev com `ddl-auto=update`, que cria a tabela automaticamente) e DB acessível.
    1. Iniciar sessão (ex.: admin@lexcv.cv / Pa$$w0rd) e ir a `/settings` → tab "Notificações".
    2. Confirmar que aparecem 8 categorias e que "Prazo vencido" NÃO está na lista.
    3. Desligar o toggle de "Nova fase" (FASE_ENTRADA). Confirmar toast de sucesso.
    4. Recarregar a página (F5) e voltar à tab "Notificações": o toggle de "Nova fase" deve continuar desligado (persistência).
    5. Voltar a ligar "Nova fase"; confirmar toast e que reativa (fica ligado após reload).
    6. (Opcional, se houver 2º utilizador no tenant) Confirmar noutra sessão que o mute do 1º utilizador NÃO afeta os toggles do 2º (isolamento por utilizador).
    7. (Opcional, API direta) `PUT /api/v1/notificacoes/preferencias/PRAZO_VENCIDO` deve responder 400 (categoria não-silenciável).
  </how-to-verify>
  <verify>
    <human-check>"Prazo vencido" ausente da lista (8 toggles visíveis); silenciar "Nova fase" persiste após F5; reativar volta ao estado entregue; (se testado) mute isolado por utilizador e PUT de PRAZO_VENCIDO devolve 400.</human-check>
  </verify>
  <acceptance_criteria>
    - "Prazo vencido" ausente da lista; 8 toggles visíveis
    - Silenciar persiste entre recarregamentos; reativar volta ao estado entregue
    - (se testado) mute é isolado por utilizador; PUT de PRAZO_VENCIDO devolve 400
  </acceptance_criteria>
  <resume-signal>Escreva "aprovado" ou descreva os problemas encontrados</resume-signal>
  <done>Verificação humana confirmada: preferências persistem e são isoladas por utilizador; PRAZO_VENCIDO nunca silenciável na UI.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → API (cookie auth) | apiFetch envia credentials:include; a UI só mostra/altera as preferências do próprio utilizador autenticado |
| RBAC UI gate | A tab só aparece com `can.view("notificacoes")` — espelha o gate backend @PreAuthorize |

## STRIDE Threat Register (ASVS L1 — security_block_on: high)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-93-10 | Spoofing / Elevation | tab de preferências | mitigate | O frontend nunca envia userId — o backend (Plan 93-03) deriva-o do JWT; o gate `can.view("notificacoes")` é defesa em profundidade sobre o `@PreAuthorize` do servidor (a UI não é a fronteira de autorização, o backend é). |
| T-93-11 | Tampering | tentar silenciar PRAZO_VENCIDO pela UI | mitigate | PRAZO_VENCIDO é filtrado da lista renderizada; mesmo que forçado via API, o backend responde 400 (Plan 93-03). |
| T-93-12 | Race condition | duplo clique num toggle | mitigate | Toggle desativado enquanto a mutação está pending; a operação backend é idempotente (silenciar não cria 2ª linha, reativar não erra se ausente). |
| T-93-SC | Tampering | pnpm installs | accept | Zero dependências novas (@tanstack/react-query, lucide-react, apiFetch já presentes). Sem instalações; gate de legitimidade N/A. |
</threat_model>

<verification>
- `cd web && pnpm exec tsc --noEmit` e `cd web && pnpm lint` passam
- Asserção de fonte: TabId estendido, sub-componente + 3 hooks, filtro PRAZO_VENCIDO, gate can.view
- Checkpoint humano confirma persistência, reativação, ausência de PRAZO_VENCIDO e (opcional) isolamento por utilizador + 400 na API — a verificação runtime que os testes automáticos não cobrem (MINIO_ENDPOINT bloqueia full-context; aqui é browser real)
</verification>

<success_criteria>
1. O utilizador silencia/reativa categorias para si próprio via /settings, com persistência e isolamento por utilizador (NOTF-24 SC-1, SC-4)
2. PRAZO_VENCIDO nunca aparece como opção de silenciamento na UI (NOTF-24 SC-2)
3. A UI reutiliza NOTIFICACAO_CATEGORIA_OPTIONS sem duplicar a enumeração (decisão travada)
</success_criteria>

<output>
Create `.planning/phases/LEXCV-93-notf-24-prefer-ncias-de-notifica-o-por-utilizador/93-04-SUMMARY.md` when done
</output>
