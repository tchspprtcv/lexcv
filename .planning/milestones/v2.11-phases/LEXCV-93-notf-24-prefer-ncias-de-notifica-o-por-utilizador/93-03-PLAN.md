---
phase: 93-notf-24-prefer-ncias-de-notifica-o-por-utilizador
plan: 03
type: execute
wave: 3
depends_on: [93-01, 93-02]
files_modified:
  - backend/src/main/java/com/lexcv/controllers/NotificacaoController.java
autonomous: true
requirements: [NOTF-24]

must_haves:
  truths:
    - "GET /api/v1/notificacoes/preferencias devolve as categorias silenciadas do chamador (tenant+user do JWT), gated notificacoes:view"
    - "PUT /api/v1/notificacoes/preferencias/{categoria} silencia uma categoria; PRAZO_VENCIDO e categorias desconhecidas são rejeitadas com 400"
    - "DELETE /api/v1/notificacoes/preferencias/{categoria} reativa (remove o silenciamento) uma categoria"
    - "Nenhum endpoint aceita userId do body — tenant e user vêm sempre de getTenantId()/getUserId()"
  artifacts:
    - path: "backend/src/main/java/com/lexcv/controllers/NotificacaoController.java"
      provides: "3 endpoints de preferência dual-scoped delegando a NotificacaoService"
      contains: "/preferencias"
  key_links:
    - from: "NotificacaoController preferências (PUT/DELETE/GET)"
      to: "NotificacaoService.silenciarCategoria / reativarCategoria / listarCategoriasSilenciadas"
      via: "delegação total ao serviço (NotificacaoService = único ponto de escrita)"
      pattern: "notificacaoService\\.(silenciarCategoria|reativarCategoria|listarCategoriasSilenciadas)"
---

<objective>
Expor os 3 endpoints REST de preferências de notificação em `NotificacaoController` — o controlador já dual-scoped por tenant+user — delegando por completo aos métodos de serviço do Plan 93-02 (mantendo `NotificacaoService` como único ponto de escrita do subsistema, conforme o próprio doc-comment do controlador). A rejeição de `PRAZO_VENCIDO`/categorias desconhecidas vive no serviço e traduz-se aqui em `400 Bad Request`.

Purpose: dar ao frontend (Plan 93-04) uma API self-service para ler e alternar mutes, sem nunca aceitar userId do pedido (sempre do JWT) — Critérios de Sucesso 1, 2 e 4.
Output: `NotificacaoController.java` com GET/PUT/DELETE `/preferencias`.
</objective>

<execution_context>
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/LEXCV-93-notf-24-prefer-ncias-de-notifica-o-por-utilizador/93-CONTEXT.md
@.planning/research/ARCHITECTURE.md
@.planning/phases/LEXCV-93-notf-24-prefer-ncias-de-notifica-o-por-utilizador/93-02-SUMMARY.md

<interfaces>
<!-- Métodos de serviço criados no Plan 93-02 (delegar diretamente) -->
```java
List<String> listarCategoriasSilenciadas(UUID tenantId, UUID userId);
void silenciarCategoria(UUID tenantId, UUID userId, String categoria);   // lança IllegalArgumentException p/ desconhecida ou PRAZO_VENCIDO
void reativarCategoria(UUID tenantId, UUID userId, String categoria);
```

<!-- Padrões EXATOS já no NotificacaoController.java a copiar -->
<!-- private UUID getTenantId() { ...UserPrincipal.getTenantId() } -->
<!-- private UUID getUserId()  { ...UserPrincipal.getUserId() } -->
<!-- @PreAuthorize("hasAuthority('notificacoes:view')") em cada método -->
<!-- marcarLida (linha 93-101): PatchMapping("/{id}/lida"), try->200/404, corpo via Map.of(...) -->
<!-- marcarTodasLidas (linha 103-108): PostMapping("/ler-todas"), Map.of("marcadas", n) -->
<!-- listar (linha 64-84): GetMapping, ResponseEntity.ok(Map.of(...)), badRequest via Map.of("message", ...) -->

<!-- RBAC: reutilizar 'notificacoes:view' (self-service; já semeado aos 4 perfis). NÃO criar novo scope. -->
<!-- NUNCA aceitar userId do body — usar sempre getUserId() do JWT (padrão de todo o ficheiro). -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Endpoints GET/PUT/DELETE /preferencias</name>
  <files>backend/src/main/java/com/lexcv/controllers/NotificacaoController.java</files>
  <read_first>
    - backend/src/main/java/com/lexcv/controllers/NotificacaoController.java (ficheiro inteiro — getTenantId/getUserId, @PreAuthorize, os 4 endpoints existentes como padrão de estilo/erro)
    - backend/src/main/java/com/lexcv/services/NotificacaoService.java (métodos silenciarCategoria/reativarCategoria/listarCategoriasSilenciadas criados no Plan 93-02)
    - .planning/phases/LEXCV-93-notf-24-prefer-ncias-de-notifica-o-por-utilizador/93-CONTEXT.md (discrição: seguir o padrão REST já usado no resto do NotificacaoController)
  </read_first>
  <action>
    (NOTF-24) Adicionar 3 métodos a `NotificacaoController.java`, cada um `@PreAuthorize("hasAuthority('notificacoes:view')")`, escopados por `getTenantId()` + `getUserId()` (nunca userId do body). Imports novos: `org.springframework.web.bind.annotation.PutMapping`, `org.springframework.web.bind.annotation.DeleteMapping`, `java.util.List`. `NotificacaoService` já está injetado (`notificacaoService`).

    1. `GET /preferencias` → `@GetMapping("/preferencias") listarPreferencias()`: `List<String> silenciadas = notificacaoService.listarCategoriasSilenciadas(getTenantId(), getUserId());` e `return ResponseEntity.ok(Map.of("silenciadas", silenciadas));`. Chave `"silenciadas"` — o frontend deriva o estado de cada toggle (uma categoria está silenciada sse consta desta lista).

    2. `PUT /preferencias/{categoria}` → `@PutMapping("/preferencias/{categoria}") silenciar(@PathVariable String categoria)`: dentro de try, `notificacaoService.silenciarCategoria(getTenantId(), getUserId(), categoria); return ResponseEntity.ok(Map.of("categoria", categoria, "silenciada", true));`. `catch (IllegalArgumentException ex) { return ResponseEntity.badRequest().body(Map.of("message", ex.getMessage())); }`. É este catch que traduz a rejeição de PRAZO_VENCIDO/categoria desconhecida (lançada pelo serviço, Plan 93-02) num `400` — Critério de Sucesso 2 (tentativa direta via API é rejeitada).

    3. `DELETE /preferencias/{categoria}` → `@DeleteMapping("/preferencias/{categoria}") reativar(@PathVariable String categoria)`: `notificacaoService.reativarCategoria(getTenantId(), getUserId(), categoria); return ResponseEntity.ok(Map.of("categoria", categoria, "silenciada", false));`. Idempotente (o serviço não erra se a linha não existir) — Critério de Sucesso 4 (reativar volta a entregar).

    NÃO persistir preferências diretamente a partir do controlador (via repositório) — delegar sempre ao serviço, tal como `marcarLida`/`marcarTodasLidas` já fazem (doc-comment da classe: "keeps NotificacaoService the sole write path"). NÃO adicionar novo scope. NÃO alterar os 4 endpoints existentes.
  </action>
  <verify>
    <automated>cd backend && node -e "const fs=require('fs'); const strip=s=>s.split('\n').filter(l=>!l.trim().startsWith('//')&&!l.trim().startsWith('*')&&!l.trim().startsWith('/*')).join('\n'); const c=strip(fs.readFileSync('src/main/java/com/lexcv/controllers/NotificacaoController.java','utf8')); const gets=/@GetMapping\(\"\/preferencias\"\)/.test(c); const put=/@PutMapping\(\"\/preferencias\/\{categoria\}\"\)/.test(c); const del=/@DeleteMapping\(\"\/preferencias\/\{categoria\}\"\)/.test(c); const deleg=/notificacaoService\.listarCategoriasSilenciadas/.test(c) && /notificacaoService\.silenciarCategoria/.test(c) && /notificacaoService\.reativarCategoria/.test(c); const scoped=/getTenantId\(\)\s*,\s*getUserId\(\)/.test(c); const gated=(c.match(/hasAuthority\('notificacoes:view'\)/g)||[]).length>=7; const rejects=/badRequest\(\)\.body\(Map\.of\(\"message\"/.test(c); const noBodyUser=!/@RequestBody[\s\S]{0,120}userId/.test(c); process.exit((gets&&put&&del&&deleg&&scoped&&gated&&rejects&&noBodyUser)?0:1)"</automated>
    <automated>cd backend && mvn -q -DskipTests compile</automated>
  </verify>
  <acceptance_criteria>
    - Existem `@GetMapping("/preferencias")`, `@PutMapping("/preferencias/{categoria}")` e `@DeleteMapping("/preferencias/{categoria}")`, todos `@PreAuthorize("hasAuthority('notificacoes:view')")`
    - Cada endpoint escopa por `getTenantId(), getUserId()` e delega ao serviço; nenhum aceita userId do body
    - PUT devolve 400 (`badRequest`+`message`) quando o serviço lança IllegalArgumentException (PRAZO_VENCIDO / categoria desconhecida)
    - `cd backend && mvn -q -DskipTests compile` compila
  </acceptance_criteria>
  <done>API self-service de preferências disponível e segura, delegando ao serviço; PRAZO_VENCIDO rejeitado com 400.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → Next rewrite → Spring API | Pedidos autenticados a /notificacoes/preferencias; input não-confiável = {categoria} na rota |
| self-service | O user só pode gerir as SUAS preferências — userId vem do JWT, nunca do pedido |

## STRIDE Threat Register (ASVS L1 — security_block_on: high)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-93-07 | Elevation of Privilege / IDOR | endpoints de preferência | mitigate | userId e tenantId derivam SEMPRE de `getUserId()`/`getTenantId()` (UserPrincipal do JWT); nenhum endpoint aceita userId do body/query. Gated `notificacoes:view` (semeado aos 4 perfis). |
| T-93-08 | Tampering | PUT /preferencias/{categoria} com PRAZO_VENCIDO ou lixo | mitigate | O serviço valida via `CategoriaNotificacao` e lança IllegalArgumentException; o controlador traduz em 400. Uma tentativa direta via API de silenciar PRAZO_VENCIDO é rejeitada (Critério 2). |
| T-93-09 | Information Disclosure | GET /preferencias | mitigate | `listarCategoriasSilenciadas(getTenantId(), getUserId())` — dual-scoped; devolve só categorias (nomes de enum), sem PII nem dados de outro user/tenant. |
| T-93-SC | Tampering | npm/pip/cargo installs | accept | Zero dependências novas (Spring Web já presente). Sem instalações; gate de legitimidade N/A. |
</threat_model>

<verification>
- `cd backend && mvn -q -DskipTests compile` compila com os 3 endpoints
- Asserção de fonte: 3 mappings, delegação ao serviço, dual-scoping getTenantId+getUserId, gating notificacoes:view, 400 na rejeição, sem userId de body
- Verificação HTTP end-to-end (200/400, isolamento entre 2 users) fica para o Plan 93-04 (checkpoint humano) + AUD-01/Phase 97 — um teste full-context aqui esbarraria no MINIO_ENDPOINT (Pitfall 5)
</verification>

<success_criteria>
1. API expõe leitura e alternância de mutes por categoria, self-service e dual-scoped (NOTF-24 SC-1)
2. Silenciar PRAZO_VENCIDO via API é rejeitado com 400 (NOTF-24 SC-2)
3. DELETE reativa uma categoria previamente silenciada (NOTF-24 SC-4)
</success_criteria>

<output>
Create `.planning/phases/LEXCV-93-notf-24-prefer-ncias-de-notifica-o-por-utilizador/93-03-SUMMARY.md` when done
</output>
