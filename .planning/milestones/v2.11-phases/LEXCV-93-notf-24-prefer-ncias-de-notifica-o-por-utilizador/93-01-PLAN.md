---
phase: 93-notf-24-prefer-ncias-de-notifica-o-por-utilizador
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/main/java/com/lexcv/models/NotificacaoPreferencia.java
  - backend/src/main/java/com/lexcv/models/CategoriaNotificacao.java
  - backend/src/main/java/com/lexcv/repositories/NotificacaoPreferenciaRepository.java
  - backend/migrations/93-create-notificacao-preferencia-table.sql
autonomous: true
requirements: [NOTF-24]

must_haves:
  truths:
    - "Existe uma tabela t_notificacao_preferencia (join tenant_id + user_id + categoria) com constraint única, auto-criada em dev via ddl-auto e documentada como migração manual para produção (ddl-auto=validate)"
    - "NotificacaoPreferenciaRepository expõe existsByTenantIdAndUserIdAndCategoria, findByTenantIdAndUserId e deleteByTenantIdAndUserIdAndCategoria — todas escopadas por tenant_id + user_id"
    - "CategoriaNotificacao enumera as 9 categorias e marca PRAZO_VENCIDO como a única não-silenciável — fonte de verdade única de silenciabilidade no backend"
  artifacts:
    - path: "backend/src/main/java/com/lexcv/models/NotificacaoPreferencia.java"
      provides: "Entidade join-table tenant-scoped (presença de linha = categoria silenciada)"
      contains: "t_notificacao_preferencia"
    - path: "backend/src/main/java/com/lexcv/models/CategoriaNotificacao.java"
      provides: "Enum das 9 categorias + flag de silenciabilidade + fromString/isSilenciavelCategoria"
      contains: "PRAZO_VENCIDO"
    - path: "backend/src/main/java/com/lexcv/repositories/NotificacaoPreferenciaRepository.java"
      provides: "Contrato de leitura/escrita das preferências (dual-scoped tenant+user)"
      contains: "existsByTenantIdAndUserIdAndCategoria"
    - path: "backend/migrations/93-create-notificacao-preferencia-table.sql"
      provides: "Migração manual da tabela + índice único"
      contains: "CREATE TABLE t_notificacao_preferencia"
  key_links:
    - from: "NotificacaoService.criar (Plan 93-02)"
      to: "NotificacaoPreferenciaRepository.existsByTenantIdAndUserIdAndCategoria"
      via: "contrato de leitura do mute guard"
      pattern: "existsByTenantIdAndUserIdAndCategoria"
    - from: "NotificacaoController preferências (Plan 93-03) e NotificacaoService (Plan 93-02)"
      to: "CategoriaNotificacao.isSilenciavelCategoria"
      via: "validação de silenciabilidade partilhada (PRAZO_VENCIDO rejeitado)"
      pattern: "isSilenciavelCategoria"
---

<objective>
Criar a fundação de dados da NOTF-24: a entidade join-table `NotificacaoPreferencia` (tenant_id, user_id, categoria — presença de linha = categoria silenciada, ausência = entrega default-on), o seu repositório dual-scoped, o enum canónico `CategoriaNotificacao` (9 valores, com `PRAZO_VENCIDO` marcado como única não-silenciável) e a migração manual da tabela.

Purpose: dar aos planos 93-02 (guard em `criar()`) e 93-03 (endpoints) contratos estáveis e já compiláveis contra os quais implementar — interface-first, para os planos a jusante não terem de explorar o código. Espelha a convenção de join-table já usada por `ClienteAdvogado`/`ClienteAdministrativo`.
Output: 3 ficheiros Java novos + 1 script de migração manual.
</objective>

<execution_context>
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/LEXCV-93-notf-24-prefer-ncias-de-notifica-o-por-utilizador/93-CONTEXT.md
@.planning/research/ARCHITECTURE.md

<interfaces>
<!-- Convenção EXATA de join-table tenant-scoped a copiar (ClienteAdvogado.java) -->
<!-- @Entity @Table(name=..., uniqueConstraints=@UniqueConstraint(columnNames={"tenant_id","cliente_id","user_id"})) -->
<!-- @Getter @Setter @NoArgsConstructor @AllArgsConstructor @Builder -->
<!-- @Id @GeneratedValue(strategy=GenerationType.UUID) UUID id; -->
<!-- @Column(name="...", nullable=false) UUID clienteId/userId/tenantId; -->
<!-- @Column(name="created_at", updatable=false) LocalDateTime createdAt; @PrePersist onCreate(){createdAt=LocalDateTime.now();} -->

<!-- Convenção de repositório derivado (ClienteAdvogadoRepository.java) -->
```java
public interface ClienteAdvogadoRepository extends JpaRepository<ClienteAdvogado, UUID> {
    List<ClienteAdvogado> findByClienteIdAndTenantId(UUID clienteId, UUID tenantId);
    void deleteByClienteIdAndUserIdAndTenantId(UUID clienteId, UUID userId, UUID tenantId);
}
```

<!-- As 9 categorias canónicas (hoje literais String espalhadas; frontend já as tem em web/src/types/notificacoes.ts) -->
<!-- FASE_ENTRADA, DOCUMENTO_NOVO, PROCESSO_ATRIBUIDO, PARECER_ATRIBUIDO, PRAZO_PROXIMO, -->
<!-- PRAZO_VENCIDO, EVENTO_PROXIMO, EVENTO_VENCIDO, HONORARIO_ATRASADO -->

<!-- Convenção de migração manual (backend/migrations/86-create-notificacao-table.sql) -->
<!-- Cabeçalho explica: ddl-auto=update em dev auto-cria; ddl-auto=validate em prod NÃO cria -> correr manualmente. -->
<!-- Sem Flyway/Liquibase no repo. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Entidade NotificacaoPreferencia + enum CategoriaNotificacao</name>
  <files>backend/src/main/java/com/lexcv/models/NotificacaoPreferencia.java, backend/src/main/java/com/lexcv/models/CategoriaNotificacao.java</files>
  <read_first>
    - backend/src/main/java/com/lexcv/models/ClienteAdvogado.java (ficheiro inteiro — o padrão EXATO de join-table tenant-scoped a copiar: anotações, campos, @PrePersist)
    - backend/src/main/java/com/lexcv/models/Notificacao.java (linhas 13-16 — o padrão de @Table + @UniqueConstraint nomeada, e o nome de tabela t_notificacao)
    - backend/src/main/java/com/lexcv/models/DocumentoTipo.java (confirmar que enums vivem no package com.lexcv.models)
    - .planning/phases/LEXCV-93-notf-24-prefer-ncias-de-notifica-o-por-utilizador/93-CONTEXT.md (decisões travadas: entidade NotificacaoPreferencia, só PRAZO_VENCIDO não-silenciável)
  </read_first>
  <action>
    Criar dois ficheiros no package `com.lexcv.models`.

    (1) `NotificacaoPreferencia.java` — entidade JPA join-table, copiando a forma de `ClienteAdvogado.java`:
    - Anotações de classe: `@Entity`, `@Table(name = "t_notificacao_preferencia", uniqueConstraints = @UniqueConstraint(name = "uk_notificacao_preferencia", columnNames = {"tenant_id", "user_id", "categoria"}))`, `@Getter`, `@Setter`, `@NoArgsConstructor`, `@AllArgsConstructor`, `@Builder`.
    - Campos: `@Id @GeneratedValue(strategy = GenerationType.UUID) private UUID id;`; `@Column(name = "tenant_id", nullable = false) private UUID tenantId;`; `@Column(name = "user_id", nullable = false) private UUID userId;`; `@Column(nullable = false) private String categoria;`; `@Column(name = "created_at", updatable = false) private LocalDateTime createdAt;`.
    - `@PrePersist protected void onCreate() { this.createdAt = LocalDateTime.now(); }`.
    - Imports: `jakarta.persistence.*`, `lombok.*`, `java.time.LocalDateTime`, `java.util.UUID`.
    - Semântica travada (93-CONTEXT.md): presença de uma linha = a categoria está silenciada para aquele user; ausência = entrega default-on. Não guardar flag booleana — a existência da linha É a preferência.

    (2) `CategoriaNotificacao.java` — enum canónico das 9 categorias, fonte de verdade única de silenciabilidade no backend (a pesquisa recomenda um enum SÓ para validar toggles de preferência; NÃO retrofitar os 9 call sites de String hardcoded existentes — mantê-los como estão).
    - Constantes com flag de silenciabilidade no construtor: `FASE_ENTRADA(true)`, `DOCUMENTO_NOVO(true)`, `PROCESSO_ATRIBUIDO(true)`, `PARECER_ATRIBUIDO(true)`, `PRAZO_PROXIMO(true)`, `PRAZO_VENCIDO(false)`, `EVENTO_PROXIMO(true)`, `EVENTO_VENCIDO(true)`, `HONORARIO_ATRASADO(true)`. Só `PRAZO_VENCIDO` é `false` (decisão travada: única categoria não-silenciável).
    - Campo `private final boolean silenciavel;`, construtor `CategoriaNotificacao(boolean silenciavel)`, getter `public boolean isSilenciavel()`.
    - Helper estático `public static Optional<CategoriaNotificacao> fromString(String valor)`: devolve `Optional.empty()` para null/desconhecido (usar try/catch sobre `valueOf` ou um stream `.filter(c -> c.name().equals(valor))`), NUNCA lançar — os planos 93-02/93-03 dependem deste contrato não-atirador para distinguir "categoria desconhecida" de "válida mas não-silenciável".
    - Helper estático `public static boolean isSilenciavelCategoria(String valor)`: `fromString(valor).map(CategoriaNotificacao::isSilenciavel).orElse(false)` — devolve `false` para categorias desconhecidas E para `PRAZO_VENCIDO`. É este método que o mute guard de `criar()` (Plan 93-02) e a validação de endpoint (Plan 93-03) consomem.
    - Imports: `java.util.Optional`.

    NÃO tocar em nenhum ficheiro existente nesta task. NÃO adicionar uma nova permissão/scope — a NOTF-24 reutiliza `notificacoes:view` (já semeada aos 4 perfis no DatabaseSeeder e já em KNOWN_SCOPES no frontend); confirmar por inspeção, não alterar o seeder.
  </action>
  <verify>
    <automated>cd backend && node -e "const fs=require('fs'); const strip=s=>s.split('\n').filter(l=>!l.trim().startsWith('//')&&!l.trim().startsWith('*')&&!l.trim().startsWith('/*')).join('\n'); const e=strip(fs.readFileSync('src/main/java/com/lexcv/models/NotificacaoPreferencia.java','utf8')); const c=strip(fs.readFileSync('src/main/java/com/lexcv/models/CategoriaNotificacao.java','utf8')); const ok = /t_notificacao_preferencia/.test(e) && /columnNames\s*=\s*\{\s*\"tenant_id\"\s*,\s*\"user_id\"\s*,\s*\"categoria\"\s*\}/.test(e) && /@PrePersist/.test(e) && /PRAZO_VENCIDO\(false\)/.test(c) && /FASE_ENTRADA\(true\)/.test(c) && /isSilenciavelCategoria/.test(c) && /Optional<CategoriaNotificacao>\s+fromString/.test(c) && (c.match(/\b(FASE_ENTRADA|DOCUMENTO_NOVO|PROCESSO_ATRIBUIDO|PARECER_ATRIBUIDO|PRAZO_PROXIMO|PRAZO_VENCIDO|EVENTO_PROXIMO|EVENTO_VENCIDO|HONORARIO_ATRASADO)\(/g)||[]).length===9; process.exit(ok?0:1)"</automated>
  </verify>
  <acceptance_criteria>
    - `NotificacaoPreferencia.java` existe no package com.lexcv.models, com `@Table(name="t_notificacao_preferencia")`, constraint única em (tenant_id, user_id, categoria), e `@PrePersist` de createdAt
    - `CategoriaNotificacao.java` enumera exatamente as 9 categorias; só `PRAZO_VENCIDO` tem `(false)`
    - `CategoriaNotificacao.fromString(String)` devolve `Optional` (nunca lança) e `isSilenciavelCategoria(String)` devolve false para desconhecido e para PRAZO_VENCIDO
    - Nenhum ficheiro existente foi alterado; nenhum novo scope/permission criado
  </acceptance_criteria>
  <done>Entidade e enum criados no package models, com a semântica presença-de-linha=silenciado e PRAZO_VENCIDO não-silenciável codificada.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: NotificacaoPreferenciaRepository + migração manual</name>
  <files>backend/src/main/java/com/lexcv/repositories/NotificacaoPreferenciaRepository.java, backend/migrations/93-create-notificacao-preferencia-table.sql</files>
  <read_first>
    - backend/src/main/java/com/lexcv/repositories/ClienteAdvogadoRepository.java (padrão EXATO de repositório derivado, incluindo o `void deleteBy...` derivado)
    - backend/migrations/86-create-notificacao-table.sql (cabeçalho de migração manual a replicar — porquê manual, ddl-auto=validate em prod)
    - backend/src/main/java/com/lexcv/models/NotificacaoPreferencia.java (a entidade criada na Task 1 — mapear colunas/tipos exatamente)
  </read_first>
  <action>
    (1) Criar `NotificacaoPreferenciaRepository.java` no package `com.lexcv.repositories`, `interface ... extends JpaRepository<NotificacaoPreferencia, UUID>`, com `@Repository`, com exatamente três métodos derivados (todos dual-scoped tenant+user, mitigando a fuga cross-tenant/cross-user do Pitfall 10):
    - `boolean existsByTenantIdAndUserIdAndCategoria(UUID tenantId, UUID userId, String categoria);` — o contrato de leitura do mute guard de `criar()` (Plan 93-02).
    - `List<NotificacaoPreferencia> findByTenantIdAndUserId(UUID tenantId, UUID userId);` — alimenta o endpoint GET de listagem (Plan 93-03) e o método de serviço `listarCategoriasSilenciadas` (Plan 93-02).
    - `void deleteByTenantIdAndUserIdAndCategoria(UUID tenantId, UUID userId, String categoria);` — delete derivado (precedente: `ClienteAdvogadoRepository.deleteByClienteIdAndUserIdAndTenantId`); será chamado dentro de um método `@Transactional` do serviço (Plan 93-02) para "reativar" uma categoria. NÃO usar `@Modifying @Query` — o codebase tem zero `@Modifying`; delete derivado é a convenção.
    - Imports: `com.lexcv.models.NotificacaoPreferencia`, `org.springframework.data.jpa.repository.JpaRepository`, `org.springframework.stereotype.Repository`, `java.util.List`, `java.util.UUID`.

    (2) Criar `backend/migrations/93-create-notificacao-preferencia-table.sql` replicando o cabeçalho-comentário do 86-create-notificacao-table.sql (é uma migração manual OBRIGATÓRIA: ddl-auto=update auto-cria em dev/CI; ddl-auto=validate em prod NUNCA cria — correr manualmente antes/durante o deploy; sem runner de migração no repo). O corpo:
    - `CREATE TABLE t_notificacao_preferencia ( id UUID PRIMARY KEY, tenant_id UUID NOT NULL, user_id UUID NOT NULL, categoria VARCHAR(255) NOT NULL, created_at TIMESTAMP NOT NULL );`
    - `CREATE UNIQUE INDEX uk_notificacao_preferencia ON t_notificacao_preferencia (tenant_id, user_id, categoria);`
    - Tipos/nomes de coluna têm de bater exatamente com a entidade (categoria é String → VARCHAR(255), como no t_notificacao). Registar no SUMMARY, na secção de tooling, que este script fica pendente de execução manual contra a BD de produção (junta-se aos scripts 74/86/88 já pendentes em STATE.md).
  </action>
  <verify>
    <automated>cd backend && node -e "const fs=require('fs'); const strip=s=>s.split('\n').filter(l=>!l.trim().startsWith('//')&&!l.trim().startsWith('*')&&!l.trim().startsWith('/*')).join('\n'); const r=strip(fs.readFileSync('src/main/java/com/lexcv/repositories/NotificacaoPreferenciaRepository.java','utf8')); const sql=fs.readFileSync('migrations/93-create-notificacao-preferencia-table.sql','utf8'); const ok = /extends\s+JpaRepository<NotificacaoPreferencia,\s*UUID>/.test(r) && /existsByTenantIdAndUserIdAndCategoria/.test(r) && /findByTenantIdAndUserId\b/.test(r) && /deleteByTenantIdAndUserIdAndCategoria/.test(r) && !/@Modifying/.test(r) && /CREATE TABLE t_notificacao_preferencia/i.test(sql) && /CREATE UNIQUE INDEX uk_notificacao_preferencia/i.test(sql); process.exit(ok?0:1)"</automated>
    <automated>cd backend && mvn -q -DskipTests compile</automated>
  </verify>
  <acceptance_criteria>
    - `NotificacaoPreferenciaRepository` tem os 3 métodos derivados dual-scoped e nenhum `@Modifying`
    - `93-create-notificacao-preferencia-table.sql` cria a tabela + índice único, com o cabeçalho de migração-manual padrão
    - `cd backend && mvn -q -DskipTests compile` compila com todos os ficheiros novos (entidade, enum, repositório)
  </acceptance_criteria>
  <done>Repositório dual-scoped e migração manual criados; o backend compila com a fundação de dados completa.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tenant + user isolation | `NotificacaoPreferencia` é o primeiro recurso desta milestone keyed por (tenant_id, user_id) — a preferência de um user nunca pode ser lida/escrita por outro user do mesmo tenant, nem cross-tenant |
| schema (dev vs prod) | ddl-auto=update cria a tabela em dev/CI; prod (validate) exige a migração manual |

## STRIDE Threat Register (ASVS L1 — security_block_on: high)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-93-01 | Information Disclosure | NotificacaoPreferenciaRepository | mitigate | Todos os 3 finders derivados incluem `TenantId` E `UserId` no nome/predicado — nunca `findByUserId` sozinho (Pitfall 10: "preferences keyed by user_id alone leak cross-tenant"). A constraint única `(tenant_id, user_id, categoria)` reforça a isolação a nível de BD. |
| T-93-02 | Elevation of Privilege | flag de silenciabilidade | mitigate | `PRAZO_VENCIDO(false)` codifica a categoria crítica não-silenciável numa fonte única; `isSilenciavelCategoria` é o único ponto que os planos a jusante consultam, evitando divergência de "quem pode ser silenciado". |
| T-93-03 | Tampering (schema) | migração manual | mitigate | Cabeçalho documenta a execução manual obrigatória em prod (ddl-auto=validate); sem ele, o arranque em prod falha em schema-validation em vez de silenciosamente ignorar preferências. |
| T-93-SC | Tampering | npm/pip/cargo installs | accept | Zero dependências novas nesta fase (só JPA/Lombok já presentes). Sem tarefas de instalação; gate de legitimidade de pacotes N/A. |
</threat_model>

<verification>
- `cd backend && mvn -q -DskipTests compile` compila com a entidade, o enum, o repositório novos
- Asserções de fonte confirmam: 9 categorias no enum, só PRAZO_VENCIDO não-silenciável, 3 métodos de repositório dual-scoped, tabela+índice na migração
- Verificação de isolamento tenant+user a nível de comportamento é feita no Plan 93-02 (testes Mockito do serviço) — aqui garante-se apenas que o contrato de dados existe e compila
</verification>

<success_criteria>
1. Entidade `NotificacaoPreferencia` (join tenant_id+user_id+categoria, presença=silenciado) existe e compila (NOTF-24)
2. `CategoriaNotificacao` fixa as 9 categorias e a única não-silenciável (PRAZO_VENCIDO), com contrato fromString/isSilenciavelCategoria não-atirador
3. Repositório dual-scoped + migração manual prontos como fundação para os planos 93-02 e 93-03
</success_criteria>

<output>
Create `.planning/phases/LEXCV-93-notf-24-prefer-ncias-de-notifica-o-por-utilizador/93-01-SUMMARY.md` when done
</output>
