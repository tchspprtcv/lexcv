---
phase: 97-auditoria-de-milestone
plan: 03
status: closed
generated: 2026-07-14
sources:
  - 75-HUMAN-UAT.md
  - 76-HUMAN-UAT.md
  - 79-HUMAN-UAT.md
  - 81-HUMAN-UAT.md
  - 82-HUMAN-UAT.md
  - 84-HUMAN-UAT.md
  - 85-HUMAN-UAT.md
  - 89-HUMAN-UAT.md
  - 89-VERIFICATION.md
---

# 97-UAT: Consolidated UAT Closure Record (Phases 75, 76, 79, 81, 82, 84, 85, 89)

Satisfies AUD-02. Every pending scenario across the 8 historical phases gets an explicit
verdict below: `LIVE-VERIFIED`, `CODE-VERIFIED`, `NEEDS-HUMAN-VISUAL`, `BLOCKED-with-reason`,
or `OPEN-WITH-REASON`. None are left as a bare "pending".

## Live-environment attempt and its actual limits (read this first)

A live backend (`localhost:8080`) and frontend (`localhost:3000`) are running this session —
the first time this milestone that's been possible. Real verification was attempted first:

- **Public/unauthenticated checks — actually executed live:**
  - `GET /api/v1/setup/status` → `200 {"initialized":true}` (confirms backend fully up, DB
    reachable, JSON contract intact).
  - `GET /api/v1/eventos`, `/api/v1/notificacoes/unread-count`, `/api/v1/notificacoes`,
    `/api/v1/honorarios`, `/api/v1/processos/intake`, `/api/v1/clientes/{id}/documentos`
    (all unauthenticated) → all returned `403` (not `500`), confirming Spring Security's
    `authorizeHttpRequests().anyRequest().authenticated()` rule is live and enforced exactly
    as `SecurityConfig.java` declares — a real, live-observed data point, not an assumption.
- **Why authenticated live curl (the plan's Task 1 default action) was NOT attempted:**
  this session's explicit instruction to this executor states an authenticated admin session
  already exists in the browser and directs Claude to **not** attempt login itself, using only
  already-authenticated session artifacts or public endpoints. This executor has no browser
  automation tool and cannot access the browser's own cookie store (doing so would itself be a
  form of credential extraction, out of scope). Consequently every authenticated business-logic
  assertion below (POST/PUT/PATCH scenarios, filtered GETs) is recorded as **BLOCKED-with-reason:
  no authenticated curl session available to this executor per this session's security
  instruction**, with **CODE-VERIFIED** used as the documented fallback — reading the exact
  controller method that would handle the request and confirming its behavior matches the
  scenario's expectation. This is stricter than the plan's own fallback path (login with seed
  default, then code-audit only if rejected): here, login is never attempted at all, so Claude
  literally never receives or handles a password, fully satisfying T-97-03-03.
- No live test data was created or deleted by this executor as a result (no authenticated
  writes were possible). The Phase 89 fan-out/mute/risco live confirmations already completed
  earlier this session (see below) stand as-is and are not repeated.

**Already confirmed live this session (per 97-CONTEXT.md, not re-tested here):**
NOTF-24 (mute toggle round-trip), AGD-35 (`GET /eventos` risco calculation), NOTF-25 partial
(`FASE_ENTRADA` fan-out via a real created Fase), NOTF-26 (full snooze flow, badge/history).

---

## Phase 75 — Componente Único View/Edit (`web/src/app/(dashboard)/clientes/[id]/page.tsx`)

| # | Scenario | Verdict | Evidence |
|---|----------|---------|----------|
| 1 | Paridade visual do modo leitura (dl/dd grid) | CODE-VERIFIED | `clientes/[id]/page.tsx:629-664` renders a `<dl className="grid grid-cols-3 gap-x-4 gap-y-3 text-sm">` with `<dt>`/`<dd>` pairs (Nome, Tipo, Email, Telefone, Localidade, Morada/Sede, Ativo, Criado) in read mode — matches the pre-existing detail-page grid layout described in the scenario. Pixel-level "no regression" comparison against the pre-Phase-75 screenshot is inherently visual — flagged as a secondary NEEDS-HUMAN-VISUAL confirmation, but the structural/markup claim is code-verified. |
| 2 | Round-trip de guardar (Editar → Guardar) | CODE-VERIFIED | `clientes/[id]/page.tsx:285-324` (`onSubmit`): calls `update.mutateAsync(payload)` (PUT via `useUpdateCliente`), `toast.success("Cliente atualizado com sucesso.")`, then `setIsEditing(false)` — no navigation, toggles back to read mode with the mutation's resolved (fresh) data. |
| 3 | Cancelar descarta alterações sem confirmação | CODE-VERIFIED | `clientes/[id]/page.tsx:326-339` (`onCancel`): calls `form.reset(buildDefaultValues(cliente.data))` (reverts to server-loaded values) and `setIsEditing(false)` directly on click — no `window.confirm`/dialog, no route change. |
| 4 | Gating de CRUD nos sub-componentes | CODE-VERIFIED | All sub-components (`ClienteDocumentosEntreguesTab`, `ClienteContactosCard`, `ClienteNotasCard`, advogados/administrativos cards) gate their Adicionar/Editar/Remover controls behind `canEditClientes && editable` (grep confirms 20+ call sites, e.g. lines 897, 932, 1287, 1539, 1644, 1815, 2018). Procuração's Ver/Download affordance is unconditional (`ClienteProcuracaoCard`, lines ~1530-1565: the download link renders regardless of `canEditClientes`/`editable`; only the upload/remove buttons are gated). |
| 5 | Links da lista de clientes → ficha unificada | CODE-VERIFIED | `clientes/page.tsx` (mobile+desktop rows and pencil icon) all resolve to `` `/clientes/${encodeURIComponent(c.id)}` `` (lines 429, 457, 463, 553, 593, 608) — zero references to `/editar`. Filesystem check confirms `web/src/app/(dashboard)/clientes/` only contains `[id]`, `[id]/ficha`, `merge`, `novo` — the old `/editar` route no longer exists at all. |

**Phase 75 note on scenario 1:** the underlying claim (correct dl/dd markup, no missing fields) is code-verified; the purely visual "identical to pre-fase screenshot" judgment is NEEDS-HUMAN-VISUAL as a secondary, non-blocking confirmation — no code regression risk identified.

## Phase 76 — Separadores: Dados, Contactos e Notas (7-tab ficha)

| # | Scenario | Verdict | Evidence |
|---|----------|---------|----------|
| 1 | Click-through dos 7 separadores + tab por defeito | CODE-VERIFIED (scenario superseded by later phases — see note) | `clientes/[id]/page.tsx:136` (`useState<TabKey>("dados")` default), lines 416-471 render 7 tab buttons (Dados, Contactos e Notas, Processos, Pareceres, Documentos Entregues, Documentos a Tratar, Deslocações) each wired to `setTab(...)`, and lines 474-1568 branch on `tab === "..."` to render real content for all 7 — a grep for "Em breve" across the entire `clientes/` tree returns zero matches. **Note:** the original scenario (written at Phase 76 close) expected 5 of the 7 tabs to show an "Em breve" placeholder; Phases 77 (Processos/Pareceres), 78 (Documentos a Tratar/Deslocações relocation) and 79 (Documentos Entregues real upload) subsequently wired all 5 placeholders to real data before this audit — the placeholder behavior described in the original scenario no longer exists by design, superseded by later, already-shipped work. The tab-switching mechanic itself (instant, no spinner, no network call on switch — content is pre-fetched per mounted sub-component) is confirmed by the conditional-render structure (no `useEffect`+fetch keyed on `tab`). |
| 2 | Erros de validação visíveis ao mudar de separador | CODE-VERIFIED | `clientes/[id]/page.tsx:382-390`: the "Guardar" button's `form.handleSubmit(onSubmit, () => { setTab("dados"); toast.error("Existem campos por corrigir no separador Dados."); })` — the second (`onInvalid`) argument switches back to "dados" and fires a toast whenever validation fails while parked on a different tab. |
| 3 | Reset dos diálogos de intake ao trocar de separador | CODE-VERIFIED | `clientes/[id]/page.tsx:215-224`: a `React.useEffect` keyed on `[tab]` explicitly closes and clears `addDocATratarModal`/`newDocATratar` and `addDeslocacaoModal`/`newDeslocacao` whenever `tab` is not `"documentosATratar"`/`"deslocacoes"` respectively — the code comment documents this exact rationale (dialogs are unmounted, not hidden, so a stale `open=true` would silently reopen with residual draft text on return). |
| 4 | Scroll horizontal da fila de separadores em mobile | CODE-VERIFIED | `clientes/[id]/page.tsx:416-417`: the 7 tab buttons are wrapped in `<div className="overflow-x-auto"><div className="flex gap-2 w-max">` — horizontal scroll container, no `flex-wrap`, matches the expected behavior exactly. |

## Phase 79 — Documentos Entregues (upload real)

| # | Scenario | Verdict | Evidence |
|---|----------|---------|----------|
| 1 | Fluxo de upload em direto | CODE-VERIFIED (live confirmation BLOCKED-with-reason: no authenticated session) | `clientes/[id]/page.tsx` `ClienteDocumentosEntreguesTab` (1223-1390): `FileDropZone` (drag/select), `novoTipo` bound to a `<input list=...>` datalist combobox derived from existing types (`tipoOptions`, 1244-1254), `progresso` state renders a progress bar (1331-1343), `onConfirmarUpload` → `toast.success("Documento enviado com sucesso.")` (1271) then closes the dialog and resets state; `useDocumentos({cliente_id})` (`use-documentos.ts:23-29`) routes to `GET /clientes/{id}/documentos` (the Phase 79 endpoint) so the list refetches via the same query key the mutation invalidates. |
| 2 | Fluxo de listagem/download/remoção em direto | CODE-VERIFIED (live BLOCKED-with-reason) | Listing: same `useDocumentos({cliente_id})` call. Download: `useDownloadDocumento(id)` → `GET /documentos/{id}/download` (`ResourceController.java:2829`), tenant-checked before serving. Removal: `ClienteDocumentoEntregueRow.onDelete` (1413-1419) calls `window.confirm("Apagar este documento?")` before `del.mutateAsync()`, then `toast.success("Documento apagado com sucesso.")` — matches "pede confirmação e atualiza a lista após confirmar" exactly. |
| 3 | Gating modo leitura vs modo edição | CODE-VERIFIED | `ClienteDocumentosEntreguesTab`'s "Adicionar" trigger is gated `editable && canEditDocumentos` (line 1287); each row's delete control is gated the same way inside `ClienteDocumentoEntregueRow`. |

## Phase 81 — Backend CRUD Decisões/Factos/Testemunhas, wiring Juízo/Origem

| # | Scenario | Verdict | Evidence |
|---|----------|---------|----------|
| 1 | `POST /processos/intake` sem `origem` → 422 + `camposEmFalta` | CODE-VERIFIED (live BLOCKED-with-reason) | `ResourceController.java:1256-1261` (`createProcessoIntake`): `if (processo.getOrigem() == null) return ResponseEntity.status(UNPROCESSABLE_ENTITY).body(Map.of("message", ..., "camposEmFalta", List.of("origem")))`. Exact match to the scenario's expectation. Live curl of this exact request is safe (no auth needed to trigger 422 correctly, but `getTenantId()` requires an authenticated principal to even reach the handler) — BLOCKED per the credentials note above; code path is unambiguous and unconditional. |
| 2 | `origem` imutável após PUT + `juizo` atualizável, refletido em detail+list | CODE-VERIFIED (live BLOCKED-with-reason) | `updateProcesso` (`ResourceController.java:1186-1216`) never calls `processo.setOrigem(...)` — line 1208's comment states explicitly: `"origem is intentionally excluded: immutable after intake, only writable via POST /processos/intake"`. It does call `processo.setJuizo(payload.getJuizo())` (line 1206). Both `juizo`/`origem` are included in the processo-list DTO mapping (line 1021-1022: `m.put("juizo", ...)`, `m.put("origem", ...)`), so `GET /processos` (list) and `GET /processos/{id}` (detail) surface the same values. |
| 3 | Double-check ownership 404 across Decisão/Facto/Testemunha | CODE-VERIFIED (live BLOCKED-with-reason) | Every update/delete endpoint for all three entities (`updateDecisao`/`deleteDecisao` 2002-2056, `updateFacto`/`deleteFacto` 2218-2266, `updateTestemunha`/`deleteTestemunha` 2127-2178) follows the identical two-step check: `(1)` processo lookup + tenant match → 404 if fails, `(2)` child lookup + `child.getProcessoId().equals(id)` → 404 if the child belongs to a *different* processo than the one in the path. A PUT/DELETE using processo B's id in the path with processo A's child id fails step (2) and returns 404, exactly as required. |
| 4 | Forged `ordem: 999` on concurrent `POST .../factos` → distinct server-computed values | CODE-VERIFIED (live BLOCKED-with-reason) | `createFacto` (`ResourceController.java:2192-2216`) never reads `facto.getOrdem()` from the request body for creation — inside `synchronized (FactoRepository.class) { int nextOrdem = factoRepository.findMaxOrdemByProcessoId(id).orElse(0) + 1; facto.setOrdem(nextOrdem); ... }` — any client-supplied `ordem` value is silently overwritten before save. The `synchronized` block serializes concurrent creates within this JVM instance; `DataIntegrityViolationException` (from the DB-level unique constraint, dev `ddl-auto=update`-created) is caught and mapped to `409` as a backstop for any residual race (line 2211-2214). |
| 5 | Migration script vs. `ddl-auto=validate` (prod-like DB) | **OPEN-WITH-REASON** | This dev environment runs `ddl-auto=update`, not `ddl-auto=validate` — **no `ddl-auto=validate` database is available in this session**, so the exact scenario (confirming `backend/migrations/81-add-facto-ordem-unique-constraint.sql` applies cleanly to a prod-like DB) cannot be live-verified here. The constraint IS confirmed enforced in dev: it auto-exists via `ddl-auto=update`, and scenario #4's concurrency path already exercises its `DataIntegrityViolationException → 409` catch. Recommendation: review the migration script's SQL syntax (`ALTER TABLE t_facto ADD CONSTRAINT uk_facto_processo_ordem UNIQUE (processo_id, ordem);`) against a staging/prod-like DB before the next deploy — it is syntactically simple standard SQL and no red flags were found on inspection, but this executor did not — and will not — attempt a risky in-place `ddl-auto` flip against the shared dev DB to force this scenario. |

## Phase 82 — Criação automática de Honorário na formalização

| # | Scenario | Verdict | Evidence |
|---|----------|---------|----------|
| 1 | Formalizar TRIAGEM→ATIVO renders `/financeiro` pages without crashing; `valorTotal` shows "A confirmar"/blank not "null" | NEEDS-HUMAN-VISUAL (rendering, requires authenticated browser) + CODE-VERIFIED (data contract) | `formalizarProcesso` (`ResourceController.java:1474-1487`) always creates `Honorario.builder().valorTotal(null)...` — confirms the backend never returns a literal string `"null"`, it's JSON `null`; the frontend's blank/"A confirmar" rendering of a `null` `valorTotal` is a frontend formatting concern outside this plan's `<files_modified>` (only `97-UAT.md`) — verified at the data-contract level; the actual page-render/no-crash confirmation needs a live authenticated browser session this executor cannot drive. |
| 2 | Second `formalizar` call → 409; exactly one Honorário row | CODE-VERIFIED (live BLOCKED-with-reason) | `formalizarProcesso` (line 1426-1429): `if (!"TRIAGEM".equalsIgnoreCase(processo.getEstado())) return ResponseEntity.status(CONFLICT)...` — after the first successful call sets `estado="ATIVO"` (line 1472), any retry/replay hits this guard and returns 409 *before* reaching the Honorário-creation block at all. Combined with the idempotent existence-check (`honorarioRepository.findByProcessoId(id).isEmpty()`, line 1476) and the unique constraint backstop, exactly one row is guaranteed even against a replayed request that somehow bypassed the state guard. |
| 3 | Two genuinely concurrent `formalizar` calls → exactly one Honorário row, no 500 | CODE-VERIFIED (live BLOCKED-with-reason) | Same code path as #2: the existence-check + `try { honorarioRepository.save(...) } catch (DataIntegrityViolationException ex) { /* outro formalizar concorrente já criou o honorário — seguro ignorar */ }` (lines 1476-1486) explicitly absorbs the race without a 500 — the losing concurrent request's exception is swallowed, not rethrown. |
| 4 | Migration script vs. `ddl-auto=validate` (prod-like DB) | **OPEN-WITH-REASON** | Identical constraint to Phase 81 #5 — **no `ddl-auto=validate` database available in this session**. The `@UniqueConstraint(processo_id)` on `Honorario` is confirmed enforced in dev via scenario #2/#3's guard+catch logic above. Recommendation: review `backend/migrations/82-add-honorario-processo-unique-constraint.sql` (`ALTER TABLE t_honorario ADD CONSTRAINT uk_honorario_processo UNIQUE (processo_id);`) against staging/prod before next deploy; not executed here for the same reason as 81 #5. |

## Phase 84 — Frontend UI: Intake, Dados sub-secções, Documentos, Termo de Honorários

| # | Scenario | Verdict | Evidence |
|---|----------|---------|----------|
| 1 | Origem required at intake step 1 (no network call if unset); read-only Origem post-formalização | CODE-VERIFIED | `web/src/schemas/processos.ts:27-33`: `origem: origemProcessoSchema` (a non-optional `z.enum`) in the intake form schema — react-hook-form's zodResolver blocks `handleSubmit` client-side before any `apiFetch` call fires. `novo/page.tsx:328-341` renders the placeholder-first `<select>` with inline error text bound to `intakeForm.formState.errors.origem`. Post-formalização, `processos/[id]/page.tsx:771-773` renders Origem as a `<dd>` (read-only text via `origemProcessoToLabel`), and `processos/[id]/editar/page.tsx` has zero `origem` field/register anywhere — no edit affordance exists. |
| 2 | Juízo pre-fills on `/editar`, persists to ficha | CODE-VERIFIED | `processos/[id]/editar/page.tsx:91` (`juizo: processo.data.juizo` in `defaultValues`) confirms pre-fill; `:208-216` renders the editable input; the ficha's read-only row (`processos/[id]/page.tsx:768-769`) reads `processo.data.juizo` directly, reflecting whatever the PUT persisted (backend confirmed at Phase 81 #2 above: `setJuizo` is applied on every update). |
| 3 | Termo de Honorários print gating + CVE formatting + print CSS | CODE-VERIFIED (data/gating logic) + NEEDS-HUMAN-VISUAL (actual print-preview layout) | `termo-honorarios/page.tsx:77,92` (`isBlocked = !honorario \|\| honorario.valorTotal === null`, `disabled={isBlocked}` on the Imprimir button), `:100-108` (red message + working `<Link href={/financeiro/${honorario.id}}>`), `:45-48` (`fmtMoney` uses `toLocaleString("pt-CV", {style:"currency", currency:"CVE"})`, never a raw number). `PRINT_CSS` (lines 23-36) hides `aside, header, [data-print-hide], .bottom-nav, .ficha-print-btn` and sets `@page { size: A4; margin: 2cm; }`. The actual paginated/rendered print-preview appearance is a judgment call this headless executor cannot make — NEEDS-HUMAN-VISUAL for that piece specifically; the gating/formatting logic itself is unambiguous from source. |
| 4 | Partes/Fases dialog reset on Cancelar (WR-02); untouched-row Fases "Guardar" preserves current status (CR-01) | CODE-VERIFIED | `onOpenAddParte`/`onOpenAddFase` (`processos/[id]/page.tsx:475-476, 496-497`) call `parteForm.reset(...)`/`faseForm.reset(...)` with blank defaults on every dialog open (not just on unmount), so a Cancelar→reopen never shows stale text. Fases: the per-row "Guardar" button (`onClick={() => onUpdateFaseStatus(f.id, f.status)}`, line 1782) always passes the row's live `f.status`; `onUpdateFaseStatus` (line 517-518) internally resolves `faseDraftStatus[faseId] ?? currentStatus` — an untouched row (no draft entry) saves its own unchanged current status, never empty/undefined. |
| 5 | Decisão-attached PDF appears in Documentos tab; Testemunha edit dialog pre-fills | CODE-VERIFIED | `createDecisao` (`ResourceController.java:1925-1998`) persists the uploaded file as a `Documento` row with `processoId(id)` (lines 1972-1987) — the exact same table/column `GET /processos/{id}/documentos` queries (`documentoRepository.findByTenantIdAndProcessoId`, line 2809-2816), so the attachment surfaces there automatically, no separate upload step. `onOpenEditTestemunha` (`processos/[id]/page.tsx:592-593`): `testemunhaForm.reset({ nome: t.nome, tipo: t.tipo, contacto: t.contacto, notas: t.notas })` — pre-fills every field from the clicked row. |
| 6 | Facto reorder re-sorts table; processo-tab-uploaded document appears on generic `/documentos` | CODE-VERIFIED | `processos/[id]/page.tsx:2077`: `[...factos.data].sort((a, b) => a.ordem - b.ordem)` — table order is always derived fresh from `ordem`, so any successful `updateFacto` (which persists a new `ordem`, confirmed in Phase 81 #3/#4 evidence above) immediately re-sorts on refetch. `documentos/page.tsx:145,161,181,242-244`: the generic list renders a "Processo" column and each row component receives `processoId={d.processo_id}`, displaying the association. |
| 7 | RBAC per-tab gating (`processos:view`-only hides all CRUD incl. Fases status select; `processos:edit`-without-`documentos:edit` sees read-only Documentos) | CODE-VERIFIED (live BLOCKED-with-reason — requires a second test user without full scopes) | The Fases status `<select disabled={!canEditProcessos}>` (line 1770) and its "Guardar" button `disabled={!canEditProcessos \|\| ...}` (line 1783) — this is WR-03's fix, confirmed present. `canEditDocumentos = permissions.can.edit("documentos")` is a scope distinct from `processos:edit` (`processos/[id]/page.tsx:231`), gating the Documentos tab's own Adicionar/remove controls independently (lines 2576, 2744) — a user with `processos:edit` but not `documentos:edit` sees full CRUD on the other tabs but a read-only Documentos list, exactly as required. Exercising this with an actual second RBAC-limited login is a live-session action this executor cannot perform without credentials. |

## Phase 85 — Consolidação da Lógica de Prazo Crítico (open product decision)

| # | Scenario | Verdict | Evidence |
|---|----------|---------|----------|
| 1 | ALTA-priority `Evento` with null `dataFim` — should it count as urgent in `agendaUrgentesCount`/`prazosCriticosCount`? | **OPEN PRODUCT DECISION — carried forward, not silently dropped** | Located via `grep -n "isEventoCritico" backend/src/main/java/com/lexcv/controllers/ResourceController.java` (line number as of this audit: **method at line 3142**, docblock starting at **line 3129** — grep `isEventoCritico` to relocate if it has moved). The docblock states verbatim (Portuguese, backend source): *"eventos com prioridade ALTA e dataFim nula resultam em risco="ok" (computeRiscoEvento trata data nula como "ok") e por isso NÃO são contados como urgentes/críticos aqui — comportamento aceite explicitamente para este corner case (lembrete de prioridade ALTA sem data de fim definida). Isto é uma mudança face à lógica anterior, que contava qualquer evento ALTA independentemente da data. Se o produto decidir que este corner case deve contar como urgente/crítico por definição, tratar "ALTA" + dataFim nula como urgente explicitamente antes de delegar em computeRiscoEvento."* Current behavior: **excludes** such events from both `agendaUrgentesCount` (dashboard KPI) and `prazosCriticosCount` (processos dashboard KPI) — both call sites (`agendaUrgentesCount`, line ~3147-3152; `prazosCriticosCount`, line ~3257-3260) filter through the same `isEventoCritico` helper. This remains an explicit product decision for the user; this audit does not invent an answer. |

## Phase 89 — Sino e Página de Notificações (all 11 scenarios individually)

| # | Scenario (89-HUMAN-UAT.md) | Verdict | Evidence |
|---|---------|---------|----------|
| 1 | Badge counter/cap/refocus (NOTF-08) | CODE-VERIFIED | `web/src/hooks/use-notificacoes.ts:42-50` (`useNotificacoesUnreadCount`): `refetchInterval: 30_000`, `refetchOnWindowFocus: true` (unconditional on this hook, overriding the app-wide default of `false`). `notification-bell.tsx:93` (`count > 9 ? "9+" : count`). Re-confirmed unchanged from 89-VERIFICATION.md's citation (same file, values match exactly). |
| 2 | Dropdown list + empty state (NOTF-09) | CODE-VERIFIED | `notification-bell.tsx:58` (`useNotificacoes({ size: 20 }, { poll: true })`, over-fetched to 20 as of Phase 96's snooze work, filtered client-side and `.slice(0, 10)` at line 67 — still surfaces "up to 10", the WR-01-driven over-fetch does not change the observable contract), header "Notificações" (line 100), empty state `"Sem notificações por agora."` (lines 120-123). |
| 3 | Fused click = mark+navigate (NOTF-10, bell) | CODE-VERIFIED, cross-referenced | `notification-bell.tsx:133-145`: `isInternalLinkUrl(n.linkUrl) ? <Link href={n.linkUrl} onClick={() => { marcarLida.mutate(n.id); setOpen(false); }}>` — `.mutate` (fire-and-forget, not awaited) alongside native `<Link>` navigation; non-internal/no-link items render plain text with an inline `Check` button when unread. Cross-referenced with the live-attempted `PATCH /notificacoes/{id}/lida` contract (`NotificacaoController.java:102-109`) — BLOCKED live (no auth session) but the endpoint contract matches the hook's call exactly (`use-notificacoes.ts:56-64`). |
| 4 | Mark-all (NOTF-11, bell) | CODE-VERIFIED, cross-referenced | `notification-bell.tsx:69-76,101-110`: `handleMarcarTodas` → `marcarTodas.mutateAsync()` → `toast.success(...)`; button `disabled={count === 0 \|\| marcarTodas.isPending}`. Cross-referenced with `POST /notificacoes/ler-todas` (`NotificacaoController.java:112-116`) — same BLOCKED-live/CODE-VERIFIED-contract status as #3. |
| 5 | `/notificacoes` access + layout (NOTF-12) | CODE-VERIFIED | `notificacoes/page.tsx:118` (`<h1>Notificações</h1>` + subtitle at line 119-121). Grep for "notificac" (case-insensitive) across `web/src/components/shared/` returns only `notification-bell.tsx` and `notificacao-snooze-control.tsx` — no sidebar/bottom-nav entry exists. Live-visiting the route requires an authenticated session (BLOCKED); the route's static shape is unambiguous from source. |
| 6 | Category/read-state filters (NOTF-13) | CODE-VERIFIED, cross-referenced | `notificacoes/page.tsx:85-93` (`onCategoriaChange`/`onLidaFilterChange`, both call `setPage(0)`), `:163-181` (3-way chip group, `aria-pressed`). Cross-referenced with `NotificacaoController.listar` (`@RequestParam(required=false) String categoria`, `@RequestParam(required=false) Boolean lida`, lines 71-75) — exact param-name match confirmed live-reachable (403 unauthenticated, not 404/500 — route exists and is wired). |
| 7 | Standalone mark-one + pagination (NOTF-10/page) | CODE-VERIFIED | `notificacoes/page.tsx:307-319` (per-row `Check` button, `onClick={() => onMarcarLida(id)}`, only rendered `!lida`). Pagination: lines 228-250 (`disabled={page===0}` / `disabled={page+1 >= totalPages}`, "Página X de Y", only rendered when `totalPages > 1`). |
| 8 | Cross-surface invalidation | CODE-VERIFIED (wiring) / NEEDS-HUMAN-VISUAL (live two-surface reflection) | `use-notificacoes.ts:61-63,75-77`: both `useMarcarNotificacaoLida` and `useMarcarTodasNotificacoesLidas` call `queryClient.invalidateQueries({ queryKey: ["notificacoes"] })` — the shared top-level prefix used by list/unread-count queries in both the bell and the page (`queryKey: ["notificacoes","list",...]` / `["notificacoes","unread-count"]`), so TanStack Query v5's default prefix-match invalidates both surfaces from a single mutation. Verifying two live mounted surfaces (bell + page open in the same session) actually reflect this in real time requires an interactive two-panel browser session this executor cannot drive — NEEDS-HUMAN-VISUAL for the live reflection itself, though the wiring is unambiguous. |
| 9 | RBAC gate `notificacoes:view` | CODE-VERIFIED | `notificacoes/page.tsx:35-45`: `const canView = permissions.can.view("notificacoes"); if (!permissions.isLoading && !canView) return <AccessDeniedState .../>`. Backend defense-in-depth confirmed present on `NotificacaoController` (`@PreAuthorize("hasAuthority('notificacoes:view')")`, unchanged since 89-VERIFICATION.md). |
| 10 | (WR-01 follow-up) Same-origin link-guard visual | NEEDS-HUMAN-VISUAL | `notificacao-categoria.ts:106-113` (`isInternalLinkUrl`) re-read fresh this session — byte-for-byte unchanged from 89-VERIFICATION.md's citation (parser-based check against `INTERNAL_URL_SENTINEL`). The underlying logic is already proven correct by 16/16 independent behavioral test cases (89-VERIFICATION.md) plus 3 rounds of static code review — this item is a rendered non-clickable-text visual confirmation only, requires writing a bypass `linkUrl` directly to the DB (no production trigger generates one) and viewing it in a real browser. Not a functional risk; purely a final visual sign-off. |
| 11 | (WR-02 follow-up) Pagination-flash-fix visual | NEEDS-HUMAN-VISUAL | `notificacoes/page.tsx:62-79`: the `lastTotalPages` derived-during-render clamp (React's documented "adjusting state during render" pattern, replacing a `useEffect`+`setState` that produced a visible flash) is present and matches 89-VERIFICATION.md's description of the fix exactly (comment at lines 66-72 explains the exact same rationale). `pnpm lint` was not re-run this session (out of scope for a docs-only plan touching only `97-UAT.md`), but the source fix itself is unchanged since the verifier's last confirmed-clean lint run. The frame-by-frame visual transition itself remains a live-browser confirmation only. |

---

## Summary Table

| Phase | Total Scenarios | LIVE-VERIFIED | CODE-VERIFIED | NEEDS-HUMAN-VISUAL | OPEN-WITH-REASON | Closure Statement |
|-------|------------------|----------------|----------------|----------------------|-------------------|--------------------|
| 75 | 5 | 0 | 5 (1 with a secondary visual note) | 0 (folded into #1) | 0 | All 5 scenarios confirmed correct at the code level; unified view/edit toggle, round-trip save, cancel-discard, CRUD gating, and list routing all verified against current source. Closed. |
| 76 | 4 | 0 | 4 | 0 | 0 | All 4 scenarios confirmed; scenario 1's original "Em breve" expectation is superseded by Phases 77-79 wiring all tabs to real data — noted explicitly, not a regression. Closed. |
| 79 | 3 | 0 | 3 | 0 | 0 | Upload/list/download/delete flow and RBAC gating all confirmed at the code level; live curl blocked on credentials per this session's policy. Closed. |
| 81 | 5 | 0 | 4 | 0 | 1 | Scenarios #1-4 (validation, immutability, ownership double-check, server-computed ordem) confirmed at the code level. Scenario #5 (prod-like-DB migration) explicitly open — no `ddl-auto=validate` DB available this session. Closed with one documented open item. |
| 82 | 4 | 0 | 3 (1 partly NEEDS-HUMAN-VISUAL) | 1 (folded into #1) | 1 | Scenarios #1-3 (idempotent Honorário creation, 409 on retry, concurrency-safe) confirmed at the code/data-contract level; #1's page-render/no-crash claim needs a live browser. Scenario #4 (prod-like-DB migration) explicitly open, same reason as Phase 81 #5. Closed with one documented open item. |
| 84 | 7 | 0 | 7 (2 with secondary visual/live notes) | 0 (folded) | 0 | Origem/Juízo intake+ficha wiring, termo-honorários gating, dialog resets, Decisão→Documentos linkage, Facto reorder, and RBAC per-tab gating all confirmed at the code level. Print-preview layout and a second-user RBAC walkthrough remain visual/live-only confirmations, noted per-scenario. Closed. |
| 85 | 1 | 0 | 0 | 0 | 1 (product decision) | The ALTA+null-dataFim KPI exclusion is confirmed as current, intentional, documented behavior — the underlying product question is carried forward for the user's decision, not resolved by this audit. Open by design. |
| 89 | 11 | 0 | 8 | 3 (2 of which also have a CODE-VERIFIED wiring component) | 0 | All 11 scenarios individually verified at the code level (data/wiring layer); 3 remain NEEDS-HUMAN-VISUAL for purely visual/live-timing confirmation (cross-surface real-time reflection, WR-01 bypass-string render, WR-02 frame-level flash) — none indicate a functional defect; all 3 are backed by prior independent static/behavioral evidence (89-VERIFICATION.md). Closed. |

**Overall AUD-02 status:** all ~38 scenarios across the 8 phases have an explicit verdict; zero are left as a bare "pending". 2 scenarios (Phase 81 #5, Phase 82 #4) are open-with-reason pending a prod-like DB migration review, and 1 (Phase 85) is an open product decision — both categories were already known/expected going into this phase and are carried forward to the user, not silently dropped. All remaining scenarios are closed either by code-level confirmation (the large majority, given the credential/browser-automation constraints of this headless session) or flagged NEEDS-HUMAN-VISUAL where correctness genuinely depends on rendered layout, print preview, or live interactive/two-panel browser behavior a headless executor cannot judge.
