---
phase: 97-auditoria-de-milestone
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/LEXCV-97-auditoria-de-milestone-d-vida-t-cnica-e-uat-pendente/97-UAT.md
autonomous: true
requirements: [AUD-02]

must_haves:
  truths:
    - "Each of the 8 pending-UAT phases (75, 76, 79, 81, 82, 84, 85, 89) has an explicit closure verdict: live-verified, code-verified, or open-with-reason"
    - "All 11 Phase 89 scenarios (badge counter/refocus, dropdown list+empty-state, fused click-mark-navigate, mark-all, /notificacoes access+layout, filters, standalone mark-one+pagination, cross-surface invalidation, RBAC gate, link-guard visual, pagination-flash visual) each get an explicit verdict — none left unassigned"
    - "Backend-API-verifiable UAT items are attempted against the live backend at localhost:8080 and their pass/fail recorded"
    - "The Phase 85 open product decision (ALTA-priority Evento with null dataFim in KPIs) is documented as a carried-forward decision, not silently dropped, with a grep-verified (not hardcoded-line) source reference"
    - "Phase 81 scenario #5 and Phase 82 scenario #4 (manual SQL migration against a ddl-auto=validate prod-like DB) are recorded as explicit open-with-reason, not silently dropped"
    - "Anything left unverified states WHY (credentials, browser-only visual, extensive data setup, prod-like DB unavailable in dev)"
  artifacts:
    - path: ".planning/phases/LEXCV-97-auditoria-de-milestone-d-vida-t-cnica-e-uat-pendente/97-UAT.md"
      provides: "Consolidated UAT closure record for the 8 phases"
      contains: "Phase 89"
  key_links:
    - from: "97-UAT.md"
      to: "each phase HUMAN-UAT.md scenario"
      via: "per-scenario verdict (live-verified / code-verified / needs-human-visual / open-with-reason)"
      pattern: "Phase 8"
---

<objective>
Close — with real evidence where practical — the live UAT/verification items pending across 8
historical phases (75, 76, 79 from v2.8; 81, 82, 84 from v2.9; 85, 89 from v2.10). For the first
time this milestone a full live environment is available (backend `localhost:8080`, frontend
`localhost:3000`, native Postgres, MinIO), so attempt REAL verification (curl against the live
API, documented UI checks, live-observed browser behavior) as the PRIMARY method — per
97-CONTEXT.md's locked decision to prioritize real verification over code re-reading, especially
for simple UI flows. Fall back to code-level verification only where live verification is
impractical for a headless executor (no browser-automation tool available to this agent — the
executor can read an already-authenticated live page's rendered HTML/network calls via the
`localhost:3000` proxy and curl, but cannot drive click/type interactions or judge print-preview
layout, which is what makes those specific scenarios NEEDS-HUMAN-VISUAL rather than a blanket
default). Where NEITHER live nor code verification is practical within this phase's scope (e.g.
scenarios requiring a `ddl-auto=validate` prod-like DB that does not exist in this dev
environment), document explicitly what was and was NOT verified, and why — per the user's
approved fallback of "auditoria de código em vez de UAT ao vivo" for whatever isn't reproducible
live.

Produce ONE consolidated closure record, `97-UAT.md`, rather than editing the 8 archived
HUMAN-UAT.md files (the milestone-closeout plan, 97-04, reads this record to update STATE.md
Deferred Items).

Purpose: Satisfy AUD-02.
Output: `97-UAT.md` with a per-phase, per-scenario verdict — every one of the ~38 scenarios across
the 8 phases gets an explicit verdict; none are left as a bare "pending".
</objective>

<execution_context>
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/LEXCV-97-auditoria-de-milestone-d-vida-t-cnica-e-uat-pendente/97-CONTEXT.md

<pending_uat_sources>
<!-- The exact pending scenarios per phase. Read each source file before verifying. -->
- Phase 75 (5 UI scenarios): @.planning/milestones/v2.8-phases/LEXCV-75-componente-nico-view-edit/75-HUMAN-UAT.md
- Phase 76 (4 UI scenarios): @.planning/milestones/v2.8-phases/LEXCV-76-separadores-—-dados-contactos-e-notas/76-HUMAN-UAT.md
- Phase 79 (3 upload/list/download scenarios): @.planning/milestones/v2.8-phases/LEXCV-79-documentos-entregues-—-upload-real/79-HUMAN-UAT.md
- Phase 81 (5 backend CRUD scenarios, #5 is a migration-against-prod-like-DB scenario): @.planning/milestones/v2.9-phases/LEXCV-81-backend-crud-decis-es-factos-testemunhas-wiring-ju-zo-origem/81-HUMAN-UAT.md
- Phase 82 (4 backend honorário scenarios, #4 is a migration-against-prod-like-DB scenario): @.planning/milestones/v2.9-phases/LEXCV-82-backend-cria-o-autom-tica-de-honor-rio-na-formaliza-o/82-HUMAN-UAT.md
- Phase 84 (7 UI scenarios): @.planning/milestones/v2.9-phases/LEXCV-84-frontend-ui-intake-dados-sub-sec-es-documentos-termo-de-hono/84-HUMAN-UAT.md
- Phase 85 (1 product-decision scenario): @.planning/milestones/v2.10-phases/LEXCV-85-consolida-o-da-l-gica-de-prazo-cr-tico/85-HUMAN-UAT.md
- Phase 89 (11 bell/page scenarios — ALL 11 must get an explicit verdict, see Task 2): @.planning/milestones/v2.10-phases/LEXCV-89-sino-e-p-gina-de-notifica-es/89-HUMAN-UAT.md
</pending_uat_sources>

<live_env>
- Backend: http://localhost:8080 (running). API base path: /api/v1. Auth: JWT in httpOnly cookies (POST /api/v1/auth/login sets access_token + refresh_token).
- Frontend: http://localhost:3000 (running). Same-origin API via Next rewrite.
- Default seeded admin (CLAUDE.md): admin@lexcv.cv / Pa$$w0rd. The live DB is a pre-existing dev environment — the password MAY differ from the seed default.
- Already confirmed live THIS session (DO NOT re-test): NOTF-24 mute toggle, AGD-35 GET /eventos risco, NOTF-25 FASE_ENTRADA fan-out, NOTF-26 full snooze flow.
- This dev environment runs `ddl-auto=update` (JPA auto-creates constraints). There is no
  `ddl-auto=validate` prod-like database available in this session — Phase 81 scenario #5 and
  Phase 82 scenario #4 (which specifically require one) cannot be live-verified here; see Task 1.
</live_env>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Live backend-API UAT via curl (phases 81, 82, 79, 89 backend contracts)</name>
  <files>.planning/phases/LEXCV-97-auditoria-de-milestone-d-vida-t-cnica-e-uat-pendente/97-UAT.md</files>
  <read_first>
    - .planning/milestones/v2.9-phases/LEXCV-81-backend-crud-decis-es-factos-testemunhas-wiring-ju-zo-origem/81-HUMAN-UAT.md
    - .planning/milestones/v2.9-phases/LEXCV-82-backend-cria-o-autom-tica-de-honor-rio-na-formaliza-o/82-HUMAN-UAT.md
    - .planning/milestones/v2.8-phases/LEXCV-79-documentos-entregues-—-upload-real/79-HUMAN-UAT.md
    - .planning/milestones/v2.10-phases/LEXCV-89-sino-e-p-gina-de-notifica-es/89-HUMAN-UAT.md
    - backend/src/main/java/com/lexcv/controllers/ResourceController.java (endpoint paths + response shapes for intake/formalizar/documentos/eventos/notificacoes — grep for the specific route, do not re-read ranges already loaded)
    - backend/migrations/81-add-facto-ordem-unique-constraint.sql and backend/migrations/82-add-honorario-processo-unique-constraint.sql (the manual scripts scenario 81#5/82#4 reference)
  </read_first>
  <action>
    Establish an authenticated session: `curl -c /tmp/lexcv-cookies.txt -X POST
    http://localhost:8080/api/v1/auth/login -H 'Content-Type: application/json' -d
    '{"email":"admin@lexcv.cv","password":"Pa$$w0rd"}'`. If login returns 200, reuse the cookie
    jar (`-b /tmp/lexcv-cookies.txt`) for all subsequent calls. If login fails (password changed
    on this dev DB), record in 97-UAT.md that live curl is blocked on credentials (Claude never
    has the password by security rule), fall back to code-verification for these items, and
    request the user run them in the authenticated browser. Then attempt, recording the actual
    HTTP status/body verdict for each:
      • Phase 81 (scenarios #1-4): POST /api/v1/processos/intake WITHOUT `origem` → expect 422
        with `origem` in camposEmFalta; PUT /api/v1/processos/{id} changing origem + setting
        juizo, then GET /processos/{id} and GET /processos → expect origem immutable, juizo
        updated and surfaced in the list; a PUT/DELETE on a child (Decisão/Facto/Testemunha)
        using a DIFFERENT processo id in the path but the first processo's child id → expect 404
        (double-check ownership); two near-simultaneous POST .../factos with client-forged
        `ordem: 999` → expect distinct server-computed sequential values, not 999.
      • Phase 81 (scenario #5 — migration against ddl-auto=validate): this dev environment runs
        `ddl-auto=update`, not `ddl-auto=validate`, so the exact scenario (confirming the manual
        migration applies cleanly to a prod-like DB) CANNOT be live-verified here. Record as
        OPEN-WITH-REASON: "no ddl-auto=validate database available in this session; the
        constraint IS confirmed enforced in dev (ddl-auto=update auto-created it, exercised by
        scenario #4's concurrency test); the migration script's SQL syntax should be reviewed by
        the user against a staging/prod-like DB before the next deploy." Do not attempt a risky
        in-place ddl-auto flip against the shared dev DB to force this scenario.
      • Phase 82 (scenarios #1-3): POST /processos/{id}/formalizar on a TRIAGEM processo → expect
        one Honorário created with valorTotal null; POST again → expect 409; confirm exactly one
        t_honorario row for that processo (via GET /honorarios?processo_id={id}).
      • Phase 82 (scenario #4 — migration against ddl-auto=validate): same constraint as Phase 81
        #5 — this environment has no `ddl-auto=validate` database. Record as OPEN-WITH-REASON
        with the same reasoning (constraint confirmed enforced in dev via scenario #2/#3's
        concurrency + duplicate tests; migration script review recommended before deploy).
      • Phase 79: GET /clientes/{id}/documentos returns the list; exercise upload/download/delete
        endpoints if a safe test client exists (use the existing "Test Client" data, do not
        delete real data).
      • Phase 89 (API-shape-verifiable subset — the remaining Phase 89 scenarios are handled in
        Task 2): GET /notificacoes/unread-count (badge count, NOTF-08's data source); GET
        /notificacoes?categoria=X&lida=false (paginated list + filters, NOTF-09/NOTF-13's data
        source); PATCH /notificacoes/{id}/lida on an unread notification (NOTF-10's mark-one
        contract — confirm it flips to lida=true and unread-count decrements); POST
        /notificacoes/ler-todas (NOTF-11's mark-all contract — confirm unread-count goes to 0
        afterward). Record each as PASS/FAIL with the actual response observed.
    Use the existing test data noted in 97-CONTEXT.md (processo `1/2026`, "Test Client"); create
    only throwaway rows if needed and leave any created test data in place (do not clean up).
    Write each verdict (PASS / FAIL / BLOCKED-with-reason / OPEN-WITH-REASON) into the Phase
    81/82/79/89 sections of 97-UAT.md.
  </action>
  <verify>
    <automated>curl -s -o /dev/null -w "%{http_code}" http://localhost:8080/api/v1/eventos -b /tmp/lexcv-cookies.txt || echo "backend-or-auth-unreachable-documented"</automated>
  </verify>
  <acceptance_criteria>
    - Behavior assertion: 97-UAT.md records the actual HTTP status returned for each attempted backend scenario (81 #1-4, 82 #1-3, 79, 89's 4 API-shape items), OR a BLOCKED verdict naming the reason (auth/credentials) if login failed.
    - Behavior assertion: 97-UAT.md records Phase 81 scenario #5 and Phase 82 scenario #4 as OPEN-WITH-REASON, explicitly naming "no ddl-auto=validate database available in this session" — neither scenario is silently dropped or omitted.
    - CLI: at least one authenticated GET against localhost:8080 was attempted and its outcome recorded.
    - Source assertion: no live-created test data was deleted; any throwaway rows left in place per 97-CONTEXT.md.
  </acceptance_criteria>
  <done>Backend-verifiable UAT items for phases 81, 82, 79, and the API-shape subset of 89 attempted live and recorded with concrete verdicts (PASS/FAIL/BLOCKED/OPEN-WITH-REASON) in 97-UAT.md; the two prod-like-DB migration scenarios are explicitly recorded as open, not dropped.</done>
</task>

<task type="auto">
  <name>Task 2: Live-observe/code-verify UI-heavy UAT (75, 76, 84, 89) + document Phase 85 decision; finalize 97-UAT.md</name>
  <files>.planning/phases/LEXCV-97-auditoria-de-milestone-d-vida-t-cnica-e-uat-pendente/97-UAT.md</files>
  <read_first>
    - .planning/milestones/v2.8-phases/LEXCV-75-componente-nico-view-edit/75-HUMAN-UAT.md
    - .planning/milestones/v2.8-phases/LEXCV-76-separadores-—-dados-contactos-e-notas/76-HUMAN-UAT.md
    - .planning/milestones/v2.9-phases/LEXCV-84-frontend-ui-intake-dados-sub-sec-es-documentos-termo-de-hono/84-HUMAN-UAT.md
    - .planning/milestones/v2.10-phases/LEXCV-85-consolida-o-da-l-gica-de-prazo-cr-tico/85-HUMAN-UAT.md
    - .planning/milestones/v2.10-phases/LEXCV-89-sino-e-p-gina-de-notifica-es/89-VERIFICATION.md (the WR-01/WR-02 static+behavioral confirmations already on record for scenarios 10/11)
    - web/src/app/(dashboard)/clientes/[id]/page.tsx (unified view/edit toggle + 7 tabs for phases 75/76)
    - web/src/app/(dashboard)/processos/[id]/page.tsx and termo-honorarios (for phase 84 items — grep for the specific tab/route)
    - web/src/components/shared/notification-bell.tsx (Phase 89 bell: badge, dropdown, polling, fused click, mark-all)
    - web/src/app/(dashboard)/notificacoes/page.tsx (Phase 89 page: layout, filters, standalone mark-one, pagination, RBAC gate)
    - web/src/lib/notificacao-categoria.ts (the isInternalLinkUrl same-origin guard, scenario 10)
    - backend/src/main/java/com/lexcv/controllers/ResourceController.java — grep for `isEventoCritico` (the Phase 85 KPI gap method + its docblock; do NOT rely on a hardcoded line number since it drifts — locate it fresh via grep every time)
  </read_first>
  <action>
    97-CONTEXT.md's locked decision prioritizes REAL verification over code re-reading, especially
    for simple flows. For Phase 75 (unified view/edit toggle, round-trip save, cancel-discards,
    CRUD gating, list links) and Phase 76 (7 tabs + default tab, validation-driven tab switch,
    dialog reset, mobile scroll) — the live-authenticated session at localhost:3000 already exists
    per 97-CONTEXT.md — ATTEMPT a live-observed check FIRST for each scenario (open the relevant
    page/route via the live frontend and read its actual rendered state/network calls). Only fall
    back to code-level verification for a specific scenario if live observation is genuinely
    impractical for this headless executor (no interactive click/type/drag driver available, or
    the check is inherently visual/print-layout judgment) — and when falling back, STATE
    explicitly in 97-UAT.md why the live attempt was skipped or insufficient for that scenario
    (e.g. "requires drag interaction not available to this agent" or "requires visual print-preview
    judgment"). For Phase 84 (intake Origem/Juízo, termo-honorários print gating, Partes/Fases
    dialog reset, Decisão→Documentos linkage, RBAC per-tab gating) apply the same live-first
    approach where the flow is a simple read/click-through, and code-verify (naming the file +
    function/handler/JSX) where it depends on rendered print-preview layout specifically.
    For each scenario write one of: `LIVE-VERIFIED` (observed against the running localhost:3000
    session, state what was seen), `CODE-VERIFIED` (behavior confirmed in source, name the file +
    function/handler/JSX), or `NEEDS-HUMAN-VISUAL` (both attempts insufficient — correctness
    depends on rendered layout/print preview/interactive drag a headless agent cannot judge; state
    so and request the user's browser walkthrough).

    For Phase 89 — ALL 11 scenarios from 89-HUMAN-UAT.md must receive an explicit verdict (do not
    leave any unassigned):
      1. Badge counter/cap/refocus (NOTF-08): grep `notification-bell.tsx` for the polling
         interval and `refetchOnWindowFocus` override → CODE-VERIFIED with the exact
         config values found (or LIVE-VERIFIED if the bell is observed live incrementing).
      2. Dropdown list + empty state (NOTF-09): grep the same file for the header text, the
         10-item slice, and the empty-state string → CODE-VERIFIED / LIVE-VERIFIED.
      3. Fused click = mark+navigate (NOTF-10, bell): grep the click handler wiring navigation +
         mark-as-read together → CODE-VERIFIED, cross-referenced with Task 1's PATCH .../lida
         API-level confirmation.
      4. Mark-all (NOTF-11, bell): grep the "Marcar todas" handler → CODE-VERIFIED,
         cross-referenced with Task 1's POST .../ler-todas confirmation.
      5. /notificacoes access + layout (NOTF-12): grep `notificacoes/page.tsx` for the h1, the
         absence of a sidebar/bottom-nav entry → CODE-VERIFIED / LIVE-VERIFIED (visit the route
         live).
      6. Category/read-state filters (NOTF-13): grep the filter select/chips wiring →
         CODE-VERIFIED, cross-referenced with Task 1's categoria/lida query-param confirmation.
      7. Standalone mark-one + pagination (NOTF-10/page): grep the page's per-row mark button and
         the Anterior/Seguinte pagination controls → CODE-VERIFIED / LIVE-VERIFIED.
      8. Cross-surface invalidation: grep for the shared `["notificacoes"]` query-key prefix used
         by both the bell and the page's TanStack Query hooks → CODE-VERIFIED for the wiring;
         mark the actual real-time cross-tab reflection as NEEDS-HUMAN-VISUAL (verifying two live
         surfaces update in sync requires an interactive two-panel browser session this executor
         cannot drive) — state this reason explicitly.
      9. RBAC gate `notificacoes:view` (AccessDeniedState): grep `notificacoes/page.tsx` for the
         permission check + `AccessDeniedState` usage → CODE-VERIFIED.
      10. Same-origin link-guard visual (WR-01 follow-up): 89-VERIFICATION.md already records 16/16
          behavioral test cases passing plus independent static analysis confirming
          `isInternalLinkUrl` is correct — grep `notificacao-categoria.ts` to reconfirm the guard
          is unchanged since that verification. Verdict: NEEDS-HUMAN-VISUAL, explicitly noting the
          underlying logic is already proven correct by prior tests and only the rendered
          non-clickable-text visual confirmation remains open (not a functional risk).
      11. Pagination-flash-fix visual (WR-02 follow-up): 89-VERIFICATION.md/`pnpm lint` already
          confirm the `react-hooks/set-state-in-effect` root cause is eliminated in this file.
          Verdict: NEEDS-HUMAN-VISUAL, same reasoning — the root cause is confirmed gone; only the
          frame-by-frame visual transition remains open.

    For Phase 85: the single item is an OPEN PRODUCT DECISION (should an ALTA-priority Evento with
    null dataFim count as urgent in `agendaUrgentesCount`/`prazosCriticosCount`?). Locate the
    current behavior by running `grep -n "isEventoCritico" backend/src/main/java/com/lexcv/controllers/ResourceController.java`
    (do not hardcode a line number in 97-UAT.md — cite the method name `isEventoCritico` and its
    preceding docblock, found via this grep, so the reference does not drift). Confirm the code
    currently EXCLUDES such events (the docblock states dataFim=null → risco="ok", not counted).
    Do NOT invent an answer; record it in 97-UAT.md as a carried-forward product decision for the
    user, quoting the docblock's actual text and citing `isEventoCritico()` by name (with today's
    grep-confirmed line number as a convenience reference, explicitly marked "line number as of
    this audit — grep isEventoCritico to relocate if it has moved").

    Finalize 97-UAT.md with a summary table: one row per phase (75/76/79/81/82/84/85/89), columns
    = total scenarios / live-verified / code-verified / needs-human-visual / open-with-reason,
    plus a one-line overall closure statement per phase. The Phase 89 row must show 11 total
    scenarios with all 11 accounted for across the verdict columns.
  </action>
  <verify>
    <automated>grep -c "Phase 75\|Phase 76\|Phase 79\|Phase 81\|Phase 82\|Phase 84\|Phase 85\|Phase 89" .planning/phases/LEXCV-97-auditoria-de-milestone-d-vida-t-cnica-e-uat-pendente/97-UAT.md</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: 97-UAT.md contains a section for all 8 phases and a summary table with per-phase scenario counts across the verdict categories.
    - Behavior assertion: each of the 8 phases' scenarios has a LIVE-VERIFIED, CODE-VERIFIED, NEEDS-HUMAN-VISUAL, or OPEN-WITH-REASON verdict; no scenario left as a bare "pending".
    - Behavior assertion: all 11 Phase 89 scenarios (from 89-HUMAN-UAT.md) each have an individually-stated verdict in 97-UAT.md — the Phase 89 section is not summarized as a single blob.
    - Behavior assertion: for Phase 75/76, a live-observation attempt is recorded as the first attempt for each scenario, with code-verification used only as a stated fallback where live observation was impractical (with the specific reason named).
    - Source assertion: the Phase 85 KPI item is recorded as an open product decision, citing `isEventoCritico()` by name (located via grep, not a hardcoded line number treated as ground truth) and quoting its docblock's stated current behavior.
  </acceptance_criteria>
  <done>All UI-heavy UAT scenarios (75/76/84/89, all 11 of Phase 89 individually) have a documented verdict with live-observation attempted first where practical, the Phase 85 decision is captured for the user via a grep-verified source reference, and 97-UAT.md is a complete, self-contained closure record.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| executor → live backend | Executor authenticates as admin via login; credentials are the seed default and may be rejected — no secret is ever hard-required |
| test data → production data | Verification must not delete or corrupt real tenant data; only throwaway rows may be created and are left in place |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-97-03-01 | Repudiation | UAT closure record | mitigate | Every scenario (all ~38, including all 11 of Phase 89) gets an explicit, attributable verdict in 97-UAT.md — no silent "pending" |
| T-97-03-02 | Tampering | live dev data | mitigate | Reuse existing test data; create only throwaway rows; never delete existing rows (Task 1/2) |
| T-97-03-03 | Information Disclosure | admin credentials | accept | Claude never receives the password; login uses seed default and falls back to code-audit if rejected |
| T-97-03-04 | Repudiation | Phase 85 source citation | mitigate | Cite `isEventoCritico()` by name via fresh grep, not a hardcoded line number, to prevent planting a stale/incorrect citation into the evidence record (Task 2) |
| T-97-03-SC | Tampering | package installs | accept | No npm/pip/cargo installs — verification uses curl + source reads only |
</threat_model>

<verification>
- 97-UAT.md exists and covers all 8 phases with a summary table, including all 11 Phase 89 scenarios individually.
- At least the backend-verifiable phases (81/82/79/89) show a concrete HTTP-status verdict or a
  BLOCKED-with-reason note; Phase 81 #5 and Phase 82 #4 show OPEN-WITH-REASON.
- No existing live test data was deleted (Task 1/2 only add throwaway rows).
- The Phase 85 citation in 97-UAT.md references `isEventoCritico()` located via grep, not a stale hardcoded line range.
</verification>

<success_criteria>
AUD-02 satisfied: every pending UAT scenario across phases 75, 76, 79, 81, 82, 84, 85, 89 —
including all 11 of Phase 89 individually — is closed with real evidence where practical (live
observation attempted first for simple UI flows), and each remaining-open item has an explicit
recorded reason — captured in a single 97-UAT.md the closeout plan can act on.
</success_criteria>

<output>
Create `.planning/phases/LEXCV-97-auditoria-de-milestone-d-vida-t-cnica-e-uat-pendente/97-UAT.md`
(the closure record) and `.planning/phases/LEXCV-97-auditoria-de-milestone-d-vida-t-cnica-e-uat-pendente/97-03-SUMMARY.md` when done.
</output>
