---
phase: 97-auditoria-de-milestone
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/main/java/com/lexcv/services/NotificacaoService.java
  - backend/src/main/java/com/lexcv/repositories/NotificacaoRepository.java
  - backend/src/main/java/com/lexcv/repositories/NotificacaoPreferenciaRepository.java
  - backend/src/main/java/com/lexcv/controllers/NotificacaoController.java
autonomous: true
requirements: [AUD-01]

must_haves:
  truths:
    - "Every NotificacaoPreferencia read/write query filters by tenant_id AND user_id (never user_id alone)"
    - "resolverEquipaCliente resolves the client team exclusively through tenant-scoped junction queries (findByClienteIdAndTenantId)"
    - "Every snoozedUntil-touching query (unread-count, mark-all-read, snooze mutation) filters by tenant_id AND destinatario_id"
    - "Any tenant-isolation gap discovered in Phase 93/95/96 surfaces is fixed inside this plan, not just noted"
  artifacts:
    - path: "backend/src/main/java/com/lexcv/repositories/NotificacaoPreferenciaRepository.java"
      provides: "Dual-scoped (tenant+user) preference queries"
      contains: "TenantIdAndUserId"
    - path: "backend/src/main/java/com/lexcv/services/NotificacaoService.java"
      provides: "Tenant-scoped team resolution + snooze mutation"
      contains: "findByClienteIdAndTenantId"
    - path: "backend/src/main/java/com/lexcv/repositories/NotificacaoRepository.java"
      provides: "Tenant+destinatario scoped snooze-visibility queries"
      contains: "tenantId"
  key_links:
    - from: "NotificacaoService.resolverEquipaCliente"
      to: "ClienteAdvogadoRepository.findByClienteIdAndTenantId / ClienteAdministrativoRepository.findByClienteIdAndTenantId"
      via: "tenant-scoped junction lookup"
      pattern: "findByClienteIdAndTenantId"
    - from: "NotificacaoService.snooze"
      to: "NotificacaoRepository.findByIdAndTenantIdAndDestinatarioId"
      via: "tenant+recipient scoped fetch before mutation"
      pattern: "findByIdAndTenantIdAndDestinatarioId"
---

<objective>
Final cross-cutting confirmation that every NEW or CHANGED query introduced by this milestone's
notification surfaces filters by `tenant_id` — not merely by `user_id`, `cliente_id`, or
`destinatario_id`. The three surfaces in scope are: `NotificacaoPreferencia` (Phase 93 mute
preferences), `resolverEquipaCliente` / team fan-out (Phase 95), and the `snoozedUntil`
visibility + mutation queries (Phase 96).

These were each reviewed during their own phase code-review, so this is a confirmation pass, not
fresh discovery. The value is a single documented tenant-isolation verdict across all three
before milestone close. If any query is found to drop `tenant_id`, fix it here.

Purpose: Satisfy AUD-01 — tenant isolation is the platform's primary data-isolation boundary
(CLAUDE.md: "must not be bypassed"), and these are the milestone's newest data surfaces.
Output: A confirmed tenant-scoping verdict recorded in this plan's SUMMARY, plus any fix commits.
</objective>

<execution_context>
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/LEXCV-97-auditoria-de-milestone-d-vida-t-cnica-e-uat-pendente/97-CONTEXT.md

<interfaces>
<!-- Tenant-scoping contracts the auditor must confirm are honored everywhere. Extracted from codebase. -->

From NotificacaoPreferenciaRepository.java (Phase 93 — MUST all be dual-scoped tenant+user):
- boolean existsByTenantIdAndUserIdAndCategoria(UUID tenantId, UUID userId, String categoria)
- List<NotificacaoPreferencia> findByTenantIdAndUserId(UUID tenantId, UUID userId)
- void deleteByTenantIdAndUserIdAndCategoria(UUID tenantId, UUID userId, String categoria)
- void upsertSilenciar(...) — native INSERT ... ON CONFLICT (tenant_id, user_id, categoria) DO NOTHING

From NotificacaoService.java (Phase 95 team resolution):
- LinkedHashSet<UUID> resolverEquipaCliente(UUID tenantId, UUID clienteId)
  → iterates clienteAdvogadoRepository.findByClienteIdAndTenantId(clienteId, tenantId)
    and clienteAdministrativoRepository.findByClienteIdAndTenantId(clienteId, tenantId)

From ClienteAdvogadoRepository.java / ClienteAdministrativoRepository.java:
- List<ClienteAdvogado> findByClienteIdAndTenantId(UUID clienteId, UUID tenantId)  // tenant-scoped
- List<ClienteAdministrativo> findByClienteIdAndTenantId(UUID clienteId, UUID tenantId)  // tenant-scoped

From NotificacaoRepository.java (Phase 96 snooze — MUST all carry tenant_id + destinatario_id):
- long countByTenantIdAndDestinatarioIdAndLidaFalse(tenantId, destinatarioId, agora)  // JPQL + snoozedUntil predicate
- List<Notificacao> findByTenantIdAndDestinatarioIdAndLidaFalse(tenantId, destinatarioId, agora)  // JPQL + snoozedUntil predicate
- Optional<Notificacao> findByIdAndTenantIdAndDestinatarioId(id, tenantId, destinatarioId)  // backs snooze() + marcarLida()
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit Phase 93 preference surface for tenant+user dual-scoping</name>
  <files>backend/src/main/java/com/lexcv/repositories/NotificacaoPreferenciaRepository.java, backend/src/main/java/com/lexcv/services/NotificacaoService.java, backend/src/main/java/com/lexcv/controllers/NotificacaoController.java</files>
  <read_first>
    - backend/src/main/java/com/lexcv/models/NotificacaoPreferencia.java (entity — confirm tenant_id + user_id columns and the unique key)
    - backend/src/main/java/com/lexcv/repositories/NotificacaoPreferenciaRepository.java (all four query methods)
    - backend/src/main/java/com/lexcv/services/NotificacaoService.java (methods: the criar() mute guard, silenciarCategoria, reativarCategoria, listarCategoriasSilenciadas)
    - backend/src/main/java/com/lexcv/controllers/NotificacaoController.java (the GET/PUT/DELETE /notificacoes/preferencias endpoints — confirm they pass getTenantId() into every service call)
  </read_first>
  <action>
    Trace every read and write to `t_notificacao_preferencia`. Confirm each of the four
    repository methods (`existsByTenantIdAndUserIdAndCategoria`, `findByTenantIdAndUserId`,
    `deleteByTenantIdAndUserIdAndCategoria`, `upsertSilenciar`) carries BOTH `tenant_id` and
    `user_id`, and that the native `upsertSilenciar` ON CONFLICT target is
    `(tenant_id, user_id, categoria)`. Confirm the mute guard inside `NotificacaoService.criar()`
    keys off the destinatário's tenant + user, so silencing cannot leak across tenants, and that
    `NotificacaoController` derives `tenant_id` from `UserPrincipal.getTenantId()` (never from a
    request param/body) for every preferences endpoint. If ANY query is scoped by `user_id`
    alone (no `tenant_id`), fix it by adding the `TenantId` predicate to the derived-query name
    or the JPQL/native WHERE clause and repoint callers to pass `getTenantId()`. Record the
    per-method verdict (COVERED / FIXED) in the SUMMARY. Do not touch Phase 95/96 surfaces here.
  </action>
  <verify>
    <automated>cd backend && grep -c "TenantIdAndUserId\|tenant_id, user_id, categoria" src/main/java/com/lexcv/repositories/NotificacaoPreferenciaRepository.java</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: every method in NotificacaoPreferenciaRepository.java names both TenantId and UserId (or the native query filters both columns); zero methods scope by user_id alone.
    - Source assertion: NotificacaoController preferences endpoints pass a tenant id sourced from UserPrincipal.getTenantId(), never from @RequestParam/@RequestBody.
    - Behavior assertion: SUMMARY records a COVERED/FIXED verdict for each of the four preference queries and the criar() mute guard.
    - Test-command: `mvn -q -DskipTests package` (backend) succeeds if any fix was applied.
  </acceptance_criteria>
  <done>All Phase 93 preference reads/writes proven dual-scoped (tenant+user), or fixed to be; verdict recorded per method.</done>
</task>

<task type="auto">
  <name>Task 2: Audit Phase 95 team resolution and Phase 96 snooze for tenant scoping</name>
  <files>backend/src/main/java/com/lexcv/services/NotificacaoService.java, backend/src/main/java/com/lexcv/repositories/NotificacaoRepository.java</files>
  <read_first>
    - backend/src/main/java/com/lexcv/services/NotificacaoService.java (resolverEquipaCliente, notificarFaseEntrada, notificarProcessoAtribuido, notificarDocumentoNovo, criarComFanOutAdmin, snooze, marcarTodasLidas)
    - backend/src/main/java/com/lexcv/repositories/ClienteAdvogadoRepository.java and ClienteAdministrativoRepository.java (confirm findByClienteIdAndTenantId exists and filters tenant_id)
    - backend/src/main/java/com/lexcv/repositories/NotificacaoRepository.java (countByTenantIdAndDestinatarioIdAndLidaFalse, findByTenantIdAndDestinatarioIdAndLidaFalse, findByIdAndTenantIdAndDestinatarioId)
    - backend/src/main/java/com/lexcv/controllers/ResourceController.java (the 4 notificar* call sites — confirm each passes getTenantId() and the process's clienteId, not a raw request value)
  </read_first>
  <action>
    For Phase 95: confirm `resolverEquipaCliente(tenantId, clienteId)` resolves team members
    ONLY via `clienteAdvogadoRepository.findByClienteIdAndTenantId` and
    `clienteAdministrativoRepository.findByClienteIdAndTenantId` — both tenant-scoped — and never
    via a `findByClienteId`-only variant. Confirm the 4 ResourceController call sites feed
    `getTenantId()` (from the security context) and the process-derived `clienteId`. For Phase 96:
    confirm the snoozedUntil-aware count/mark-all-read queries in NotificacaoRepository both filter
    `n.tenantId = :tenantId AND n.destinatarioId = :destinatarioId`, and that
    `NotificacaoService.snooze` fetches via `findByIdAndTenantIdAndDestinatarioId` before mutating
    (so a snooze cannot touch another tenant's or recipient's row). If any junction lookup or
    snooze query drops `tenant_id`, fix it and repoint callers. Record per-surface verdict in the
    SUMMARY. As a documented option (Claude's discretion per 97-CONTEXT.md), the live cross-tenant
    probe requiring a second seeded tenant MAY be skipped — if skipped, state in the SUMMARY that
    the code audit gives sufficient confidence and why; if performed, curl the preferences/snooze
    endpoints as two users in different tenants against localhost:8080 and record that neither
    sees the other's rows.
  </action>
  <verify>
    <automated>cd backend && grep -c "findByClienteIdAndTenantId" src/main/java/com/lexcv/services/NotificacaoService.java && grep -c "n.tenantId = :tenantId AND n.destinatarioId = :destinatarioId" src/main/java/com/lexcv/repositories/NotificacaoRepository.java</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: resolverEquipaCliente references only findByClienteIdAndTenantId (both junction repos); no findByClienteId-only call exists on those repos.
    - Source assertion: both snoozedUntil-aware queries in NotificacaoRepository.java filter tenantId AND destinatarioId; snooze() fetches via findByIdAndTenantIdAndDestinatarioId.
    - Behavior assertion: SUMMARY records a COVERED/FIXED verdict for team resolution and for snooze visibility+mutation, and an explicit statement on whether the live cross-tenant probe was run (with reason if skipped).
    - Test-command: `mvn -q -DskipTests package` (backend) succeeds if any fix was applied.
  </acceptance_criteria>
  <done>Phase 95 team resolution and Phase 96 snooze queries proven tenant-scoped (or fixed); AUD-01 verdict complete and recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authenticated user → notification API | Every request must be scoped to the caller's tenant derived server-side from the JWT, never from client-supplied ids |
| tenant A ↔ tenant B | Notification preferences, team membership, and snoozed rows must never cross the tenant boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-97-01-01 | Information Disclosure | NotificacaoPreferencia queries | mitigate | Confirm/enforce tenant_id + user_id on all 4 repo methods (Task 1) |
| T-97-01-02 | Information Disclosure | resolverEquipaCliente / junction repos | mitigate | Confirm/enforce findByClienteIdAndTenantId tenant scoping (Task 2) |
| T-97-01-03 | Tampering | NotificacaoService.snooze mutation | mitigate | Confirm fetch via findByIdAndTenantIdAndDestinatarioId before mutating (Task 2) |
| T-97-01-04 | Elevation of Privilege | NotificacaoController tenant source | mitigate | Confirm tenant id comes from UserPrincipal.getTenantId(), not request input (Task 1) |
| T-97-01-SC | Tampering | package installs | accept | No npm/pip/cargo installs in this plan — audit-only, no new dependencies |
</threat_model>

<verification>
- `mvn -q -DskipTests package` compiles cleanly (only relevant if a fix was applied).
- If any tenant-scoping fix was made to NotificacaoService/NotificacaoRepository, run the
  existing `NotificacaoServiceTest` (`mvn -q test -Dtest=NotificacaoServiceTest`) to confirm no
  regression in the mute/fan-out/team logic.
- SUMMARY contains an explicit per-surface COVERED/FIXED verdict for Phase 93, 95, and 96 queries.
</verification>

<success_criteria>
AUD-01 satisfied: the preference table (Phase 93), team resolution (Phase 95), and snoozedUntil
queries (Phase 96) are each confirmed to filter explicitly by tenant_id across every new query of
this milestone — with any exception fixed in this plan and the verdict documented for the
milestone-closeout consolidation (Plan 97-04) to read.
</success_criteria>

<output>
Create `.planning/phases/LEXCV-97-auditoria-de-milestone-d-vida-t-cnica-e-uat-pendente/97-01-SUMMARY.md` when done, including the per-surface tenant-scoping verdict.
</output>
