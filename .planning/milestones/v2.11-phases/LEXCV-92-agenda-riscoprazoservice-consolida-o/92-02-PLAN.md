---
phase: 92-agenda-riscoprazoservice-consolida-o
plan: 02
type: execute
wave: 2
depends_on: [92-01]
files_modified:
  - web/src/types/eventos.ts
  - web/src/hooks/use-eventos.ts
  - web/src/app/(dashboard)/agenda/page.tsx
autonomous: false
requirements: [AGD-34, AGD-35]

must_haves:
  truths:
    - "A Agenda deixa de calcular o seu próprio veredito de prazo crítico: o contador 'Urgentes' passa a derivar do campo risco do backend, não de prioridade === ALTA"
    - "Prazos e Eventos na Agenda transportam o campo risco calculado pelo backend através de allUnifiedEvents (o de Prazos deixa de ser descartado; o de Eventos é o campo novo do plano 92-01)"
    - "Não resta nenhum cálculo de risco/prazo-crítico do lado do cliente no frontend"
    - "O hook morto useUpcomingEventos e o tipo UpcomingEvento são removidos"
  artifacts:
    - path: "web/src/types/eventos.ts"
      provides: "Campo risco no interface Evento; interface UpcomingEvento removida"
      contains: "risco?: PrazoRisco"
    - path: "web/src/hooks/use-eventos.ts"
      provides: "useUpcomingEventos e invalidações órfãs de ['eventos','upcoming'] removidas"
      contains: "useEventos"
    - path: "web/src/app/(dashboard)/agenda/page.tsx"
      provides: "allUnifiedEvents transporta risco; weekStats.urgentes usa risco do backend"
      contains: "risco: p.risco"
  key_links:
    - from: "web/src/app/(dashboard)/agenda/page.tsx weekStats.urgentes"
      to: "campo risco do backend (GET /eventos e GET /prazos)"
      via: "filtro risco === proximo || risco === vencido sobre eventos ativos"
      pattern: "risco === \"proximo\""
    - from: "allUnifiedEvents (mapeamento pzs)"
      to: "Prazo.risco"
      via: "cópia explícita risco: p.risco no objeto unificado"
      pattern: "risco: p\\.risco"
---

<objective>
Fazer `agenda/page.tsx` confiar inteiramente no `RiscoPrazoService` (via campo `risco` do backend) tanto para Prazos como para Eventos, eliminando o último veredito de "prazo crítico" calculado no cliente (o proxy grosseiro `prioridade === "ALTA"` do contador "Urgentes"), e limpar o hook/tipo mortos `useUpcomingEventos`/`UpcomingEvento` cujo endpoint foi removido no plano 92-01 (AGD-34, AGD-35).

Purpose: fechar a "5ª implementação divergente de prazo crítico" — a Agenda passa a concordar com o Dashboard e o job diário sobre o que é "próximo"/"vencido" para os mesmos dados, sem introduzir novo trabalho de UI (os campos já são consumidos; só se corrige a fonte da verdade).
Output: `allUnifiedEvents` transporta `risco`; `weekStats.urgentes` baseado em `risco`; `useUpcomingEventos`/`UpcomingEvento` eliminados.
</objective>

<execution_context>
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/LEXCV-92-agenda-riscoprazoservice-consolida-o/92-CONTEXT.md
@.planning/research/PITFALLS.md
@.planning/phases/LEXCV-92-agenda-riscoprazoservice-consolida-o/92-01-SUMMARY.md
@web/AGENTS.md

<interfaces>
<!-- Contrato do backend após o plano 92-01: GET /eventos devolve `risco` por evento. -->
GET /api/v1/eventos            -> Evento[]  (cada Evento agora tem risco?: "ok"|"proximo"|"vencido")
GET /api/v1/prazos             -> Prazo[]   (Prazo.risco já existe hoje, tipo PrazoRisco não-opcional)

<!-- Tipos existentes -->
// web/src/types/processos.ts:11  export type PrazoRisco = "ok" | "proximo" | "vencido";
// web/src/types/processos.ts:267 Prazo.risco: PrazoRisco  (não-opcional)
// web/src/types/eventos.ts       interface Evento  (SEM risco hoje — adicionar); interface UpcomingEvento (REMOVER)

<!-- Helpers partilhados já existentes (NÃO reinventar mapeamento de cor/label) -->
// web/src/lib/prazos.ts  prazosRiscoToVariant(risco): "green"|"amber"|"red";  prazosRiscoToLabel(risco): string

<!-- agenda/page.tsx — pontos exatos a alterar -->
// linhas 85-108 allUnifiedEvents: mapeia eventos (evs, faz spread {...e}) + prazos (pzs, objeto literal que HOJE descarta p.risco)
// linha 166 weekStats.urgentes = active.filter((e) => e.prioridade === "ALTA").length   <-- proxy a substituir
// linha 568 getCategoria(e): categoriza por tipo/título (PRAZO/AUDIENCIA/DILIGENCIA/REUNIAO) — conceito SEPARADO de risco, NÃO alterar
// NOTA: existe uma const LOCAL `upcoming` (linha 142) não relacionada com o endpoint — NÃO renomear nem remover

<!-- use-eventos.ts — pontos exatos a remover -->
// linha 5   import { ..., UpcomingEvento } from "@/types/eventos"  -> remover UpcomingEvento do import
// linhas 104,123,141,159  queryClient.invalidateQueries({ queryKey: ["eventos", "upcoming"] })  -> remover (chave órfã)
// linhas 165-172  export function useUpcomingEventos(...)  -> remover função inteira
// useUpcomingEventos NÃO tem call sites (grep-confirmado) — é código morto
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Tipo Evento.risco + remoção do hook/tipo mortos de upcoming</name>
  <files>web/src/types/eventos.ts, web/src/hooks/use-eventos.ts</files>
  <read_first>
    - web/src/types/eventos.ts (ficheiro inteiro — interface Evento nas linhas 3-19, interface UpcomingEvento nas linhas 47-53)
    - web/src/hooks/use-eventos.ts (ficheiro inteiro — import linha 5, invalidações órfãs 104/123/141/159, useUpcomingEventos 165-172)
    - web/src/types/processos.ts linha 11 (export type PrazoRisco)
  </read_first>
  <action>
    (AGD-35 — limpeza do endpoint legado + contrato do campo risco no frontend)

    1. Em `web/src/types/eventos.ts`:
       - Adicionar `import type { PrazoRisco } from "@/types/processos";` no topo.
       - Adicionar `risco?: PrazoRisco;` ao interface `Evento` (opcional, pois nem todos os endpoints que devolvem `Evento` o preenchem — p.ex. `GET /eventos/{id}`; só `GET /eventos` o preenche a partir do plano 92-01). Isto espelha a discrição de 92-CONTEXT.md ("replicar o padrão já usado para o campo risco de Prazos").
       - Remover por completo o interface `UpcomingEvento` (linhas 47-53) — o seu endpoint `GET /eventos/upcoming` foi eliminado no plano 92-01.

    2. Em `web/src/hooks/use-eventos.ts`:
       - Remover `UpcomingEvento` da lista de imports da linha 5 (manter `Evento, EventoCreateRequest, EventoUpdateRequest`).
       - Remover a função exportada `useUpcomingEventos` inteira (linhas ~165-172) — código morto, sem call sites.
       - Remover as quatro linhas `queryClient.invalidateQueries({ queryKey: ["eventos", "upcoming"] })` em `useToggleEventoConcluido` (~104), `useSetEventoConcluido` (~123), `useDeleteEvento` (~141) e `useDeleteEventoInstance` (~159) — a chave `["eventos","upcoming"]` fica órfã após a remoção do hook. Manter as restantes invalidações (`["eventos","list"]`, `setQueryData` de detail) intactas.

    Não alterar mais nenhum hook. Não tocar em `useEventos`/`useEvento`/mutações além da remoção das invalidações órfãs.
  </action>
  <verify>
    <automated>cd web && node -e "const fs=require('fs'); const strip=s=>s.split('\n').filter(l=>!l.trim().startsWith('//')).join('\n'); const t=strip(fs.readFileSync('src/types/eventos.ts','utf8')); const h=strip(fs.readFileSync('src/hooks/use-eventos.ts','utf8')); const ok=/risco\?:\s*PrazoRisco/.test(t) && /import type \{ PrazoRisco \}/.test(t) && !/interface UpcomingEvento/.test(t) && !/useUpcomingEventos/.test(h) && !/UpcomingEvento/.test(h) && !/\[\"eventos\", \"upcoming\"\]/.test(h) && !/eventos\/upcoming/.test(h); process.exit(ok?0:1)"</automated>
    <automated>cd web && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `Evento` tem `risco?: PrazoRisco` e importa `PrazoRisco`; `UpcomingEvento` já não existe em eventos.ts
    - `useUpcomingEventos`, o import `UpcomingEvento` e as 4 invalidações `["eventos","upcoming"]` já não existem em use-eventos.ts
    - `cd web && npx tsc --noEmit` passa
  </acceptance_criteria>
  <done>Contrato de tipo do risco de Evento no sítio; hook/tipo mortos de upcoming removidos; typecheck limpo.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Agenda usa risco do backend para Prazos e Eventos; remove o proxy prioridade === ALTA</name>
  <files>web/src/app/(dashboard)/agenda/page.tsx</files>
  <read_first>
    - web/src/app/(dashboard)/agenda/page.tsx linhas 85-108 (allUnifiedEvents — evs faz spread {...e}; pzs é objeto literal que descarta p.risco)
    - web/src/app/(dashboard)/agenda/page.tsx linhas 153-169 (weekStats — urgentes na linha 166)
    - web/src/app/(dashboard)/agenda/page.tsx linhas 568-620 (getCategoria — NÃO alterar)
    - web/src/lib/prazos.ts (helpers partilhados; contexto, não obrigatório usar nesta tarefa)
    - .planning/phases/LEXCV-92-agenda-riscoprazoservice-consolida-o/92-CONTEXT.md secção decisions
  </read_first>
  <action>
    (AGD-34) Fazer a Agenda confiar no `risco` do backend e apagar o cálculo de risco do cliente. Alterações mínimas e cirúrgicas — sem novos componentes, layouts, estados visuais ou badges (deep_work_rules: nenhum trabalho novo de UI).

    1. No mapeamento `pzs` de `allUnifiedEvents` (linhas 95-106), adicionar `risco: p.risco,` ao objeto literal unificado do Prazo. Hoje o campo é silenciosamente descartado; `p.risco` já existe em `Prazo` (tipo `PrazoRisco` não-opcional).

    2. No mapeamento `evs` (linhas 86-94): o spread `{ ...e }` (em ambos os ramos — override otimista e normal) já transporta `e.risco` automaticamente assim que o tipo `Evento` passa a ter `risco` (Task 1). Nenhuma alteração adicional ao `evs` além de confiar nesse spread. NÃO remover campos existentes.

    3. Em `weekStats` (linhas 153-169), substituir a linha 166
       `const urgentes = active.filter((e) => e.prioridade === "ALTA").length;`
       por um contador baseado no risco do backend:
       `const urgentes = active.filter((e) => e.risco === "proximo" || e.risco === "vencido").length;`
       Isto remove o último proxy cego a datas (`prioridade === "ALTA"` contava um prazo ALTA a 60 dias como urgente e ignorava um MÉDIA para amanhã). "Urgentes" passa a significar "sinalizado pelo RiscoPrazoService como próximo ou vencido" — decisão de produto registada aqui (a alternativa `vencido`-apenas foi rejeitada porque itens a aproximar-se dentro da janela de 7/3 dias são precisamente o que "urgente" deve capturar numa vista semanal). Manter `prazosAtivos` e `audiencias` inalterados (continuam a usar `getCategoria`).

    4. NÃO alterar `getCategoria` (linha 568): categorizar por tipo/título (PRAZO/AUDIENCIA/DILIGENCIA/REUNIAO) é um conceito distinto de risco e permanece. NÃO adicionar badges de risco novos nas pills do calendário nem noutro sítio (fora de âmbito por deep_work_rules). NÃO renomear a const local `upcoming` (linha 142).

    Após esta tarefa, nenhum cálculo de "prazo crítico"/risco deve restar no frontend — a única aritmética de risco vive no backend (`RiscoPrazoService`).
  </action>
  <verify>
    <automated>cd web && node -e "const fs=require('fs'); const strip=s=>s.split('\n').filter(l=>!l.trim().startsWith('//')).join('\n'); const s=strip(fs.readFileSync('src/app/(dashboard)/agenda/page.tsx','utf8')); const ok=/risco: p\.risco/.test(s) && /e\.risco === \"proximo\"/.test(s) && /e\.risco === \"vencido\"/.test(s) && !/prioridade === \"ALTA\"/.test(s); process.exit(ok?0:1)"</automated>
    <automated>cd web && npx tsc --noEmit</automated>
    <automated>cd web && pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - `allUnifiedEvents` transporta `risco` para Prazos (`risco: p.risco`) e Eventos (via spread `{...e}`)
    - `weekStats.urgentes` usa `e.risco === "proximo" || e.risco === "vencido"`; nenhuma ocorrência de `prioridade === "ALTA"` resta no ficheiro
    - `getCategoria` inalterado; sem badges/componentes/estados visuais novos
    - `cd web && npx tsc --noEmit` passa e `cd web && pnpm build` compila
  </acceptance_criteria>
  <done>Agenda deriva o risco de Prazos e Eventos do backend; o proxy prioridade===ALTA foi eliminado; nenhum cálculo de risco resta no cliente.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verificação visual da consolidação de risco na Agenda</name>
  <files>web/src/app/(dashboard)/agenda/page.tsx</files>
  <action>
    Checkpoint humano (sem escrita de código): confirmar visualmente que a Agenda passou a derivar o risco do backend. Não implementar nada nesta tarefa — as Tasks 1 e 2 já entregaram as alterações; esta tarefa pausa para verificação humana antes de fechar a fase. Executar os passos de `<how-to-verify>` e aguardar o sinal de retoma.
  </action>
  <what-built>
    A Agenda (`/agenda`) passou a usar o campo `risco` calculado pelo backend (RiscoPrazoService) tanto para Prazos como para Eventos. O contador "Urgentes" no cartão "Visão Geral da Semana" deixou de usar o proxy `prioridade === "ALTA"` e passa a contar itens com risco `proximo` ou `vencido`. O endpoint órfão `/eventos/upcoming` e o hook morto `useUpcomingEventos` foram removidos.
  </what-built>
  <how-to-verify>
    1. Arrancar backend (`cd backend && mvn spring-boot:run`, com `backend/.env` e Postgres acessível) e frontend (`cd web && pnpm dev`).
    2. Autenticar (admin@lexcv.cv / admin123) e abrir http://localhost:3000/agenda.
    3. Confirmar que o calendário e a lista carregam sem erros de consola e que Prazos/Eventos aparecem normalmente.
    4. No cartão escuro "Visão Geral da Semana" (lado direito), verificar o número "Urgentes". Este valor MUDOU deliberadamente face ao anterior: agora reflete proximidade real do prazo (próximo/vencido dentro dos limiares 7 dias ALTA / 3 dias restante), não apenas prioridade ALTA. Um prazo ALTA distante já não conta; um prazo próximo de prioridade média agora conta. Isto é uma correção de correção, não uma regressão.
    5. (Opcional, cross-check) Comparar "prazos a vencer/críticos" no Dashboard para os mesmos dados — devem concordar quanto ao que é próximo/vencido (nota: uma pequena divergência de contagem entre Agenda e KPI do Dashboard pode existir por o KPI usar data-fim vs. a Agenda usar data-início — discrepância pré-existente já documentada para auditoria da Phase 97, fora do âmbito desta fase).
    6. Confirmar na aba Network que não há chamadas a `/eventos/upcoming`.
  </how-to-verify>
  <verify>
    <human-check>Agenda carrega sem erros; "Urgentes" reflete risco por proximidade (próximo/vencido), não prioridade ALTA; sem chamadas de rede a /eventos/upcoming.</human-check>
  </verify>
  <resume-signal>Escrever "approved" se a Agenda carrega e a contagem "Urgentes" reflete risco por proximidade; caso contrário, descrever o problema observado.</resume-signal>
  <done>Verificação humana confirmada: Agenda usa risco do backend e o contador Urgentes reflete proximidade real; sem regressões visíveis.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| resposta da API → render no browser | O frontend passa a confiar no `risco` calculado pelo backend em vez de o recomputar |
| autoridade de risco | A verdade de risco reside exclusivamente no backend (RiscoPrazoService); o cliente não recalcula |

## STRIDE Threat Register (ASVS L1 — security_block_on: high)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-92-04 | Tampering (integridade de lógica) | veredito de risco calculado no cliente | mitigate | Remover o proxy `prioridade === "ALTA"` elimina uma segunda implementação divergente de risco; a Agenda passa a refletir a única fonte de verdade do backend, reduzindo o risco de deriva silenciosa entre superfícies. |
| T-92-05 | Information Disclosure | campo `risco` renderizado | accept | `risco` é um enum de baixa sensibilidade (ok/proximo/vencido) derivado de dados já devolvidos e já visíveis ao utilizador autenticado; não expõe PII nem novos dados. |
| T-92-SC | Tampering | npm/pnpm installs | mitigate | Sem novas dependências nesta fase (reutiliza TanStack Query, tipos e helpers existentes). Sem tarefas de instalação; gate de legitimidade N/A. |
</threat_model>

<verification>
- `cd web && npx tsc --noEmit` passa com os três ficheiros alterados
- `cd web && pnpm build` compila
- `cd web && pnpm lint` não reporta NOVOS problemas referentes aos ficheiros tocados (baseline pré-existente inalterada)
- Grep confirma zero `prioridade === "ALTA"`, zero `useUpcomingEventos`/`UpcomingEvento` e zero `eventos/upcoming` em `web/src`
- Checkpoint humano: Agenda carrega; "Urgentes" reflete risco por proximidade; sem chamadas a `/eventos/upcoming`
</verification>

<success_criteria>
1. `agenda/page.tsx` usa o `risco` do backend para Prazos em vez de recalcular veredito próprio (AGD-34)
2. Eventos na Agenda usam o `risco` novo de `GET /eventos` (do plano 92-01), transportado por `allUnifiedEvents` (AGD-34/AGD-35)
3. Prazos e Eventos na Agenda concordam com Dashboard/job diário sobre "próximo"/"vencido" (mesma fonte RiscoPrazoService)
4. Nenhuma 5ª implementação divergente de "prazo crítico" resta no frontend; hook/tipo mortos de upcoming removidos
</success_criteria>

<output>
Create `.planning/phases/LEXCV-92-agenda-riscoprazoservice-consolida-o/92-02-SUMMARY.md` when done
</output>
