---
phase: 40-mapeamento-e-alinhamento-casing-agenda
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/types/eventos.ts
  - web/src/schemas/eventos.ts
  - web/src/hooks/use-eventos.ts
  - web/src/app/(dashboard)/agenda/page.tsx
  - web/src/app/(dashboard)/agenda/[id]/page.tsx
  - web/src/app/(dashboard)/agenda/[id]/editar/page.tsx
  - web/src/app/(dashboard)/agenda/novo/page.tsx
autonomous: true
requirements:
  - AGD-33-01

must_haves:
  truths:
    - "TypeScript interfaces for Evento and Evento requests use camelCase"
    - "Zod validation schemas use camelCase for form and filter values"
    - "TanStack Query hooks read and write camelCase properties to the REST API"
    - "Date inputs strip timezone offset details and serialize as YYYY-MM-DDTHH:mm:ss"
    - "All Agenda pages render and submit data using camelCase"
  artifacts:
    - path: "web/src/types/eventos.ts"
      provides: "camelCase Evento types"
      contains: "dataInicio"
    - path: "web/src/schemas/eventos.ts"
      provides: "camelCase Zod schemas"
      contains: "dataInicio"
  key_links:
    - from: "web/src/app/(dashboard)/agenda/novo/page.tsx"
      to: "web/src/hooks/use-eventos.ts"
      via: "useCreateEvento"
      pattern: "useCreateEvento"
---

<objective>
Refactor the frontend Agenda data layer and pages to use camelCase naming conventions, matching the Spring Boot backend default Jackson serialization, and format date payloads as local ISO strings without timezone offsets.
</objective>

<execution_context>
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/LEXCV-40-mapeamento-e-alinhamento-casing-agenda/40-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refatorar tipos e interfaces da Agenda para camelCase</name>
  <files>web/src/types/eventos.ts</files>
  <read_first>
    - web/src/types/eventos.ts
  </read_first>
  <action>
Modify `web/src/types/eventos.ts` to replace all snake_case properties with camelCase:
- `tenant_id` -> `tenantId`
- `processo_id` -> `processoId`
- `data_inicio` -> `dataInicio`
- `data_fim` -> `dataFim`
Keep all other types (EventoPrioridade) intact.
  </action>
  <verify>
    <automated>grep -c "dataInicio" web/src/types/eventos.ts</automated>
    <automated>grep -c "data_inicio" web/src/types/eventos.ts</automated> <!-- should return 0 -->
  </verify>
  <acceptance_criteria>
    - file web/src/types/eventos.ts contains dataInicio, dataFim, processoId, tenantId
    - no data_inicio, data_fim, processo_id, tenant_id properties remain in the interface definitions
  </acceptance_criteria>
  <done>web/src/types/eventos.ts refactored to camelCase.</done>
</task>

<task type="auto">
  <name>Task 2: Refatorar schemas Zod da Agenda para camelCase</name>
  <files>web/src/schemas/eventos.ts</files>
  <read_first>
    - web/src/schemas/eventos.ts
  </read_first>
  <action>
Modify `web/src/schemas/eventos.ts` to replace all snake_case schema property names with camelCase:
- `processo_id` -> `processoId`
- `data_inicio` -> `dataInicio`
- `data_fim` -> `dataFim`
In `eventoFiltroSchema`, keep keys camelCase.
  </action>
  <verify>
    <automated>grep -c "dataInicio" web/src/schemas/eventos.ts</automated>
    <automated>grep -c "data_inicio" web/src/schemas/eventos.ts</automated> <!-- should return 0 -->
  </verify>
  <acceptance_criteria>
    - z.object in web/src/schemas/eventos.ts uses dataInicio, dataFim, processoId keys
    - no data_inicio, data_fim, or processo_id keys remain in the Zod schemas
  </acceptance_criteria>
  <done>web/src/schemas/eventos.ts updated to camelCase.</done>
</task>

<task type="auto">
  <name>Task 3: Refatorar hooks use-eventos para camelCase</name>
  <files>web/src/hooks/use-eventos.ts</files>
  <read_first>
    - web/src/hooks/use-eventos.ts
  </read_first>
  <action>
Modify `web/src/hooks/use-eventos.ts` to query using camelCase parameters, and mutate with camelCase request payloads.
Keep query key patterns. Ensure `concluido` query parameter is handled correctly.
  </action>
  <verify>
    <automated>grep -c "dataInicio" web/src/hooks/use-eventos.ts</automated>
  </verify>
  <acceptance_criteria>
    - useEventos, useCreateEvento, useUpdateEvento query structures use camelCase properties
  </acceptance_criteria>
  <done>web/src/hooks/use-eventos.ts updated to camelCase.</done>
</task>

<task type="auto">
  <name>Task 4: Atualizar páginas de Agenda para camelCase e formatar datas sem offset</name>
  <files>
    - web/src/app/(dashboard)/agenda/page.tsx
    - web/src/app/(dashboard)/agenda/[id]/page.tsx
    - web/src/app/(dashboard)/agenda/[id]/editar/page.tsx
    - web/src/app/(dashboard)/agenda/novo/page.tsx
  </files>
  <read_first>
    - web/src/app/(dashboard)/agenda/page.tsx
    - web/src/app/(dashboard)/agenda/[id]/page.tsx
    - web/src/app/(dashboard)/agenda/[id]/editar/page.tsx
    - web/src/app/(dashboard)/agenda/novo/page.tsx
  </read_first>
  <action>
Update references to `data_inicio`, `data_fim`, `processo_id` in the components to camelCase.
In form submissions (novo/page.tsx and editar/page.tsx), convert date picker values (ISO strings) to local datetime format by removing timezone offsets (e.g. `new Date(values.dataInicio).toISOString().slice(0, 19)`). This produces `YYYY-MM-DDTHH:mm:ss`, which Spring Boot parses directly into LocalDateTime without exceptions.
  </action>
  <verify>
    <automated>grep -c "dataInicio" web/src/app/(dashboard)/agenda/page.tsx</automated>
    <automated>grep -c "dataInicio" web/src/app/(dashboard)/agenda/novo/page.tsx</automated>
    <automated>grep -c "dataInicio" web/src/app/(dashboard)/agenda/[id]/editar/page.tsx</automated>
    <automated>grep -c "dataInicio" web/src/app/(dashboard)/agenda/[id]/page.tsx</automated>
  </verify>
  <acceptance_criteria>
    - All 4 pages reference camelCase keys for Evento properties
    - Timezone offset is stripped when submitting date payloads (using `.slice(0, 19)` or similar)
  </acceptance_criteria>
  <done>Agenda page files refactored to camelCase and timezone-offset handling implemented.</done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-40-01 | Information Disclosure | Evento payloads leaked in logs | mitigate | Do not print request payload dates or fields to application logs; rely on standard Spring Boot masking. |
| T-40-02 | Denial of Service | Malformed Date formats causing infinite exceptions | mitigate | Validate formats in the parser, catching exceptions and returning HTTP 400 instead of allowing threads to hang. |
</threat_model>

<verification>
Verify build passes and no type issues remain:

```bash
# Frontend build
pnpm --filter web build
```
</verification>

<success_criteria>
- Types, schemas, hooks, and page components use camelCase properties exclusively for Evento objects
- Date payloads sent to backend are formatted in local ISO style without timezone offsets (e.g. `YYYY-MM-DDTHH:mm:ss`)
- Frontend application compiles successfully via pnpm build
</success_criteria>

<output>
Create `.planning/phases/LEXCV-40-mapeamento-e-alinhamento-casing-agenda/40-01-SUMMARY.md` when done.
</output>
