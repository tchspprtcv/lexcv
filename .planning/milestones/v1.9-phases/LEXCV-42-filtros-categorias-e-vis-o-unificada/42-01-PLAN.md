---
phase: 42-filtros-categorias-e-vis-o-unificada
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/main/java/com/lexcv/controllers/ResourceController.java
  - web/src/types/processos.ts
  - web/src/hooks/use-processos.ts
  - web/src/app/(dashboard)/agenda/page.tsx
autonomous: true
requirements:
  - AGD-31-02
  - AGD-32-01
  - AGD-32-02
  - AGD-32-03
must_haves:
  truths:
    - "Utilizador pode filtrar o calendário por Categoria de Evento e Estado de Conclusão"
    - "Utilizador pode visualizar prazos de processos e eventos gerais de forma unificada no calendário da agenda"
    - "Utilizador pode filtrar a agenda por Processo específico através de um seletor visual"
    - "Estados de carregamento (loading spinners/skeletons) e de erro são exibidos na tela"
  artifacts:
    - path: "web/src/app/(dashboard)/agenda/page.tsx"
      provides: "unified filter UI"
      contains: "allUnifiedEvents"
    - path: "backend/src/main/java/com/lexcv/controllers/ResourceController.java"
      provides: "Global GET /prazos endpoint"
      contains: "listAllPrazos"
---

<objective>
Refactor monthly agenda calendar page to implement visual filters, load and display deadlines uniformly, display loading and error feedback states, and link virtual items to corresponding process detail pages.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Criar endpoint global de prazos no backend</name>
  <files>backend/src/main/java/com/lexcv/controllers/ResourceController.java</files>
  <action>
Expose global GET /prazos endpoint to return all deadlines of current tenant.
  </action>
  <verify>
    <automated>grep -c "listAllPrazos" backend/src/main/java/com/lexcv/controllers/ResourceController.java</automated>
  </verify>
  <acceptance_criteria>
    - GET /prazos endpoint returns all tenant deadlines.
  </acceptance_criteria>
  <done>Global prazos endpoint created.</done>
</task>

<task type="auto">
  <name>Task 2: Atualizar tipos e criar query hook useAllPrazos</name>
  <files>
    - web/src/types/processos.ts
    - web/src/hooks/use-processos.ts
  </files>
  <action>
Modify Prazo interface and create useAllPrazos TanStack Query hook calling GET /prazos.
  </action>
  <verify>
    <automated>grep -c "useAllPrazos" web/src/hooks/use-processos.ts</automated>
  </verify>
  <acceptance_criteria>
    - useAllPrazos queries GET /prazos.
  </acceptance_criteria>
  <done>Frontend types and Query hooks updated.</done>
</task>

<task type="auto">
  <name>Task 3: Implementar filtros visuais e visão unificada no calendário</name>
  <files>web/src/app/(dashboard)/agenda/page.tsx</files>
  <action>
Update agenda monthly calendar page:
1. Define filtering states for Processo, Categoria, and Estado.
2. Combine events and deadlines into a single array and filter client-side.
3. Render loader spinner under loading status.
4. Route deadlines click to their processes.
  </action>
  <verify>
    <automated>grep -c "selectedProcessoId" web/src/app/(dashboard)/agenda/page.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Filters and unified deadline items rendered inside the monthly calendar grid correctly.
  </acceptance_criteria>
  <done>Agenda page unified filters and deadlines rendering implemented.</done>
</task>

</tasks>

<verification>
Verify build passes:
```bash
pnpm --filter web build
```
</verification>

<success_criteria>
- Calendar page displays unified events and process deadlines
- Utilizador can interactively filter by process, category, and conclusion status
- Spinner loader icon is rendered while loading data
</success_criteria>
