---
phase: 107-m-dulos-documentos-financeiro
plan: 05
type: execute
wave: 2
depends_on: [107-01]
files_modified:
  - web/src/app/(dashboard)/processos/[id]/page.tsx
  - web/src/app/(dashboard)/clientes/[id]/page.tsx
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [DOF-01, DOF-02]
must_haves:
  truths:
    - "The Documento.tipo field in ProcessoDocumentosTab's upload dialog is a creatable Combobox (pick an existing tipo or type a new one and commit it)"
    - "The Documento.tipo field in ClienteDocumentosEntreguesTab's upload dialog is the same creatable Combobox (byte-identical sibling migration)"
    - "The upload progress bar in both tabs renders via the official Progress component, not a hand-rolled bg-blue-600 div"
    - "A freshly-typed tipo not present in tipoOptions still displays in the trigger and is submitted on upload"
    - "REQUIREMENTS.md DOF-V2-01 is marked closed/covered by this phase (fulfilled by the Cliente datalist→Combobox migration)"
  artifacts:
    - path: "web/src/app/(dashboard)/processos/[id]/page.tsx"
      provides: "ProcessoDocumentosTab: creatable Combobox (tipo) + Progress upload bar"
      contains: "Progress value={progresso"
    - path: "web/src/app/(dashboard)/clientes/[id]/page.tsx"
      provides: "ClienteDocumentosEntreguesTab: creatable Combobox (tipo) + Progress upload bar"
      contains: "Progress value={progresso"
  key_links:
    - from: "web/src/app/(dashboard)/processos/[id]/page.tsx"
      to: "web/src/components/shared/combobox.tsx"
      via: "creatable Combobox wired to plain novoTipo/setNovoTipo useState"
      pattern: "creatable"
    - from: "web/src/app/(dashboard)/clientes/[id]/page.tsx"
      to: "web/src/components/shared/combobox.tsx"
      via: "creatable Combobox wired to plain novoTipo/setNovoTipo useState"
      pattern: "creatable"
---

<objective>
Migrate the `Documento.tipo` field in the two byte-identical sibling upload dialogs (`ProcessoDocumentosTab` in `processos/[id]`, `ClienteDocumentosEntreguesTab` in `clientes/[id]`) from a native `<input list=…>`+`<datalist>` to the shared creatable `Combobox` from Plan 01, migrate both tabs' hand-rolled upload progress bars to the official `Progress` component, and close `DOF-V2-01` in REQUIREMENTS.md (the Cliente datalist→Combobox migration fulfills it).

Purpose: DOF-01 (Progress ×2) + DOF-02 (creatable Combobox for `Documento.tipo`, the user-approved scope expansion beyond a literal `<select>` swap). Both sibling files migrate in lockstep with the same excerpts (only `datalistId` and the mutation payload key `processo_id` vs `cliente_id` differ). Wiring is plain `useState` (`novoTipo`/`setNovoTipo`), NOT react-hook-form.
Output: two migrated ficha files + a REQUIREMENTS.md doc update.
</objective>

<execution_context>
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/LEXCV-107-m-dulos-documentos-financeiro/107-CONTEXT.md
@.planning/phases/LEXCV-107-m-dulos-documentos-financeiro/107-PATTERNS.md
@.planning/phases/LEXCV-107-m-dulos-documentos-financeiro/107-UI-SPEC.md
@web/src/components/ui/progress.tsx

<interfaces>
<!-- Combobox contract (from Plan 107-01, LOCKED). Import from @/components/shared/combobox -->
<!-- Creatable mode: pass creatable={true}; a "Usar \"{query}\"" item commits a brand-new typed value. -->
<!-- Wire with plain state: value={novoTipo} onChange={setNovoTipo} (NOT RHF Controller — host state is React.useState). -->
<!-- triggerClassName="rounded-none" to match the Dialog's sibling-element literal-class convention. -->
<!-- options={tipoOptions.map((t) => ({ value: t, label: t }))} -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ProcessoDocumentosTab — creatable Combobox (tipo) + Progress upload bar</name>
  <files>web/src/app/(dashboard)/processos/[id]/page.tsx</files>
  <read_first>
    - web/src/app/(dashboard)/processos/[id]/page.tsx (targeted range 2500-2649 — `ProcessoDocumentosTab` starts line 2500; state `novoTipo`/`setNovoTipo` line 2512; `tipoOptions` useMemo lines 2519-2529; the `<input list=…>`+`<datalist>` block lines 2591-2607; the progress block lines 2608-2621; sibling `Button`s already carry `rounded-none`, e.g. lines 2571/2628/2638; upload uses `novoTipo.trim()` at line 2544)
    - web/src/components/shared/combobox.tsx (the Plan-01 component — creatable mode)
    - web/src/components/ui/progress.tsx (Progress wrapper; value 0-100)
    - .planning/phases/LEXCV-107-m-dulos-documentos-financeiro/107-UI-SPEC.md (Combobox creatable-mode table + Copywriting Contract; Progress table + finding #3)
    - .planning/phases/LEXCV-107-m-dulos-documentos-financeiro/107-PATTERNS.md (ProcessoDocumentosTab section — exact excerpts)
  </read_first>
  <action>
    Two changes inside `ProcessoDocumentosTab` (function at line 2500):
    (1) `datalist` → creatable Combobox (lines 2591-2607): replace the `<input id={datalistId} list=… className="flex h-9 w-full rounded-none border …" value={novoTipo} onChange={(e) => setNovoTipo(e.target.value)} disabled={upload.isPending} />` and its adjacent `<datalist>…</datalist>` with `<Combobox id={datalistId} value={novoTipo} onChange={setNovoTipo} options={tipoOptions.map((t) => ({ value: t, label: t }))} creatable emptyMessage="Nenhuma sugestão." placeholder="Selecionar ou escrever tipo..." searchPlaceholder="Pesquisar ou escrever novo tipo..." triggerClassName="rounded-none" />`. Keep the surrounding `<div className="space-y-2"><Label htmlFor={datalistId}>Tipo</Label>…</div>` structure (`htmlFor={datalistId}` on the Label, `id={datalistId}` on the Combobox — both required for the label association to hold). Wire with plain state (`novoTipo`/`setNovoTipo`), NOT Controller. Do NOT touch `tipoOptions` (unchanged suggestion source) or the upload call at line 2544. Import `Combobox` from `@/components/shared/combobox`. Per 107-UI-SPEC.md's Copywriting Contract, creatable-mode Comboboxes MUST override the default `emptyMessage` ("Nenhum resultado encontrado.") with "Nenhuma sugestão." — the default is written for closed-list mode where "no match" is a dead end, not for creatable mode where the user can still type a new value.
    (2) Progress bar (lines 2608-2621): replace ONLY the two inner hand-rolled `<div>`s (`h-2 w-full rounded-full bg-neutral-200 dark:bg-neutral-700` track + its `h-2 rounded-full bg-blue-600 transition-all` child) with `<Progress value={progresso ?? 0} />`, keeping the "A enviar…"/{progresso}% label row untouched. Import `Progress` from `@/components/ui/progress`.
  </action>
  <verify>
    <automated>grep -q 'from "@/components/shared/combobox"' "web/src/app/(dashboard)/processos/[id]/page.tsx" && grep -q 'creatable' "web/src/app/(dashboard)/processos/[id]/page.tsx" && grep -q 'triggerClassName="rounded-none"' "web/src/app/(dashboard)/processos/[id]/page.tsx" && grep -q 'id={datalistId}' "web/src/app/(dashboard)/processos/[id]/page.tsx" && grep -q 'emptyMessage="Nenhuma sugestão."' "web/src/app/(dashboard)/processos/[id]/page.tsx" && grep -q '<Progress value={progresso ?? 0}' "web/src/app/(dashboard)/processos/[id]/page.tsx" && ! grep -q '<datalist' "web/src/app/(dashboard)/processos/[id]/page.tsx" && ! grep -q 'h-2 rounded-full bg-blue-600 transition-all' "web/src/app/(dashboard)/processos/[id]/page.tsx"</automated>
  </verify>
  <done>
    The tipo field is a creatable `Combobox` wired to `novoTipo`/`setNovoTipo` with `triggerClassName="rounded-none"`, `id={datalistId}`, and `emptyMessage="Nenhuma sugestão."`; the `<datalist>` is gone; the progress bar is `<Progress value={progresso ?? 0} />` with the label row intact; the `bg-blue-600` progress fill div is gone.
  </done>
  <acceptance_criteria>
    - Source assertion: `Combobox` imported from `@/components/shared/combobox`, rendered with `creatable` + `triggerClassName="rounded-none"` + `id={datalistId}` + `emptyMessage="Nenhuma sugestão."` + `value={novoTipo} onChange={setNovoTipo}`; the `<input list=…>`/`<datalist>` pair is gone.
    - Source assertion: `<Progress value={progresso ?? 0} />` present; `h-2 rounded-full bg-blue-600 transition-all` absent; the "A enviar…"/{progresso}% label row retained.
    - Behavior: typing a tipo not in `tipoOptions` shows a `Usar "…"` item; selecting it sets `novoTipo` to the typed value and it displays in the trigger; uploading submits that value.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: ClienteDocumentosEntreguesTab — same migration (sibling) + close DOF-V2-01 in REQUIREMENTS.md</name>
  <files>web/src/app/(dashboard)/clientes/[id]/page.tsx, .planning/REQUIREMENTS.md</files>
  <read_first>
    - web/src/app/(dashboard)/clientes/[id]/page.tsx (targeted range 1232-1361 — `ClienteDocumentosEntreguesTab` starts line 1232; state `novoTipo`/`setNovoTipo` line 1246; `tipoOptions` line 1253; the `<input list=…>`+`<datalist>` block lines 1323-1339; the progress block lines 1340-1353; upload uses `cliente_id` payload key at line 1278)
    - web/src/app/(dashboard)/processos/[id]/page.tsx (the sibling migrated in Task 1 — replicate its exact shape; only `datalistId` scoping and the mutation payload key differ)
    - web/src/components/shared/combobox.tsx (creatable mode)
    - .planning/REQUIREMENTS.md (line 84 `DOF-V2-01` in the v2 section, and the Traceability/Documentos rows — this doc-close is why REQUIREMENTS.md is in files_modified)
    - .planning/phases/LEXCV-107-m-dulos-documentos-financeiro/107-PATTERNS.md (ClienteDocumentosEntreguesTab section + the metadata note flagging the DOF-V2-01 doc update)
  </read_first>
  <action>
    (1) In `ClienteDocumentosEntreguesTab` (function at line 1232), apply the identical two changes as Task 1's ProcessoDocumentosTab: replace the `<input list=…>`+`<datalist>` pair (lines 1323-1339) with the same creatable `<Combobox id={datalistId} value={novoTipo} onChange={setNovoTipo} options={tipoOptions.map((t) => ({ value: t, label: t }))} creatable emptyMessage="Nenhuma sugestão." placeholder="Selecionar ou escrever tipo..." searchPlaceholder="Pesquisar ou escrever novo tipo..." triggerClassName="rounded-none" />`, and replace the progress block's two inner `<div>`s (lines 1340-1353) with `<Progress value={progresso ?? 0} />` (label row untouched). Import `Combobox` from `@/components/shared/combobox` and `Progress` from `@/components/ui/progress`. Leave the `cliente_id` upload payload and `tipoOptions` unchanged.
    (2) In `.planning/REQUIREMENTS.md`, annotate the `DOF-V2-01` bullet (line 84, "Combobox para o campo tipo-de-documento em Documentos Entregues") in place with a short parenthetical noting it is covered/resolved by Phase 107 (this phase's Cliente datalist→Combobox migration fulfills it) — the annotation MUST include the token `107` on that same bullet line. Do NOT delete the historical row; keep v2 traceability honest.
  </action>
  <verify>
    <automated>grep -q 'from "@/components/shared/combobox"' "web/src/app/(dashboard)/clientes/[id]/page.tsx" && grep -q 'triggerClassName="rounded-none"' "web/src/app/(dashboard)/clientes/[id]/page.tsx" && grep -q 'id={datalistId}' "web/src/app/(dashboard)/clientes/[id]/page.tsx" && grep -q 'emptyMessage="Nenhuma sugestão."' "web/src/app/(dashboard)/clientes/[id]/page.tsx" && grep -q '<Progress value={progresso ?? 0}' "web/src/app/(dashboard)/clientes/[id]/page.tsx" && ! grep -q '<datalist' "web/src/app/(dashboard)/clientes/[id]/page.tsx" && ! grep -q 'h-2 rounded-full bg-blue-600 transition-all' "web/src/app/(dashboard)/clientes/[id]/page.tsx" && grep 'DOF-V2-01' .planning/REQUIREMENTS.md | grep -q '107' && cd web && pnpm exec tsc --noEmit</automated>
  </verify>
  <done>
    ClienteDocumentosEntreguesTab mirrors the Processo sibling (creatable Combobox with `id={datalistId}`/`emptyMessage="Nenhuma sugestão."` + Progress, `<datalist>` and `bg-blue-600` gone); the `DOF-V2-01` bullet in REQUIREMENTS.md references Phase 107 as its resolution; `pnpm exec tsc --noEmit` passes.
  </done>
  <acceptance_criteria>
    - Source assertion: creatable `Combobox` (with `id={datalistId}` + `emptyMessage="Nenhuma sugestão."`) + `<Progress value={progresso ?? 0} />` present in `clientes/[id]/page.tsx`; `<datalist>` and `bg-blue-600 transition-all` progress fill gone.
    - Source assertion: the `DOF-V2-01` line in REQUIREMENTS.md contains the token `107`.
    - CLI: `cd web && pnpm exec tsc --noEmit` exits 0.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → Next rewrite → Spring API | Existing document-upload endpoint (`useUploadDocumentoComProgresso`). The `tipo` value remains free text (creatable Combobox commits the same trimmed string the old datalist did). No new server input; upload logic unchanged. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-107-09 | Tampering | creatable Combobox `tipo` value | accept | `Documento.tipo` is already unconstrained free text server-side (no enum in any layer); the Combobox commits the same trimmed string as the prior `<datalist>`. Input surface is unchanged/narrowed, not widened. |
| T-107-10 | Information Disclosure | `tipoOptions` suggestions | accept | Suggestions are derived from the tipos already present on that specific processo/cliente's documents — data the user is already authorized to view on the same page. |
| T-107-SC | Tampering | npm/pnpm installs | accept | Zero new dependencies (UI-SPEC Registry Safety); consumes the Plan-01 Combobox + already-installed `Progress`. |
</threat_model>

<verification>
- Both sibling tabs: creatable `Combobox` for tipo (plain-state wired, `rounded-none`) + `<Progress value={progresso ?? 0} />`; `<datalist>` and `bg-blue-600` progress fill removed from both.
- REQUIREMENTS.md `DOF-V2-01` bullet references Phase 107 as its resolution.
- `pnpm exec tsc --noEmit` passes.
- Plan-level build/lint is covered by the Wave-3 holistic gate (Plan 06).
</verification>

<success_criteria>
- DOF-01 satisfied for the 2 ficha upload bars (Progress); DOF-02 satisfied for `Documento.tipo` (creatable Combobox) in both ficha document tabs.
- `DOF-V2-01` closed in REQUIREMENTS.md rather than left deferred.
</success_criteria>

<output>
Create `.planning/phases/LEXCV-107-m-dulos-documentos-financeiro/107-05-SUMMARY.md` when done.
</output>
