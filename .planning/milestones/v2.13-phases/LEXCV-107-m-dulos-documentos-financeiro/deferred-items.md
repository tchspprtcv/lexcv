# Deferred Items — Phase 107

Items discovered during execution/verification that are out of scope for the current
phase (pre-existing, not introduced by this phase's changes) and therefore not
auto-fixed per the Scope Boundary rule.

## From 107-06 human-verify checkpoint (live UAT)

| Item | File | Evidence | Status |
|------|------|----------|--------|
| **Pre-existing backend bug: every NEW document upload (not replace/new-version) crashes with a Hibernate optimistic-locking error, 100% reproducible** | `backend/src/main/java/com/lexcv/controllers/ResourceController.java` (`uploadDocumento`, the `replaceId == null` branch, ~lines 2730-2753) | Reproduced live twice during UAT (small text file, then a 15MB binary file) via `/documentos/novo`: `POST /api/v1/documentos/upload` → `500`, backend log shows `org.hibernate.StaleObjectStateException: Row was updated or deleted by another transaction (or unsaved-value mapping was incorrect): [com.lexcv.models.Documento#<fresh-random-uuid>]` — a different random UUID each time, confirming this isn't about one stale row, it's structural. Root cause: the "create new" branch builds `Documento.builder().id(documentoId)....versao(1).build()`, explicitly setting BOTH an explicit ID and a non-null `@Version` field on a never-persisted entity; Spring Data JPA's `save()` uses "is version null?" to decide insert-vs-merge for `@Version`-annotated entities, so a non-null version on a brand-new entity routes to `merge()`, which then fails to find a matching existing row. Confirmed via `git log` that `ResourceController.java` was last touched by an unrelated Phase 97 commit (`f0c62ff`, 2026-07-14) — this predates and is unrelated to any Phase 107 (or any shadcn/ui migration) work; zero backend files were touched by any of this phase's 6 plans. | **Not fixed** — backend Java bug, entirely outside this phase's frontend-only scope (Progress/Select/NativeSelect/Combobox migration). Flagged via a dedicated background task (see below) given its severity: it currently blocks ALL new document uploads app-wide (via `/documentos/novo`, the Processo ficha's Documentos tab, and the Cliente ficha's Documentos Entregues tab — all three call the same endpoint). The sibling "replace/new version" upload path is unaffected (it fetches an existing managed entity via `findById` first, a correct update flow). |

This finding blocked one part of live UAT: a full "select/type a tipo → upload → confirm Progress bar animates → confirm stored document" walkthrough could not be completed anywhere in this environment, since the final `POST` always fails regardless of which UI (native input, datalist, or the new Combobox) supplied the `tipo` value. This is NOT a Phase 107 regression — it predates the phase and reproduces identically with or without this phase's changes (the failure happens server-side, after the frontend has already successfully built and sent the request). Phase 107's own scope (the `Progress`/`Combobox`/`Select`/`NativeSelect` migrations themselves) was still verified thoroughly:
- The closed-list Combobox (Documentos list filters) was verified fully live and end-to-end: opened, searched, selected "1/2026", confirmed the trigger displayed the label, clicked "Filtrar", and confirmed via network inspection that the correct underlying UUID (`e53d3192-1a7e-40ea-af5d-e561e97bd2d2`) was sent to `GET /api/v1/processos/{id}/documentos` — proving the label-to-value resolution works correctly, independent of the unrelated upload bug.
- The creatable Combobox (`Documento.tipo` in the Processo ficha's Documentos tab) was verified fully live: opened with the correct empty-state copy ("Nenhuma sugestão." — confirming the plan-checker's WR-fix landed), typed "Procuração", confirmed the `Usar "Procuração"` create-item appeared with the exact locked copy, clicked it, and confirmed the trigger updated to display "Procuração" — the complete creatable-mode interaction, independent of the (separately broken) final upload step.
- `Progress`, `NativeSelect` (`confidencialidade`, honorário `processoId`), and Financeiro `Select` filters were all confirmed via direct source/DOM inspection (exact classes, options, and wiring matching the locked contracts) plus the holistic build/lint/regression gate — live click-through on the Financeiro `Select` triggers specifically was inconclusive due to Browser-pane automation friction with Radix Select's pointer-event handling (not a reproducible app issue — the identical `Select` composition was already confirmed working live in Phase 106 for Agenda's list filters).

## From 107-UI-REVIEW.md (retroactive 6-pillar audit, 19/24 → fixed to closer to clean)

Two of the audit's three priority findings were fixed directly (Combobox `loading` prop wired to `processos.isPending`/`clientes.isPending` on the Documentos filters; `rounded-none` sibling-consistency restored in the Cliente ficha's upload dialog — see commit `697e768`). The third was considered and deliberately not changed:

| Item | File | Evidence | Status |
|------|------|----------|--------|
| The "Todos" sentinel option is modeled as a real matching `options` entry, so the trigger's `isPlaceholder`/`text-muted-foreground` styling never fires for the 2 Documentos filters — the default "Todos..." state always renders at full foreground strength | `web/src/components/shared/combobox.tsx:51,64-66` (`selected = options.find(o => o.value === value)` matches the sentinel), `web/src/app/(dashboard)/documentos/page.tsx:77,87` (sentinel seeded into `processoOptions`/`clienteOptions`) | UI-SPEC's Component Inventory declares "placeholder in `text-muted-foreground`" as a binding rule for the trigger's unselected state; the sentinel technically isn't "unselected," it's a real, always-present default selection (mirroring exactly how Financeiro's own `Select` migration in this same phase treats its `"todos"` sentinel — also never muted, also always a real selected value, per `financeiro/page.tsx`). | **Not changed — considered and rejected as a fix.** Muting a value that IS the active filter state (as opposed to "nothing chosen yet") would be visually inconsistent with the Financeiro `Select`'s identical sentinel treatment shipped in the very same phase, and would require special-casing the trigger-label logic differently between the 2 Combobox call sites and the 2 Select call sites for what is the same underlying UX concept ("no filter applied" as a real, addressable state). Treating the sentinel as a genuine selection (current behavior) is the more internally-consistent choice across this phase's own two list-filter patterns. Not a functional defect — zero user impact reported in the audit itself. |

