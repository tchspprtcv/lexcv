---
phase: 107-m-dulos-documentos-financeiro
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/app/(dashboard)/financeiro/page.tsx
  - web/src/app/(dashboard)/financeiro/novo/page.tsx
  - web/src/app/(dashboard)/financeiro/[id]/page.tsx
autonomous: true
requirements: [DOF-02]
must_haves:
  truths:
    - "The Financeiro list Processo and Estado filters render as Radix Select, each with a non-empty 'todos' sentinel"
    - "Both filter predicates compare against '!== todos' (not truthiness), and the clear-filters control resets to 'todos'"
    - "The honorário-create processoId field renders as NativeSelect (className w-full) with the dynamic processo option list"
    - "The local selectClassName constant in financeiro/novo is deleted"
    - "The three Financeiro RBAC gates (page/novo/[id]) use permissions.isFetched"
  artifacts:
    - path: "web/src/app/(dashboard)/financeiro/page.tsx"
      provides: "Select filters + todos sentinel + predicate fix + isFetched gate"
      contains: "<Select value={filtroProcesso}"
    - path: "web/src/app/(dashboard)/financeiro/novo/page.tsx"
      provides: "NativeSelect processoId + selectClassName deleted + isFetched gate"
      contains: "NativeSelect id=\"processoId\""
    - path: "web/src/app/(dashboard)/financeiro/[id]/page.tsx"
      provides: "isFetched RBAC gate"
      contains: "permissions.isFetched"
  key_links:
    - from: "web/src/app/(dashboard)/financeiro/page.tsx"
      to: "@/components/ui/select"
      via: "import Select/SelectTrigger/SelectContent/SelectItem/SelectValue"
      pattern: "from \"@/components/ui/select\""
    - from: "web/src/app/(dashboard)/financeiro/novo/page.tsx"
      to: "@/components/ui/native-select"
      via: "import NativeSelect"
      pattern: "from \"@/components/ui/native-select\""
---

<objective>
Migrate the Financeiro module's list filters to Radix `Select` (with the `"todos"` sentinel + predicate fix required because both filters currently use an empty-string `""` sentinel that Radix `Select.Item` cannot accept), migrate the honorário-create `processoId` field to `NativeSelect`, delete the dead `selectClassName` constant, and bundle the `isFetched` RBAC-race fix into all three Financeiro files.

Purpose: DOF-02 (Select for list filters, NativeSelect for the enum/dynamic-list form field). The edit-honorário Dialog has no select-worthy field (confirmed: valorTotal/dataAcordo/descricao all `Input`); `Pagamento.metodo` stays free text (locked exclusion — no enum exists). financeiro/[id] is RBAC-only.
Output: three migrated route files; no dependency on the Wave-1 Combobox.
</objective>

<execution_context>
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/LEXCV-107-m-dulos-documentos-financeiro/107-CONTEXT.md
@.planning/phases/LEXCV-107-m-dulos-documentos-financeiro/107-PATTERNS.md
@.planning/phases/LEXCV-107-m-dulos-documentos-financeiro/107-UI-SPEC.md
@web/src/components/ui/select.tsx
@web/src/components/ui/native-select.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: financeiro/page — Processo + Estado filters to Select (with todos sentinel + predicate fix) + isFetched gate</name>
  <files>web/src/app/(dashboard)/financeiro/page.tsx</files>
  <read_first>
    - web/src/app/(dashboard)/financeiro/page.tsx (the file being modified — RBAC gate line 103; filter state lines 142-143; predicates lines 150-151; Processo select lines 209-224; Estado select lines 226-238; clear-filters button + reset lines 260-272)
    - web/src/app/(dashboard)/agenda/page.tsx (the exact precedent this migration replicates: `"todos"` state default lines 44-46, `!== "todos"` predicate line 123, `<Select>` composition lines 246-258)
    - web/src/components/ui/select.tsx (Radix Select — SelectTrigger ships w-fit; Select.Item cannot take value="")
    - .planning/phases/LEXCV-107-m-dulos-documentos-financeiro/107-UI-SPEC.md (Select section + Verified-against-source finding #2 — BOTH filters need the sentinel/predicate fix, not just one)
  </read_first>
  <action>
    Four coordinated changes in `financeiro/page.tsx`:
    (1) RBAC gate line 103: `if (!permissions.isLoading && !canViewFinanceiro) {` → `if (permissions.isFetched && !canViewFinanceiro) {`.
    (2) Sentinel state (lines 142-143): change `React.useState("")` for `filtroProcesso` to `React.useState("todos")`, and change the `filtroStatus` state from `React.useState<"" | "Pendente" | "Parcialmente Pago" | "Pago">("")` to `React.useState<"todos" | "Pendente" | "Parcialmente Pago" | "Pago">("todos")`.
    (3) Predicates (lines 150-151): change `if (filtroProcesso)` → `if (filtroProcesso !== "todos")` and `if (filtroStatus)` → `if (filtroStatus !== "todos")`. Also update the clear-filters control (lines 260-272): its visibility condition currently tests truthiness of `filtroProcesso || filtroStatus || …` — the two status/processo terms must become `filtroProcesso !== "todos"` / `filtroStatus !== "todos"` (leave the `filtroDataDe`/`filtroDataAte` terms as-is), and its reset handler must set `filtroProcesso`/`filtroStatus` back to `"todos"` (not `""`).
    (4) Replace the Processo `<select value={filtroProcesso} onChange={(e) => setFiltroProcesso(e.target.value)} className="rounded-md border …">` (lines 209-224) and the Estado `<select value={filtroStatus} …>` (lines 226-238) with the `<Select value={…} onValueChange={…}><SelectTrigger size="default"><SelectValue /></SelectTrigger><SelectContent>…</SelectContent></Select>` composition from agenda/page.tsx lines 246-258. Processo options: `<SelectItem value="todos">Todos</SelectItem>` + one `<SelectItem key={p.id} value={p.id}>{p.numero ?? p.titulo ?? p.id}</SelectItem>` per `processos.data`. Estado options: `<SelectItem value="todos">Todos</SelectItem>` + `"Pendente"`/`"Parcialmente Pago"`/`"Pago"` reused verbatim. For Estado's `onValueChange`, cast the value to the union type at the call site matching the existing `as` style. Do NOT add any fixed-width className to `SelectTrigger` — leave the shipped `w-fit` (per UI-SPEC: Financeiro's filter row has no prior fixed-width convention). Keep each surrounding `<div className="flex flex-col gap-1"><label className="text-xs font-medium text-neutral-500 dark:text-neutral-400">…</label>…</div>` wrapper unchanged. Import `Select, SelectContent, SelectItem, SelectTrigger, SelectValue` from `@/components/ui/select`.
  </action>
  <verify>
    <automated>grep -q 'permissions.isFetched && !canViewFinanceiro' "web/src/app/(dashboard)/financeiro/page.tsx" && grep -q 'React.useState("todos")' "web/src/app/(dashboard)/financeiro/page.tsx" && grep -q 'filtroProcesso !== "todos"' "web/src/app/(dashboard)/financeiro/page.tsx" && grep -q 'filtroStatus !== "todos"' "web/src/app/(dashboard)/financeiro/page.tsx" && grep -q '<Select value={filtroProcesso}' "web/src/app/(dashboard)/financeiro/page.tsx" && grep -q '<Select value={filtroStatus}' "web/src/app/(dashboard)/financeiro/page.tsx" && ! grep -q 'onChange={(e) => setFiltroProcesso' "web/src/app/(dashboard)/financeiro/page.tsx"</automated>
  </verify>
  <done>
    Both filters render as Radix `Select` with a `"todos"` sentinel; both predicates and the clear-filters control use `!== "todos"`; the RBAC gate uses `isFetched`; no native `<select onChange={... setFiltro...}>` remains.
  </done>
  <acceptance_criteria>
    - Source assertion: `permissions.isFetched && !canViewFinanceiro` present.
    - Source assertion: `filtroProcesso` and `filtroStatus` both default to `"todos"`; both predicates use `!== "todos"`; the clear-filters reset sets them back to `"todos"`.
    - Source assertion: `<Select value={filtroProcesso}` and `<Select value={filtroStatus}` present; the two former native-select `onChange` handlers are gone.
    - Behavior: default view (sentinel `"todos"`) shows the full honorário list; selecting a specific Processo or Estado narrows it; "Limpar" restores the full list.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: financeiro/novo — processoId to NativeSelect + delete selectClassName + isFetched gate</name>
  <files>web/src/app/(dashboard)/financeiro/novo/page.tsx</files>
  <read_first>
    - web/src/app/(dashboard)/financeiro/novo/page.tsx (the file being modified — RBAC gate line 28; `selectClassName` const lines 21-22; processoId `<select>` lines 104-122; keep line 175 submit-disable untouched)
    - web/src/app/(dashboard)/processos/novo/page.tsx (lines 301-314 — the dynamic-option-list `NativeSelect` bound via register() with a loading/error-aware placeholder option, exact shape to replicate)
    - web/src/components/ui/native-select.tsx (wrapper defaults to w-fit — className="w-full" mandatory)
  </read_first>
  <action>
    Three changes in `financeiro/novo/page.tsx`:
    (1) RBAC gate line 28: `if (!permissions.isLoading && !canCreateFinanceiro) {` → `if (permissions.isFetched && !canCreateFinanceiro) {`. Do NOT touch line 175 (`disabled={... || permissions.isLoading || !canCreateFinanceiro}`) — legitimate submit guard, out of scope.
    (2) Delete the `const selectClassName = "flex h-9 w-full rounded-md border …";` declaration entirely (lines 21-22) — no other consumer remains after change (3).
    (3) processoId field (lines 104-122): replace `<select id="processoId" className={selectClassName} disabled={processos.isPending || processos.isError} {...form.register("processoId")}>` with `<NativeSelect id="processoId" size="default" className="w-full" disabled={processos.isPending || processos.isError} {...form.register("processoId")}>`. Keep the loading/error-aware placeholder `<option value="">…</option>`, the dynamic `.map` of `processos.data` options, and both error-paragraph blocks below unchanged. Import `NativeSelect` from `@/components/ui/native-select`.
  </action>
  <verify>
    <automated>grep -q 'permissions.isFetched && !canCreateFinanceiro' "web/src/app/(dashboard)/financeiro/novo/page.tsx" && ! grep -q 'const selectClassName' "web/src/app/(dashboard)/financeiro/novo/page.tsx" && grep -q 'NativeSelect id="processoId"' "web/src/app/(dashboard)/financeiro/novo/page.tsx" && grep -q 'className="w-full"' "web/src/app/(dashboard)/financeiro/novo/page.tsx" && grep -q 'permissions.isLoading || !canCreateFinanceiro' "web/src/app/(dashboard)/financeiro/novo/page.tsx"</automated>
  </verify>
  <done>
    processoId is a `NativeSelect className="w-full"` keeping its dynamic option list and error blocks; `selectClassName` const is gone; the create gate uses `isFetched`; line 175 submit-disable still uses `permissions.isLoading`.
  </done>
  <acceptance_criteria>
    - Source assertion: `permissions.isFetched && !canCreateFinanceiro` present; `const selectClassName` absent.
    - Source assertion: `NativeSelect id="processoId"` with `className="w-full"` and `disabled={processos.isPending || processos.isError}` present; dynamic `processos.data` option map preserved.
    - Source assertion: the submit-guard `permissions.isLoading || !canCreateFinanceiro` (line ~175) unchanged.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: financeiro/[id] — isFetched RBAC gate (detail)</name>
  <files>web/src/app/(dashboard)/financeiro/[id]/page.tsx</files>
  <read_first>
    - web/src/app/(dashboard)/financeiro/[id]/page.tsx (the file being modified — RBAC gate line 80; confirm the edit-honorário Dialog lines 270-329 has no select-worthy field and line 478 submit-disable must NOT change; `metodo` at ~466-471 stays a plain Input)
    - web/src/app/(dashboard)/agenda/page.tsx (line 27 — the `permissions.isFetched` precedent)
  </read_first>
  <action>
    Single change in `financeiro/[id]/page.tsx` line 80: `if (!permissions.isLoading && !canViewFinanceiro) {` → `if (permissions.isFetched && !canViewFinanceiro) {`. No UI-component migration in this file: the edit-honorário Dialog (valorTotal/dataAcordo/descricao, all `Input`) and the `metodo` field (free text, locked exclusion) are left unchanged. Do NOT touch line 478 (submit-guard `permissions.isLoading` use).
  </action>
  <verify>
    <automated>grep -q 'permissions.isFetched && !canViewFinanceiro' "web/src/app/(dashboard)/financeiro/[id]/page.tsx" && ! grep -q '!permissions.isLoading && !canViewFinanceiro' "web/src/app/(dashboard)/financeiro/[id]/page.tsx" && grep -q 'permissions.isLoading || !canEditFinanceiro' "web/src/app/(dashboard)/financeiro/[id]/page.tsx"</automated>
  </verify>
  <done>
    financeiro/[id] view gate uses `permissions.isFetched && !canViewFinanceiro`; the submit-guard at line 478 still uses `permissions.isLoading`; nothing else changed.
  </done>
  <acceptance_criteria>
    - Source assertion: `permissions.isFetched && !canViewFinanceiro` present; `!permissions.isLoading && !canViewFinanceiro` absent.
    - Source assertion: the submit-guard `permissions.isLoading || !canEditFinanceiro` (line ~478) unchanged.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → Next rewrite → Spring API | Existing honorário read/create endpoints; list filters are applied client-side over an already-authorized dataset. Client RBAC gate is defense-in-depth; server `@PreAuthorize` is the real boundary (unchanged). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-107-05 | Elevation of Privilege | client RBAC gates on financeiro page/novo/[id] | mitigate | Switch to `permissions.isFetched && !canX`, closing the pre-resolve render race (matches Phases 103/105/106). Server `@PreAuthorize` unchanged. |
| T-107-06 | Tampering | filter sentinel/predicate logic change | mitigate | Sentinel `"todos"` + `!== "todos"` predicate is a semantically-identical replacement for the prior truthiness check; verified by the "default view shows full list" behavior assertion. No server input changes. |
| T-107-SC | Tampering | npm/pnpm installs | accept | Zero new dependencies (UI-SPEC Registry Safety); `Select`/`NativeSelect` installed Phase 101. |
</threat_model>

<verification>
- financeiro/page: both filters are `<Select>` with `"todos"` sentinel; both predicates + clear control use `!== "todos"`; native filter selects gone.
- financeiro/novo: `NativeSelect id="processoId" className="w-full"` present; `selectClassName` const gone.
- All three files: `isFetched` gate applied; the three legitimate `isLoading` submit-guards untouched.
- Plan-level build/lint is covered by the Wave-3 holistic gate (Plan 06).
</verification>

<success_criteria>
- DOF-02 satisfied for the Financeiro module (Select filters + NativeSelect processoId).
- 3 of the 6 bundled RBAC gates fixed here (financeiro/page:103, financeiro/novo:28, financeiro/[id]:80).
</success_criteria>

<output>
Create `.planning/phases/LEXCV-107-m-dulos-documentos-financeiro/107-03-SUMMARY.md` when done.
</output>
