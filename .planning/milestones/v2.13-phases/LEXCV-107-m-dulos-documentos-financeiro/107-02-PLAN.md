---
phase: 107-m-dulos-documentos-financeiro
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/app/(dashboard)/documentos/novo/page.tsx
  - web/src/app/(dashboard)/documentos/[id]/page.tsx
autonomous: true
requirements: [DOF-01, DOF-02]
must_haves:
  truths:
    - "The upload progress bar in documentos/novo renders via the official Progress component (bg-primary, theme-aware), not a hand-rolled bg-blue-600 div"
    - "The confidencialidade field renders as NativeSelect (className w-full) with the 4 fixed enum options"
    - "The documentos list-create and detail RBAC gates use permissions.isFetched (not !permissions.isLoading)"
  artifacts:
    - path: "web/src/app/(dashboard)/documentos/novo/page.tsx"
      provides: "Progress upload bar + NativeSelect confidencialidade + isFetched gate"
      contains: "Progress value={progresso"
    - path: "web/src/app/(dashboard)/documentos/[id]/page.tsx"
      provides: "isFetched RBAC gate"
      contains: "permissions.isFetched"
  key_links:
    - from: "web/src/app/(dashboard)/documentos/novo/page.tsx"
      to: "@/components/ui/progress"
      via: "import Progress"
      pattern: "from \"@/components/ui/progress\""
    - from: "web/src/app/(dashboard)/documentos/novo/page.tsx"
      to: "@/components/ui/native-select"
      via: "import NativeSelect"
      pattern: "from \"@/components/ui/native-select\""
---

<objective>
Migrate the standalone document-upload form (`documentos/novo`) to the official `Progress` bar and a `NativeSelect` for `confidencialidade`, and bundle the `isFetched` RBAC-race fix into both `documentos/novo` and `documentos/[id]` (detail).

Purpose: DOF-01 (Progress) + DOF-02 (NativeSelect for the enum-real `confidencialidade` field). The `Documento.tipo` field on this form stays a plain `Input` (locked exclusion — no processo/cliente context to source suggestions from). No Combobox here.
Output: two migrated route files; no dependency on the Wave-1 Combobox component.
</objective>

<execution_context>
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/LEXCV-107-m-dulos-documentos-financeiro/107-CONTEXT.md
@.planning/phases/LEXCV-107-m-dulos-documentos-financeiro/107-PATTERNS.md
@.planning/phases/LEXCV-107-m-dulos-documentos-financeiro/107-UI-SPEC.md
@web/src/components/ui/progress.tsx
@web/src/components/ui/native-select.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: documentos/novo — Progress bar + NativeSelect confidencialidade + isFetched gate</name>
  <files>web/src/app/(dashboard)/documentos/novo/page.tsx</files>
  <read_first>
    - web/src/app/(dashboard)/documentos/novo/page.tsx (the file being modified — read the RBAC gate at line 120, the progress block at lines 179-192, the confidencialidade select at lines 231-246, and note lines 207-213 `tipo` Input and line 261 submit-disable which must NOT change)
    - web/src/components/ui/progress.tsx (thin radix-ui wrapper; `value` prop 0-100; ships bg-muted track / bg-primary indicator / h-1.5 / rounded-full)
    - web/src/components/ui/native-select.tsx (wrapper defaults to w-fit — className="w-full" mandatory; size="default" → h-9)
    - web/src/app/(dashboard)/processos/novo/page.tsx (lines 301-314 — the established `<NativeSelect id=... size="default" className="w-full" {...register(...)}>` shape to replicate)
    - .planning/phases/LEXCV-107-m-dulos-documentos-financeiro/107-PATTERNS.md (documentos/novo section — exact excerpts and the three sub-changes)
  </read_first>
  <action>
    Make three changes in `documentos/novo/page.tsx`:
    (1) RBAC gate at line 120: replace `if (!permissions.isLoading && !canCreateDocumentos) {` with `if (permissions.isFetched && !canCreateDocumentos) {`. Do NOT touch line 261 (`disabled={... || permissions.isLoading || !canCreateDocumentos}`) — that is the legitimate submit-guard use of isLoading, out of scope.
    (2) Progress bar (lines 179-192): replace ONLY the two inner hand-rolled `<div>`s (the `h-2 w-full rounded-full bg-neutral-200 dark:bg-neutral-700` track wrapper and its `h-2 rounded-full bg-blue-600 transition-all` child with inline `style={{ width }}`) with a single `<Progress value={progresso ?? 0} />`. Keep the `<div className="flex justify-between text-xs text-neutral-500">…"A enviar…"…{progresso}%…</div>` label row (lines 181-184) untouched. Import `Progress` from `@/components/ui/progress`.
    (3) confidencialidade (lines 231-246): replace the raw `<select id="confidencialidade" className="flex h-9 w-full rounded-md border …" {...form.register("confidencialidade")}>` with `<NativeSelect id="confidencialidade" size="default" className="w-full" {...form.register("confidencialidade")}>`, keeping the four `<option>`s (PUBLICO/INTERNO/CONFIDENCIAL/RESTRITO) and the error-paragraph block below unchanged. Delete the inline `className="flex h-9 w-full rounded-md border …"` entirely (NativeSelect's own styling replaces it). Import `NativeSelect` from `@/components/ui/native-select`. Do NOT migrate the `tipo` field (lines 207-213) — it stays a plain `Input` (locked exclusion).
  </action>
  <verify>
    <automated>grep -q 'permissions.isFetched && !canCreateDocumentos' "web/src/app/(dashboard)/documentos/novo/page.tsx" && ! grep -q 'h-2 rounded-full bg-blue-600 transition-all' "web/src/app/(dashboard)/documentos/novo/page.tsx" && grep -q '<Progress value={progresso ?? 0}' "web/src/app/(dashboard)/documentos/novo/page.tsx" && grep -q 'NativeSelect id="confidencialidade"' "web/src/app/(dashboard)/documentos/novo/page.tsx" && grep -q 'permissions.isLoading || !canCreateDocumentos' "web/src/app/(dashboard)/documentos/novo/page.tsx"</automated>
  </verify>
  <done>
    isFetched gate applied at line 120; progress track/fill divs replaced by `<Progress value={progresso ?? 0} />` with the label row intact; confidencialidade is a `NativeSelect className="w-full"` with the 4 options; line 261 submit-disable still uses `permissions.isLoading`.
  </done>
  <acceptance_criteria>
    - Source assertion: `permissions.isFetched && !canCreateDocumentos` present; `!permissions.isLoading && !canCreateDocumentos` absent.
    - Source assertion: `h-2 rounded-full bg-blue-600 transition-all` absent; `<Progress value={progresso ?? 0} />` present; the "A enviar…"/{progresso}% label row still present.
    - Source assertion: `NativeSelect id="confidencialidade"` with `className="w-full"` present; the 4 enum `<option>`s preserved.
    - Source assertion: the submit-guard `permissions.isLoading || !canCreateDocumentos` (line ~261) is unchanged.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: documentos/[id] — isFetched RBAC gate (detail)</name>
  <files>web/src/app/(dashboard)/documentos/[id]/page.tsx</files>
  <read_first>
    - web/src/app/(dashboard)/documentos/[id]/page.tsx (the file being modified — RBAC gate at line 25; nothing else changes in this file)
    - web/src/app/(dashboard)/agenda/page.tsx (line 27 — the fixed-state precedent `if (permissions.isFetched && !canViewAgenda) {`)
  </read_first>
  <action>
    Single change in `documentos/[id]/page.tsx` line 25: replace `if (!permissions.isLoading && !canViewDocumentos) {` with `if (permissions.isFetched && !canViewDocumentos) {`. No other change in this file — no UI-component migration, no Progress, no Select.
  </action>
  <verify>
    <automated>grep -q 'permissions.isFetched && !canViewDocumentos' "web/src/app/(dashboard)/documentos/[id]/page.tsx" && ! grep -q '!permissions.isLoading && !canViewDocumentos' "web/src/app/(dashboard)/documentos/[id]/page.tsx"</automated>
  </verify>
  <done>
    documentos/[id] detail gate uses `permissions.isFetched && !canViewDocumentos`; the old `!permissions.isLoading` form is gone; no other change to the file.
  </done>
  <acceptance_criteria>
    - Source assertion: `permissions.isFetched && !canViewDocumentos` present; `!permissions.isLoading && !canViewDocumentos` absent.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → Next rewrite → Spring API | Existing document-upload (`useUploadDocumentoComProgresso`) and read endpoints; unchanged by this plan. Client RBAC gate is defense-in-depth only — real authz is server-side `@PreAuthorize`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-107-03 | Elevation of Privilege | client RBAC gate on documentos/novo + detail | mitigate | Switch guard to `permissions.isFetched && !canX`, closing the race window where the page briefly renders before permissions resolve (same fix Phases 103/105/106 established). Server `@PreAuthorize` remains the real boundary and is unchanged. |
| T-107-04 | Tampering | Progress `value` binding | accept | `value={progresso ?? 0}` is a display-only 0-100 number from an existing local upload-progress state; no new server input. Upload logic (`XMLHttpRequest.upload.onprogress`) unchanged. |
| T-107-SC | Tampering | npm/pnpm installs | accept | Zero new dependencies (UI-SPEC Registry Safety); `Progress`/`NativeSelect` installed Phase 101. |
</threat_model>

<verification>
- Both files: grep confirms the `isFetched` substitution landed and no `!permissions.isLoading && !canX` gate remains in either.
- documentos/novo: `<Progress value={progresso ?? 0} />` present, `bg-blue-600 transition-all` gone, `NativeSelect id="confidencialidade"` present.
- Plan-level build/lint is covered by the Wave-3 holistic gate (Plan 06).
</verification>

<success_criteria>
- DOF-01 satisfied for the standalone upload site (Progress), DOF-02 satisfied for `confidencialidade` (NativeSelect).
- 2 of the 6 bundled RBAC gates fixed here (documentos/novo:120, documentos/[id]:25).
</success_criteria>

<output>
Create `.planning/phases/LEXCV-107-m-dulos-documentos-financeiro/107-02-SUMMARY.md` when done.
</output>
