---
phase: 108-m-dulo-pareceres
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/app/(dashboard)/pareceres/nova/page.tsx
autonomous: true
requirements: [PARC-18]
must_haves:
  truths:
    - "All 4 create-form fields (clienteId/processoId/prioridade/advogadoId) render as NativeSelect with className w-full, and zero native <select> elements remain"
    - "The local selectClassName constant is deleted; the textareaClassName constant is preserved (still used by the descricao textarea)"
    - "Both nova RBAC sites (view gate + submit-disable) use permissions.isFetched, not permissions.isLoading"
  artifacts:
    - path: "web/src/app/(dashboard)/pareceres/nova/page.tsx"
      provides: "4 NativeSelect form fields + selectClassName deleted + isFetched"
      contains: "NativeSelect id=\"clienteId\""
  key_links:
    - from: "web/src/app/(dashboard)/pareceres/nova/page.tsx"
      to: "@/components/ui/native-select"
      via: "import NativeSelect"
      pattern: "from \"@/components/ui/native-select\""
---

<objective>
Migrate the 4 native `<select>` fields in the Pareceres create form (`clienteId`, `processoId`, `prioridade`, `advogadoId`) to `NativeSelect`, delete the now-dead `selectClassName` constant, and bundle the `isFetched` RBAC-race fix at both permission sites (view gate + submit-button disable).

Purpose: PARC-18 for the Pareceres create form (NativeSelect for RHF-bound fields, matching the Processos/Documentos precedent from Phases 105/107). `NativeSelect` is `register()`-compatible — a direct drop-in for each `<select {...form.register(...)}>`.
Output: one migrated route file, no dependency on any other Phase 108 plan (owns `pareceres/nova/page.tsx` exclusively).
</objective>

<execution_context>
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/LEXCV-108-m-dulo-pareceres/108-CONTEXT.md
@.planning/phases/LEXCV-108-m-dulo-pareceres/108-PATTERNS.md
@.planning/phases/LEXCV-108-m-dulo-pareceres/108-UI-SPEC.md
@web/src/components/ui/native-select.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: 4 create-form selects to NativeSelect + delete selectClassName + isFetched RBAC (both sites)</name>
  <files>web/src/app/(dashboard)/pareceres/nova/page.tsx</files>
  <read_first>
    - web/src/app/(dashboard)/pareceres/nova/page.tsx (the file being modified — `selectClassName` const lines 25-26; `textareaClassName` const lines 28-29 [KEEP — used by descricao textarea line 170]; view gate line 35; clienteId `<select>` lines 127-139; processoId lines 152-163; prioridade lines 191-195; advogadoId lines 200-207; submit-disable expression lines 216-221 with `permissions.isLoading` on line 219)
    - web/src/app/(dashboard)/processos/novo/page.tsx (lines 301-319 — the dynamic, loading/error-aware `NativeSelect` bound via `register()` with a loading-aware placeholder option and an error-paragraph below; exact structural match for `clienteId`)
    - web/src/app/(dashboard)/documentos/novo/page.tsx (lines 230-235 — the fixed-enum `NativeSelect` with no empty option; exact match for `prioridade`)
    - web/src/components/ui/native-select.tsx (the primitive — wrapper defaults to `w-fit`, so `className="w-full"` is mandatory; `size` prop defaults to `"default"` = `h-9`; `register()`-compatible via `{...props}` spread onto the inner `<select>`)
    - .planning/phases/LEXCV-108-m-dulo-pareceres/108-UI-SPEC.md (Component Inventory → NativeSelect section — `className="w-full rounded-none"` for these 4 fields, preserve clienteId's loading/disabled behavior verbatim)
  </read_first>
  <action>
    Six changes in `nova/page.tsx`:
    (1) View gate line 35: change `if (!permissions.isLoading && !canCreatePareceres) {` to `if (permissions.isFetched && !canCreatePareceres) {`.
    (2) Submit-disable expression (lines 216-221): change the `permissions.isLoading` clause on line 219 to `!permissions.isFetched` (this site is explicitly listed in 108-CONTEXT.md as one of the 7 RBAC sites to fix — unlike Phase 107 it is NOT left as `isLoading`). Keep the other three clauses (`form.formState.isSubmitting`, `createParecer.isPending`, `!canCreatePareceres`) unchanged.
    (3) `clienteId` (lines 127-139): replace `<select id="clienteId" className={selectClassName} disabled={clientes.isPending || clientes.isError} {...form.register("clienteId")}>` with `<NativeSelect id="clienteId" size="default" className="w-full rounded-none" disabled={clientes.isPending || clientes.isError} {...form.register("clienteId")}>`. Keep the loading-aware first `<option value="">{clientes.isPending ? "A carregar..." : "Selecionar cliente"}</option>`, the dynamic `.map` of `clientes.data`, and both error/validation `<p>` blocks below (lines 140-147) unchanged.
    (4) `processoId` (lines 152-163): replace `<select id="processoId" className={selectClassName} {...form.register("processoId")}>` with `<NativeSelect id="processoId" size="default" className="w-full rounded-none" {...form.register("processoId")}>`. Keep the `<option value="">Nenhum processo associado</option>` and the dynamic `.map` of `processos.data` unchanged.
    (5) `prioridade` (lines 191-195): replace `<select id="prioridade" className={selectClassName} {...form.register("prioridade")}>` with `<NativeSelect id="prioridade" size="default" className="w-full rounded-none" {...form.register("prioridade")}>`. Keep the 3 fixed `<option>`s (ALTA/MEDIA/BAIXA, copy "Alta"/"Média"/"Baixa") unchanged — no empty option (form default is `"MEDIA"`).
    (6) `advogadoId` (lines 200-207): replace `<select id="advogadoId" className={selectClassName} {...form.register("advogadoId")}>` with `<NativeSelect id="advogadoId" size="default" className="w-full rounded-none" {...form.register("advogadoId")}>`. Keep the `<option value="">Atribuir mais tarde</option>` and the dynamic `.map` of `advogados` unchanged.
    Then delete the `const selectClassName = "...";` declaration (lines 25-26) entirely — after changes (3)-(6) it has zero remaining consumers. Do NOT delete `textareaClassName` (lines 28-29) — the descricao `<textarea>` on line 170 still uses it (out of scope). Add `import { NativeSelect } from "@/components/ui/native-select";` at the top.
  </action>
  <verify>
    <automated>grep -q 'from "@/components/ui/native-select"' "web/src/app/(dashboard)/pareceres/nova/page.tsx" && grep -q 'NativeSelect id="clienteId"' "web/src/app/(dashboard)/pareceres/nova/page.tsx" && grep -q 'NativeSelect id="processoId"' "web/src/app/(dashboard)/pareceres/nova/page.tsx" && grep -q 'NativeSelect id="prioridade"' "web/src/app/(dashboard)/pareceres/nova/page.tsx" && grep -q 'NativeSelect id="advogadoId"' "web/src/app/(dashboard)/pareceres/nova/page.tsx" && ! grep -q 'const selectClassName' "web/src/app/(dashboard)/pareceres/nova/page.tsx" && grep -q 'const textareaClassName' "web/src/app/(dashboard)/pareceres/nova/page.tsx" && grep -q 'permissions.isFetched && !canCreatePareceres' "web/src/app/(dashboard)/pareceres/nova/page.tsx" && ! grep -q 'permissions.isLoading' "web/src/app/(dashboard)/pareceres/nova/page.tsx" && ! grep -q '<select' "web/src/app/(dashboard)/pareceres/nova/page.tsx"</automated>
  </verify>
  <done>
    All 4 form fields are `NativeSelect className="w-full rounded-none"` keeping their dynamic option lists, loading/disabled behavior, and error blocks; `selectClassName` is gone; `textareaClassName` remains; both RBAC sites use `isFetched`; zero native `<select>` and zero `permissions.isLoading` remain.
  </done>
  <acceptance_criteria>
    - Source assertion: `NativeSelect id="clienteId"`, `NativeSelect id="processoId"`, `NativeSelect id="prioridade"`, `NativeSelect id="advogadoId"` all present, each with `className="w-full rounded-none"`.
    - Source assertion: `const selectClassName` absent; `const textareaClassName` present; `<select` absent (all 4 migrated).
    - Source assertion: `permissions.isFetched && !canCreatePareceres` present; `permissions.isLoading` absent (both the gate and the submit-disable clause fixed).
    - Behavior: the form loads with `clienteId` showing "A carregar..." then "Selecionar cliente" (disabled while `clientes.isPending`); selecting a cliente populates the scoped `processoId` list; submitting creates a parecer and navigates to its detail page.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → Next rewrite → Spring API | Existing parecer-create endpoint. Field values are RHF-validated (Zod) then POSTed; the server applies tenant scoping + `@PreAuthorize("hasAuthority('pareceres:create')")`. Client RBAC gate + submit-disable are defense-in-depth only. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-108-03 | Elevation of Privilege | client RBAC view gate + submit-disable on nova | mitigate | Both sites switch to `permissions.isFetched`, closing the pre-resolve render race and the "submit enabled before permissions resolve" window. Server `@PreAuthorize` remains authoritative. |
| T-108-04 | Tampering | component-identity swap (`<select>` → `NativeSelect`) | mitigate | Pure `register()`-compatible drop-in; the Zod schema (`parecerCreateFormSchema`) and submit path are unchanged, so no new/wider input is accepted. `NativeSelect` renders the same value set as the prior `<select>`. |
| T-108-SC | Tampering | npm/pnpm installs | accept | Zero new dependencies; `NativeSelect` installed in Phase 101, prior consumers exist (Processos/Documentos forms). |
</threat_model>

<verification>
- All 4 fields are `NativeSelect className="w-full rounded-none"`; zero native `<select>` remain.
- `selectClassName` deleted; `textareaClassName` preserved.
- Both RBAC sites use `permissions.isFetched`; zero `permissions.isLoading` remain in the file.
- Plan-level `pnpm build`/`pnpm lint` is covered by the Wave-2 holistic gate (Plan 04).
</verification>

<success_criteria>
- PARC-18 satisfied for the Pareceres create form (4 NativeSelect fields).
- 2 of the 7 bundled RBAC sites fixed here (pareceres/nova/page.tsx:35 gate + :219 submit-disable).
</success_criteria>

<output>
Create `.planning/phases/LEXCV-108-m-dulo-pareceres/108-02-SUMMARY.md` when done.
</output>
