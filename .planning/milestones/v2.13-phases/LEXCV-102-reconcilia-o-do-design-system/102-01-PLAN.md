---
phase: 102-reconcilia-o-do-design-system
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/components/ui/button.tsx
  - web/src/components/ui/badge.tsx
  - web/src/components/ui/input.tsx
  - web/src/components/ui/label.tsx
  - web/src/components/ui/radio-group.tsx
  - web/src/components/ui/switch.tsx
  - web/src/components/ui/textarea.tsx
  - web/src/components/ui/calendar.tsx
  - web/src/components/ui/breadcrumb.tsx
  - web/package.json
autonomous: true
requirements: [DSR-01, DSR-02]
must_haves:
  truths:
    - "Button keeps all 5 variants (default/secondary/outline/ghost/link) and 4 sizes byte-for-byte — no bg-primary swap"
    - "Badge keeps all 9 variants including the custom gray; the 3 gray call sites still compile"
    - "Form primitives (input/label/radio-group/switch/textarea) keep their neutral-scale styling unchanged"
    - "buttonVariants is exported from button.tsx and imported by calendar.tsx (no duplicated CVA config)"
    - "Slot import aliasing is identical between button.tsx and breadcrumb.tsx"
    - "shadcn CLI package lives in devDependencies, not dependencies"
    - "pnpm build passes — the 93 imports across 38 consumer files still compile and typecheck"
  artifacts:
    - path: "web/src/components/ui/button.tsx"
      provides: "Reconciled Button primitive with exported buttonVariants"
      contains: "export { Button, buttonVariants }"
    - path: "web/src/components/ui/badge.tsx"
      provides: "Reconciled Badge with all 9 variants preserved"
      contains: "gray:"
    - path: "web/src/components/ui/calendar.tsx"
      provides: "Calendar consuming buttonVariants from button.tsx (deduped)"
      contains: "buttonVariants"
    - path: "web/package.json"
      provides: "shadcn moved to devDependencies"
  key_links:
    - from: "web/src/components/ui/calendar.tsx"
      to: "web/src/components/ui/button.tsx"
      via: "import buttonVariants"
      pattern: "buttonVariants.*from.*@/components/ui/button"
    - from: "web/src/app/(dashboard)/processos/page.tsx"
      to: "web/src/components/ui/badge.tsx"
      via: "Badge variant=gray"
      pattern: "variant=.*gray"
---

<objective>
Reconcile the Rule-C "component-identity" primitives (`button`, `badge`, and the five form primitives `input`/`label`/`radio-group`/`switch`/`textarea`) against the official shadcn registry using the diff-first protocol — preserving every custom variant/prop byte-for-byte — and close the three inherited Phase-101 code-review findings that require editing `button.tsx` (which was reserved for this phase): the duplicated `buttonVariants` CVA config, the inconsistent `Slot` alias, and the misplaced `shadcn` dependency.

Purpose: These files carry the app's visual identity (button's 5 variants, badge's 9 variants including `gray`). A blind CLI overwrite would silently swap them to `--primary` blue — an unrequested redesign. This plan reconciles them without any visible color change, so it needs no human checkpoint. It runs in parallel with the surface-reconciliation plan (102-02) and the tooltip plan (102-03) — zero file overlap.
Output: 9 reconciled/cleaned files that produce identical rendered pixels to today, with the inherited dedup debt closed.
</objective>

<execution_context>
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/LEXCV-102-reconcilia-o-do-design-system/102-UI-SPEC.md
@.planning/phases/LEXCV-102-reconcilia-o-do-design-system/102-PATTERNS.md
@.planning/phases/LEXCV-102-reconcilia-o-do-design-system/102-CONTEXT.md

<interfaces>
<!-- Authoritative reconciliation rule from 102-UI-SPEC.md Color section: -->
<!-- Rule A: identical-value substitution (apply). Rule B: convergent-but-different (apply + flag). -->
<!-- Rule C: component identity — NEVER substitute this phase. button/badge/input/label/radio-group/switch/textarea are ALL Rule C. -->
<!-- Every file below is reconciled via `pnpm dlx shadcn@latest add <component> --diff` (preview only, never blind --overwrite), then hand-edited. -->
<!-- Button current API (must be preserved exactly): -->
<!--   variants: default | secondary | outline | ghost | link  (5) -->
<!--   sizes: default | sm | lg | icon  (4) -->
<!--   default = "bg-neutral-900 text-neutral-50 hover:bg-neutral-900/90"  (NOT bg-primary) -->
<!--   asChild via SlotPrimitive.Slot; plain function component (NOT forwardRef) — calendar.tsx casts it, do not change that -->
<!-- Badge current API (must be preserved exactly): 9 variants -->
<!--   default | secondary | outline | blue | green | amber | red | purple | gray -->
<!-- calendar.tsx today duplicates button.tsx's buttonVariants CVA byte-for-byte (calendar.tsx:21-45). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reconcile button.tsx and badge.tsx (Rule C) + export buttonVariants + Slot alias</name>
  <files>web/src/components/ui/button.tsx, web/src/components/ui/badge.tsx</files>
  <read_first>
    - web/src/components/ui/button.tsx (current full content — diff target)
    - web/src/components/ui/badge.tsx (current full content — diff target)
    - web/src/components/ui/breadcrumb.tsx (to see its current Slot alias convention: `import { Slot } from "radix-ui"` used as `Slot.Root`)
    - .planning/phases/LEXCV-102-reconcilia-o-do-design-system/102-UI-SPEC.md (Color section, Rule C rows for button/badge)
    - .planning/phases/LEXCV-102-reconcilia-o-do-design-system/102-PATTERNS.md (button.tsx + badge.tsx pattern assignments, full current content quoted)
    - .planning/phases/LEXCV-101-funda-o-cli-init-e-design-tokens/101-REVIEW.md (findings IN-04 Slot alias, IN-05 buttonVariants dedup)
  </read_first>
  <action>
    Run `pnpm dlx shadcn@latest add button --diff` and `pnpm dlx shadcn@latest add badge --diff` from web/ to preview the registry delta (preview only — NEVER pass --overwrite). Read each diff, then hand-edit as follows.

    button.tsx (Rule C — preserve identity): Keep all 5 variant classes exactly as today — default = "bg-neutral-900 text-neutral-50 hover:bg-neutral-900/90", secondary = "bg-neutral-100 text-neutral-900 hover:bg-neutral-100/80", outline (with its dark: classes), ghost, link. Keep all 4 sizes exactly — default "h-9 px-4 py-2", sm "h-8 rounded-md px-3 text-xs", lg "h-10 rounded-md px-8", icon "h-9 w-9". Do NOT adopt the upstream registry's bg-primary-based default variant, its new `destructive` variant, or its `xs` size — per 102-UI-SPEC.md the `--primary` institutional blue is reserved for a closed list that excludes button.tsx. Keep it a plain function component (do NOT add React.forwardRef) — calendar.tsx casts it and relies on React 19 ref-through-props (confirmed safe by 101-REVIEW.md WR-03). Accept only genuine structural/a11y additions the diff reveals as truly missing (audit for any dropped prop before accepting). Then add `buttonVariants` to the export statement so it becomes `export { Button, buttonVariants }` (closes 101-REVIEW.md IN-05). Standardize the Slot alias: keep button.tsx's existing `import { Slot as SlotPrimitive } from "radix-ui"` + `SlotPrimitive.Slot` convention — this is the convention breadcrumb.tsx will be changed to match in Task 3.

    badge.tsx (Rule C — preserve all 9 variants): Keep default/secondary/outline neutral values AND all 6 custom color variants (blue, green, amber, red, purple, gray) verbatim. gray = "border-transparent bg-neutral-100 text-neutral-700 dark:bg-neutral-800 dark:text-neutral-400". Keep `rounded-full` (badge is a pill, not affected by --radius) and defaultVariants variant:"secondary". Do NOT drop any variant. Preserve the `data-slot="badge"` attribute.
  </action>
  <verify>
    <automated>cd web && grep -c "gray:" src/components/ui/badge.tsx && grep -q "export { Button, buttonVariants }" src/components/ui/button.tsx && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - button.tsx exports both Button and buttonVariants (exact string `export { Button, buttonVariants }` present)
    - button.tsx still declares exactly the 5 variants default/secondary/outline/ghost/link and the 4 sizes default/sm/lg/icon; grep confirms `bg-neutral-900` present in the default variant and grep confirms `bg-primary` is ABSENT from button.tsx
    - badge.tsx still declares all 9 variants; grep `gray:` returns 1, and grep for each of blue/green/amber/red/purple returns a match
    - button.tsx uses `import { Slot as SlotPrimitive }` and `SlotPrimitive.Slot` (unchanged aliasing)
    - button.tsx remains a plain `export function Button` (no `React.forwardRef` added)
  </acceptance_criteria>
  <done>Button and Badge reconciled with zero color/variant loss; buttonVariants exported; grep confirms the identity classes survive.</done>
</task>

<task type="auto">
  <name>Task 2: Reconcile the five form primitives (Rule C — audit, expect no color change)</name>
  <files>web/src/components/ui/input.tsx, web/src/components/ui/label.tsx, web/src/components/ui/radio-group.tsx, web/src/components/ui/switch.tsx, web/src/components/ui/textarea.tsx</files>
  <read_first>
    - web/src/components/ui/input.tsx (current full content)
    - web/src/components/ui/label.tsx (current full content)
    - web/src/components/ui/radio-group.tsx (current full content)
    - web/src/components/ui/switch.tsx (current full content)
    - web/src/components/ui/textarea.tsx (current full content)
    - .planning/phases/LEXCV-102-reconcilia-o-do-design-system/102-UI-SPEC.md (Color table row: input/textarea/label/radio-group/switch = Rule C, "none — preserve all current neutral-scale values")
    - .planning/phases/LEXCV-102-reconcilia-o-do-design-system/102-PATTERNS.md (the input/label/radio-group/switch/textarea section: none contain rounded-none or dark:bg-[#020617] magic-hex — expect the diff to show no color/radius changes to accept)
  </read_first>
  <action>
    Run `pnpm dlx shadcn@latest add input --diff`, `... add label --diff`, `... add radio-group --diff`, `... add switch --diff`, `... add textarea --diff` from web/ (preview only, never --overwrite). These are all Rule C: preserve every current neutral-scale value exactly (border-neutral-200, dark:border-neutral-800, placeholder-neutral-*, focus ring, checked-state fill). Per the pattern map, none of these five files contain `rounded-none` or a `dark:bg-[#020617]`/`dark:bg-slate-950` magic-hex surface color, so the expected outcome is that the diff surfaces NO color/radius change to accept. Do NOT swap any neutral value to `--primary`/`bg-primary`. Accept only a genuinely-missing accessibility attribute if the diff reveals one (grep the file's current state first to confirm it is actually absent before adding). Preserve every `data-slot` attribute and the unified `radix-ui` import convention (e.g. `import { Label as LabelPrimitive } from "radix-ui"`) — do NOT reintroduce any scoped `@radix-ui/react-*` import.
  </action>
  <verify>
    <automated>cd web && ! grep -RIln "bg-primary\|@radix-ui/react-" src/components/ui/input.tsx src/components/ui/label.tsx src/components/ui/radio-group.tsx src/components/ui/switch.tsx src/components/ui/textarea.tsx && echo "no bg-primary / no scoped radix imports"</automated>
  </verify>
  <acceptance_criteria>
    - None of the five files contains `bg-primary` or a scoped `@radix-ui/react-*` import (grep returns nothing)
    - Each file retains its `data-slot` attribute
    - No `rounded-none`→other-radius or neutral→primary substitution was applied (the files' color classes are unchanged from the pre-task state except a genuinely-missing a11y attribute, if any)
  </acceptance_criteria>
  <done>All five form primitives audited via diff-first; neutral-scale identity preserved; no bg-primary/scoped-radix regression.</done>
</task>

<task type="auto">
  <name>Task 3: Dedup buttonVariants in calendar.tsx, uniform Slot alias in breadcrumb.tsx, move shadcn to devDependencies, verify build</name>
  <files>web/src/components/ui/calendar.tsx, web/src/components/ui/breadcrumb.tsx, web/package.json</files>
  <read_first>
    - web/src/components/ui/calendar.tsx (lines ~16-53: the duplicated CVA config block + the ButtonWithRef cast that must be preserved)
    - web/src/components/ui/breadcrumb.tsx (lines ~2 and ~48: `import { Slot } from "radix-ui"` and `Slot.Root`)
    - web/src/components/ui/button.tsx (as edited in Task 1 — confirm it now exports buttonVariants and uses SlotPrimitive.Slot)
    - web/package.json (dependencies block containing `"shadcn": "^4.13.0"`, and the devDependencies block)
    - .planning/phases/LEXCV-102-reconcilia-o-do-design-system/102-PATTERNS.md (calendar.tsx dedupe section; breadcrumb.tsx Slot aliasing section; package.json one-line-move section)
    - .planning/phases/LEXCV-101-funda-o-cli-init-e-design-tokens/101-REVIEW.md (IN-01 unnecessary eslint-disable, IN-04, IN-05, IN-06)
  </read_first>
  <action>
    calendar.tsx: Remove the locally-duplicated `const buttonVariants = cva(...)` block (calendar.tsx ~lines 21-45) and its explanatory comment (~lines 16-20), and instead import buttonVariants from button.tsx — change the existing button import to `import { Button, buttonVariants } from "@/components/ui/button"`. PRESERVE the `ButtonWithRef` cast (~lines 51-53) and its use in `CalendarDayButton` verbatim — this is an unrelated ref-forwarding workaround (101-REVIEW.md WR-03), not styling duplication. Optionally (bonus, closes 101-REVIEW.md IN-01) remove the now-unnecessary `// eslint-disable-next-line react-hooks/exhaustive-deps` above the mount-check effect's `[]` dependency array — the effect only reads the stable `ref`, so the directive is a no-op.

    breadcrumb.tsx: Standardize the Slot alias to match button.tsx — change `import { Slot } from "radix-ui"` to `import { Slot as SlotPrimitive } from "radix-ui"` and change the `asChild ? Slot.Root : "a"` usage to `asChild ? SlotPrimitive.Slot : "a"` (closes 101-REVIEW.md IN-04). Verify `Slot.Root` and `SlotPrimitive.Slot` resolve to the same primitive — both are the radix-ui Slot; the app already renders breadcrumb this way, so this is a pure rename.

    web/package.json: Remove `"shadcn": "^4.13.0"` from the `dependencies` block and add it to `devDependencies` alphabetically between `eslint-config-next` and `tailwindcss` (closes 101-REVIEW.md IN-06 / 101-UI-REVIEW.md Priority Fix #3). Then run `pnpm install` in web/ to update pnpm-lock.yaml's classification.

    Finally run the build gate. NOTE (Phase 101 learning): if this plan executes in an isolated git worktree, run `pnpm install` in web/ first — worktree node_modules do NOT sync from the main checkout. grep-only verification is insufficient (Phase 101 missed a real cascade bug that way); `pnpm build` is the definitive gate.
  </action>
  <verify>
    <automated>cd web && pnpm install && grep -q "buttonVariants } from \"@/components/ui/button\"" src/components/ui/calendar.tsx && grep -q "SlotPrimitive.Slot" src/components/ui/breadcrumb.tsx && ! grep -A20 '"dependencies"' package.json | grep -q '"shadcn"' && pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - calendar.tsx imports buttonVariants from "@/components/ui/button" and no longer contains a local `const buttonVariants = cva(` declaration (grep for `const buttonVariants = cva` in calendar.tsx returns nothing)
    - calendar.tsx still contains the ButtonWithRef cast (preserved)
    - breadcrumb.tsx uses `SlotPrimitive.Slot` (grep match) and no longer uses `Slot.Root`
    - `"shadcn"` appears under devDependencies and NOT under dependencies in web/package.json
    - `pnpm build` exits 0 (Next build typechecks all app files — the 93 imports across 38 consumer files still compile)
  </acceptance_criteria>
  <done>buttonVariants deduplicated, Slot alias uniform, shadcn moved to devDependencies, and `pnpm build` is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| shadcn registry (network) → local files | `add <component> --diff` fetches registry JSON at scaffold time; used for PREVIEW only in this plan, no file is written by the CLI — all edits are hand-applied |
| build tooling → production bundle | package.json/lockfile classification change affects what ships in prod deps |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-102-01 | Tampering | button.tsx / badge.tsx variant loss | mitigate | Diff-first protocol (never blind --overwrite) + grep gate confirming all 5 button / 9 badge variants survive + `pnpm build` typecheck of all call sites |
| T-102-02 | Information Disclosure | none (pure client-side styling, no data/auth surface touched) | accept | Reconciliation touches only presentational primitives; no RBAC/tenant/data-flow code in scope |
| T-102-SC | Supply Chain | npm/pnpm installs | accept | NO new packages installed this plan — all Radix deps were added and unified in Phase 101. The only package.json change is MOVING the existing pinned `shadcn` (already vetted in Phase 101's legitimacy gate) from dependencies to devDependencies; `pnpm install` only re-classifies, adds nothing new. No package-legitimacy checkpoint required (RESEARCH.md confirms zero net-new installs this phase). |
</threat_model>

<verification>
- `pnpm build` passes in web/ (definitive gate — typechecks all app files, proves the 93 imports / 38 consumer files still compile).
- grep confirms button (5 variants, `bg-neutral-900` default, no `bg-primary`) and badge (9 variants incl `gray`) identity survives.
- grep confirms no scoped `@radix-ui/react-*` import reintroduced in any touched file.
- grep confirms `shadcn` is under devDependencies, absent from dependencies.
- calendar.tsx imports buttonVariants (no duplicate CVA); breadcrumb.tsx uses the unified SlotPrimitive.Slot alias.
</verification>

<success_criteria>
- The 7 Rule-C components (button, badge, input, label, radio-group, switch, textarea) render identical pixels to before — no color/variant/radius change.
- Inherited Phase-101 debt closed: buttonVariants exported+deduped, Slot alias uniform, shadcn in devDependencies.
- `pnpm build` green.
</success_criteria>

<output>
Create `.planning/phases/LEXCV-102-reconcilia-o-do-design-system/102-01-SUMMARY.md` when done.
</output>
