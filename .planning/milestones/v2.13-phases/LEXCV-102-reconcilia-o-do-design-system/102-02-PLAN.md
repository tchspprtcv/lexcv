---
phase: 102-reconcilia-o-do-design-system
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/components/ui/card.tsx
  - web/src/components/ui/popover.tsx
  - web/src/components/ui/dialog.tsx
  - web/src/components/ui/alert-dialog.tsx
  - web/src/components/ui/table.tsx
  - web/src/components/ui/sheet.tsx
autonomous: true
requirements: [DSR-01, DSR-02]
must_haves:
  truths:
    - "Card, dialog, alert-dialog and popover surfaces reference --card/--popover tokens in dark mode (elevation contrast) instead of the flat dark:bg-[#020617]/dark:bg-slate-950 magic hex"
    - "rounded-none literals in card/dialog/alert-dialog are replaced by the tokenized rounded-lg (resolves to 0 today via --radius:0rem)"
    - "bg-white surfaces reference the exact-match bg-card/bg-popover token"
    - "AlertDialogAction/AlertDialogCancel button colors and the sr-only Fechar labels are preserved verbatim (Rule C)"
    - "pnpm build passes — all consumer files of these 6 components still compile and typecheck"
  artifacts:
    - path: "web/src/components/ui/card.tsx"
      provides: "Card with tokenized dark surface + radius"
      contains: "dark:bg-card"
    - path: "web/src/components/ui/dialog.tsx"
      provides: "Dialog with tokenized popover surface + preserved Fechar label"
      contains: "Fechar"
    - path: "web/src/components/ui/popover.tsx"
      provides: "Popover with tokenized dark surface"
      contains: "dark:bg-popover"
    - path: "web/src/components/ui/alert-dialog.tsx"
      provides: "AlertDialog with tokenized surface + preserved Action/Cancel identity"
      contains: "dark:bg-popover"
  key_links:
    - from: "web/src/components/ui/card.tsx"
      to: "globals.css --card token"
      via: "dark:bg-card utility"
      pattern: "dark:bg-card"
    - from: "web/src/components/ui/dialog.tsx"
      to: "globals.css --popover token"
      via: "dark:bg-popover utility"
      pattern: "dark:bg-popover"
---

<objective>
Reconcile the six "surface" primitives (`card`, `popover`, `dialog`, `alert-dialog`, `table`, `sheet`) against the official shadcn registry, applying the Rule-A (identical-value) and Rule-B (convergent-but-different) substitutions from 102-UI-SPEC.md: swap hardcoded `rounded-none`→`rounded-lg` and `bg-white`→`bg-card`/`bg-popover` (Rule A, visually identical today), and swap the flat `dark:bg-[#020617]`/`dark:bg-slate-950` magic hex → `dark:bg-card`/`dark:bg-popover` (Rule B — a REAL, intentional dark-mode elevation improvement).

Purpose: These four surface components currently render with ZERO elevation contrast from the page background in dark mode (all four dark surfaces are literally `#020617`, the same as `--background` dark). Adopting `--card`/`--popover` (`oklch(0.205 0 0)`, a lighter gray) is the semantically-correct elevation fix. Because it changes rendered pixels, plan 102-04 will put it in front of the mandatory human visual checkpoint (light+dark). This plan produces the change; it does NOT sign it off. Runs in parallel with 102-01 and 102-03 (zero file overlap).
Output: 6 reconciled surface files; the visible delta is dark-mode elevation on cards/dialogs/popovers.
</objective>

<execution_context>
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/LEXCV-102-reconcilia-o-do-design-system/102-UI-SPEC.md
@.planning/phases/LEXCV-102-reconcilia-o-do-design-system/102-PATTERNS.md
@.planning/phases/LEXCV-102-reconcilia-o-do-design-system/102-CONTEXT.md

<interfaces>
<!-- Token resolution (web/src/app/globals.css, from Phase 101 — do NOT edit globals.css this phase): -->
<!--   --radius: 0rem  → rounded-lg resolves through --radius-lg → var(--radius) → 0 (identical pixel to rounded-none today) -->
<!--   light: --card = oklch(1 0 0) = white (exact match for bg-white); --popover = oklch(1 0 0) = white -->
<!--   dark:  --card = oklch(0.205 0 0) (lighter gray); --popover = oklch(0.205 0 0) (lighter gray) -->
<!--   dark:  --background = #020617 (flat). Tailwind slate-950 hex == #020617 == today's flat dark surface. -->
<!-- Reconciliation buckets per 102-UI-SPEC.md Color table: -->
<!--   card:         A (rounded-none→rounded-lg, bg-white→bg-card) + B (dark:bg-[#020617]→dark:bg-card) -->
<!--   dialog:       A (rounded-none→rounded-lg, bg-white→bg-popover) + B (dark:bg-[#020617]→dark:bg-popover) -->
<!--   alert-dialog: A + B (same as dialog) + C (AlertDialogAction bg-neutral-900 / Cancel outline PRESERVED) -->
<!--   popover:      B only (dark:bg-slate-950→dark:bg-popover); rounded-md already fine -->
<!--   table:        B optional/Claude's discretion (bg-neutral-50/dark:bg-neutral-900/30 → bg-muted/hover:bg-muted/50) -->
<!--   sheet:        cosmetic only, hand-edit color literals (bg-white→bg-popover exact; dark:bg-neutral-950 discretion), NEVER re-scaffold -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reconcile card.tsx and popover.tsx (Rule A radius + Rule B dark surface)</name>
  <files>web/src/components/ui/card.tsx, web/src/components/ui/popover.tsx</files>
  <read_first>
    - web/src/components/ui/card.tsx (current full content — the Card root className is the diff target)
    - web/src/components/ui/popover.tsx (current full content — PopoverContent className is the diff target)
    - .planning/phases/LEXCV-102-reconcilia-o-do-design-system/102-UI-SPEC.md (Color table rows: card, popover; and the "Why Rule B is a real, expected change" note)
    - .planning/phases/LEXCV-102-reconcilia-o-do-design-system/102-PATTERNS.md (card.tsx section with concrete substitutions; popover.tsx section)
  </read_first>
  <action>
    Run `pnpm dlx shadcn@latest add card --diff` and `... add popover --diff` from web/ (preview only, never --overwrite). Then hand-edit.

    card.tsx (Card root className): apply `rounded-none`→`rounded-lg` (Rule A), `bg-white`→`bg-card` (Rule A exact match), and `dark:bg-[#020617]`→`dark:bg-card` (Rule B — the intentional elevation change). Preserve `hover:shadow-md`, the border treatment (border-slate-200 / dark:border-slate-800), and the text colors (text-slate-950 / dark:text-slate-50) — leave those unless the diff maps them onto text-card-foreground (an acceptable equivalent). Keep CardHeader/CardTitle/CardDescription/CardContent/CardFooter and the `data-slot="card"` attribute unchanged.

    popover.tsx (PopoverContent className): apply only the Rule-B substitution `dark:bg-slate-950`→`dark:bg-popover`. Preserve `rounded-md` (already token-friendly, no radius change), the border/shadow/animation classes, and the existing `React.forwardRef` structure. Do not touch bg-white handling if present (light --popover = white, exact match).
  </action>
  <verify>
    <automated>cd web && grep -q "dark:bg-card" src/components/ui/card.tsx && grep -q "rounded-lg" src/components/ui/card.tsx && ! grep -q "bg-\[#020617\]" src/components/ui/card.tsx && grep -q "dark:bg-popover" src/components/ui/popover.tsx && ! grep -q "slate-950" src/components/ui/popover.tsx && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - card.tsx contains `dark:bg-card`, `bg-card`, `rounded-lg`; contains NO `bg-[#020617]` and NO `rounded-none`
    - card.tsx still contains `hover:shadow-md` and the `data-slot="card"` attribute
    - popover.tsx contains `dark:bg-popover`; contains NO `dark:bg-slate-950`
    - popover.tsx still contains `rounded-md` and its animation/data-[side] classes
  </acceptance_criteria>
  <done>Card and Popover surfaces are tokenized; dark-mode elevation change applied; Rule-A radius/bg swaps done.</done>
</task>

<task type="auto">
  <name>Task 2: Reconcile dialog.tsx and alert-dialog.tsx (Rule A + B + Rule C on action buttons + preserve Fechar)</name>
  <files>web/src/components/ui/dialog.tsx, web/src/components/ui/alert-dialog.tsx</files>
  <read_first>
    - web/src/components/ui/dialog.tsx (current full content — DialogContent className ~line 41, DialogPrimitive.Close with the sr-only Fechar label ~line 49)
    - web/src/components/ui/alert-dialog.tsx (current full content — AlertDialogContent ~line 39, AlertDialogAction/AlertDialogCancel ~lines 96-124)
    - .planning/phases/LEXCV-102-reconcilia-o-do-design-system/102-UI-SPEC.md (Color table rows: dialog, alert-dialog — note showCloseButton decision and Rule C on Action/Cancel)
    - .planning/phases/LEXCV-102-reconcilia-o-do-design-system/102-PATTERNS.md (dialog.tsx + alert-dialog.tsx sections with concrete substitutions)
  </read_first>
  <action>
    Run `pnpm dlx shadcn@latest add dialog --diff` and `... add alert-dialog --diff` from web/ (preview only, never --overwrite). Then hand-edit.

    dialog.tsx (DialogContent className): apply `rounded-none`→`rounded-lg` (Rule A), `bg-white`→`bg-popover` (Rule A exact match), `dark:bg-[#020617]`→`dark:bg-popover` (Rule B). PRESERVE verbatim the DialogPrimitive.Close markup and its `<span className="sr-only">Fechar</span>` accessibility label — do NOT drop or re-translate it. Do NOT add the registry's `showCloseButton` prop: default to today's always-visible close button (per 102-UI-SPEC.md, only reconsider if a grep of call sites shows one manually renders its own close affordance inside DialogContent — grep confirms none does). Keep the animation/data-[state] classes and `data-slot="dialog-content"`.

    alert-dialog.tsx: On AlertDialogContent apply the same three substitutions as dialog (`rounded-none`→`rounded-lg`, `bg-white`→`bg-popover`, `dark:bg-[#020617]`→`dark:bg-popover`). On AlertDialogAction and AlertDialogCancel — Rule C, PRESERVE verbatim: AlertDialogAction keeps its hardcoded `bg-neutral-900 text-neutral-50 hover:bg-neutral-900/90`; AlertDialogCancel keeps its hardcoded outline styling. Do NOT retarget either to `--primary`/`--destructive` — call sites needing a destructive-red confirm already pass their own override className, unaffected by this reconciliation. Preserve any sr-only labels.
  </action>
  <verify>
    <automated>cd web && grep -q "Fechar" src/components/ui/dialog.tsx && grep -q "dark:bg-popover" src/components/ui/dialog.tsx && ! grep -q "bg-\[#020617\]" src/components/ui/dialog.tsx && grep -q "bg-neutral-900" src/components/ui/alert-dialog.tsx && grep -q "dark:bg-popover" src/components/ui/alert-dialog.tsx && ! grep -q "bg-\[#020617\]" src/components/ui/alert-dialog.tsx && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - dialog.tsx contains the sr-only `Fechar` label, `dark:bg-popover`, `rounded-lg`; contains NO `bg-[#020617]` and NO `rounded-none`
    - dialog.tsx has NOT gained a `showCloseButton` prop
    - alert-dialog.tsx contains `dark:bg-popover`, `rounded-lg`; contains NO `bg-[#020617]`
    - alert-dialog.tsx AlertDialogAction still contains `bg-neutral-900` (Rule C preserved); no `bg-primary`/`bg-destructive` added to Action/Cancel
  </acceptance_criteria>
  <done>Dialog and AlertDialog surfaces tokenized; Fechar labels and Action/Cancel identity preserved; no showCloseButton drift.</done>
</task>

<task type="auto">
  <name>Task 3: Reconcile table.tsx (optional Rule B) and sheet.tsx (cosmetic), then verify build</name>
  <files>web/src/components/ui/table.tsx, web/src/components/ui/sheet.tsx</files>
  <read_first>
    - web/src/components/ui/table.tsx (current full content — TableFooter/TableRow neutral-scale classes)
    - web/src/components/ui/sheet.tsx (current full content — SheetContent color literals ~lines 43-72, the sr-only Fechar label ~line 67, sideVariants)
    - .planning/phases/LEXCV-102-reconcilia-o-do-design-system/102-UI-SPEC.md (Color table rows: table, sheet — both Claude's Discretion)
    - .planning/phases/LEXCV-102-reconcilia-o-do-design-system/102-PATTERNS.md (table.tsx + sheet.tsx sections)
  </read_first>
  <action>
    Run `pnpm dlx shadcn@latest add table --diff` and `... add sheet --diff` from web/ (preview only, never --overwrite).

    table.tsx (Rule B optional — Claude's Discretion): You MAY converge `bg-neutral-50`/`dark:bg-neutral-900/30` (TableFooter, TableRow hover/selected) to `bg-muted`/`hover:bg-muted/50` — low risk. If you apply it, it will be visually confirmed at the 102-04 checkpoint. PRESERVE verbatim the TableCaption/TableHead/TableCell neutral-scale text colors (Rule C — no accent tint on table chrome) and every `data-slot` attribute. If you judge the muted convergence not clearly identical, leave the literals as-is and note the decision in the summary.

    sheet.tsx (structurally FROZEN per CONTEXT.md — hand-edit color literals ONLY, never `add sheet --overwrite`): apply `bg-white`→`bg-popover` (exact match, no visual change). For `dark:bg-neutral-950` (a third distinct value from both --background and --card/--popover) — Claude's Discretion: recommended to converge to `dark:bg-popover` for consistency with dialog/card's newly-adopted elevated surface; confirm the choice at the 102-04 checkpoint. PRESERVE the `sr-only` "Fechar" label and the `sideVariants` structure verbatim. Do NOT re-scaffold the file.

    Then run the build gate. NOTE (Phase 101 learning): if executing in an isolated worktree, run `pnpm install` in web/ first — worktree node_modules do NOT sync from the main checkout. `pnpm build` is the definitive gate; grep alone is insufficient.
  </action>
  <verify>
    <automated>cd web && pnpm install && grep -q "Fechar" src/components/ui/sheet.tsx && ! grep -q "bg-white" src/components/ui/sheet.tsx && pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - sheet.tsx contains `bg-popover` (replacing bg-white) and still contains the sr-only `Fechar` label and `sideVariants`
    - sheet.tsx was hand-edited, not re-scaffolded (git diff shows only color-literal line changes, no structural rewrite)
    - table.tsx TableCaption/TableHead/TableCell text colors are unchanged (Rule C); every data-slot attribute preserved
    - `pnpm build` exits 0 (all consumers of card/popover/dialog/alert-dialog/table/sheet still compile)
  </acceptance_criteria>
  <done>Table and Sheet reconciled per discretion rules; Fechar preserved; `pnpm build` green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| shadcn registry (network) → local files | `add <component> --diff` fetches registry JSON for PREVIEW only; no file written by CLI, all edits hand-applied |
| client render → user | dark-mode surface color changes are the only user-observable effect |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-102-03 | Tampering | dialog/alert-dialog dropped a11y (sr-only Fechar) or dropped Action/Cancel identity | mitigate | Diff-first protocol + explicit grep gate asserting `Fechar` and `bg-neutral-900` survive; Rule C preserves Action/Cancel colors byte-for-byte |
| T-102-04 | Tampering | Rule-B dark surface applied silently without human sign-off | mitigate | This plan produces the change; plan 102-04's mandatory human visual checkpoint (light+dark, getComputedStyle) signs it off before phase close |
| T-102-05 | Information Disclosure | none (presentational surfaces only, no data/auth/tenant code) | accept | Reconciliation touches only styling literals on client components |
| T-102-SC | Supply Chain | npm/pnpm installs | accept | NO net-new packages this plan — all Radix deps present since Phase 101; `pnpm install` only refreshes the existing lockfile. No package-legitimacy checkpoint required. |
</threat_model>

<verification>
- `pnpm build` passes in web/ (definitive gate — all consumers of the 6 surface components compile/typecheck).
- grep confirms `dark:bg-[#020617]`/`dark:bg-slate-950` fully removed from card/dialog/alert-dialog/popover; replaced by `dark:bg-card`/`dark:bg-popover`.
- grep confirms `rounded-none` removed from card/dialog/alert-dialog (replaced by `rounded-lg`).
- grep confirms the sr-only `Fechar` label survives in dialog.tsx and sheet.tsx, and `bg-neutral-900` survives in alert-dialog.tsx (Rule C).
- The visible Rule-B elevation change is left for the 102-04 human visual checkpoint to sign off (NOT auto-approved here).
</verification>

<success_criteria>
- Card/dialog/alert-dialog/popover reference `--card`/`--popover` tokens in dark mode (elevation contrast), and tokenized `rounded-lg`/`bg-card`/`bg-popover` for Rule-A cases.
- AlertDialog action-button identity and all sr-only Fechar labels preserved.
- `pnpm build` green.
</success_criteria>

<output>
Create `.planning/phases/LEXCV-102-reconcilia-o-do-design-system/102-02-SUMMARY.md` when done. Explicitly record in the summary which Rule-B/optional substitutions were applied (table muted convergence yes/no; sheet dark surface choice) so the 102-04 checkpoint knows what to look at.
</output>
