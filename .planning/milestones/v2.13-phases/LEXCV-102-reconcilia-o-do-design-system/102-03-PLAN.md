---
phase: 102-reconcilia-o-do-design-system
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/app/providers.tsx
  - web/src/app/(dashboard)/clientes/page.tsx
  - web/src/app/(dashboard)/settings/page.tsx
  - web/src/components/shared/dashboard-shell.tsx
autonomous: true
requirements: [DSR-02, DSR-03]
must_haves:
  truths:
    - "A single TooltipProvider is mounted in providers.tsx with delayDuration explicitly set to 700"
    - "The clientes row-action icon buttons (Eye/Printer/Pencil/Trash2 desktop, Eye/Pencil mobile) show a Portuguese tooltip on hover/focus and carry a matching aria-label"
    - "The settings user-table row-action icon buttons (Edit always + Trash2 when user.id !== currentUserId) show a Portuguese tooltip on hover/focus and carry a matching aria-label"
    - "The sidebar footer LogOut icon button (desktop + mobile drawer) shows a Terminar sessão tooltip and carries a matching aria-label"
    - "A broadened grep across ALL web/src/app/(dashboard)/**/page.tsx (searching Edit as well as Eye/Pencil/Trash2/Printer) leaves no explicit-action icon-only row button unwrapped outside the recorded out-of-scope set"
    - "pnpm build passes — the double-asChild (TooltipTrigger asChild > Button asChild > Link) nesting compiles"
  artifacts:
    - path: "web/src/app/providers.tsx"
      provides: "Global single TooltipProvider mount"
      contains: "delayDuration={700}"
    - path: "web/src/app/(dashboard)/clientes/page.tsx"
      provides: "Row-action icon buttons wrapped in Tooltip"
      contains: "TooltipContent"
    - path: "web/src/app/(dashboard)/settings/page.tsx"
      provides: "User-table row-action Edit/Trash2 icon buttons wrapped in Tooltip + aria-label"
      contains: "TooltipContent"
    - path: "web/src/components/shared/dashboard-shell.tsx"
      provides: "Sidebar LogOut button wrapped in Tooltip"
      contains: "Terminar sessão"
  key_links:
    - from: "web/src/app/providers.tsx"
      to: "web/src/components/ui/tooltip.tsx"
      via: "TooltipProvider import + mount"
      pattern: "TooltipProvider delayDuration=\\{700\\}"
    - from: "web/src/app/(dashboard)/clientes/page.tsx"
      to: "web/src/components/ui/tooltip.tsx"
      via: "Tooltip/TooltipTrigger/TooltipContent import"
      pattern: "TooltipTrigger"
    - from: "web/src/app/(dashboard)/settings/page.tsx"
      to: "web/src/components/ui/tooltip.tsx"
      via: "Tooltip/TooltipTrigger/TooltipContent import"
      pattern: "TooltipTrigger"
---

<objective>
Deliver DSR-03: mount a single `TooltipProvider` (with `delayDuration={700}`) in `web/src/app/providers.tsx`, and wrap the app's literal icon-only row-action buttons — the clientes row actions, the settings user-table row actions, and the sidebar footer logout — in `Tooltip`, adding a matching `aria-label` to each trigger.

Purpose: These icon-only buttons ship today with zero accessible label and zero tooltip. DSR-03's literal scope is the app's icon-only row actions plus the sidebar. A broadened grep across ALL of web/src/app/(dashboard)/**/page.tsx — searching `Edit` as well as `Eye`/`Pencil`/`Trash2`/`Printer` and the `h-8 w-8 p-0` / `h-9 w-9 p-0` / `size="icon"` sizings (see the recorded audit in <interfaces>) — confirms exactly TWO explicit-action row-action surfaces: `clientes/page.tsx`'s Eye/Printer/Pencil/Trash2 buttons AND `settings/page.tsx`'s Edit/Trash2 buttons (`h-8 w-8 p-0`, the identical bare-icon pattern the earlier narrow grep missed) — plus `dashboard-shell.tsx`'s LogOut button (desktop + mobile-drawer duplicate). `processos`/`pareceres` row cells use a single bare `MoreVertical` (⋮) icon inside a `Button asChild > Link` that NAVIGATES to the detail page — a "more options / open" navigate-to-detail affordance, not an explicit-action icon like Edit/Eye/Trash2 — and are OUT of DSR-03's literal scope (do not force tooltips onto them). Calendar Chevron prev/next (agenda), the disabled pagination placeholder (processos), the back-nav arrow (processos/dashboard), and notificacoes' Check button (which already carries `aria-label` + native `title`) are likewise out of scope. Runs in parallel with 102-01 and 102-02 (zero file overlap — settings/page.tsx is a consumer page touched by neither Rule-C nor Rule-B plans). Button's asChild/Slot composition is preserved by 102-01 (Rule C), so the double-asChild nesting stays valid.
Output: 4 files; every literal icon-only row-action button in scope (clientes + settings) plus the sidebar logout gains a hover/focus tooltip + aria-label.
</objective>

<execution_context>
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/LEXCV-102-reconcilia-o-do-design-system/102-UI-SPEC.md
@.planning/phases/LEXCV-102-reconcilia-o-do-design-system/102-PATTERNS.md
@.planning/phases/LEXCV-102-reconcilia-o-do-design-system/102-CONTEXT.md

<interfaces>
<!-- tooltip.tsx (installed Phase 101) exports: Tooltip, TooltipContent, TooltipProvider, TooltipTrigger -->
<!-- CRITICAL: tooltip.tsx's TooltipProvider defaults delayDuration=0 (instant). The decided behavior is 700ms. -->
<!--   → delayDuration={700} MUST be passed EXPLICITLY at the mount in providers.tsx. Omitting it silently ships instant tooltips. -->
<!-- providers.tsx is the real client-providers wrapper (QueryClientProvider + NextThemesProvider). NOT layout.tsx. -->
<!-- Tooltip copy (pt-PT/pt-CV, authoritative from 102-UI-SPEC.md): -->
<!--   Eye → "Ver detalhes" | Pencil/Edit → "Editar" | Trash2 → "Eliminar" | Printer → "Imprimir" | LogOut → "Terminar sessão" -->
<!-- clientes/page.tsx icon-only buttons (confirmed line ranges): -->
<!--   mobile card ~455-467: Eye (Button asChild>Link, always), Pencil (Button asChild>Link, if canEditClientes) -->
<!--   desktop ClienteRow ~590-624: Eye (asChild>Link), Printer (asChild>Link target _blank), Pencil (asChild>Link if canEditClientes), Trash2 (plain Button onClick if canEditClientes) -->
<!-- settings/page.tsx icon-only buttons (confirmed line ranges ~458-473, user-table row actions): -->
<!--   Edit   (plain Button variant="ghost" h-8 w-8 p-0, onClick={() => handleEditClick(user)}, ALWAYS rendered) — copy "Editar" -->
<!--   Trash2 (plain Button variant="ghost" h-8 w-8 p-0, onClick={() => handleDeleteClick(user.id)}, rendered ONLY inside {user.id !== currentUserId && (...)}) — copy "Eliminar" -->
<!--   BOTH are plain onClick Buttons (no asChild) → use the single-level Tooltip>TooltipTrigger asChild>Button pattern (identical to the clientes Trash2 case). -->
<!--   The other h-8 w-8 p-0 Button at ~507 is the form-panel close button (rounded-full border) — NOT a row action — SKIP. -->
<!-- dashboard-shell.tsx LogOut buttons: line ~154 (desktop) and ~234 (mobile drawer duplicate), both onClick={onLogout} -->
<!-- Menu hamburger (~248) already has aria-label="Abrir menu" — SKIP (mobile-only/touch-first, per 102-UI-SPEC.md) -->
<!-- asChild nesting rule (Radix): TooltipTrigger asChild wrapping a Button asChild wrapping a Link is supported (each asChild merges one level). -->
<!-- ============================================================================ -->
<!-- RECORDED DECISION — DSR-03 "ícones da sidebar colapsada" interpretation (audit trail, do not treat as silent): -->
<!--   The dashboard has NO literal "collapsed sidebar showing only icons" mode — dashboard-shell.tsx and bottom-nav.tsx -->
<!--   ALWAYS render icon+label, never an icon-only rail. DSR-03's phrase "ícones da sidebar colapsada" is therefore -->
<!--   interpreted, deliberately and on the evidence, as the ONE genuinely icon-only control in the sidebar chrome: -->
<!--   the sidebar-footer LogOut button (desktop + mobile-drawer duplicate). This is a recorded, evidence-based reading -->
<!--   of the requirement text, NOT a silent assumption. The executor MUST restate this decision verbatim in -->
<!--   102-03-SUMMARY.md so it persists as a design-record for the phase audit trail. -->
<!-- ============================================================================ -->
<!-- RECORDED AUDIT — broadened icon-only row-action grep across web/src/app/(dashboard)/**/page.tsx: -->
<!--   Searched Edit|Eye|Pencil|Trash2|Printer AND the h-8 w-8 p-0 / h-9 w-9 p-0 / size="icon" sizings. (The earlier -->
<!--   narrow Eye|Pencil|Trash2|Printer grep in 102-PATTERNS.md omitted `Edit` and thus MISSED settings/page.tsx — this -->
<!--   is the corrected, exhaustive search.) Result — exactly TWO explicit-action row-action surfaces in scope: -->
<!--   clientes/page.tsx and settings/page.tsx. Verified OUT of scope (do NOT wrap): -->
<!--     • processos/page.tsx:381 & pareceres/page.tsx:469 = bare MoreVertical (⋮) Button asChild>Link → navigate-to-detail -->
<!--       affordance, not an explicit-action icon; -->
<!--     • agenda/page.tsx:198/219 = ChevronLeft/ChevronRight calendar month prev/next nav; -->
<!--     • notificacoes/page.tsx:312 = Check "Marcar como lida" — already has aria-label + native title; -->
<!--     • processos/dashboard/page.tsx:30 = back-nav arrow; processos/page.tsx:206 = disabled pagination placeholder. -->
<!--   The executor MUST re-run this broadened grep post-edit (see Task 2 verify) to confirm no NEW/other explicit-action -->
<!--   icon-only row button remains unwrapped before considering DSR-03 closed. -->
<!-- ============================================================================ -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Mount a single TooltipProvider in providers.tsx with delayDuration={700}</name>
  <files>web/src/app/providers.tsx</files>
  <read_first>
    - web/src/app/providers.tsx (current full content — the QueryClientProvider/NextThemesProvider wrapper)
    - web/src/components/ui/tooltip.tsx (confirm export list + that TooltipProvider default delayDuration is 0)
    - .planning/phases/LEXCV-102-reconcilia-o-do-design-system/102-UI-SPEC.md (TooltipProvider mount section — the explicit delayDuration={700} requirement)
    - .planning/phases/LEXCV-102-reconcilia-o-do-design-system/102-PATTERNS.md (providers.tsx section + the correction that the mount point is providers.tsx, NOT layout.tsx)
  </read_first>
  <action>
    In web/src/app/providers.tsx add `import { TooltipProvider } from "@/components/ui/tooltip";` and wrap the existing `{children}` INSIDE `NextThemesProvider` with `<TooltipProvider delayDuration={700}>{children}</TooltipProvider>`. The `delayDuration={700}` prop is REQUIRED explicitly — tooltip.tsx defaults it to 0 (instant), which is NOT the 700ms decided in 102-CONTEXT.md. Mount it exactly once — do not add any second nested TooltipProvider anywhere else in the app. Do NOT edit layout.tsx (it only renders `<Providers>` and holds no context providers).
  </action>
  <verify>
    <automated>cd web && grep -q "delayDuration={700}" src/app/providers.tsx && grep -q "TooltipProvider" src/app/providers.tsx && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - providers.tsx imports TooltipProvider from "@/components/ui/tooltip"
    - providers.tsx wraps children with `<TooltipProvider delayDuration={700}>` inside NextThemesProvider (exact literal `delayDuration={700}` present)
    - layout.tsx is unchanged; there is exactly one TooltipProvider in the app (grep across src for `TooltipProvider` mount usage returns only providers.tsx)
  </acceptance_criteria>
  <done>A single TooltipProvider is mounted with explicit 700ms delay; all downstream Tooltip consumers rely on it.</done>
</task>

<task type="auto">
  <name>Task 2: Wrap icon-only row-action buttons (clientes + settings) and sidebar LogOut in Tooltip, add aria-labels, re-grep for coverage, verify build</name>
  <files>web/src/app/(dashboard)/clientes/page.tsx, web/src/app/(dashboard)/settings/page.tsx, web/src/components/shared/dashboard-shell.tsx</files>
  <read_first>
    - web/src/app/(dashboard)/clientes/page.tsx (mobile card block ~440-468 and desktop ClienteRow ~586-627; note existing imports from @/components/ui — Badge/Button/Card/Input/Table)
    - web/src/app/(dashboard)/settings/page.tsx (user-table row-action block ~456-475: the Edit and conditional Trash2 buttons; note existing lucide imports Edit/Trash2 at top ~10-11, and the currentUserId variable)
    - web/src/components/shared/dashboard-shell.tsx (LogOut buttons at ~154 and ~234; existing imports at top — Button from @/components/ui/button, Sheet, Input)
    - web/src/components/ui/tooltip.tsx (export list: Tooltip, TooltipContent, TooltipProvider, TooltipTrigger)
    - .planning/phases/LEXCV-102-reconcilia-o-do-design-system/102-PATTERNS.md (Tooltip rollout section — Analog 1 sidebar LogOut, Analog 2 row actions, incl the double-asChild target patterns and the plain-Button single-asChild pattern)
    - .planning/phases/LEXCV-102-reconcilia-o-do-design-system/102-UI-SPEC.md (Tooltip copy table)
  </read_first>
  <action>
    Add `import { Tooltip, TooltipContent, TooltipTrigger } from "@/components/ui/tooltip";` to clientes/page.tsx, settings/page.tsx, and dashboard-shell.tsx (no TooltipProvider import needed — mounted globally in Task 1).

    clientes/page.tsx — wrap each icon-only button. For the `Button asChild` buttons (Eye/Printer/Pencil wrapping a Link), use the double-asChild pattern: `<Tooltip><TooltipTrigger asChild><Button asChild ...aria-label="..."><Link ...><Icon/></Link></Button></TooltipTrigger><TooltipContent>...</TooltipContent></Tooltip>`. For the plain Trash2 delete button (onClick, no asChild), use single-level: `<Tooltip><TooltipTrigger asChild><Button aria-label="Eliminar" ... onClick={onDelete}><Trash2/></Button></TooltipTrigger><TooltipContent>Eliminar</TooltipContent></Tooltip>`. Apply to BOTH the desktop ClienteRow set (Eye→"Ver detalhes", Printer→"Imprimir", Pencil→"Editar", Trash2→"Eliminar") AND the mobile card set (Eye→"Ver detalhes", Pencil→"Editar"). Add a matching `aria-label` (same Portuguese text) to each Button trigger. Do NOT change any existing className, size, variant, href, onClick, target, or the canEditClientes conditionals — only wrap and add aria-label.

    settings/page.tsx — wrap the two user-table row-action buttons at ~lines 458-473. BOTH are plain `Button variant="ghost"` with onClick (NO asChild), so use the single-level pattern (identical to the clientes Trash2 case): for the Edit button, `<Tooltip><TooltipTrigger asChild><Button variant="ghost" {existing className unchanged} aria-label="Editar" onClick={() => handleEditClick(user)}><Edit className="h-4 w-4"/></Button></TooltipTrigger><TooltipContent>Editar</TooltipContent></Tooltip>`; for the Trash2 button, the same shape with `aria-label="Eliminar"` and `<TooltipContent>Eliminar</TooltipContent>`. Preserve the `{user.id !== currentUserId && (...)}` conditional wrapping the Trash2 button VERBATIM — wrap only the Button inside that conditional, do not move or remove the conditional. Do NOT change any className, variant, or onClick. SKIP the ~line 507 `h-8 w-8 p-0 rounded-full` form-panel close button (it is not a row action).

    dashboard-shell.tsx — wrap the LogOut button at BOTH ~line 154 (desktop) and ~line 234 (mobile drawer duplicate) with `<Tooltip><TooltipTrigger asChild><Button ...aria-label="Terminar sessão" onClick={onLogout}><LogOut/></Button></TooltipTrigger><TooltipContent>Terminar sessão</TooltipContent></Tooltip>`. Preserve the existing className/variant/onClick verbatim. SKIP the Menu hamburger (~248) — it is mobile-only/touch-first and already has aria-label="Abrir menu". (Recorded decision: this sidebar-footer LogOut button IS DSR-03's "ícones da sidebar colapsada" — see the <interfaces> recorded-decision note; restate it in the SUMMARY.)

    Then re-run the broadened DSR-03 coverage grep across ALL dashboard pages (see verify) to prove no other explicit-action icon-only row button was missed, and run the build gate. NOTE (Phase 101 learning): if executing in an isolated worktree, run `pnpm install` in web/ first — worktree node_modules do NOT sync from the main checkout. `pnpm build` is the definitive gate (Phase 101 found grep-only verification missed a real cascade bug).
  </action>
  <verify>
    <automated>cd web && pnpm install && grep -c "TooltipContent" src/app/\(dashboard\)/clientes/page.tsx && grep -c "TooltipContent" src/app/\(dashboard\)/settings/page.tsx && grep -q "Terminar sessão" src/components/shared/dashboard-shell.tsx && grep -q "aria-label=\"Ver detalhes\"" src/app/\(dashboard\)/clientes/page.tsx && grep -q "aria-label=\"Editar\"" src/app/\(dashboard\)/settings/page.tsx && grep -rn "Edit\|Eye\|Pencil\|Trash2\|Printer" 'src/app/(dashboard)' --include=page.tsx | grep "h-8 w-8 p-0\|h-9 w-9 p-0" | grep -v "clientes/page.tsx\|settings/page.tsx" || echo NO_OTHER_ROW_ACTIONS && pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - clientes/page.tsx: the 4 desktop row-action buttons (Eye/Printer/Pencil/Trash2) and 2 mobile buttons (Eye/Pencil) are each wrapped in Tooltip/TooltipTrigger/TooltipContent, each Button carries an aria-label matching its tooltip copy; grep `TooltipContent` returns >= 6
    - settings/page.tsx: the Edit button and the conditional Trash2 button are each wrapped in Tooltip/TooltipTrigger/TooltipContent with aria-label "Editar"/"Eliminar" and matching TooltipContent; grep `TooltipContent` returns >= 2; the `{user.id !== currentUserId && ...}` conditional is unchanged
    - dashboard-shell.tsx: both LogOut buttons (desktop + mobile drawer) are wrapped in Tooltip with `Terminar sessão` content and aria-label; grep `Terminar sessão` returns >= 2
    - DSR-03 closure re-grep: the broadened grep (Edit|Eye|Pencil|Trash2|Printer against h-8/h-9 icon sizings) across web/src/app/(dashboard)/**/page.tsx returns NO explicit-action icon-only row button outside clientes/page.tsx + settings/page.tsx (the only expected residual matches are the recorded out-of-scope MoreVertical/Chevron/back-nav/disabled/notificacoes-Check items — none of which are explicit-action Edit/Eye/Pencil/Trash2/Printer icons)
    - No existing className/variant/size/href/onClick/target/RBAC conditional was altered (git diff shows only additive Tooltip wrapping + aria-label + import line)
    - The Menu hamburger button was NOT wrapped (still just aria-label="Abrir menu")
    - `pnpm build` exits 0 (the double-asChild nesting compiles)
  </acceptance_criteria>
  <done>Every literal icon-only row-action button in DSR-03 scope (clientes + settings) plus the sidebar LogOut shows a Portuguese tooltip + carries aria-label; the broadened re-grep confirms no other explicit-action row icon was missed; build green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client render → user | Tooltip content is static Portuguese copy; aria-labels are static — no user input flows into them |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-102-06 | Tampering | RBAC/identity conditional (canEditClientes; settings `user.id !== currentUserId`) accidentally altered while wrapping buttons | mitigate | Explicit acceptance criterion: only additive Tooltip wrapping + aria-label; git diff must show no change to the conditionals; `pnpm build` typechecks |
| T-102-07 | Denial of Service | second nested TooltipProvider causing double-provider render | mitigate | Task 1 mounts exactly one provider; acceptance criterion greps for a single mount |
| T-102-08 | Information Disclosure | none (static copy, no data/auth surface) | accept | Tooltips/aria-labels are hardcoded presentational strings |
| T-102-11 | Repudiation | DSR-03 declared closed while an explicit-action icon-only row button (e.g. the settings Edit missed by the narrow grep) remains unwrapped | mitigate | Task 2 re-runs the broadened Edit|Eye|Pencil|Trash2|Printer grep across ALL dashboard page.tsx and asserts zero out-of-scope explicit-action residuals before closing DSR-03 |
| T-102-SC | Supply Chain | npm/pnpm installs | accept | NO net-new packages — tooltip.tsx + radix-ui already installed in Phase 101; `pnpm install` only refreshes existing lockfile. No package-legitimacy checkpoint required. |
</threat_model>

<verification>
- `pnpm build` passes in web/ (definitive gate — the double-asChild TooltipTrigger>Button>Link nesting compiles; RBAC/identity conditionals still typecheck).
- grep confirms exactly one TooltipProvider mount (providers.tsx) with `delayDuration={700}`.
- grep confirms row-action buttons wrapped: TooltipContent count >= 6 in clientes/page.tsx, >= 2 in settings/page.tsx, and LogOut wrapped (Terminar sessão count >= 2 in dashboard-shell.tsx).
- Broadened DSR-03 closure grep (searching `Edit` alongside Eye/Pencil/Trash2/Printer across every dashboard page.tsx) confirms no explicit-action icon-only row button remains unwrapped outside the recorded out-of-scope set.
- The actual hover/focus behavior + 700ms delay is confirmed at the 102-04 human visual checkpoint.
</verification>

<success_criteria>
- Single TooltipProvider (700ms) mounted in providers.tsx.
- All literal icon-only row-action buttons in DSR-03 scope (clientes row actions + settings user-table row actions + sidebar LogOut) show a Portuguese tooltip on hover/focus and carry a matching aria-label.
- processos/pareceres MoreVertical navigate-to-detail links, calendar Chevrons, the back-nav arrow, the disabled pagination placeholder, notificacoes' already-labelled Check, and the Menu hamburger are correctly left out of scope — confirmed by the broadened re-grep.
- The DSR-03 "ícones da sidebar colapsada" → sidebar-footer LogOut interpretation is recorded in 102-03-SUMMARY.md as a deliberate, evidence-based design decision.
- `pnpm build` green.
</success_criteria>

<output>
Create `.planning/phases/LEXCV-102-reconcilia-o-do-design-system/102-03-SUMMARY.md` when done. It MUST restate the recorded DSR-03 "sidebar colapsada → footer LogOut" interpretation and report the broadened-grep coverage result.
</output>
