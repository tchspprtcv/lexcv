---
phase: 103-m-dulo-dashboard
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/app/(dashboard)/dashboard/page.tsx
autonomous: true
requirements: [DASH-01, DASH-02]
must_haves:
  truths:
    - "While useDashboardKpis().isLoading is true, the KPI card row renders Skeleton placeholders (one skeleton card per RBAC-visible KPI, respecting the same canView* gates) instead of the bare em-dash fallback"
    - "While loading, the 'Atividade Recente' card renders exactly 3 Skeleton rows instead of the hardcoded static entries"
    - "When 'Prazos Urgentes' has zero events AND is not loading, it renders the Empty component (not the italic 'Sem urgências.' text); nothing extra renders while its query is still in flight"
    - "When 'Processos Recentes' has zero processos AND is not loading, it renders the Empty component in place of the Table (not a blank table body)"
    - "Every EmptyTitle instance uses className='text-sm font-semibold' (14px/600), never the shipped default text-lg font-medium"
    - "The ad hoc 'Sem urgências.' string no longer exists anywhere in the file"
    - "pnpm build passes (typecheck + compile)"
  artifacts:
    - path: "web/src/app/(dashboard)/dashboard/page.tsx"
      provides: "Skeleton loading states for KPI cards + Atividade Recente (DASH-01) and Empty zero-data states for Prazos Urgentes / Processos Recentes / defensive Atividade Recente (DASH-02)"
      contains: "from \"@/components/ui/skeleton\""
    - path: "web/src/app/(dashboard)/dashboard/page.tsx"
      provides: "Empty primitive family imported and used with the mandatory EmptyTitle typography override"
      contains: "EmptyTitle className=\"text-sm font-semibold\""
  key_links:
    - from: "web/src/app/(dashboard)/dashboard/page.tsx"
      to: "web/src/components/ui/skeleton.tsx"
      via: "Skeleton import + loading-branch usage"
      pattern: "from \"@/components/ui/skeleton\""
    - from: "web/src/app/(dashboard)/dashboard/page.tsx"
      to: "web/src/components/ui/empty.tsx"
      via: "Empty/EmptyHeader/EmptyMedia/EmptyTitle/EmptyDescription import + zero-data usage"
      pattern: "from \"@/components/ui/empty\""
    - from: "web/src/app/(dashboard)/dashboard/page.tsx#DashboardKpis"
      to: "useDashboardKpis().isLoading"
      via: "loading-branch reads the isLoading field currently discarded"
      pattern: "isLoading"
---

<objective>
Deliver DASH-01 and DASH-02 on the single institutional Dashboard page (`web/src/app/(dashboard)/dashboard/page.tsx`): swap the module's missing/ad hoc loading and zero-data states for the official `Skeleton` and `Empty` primitives (both already installed since Phase 101, never yet consumed).

Purpose: This is the lowest-primitive-need module of the v2.13 milestone — it validates the Phase-101 token layer (`Skeleton` renders `bg-muted`, `Empty`'s icon wrapper renders `bg-muted text-foreground`, both already reconciled/approved in Phase 102) on a low-risk surface before deeper modules adopt it.

IMPORTANT — verified-against-source scope correction (from 103-UI-SPEC.md): there is NO literal "A carregar..." string in this file today (that string lives only in the out-of-scope `processos/dashboard/page.tsx`). The real gaps DASH-01/DASH-02 close are: (1) `DashboardKpis` calls `useDashboardKpis()` but never reads `.isLoading` — every number falls back to a bare `"—"` em-dash, indistinguishable from a real zero (a MISSING loading state, not present ad hoc text); (2) "Atividade Recente" is 100% hardcoded static JSX with fake timestamps, wired to no hook — flag it, do not invent backend/hook work; (3) "Prazos Urgentes" already has an ad hoc `"Sem urgências."` italic string on real data (the cleanest DASH-02 target); (4) "Processos Recentes" renders a silently blank `<TableBody>` when empty. "Status dos Processos" is a static decorative chart placeholder — OUT of scope (deferred Chart work, DASH-V2-01), do not touch.

Output: 1 file modified; KPI cards + Atividade Recente gain Skeleton loading states; Prazos Urgentes + Processos Recentes (+ defensive Atividade Recente) gain Empty zero-data states, all using the mandatory `EmptyTitle` typography override.
</objective>

<execution_context>
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/LEXCV-103-m-dulo-dashboard/103-UI-SPEC.md
@.planning/phases/LEXCV-103-m-dulo-dashboard/103-CONTEXT.md

<interfaces>
<!-- Skeleton (web/src/components/ui/skeleton.tsx, installed Phase 101, never yet consumed) -->
<!--   export: Skeleton -->
<!--   base classes: "animate-pulse rounded-md bg-muted" (theme-aware neutral; rounded-md resolves to 0 via --radius: 0rem — sharp corners, correct for institutional identity) -->
<!--   Usage: <Skeleton className="h-10 w-10" /> — pass explicit h-*/w-* to shape each block. -->
<!-- Empty family (web/src/components/ui/empty.tsx, installed Phase 101, never yet consumed) -->
<!--   exports: Empty, EmptyHeader, EmptyMedia, EmptyTitle, EmptyDescription, EmptyContent -->
<!--   Empty base: "flex w-full min-w-0 flex-1 flex-col items-center justify-center gap-4 rounded-lg border-dashed p-12 text-center text-balance" -->
<!--     (border-dashed WITHOUT a border-width utility renders NO visible border — do not add one, do not strip one) -->
<!--   EmptyMedia variant="icon": "flex size-10 ... rounded-lg bg-muted text-foreground [&_svg:not([class*='size-'])]:size-6" -->
<!--     → already NEUTRAL by construction (bg-muted, no accent) — matches CONTEXT.md "ícone neutro ... sem cor de destaque". DO NOT override variant or add an accent color. -->
<!--   EmptyTitle SHIPPED default: "font-heading text-lg font-medium tracking-tight" (18px / weight 500) -->
<!--     → this VIOLATES the phase typography cap (would add a 5th size + 3rd weight). -->
<!--     → MANDATORY override for EVERY EmptyTitle instance: <EmptyTitle className="text-sm font-semibold"> (resolves to 14px/600, both already in the declared scale). NOT optional polish. -->
<!--   EmptyDescription SHIPPED default: "text-sm/relaxed text-muted-foreground ..." → already matches the Body role, NO override needed. -->
<!-- Hooks (all standard TanStack @tanstack/react-query useQuery → every one exposes .isLoading and .data): -->
<!--   useDashboardKpis()  queryKey ["dashboard","kpis"]  → calling it a 2nd time in AtividadeRecenteCard is FREE (shared cache/loading state, no duplicate network request). data shape: DashboardKpis (total_clientes, processos_ativos, prazos_vencer, valores_recebidos_mes) — NO activity-feed field exists. -->
<!--   useEventos({ concluido: false })  → PrazosUrgentesCard already holds `const urgentes = useEventos(...)`; read urgentes.isLoading for the loading guard. -->
<!--   useProcessos() / useClientes({})  → RecentProcessos wrappers already hold these; read processos.isLoading for the loading guard. -->
<!-- Current file structure (web/src/app/(dashboard)/dashboard/page.tsx, ~416 lines): -->
<!--   DashboardPage (main) → renders <DashboardKpis .../>, then a 3-col grid (ProcessosStatusCard + PrazosUrgentesCard), then a 3-col grid (RecentProcessos* + an INLINE hardcoded "Atividade Recente" <Card> at lines ~78-114). -->
<!--   DashboardKpis (lines ~120-219): builds `cards` array with per-KPI canView* gating, `if (!cards.length) return null;`, renders grid "grid gap-4 grid-cols-1 sm:grid-cols-2 lg:grid-cols-4". Each card: Card>CardContent p-5 > (flex items-start justify-between gap-3: icon chip h-10 w-10 + trend Badge) > label (mt-5, off-scale pre-existing) > number (mt-1 text-3xl, "?? '—'"). -->
<!--   PrazosUrgentesCard (lines ~264-313): urgentEventos = (urgentes.data ?? []).slice(0,2). Ternary: length ? map(...) : <div ...italic>Sem urgências.</div>. Then a "Ver Agenda Completa" button. CardContent className="space-y-4". -->
<!--   RecentProcessosCardWithClientes / RecentProcessosCardNoClientes (wrappers, ~315-329) → both compute recentProcessos = (processos.data ?? []).slice(0,3) and render <RecentProcessosCard .../>. -->
<!--   RecentProcessosCard (~331-415): <CardContent className="p-0"><Table>...<TableBody>{recentProcessos.map(...)}</TableBody></Table></CardContent> — empty array = blank tbody today. -->
<!-- lucide-react icons currently imported: FileText, Plus, Scale, Timer, Users, Wallet. New needed: CalendarCheck, FolderOpen, Inbox, and the type LucideIcon. -->
<!-- ============================================================================ -->
<!-- SPACING (from 103-UI-SPEC.md, authoritative — exact values, no fenced code allowed here): -->
<!--   KPI skeleton card: row1 flex items-start justify-between gap-3 → Skeleton h-10 w-10 (icon slot) + Skeleton h-5 w-14 rounded-full (badge slot); -->
<!--     label row Skeleton h-3 w-24 with mt-4 (16px, ON-scale — deliberately NOT the real content's off-scale mt-5; do NOT "fix" the real mt-5, it is untouched pre-existing debt); -->
<!--     number row Skeleton h-8 w-16 with mt-1 (4px). -->
<!--   Atividade Recente skeleton row (x3): flex gap-4 → Skeleton h-10 w-10 (icon-chip slot) + a min-w-0 pt-1 column holding Skeleton h-4 w-40 (title) and Skeleton h-3 w-24 with mt-1 (timestamp, matching the real content's own mt-1 at file lines 92/101/110). -->
<!--   The row-to-row gap for BOTH the KPI grid (gap-4) and Atividade Recente (the existing CardContent space-y-5) comes from the UNCHANGED existing container — inherit it, do not introduce a new gap value. -->
<!-- ============================================================================ -->
<!-- EMPTY copy (pt-PT/pt-CV, authoritative from 103-UI-SPEC.md) + icon (lucide, EmptyMedia variant="icon", neutral): -->
<!--   Prazos Urgentes   → CalendarCheck · "Sem prazos urgentes"   · "Não há eventos urgentes nos próximos dias." -->
<!--   Processos Recentes → FolderOpen   · "Sem processos recentes" · "Os processos criados mais recentemente aparecerão aqui." -->
<!--   Atividade Recente  → Inbox        · "Sem atividade recente"  · "A atividade mais recente vai aparecer aqui." (DEFENSIVE dead-code branch today) -->
<!-- ============================================================================ -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: DASH-01 — Skeleton loading states for KPI cards + Atividade Recente</name>
  <files>web/src/app/(dashboard)/dashboard/page.tsx</files>
  <read_first>
    - web/src/app/(dashboard)/dashboard/page.tsx (FULL current file — the DashboardPage main return, the DashboardKpis component ~120-219, and the inline hardcoded "Atividade Recente" Card at ~78-114)
    - web/src/components/ui/skeleton.tsx (confirm the single export `Skeleton` and its base classes animate-pulse rounded-md bg-muted)
    - web/src/hooks/use-dashboard-kpis.ts (confirm useDashboardKpis is a standard useQuery → .isLoading + .data available; queryKey ["dashboard","kpis"])
    - .planning/phases/LEXCV-103-m-dulo-dashboard/103-UI-SPEC.md (Loading & Empty State Contract → the "Loading (DASH-01)" table with exact Skeleton className values)
    - .planning/phases/LEXCV-103-m-dulo-dashboard/103-CONTEXT.md (Estados de Loading decisions: rectangular blocks mimicking real card shape, 3 skeleton rows for Atividade Recente, no custom fade transition)
  </read_first>
  <action>
    Add `import { Skeleton } from "@/components/ui/skeleton";` to the file.

    KPI cards (DASH-01, in the `DashboardKpis` component): the hook `const kpis = useDashboardKpis();` is already called — now READ its `.isLoading`. Compute the count of RBAC-visible KPIs as the number of truthy values among `canViewClientes, canViewProcessos, canViewAgenda, canViewFinanceiro`. When `kpis.isLoading` is true, return the SAME grid wrapper `<div className="grid gap-4 grid-cols-1 sm:grid-cols-2 lg:grid-cols-4">` containing that many `<KpiCardSkeleton />` instances (return `null` if zero are visible, mirroring the existing `if (!cards.length) return null;`). Define a local component `KpiCardSkeleton` that renders `<Card><CardContent className="p-5">` with: row 1 `<div className="flex items-start justify-between gap-3"><Skeleton className="h-10 w-10" /><Skeleton className="h-5 w-14 rounded-full" /></div>`, then a label Skeleton `<Skeleton className="h-3 w-24 mt-4" />`, then a number Skeleton `<Skeleton className="h-8 w-16 mt-1" />`. Use `mt-4` (16px, on-scale) for the label row — do NOT reuse the real markup's off-scale `mt-5`, and do NOT modify the real KPI cards' existing `mt-5`/`mt-1`/`?? "—"` — the `?? "—"` fallback REMAINS as the settled zero/error fallback; you are only ADDING the isLoading branch ahead of it.

    Atividade Recente (DASH-01): extract the inline hardcoded "Atividade Recente" `<Card>` block (currently at ~lines 78-114 of DashboardPage's return) into a new local component `AtividadeRecenteCard`, and replace the inline block in DashboardPage's return with `<AtividadeRecenteCard />`. Inside `AtividadeRecenteCard`: call `const kpis = useDashboardKpis();` (safe/free — TanStack shares cache+loading by queryKey ["dashboard","kpis"], NOT a duplicate request) and represent the 3 real hardcoded entries as a static array (keep their existing icon chip + title + timestamp markup verbatim when rendered). Keep the exact existing `<CardHeader ...>Atividade Recente</CardHeader>` and `<CardContent className="pt-6 space-y-5">` structure so there is zero layout shift. When `kpis.isLoading` is true, render exactly 3 skeleton rows inside that CardContent, each: `<div className="flex gap-4"><Skeleton className="h-10 w-10" /><div className="min-w-0 pt-1"><Skeleton className="h-4 w-40" /><Skeleton className="h-3 w-24 mt-1" /></div></div>`. When not loading, map over the entries array to render the 3 real rows exactly as they render today. Do NOT add any `transition`/`animate-in` wrapper around the swap (CONTEXT.md: no custom fade — rely on React's native re-render when isLoading flips).

    Do NOT touch ProcessosStatusCard, PrazosUrgentesCard, or RecentProcessosCard in this task (Prazos/Processos Skeleton is explicitly OUT of scope this phase; their Empty states are Task 2).
  </action>
  <verify>
    <automated>cd web && grep -q 'from "@/components/ui/skeleton"' 'src/app/(dashboard)/dashboard/page.tsx' && grep -q 'KpiCardSkeleton' 'src/app/(dashboard)/dashboard/page.tsx' && grep -q 'AtividadeRecenteCard' 'src/app/(dashboard)/dashboard/page.tsx' && grep -q 'h-3 w-24 mt-4' 'src/app/(dashboard)/dashboard/page.tsx' && pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - The file imports `Skeleton` from "@/components/ui/skeleton".
    - `DashboardKpis` reads `kpis.isLoading` and, while true, returns the same 4-col grid wrapper containing one `KpiCardSkeleton` per RBAC-visible KPI (count = truthy among canViewClientes/Processos/Agenda/Financeiro); returns null when zero are visible.
    - `KpiCardSkeleton` renders inside Card>CardContent p-5 with the exact skeleton className set: h-10 w-10 + h-5 w-14 rounded-full (row 1), h-3 w-24 mt-4 (label), h-8 w-16 mt-1 (number). The real KPI cards' existing `mt-5`, `mt-1`, and `?? "—"` fallbacks are UNCHANGED.
    - The inline hardcoded "Atividade Recente" Card is extracted to `AtividadeRecenteCard`; DashboardPage's return now renders `<AtividadeRecenteCard />` in the same grid slot.
    - `AtividadeRecenteCard`, while loading, renders exactly 3 skeleton rows (each: flex gap-4 with h-10 w-10 icon Skeleton + a min-w-0 pt-1 column of h-4 w-40 and h-3 w-24 mt-1); while not loading, renders the 3 original entries with their existing markup; the CardHeader and CardContent (pt-6 space-y-5) structure is preserved (zero layout shift).
    - No `transition`/`animate-in` wrapper was added around the skeleton→content swap.
    - `pnpm build` exits 0.
  </acceptance_criteria>
  <done>KPI cards render RBAC-count-matched Skeleton placeholders while useDashboardKpis().isLoading; Atividade Recente renders 3 Skeleton rows while loading; build green.</done>
</task>

<task type="auto">
  <name>Task 2: DASH-02 — Empty zero-data states for Prazos Urgentes, Processos Recentes, and defensive Atividade Recente</name>
  <files>web/src/app/(dashboard)/dashboard/page.tsx</files>
  <read_first>
    - web/src/app/(dashboard)/dashboard/page.tsx (POST Task-1 state — the PrazosUrgentesCard ~264-313, the RecentProcessos* wrappers + RecentProcessosCard ~315-415, and the AtividadeRecenteCard created in Task 1)
    - web/src/components/ui/empty.tsx (confirm exports Empty/EmptyHeader/EmptyMedia/EmptyTitle/EmptyDescription; confirm EmptyTitle's shipped default is `text-lg font-medium` — the reason the override is mandatory; confirm EmptyMedia variant="icon" is bg-muted text-foreground, already neutral)
    - .planning/phases/LEXCV-103-m-dulo-dashboard/103-UI-SPEC.md (Empty (DASH-02) table → exact copy + icon per section, the mandatory EmptyTitle override, the Prazos "not-loading" guard, the Processos p-0 relax)
    - .planning/phases/LEXCV-103-m-dulo-dashboard/103-CONTEXT.md (Estados Vazios decisions: neutral icon, short institutional pt-PT copy)
  </read_first>
  <action>
    Add `import { Empty, EmptyHeader, EmptyMedia, EmptyTitle, EmptyDescription } from "@/components/ui/empty";`. Add `CalendarCheck, FolderOpen, Inbox` to the existing lucide-react import, and import the type `LucideIcon` from lucide-react.

    Define a local helper component `EmptyState` with props `{ icon: LucideIcon; title: string; description: string }` that renders `<Empty><EmptyHeader><EmptyMedia variant="icon">{createElement of the icon, className "h-6 w-6" or default}</EmptyMedia><EmptyTitle className="text-sm font-semibold">{title}</EmptyTitle><EmptyDescription>{description}</EmptyDescription></EmptyHeader></Empty>`. The `EmptyTitle className="text-sm font-semibold"` override is MANDATORY (14px/600) — using the raw EmptyTitle default (text-lg font-medium) FAILS the typography contract. Do NOT override EmptyMedia's `variant="icon"` and do NOT add any accent color to the icon (it must stay neutral bg-muted per CONTEXT.md). Do NOT add a border to Empty.

    Prazos Urgentes (PrazosUrgentesCard): the component already holds `const urgentes = useEventos({ concluido: false });`. Replace the empty branch — currently `<div className="text-sm text-slate-500 dark:text-slate-400 italic">Sem urgências.</div>` — so the ternary becomes: when `urgentEventos.length` is truthy → the existing map; else → `urgentes.isLoading ? null : <EmptyState icon={CalendarCheck} title="Sem prazos urgentes" description="Não há eventos urgentes nos próximos dias." />`. The `null` while loading is deliberate (Prazos Urgentes gets no Skeleton this phase — suppress rendering anything extra until the query settles, matching today's behavior). The "Ver Agenda Completa" button below the ternary stays unchanged. The literal string "Sem urgências." must be fully removed.

    Processos Recentes: thread the loading state through so an Empty flash during initial load is avoided. In BOTH wrapper components `RecentProcessosCardWithClientes` and `RecentProcessosCardNoClientes`, pass `isLoading={processos.isLoading}` down to `RecentProcessosCard`. Widen `RecentProcessosCard`'s props to accept `isLoading: boolean`. In `RecentProcessosCard`, when `recentProcessos.length === 0 && !isLoading`, render `<CardContent><EmptyState icon={FolderOpen} title="Sem processos recentes" description="Os processos criados mais recentemente aparecerão aqui." /></CardContent>` (plain CardContent WITHOUT `p-0`, so Empty's own p-12 isn't fought); otherwise render the existing `<CardContent className="p-0"><Table>...</Table></CardContent>` exactly as today (a blank tbody during load is acceptable and matches today's behavior). Keep the CardHeader unchanged.

    Atividade Recente (defensive): in `AtividadeRecenteCard` (from Task 1), the entries array is static length 3 today, so the empty case is dead code — but wire it defensively for correctness. When not loading AND the entries array length is 0, render `<EmptyState icon={Inbox} title="Sem atividade recente" description="A atividade mais recente vai aparecer aqui." />` inside the same CardContent. This branch is expected-unreachable today (do NOT invent a backend/hook to make it reachable — explicitly out of scope, matches the DASH-V2 deferred pattern); it exists only so the array-based render is correct if the data source ever changes.

    Do NOT touch ProcessosStatusCard (the static decorative chart placeholder — deferred DASH-V2-01).
  </action>
  <verify>
    <automated>cd web && grep -q 'from "@/components/ui/empty"' 'src/app/(dashboard)/dashboard/page.tsx' && grep -q 'EmptyTitle className="text-sm font-semibold"' 'src/app/(dashboard)/dashboard/page.tsx' && grep -q 'Sem prazos urgentes' 'src/app/(dashboard)/dashboard/page.tsx' && grep -q 'Sem processos recentes' 'src/app/(dashboard)/dashboard/page.tsx' && grep -q 'Sem atividade recente' 'src/app/(dashboard)/dashboard/page.tsx' && ! grep -q 'Sem urgências' 'src/app/(dashboard)/dashboard/page.tsx' && pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - The file imports the Empty family from "@/components/ui/empty" and the icons CalendarCheck/FolderOpen/Inbox (plus the LucideIcon type) from lucide-react.
    - A local `EmptyState` helper exists and every EmptyTitle it renders carries `className="text-sm font-semibold"` (no raw text-lg font-medium default anywhere); EmptyMedia keeps variant="icon" with no accent color override.
    - Prazos Urgentes: the empty branch renders `<EmptyState icon={CalendarCheck} title="Sem prazos urgentes" .../>` only when not loading (renders null while urgentes.isLoading); the string "Sem urgências." is gone from the file.
    - Processos Recentes: both wrappers pass `isLoading={processos.isLoading}`; RecentProcessosCard renders `EmptyState` (FolderOpen, "Sem processos recentes") in a non-p-0 CardContent when `recentProcessos.length === 0 && !isLoading`, else the existing p-0 Table.
    - Atividade Recente: a defensive empty-array branch renders `<EmptyState icon={Inbox} title="Sem atividade recente" .../>`; no backend/hook was invented for it.
    - ProcessosStatusCard is untouched.
    - `pnpm build` exits 0.
  </acceptance_criteria>
  <done>Prazos Urgentes and Processos Recentes render the Empty primitive (with the mandatory EmptyTitle override) on settled zero-data, the ad hoc "Sem urgências." string is removed, Atividade Recente carries a defensive Empty branch, and build is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server data → client render | `useDashboardKpis`/`useEventos`/`useProcessos`/`useClientes` payloads flow into read-only presentational render; no user input is introduced by this phase |
| client render → user | Skeleton blocks and Empty copy are static, hardcoded Portuguese strings — no dynamic/user-controlled content flows into them |

**Security surface note (per planning context):** This phase is purely presentational — it consumes already-installed `Skeleton`/`Empty` primitives, adds ZERO new dependencies, touches NO auth flow, NO data-mutation path, NO new endpoint or query, and NO tenant-scoping logic. Security surface is minimal by construction.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-103-01 | Tampering | The per-KPI RBAC gates (canViewClientes/Processos/Agenda/Financeiro) accidentally altered while adding the isLoading skeleton branch, causing a skeleton (or later a real card) to render for a KPI the user may not view | mitigate | Skeleton count is derived from the SAME canView* booleans; acceptance criterion + `pnpm build` typecheck; git diff must show only additive loading/empty branches, no change to the existing gating or `?? "—"` fallbacks |
| T-103-02 | Information Disclosure | Empty/Skeleton copy leaking data | accept | All Skeleton dimensions and Empty titles/descriptions are static hardcoded strings; no KPI value, processo, evento, or tenant data is rendered into them |
| T-103-03 | Denial of Service | Calling `useDashboardKpis()` a second time inside AtividadeRecenteCard triggering a duplicate network request | accept | TanStack Query dedupes by queryKey ["dashboard","kpis"] — the second call shares cache/loading state, no extra request; documented in the interfaces block |
| T-103-SC | Tampering (Supply Chain) | npm/pnpm package installs | accept | NO net-new packages this phase — `Skeleton`/`Empty`/`lucide-react`/`radix-ui` all already installed since Phase 101; no `shadcn add` / `pnpm add` runs here. No package-legitimacy checkpoint required (no RESEARCH.md Package Legitimacy Gate applies). |
</threat_model>

<verification>
- `pnpm build` passes in web/ — the DEFINITIVE gate (Phase 101 learned grep-only verification misses real bugs, e.g. the CSS-cascade tie-break). Typecheck confirms the widened `RecentProcessosCard` props (isLoading) and the LucideIcon-typed EmptyState helper compile.
- grep confirms wiring: `from "@/components/ui/skeleton"` and `from "@/components/ui/empty"` imports present; `KpiCardSkeleton` + `AtividadeRecenteCard` defined; the mandatory `EmptyTitle className="text-sm font-semibold"` override present.
- grep confirms the 3 Empty headings present ("Sem prazos urgentes" / "Sem processos recentes" / "Sem atividade recente") and the ad hoc "Sem urgências." string REMOVED.
- Because Skeleton (transient, sub-second) and Empty (zero-data only) states are hard to trigger by simple navigation, live confirmation — if performed during phase verification — is: throttle the network (DevTools) or cold-load to see Skeleton; filter/seed to zero eventos and zero processos to see Empty. This is guidance for the verifier, not a blocking checkpoint (the token layer these primitives render on — bg-muted — was already visually approved at the Phase 102 checkpoint).
</verification>

<success_criteria>
- DASH-01: KPI cards render RBAC-count-matched `Skeleton` placeholders (rectangular blocks mimicking icon+badge+label+number) while `useDashboardKpis().isLoading`; "Atividade Recente" renders 3 `Skeleton` rows while loading. No "A carregar..." text is introduced (none existed) — the closed gap is the previously-missing loading state and the discarded `.isLoading`.
- DASH-02: "Prazos Urgentes" (replacing "Sem urgências.") and "Processos Recentes" (replacing the blank table body) render the `Empty` primitive on settled zero-data, with a defensive `Empty` branch on Atividade Recente. Every `EmptyTitle` uses the `text-sm font-semibold` override; every `EmptyMedia` icon stays neutral.
- Out-of-scope confirmed untouched: "Status dos Processos" static chart placeholder; the real KPI cards' pre-existing `mt-5`/`font-bold`/`?? "—"` markup; no backend/hook invented for Atividade Recente.
- `pnpm build` green.
</success_criteria>

<output>
Create `.planning/phases/LEXCV-103-m-dulo-dashboard/103-01-SUMMARY.md` when done. It MUST record: (1) the verified-against-source correction that no "A carregar..." string existed (the closed gap was a missing loading state + discarded `.isLoading`, plus the ad hoc "Sem urgências." string); (2) that Atividade Recente's Empty branch is intentionally defensive dead-code today (no backend/hook invented, per DASH-V2 deferred pattern); (3) the pnpm build result.
</output>
