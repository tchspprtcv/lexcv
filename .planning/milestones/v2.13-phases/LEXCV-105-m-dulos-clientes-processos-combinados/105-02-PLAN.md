---
phase: 105-m-dulos-clientes-processos-combinados
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/app/(dashboard)/processos/[id]/page.tsx
autonomous: true
requirements: [CLP-02, CLP-03, CLP-05]
must_haves:
  truths:
    - "Ficha de Processo tab bar renders via Tabs/TabsList/TabsTrigger/TabsContent (role=tablist, aria-selected) — the manual variant={tab===...} Button-toggle array is gone; the existing flex flex-wrap responsive behavior is preserved"
    - "The existing ?tab= deep-link sync (useSearchParams + useEffect calling setTab) is preserved unchanged; Tabs consumes the same tab/setTab state via value/onValueChange"
    - "The 1 RBAC-gated tab (auditoria) omits its TabsTrigger when canManageProcessos is false, and its TabsContent keeps the inner canManageProcessos check"
    - "All 8 native <select> in the file are NativeSelect size=default; the const selectClassName declaration is deleted; textareaClassName is left intact"
    - "The <h1> at ~line 710 reads font-semibold (reconciled from font-bold) — and no other font-bold usage in the file (badges/buttons/labels) is touched; the header is a Breadcrumb (Processos link -> current processo identifier)"
  artifacts:
    - path: "web/src/app/(dashboard)/processos/[id]/page.tsx"
      provides: "Ficha de Processo tab shell migrated to Tabs; header Breadcrumb + h1 reconciled; all selects NativeSelect"
      contains: "TabsContent"
  key_links:
    - from: "web/src/app/(dashboard)/processos/[id]/page.tsx"
      to: "existing ?tab= sync (useSearchParams/useEffect/setTab)"
      via: "Tabs value={tab} onValueChange={(v) => setTab(v as TabKey)} over the unchanged tab/setTab state"
      pattern: "Tabs value=\\{tab\\}"
    - from: "web/src/app/(dashboard)/processos/[id]/page.tsx"
      to: "canManageProcessos"
      via: "conditional auditoria TabsTrigger (omit) + inner content gate"
      pattern: "canManageProcessos"
---

<objective>
Migrate the Ficha de Processo (`processos/[id]/page.tsx`, 2758 lines) **tab shell + header + selects** to real `Tabs`, `Breadcrumb`, and `NativeSelect`. This plan owns the file first (Wave 1); the inner tab-content migrations (Partes/Fases/Testemunhas tables, Testemunhas Avatar, Documentos DataTable) are done by 105-03 (Wave 2) after this establishes the `TabsContent` wrappers. Splitting the file across two waves keeps each plan under the ~50% context budget for a 2758-line file with 6 patterns.

Purpose: Close the tab-shell half of CLP-02 (Processo Tabs, delivered same phase as the Cliente ficha), the Processo half of CLP-03 (NativeSelect), and the Processo-ficha half of CLP-05 (Breadcrumb + the locked font-semibold h1 reconciliation).
Output: `processos/[id]/page.tsx` tab shell/header/selects migrated; `pnpm build` green. Inner tab content left as-is for 105-03.
</objective>

<execution_context>
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/LEXCV-105-m-dulos-clientes-processos-combinados/105-CONTEXT.md
@.planning/phases/LEXCV-105-m-dulos-clientes-processos-combinados/105-PATTERNS.md
@.planning/phases/LEXCV-105-m-dulos-clientes-processos-combinados/105-UI-SPEC.md
@web/src/app/(dashboard)/processos/[id]/page.tsx
@web/src/components/ui/tabs.tsx
@web/src/components/ui/native-select.tsx
@web/src/components/ui/breadcrumb.tsx

<interfaces>
<!-- Primitive contracts. Full before/after excerpts in 105-PATTERNS.md Shared Patterns 1,2,5. -->
- Tabs / NativeSelect / Breadcrumb signatures: identical to those documented in 105-01-PLAN.md's interfaces block and 105-PATTERNS.md.
- Processos-specific ?tab= sync (105-PATTERNS.md Pattern 1): `const [tab, setTab] = React.useState<TabKey>(initialTab)` seeded from `searchParams.get("tab")` against the `TAB_KEYS` array, plus a `useEffect([searchParams])` that calls `setTab` when the param changes. This block needs ZERO changes — Tabs simply reads `tab` via `value=` and writes via `onValueChange`.
- RBAC vars (verified): canViewProcessos (line 205), canEditProcessos, canManageProcessos (line 207), canEditDocumentos (line 231). Auditoria tab gated by canManageProcessos (trigger ~line 1302, content dispatch ~line 2293).
- Table primitives are NOT yet imported in this file — do NOT add that import here; it is added by 105-03.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Header Breadcrumb + h1 font-semibold + NativeSelect (8 selects)</name>
  <files>web/src/app/(dashboard)/processos/[id]/page.tsx</files>
  <read_first>
    - 105-PATTERNS.md Shared Patterns 2 (NativeSelect Variant B/C) and 5 (Breadcrumb 2-level + h1 note).
    - 105-UI-SPEC.md Scope note #4 (font-semibold is the LOCKED final value, NOT font-bold — only the single header <h1>, not other font-bold elements) and Scope note #5 (4-way select styling all collapses to NativeSelect; the stray focus-visible:ring-blue-500 is intentionally dropped).
    - processos/[id]/page.tsx around: header block (~lines 709-719) incl. `<h1 className="text-2xl font-bold">Processo</h1>` at line 710; selectClassName const (line 162) + textareaClassName const (line 165); enumerate all 8 `<select` sites by grepping `<select` in-file (includes the Fase status select ~line 1762, the ReatribuirResponsavelControl select ~lines 2433-2450, and the Testemunha `tipo` dialog select ~lines 2155-2166).
  </read_first>
  <action>
    Add imports: `NativeSelect` from "@/components/ui/native-select"; `Breadcrumb, BreadcrumbList, BreadcrumbItem, BreadcrumbLink, BreadcrumbPage, BreadcrumbSeparator` from "@/components/ui/breadcrumb". (Tabs import is added in Task 2.)

    h1 reconciliation — PRECISION REQUIRED: change ONLY the single header heading `<h1 className="text-2xl font-bold">Processo</h1>` at ~line 710 to `<h1 className="text-2xl font-semibold">Processo</h1>`. Do NOT touch any of the other `font-bold` occurrences in this file (they are on Badges at ~793, Buttons at ~813/836/851/891/916/935, and label text at ~965/993 — all explicitly out of scope; the h1-weight fix is scoped to the header heading only, per UI-SPEC Scope note #4). Use `font-semibold` (600), never `font-bold` (700) — the design system caps at two weights (400/600).

    Breadcrumb: replace the header `<div className="text-sm text-neutral-500 ..."><Link href="/processos">Processos</Link> / <span>{identifier}</span></div>` with a `Breadcrumb`: `Processos` as `BreadcrumbLink asChild` wrapping `<Link href="/processos">`, a `BreadcrumbSeparator`, then `BreadcrumbPage` with the exact existing fallback chain `{processo.data?.numero ?? processo.data?.titulo ?? "…"}`. Leave the separate "Voltar" Button untouched.

    NativeSelect: grep the file for every `<select` (exactly 8). Migrate each per its variant — RHF-bound selects become `<NativeSelect id="X" size="default" {...form.register("X")}>` (drop `className={selectClassName}`); controlled selects become `<NativeSelect value={...} onChange={...} size="default">` (drop the inline className, including the stray `focus-visible:ring-blue-500` on the ReatribuirResponsavelControl/list-filter variant — its loss is intentional per UI-SPEC Color). Keep every `<option>` child unchanged. IMPORTANT: the Fase status select (~line 1762) is currently inside a `<td>` — migrate the `<select>` element to `<NativeSelect>` in place, but LEAVE the surrounding `<td>`/`<table>` markup alone (105-03 converts the Fases table scaffolding). After all 8 selects are migrated, DELETE `const selectClassName` (line 162); do NOT delete `const textareaClassName` (line 165) — it feeds `<textarea>` elements out of scope.
  </action>
  <verify>
    <automated>cd web && grep -c "<select" "src/app/(dashboard)/processos/[id]/page.tsx" | grep -qx 0 && grep -c "const selectClassName" "src/app/(dashboard)/processos/[id]/page.tsx" | grep -qx 0 && grep -q "const textareaClassName" "src/app/(dashboard)/processos/[id]/page.tsx" && grep -q "BreadcrumbList" "src/app/(dashboard)/processos/[id]/page.tsx" && grep -q '<h1 className="text-2xl font-semibold">Processo</h1>' "src/app/(dashboard)/processos/[id]/page.tsx" && grep -c 'font-bold' "src/app/(dashboard)/processos/[id]/page.tsx" | grep -vqx 0 && echo GATE_PASS</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "<select"` returns 0 (all 8 raw selects migrated).
    - `const selectClassName` removed; `const textareaClassName` retained.
    - The header `<h1>Processo</h1>` reads `font-semibold`; other `font-bold` usages (badges/buttons/labels) still present and unchanged (the gate asserts font-bold count is NOT zero to prove they were not swept).
    - Header renders a Breadcrumb (BreadcrumbList/BreadcrumbLink asChild/BreadcrumbPage); the `<span>/</span>` div is gone.
    - No `focus-visible:ring-blue-500` remains on any migrated select (folded into NativeSelect's own ring).
  </acceptance_criteria>
  <done>Header is a Breadcrumb, the single page h1 is font-semibold (others untouched), all 8 selects are NativeSelect, selectClassName deleted, textareaClassName kept.</done>
</task>

<task type="auto">
  <name>Task 2: Tabs migration (8 triggers, ?tab= sync preserved, flex-wrap preserved)</name>
  <files>web/src/app/(dashboard)/processos/[id]/page.tsx</files>
  <read_first>
    - 105-PATTERNS.md Shared Pattern 1 — the Processos-specific parts: the tab bar (~lines 1252-1311), the `?tab=` sync (~lines 233-250), the Auditoria trigger gating (~lines 1302-1310), and the content dispatch (`tab === "auditoria" && canManageProcessos ? (...)` at ~line 2293).
    - 105-UI-SPEC.md Component Inventory CLP-01/02 tab table + the 8-trigger list (timeline, partes, fases, decisoes, factos, testemunhas, documentos, auditoria*).
    - processos/[id]/page.tsx: the full `tab === "x" ? (...) : ...` content-dispatch chain (spans ~line 1312 to ~line 2300). Read enough to locate each of the 8 tabs' content-block boundaries.
  </read_first>
  <action>
    Add import: `Tabs, TabsList, TabsTrigger, TabsContent` from "@/components/ui/tabs".

    Wrap the tab region in `<Tabs value={tab} onValueChange={(v) => setTab(v as TabKey)}>`. Leave the existing `tab`/`setTab` useState AND the `?tab=` useSearchParams/useEffect sync block (~lines 233-250) COMPLETELY untouched — Tabs consumes the same state, so the deep-link sync keeps working with zero changes.

    Tab bar: replace the `flex flex-wrap` `<div>` of `<Button variant={tab === "X" ? "secondary" : "outline"}>` buttons with `<TabsList variant="default">` of `<TabsTrigger value="X">Label</TabsTrigger>`. Preserve the Processos-specific responsive behavior: keep an outer `flex flex-wrap` wrapper around the TabsList (TabsList is `inline-flex w-fit` so it does not wrap on its own) — do NOT convert it to Clientes' overflow-x-auto (locked: each ficha keeps its own responsive treatment). Reuse each Button's exact label verbatim (Timeline, Partes, Fases, Decisões, Factos, Testemunhas, Documentos, Auditoria).

    RBAC gating: wrap only the `auditoria` trigger in `{canManageProcessos ? <TabsTrigger value="auditoria">Auditoria</TabsTrigger> : null}` (omit, never disable).

    Content: convert each arm of the dispatch chain into `<TabsContent value="x">{...existing JSX...}</TabsContent>` (no forceMount). Do NOT modify the JSX inside each arm — 105-03 migrates the Partes/Fases/Testemunhas/Documentos tab internals afterward; this task only relocates the existing content into TabsContent wrappers. For auditoria, keep the inner gate: `<TabsContent value="auditoria">{canManageProcessos ? (...) : null}</TabsContent>` (T-105-02 Information Disclosure guard). NOTE: the Timeline tab contains its own `selectedTipos`/`aria-pressed` filter-chip group — that is NOT the main tab bar and is NOT part of this migration; leave it exactly as-is inside its TabsContent.
  </action>
  <verify>
    <automated>cd web && grep -c 'variant={tab ===' "src/app/(dashboard)/processos/[id]/page.tsx" | grep -qx 0 && grep -q '<TabsList variant="default"' "src/app/(dashboard)/processos/[id]/page.tsx" && [ "$(grep -c '<TabsTrigger' "src/app/(dashboard)/processos/[id]/page.tsx")" -eq 8 ] && [ "$(grep -c '<TabsContent' "src/app/(dashboard)/processos/[id]/page.tsx")" -eq 8 ] && grep -q 'flex flex-wrap' "src/app/(dashboard)/processos/[id]/page.tsx" && grep -q 'searchParams.get("tab")' "src/app/(dashboard)/processos/[id]/page.tsx" && pnpm build && echo GATE_PASS</automated>
  </verify>
  <acceptance_criteria>
    - Zero `variant={tab ===` occurrences remain.
    - Exactly 8 `<TabsTrigger` and 8 `<TabsContent`.
    - `flex flex-wrap` wrapper preserved (not converted to overflow-x-auto).
    - `searchParams.get("tab")` sync block still present and unchanged.
    - `pnpm build` completes with no new type errors.
  </acceptance_criteria>
  <done>Processo ficha tab bar is real Tabs; ?tab= deep-link sync preserved; auditoria trigger RBAC-gated; flex-wrap preserved; inner tab content untouched (handed to 105-03); `pnpm build` green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser -> Next proxy -> Spring API | Unchanged this phase: no new fetch/input/mutation. Existing hooks reused as-is. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-105-02 | Information Disclosure | RBAC-gated auditoria tab on the Processo ficha | mitigate | Keep `canManageProcessos` on BOTH the TabsTrigger (omit when false) AND inside the TabsContent — never render auditoria content from tab state alone. |
| T-105-SC | Tampering | package installs | accept | Zero package installs this phase — Tabs/NativeSelect/Breadcrumb installed in Phase 101. No new dependency surface. |
</threat_model>

<verification>
- `cd web && pnpm build` passes.
- `cd web && pnpm lint` reports no new errors on the file.
- Task grep gates pass; `?tab=` sync block confirmed intact.
</verification>

<success_criteria>
- CLP-02 (tab shell): 8 tabs via Tabs primitives, ?tab= sync preserved, auditoria RBAC-gated, flex-wrap preserved.
- CLP-03 (Processo file): all 8 selects are NativeSelect, selectClassName deleted, stray blue focus ring dropped.
- CLP-05 (Processo ficha): Breadcrumb header + h1 reconciled to font-semibold (single header only).
- `pnpm build` green.
</success_criteria>

<output>
Create `.planning/phases/LEXCV-105-m-dulos-clientes-processos-combinados/105-02-SUMMARY.md` when done.
</output>
