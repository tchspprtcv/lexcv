---
phase: 105-m-dulos-clientes-processos-combinados
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/app/(dashboard)/clientes/page.tsx
  - web/src/app/(dashboard)/clientes/novo/page.tsx
  - web/src/app/(dashboard)/clientes/merge/page.tsx
autonomous: true
requirements: [CLP-03, CLP-05]
must_haves:
  truths:
    - "The 2 list-filter <select> on clientes/page.tsx (draftTipo, draftAtivo) are NativeSelect size=default; the inline h-10/rounded-none/ring-blue-500 stylings are gone"
    - "The 2 RHF-bound <select> on clientes/novo/page.tsx are NativeSelect size=default and its const selectClassName is deleted (textareaClassName kept); a net-new Breadcrumb (Clientes -> Novo Cliente) sits under the h1"
    - "The 2 controlled <select> on clientes/merge/page.tsx are NativeSelect size=default; a net-new Breadcrumb (Clientes -> Merge) sits under the h1"
    - "No page in this plan changes its <h1> weight (all three are already font-semibold); the existing Voltar buttons are left untouched"
  artifacts:
    - path: "web/src/app/(dashboard)/clientes/novo/page.tsx"
      provides: "NativeSelect + net-new Breadcrumb"
      contains: "BreadcrumbList"
    - path: "web/src/app/(dashboard)/clientes/merge/page.tsx"
      provides: "NativeSelect + net-new Breadcrumb"
      contains: "BreadcrumbList"
  key_links:
    - from: "web/src/app/(dashboard)/clientes/novo/page.tsx"
      to: "@/components/ui/breadcrumb + @/components/ui/native-select"
      via: "Breadcrumb under the h1; NativeSelect replaces each register()-bound select"
      pattern: "NativeSelect"
---

<objective>
Migrate the three Clientes secondary pages — the list page (`clientes/page.tsx`), the create form (`clientes/novo/page.tsx`), and the merge action page (`clientes/merge/page.tsx`) — to `NativeSelect`, and add a net-new `Breadcrumb` to the two form/action pages. Light, self-contained files with zero overlap with the ficha plans, so this runs in Wave 1 in parallel.

Purpose: Close the Clientes-secondary share of CLP-03 (NativeSelect across all Clientes/Processos surfaces, not just the ficha) and CLP-05 (Breadcrumb on the 6 pages — the two Clientes form/action pages here).
Output: 3 files migrated; `pnpm build` green.
</objective>

<execution_context>
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/LEXCV-105-m-dulos-clientes-processos-combinados/105-CONTEXT.md
@.planning/phases/LEXCV-105-m-dulos-clientes-processos-combinados/105-PATTERNS.md
@.planning/phases/LEXCV-105-m-dulos-clientes-processos-combinados/105-UI-SPEC.md
@web/src/app/(dashboard)/clientes/page.tsx
@web/src/app/(dashboard)/clientes/novo/page.tsx
@web/src/app/(dashboard)/clientes/merge/page.tsx
@web/src/components/ui/native-select.tsx
@web/src/components/ui/breadcrumb.tsx

<interfaces>
<!-- Before/after excerpts in 105-PATTERNS.md Shared Patterns 2 (Variants A/C/D) and 5 (Breadcrumb net-new). -->
- NativeSelect from "@/components/ui/native-select": size="default"; RHF -> `<NativeSelect {...form.register("X")}>`, controlled -> `<NativeSelect value={..} onChange={..}>`. No className needed.
- Breadcrumb set from "@/components/ui/breadcrumb": Breadcrumb/BreadcrumbList/BreadcrumbItem/BreadcrumbLink(asChild Link)/BreadcrumbPage/BreadcrumbSeparator.
- Copy contract (105-UI-SPEC.md): novo -> `Clientes` (Link /clientes) -> `Novo Cliente`; merge -> `Clientes` (Link /clientes) -> `Merge` (shorter than the h1 "Merge de clientes"). All three h1s are already font-semibold — no weight change.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: clientes/page.tsx list filters -> NativeSelect</name>
  <files>web/src/app/(dashboard)/clientes/page.tsx</files>
  <read_first>
    - 105-PATTERNS.md Shared Pattern 2 Variant C (list-filter controlled selects) — before/after.
    - clientes/page.tsx: the 2 filter selects (`draftTipo` ~lines 343-351, `draftAtivo` ~lines 357-365) with their `h-10 ... rounded-none ... focus-visible:ring-blue-500` inline className.
  </read_first>
  <action>
    Add import `NativeSelect` from "@/components/ui/native-select". Replace each of the 2 filter `<select value={...} onChange={...} className="h-10 ...">...</select>` with `<NativeSelect value={...} onChange={...} size="default">...</NativeSelect>`, keeping the `<option>` children unchanged and dropping the entire inline className (including the stray `focus-visible:ring-blue-500` — intentional per UI-SPEC Color). This page has no `const selectClassName` and no Breadcrumb scope (it is a list page, not one of the 6 Breadcrumb pages) — do not add either.
  </action>
  <verify>
    <automated>cd web && grep -c "<select" "src/app/(dashboard)/clientes/page.tsx" | grep -qx 0 && grep -q "NativeSelect" "src/app/(dashboard)/clientes/page.tsx" && grep -c "ring-blue-500" "src/app/(dashboard)/clientes/page.tsx" | grep -qx 0 && echo GATE_PASS</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "<select"` returns 0; both filters are `<NativeSelect`.
    - No `ring-blue-500` remains.
    - No Breadcrumb added (out of scope for the list page).
  </acceptance_criteria>
  <done>Both Clientes list filters are NativeSelect with the stray blue ring dropped.</done>
</task>

<task type="auto">
  <name>Task 2: clientes/novo/page.tsx -> NativeSelect + Breadcrumb</name>
  <files>web/src/app/(dashboard)/clientes/novo/page.tsx</files>
  <read_first>
    - 105-PATTERNS.md Shared Pattern 2 Variant A (RHF selectClassName) and Pattern 5 net-new Breadcrumb.
    - clientes/novo/page.tsx: the 2 RHF selects at ~lines 238 and 273 (`className={selectClassName}`), the `const selectClassName` (line 31) + `const textareaClassName` (line 34), the header block (~lines 129-136) with the h1 + subtitle p + the separate Voltar Button.
  </read_first>
  <action>
    Add imports `NativeSelect` and the Breadcrumb set. Replace both `<select className={selectClassName} {...form.register("X")}>` with `<NativeSelect id="X" size="default" {...form.register("X")}>` (options unchanged). DELETE `const selectClassName` (line 31); KEEP `const textareaClassName` (line 34, used by the `<textarea>` at ~line 293). Insert a Breadcrumb directly below the `<h1 className="text-2xl font-semibold">Novo cliente</h1>` (above or below the subtitle `<p>`): `Clientes` as `BreadcrumbLink asChild` -> `<Link href="/clientes">`, `BreadcrumbSeparator`, `BreadcrumbPage`>`Novo Cliente`. Leave the h1 (already font-semibold) and the separate Voltar Button untouched.
  </action>
  <verify>
    <automated>cd web && grep -c "<select" "src/app/(dashboard)/clientes/novo/page.tsx" | grep -qx 0 && grep -c "const selectClassName" "src/app/(dashboard)/clientes/novo/page.tsx" | grep -qx 0 && grep -q "const textareaClassName" "src/app/(dashboard)/clientes/novo/page.tsx" && grep -q "BreadcrumbList" "src/app/(dashboard)/clientes/novo/page.tsx" && echo GATE_PASS</automated>
  </verify>
  <acceptance_criteria>
    - Both selects are NativeSelect; `const selectClassName` deleted; `const textareaClassName` retained.
    - Breadcrumb (Clientes link -> Novo Cliente) rendered under the h1; Voltar Button unchanged.
  </acceptance_criteria>
  <done>clientes/novo has NativeSelect selects, selectClassName removed, and a net-new Breadcrumb.</done>
</task>

<task type="auto">
  <name>Task 3: clientes/merge/page.tsx -> NativeSelect + Breadcrumb</name>
  <files>web/src/app/(dashboard)/clientes/merge/page.tsx</files>
  <read_first>
    - 105-PATTERNS.md Shared Pattern 2 Variant D (merge controlled selects, `h-10 ... rounded-md ... ring-neutral-950`) and Pattern 5 net-new Breadcrumb.
    - clientes/merge/page.tsx: the 2 controlled selects (`primaryId` ~lines 92-103, `secondaryId` ~lines 108-119) and the header block (~lines 68-76).
  </read_first>
  <action>
    Add imports `NativeSelect` and the Breadcrumb set. Replace both `<select value={...} onChange={...} className="h-10 ...">` with `<NativeSelect value={...} onChange={...} size="default">` (options `<option value="">Selecionar...</option>` + the mapped options unchanged; drop the inline className). This file has no `const selectClassName` to delete. Insert a Breadcrumb under the page `<h1>` (already font-semibold): `Clientes` -> `<Link href="/clientes">`, `BreadcrumbSeparator`, `BreadcrumbPage`>`Merge` (short form per the Copywriting Contract — the module root already disambiguates). Leave the h1 and any Voltar Button untouched.
  </action>
  <verify>
    <automated>cd web && grep -c "<select" "src/app/(dashboard)/clientes/merge/page.tsx" | grep -qx 0 && grep -q "NativeSelect" "src/app/(dashboard)/clientes/merge/page.tsx" && grep -q "BreadcrumbList" "src/app/(dashboard)/clientes/merge/page.tsx" && pnpm build && echo GATE_PASS</automated>
  </verify>
  <acceptance_criteria>
    - Both selects are NativeSelect.
    - Breadcrumb (Clientes link -> Merge) rendered under the h1.
    - `pnpm build` completes with no new type errors across all 3 files in this plan.
  </acceptance_criteria>
  <done>clientes/merge has NativeSelect selects and a net-new Breadcrumb; `pnpm build` green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser -> Next proxy -> Spring API | Unchanged: select/breadcrumb swaps add no new input, fetch, or navigation target beyond existing routes (/clientes). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-105-04 | Tampering | NativeSelect form values (novo/merge) | accept | NativeSelect renders the same real `<select>` with identical name/register bindings and the same server-side validation downstream — no change to the input contract. |
| T-105-SC | Tampering | package installs | accept | Zero installs — NativeSelect/Breadcrumb installed in Phase 101. |
</threat_model>

<verification>
- `cd web && pnpm build` passes.
- `cd web && pnpm lint` reports no new errors on the 3 files.
- Task grep gates pass.
</verification>

<success_criteria>
- CLP-03 (Clientes secondary): all 6 selects across the 3 files are NativeSelect; selectClassName deleted where present; stray blue ring dropped.
- CLP-05 (Clientes form/action pages): net-new Breadcrumb on novo + merge.
- `pnpm build` green.
</success_criteria>

<output>
Create `.planning/phases/LEXCV-105-m-dulos-clientes-processos-combinados/105-04-SUMMARY.md` when done.
</output>
