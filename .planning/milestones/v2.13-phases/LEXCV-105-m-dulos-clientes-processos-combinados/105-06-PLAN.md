---
phase: 105-m-dulos-clientes-processos-combinados
plan: 06
type: execute
wave: 3
depends_on: [105-01, 105-02, 105-03, 105-04, 105-05]
files_modified: []
autonomous: false
requirements: [CLP-01, CLP-02, CLP-03, CLP-04, CLP-05]
must_haves:
  truths:
    - "A holistic pnpm build + pnpm lint across web/ passes with zero new errors after all five migration plans land"
    - "A regression grep sweep confirms zero raw <select>/<table> and zero variant={tab===...} toggle buttons remain across the 9 in-scope files, and both const selectClassName declarations are gone while both const textareaClassName declarations survive"
    - "A human reviewer confirms both fichas render correct Tabs (keyboard arrow-nav, aria-selected, ?tab= deep link on Processo), NativeSelect, Avatar, and Breadcrumb in light AND dark themes"
    - "The RBAC-conditional tab set is verified against the real 4-role matrix (ADMIN/ADVOGADO/TECNICO/ASSISTENTE) at real mobile widths — gated triggers omitted when unpermitted, overflow-x-auto (Cliente) and flex-wrap (Processo) behaving as before"
  artifacts:
    - path: ".planning/phases/LEXCV-105-m-dulos-clientes-processos-combinados/105-06-SUMMARY.md"
      provides: "Final phase gate result + human sign-off evidence"
  key_links:
    - from: "all 5 migration plans"
      to: "single holistic build + human visual gate"
      via: "Wave 3 checkpoint after Waves 1-2 complete"
      pattern: "pnpm build"
---

<objective>
Final holistic gate for Phase 105. After the five migration plans land (Waves 1-2), run one cross-cutting build/lint/regression sweep and a mandatory human visual checkpoint covering both fichas in both themes and — critically — the RBAC-conditional tab set against the real 4-role matrix at real mobile widths. This mirrors the closing checkpoint pattern of Phases 102 (102-04) and 104 (104-06) in this milestone, and satisfies the ROADMAP's explicit demand that RBAC×mobile tab overflow be verified functionally, not by documentation.

Purpose: Guarantee the phase's core risk — a visibly inconsistent window between the two fichas, or a silently mis-gated tab — is closed before the phase is marked complete.
Output: `105-06-SUMMARY.md` recording the gate result and the human sign-off evidence.
</objective>

<execution_context>
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/LEXCV-105-m-dulos-clientes-processos-combinados/105-CONTEXT.md
@.planning/phases/LEXCV-105-m-dulos-clientes-processos-combinados/105-UI-SPEC.md
@.planning/phases/LEXCV-105-m-dulos-clientes-processos-combinados/105-01-SUMMARY.md
@.planning/phases/LEXCV-105-m-dulos-clientes-processos-combinados/105-02-SUMMARY.md
@.planning/phases/LEXCV-105-m-dulos-clientes-processos-combinados/105-03-SUMMARY.md
@.planning/phases/LEXCV-105-m-dulos-clientes-processos-combinados/105-04-SUMMARY.md
@.planning/phases/LEXCV-105-m-dulos-clientes-processos-combinados/105-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Holistic build + lint + regression grep sweep</name>
  <files>web/</files>
  <read_first>
    - All five SUMMARY files (105-01..05) to confirm what each plan reported migrating.
    - 105-UI-SPEC.md Checker Sign-Off list (the 6 dimensions) to frame the sweep.
  </read_first>
  <action>
    From `web/`, run `pnpm build` and `pnpm lint` and confirm both pass with zero NEW errors (compare against the known-clean baseline from Phase 104). Then run a regression grep sweep across the 9 in-scope files (`clientes/[id]/page.tsx`, `processos/[id]/page.tsx`, `clientes/page.tsx`, `processos/page.tsx`, `clientes/novo/page.tsx`, `processos/novo/page.tsx`, `processos/[id]/editar/page.tsx`, `clientes/merge/page.tsx`, `processos/[id]/documentos-columns.tsx`) asserting:
    - zero `<select ` (raw) across all 8 page files;
    - zero `<table`/`<thead`/`<tbody` in `processos/[id]/page.tsx`;
    - zero `variant={tab ===` in both ficha files;
    - zero `const selectClassName` in any of the 5 files that had one (clientes/[id], processos/[id], clientes/novo, processos/novo, processos/[id]/editar), AND `const textareaClassName` still present in each of those files where it existed;
    - `processos/[id]/page.tsx` header h1 reads `font-semibold`, while `processos/novo/page.tsx` h1 still reads `font-bold text-slate-900` (intentional out-of-scope preservation);
    - `documentos-columns.tsx` exists and has no `id: "processo"` column.
    Record the exact command outputs. If any assertion fails, STOP and report the specific file + gap (do not paper over it) — a failed sweep means a migration plan left a regression that must be fixed before the human checkpoint.
  </action>
  <verify>
    <automated>cd web && pnpm build && pnpm lint && for f in "src/app/(dashboard)/clientes/[id]/page.tsx" "src/app/(dashboard)/processos/[id]/page.tsx" "src/app/(dashboard)/clientes/page.tsx" "src/app/(dashboard)/processos/page.tsx" "src/app/(dashboard)/clientes/novo/page.tsx" "src/app/(dashboard)/processos/novo/page.tsx" "src/app/(dashboard)/processos/[id]/editar/page.tsx" "src/app/(dashboard)/clientes/merge/page.tsx"; do [ "$(grep -c '<select ' "$f")" -eq 0 ] || { echo "RAW SELECT in $f"; exit 1; }; done && [ "$(grep -c '<table' "src/app/(dashboard)/processos/[id]/page.tsx")" -eq 0 ] && [ "$(grep -c 'variant={tab ===' "src/app/(dashboard)/clientes/[id]/page.tsx")" -eq 0 ] && [ "$(grep -c 'variant={tab ===' "src/app/(dashboard)/processos/[id]/page.tsx")" -eq 0 ] && test -f "src/app/(dashboard)/processos/[id]/documentos-columns.tsx" && echo SWEEP_PASS</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm build` and `pnpm lint` both pass with zero new errors.
    - Every regression assertion above holds; command outputs recorded in the SUMMARY.
    - The intentional preservations (processos/novo h1 font-bold, both textareaClassName consts) are confirmed present, not swept.
  </acceptance_criteria>
  <done>Holistic build/lint green and the regression sweep confirms a clean, fully-migrated surface with intentional preservations intact.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Mandatory human visual checkpoint — both fichas, both themes, 4-role RBAC × mobile</name>
  <action>
    After Task 1's gate is green, start the dev server (`pnpm --dir web dev`) with the backend reachable so RBAC data loads, present the checklist in `<how-to-verify>`, and PAUSE for the human verdict. This is a blocking human-verify checkpoint — never auto-approve (`workflow.auto_advance` is ignored). Record the human's concrete evidence (themes, roles, viewport widths, ?tab= deep link) and the approved/issues verdict in 105-06-SUMMARY.md; if issues are reported, enumerate them for a `/gsd:plan-phase 105 --gaps` closure plan rather than silently closing the phase.
  </action>
  <what-built>
    All five migration plans: Ficha de Cliente (7 Tabs) and Ficha de Processo (8 Tabs) on real Tabs; every native select -> NativeSelect across 8 pages; Avatar on Advogados/Administrativos (Cliente ResponsaveisCard) and Testemunhas (Processo); Breadcrumb on 6 pages; Partes/Fases/Testemunhas on reconciled Table primitives; Processo Documentos tab on the shared DataTable.
  </what-built>
  <how-to-verify>
    Run `cd web && pnpm dev` and, in a real browser (with the backend reachable so RBAC data loads):

    1. THEMES: Toggle light AND dark. On both `/clientes/{id}` and `/processos/{id}`, confirm the TabsList renders as a segmented pill (variant="default"), the active tab is lifted (shadow), and there is no visual regression vs. the old button bar. Confirm Avatar circles show initials with the neutral `bg-muted` fill (NOT a blue tint), Breadcrumbs read correctly (`Clientes`/`Processos` link -> identifier), and NativeSelect fields look consistent (single rounded style, neutral focus ring).
    2. TABS ACCESSIBILITY: On both fichas, click a tab, then use LEFT/RIGHT arrow keys — focus should move between triggers (Radix keyboard nav) and Enter/Space should activate. Inspect the active trigger has `aria-selected="true"` and the list has `role="tablist"`.
    3. PROCESSO ?tab= DEEP LINK: Visit `/processos/{id}?tab=documentos` directly — the Documentos tab (now a DataTable) must open selected; switch tabs and confirm the URL-driven state still syncs.
    4. RBAC × 4 ROLES × MOBILE (the load-bearing check): Log in as each of ADMIN, ADVOGADO, TECNICO, ASSISTENTE. On the Cliente ficha confirm the `Processos`/`Pareceres` triggers appear ONLY when the role can view them (omitted, not greyed) and the gated content shows AccessDeniedState if reached. On the Processo ficha confirm the `Auditoria` trigger appears only for manage permission. Narrow the viewport to a real phone width (~375px): the Cliente tab bar should scroll horizontally (overflow-x-auto); the Processo tab bar should wrap to multiple rows (flex-wrap). Confirm the conditional (fewer) triggers still overflow/wrap correctly for the lower-permission roles.
    5. TABLES/DATATABLE: On the Processo ficha, open Partes, Fases, Testemunhas — confirm they render as clean tables (reconciled primitives), the Fases inline select + Guardar still work, "Adicionar" dialogs still open, and per-row delete still prompts. Open Documentos — confirm the DataTable sorts by column header and paginates, download/delete still work, and there is no redundant "Processo" column.
  </how-to-verify>
  <resume-signal>Type "approved" (optionally with the themes/roles/widths you exercised), or describe any issue found for a gap-closure plan.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser -> Next proxy -> Spring API | Verification-only plan — changes nothing. The human RBAC×role walkthrough is itself the T-105-01/T-105-02 Information Disclosure mitigation's acceptance test. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-105-06 | Information Disclosure | RBAC-gated tabs across both fichas | mitigate | The Task 2 checkpoint's step 4 is the explicit acceptance test that gated triggers are omitted per-role and gated content is unreachable without permission. |
| T-105-SC | Tampering | package installs | accept | Zero installs this phase. |
</threat_model>

<verification>
- Task 1 holistic build/lint/regression sweep passes.
- Task 2 human checkpoint returns "approved" with evidence (themes, 4 roles, mobile widths, ?tab= deep link).
- If the human reports an issue, capture it as a gap for `/gsd:plan-phase 105 --gaps`.
</verification>

<success_criteria>
- All of CLP-01..CLP-05 verified end-to-end across both modules, both themes, and the real 4-role RBAC matrix at mobile widths.
- `pnpm build`/`pnpm lint` green; regression sweep clean.
- Human sign-off recorded.
</success_criteria>

<output>
Create `.planning/phases/LEXCV-105-m-dulos-clientes-processos-combinados/105-06-SUMMARY.md` when done, recording the sweep outputs and the human sign-off evidence.
</output>
