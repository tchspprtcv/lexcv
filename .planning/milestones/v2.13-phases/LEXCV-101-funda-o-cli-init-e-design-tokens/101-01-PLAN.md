---
phase: 101-funda-o-cli-init-e-design-tokens
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: false
requirements: [FND-06, FND-07, FND-08]
must_haves:
  truths:
    - "Every net-new third-party npm package introduced by Phase 101 is confirmed to exist on npmjs.com with a legitimate publisher and non-trivial download history before any install task runs"
    - "react-day-picker resolves to a real 9.14.0 release (not a typosquat) and sonner/tw-animate-css/cmdk/date-fns/radix-ui are the canonical packages, not look-alikes"
  artifacts:
    - path: ".planning/phases/LEXCV-101-funda-o-cli-init-e-design-tokens/101-01-SUMMARY.md"
      provides: "Recorded legitimacy verdict per package (approved/rejected) for downstream install plans"
  key_links:
    - from: "downstream install plans (101-02, 101-03, 101-04, 101-05)"
      to: "this gate's approval"
      via: "wave dependency — all install work is gated behind this human approval"
      pattern: "depends_on"
---

<objective>
Supply-chain gate for the whole of Phase 101. No `## Package Legitimacy Audit` table exists in `.planning/research/` (verified absent in STACK.md / ARCHITECTURE.md / PITFALLS.md / SUMMARY.md / FEATURES.md), so the GSD package-legitimacy fallback applies: every net-new npm package this phase installs is treated as `[ASSUMED]` and must clear a blocking human verification before any `pnpm add` / `shadcn init` / `shadcn add` / `shadcn migrate` runs.

Purpose: Phases 101-02 through 101-05 install `tw-animate-css`, `sonner`, `cmdk`, `react-day-picker@9.14.0`, `date-fns`, the unified `radix-ui` package, and ~10 scoped `@radix-ui/react-*` primitives. This gate stops a typosquat / hijacked-package from entering the build of a production legal-practice platform.
Output: A recorded human approval (or rejection) per package, consumed by every downstream install plan as a wave-1 dependency.
</objective>

<execution_context>
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/STACK.md
@.planning/phases/LEXCV-101-funda-o-cli-init-e-design-tokens/101-UI-SPEC.md

Net-new third-party packages this phase installs (source: STACK.md "Recommended Stack" + "Supporting Libraries" tables, versions checked live against the npm registry per STACK.md Sources):
- tw-animate-css @ 1.4.0 (replaces deprecated tailwindcss-animate)
- sonner (latest; replaces deprecated @radix-ui/react-toast)
- cmdk @ 1.1.1 (Command palette; pulled by `shadcn add command`)
- react-day-picker @ 9.14.0 (Calendar; MUST NOT resolve to broken v10 — open issue shadcn-ui/ui#10914)
- date-fns @ 4.4.0 (pulled alongside react-day-picker by `shadcn add calendar`)
- radix-ui (unified package; pulled by `shadcn migrate radix`)
- scoped Radix family (same publisher as the 8 @radix-ui/react-* already in web/package.json): @radix-ui/react-select, -tabs, -dropdown-menu, -tooltip, -checkbox, -avatar, -separator, -progress, -accordion, -navigation-menu
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Package legitimacy verification (blocking, never auto-approvable)</name>
  <files>.planning/phases/LEXCV-101-funda-o-cli-init-e-design-tokens/101-01-SUMMARY.md</files>
  <read_first>
    - .planning/research/STACK.md (Recommended Stack, Supporting Libraries, What NOT to Use, Sources — the version numbers and the react-day-picker v10 warning are all here)
    - .planning/phases/LEXCV-101-funda-o-cli-init-e-design-tokens/101-UI-SPEC.md (Registry Safety section — confirms official shadcn registry only, no third-party registries)
    - web/package.json (the 8 @radix-ui/react-* packages already trusted/in use — establishes the Radix publisher precedent)
  </read_first>
  <action>
    Run the automated npm-registry probe (see `<verify><automated>`) to confirm every net-new Phase 101 package name resolves to a real, current release, and that react-day-picker offers 9.14.0. Then present the 6 genuinely-new packages (tw-animate-css, sonner, cmdk, react-day-picker, date-fns, radix-ui) plus the scoped @radix-ui/react-* family to the human for legitimacy sign-off against their npmjs.com pages (publisher, weekly downloads, source repository). This is a blocking-human gate: pause for the human verdict; never auto-approve (workflow.auto_advance is ignored for legitimacy checkpoints). Record each verdict in the plan SUMMARY. If any package is rejected, flag the downstream install(s) that must be blocked rather than silently proceeding.
  </action>
  <what-built>
    Nothing is built or installed yet. This gate runs BEFORE the first install of Phase 101. The automated step queries the public npm registry to confirm each net-new package name resolves to a real, current release; the human step confirms each is the canonical package (not a typosquat / hijacked name) before approving downstream installs.
  </what-built>
  <how-to-verify>
    1. Run the automated registry probe (see `<verify><automated>`). Confirm every package name returns a version with no "not found" errors, and that `react-day-picker` shows `9.14.0` available (NOT that it silently upgrades to a v10 line).
    2. For each of the 6 genuinely-new packages, open its page and confirm publisher + weekly downloads + repository link look legitimate:
       - https://www.npmjs.com/package/tw-animate-css  (expect: CSS-first animate utilities, actively maintained, links to a real repo)
       - https://www.npmjs.com/package/sonner  (expect: authored by emilkowalski — the well-known toast library, high weekly downloads)
       - https://www.npmjs.com/package/cmdk  (expect: authored by pacocoursey, high downloads)
       - https://www.npmjs.com/package/react-day-picker  (expect: gpbl / react-day-picker, confirm 9.14.0 is a published version)
       - https://www.npmjs.com/package/date-fns  (expect: date-fns org, very high downloads)
       - https://www.npmjs.com/package/radix-ui  (expect: the WorkOS/Radix org unified package, links to radix-ui/primitives)
    3. The 10 scoped `@radix-ui/react-*` primitives share the exact publisher/org as the 8 `@radix-ui/react-*` packages already installed in web/package.json (react-dialog, react-popover, react-switch, etc.) — confirm they are the same `@radix-ui` npm org, not a re-scoped look-alike.
    4. If ANY package looks illegitimate (wrong publisher, near-zero downloads, name off by a character, no source repo), REJECT it and record which downstream install must be blocked.
  </how-to-verify>
  <verify>
    <automated>for p in tw-animate-css sonner cmdk date-fns radix-ui @radix-ui/react-select @radix-ui/react-tabs @radix-ui/react-dropdown-menu @radix-ui/react-tooltip @radix-ui/react-checkbox @radix-ui/react-avatar @radix-ui/react-separator @radix-ui/react-progress @radix-ui/react-accordion @radix-ui/react-navigation-menu; do echo "== $p =="; pnpm view "$p" version dist-tags.latest maintainers 2>&1 | head -5; done; echo "== react-day-picker (must offer 9.14.0) =="; pnpm view react-day-picker@9.14.0 version 2>&1 | head -2</automated>
  </verify>
  <acceptance_criteria>
    - The automated probe prints a resolved version for all 15 listed packages with zero "ERR_PNPM_NO_MATCHING_VERSION" / "404 Not Found" lines.
    - `pnpm view react-day-picker@9.14.0 version` prints exactly `9.14.0` (proves the pin target is a real published release).
    - A human has recorded, in the plan SUMMARY, an explicit approved/rejected verdict for each of the 6 new packages (tw-animate-css, sonner, cmdk, react-day-picker, date-fns, radix-ui) plus a single grouped verdict for the scoped `@radix-ui/react-*` family.
    - No package is marked `[SLOP]`; any package that cannot be confirmed legitimate is REJECTED and its downstream install task is flagged as blocked (do not silently proceed).
  </acceptance_criteria>
  <resume-signal>Type "approved" to unblock the Phase 101 install plans, or list the rejected package(s) and the downstream install(s) to block.</resume-signal>
  <done>Every net-new Phase 101 package has an explicit human legitimacy verdict recorded in the SUMMARY (approved or rejected), the automated probe confirmed all names resolve and react-day-picker@9.14.0 is a real release, and downstream install plans are cleared to run (or specific installs are flagged blocked).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev/CI ← npm registry | Untrusted third-party package code enters the build of a production legal-practice platform |
| shadcn official registry → local repo | The CLI writes generated component source into `src/components/ui/` at scaffold time |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-101-SC | Tampering | npm installs (tw-animate-css, sonner, cmdk, react-day-picker, date-fns, radix-ui, scoped @radix-ui/react-*) | mitigate | This blocking-human legitimacy gate verifies each package on npmjs.com before any install; official shadcn registry only (no third-party registries per UI-SPEC Registry Safety); never auto-approvable (`workflow.auto_advance` ignored) |
| T-101-DP | Tampering | react-day-picker version resolution | mitigate | Pin target confirmed as a real 9.14.0 release here; the actual pin is enforced in 101-03 to avoid the broken v10 line (issue #10914) |
| T-101-SP | Spoofing | package name typosquat (e.g. `radix-ui` vs a look-alike) | mitigate | Human confirms publisher/org + repository link matches the canonical maintainer for each new package |
</threat_model>

<verification>
- Automated registry probe resolves all 15 packages; `react-day-picker@9.14.0` confirmed published.
- Human legitimacy verdict recorded per new package; no `[SLOP]`, no unresolved `[ASSUMED]` proceeds to install.
</verification>

<success_criteria>
- All net-new Phase 101 packages have an explicit human legitimacy verdict recorded before any install runs.
- Downstream install plans (101-02..101-05) may proceed only after this gate is approved.
</success_criteria>

<output>
Create `.planning/phases/LEXCV-101-funda-o-cli-init-e-design-tokens/101-01-SUMMARY.md` when done, recording the per-package legitimacy verdict.
</output>
