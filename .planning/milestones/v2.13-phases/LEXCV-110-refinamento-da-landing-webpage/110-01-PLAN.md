---
phase: 110-refinamento-da-landing-webpage
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - webpage/src/components/ui/sheet.tsx
  - webpage/src/components/site-header.tsx
autonomous: true
requirements: [LDG-17]
user_setup: []

must_haves:
  truths:
    - "On a mobile viewport (<768px) a hamburger button (aria-label=\"Abrir menu\", md:hidden) is visible in the SiteHeader"
    - "Tapping the hamburger opens a Sheet drawer (side=right) listing the 3 anchor nav links (Funcionalidades/Confiança/Contacto) plus an Entrar CTA"
    - "Tapping any drawer anchor link or the drawer Entrar CTA closes the drawer via a direct onClick setOpen(false) — never a useEffect(pathname) effect"
    - "The header-level Entrar button carries hidden md:inline-flex, so Entrar never renders twice on mobile"
    - "Desktop (≥768px) header is visually unchanged: 3 nav anchors + ThemeToggle + Entrar button, and the hamburger is not visible"
  artifacts:
    - path: "webpage/src/components/ui/sheet.tsx"
      provides: "Sheet primitive (byte-identical copy of web/'s post-Phase-102 sheet.tsx)"
      exports: ["Sheet", "SheetTrigger", "SheetContent", "SheetTitle", "SheetClose", "SheetPortal", "SheetOverlay", "SheetHeader", "SheetFooter", "SheetDescription"]
      min_lines: 130
    - path: "webpage/src/components/site-header.tsx"
      provides: "Client-component header with Sheet mobile nav + shared NAV_LINKS"
      contains: "use client"
      min_lines: 40
  key_links:
    - from: "webpage/src/components/site-header.tsx"
      to: "webpage/src/components/ui/sheet.tsx"
      via: "import Sheet/SheetTrigger/SheetContent/SheetTitle"
      pattern: "from \"@/components/ui/sheet\""
    - from: "webpage/src/components/site-header.tsx (each drawer anchor)"
      to: "Sheet open state"
      via: "onClick setOpen(false)"
      pattern: "setOpen\\(false\\)"
---

<objective>
Add functional mobile navigation to the public landing header (`webpage/`), closing LDG-17. Today `webpage/`'s `SiteHeader` has ZERO mobile navigation — the 3 nav anchors are `hidden md:flex`, so on phones there is no way to reach them. This plan copies the `Sheet` primitive into `webpage/` verbatim from `web/`, then converts `site-header.tsx` into a Client Component that renders a hamburger-triggered `Sheet` drawer holding the same nav links plus the Entrar CTA.

Purpose: eliminate a real functional gap (mobile users cannot navigate the landing) while preserving the exact desktop header that ships today.
Output: `webpage/src/components/ui/sheet.tsx` (new, verbatim copy) and a rewritten `webpage/src/components/site-header.tsx`.

Scope boundary: `webpage/` is a SEPARATE Next.js app from `web/`. All build/lint commands run in `webpage/`, never `web/`. Do NOT reconcile or modify `webpage/src/components/ui/card.tsx` or `button.tsx` (out of scope — see UI-SPEC Divergence Notes).
</objective>

<execution_context>
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/LEXCV-110-refinamento-da-landing-webpage/110-CONTEXT.md
@.planning/phases/LEXCV-110-refinamento-da-landing-webpage/110-UI-SPEC.md
@.planning/phases/LEXCV-110-refinamento-da-landing-webpage/110-PATTERNS.md

<interfaces>
<!-- Contracts the executor consumes. Extracted from source — no codebase exploration needed. -->

Sheet primitive exports (from web/src/components/ui/sheet.tsx, being copied verbatim):
  Sheet            = Dialog.Root         (controlled via open / onOpenChange)
  SheetTrigger     = Dialog.Trigger      (use asChild to wrap the hamburger <button>)
  SheetContent     = Dialog.Content      (prop: side?: "top"|"right"|"bottom"|"left", default "right"; renders its own X close button + overlay)
  SheetTitle       = Dialog.Title        (Radix requires a Title for aria-labelledby; render <SheetTitle className="sr-only">Menu</SheetTitle> as first child of SheetContent)
  (also exported, unused here: SheetClose, SheetPortal, SheetOverlay, SheetHeader, SheetFooter, SheetDescription)

Current webpage/src/components/site-header.tsx shape (base to modify):
  export function SiteHeader({ branding }: { branding: BrandingResponse })
  - header (sticky, h-16) > div (flex) > BrandMark + right-group
  - right-group: <nav className="hidden md:flex ..."> 3 anchors </nav>, then ThemeToggle + Entrar <Button asChild variant="secondary" className="rounded-none"><a href="/login">Entrar</a></Button>
  - the 3 anchors: #funcionalidades "Funcionalidades", #confianca "Confiança", #contacto "Contacto"
  - NO "use client" directive today (must be added)

Reference-only structural analogs (do NOT copy the useEffect(pathname) anti-pattern):
  web/src/components/shared/dashboard-shell.tsx — hamburger trigger markup + Sheet open/onOpenChange wiring
  web/src/components/shared/sidebar-nav.tsx — click-to-close via direct onClick (the correct pattern)
  webpage/src/components/theme-toggle.tsx — the h-9 w-9 (36px) sizing the hamburger must match
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Copy the Sheet primitive into webpage/ verbatim</name>
  <files>webpage/src/components/ui/sheet.tsx</files>
  <read_first>
    - web/src/components/ui/sheet.tsx (the source — copy this exactly)
    - webpage/src/lib/utils.ts (confirm `cn` export resolves identically — it does, byte-identical behavior)
    - webpage/package.json (confirm `radix-ui` + `lucide-react` are already dependencies — they are)
  </read_first>
  <action>
    Create `webpage/src/components/ui/sheet.tsx` as a byte-identical copy of `web/src/components/ui/sheet.tsx`. Use a shell copy (`cp web/src/components/ui/sheet.tsx webpage/src/components/ui/sheet.tsx`) so the bytes are guaranteed identical. Make ZERO edits: the imports (`radix-ui`'s `Dialog as DialogPrimitive`, `lucide-react`'s `X`, `@/lib/utils`'s `cn`) all resolve unchanged in `webpage/` because the `@/*` alias, `radix-ui@^1.6.2`, `lucide-react@^0.543.0`, and `webpage/src/lib/utils.ts` all already exist. The file must keep its `"use client"` directive (line 1) and all 10 exports (Sheet, SheetClose, SheetContent, SheetDescription, SheetFooter, SheetHeader, SheetOverlay, SheetPortal, SheetTitle, SheetTrigger). Do NOT add `@import "shadcn/tailwind.css"` anywhere and do NOT touch `globals.css` — per UI-SPEC Divergence Note §2 the Sheet's animations use `tw-animate-css` which both apps already import.
  </action>
  <verify>
    <automated>diff web/src/components/ui/sheet.tsx webpage/src/components/ui/sheet.tsx && grep -q 'SheetTrigger,' webpage/src/components/ui/sheet.tsx && grep -q 'from "radix-ui"' webpage/src/components/ui/sheet.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `diff web/src/components/ui/sheet.tsx webpage/src/components/ui/sheet.tsx` prints nothing (files are byte-identical, exit 0)
    - `grep -q 'from "radix-ui"' webpage/src/components/ui/sheet.tsx` succeeds (import path unchanged, not rewritten to a scoped package)
    - The export block contains all of: Sheet, SheetTrigger, SheetContent, SheetTitle (grep each succeeds)
    - `globals.css` was NOT modified (no new `@import` line added this task)
  </acceptance_criteria>
  <done>webpage/src/components/ui/sheet.tsx exists and is a byte-for-byte copy of web/'s sheet.tsx.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite SiteHeader as a Client Component with a Sheet mobile drawer</name>
  <files>webpage/src/components/site-header.tsx</files>
  <read_first>
    - webpage/src/components/site-header.tsx (current 27-line file — the exact base to modify)
    - webpage/src/components/ui/sheet.tsx (the primitive created in Task 1 — its exports/props)
    - webpage/src/components/theme-toggle.tsx (confirm its h-9 w-9 sizing the hamburger must match)
    - web/src/components/shared/dashboard-shell.tsx (structural reference ONLY: hamburger trigger markup lines ~112-119, Sheet open/onOpenChange wiring — do NOT copy its useEffect(pathname) at lines ~54-56)
    - web/src/components/shared/sidebar-nav.tsx (the correct click-to-close pattern: direct onClick, lines ~39-53)
    - .planning/phases/LEXCV-110-refinamento-da-landing-webpage/110-UI-SPEC.md (Copywriting Contract + Header layout resolution sections)
  </read_first>
  <action>
    Rewrite `webpage/src/components/site-header.tsx` to add mobile navigation via `Sheet`, keeping the desktop header pixel-identical to today. Concrete requirements:

    1. Add `"use client";` as the very first line (needed for `React.useState`). Keep existing imports (`BrandMark`, `ThemeToggle`, `Button`, `BrandingResponse`) and add: `import * as React from "react"` (or `useState`), `import { Menu } from "lucide-react"`, and `import { Sheet, SheetContent, SheetTrigger, SheetTitle } from "@/components/ui/sheet"`.

    2. Extract a `NAV_LINKS` constant (array of `{ href, label }`) holding the 3 anchor links exactly as they exist today: `{ href: "#funcionalidades", label: "Funcionalidades" }`, `{ href: "#confianca", label: "Confiança" }`, `{ href: "#contacto", label: "Contacto" }`. Name and file location are executor's discretion per CONTEXT.md (inside site-header.tsx is fine). The desktop `<nav className="hidden md:flex ...">` must render these by mapping over NAV_LINKS (keep the existing per-anchor className `text-sm font-semibold text-slate-600 hover:text-blue-600 dark:text-slate-300 dark:hover:text-blue-400`) — do not leave the 3 anchors hardcoded AND also in the array (avoid the duplicate list CONTEXT.md warns against).

    3. Add `const [open, setOpen] = React.useState(false)`. Wrap a `Sheet open={open} onOpenChange={setOpen}`. Its `SheetTrigger asChild` wraps a `<button type="button" aria-label="Abrir menu" className="md:hidden ... h-9 w-9 ...">` containing `<Menu className="h-5 w-5" />`. The `aria-label` string MUST be exactly `Abrir menu` (matches web/'s convention per UI-SPEC). Keep the trigger at `h-9 w-9` (36px) to match the adjacent `ThemeToggle` — do NOT use a 44px touch target.

    4. `SheetContent` uses `side="right"` and `className="w-72 sm:max-w-sm"`. Its FIRST child must be `<SheetTitle className="sr-only">Menu</SheetTitle>` (Radix Dialog requires a Title for aria-labelledby, or React logs an a11y warning). Inside, render the 3 NAV_LINKS as `<a key={link.href} href={link.href} onClick={() => setOpen(false)} ...>{link.label}</a>`, then an Entrar CTA `<a href="/login" onClick={() => setOpen(false)}>Entrar</a>` (a Button asChild is acceptable; keep `/login` as a plain anchor href, NOT next/link — /login lives in web/, not webpage/'s route table). Every clickable item in the drawer calls `setOpen(false)`.

    5. Close-on-navigate MUST be the direct inline `onClick={() => setOpen(false)}` on each anchor/CTA. Do NOT add `usePathname`, do NOT add any `React.useEffect(..., [pathname])`. The 3 links are same-page `#anchor` links that never change `pathname`, so a pathname effect would never fire — this is exactly the WR-01 anti-pattern class fixed in Phase 109 (see 109-REVIEW.md).

    6. Change the existing header-level Entrar `Button` className from `rounded-none` to `rounded-none hidden md:inline-flex` so it is desktop-only and does not duplicate the drawer's Entrar on mobile. Keep its `variant="secondary"`, `asChild`, and `<a href="/login">Entrar</a>` content unchanged.

    7. `ThemeToggle` stays rendered at all breakpoints (it is NOT part of NAV_LINKS and never goes inside the Sheet). Mobile right-side group = ThemeToggle + hamburger trigger; desktop right-side group = nav + ThemeToggle + Entrar button.
  </action>
  <verify>
    <automated>cd webpage && pnpm build && grep -q '"use client"' src/components/site-header.tsx && grep -q 'aria-label="Abrir menu"' src/components/site-header.tsx && grep -q 'setOpen(false)' src/components/site-header.tsx && grep -q 'side="right"' src/components/site-header.tsx && grep -q 'hidden md:inline-flex' src/components/site-header.tsx && grep -q 'sr-only' src/components/site-header.tsx && ! grep -q 'usePathname' src/components/site-header.tsx && ! grep -q 'useEffect' src/components/site-header.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm build` in `webpage/` completes with zero type errors (Next build = typecheck gate; webpage/ has no standalone tsc script)
    - `grep -q '"use client"' webpage/src/components/site-header.tsx` succeeds (now a Client Component)
    - `grep -q 'aria-label="Abrir menu"' webpage/src/components/site-header.tsx` succeeds (exact copywriting-contract string)
    - `grep -q 'side="right"' webpage/src/components/site-header.tsx` and `grep -q 'w-72' webpage/src/components/site-header.tsx` both succeed
    - `grep -q 'hidden md:inline-flex' webpage/src/components/site-header.tsx` succeeds (header Entrar is desktop-only)
    - `grep -q 'setOpen(false)' webpage/src/components/site-header.tsx` succeeds (click-to-close present)
    - `grep -q 'sr-only' webpage/src/components/site-header.tsx` succeeds (visually-hidden SheetTitle present)
    - `! grep -q 'usePathname' webpage/src/components/site-header.tsx` — usePathname is NOT imported (anti-pattern avoided)
    - `! grep -q 'useEffect' webpage/src/components/site-header.tsx` — no pathname-watching effect
    - `grep -c '#funcionalidades\|#confianca\|#contacto' webpage/src/components/site-header.tsx` shows the 3 anchor hrefs appear exactly once each in a single NAV_LINKS source (not duplicated as hardcoded literals AND array entries)
  </acceptance_criteria>
  <done>Mobile viewport shows a working hamburger→Sheet drawer with the 3 nav links + Entrar (all closing on click); desktop header is unchanged with the Entrar button desktop-only; build passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser (client) rendering | Static marketing content + client-only Sheet open/close state. No server input crosses into this plan; no network calls, no auth, no user-supplied data are introduced. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-110-01 | Tampering / Information Disclosure | site-header.tsx nav hrefs (`#funcionalidades`, `#confianca`, `#contacto`, `/login`) | accept | All hrefs are static, hardcoded, in-file string literals from NAV_LINKS — no user/dynamic input, no injection surface. `/login` is an intentional cross-zone plain `<a>` (web/ owns that route). |
| T-110-02 | Spoofing (clickjacking of the drawer) | Sheet drawer overlay | accept | Radix Dialog overlay + focus trap are inherited unchanged from the vetted `web/` primitive; public landing exposes no privileged action inside the drawer (only nav + a login link). |
| T-110-SC | Tampering (supply chain) | new file webpage/src/components/ui/sheet.tsx | accept | No npm/pip/cargo install this phase. `sheet.tsx` is a byte-identical in-repo copy of a file already vetted through Phase 101's package-legitimacy gate; `radix-ui` is already a webpage/ dependency. No registry fetch, no new package. |
</threat_model>

<verification>
- `cd webpage && pnpm build` passes (typecheck + build).
- `webpage/src/components/ui/sheet.tsx` is byte-identical to `web/src/components/ui/sheet.tsx`.
- `site-header.tsx` is a Client Component exposing a `Sheet` drawer with the 3 anchors + Entrar, each closing on click, and the header Entrar button gated `hidden md:inline-flex`.
- No `usePathname`/`useEffect(pathname)` anywhere in `site-header.tsx`.
</verification>

<success_criteria>
LDG-17 satisfied: the SiteHeader shows functional mobile navigation via a reused `Sheet` (previously zero mobile nav existed), while the desktop header remains identical to today and the Entrar CTA never appears twice.
</success_criteria>

<output>
Create `.planning/phases/LEXCV-110-refinamento-da-landing-webpage/110-01-SUMMARY.md` when done.
</output>
