---
phase: 109-notifica-es-settings-setup-wizard
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/components/shared/notification-bell.tsx
  - web/src/app/setup/page.tsx
autonomous: true
requirements: [NTF-29, NTF-30]

must_haves:
  truths:
    - "The notification-bell unread counter renders via the official Badge component (not a manual <span>)"
    - "The counter keeps its exact position/size (absolute -top-0.5 -right-0.5 h-4 w-4) and color logic (red normal / slate-400 on fetch error) and cap ('9+' / '!')"
    - "The /setup wizard shows a linear Progress bar always visible from mount"
    - "The Progress value reflects wizard phase: idle=33%, submitting=66%, success=100%"
    - "The 3 Checklist lines are preserved verbatim as a legend below the Progress bar"
  artifacts:
    - path: "web/src/components/shared/notification-bell.tsx"
      provides: "Badge-based unread counter replacing the manual span"
      contains: "<Badge"
    - path: "web/src/app/setup/page.tsx"
      provides: "Progress indicator derived from form/submit state"
      contains: "<Progress"
  key_links:
    - from: "web/src/components/shared/notification-bell.tsx"
      to: "@/components/ui/badge"
      via: "Badge for unread counter"
      pattern: "<Badge"
    - from: "web/src/app/setup/page.tsx"
      to: "@/components/ui/progress"
      via: "Progress import + render"
      pattern: "<Progress"
    - from: "web/src/app/setup/page.tsx"
      to: "form.formState.isSubmitting / successMessage"
      via: "wizardPhase derivation"
      pattern: "isSubmitting"
---

<objective>
Two independent single-file component-identity swaps, sharing no files with Plan 109-01:
- NTF-29: replace the manual `<span>` unread counter in `notification-bell.tsx` (lines 88-95) with the official `Badge` primitive, reproducing today's position/size/color/cap logic 1:1 via a `className` override.
- NTF-30: replace the static "Checklist" text block in the `/setup` wizard (`setup/page.tsx` lines 261-270) with a linear `Progress` bar whose value is derived from the wizard's existing form/submit state (no new business state), keeping the 3 checklist lines as a legend.

Purpose: Adopt the official `Badge` and `Progress` primitives for the last two NTF surfaces, without touching the bell's `Popover` composition or the setup submission logic.
Output: `web/src/components/shared/notification-bell.tsx` + `web/src/app/setup/page.tsx`, each with a single localized change.
</objective>

<execution_context>
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/LEXCV-109-notifica-es-settings-setup-wizard/109-CONTEXT.md
@.planning/phases/LEXCV-109-notifica-es-settings-setup-wizard/109-PATTERNS.md
@.planning/phases/LEXCV-109-notifica-es-settings-setup-wizard/109-UI-SPEC.md

# Progress analog: the only other in-repo Progress consumer
@web/src/app/(dashboard)/documentos/novo/page.tsx
@web/src/components/ui/badge.tsx
@web/src/components/ui/progress.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate notification-bell unread counter from &lt;span&gt; to Badge (NTF-29)</name>
  <read_first>
    - web/src/components/shared/notification-bell.tsx (current counter at lines 87-95; `showBadge`/`unread.isError`/`count` logic at 50-52; note `Badge` is ALREADY imported at line 8 and used for the category chip at line 36; note `cn` is NOT currently imported)
    - web/src/components/ui/badge.tsx (badgeVariants base classes: `inline-flex items-center rounded-full border px-2.5 py-0.5 text-xs font-medium`; default variant is `secondary`)
    - web/src/lib/utils.ts (the `cn` = twMerge(clsx(...)) helper)
    - .planning/phases/LEXCV-109-notifica-es-settings-setup-wizard/109-UI-SPEC.md ("Component Inventory → Badge") and 109-PATTERNS.md ("notification-bell.tsx" section)
  </read_first>
  <files>web/src/components/shared/notification-bell.tsx</files>
  <action>
    Replace the manual `<span>` counter (the block rendered under `{showBadge && (...)}` at lines 88-95) with a `<Badge>` element. Keep the base variant default (`secondary`) and override appearance entirely via `className`, using `cn(...)` to guarantee Tailwind class-conflict-safe resolution. The className must reproduce the current visuals exactly: `absolute -top-0.5 -right-0.5 h-4 w-4 rounded-full p-0 flex items-center justify-center text-[10px] font-bold leading-none text-white border-transparent` plus the conditional color `unread.isError ? "bg-slate-400" : "bg-red-500"`. Here `flex` overrides the shipped `inline-flex`, `p-0` overrides the shipped `px-2.5 py-0.5`, and `bg-red-500`/`bg-slate-400` override the shipped `bg-neutral-100` (all same-property-group overrides that `cn`/twMerge resolves correctly). Do NOT use the existing soft `"red"` Badge variant — it is too muted (`bg-red-100 text-red-700`); the vibrant `bg-red-500` is required (locked in CONTEXT).

    Keep the `children` logic verbatim: `unread.isError ? "!" : count > 9 ? "9+" : count`. Keep the `{showBadge && ...}` conditional unchanged. Add `import { cn } from "@/lib/utils";` (currently absent in this file). Do NOT touch the `Popover`/`PopoverTrigger`/`PopoverContent`, the notification list, the snooze control, mark-all/mark-one, or the category-chip `Badge` at line 36 — only the counter block changes.
  </action>
  <verify>
    <automated>cd web && grep -q 'import { cn } from "@/lib/utils"' src/components/shared/notification-bell.tsx && grep -q 'bg-red-500' src/components/shared/notification-bell.tsx && grep -q '-top-0.5 -right-0.5 h-4 w-4' src/components/shared/notification-bell.tsx && [ "$(grep -vE '^\s*(//|\*|/\*)' src/components/shared/notification-bell.tsx | grep -c '<span')" = "0" ] && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - The unread counter renders as `<Badge className={cn("absolute -top-0.5 -right-0.5 h-4 w-4 rounded-full p-0 flex items-center justify-center text-[10px] font-bold leading-none text-white border-transparent", unread.isError ? "bg-slate-400" : "bg-red-500")}>`.
    - Badge `children` = `unread.isError ? "!" : count > 9 ? "9+" : count`; wrapped in `{showBadge && ...}`.
    - `import { cn } from "@/lib/utils"` is present; no manual `<span>` remains as the counter (grep shows 0 `<span` outside comments).
    - The category-chip `Badge` (line 36), the `Popover` composition, snooze control, and mark-read handlers are unchanged.
  </acceptance_criteria>
  <done>Unread counter is an official Badge with the exact original position/size/color/cap; bell Popover untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Replace /setup Checklist block with a Progress indicator (NTF-30)</name>
  <read_first>
    - web/src/app/setup/page.tsx (Checklist block lines 261-270; state signals: `serverError` (31/281), `successMessage` (32/287), `form.formState.isSubmitting` (296); note `Progress` is NOT yet imported — current imports are Button, Card*, Input, Label, apiFetch, setupSchema, types)
    - web/src/app/(dashboard)/documentos/novo/page.tsx (the established "label + percentage row above the bar" Progress composition)
    - web/src/components/ui/progress.tsx (value is 0-100; fill is `bg-primary`, no override needed)
    - .planning/phases/LEXCV-109-notifica-es-settings-setup-wizard/109-UI-SPEC.md ("Component Inventory → Progress") and 109-PATTERNS.md ("setup/page.tsx" section)
  </read_first>
  <files>web/src/app/setup/page.tsx</files>
  <action>
    Add `import { Progress } from "@/components/ui/progress";`. Derive a wizard phase as a computed value inside the component body (no new `useState`, no new business state) — name it at executor discretion (e.g. `wizardPhase`): `successMessage ? "success" : form.formState.isSubmitting ? "submitting" : "idle"`, and a `wizardProgress` number mapped `success → 100`, `submitting → 66`, `idle → 33`. Do NOT add a distinct "error" branch: a failed submit leaves `isSubmitting=false` and `successMessage=null`, correctly falling back to `idle`/33% (the user is back to a resubmittable form). `serverError` keeps its own separate inline box unchanged.

    Replace the inner content of the existing Checklist card (`<div className="space-y-3 border border-slate-200 bg-slate-50 p-5 dark:border-slate-800 dark:bg-[#020617]">`, lines 261-270) — keep that exact outer container — with: a header row `<div className="flex items-center justify-between text-xs font-semibold uppercase tracking-[0.22em] text-slate-500 dark:text-slate-400">` containing `<span>Progresso</span>` and `<span>{wizardProgress}%</span>`; then `<Progress value={wizardProgress} />` (always rendered from mount, never conditionally hidden); then the 3 existing checklist lines preserved verbatim as a legend `<div className="space-y-3 text-sm leading-6 text-slate-600 dark:text-slate-400">` with the exact `<p>` copy "1. Criação do primeiro tenant institucional." / "2. Criação do utilizador administrador com password hasheada." / "3. Fecho definitivo do wizard após sucesso." Renaming the label "Checklist" → "Progresso" is the recommended default (keeping "Checklist" is an acceptable alternative). Do NOT touch the adjacent "Regras da Password" card, the `serverError`/`successMessage` boxes, or the submit Button.
  </action>
  <verify>
    <automated>cd web && grep -q 'import { Progress } from "@/components/ui/progress"' src/app/setup/page.tsx && grep -q '<Progress value=' src/app/setup/page.tsx && grep -q 'isSubmitting' src/app/setup/page.tsx && grep -q 'Fecho definitivo do wizard após sucesso' src/app/setup/page.tsx && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `import { Progress } from "@/components/ui/progress"` is present.
    - A computed wizard phase derives `success/submitting/idle` from `successMessage` and `form.formState.isSubmitting` (no new `useState`), mapped to `100/66/33`.
    - The Checklist card's outer container is preserved; its inner content is now: a `Progresso` (or `Checklist`) label + `{wizardProgress}%` header row, a `<Progress value={wizardProgress} />` bar (always rendered), and the 3 verbatim checklist `<p>` lines as a legend below.
    - No `bg-primary` override on `<Progress>`; the "Regras da Password" card, error/success boxes, and submit button are unchanged.
  </acceptance_criteria>
  <done>The /setup wizard shows an always-visible Progress bar (33/66/100%) derived from existing submit state, with the 3 checklist lines retained as a legend.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| unauthenticated visitor → /setup | `/setup` is a public first-run route (proxy.ts gates un-initialized installs here); the Progress indicator renders pre-auth. |
| authenticated browser → bell data | `notification-bell.tsx` renders within the authenticated shell; the unread count is already fetched via an authorized query. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-109-03 | Information Disclosure | /setup Progress indicator | accept | The Progress `value` (33/66/100) is derived purely from local form/submit state; it reveals no tenant/user/credential data. `/setup` is already public and its submission logic is unchanged by this plan. |
| T-109-04 | Information Disclosure | Bell unread Badge | accept | The Badge renders an already-fetched unread `count` / error sentinel; no new data is exposed and the `useNotificacoesUnreadCount` query is unchanged. |
| T-109-SC | Tampering | npm installs | accept | Zero new dependencies — `Badge` and `Progress` (and their `radix-ui` backing for Progress) were vetted/installed in Phase 101's package-legitimacy gate and already have prior consumers. No `shadcn add`/`pnpm add`. |
</threat_model>

<verification>
- `notification-bell.tsx`: unread counter is a `<Badge>` with the exact original className override; `cn` imported; Popover untouched.
- `setup/page.tsx`: `<Progress>` imported and rendered with a value derived from existing submit state; 3 checklist lines retained.
- Authoritative `pnpm build`/`pnpm lint` gate runs in Plan 109-03.
</verification>

<success_criteria>
NTF-29 satisfied: the bell unread counter is the official `Badge` (red normal / slate-400 error, `9+`/`!` cap, same position). NTF-30 satisfied: the `/setup` wizard shows a linear `Progress` bar (33/66/100%) from existing state, with the checklist lines preserved as a legend. No third-party Stepper, no new npm dependency.
</success_criteria>

<output>
Create `.planning/phases/LEXCV-109-notifica-es-settings-setup-wizard/109-02-SUMMARY.md` when done.
</output>
