---
phase: 109-notifica-es-settings-setup-wizard
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/components/shared/user-menu.tsx
  - web/src/components/shared/dashboard-shell.tsx
autonomous: true
requirements: [NTF-28]

must_haves:
  truths:
    - "Clicking the topbar avatar/name block opens a DropdownMenu (no longer navigates directly to /settings)"
    - "The user menu shows exactly: Perfil, Configurações, separator, Terminar sessão — in that order"
    - "The same shared UserMenu renders at all 3 call sites (topbar header, desktop sidebar footer, mobile Sheet footer)"
    - "Selecting 'Terminar sessão' from the menu logs the user out (calls the existing onLogout)"
    - "The desktop sidebar and Sheet footers no longer show a standalone icon-only logout button (folded into the menu)"
  artifacts:
    - path: "web/src/components/shared/user-menu.tsx"
      provides: "Shared UserMenu DropdownMenu component (topbar + sidebar variants)"
      contains: "DropdownMenuTrigger"
      min_lines: 40
    - path: "web/src/components/shared/dashboard-shell.tsx"
      provides: "3 UserMenu call sites; removed inline avatar+logout blocks"
      contains: "UserMenu"
  key_links:
    - from: "web/src/components/shared/dashboard-shell.tsx"
      to: "web/src/components/shared/user-menu.tsx"
      via: "import + 3 render sites"
      pattern: "<UserMenu"
    - from: "web/src/components/shared/user-menu.tsx"
      to: "@/components/ui/dropdown-menu"
      via: "DropdownMenu primitive import"
      pattern: "DropdownMenu"
    - from: "web/src/components/shared/user-menu.tsx"
      to: "onLogout prop"
      via: "DropdownMenuItem onSelect"
      pattern: "onSelect=\\{onLogout\\}"
---

<objective>
Build the shared `UserMenu` DropdownMenu component and consume it at all 3 user-menu call sites in `dashboard-shell.tsx`, satisfying NTF-28. The topbar avatar+name block, the desktop sidebar footer, and the mobile Sheet footer today are 3 near-duplicate inline blocks that navigate directly to `/settings` on click (and, for the two footers, expose logout via a structurally-independent adjacent Tooltip+Button). This plan consolidates all 3 into one `DropdownMenuTrigger asChild` menu with locked items Perfil → Configurações → separator → Terminar sessão.

Purpose: Replace the ad-hoc direct-`Link` user block with the official `DropdownMenu` primitive (its first real consumer in `web/src`), giving accessible keyboard/focus semantics and unifying the logout action into the menu.
Output: `web/src/components/shared/user-menu.tsx` (new) + `web/src/components/shared/dashboard-shell.tsx` (3 call sites rewired, dead inline blocks + now-unused imports removed).
</objective>

<execution_context>
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/LEXCV-109-notifica-es-settings-setup-wizard/109-CONTEXT.md
@.planning/phases/LEXCV-109-notifica-es-settings-setup-wizard/109-PATTERNS.md
@.planning/phases/LEXCV-109-notifica-es-settings-setup-wizard/109-UI-SPEC.md

# Structural analog: the only existing DropdownMenu consumer in web/src
@web/src/components/shared/data-table/data-table-view-options.tsx
@web/src/components/ui/dropdown-menu.tsx

<interfaces>
<!-- The UserMenu prop shape (executor discretion on exact typing per 109-CONTEXT.md).
     Recommended: { variant: "topbar" | "sidebar"; me: ReturnType<typeof useMe>["data"]; onLogout: () => void }
     `me.data` fields consumed: me.nome (string), me.roles (string[]), me.avatar_url (string | null-ish).
     Read web/src/hooks/use-me.ts + web/src/types/auth.ts for the exact type. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create shared UserMenu DropdownMenu component</name>
  <read_first>
    - web/src/components/shared/dashboard-shell.tsx (current 3 inline blocks: topbar lines 294-311, sidebar footer 135-164, Sheet footer 220-249; the onLogout function lines 71-79 — passed in as a prop, NOT re-implemented)
    - web/src/components/shared/data-table/data-table-view-options.tsx (the sole existing DropdownMenu consumer — load-bearing precedent for `DropdownMenuTrigger asChild` + `DropdownMenuContent align/width` override)
    - web/src/components/ui/dropdown-menu.tsx (primitive API; note DropdownMenuItem's `variant` prop is "default" | "destructive")
    - web/src/hooks/use-me.ts + web/src/types/auth.ts (exact `me` data type for the prop)
    - .planning/phases/LEXCV-109-notifica-es-settings-setup-wizard/109-PATTERNS.md (section "user-menu.tsx" — verbatim JSX to extract) and 109-UI-SPEC.md ("Component Inventory → DropdownMenu")
  </read_first>
  <files>web/src/components/shared/user-menu.tsx</files>
  <action>
    Create the client component `UserMenu` (`"use client"`). It takes a `variant` ("topbar" | "sidebar"), the `me` data object, and an `onLogout: () => void` callback (passed through unchanged from DashboardShell). Render exactly one `DropdownMenu` with a single `DropdownMenuTrigger asChild` wrapping one non-navigating trigger element — a `<button type="button">` (or `<div role="button" tabIndex={0}>`) — that contains the existing avatar+name/role JSX, restructured from the current 1-2 separate `<Link href="/settings">`s into that single trigger. Do NOT keep any inner `<Link href="/settings">` on the trigger: per D-CONTEXT the whole block stops navigating directly; navigation now happens via the menu items.

    The trigger visuals differ by variant, preserved pixel-for-pixel from dashboard-shell.tsx: `variant="topbar"` = right-aligned name (`text-sm font-semibold`, hidden `<sm` via `hidden sm:block`) + role text, then the circular gradient avatar (`h-9 w-9 rounded-full bg-gradient-to-tr from-blue-600 to-indigo-500 ... ring-2`); `variant="sidebar"` = square avatar (`h-9 w-9 rounded-md bg-slate-800`) then name (`text-sm font-medium truncate text-slate-200`) + role (`text-[10px] ... uppercase tracking-wider`). Preserve the initials-fallback logic verbatim: split `me?.nome ?? "U"` on spaces, take the first 2 parts, map to first char, join, uppercase; render `me.avatar_url` as an `<img className="w-full h-full object-cover">` when present.

    `DropdownMenuContent`: `className="w-56"` (fixed 224px, NOT the shipped trigger-matched width), `align={variant === "topbar" ? "end" : "start"}`. Do NOT set a `side` prop — rely on Radix default `avoidCollisions` auto-flip (the two footer instances open upward automatically), matching this project's Select/Popover convention.

    Menu items in locked order: (1) `<DropdownMenuItem asChild><Link href="/profile">Perfil</Link></DropdownMenuItem>` — no icon; `/profile` is an existing route, reuse as-is. (2) `<DropdownMenuItem asChild><Link href="/settings"><Settings className="mr-2 h-4 w-4" />Configurações</Link></DropdownMenuItem>` — reuse the already-imported `Settings` lucide icon and exact copy "Configurações". (3) `<DropdownMenuSeparator />`. (4) `<DropdownMenuItem variant="default" onSelect={onLogout}><LogOut className="mr-2 h-4 w-4" />Terminar sessão</DropdownMenuItem>` — reuse `LogOut` lucide icon and exact copy "Terminar sessão". Use `variant="default"` explicitly, NOT `"destructive"` (logout is a normal reversible action; `--destructive` is reserved for irreversible/data-destructive actions per the Color contract).

    Imports: `Link` from next/link; `Settings`, `LogOut` from lucide-react; `DropdownMenu`, `DropdownMenuContent`, `DropdownMenuItem`, `DropdownMenuSeparator`, `DropdownMenuTrigger` from @/components/ui/dropdown-menu; the `me` type via `web/src/types/auth` or `ReturnType<typeof useMe>["data"]` (executor discretion). This is a pure presentational component — no async, no try/catch of its own; `onLogout` already owns its flow.
  </action>
  <verify>
    <automated>cd web && grep -q "DropdownMenuTrigger" src/components/shared/user-menu.tsx && grep -q 'href="/profile"' src/components/shared/user-menu.tsx && grep -q "Terminar sessão" src/components/shared/user-menu.tsx && grep -q "onSelect={onLogout}" src/components/shared/user-menu.tsx && grep -q 'variant="default"' src/components/shared/user-menu.tsx && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `web/src/components/shared/user-menu.tsx` exists, starts with `"use client"`, and exports a `UserMenu` component.
    - Renders one `DropdownMenu` > `DropdownMenuTrigger asChild` > single non-navigating trigger (`<button type="button">` or `<div role="button" tabIndex={0}>`) containing the avatar+name/role JSX; no `<Link href="/settings">` remains inside the trigger.
    - `DropdownMenuContent` has `className="w-56"` and `align={variant === "topbar" ? "end" : "start"}`, and no `side` prop.
    - Menu items appear in exactly this order: Perfil (→ `/profile`, no icon), Configurações (→ `/settings`, `Settings` icon `mr-2 h-4 w-4`), `DropdownMenuSeparator`, Terminar sessão (`LogOut` icon `mr-2 h-4 w-4`, `variant="default"`, `onSelect={onLogout}`).
    - `variant="topbar"` renders the circular gradient avatar with right-aligned name/role (`hidden sm:block`); `variant="sidebar"` renders the square `rounded-md bg-slate-800` avatar with left-aligned name/role.
    - Initials fallback and `avatar_url` img rendering preserved verbatim from the source blocks.
    - grep gate in `<verify>` prints `OK`.
  </acceptance_criteria>
  <done>New shared UserMenu component exists, wraps a non-navigating trigger as `DropdownMenuTrigger asChild`, and exposes Perfil/Configurações/separator/Terminar sessão with logout wired to the onLogout prop.</done>
</task>

<task type="auto">
  <name>Task 2: Wire UserMenu into dashboard-shell.tsx at all 3 call sites + remove dead code</name>
  <read_first>
    - web/src/components/shared/dashboard-shell.tsx (full file — the 3 blocks to replace: topbar 294-311, sidebar footer 135-164, Sheet footer 220-249; imports lines 3-33; onLogout 71-79)
    - web/src/components/shared/user-menu.tsx (the component created in Task 1 — its exact prop names)
    - .planning/phases/LEXCV-109-notifica-es-settings-setup-wizard/109-PATTERNS.md (section "dashboard-shell.tsx" — replacement pattern per call site) and 109-UI-SPEC.md ("Verified-against-source finding 1")
  </read_first>
  <files>web/src/components/shared/dashboard-shell.tsx</files>
  <action>
    Replace all 3 inline user blocks with `<UserMenu>` (import it from `@/components/shared/user-menu`). (a) Topbar: replace the `<div className="flex items-center gap-3 pl-1">…</div>` block (currently lines 294-311, inside the header's `ml-auto` row) with `<UserMenu variant="topbar" me={me.data} onLogout={onLogout} />`. (b) Desktop sidebar footer (currently 135-164): keep the outer decorative wrapper `<div className="flex items-center gap-3 rounded-lg bg-slate-900/50 dark:bg-slate-900/30 px-3 py-3 border border-slate-800/50">` and make its sole child `<UserMenu variant="sidebar" me={me.data} onLogout={onLogout} />`; remove the standalone `Tooltip`+`Button` "Terminar sessão" control (lines 155-162) — its logout behavior now lives inside the menu. (c) Mobile Sheet footer (currently 220-249): identical treatment to (b) — keep the outer wrapper, single `<UserMenu variant="sidebar" ... />` child, remove the Tooltip+Button logout (lines 240-247).

    Then remove imports that become unused after this consolidation. Verify each is truly unused by grepping the file first, but the expected removals are: `Button` (from @/components/ui/button — its only 2 uses were the removed logout buttons; the header hamburger is a native `<button>`, not this component), `Tooltip`/`TooltipContent`/`TooltipTrigger` (only used at the 2 removed logout sites), and `LogOut` (from lucide-react — only used at the 2 removed logout buttons; it now lives in user-menu.tsx instead). Do NOT remove `Settings` (still used by the "Configurações" links in the "Sistema" nav sections), `Link`, `cn`, `useMe`, or any nav icons. Leave the `NAV.filter(...)` / `hasPermission(me.data?.permissions, ...)` logic and the `onLogout` function untouched — do NOT add any loading/isFetched guard (explicitly deferred/out of scope).
  </action>
  <verify>
    <automated>cd web && test "$(grep -c '<UserMenu' src/components/shared/dashboard-shell.tsx)" = "3" && [ "$(grep -vE '^\s*(//|\*|/\*)' src/components/shared/dashboard-shell.tsx | grep -c 'aria-label=\"Terminar sessão\"')" = "0" ] && pnpm lint 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `<UserMenu` appears exactly 3 times in dashboard-shell.tsx: `variant="topbar"` once (header), `variant="sidebar"` twice (desktop `<aside>` footer + mobile `<SheetContent>` footer).
    - The sidebar/Sheet footers retain the outer `rounded-lg bg-slate-900/50 … border` wrapper; the standalone Tooltip+Button logout controls are gone (no `aria-label="Terminar sessão"` remains outside comments).
    - Now-unused imports removed: `Button`, `Tooltip`/`TooltipContent`/`TooltipTrigger`, `LogOut`. `Settings`, `Link`, `cn`, `useMe`, `hasPermission`, nav icons remain.
    - `onLogout` (lines 71-79) and the `NAV.filter(...hasPermission...)` nav-filtering logic are unchanged; no isFetched/loading guard added.
    - `pnpm lint` reports no new unused-variable or other errors introduced in dashboard-shell.tsx.
  </acceptance_criteria>
  <done>All 3 user blocks render the shared UserMenu; the two inline logout buttons and their now-orphaned imports are removed; lint is clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authenticated browser → session/auth | The user menu (incl. logout) renders only inside `DashboardShell`, which already redirects unauthenticated users to `/login` (dashboard-shell.tsx:60-65). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-109-01 | Elevation of Privilege | UserMenu items (Perfil/Configurações/Terminar sessão) | accept | Menu exposes only the current user's own navigation + logout; no privileged/tenant-crossing action. Rendered exclusively within the already-authenticated DashboardShell. No new authorization surface introduced. |
| T-109-02 | Tampering | onLogout handler | accept | `onLogout` (clearTokens + redirect to `/login`) is passed through byte-for-byte unchanged from DashboardShell into a `DropdownMenuItem onSelect`; no new code path, idempotent, non-destructive. |
| T-109-SC | Tampering | npm/radix installs | accept | Zero new dependencies this phase — `DropdownMenu` and its `radix-ui` backing were vetted and installed in Phase 101's package-legitimacy gate; this plan is only its first consumer. No `shadcn add`/`pnpm add` runs. |
</threat_model>

<verification>
- `web/src/components/shared/user-menu.tsx` exists and exports `UserMenu` with topbar + sidebar variants and the locked item order.
- `<UserMenu` is rendered exactly 3× in dashboard-shell.tsx; both inline logout buttons and their orphaned imports are removed.
- `pnpm lint` is clean (authoritative typecheck/build gate runs in Plan 109-03).
</verification>

<success_criteria>
NTF-28 satisfied: the topbar/sidebar/Sheet user menu is a single shared `DropdownMenu` (Perfil / Configurações / separator / Terminar sessão), no longer navigating directly to `/settings` on click, with logout folded into the menu. The notification bell's `Popover` is untouched.
</success_criteria>

<output>
Create `.planning/phases/LEXCV-109-notifica-es-settings-setup-wizard/109-01-SUMMARY.md` when done.
</output>
