---
phase: 106-m-dulo-agenda
plan: 03
type: execute
wave: 2
depends_on: [106-01]
files_modified:
  - web/src/app/(dashboard)/agenda/[id]/editar/page.tsx
autonomous: true
requirements: [AGD-36]
must_haves:
  truths:
    - "agenda/[id]/editar access-denied gate (line 65) uses permissions.isFetched && !canEditAgenda"
    - "The 3 RHF-bound <select> (processoId, tipo, prioridade) are NativeSelect size=default className='w-full'; the const selectClassName is deleted and const textareaClassName is kept (used by the descricao textarea)"
    - "dataInicio and dataFim use the shared DatePickerField withTime via Controller; NO recurrence field is added (editar has no recurrence UI today — deferred)"
    - "No <Input type='datetime-local'> remains; the toDateTimeLocalValue helper still feeds field.value on load and the form still submits the strings eventoFormSchema accepts unchanged"
  artifacts:
    - path: "web/src/app/(dashboard)/agenda/[id]/editar/page.tsx"
      provides: "Edit-Evento form on NativeSelect + DatePickerField (AGD-36)"
      contains: "DatePickerField"
  key_links:
    - from: "web/src/app/(dashboard)/agenda/[id]/editar/page.tsx"
      to: "@/components/shared/date-picker-field (DatePickerField)"
      via: "Controller control={form.control} wraps DatePickerField for dataInicio/dataFim"
      pattern: "Controller"
---

<objective>
Migrate the edit-Evento form (`agenda/[id]/editar/page.tsx`) to `NativeSelect` for its 3 RHF-bound selects and to the shared `DatePickerField` for its 2 date+time fields (`dataInicio`/`dataFim`), and apply the bundled RBAC `isFetched` fix. This form has no recurrence UI today and MUST NOT gain one (deferred).

Purpose: Close the edit-form share of AGD-36, delivered together with the create form (106-02) so the two Agenda forms never diverge visually.
Output: 1 file migrated; `pnpm build` green. Runs in Wave 2, parallel with 106-02 (different file).
</objective>

<execution_context>
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/LEXCV-106-m-dulo-agenda/106-CONTEXT.md
@.planning/phases/LEXCV-106-m-dulo-agenda/106-PATTERNS.md
@.planning/phases/LEXCV-106-m-dulo-agenda/106-UI-SPEC.md
@web/src/app/(dashboard)/agenda/[id]/editar/page.tsx
@web/src/components/ui/native-select.tsx
@web/src/schemas/eventos.ts

<interfaces>
<!-- Contracts the executor needs; DatePickerField created in 106-01, use directly. -->
- DatePickerField from "@/components/shared/date-picker-field" (created in 106-01): props { value: string | undefined; onChange: (v: string) => void; withTime?: boolean }. For datetime it emits/consumes the YYYY-MM-DDTHH:mm shape.
- NativeSelect from "@/components/ui/native-select": wrapper defaults to w-fit — ALWAYS pass className="w-full" on these RHF fields (106-CONTEXT.md Established Patterns). register()-compatible.
- Controller from react-hook-form: wire DatePickerField (renders Button+Calendar, not a native input) with Controller, not register.
- toDateTimeLocalValue (already in this file, lines 37-45): produces YYYY-MM-DDTHH:mm — exactly the shape DatePickerField's value prop expects. It already feeds form.reset (lines 100-114) on load; leave it unchanged.
- eventoFormSchema (web/src/schemas/eventos.ts): validates dataInicio/dataFim as Date-parseable with dataFim >= dataInicio. No schema change needed.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: RBAC fix + 3 NativeSelect + drop selectClassName</name>
  <files>web/src/app/(dashboard)/agenda/[id]/editar/page.tsx</files>
  <read_first>
    - 106-PATTERNS.md Shared Pattern 1 (RBAC, editar line 65 — and the warning NOT to touch line 287's submit-button `permissions.isLoading` guard) and Shared Pattern 2 (NativeSelect before/after, the 3 occurrences table for editar, selectClassName elimination, keep textareaClassName).
    - 106-UI-SPEC.md "Component Inventory > NativeSelect" (fields list: processoId/tipo/prioridade in editar; size=default).
    - agenda/[id]/editar/page.tsx: line 65 (RBAC gate), line 25 (const selectClassName), line 28 (const textareaClassName), the 3 selects at lines 182-196 (processoId, note disabled={processos.isLoading || processos.isError}), 209-220 (tipo), 237-241 (prioridade).
  </read_first>
  <action>
    Add `import { NativeSelect } from "@/components/ui/native-select";`. Change the access gate at line 65 from `if (!permissions.isLoading && !canEditAgenda) {` to `if (permissions.isFetched && !canEditAgenda) {` — do NOT touch line 287's `disabled={... || permissions.isLoading || ...}` submit-button guard. Replace each of the 3 `<select ... className={selectClassName} {...form.register("X")}>` with `<NativeSelect id="X" size="default" className="w-full" {...form.register("X")}>`, keeping every `<option>` child verbatim (including the dynamic processoId placeholder and its `disabled={processos.isLoading || processos.isError}` prop). DELETE `const selectClassName` at line 25 once all 3 migrate; KEEP `const textareaClassName` at line 28 (still used by the descricao `<textarea>` at line 266).
  </action>
  <verify>
    <automated>cd web && grep -c "<select" "src/app/(dashboard)/agenda/[id]/editar/page.tsx" | grep -qx 0 && grep -c "const selectClassName" "src/app/(dashboard)/agenda/[id]/editar/page.tsx" | grep -qx 0 && grep -q "const textareaClassName" "src/app/(dashboard)/agenda/[id]/editar/page.tsx" && grep -q "permissions.isFetched && !canEditAgenda" "src/app/(dashboard)/agenda/[id]/editar/page.tsx" && grep -q "NativeSelect" "src/app/(dashboard)/agenda/[id]/editar/page.tsx" && echo GATE_PASS</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "<select"` returns 0; all 3 form selects are `<NativeSelect ... className="w-full">`.
    - `const selectClassName` deleted; `const textareaClassName` retained.
    - Access gate reads `permissions.isFetched && !canEditAgenda`; the submit-button `permissions.isLoading` guard is untouched.
  </acceptance_criteria>
  <done>The 3 RHF selects are NativeSelect (w-full), selectClassName removed, RBAC race fixed.</done>
</task>

<task type="auto">
  <name>Task 2: dataInicio/dataFim → DatePickerField via Controller (no recurrence field)</name>
  <files>web/src/app/(dashboard)/agenda/[id]/editar/page.tsx</files>
  <read_first>
    - 106-PATTERNS.md Shared Pattern 4 (call-sites table: dataInicio/dataFim withTime=true in editar; recurrenceEndDate is NOT in editar — do not add it) and 4(c) (Controller idiom). Also the note that toDateTimeLocalValue (lines 37-45) already produces the value shape DatePickerField expects.
    - 106-UI-SPEC.md "AGD-36" date+time variant + Scope note #1 (editar has no recurrence UI; building one is deferred — do not add).
    - agenda/[id]/editar/page.tsx: line 7 (`import { useForm, type FieldErrors } from "react-hook-form"`), lines 247-253 (dataInicio Input), 255-261 (dataFim Input), the form.reset (lines 100-114) and onSubmit (lines 116-137).
  </read_first>
  <action>
    Upgrade the import at line 7 to `import { Controller, useForm, type FieldErrors } from "react-hook-form";` and add `import { DatePickerField } from "@/components/shared/date-picker-field";`. Replace the `dataInicio` `<Input id="dataInicio" type="datetime-local" {...form.register("dataInicio")} />` with `<Controller control={form.control} name="dataInicio" render={({ field }) => (<DatePickerField value={field.value} onChange={field.onChange} withTime />)} />`, keeping the surrounding `<Label>`/error `<p>` block and the `grid gap-4 sm:grid-cols-3` slot unchanged. Do the same for `dataFim` (withTime). Do NOT add a recurrenceRule select or a recurrenceEndDate field — this form intentionally has none (deferred per 106-CONTEXT.md / UI-SPEC Scope note #1). Do NOT change the Input import (still used by the titulo field), the toDateTimeLocalValue helper, form.reset, or onSubmit.
  </action>
  <verify>
    <automated>cd web && grep -c "datetime-local" "src/app/(dashboard)/agenda/[id]/editar/page.tsx" | grep -qx 0 && grep -q "DatePickerField" "src/app/(dashboard)/agenda/[id]/editar/page.tsx" && grep -q "Controller" "src/app/(dashboard)/agenda/[id]/editar/page.tsx" && grep -c "recurrenceEndDate" "src/app/(dashboard)/agenda/[id]/editar/page.tsx" | grep -qx 0 && pnpm build && echo GATE_PASS</automated>
  </verify>
  <acceptance_criteria>
    - No `datetime-local` Input remains; both `dataInicio`/`dataFim` render `DatePickerField` (withTime) via `Controller`.
    - No `recurrenceEndDate` reference is added to the editar form (grep count stays 0).
    - `pnpm build` completes with no new type errors.
  </acceptance_criteria>
  <done>Both edit-form date fields use the shared Calendar-backed DatePickerField; no recurrence UI introduced; `pnpm build` green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → Next proxy → Spring API | Unchanged. The update-Evento PUT payload shape and route are identical; only the input widgets change. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-106-03-T | Tampering | Date strings emitted by DatePickerField into RHF | mitigate | eventoFormSchema still validates dataInicio/dataFim as Date-parseable with dataFim >= dataInicio; the DatePickerField deselect guard (106-01) prevents a stray click from nulling a required field; toDateTimeLocalValue round-trips the loaded value unchanged. |
| T-106-03-E | Elevation of Privilege | Client RBAC gate (`permissions.isFetched && !canEditAgenda`) | accept | Deny branch preserved (AccessDeniedState); server-side update is gated by backend `@PreAuthorize("hasAuthority('agenda:edit')")` — unchanged. |
| T-106-SC | Tampering | npm/pip/cargo installs | accept | Zero installs — DatePickerField built from Phase-101 primitives; date-fns already installed. |
</threat_model>

<verification>
- `cd web && pnpm build` passes.
- `cd web && pnpm lint` reports no new errors.
- Task grep gates pass (no native `<select>`, no datetime-local Input, DatePickerField + Controller present, no recurrenceEndDate added, RBAC isFetched).
</verification>

<success_criteria>
- AGD-36 (edit form): dataInicio/dataFim (with time) use the Calendar-backed DatePickerField; no recurrence UI added.
- 3 RHF selects on NativeSelect (w-full), selectClassName removed, textareaClassName kept.
- RBAC race fixed at line 65; `pnpm build` green.
</success_criteria>

<output>
Create `.planning/phases/LEXCV-106-m-dulo-agenda/106-03-SUMMARY.md` when done.
</output>
