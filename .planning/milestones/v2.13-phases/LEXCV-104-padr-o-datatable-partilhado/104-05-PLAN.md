---
phase: 104-padr-o-datatable-partilhado
plan: 05
type: execute
wave: 3
depends_on: [104-02]
files_modified:
  - web/src/app/(dashboard)/documentos/columns.tsx
  - web/src/app/(dashboard)/documentos/page.tsx
  - web/src/app/(dashboard)/notificacoes/page.tsx
autonomous: true
requirements: [DTB-02, DTB-03]
must_haves:
  truths:
    - "Documentos desktop list renders via <DataTable> through the reconciled Table primitives — its raw hand-written <table> is gone entirely (first-ever Table adoption for this screen)"
    - "Documentos' Confidencialidade column renders a real Badge with a newly-invented closed-set mapping (never a hand-rolled span); Tipo keeps its existing blue Badge"
    - "Documentos' Apagar action stays gated by canEditDocumentos with its window.confirm() guard preserved 1:1"
    - "/notificacoes' hand-rolled Anterior/Seguinte button-pair pager is replaced by the official shadcn Pagination primitive, with identical shape (no numbered links), identical copy, identical disabled-at-bounds logic, and the unchanged useNotificacoes({page,size}) server-pagination contract"
    - "Documentos' mobile card branch, filters, and outer guard, and /notificacoes' filters/list/mutations are otherwise unchanged"
  artifacts:
    - path: "web/src/app/(dashboard)/documentos/columns.tsx"
      provides: "ColumnDef<Documento>[] porting DocumentoRow cells; Confidencialidade→Badge mapping invented from the closed set; Ver. not sortable"
  key_links:
    - from: "web/src/app/(dashboard)/documentos/page.tsx"
      to: "DataTable through Table primitives"
      via: "raw <table>/<tr>/<td> replaced with <DataTable>; no raw table markup remains"
      pattern: "DataTable"
    - from: "web/src/app/(dashboard)/notificacoes/page.tsx"
      to: "web/src/components/ui/pagination.tsx"
      via: "official Pagination replaces the hand-rolled Button pair; client-side setPage via onClick (not Link)"
      pattern: "Pagination"
---

<objective>
Migrate the desktop list branch of Documentos to the shared `<DataTable>` (its first-ever `Table`-primitive adoption, off a raw `<table>`), and swap `/notificacoes`' hand-rolled pager for the official shadcn `Pagination` primitive added in 104-02. Covers DTB-02 for Documentos and closes DTB-03 (whose full scope is `/notificacoes` alone — verified the only genuinely server-paginated list in the app).

Purpose: Bring Documentos onto the semantic Table primitive + closed Badge vocabulary, and make `/notificacoes` use the official Pagination component for accessibility/consistency — a component swap only, not a UX redesign of an already-correct interaction.
Output: `documentos/columns.tsx`, the Documentos `page.tsx` desktop branch on `<DataTable>`, and `/notificacoes` on official `Pagination`.
</objective>

<execution_context>
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/LEXCV-104-padr-o-datatable-partilhado/104-PATTERNS.md
@.planning/phases/LEXCV-104-padr-o-datatable-partilhado/104-UI-SPEC.md
@web/AGENTS.md

<interfaces>
Shared pattern to consume (built in 104-02, do NOT modify):
  web/src/components/shared/data-table/data-table.tsx           → DataTable<TData,TValue>({ columns, data }) — renders through Table primitives
  web/src/components/shared/data-table/data-table-column-header.tsx → DataTableColumnHeader({ column, title })
  web/src/components/ui/pagination.tsx (added in 104-02)        → Pagination, PaginationContent, PaginationItem, PaginationPrevious, PaginationNext (+ PaginationLink)

Badge variant vocabulary (closed set — web/src/components/ui/badge.tsx):
  "default" | "secondary" | "outline" | "blue" | "green" | "amber" | "red" | "purple" | "gray"

Verified correction (104-PATTERNS.md): Documentos' Confidencialidade is PLAIN TEXT today in BOTH mobile and desktop branches — no existing confidencialidade→variant mapping exists anywhere. A reasonable closed-set mapping must be INVENTED for this column. The `confidencialidade` enum is a fixed 4-value set — `PUBLICO | INTERNO | CONFIDENCIAL | RESTRITO` — verified against web/src/schemas/documentos.ts:26 and the upload form options (documentos/novo/page.tsx). Tipo already uses Badge variant="blue" in the mobile card — carry that forward unchanged.

Next.js 16 caveat (web/AGENTS.md): shadcn's PaginationPrevious/PaginationNext render an <a> by default. This app drives pagination with client-side setPage state, NOT URL routing — so render them with onClick (button-style, asChild={false} or a <button> wrapper), never a next/link <Link>. Verify the exact prop shape against the CLI-generated pagination.tsx.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Documentos — first Table adoption + DataTable + Confidencialidade Badge</name>
  <files>web/src/app/(dashboard)/documentos/columns.tsx, web/src/app/(dashboard)/documentos/page.tsx</files>
  <read_first>
    - web/src/app/(dashboard)/documentos/page.tsx (FULL file — raw `<table>` desktop branch ~lines 138-193, DocumentoRow ~lines 277-344, tipo Badge in mobile card ~lines 236-240, plain-text confidencialidade ~line 331, Apagar action gated by canEditDocumentos with window.confirm ~lines 335-341, outer guard ~lines 126-136)
    - web/src/schemas/documentos.ts (line 26 — the authoritative 4-value `confidencialidade` enum: PUBLICO, INTERNO, CONFIDENCIAL, RESTRITO)
    - .planning/phases/LEXCV-104-padr-o-datatable-partilhado/104-PATTERNS.md (documentos/columns.tsx section — cell list, the "invent a confidencialidade mapping" correction, Ver. not sortable, carry tipo→blue forward)
    - .planning/phases/LEXCV-104-padr-o-datatable-partilhado/104-UI-SPEC.md (Scope note #1 + Per-Screen Adoption Contract row for Documentos)
    - web/src/components/shared/data-table/data-table.tsx, data-table-column-header.tsx; web/src/components/ui/table.tsx; web/src/components/ui/badge.tsx
  </read_first>
  <action>
    Create `documentos/columns.tsx` porting `DocumentoRow`'s cell CONTENT into `ColumnDef<Documento>[]` (raw `<tr>`/`<td>` today — no Table markup to lift): Nome (link; sortable, `enableHiding:false`), Tipo (carry the existing mobile-card `Badge variant="blue"` forward unchanged; sortable), Processo (id/link; sortable), Cliente (id/link; sortable), Confidencialidade (currently plain text — render a real `Badge` using this EXACT, EXHAUSTIVE closed-set mapping over ALL FOUR enum values, ordered by increasing sensitivity and drawn from the badge.tsx vocabulary — `PUBLICO`→"gray" (least sensitive, neutral), `INTERNO`→"blue" (internal-only, informational), `CONFIDENCIAL`→"amber" (caution), `RESTRITO`→"red" (most sensitive, highest restriction). Pick exactly ONE definitive variant per value — no either/or, no "or amber" alternatives — and handle every one of the 4 literals; never a hand-rolled span; sortable. Reuse Tipo's `variant="blue"` for INTERNO is intentional and fine — they are distinct columns), Ver. (`v{versao ?? 1}`; `enableSorting:false` — single incrementing int, low value), Tamanho (bytes; sortable, numeric), Criado (`new Date(...).toLocaleString("pt-CV")`; sortable, date), Ações (Apagar only, gated by `canEditDocumentos`, `window.confirm()` guard identical to Clientes' delete pattern; `enableSorting:false`, `enableHiding:false`). Wrap sortable headers with `DataTableColumnHeader`. In `page.tsx`, replace the ENTIRE raw `<table>...</table>` desktop branch with `<div className="hidden md:block"><DataTable columns={columns} data={rows} /></div>` (thread any needed handlers/permission flags through a `columns(...)` factory since column defs can't call hooks). Leave the mobile card branch, filter Card, `useDocumentos`/filters, download logic, and the outer guard untouched. Preserve the `canEditDocumentos` gate on the Apagar action exactly.
  </action>
  <verify>
    <automated>test -f 'web/src/app/(dashboard)/documentos/columns.tsx' && grep -q 'DataTable' 'web/src/app/(dashboard)/documentos/page.tsx' && ! grep -Eq '<table[ >]|<thead|getFilteredRowModel' 'web/src/app/(dashboard)/documentos/page.tsx' && grep -q 'Badge' 'web/src/app/(dashboard)/documentos/columns.tsx' && for V in PUBLICO INTERNO CONFIDENCIAL RESTRITO; do grep -Eq "\"$V\"" 'web/src/app/(dashboard)/documentos/columns.tsx' || { echo "BLOCKER: confidencialidade value $V is unhandled in columns.tsx" >&2; exit 1; }; done && grep -q 'canEditDocumentos' 'web/src/app/(dashboard)/documentos/columns.tsx' 'web/src/app/(dashboard)/documentos/page.tsx' && grep -q 'window.confirm' 'web/src/app/(dashboard)/documentos/columns.tsx' 'web/src/app/(dashboard)/documentos/page.tsx' && pnpm --dir web exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `documentos/columns.tsx` exists; `documentos/page.tsx` desktop branch renders `<DataTable>` and contains no raw `<table>`/`<thead>` markup.
    - Confidencialidade renders a real `Badge` with an EXHAUSTIVE, unambiguous mapping over all 4 enum values — `PUBLICO`→"gray", `INTERNO`→"blue", `CONFIDENCIAL`→"amber", `RESTRITO`→"red" (exactly one variant each, no either/or) — and Tipo keeps `variant="blue"`; no hand-rolled status span.
    - The verify loop fails the task if ANY of the 4 literals `PUBLICO`/`INTERNO`/`CONFIDENCIAL`/`RESTRITO` is left unhandled in `columns.tsx`.
    - The `canEditDocumentos` gate and `window.confirm()` guard on Apagar are preserved (grep finds both in the migrated code).
    - No `getFilteredRowModel`; mobile branch, filters, and download logic unchanged.
    - `pnpm --dir web exec tsc --noEmit` passes.
  </acceptance_criteria>
  <done>Documentos desktop list is DataTable-driven through the Table primitives, Confidencialidade renders an exhaustive 4-value closed Badge mapping (PUBLICO→gray, INTERNO→blue, CONFIDENCIAL→amber, RESTRITO→red) and Tipo keeps variant="blue", the Apagar RBAC gate is preserved, and the raw table is eliminated.</done>
</task>

<task type="auto">
  <name>Task 2: /notificacoes — swap the hand-rolled pager for the official Pagination primitive (DTB-03)</name>
  <files>web/src/app/(dashboard)/notificacoes/page.tsx</files>
  <read_first>
    - web/src/app/(dashboard)/notificacoes/page.tsx (FULL file — the hand-rolled Anterior/Seguinte Button-pair pager ~lines 228-250, the useNotificacoes({page, size:20}) data contract ~lines 55-60, the totalPages/page/setPage state)
    - web/src/components/ui/pagination.tsx (the CLI-generated primitive added in 104-02 — confirm the exact prop shape of PaginationPrevious/PaginationNext and whether they render <a>)
    - .planning/phases/LEXCV-104-padr-o-datatable-partilhado/104-PATTERNS.md (notificacoes/page.tsx section — the swap contract; the <a>-vs-onClick caveat) and 104-UI-SPEC.md (Pagination Contract, /notificacoes row)
    - web/AGENTS.md (Next.js 16 caveat)
  </read_first>
  <action>
    Replace ONLY the hand-rolled `Button`-pair pager block with the official `Pagination`/`PaginationContent`/`PaginationItem`/`PaginationPrevious`/`PaginationNext` from `web/src/components/ui/pagination.tsx`. Preserve the EXACT same minimal shape: no numbered `PaginationLink`s; keep the "Página {page + 1} de {list.data.totalPages}" text; keep the disabled-at-bounds logic (`page === 0` for Previous, `page + 1 >= totalPages` for Next); keep the unchanged `useNotificacoes({ page, size: 20 })` server-pagination contract and the `setPage` state. Because this app uses client-side `setPage` (not URL-driven routing) and shadcn's `PaginationPrevious`/`PaginationNext` render an `<a>` by default, wire them with an `onClick` handler (button-rendering, not a next/link `<Link>`) — verify the exact prop shape against the CLI-generated `pagination.tsx` at implementation time. Do NOT change the notifications list, filters, RBAC gate, or mark-as-read mutations. This is a component swap for accessibility/consistency only.
  </action>
  <verify>
    <automated>grep -q 'PaginationContent\|PaginationPrevious\|PaginationNext' 'web/src/app/(dashboard)/notificacoes/page.tsx' && grep -q 'from "@/components/ui/pagination"' 'web/src/app/(dashboard)/notificacoes/page.tsx' && ! grep -q 'PaginationLink' 'web/src/app/(dashboard)/notificacoes/page.tsx' && grep -q 'useNotificacoes' 'web/src/app/(dashboard)/notificacoes/page.tsx' && pnpm --dir web build</automated>
  </verify>
  <acceptance_criteria>
    - `/notificacoes` imports and renders `Pagination`/`PaginationContent`/`PaginationPrevious`/`PaginationNext` from `@/components/ui/pagination`; the hand-rolled Anterior/Seguinte Button-pair is gone.
    - No numbered `PaginationLink` is introduced; the "Página {n} de {total}" copy and disabled-at-bounds logic are preserved; Previous/Next use onClick (client-side setPage), not a `<Link>`.
    - `useNotificacoes({ page, size: 20 })` and the mark-as-read mutations/RBAC gate are unchanged.
    - `pnpm --dir web build` completes successfully (full build for Documentos first-Table adoption + notificacoes swap slice).
  </acceptance_criteria>
  <done>/notificacoes uses the official Pagination primitive with identical shape/copy/behavior over the unchanged server-pagination contract; DTB-03 closed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser (per-role) ← Documentos Apagar action | The delete affordance is a UI-layer expression of the canEditDocumentos backend authorization; a careless port could show it to a role that shouldn't have it |
| /notificacoes ← server-paginated, recipient-scoped data | The pager change must not alter which page/size is requested nor the recipient-scoped, tenant-scoped useNotificacoes contract |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-104-05-EP | Elevation of Privilege | Documentos Apagar action RBAC gate (canEditDocumentos) | mitigate | Port the `canEditDocumentos` gate + `window.confirm()` guard into columns.tsx 1:1; verified by grep for canEditDocumentos and window.confirm plus the build gate |
| T-104-05-TA | Tampering | /notificacoes pager swap | mitigate | Component swap only — the `useNotificacoes({page,size})` server-pagination contract, disabled-at-bounds logic, and mark-as-read mutations are untouched; no change to what data is requested |
| T-104-05-ID | Information Disclosure | Documentos client-side sort/paginate | accept | Data already fetched + server-filtered + tenant-scoped by useDocumentos; no getFilteredRowModel; row links reuse existing tenant-scoped ids |
</threat_model>

<verification>
- Documentos desktop branch renders `<DataTable>`; raw table gone; Confidencialidade renders the exhaustive 4-value Badge mapping (PUBLICO/INTERNO/CONFIDENCIAL/RESTRITO all handled) and Tipo uses variant="blue"; Apagar RBAC gate preserved.
- /notificacoes uses official Pagination (no numbered links); server-pagination contract + mutations unchanged.
- No getFilteredRowModel; `pnpm --dir web build` green.
</verification>

<success_criteria>
- Documentos desktop list uses the shared DataTable through the Table primitive (DTB-02); /notificacoes uses the official Pagination primitive (DTB-03) with zero UX/behavior change and zero RBAC regression.
</success_criteria>

<output>
Create `.planning/phases/LEXCV-104-padr-o-datatable-partilhado/104-05-SUMMARY.md` when done.
</output>
