---
phase: 104-padr-o-datatable-partilhado
plan: 06
type: execute
wave: 4
depends_on: [104-03, 104-04, 104-05]
files_modified: []
autonomous: false
requirements: [DTB-01, DTB-02, DTB-03]
must_haves:
  truths:
    - "A single holistic `pnpm build` of web/ passes with zero errors after all 5 screens + /notificacoes are migrated"
    - "No useReactTable instance anywhere in web/ configures getFilteredRowModel() — client-side re-filtering is proven absent repo-wide"
    - "No hand-rolled status-pill <span> survives on any migrated screen (Financeiro's statusBadgeClass fully removed); every status/type cell uses the closed Badge vocabulary"
    - "A human has visually confirmed sorting, pagination, and the column-visibility toolbar on all 5 desktop lists in BOTH light and dark themes, and confirmed mobile card views are unchanged"
    - "The human confirmed no RBAC row-action regression (delete/edit affordances match pre-migration behavior)"
  artifacts:
    - path: ".planning/phases/LEXCV-104-padr-o-datatable-partilhado/104-06-SUMMARY.md"
      provides: "Recorded automated-gate results + human visual sign-off (approved/issues) closing the phase"
  key_links:
    - from: "phase completion"
      to: "human visual sign-off"
      via: "blocking checkpoint per 104-CONTEXT.md's locked decision"
      pattern: "checkpoint"
---

<objective>
Close Phase 104 with the two gates 104-CONTEXT.md locked as mandatory: (1) an automated holistic build + regression-grep gate over the whole migrated surface, and (2) a mandatory human visual checkpoint verifying sorting, pagination, and the filter/column-visibility toolbar across all 5 desktop lists in both themes. This is a verification-only plan — it modifies no source files.

Purpose: Phases 101 and 103 learned that grep-only checks miss real bugs; this plan runs a single real `pnpm build` across the fully-migrated app and pairs it with human eyes on the interactive behavior (which no automated check in this repo covers — there are no visual/E2E tests). Covers the closing verification of DTB-01/02/03.
Output: Recorded gate results + human sign-off in 104-06-SUMMARY.md.
</objective>

<execution_context>
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/LEXCV-104-padr-o-datatable-partilhado/104-CONTEXT.md
@.planning/phases/LEXCV-104-padr-o-datatable-partilhado/104-UI-SPEC.md
@.planning/phases/LEXCV-104-padr-o-datatable-partilhado/104-03-SUMMARY.md
@.planning/phases/LEXCV-104-padr-o-datatable-partilhado/104-04-SUMMARY.md
@.planning/phases/LEXCV-104-padr-o-datatable-partilhado/104-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Holistic build + regression-grep gate over the whole migrated surface</name>
  <files>.planning/phases/LEXCV-104-padr-o-datatable-partilhado/104-06-SUMMARY.md</files>
  <read_first>
    - .planning/phases/LEXCV-104-padr-o-datatable-partilhado/104-03-SUMMARY.md, 104-04-SUMMARY.md, 104-05-SUMMARY.md (confirm all 5 screens + /notificacoes reported migrated)
    - .planning/phases/LEXCV-104-padr-o-datatable-partilhado/104-UI-SPEC.md (the invariants to assert: no getFilteredRowModel, no row-selection, closed Badge set, official Pagination on /notificacoes)
  </read_first>
  <action>
    Run one clean `pnpm --dir web build` after all wave-3 plans have merged, and run the repo-wide regression greps (see `<verify><automated>`) that assert the phase's hard invariants: (a) zero `getFilteredRowModel` anywhere under web/src (client-side re-filtering must not exist on ANY useReactTable instance); (b) zero `statusBadgeClass` remaining in the migrated financeiro files (the hand-rolled span is fully gone); (c) no `Checkbox`/row-selection import inside the shared data-table/ dir; (d) `/notificacoes` imports from `@/components/ui/pagination`. Record every command's result (pass/fail with output excerpts) in 104-06-SUMMARY.md. If the build fails or any invariant grep fails, STOP and report the failure rather than proceeding to the human checkpoint. Use filtered greps where a bare token could false-positive.
  </action>
  <verify>
    <automated>pnpm --dir web build && ! grep -rq 'getFilteredRowModel' web/src && ! grep -rq 'statusBadgeClass' web/src/app/'(dashboard)'/financeiro && ! grep -riq 'checkbox' web/src/components/shared/data-table && grep -rq 'from "@/components/ui/pagination"' web/src/app/'(dashboard)'/notificacoes/page.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --dir web build` completes with zero errors across the fully-migrated app.
    - `grep -r getFilteredRowModel web/src` returns nothing (no client-side re-filtering anywhere).
    - `grep -r statusBadgeClass web/src/app/(dashboard)/financeiro` returns nothing.
    - No `checkbox` (case-insensitive) under web/src/components/shared/data-table.
    - `/notificacoes/page.tsx` imports from `@/components/ui/pagination`.
    - All results recorded in 104-06-SUMMARY.md.
  </acceptance_criteria>
  <done>The holistic build passes and every phase invariant grep is green, all recorded in the SUMMARY; the phase is ready for the human visual checkpoint.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Mandatory human visual checkpoint — sorting / pagination / toolbar on all 5 screens, both themes</name>
  <files>.planning/phases/LEXCV-104-padr-o-datatable-partilhado/104-06-SUMMARY.md</files>
  <read_first>
    - .planning/phases/LEXCV-104-padr-o-datatable-partilhado/104-CONTEXT.md (the locked decision mandating this checkpoint at phase close)
    - .planning/phases/LEXCV-104-padr-o-datatable-partilhado/104-UI-SPEC.md (Per-Screen Adoption Contract, Typography 12px/600 headers, Color: sort icons never accent-blue, Pagination Contract copy)
  </read_first>
  <action>
    After Task 1's gate is green, start the dev server (`pnpm --dir web dev`) so the human can verify the five migrated lists + /notificacoes in a real browser, then present the per-screen / both-theme checklist in `<how-to-verify>` and PAUSE for the human verdict. This is a blocking human-verify checkpoint — never auto-approve (`workflow.auto_advance` is ignored). Record the human's concrete evidence (screens, themes, anomalies) and the approved/issues verdict in 104-06-SUMMARY.md; if issues are reported, enumerate them for a `/gsd:plan-phase 104 --gaps` closure plan rather than silently closing the phase.
  </action>
  <what-built>
    All 5 desktop lists (Clientes, Processos, Pareceres, Financeiro, Documentos) now render through the shared DataTable (sortable headers + real client-side pagination + column-visibility toggle), and /notificacoes uses the official Pagination primitive. Task 1 already proved the whole app builds and that the hard invariants (no client-side filtering, no leftover hand-rolled status spans, no row-selection) hold. This checkpoint verifies the interactive behavior and visual result that no automated test in this repo covers.
  </what-built>
  <how-to-verify>
    Run `pnpm --dir web dev` and, in a real browser, for EACH of the 5 desktop lists at a desktop width (≥ md), in BOTH light and dark themes:
    1. Sorting: click a sortable column header — rows reorder; the sort icon cycles ChevronsUpDown → ArrowUp → ArrowDown; confirm the icon renders in muted/foreground color, NOT accent blue.
    2. Pagination: with enough rows, confirm "Anterior"/"Seguinte" actually page (not the old dead buttons), "Página n de total" updates, and the "Linhas por página" Select (10/20/50) changes page size.
    3. Column visibility: open the toolbar's column toggle (SlidersHorizontal icon, tooltip "Colunas visíveis") and hide/show a column; confirm the primary identity column and Ações cannot be hidden.
    4. Headers: confirm column headers are 12px/600 uppercase muted (consistent across all 5), not the old 10px look.
    5. Badges: confirm Financeiro's Estado now shows real Badges (Pago→green, Parcialmente Pago→blue, else→amber) and Documentos' Confidencialidade shows a Badge.
    6. Mobile: shrink below md — confirm the card views are unchanged on all 5 screens (no table appears).
    7. /notificacoes: confirm Previous/Next still page the server-side history exactly as before (no numbered links, same copy).
    8. RBAC: confirm per-row actions (delete/edit) appear exactly as they did before the migration for your role.
    Record concrete evidence (which screens, which theme, any anomalies) in 104-06-SUMMARY.md.
  </how-to-verify>
  <verify>
    <automated>pnpm --dir web build</automated>
    <human-check>Sorting, pagination, column-visibility toolbar, 12px/600 headers, Financeiro/Documentos Badge migrations, unchanged mobile cards, /notificacoes Pagination swap, and per-row RBAC actions all confirmed on Clientes/Processos/Pareceres/Financeiro/Documentos in BOTH light and dark themes.</human-check>
  </verify>
  <resume-signal>Type "approved" (with the screens/themes you verified) to close Phase 104, or list the specific screens/behaviors that need a gap-closure fix.</resume-signal>
  <done>A human has recorded, in 104-06-SUMMARY.md, an explicit approved verdict confirming sorting, pagination, column-visibility, header typography, Badge migrations, unchanged mobile cards, the /notificacoes Pagination swap, and no RBAC regression across all 5 screens in both themes — or listed concrete issues for a gap-closure plan.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| release ← human visual + build sign-off | The phase ships to a production legal-practice app with no automated visual/E2E coverage; the human checkpoint is the last gate against a silent RBAC or dual-view regression |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-104-06-EP | Elevation of Privilege | per-row RBAC action affordances across all 5 migrated screens | mitigate | Human checkpoint step 8 explicitly re-verifies delete/edit affordances match pre-migration behavior for the tester's role; any mismatch blocks close |
| T-104-06-ID | Information Disclosure | client-side re-filtering regression | mitigate | Task 1's repo-wide `getFilteredRowModel` grep proves no useReactTable instance re-filters the server-authorized data client-side |
| T-104-06-SC | Tampering | supply-chain (recap) | accept | `@tanstack/react-table` legitimacy already gated in 104-01; no new package is added in this plan |
</threat_model>

<verification>
- Holistic `pnpm --dir web build` green; all invariant greps pass and are recorded.
- Human visual sign-off recorded for sorting/pagination/toolbar/headers/badges/mobile/notificacoes/RBAC across all 5 screens in both themes.
</verification>

<success_criteria>
- The fully-migrated app builds clean, contains zero client-side re-filtering, and has an explicit human visual approval closing DTB-01/02/03 — or a concrete issue list for a gap-closure plan.
</success_criteria>

<output>
Create `.planning/phases/LEXCV-104-padr-o-datatable-partilhado/104-06-SUMMARY.md` when done, recording both the automated-gate results and the human visual verdict.
</output>
