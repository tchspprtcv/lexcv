---
phase: 104-padr-o-datatable-partilhado
plan: 04
type: execute
wave: 3
depends_on: [104-02]
files_modified:
  - web/src/app/(dashboard)/pareceres/columns.tsx
  - web/src/app/(dashboard)/pareceres/page.tsx
  - web/src/app/(dashboard)/financeiro/columns.tsx
  - web/src/app/(dashboard)/financeiro/page.tsx
autonomous: true
requirements: [DTB-02]
must_haves:
  truths:
    - "Pareceres desktop list (hidden md:block) renders via <DataTable> with sortable Estado/Cliente/Prioridade/Prazo/Criado headers and functional pagination"
    - "Financeiro desktop list renders via <DataTable> through the reconciled Table primitives — its raw hand-written <table> is gone entirely (first-ever Table adoption for this screen)"
    - "Financeiro's hand-rolled statusBadgeClass <span> is replaced by the real Badge component using the mapping already implied by its own mobile card branch (Pago→green, Parcialmente Pago→blue, else→amber)"
    - "Both screens' mobile card branches, filter/toolbar Cards, use-* hooks, filters state, CSV export, and outer guards are unchanged"
    - "Pareceres' dual data source (usePareceres / usePesquisarPareceres) still selects the active rows array — only what feeds DataTable's data prop, not the column defs"
  artifacts:
    - path: "web/src/app/(dashboard)/pareceres/columns.tsx"
      provides: "columns(clienteNomeById) factory porting the Pareceres row block + statusVariant/formatDate helpers"
    - path: "web/src/app/(dashboard)/financeiro/columns.tsx"
      provides: "columns(processoById, clienteNomeById) factory; Estado cell uses real Badge (green/blue/amber), never statusBadgeClass"
  key_links:
    - from: "web/src/app/(dashboard)/financeiro/page.tsx"
      to: "DataTable through Table primitives"
      via: "raw <table>/<thead>/<tbody> replaced with <DataTable>; no raw table markup remains"
      pattern: "DataTable"
    - from: "web/src/app/(dashboard)/pareceres/page.tsx"
      to: "columns(clienteNomeById)"
      via: "active rows array (search-toggled) passed as data prop"
      pattern: "DataTable"
---

<objective>
Migrate the desktop list branch of Pareceres and Financeiro to the shared `<DataTable>`. Pareceres already uses the reconciled `Table`; Financeiro does NOT — its desktop branch is a raw hand-written `<table>` today, so for Financeiro this is simultaneously its first `Table`-primitive adoption AND its DataTable adoption, plus a mandatory Badge migration off its hand-rolled `statusBadgeClass` span. Covers DTB-02 for these two screens.

Purpose: Bring Financeiro onto the semantic Table primitive and the closed Badge vocabulary as a natural side effect of the DataTable rewrite (the same way Phase 102 normalized Card dark-mode tokens), while giving both screens real sorting + pagination without duplicating server-side filtering.
Output: `pareceres/columns.tsx`, `financeiro/columns.tsx`, and the two `page.tsx` desktop branches rewired to `<DataTable>`.
</objective>

<execution_context>
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/LEXCV-104-padr-o-datatable-partilhado/104-PATTERNS.md
@.planning/phases/LEXCV-104-padr-o-datatable-partilhado/104-UI-SPEC.md
@web/AGENTS.md

<interfaces>
Shared pattern to consume (built in 104-02, do NOT modify):
  web/src/components/shared/data-table/data-table.tsx           → DataTable<TData,TValue>({ columns, data }) — renders through Table primitives
  web/src/components/shared/data-table/data-table-column-header.tsx → DataTableColumnHeader({ column, title })

Badge variant vocabulary (closed set — web/src/components/ui/badge.tsx):
  "default" | "secondary" | "outline" | "blue" | "green" | "amber" | "red" | "purple" | "gray"

Cross-hook lookup Maps (clienteNomeById, processoById) MUST be passed into columns(...) as factory arguments — column defs are plain objects and cannot call hooks.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pareceres — extract columns.tsx and rewire the desktop branch to DataTable</name>
  <files>web/src/app/(dashboard)/pareceres/columns.tsx, web/src/app/(dashboard)/pareceres/page.tsx</files>
  <read_first>
    - web/src/app/(dashboard)/pareceres/page.tsx (FULL file — desktop Table branch ~lines 438-486, row block ~lines 450-483, module-scope statusVariant/formatDate helpers ~lines 24-41, clienteNomeById Map ~lines 84-87, dual data source usePareceres/usePesquisarPareceres toggle ~lines 89-95, Ações cell ~lines 469-480)
    - .planning/phases/LEXCV-104-padr-o-datatable-partilhado/104-PATTERNS.md (pareceres/columns.tsx section — statusVariant mapping, clienteNomeById factory-arg, dual-source note)
    - web/src/components/shared/data-table/data-table.tsx, data-table-column-header.tsx; web/src/components/ui/badge.tsx
  </read_first>
  <action>
    Create `pareceres/columns.tsx` as a `columns(clienteNomeById)` factory porting the row block into `ColumnDef<Parecer>[]`: Estado (`Badge variant={statusVariant(s.status)}`: PENDENTE→"gray", EM_ELABORACAO→"blue", EM_REVISAO→"amber", CONCLUIDO→"green", else→"secondary" — port the helper verbatim or import it; sortable, `enableHiding:false`), Cliente (resolved via `clienteNomeById`; sortable), Prioridade (plain formatted text; sortable), Prazo (formatDate; sortable), Criado (formatDate; sortable), Ações (single `Tooltip`+`Button variant="ghost"` MoreVertical, no delete; `enableSorting:false`, `enableHiding:false`). Wrap sortable headers with `DataTableColumnHeader`. Keep `statusVariant`/`formatDate` either imported from page.tsx module scope or copied into columns.tsx unchanged. In `page.tsx`, keep the dual-source logic (whichever `rows` array `searchActive` selects) exactly as-is — it only determines the `data` prop — and replace ONLY the `hidden md:block` `<Table>` branch with `<div className="hidden md:block"><DataTable columns={columns(clienteNomeById)} data={rows} /></div>`. Leave the mobile branch, filter Card, hooks, and outer guard untouched.
  </action>
  <verify>
    <automated>test -f 'web/src/app/(dashboard)/pareceres/columns.tsx' && grep -q 'DataTable' 'web/src/app/(dashboard)/pareceres/page.tsx' && grep -q 'statusVariant' 'web/src/app/(dashboard)/pareceres/columns.tsx' && ! grep -Eq '<table[ >]|getFilteredRowModel' 'web/src/app/(dashboard)/pareceres/columns.tsx' 'web/src/app/(dashboard)/pareceres/page.tsx' && pnpm --dir web exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `pareceres/columns.tsx` exists as a `columns(clienteNomeById)` factory; page.tsx desktop branch renders `<DataTable>`.
    - The statusVariant Badge mapping resolves only to closed-set variants (gray/blue/amber/green/secondary).
    - The dual data-source toggle still selects the rows array feeding `data`; no raw `<table>`, no `getFilteredRowModel`.
    - `pnpm --dir web exec tsc --noEmit` passes.
  </acceptance_criteria>
  <done>Pareceres desktop list is DataTable-driven with the status badge and cross-hook cliente resolution preserved; dual data source intact.</done>
</task>

<task type="auto">
  <name>Task 2: Financeiro — first Table adoption + DataTable + Badge migration off statusBadgeClass</name>
  <files>web/src/app/(dashboard)/financeiro/columns.tsx, web/src/app/(dashboard)/financeiro/page.tsx</files>
  <read_first>
    - web/src/app/(dashboard)/financeiro/page.tsx (FULL file — raw `<table>`/`<thead>`/`<tbody>` desktop branch ~lines 300-369, row body ~lines 317-367, hand-rolled statusBadgeClass span ~lines 357-361, the REAL Badge mapping already used in the mobile card branch ~lines 384-389, module helpers formatMoneyCVE/calcHonorarioStatus ~lines 15-27/92-105, processoById/clienteNomeById Maps ~lines 135-136, CSV export logic)
    - .planning/phases/LEXCV-104-padr-o-datatable-partilhado/104-PATTERNS.md (financeiro/columns.tsx section — the EXACT Badge mapping to carry from the mobile card: Pago→green, Parcialmente Pago→blue, else→amber; NOT the illustrative green/amber/red in UI-SPEC line 71)
    - .planning/phases/LEXCV-104-padr-o-datatable-partilhado/104-UI-SPEC.md (Scope note #1 + Per-Screen Adoption Contract row for Financeiro; Typography note about raw <th> font-medium being replaced)
    - web/src/components/shared/data-table/data-table.tsx, data-table-column-header.tsx; web/src/components/ui/table.tsx; web/src/components/ui/badge.tsx
  </read_first>
  <action>
    Create `financeiro/columns.tsx` as a `columns(processoById, clienteNomeById)` factory porting the raw-table row body's CELL CONTENT into `ColumnDef<Honorario>[]` (there is no existing `Table`/`TableRow` markup to lift structurally — only the `<td>` content logic): Honorário (`#{h.id}` link; sortable, `enableHiding:false`), Processo (link via `processoById`; sortable), Cliente (link via `clienteNomeById`; sortable), Total (`formatMoneyCVE`; sortable, numeric), Data do Acordo (`formatDate`; sortable, date), Estado (sortable). The Estado cell is the mandatory Badge migration: replace `<span className={statusBadgeClass[status]}>` with the REAL Badge component, mapping `calcHonorarioStatus()`'s value verbatim from the mobile card's existing precedent — `status === "Pago" ? "green" : status === "Parcialmente Pago" ? "blue" : "amber"` — do NOT invent new colors and do NOT keep `statusBadgeClass`. Wrap sortable headers with `DataTableColumnHeader`. In `page.tsx`, build `processoById`/`clienteNomeById` as today, and replace the ENTIRE raw `<table>...</table>` desktop branch with `<div className="hidden md:block"><DataTable columns={columns(processoById, clienteNomeById)} data={rows} /></div>` — the DataTable renders through the reconciled `Table` primitives, so no raw table markup must remain in the desktop branch. Leave the mobile card branch, filter/toolbar Card, CSV export, `use-*`/filters, and the outer guard untouched. Verify at implementation time that the column set still matches (columns may have drifted since the research pass).
  </action>
  <verify>
    <automated>test -f 'web/src/app/(dashboard)/financeiro/columns.tsx' && grep -q 'DataTable' 'web/src/app/(dashboard)/financeiro/page.tsx' && ! grep -q 'statusBadgeClass' 'web/src/app/(dashboard)/financeiro/columns.tsx' && ! grep -Eq '<table[ >]|<thead|getFilteredRowModel' 'web/src/app/(dashboard)/financeiro/page.tsx' && grep -q 'Badge' 'web/src/app/(dashboard)/financeiro/columns.tsx' && grep -q 'processoById' 'web/src/app/(dashboard)/financeiro/columns.tsx' && pnpm --dir web build</automated>
  </verify>
  <acceptance_criteria>
    - `financeiro/columns.tsx` exists as a `columns(processoById, clienteNomeById)` factory; `financeiro/page.tsx` desktop branch renders `<DataTable>`.
    - The raw `<table>`/`<thead>` markup is gone from `financeiro/page.tsx` (grep finds none in the desktop branch); the Estado cell uses `<Badge variant=...>` (green/blue/amber) and `statusBadgeClass` no longer appears in columns.tsx.
    - `processoById`/`clienteNomeById` are threaded through the factory; no `getFilteredRowModel`.
    - The mobile card branch and CSV export are unchanged.
    - `pnpm --dir web build` completes successfully (full Next build/typecheck for this plan's two-screen slice — Financeiro's first-Table adoption is real risk, so grep alone is insufficient).
  </acceptance_criteria>
  <done>Financeiro desktop list is DataTable-driven through the Table primitives, its status column uses the closed Badge vocabulary (green/blue/amber), the raw table and statusBadgeClass span are eliminated, and CSV export + mobile view are intact; full build green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DataTable ← already-fetched, already-server-filtered array | Sorting/pagination happen client-side over data the use-* hooks already fetched (tenant-scoped, server-filtered); no new fetch |
| Financeiro row links ← tenant-scoped ids | Processo/Cliente links resolve ids the existing tenant-scoped hooks already returned; no new id source is introduced |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-104-04-ID | Information Disclosure | client-side sort/paginate over the full fetched array | accept | Data already fetched + server-filtered by the existing use-* hooks; no getFilteredRowModel duplicates the server's authorization-aware filter; row links reuse the same tenant-scoped ids as today |
| T-104-04-TA | Tampering | Financeiro Estado Badge migration | mitigate | Map only via `calcHonorarioStatus()`'s existing output to closed-set Badge variants (green/blue/amber) from the mobile-card precedent — no new status semantics, no logic change to how status is computed |
| T-104-04-EP | Elevation of Privilege | Pareceres/Financeiro row actions + any permission-gated links | mitigate | Port existing permission gating 1:1 when moving cells into columns.tsx; the full `pnpm build` gate catches a dropped gate as a type error where applicable |
</threat_model>

<verification>
- Both desktop branches render `<DataTable>`; Financeiro's raw table and statusBadgeClass span are gone.
- All status cells use closed-set Badge variants (Financeiro: green/blue/amber from the mobile-card precedent).
- Cross-hook Maps threaded through factories; no getFilteredRowModel; mobile/filter/CSV code untouched.
- `pnpm --dir web build` green.
</verification>

<success_criteria>
- Pareceres and Financeiro desktop lists use the shared DataTable; Financeiro is brought onto the Table primitive + closed Badge vocabulary with zero client-side re-filtering.
</success_criteria>

<output>
Create `.planning/phases/LEXCV-104-padr-o-datatable-partilhado/104-04-SUMMARY.md` when done.
</output>
