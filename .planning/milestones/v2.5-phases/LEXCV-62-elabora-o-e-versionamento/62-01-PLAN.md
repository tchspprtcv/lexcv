---
phase: 62-elabora-o-e-versionamento
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/main/java/com/lexcv/models/ParecerVersao.java
  - backend/src/main/java/com/lexcv/repositories/ParecerVersaoRepository.java
autonomous: true
requirements: [PARV-02]
must_haves:
  truths:
    - "A ParecerVersao entity exists and maps to table t_parecer_versao"
    - "Each version carries solicitacaoId, numeroVersao, conteudo (nullable), caminhoAnexo (nullable), criadoPorId and createdAt"
    - "The repository can list versions of a solicitação and compute the next sequential version number"
  artifacts:
    - path: "backend/src/main/java/com/lexcv/models/ParecerVersao.java"
      provides: "Immutable JPA entity for a parecer version"
      contains: "class ParecerVersao"
      min_lines: 40
    - path: "backend/src/main/java/com/lexcv/repositories/ParecerVersaoRepository.java"
      provides: "Spring Data repository with list + max-version query"
      contains: "findMaxNumeroVersaoBySolicitacaoId"
  key_links:
    - from: "backend/src/main/java/com/lexcv/repositories/ParecerVersaoRepository.java"
      to: "backend/src/main/java/com/lexcv/models/ParecerVersao.java"
      via: "JpaRepository<ParecerVersao, UUID>"
      pattern: "JpaRepository<ParecerVersao"
---

<objective>
Create the data layer for parecer versioning: the immutable `ParecerVersao` JPA entity (table `t_parecer_versao`) and its Spring Data repository, including the per-solicitação max-version query used to compute sequential version numbers.

Purpose: Subsequent plan (62-02) adds controller endpoints that depend on this entity and repository existing with the exact field/method names referenced here.
Output: `ParecerVersao.java`, `ParecerVersaoRepository.java`.
</objective>

<execution_context>
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/LEXCV-62-elabora-o-e-versionamento/62-CONTEXT.md
@.planning/phases/LEXCV-62-elabora-o-e-versionamento/62-PATTERNS.md

<interfaces>
<!-- Existing analogs the executor must mirror. Do not invent new conventions. -->
ParecerSolicitacao.java (Phase 61) — entity skeleton: package com.lexcv.models, jakarta.persistence.*, Lombok @Getter/@Setter/@NoArgsConstructor/@AllArgsConstructor/@Builder, @Id @GeneratedValue(strategy = GenerationType.UUID) UUID id, @Column(name="tenant_id", nullable=false) UUID tenantId, @Column(name="created_at", updatable=false) LocalDateTime createdAt with @PrePersist onCreate().
Cliente.java — sequential numbering field is a plain @Column Integer (no DB sequence; computed in controller).
Documento.java — anexo reference field is @Column(name="caminho_arquivo") String caminhoArquivo.
ParecerSolicitacaoRepository — extends JpaRepository<ParecerSolicitacao, UUID>; ClienteRepository has a @Query("SELECT MAX(...) ...") returning Optional<Integer>.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ParecerVersao entity</name>
  <read_first>
    - backend/src/main/java/com/lexcv/models/ParecerSolicitacao.java (entity skeleton, tenantId column, createdAt + @PrePersist)
    - backend/src/main/java/com/lexcv/models/Documento.java (caminhoArquivo field naming)
    - backend/src/main/java/com/lexcv/models/Cliente.java (sequential numbering field as plain Integer column)
    - .planning/phases/LEXCV-62-elabora-o-e-versionamento/62-PATTERNS.md (exact field-by-field mapping, lines 17-85)
  </read_first>
  <files>backend/src/main/java/com/lexcv/models/ParecerVersao.java</files>
  <action>
    Create `com.lexcv.models.ParecerVersao` annotated `@Entity @Table(name = "t_parecer_versao")` with Lombok `@Getter @Setter @NoArgsConstructor @AllArgsConstructor @Builder`. Fields, with exact column names:
    - `UUID id` — `@Id @GeneratedValue(strategy = GenerationType.UUID)`
    - `UUID tenantId` — `@Column(name = "tenant_id", nullable = false)` (defense-in-depth: carry own tenant_id like ParecerSolicitacao/Documento, per 62-PATTERNS.md Shared Patterns > Tenant Isolation)
    - `UUID solicitacaoId` — `@Column(name = "solicitacao_id", nullable = false)`
    - `Integer numeroVersao` — `@Column(name = "numero_versao")` (plain Integer, set by controller, no DB sequence — mirror Cliente.numeroSequencial)
    - `String conteudo` — `@Column(columnDefinition = "TEXT")`, nullable (a version may be anexo-only)
    - `String caminhoAnexo` — `@Column(name = "caminho_anexo")`, nullable (mirrors Documento.caminhoArquivo)
    - `UUID criadoPorId` — `@Column(name = "criado_por_id")` (advogado/User who created the version; analogous to ParecerSolicitacao.advogadoId)
    - `LocalDateTime createdAt` — `@Column(name = "created_at", updatable = false)`
    Add a `@PrePersist protected void onCreate()` that sets `this.createdAt = LocalDateTime.now()`. Do NOT add an `updatedAt` field or any setter-based mutation hook — versions are immutable by design (CONTEXT.md). Imports: jakarta.persistence.*, lombok.*, java.time.LocalDateTime, java.util.UUID.
  </action>
  <verify>
    <automated>cd backend && mvn -DskipTests -q compile 2>&1 | grep -iE "error|BUILD FAILURE" && exit 1 || exit 0</automated>
  </verify>
  <acceptance_criteria>
    - File backend/src/main/java/com/lexcv/models/ParecerVersao.java exists and declares `class ParecerVersao`
    - Source contains `@Table(name = "t_parecer_versao")`
    - Source contains columns `tenant_id`, `solicitacao_id`, `numero_versao`, `caminho_anexo`, `criado_por_id`, `created_at`
    - Source contains `columnDefinition = "TEXT"` on the `conteudo` field
    - Source contains a `@PrePersist` method setting `createdAt`
    - Source contains NO field named `updatedAt`
    - `mvn -DskipTests compile` succeeds with no errors
  </acceptance_criteria>
  <done>ParecerVersao compiles, maps to t_parecer_versao, and exposes the seven fields above with immutable createdAt.</done>
</task>

<task type="auto">
  <name>Task 2: Create ParecerVersaoRepository</name>
  <read_first>
    - backend/src/main/java/com/lexcv/repositories/ParecerSolicitacaoRepository.java (repository structure/style)
    - backend/src/main/java/com/lexcv/repositories/ClienteRepository.java (MAX(...) @Query returning Optional<Integer>)
    - backend/src/main/java/com/lexcv/models/ParecerVersao.java (entity created in Task 1 — field names numeroVersao, solicitacaoId)
  </read_first>
  <files>backend/src/main/java/com/lexcv/repositories/ParecerVersaoRepository.java</files>
  <action>
    Create interface `com.lexcv.repositories.ParecerVersaoRepository extends JpaRepository<ParecerVersao, UUID>`. Declare two methods:
    - `List<ParecerVersao> findBySolicitacaoId(UUID solicitacaoId)` — derived query, returns all versions of a solicitação (used for the list/history endpoint and comparison).
    - `Optional<Integer> findMaxNumeroVersaoBySolicitacaoId(@Param("solicitacaoId") UUID solicitacaoId)` annotated `@Query("SELECT MAX(v.numeroVersao) FROM ParecerVersao v WHERE v.solicitacaoId = :solicitacaoId")` — scoped by solicitacaoId (versioning is per-solicitação, NOT per-tenant; tenant isolation is enforced in the controller via the parent solicitação check). Imports: com.lexcv.models.ParecerVersao, org.springframework.data.jpa.repository.JpaRepository, org.springframework.data.jpa.repository.Query, org.springframework.data.repository.query.Param, java.util.List, java.util.Optional, java.util.UUID.
  </action>
  <verify>
    <automated>cd backend && mvn -DskipTests -q compile 2>&1 | grep -iE "error|BUILD FAILURE" && exit 1 || exit 0</automated>
  </verify>
  <acceptance_criteria>
    - File backend/src/main/java/com/lexcv/repositories/ParecerVersaoRepository.java exists
    - Source contains `extends JpaRepository<ParecerVersao, UUID>`
    - Source contains method `findBySolicitacaoId`
    - Source contains method `findMaxNumeroVersaoBySolicitacaoId` with `@Query` selecting `MAX(v.numeroVersao)` filtered by `v.solicitacaoId = :solicitacaoId`
    - `mvn -DskipTests compile` succeeds with no errors
  </acceptance_criteria>
  <done>Repository compiles, lists versions by solicitação, and computes the max version number scoped to a single solicitação.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new in this plan) | Pure persistence layer; no request handling, no external input crosses here. Trust boundaries are introduced in 62-02 (controller). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-62-01 | Information Disclosure | ParecerVersao tenant scoping | mitigate | Entity carries own `tenant_id` column (defense-in-depth) so a future query path cannot leak cross-tenant rows even if a parent check is missed; enforcement of the value happens in 62-02. |
| T-62-02 | Tampering | numero_versao integrity | accept | numeroVersao is set server-side only (no setter exposed to request body); immutable entity has no UPDATE path. Concurrency hardening (synchronized MAX+1) lives in 62-02 where rows are written. |
</threat_model>

<verification>
- `mvn -DskipTests package` succeeds (entity + repository compile against the existing codebase).
- `ddl-auto=update` in dev will create `t_parecer_versao` on next boot — no manual migration needed.
</verification>

<success_criteria>
- ParecerVersao.java and ParecerVersaoRepository.java exist and compile.
- Entity is immutable (no updatedAt, no update hook) and tenant-scoped.
- Repository exposes findBySolicitacaoId and findMaxNumeroVersaoBySolicitacaoId with the exact signatures 62-02 will call.
</success_criteria>

<output>
Create `.planning/phases/LEXCV-62-elabora-o-e-versionamento/62-01-SUMMARY.md` when done.
</output>
