---
phase: 64-auditoria-e-pesquisa-avan-ada
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/main/java/com/lexcv/controllers/ParecerController.java
autonomous: true
requirements: [PARA-01]
must_haves:
  truths:
    - "Criar uma solicitação de parecer grava um AuditLog com acao=parecer_criar"
    - "Atribuir advogado grava um AuditLog com acao=parecer_atribuir"
    - "Criar uma versão grava um AuditLog com acao=parecer_versao_criar"
    - "Aprovar uma versão grava um AuditLog com acao=parecer_aprovar"
    - "Entregar uma solicitação grava um AuditLog com acao=parecer_entregar"
    - "Cada registo de auditoria tem tenantId e autorId do utilizador autenticado (nunca nulo)"
  artifacts:
    - path: "backend/src/main/java/com/lexcv/controllers/ParecerController.java"
      provides: "5 chamadas auditLogRepository.save() nos endpoints de transição"
      contains: "auditLogRepository"
  key_links:
    - from: "ParecerController.java"
      to: "AuditLogRepository"
      via: "constructor-injected field + save() calls"
      pattern: "auditLogRepository\\.save"
---

<objective>
Adicionar auditoria automática aos 5 endpoints de transição do ParecerController: cada ação relevante (criar solicitação, atribuir advogado, criar versão, aprovar versão, entregar) grava um registo em AuditLog usando o padrão manual de save() já estabelecido no resto do codebase.

Purpose: Satisfaz PARA-01 — rastreabilidade completa de ações sobre pareceres no histórico/timeline existente.
Output: ParecerController.java com AuditLogRepository injetado e 5 chamadas de auditoria.
</objective>

<execution_context>
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/LEXCV-64-auditoria-e-pesquisa-avan-ada/64-CONTEXT.md
@.planning/phases/LEXCV-64-auditoria-e-pesquisa-avan-ada/64-PATTERNS.md

<interfaces>
From backend/src/main/java/com/lexcv/models/AuditLog.java — builder fields used:
  tenantId (UUID), processoId (UUID, nullable), acao (String), entidadeTipo (String),
  entidadeId (String), autorId (UUID). Timestamp auto via @PrePersist.

From backend/src/main/java/com/lexcv/repositories/AuditLogRepository.java:
  save(AuditLog) — standard JpaRepository.

Authenticated-user extraction idiom (already at ParecerController lines 254-255, 289-290, 344-345):
  Authentication auth = SecurityContextHolder.getContext().getAuthentication();
  UserPrincipal principal = (UserPrincipal) auth.getPrincipal();
  principal.getUserId() -> autorId ; principal.getTenantId() -> tenantId
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Injetar AuditLogRepository e adicionar auditoria aos 5 endpoints de transição</name>
  <files>backend/src/main/java/com/lexcv/controllers/ParecerController.java</files>
  <read_first>
    - backend/src/main/java/com/lexcv/controllers/ParecerController.java (ficheiro a modificar — ver estado atual dos 5 endpoints e da lista de campos injetados nas linhas ~37-42)
    - backend/src/main/java/com/lexcv/models/AuditLog.java (campos do builder e @PrePersist timestamp)
    - backend/src/main/java/com/lexcv/repositories/AuditLogRepository.java (assinatura save)
    - backend/src/main/java/com/lexcv/controllers/ResourceController.java (padrão de referência: linhas 1133-1141 conflict_check_decisao, 1314-1322 transicao_estado, 2051-2058 documento_download)
    - .planning/phases/LEXCV-64-auditoria-e-pesquisa-avan-ada/64-PATTERNS.md (tabela de mapeamento acao/entidadeTipo/entidadeId por endpoint)
  </read_first>
  <behavior>
    - createSolicitacao: após parecerSolicitacaoRepository.save, grava AuditLog acao=parecer_criar, entidadeTipo=parecer_solicitacao, entidadeId=saved.getId().toString(), autorId=principal.getUserId(), tenantId, processoId=null
    - atribuirAdvogado: após save, grava AuditLog acao=parecer_atribuir, entidadeTipo=parecer_solicitacao, entidadeId=solicitacao.getId().toString(), processoId=null
    - createVersao: após parecerVersaoRepository.save, grava AuditLog acao=parecer_versao_criar, entidadeTipo=parecer_versao, entidadeId=saved.getId().toString(), processoId=null
    - aprovarVersao: após save, grava AuditLog acao=parecer_aprovar, entidadeTipo=parecer_versao, entidadeId=versao.getId().toString(), processoId=null
    - entregarSolicitacao: após save, grava AuditLog acao=parecer_entregar, entidadeTipo=parecer_solicitacao, entidadeId=solicitacao.getId().toString(), processoId=null
    - autorId e tenantId nunca nulos nestas 5 ações
  </behavior>
  <action>
    Adicionar os imports `com.lexcv.models.AuditLog` e `com.lexcv.repositories.AuditLogRepository` ao bloco de imports. Adicionar um campo `private final AuditLogRepository auditLogRepository;` à lista de campos constructor-injected (linhas ~37-42) e ao construtor, seguindo exatamente o padrão dos repositórios já injetados na mesma lista.

    Em cada um dos 5 endpoints, imediatamente após o save() da entidade primária e antes do `return ResponseEntity...`, inserir uma chamada `auditLogRepository.save(AuditLog.builder()...build())` com os valores exatos da tabela em 64-PATTERNS.md. Usar comentário `// Audit record — PARA-01: autor_id from SecurityContext` no estilo das linhas 1133/1314 do ResourceController. Definir `.processoId(null)` explicitamente em todos.

    Os endpoints `createSolicitacao` e `atribuirAdvogado` NÃO extraem atualmente Authentication/UserPrincipal no corpo do método — adicionar as duas linhas de extração (`Authentication auth = SecurityContextHolder.getContext().getAuthentication(); UserPrincipal principal = (UserPrincipal) auth.getPrincipal();`) antes da chamada de auditoria, idênticas às já presentes em aprovarVersao/entregarVersao/createVersao. Os outros 3 endpoints já têm `principal` disponível — reutilizar a variável existente, não redeclarar.

    Confirmar que `SecurityContextHolder`, `Authentication` e `UserPrincipal` já estão importados (estão, via getTenantId nas linhas 44-48). Usar `principal.getTenantId()` para tenantId e `principal.getUserId()` para autorId.
  </action>
  <verify>
    <automated>cd backend && mvn -q -DskipTests compile 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "auditLogRepository.save" backend/src/main/java/com/lexcv/controllers/ParecerController.java` retorna 5
    - `grep -o "parecer_criar\|parecer_atribuir\|parecer_versao_criar\|parecer_aprovar\|parecer_entregar" backend/src/main/java/com/lexcv/controllers/ParecerController.java | sort -u | wc -l` retorna 5
    - `grep -c "private final AuditLogRepository auditLogRepository" backend/src/main/java/com/lexcv/controllers/ParecerController.java` retorna 1
    - `mvn -DskipTests compile` termina com BUILD SUCCESS
    - Nenhuma chamada de auditoria usa autorId derivado do payload do request (só de principal.getUserId())
  </acceptance_criteria>
  <done>Os 5 endpoints de transição gravam AuditLog com acao/entidadeTipo/entidadeId/autorId/tenantId corretos; backend compila.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→ParecerController | Utilizador autenticado dispara transições; payload não confiável |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-64-01 | Repudiation | 5 endpoints de transição | mitigate | Grava AuditLog com autorId de SecurityContext (nunca do payload), tenantId scoped |
| T-64-02 | Spoofing | autorId em AuditLog | mitigate | autorId sempre de principal.getUserId(); proibido derivar do request body |
| T-64-03 | Tampering | tenant isolation nos registos de auditoria | mitigate | tenantId de principal.getTenantId(), consistente com getTenantId() existente |
| T-64-SC | Tampering | npm/pip/cargo installs | accept | Nenhuma instalação de package nova neste plano |
</threat_model>

<verification>
- Backend compila (`mvn -DskipTests compile`)
- 5 chamadas de auditoria presentes com os 5 valores de acao distintos
- AuditLogRepository injetado como campo final
</verification>

<success_criteria>
Criar, atribuir, criar versão, aprovar e entregar geram automaticamente um registo em AuditLog com entidadeTipo parecer_solicitacao/parecer_versao e autorId do utilizador autenticado.
</success_criteria>

<output>
Create `.planning/phases/LEXCV-64-auditoria-e-pesquisa-avan-ada/64-01-SUMMARY.md` when done
</output>
