# Deferred Items — Phase 115.1 Plan 01

Items found during execution that are out of scope for this plan's task changes (Scope Boundary rule: only auto-fix issues directly caused by the current task's changes).

## Pre-existing ESLint errors in `web/src/app/(dashboard)/clientes/[id]/page.tsx`

Found while running the Task 1 verification command (`pnpm exec eslint "src/app/(dashboard)/clientes/[id]/page.tsx"`). This was the first `pnpm install` + `pnpm exec eslint` run against this specific worktree checkout (node_modules did not previously exist here), so these pre-existing issues surfaced for the first time in this session. Confirmed via `git diff` that none of the flagged lines fall within this plan's edits (RBAC consts ~L147-149, TabsContent prop-passes ~L875/885, `ClienteProcessosTab`/`ClienteParecerTab` signatures+wrappers ~L1093-1120/1201-1222) — all 4 findings sit in unrelated pre-existing code far from these edits.

| Rule | Severity | Line | Description |
|------|----------|------|--------------|
| `react-hooks/incompatible-library` | warning | 374 | `const tipoValue = form.watch("tipo");` in `ClienteDetailContent` — React Compiler cannot memoize `react-hook-form`'s `watch()` return |
| `react-hooks/set-state-in-effect` | error | 1691 | `if (!editable) setModalOpen(false);` inside a `React.useEffect` (a sub-component below `ClienteDocumentosEntreguesTab`) |
| `react-hooks/set-state-in-effect` | error | 1846 | `setEditingId(null)` (+2 sibling setters) inside a `React.useEffect` (`ResponsaveisCard`-style sub-component) |
| `react-hooks/set-state-in-effect` | error | 2055 | `setEditingId(null)` (+2 sibling setters) inside a `React.useEffect` (another list-editing sub-component) |

**Status:** Not fixed — out of scope per Scope Boundary rule (pre-existing, unrelated to this task's changes). Not re-run/re-verified after logging, per "Do NOT re-run builds hoping they resolve themselves."

**Recommendation:** Candidate for a dedicated future cleanup task/phase — these 3 errors + 1 warning likely also affect the file's project-wide `pnpm lint` result and should be triaged (each is a `React.useEffect` synchronously calling `setState` to reset child-dialog/edit state when a parent `editable`/`tab` flag flips — the standard fix is deriving the reset from render-time state or a key-based remount rather than an effect).

## Pre-existing ESLint warning in `web/src/app/(dashboard)/processos/novo/page.tsx`

Found while running the Task 2 verification command (`pnpm exec eslint "src/app/(dashboard)/processos/novo/page.tsx"`). Confirmed via `git diff` that the flagged line is untouched by this plan's edits (import line 3, `searchParams`/`lockedClienteId` declarations ~L64-65, `cliente_id` default ~L89, `disabled` expression ~L308, new helper `<p>` ~L318-322).

| Rule | Severity | Line | Description |
|------|----------|------|--------------|
| `@typescript-eslint/no-unused-vars` | warning | 123 | `const { estado: _estado, ...intakeValues } = values;` in `onStep1Submit` — the underscore-prefixed destructure-to-omit pattern isn't covered by this project's ESLint `varsIgnorePattern` config |

**Status:** Not fixed — out of scope per Scope Boundary rule (pre-existing, unrelated to this task's changes).

**Recommendation:** Either add a `varsIgnorePattern: "^_"` to the relevant ESLint rule config (would silence this project-wide for the established convention), or rename to a rest-omit without a temp binding if the target TS/ESLint version supports it.

## Pre-existing ESLint warning in `web/src/app/(dashboard)/pareceres/nova/page.tsx`

Found while running the Task 3 verification command (`pnpm exec eslint "src/app/(dashboard)/pareceres/nova/page.tsx"`). Confirmed via `git diff` that the flagged line is untouched by this plan's edits (import line 2, `searchParams`/`lockedClienteId` declarations ~L47-48, `clienteId` default ~L58, `disabled` expression ~L129, new helper `<p>` ~L137-141).

| Rule | Severity | Line | Description |
|------|----------|------|--------------|
| `react-hooks/incompatible-library` | warning | 68 | `const clienteIdValue = form.watch("clienteId");` — same class of finding as the `clientes/[id]/page.tsx:374` entry above (React Compiler cannot memoize `react-hook-form`'s `watch()` return) |

**Status:** Not fixed — out of scope per Scope Boundary rule (pre-existing, unrelated to this task's changes; same root cause as the `clientes/[id]/page.tsx` finding above, likely present at every `form.watch(...)` call site across the app).
