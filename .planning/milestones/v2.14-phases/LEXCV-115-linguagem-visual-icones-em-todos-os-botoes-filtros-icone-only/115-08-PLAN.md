---
phase: 115-linguagem-visual-icones-em-todos-os-botoes-filtros-icone-only
plan: 08
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/app/(dashboard)/financeiro/page.tsx
  - web/src/app/(dashboard)/financeiro/novo/page.tsx
  - web/src/app/(dashboard)/financeiro/[id]/page.tsx
autonomous: true
requirements: [ICON-01, FICO-01]
must_haves:
  truths:
    - "Financeiro list: Exportar CSV (Download) and the conditionally-rendered Limpar filtros (X) are icon-only with Tooltip+aria-label; the conditional render is preserved; no Aplicar button invented"
    - "Financeiro 'Novo honorário' keeps text and gains a Plus icon"
    - "Honorário create + detail buttons (Voltar/submit/Cancelar/Guardar/Editar/Apagar/Adicionar-pagamento/Ver processo/Conta-corrente) carry icons"
  artifacts:
    - path: "web/src/app/(dashboard)/financeiro/page.tsx"
      provides: "FICO-01 icon-only Exportar(Download)/Limpar(X) + ICON-01 Novo honorário; new lucide-react import (module had none)"
      contains: "aria-label=\"Exportar CSV\""
    - path: "web/src/app/(dashboard)/financeiro/novo/page.tsx"
      provides: "Voltar/submit/Cancelar icons"
      contains: "lucide-react"
    - path: "web/src/app/(dashboard)/financeiro/[id]/page.tsx"
      provides: "10 gap buttons iconed (Voltar/Ver processo/Conta-corrente/Editar/Guardar/Cancelar/Apagar/Adicionar-pagamento)"
      contains: "lucide-react"
  key_links:
    - from: "financeiro/page.tsx Exportar/Limpar"
      to: "Tooltip primitive + aria-label"
      via: "icon-only conversion, conditional render preserved"
      pattern: "aria-label=\"(Exportar CSV|Limpar filtros)\""
---

<objective>
Deliver the Financeiro-module icon work. This module imports NO lucide-react today — every button is a gap and every file needs a new import. FICO-01: convert the list "Exportar CSV" (→ `Download`) and the conditionally-rendered "Limpar filtros" (→ `X`) to icon-only + Tooltip + aria-label, preserving both conditional-render guards. ICON-01: icons on "Novo honorário" (keep text) plus the create and detail pages.

Purpose: Own the three Financeiro files in one parallel plan. Financeiro has NO Aplicar filter button (Processo/Estado auto-apply via `Select onValueChange`) — do not invent one (UI-SPEC "Financeiro" row).
Output: 3 modified files; 16 ICON-01 gaps (incl. 2 FICO-01 conversions) closed.
</objective>

<execution_context>
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/LEXCV-115-linguagem-visual-icones-em-todos-os-botoes-filtros-icone-only/115-UI-SPEC.md
@.planning/phases/LEXCV-115-linguagem-visual-icones-em-todos-os-botoes-filtros-icone-only/115-CONTEXT.md

Exact citations: `115-UI-SPEC.md` → "FICO-01 — Filter Action Button Inventory" (Financeiro row) + "Financeiro module" audit table. EVIDENCE, verified 2026-07-21 (confirmed live: financeiro/page.tsx has no lucide import; Exportar CSV at L175-180, conditional on `canViewFinanceiro && filteredList.length > 0`; Limpar filtros at L264-274, conditional on any active filter).

FICO-01 pattern + locked copy: same as Plan 02. Exportar→`Exportar CSV`, Limpar→`Limpar filtros` (tooltip == aria-label). Both currently `variant="outline"` — keep that variant, icon-only. CRITICAL: preserve each button's surrounding conditional-render guard (`{cond ? <Button/> : null}`) — the `<Tooltip>…</Tooltip>` goes INSIDE the conditional. Import Tooltip primitives; TooltipProvider is app-global. Financeiro has NO Aplicar button — do not invent one.

Vocabulary: "Novo honorário" (L183, asChild+Link)→`Plus` (icon inside Link); Voltar→`ArrowLeft`; submit-create→`Plus`; Cancelar→`X`; Guardar→`Save`; Editar→`Pencil`; Apagar→`Trash2`; Adicionar (pagamento)→`Plus`; "Ver processo" / "Conta-corrente do cliente" (navigate to related view)→`ArrowRight`.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Financeiro list (FICO-01 + Novo honorário) + honorário create</name>
  <files>web/src/app/(dashboard)/financeiro/page.tsx, web/src/app/(dashboard)/financeiro/novo/page.tsx</files>
  <read_first>
    - web/src/app/(dashboard)/financeiro/page.tsx (imports + L170-190, L260-276)
    - web/src/app/(dashboard)/financeiro/novo/page.tsx (imports + L89, L170, L177)
    - 115-UI-SPEC.md "FICO-01" Financeiro row + "Financeiro module" table
  </read_first>
  <behavior>
    - Exportar CSV (L175-180, `variant="outline"`, inside `{canViewFinanceiro && filteredList.length > 0 ? … : null}`) → icon-only `Download` + `size="icon"` + `aria-label="Exportar CSV"` + Tooltip; the export onClick and the conditional guard both preserved.
    - Limpar filtros (L264-274, `variant="outline"`, inside `{anyFilterActive ? … : null}`) → icon-only `X` + aria-label "Limpar filtros" + Tooltip; the clear onClick and conditional guard preserved.
    - Novo honorário (L183, asChild+Link) keeps text, gains `Plus` inside the Link.
    - novo page: Voltar/submit/Cancelar iconed.
  </behavior>
  <action>
    In `financeiro/page.tsx`: add a NEW `import { ArrowRight, Download, Plus, X } from "lucide-react";` (none exists today; ArrowRight is for the detail page but keep this file's import to only what it uses — Download/Plus/X). Add `Tooltip, TooltipTrigger, TooltipContent` from `@/components/ui/tooltip`. Convert Exportar (L175) and Limpar (L264) to icon-only per the UI-SPEC pattern, keeping the `variant="outline"` and, crucially, leaving the outer `{cond ? … : null}` guard intact (wrap only the Button in Tooltip, inside the conditional). Add `Plus` inside the Novo honorário Link (L183), keep text. Do NOT invent an Aplicar button.
    In `financeiro/novo/page.tsx`: add a new lucide-react import. L89 Voltar→`ArrowLeft`; L170 submit→`Plus`; L177 Cancelar→`X`. Icons before text; keep text/handlers.
  </action>
  <acceptance_criteria>
    - Source assertion: `financeiro/page.tsx` has `aria-label="Exportar CSV"` and `aria-label="Limpar filtros"`, each on a `size="icon"` Button wrapped by `TooltipTrigger asChild`, each still inside its original `? … : null` conditional.
    - Source assertion: Novo honorário renders `<Plus` inside its Link with text preserved.
    - Source assertion: `financeiro/novo/page.tsx` gained a lucide-react import and renders ArrowLeft/Plus/X.
    - Negative assertion: no "Aplicar" filter button exists in financeiro/page.tsx.
  </acceptance_criteria>
  <verify>
    <automated>grep -nE "aria-label=\"(Exportar CSV|Limpar filtros)\"" "web/src/app/(dashboard)/financeiro/page.tsx" (expect 2) && grep -nE "\\? \\(|: null" "web/src/app/(dashboard)/financeiro/page.tsx" | head (guards still present) && grep -nE "from \"lucide-react\"" "web/src/app/(dashboard)/financeiro/novo/page.tsx" (expect 1)</automated>
  </verify>
  <done>Financeiro list Exportar/Limpar icon-only with Tooltip+aria-label and conditional guards intact; Novo honorário iconed; create page iconed; no Aplicar invented; lint clean.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Honorário detail page (10 gaps)</name>
  <files>web/src/app/(dashboard)/financeiro/[id]/page.tsx</files>
  <read_first>
    - web/src/app/(dashboard)/financeiro/[id]/page.tsx (imports + L94, L256, L260, L265, L273, L317, L319, L335, L476, L524)
    - 115-UI-SPEC.md "Financeiro module" [id]/page.tsx row
  </read_first>
  <action>
    Add a new lucide-react import covering: `ArrowLeft, ArrowRight, Pencil, Save, X, Trash2, Plus`. Map the 10 buttons: L94 Voltar→`ArrowLeft`; L256 Voltar→`ArrowLeft`; L260 "Conta-corrente do cliente"→`ArrowRight`; L265 "Ver processo"→`ArrowRight`; L273 Editar→`Pencil`; L317 Cancelar→`X`; L319 Guardar→`Save`; L335 Apagar (honorário)→`Trash2`; L476 Adicionar (pagamento)→`Plus`; L524 Apagar (per-pagamento row)→`Trash2`. Icons before text; keep text/handlers and any existing delete confirmations.
  </action>
  <acceptance_criteria>
    - Source assertion: one lucide-react import with the 7 names; each of the 10 cited buttons renders its vocabulary-mapped icon with text preserved.
    - Behavior: delete confirmations unchanged.
  </acceptance_criteria>
  <verify>
    <automated>grep -nE "from \"lucide-react\"" "web/src/app/(dashboard)/financeiro/[id]/page.tsx" (expect 1) && grep -cE "<(ArrowLeft|ArrowRight|Pencil|Save|X |Trash2|Plus)" "web/src/app/(dashboard)/financeiro/[id]/page.tsx" (expect ~10)</automated>
  </verify>
  <done>All 10 honorário-detail buttons carry vocabulary-correct icons with text preserved; import resolves; lint clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Adds icons/Tooltip wrappers to existing client-side buttons. No new input, data flow, network call, or auth/authz change. FICO-01 conversion preserves export/clear handlers AND their `canViewFinanceiro`/active-filter conditional-render guards; delete confirmations untouched. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-115-01 | Tampering | icon/Tooltip markup in financeiro .tsx | accept | Static compile-time glyph imports; no untrusted input crosses a boundary. The Exportar guard (`canViewFinanceiro`) is a UI convenience, not the security control — backend RBAC still gates the data; preserving it avoids a cosmetic regression only. |
| T-115-A11Y | WCAG 4.1.2 (a11y) | icon-only Exportar/Limpar (FICO-01) | mitigate | Icon-only removes the visible label; mitigated by `aria-label` + `Tooltip` identical copy on both (FICO-01 AC4). |
| T-115-SC | Tampering | npm/pnpm install | accept | No package installed — new named imports from already-installed `lucide-react` (module previously used none). No legitimacy gate applies. |
</threat_model>

<verification>
- `cd web && pnpm lint` clean on the 3 files (this module had zero lucide imports — confirm all 3 new import lines are correct and fully used).
- Grep proves the 2 locked aria-labels + Tooltip wrapping + preserved conditional guards, and vocabulary icons across create/detail; no invented Aplicar.
- FICO-01 interactive verification deferred to Plan 11.
</verification>

<success_criteria>
- Financeiro list Exportar/Limpar icon-only with Tooltip+aria-label, conditional renders intact, no Aplicar invented — FICO-01 for this module complete.
- All 16 Financeiro gap buttons carry icons (ICON-01); 3 new lucide-react imports, none unused.
- Confirmations untouched; lint clean.
</success_criteria>

<output>
Create `.planning/phases/LEXCV-115-linguagem-visual-icones-em-todos-os-botoes-filtros-icone-only/115-08-SUMMARY.md` when done.
</output>
