---
phase: 115-linguagem-visual-icones-em-todos-os-botoes-filtros-icone-only
plan: 10
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/app/(dashboard)/settings/page.tsx
  - web/src/app/(dashboard)/dashboard/page.tsx
  - web/src/app/(auth)/login/page.tsx
autonomous: true
requirements: [ICON-01]
must_haves:
  truths:
    - "Settings 'Guardar Utilizador' (Save), Cancelar (X), and 'Tentar novamente' (RotateCcw) carry icons; the already-icon-only close-X gains an aria-label"
    - "Dashboard 'Ver Agenda Completa' gains an ArrowRight icon"
    - "Login 'Entrar' gains a LogIn icon"
  artifacts:
    - path: "web/src/app/(dashboard)/settings/page.tsx"
      provides: "Guardar Utilizador/Cancelar/Tentar novamente icons + close-X aria-label"
      contains: "RotateCcw"
    - path: "web/src/app/(dashboard)/dashboard/page.tsx"
      provides: "Ver Agenda Completa ArrowRight"
      contains: "ArrowRight"
    - path: "web/src/app/(auth)/login/page.tsx"
      provides: "Entrar LogIn icon"
      contains: "LogIn"
  key_links:
    - from: "settings/page.tsx buttons"
      to: "lucide-react (reuse existing Save/X, add RotateCcw)"
      via: "icon adds; do not touch existing Edit/Plus/Trash2/Save-Loader2"
      pattern: "RotateCcw"
---

<objective>
Close the remaining ICON-01 gaps in the top-level miscellaneous pages: Settings (Guardar Utilizador→`Save`, Cancelar→`X`, "Tentar novamente"→`RotateCcw`, plus an `aria-label` on the already-icon-only close-`X`), Dashboard ("Ver Agenda Completa"→`ArrowRight`), and Login ("Entrar"→`LogIn`).

Purpose: Own the three misc pages in one parallel plan. Settings already imports most icons (Plus/Edit/Trash2/Save/Loader2/X) — reuse them; only `RotateCcw` is a new import there. Respect the pre-existing `Edit` (not `Pencil`) usage in Settings — do NOT normalize it.
Output: 3 modified files; 5 ICON-01 gaps closed + 1 optional close-X aria-label.
</objective>

<execution_context>
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/LEXCV-115-linguagem-visual-icones-em-todos-os-botoes-filtros-icone-only/115-UI-SPEC.md
@.planning/phases/LEXCV-115-linguagem-visual-icones-em-todos-os-botoes-filtros-icone-only/115-CONTEXT.md

Exact citations: `115-UI-SPEC.md` → "Dashboard, Settings, Setup, Login" table. EVIDENCE, verified 2026-07-21.

Vocabulary: "Guardar Utilizador" (settings L678)→`Save` (reuse the L776 Save/Loader2 pending pattern if the pending boolean is already at hand); Cancelar (settings L666, paired with L678)→`X`; "Tentar novamente" (settings L897)→`RotateCcw` (retry/reset); "Ver Agenda Completa" (dashboard L465)→`ArrowRight`; "Entrar" (login L89)→`LogIn`.

Settings reuse note (UI-SPEC): `Plus` (L361), `Edit` (L467), `Trash2`-pattern (L478), `Save`/`Loader2` (L776) are already imported and compliant — do NOT touch them, and do NOT normalize the pre-existing `Edit` to `Pencil` (Scope Boundaries §2). Only `RotateCcw` is a genuinely new import in settings; `Save`/`X` are already present. Special (optional, nearly free): settings L522 close-`X` is already icon-only but MISSING `aria-label` — add `aria-label="Fechar"` (recommended, not blocking).

Login/Dashboard: check whether each already imports lucide-react; dashboard already imports Plus (L91) + an Eye-pattern (compliant) so extend it; login likely needs a new import for `LogIn`.
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Settings — Guardar/Cancelar/Tentar novamente + close-X aria-label</name>
  <files>web/src/app/(dashboard)/settings/page.tsx</files>
  <read_first>
    - web/src/app/(dashboard)/settings/page.tsx (imports + L515-525, L660-680, L890-900) — confirm which icons are already imported before editing
    - 115-UI-SPEC.md "Dashboard, Settings, Setup, Login" settings row + Scope Boundaries §2
  </read_first>
  <action>
    Extend the existing lucide-react import with `RotateCcw` only (Save/X already present — verify and reuse; do not duplicate). L678 "Guardar Utilizador"→`Save` (reuse the existing Save/Loader2 pending-swap pattern if the pending boolean is in scope); L666 Cancelar→`X`; L897 "Tentar novamente"→`RotateCcw`. Optionally add `aria-label="Fechar"` to the already-icon-only close-`X` at L522 (do not otherwise change it). Icons before text on the text buttons; keep text/handlers. Do NOT touch L361 Plus, L467 Edit, L478 Trash2-pattern, L776 Save/Loader2 (all compliant) and do NOT convert `Edit` to `Pencil`.
  </action>
  <acceptance_criteria>
    - Source assertion: import now includes `RotateCcw`; L678 renders `<Save`, L666 renders `<X`, L897 renders `<RotateCcw`; each keeps its text.
    - Source assertion: the L522 close-X has an `aria-label` (if the optional fix was applied).
    - Untouched: L361/L467/L478/L776 byte-identical; `Edit` not renamed.
  </acceptance_criteria>
  <verify>
    <automated>grep -nE "RotateCcw" "web/src/app/(dashboard)/settings/page.tsx" (import + usage) && grep -nE "<Save|<X " "web/src/app/(dashboard)/settings/page.tsx" && grep -cE "Pencil" "web/src/app/(dashboard)/settings/page.tsx" (expect 0 — Edit not normalized)</automated>
  </verify>
  <done>Settings Guardar/Cancelar/Tentar novamente iconed with text; close-X aria-label added; compliant buttons + existing Edit untouched; lint clean.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Dashboard + Login</name>
  <files>web/src/app/(dashboard)/dashboard/page.tsx, web/src/app/(auth)/login/page.tsx</files>
  <read_first>
    - web/src/app/(dashboard)/dashboard/page.tsx (imports + L465)
    - web/src/app/(auth)/login/page.tsx (imports + L89)
    - 115-UI-SPEC.md "Dashboard, Settings, Setup, Login" dashboard + login rows
  </read_first>
  <action>
    `dashboard/page.tsx`: extend the existing lucide-react import (already has Plus + Eye-pattern) with `ArrowRight`. L465 "Ver Agenda Completa"→`ArrowRight` (inside Link if asChild), keep text. Do not touch L91 Plus / L588 Eye-pattern (compliant).
    `login/page.tsx`: add `LogIn` (new import if the file has none). L89 "Entrar"→`LogIn`, keep text. Optional `Loader2` pending swap only where an `isSubmitting`/`isPending` boolean is already wired into `disabled`.
  </action>
  <acceptance_criteria>
    - Source assertion: dashboard renders `<ArrowRight` on "Ver Agenda Completa" with text; login renders `<LogIn` on "Entrar" with text.
    - Untouched: dashboard L91 Plus / L588 Eye-pattern byte-identical.
  </acceptance_criteria>
  <verify>
    <automated>grep -nE "<ArrowRight" "web/src/app/(dashboard)/dashboard/page.tsx" && grep -nE "LogIn" "web/src/app/(auth)/login/page.tsx" (import + usage)</automated>
  </verify>
  <done>Dashboard "Ver Agenda Completa" and Login "Entrar" iconed with text preserved; imports resolve; compliant buttons untouched; lint clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Adds icons to existing client-side buttons on Settings/Dashboard/Login. No new input, data flow, network call, or auth/authz change. The Login "Entrar" button's submit behavior and the Settings user-management handlers are untouched — only a glyph is added. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-115-01 | Tampering | icon markup in settings/dashboard/login .tsx | accept | Static compile-time glyph imports; no untrusted input crosses a boundary; the login submit + settings admin handlers unchanged (adding a `LogIn`/`Save` glyph does not alter form action or credentials handling). |
| T-115-A11Y | WCAG 4.1.2 (a11y) | already-icon-only close-X (settings L522) | mitigate | Optional but recommended: adds `aria-label="Fechar"` to a previously-unlabeled icon-only button, restoring its accessible name. The new icon+text buttons retain their visible text as the accessible name. |
| T-115-SC | Tampering | npm/pnpm install | accept | No package installed — additional named exports from already-installed `lucide-react`. No legitimacy gate applies. |
</threat_model>

<verification>
- `cd web && pnpm lint` clean on the 3 files.
- Grep proves RotateCcw + Save/X in settings, ArrowRight in dashboard, LogIn in login; `Pencil` count 0 in settings (Edit not normalized).
- No interactive checkpoint needed (all icon+text); included in Plan 11's do-not-regress spot-check.
</verification>

<success_criteria>
- Settings/Dashboard/Login gap buttons carry vocabulary-correct icons with text preserved (ICON-01).
- Settings' pre-existing `Edit` and other compliant buttons untouched; only `RotateCcw` newly imported there; lint clean.
</success_criteria>

<output>
Create `.planning/phases/LEXCV-115-linguagem-visual-icones-em-todos-os-botoes-filtros-icone-only/115-10-SUMMARY.md` when done.
</output>
