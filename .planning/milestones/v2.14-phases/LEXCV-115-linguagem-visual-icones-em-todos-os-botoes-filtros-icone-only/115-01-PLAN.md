---
phase: 115-linguagem-visual-icones-em-todos-os-botoes-filtros-icone-only
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/components/shared/data-table/data-table-pagination.tsx
  - web/src/components/shared/access-denied-state.tsx
  - web/src/components/shared/notificacao-snooze-control.tsx
  - web/src/components/profile/user-profile-form.tsx
  - web/src/components/profile/user-password-form.tsx
autonomous: true
requirements: [ICON-01]
must_haves:
  truths:
    - "Every DataTable list footer (Clientes/Processos/Documentos/Financeiro/Pareceres) shows a ChevronLeft on Anterior and ChevronRight on Seguinte"
    - "Every RBAC access-denied screen app-wide shows an ArrowLeft on its Voltar button"
    - "The snooze confirm (Adiar), profile-form Cancelar/Guardar, and password-form Limpar/submit each carry an icon"
  artifacts:
    - path: "web/src/components/shared/data-table/data-table-pagination.tsx"
      provides: "Paginated Anterior/Seguinte buttons with chevron icons (highest-leverage: one file, every list page)"
      contains: "ChevronLeft"
    - path: "web/src/components/shared/access-denied-state.tsx"
      provides: "Voltar button with ArrowLeft inside its asChild Link (rendered on every access-denied screen)"
      contains: "ArrowLeft"
    - path: "web/src/components/profile/user-profile-form.tsx"
      provides: "Cancelar (X) + Guardar (Save) icons"
      contains: "Save"
    - path: "web/src/components/profile/user-password-form.tsx"
      provides: "Limpar (X) + submit (Save) icons"
      contains: "lucide-react"
    - path: "web/src/components/shared/notificacao-snooze-control.tsx"
      provides: "Adiar confirm button with Check icon"
      contains: "Check"
  key_links:
    - from: "data-table-pagination.tsx"
      to: "lucide-react"
      via: "named import of ChevronLeft/ChevronRight"
      pattern: "ChevronLeft|ChevronRight"
    - from: "access-denied-state.tsx"
      to: "lucide-react"
      via: "ArrowLeft added to existing Lock import"
      pattern: "ArrowLeft"
---

<objective>
Add Lucide icons (ICON-01) to the five highest-leverage shared/leaf components — one edit each fixes every downstream call site. `data-table-pagination.tsx` renders in every list page; `access-denied-state.tsx` renders on every RBAC-gated screen. No FICO-01 (no icon-only conversion) in this plan — every button keeps its visible text and gains an icon.

Purpose: Establish the icon+text pattern on the components with the widest reach, so the module plans (02-10) benefit and the phase's ICON-01 coverage starts from the shared layer.
Output: 5 modified `.tsx` files, 8 gap buttons closed.
</objective>

<execution_context>
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/LEXCV-115-linguagem-visual-icones-em-todos-os-botoes-filtros-icone-only/115-UI-SPEC.md
@.planning/phases/LEXCV-115-linguagem-visual-icones-em-todos-os-botoes-filtros-icone-only/115-CONTEXT.md

The icon vocabulary and the exact per-file/line inventory live in `115-UI-SPEC.md` (sections "Icon Vocabulary Contract" and "ICON-01 — Full Button Audit"). It is EVIDENCE — grep/read-verified against live code 2026-07-21 — not a proposal. Do not re-audit; execute against its tables.

Verified-live primitives:
- `Button` supports `size="icon"` (→ `h-9 w-9`) and auto-sizes any child `svg` via `[&_svg]:size-4` (web/src/components/ui/button.tsx). Adding `className="h-4 w-4"` on each icon matches the 44 already-compliant instances — keep doing that for consistency.
- `TooltipProvider` is already mounted once at web/src/app/providers.tsx:30 (delayDuration 700) — no per-call-site wiring (not needed in this plan, none of these are FICO-01).

asChild rule (applies to `access-denied-state.tsx` here and recurs elsewhere): when a button is `<Button asChild><Link>…</Link></Button>`, the icon element goes INSIDE the `<Link>` (as a sibling of the label text), never as a direct child of `<Button>`. Verified compliant example: `clientes/page.tsx:285-289` renders `<Plus/>` inside the Link.

Do-not-touch: the 44 already-compliant instances (UI-SPEC Scope Boundaries §2). None are in this plan's files_modified.
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Chevron pagination + access-denied Voltar (highest leverage)</name>
  <files>web/src/components/shared/data-table/data-table-pagination.tsx, web/src/components/shared/access-denied-state.tsx</files>
  <read_first>
    - web/src/components/shared/data-table/data-table-pagination.tsx (full — 82 lines)
    - web/src/components/shared/access-denied-state.tsx (full — 38 lines)
    - 115-UI-SPEC.md section "Shared components (`components/shared/`)"
  </read_first>
  <action>
    In `data-table-pagination.tsx`: add `import { ChevronLeft, ChevronRight } from "lucide-react";`. Put `<ChevronLeft className="h-4 w-4" />` before the "Anterior" text inside the Anterior Button (currently ~L60-68), and `<ChevronRight className="h-4 w-4" />` after the "Seguinte" text inside the Seguinte Button (~L69-77). Keep both buttons' `variant="outline" size="sm"`, `onClick`, and `disabled` props exactly as-is; keep the visible text (ICON-01 = icon + text, not icon-only). Per ICON-01 vocabulary, back/forward navigation uses directional chevrons.
    In `access-denied-state.tsx`: extend the existing `import { Lock } from "lucide-react";` to `import { ArrowLeft, Lock } from "lucide-react";`. The Voltar button is `<Button asChild variant="outline"><Link href={backHref}>{backLabel}</Link></Button>` (~L31-33) — place `<ArrowLeft className="h-4 w-4" />` INSIDE the `<Link>`, before `{backLabel}` (asChild renders the Link, so the icon must be the Link's child, not the Button's). Per UI-SPEC, Voltar → `ArrowLeft` (precedent: processos/dashboard/page.tsx:32).
  </action>
  <acceptance_criteria>
    - Source assertion: `data-table-pagination.tsx` imports `ChevronLeft` and `ChevronRight` from lucide-react and uses each exactly once inside the Anterior/Seguinte buttons respectively.
    - Source assertion: `access-denied-state.tsx` imports `ArrowLeft` and renders `<ArrowLeft` inside the `<Link>` element (grep shows ArrowLeft on a line between the `<Link` and `{backLabel}`).
    - Behavior: both buttons still carry their visible Portuguese text ("Anterior"/"Seguinte"/backLabel) — no text removed.
    - Untouched: the `Select` rows-per-page block, the `Lock` icon, and all `disabled`/`onClick` handlers are byte-identical to before.
  </acceptance_criteria>
  <verify>
    <automated>grep -nE "ChevronLeft|ChevronRight" web/src/components/shared/data-table/data-table-pagination.tsx (expect >=3: one import line with both + one usage each) && grep -nE "ArrowLeft" web/src/components/shared/access-denied-state.tsx (expect >=2: import + usage)</automated>
  </verify>
  <done>Both shared components import and render their chevron/arrow icons with visible text preserved; imports resolve (no unused/undefined).</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Snooze confirm + profile forms</name>
  <files>web/src/components/shared/notificacao-snooze-control.tsx, web/src/components/profile/user-profile-form.tsx, web/src/components/profile/user-password-form.tsx</files>
  <read_first>
    - web/src/components/shared/notificacao-snooze-control.tsx (full)
    - web/src/components/profile/user-profile-form.tsx (full)
    - web/src/components/profile/user-password-form.tsx (full)
    - 115-UI-SPEC.md section "Icon Vocabulary Contract" (Cancelar→X, Guardar→Save, Confirmar→Check)
  </read_first>
  <action>
    `notificacao-snooze-control.tsx`: the "Adiar" confirm button inside the popover is the gap (the `Clock` trigger is already compliant — do not touch it). Add `Check` to the existing lucide-react import and render `<Check className="h-4 w-4" />` before the "Adiar" text. Per UI-SPEC vocabulary, the snooze-confirm submit → `Check` (affirmative-submit family).
    `user-profile-form.tsx`: add `import { Save, X } from "lucide-react";` (no lucide import exists today). Add `<X className="h-4 w-4" />` to the Cancelar (reset) button and `<Save className="h-4 w-4" />` to the Guardar (submit) button, before their text. If the Guardar button already threads an `isSubmitting`/`isPending` boolean into `disabled`, optionally swap the icon for `<Loader2 className="h-4 w-4 animate-spin" />` while pending (UI-SPEC pending-state pattern) — only if the boolean is already at hand; do not add new state.
    `user-password-form.tsx`: add `import { Save, X } from "lucide-react";`. The "Limpar" button here is a form-reset (not a filter-clear) → still `X` per UI-SPEC note. The submit ("Alterar Password") → `Save`. Place each icon before the button text; keep text.
    Keep all three files' buttons with visible text (ICON-01, no icon-only conversion).
  </action>
  <acceptance_criteria>
    - Source assertion: `notificacao-snooze-control.tsx` imports and uses `Check` on the Adiar button; the `Clock` trigger is unchanged.
    - Source assertion: both profile forms add a new `lucide-react` import line and render `X` on the reset/cancel button and `Save` on the submit button.
    - Behavior: all five buttons keep their visible text; no button converted to icon-only; no Tooltip added (these are not FICO-01 buttons).
  </acceptance_criteria>
  <verify>
    <automated>grep -nE "Check" web/src/components/shared/notificacao-snooze-control.tsx (expect Adiar usage) && grep -nE "from \"lucide-react\"" web/src/components/profile/user-profile-form.tsx web/src/components/profile/user-password-form.tsx (expect one import line each) && grep -nE "Save|X " web/src/components/profile/user-password-form.tsx</automated>
  </verify>
  <done>Snooze-confirm + both profile forms carry their action icons with text preserved; new lucide imports resolve; no unused imports.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan only adds Lucide icon elements + named imports to existing client-side buttons. No new input, no data flow, no network call, no auth/authz change, no state mutation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-115-01 | Tampering | icon markup in shared .tsx buttons | accept | No untrusted input crosses any boundary; icons are static compile-time imports of glyphs. No new attack surface. |
| T-115-SC | Tampering | npm/pnpm install | accept | No package installed — this imports additional named exports from the already-installed `lucide-react` (UI-SPEC Registry Safety: "never a new package"). No package-legitimacy gate applies. |
</threat_model>

<verification>
- `cd web && pnpm lint` reports no new errors on the 5 touched files (catches unused/undefined icon imports).
- Grep confirms each new icon name appears as both an import and a JSX usage in its file.
- No file outside `files_modified` changed (`git diff --name-only` limited to this plan's 5 files).
</verification>

<success_criteria>
- 8 ICON-01 gap buttons closed across 5 shared/leaf components, all keeping visible text.
- `data-table-pagination.tsx` and `access-denied-state.tsx` fixes propagate to every list page and every access-denied screen (single-file, multi-surface leverage).
- Lint clean; the 44 do-not-touch compliant instances untouched.
</success_criteria>

<output>
Create `.planning/phases/LEXCV-115-linguagem-visual-icones-em-todos-os-botoes-filtros-icone-only/115-01-SUMMARY.md` when done.
</output>
