---
phase: 115-linguagem-visual-icones-em-todos-os-botoes-filtros-icone-only
plan: 09
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/app/(dashboard)/pareceres/page.tsx
  - web/src/app/(dashboard)/pareceres/[id]/page.tsx
  - web/src/app/(dashboard)/pareceres/nova/page.tsx
  - web/src/app/(dashboard)/notificacoes/page.tsx
autonomous: true
requirements: [ICON-01]
must_haves:
  truths:
    - "Pareceres and Notificações filter-adjacent buttons (pareceres Aplicar/Limpar x2 panels, notificacoes Limpar filtros) gain an icon but KEEP their visible text — NOT converted to icon-only, NO Tooltip added (these modules are excluded from FICO-01)"
    - "Pareceres 'Nova Solicitação', 'Submeter Versão', 'Entregar Parecer', Download, and create-page Cancelar/submit carry icons"
  artifacts:
    - path: "web/src/app/(dashboard)/pareceres/page.tsx"
      provides: "Nova Solicitação (Plus) + 4 filter Aplicar/Limpar (Check/X) WITH TEXT PRESERVED"
      contains: "lucide-react"
    - path: "web/src/app/(dashboard)/pareceres/[id]/page.tsx"
      provides: "Download + Submeter Versão (Send) + Entregar Parecer (Send)"
      contains: "Send"
    - path: "web/src/app/(dashboard)/pareceres/nova/page.tsx"
      provides: "Cancelar/submit/Cancelar icons"
      contains: "lucide-react"
    - path: "web/src/app/(dashboard)/notificacoes/page.tsx"
      provides: "Limpar filtros icon WITH TEXT preserved"
      contains: "lucide-react"
  key_links:
    - from: "pareceres/page.tsx + notificacoes/page.tsx filter buttons"
      to: "text label"
      via: "icon added, text NOT removed, no Tooltip wrap (FICO-01 excluded)"
      pattern: "(Aplicar|Limpar|Filtrar)"
---

<objective>
Deliver ICON-01 for the two modules explicitly EXCLUDED from FICO-01 (Pareceres, Notificações). The critical constraint: their filter-adjacent buttons (pareceres Aplicar/Limpar in both the dual-view and advanced-search panels; notificacoes "Limpar filtros") get an icon WHILE KEEPING their visible text — they are NOT converted to icon-only and get NO Tooltip wrap. Also icon the other Pareceres gap buttons (Nova Solicitação, Submeter Versão, Entregar Parecer, Download, create-page buttons).

Purpose: Honor the locked CONTEXT.md decision ("não expandir a Pareceres/Notificações") — FICO-01 is FICO-01-scoped only; ICON-01 still applies to these modules (UI-SPEC Scope Boundaries §1). Own these four files in one parallel plan.
Output: 4 modified files; 12 ICON-01 gaps closed, ZERO icon-only conversions.
</objective>

<execution_context>
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/LEXCV-115-linguagem-visual-icones-em-todos-os-botoes-filtros-icone-only/115-UI-SPEC.md
@.planning/phases/LEXCV-115-linguagem-visual-icones-em-todos-os-botoes-filtros-icone-only/115-CONTEXT.md

Exact citations: `115-UI-SPEC.md` → "Out-of-FICO-01-scope modules (ICON-01 still applies)" table + Scope Boundaries §1. EVIDENCE, verified 2026-07-21.

HARD CONSTRAINT (UI-SPEC Scope Boundaries §1 + CONTEXT.md FICO-01 decision): for pareceres/page.tsx L185/L188/L370/L377 (Aplicar/Limpar in the two filter panels) and notificacoes/page.tsx L211 ("Limpar filtros") — add an icon and KEEP the text (icon + text). Do NOT add `size="icon"`, do NOT remove the text, do NOT wrap in `Tooltip`. These five buttons are icon+text, exactly like every other ICON-01 button. This is the deliberate difference between these modules and the FICO-01 modules.

Vocabulary: "Nova Solicitação" (L155)→`Plus`; Aplicar→`Check`; Limpar→`X`; Download (L121)→`Download`; "Submeter Versão" (L461)→`Send`; "Entregar Parecer" (L515)→`Send` (both are "submit to next workflow stage"); create-page Cancelar→`X`; create-page submit (L202)→`Plus` (create solicitação). Icons before text.

Do-not-touch: pareceres/page.tsx L165 Filter, L174 Search — already compliant; notificacoes/page.tsx L131 CheckCheck, L341 Check — already compliant.
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Pareceres list + Notificações — icons WITH TEXT (no icon-only, no Tooltip)</name>
  <files>web/src/app/(dashboard)/pareceres/page.tsx, web/src/app/(dashboard)/notificacoes/page.tsx</files>
  <read_first>
    - web/src/app/(dashboard)/pareceres/page.tsx (imports + L150-190, L365-380)
    - web/src/app/(dashboard)/notificacoes/page.tsx (imports + L205-215)
    - 115-UI-SPEC.md "Out-of-FICO-01-scope modules" table + Scope Boundaries §1
  </read_first>
  <action>
    `pareceres/page.tsx`: extend the existing lucide-react import (already has Filter/Search) with `Plus, Check, X`. L155 "Nova Solicitação"→`Plus` (inside Link if asChild), keep text. L185 Aplicar→`Check`, L188 Limpar→`X` (dual-view panel); L370 Aplicar→`Check`, L377 Limpar→`X` (advanced-search panel) — ALL FOUR keep their visible text, NO `size="icon"`, NO Tooltip. Do not touch L165 Filter / L174 Search.
    `notificacoes/page.tsx`: extend the existing lucide-react import (already has CheckCheck/Check) with `X`. L211 "Limpar filtros"→`X`, keep text, no Tooltip, no icon-only. Do not touch L131 CheckCheck / L341 Check.
  </action>
  <acceptance_criteria>
    - Source assertion: pareceres L185/L188/L370/L377 and notificacoes L211 each render an icon AND retain their Portuguese text child; none has `size="icon"`; none is wrapped in `Tooltip`.
    - Source assertion: "Nova Solicitação" renders `<Plus` with text.
    - Negative assertion: grep for `TooltipTrigger` in these two files returns no NEW occurrence introduced by this task (pareceres/notificacoes are FICO-01-excluded).
    - Untouched: pareceres L165/L174 and notificacoes L131/L341 byte-identical.
  </acceptance_criteria>
  <verify>
    <automated>grep -nE "size=\"icon\"" "web/src/app/(dashboard)/pareceres/page.tsx" "web/src/app/(dashboard)/notificacoes/page.tsx" (expect NONE newly added on the filter buttons) && grep -cE "TooltipTrigger" "web/src/app/(dashboard)/pareceres/page.tsx" "web/src/app/(dashboard)/notificacoes/page.tsx" (expect 0) && grep -nE "<Plus|<Check|<X " "web/src/app/(dashboard)/pareceres/page.tsx"</automated>
  </verify>
  <done>Pareceres + Notificações filter buttons gain icons with text preserved and NO Tooltip/icon-only; Nova Solicitação iconed; compliant buttons untouched; lint clean.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Parecer detail + create pages</name>
  <files>web/src/app/(dashboard)/pareceres/[id]/page.tsx, web/src/app/(dashboard)/pareceres/nova/page.tsx</files>
  <read_first>
    - web/src/app/(dashboard)/pareceres/[id]/page.tsx (imports + L121, L461, L515)
    - web/src/app/(dashboard)/pareceres/nova/page.tsx (imports + L112, L202, L214)
    - 115-UI-SPEC.md "Out-of-FICO-01-scope modules" rows for [id]/page.tsx and nova/page.tsx
  </read_first>
  <action>
    `pareceres/[id]/page.tsx` (add lucide-react import): L121 Download→`Download`; L461 "Submeter Versão"→`Send`; L515 "Entregar Parecer"→`Send`. Keep text; icons before text; preserve the irreversible-delivery confirmation exactly.
    `pareceres/nova/page.tsx` (add lucide-react import): L112 Cancelar→`X`; L202 submit→`Plus`; L214 Cancelar→`X`. Keep text/handlers. Optional `Loader2` pending swap only where an `isSubmitting`/`isPending` boolean is already wired into `disabled`.
  </action>
  <acceptance_criteria>
    - Source assertion: `[id]/page.tsx` imports `Send`+`Download` and renders them on the 3 cited buttons; `nova/page.tsx` renders X/Plus/X on its 3 buttons.
    - Behavior: entrega confirmation unchanged; all buttons keep text.
  </acceptance_criteria>
  <verify>
    <automated>grep -nE "Send|Download" "web/src/app/(dashboard)/pareceres/[id]/page.tsx" && grep -nE "from \"lucide-react\"" "web/src/app/(dashboard)/pareceres/nova/page.tsx" (expect 1) && grep -nE "<Plus|<X " "web/src/app/(dashboard)/pareceres/nova/page.tsx"</automated>
  </verify>
  <done>Parecer detail + create buttons iconed with text preserved; confirmation untouched; imports resolve; lint clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Adds icons to existing client-side buttons. No new input, data flow, network call, or auth/authz change. No icon-only conversion here (FICO-01 excluded), so no accessible-name regression risk — visible text remains the accessible name. Irreversible-delivery confirmation untouched. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-115-01 | Tampering | icon markup in pareceres/notificacoes .tsx | accept | Static compile-time glyph imports; no untrusted input crosses a boundary; entrega confirmation unchanged. |
| T-115-A11Y | WCAG 4.1.2 (a11y) | filter buttons (icon + text, NOT icon-only) | accept | No accessible-name regression: these modules keep visible text as the accessible name (FICO-01 deliberately excluded). No aria-label needed — the text label suffices. |
| T-115-SC | Tampering | npm/pnpm install | accept | No package installed — additional named exports from already-installed `lucide-react`. No legitimacy gate applies. |
</threat_model>

<verification>
- `cd web && pnpm lint` clean on the 4 files.
- Grep proves ZERO new `TooltipTrigger` and no `size="icon"` on the 5 filter buttons (FICO-01 exclusion honored), plus vocabulary icons with text on all 12 gaps.
- No interactive checkpoint needed for these buttons (icon+text, not icon-only) — but they are included in Plan 11's do-not-regress spot-check.
</verification>

<success_criteria>
- Pareceres + Notificações filter buttons carry icons WITH TEXT (no icon-only, no Tooltip) — CONTEXT.md FICO-01 exclusion honored.
- All 12 ICON-01 gaps in these modules closed; compliant buttons untouched; lint clean.
</success_criteria>

<output>
Create `.planning/phases/LEXCV-115-linguagem-visual-icones-em-todos-os-botoes-filtros-icone-only/115-09-SUMMARY.md` when done.
</output>
