---
phase: 74-enum-documento-tipo-bi-nif-restri-o-por-tipo
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/main/java/com/lexcv/models/DocumentoTipo.java
  - backend/src/main/java/com/lexcv/controllers/ResourceController.java
  - backend/migrations/74-cleanup-nif-documento-tipo.sql
autonomous: true
requirements: [CLI-20, CLI-21, CLI-22, CLI-23, CLI-24]
must_haves:
  truths:
    - "DocumentoTipo enum contains BI and no longer contains NIF"
    - "Creating a cliente with an invalid tipo/documento_tipo combination is rejected with HTTP 400 and a Portuguese message"
    - "Updating a cliente with an invalid tipo/documento_tipo combination is rejected with HTTP 400"
    - "A standalone SQL script exists to null out legacy NIF documento_tipo rows before the enum change is deployed"
  artifacts:
    - path: "backend/src/main/java/com/lexcv/models/DocumentoTipo.java"
      provides: "Enum value set BI, CNI, PASSAPORTE, REG_COMERCIAL (NIF removed)"
      contains: "BI"
    - path: "backend/src/main/java/com/lexcv/controllers/ResourceController.java"
      provides: "tipo x documentoTipo combination validation in createCliente and updateCliente"
      contains: "Tipo de documento"
    - path: "backend/migrations/74-cleanup-nif-documento-tipo.sql"
      provides: "Defensive UPDATE that nulls documento_tipo/documento_numero for legacy NIF rows"
      contains: "documento_tipo = 'NIF'"
  key_links:
    - from: "ResourceController.createCliente"
      to: "DocumentoTipo"
      via: "Set.of(DocumentoTipo.CNI, DocumentoTipo.BI, DocumentoTipo.PASSAPORTE) membership check"
      pattern: "DocumentoTipo\\.(CNI|BI|PASSAPORTE|REG_COMERCIAL)"
---

<objective>
Update the backend `DocumentoTipo` enum (add `BI`, remove `NIF`), add tipo/documento_tipo cross-field validation to `createCliente` and `updateCliente`, and provide a standalone defensive SQL cleanup script for legacy `NIF` rows.

Purpose: The identification card in later phases (CLI-19) and the frontend dropdown (Plan 03) depend on the final, validated enum value set. Backend is the source of truth for the value set and the rejection of invalid combinations (CLI-24 backend half).

Output: Modified `DocumentoTipo.java`, modified `ResourceController.java` (two validation blocks), new `backend/migrations/74-cleanup-nif-documento-tipo.sql`.
</objective>

<execution_context>
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/LEXCV-74-enum-documento-tipo-bi-nif-restri-o-por-tipo/74-CONTEXT.md
@.planning/phases/LEXCV-74-enum-documento-tipo-bi-nif-restri-o-por-tipo/74-PATTERNS.md

<interfaces>
<!-- Executor uses these directly — no codebase exploration needed. -->

Current backend/src/main/java/com/lexcv/models/DocumentoTipo.java:
```java
package com.lexcv.models;

public enum DocumentoTipo {
    NIF,
    CNI,
    PASSAPORTE,
    REG_COMERCIAL
}
```

Cliente entity fields (Cliente.java, tipo is a String "PARTICULAR"/"EMPRESA"; documentoTipo is @Enumerated(EnumType.STRING)):
```java
@Enumerated(EnumType.STRING)
@Column(name = "documento_tipo")
private DocumentoTipo documentoTipo;
```

Ad-hoc BAD_REQUEST validation style already used throughout ResourceController.java:
```java
return ResponseEntity.status(HttpStatus.BAD_REQUEST)
        .body(Map.of("message", "<mensagem em português>"));
```
HttpStatus, ResponseEntity, Map, and com.lexcv.models.* are already imported — no new imports needed.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author defensive NIF cleanup SQL script</name>
  <read_first>
    - .planning/phases/LEXCV-74-enum-documento-tipo-bi-nif-restri-o-por-tipo/74-CONTEXT.md (Modelo de Dados & Migração decisions)
    - .planning/phases/LEXCV-74-enum-documento-tipo-bi-nif-restri-o-por-tipo/74-PATTERNS.md ("No Analog Found" section — sequencing + rationale)
    - backend/src/main/resources/application.yml (confirm ddl-auto=update, no Flyway/Liquibase)
  </read_first>
  <files>backend/migrations/74-cleanup-nif-documento-tipo.sql</files>
  <action>
    Create the `backend/migrations/` directory and a new file `74-cleanup-nif-documento-tipo.sql`. The file contains the single statement `UPDATE t_cliente SET documento_tipo = NULL, documento_numero = NULL WHERE documento_tipo = 'NIF';` plus a leading SQL comment header (lines beginning with `--`) that states: (a) this is a one-off defensive cleanup that MUST be run manually against the database (psql or DBeaver) BEFORE deploying the code change that removes `NIF` from `DocumentoTipo.java`; (b) the reason — with `@Enumerated(EnumType.STRING)` Hibernate throws on deserializing any legacy row whose `documento_tipo` value no longer exists in the Java enum; (c) that this is NOT a feature migration (the user's "corte limpo, sem migração de dados" decision stands) — it only nulls crash-inducing legacy values; (d) that no automated migration runner exists in this repo (no Flyway/Liquibase, only `ddl-auto=update`), so execution is manual. Both `documento_tipo` and `documento_numero` are nulled because the number is meaningless without its type.
    Do NOT wire this script into DatabaseSeeder.java or any startup runner — it is explicitly manual (per CONTEXT.md, reusing the SEED_ENABLED seeder is rejected).
  </action>
  <acceptance_criteria>
    - File `backend/migrations/74-cleanup-nif-documento-tipo.sql` exists.
    - It contains the exact predicate `WHERE documento_tipo = 'NIF'` and sets both `documento_tipo = NULL` and `documento_numero = NULL`.
    - It contains a comment header explaining manual execution and the before-deploy sequencing requirement.
    - `grep -rn "74-cleanup" backend/src/` returns no matches (script is not referenced from application code).
  </acceptance_criteria>
  <verify>
    <automated>test -f backend/migrations/74-cleanup-nif-documento-tipo.sql && grep -q "documento_tipo = 'NIF'" backend/migrations/74-cleanup-nif-documento-tipo.sql && grep -q "documento_numero = NULL" backend/migrations/74-cleanup-nif-documento-tipo.sql</automated>
  </verify>
  <done>SQL cleanup script exists with the correct UPDATE statement and manual-execution comment header; not referenced from any Java code.</done>
</task>

<task type="auto">
  <name>Task 2: Update DocumentoTipo enum (add BI, remove NIF)</name>
  <read_first>
    - backend/src/main/java/com/lexcv/models/DocumentoTipo.java (file to modify)
    - backend/src/main/java/com/lexcv/models/TipoCliente.java (analog — flat enum shape, no annotations)
  </read_first>
  <files>backend/src/main/java/com/lexcv/models/DocumentoTipo.java</files>
  <action>
    Edit `DocumentoTipo.java` so the enum constants are exactly `BI, CNI, PASSAPORTE, REG_COMERCIAL` (add `BI`, remove `NIF`, keep `CNI`, `PASSAPORTE`, `REG_COMERCIAL`). Keep the flat structure identical to `TipoCliente.java` — no annotations, no `@JsonProperty`, one constant per line, `package com.lexcv.models;` header unchanged. Ordering is not enforced elsewhere; place `BI` logically before `CNI`.
  </action>
  <acceptance_criteria>
    - `grep -c "BI" backend/src/main/java/com/lexcv/models/DocumentoTipo.java` returns at least 1.
    - `grep -c "NIF" backend/src/main/java/com/lexcv/models/DocumentoTipo.java` returns 0.
    - Enum still contains CNI, PASSAPORTE, REG_COMERCIAL.
  </acceptance_criteria>
  <verify>
    <automated>grep -q "BI" backend/src/main/java/com/lexcv/models/DocumentoTipo.java && ! grep -q "NIF" backend/src/main/java/com/lexcv/models/DocumentoTipo.java && grep -q "REG_COMERCIAL" backend/src/main/java/com/lexcv/models/DocumentoTipo.java</automated>
  </verify>
  <done>Enum contains BI, CNI, PASSAPORTE, REG_COMERCIAL and no NIF; compiles as part of the next build.</done>
</task>

<task type="auto">
  <name>Task 3: Add tipo x documento_tipo validation to createCliente and updateCliente</name>
  <read_first>
    - backend/src/main/java/com/lexcv/controllers/ResourceController.java (createCliente ~lines 218-242, updateCliente ~lines 254-288; ad-hoc BAD_REQUEST analogs at createEvento ~1857-1863 and createProcesso ~922-929)
    - .planning/phases/LEXCV-74-enum-documento-tipo-bi-nif-restri-o-por-tipo/74-CONTEXT.md (Validação e Erros decisions)
    - backend/src/main/java/com/lexcv/models/DocumentoTipo.java (updated enum, from Task 2)
  </read_first>
  <files>backend/src/main/java/com/lexcv/controllers/ResourceController.java</files>
  <action>
    Add an ad-hoc cross-field validation check in BOTH `createCliente` and `updateCliente`. The check: when `documentoTipo != null`, it must be valid for the cliente `tipo` — for `tipo == "PARTICULAR"`, allowed set is `Set.of(DocumentoTipo.CNI, DocumentoTipo.BI, DocumentoTipo.PASSAPORTE)`; for `tipo == "EMPRESA"`, the only allowed value is `DocumentoTipo.REG_COMERCIAL`. If `documentoTipo` is set but the combination is invalid (or tipo is neither PARTICULAR nor EMPRESA while a documentoTipo is present), return `ResponseEntity.status(HttpStatus.BAD_REQUEST).body(Map.of("message", "Tipo de documento inválido para o tipo de cliente selecionado"))`. When `documentoTipo == null`, skip the check (field stays optional).
    In `createCliente`: insert the check at the top of the method body, before `cliente.setTenantId(getTenantId())`, reading from the incoming `cliente` object.
    In `updateCliente`: insert the check after the `cliente == null` NOT_FOUND tenant guard and before the field copies, reading `tipo`/`documentoTipo` from the incoming `payload` object.
    Use the existing `Map.of("message", ...)` + `HttpStatus.BAD_REQUEST` idiom — no structured per-field error (that pattern is deliberately not used here, per CONTEXT.md). Do NOT retroactively validate other existing rows — only the incoming request is validated.
  </action>
  <acceptance_criteria>
    - createCliente and updateCliente each contain a check referencing `DocumentoTipo.REG_COMERCIAL` and the PARTICULAR set (CNI/BI/PASSAPORTE).
    - Invalid-combination path returns HTTP 400 with body `{"message": "Tipo de documento inválido para o tipo de cliente selecionado"}`.
    - `mvn -q -DskipTests compile` succeeds.
    - Behavior: POST /clientes with tipo=EMPRESA and documento_tipo=CNI returns 400; POST with tipo=EMPRESA and documento_tipo=REG_COMERCIAL passes validation; POST with documento_tipo omitted passes validation.
  </acceptance_criteria>
  <verify>
    <automated>cd backend && grep -q "REG_COMERCIAL" src/main/java/com/lexcv/controllers/ResourceController.java && grep -q "Tipo de documento inválido para o tipo de cliente" src/main/java/com/lexcv/controllers/ResourceController.java && mvn -q -DskipTests compile</automated>
  </verify>
  <done>Both create and update reject invalid tipo/documento_tipo combinations with 400 + Portuguese message; backend compiles.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → POST/PUT /clientes | Untrusted request body (tipo, documento_tipo, documento_numero) crosses into persistence |
| DBA → migration SQL | Manually-run script mutates production data before deploy |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-74-01 | Tampering | createCliente/updateCliente accepting invalid documento_tipo for tipo | mitigate | Server-side ad-hoc combination check returns 400 before persist (Task 3); never trusts frontend filtering alone (CLI-24 backend half) |
| T-74-02 | Denial of Service | Legacy NIF rows crash Hibernate deserialization after enum change | mitigate | Standalone SQL script (Task 1) nulls legacy NIF rows; documented to run BEFORE deploying the enum removal |
| T-74-03 | Information Disclosure | Cross-tenant cliente mutation via updateCliente | accept | updateCliente already enforces `cliente.getTenantId().equals(getTenantId())` NOT_FOUND guard (unchanged by this plan); no new surface |
| T-74-04 | Tampering | Malformed/oversized documento_numero | accept | Out of phase scope — no per-type number-format validation requested for this phase (CONTEXT.md); existing optional-string handling unchanged |
| T-74-SC | Tampering | npm/pip/cargo installs | accept | No new package installs in this plan — pure Java edits + one SQL file |
</threat_model>

<verification>
- `mvn -q -DskipTests compile` succeeds with the updated enum and controller.
- Enum contains BI, not NIF (grep).
- Both createCliente and updateCliente reject an EMPRESA+CNI combination with HTTP 400.
- SQL cleanup script exists and is not wired into any runtime code path.
</verification>

<success_criteria>
- DocumentoTipo enum = {BI, CNI, PASSAPORTE, REG_COMERCIAL} (CLI-20, CLI-21 backend).
- Invalid tipo/documento_tipo combinations rejected at create and update with 400 + `{"message": "..."}` (CLI-22, CLI-23, CLI-24 backend).
- Defensive NIF cleanup script authored with manual-execution instructions and correct before-deploy sequencing note.
</success_criteria>

<output>
Create `.planning/phases/LEXCV-74-enum-documento-tipo-bi-nif-restri-o-por-tipo/74-01-SUMMARY.md` when done.
</output>
