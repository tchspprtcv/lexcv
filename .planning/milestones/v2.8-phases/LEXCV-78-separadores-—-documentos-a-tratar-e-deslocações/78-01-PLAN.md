---
phase: 78-separadores-documentos-a-tratar-e-deslocacoes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/app/(dashboard)/clientes/[id]/page.tsx
autonomous: true
requirements: [CLI-30, CLI-31]

must_haves:
  truths:
    - "Ao selecionar o separador 'Documentos a Tratar' em modo edição, o utilizador vê a lista de documentos a tratar (com o botão Adicionar e a lista/estado-vazio), já não um placeholder 'Em breve'"
    - "Ao selecionar o separador 'Deslocações' em modo edição, o utilizador vê a lista de deslocações (descrição/local/data), já não um placeholder 'Em breve'"
    - "O separador 'Dados' já não mostra as secções 'Documentos a Tratar' nem 'Deslocações' (foram movidas para os seus próprios separadores)"
    - "Adicionar um documento a tratar num separador e depois mudar para outro separador fecha e limpa o diálogo 'Adicionar Documento a Tratar' (sem reabrir com texto obsoleto ao voltar)"
    - "Adicionar uma deslocação num separador e depois mudar para outro separador fecha e limpa o diálogo 'Adicionar Deslocação'"
    - "Ambas as listas continuam a ser persistidas no clique de 'Guardar' do cabeçalho, independentemente do separador ativo (comportamento inalterado)"
    - "Em modo leitura, ambas as secções continuam totalmente ocultas (dentro do mesmo isEditing-gate de hoje), sem introduzir uma vista read-only nova"
  artifacts:
    - path: "web/src/app/(dashboard)/clientes/[id]/page.tsx"
      provides: "Relocated 'Documentos a Tratar' and 'Deslocações' JSX blocks into their own tab branches, isEditing-gated; extended dialog-reset useEffect"
      contains: 'tab === "documentosATratar"'
  key_links:
    - from: 'tab === "documentosATratar"'
      to: "documentosATratar list JSX + confirmAddDocATratar + addDocATratarModal"
      via: "conditional tab branch renders the relocated block instead of PlaceholderEmBreve"
      pattern: 'tab === "documentosATratar" \?'
    - from: 'tab === "deslocacoes"'
      to: "deslocacoes list JSX + confirmAddDeslocacao + addDeslocacaoModal"
      via: "conditional tab branch renders the relocated block instead of PlaceholderEmBreve"
      pattern: 'tab === "deslocacoes" \?'
    - from: "dialog-reset useEffect"
      to: "addDocATratarModal / addDeslocacaoModal"
      via: "per-dialog tab conditions (tab !== documentosATratar / tab !== deslocacoes)"
      pattern: 'tab !== "documentosATratar"'
---

<objective>
Relocate the existing "Documentos a Tratar" and "Deslocações" list sections out of the `tab === "dados"` branch and into their own dedicated tab branches (`documentosATratar` / `deslocacoes`), replacing the two `PlaceholderEmBreve` placeholders introduced in Phase 76. Extend the Phase 76 CR-01 dialog-reset `useEffect` so each moved dialog resets against its own tab key.

This is a pure JSX/state relocation within ONE file — same markup, same hooks, same handlers, same copy, byte-for-byte. Zero new fields, zero new data, zero behavior change. The isEditing gate and the `canEditClientes && editable` gating that carries today are preserved exactly.

Purpose: Closes CLI-30 (Documentos a Tratar isolado no seu separador) and CLI-31 (Deslocações isolado no seu separador), completing the tab restructuring for the client ficha.
Output: Updated `web/src/app/(dashboard)/clientes/[id]/page.tsx` — two tab branches now render real content; `documentosEntregues` branch stays `PlaceholderEmBreve` (Phase 79 scope).
</objective>

<execution_context>
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/LEXCV-78-separadores-—-documentos-a-tratar-e-deslocações/78-CONTEXT.md
@.planning/phases/LEXCV-78-separadores-—-documentos-a-tratar-e-deslocações/78-PATTERNS.md
@.planning/phases/LEXCV-78-separadores-—-documentos-a-tratar-e-deslocações/78-UI-SPEC.md

<interfaces>
<!-- Contracts already in the file the executor is editing. No codebase exploration needed. -->

TabKey union (page.tsx, lines ~77-84) — already includes both target keys, NO type change needed:
  type TabKey = "dados" | "contactosNotas" | "processos" | "pareceres" | "documentosEntregues" | "documentosATratar" | "deslocacoes"

State (page.tsx, lines ~186-202) — declared at ClienteDetailContent scope, DO NOT MOVE:
  documentosATratar: DocumentoATratar[]        (setDocumentosATratar)
  deslocacoes: Deslocacao[]                     (setDeslocacoes)
  addDocATratarModal: boolean                   (setAddDocATratarModal)
  newDocATratar: { descricao: string }          (setNewDocATratar)
  addDeslocacaoModal: boolean                   (setAddDeslocacaoModal)
  newDeslocacao: { descricao; local; data }     (setNewDeslocacao)

Handlers (page.tsx, lines ~275-287) — component-scope, DO NOT MOVE:
  confirmAddDocATratar()   — validates newDocATratar.descricao.trim(), appends, resets, closes modal
  confirmAddDeslocacao()   — validates newDeslocacao.descricao.trim(), appends, resets, closes modal

Persistence — onSubmit (lines ~313-314), onCancel (lines ~337-338), load-effect (lines ~263-265)
  all read/write documentosATratar/deslocacoes OUTSIDE the tab-conditional JSX. DO NOT TOUCH.

Sibling analog for wrapper shape — ClienteProcessosTab (lines ~1123-1192) / ClienteParecerTab (~1213+)
  render their content inside <Card><CardContent>...</CardContent></Card>.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Relocate "Documentos a Tratar" and "Deslocações" blocks into their own tab branches + extend dialog-reset useEffect</name>
  <files>web/src/app/(dashboard)/clientes/[id]/page.tsx</files>
  <read_first>
    - web/src/app/(dashboard)/clientes/[id]/page.tsx (the ENTIRE file — you are moving JSX between two distant regions and editing a useEffect; read it fully before touching anything to confirm current line numbers, since edits shift line numbers)
    - .planning/phases/LEXCV-78-separadores-—-documentos-a-tratar-e-deslocações/78-PATTERNS.md (Sections B, C, D, E, F — exact current JSX and the discrepancy resolution)
    - .planning/phases/LEXCV-78-separadores-—-documentos-a-tratar-e-deslocações/78-CONTEXT.md (Estado Vazio & Visibilidade decision — corrected)
    - .planning/phases/LEXCV-78-separadores-—-documentos-a-tratar-e-deslocações/78-UI-SPEC.md (Interaction Contract section)
  </read_first>
  <action>
    This is a cut-and-paste relocation inside ONE file, plus a useEffect widening. Do NOT change any markup, class names, copy, field set, or handler logic — the visual/behavioral output must be byte-for-byte identical, only its parent conditional branch changes.

    Step 1 — Cut the "Documentos a Tratar" JSX block. Locate the block that starts at the `{/* Documentos a Tratar */}` comment (currently ~line 890) and runs through the closing `</div>` of that section's outer `<div className="space-y-2">` (currently ~line 939). This block currently lives inside the "Intake do Caso" Card (inside the `{isEditing ? ( ... ) : null}` gate at ~line 774). Remove it from that location.

    Step 2 — Cut the "Deslocações" JSX block. Locate the block starting at `{/* Deslocações */}` (currently ~line 941) through its closing `</div>` (currently ~line 1013). Remove it from the "Intake do Caso" Card too.

    Step 3 — Replace the two placeholder branches. In the tab ternary chain (currently ~lines 1080-1083), replace `tab === "documentosATratar" ? ( <PlaceholderEmBreve /> )` with a branch that renders the relocated "Documentos a Tratar" block, and replace `tab === "deslocacoes" ? ( <PlaceholderEmBreve /> )` with a branch that renders the relocated "Deslocações" block. Leave the `tab === "documentosEntregues" ? ( <PlaceholderEmBreve /> )` branch (currently ~line 1078-1079) UNTOUCHED — that is Phase 79 scope. Leave the final `: null}` untouched.

    Step 4 — Preserve the isEditing gate exactly. Per the corrected CONTEXT.md decision (and PATTERNS.md Section F resolution option 2): both relocated blocks must stay gated by `{isEditing ? ( ... ) : null}` exactly as they are today — in read mode, both sections render NOTHING. Do NOT introduce a new always-visible read-only list. So inside each new tab branch, wrap the relocated block in `isEditing ? ( <Card><CardContent>...relocated block...</CardContent></Card> ) : null`. Match the sibling wrapper shape used by ClienteProcessosTab/ClienteParecerTab (`<Card><CardContent>`). Wrapper padding/spacing className is your discretion as long as visual output matches today (the relocated block's own `<div className="space-y-2">` already carries internal spacing; a plain `<CardContent>` or `<CardContent className="pt-6">` is fine).

    Step 5 — Extend the dialog-reset useEffect (currently ~lines 211-220). Today it resets all three intake dialogs whenever `tab !== "dados"`. Split the guard into per-dialog conditions: keep `addDocEntreModal`/`newDocEntre` resetting on `tab !== "dados"` (unchanged — Documentos Entregues stays in Dados until Phase 79); reset `addDocATratarModal` + `setNewDocATratar({ descricao: "" })` when `tab !== "documentosATratar"`; reset `addDeslocacaoModal` + `setNewDeslocacao({ descricao: "", local: "", data: "" })` when `tab !== "deslocacoes"`. Keep the dependency array `[tab]`. One effect with three per-dialog `if` blocks is the clean shape.

    Do NOT move the state declarations (~lines 186-202), the confirm handlers (~lines 275-287), or any onSubmit/onCancel/load-effect references — those stay at component scope because both lists are staged locally and persisted on the parent form's "Guardar" regardless of active tab. Do NOT add a `data` field to Documentos a Tratar (its only field is `descricao` — confirmed). Do NOT delete the `PlaceholderEmBreve` function (still used by the documentosEntregues branch).
  </action>
  <acceptance_criteria>
    - `pnpm --dir web lint` passes with no new errors/warnings introduced by this change
    - `pnpm --dir web build` (or `pnpm --dir web exec tsc --noEmit`) succeeds — no TypeScript errors, no unused-variable errors (all moved state/handlers still referenced)
    - Source assertion: `grep -n 'tab === "documentosATratar"' page.tsx` shows the branch now renders the relocated block, NOT `<PlaceholderEmBreve />`
    - Source assertion: `grep -c 'PlaceholderEmBreve' page.tsx` shows the count dropped by exactly 2 (from 3 placeholder call-sites to 1 — only documentosEntregues remains; the function definition still present so total references drop from 4 to 2)
    - Source assertion: `grep -n 'tab !== "documentosATratar"' page.tsx` and `grep -n 'tab !== "deslocacoes"' page.tsx` both match inside the dialog-reset useEffect
    - Source assertion: the `{/* Documentos a Tratar */}` and `{/* Deslocações */}` comment markers no longer appear inside the "Intake do Caso" Card region (they now appear inside the new tab branches)
    - Behavior (read mode): with `isEditing === false`, the "Documentos a Tratar" and "Deslocações" tab branches render nothing (still wrapped in `isEditing ? (...) : null`) — matches today's hidden-in-read-mode behavior, NO new read-only list added
    - Source assertion: no `data` field added to `newDocATratar` — it remains `{ descricao: string }`
    - The "Dados" tab no longer contains the two relocated sections; the `serverError` line and Documentos Entregues section remain in the Dados/Intake card untouched
  </acceptance_criteria>
  <verify>
    <automated>cd web && pnpm exec tsc --noEmit && pnpm lint</automated>
  </verify>
  <done>Both sections render inside their own tab branches (isEditing-gated), placeholders removed for those two tabs, dialog-reset useEffect resets each dialog against its own tab key, documentosEntregues placeholder + all state/handlers/persistence untouched, tsc and lint clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → this page component | User is already authenticated (page gated by session + `canEditClientes` for edit mode). No new network boundary is crossed — this is a client-side JSX relocation within one already-authorized page. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-78-01 | Information Disclosure | Relocated "Documentos a Tratar" / "Deslocações" sections | mitigate | Preserve the exact `isEditing ? (...) : null` gate on both relocated blocks so read-mode visibility is byte-for-byte identical to today. Verify via acceptance criteria that no data becomes visible in read mode that was hidden before, and nothing that was visible becomes hidden. No data field set changes. |
| T-78-02 | Elevation of Privilege | "Adicionar" / remove ("✕") CRUD affordances | mitigate | Keep the existing `canEditClientes && editable` gating carried by the moved JSX unchanged — relocation must not drop or weaken the gate. Backend `@PreAuthorize` on the underlying cliente update endpoint is unchanged and remains authoritative; this UI gate is defense-in-depth, not the sole control. |
| T-78-03 | Tampering | npm/pip/cargo installs | accept | No package installs in this phase — zero new dependencies, zero `package.json` changes. No supply-chain surface introduced. |
</threat_model>

<verification>
- `cd web && pnpm exec tsc --noEmit` — no type errors
- `cd web && pnpm lint` — no new lint errors
- Manual (during human-verify, optional): enter edit mode, open "Documentos a Tratar" tab → add an item → switch to "Deslocações" tab and back → dialog is closed and draft cleared; "Guardar" persists both lists regardless of active tab.
</verification>

<success_criteria>
- Separador "Documentos a Tratar" renders the real list (isEditing-gated), not a placeholder — CLI-30 satisfied
- Separador "Deslocações" renders the real list (descrição/local/data, isEditing-gated), not a placeholder — CLI-31 satisfied
- Dialog-reset useEffect resets each moved dialog against its own tab key; documentosEntregues dialog keeps its `tab !== "dados"` condition
- No behavior/field/data changes; `documentosEntregues` branch and all state/handlers/persistence untouched
- tsc and lint clean
</success_criteria>

<output>
Create `.planning/phases/LEXCV-78-separadores-—-documentos-a-tratar-e-deslocações/78-01-SUMMARY.md` when done
</output>
