---
phase: 75-componente-nico-view-edit
plan: 02
type: execute
wave: 2
depends_on: ["75-01"]
files_modified:
  - web/src/app/(dashboard)/clientes/[id]/page.tsx
autonomous: true
requirements: [CLI-13]
must_haves:
  truths:
    - "In view mode, the Adicionar/Editar/Remover affordances on Contactos, Notas, Advogados, Administrativos, and Procuração are hidden"
    - "In edit mode (and only with clientes:edit permission), those same affordances are visible and functional"
    - "The Ver/Download button on Procuração remains visible in both modes"
  artifacts:
    - path: "web/src/app/(dashboard)/clientes/[id]/page.tsx"
      provides: "Four inline sub-components gated by an editable prop ANDed with canEditClientes"
      contains: "canEditClientes && editable"
  key_links:
    - from: "ClienteDetailContent"
      to: "ClienteContactosCard/ClienteNotasCard/ResponsaveisCard/ProcuracaoCard"
      via: "editable={isEditing} prop passed at each call site"
      pattern: "editable=\\{isEditing\\}"
---

<objective>
Add an `editable: boolean` prop to the four inline sub-components inside `[id]/page.tsx` (`ClienteContactosCard`, `ClienteNotasCard`, `ResponsaveisCard`, `ProcuracaoCard`) and AND-gate every inline CRUD affordance on `canEditClientes && editable`. Pass `editable={isEditing}` from the parent so these sub-cards' add/edit/remove controls are hidden in view mode and shown in edit mode.

Purpose: Completes CLI-13 for the sub-component CRUD affordances not covered by plan 75-01 (which handled the main form fields). Ensures ALL edit controls are inactive/hidden in read mode, per success criterion 3 of the phase.

Output: The four sub-components accept `editable` and hide their edit affordances when it is false; call sites pass `editable={isEditing}`.
</objective>

<execution_context>
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/LEXCV-75-componente-nico-view-edit/75-CONTEXT.md
@.planning/phases/LEXCV-75-componente-nico-view-edit/75-PATTERNS.md
@.planning/phases/LEXCV-75-componente-nico-view-edit/75-UI-SPEC.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add editable prop and AND-gate Contactos + Notas cards</name>
  <read_first>
    - web/src/app/(dashboard)/clientes/[id]/page.tsx (post-75-01 merged file; ClienteContactosCard and ClienteNotasCard inline function bodies)
    - .planning/phases/LEXCV-75-componente-nico-view-edit/75-PATTERNS.md (ClienteContactosCard / ClienteNotasCard sections — exact affordance line references)
    - .planning/phases/LEXCV-75-componente-nico-view-edit/75-UI-SPEC.md (Interaction Contract section 3 — AND semantics, hide-not-disable, editable naming)
  </read_first>
  <action>
    Add editable: boolean to the props type of both ClienteContactosCard and ClienteNotasCard inline functions. In ClienteContactosCard, change every existing {canEditClientes ? (...) : null} guard around edit affordances to {canEditClientes &amp;&amp; editable ? (...) : null}: the "Adicionar" input row + button, and the per-item Editar/Remover/Guardar/Cancelar buttons. In ClienteNotasCard, apply the identical change to the "Adicionar nota" block and the per-item Editar/Remover/Guardar/Cancelar buttons. Use the positive `editable` naming (not readOnly) consistently. Keep the hide-not-disable pattern — do NOT introduce a disabled/grayed visual state. At the two call sites in ClienteDetailContent, add editable={isEditing} alongside the existing canEditClientes={canEditClientes} prop.
  </action>
  <verify>
    <automated>cd web &amp;&amp; pnpm tsc --noEmit 2>&amp;1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - source: both ClienteContactosCard and ClienteNotasCard prop types declare `editable: boolean` (grep `editable: boolean`).
    - source: no bare `{canEditClientes ? (` guard remains around an Adicionar/Editar/Remover affordance inside these two components — each is now `canEditClientes && editable` (grep `canEditClientes && editable` returns matches; grep confirms no orphaned bare guard on those affordances).
    - source: both call sites pass `editable={isEditing}` (grep count of `editable={isEditing}` is at least 2 after this task).
    - behavior: `cd web && pnpm tsc --noEmit` passes.
  </acceptance_criteria>
  <done>Contactos and Notas cards hide their add/edit/remove controls unless canEditClientes AND editable; call sites pass editable={isEditing}. tsc passes.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add editable prop and AND-gate Responsaveis + Procuracao cards</name>
  <read_first>
    - web/src/app/(dashboard)/clientes/[id]/page.tsx (post-75-01 merged file; ResponsaveisCard and ProcuracaoCard inline function bodies and their call sites)
    - .planning/phases/LEXCV-75-componente-nico-view-edit/75-PATTERNS.md (ResponsaveisCard / ProcuracaoCard sections — exact affordance line references and the Ver/Download exception)
    - .planning/phases/LEXCV-75-componente-nico-view-edit/75-UI-SPEC.md (Interaction Contract section 3 — Ver/Download stays visible in both modes)
  </read_first>
  <action>
    Add editable: boolean to the props type of ResponsaveisCard and ProcuracaoCard. In ResponsaveisCard, change {canEditClientes ? (...) : null} to {canEditClientes &amp;&amp; editable ? (...) : null} on the "Adicionar" button (opens modal) and the per-item Remover button; leave the Dialog's own internal Cancelar/Adicionar buttons ungated (they are only reachable if the already-gated trigger rendered). In ProcuracaoCard, AND-gate: the Remover button, the "Substituir ficheiro" FileDropZone block, and the initial-upload FileDropZone branch (when no procuração exists — the {canEditClientes ? &lt;FileDropZone/&gt; : &lt;div&gt;Sem procuração carregada.&lt;/div&gt;} becomes {canEditClientes &amp;&amp; editable ? &lt;FileDropZone/&gt; : &lt;div&gt;Sem procuração carregada.&lt;/div&gt;}). Do NOT gate the "Ver / Download" button — it is a read action visible in both modes per UI-SPEC.md section 3. FileDropZone itself (web/src/components/shared/file-drop-zone.tsx) needs NO changes. At the call sites in ClienteDetailContent — the two ResponsaveisCard calls (Advogados and Administrativos) and the ProcuracaoCard call — add editable={isEditing} alongside the existing canEditClientes={canEditClientes}.
  </action>
  <verify>
    <automated>cd web &amp;&amp; pnpm tsc --noEmit 2>&amp;1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - source: ResponsaveisCard and ProcuracaoCard prop types declare `editable: boolean`.
    - source: the ProcuracaoCard "Ver / Download" button is NOT wrapped in an `editable` guard (it remains visible in view mode).
    - source: all sub-component call sites in ClienteDetailContent now pass `editable={isEditing}` (grep count of `editable={isEditing}` is 5 total across the file: Contactos, Notas, Advogados, Administrativos, Procuração).
    - source: web/src/components/shared/file-drop-zone.tsx is unchanged (no diff).
    - behavior: `cd web && pnpm tsc --noEmit` passes.
  </acceptance_criteria>
  <done>Responsaveis and Procuracao cards hide their add/remove/upload controls unless canEditClientes AND editable, while Ver/Download stays visible; all 5 call sites pass editable={isEditing}. tsc passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client UI edit gate → server authorization | The new `editable` prop is a UX/visual gate ANDed with the existing `canEditClientes` RBAC flag; neither is the real authorization boundary. Each sub-resource mutation (contacto/nota/responsável/procuração add/remove) is authorized server-side by its own `@PreAuthorize` on the backend endpoint. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-75-04 | Elevation of Privilege | `editable` prop on the four sub-cards | accept | `editable` is a pure UX toggle that only hides affordances; it is ANDed with (never replaces) the existing `canEditClientes` RBAC gate, and every underlying mutation remains authorized server-side by its endpoint's `@PreAuthorize`. Forcing `editable=true` in devtools reveals buttons but cannot bypass RBAC or server authorization. This must not be mistaken for an authorization mechanism — it is visual gating only. |
| T-75-05 | Information Disclosure | Ver/Download button on Procuração | accept | Ver/Download remains ungated by `editable` (read action, both modes) but is still gated by `canEditClientes` as today AND authorized server-side by the presigned-download endpoint's `@PreAuthorize`; no change to the disclosure surface. |
| T-75-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages installed by this plan; nothing to audit. |
</threat_model>

<verification>
- `cd web && pnpm tsc --noEmit` passes.
- `cd web && pnpm lint` passes for the modified file.
- grep confirms exactly 5 `editable={isEditing}` call sites and no bare `canEditClientes ?` guards remain around edit affordances (except the intentionally ungated Ver/Download).
- Manual/visual (deferred to human_verify): in view mode all add/edit/remove buttons on the four cards are hidden; in edit mode they appear (for a user with clientes:edit); Ver/Download is visible in both modes.
</verification>

<success_criteria>
- CLI-13 (sub-component portion): all inline CRUD affordances on Contactos, Notas, Advogados, Administrativos, and Procuração are hidden in view mode and shown in edit mode, ANDed with existing RBAC. Ver/Download stays visible in both modes.
</success_criteria>

<output>
Create `.planning/phases/LEXCV-75-componente-nico-view-edit/75-02-SUMMARY.md` when done.
</output>
