---
milestone: v2.18
audited: 2026-09-24
status: passed
---

# Milestone v2.18 Audit: Rebrand ALCv → LexCV

## Requirement Coverage

9/9 v1 requirements satisfied, mapped 1:1 to phases, zero orphans:

| Requirement | Phase | Status |
|---|---|---|
| IDENT-01 | 129 | ✓ Satisfied |
| IDENT-02 | 129 | ✓ Satisfied |
| IDENT-03 | 129 | ✓ Satisfied |
| FRONT-01 | 130 | ✓ Satisfied |
| FRONT-02 | 130 | ✓ Satisfied |
| FRONT-03 | 130 | ✓ Satisfied |
| BACK-01 | 131 | ✓ Satisfied |
| BACK-02 | 131 | ✓ Satisfied |
| CONFIG-01 | 132 | ✓ Satisfied |

## Phase Verification Summary

| Phase | Verdict | Notes |
|---|---|---|
| 129 — Identidade e Documentação | passed | 4 docs técnicos + manual (md/html/pptx regenerados) + 8 docs comerciais (6 `.docx` regenerados) |
| 130 — Frontend — Marca LexCV | passed | `web/`+`webpage/` build limpo, 45/45 asserções de 3 scripts de verificação PASS |
| 131 — Backend — Tenant Reservado LexCV | passed | 432/432 testes backend, SpotBugs limpo, migração SQL `131` documentada |
| 132 — Configuração | passed | `mvn package` limpo |

## Cross-Phase Integration Check

- **Frontend↔backend consistency (Phase 130 ↔ Phase 131):** `web/src/app/(dashboard)/plataforma/columns.tsx`'s `TENANT_RESERVADO` and the backend's reserved-tenant literal (`DatabaseSeeder`, `PlatformAdminController`, `MigracaoPapeisEscritorioService`) are both `"LexCV"` now — confirmed by direct grep of both, zero drift between the two independently-defined literals.
- **Domain preservation consistency (Phase 129/130/132):** the real production domain `alcv.tech`/`www.alcv.tech` was left unchanged in all 5 files where it appears (`DEPLOYMENT.md`, `docs/MANUAL_DO_UTILIZADOR.md`, `docs/html/build.py`, `docs/pptx/build.js`, `docker-compose.hostinger.yml`) — same decision applied consistently across every phase that touched a file containing it.
- **Migration ordering (Phase 131):** the new `131-rename-tenant-reservado-lexcv.sql` is correctly sequenced after `128` in `backend/migrations/README.md`'s Path B table (item 18/18) and the re-run-safety table (now 8/18 idempotent scripts).

## Full-Repo Final Sweep

```
grep -rli -i "alcv" . | grep -v -E "node_modules|\.git/|\.next/|target/|dist/|\.planning/milestones/|\.planning/research/|\.planning/MILESTONES\.md|\.planning/RETROSPECTIVE\.md|\.planning/v2\.(3|5|16)-"
```

Every remaining hit is one of:
1. This milestone's own phase artifacts and planning docs (PROJECT.md/REQUIREMENTS.md/ROADMAP.md/STATE.md/phases/) discussing the rename itself — expected, not a gap.
2. The 5 files where the real production domain `alcv.tech` was deliberately preserved (Phase 129/132 decision) — expected, not a gap.
3. `backend/migrations/120b-backfill-tenant-plano.sql`'s comment, describing what that migration did *at the time it ran* (referencing the then-current "ALCv" tenant name) — historical execution record, same category as `.planning/milestones/*`, correctly left untouched; this script was not in REQUIREMENTS.md's scope (only the new `131` migration was).
4. Two genuine out-of-scope findings, below.

## Findings Outside Declared Scope — Resolved Before Close

Discovered during the final sweep, **not** part of REQUIREMENTS.md's declared scope (which
enumerated exact files per requirement). The user chose to address both before completing the
milestone rather than accept them as debt:

1. **`.docs/manual_utilizador_lexcv.md`** — a second, separate user manual (with its own
   `screenshots/` folder) distinct from `docs/MANUAL_DO_UTILIZADOR.md` (the one IDENT-02 scoped).
   **Fixed:** rebranded ALCv → LexCV, same pattern as Phase 129.
2. **`business/apresentacoes/apresentacao-alcv.pptx`** — flagged as blocked in
   `.planning/TAREFAS.md` task T-003 (owned by the separate `lexcv-coordenador` multi-agent
   workflow, not this GSD milestone system): "ALCV" é um cliente, uma marca anterior, ou lixo?
   **Resolved:** direct content comparison against `apresentacao-lexcv.pptx` (21 slides "ALCV"
   throughout vs. 20 slides, zero ALCv mentions, same deck already fully rebranded) confirmed
   "ALCV" is this product's previous brand — not a client, not garbage — and the LexCV version is
   already its complete replacement. Obsolete `apresentacao-alcv.pptx` removed (`git rm`,
   recoverable from git history); T-003 closed in `.planning/TAREFAS.md`.

A fresh full-repo sweep after both fixes confirms zero remaining "ALCv" outside this milestone's
own planning artifacts, the preserved production domain, and the one historical migration comment
noted above.

## Build/Test Health

- `web/`: `pnpm build` clean (27 routes), 3 verify scripts 45/45 PASS.
- `webpage/`: `pnpm build` clean (2 routes).
- `backend/`: `mvn test` 432/432 pass, `mvn spotbugs:check` clean, `mvn package` clean.
- **Environment caveat:** this container has no JDK 23 installed (network policy blocks fetching one). All backend Maven commands were run with `-Dmaven.compiler.release=21` passed on the command line only — `backend/pom.xml`'s declared `<java.version>23</java.version>` was never altered for this purpose (Phase 132 only touched `<description>`). Recommend confirming green on CI (which runs real JDK 23) before considering the backend changes fully proven, though nothing in this milestone's diff uses any Java 23-specific language feature — it is string-literal-only.

## Verdict

**Status: `passed`** — all 9 requirements satisfied, all 4 phases passed, zero regressions, full
test/build suite green, and both out-of-scope findings from the final sweep were resolved before
close at the user's request (not deferred as debt). Residual caveat, not a defect: this session's
container has no JDK 23, so backend verification ran with `-Dmaven.compiler.release=21` on the
command line only (`pom.xml` untouched) — recommend confirming green on CI, which runs real JDK 23.
