---
phase: "57"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/main/java/com/lexcv/models/TipoCliente.java
  - backend/src/main/java/com/lexcv/models/DadosTipo.java
  - backend/src/main/java/com/lexcv/models/DadosTipoConverter.java
  - backend/src/main/java/com/lexcv/models/Cliente.java
  - backend/src/main/java/com/lexcv/repositories/ClienteRepository.java
autonomous: true
requirements: [PERF-01, PERF-03, PERF-04, PART-01, PART-02, EMP-01]

must_haves:
  truths:
    - "Cliente entity has numero_sequencial (Integer) and numero_cliente (String) fields persisted to t_cliente"
    - "Cliente entity has avencado (Boolean) persisted to t_cliente"
    - "Cliente entity has dados_tipo (TEXT) persisted to t_cliente, deserialized to DadosTipo POJO"
    - "TipoCliente enum with PARTICULAR and EMPRESA values exists and is used on tipo field"
    - "ClienteRepository has findMaxNumeroSequencialByTenantId query scoped strictly per tenant"
  artifacts:
    - path: "backend/src/main/java/com/lexcv/models/TipoCliente.java"
      provides: "Enum PARTICULAR | EMPRESA"
    - path: "backend/src/main/java/com/lexcv/models/DadosTipo.java"
      provides: "POJO for type-specific fields (particular + empresa)"
    - path: "backend/src/main/java/com/lexcv/models/DadosTipoConverter.java"
      provides: "JPA AttributeConverter DadosTipo <-> JSON String"
    - path: "backend/src/main/java/com/lexcv/models/Cliente.java"
      provides: "Extended entity with all new fields"
    - path: "backend/src/main/java/com/lexcv/repositories/ClienteRepository.java"
      provides: "MAX+1 tenant-scoped query"
  key_links:
    - from: "Cliente.java"
      to: "DadosTipoConverter.java"
      via: "@Convert(converter = DadosTipoConverter.class) on dadosTipo field"
    - from: "ClienteRepository.java"
      to: "t_cliente.numero_sequencial"
      via: "@Query SELECT MAX(c.numeroSequencial) FROM Cliente c WHERE c.tenantId = :tenantId"
---

<objective>
Extend the Cliente entity and repository with all new fields required by Phase 57: numero_cliente generation support, avencado flag, TipoCliente enum type safety, and JSON-backed dados_tipo column.

Purpose: Establishes the data layer contracts that the controller (Plan 57-02) will orchestrate. Without these, numero_cliente cannot be generated and type-specific fields cannot be persisted.

Output: TipoCliente enum, DadosTipo POJO, DadosTipoConverter, extended Cliente entity, extended ClienteRepository.
</objective>

<execution_context>
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/LEXCV-57-backend-schema-api/57-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create TipoCliente enum and DadosTipo POJO with JPA converter</name>
  <files>
    backend/src/main/java/com/lexcv/models/TipoCliente.java
    backend/src/main/java/com/lexcv/models/DadosTipo.java
    backend/src/main/java/com/lexcv/models/DadosTipoConverter.java
  </files>
  <read_first>
    - backend/src/main/java/com/lexcv/models/DocumentoTipo.java (enum pattern to replicate)
    - backend/src/main/java/com/lexcv/models/Cliente.java (understand current package and imports)
  </read_first>
  <action>
    Create three files in package com.lexcv.models.

    TipoCliente.java: plain enum with values PARTICULAR and EMPRESA. No annotations needed beyond the package declaration.

    DadosTipo.java: Jackson-serializable POJO with @JsonInclude(JsonInclude.Include.NON_NULL) at class level. Fields (all nullable):
    - Integer idade
    - String sexo
    - String nacionalidade
    - String biPassaporte
    - String nomeComercial
    - String nif (empresa NIF — distinct from the Cliente.nif field)
    - String sede
    - String representanteLegal
    - String cargoRepresentante

    Use Lombok @Data @NoArgsConstructor @AllArgsConstructor @Builder on DadosTipo. Import com.fasterxml.jackson.annotation.JsonInclude.

    DadosTipoConverter.java: implements AttributeConverter<DadosTipo, String> with @Converter annotation. Use ObjectMapper for both convertToDatabaseColumn (serialize to JSON String, return null on null input or exception) and convertToEntityAttribute (deserialize from JSON String, return null on null/blank input or exception). Declare a private static final ObjectMapper MAPPER = new ObjectMapper(). Import jakarta.persistence.AttributeConverter, jakarta.persistence.Converter, com.fasterxml.jackson.databind.ObjectMapper.
  </action>
  <verify>
    <automated>cd backend && mvn -pl . compile -q 2>&amp;1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - TipoCliente.java exists at backend/src/main/java/com/lexcv/models/TipoCliente.java with enum values PARTICULAR and EMPRESA
    - DadosTipo.java has all 9 fields listed above; class annotated with @JsonInclude(JsonInclude.Include.NON_NULL)
    - DadosTipoConverter.java implements AttributeConverter<DadosTipo, String> and is annotated with @Converter
    - mvn compile exits 0 with no errors referencing these three files
  </acceptance_criteria>
  <done>Three new files compile cleanly; TipoCliente, DadosTipo, DadosTipoConverter are importable from com.lexcv.models</done>
</task>

<task type="auto">
  <name>Task 2: Extend Cliente entity and ClienteRepository</name>
  <files>
    backend/src/main/java/com/lexcv/models/Cliente.java
    backend/src/main/java/com/lexcv/repositories/ClienteRepository.java
  </files>
  <read_first>
    - backend/src/main/java/com/lexcv/models/Cliente.java (MUST read current state before editing)
    - backend/src/main/java/com/lexcv/repositories/ClienteRepository.java (MUST read current state before editing)
    - backend/src/main/java/com/lexcv/models/DadosTipoConverter.java (just created — verify converter class name)
  </read_first>
  <action>
    Edit Cliente.java — add four new fields after the existing `detalhesAdicionais` field and before `createdAt`:

    1. `@Column(name = "numero_sequencial") private Integer numeroSequencial;` — used for MAX+1 calculation per D-02
    2. `@Column(name = "numero_cliente", length = 20) private String numeroCliente;` — stored formatted string CLI-XXXX per D-01
    3. `@Column(name = "avencado") private Boolean avencado;` — per D-08
    4. `@Column(name = "dados_tipo", columnDefinition = "TEXT") @Convert(converter = DadosTipoConverter.class) private DadosTipo dadosTipo;` — per D-06, D-07

    Do NOT change the existing `tipo` (String) field — it stays as String per D-04. The `tipo` field will hold values "PARTICULAR" or "EMPRESA" at runtime (D-05 enum is used in controller logic, not on the entity field itself, to avoid breaking the existing string-based filtering in listClientes).

    Add imports: com.lexcv.models.DadosTipoConverter, jakarta.persistence.Convert.

    Do NOT change @PrePersist — numero_cliente generation happens in the controller per D-03 (before save).

    Edit ClienteRepository.java — add one method using @Query:
    `@Query("SELECT MAX(c.numeroSequencial) FROM Cliente c WHERE c.tenantId = :tenantId") Optional<Integer> findMaxNumeroSequencialByTenantId(@Param("tenantId") UUID tenantId);`

    Add imports: org.springframework.data.jpa.repository.Query, org.springframework.data.repository.query.Param, java.util.Optional.
  </action>
  <verify>
    <automated>cd backend && mvn -pl . compile -q 2>&amp;1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - Cliente.java has fields numeroSequencial (Integer, column "numero_sequencial"), numeroCliente (String, column "numero_cliente"), avencado (Boolean, column "avencado"), dadosTipo (DadosTipo, column "dados_tipo", @Convert with DadosTipoConverter)
    - ClienteRepository.java has findMaxNumeroSequencialByTenantId returning Optional<Integer> with @Query annotation
    - mvn compile exits 0
    - grep "numero_sequencial" backend/src/main/java/com/lexcv/models/Cliente.java returns a match
    - grep "findMaxNumeroSequencialByTenantId" backend/src/main/java/com/lexcv/repositories/ClienteRepository.java returns a match
  </acceptance_criteria>
  <done>Cliente entity has all 4 new fields; ClienteRepository has MAX+1 query scoped by tenantId; full compile succeeds</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API → DB | dados_tipo JSON written to TEXT column in t_cliente |
| Tenant context → Repository | MAX+1 query must be scoped per tenant to prevent cross-tenant sequence leak |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-57-01 | Information Disclosure | findMaxNumeroSequencialByTenantId | mitigate | Query uses WHERE c.tenantId = :tenantId — never unscoped global MAX. Tenant isolation enforced at JPQL level. |
| T-57-02 | Tampering | DadosTipoConverter.convertToEntityAttribute | mitigate | Deserialize to typed DadosTipo POJO only; no polymorphic Jackson type handling (@JsonTypeInfo absent). Arbitrary class injection is not possible. |
| T-57-03 | Tampering | Cliente.dadosTipo | accept | dados_tipo is TEXT (not JSONB with functions); no DB-side JSON execution risk. Low-value target. |
</threat_model>

<verification>
mvn -pl backend compile exits 0.
All five files exist at the correct paths.
grep confirms numero_sequencial and dadosTipo fields in Cliente.java.
grep confirms findMaxNumeroSequencialByTenantId in ClienteRepository.java.
</verification>

<success_criteria>
Full Maven compile succeeds with no errors. The data layer is ready for Plan 57-02 to wire numero_cliente generation and dados_tipo persistence in the controller.
</success_criteria>

<output>
Create .planning/phases/LEXCV-57-backend-schema-api/57-01-SUMMARY.md when done.
</output>
