---
phase: "59"
plan: "06"
type: execute
wave: 4
depends_on: ["59-04"]
files_modified:
  - web/src/app/(dashboard)/clientes/[id]/editar/page.tsx
autonomous: false
requirements: [INT-01, INT-04, INT-05, INT-06, INT-07]

must_haves:
  truths:
    - "Edit page has a 'Descrição do Caso' textarea field that submits via PUT /clientes/{id}"
    - "Edit page has Documentos Entregues list: add via modal (descricao + data fields), remove with X button"
    - "Edit page has Documentos a Tratar list: add via modal (descricao field), remove with X button"
    - "Edit page has Deslocações list: add via modal (descricao + local + data fields), remove with X button"
    - "Edit page has Honorários Propostos inline form (total, totalPorExtenso, previsao) submitted via PUT (per D-13)"
  artifacts:
    - path: web/src/app/(dashboard)/clientes/[id]/editar/page.tsx
      provides: All intake list sections and honorários form integrated with react-hook-form and PUT /clientes/{id}
  key_links:
    - from: editar/page.tsx form
      to: PUT /api/v1/clientes/{id}
      via: "useUpdateCliente mutation; payload includes descricaoCaso, documentosEntregues, etc."
    - from: intake list local state
      to: react-hook-form setValue
      via: "setValue('documentos_entregues', updatedList) when item added or removed"
---

<objective>
Extend the cliente edit page with all intake fields: descricao_caso text area, three list-based JSON sections (with add-via-modal and remove), and an inline honorários propostos form. All changes are submitted together via the existing PUT /clientes/{id} hook.

Purpose: Delivers INT-01 and INT-04 through INT-07 in the edit page where the user manages these fields.
Output: Updated editar/page.tsx with descricaoCaso, three add/remove list sections, honorariosPropostos inline form.
</objective>

<execution_context>
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/LEXCV-59-procuracao-intake/59-CONTEXT.md
@.planning/phases/LEXCV-59-procuracao-intake/59-RESEARCH.md
@.planning/phases/LEXCV-59-procuracao-intake/59-04-SUMMARY.md

<interfaces>
<!-- Edit page current structure (must read before editing) -->
web/src/app/(dashboard)/clientes/[id]/editar/page.tsx — single card form with react-hook-form.
Uses useUpdateCliente which calls PUT /clientes/{id}.
Uses clienteFormSchema for validation.

<!-- Zod schema additions from Plan 04 -->
clienteFormSchema now includes:
  descricao_caso: z.string().trim().optional()
  honorarios_propostos: z.object({ total: z.number().optional(), totalPorExtenso: z.string().trim().optional(), previsao: z.string().trim().optional() }).optional()

<!-- Dialog component (same as Plan 05) -->
Dialog, DialogTrigger, DialogContent, DialogHeader, DialogTitle, DialogFooter from "@/components/ui/dialog"

<!-- Types from Plan 04 -->
DocumentoEntregue: { descricao: string; data?: string }
DocumentoATratar: { descricao: string }
Deslocacao: { descricao: string; local?: string; data?: string }
HonorariosPropostos: { total?: number; totalPorExtenso?: string; previsao?: string }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add descricaoCaso field and honorariosPropostos inline form to edit page</name>
  <files>
    web/src/app/(dashboard)/clientes/[id]/editar/page.tsx
  </files>
  <read_first>
    web/src/app/(dashboard)/clientes/[id]/editar/page.tsx
    web/src/schemas/clientes.ts
  </read_first>
  <action>
    Read the full edit page to understand form registration patterns, defaultValues, and the FormField usage before editing.

    In the form defaultValues, add:
      descricao_caso: cliente?.descricao_caso ?? "",
      honorarios_propostos: {
        total: cliente?.honorarios_propostos?.total ?? undefined,
        totalPorExtenso: cliente?.honorarios_propostos?.totalPorExtenso ?? "",
        previsao: cliente?.honorarios_propostos?.previsao ?? "",
      }

    Add a new section below the existing form fields (above or below detalhes_adicionais, using the same <hr /> or separator pattern already in the page). Section heading: "Intake do Caso".

    Add a FormField for descricao_caso: renders a <Textarea> (import from "@/components/ui/textarea" if available, else <textarea className="...">). Label: "Descrição do Caso". No required validation.

    Add an inline form section for Honorários Propostos with heading "Honorários Propostos". Three fields using existing FormField/Input patterns:
      - "Total" → honorarios_propostos.total (type="number", step="0.01"; use register or Controller with valueAsNumber)
      - "Valor por Extenso" → honorarios_propostos.totalPorExtenso (text input)
      - "Previsão" → honorarios_propostos.previsao (text input, e.g. "6 meses")
    These are inline, not in a modal (per D-13 and RESEARCH.md Risk 6 recommendation).

    In the form submit handler (onSubmit), ensure honorarios_propostos and descricao_caso are included in the payload passed to updateCliente.mutate(). Map form field names to camelCase if the API expects camelCase (backend uses Jackson camelCase by default). Send as:
      descricaoCaso: data.descricao_caso,
      honorariosPropostos: data.honorarios_propostos (omit if undefined)
  </action>
  <verify>
    <automated>cd web && pnpm build 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - editar/page.tsx contains "Descrição do Caso" label text
    - editar/page.tsx contains "honorarios_propostos.totalPorExtenso" or "honorariosPropostos.totalPorExtenso" field registration
    - editar/page.tsx contains "Honorários Propostos" section heading
    - editar/page.tsx form submit handler includes descricaoCaso in the PUT payload
    - pnpm build exits 0
  </acceptance_criteria>
  <done>descricaoCaso textarea and honorariosPropostos inline form render in edit page; pnpm build exits 0.</done>
</task>

<task type="auto">
  <name>Task 2: Add three JSON list sections (documentos entregues, a tratar, deslocações) with add-modal and remove</name>
  <files>
    web/src/app/(dashboard)/clientes/[id]/editar/page.tsx
  </files>
  <read_first>
    web/src/app/(dashboard)/clientes/[id]/editar/page.tsx
    web/src/components/ui/dialog.tsx
  </read_first>
  <action>
    Read the edit page after Task 1 changes. Add imports for: Dialog, DialogTrigger, DialogContent, DialogHeader, DialogTitle, DialogFooter from "@/components/ui/dialog"; useState from "react"; DocumentoEntregue, DocumentoATratar, Deslocacao from "@/types/clientes".

    Add local state (outside react-hook-form, managed with useState + synced via setValue):
      const [documentosEntregues, setDocumentosEntregues] = useState<DocumentoEntregue[]>(cliente?.documentos_entregues ?? [])
      const [documentosATratar, setDocumentosATratar] = useState<DocumentoATratar[]>(cliente?.documentos_a_tratar ?? [])
      const [deslocacoes, setDeslocacoes] = useState<Deslocacao[]>(cliente?.deslocacoes ?? [])

    For each list, also maintain modal-open state and a local "new item" form state (separate from react-hook-form to keep the modal simple):
      const [addDocEntreModal, setAddDocEntreModal] = useState(false)
      const [newDocEntre, setNewDocEntre] = useState<{ descricao: string; data: string }>({ descricao: "", data: "" })
      (same pattern for the other two lists)

    In the form's onSubmit handler, include the three lists in the payload:
      documentosEntregues: documentosEntregues,
      documentosATratar: documentosATratar,
      deslocacoes: deslocacoes,
    (camelCase matching what the backend PUT handler expects)

    Add three sections to the page JSX below the honorariosPropostos section:

    Section "Documentos Entregues":
      Heading + "Adicionar" button (DialogTrigger).
      Dialog modal with title "Adicionar Documento Entregue", two text inputs (Descrição*, Data), "Confirmar" button in footer.
      On confirm: setDocumentosEntregues(prev => [...prev, newDocEntre]); setAddDocEntreModal(false); reset newDocEntre.
      List renders each item as a row: "descricao — data" + X button that calls setDocumentosEntregues(prev => prev.filter((_, i) => i !== index)).

    Section "Documentos a Tratar":
      Same pattern but modal has only one input (Descrição). List shows descricao + X.

    Section "Deslocações":
      Modal has three inputs: Descrição, Local, Data. List shows "descricao (local) — data" + X.

    Keep each modal to 2-3 inputs only (per Risk 6 in RESEARCH.md). No nested modals.
  </action>
  <verify>
    <automated>cd web && pnpm build 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - editar/page.tsx contains "Documentos Entregues" heading text
    - editar/page.tsx contains "Documentos a Tratar" heading text
    - editar/page.tsx contains "Deslocações" heading text
    - editar/page.tsx contains three Dialog components (one per list section)
    - editar/page.tsx form submit handler includes "documentosEntregues" in PUT payload
    - pnpm build exits 0
  </acceptance_criteria>
  <done>Three JSON list sections with add-modal and remove are in the edit page; pnpm build exits 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Edit form → PUT /clientes/{id} | JSON body with intake arrays; backend deserializes via AttributeConverters |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-59-11 | Tampering | descricao_caso XSS in display | accept | React auto-escapes text content; no dangerouslySetInnerHTML used |
| T-59-12 | Tampering | honorariosPropostos.total type coercion | mitigate | Use type="number" + valueAsNumber in react-hook-form; backend uses BigDecimal which handles decimal precision |
| T-59-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed |
</threat_model>

<verification>
cd web && pnpm build
Expected: exit 0

Manual verification on dev server:
- Visit /clientes/{id}/editar
- Expect "Intake do Caso" section with "Descrição do Caso" textarea
- Expect "Honorários Propostos" section with total/totalPorExtenso/previsao inputs
- Expect "Documentos Entregues" list with "Adicionar" button that opens modal
- Expect "Documentos a Tratar" list with same pattern
- Expect "Deslocações" list with three-field modal
- Submit form → PUT /clientes/{id} sends all intake fields in payload
</verification>

<success_criteria>
pnpm build exits 0. Edit page submits descricaoCaso, documentosEntregues, documentosATratar, deslocacoes, and honorariosPropostos in PUT /clientes/{id} payload.
</success_criteria>

<output>
Create .planning/phases/LEXCV-59-procuracao-intake/59-06-SUMMARY.md when done.
</output>
