---
phase: "59"
plan: "03"
type: execute
wave: 2
depends_on: ["59-01", "59-02"]
files_modified:
  - backend/src/main/java/com/lexcv/controllers/ResourceController.java
autonomous: true
requirements: [PROC-01, PROC-02, INT-01, INT-02, INT-03, INT-04, INT-05, INT-06, INT-07]

must_haves:
  truths:
    - "POST /clientes/{id}/procuracao uploads file to MinIO, sets procuracaoKey on Cliente row, returns { procuracaoKey }"
    - "GET /clientes/{id}/procuracao/download returns { url, expiresIn } presigned URL"
    - "DELETE /clientes/{id}/procuracao removes MinIO object and clears procuracaoKey"
    - "PUT /clientes/{id} accepts descricaoCaso, documentosEntregues, documentosATratar, deslocacoes, honorariosPropostos"
    - "GET/POST/DELETE /clientes/{id}/advogados and /administrativos work with role validation"
  artifacts:
    - path: backend/src/main/java/com/lexcv/controllers/ResourceController.java
      provides: All 11 new endpoint methods
  key_links:
    - from: ResourceController.uploadProcuracao
      to: StorageService.upload + storageService.delete
      via: "upload new, then delete old (safe ordering)"
    - from: ResourceController.addAdvogado
      to: UserRepository.findByTenantIdAndRoleName
      via: "role validation before persisting ClienteAdvogado"
---

<objective>
Add all new backend endpoints to ResourceController: procuração upload/download/delete, extended PUT /clientes/{id} for intake fields, and advogados/administrativos GET/POST/DELETE sub-resources.

Purpose: Completes the backend API surface for the entire Phase 59 feature set.
Output: ResourceController.java with 11 new endpoint methods.
</objective>

<execution_context>
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/LEXCV-59-procuracao-intake/59-CONTEXT.md
@.planning/phases/LEXCV-59-procuracao-intake/59-RESEARCH.md

<interfaces>
<!-- StorageService API -->
From backend/src/main/java/com/lexcv/services/StorageService.java:
  String upload(UUID tenantId, UUID documentoId, String filename, InputStream inputStream, String contentType, long size)
  String presignedDownloadUrl(String objectKey)  // returns URL string
  void delete(String objectKey)

<!-- Existing upload pattern reference (documentos section ~line 1719+) -->
From ResourceController.java (must read): existing documentos upload endpoint for presigned URL pattern,
existing contactos endpoints for sub-resource CRUD pattern (GET/POST/DELETE),
existing updateCliente handler to know which fields are currently set.

<!-- getTenantId() is a private method already in ResourceController returning UUID from security context -->
<!-- clienteRepository, userRepository already @Autowired in ResourceController -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add procuração endpoints and extend PUT /clientes/{id} for intake fields</name>
  <files>
    backend/src/main/java/com/lexcv/controllers/ResourceController.java
  </files>
  <read_first>
    backend/src/main/java/com/lexcv/controllers/ResourceController.java
    backend/src/main/java/com/lexcv/services/StorageService.java
  </read_first>
  <action>
    Read ResourceController.java to understand the existing updateCliente method and the existing documentos upload pattern before editing.

    Add @Autowired fields for ClienteAdvogadoRepository and ClienteAdministrativoRepository at the class field level (alongside existing @Autowired repositories).

    Add three procuração endpoint methods inside ResourceController, grouped together after the existing cliente endpoints:

    POST /clientes/{id}/procuracao (consumes = MULTIPART_FORM_DATA_VALUE):
      @PreAuthorize("hasAuthority('clientes:edit')")
      Resolve cliente by id, validate tenantId match (return 404 if not found/wrong tenant).
      Store old procuracaoKey. Generate syntheticId = UUID.randomUUID().
      Call storageService.upload(getTenantId(), syntheticId, file.getOriginalFilename(), file.getInputStream(), file.getContentType(), file.getSize()) to get newKey.
      If oldKey != null, call storageService.delete(oldKey) AFTER successful upload.
      Set cliente.setProcuracaoKey(newKey), call clienteRepository.save(cliente).
      Return ResponseEntity.ok(Map.of("procuracaoKey", newKey)).

    GET /clientes/{id}/procuracao/download:
      @PreAuthorize("hasAuthority('clientes:view')")
      Resolve cliente, validate tenant. If procuracaoKey is null, return 404 with body Map.of("message", "Procuração não encontrada").
      Call storageService.presignedDownloadUrl(cliente.getProcuracaoKey()) to get url.
      Return ResponseEntity.ok(Map.of("url", url, "expiresIn", 3600)).

    DELETE /clientes/{id}/procuracao:
      @PreAuthorize("hasAuthority('clientes:edit')")
      Resolve cliente, validate tenant. If procuracaoKey is null, return 404.
      Call storageService.delete(cliente.getProcuracaoKey()).
      Set cliente.setProcuracaoKey(null), save. Return ResponseEntity.noContent().build().

    Extend the existing updateCliente method (PUT /clientes/{id}) to additionally set these fields if not null in payload:
      descricaoCaso, documentosEntregues, documentosATratar, deslocacoes, honorariosPropostos.
    Do NOT add procuracaoKey to the updateCliente handler — it is managed by its own endpoint only.
  </action>
  <verify>
    <automated>cd backend && mvn -DskipTests package -q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - ResourceController.java contains @PostMapping(value = "/clientes/{id}/procuracao", consumes = MediaType.MULTIPART_FORM_DATA_VALUE)
    - ResourceController.java contains @GetMapping("/clientes/{id}/procuracao/download")
    - ResourceController.java contains @DeleteMapping("/clientes/{id}/procuracao")
    - ResourceController.java updateCliente handler contains "setDescricaoCaso" and "setHonorariosPropostos"
    - ResourceController.java does NOT contain "setProcuracaoKey" inside the updateCliente handler body
    - mvn -DskipTests package exits 0
  </acceptance_criteria>
  <done>Three procuração endpoints and extended PUT compile; mvn -DskipTests package exits 0.</done>
</task>

<task type="auto">
  <name>Task 2: Add advogados and administrativos sub-resource endpoints</name>
  <files>
    backend/src/main/java/com/lexcv/controllers/ResourceController.java
  </files>
  <read_first>
    backend/src/main/java/com/lexcv/controllers/ResourceController.java
    backend/src/main/java/com/lexcv/repositories/ClienteAdvogadoRepository.java
    backend/src/main/java/com/lexcv/repositories/ClienteAdministrativoRepository.java
    backend/src/main/java/com/lexcv/repositories/UserRepository.java
  </read_first>
  <action>
    Add six endpoint methods in ResourceController following the sub-resource pattern (same as contactos GET/POST/DELETE):

    GET /clientes/{id}/advogados:
      @PreAuthorize("hasAuthority('clientes:view')")
      Resolve cliente + tenant validation.
      List<ClienteAdvogado> links = clienteAdvogadoRepository.findByClienteIdAndTenantId(id, getTenantId()).
      Collect userIds, load User objects for each via userRepository, return list of User objects.
      Return ResponseEntity.ok(users).

    POST /clientes/{id}/advogados/{userId}:
      @PreAuthorize("hasAuthority('clientes:edit')")
      Resolve cliente + tenant validation (404 if not found).
      Load User by userId; if not found or user.getTenantId() != getTenantId(), return 404.
      Role validation: if user.getRoles().stream().noneMatch(r -> "ADVOGADO".equals(r.getNome())), return 400 with body Map.of("message", "Utilizador não tem o papel ADVOGADO").
      Check for duplicate: if clienteAdvogadoRepository.findByClienteIdAndUserIdAndTenantId(id, userId, getTenantId()).isPresent(), return 409.
      Save new ClienteAdvogado(id, userId, getTenantId()).
      Return ResponseEntity.status(HttpStatus.CREATED).body(Map.of("userId", userId)).

    DELETE /clientes/{id}/advogados/{userId}:
      @PreAuthorize("hasAuthority('clientes:edit')")
      Resolve cliente + tenant validation.
      Call clienteAdvogadoRepository.deleteByClienteIdAndUserIdAndTenantId(id, userId, getTenantId()).
      Return ResponseEntity.noContent().build().

    GET /clientes/{id}/administrativos: same as GET advogados but uses clienteAdministrativoRepository.
    POST /clientes/{id}/administrativos/{userId}: same as POST advogados but role check is ASSISTENTE or TECNICO:
      user.getRoles().stream().noneMatch(r -> "ASSISTENTE".equals(r.getNome()) || "TECNICO".equals(r.getNome()))
      Return 400 with message "Utilizador não tem o papel ASSISTENTE ou TECNICO" if validation fails.
    DELETE /clientes/{id}/administrativos/{userId}: same pattern as DELETE advogados.

    Use @Transactional on the DELETE methods that call repository delete operations.
  </action>
  <verify>
    <automated>cd backend && mvn -DskipTests package -q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - ResourceController.java contains @GetMapping("/clientes/{id}/advogados") and @GetMapping("/clientes/{id}/administrativos")
    - ResourceController.java contains @PostMapping("/clientes/{id}/advogados/{userId}") with "ADVOGADO" role check
    - ResourceController.java contains @PostMapping("/clientes/{id}/administrativos/{userId}") with "ASSISTENTE" and "TECNICO" role check
    - ResourceController.java contains @DeleteMapping("/clientes/{id}/advogados/{userId}") and @DeleteMapping("/clientes/{id}/administrativos/{userId}")
    - mvn -DskipTests package exits 0
  </acceptance_criteria>
  <done>All 6 advogados/administrativos endpoints compile; mvn -DskipTests package exits 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| POST /clientes/{id}/procuracao | File upload — untrusted file content/filename |
| POST /clientes/{id}/advogados/{userId} | userId path param — must be validated for role and tenant |
| PUT /clientes/{id} | JSON body — untrusted; procuracaoKey must not be settable here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-59-04 | Spoofing | procuracao upload endpoint | mitigate | tenantId validated from security context (not client); client cannot supply tenantId |
| T-59-05 | Elevation of Privilege | POST /advogados/{userId} | mitigate | Server verifies ADVOGADO role via user.getRoles() before saving junction row |
| T-59-06 | Tampering | procuracaoKey via PUT body | mitigate | updateCliente handler does not call setProcuracaoKey() |
| T-59-07 | Denial of Service | Large file upload to procuracao endpoint | accept | Spring Boot multipart size limits apply (existing config); procuração is a single file, low volume |
| T-59-SC | Tampering | npm/pip/cargo installs | accept | No new package installs |
</threat_model>

<verification>
Run: cd backend && mvn -DskipTests package
Expected: BUILD SUCCESS

Run: grep -c "procuracao\|advogados\|administrativos" backend/src/main/java/com/lexcv/controllers/ResourceController.java
Expected: count > 10 (multiple endpoint references)
</verification>

<success_criteria>
mvn -DskipTests package exits 0 with all 11 new endpoint methods present in ResourceController.
The app starts (mvn spring-boot:run) and GET /api/v1/clientes/{id}/advogados returns 200 with an empty array for a valid client.
</success_criteria>

<output>
Create .planning/phases/LEXCV-59-procuracao-intake/59-03-SUMMARY.md when done.
</output>
