---
phase: "59"
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/main/java/com/lexcv/models/User.java
  - backend/src/main/java/com/lexcv/models/ClienteAdvogado.java
  - backend/src/main/java/com/lexcv/models/ClienteAdministrativo.java
  - backend/src/main/java/com/lexcv/repositories/UserRepository.java
  - backend/src/main/java/com/lexcv/repositories/ClienteAdvogadoRepository.java
  - backend/src/main/java/com/lexcv/repositories/ClienteAdministrativoRepository.java
autonomous: true
requirements: [INT-02, INT-03]

must_haves:
  truths:
    - "User entity has numeroCedula field (per planning decision)"
    - "ClienteAdvogado and ClienteAdministrativo entities exist with (cliente_id, user_id) unique constraints and tenantId"
    - "UserRepository has findByTenantIdAndRoleName and findByTenantIdAndRoleNameIn JPQL queries"
  artifacts:
    - path: backend/src/main/java/com/lexcv/models/ClienteAdvogado.java
      provides: Junction entity for t_cliente_advogado with @UniqueConstraint on (cliente_id, user_id)
    - path: backend/src/main/java/com/lexcv/models/ClienteAdministrativo.java
      provides: Junction entity for t_cliente_administrativo with @UniqueConstraint on (cliente_id, user_id)
    - path: backend/src/main/java/com/lexcv/repositories/ClienteAdvogadoRepository.java
      provides: findByClienteIdAndTenantId, findByClienteIdAndUserIdAndTenantId, deleteByClienteIdAndUserIdAndTenantId
    - path: backend/src/main/java/com/lexcv/repositories/UserRepository.java
      provides: findByTenantIdAndRoleName, findByTenantIdAndRoleNameIn
  key_links:
    - from: ResourceController.java (Wave 2)
      to: ClienteAdvogadoRepository.java
      via: "@Autowired injection in Wave 2 plan"
---

<objective>
Add numeroCedula to User entity, create ClienteAdvogado and ClienteAdministrativo junction entities with repositories, and extend UserRepository with role-filtered query methods.

Purpose: Establishes the data model for advogados/administrativos assignment (D-06, D-07, D-08). Wave 2 endpoints depend on these repositories.
Output: User.java extended, 4 new Java files.
</objective>

<execution_context>
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/LEXCV-59-procuracao-intake/59-CONTEXT.md
@.planning/phases/LEXCV-59-procuracao-intake/59-RESEARCH.md

<interfaces>
<!-- User.java current key patterns (read the file before editing) -->
From backend/src/main/java/com/lexcv/models/User.java:
  @Entity @Table(name = "t_user") @Data @Builder @NoArgsConstructor @AllArgsConstructor
  Fields: UUID id, UUID tenantId, String nome, String email, String passwordHash,
          Boolean ativo, String telefone, String avatarUrl,
          LocalDateTime createdAt, LocalDateTime updatedAt,
          Set<Role> roles (ManyToMany t_user_role), Set<Permission> permissions

<!-- UserRepository current state -->
From backend/src/main/java/com/lexcv/repositories/UserRepository.java:
  Optional<User> findByEmail(String email);
  List<User> findByTenantId(UUID tenantId);

<!-- ClienteContacto pattern for junction entity structure -->
From backend/src/main/java/com/lexcv/models/ (read ClienteContacto.java or ClienteNota.java if they exist to verify exact pattern)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add numeroCedula to User and create junction entities</name>
  <files>
    backend/src/main/java/com/lexcv/models/User.java,
    backend/src/main/java/com/lexcv/models/ClienteAdvogado.java,
    backend/src/main/java/com/lexcv/models/ClienteAdministrativo.java
  </files>
  <read_first>
    backend/src/main/java/com/lexcv/models/User.java
    backend/src/main/java/com/lexcv/models/Cliente.java
  </read_first>
  <action>
    In User.java: add one nullable field after the existing fields:
      @Column(name = "numero_cedula")
      private String numeroCedula;
    This field is optional and not required in any existing flow. No other changes to User.java.

    Create ClienteAdvogado.java in package com.lexcv.models:
      @Entity
      @Table(name = "t_cliente_advogado",
             uniqueConstraints = @UniqueConstraint(columnNames = {"cliente_id", "user_id"}))
      @Data @NoArgsConstructor @AllArgsConstructor @Builder
      Fields:
        @Id @GeneratedValue(strategy = GenerationType.UUID) private UUID id;
        @Column(name = "cliente_id", nullable = false) private UUID clienteId;
        @Column(name = "user_id", nullable = false) private UUID userId;
        @Column(name = "tenant_id", nullable = false) private UUID tenantId;
        @Column(name = "created_at", updatable = false) private LocalDateTime createdAt;
        @PrePersist protected void onCreate() { this.createdAt = LocalDateTime.now(); }

    Create ClienteAdministrativo.java in package com.lexcv.models with the exact same structure but table name "t_cliente_administrativo".

    Imports needed for both junction entities: jakarta.persistence.*, java.util.UUID, java.time.LocalDateTime, lombok annotations.
  </action>
  <verify>
    <automated>cd backend && mvn -DskipTests package -q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - User.java contains "private String numeroCedula"
    - ClienteAdvogado.java exists with @Table(name = "t_cliente_advogado") and @UniqueConstraint on {"cliente_id", "user_id"}
    - ClienteAdministrativo.java exists with @Table(name = "t_cliente_administrativo") and @UniqueConstraint on {"cliente_id", "user_id"}
    - mvn -DskipTests package exits 0
  </acceptance_criteria>
  <done>User.java has numeroCedula; both junction entity files compile cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Create repositories for junction entities and extend UserRepository</name>
  <files>
    backend/src/main/java/com/lexcv/repositories/ClienteAdvogadoRepository.java,
    backend/src/main/java/com/lexcv/repositories/ClienteAdministrativoRepository.java,
    backend/src/main/java/com/lexcv/repositories/UserRepository.java
  </files>
  <read_first>
    backend/src/main/java/com/lexcv/repositories/UserRepository.java
    backend/src/main/java/com/lexcv/repositories/ClienteRepository.java
  </read_first>
  <action>
    Create ClienteAdvogadoRepository.java in package com.lexcv.repositories:
      @Repository
      public interface ClienteAdvogadoRepository extends JpaRepository<ClienteAdvogado, UUID> {
        List<ClienteAdvogado> findByClienteIdAndTenantId(UUID clienteId, UUID tenantId);
        Optional<ClienteAdvogado> findByClienteIdAndUserIdAndTenantId(UUID clienteId, UUID userId, UUID tenantId);
        void deleteByClienteIdAndUserIdAndTenantId(UUID clienteId, UUID userId, UUID tenantId);
      }

    Create ClienteAdministrativoRepository.java with the exact same structure but generic type ClienteAdministrativo.

    In UserRepository.java, add two @Query methods:
      @Query("SELECT u FROM User u JOIN u.roles r WHERE u.tenantId = :tenantId AND r.nome = :roleName")
      List<User> findByTenantIdAndRoleName(@Param("tenantId") UUID tenantId, @Param("roleName") String roleName);

      @Query("SELECT u FROM User u JOIN u.roles r WHERE u.tenantId = :tenantId AND r.nome IN :roleNames")
      List<User> findByTenantIdAndRoleNameIn(@Param("tenantId") UUID tenantId, @Param("roleNames") List<String> roleNames);

    Add imports: org.springframework.data.jpa.repository.Query, org.springframework.data.repository.query.Param, java.util.List.
  </action>
  <verify>
    <automated>cd backend && mvn -DskipTests package -q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - ClienteAdvogadoRepository.java exists with findByClienteIdAndTenantId, findByClienteIdAndUserIdAndTenantId, deleteByClienteIdAndUserIdAndTenantId
    - ClienteAdministrativoRepository.java exists with same three methods on ClienteAdministrativo
    - UserRepository.java contains findByTenantIdAndRoleName and findByTenantIdAndRoleNameIn with @Query annotations
    - mvn -DskipTests package exits 0
  </acceptance_criteria>
  <done>All four repository files compile and UserRepository has role-filtered queries.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| POST /clientes/{id}/advogados/{userId} | Client-supplied userId must be validated server-side for role |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-59-02 | Elevation of Privilege | POST /clientes/{id}/advogados/{userId} | mitigate | Wave 2: endpoint validates user.getRoles() contains ADVOGADO before persisting junction row |
| T-59-03 | Elevation of Privilege | POST /clientes/{id}/administrativos/{userId} | mitigate | Wave 2: endpoint validates ASSISTENTE or TECNICO role |
| T-59-SC | Tampering | npm/pip/cargo installs | accept | No new package installs in this plan |
</threat_model>

<verification>
Run: cd backend && mvn -DskipTests package
Expected: BUILD SUCCESS

Run: grep "findByTenantIdAndRoleName" backend/src/main/java/com/lexcv/repositories/UserRepository.java
Expected: two method signatures found
</verification>

<success_criteria>
mvn -DskipTests package exits 0 with ClienteAdvogado, ClienteAdministrativo, their repositories, and User.numeroCedula all in place.
</success_criteria>

<output>
Create .planning/phases/LEXCV-59-procuracao-intake/59-02-SUMMARY.md when done.
</output>
