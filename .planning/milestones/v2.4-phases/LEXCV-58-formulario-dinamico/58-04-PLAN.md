---
phase: 58-formulario-dinamico
plan: 04
type: execute
wave: 2
depends_on: [58-01]
files_modified:
  - web/src/app/(dashboard)/clientes/[id]/editar/page.tsx
autonomous: true
requirements: [PERF-03, PERF-04]

must_haves:
  truths:
    - "Edit form loads existing tipo, avencado, and dados_tipo from the API and pre-populates the form via form.reset per D-03"
    - "Edit form shows RadioGroup tipo selector identical to novo/page.tsx per D-01"
    - "Edit form shows tipo-conditional field sections populated from dados_tipo per D-03"
    - "Edit form shows Avençado Switch reflecting the current avencado value per D-05"
    - "Edit form header shows CLI-XXXX badge (read-only) when numero_cliente is set per D-04"
    - "Confirmation Dialog on tipo change works identically to novo/page.tsx per D-02"
    - "Submit payload includes tipo, avencado, dados_tipo but NOT numero_cliente"
  artifacts:
    - path: "web/src/app/(dashboard)/clientes/[id]/editar/page.tsx"
      provides: "Refactored edit form with dynamic fields, form.reset mapping, numero_cliente badge"
      contains: "pendingTipo"
  key_links:
    - from: "web/src/app/(dashboard)/clientes/[id]/editar/page.tsx"
      to: "web/src/components/ui/radio-group.tsx"
      via: "import RadioGroup, RadioGroupItem"
    - from: "web/src/app/(dashboard)/clientes/[id]/editar/page.tsx"
      to: "web/src/schemas/clientes.ts"
      via: "zodResolver — avencado and dados_tipo now in schema"
    - from: "form.reset()"
      to: "cliente.data.dados_tipo"
      via: "dados_tipo: cliente.data.dados_tipo ?? {}"
---

<objective>
Refactor the edit-client form (/clientes/[id]/editar) to match the novo/page.tsx behavior plus: pre-populate tipo/avencado/dados_tipo from the existing client data via form.reset, and display the numero_cliente badge (read-only) in the page header.

Purpose: Per D-01 through D-05, D-07 — the edit form must show current client type with pre-filled dynamic fields and allow the user to change them with the same confirmation dialog pattern as novo. D-04 requires numero_cliente to appear as a read-only badge in the edit header.
Output: Fully refactored [id]/editar/page.tsx mirroring novo/page.tsx with the additional form.reset mapping and badge in the header.
</objective>

<execution_context>
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/LEXCV-58-formulario-dinamico/58-CONTEXT.md
@.planning/phases/LEXCV-58-formulario-dinamico/58-PATTERNS.md
@.planning/phases/LEXCV-58-formulario-dinamico/58-01-SUMMARY.md
@.planning/phases/LEXCV-58-formulario-dinamico/58-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor editar/page.tsx — same as novo plus form.reset mapping and header badge</name>
  <files>web/src/app/(dashboard)/clientes/[id]/editar/page.tsx</files>
  <read_first>
    - web/src/app/(dashboard)/clientes/[id]/editar/page.tsx (read all lines — current imports, form setup, useEffect with form.reset, page title area ~line 118, field layout, onSubmit)
    - web/src/app/(dashboard)/clientes/novo/page.tsx (read after plan 58-03 executes — the refactored version is the reference implementation; all patterns are identical)
    - .planning/phases/LEXCV-58-formulario-dinamico/58-PATTERNS.md (editar-specific patterns: form.reset extension, page title badge addition)
    - web/src/types/clientes.ts (confirm ClienteUpdateRequest has tipo, avencado, dados_tipo but not numero_cliente)
  </read_first>
  <action>
    Refactor web/src/app/(dashboard)/clientes/[id]/editar/page.tsx applying all changes from plan 58-03 (novo/page.tsx refactor) plus two editar-specific additions:

    APPLY ALL CHANGES FROM PLAN 58-03 IDENTICALLY:
    - Add same imports: Controller from react-hook-form, RadioGroup + RadioGroupItem from @/components/ui/radio-group, Switch from @/components/ui/switch, Dialog + DialogContent + DialogDescription + DialogFooter + DialogHeader + DialogTitle from @/components/ui/dialog
    - Add pendingTipo state, watchedTipo watch, onTipoChange function, confirmTipoChange function (identical logic)
    - Replace tipo Input with Controller-wrapped RadioGroup at the top of the form
    - Add conditional PARTICULAR field section (idade/sexo/nacionalidade) and EMPRESA field section (nome_comercial/sede/representante_legal/cargo) with identical bordered div styling
    - Add Avençado Controller-wrapped Switch before the submit buttons
    - Add confirmation Dialog outside the form (identical to novo)
    - Update onSubmit payload to include tipo, avencado, dados_tipo — do NOT include numero_cliente

    EDITAR-SPECIFIC ADDITION 1 — extend form.reset in the useEffect (located ~lines 72-87):
    The existing useEffect calls form.reset with nome, nif, email, etc. Extend it to also reset:
    - tipo: (cliente.data.tipo as "PARTICULAR" | "EMPRESA" | undefined) ?? undefined
    - avencado: cliente.data.avencado ?? false
    - dados_tipo: cliente.data.dados_tipo ?? {}
    Keep all existing field mappings (nome, nif, email, telefone, localidade, morada, documento_tipo with fallback to documentoTipo, documento_numero with fallback to documentoNumero, ramo_atividade with fallback to ramoAtividade, detalhes_adicionais with fallback to detalhesAdicionais). Do NOT reset numero_cliente into the form — it is display-only.

    EDITAR-SPECIFIC ADDITION 2 — add numero_cliente badge to page title area (~line 118):
    After the h1 "Editar cliente" heading, add a conditional block: if cliente.data?.numero_cliente exists, render a Badge variant="blue" className="rounded-none font-mono font-bold text-[10px] w-fit" containing {cliente.data.numero_cliente}. Import Badge from "@/components/ui/badge" if not already imported.

    DEFAULT VALUES in useForm: set defaultValues with tipo: undefined, avencado: false, dados_tipo: {} alongside existing fields.
  </action>
  <verify>
    <automated>cd web && pnpm build 2>&1 | grep -E "error TS|Type error" | head -30</automated>
  </verify>
  <acceptance_criteria>
    - web/src/app/(dashboard)/clientes/[id]/editar/page.tsx contains import { RadioGroup, RadioGroupItem } from "@/components/ui/radio-group"
    - web/src/app/(dashboard)/clientes/[id]/editar/page.tsx contains import { Switch } from "@/components/ui/switch"
    - web/src/app/(dashboard)/clientes/[id]/editar/page.tsx contains pendingTipo
    - web/src/app/(dashboard)/clientes/[id]/editar/page.tsx contains "dados_tipo: cliente.data.dados_tipo"
    - web/src/app/(dashboard)/clientes/[id]/editar/page.tsx contains "avencado: cliente.data.avencado"
    - web/src/app/(dashboard)/clientes/[id]/editar/page.tsx contains "numero_cliente" (for the badge display)
    - web/src/app/(dashboard)/clientes/[id]/editar/page.tsx does NOT contain numero_cliente as a form.reset field or in the ClienteUpdateRequest payload
    - pnpm build exits 0
  </acceptance_criteria>
  <done>Edit form has dynamic tipo selection with pre-populated fields from existing client data, Avençado Switch, confirmation Dialog, and read-only CLI-XXXX badge in the header. Build passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Form → API (PUT /api/v1/clientes/{id}) | Update payload must not include numero_cliente |
| API → form.reset | dados_tipo from backend populates form fields; never routed through user-editable numero_cliente field |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-58-04-01 | Tampering | ClienteUpdateRequest payload | mitigate | numero_cliente excluded from form defaultValues, form.reset, and onSubmit payload; acceptance criteria grep confirms absence from payload |
| T-58-04-02 | Tampering | form.reset with dados_tipo from API | accept | dados_tipo is structured data from the same tenant's own client record; no cross-tenant risk; RBAC gate (clientes:edit) already enforced at the route level |
</threat_model>

<verification>
pnpm build exits 0. Manual browser check: navigate to /clientes/{id}/editar for an existing client with tipo=PARTICULAR; form pre-populates RadioGroup to Particular and shows idade/sexo/nacionalidade fields populated; switching to Empresa shows Dialog; CLI-XXXX badge visible in header if numero_cliente is set; Avençado Switch reflects current avencado value.
</verification>

<success_criteria>
- Edit form pre-populates tipo, avencado, dados_tipo from API via form.reset
- RadioGroup, conditional field sections, Switch, and Dialog present (identical to novo)
- Header shows CLI-XXXX badge when numero_cliente is set on the client
- Submit payload includes tipo, avencado, dados_tipo; excludes numero_cliente
- pnpm build exits 0
</success_criteria>

<output>
Create `.planning/phases/LEXCV-58-formulario-dinamico/58-04-SUMMARY.md` when done
</output>
