---
phase: 58-formulario-dinamico
plan: 03
type: execute
wave: 2
depends_on: [58-01]
files_modified:
  - web/src/app/(dashboard)/clientes/novo/page.tsx
autonomous: true
requirements: [PERF-03, PERF-04]

must_haves:
  truths:
    - "RadioGroup at top of form shows Particular/Empresa options per D-01"
    - "Changing tipo when another tipo is already selected shows a confirmation Dialog per D-02"
    - "Confirming the Dialog clears the previous tipo's dados_tipo fields and applies the new tipo"
    - "Selecting Particular shows idade/sexo/nacionalidade field section; selecting Empresa shows nome_comercial/sede/representante_legal/cargo section per D-03"
    - "Avençado Switch appears in the form; checked state maps to avencado boolean per D-05"
    - "Form submit payload includes tipo, avencado, dados_tipo but NOT numero_cliente"
  artifacts:
    - path: "web/src/app/(dashboard)/clientes/novo/page.tsx"
      provides: "Refactored create form with dynamic fields, confirmation dialog, Switch"
      contains: "pendingTipo"
  key_links:
    - from: "web/src/app/(dashboard)/clientes/novo/page.tsx"
      to: "web/src/components/ui/radio-group.tsx"
      via: "import RadioGroup, RadioGroupItem"
    - from: "web/src/app/(dashboard)/clientes/novo/page.tsx"
      to: "web/src/components/ui/switch.tsx"
      via: "import Switch"
    - from: "web/src/app/(dashboard)/clientes/novo/page.tsx"
      to: "web/src/schemas/clientes.ts"
      via: "zodResolver(clienteSchema) — must include avencado and dados_tipo"
---

<objective>
Refactor the create-client form (/clientes/novo) to include a RadioGroup tipo selector at the top, dynamic conditional field sections for Particular vs Empresa, a confirmation Dialog on tipo change, and an Avençado Switch.

Purpose: Per D-01 through D-05 — the form must dynamically adapt to the selected client type. This is the primary UX deliverable of Phase 58 on the create path.
Output: Fully refactored novo/page.tsx with all dynamic form behavior.
</objective>

<execution_context>
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/LEXCV-58-formulario-dinamico/58-CONTEXT.md
@.planning/phases/LEXCV-58-formulario-dinamico/58-PATTERNS.md
@.planning/phases/LEXCV-58-formulario-dinamico/58-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor novo/page.tsx — RadioGroup, conditional fields, Switch, Dialog</name>
  <files>web/src/app/(dashboard)/clientes/novo/page.tsx</files>
  <read_first>
    - web/src/app/(dashboard)/clientes/novo/page.tsx (read all lines — current imports, defaultValues, form setup, field layout, onSubmit payload, return JSX)
    - web/src/schemas/clientes.ts (confirm avencado and dados_tipo are now in schema after plan 58-01)
    - web/src/types/clientes.ts (confirm ClienteCreateRequest has tipo, avencado, dados_tipo but not numero_cliente)
    - .planning/phases/LEXCV-58-formulario-dinamico/58-PATTERNS.md (all implementation patterns for this file: new imports, defaultValues, pendingTipo state, RadioGroup JSX, conditional field sections, Switch, Dialog, onSubmit payload)
  </read_first>
  <action>
    Refactor web/src/app/(dashboard)/clientes/novo/page.tsx with these changes in order:

    IMPORTS — add to the existing import block:
    - import { Controller } from "react-hook-form" (if not already imported)
    - import { RadioGroup, RadioGroupItem } from "@/components/ui/radio-group"
    - import { Switch } from "@/components/ui/switch"
    - import { Dialog, DialogContent, DialogDescription, DialogFooter, DialogHeader, DialogTitle } from "@/components/ui/dialog"

    DEFAULT VALUES — in the useForm call, replace the defaultValues object with: nome: "", nif: "", tipo: undefined, avencado: false, email: "", telefone: "", localidade: "", morada: "", documento_tipo: "", documento_numero: "", ramo_atividade: "", detalhes_adicionais: "", dados_tipo: {}

    STATE AND HANDLERS — add inside the page component function, after the form declaration:
    - const [pendingTipo, setPendingTipo] = React.useState<"PARTICULAR" | "EMPRESA" | null>(null)
    - const watchedTipo = form.watch("tipo")
    - function onTipoChange(newTipo: "PARTICULAR" | "EMPRESA"): if form.getValues("tipo") exists and differs from newTipo, call setPendingTipo(newTipo); otherwise call form.setValue("tipo", newTipo, { shouldValidate: true })
    - function confirmTipoChange(): if pendingTipo is "PARTICULAR", clear dados_tipo.nome_comercial, dados_tipo.sede, dados_tipo.representante_legal, dados_tipo.cargo to ""; if pendingTipo is "EMPRESA", set dados_tipo.idade to undefined, dados_tipo.sexo to "", dados_tipo.nacionalidade to ""; then call form.setValue("tipo", pendingTipo, { shouldValidate: true }) and setPendingTipo(null)

    TIPO SELECTOR — replace the existing tipo Input field (it was a plain text input) with a Controller-wrapped RadioGroup using control={form.control} name="tipo". The RadioGroup has className="flex gap-6". Inside render the two RadioGroupItem + Label pairs: value="PARTICULAR" id="tipo-particular" label "Particular", and value="EMPRESA" id="tipo-empresa" label "Empresa". Add error display below using form.formState.errors.tipo?.message. Place the entire tipo field block at the TOP of the form, before the nome field section, per D-01.

    CONDITIONAL FIELD SECTIONS — insert after the RadioGroup block, before the common fields (email/telefone). Use {watchedTipo === "PARTICULAR" && (...)} to conditionally render a bordered div with h4 "Dados Pessoais" and a 3-column grid containing: dados_tipo.idade (type="number", register with valueAsNumber: true), dados_tipo.sexo (text input), dados_tipo.nacionalidade (text input). Use {watchedTipo === "EMPRESA" && (...)} to conditionally render a bordered div with h4 "Dados da Empresa" and a 2-column grid containing: dados_tipo.nome_comercial (with * required marker and error display), dados_tipo.sede, dados_tipo.representante_legal (with * required marker and error display), dados_tipo.cargo. Error display for nested dados_tipo fields uses: (form.formState.errors.dados_tipo as Record<string, {message?: string}>)?.fieldName?.message. Bordered divs use: className="space-y-4 p-4 border border-neutral-200 dark:border-neutral-800 bg-neutral-50/50 dark:bg-neutral-900/20". All Input fields use className="rounded-none".

    AVENÇADO SWITCH — add before the submit button section, after the last field section. Use Controller with control={form.control} name="avencado" render={({ field }) => Switch with id="avencado" checked={field.value ?? false} onCheckedChange={field.onChange}}. Wrap in a div with className="flex items-center gap-3" and a Label for="avencado" with text "Avençado".

    CONFIRMATION DIALOG — add outside the form element but inside the page return, before the closing page div. Dialog open={!!pendingTipo} onOpenChange={(open) => { if (!open) setPendingTipo(null) }}. DialogTitle "Mudar tipo de cliente". DialogDescription "Mudar o tipo irá limpar os dados de {pendingTipo === "PARTICULAR" ? "Empresa" : "Particular"}. Continuar?". DialogFooter with outline Button "Cancelar" (onClick={() => setPendingTipo(null)}) and primary Button "Continuar" (onClick={confirmTipoChange}).

    ON SUBMIT PAYLOAD — in the onSubmit handler, update the ClienteCreateRequest payload to include tipo: values.tipo, avencado: values.avencado, dados_tipo: values.dados_tipo. Ensure numero_cliente is NOT included anywhere in the payload (it is backend-generated).
  </action>
  <verify>
    <automated>cd web && pnpm build 2>&1 | grep -E "error TS|Type error" | head -30</automated>
  </verify>
  <acceptance_criteria>
    - web/src/app/(dashboard)/clientes/novo/page.tsx contains import { RadioGroup, RadioGroupItem } from "@/components/ui/radio-group"
    - web/src/app/(dashboard)/clientes/novo/page.tsx contains import { Switch } from "@/components/ui/switch"
    - web/src/app/(dashboard)/clientes/novo/page.tsx contains pendingTipo
    - web/src/app/(dashboard)/clientes/novo/page.tsx contains watchedTipo
    - web/src/app/(dashboard)/clientes/novo/page.tsx contains confirmTipoChange
    - web/src/app/(dashboard)/clientes/novo/page.tsx contains "PARTICULAR"
    - web/src/app/(dashboard)/clientes/novo/page.tsx contains "dados_tipo.nome_comercial"
    - web/src/app/(dashboard)/clientes/novo/page.tsx contains "avencado"
    - web/src/app/(dashboard)/clientes/novo/page.tsx does NOT contain "numero_cliente" in the payload object (confirm no assignment to numero_cliente in the onSubmit function)
    - pnpm build exits 0
  </acceptance_criteria>
  <done>Create-client form has RadioGroup tipo selector, conditional Particular/Empresa field sections, confirmation Dialog on tipo change, and Avençado Switch. Build passes with zero TS errors.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Form → API (POST /api/v1/clientes) | Form submit payload must not include numero_cliente |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-58-03-01 | Tampering | ClienteCreateRequest payload | mitigate | numero_cliente excluded from defaultValues, schema, and onSubmit payload construction; acceptance criteria grep confirms absence |
| T-58-03-02 | Tampering | dados_tipo field clearing | accept | confirmTipoChange clears opposite tipo fields on the client before submit; backend should treat dados_tipo as a replace operation |
</threat_model>

<verification>
pnpm build exits 0. Manual browser check: navigate to /clientes/novo; Particular/Empresa RadioGroup visible at top; selecting Particular shows idade/sexo/nacionalidade; selecting Empresa shows nome_comercial/representante_legal/etc; switching tipo after filling shows confirmation Dialog; Avençado Switch toggles.
</verification>

<success_criteria>
- Form shows RadioGroup tipo selector at the top
- Conditional field sections show/hide based on watchedTipo
- pendingTipo Dialog appears when user changes tipo after it was already set
- Avençado Switch present and wired via Controller
- Submit payload includes tipo, avencado, dados_tipo; excludes numero_cliente
- pnpm build exits 0
</success_criteria>

<output>
Create `.planning/phases/LEXCV-58-formulario-dinamico/58-03-SUMMARY.md` when done
</output>
