---
milestone: v2.10
audited: 2026-07-10T16:30:00Z
status: tech_debt
resolution: "0 gaps_found. All 16 requirements satisfied across 3 independent sources (REQUIREMENTS.md traceability, SUMMARY.md frontmatter, VERIFICATION.md coverage tables). Integration checker found 0 BLOCKER-level cross-phase issues — only 2 non-blocking WARNINGs (inconsistent tab deep-link on DOCUMENTO_NOVO notifications; manual-migration-required DB dedup constraint). Remaining tech debt is overwhelmingly live-UAT deferral across 4 of 5 phases, driven by a single recurring pre-existing environment blocker (MINIO_ENDPOINT not substituted in this session) plus this project's standing lack of H2/Testcontainers integration-test infrastructure — not code defects introduced by this milestone."
scores:
  requirements: 16/16
  phases: 5/5
  integration: 16/16
  flows: 4/4
gaps:
  requirements: []
  integration: []
  flows: []
nyquist:
  compliant_phases: 0
  partial_phases: 0
  missing_phases: 5
  overall: "not applicable — workflow.nyquist_validation requires phase-level RESEARCH.md with a Validation Architecture section; research_enabled: false throughout this milestone (research-first was chosen at milestone start, producing SUMMARY.md files consumed directly by the roadmapper, not per-phase RESEARCH.md files with Nyquist sections)"
---

# Milestone v2.10 — Notificações e Alertas — Audit Report

**Audited:** 2026-07-10
**Status:** tech_debt (no blockers — all requirements and cross-phase wiring verified; accumulated live-UAT deferral needs review before/after close)

## Phase Verification Summary

| Phase | Status | Score | Notes |
|-------|--------|-------|-------|
| 85 — Consolidação da Lógica de "Prazo Crítico" | human_needed | 5/5 | 1 open product-decision item (ALTA-priority Evento with null dataFim) never answered across 3 review iterations — a business ambiguity, not a code defect |
| 86 — Infraestrutura de Notificações | human_needed | 11/11 plan truths (2/4 ROADMAP criteria clean, 2 uncertain) | The one native `nativeQuery=true`+`Pageable` combination in this codebase (`buscarPorFiltros`) has never executed against real Postgres — no H2/Testcontainers exists in this backend. Also: RBAC screen visual confirmation pending (low risk, mechanical reuse) |
| 87 — Alertas de Eventos (Fase/Documento/Atribuição/Parecer) | human_needed | 7/7 | Live E2E walkthrough blocked by MinIO env issue; 2 code-review-fixed defects (ParecerController partial-update data-loss bug, concurrent numeroVersao lock) have zero automated test coverage in this codebase |
| 88 — Verificação Diária de Prazos e Honorários | **passed** | 7/7 | Only clean pass in the milestone — pure backend, fully covered by 44 automated tests (re-run fresh, not trusted from prior reports), zero human-verification items |
| 89 — Sino e Página de Notificações | human_needed | 15/15 | Live E2E walkthrough blocked by the same MinIO env issue as Phase 87; 2 code-review-fixed defects (same-origin URL guard, pagination clamp) flagged for a final visual confirmation pass despite passing independent behavioral/lint re-verification |

No phase reported a code-level FAILED truth or BLOCKER anti-pattern. Every "human_needed" status traces to live-browser/HTTP verification gaps — either the recurring `MINIO_ENDPOINT` environment blocker (Phases 87, 89) or this project's standing absence of integration-test infrastructure (Phases 85, 86) — not defects found in the shipped code.

## Requirements Coverage (3-source cross-reference)

All 16 `NOTF-08`..`NOTF-23` requirements are checked `[x]` in `REQUIREMENTS.md`, mapped to a phase in its traceability table (status: Complete for all 16), and referenced in that phase's `SUMMARY.md` `requirements-completed` frontmatter (union across all 14 plan summaries reconstructs the same 16-ID set with no gaps). Every phase's own `VERIFICATION.md` independently re-derived and confirmed the same coverage against the live codebase — not by trusting the traceability table or SUMMARY frontmatter at face value.

**No orphans:** no requirement appears in the traceability table without a matching entry in at least one phase's `VERIFICATION.md` Requirements Coverage table.

## Cross-Phase Integration (gsd-integration-checker)

**Zero BLOCKER-level findings.** The integration checker independently re-ran `mvn compile` (backend), `tsc --noEmit` (frontend), and the full `AlertasDiariosJobTest`/`NotificacaoServiceTest`/`RiscoPrazoServiceTest` suite (44/44 passing) as executable cross-checks on its static analysis — not relying on any phase's own claims.

All 7 explicitly-audited integration seams resolved to WIRED:
1. Phase 85 → 88: `AlertasDiariosJob` calls `RiscoPrazoService`'s real methods, never a re-implementation (confirmed: zero re-implemented threshold literals in the job file).
2. Phase 86 → 87/88: every notification-creating call path in both phases goes exclusively through `NotificacaoService.criar`/`notificarAdmins` — a full-backend grep for `notificacaoRepository.save|saveAll` and `Notificacao.builder()` finds hits only inside `NotificacaoService.java` itself.
3. Phase 86 → 89: `use-notificacoes.ts`'s 4 hooks match `NotificacaoController`'s current REST surface byte-for-byte (routes, params, response shapes) — verified against the *current* backend contract, not what Phase 89's own plan assumed before execution.
4. Phase 87/88 → 89: exact 1:1 categoria parity — the 9 backend-emitted values (`FASE_ENTRADA`, `DOCUMENTO_NOVO`, `PROCESSO_ATRIBUIDO`, `PARECER_ATRIBUIDO`, `PRAZO_PROXIMO`, `PRAZO_VENCIDO`, `EVENTO_PROXIMO`, `EVENTO_VENCIDO`, `HONORARIO_ATRASADO`) match Phase 89's label/badge map exactly — no orphans either direction.
5. E2E flow (fase entrada → notification → sino → `?tab=fases` navigation): traced line-by-line through the full chain, including the `useEffect` re-sync Phase 87's own code review added to keep the deep-link working post-mount.
6. E2E flow (prazo → vencido → idempotent notification → filterable on `/notificacoes`): confirmed backed by both an application-level existence check and a DB-level `uk_notificacao_dedup` unique index.
7. RBAC: `notificacoes:view` seeded for all 4 roles, is the only scope ever checked, and the frontend's `KNOWN_SCOPES`/permission fallback chain always resolves consistently with the backend — no false access-denied risk for any role.

### Non-blocking findings

- **WARNING — Inconsistent tab deep-link on `DOCUMENTO_NOVO` (NOTF-16):** `notificarDocumentoNovo`'s `linkUrl` omits the `?tab=documentos` suffix that `notificarFaseEntrada` (NOTF-15) already established as the generic pattern for landing a user directly on the relevant tab. Not a broken link — the base route resolves and passes the same-origin guard — just an inconsistently-applied UX nicety. Candidate one-line fix for a future small phase, not a milestone blocker.
- **WARNING — Manual production migration required for DB-level dedup backstop (NOTF-20/21/23):** `backend/migrations/88-add-notificacao-dedup-unique-constraint.sql` (and `86-create-notificacao-table.sql`) are not auto-applied in prod (`ddl-auto: validate`); someone must run them by hand before/alongside deploy. This is an existing, well-documented project convention (identical to Phase 82's `uk_honorario_processo`), not a wiring defect — the primary idempotency mechanism (the application-level existence check) does not depend on this constraint to function correctly.

## Tech Debt (non-blocking, deferred)

| Category | Item | Status |
|----------|------|--------|
| verification_gap | Phase 85 — product decision never made: should an ALTA-priority `Evento` with null `dataFim` count as urgent in the two dashboard KPIs? Code review declined to resolve unilaterally 3 times | human_needed, no HUMAN-UAT.md (single product question, not a multi-step UAT) |
| verification_gap | Phase 86 — live round-trip of `GET /notificacoes`'s native query (first `nativeQuery=true`+`Pageable` combo in this codebase) never executed against real Postgres; RBAC Settings-screen visual confirmation pending | human_needed, `86-HUMAN-UAT.md` persisted |
| verification_gap | Phase 87 — Plan 87-04's full live E2E walkthrough (Reatribuir flow + `t_notificacao` row confirmation + `?tab=fases` deep-link) blocked by the MinIO env issue; `ParecerController.updateSolicitacao` partial-update fix and concurrent `numeroVersao` DB lock have zero automated test coverage | human_needed, `87-HUMAN-UAT.md` persisted |
| verification_gap | Phase 89 — full live E2E walkthrough (11 items: sino/página/filtros/paginação/cross-surface + 2 REVIEW-FIX visual follow-ups) blocked by the same MinIO env issue | human_needed, `89-HUMAN-UAT.md` persisted |
| tooling | `backend/migrations/86-create-notificacao-table.sql` and `88-add-notificacao-dedup-unique-constraint.sql` are standalone manual-execution scripts — no migration runner exists in this repo | must be run manually against the database before/alongside deploy |
| tooling | No H2/Testcontainers integration-test infrastructure exists anywhere in this backend — a project-wide, pre-existing limitation that specifically blocked live verification of Phase 86's native query and Phase 87's concurrent-lock fix | candidate for a future dedicated testing-infrastructure phase |
| tooling | `backend/spotbugs-exclude.xml`/SpotBugs SAST tooling broken against JDK 23 bytecode (discovered during Phase 87, unrelated background task) | flagged separately, not part of this milestone's scope |
| cosmetic | `notificarDocumentoNovo`'s `linkUrl` doesn't use the `?tab=documentos` deep-link pattern that `notificarFaseEntrada` established | candidate one-line fix, non-blocking |
| nyquist | All 5 phases show MISSING `*-VALIDATION.md` (discovery only — `workflow.nyquist_validation` is enabled, but this milestone's `research_enabled: false` setting meant no phase produced a RESEARCH.md with a Validation Architecture section to begin with) | informational, not auto-actioned |

## Recommendation

No gaps to close — every requirement, every cross-phase wire, and every E2E flow this audit checked is verified against the current codebase, not against any phase's own narrative. The accumulated tech debt above is exclusively **live-verification depth**, concentrated in a single recurring root cause (the MinIO env blocker) plus one pre-existing, project-wide infrastructure gap (no integration-test harness) — not evidence of incomplete or incorrect functionality. Milestone ready for `/gsd:complete-milestone v2.10`.
