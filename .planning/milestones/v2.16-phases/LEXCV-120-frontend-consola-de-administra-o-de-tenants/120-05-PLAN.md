---
phase: 120-frontend-consola-de-administra-o-de-tenants
plan: 05
type: execute
wave: 5
depends_on: [120-04]
files_modified:
  - web/src/app/(dashboard)/plataforma/page.tsx
  - web/scripts/verify-consola-tenants.mjs
  - web/package.json
autonomous: true
requirements: [PROV-02, PROV-03, PROV-04, PROV-05]

must_haves:
  truths:
    - "A rota /plataforma existe dentro do DashboardShell e mostra a lista de todos os tenants com plano, ocupacao e estado"
    - "Um utilizador sem o papel PLATAFORMA_ADMIN que va a /plataforma recebe AccessDeniedState em vez do ecra"
    - "Criar Tenant abre o painel inline, cria o tenant e volta a lista atualizada com toast de sucesso, sem recarregar a pagina"
    - "Editar abre um Dialog com Plano e Limite de Utilizadores e grava os dois em conjunto"
    - "Suspender e Reativar abrem um AlertDialog de confirmacao distinto cada um, com a copy do UI-SPEC"
    - "A pesquisa por nome filtra a lista do lado do cliente, em desktop e em mobile"
  artifacts:
    - path: "web/src/app/(dashboard)/plataforma/page.tsx"
      provides: "Consola de administracao de tenants completa (lista, criar, editar, suspender/reativar)"
      min_lines: 220
    - path: "web/scripts/verify-consola-tenants.mjs"
      provides: "Gate executavel de assercoes de origem sobre os 4 ficheiros da consola"
      min_lines: 80
  key_links:
    - from: "web/src/app/(dashboard)/plataforma/page.tsx"
      to: "web/src/hooks/use-platform-admin.ts"
      via: "useTenantsAdmin + as 3 mutacoes"
      pattern: "useTenantsAdmin"
    - from: "web/src/app/(dashboard)/plataforma/page.tsx"
      to: "web/src/components/shared/data-table/data-table.tsx"
      via: "DataTable com as colunas de plataforma, dentro de hidden md:block"
      pattern: "<DataTable"
    - from: "web/package.json"
      to: "web/scripts/verify-consola-tenants.mjs"
      via: "script npm verify:consola-tenants"
      pattern: "verify:consola-tenants"
---

<objective>
Compor o ecra `/plataforma` a partir das pecas ja construidas e fechar a fase com um gate executavel que prova, por leitura de origem, as afirmacoes que nao dependem de renderizacao ao vivo.

Purpose: e aqui que os 4 Success Criteria da Phase 120 se tornam observaveis num ecra. Todas as pecas dificeis ja existem — endpoints (Plan 02), hooks e navegacao (Plan 03), colunas e painel de criacao (Plan 04) — pelo que este plano e composicao e estado, nao invencao. O gate executavel segue o precedente `verify:limite-utilizadores` da Phase 118: torna repetivel a verificacao das assercoes estruturais, deixando ao checkpoint humano (Plan 06) apenas o que so se prova a ver funcionar.

Output: `page.tsx`, `web/scripts/verify-consola-tenants.mjs` e o script npm `verify:consola-tenants`.
</objective>

<execution_context>
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@web/AGENTS.md
@.planning/phases/LEXCV-120-frontend-consola-de-administra-o-de-tenants/120-UI-SPEC.md
@.planning/phases/LEXCV-120-frontend-consola-de-administra-o-de-tenants/120-PATTERNS.md
@.planning/phases/LEXCV-120-frontend-consola-de-administra-o-de-tenants/120-03-SUMMARY.md
@.planning/phases/LEXCV-120-frontend-consola-de-administra-o-de-tenants/120-04-SUMMARY.md
</context>

<interfaces>
<!-- Contratos ja confirmados no planeamento. NAO reexplorar o codebase para os redescobrir. -->

Do Plan 03 (`web/src/hooks/use-platform-admin.ts`):
- `useTenantsAdmin()` -> `UseQueryResult<TenantAdminSummary[]>`, chave `["platform","tenants","list"]`, `staleTime` 30s.
- `useCreateTenant()` -> mutacao com `mutateAsync(payload: SetupInitializeRequest)`.
- `useUpdateTenant()` -> mutacao com `mutateAsync({ id, payload: TenantUpdateRequest })`.
- `useSetTenantAtivo()` -> mutacao com `mutateAsync({ id, ativo })`.
As 3 mutacoes ja invalidam a listagem no `onSuccess`. Nenhuma dispara toasts — a copy dos toasts pertence a este ecra.

Do Plan 04:
- `web/src/app/(dashboard)/plataforma/columns.tsx` -> `columns({ onEdit, onToggleAtivo }): ColumnDef<TenantAdminSummary>[]` e `TENANT_RESERVADO` (`"LexCV"`).
- `web/src/app/(dashboard)/plataforma/criar-tenant-panel.tsx` -> `CriarTenantPanel({ onCancel, onSubmit, isSubmitting })`, onde `onSubmit: (payload: SetupInitializeRequest) => Promise<void>`.

`web/src/components/shared/data-table/data-table.tsx` — `DataTable<TData, TValue>({ columns, data, getRowId? })`. Configura apenas core/sorted/pagination row models: **nao faz filtragem** por decisao de arquitetura da Phase 104; a filtragem pertence ao ecra.

`web/src/components/shared/access-denied-state.tsx` (42 linhas) — `AccessDeniedState({ title?, description?, backHref?, backLabel? })`.

`web/src/hooks/use-me.ts` — `useMe()` -> `UseQueryResult<MeResponse>` com `isFetched`; `MeResponse.roles: Role[]` ja inclui `"PLATAFORMA_ADMIN"` desde o Plan 03.

`web/src/hooks/use-toast.ts` — `toast.success(msg)` / `toast.error(msg)`, sonner por baixo.

`web/src/app/(dashboard)/clientes/page.tsx` — analogo do ecra: guard de RBAC no topo, bloco mobile `<div className="md:hidden divide-y …">` com cards empilhados (avatar de iniciais + nome + badges + botoes `h-12 w-12 p-0`), e bloco desktop `<div className="hidden md:block"><DataTable columns={…} data={…} getRowId={(c) => c.id} /></div>`. **NAO copiar** a fila de 4 cards de KPI (linhas 304-332) — o UI-SPEC exclui-a explicitamente para este ecra.

`web/src/app/(dashboard)/settings/page.tsx` (`UserManagementTab`) — analogo do alternador de painel: `const [isFormOpen, setIsFormOpen] = React.useState(false)` e o Card de lista renderizado quando fechado / o Card de formulario quando aberto. **NAO copiar** `setTimeout(() => window.location.reload(), 1000)` (linhas 249-251, 306-309) — artefacto da era da base de dados simulada, proibido pelo UI-SPEC.

`web/src/app/(dashboard)/financeiro/[id]/page.tsx` linhas 284-348 — forma do `Dialog` com formulario e `DialogFooter` (`DialogClose asChild` no Cancelar, botao de submissao com texto alternativo "A guardar...").
`web/src/app/(dashboard)/agenda/[id]/page.tsx` linhas 187-238 — forma do `AlertDialog` de accao consequente (`AlertDialogCancel` / `AlertDialogAction` com `onClick={(e) => { e.preventDefault(); … }}`).

Copy fixada pelo `120-UI-SPEC.md` — usar verbatim:
- `<h1>`: "Administração de Tenants"; breadcrumb "LexCV / Consola de Plataforma".
- CardTitle "Tenants Registados"; CardDescription "Lista de organizações com acesso à plataforma LexCV."
- Pesquisa: placeholder "Pesquisar tenant por nome..."
- CTA: "Criar Tenant" (icone `Plus`), `className="bg-blue-600 hover:bg-blue-700 text-white …"`.
- Toasts: "Tenant criado com sucesso." / "Tenant atualizado com sucesso." / "Tenant suspenso com sucesso." / "Tenant reativado com sucesso."
- Dialog de edicao: DialogTitle "Editar Tenant"; DialogDescription "Ajuste o plano e o limite de utilizadores de {nome}."; Label "Plano" (`NativeSelect` com STARTER/STANDARD/ENTERPRISE, valor cru como `value` e como texto); Label "Limite de Utilizadores" (`Input type="number"`, placeholder "Sem limite", helper "Deixe em branco para não aplicar limite de utilizadores."); rodape "Cancelar" (outline) / "Guardar" (`bg-blue-600 hover:bg-blue-700 text-white`).
- AlertDialog de suspensao: AlertDialogTitle "Suspender Tenant"; AlertDialogDescription "Esta ação bloqueia de imediato o acesso de todos os utilizadores de {nome}, incluindo sessões já iniciadas. Pode reativar o tenant a qualquer momento."; "Cancelar" / "Suspender" (`bg-red-600 hover:bg-red-700 text-white`).
- AlertDialog de reativacao: AlertDialogTitle "Reativar Tenant"; AlertDialogDescription "Os utilizadores de {nome} recuperam o acesso de imediato."; "Cancelar" / "Reativar" (`bg-emerald-600 hover:bg-emerald-700 text-white`).
- Acesso negado: `AccessDeniedState` com description "Não tem permissão para aceder à consola de administração de tenants." e `backHref="/dashboard"`.

`web/package.json` — seccao `scripts` ja tem `verify:juizo-origem` e `verify:limite-utilizadores`, ambos `node scripts/<ficheiro>.mjs`. `web/scripts/verify-limite-utilizadores-indicator.mjs` (181 linhas) e o modelo: Node puro sem dependencias, `fs/promises` + `path` + `fileURLToPath`, funcao `stripComments(source)` para normalizar antes de testar, um predicado por assercao, `PASS <id>` / `FAIL <id> — <motivo>` por linha e `process.exit(1)` se alguma falhar.

Nota Next.js 16 (`web/AGENTS.md`): `page.tsx` e um componente de cliente (`"use client"`) dentro do route group `(dashboard)` existente, tal como `clientes/page.tsx`. Nao introduz rota dinamica, nao usa `params`/`searchParams` assincronos, nao toca em `proxy.ts`. Se surgir alguma duvida sobre convencoes do App Router nesta versao, consultar `web/node_modules/next/dist/docs/` em vez de assumir convencoes antigas.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Ecra /plataforma — guard, lista, pesquisa, cards mobile e painel de criacao</name>

  <files>
    web/src/app/(dashboard)/plataforma/page.tsx (novo)
  </files>

  <read_first>
    - `web/src/app/(dashboard)/clientes/page.tsx` linhas 1-100 e 470-550 — o guard de RBAC no topo do componente e os dois blocos de renderizacao (mobile `md:hidden` com cards, desktop `hidden md:block` com `DataTable`).
    - `web/src/app/(dashboard)/settings/page.tsx` linhas 180-230 e 380-430 — o estado `isFormOpen`/`searchTerm` e o Card de lista com CTA no `CardHeader` e `Input` de pesquisa no topo do `CardContent`.
    - `web/src/components/shared/access-denied-state.tsx` (ficheiro completo, 42 linhas) — as props aceites.
    - `web/src/app/(dashboard)/plataforma/columns.tsx` e `criar-tenant-panel.tsx` (Plan 04) — as assinaturas exatas que este ficheiro vai consumir.
    - `.planning/phases/LEXCV-120-frontend-consola-de-administra-o-de-tenants/120-UI-SPEC.md` seccoes "Route & navigation", "Default view — Tenants list" e "Criar Tenant (inline panel, not a modal)".
  </read_first>

  <action>
    Criar `web/src/app/(dashboard)/plataforma/page.tsx` com `"use client"`.

    **Guard de pagina (defesa em profundidade).** `const me = useMe();` e, quando `me.isFetched && !me.data?.roles?.includes("PLATAFORMA_ADMIN")`, devolver `<AccessDeniedState description="Não tem permissão para aceder à consola de administração de tenants." backHref="/dashboard" />`. Escrever um comentario curto a registar que a camada autoritativa e o `@PreAuthorize("hasRole('PLATAFORMA_ADMIN')")` de classe do `PlatformAdminController` e que este guard e apenas espelho de UX — mesma disciplina de duas camadas que `clientes/page.tsx` ja pratica.

    **Cabecalho da pagina.** Breadcrumb "LexCV / Consola de Plataforma" e `<h1 className="text-3xl font-bold tracking-tight">Administração de Tenants</h1>`, seguindo o mesmo bloco de cabecalho das outras paginas do `(dashboard)`. **Sem fila de cards de KPI** — o UI-SPEC exclui-a explicitamente.

    **Estado.** `const [isFormOpen, setIsFormOpen] = React.useState(false);` e `const [searchTerm, setSearchTerm] = React.useState("");`. Os estados dos dialogos ficam para a Task 2 — nesta task declarar apenas os handlers `onEdit`/`onToggleAtivo` como funcoes vazias com um `// Task 2` a marcar o ponto de ligacao, para as colunas ja poderem ser instanciadas.

    **Dados.** `const tenants = useTenantsAdmin();` e `const criarTenant = useCreateTenant();`. Derivar a lista filtrada com `React.useMemo`, comparando `nome` em minusculas contra `searchTerm.trim().toLowerCase()` (filtragem do lado do cliente, porque o `DataTable` partilhado nao configura row filtering — decisao de arquitetura da Phase 104 — e o endpoint nao aceita filtros).

    **Card de lista** (renderizado quando `!isFormOpen`): `Card` com as classes de `UserManagementTab`; `CardHeader className="flex flex-row items-center justify-between space-y-0"` com CardTitle "Tenants Registados" + CardDescription "Lista de organizações com acesso à plataforma LexCV." a esquerda e, a direita, o botao "Criar Tenant" com icone `Plus` e `className="bg-blue-600 hover:bg-blue-700 text-white …"` que faz `setIsFormOpen(true)`; `CardContent className="space-y-4"` com o `Input` de pesquisa (placeholder "Pesquisar tenant por nome...") seguido dos dois blocos de tabela.

    **Bloco mobile** (`<div className="md:hidden divide-y divide-slate-200 dark:divide-slate-800">`): um cartao por tenant filtrado, espelhando estruturalmente o bloco mobile de `clientes/page.tsx` — avatar de iniciais com as mesmas classes, nome em `font-bold` como texto simples (nao link), badge "Plataforma" (`variant="outline"`) quando `nome === TENANT_RESERVADO`, badges de plano e estado inline, e por baixo os dois botoes de accao a `h-12 w-12 p-0` (alvo tactil de 48px), cada um envolvido em `Tooltip` e com a mesma regra de desactivacao da linha reservada, incluindo o `<span tabIndex={0}>` — a guarda tem de existir nos dois modos de apresentacao, nao so no desktop.

    **Bloco desktop** (`<div className="hidden md:block">`): `<DataTable columns={tenantColumns} data={tenantsFiltrados} getRowId={(t) => t.id} />`, com `const tenantColumns = React.useMemo(() => columns({ onEdit, onToggleAtivo }), [onEdit, onToggleAtivo]);` (envolver `onEdit`/`onToggleAtivo` em `React.useCallback` para o memo nao se invalidar em cada render).

    **Estados de carregamento e erro.** Enquanto `tenants.isLoading`, mostrar um estado de carregamento simples dentro do `CardContent` (texto ou skeleton, conforme o que as outras listas deste app ja fazem). Se `tenants.isError`, mostrar uma mensagem inline — o `apiFetch` ja fez o toast automatico, nao duplicar.

    **Painel de criacao** (renderizado quando `isFormOpen`, em vez do Card de lista): `<CriarTenantPanel onCancel={() => setIsFormOpen(false)} isSubmitting={criarTenant.isPending} onSubmit={…} />`. O `onSubmit` faz `await criarTenant.mutateAsync(payload)`, e em caso de sucesso dispara `toast.success("Tenant criado com sucesso.")` e `setIsFormOpen(false)`; em caso de erro, apanha a excecao e mantem o painel aberto (o `apiFetch` ja mostrou o toast com a mensagem do backend, incluindo "Já existe um utilizador com este email.") — nunca deixar a excecao subir sem tratamento e nunca fechar o painel num erro, para o operador nao perder o que escreveu.

    Proibicoes: NAO usar `window.location.reload()` em lado nenhum. NAO acrescentar fila de KPIs. NAO transformar o nome do tenant em `<Link>`. NAO acrescentar accao de eliminar tenant. NAO duplicar o literal `"LexCV"` — importar `TENANT_RESERVADO` de `./columns`. NAO chamar `apiFetch` diretamente — sempre pelos hooks do Plan 03.
  </action>

  <verify>
    <automated>cd web && npx tsc --noEmit -p tsconfig.json && pnpm lint && grep -q 'AccessDeniedState' "src/app/(dashboard)/plataforma/page.tsx" && grep -q 'CriarTenantPanel' "src/app/(dashboard)/plataforma/page.tsx" && grep -q '<DataTable' "src/app/(dashboard)/plataforma/page.tsx" && test $(grep -c 'window.location.reload' "src/app/(dashboard)/plataforma/page.tsx") -eq 0 && test $(grep -c 'TENANT_RESERVADO' "src/app/(dashboard)/plataforma/page.tsx") -ge 1</automated>
  </verify>

  <acceptance_criteria>
    - `npx tsc --noEmit` e `pnpm lint` terminam ambos com exit code 0.
    - O guard usa `me.isFetched && !me.data?.roles?.includes("PLATAFORMA_ADMIN")` e devolve `AccessDeniedState` com a description exata `Não tem permissão para aceder à consola de administração de tenants.` e `backHref="/dashboard"`.
    - Existe exatamente um `<DataTable` e ele esta dentro de um contentor com `hidden md:block`; existe exatamente um bloco `md:hidden` com os cards mobile.
    - `grep -c 'window.location.reload'` devolve 0.
    - `grep -c '"LexCV"'` devolve 0 no `page.tsx` — o literal vem por `TENANT_RESERVADO` importado de `./columns`.
    - As frases `Tenants Registados`, `Lista de organizações com acesso à plataforma LexCV.`, `Pesquisar tenant por nome...`, `Criar Tenant`, `Administração de Tenants` e `Tenant criado com sucesso.` aparecem verbatim.
    - `grep -c 'KPI\|kpi'` devolve 0 — nenhuma fila de indicadores.
    - `grep -c '<Link'` devolve 0.
    - O bloco mobile contem `tabIndex={0}` — a guarda do tenant reservado esta presente tambem em mobile.
    - `pnpm build` termina com exit code 0.
  </acceptance_criteria>

  <done>A rota `/plataforma` existe, e gated por papel, lista os tenants em desktop e mobile com pesquisa por nome, e cria tenants pelo painel inline com invalidacao de cache e toast, sem recarregar a pagina.</done>
</task>

<task type="auto">
  <name>Task 2: Dialog de edicao e AlertDialogs de suspender/reativar</name>

  <files>
    web/src/app/(dashboard)/plataforma/page.tsx
  </files>

  <read_first>
    - `web/src/app/(dashboard)/plataforma/page.tsx` — o estado da Task 1 e os handlers `onEdit`/`onToggleAtivo` marcados como pontos de ligacao.
    - `web/src/app/(dashboard)/financeiro/[id]/page.tsx` linhas 284-348 — a forma do `Dialog` com formulario, `DialogFooter`, `DialogClose asChild` e texto alternativo no botao de submissao.
    - `web/src/app/(dashboard)/agenda/[id]/page.tsx` linhas 187-238 — a forma do `AlertDialog`, incluindo `onClick={(e) => { e.preventDefault(); … }}` no `AlertDialogAction`.
    - `web/src/components/ui/native-select.tsx` (ficheiro completo) — a assinatura do `NativeSelect` a usar no campo Plano.
    - `.planning/phases/LEXCV-120-frontend-consola-de-administra-o-de-tenants/120-UI-SPEC.md` seccoes "Editar Tenant (Dialog, modal)" e "Suspender/Reativar (AlertDialog, per row)" — a copy verbatim e a proibicao explicita de fundir a suspensao no Dialog de edicao.
  </read_first>

  <action>
    Acrescentar ao `page.tsx` o estado dos dois dialogos, ambos controlados a partir da pagina (nao das linhas): `const [tenantEmEdicao, setTenantEmEdicao] = React.useState<TenantAdminSummary | null>(null);` e `const [tenantEmAlteracaoDeEstado, setTenantEmAlteracaoDeEstado] = React.useState<TenantAdminSummary | null>(null);`. Ligar `onEdit` a `setTenantEmEdicao` e `onToggleAtivo` a `setTenantEmAlteracaoDeEstado`, substituindo os corpos vazios da Task 1. Instanciar `const atualizarTenant = useUpdateTenant();` e `const alterarEstado = useSetTenantAtivo();`.

    **Dialog "Editar Tenant"** (`Dialog`, nao `AlertDialog` — e uma edicao reversivel de formulario, nao uma accao imediata), aberto quando `tenantEmEdicao !== null`, `onOpenChange` a repor `null`. DialogTitle "Editar Tenant"; DialogDescription "Ajuste o plano e o limite de utilizadores de {nome}." com o nome do tenant interpolado. Exatamente 2 campos, com estado local sincronizado a partir do tenant selecionado quando o dialogo abre:
    - Label "Plano" + `NativeSelect` com 3 opcoes, valor cru como `value` e como texto visivel (STARTER / STANDARD / ENTERPRISE).
    - Label "Limite de Utilizadores" + `Input type="number"` com placeholder "Sem limite" e, por baixo, o helper "Deixe em branco para não aplicar limite de utilizadores.". Campo vazio significa `null`; um valor preenchido tem de ser inteiro `>= 1` (validar antes de submeter e mostrar erro inline, sem chamar a API — o backend rejeita na mesma com `400`, esta e a camada de UX).
    Rodape: "Cancelar" (`variant="outline"`, fecha sem gravar) e "Guardar" (`type="submit"`, `className="bg-blue-600 hover:bg-blue-700 text-white"`, `disabled` enquanto `atualizarTenant.isPending`, texto alternativo "A guardar..."). Ao submeter: `await atualizarTenant.mutateAsync({ id, payload: { plano, limiteUtilizadores } })`, depois `toast.success("Tenant atualizado com sucesso.")` e fechar; em erro, manter aberto (o toast do `apiFetch` ja informou).

    **AlertDialog de alteracao de estado**, aberto quando `tenantEmAlteracaoDeEstado !== null`. O conteudo depende de `tenantEmAlteracaoDeEstado.ativo`:
    - A suspender (`ativo === true`): AlertDialogTitle "Suspender Tenant"; AlertDialogDescription "Esta ação bloqueia de imediato o acesso de todos os utilizadores de {nome}, incluindo sessões já iniciadas. Pode reativar o tenant a qualquer momento."; `AlertDialogAction` com o rotulo "Suspender" e `className="bg-red-600 hover:bg-red-700 text-white"`.
    - A reativar (`ativo === false`): AlertDialogTitle "Reativar Tenant"; AlertDialogDescription "Os utilizadores de {nome} recuperam o acesso de imediato."; `AlertDialogAction` com o rotulo "Reativar" e `className="bg-emerald-600 hover:bg-emerald-700 text-white"`.
    Em ambos, `AlertDialogCancel` com "Cancelar". No `AlertDialogAction`, usar `onClick={(e) => { e.preventDefault(); void confirmar(); }}` (mesmo padrao de `agenda/[id]/page.tsx`) para o dialogo nao fechar antes de a mutacao terminar. `confirmar` faz `await alterarEstado.mutateAsync({ id, ativo: !tenantEmAlteracaoDeEstado.ativo })` e depois `toast.success(...)` com "Tenant suspenso com sucesso." ou "Tenant reativado com sucesso." conforme a direccao, fechando a seguir. Em erro (por exemplo, a tentativa de suspender a tenant reservada devolve `400` com a mensagem do backend), manter o dialogo aberto — o toast automatico do `apiFetch` ja explicou o motivo.

    **Nunca** fundir a alternancia de estado no Dialog de edicao: o UI-SPEC proibe-o explicitamente porque suspender tem garantia de efeito imediato e e uma accao directa, enquanto o Dialog de edicao e um rascunho adiado ate ao "Guardar". Sao dois componentes distintos, com dois gatilhos distintos.

    Proibicoes: NAO acrescentar um campo `ativo` ao Dialog de edicao. NAO usar `Dialog` para a confirmacao de suspensao (tem de ser `AlertDialog`). NAO usar `bg-destructive` no botao de suspender — o UI-SPEC fixa `bg-red-600 hover:bg-red-700 text-white`. NAO construir uma unica frase de confirmacao generica para as duas direccoes. NAO usar `window.confirm`.
  </action>

  <verify>
    <automated>cd web && npx tsc --noEmit -p tsconfig.json && pnpm lint && pnpm build && grep -q 'Suspender Tenant' "src/app/(dashboard)/plataforma/page.tsx" && grep -q 'Reativar Tenant' "src/app/(dashboard)/plataforma/page.tsx" && grep -q 'bg-red-600 hover:bg-red-700' "src/app/(dashboard)/plataforma/page.tsx" && grep -q 'bg-emerald-600 hover:bg-emerald-700' "src/app/(dashboard)/plataforma/page.tsx" && test $(grep -c 'window.confirm' "src/app/(dashboard)/plataforma/page.tsx") -eq 0</automated>
  </verify>

  <acceptance_criteria>
    - `npx tsc --noEmit`, `pnpm lint` e `pnpm build` terminam todos com exit code 0.
    - Existe exatamente um `<Dialog` (edicao) e exatamente um `<AlertDialog` (alteracao de estado) no ficheiro.
    - O Dialog de edicao tem exatamente 2 campos e nenhum deles e `ativo` — `grep -c 'ativo' ` dentro do bloco do Dialog devolve 0 (verificar por leitura do bloco).
    - As 4 frases de copy dos dialogos aparecem verbatim: `Suspender Tenant`, `Esta ação bloqueia de imediato o acesso de todos os utilizadores de`, `Reativar Tenant`, `Os utilizadores de`.
    - As 3 frases de toast desta task aparecem verbatim: `Tenant atualizado com sucesso.`, `Tenant suspenso com sucesso.`, `Tenant reativado com sucesso.`
    - O botao de suspender usa `bg-red-600 hover:bg-red-700 text-white` e o de reativar `bg-emerald-600 hover:bg-emerald-700 text-white`; `grep -c 'bg-destructive'` devolve 0.
    - `grep -c 'window.confirm'` devolve 0.
    - O helper `Deixe em branco para não aplicar limite de utilizadores.` aparece verbatim.
  </acceptance_criteria>

  <done>Editar plano/limite e alternar suspenso/ativo funcionam a partir da lista, cada um no seu componente proprio, com a copy exata do UI-SPEC e sem fundir a accao imediata com o formulario adiado.</done>
</task>

<task type="auto">
  <name>Task 3: Gate executavel verify:consola-tenants</name>

  <files>
    web/scripts/verify-consola-tenants.mjs (novo)
    web/package.json
  </files>

  <read_first>
    - `web/scripts/verify-limite-utilizadores-indicator.mjs` (ficheiro completo, 181 linhas) — o modelo exato: Node puro sem dependencias, `stripComments`, um predicado por assercao, saida `PASS <id>` / `FAIL <id> — <motivo>`, `process.exit(1)` se alguma falhar.
    - `web/package.json` seccao `scripts` — a forma dos 2 scripts `verify:*` ja registados.
    - Os 4 ficheiros que o gate vai inspecionar: `web/src/app/(dashboard)/plataforma/page.tsx`, `columns.tsx`, `criar-tenant-panel.tsx` e `web/src/components/shared/dashboard-shell.tsx`.
  </read_first>

  <action>
    Criar `web/scripts/verify-consola-tenants.mjs` no mesmo estilo de `verify-limite-utilizadores-indicator.mjs`: Node puro (`fs/promises`, `path`, `url`), sem dependencias, sem import de modulos TypeScript — os ficheiros sao lidos como texto e normalizados com uma funcao `stripComments` propria antes de qualquer predicado correr (isto e obrigatorio: sem `stripComments`, um comentario que mencione um termo faria passar uma assercao que na verdade nao esta implementada).

    Implementar exatamente estas 10 assercoes, cada uma com id proprio e mensagem de falha explicativa:
    1. `role-union` — `web/src/types/auth.ts` inclui `"PLATAFORMA_ADMIN"` na uniao `Role`.
    2. `nav-dois-call-sites` — `dashboard-shell.tsx` tem 2 ocorrencias de `nav={navItems}` e 0 de `nav={NAV}`.
    3. `sidebar-nav-intocado` — `sidebar-nav.tsx` NAO contem `PLATAFORMA_ADMIN` (o gate por papel vive no shell, nao no componente generico).
    4. `guard-de-pagina` — `plataforma/page.tsx` contem `AccessDeniedState` e a verificacao `roles?.includes("PLATAFORMA_ADMIN")`.
    5. `sem-reload` — nenhum dos 3 ficheiros da consola contem `window.location.reload`.
    6. `tooltip-span-wrapper` — `columns.tsx` contem um bloco em que `<TooltipTrigger asChild>` e seguido de `<span tabIndex={0}>` que envolve um `Button` com `disabled` (reaproveitar a tecnica de localizacao de bloco do script da Phase 118, que ja resolve exatamente este problema para o mesmo padrao).
    7. `guarda-tenant-reservado` — `columns.tsx` contem a frase `Não é possível suspender o tenant da plataforma (LexCV).` byte-identica.
    8. `schema-setup-reutilizado` — `criar-tenant-panel.tsx` importa de `@/schemas/setup` e nao declara nenhum regex proprio de password (procurar a ausencia de `(?=.*` no ficheiro).
    9. `dialogos-separados` — `page.tsx` contem `Suspender Tenant` e `Reativar Tenant` como titulos distintos e o Dialog de edicao nao contem a palavra `Suspender` (a alternancia de estado nunca foi fundida no formulario de edicao).
    10. `mobile-tem-guarda` — `page.tsx` contem `tabIndex={0}` (a guarda do tenant reservado tambem existe no bloco de cards mobile, nao so nas colunas de desktop).

    Registar o script em `web/package.json` como `"verify:consola-tenants": "node scripts/verify-consola-tenants.mjs"`, imediatamente a seguir a `verify:limite-utilizadores`. Nao alterar mais nada no `package.json` — nenhuma dependencia nova.

    Escrever um comentario de topo no script a registar o que ele NAO consegue provar e que fica portanto para o checkpoint humano do Plan 06: que o tooltip realmente aparece no ecra, que a suspensao corta mesmo uma sessao ja aberta, e que os badges renderizam com as cores certas. Um gate de origem prova estrutura, nao renderizacao — e este projeto ja acumulou `verification_gap` recorrentes por confundir as duas coisas (ver Phase 114 em STATE.md).

    Proibicoes: NAO usar dependencias externas no script. NAO importar os ficheiros `.tsx` como modulos. NAO usar `grep -c` sem filtrar comentarios — todas as contagens correm sobre a fonte ja normalizada por `stripComments`. NAO escrever assercoes sobre estilo visual (cores, espacamentos) — isso e do dominio do checkpoint humano e da auditoria de UI.
  </action>

  <verify>
    <automated>cd web && node scripts/verify-consola-tenants.mjs && pnpm verify:consola-tenants && grep -q 'verify:consola-tenants' package.json && test $(node -e "const p=require('./package.json');console.log(Object.keys(p.dependencies).length+Object.keys(p.devDependencies).length)") -eq $(git show HEAD:web/package.json | node -e "let s='';process.stdin.on('data',d=>s+=d).on('end',()=>{const p=JSON.parse(s);console.log(Object.keys(p.dependencies).length+Object.keys(p.devDependencies).length)})")</automated>
  </verify>

  <acceptance_criteria>
    - `pnpm verify:consola-tenants` termina com exit code 0 e imprime 10 linhas `PASS`, uma por assercao, com os ids acima.
    - O script nao importa nenhuma dependencia externa — `grep -cE "^import .* from \"(?!node:)" scripts/verify-consola-tenants.mjs` nao encontra imports fora de `node:`.
    - O script define e usa uma funcao `stripComments` antes de qualquer predicado.
    - `web/package.json` ganhou exatamente uma linha em `scripts` e nenhuma em `dependencies`/`devDependencies` — o numero total de dependencias e identico ao de `git show HEAD:web/package.json`.
    - Forcar uma falha artificial (por exemplo, comentar temporariamente o `tabIndex={0}` de `columns.tsx`) faz o script imprimir `FAIL tooltip-span-wrapper` e sair com codigo 1; reverter a alteracao repoe o `PASS`. Registar no SUMMARY que esta contra-prova foi feita.
    - O comentario de topo do script enumera explicitamente as 3 afirmacoes que ficam para o checkpoint humano.
  </acceptance_criteria>

  <done>Existe um gate executavel de 10 assercoes que qualquer sessao futura pode correr com `pnpm verify:consola-tenants`, e a sua contra-prova (falha forcada) foi confirmada.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser -> `/plataforma` | Rota de cliente; o guard visual nao substitui a autorizacao do backend |
| Formulario de edicao -> `PUT /platform/tenants/{id}` | Valores de plano/limite escritos pelo operador, aplicados a um tenant que nao e o seu |
| Confirmacao de suspensao -> `PATCH /platform/tenants/{id}/ativo` | Accao de consequencia imediata sobre todos os utilizadores de uma organizacao |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-120-24 | Elevation of Privilege | Um utilizador sem papel de plataforma escrever `/plataforma` no browser | mitigate | Guard de pagina com `AccessDeniedState` (Task 1) como espelho de UX; a fronteira real e o `@PreAuthorize` de classe (Plan 02), que devolve `403` a qualquer pedido — nenhum dado chega ao ecra mesmo que o guard fosse contornado. O gate `guard-de-pagina` verifica a presenca do espelho |
| T-120-25 | Denial of Service | Suspender por engano o tenant errado, cortando o acesso de uma organizacao inteira de imediato | mitigate | `AlertDialog` de confirmacao obrigatorio, com o nome do tenant interpolado na descricao e a consequencia escrita por extenso ("incluindo sessões já iniciadas"). Nunca `window.confirm`, nunca accao de um so clique. A reactivacao esta sempre disponivel e a sua descricao diz que o acesso volta de imediato |
| T-120-26 | Tampering | Fundir a suspensao no formulario de edicao e o operador suspender sem se aperceber | mitigate | Proibicao explicita na `<action>` da Task 2 e assercao `dialogos-separados` no gate executavel (Task 3) |
| T-120-27 | Tampering | `limiteUtilizadores` a 0 gravado por engano, bloqueando a criacao de utilizadores num tenant | mitigate | Validacao no cliente (`>= 1` ou vazio) antes de chamar a API, mais a validacao autoritativa do backend (Plan 02, caso de teste 6) |
| T-120-28 | Information Disclosure | Estado obsoleto na lista depois de uma mutacao dar a impressao de que a accao nao surtiu efeito | mitigate | As 3 mutacoes invalidam `["platform","tenants","list"]` (Plan 03) e a assercao `sem-reload` garante que a atualizacao vem da invalidacao e nao de um recarregamento de pagina |
| T-120-29 | Repudiation | Gate de origem ser confundido com prova de que a UI funciona | mitigate | O comentario de topo do script enumera as 3 afirmacoes que so o checkpoint humano do Plan 06 pode fechar; criterio de aceitacao verifica a presenca dessa enumeracao |
| T-120-SC | Tampering | Instalacoes npm/pnpm | n/a | Zero dependencias novas — criterio de aceitacao da Task 3 compara a contagem de dependencias com `git show HEAD:web/package.json`. So e acrescentada uma entrada em `scripts`. O Package Legitimacy Gate nao se aplica |
</threat_model>

<verification>
1. `cd web && npx tsc --noEmit` exit code 0 apos cada task.
2. `cd web && pnpm lint` exit code 0.
3. `cd web && pnpm build` exit code 0.
4. `cd web && pnpm verify:consola-tenants` imprime 10 `PASS` e sai com 0.
5. Contra-prova do gate: uma falha forcada produz `FAIL <id>` e exit code 1.
6. `git diff -- web/package.json` mostra apenas uma linha nova em `scripts`.
</verification>

<success_criteria>
- Os 4 Success Criteria da Phase 120 estao implementados e alcancaveis num unico ecra: criar tenant, listar com utilizacao, ajustar plano/limite, suspender/reativar.
- A copy do ecra e byte-identica ao contrato aprovado em `120-UI-SPEC.md`.
- A actualizacao da lista apos qualquer mutacao vem de invalidacao de cache, nunca de recarregamento de pagina.
- Existe um gate repetivel que qualquer sessao futura pode correr para detetar regressoes estruturais nesta consola.
</success_criteria>

<output>
Criar `.planning/phases/LEXCV-120-frontend-consola-de-administra-o-de-tenants/120-05-SUMMARY.md` quando terminar.
</output>
