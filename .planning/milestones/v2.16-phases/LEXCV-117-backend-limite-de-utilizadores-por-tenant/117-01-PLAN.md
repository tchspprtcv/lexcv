---
phase: 117-backend-limite-de-utilizadores-por-tenant
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/main/java/com/lexcv/models/TenantPlano.java
  - backend/src/main/java/com/lexcv/models/Tenant.java
  - backend/src/main/java/com/lexcv/repositories/UserRepository.java
  - backend/migrations/117-add-tenant-plano-limite-utilizadores.sql
autonomous: true
requirements: [PLAN-01]

must_haves:
  truths:
    - "A entidade Tenant tem dois campos persistidos novos: plano (enum TenantPlano gravado por nome) e limiteUtilizadores (Integer que aceita NULL)"
    - "limiteUtilizadores a NULL significa 'sem limite' — nao existe nenhum valor sentinela (-1, MAX_VALUE) a representar essa condicao"
    - "Existe um unico metodo de repositorio reutilizavel que devolve o numero de utilizadores ativos de um tenant, filtrado por tenantId e por ativo=true"
    - "Existe um script de migracao manual que acrescenta as colunas plano e limite_utilizadores a t_tenant com os nomes exatos usados pelo mapeamento JPA"
    - "O script de migracao poe plano=ENTERPRISE no tenant ja existente e deixa limite_utilizadores a NULL, pelo que o unico tenant real de producao nunca fica bloqueado por esta fase"
    - "O backend compila depois das alteracoes, sem novas dependencias no pom.xml"
    - "Nenhum endpoint passa a devolver a entidade Tenant crua — os novos campos de faturacao nao sao serializados para nenhum cliente"
  artifacts:
    - path: "backend/src/main/java/com/lexcv/models/TenantPlano.java"
      provides: "Enum de planos comerciais persistido por nome"
      contains: "ENTERPRISE"
      min_lines: 5
    - path: "backend/src/main/java/com/lexcv/models/Tenant.java"
      provides: "Campos plano e limiteUtilizadores na entidade Tenant"
      contains: "limite_utilizadores"
    - path: "backend/src/main/java/com/lexcv/repositories/UserRepository.java"
      provides: "Contagem reutilizavel de utilizadores ativos por tenant (reusada pelas Phases 120 e 122)"
      contains: "countByTenantIdAndAtivoTrue"
    - path: "backend/migrations/117-add-tenant-plano-limite-utilizadores.sql"
      provides: "Migracao manual: 2 ALTER TABLE ADD COLUMN + backfill do tenant existente"
      contains: "ALTER TABLE t_tenant ADD COLUMN"
  key_links:
    - from: "backend/src/main/java/com/lexcv/models/Tenant.java"
      to: "backend/src/main/java/com/lexcv/models/TenantPlano.java"
      via: "campo tipado TenantPlano anotado com @Enumerated(EnumType.STRING)"
      pattern: "@Enumerated\\(EnumType\\.STRING\\)"
    - from: "backend/migrations/117-add-tenant-plano-limite-utilizadores.sql"
      to: "backend/src/main/java/com/lexcv/models/Tenant.java"
      via: "nomes de coluna identicos aos do mapeamento @Column (prod corre ddl-auto=validate)"
      pattern: "limite_utilizadores"
---

<objective>
Criar a camada de dados do limite de utilizadores por tenant: o enum `TenantPlano`, os dois campos novos na entidade `Tenant`, o método de contagem reutilizável em `UserRepository`, e o script de migração manual correspondente.

Purpose: este plano entrega os **contratos** que o Plan 02 consome para aplicar o 409, e que as Phases 120 (consola de tenants) e 122 (relatório de utilização) vão reutilizar — o Success Criteria 4 da fase exige explicitamente que a contagem de "utilizadores ativos" seja uma única função reutilizável, não duplicada.

Output: 2 ficheiros novos (`TenantPlano.java`, `117-add-tenant-plano-limite-utilizadores.sql`) e 2 modificados (`Tenant.java`, `UserRepository.java`). Zero alterações de comportamento observável nesta wave — nenhum endpoint muda ainda.
</objective>

<execution_context>
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/LEXCV-117-backend-limite-de-utilizadores-por-tenant/117-CONTEXT.md
@.planning/phases/LEXCV-117-backend-limite-de-utilizadores-por-tenant/117-PATTERNS.md

# 117-PATTERNS.md contém o conteúdo integral de Tenant.java, DocumentoTipo.java,
# UserRepository.java e dos dois scripts de migração analog — usa-o como fonte primária
# em vez de explorar o codebase.
</context>

<interfaces>
<!-- Contratos que este plano CRIA e que o Plan 02 (e as Phases 120/122) consomem. -->

Novo em `backend/src/main/java/com/lexcv/models/TenantPlano.java`:
- `public enum TenantPlano { STARTER, STANDARD, ENTERPRISE }`

Novo em `backend/src/main/java/com/lexcv/models/Tenant.java` (via Lombok `@Getter`/`@Setter`/`@Builder`, já presentes na classe):
- `TenantPlano getPlano()` / `void setPlano(TenantPlano)` / `Tenant.builder().plano(...)`
- `Integer getLimiteUtilizadores()` / `void setLimiteUtilizadores(Integer)` / `Tenant.builder().limiteUtilizadores(...)`
- Contrato semântico: `getLimiteUtilizadores() == null` significa **sem limite**.

Novo em `backend/src/main/java/com/lexcv/repositories/UserRepository.java`:
- `long countByTenantIdAndAtivoTrue(UUID tenantId)`

Já existente, usado sem alteração (confirmado em 117-PATTERNS.md):
- `TenantRepository extends JpaRepository<Tenant, UUID>` → `Optional<Tenant> findById(UUID)` herdado
</interfaces>

<source_audit>
## Multi-Source Coverage Audit (cobre o conjunto dos 2 planos da Phase 117)

**GOAL (ROADMAP Phase 117)** — "O backend aplica um limite de utilizadores ativos por tenant; `POST /api/v1/admin/users` recusa criar quando o tenant já está no limite; desativar alguém liberta a vaga de imediato."

| Success Criteria (ROADMAP) | Plano | Estado |
|---|---|---|
| SC1 — `Tenant` tem `plano`/`limiteUtilizadores` persistidos, migração aplicada, tenants existentes com default sensato, arranque não quebra | 117-01 (T1+T2) | COVERED |
| SC2 — `createUser` devolve 409 com mensagem clara no limite; cria normalmente abaixo | 117-02 (T1) | COVERED |
| SC3 — desativar (`ativo=false`) liberta vaga de imediato | 117-02 (T1, caso de teste 4) | COVERED |
| SC4 — contagem de ativos é uma única função reutilizável (reuso nas Phases 120/122) | 117-01 (T1, definida) + 117-02 (T1, consumida) | COVERED |

**REQ (REQUIREMENTS.md)**

| ID | Plano | Estado |
|---|---|---|
| PLAN-01 — Tenant tem campos `plano` e `limite_utilizadores` | 117-01 | COVERED |
| PLAN-02 — criar utilizador bloqueado (409) no limite, conta só `ativo=true` | 117-02 | COVERED |
| PLAN-04 — desativar utilizador liberta vaga imediatamente | 117-02 | COVERED |

**RESEARCH** — N/A. Research desativada para esta fase (`workflow.research=false`); não existe `117-RESEARCH.md`. Sem itens a auditar.

**CONTEXT (117-CONTEXT.md `<decisions>`)** — o ficheiro não usa IDs `D-XX`; os `CTX-NN` abaixo são etiquetas de rastreio atribuídas aqui, uma por decisão fechada, pela ordem em que aparecem no ficheiro.

| ID | Decisão | Plano/Task | Estado |
|---|---|---|---|
| CTX-01 | `plano` é enum Java `TenantPlano` (STARTER/STANDARD/ENTERPRISE), `@Enumerated(EnumType.STRING)` | 117-01 T1 | COVERED |
| CTX-02 | `limiteUtilizadores` é `Integer` nullable; `null` = sem limite; sem sentinela mágica | 117-01 T1 | COVERED |
| CTX-03 | Colunas snake_case: `plano`, `limite_utilizadores` | 117-01 T1 + T2 | COVERED |
| CTX-04 | Migração dá ao tenant existente `plano=ENTERPRISE`, `limiteUtilizadores=NULL` | 117-01 T2 | COVERED |
| CTX-05 | Verificação corre em `createUser`, depois das validações de formato, imediatamente antes do `save()` | 117-02 T1 | COVERED |
| CTX-06 | Contagem = `count(ativo=true)` do tenant do `principal`, comparada antes de persistir (o novo utilizador não conta para si próprio) | 117-02 T1 | COVERED |
| CTX-07 | `limiteUtilizadores == null` bypassa; erro é `409 CONFLICT` com `Map.of("message", "Limite de utilizadores atingido para o vosso plano.")` | 117-02 T1 | COVERED |
| CTX-08 | Contagem é um único método reutilizável (`UserRepository.countByTenantIdAndAtivoTrue` ou equivalente) | 117-01 T1 | COVERED |
| CTX-09 | Script manual `backend/migrations/117-add-tenant-plano-limite-utilizadores.sql`, convenção `NNN-descricao.sql` | 117-01 T2 | COVERED |

**Claude's Discretion exercida** (marcada como livre em CONTEXT.md):
- Nome/localização do método de contagem → `UserRepository.countByTenantIdAndAtivoTrue` (derived query pura). Escolhido em vez de um serviço dedicado por ser o analog exato já presente no mesmo ficheiro (`findByTenantId`; o sufixo `...AtivoTrue` já está provado em `findByTenantIdAndRoleNameAndAtivoTrue`) e por ser um método público de repositório diretamente reutilizável pelas Phases 120/122 sem introduzir nova camada.
- Ordem dos campos novos em `Tenant.java` → entre `logoDataUrl` e `createdAt`, mantendo `createdAt`/`@PrePersist` no fim do ficheiro.

**Deferred Ideas (117-CONTEXT.md) — confirmadas AUSENTES de ambos os planos:**
- Ecrã/endpoint para editar `plano`/`limiteUtilizadores` de um tenant (Phase 120, PROV-04) — nenhuma task cria endpoint de escrita destes campos.
- Entidade `Subscription` com histórico de planos (v2 Requirements) — nenhuma task cria esta entidade.

**Nota de fronteira (não é gap):** um tenant criado de raiz por `SetupService.initializeSystem` fica com `plano=null` e `limiteUtilizadores=null`, ou seja, sem limite. É o comportamento correto para esta fase — 117-CONTEXT.md `<domain>` declara explicitamente que "nothing in this phase provides a UI/API to set plano/limiteUtilizadores for a tenant after creation (that arrives with the Phase 120 console)". O caminho de provisionamento que define o plano no momento da criação é a Phase 119/120. Nenhuma task deste plano altera `SetupService`.

**Resultado: 0 itens MISSING.** As 4 Success Criteria, os 3 REQ IDs e as 9 decisões de contexto estão todos cobertos.
</source_audit>

<tasks>

<task type="auto">
  <name>Task 1: Enum TenantPlano, campos plano/limiteUtilizadores em Tenant, contagem reutilizavel em UserRepository</name>

  <files>
    backend/src/main/java/com/lexcv/models/TenantPlano.java (novo)
    backend/src/main/java/com/lexcv/models/Tenant.java (modificar)
    backend/src/main/java/com/lexcv/repositories/UserRepository.java (modificar)
  </files>

  <read_first>
    - `.planning/phases/LEXCV-117-backend-limite-de-utilizadores-por-tenant/117-PATTERNS.md` — contém o conteúdo integral de `Tenant.java`, `DocumentoTipo.java` e `UserRepository.java`, mais os blocos exatos a copiar de `Cliente.java`. Fonte primária desta task.
    - `.planning/phases/LEXCV-117-backend-limite-de-utilizadores-por-tenant/117-CONTEXT.md` — secção `<decisions>` (CTX-01, CTX-02, CTX-03, CTX-08).
    - `backend/src/main/java/com/lexcv/models/Tenant.java` — ficheiro a modificar.
    - `backend/src/main/java/com/lexcv/models/DocumentoTipo.java` — analog exato do enum novo.
    - `backend/src/main/java/com/lexcv/repositories/UserRepository.java` — ficheiro a modificar.
  </read_first>

  <action>
    Criar `backend/src/main/java/com/lexcv/models/TenantPlano.java` no pacote `com.lexcv.models`: enum público chamado `TenantPlano` com exatamente três constantes, por esta ordem — `STARTER`, `STANDARD`, `ENTERPRISE`. Sem métodos, sem campos, sem anotações — forma idêntica a `DocumentoTipo.java` (per CTX-01).

    Em `backend/src/main/java/com/lexcv/models/Tenant.java`, acrescentar dois campos entre o campo `logoDataUrl` e o campo `createdAt`, mantendo `createdAt` e o método `@PrePersist onCreate()` no fim do ficheiro:
    - `plano`: tipo `TenantPlano`, anotado com `@Enumerated(EnumType.STRING)` e `@Column(name = "plano")` (per CTX-01, CTX-03). Não acrescentar imports — `Tenant.java` já faz `import jakarta.persistence.*;`, que cobre `@Enumerated` e `EnumType`; `TenantPlano` fica no mesmo pacote `com.lexcv.models`, logo também dispensa import.
    - `limiteUtilizadores`: tipo `Integer` (o wrapper, nunca `int`), anotado apenas com `@Column(name = "limite_utilizadores")` (per CTX-02, CTX-03). NAO pôr `nullable = false` e NAO pôr valor por omissão — a ausência do atributo é exatamente como este codebase exprime "nullable" (ver `nif`, `email`, `telefone` no mesmo ficheiro). `null` é o contrato de "sem limite"; nunca usar `-1` nem `Integer.MAX_VALUE` como sentinela.

    Acrescentar em `Tenant.java`, imediatamente acima do campo `limiteUtilizadores`, um comentário curto de regra de negócio no formato já usado neste codebase (ver `UserRepository.java` linhas 22-28 e `ResourceController.java` linhas 247-250): citar a fase e a semântica — Phase 117, `null` = sem limite (plano Enterprise "por acordo"), consumido por `AdminController.createUser`.

    Em `backend/src/main/java/com/lexcv/repositories/UserRepository.java`, acrescentar como último método da interface uma derived query pura, sem `@Query`, com a assinatura exata `long countByTenantIdAndAtivoTrue(UUID tenantId);` (per CTX-08). Precedê-la de um comentário curto no mesmo formato dos comentários já existentes no ficheiro, declarando que é a UNICA fonte de verdade para "utilizadores ativos de um tenant" e que as Phases 120 (consola de tenants) e 122 (relatório de utilização) a vão reutilizar — Success Criteria 4 da fase. Não acrescentar imports: `UUID` já está importado. Não alterar nenhum método existente.

    Não tocar em `TenantRepository.java`, `SetupService.java`, `DatabaseSeeder.java`, `PublicController.java` nem `AuthController.java` — estão fora do âmbito desta fase (117-CONTEXT.md `<code_context>` Integration Points). Não acrescentar dependências ao `pom.xml`.
  </action>

  <verify>
    <automated>cd backend && mvn -q -DskipTests compile && grep -v '^[[:space:]]*[/*]' src/main/java/com/lexcv/models/Tenant.java | grep -c 'private TenantPlano plano;' && grep -v '^[[:space:]]*[/*]' src/main/java/com/lexcv/models/Tenant.java | grep -c 'name = "limite_utilizadores"' && grep -v '^[[:space:]]*[/*]' src/main/java/com/lexcv/models/Tenant.java | grep -c 'private Integer limiteUtilizadores;' && grep -v '^[[:space:]]*[/*]' src/main/java/com/lexcv/repositories/UserRepository.java | grep -c 'long countByTenantIdAndAtivoTrue(UUID tenantId);' && grep -c 'ENTERPRISE' src/main/java/com/lexcv/models/TenantPlano.java</automated>
  </verify>

  <acceptance_criteria>
    - `cd backend && mvn -q -DskipTests compile` termina com exit code 0.
    - `backend/src/main/java/com/lexcv/models/TenantPlano.java` existe, declara `public enum TenantPlano` e contém as constantes `STARTER`, `STANDARD` e `ENTERPRISE` (`grep -c` de cada uma devolve 1).
    - Em `Tenant.java`, filtrando linhas de comentário (`grep -v '^[[:space:]]*[/*]'`): `private TenantPlano plano;` aparece 1 vez, `private Integer limiteUtilizadores;` aparece 1 vez, `name = "limite_utilizadores"` aparece 1 vez, `@Enumerated(EnumType.STRING)` aparece 1 vez.
    - Em `Tenant.java`, filtrando comentários, `grep -c 'private int limiteUtilizadores\|Integer.MAX_VALUE'` devolve 0 — nenhum primitivo `int` e nenhum sentinela.
    - Em `UserRepository.java`, filtrando comentários, `long countByTenantIdAndAtivoTrue(UUID tenantId);` aparece exatamente 1 vez, e `git diff --numstat backend/src/main/java/com/lexcv/repositories/UserRepository.java` mostra 0 linhas removidas (nenhum método pré-existente alterado).
    - `git diff --name-only` não lista `pom.xml`, `TenantRepository.java`, `SetupService.java`, `DatabaseSeeder.java`, `PublicController.java` nem `AuthController.java`.
    - Mitigação T-117-01 verificada: `grep -rn 'ResponseEntity.ok(tenant)\|ResponseEntity.ok(t)\|body(tenant)' backend/src/main/java/com/lexcv/controllers/` devolve 0 correspondências e `grep -c 'TenantPublicInfoResponse.builder()' backend/src/main/java/com/lexcv/controllers/PublicController.java` devolve 1 — nenhum endpoint serializa a entidade `Tenant` crua.
  </acceptance_criteria>

  <done>O backend compila; `Tenant` expõe `getPlano()`/`getLimiteUtilizadores()` mapeados para as colunas `plano`/`limite_utilizadores`; `UserRepository.countByTenantIdAndAtivoTrue(UUID)` existe como único ponto de contagem de utilizadores ativos; os campos novos não são serializados por nenhum endpoint.</done>
</task>

<task type="auto">
  <name>Task 2: Script de migracao manual 117 (2 ADD COLUMN + backfill ENTERPRISE do tenant existente)</name>

  <files>
    backend/migrations/117-add-tenant-plano-limite-utilizadores.sql (novo)
    .planning/STATE.md (uma linha em "### Pending Todos")
  </files>

  <read_first>
    - `backend/migrations/96-add-notificacao-snoozed-until.sql` — template do cabeçalho "REQUIRED manual production migration script" + estilo `ALTER TABLE ... ADD COLUMN`. Copiar a estrutura dos parágrafos, adaptando o conteúdo.
    - `backend/migrations/74-cleanup-nif-documento-tipo.sql` — analog do `UPDATE` de backfill de linhas existentes.
    - `.planning/phases/LEXCV-117-backend-limite-de-utilizadores-por-tenant/117-PATTERNS.md` — secção da migração, com a composição ilustrativa dos dois analogs.
    - `backend/src/main/java/com/lexcv/models/Tenant.java` — **depois da Task 1**, para confirmar que os nomes de coluna do script batem exatamente com os `@Column(name = ...)` do mapeamento.
    - `.planning/STATE.md` — secção `### Pending Todos`, para replicar a formulação da linha já existente sobre `backend/migrations/74-cleanup-nif-documento-tipo.sql`.
  </read_first>

  <action>
    Criar `backend/migrations/117-add-tenant-plano-limite-utilizadores.sql` seguindo o template de cabeçalho já fixo neste repositório (per CTX-09), por esta ordem: linha de título → parágrafo "IMPORTANT: This is a REQUIRED manual production migration script" → parágrafo "Why: `application-prod.yml` sets `ddl-auto: validate`..." → parágrafo "There is no automated migration runner in this repository (no Flyway, no Liquibase)" → nota de semântica/backfill → as instruções SQL.

    Adaptar os parágrafos a esta fase: a tabela é `t_tenant`, a entidade é `backend/src/main/java/com/lexcv/models/Tenant.java`, os campos são `plano` e `limiteUtilizadores`, e o consumidor é `AdminController.createUser` (Phase 117, PLAN-01/PLAN-02). Na nota de semântica declarar explicitamente que `limite_utilizadores` a `NULL` significa "sem limite" e que o tenant já existente recebe `plano='ENTERPRISE'` precisamente para nunca ficar bloqueado como efeito colateral desta fase (per CTX-04).

    SQL: exatamente três instruções, por esta ordem.
    1. `ALTER TABLE t_tenant ADD COLUMN plano VARCHAR(255);` — `VARCHAR(255)` é o DDL que o Hibernate gera por omissão para um campo `@Enumerated(EnumType.STRING)`, o mesmo tipo implícito já em uso em `t_cliente.documento_tipo`.
    2. `ALTER TABLE t_tenant ADD COLUMN limite_utilizadores INTEGER;` — sem `NOT NULL` e sem `DEFAULT`; a coluna nasce `NULL` em todas as linhas existentes, que é exatamente o valor pretendido ("sem limite").
    3. `UPDATE t_tenant SET plano = 'ENTERPRISE' WHERE plano IS NULL;` — backfill idempotente do(s) tenant(s) já existente(s); a cláusula `WHERE` torna a re-execução segura.

    NAO escrever nenhum `UPDATE` sobre `limite_utilizadores` — deixá-lo `NULL` é o requisito, não um esquecimento; registar isso numa linha de comentário do cabeçalho para que uma revisão futura não o "corrija".

    NAO acrescentar `NOT NULL`, `DEFAULT`, `CHECK` nem índices; NAO criar nenhuma outra tabela; NAO alterar nenhum script de migração já existente em `backend/migrations/`.

    Por fim, acrescentar a `.planning/STATE.md`, na secção `### Pending Todos`, uma linha a registar que `backend/migrations/117-add-tenant-plano-limite-utilizadores.sql` é um script de execução manual que tem de correr contra a base de dados antes/durante o próximo deploy — replicando a formulação da linha já existente sobre `backend/migrations/74-cleanup-nif-documento-tipo.sql`. Não reescrever nem reorganizar o resto do STATE.md.
  </action>

  <verify>
    <automated>cd backend && grep -v '^[[:space:]]*--' migrations/117-add-tenant-plano-limite-utilizadores.sql | grep -c 'ALTER TABLE t_tenant ADD COLUMN' && grep -v '^[[:space:]]*--' migrations/117-add-tenant-plano-limite-utilizadores.sql | grep -c "UPDATE t_tenant SET plano = 'ENTERPRISE' WHERE plano IS NULL;" && grep -v '^[[:space:]]*--' migrations/117-add-tenant-plano-limite-utilizadores.sql | grep -c 'limite_utilizadores INTEGER' && grep -c 'REQUIRED manual production migration script' migrations/117-add-tenant-plano-limite-utilizadores.sql && grep -c '117-add-tenant-plano-limite-utilizadores.sql' ../.planning/STATE.md</automated>
  </verify>

  <acceptance_criteria>
    - `backend/migrations/117-add-tenant-plano-limite-utilizadores.sql` existe.
    - Filtrando comentários SQL (`grep -v '^[[:space:]]*--'`): `ALTER TABLE t_tenant ADD COLUMN` aparece exatamente 2 vezes; `limite_utilizadores INTEGER` aparece 1 vez; `plano VARCHAR(255)` aparece 1 vez; `UPDATE t_tenant SET plano = 'ENTERPRISE' WHERE plano IS NULL;` aparece 1 vez.
    - Filtrando comentários SQL, `grep -ci 'NOT NULL\|DEFAULT\|CHECK\|CREATE INDEX\|UPDATE t_tenant SET limite_utilizadores'` devolve 0 — nenhuma restrição extra, e o backfill nunca escreve `limite_utilizadores`.
    - O cabeçalho contém a frase `REQUIRED manual production migration script` e menciona `ddl-auto` e `Flyway`, tal como os scripts 96/82/93.
    - Os nomes de coluna do script coincidem com os `@Column(name = ...)` de `Tenant.java` (Task 1): `grep -o 'limite_utilizadores' backend/migrations/117-*.sql | head -1` e `grep -o 'limite_utilizadores' backend/src/main/java/com/lexcv/models/Tenant.java | head -1` produzem a mesma string.
    - `git status --porcelain backend/migrations/` lista apenas o ficheiro `117-*.sql` como novo; nenhum script de migração pré-existente aparece como modificado.
    - `.planning/STATE.md` contém uma referência a `117-add-tenant-plano-limite-utilizadores.sql` na secção `### Pending Todos`.
  </acceptance_criteria>

  <done>Existe um script de migração manual que acrescenta `plano` e `limite_utilizadores` a `t_tenant` com nomes e tipos compatíveis com o mapeamento JPA, e que dá `ENTERPRISE`/`NULL` ao tenant existente para nunca o bloquear; a sua execução manual está registada nos Pending Todos do STATE.md.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Schema da base de dados ↔ mapeamento JPA | Produção corre `ddl-auto: validate`; qualquer divergência entre a migração e a entidade impede o arranque da aplicação |
| Entidade `Tenant` → respostas HTTP | Os campos novos são dados comerciais/de faturação; não podem passar a ser serializados para clientes de nenhum tenant |
| Script de migração → dados de produção existentes | O backfill escreve diretamente sobre a única linha real de `t_tenant` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-117-01 | Information Disclosure | `Tenant` → `PublicController.getBranding`, `AuthController` `/auth/me` | mitigate | Confirmado nesta fase que nenhum endpoint devolve a entidade `Tenant` crua: `PublicController` mapeia explicitamente para `TenantPublicInfoResponse` e `AuthController` copia apenas `nome`+`logoDataUrl`. Task 1 inclui um gate que reprova se aparecer `ResponseEntity.ok(tenant)`/`body(tenant)` em `controllers/` ou se o mapeamento explícito de `PublicController` desaparecer |
| T-117-02 | Denial of Service | Arranque da aplicação em produção (`ddl-auto: validate`) | mitigate | A migração usa exatamente os nomes `plano` e `limite_utilizadores` do `@Column(name=...)` e tipos compatíveis (`VARCHAR(255)` para enum-STRING, `INTEGER` para `Integer`); Task 2 assere a coincidência dos nomes contra `Tenant.java` |
| T-117-03 | Tampering | Backfill do script de migração sobre `t_tenant` | mitigate | O `UPDATE` é restringido por `WHERE plano IS NULL` (idempotente, nunca sobrescreve um plano já definido) e nunca escreve `limite_utilizadores`, garantindo que o tenant real fica em `ENTERPRISE`/sem limite e nunca é bloqueado por esta fase |
| T-117-04 | Tampering | `countByTenantIdAndAtivoTrue` — contagem cross-tenant | mitigate | O nome derivado inclui obrigatoriamente o predicado `TenantId`, pelo que a contagem nunca atravessa tenants; o consumo com o tenant correto é provado no Plan 02 (T-117-06) |
| T-117-SC | Tampering | Instalações npm/pip/cargo/Maven | n/a | Esta fase não instala nenhum pacote nem acrescenta dependências ao `pom.xml` — o Package Legitimacy Gate não se aplica. Task 1 assere que `pom.xml` não aparece no diff |
</threat_model>

<verification>
1. `cd backend && mvn -q -DskipTests compile` — exit 0.
2. `cd backend && mvn test` — a suite existente continua verde (nenhum teste instancia `AdminController` ou `Tenant` de forma que estes campos quebrem; confirmado: não existe `AdminControllerTest`).
3. `cd backend && mvn spotbugs:check` — sem novos achados de SAST.
4. Gates de origem das duas tasks (greps filtrados de comentários) todos verdes.
5. `git status --porcelain` mostra apenas os 4 ficheiros de `files_modified` mais `.planning/STATE.md`.
</verification>

<success_criteria>
- `Tenant` tem `plano` (`TenantPlano`, `@Enumerated(EnumType.STRING)`, coluna `plano`) e `limiteUtilizadores` (`Integer` nullable, coluna `limite_utilizadores`).
- `null` em `limiteUtilizadores` é o único representante de "sem limite" — zero sentinelas no código.
- `UserRepository.countByTenantIdAndAtivoTrue(UUID)` existe e é a única contagem de utilizadores ativos por tenant no codebase (Success Criteria 4 da fase; reuso previsto pelas Phases 120 e 122).
- `backend/migrations/117-add-tenant-plano-limite-utilizadores.sql` existe, segue o template de cabeçalho dos scripts 96/82/93, acrescenta as 2 colunas e faz backfill de `plano='ENTERPRISE'` sem tocar em `limite_utilizadores`.
- O backend compila e a suite de testes existente permanece verde.
- Nenhum comportamento de endpoint muda nesta wave.
</success_criteria>

<output>
Criar `.planning/phases/LEXCV-117-backend-limite-de-utilizadores-por-tenant/117-01-SUMMARY.md` quando terminar.
</output>
