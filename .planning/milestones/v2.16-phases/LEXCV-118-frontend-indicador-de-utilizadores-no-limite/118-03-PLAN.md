---
phase: 118-frontend-indicador-de-utilizadores-no-limite
plan: 03
type: execute
wave: 3
depends_on: [118-01, 118-02]
files_modified:
  - .planning/phases/LEXCV-118-frontend-indicador-de-utilizadores-no-limite/118-HUMAN-UAT.md
autonomous: false
requirements: [PLAN-03]

must_haves:
  truths:
    - "GET /api/v1/auth/me devolve, contra um backend a correr de verdade, as chaves tenant_plano e tenant_limite_utilizadores no JSON"
    - "Com o tenant configurado no limite, a aba 'Gestão de Utilizadores' mostra o contador a vermelho com '· limite atingido' e o botao 'Novo Utilizador' desativado"
    - "O tooltip 'Limite de utilizadores atingido. Desative um utilizador para libertar uma vaga.' aparece ao passar o rato E ao navegar por teclado ate ao botao desativado — a prova de que o bug tooltip+disabled ficou mesmo resolvido"
    - "Com limite a NULL o contador le apenas 'X utilizadores' e o botao volta a estar ativo"
    - "Um 409 forcado (duas abas / limite reposto entre carregamentos) aparece como toast com a frase do backend sem prefixo 'API 409: ' e a UI nao rebenta"
  artifacts:
    - path: ".planning/phases/LEXCV-118-frontend-indicador-de-utilizadores-no-limite/118-HUMAN-UAT.md"
      provides: "Registo dos cenarios verificados ao vivo com veredito explicito por cenario"
      min_lines: 20
  key_links:
    - from: "web/src/app/(dashboard)/settings/page.tsx"
      to: "backend GET /api/v1/auth/me"
      via: "leitura do limite ao vivo pela query partilhada ['auth','me']"
      pattern: "tenant_limite_utilizadores"
---

<objective>
Confirmar ao vivo, contra backend + frontend a correr, os 3 comportamentos que nenhuma assercao de origem consegue provar: o tooltip disparar mesmo sobre um botao nativamente `disabled`, o contador mudar de estado visual, e o `409` chegar ao ecra como toast limpo.

Purpose: as Success Criteria 2 e 3 desta fase sao afirmacoes de renderizacao e de interacao. O historico deste projeto mostra que fechar fases com afirmacoes de renderizacao apenas verificadas estaticamente gera `verification_gap` recorrentes (ver Phase 114 em STATE.md). Alem disso, esta e a **primeira** correcao efetiva do bug de composicao `Tooltip` + `Button disabled`, divida documentada desde a Phase 102 (v2.13) — vale a pena confirmar com os proprios olhos que o tooltip aparece.

Output: `118-HUMAN-UAT.md` com veredito explicito por cenario, e o limite do tenant reposto a NULL no fim.
</objective>

<execution_context>
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/LEXCV-118-frontend-indicador-de-utilizadores-no-limite/118-UI-SPEC.md
@.planning/phases/LEXCV-118-frontend-indicador-de-utilizadores-no-limite/118-01-SUMMARY.md
@.planning/phases/LEXCV-118-frontend-indicador-de-utilizadores-no-limite/118-02-SUMMARY.md
</context>

<interfaces>
<!-- Factos de ambiente confirmados no planeamento. Nao redescobrir. -->

Esquema relevante (confirmado em `backend/migrations/117-add-tenant-plano-limite-utilizadores.sql`
e nas entidades):
- `t_tenant.plano` VARCHAR(255) — valores validos: `STARTER`, `STANDARD`, `ENTERPRISE`
- `t_tenant.limite_utilizadores` INTEGER — `NULL` = sem limite
- `t_user.tenant_id` UUID, `t_user.ativo` BOOLEAN

Credenciais e portas (CLAUDE.md):
- backend `mvn spring-boot:run` na porta 8080, requer `backend/.env` (todas as variaveis `DB_*`/`MINIO_*` obrigatorias, sem defaults)
- frontend `pnpm dev` na porta 3000, requer `web/.env.local`
- admin semeado: `admin@lexcv.cv` / `Pa$$w0rd`
- login: `POST /api/v1/auth/login` com `{"email":…,"password":…}` devolve cookies httpOnly `access_token`/`refresh_token`
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Preparar o cenario ao vivo e provar o contrato /auth/me contra o backend real</name>

  <files>
    (nenhum ficheiro de codigo — operacoes de ambiente e de base de dados)
  </files>

  <read_first>
    - `.planning/phases/LEXCV-118-frontend-indicador-de-utilizadores-no-limite/118-01-SUMMARY.md` — a forma JSON entregue, para saber exatamente que chaves procurar.
    - `CLAUDE.md` — seccao Commands (backend e frontend) e os requisitos de `backend/.env` / `web/.env.local`.
    - `backend/.env.example` — nomes das variaveis `DB_HOST`/`DB_PORT`/`DB_NAME`/`DB_USER`/`DB_PASSWORD` a usar na ligacao `psql`.
    - `.planning/STATE.md` — Blockers/Concerns, para reconhecer bloqueios ambientais ja conhecidos (Docker/Testcontainers, MinIO) em vez de os re-diagnosticar.
  </read_first>

  <action>
    Garantir que o backend (`cd backend && mvn spring-boot:run`) e o frontend (`cd web && pnpm dev`) estao
    a correr. Se um deles nao arrancar por falta de `.env`/`.env.local` ou por indisponibilidade da base
    de dados, NAO inventar workaround nem alterar codigo: registar o bloqueio ambiental e passar a Task 2
    declarando explicitamente que cenarios ficam por confirmar.

    Autenticar via CLI com o admin semeado, guardando os cookies num ficheiro temporario **fora do
    repositorio** (diretorio de scratchpad da sessao, nunca dentro de `web/` ou `backend/`), e chamar
    `GET /api/v1/auth/me` com esses cookies. Confirmar que o JSON devolvido contem as chaves
    `tenant_plano` e `tenant_limite_utilizadores` — esta e a unica prova ao vivo de que o Plan 01
    atravessa mesmo a fronteira HTTP (os testes Mockito provam o metodo, nao a serializacao Jackson).
    Registar o valor observado das duas chaves.

    Ligar a base de dados com `psql` usando as credenciais de `backend/.env` e colocar o tenant existente
    exatamente no limite, para tornar o estado "limite atingido" alcancavel (nao existe UI para editar o
    limite antes da Phase 120): definir `plano` como `STANDARD` e `limite_utilizadores` igual a contagem
    atual de `t_user` com `ativo = true` para esse `tenant_id`. Confirmar o resultado com um `SELECT` de
    leitura antes de avancar, e registar o numero obtido — e o `Y` que a UI deve mostrar.

    Anotar o valor original de `plano`/`limite_utilizadores` ANTES de os alterar (o backfill da Phase 117
    deixou `ENTERPRISE` / `NULL`) — a Task 2 termina com a reposicao desses valores.

    Proibicoes explicitas: NAO alterar nenhum ficheiro de codigo nesta task. NAO correr o
    `backend/migrations/117-*.sql` outra vez (ja aplicado; `ALTER TABLE ADD COLUMN` falharia).
    NAO desativar nem apagar utilizadores reais para manipular a contagem — mexer apenas no limite.
  </action>

  <verify>
    <automated>curl -s -c "$COOKIE_JAR" -H 'Content-Type: application/json' -d '{"email":"admin@lexcv.cv","password":"Pa$$w0rd"}' http://localhost:8080/api/v1/auth/login > /dev/null && curl -s -b "$COOKIE_JAR" http://localhost:8080/api/v1/auth/me | tee /dev/stderr | grep -q 'tenant_limite_utilizadores' && curl -s -b "$COOKIE_JAR" http://localhost:8080/api/v1/auth/me | grep -q 'tenant_plano'</automated>
  </verify>

  <acceptance_criteria>
    - `GET /api/v1/auth/me` devolve HTTP 200 e um corpo JSON que contem literalmente as chaves `tenant_plano` e `tenant_limite_utilizadores` (ambos os `grep -q` acima com exit code 0).
    - O valor de `tenant_limite_utilizadores` observado apos o `UPDATE` e um inteiro igual a contagem de `t_user` com `ativo = true` do tenant, e nao `null` — registado por escrito no SUMMARY/UAT.
    - Um `SELECT plano, limite_utilizadores FROM t_tenant;` confirma `STANDARD` e o inteiro esperado.
    - Os valores originais (`ENTERPRISE` / `NULL`) ficam registados por escrito antes da alteracao, para reposicao na Task 2.
    - `git status --porcelain` nao lista nenhum ficheiro de codigo alterado por esta task; o ficheiro de cookies nao esta dentro do repositorio.
  </acceptance_criteria>

  <done>Backend e frontend a correr, `/auth/me` prova ao vivo os 2 campos novos no JSON, e o tenant esta configurado exatamente no limite com os valores originais registados para reposicao.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verificacao humana — contador, botao desativado, tooltip e toast 409</name>

  <files>
    .planning/phases/LEXCV-118-frontend-indicador-de-utilizadores-no-limite/118-HUMAN-UAT.md (novo)
  </files>

  <read_first>
    - `.planning/phases/LEXCV-118-frontend-indicador-de-utilizadores-no-limite/118-UI-SPEC.md` — tabela de Copywriting (as 4 strings verbatim que o operador tem de reconhecer no ecra) e a Interaction Note 1 (razao pela qual o tooltip sobre um botao desativado e o ponto critico).
    - `.planning/phases/LEXCV-118-frontend-indicador-de-utilizadores-no-limite/118-02-SUMMARY.md` — a forma final do JSX entregue, para saber o que esperar no ecra.
    - Notas da Task 1 deste plano — o valor `Y` configurado e os valores originais a repor no fim.
  </read_first>

  <action>
    Apresentar ao operador o bloco `<what-built>` e a lista numerada de `<how-to-verify>`, e **parar**
    a espera do sinal de retoma. Nao prosseguir, nao auto-aprovar e nao substituir nenhum dos 8 pontos
    por analise estatica de codigo — a razao de existir deste plano e precisamente que os pontos 2 a 8
    nao sao demonstraveis por leitura de origem.

    Assim que o operador responder, escrever
    `.planning/phases/LEXCV-118-frontend-indicador-de-utilizadores-no-limite/118-HUMAN-UAT.md` com uma
    linha por ponto (1 a 9), cada uma com um veredito explicito: `CONFIRMADO`, `FALHOU — <descricao do
    que se viu>` ou `NAO VERIFICADO — <bloqueio ambiental concreto>`. Os pontos 4 (tooltip com rato) e 5
    (tooltip com teclado) levam vereditos separados, nunca fundidos.

    Se algum ponto vier `FALHOU`, NAO corrigir codigo dentro deste plano: registar a causa provavel e o
    ficheiro/linha suspeitos no UAT e no SUMMARY, e devolver o resultado ao orquestrador para tratamento
    como gap. Um `FALHOU` nos pontos 4 ou 5 e bloqueador de fase — significa que a tecnica
    `<span tabIndex={0}>` nao ficou correta e que a divida da Phase 102 continua aberta.

    Confirmar por fim (ponto 9) que a base de dados voltou ao estado original com um `SELECT` de leitura.
  </action>

  <what-built>
    - `GET /api/v1/auth/me` passou a devolver `tenant_plano` e `tenant_limite_utilizadores` (Plan 01, 4 testes Mockito verdes).
    - A aba "Gestão de Utilizadores" das Definições passou a mostrar "X/Y utilizadores" por cima do botão "Novo Utilizador", a desativar o botão quando X ≥ Y, e a explicar o motivo com um tooltip sobre o botão desativado (Plan 02, 8 asserções de origem verdes, lint e build limpos).
    - O toast local de erro deixou de colar o prefixo `API NNN: ` à mensagem do backend, para o `409` da Phase 117 se ler como frase limpa.
    - A Task 1 deste plano já colocou o tenant exatamente no limite e confirmou o JSON ao vivo.
  </what-built>

  <how-to-verify>
    1. Abrir `http://localhost:3000`, autenticar como `admin@lexcv.cv` / `Pa$$w0rd`, e ir a **Definições → Utilizadores**.
    2. **Estado no limite.** Confirmar que, por cima do botão "Novo Utilizador", aparece o contador com o formato `X/Y utilizadores · limite atingido`, a vermelho e a negrito (semibold), onde `Y` é o número que a Task 1 registou. Confirmar que o motivo é legível **sem** passar o rato por cima.
    3. **Botão desativado.** Confirmar que o botão "Novo Utilizador" está visivelmente desativado (opacidade reduzida) e que clicar nele não abre o formulário.
    4. **Tooltip com rato.** Passar o rato por cima do botão desativado e esperar ~1 segundo (o `delayDuration` global é 700ms). Confirmar que aparece o tooltip com o texto exato: `Limite de utilizadores atingido. Desative um utilizador para libertar uma vaga.` — **este é o ponto mais importante desta verificação**: é a primeira vez neste codebase que um tooltip dispara sobre um botão nativamente `disabled`.
    5. **Tooltip com teclado.** A partir do campo de pesquisa, navegar com `Tab` até chegar ao botão desativado e confirmar que o mesmo tooltip aparece só com o foco, sem rato.
    6. **Estado sem limite.** Numa consola `psql`, repor `limite_utilizadores = NULL` no `t_tenant`, recarregar a página de Definições (o `staleTime` do `useMe` é 60s — forçar recarregamento completo com `Ctrl+Shift+R`), e confirmar que: o contador passa a ler apenas `X utilizadores` (sem barra, sem "limite atingido", a cinzento) e o botão "Novo Utilizador" volta a estar ativo e a abrir o formulário.
    7. **Estado abaixo do limite.** Repor `limite_utilizadores` para um valor **maior** que a contagem atual (por exemplo, contagem + 2), recarregar, e confirmar que o contador lê `X/Y utilizadores` a cinzento e o botão continua ativo.
    8. **Toast do 409.** Com o limite ainda acima da contagem, abrir o formulário "Novo Utilizador" e preencher nome/email/password válidos **sem submeter**. Noutra consola `psql`, baixar `limite_utilizadores` para a contagem atual (simulando a corrida de duas abas). Submeter o formulário. Confirmar que: aparece um toast de erro cujo texto é a frase do backend `Limite de utilizadores atingido para o vosso plano.` **sem** o prefixo `API 409:`, que o banner de erro dentro do formulário mostra a mesma frase limpa, e que a página não rebenta nem fica em branco (o utilizador continua no formulário e pode fechá-lo normalmente).
    9. **Reposição.** No fim, repor os valores originais registados pela Task 1: `plano = 'ENTERPRISE'` e `limite_utilizadores = NULL`. Confirmar com um `SELECT` e com um recarregamento da página que o contador voltou a `X utilizadores` e o botão está ativo.

    Nota: o toast automático genérico do `apiFetch` (`Erro 409: …`) continua a aparecer em paralelo com o toast local — é comportamento pré-existente para todos os erros da aplicação e **não** é um defeito desta fase. O que se verifica no ponto 8 é apenas que o toast **local** deixou de mostrar `API 409:`.
  </how-to-verify>

  <resume-signal>
    Escrever "aprovado" se os 8 pontos passarem, ou descrever o que falhou indicando o número do ponto. Se o ponto 4 ou 5 falhar (tooltip não aparece), isso é um bloqueador — significa que a técnica do `<span tabIndex={0}>` não ficou correta e o bug documentado desde a Phase 102 continua aberto.
  </resume-signal>

  <verify>
    <automated>test -f .planning/phases/LEXCV-118-frontend-indicador-de-utilizadores-no-limite/118-HUMAN-UAT.md && test $(grep -cE 'CONFIRMADO|FALHOU|NAO VERIFICADO' .planning/phases/LEXCV-118-frontend-indicador-de-utilizadores-no-limite/118-HUMAN-UAT.md) -ge 9</automated>
  </verify>

  <acceptance_criteria>
    - Existe `.planning/phases/LEXCV-118-frontend-indicador-de-utilizadores-no-limite/118-HUMAN-UAT.md` com uma linha por cada um dos 9 pontos, cada uma com veredito explicito: `CONFIRMADO`, `FALHOU — <descricao>` ou `NAO VERIFICADO — <bloqueio ambiental>` (o `grep -cE` do bloco `<verify>` devolve pelo menos 9).
    - Os pontos 4 e 5 (tooltip com rato e com teclado) tem veredito individual e separado — nao agrupados num unico "tooltip OK".
    - O ponto 9 confirma por `SELECT` que `t_tenant.plano` voltou a `ENTERPRISE` e `limite_utilizadores` a `NULL` — a base de dados de desenvolvimento nao fica num estado artificial.
    - Qualquer ponto `FALHOU` gera uma entrada correspondente no SUMMARY com a causa provavel e o ficheiro/linha suspeitos, nunca uma reformulacao do criterio para o fazer passar.
    - `git status --porcelain` lista apenas ficheiros dentro de `.planning/` — este plano nao altera codigo.
  </acceptance_criteria>

  <done>Os 9 pontos tem veredito explicito registado em `118-HUMAN-UAT.md`, com confirmacao separada do tooltip por rato e por teclado, e a base de dados reposta ao estado original.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Sessao de desenvolvimento ↔ base de dados de desenvolvimento | Esta fase de verificacao escreve diretamente em `t_tenant` para tornar o estado "no limite" alcancavel; e uma alteracao de configuracao comercial, reversivel, nunca de dados de clientes |
| Credenciais de teste ↔ repositorio | O login por CLI usa o admin semeado e um ficheiro de cookies temporario |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-118-11 | Tampering | `UPDATE t_tenant SET limite_utilizadores = …` na base de dados de desenvolvimento | mitigate | Os valores originais (`ENTERPRISE` / `NULL`) sao registados por escrito antes da primeira alteracao (Task 1) e repostos e confirmados por `SELECT` no ponto 9 da Task 2. Apenas as colunas `plano`/`limite_utilizadores` sao tocadas — nenhum `t_user`, nenhum dado de cliente/processo. Proibicao explicita de desativar ou apagar utilizadores para manipular a contagem |
| T-118-12 | Information Disclosure | Ficheiro de cookies do login por CLI | mitigate | O `access_token`/`refresh_token` sao escritos num ficheiro temporario **fora do repositorio** (scratchpad da sessao), nunca dentro de `web/` ou `backend/`, pelo que nao podem ser commitados por acidente. Criterio de aceitacao da Task 1 verifica `git status --porcelain` limpo |
| T-118-13 | Spoofing | Uso da password do admin semeado (`Pa$$w0rd`) | accept | Credencial de desenvolvimento publica, ja documentada em `CLAUDE.md` e no `DatabaseSeeder`, usada apenas contra `localhost`. Nao e escrita em nenhum ficheiro versionado por esta fase |
| T-118-14 | Elevation of Privilege | Tentacao de "aprovar" o checkpoint sem verificacao humana real | mitigate | `gate="blocking"` e `autonomous: false`; a `<action>` proibe explicitamente auto-aprovacao e substituicao dos pontos por analise estatica. `workflow.auto_advance` esta a `false` no `config.json`, pelo que o gate nao pode ser saltado silenciosamente |
| T-118-SC | Tampering | Instalacoes npm/pip/cargo/Maven | n/a | Este plano nao instala nada e nao altera nenhum ficheiro de codigo — so documentacao de planeamento. O Package Legitimacy Gate nao se aplica |
</threat_model>

<verification>
1. `/auth/me` ao vivo devolve as 2 chaves novas (Task 1, automatizado).
2. Os 9 pontos da Task 2 tem veredito explicito em `118-HUMAN-UAT.md`.
3. Pontos 4 e 5 (tooltip rato + teclado) confirmados separadamente.
4. `SELECT plano, limite_utilizadores FROM t_tenant;` confirma a reposicao a `ENTERPRISE`/`NULL`.
5. `git status --porcelain` lista apenas ficheiros `.planning/` — zero alteracoes de codigo neste plano.
</verification>

<success_criteria>
- As Success Criteria 2 e 3 da Phase 118 deixam de ser afirmacoes de renderizacao apenas verificadas estaticamente: foram vistas a funcionar contra a aplicacao a correr.
- O tooltip sobre o botao desativado esta confirmado a disparar por rato E por teclado — a divida documentada desde a Phase 102 fica efetivamente fechada, nao apenas declarada fechada.
- A base de dados de desenvolvimento fica no estado original.
</success_criteria>

<output>
Criar `.planning/phases/LEXCV-118-frontend-indicador-de-utilizadores-no-limite/118-HUMAN-UAT.md` (veredito por ponto) e `.planning/phases/LEXCV-118-frontend-indicador-de-utilizadores-no-limite/118-03-SUMMARY.md` quando terminar.
</output>
