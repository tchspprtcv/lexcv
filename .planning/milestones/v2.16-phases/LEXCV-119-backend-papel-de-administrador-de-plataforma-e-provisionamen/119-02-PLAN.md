---
phase: 119-backend-papel-de-administrador-de-plataforma-e-provisionamen
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/main/java/com/lexcv/dtos/TenantProvisionResponse.java
  - backend/src/main/java/com/lexcv/services/SetupService.java
  - backend/src/test/java/com/lexcv/services/SetupServiceProvisionTenantTest.java
autonomous: true
requirements: [PROV-06]

must_haves:
  truths:
    - "SetupService.provisionTenant cria uma Tenant nova e o respetivo utilizador ADMIN inicial, e devolve a Tenant guardada (com id) — nunca void"
    - "provisionTenant reutiliza a validacao existente validateRequest sem a duplicar: nome em falta, email invalido e password fraca continuam a ser rejeitados com as mesmas mensagens"
    - "provisionTenant nunca le nem escreve SystemSettingRepository — nao consulta o gate singleton e nao marca o sistema como inicializado"
    - "provisionTenant pode ser chamado N vezes seguidas e cria N tenants, ao contrario de initializeSystem que so funciona uma vez"
    - "O utilizador inicial de um tenant provisionado recebe o papel ADMIN do proprio tenant, nunca PLATAFORMA_ADMIN"
    - "Um email ja existente em qualquer tenant faz provisionTenant falhar com IllegalArgumentException antes de criar a Tenant"
    - "initializeSystem mantem-se byte-a-byte inalterado, incluindo o bloco SystemSetting e o retorno void"
  artifacts:
    - path: "backend/src/main/java/com/lexcv/dtos/TenantProvisionResponse.java"
      provides: "DTO de resposta com id + nome da tenant criada, sem expor a entidade Tenant"
      contains: "private UUID id;"
    - path: "backend/src/main/java/com/lexcv/services/SetupService.java"
      provides: "Metodo provisionTenant que devolve Tenant, sem dependencia de SystemSetting"
      contains: "public Tenant provisionTenant(SetupInitializeRequest request)"
    - path: "backend/src/test/java/com/lexcv/services/SetupServiceProvisionTenantTest.java"
      provides: "Prova automatizada da criacao, da validacao reutilizada, da ausencia de SystemSetting e da repetibilidade"
      contains: "provisionTenant"
      min_lines: 130
  key_links:
    - from: "backend/src/main/java/com/lexcv/services/SetupService.java"
      to: "backend/src/main/java/com/lexcv/repositories/RoleRepository.java"
      via: "lookup do papel ADMIN do novo tenant (nunca PLATAFORMA_ADMIN)"
      pattern: "roleRepository\\.findByNome\\(\"ADMIN\"\\)"
    - from: "backend/src/main/java/com/lexcv/services/SetupService.java"
      to: "backend/src/main/java/com/lexcv/dtos/SetupInitializeRequest.java"
      via: "reutilizacao da validacao privada partilhada, sem duplicar regex"
      pattern: "validateRequest\\(request\\)"
---

<objective>
Acrescentar `SetupService.provisionTenant(SetupInitializeRequest)` — a mesma operacao "criar Tenant + utilizador ADMIN inicial" que `initializeSystem` ja faz, **menos** o gate singleton do `SystemSetting`, e **devolvendo** a `Tenant` criada. Criar o DTO `TenantProvisionResponse` que o Plan 04 usara para responder sem expor entidades.

Purpose: fecha o Success Criterion 2 da Phase 119 (PROV-06) — um caminho de codigo de provisionamento que reutiliza a validacao ja existente e nao depende de `SystemSetting.initialized`. E a peca que o `PlatformAdminController` do Plan 04 invoca; por isso este plano corre na Wave 1 e o controller na Wave 2.

Output: um DTO novo, um metodo novo em `SetupService` (com `initializeSystem` e `validateRequest` intactos), e um teste Mockito novo.
</objective>

<execution_context>
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/LEXCV-119-backend-papel-de-administrador-de-plataforma-e-provisionamen/119-CONTEXT.md
@.planning/phases/LEXCV-119-backend-papel-de-administrador-de-plataforma-e-provisionamen/119-PATTERNS.md

# 119-PATTERNS.md tem o corpo integral de initializeSystem com as linhas a DESCARTAR
# explicitamente anotadas, o corpo integral de validateRequest, e os ficheiros completos dos
# dois DTOs-analog. Usa-o como fonte primaria em vez de reexplorar o codebase.
</context>

<interfaces>
<!-- Contratos ja existentes no codebase. O executor NAO precisa de os redescobrir. -->

`backend/src/main/java/com/lexcv/dtos/SetupInitializeRequest.java` (ficheiro completo — reutilizado
tal como esta, NAO criar DTO de request novo):
```java
@Getter
@Setter
public class SetupInitializeRequest {
    private String clientName;
    private String logo;
    private String adminEmail;
    private String adminPassword;
}
```

`backend/src/main/java/com/lexcv/services/SetupService.java` — colaboradores ja injetados
(`@RequiredArgsConstructor`), nesta ordem exata (necessaria para o teste instanciar o servico):
```java
private final SystemSettingRepository systemSettingRepository;
private final TenantRepository tenantRepository;
private final UserRepository userRepository;
private final RoleRepository roleRepository;
private final PasswordEncoder passwordEncoder;
// => new SetupService(systemSettingRepository, tenantRepository, userRepository, roleRepository, passwordEncoder)
```

Helpers privados ja existentes na mesma classe — reutilizar, NAO duplicar:
```java
private void validateRequest(SetupInitializeRequest request);  // lanca IllegalArgumentException
private String normalizeLogo(String logo);                     // devolve null se em branco, senao trim()
private boolean isBlank(String value);
```

Mensagens de erro exatas que `validateRequest` ja produz (o teste assere-as literalmente):
- `"Payload de inicializacao em falta."` (com acentuacao real: `"Payload de inicialização em falta."`)
- `"O nome da empresa/cliente é obrigatório."`
- `"O email do administrador é inválido."`
- `"A password deve ter no mínimo 8 caracteres, com maiúscula, minúscula, número e caractere especial."`

Mensagens que o proprio `initializeSystem` produz e que `provisionTenant` deve reproduzir:
- `"Já existe um utilizador com este email."` -> `IllegalArgumentException`
- `"O papel ADMIN não está configurado."` -> `IllegalStateException`

`backend/src/main/java/com/lexcv/models/Tenant.java` (Lombok `@Builder` + `@Getter`/`@Setter`;
so `nome` e NOT NULL; `id` e `@GeneratedValue`):
```java
private UUID id; private String nome; private String email; private String logoDataUrl; ...
```

`backend/src/main/java/com/lexcv/dtos/TenantPublicInfoResponse.java` — o analog de estilo para o DTO
novo (as quatro anotacoes Lombok dominantes neste codebase):
```java
@Data @Builder @NoArgsConstructor @AllArgsConstructor
public class TenantPublicInfoResponse { private String nome; private String logoDataUrl; }
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: DTO TenantProvisionResponse</name>

  <files>
    backend/src/main/java/com/lexcv/dtos/TenantProvisionResponse.java (novo)
  </files>

  <read_first>
    - `.planning/phases/LEXCV-119-backend-papel-de-administrador-de-plataforma-e-provisionamen/119-PATTERNS.md` — seccao "New response DTO": os dois analogs (`TenantPublicInfoResponse`, `SetupInitializeResponse`) com os ficheiros completos e a justificacao da escolha das anotacoes.
    - `backend/src/main/java/com/lexcv/dtos/TenantPublicInfoResponse.java` — ficheiro completo, o analog de estilo direto.
    - `backend/src/main/java/com/lexcv/dtos/UserResponse.java` — segundo exemplo do mesmo quarteto de anotacoes Lombok, para confirmar a convencao dominante da pasta `dtos/`.
  </read_first>

  <action>
    Criar `backend/src/main/java/com/lexcv/dtos/TenantProvisionResponse.java` no pacote `com.lexcv.dtos`,
    com as quatro anotacoes Lombok dominantes deste codebase — `@Data`, `@Builder`, `@NoArgsConstructor`,
    `@AllArgsConstructor` — e exatamente dois campos:
    - `private UUID id;` (import `java.util.UUID`)
    - `private String nome;`

    Estes sao os dois campos que CONTEXT.md exige ("id + nome at minimum") e que o Plan 04 devolvera no 201.
    Acrescentar um Javadoc curto de classe a dizer que e a resposta de `POST /api/v1/platform/tenants` e que
    existe precisamente para nao serializar a entidade `Tenant` — a disciplina de "nunca entidade crua"
    documentada em `PublicController`.

    NAO incluir `email`, `logoDataUrl`, `plano`, `limiteUtilizadores`, `createdAt`, nem qualquer campo do
    utilizador ADMIN criado (em particular, nunca o email ou o hash da password). NAO criar um DTO de request
    novo — `SetupInitializeRequest` e reutilizado tal como esta.
  </action>

  <verify>
    <automated>cd backend && mvn -q -DskipTests compile && grep -c 'private UUID id;' src/main/java/com/lexcv/dtos/TenantProvisionResponse.java && grep -c 'private String nome;' src/main/java/com/lexcv/dtos/TenantProvisionResponse.java && grep -cE 'private .* (email|logoDataUrl|passwordHash|plano|limiteUtilizadores|createdAt);' src/main/java/com/lexcv/dtos/TenantProvisionResponse.java; test $? -ne 0</automated>
  </verify>

  <acceptance_criteria>
    - `cd backend && mvn -q -DskipTests compile` termina com exit code 0.
    - `TenantProvisionResponse.java` declara exatamente 2 campos: `grep -c 'private ' ` devolve 2.
    - Contem `private UUID id;` e `private String nome;`.
    - `grep -cE 'email|logoDataUrl|passwordHash|plano|limiteUtilizadores|createdAt'` devolve 0 — nenhum campo sensivel ou supersfluo vazou para o contrato.
    - Contem as 4 anotacoes `@Data`, `@Builder`, `@NoArgsConstructor`, `@AllArgsConstructor` (`grep -c '@Data\|@Builder\|@NoArgsConstructor\|@AllArgsConstructor'` devolve 4).
    - `git diff --name-only -- backend/pom.xml` devolve zero linhas.
  </acceptance_criteria>

  <done>Existe um DTO de resposta com `id` e `nome` da tenant, pronto para o Plan 04 construir o 201 sem tocar na entidade `Tenant`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: SetupService.provisionTenant — provisionamento sem gate singleton</name>

  <files>
    backend/src/test/java/com/lexcv/services/SetupServiceProvisionTenantTest.java (novo)
    backend/src/main/java/com/lexcv/services/SetupService.java (modificar — acrescentar um metodo)
  </files>

  <read_first>
    - `.planning/phases/LEXCV-119-backend-papel-de-administrador-de-plataforma-e-provisionamen/119-PATTERNS.md` — seccao "SetupService.java": o corpo integral de `initializeSystem` com as linhas marcadas `// DROP`, o corpo integral de `validateRequest`, e o bloco "IMPORTANT deviation to flag for the planner" que explica porque o retorno nao pode ser `void`.
    - `.planning/phases/LEXCV-119-backend-papel-de-administrador-de-plataforma-e-provisionamen/119-CONTEXT.md` — seccao `<decisions>`, bloco "New Service Method — Reuse, Don't Duplicate": as quatro decisoes bloqueadas (nome do metodo, reutilizar `validateRequest`, zero `SystemSettingRepository`, papel `ADMIN` e nao `PLATAFORMA_ADMIN`).
    - `backend/src/main/java/com/lexcv/services/SetupService.java` — ficheiro completo (125 linhas) antes de editar.
    - `backend/src/test/java/com/lexcv/services/NotificacaoServiceTest.java` ou `RiscoPrazoServiceTest.java` — convencao de teste de servico deste codebase (`@ExtendWith(MockitoExtension.class)`, mocks diretos, sem contexto Spring). Basta ler o cabecalho e um teste de cada, nao o ficheiro todo.
  </read_first>

  <behavior>
    Casos que `SetupServiceProvisionTenantTest` deve provar. Escrever ANTES de tocar em `SetupService.java`
    — devem falhar a compilar (o metodo `provisionTenant` ainda nao existe). Commitar o RED e o GREEN em
    commits separados, como nas Phases 117 e 118.

    - **Caso 1 — cria tenant + utilizador ADMIN e devolve a tenant guardada.** Request valido
      (`clientName="Escritorio Novo"`, `adminEmail="Novo@Escritorio.CV"`, `adminPassword="Pa$$w0rd"`,
      `logo=null`). `roleRepository.findByNome("ADMIN")` devolve um `Role`; `userRepository.findByEmail(...)`
      devolve `Optional.empty()`; `tenantRepository.save(any())` devolve a tenant com `id` atribuido.
      Asserir: o valor de retorno nao e nulo e o seu `getId()` e o `getNome()` sao os da tenant guardada;
      o `Tenant` capturado tem `getNome()` igual a `"Escritorio Novo"` (com `trim()` aplicado) e
      `getEmail()` igual a `"novo@escritorio.cv"` (lowercase); o `User` capturado tem `getTenantId()` igual
      ao id da tenant, `getEmail()` em lowercase, `getAtivo()` a `true`, e `getRoles()` com exatamente 1
      elemento cujo nome e `"ADMIN"`.
    - **Caso 2 — zero interaccao com o SystemSettingRepository.** No mesmo cenario do Caso 1, asserir
      `verifyNoInteractions(systemSettingRepository)`. Este e o coracao do Success Criterion 2/3: o novo
      caminho nao le o gate singleton nem marca o sistema como inicializado. Se um refactor futuro
      reintroduzir `findByIdForUpdate` ou `save` do `SystemSetting` neste metodo, este teste reprova.
    - **Caso 3 — a validacao partilhada continua a valer, sem duplicacao.** Tres sub-casos, cada um a
      asserir a mensagem exata da excecao com `assertThrows(IllegalArgumentException.class, ...)`:
      nome em branco -> `"O nome da empresa/cliente é obrigatório."`; email malformado (`"nao-e-email"`) ->
      `"O email do administrador é inválido."`; password fraca (`"fraca"`) -> a mensagem longa de password.
      Em todos, asserir tambem `verify(tenantRepository, never()).save(any())` — falha antes de persistir.
    - **Caso 4 — email ja existente e recusado antes de criar a tenant.** `userRepository.findByEmail(...)`
      devolve `Optional.of(utilizadorExistente)`. Asserir `IllegalArgumentException` com
      `"Já existe um utilizador com este email."` e `verify(tenantRepository, never()).save(any())`.
    - **Caso 5 — papel ADMIN ausente e um erro de estado, nao de input.** `roleRepository.findByNome("ADMIN")`
      devolve `Optional.empty()`. Asserir `IllegalStateException` com `"O papel ADMIN não está configurado."`
      e `verify(tenantRepository, never()).save(any())`. (O Plan 04 mapeia este tipo para 403.)
    - **Caso 6 — repetibilidade: duas chamadas seguidas criam duas tenants.** Dois requests validos com
      emails diferentes, ambos com `findByEmail` a devolver `empty()`. Asserir
      `verify(tenantRepository, times(2)).save(any())` e `verify(userRepository, times(2)).save(any())`,
      e continuar a asserir `verifyNoInteractions(systemSettingRepository)`. Prova a diferenca essencial
      face a `initializeSystem`, que so funciona uma vez.
    - **Caso 7 — nao-regressao de `initializeSystem`.** Chamar `initializeSystem` com um `SystemSetting`
      cujo `getInitialized()` e `true` e asserir `IllegalStateException` com
      `"O sistema já foi inicializado."`. Prova que acrescentar `provisionTenant` nao afrouxou o gate
      singleton do metodo vizinho (Success Criterion 3).

    Notas de montagem (confirmadas na leitura de planeamento):
    - `@Mock` para os 5 colaboradores pela ordem de `<interfaces>`; instanciar com
      `new SetupService(systemSettingRepository, tenantRepository, userRepository, roleRepository, passwordEncoder)`.
    - `tenantRepository.save(any())` deve devolver o argumento com id atribuido:
      `thenAnswer(inv -> { Tenant t = inv.getArgument(0); t.setId(UUID.randomUUID()); return t; })`.
    - Usar `ArgumentCaptor<Tenant>` e `ArgumentCaptor<User>` para inspeccionar o que foi persistido.
    - Sob strict stubs, os casos que falham na validacao nao devem stubar nada (a validacao corre primeiro);
      stubar apenas o necessario por caso.
    - Javadoc de classe no estilo de `PublicControllerTest`, a explicar que este metodo e o caminho de
      provisionamento distinto do wizard publico e porque o Caso 2 e o mais importante do ficheiro.
  </behavior>

  <action>
    Escrever primeiro `backend/src/test/java/com/lexcv/services/SetupServiceProvisionTenantTest.java` com os
    7 casos do bloco `<behavior>`. Correr `cd backend && mvn test -Dtest=SetupServiceProvisionTenantTest`,
    confirmar RED (erro de compilacao: `provisionTenant` inexistente), commitar.

    Depois, em `backend/src/main/java/com/lexcv/services/SetupService.java`, acrescentar um metodo novo
    **imediatamente a seguir** a `initializeSystem` e **antes** de `validateRequest`, anotado `@Transactional`
    (como `initializeSystem`), com a assinatura exata:
    `public Tenant provisionTenant(SetupInitializeRequest request)`

    O corpo e o de `initializeSystem` sem o bloco `SystemSetting`, e com retorno:
    1. `validateRequest(request);` — chamada ao helper privado ja existente, sem duplicar nenhuma regex nem
       nenhuma mensagem.
    2. Verificacao de email unico: `userRepository.findByEmail(request.getAdminEmail().trim().toLowerCase()).isPresent()`
       -> `IllegalArgumentException("Já existe um utilizador com este email.")`.
    3. `Role adminRole = roleRepository.findByNome("ADMIN").orElseThrow(() -> new IllegalStateException("O papel ADMIN não está configurado."));`
       — o papel e **`"ADMIN"`**, nunca `"PLATAFORMA_ADMIN"`: o primeiro utilizador de um tenant provisionado
       e o administrador do proprio escritorio, nao um operador de plataforma. Esta e uma decisao bloqueada
       de CONTEXT.md.
    4. Construir e guardar a `Tenant` com `nome` = `request.getClientName().trim()`, `email` = email em
       lowercase, `logoDataUrl` = `normalizeLogo(request.getLogo())` (helper existente).
    5. Construir e guardar o `User` com `tenantId` = id da tenant guardada, `nome` = `"Administrador"`,
       `email` em lowercase, `passwordHash` = `passwordEncoder.encode(request.getAdminPassword())`,
       `ativo(true)`, `roles(Set.of(adminRole))`.
    6. `return tenant;` — a tenant **guardada** (a que veio de `tenantRepository.save(...)`, com `id`
       preenchido), nao a instancia do builder. Este retorno e a unica divergencia deliberada face a
       `initializeSystem` (que devolve `void`): o Plan 04 precisa do `id` e do `nome` para o corpo do 201.

    Acrescentar um Javadoc de metodo a explicar: (a) que e o caminho de provisionamento gated a
    `PLATAFORMA_ADMIN` (Plan 04) e nao o wizard publico; (b) que nao toca em `SystemSetting` por desenho, e
    que por isso e repetivel; (c) que partilha `validateRequest` com `initializeSystem` deliberadamente,
    para as regras de email/password nunca divergirem entre os dois caminhos.

    Proibicoes explicitas:
    - NAO alterar uma unica linha de `initializeSystem`, `isInitialized`, `validateRequest`, `normalizeLogo`
      ou `isBlank`. `initializeSystem` continua `void` e continua a fazer o gate `SystemSetting`.
    - NAO chamar `initializeSystem` a partir de `provisionTenant` nem vice-versa; sao caminhos irmaos, nunca
      encadeados (Success Criterion 3: "nunca reaproveita o endpoint publico de /setup").
    - NAO referenciar `systemSettingRepository` dentro de `provisionTenant`, nem para ler.
    - NAO tornar `validateRequest` publico nem `protected` — o metodo novo vive na mesma classe e ja lhe acede.
    - NAO criar `SetupInitializeRequest` novo, nem acrescentar campos ao existente.
    - NAO tocar em `SetupController.java` neste plano (Success Criterion 3 exige-o inalterado; o Plan 04
      verifica-o).
    - NAO acrescentar dependencias ao `pom.xml`.
  </action>

  <verify>
    <automated>cd backend && mvn test -Dtest=SetupServiceProvisionTenantTest && grep -c 'public Tenant provisionTenant(SetupInitializeRequest request)' src/main/java/com/lexcv/services/SetupService.java && grep -c 'public void initializeSystem(SetupInitializeRequest request)' src/main/java/com/lexcv/services/SetupService.java && test $(sed -n '/public Tenant provisionTenant/,/^    }/p' src/main/java/com/lexcv/services/SetupService.java | grep -c 'systemSettingRepository') -eq 0 && test $(sed -n '/public Tenant provisionTenant/,/^    }/p' src/main/java/com/lexcv/services/SetupService.java | grep -c 'PLATAFORMA_ADMIN') -eq 0 && git diff --name-only -- src/main/java/com/lexcv/controllers/SetupController.java | wc -l | grep -qx 0</automated>
  </verify>

  <acceptance_criteria>
    - `cd backend && mvn test -Dtest=SetupServiceProvisionTenantTest` termina com exit code 0 e reporta 7 testes (ou mais, se os sub-casos do Caso 3 forem metodos separados), 0 falhas, 0 erros.
    - O ficheiro de teste contem `verifyNoInteractions(systemSettingRepository)` pelo menos 2 vezes (Casos 2 e 6).
    - Em `SetupService.java`: `grep -c 'public Tenant provisionTenant(SetupInitializeRequest request)'` devolve 1.
    - Em `SetupService.java`: `grep -c 'public void initializeSystem(SetupInitializeRequest request)'` devolve 1 — a assinatura pre-existente esta intacta.
    - **Extraindo apenas o corpo de `provisionTenant`** (`sed -n '/public Tenant provisionTenant/,/^    }/p'`): `grep -c 'systemSettingRepository'` devolve **0** e `grep -c 'PLATAFORMA_ADMIN'` devolve **0**.
    - Extraindo apenas o corpo de `provisionTenant`: `grep -c 'validateRequest(request)'` devolve 1 e `grep -c 'roleRepository.findByNome("ADMIN")'` devolve 1.
    - Extraindo apenas o corpo de `provisionTenant`: `grep -c 'return tenant;'` devolve 1.
    - Em `SetupService.java`: `grep -c 'EMAIL_PATTERN\|STRONG_PASSWORD_PATTERN\|DATA_URL_IMAGE_PATTERN'` mantem o mesmo valor de antes desta fase (as regex nao foram duplicadas nem movidas).
    - `git diff --name-only -- backend/src/main/java/com/lexcv/controllers/SetupController.java backend/pom.xml` devolve zero linhas.
    - `cd backend && mvn test` (suite unitaria completa) termina com exit code 0.
  </acceptance_criteria>

  <done>`SetupService.provisionTenant` cria tenant + ADMIN inicial e devolve a tenant guardada, reutilizando `validateRequest`, sem tocar em `SystemSetting`, e e repetivel — provado por 7 testes verdes, com `initializeSystem` inalterado e o seu gate singleton confirmado.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Chamador do servico -> `SetupService.provisionTenant` | Este plano nao expoe HTTP nenhum; a fronteira de autorizacao e imposta pelo Plan 04 (`@PreAuthorize` ao nivel da classe do `PlatformAdminController`). O metodo em si nao autoriza — assume um chamador ja autorizado |
| `provisionTenant` <-> `initializeSystem` | Fronteira deliberada: dois caminhos irmaos que partilham validacao mas NAO partilham o gate singleton; nenhum chama o outro |
| Novo tenant criado <-> tenants existentes | O tenant criado e uma linha nova isolada; o utilizador inicial recebe o `tenantId` dessa linha e o papel `ADMIN` do proprio tenant |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-119-08 | Tampering | Bypass do gate singleton de `/setup/initialize` atraves do caminho novo | mitigate | `provisionTenant` nao le nem escreve `SystemSettingRepository` — nao consegue marcar o sistema como inicializado nem desmarcar. `initializeSystem` fica byte-a-byte inalterado e o seu gate e re-provado pelo Caso 7. Gate de origem extrai o corpo do metodo novo e reprova qualquer referencia a `systemSettingRepository`. O Caso 2 (`verifyNoInteractions`) e a prova comportamental |
| T-119-14 | Elevation of Privilege | O utilizador inicial de um tenant provisionado receber `PLATAFORMA_ADMIN` em vez de `ADMIN` | mitigate | `roleRepository.findByNome("ADMIN")` fixo em codigo; o nome do papel **nao** vem do request (`SetupInitializeRequest` nao tem campo de papel e nao ganha nenhum). Gate de origem reprova a string `PLATAFORMA_ADMIN` dentro do corpo de `provisionTenant`; o Caso 1 assere que o `User` persistido tem exatamente 1 papel, de nome `"ADMIN"`. Sem isto, provisionar um escritorio criaria um operador de plataforma |
| T-119-15 | Elevation of Privilege | Divergencia das regras de password/email entre o caminho publico e o novo | mitigate | `validateRequest` e reutilizado tal como esta, sem duplicacao de regex nem de mensagens. O Caso 3 assere as tres mensagens literais. Um gate de origem confirma que as constantes `EMAIL_PATTERN`/`STRONG_PASSWORD_PATTERN`/`DATA_URL_IMAGE_PATTERN` nao foram duplicadas. Sem isto, o caminho de plataforma poderia aceitar passwords mais fracas que o wizard publico |
| T-119-16 | Spoofing | Colisao de email entre tenants (dois utilizadores com o mesmo email em tenants diferentes) | mitigate | `userRepository.findByEmail(...)` e uma verificacao **global**, nao por tenant — email e a chave de autenticacao em `JwtAuthenticationFilter`/`AuthController.login`, pelo que tem de continuar globalmente unico mesmo com varios tenants. `provisionTenant` mantem exatamente a mesma verificacao que `initializeSystem` ja fazia. Caso 4 prova a recusa antes de criar a tenant (sem tenant orfa) |
| T-119-17 | Information Disclosure | Exposicao da entidade `Tenant` ou de dados do utilizador criado na resposta | mitigate | `TenantProvisionResponse` tem exatamente 2 campos (`id`, `nome`); gate de origem reprova `email`/`logoDataUrl`/`passwordHash`/`plano`/`limiteUtilizadores`/`createdAt`. O metodo de servico devolve a entidade, mas a serializacao acontece so no Plan 04, que constroi o DTO — a disciplina "nunca entidade crua" e verificada la |
| T-119-18 | Denial of Service | Tenant orfa criada quando a criacao do utilizador falha a meio | mitigate | O metodo e `@Transactional`, como `initializeSystem` — a falha do `userRepository.save` faz rollback do `tenantRepository.save`. As validacoes que podem falhar (email duplicado, papel ausente) correm todas **antes** do primeiro `save`, provado por `verify(tenantRepository, never()).save(any())` nos Casos 3, 4 e 5 |
| T-119-SC | Tampering | Instalacoes npm/pip/cargo/Maven | n/a | Zero dependencias novas; `backend/pom.xml` fica fora do diff (gate nas duas tasks). O Package Legitimacy Gate nao se aplica |
</threat_model>

<verification>
1. `cd backend && mvn test -Dtest=SetupServiceProvisionTenantTest` — 7+ testes, 0 falhas.
2. `cd backend && mvn test` — suite unitaria completa verde.
3. Gate de origem sobre o corpo extraido de `provisionTenant`: `systemSettingRepository` 0 ocorrencias, `PLATAFORMA_ADMIN` 0 ocorrencias, `validateRequest(request)` 1, `roleRepository.findByNome("ADMIN")` 1, `return tenant;` 1.
4. `SetupController.java` e `pom.xml` fora do diff.
5. `TenantProvisionResponse.java` com exatamente 2 campos e zero campos sensiveis.
</verification>

<success_criteria>
- Existe `SetupService.provisionTenant(SetupInitializeRequest)` que devolve a `Tenant` criada com `id` preenchido.
- Chamar `provisionTenant` duas vezes cria duas tenants (repetivel), enquanto `initializeSystem` continua a falhar na segunda chamada.
- `provisionTenant` nao tem uma unica referencia a `SystemSettingRepository`.
- As regras de validacao sao literalmente as mesmas do wizard publico, por reutilizacao e nao por copia.
- Existe `TenantProvisionResponse` com `id` + `nome` e nada mais.
</success_criteria>

<output>
Create `.planning/phases/LEXCV-119-backend-papel-de-administrador-de-plataforma-e-provisionamen/119-02-SUMMARY.md` when done
</output>
