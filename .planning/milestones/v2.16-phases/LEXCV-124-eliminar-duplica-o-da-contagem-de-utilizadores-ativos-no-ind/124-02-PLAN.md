---
phase: 124-eliminar-duplica-o-da-contagem-de-utilizadores-ativos-no-ind
plan: 02
type: execute
wave: 2
depends_on: ["124-01"]
files_modified:
  - web/src/types/auth.ts
  - web/src/app/(dashboard)/settings/page.tsx
  - web/scripts/verify-limite-utilizadores-indicator.mjs
autonomous: true
requirements: ["v2.16-MILESTONE-AUDIT achado de integração #2 — contagem de utilizadores ativos, fonte única (tech debt closure, sem requisito v1 novo)"]

must_haves:
  truths:
    - "O indicador 'X/Y utilizadores' das Definições mostra o número vindo de tenant_utilizadores_ativos em GET /auth/me, não um filter() sobre a lista de GET /admin/users"
    - "A expressão de filtro client-side .ativo === true já não existe em settings/page.tsx"
    - "A convenção separada user.ativo !== false (badge da tabela) permanece intocada"
    - "Os 3 estados do indicador continuam a produzir exactamente as mesmas strings: 'N utilizadores', 'N/M utilizadores', 'N/M utilizadores · limite atingido'"
    - "O botão desativado, o tooltip de span-wrapper e as classes CSS do contador continuam byte-a-byte idênticos"
    - "pnpm verify:limite-utilizadores termina verde com 9 asserções, uma das quais impede permanentemente o regresso do filtro client-side"
    - "pnpm verify:bloqueio-rbac (Fase 121, lê o mesmo ficheiro) continua verde"
  artifacts:
    - path: "web/src/types/auth.ts"
      provides: "Campo tenant_utilizadores_ativos em MeResponse, com | null explícito, seguindo o irmão numérico"
      contains: "tenant_utilizadores_ativos?: number | null;"
    - path: "web/src/app/(dashboard)/settings/page.tsx"
      provides: "activeUserCount derivado de meData, comentário do bloco actualizado para descrever a fonte real"
      contains: "meData?.tenant_utilizadores_ativos"
    - path: "web/scripts/verify-limite-utilizadores-indicator.mjs"
      provides: "9 asserções: a de contagem reescrita para a fonte nova com guarda negativa contra o filtro antigo, mais uma asserção nova para o tipo"
      contains: "tenant_utilizadores_ativos"
  key_links:
    - from: "web/src/app/(dashboard)/settings/page.tsx"
      to: "web/src/hooks/use-me.ts"
      via: "useMe() já chamado no componente, destructurado como meData, agora também fornece a contagem"
      pattern: "meData\\?\\.tenant_utilizadores_ativos"
    - from: "web/src/types/auth.ts"
      to: "backend/src/main/java/com/lexcv/dtos/UserResponse.java"
      via: "campo tenant_utilizadores_ativos do contrato de GET /auth/me, mesmo nome snake_case em ambos os lados"
      pattern: "tenant_utilizadores_ativos"
    - from: "web/scripts/verify-limite-utilizadores-indicator.mjs"
      to: "web/src/app/(dashboard)/settings/page.tsx"
      via: "asserção com guarda negativa que reprova o regresso do filtro client-side"
      pattern: "ativo === true"
---

<objective>
Passar o indicador "X/Y utilizadores" do separador "Gestão de Utilizadores" a consumir `tenant_utilizadores_ativos` de `GET /auth/me` (entregue pelo Plano 01), remover o `filter()` client-side que era a segunda implementação da contagem, e reescrever a asserção do gate da Fase 118 que codificava essa forma antiga — para que o gate passe a **impedir** o regresso do filtro em vez de o exigir.

Purpose: Esta é a metade que efectivamente fecha o achado #2 da auditoria do marco v2.16 — o Plano 01 só criou a fonte, este plano é o que a consome e apaga o duplicado. O ponto delicado desta fase vive aqui: `web/scripts/verify-limite-utilizadores-indicator.mjs` tem uma asserção (`contagem-estrita`) cuja primeira metade exige literalmente a existência de `.ativo === true` em `settings/page.tsx`. Remover o filtro **vai** fazer essa asserção falhar, e isso é correcto, não uma regressão a contornar. `124-CONTEXT.md` D-04 é explícito: actualizar o próprio gate, nunca contorná-lo, nunca apagá-lo, nunca deixá-lo vermelho. O plano trata a falha intermédia como sinal RED verificável (Task 1) e o gate reescrito como o GREEN (Task 2).
Output: 1 linha de tipo nova, 1 linha de derivação trocada, 1 bloco de comentário reescrito, 1 asserção de gate reescrita e 1 asserção de gate nova. **Zero alterações a JSX, strings de copy, classes CSS ou ao mecanismo de tooltip.**
</objective>

<execution_context>
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/LEXCV-124-eliminar-duplica-o-da-contagem-de-utilizadores-ativos-no-ind/124-CONTEXT.md
@.planning/phases/LEXCV-124-eliminar-duplica-o-da-contagem-de-utilizadores-ativos-no-ind/124-PATTERNS.md
@.planning/phases/LEXCV-124-eliminar-duplica-o-da-contagem-de-utilizadores-ativos-no-ind/124-01-SUMMARY.md
@CLAUDE.md
@web/AGENTS.md

<interfaces>
<!-- Estado real do repositório em 2026-07-30, lido directamente (ver 124-PATTERNS.md). -->
<!-- O 124-01-SUMMARY.md confirma o nome e tipo finais do campo backend — usar esse nome, não este, se divergirem. -->

Estado actual de `MeResponse` (`web/src/types/auth.ts:18-32`) — este É o estado pós-Fase-118:

```typescript
export interface MeResponse {
  id: string;
  tenant_id: string;
  nome: string;
  email: string;
  roles: Role[];
  avatar_url?: string;
  telefone?: string;
  ativo?: boolean;
  permissions: string[];
  tenant_nome?: string;
  tenant_logo_data_url?: string;
  tenant_plano?: string | null;
  tenant_limite_utilizadores?: number | null;
}
```

Nota de deriva: `118-02-PLAN.md` mostra a forma *pré*-Fase-118 deste ficheiro e o seu texto de acção diz `tenant_plano?: string;` sem `| null`. O código realmente entregue usa `| null` em ambos os campos nullable. **Usar o ficheiro actual acima como precedente, não o texto do plano antigo.**

`web/src/hooks/use-me.ts` (ficheiro completo, 16 linhas) — **confirm-only, não alterar**:

```typescript
import { useQuery } from "@tanstack/react-query";
import { apiFetch } from "@/lib/api";
import type { MeResponse } from "@/types/auth";

export function useMe() {
  const enabled = typeof window !== "undefined" ;
  return useQuery({
    queryKey: ["auth", "me"],
    queryFn: () => apiFetch<MeResponse>("/auth/me"),
    enabled,
    staleTime: 60_000,
  });
}
```

O hook está tipado como `apiFetch<MeResponse>`, logo herda o campo novo por tipagem estrutural assim que `MeResponse` o ganhar. Nenhuma alteração ao hook.

Bloco de derivação actual em `UserManagementTab` (`web/src/app/(dashboard)/settings/page.tsx:196-211`) — é isto que a Task 1 altera:

```typescript
  // Indicador "X/Y utilizadores" (Phase 118 PLAN-03) — useMe() dedupe pela cache
  // partilhada ["auth","me"], nao e um segundo pedido de rede. X usa igualdade
  // estrita (=== true) para espelhar countByTenantIdAndAtivoTrue do backend;
  // deliberadamente diferente da convencao de exibicao do badge "Ativo" da
  // tabela mais abaixo, que trata utilizadores sem o campo definido como ativos.
  const { data: meData } = useMe();
  const activeUserCount = users?.filter((u) => u.ativo === true).length ?? 0;
  const tenantUserLimit = meData?.tenant_limite_utilizadores ?? null;
  const atUserLimit =
    tenantUserLimit !== null && activeUserCount >= tenantUserLimit;
  const userCountLabel =
    tenantUserLimit === null
      ? `${activeUserCount} utilizadores`
      : atUserLimit
        ? `${activeUserCount}/${tenantUserLimit} utilizadores · limite atingido`
        : `${activeUserCount}/${tenantUserLimit} utilizadores`;
```

Bloco de render (`:391-400`) — **não alterar, reproduzido só como prova de não-regressão**:

```tsx
<div className="flex flex-col items-end gap-2">
  <span
    className={
      atUserLimit
        ? "text-xs font-semibold text-red-600 dark:text-red-400"
        : "text-xs text-slate-500 dark:text-slate-400"
    }
  >
    {userCountLabel}
  </span>
  {atUserLimit ? (
    <Tooltip>
      <TooltipTrigger asChild>
        <span tabIndex={0}>
```

Asserção do gate que **vai** falhar e tem de ser reescrita (`web/scripts/verify-limite-utilizadores-indicator.mjs:114-121`, verbatim):

```javascript
    {
      id: "contagem-estrita",
      descricao:
        "settings/page.tsx contem '.ativo === true' (contagem estrita, espelha countByTenantIdAndAtivoTrue) E continua a conter 'user.ativo !== false' (convencao de exibicao do badge da tabela, inalterada)",
      predicate: () =>
        settingsPage.includes(".ativo === true") &&
        settingsPage.includes("user.ativo !== false"),
    },
```

Forma da asserção do tipo, a copiar para a asserção nova (`:86-92`):

```javascript
    {
      id: "types-auth-tenant-limite",
      descricao:
        "web/src/types/auth.ts contem 'tenant_limite_utilizadores?: number | null;' (com '| null' explicito)",
      predicate: () =>
        authTypes.includes("tenant_limite_utilizadores?: number | null;"),
    },
```

Os 8 ids de asserção existentes, na ordem actual do array (`:79-158`): `types-auth-tenant-plano`, `types-auth-tenant-limite`, `toast-prefix-generico`, `use-me-auto-fetch`, `contagem-estrita`, `copy-contract`, `span-wrapper-tooltip`, `layout-stack`.

Helper `stripComments()` (`:34-49`) — **crítico para esta fase**: corre sobre ambos os ficheiros **antes** de qualquer predicado, e remove blocos `{/* ... */}`, blocos `/* ... */` e linhas que começam por `//` ou por `*`. Consequência dupla: (a) mencionar um token num comentário **não** satisfaz um predicado positivo do gate; (b) mencionar um token num comentário **não** quebra um predicado negativo do gate. Não duplicar este helper. **Atenção:** isto vale para o gate `.mjs`; os greps de shell dos critérios de aceitação deste plano correm sobre o ficheiro inteiro (ver `<baseline_measurements>`), onde um comentário **conta**.

Harness PASS/FAIL (`:160-179`) — **não alterar**; imprime `PASS <id>` ou `FAIL <id> — <motivo>` por asserção e faz `process.exit(failures === 0 ? 0 : 1)`.

Invalidação de cache já existente (`web/src/hooks/use-admin.ts:30-35`) — **confirm-only, nenhuma lógica nova necessária**: `useAdminSaveUser.onSuccess` já invalida `["admin","users"]` **e** `["auth","me"]`, logo criar/editar/reactivar um utilizador já refresca a query de onde o contador novo lê.
</interfaces>

<baseline_measurements>
<!-- Contagens reais medidas no repositório durante o planeamento (2026-07-30), com grep -cF -->
<!-- sobre o ficheiro inteiro (sem filtro de comentários). São a linha-base dos gates de aceitação. -->

| Ficheiro | Padrão (`grep -cF`) | Antes desta task | Depois desta task |
|---|---|---|---|
| `settings/page.tsx` | `ativo === true` | 1 | **0** |
| `settings/page.tsx` | `user.ativo !== false` | **1** | **1** (inalterado) |
| `settings/page.tsx` | `meData?.tenant_utilizadores_ativos` | 0 | **1** |
| `settings/page.tsx` | `useAdminUsers` | 2 | **2** (inalterado) |
| `settings/page.tsx` | `const { data: meData } = useMe();` | 1 | **1** (inalterado) |
| `types/auth.ts` | `tenant_limite_utilizadores?: number \| null;` | 1 | **1** (inalterado) |
| `types/auth.ts` | `tenant_utilizadores_ativos?: number \| null;` | 0 | **1** |
| `verify-limite-utilizadores-indicator.mjs` | `id: "` | 8 | **9** |
| `verify-limite-utilizadores-indicator.mjs` | `contagem-estrita` | 1 | **0** |

**Correcção ao `124-PATTERNS.md`** — a sua secção 7 afirma que "`user.ativo !== false` appears 4 times elsewhere". Isso conta a *família* de padrões `.ativo !== false` (linhas 281, 511, 652, 653, 657), não a string literal. A string literal em minúsculas `user.ativo !== false` aparece **exactamente 1 vez** (linha 511, o badge da tabela) — as outras ocorrências são `editingUser.ativo !== false`, com `U` maiúsculo, que não casam. **Usar 1, não 4.** Uma ocorrência é suficiente e é o que o predicado `settingsPage.includes("user.ativo !== false")` do gate exige.

**Quirks deste ambiente (Git Bash/MSYS2, GNU grep 3.0) — registados pela Fase 123 e reconfirmados durante este planeamento:**
1. `grep -vE '^[[:space:]]*(//|\*|/\*)'` (a construção "excluir linhas de comentário") **funciona correctamente** — confirmado invocando o binário real directamente (`/usr/bin/grep`), que devolve 268/308 linhas sobreviventes para `AuthController.java`. **A causa real de uma alegação anterior de que esta construção "está quebrada" não é o GNU grep nem o MSYS2 — é o hook do `rtk` deste utilizador (ver `RTK.md` global), que reescreve silenciosamente `grep ...` simples para `rtk grep ...`, uma reimplementação própria ("strips whitespace, truncates, groups by file") com semântica diferente da do grep real.** Confirmado nesta sessão: a mesma invocação como `grep` simples devolveu `0`; a mesma invocação como `/usr/bin/grep` (contorna o hook) devolveu `268`. Por isso **todos** os gates deste plano usam `grep -cF` sobre o ficheiro inteiro — não porque a exclusão de comentários esteja quebrada, mas porque padrões literais simples com `-cF` são a forma mais robusta a este tipo de reescrita de comando. Consequência deliberada: um comentário que reproduza literalmente `ativo === true` faria o gate de shell reprovar (mesmo que o gate `.mjs` o ignorasse por causa do `stripComments`) — o que é exactamente o comportamento desejado. **Nota para fases futuras:** preferir `grep -cF` com padrões literais simples, ou `/usr/bin/grep` directo para padrões complexos (alternância, classes de caracteres, `-v`) — bare `grep` com esses padrões pode passar pelo hook do `rtk` e devolver contagens erradas silenciosamente.
2. Em ERE, `\(`/`\)` disparam "Unmatched ( or \(" neste grep. Usar bracket expressions: `[(]`, `[)]`.
3. `|` sem a flag `-E` é tratado como carácter literal — usar `-E` quando houver alternância, ou `-F` quando o padrão for literal (é o caso de quase todos os gates deste plano, porque `?`, `|` e `.` aparecem nas strings procuradas).
4. Caminhos com parênteses (`src/app/(dashboard)/...`) têm de vir entre aspas em todos os comandos.
</baseline_measurements>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Trocar a fonte de activeUserCount para tenant_utilizadores_ativos e remover o filtro client-side</name>

  <files>
    web/src/types/auth.ts
    web/src/app/(dashboard)/settings/page.tsx
  </files>

  <read_first>
    - `.planning/phases/LEXCV-124-eliminar-duplica-o-da-contagem-de-utilizadores-ativos-no-ind/124-01-SUMMARY.md` — **ler primeiro.** Confirma o nome e o tipo finais do campo tal como foi realmente entregue no backend. Se divergir de `tenant_utilizadores_ativos`, usar o nome do SUMMARY (o contrato HTTP manda, não este plano).
    - `web/src/types/auth.ts` — ficheiro completo (32 linhas). O ponto de inserção é a última propriedade de `MeResponse`, depois de `tenant_limite_utilizadores`.
    - `web/src/app/(dashboard)/settings/page.tsx` — o ficheiro a alterar. Ler em particular: os imports (`:26`, `:28-29`, para confirmar que `useMe` e `useAdminUsers` já estão importados e que **nenhum import novo nem removido é necessário** — a lista `users` continua a ser precisa para a tabela de gestão), o bloco de derivação completo (`:196-211`, o alvo), a linha `:511` (a única ocorrência literal de `user.ativo !== false`, que **não** é tocada), e o bloco de render (`:391-400`, que **não** é alterado).
    - `web/src/hooks/use-me.ts` — ficheiro completo (16 linhas), para confirmar por leitura que o hook está tipado `apiFetch<MeResponse>` e portanto herda o campo novo sem alteração. **Não alterar este ficheiro.**
    - `.planning/phases/LEXCV-124-eliminar-duplica-o-da-contagem-de-utilizadores-ativos-no-ind/124-PATTERNS.md` — secções 5, 6 e 7. A secção 7 contém o facto que autoriza a remoção: `.ativo === true` aparece **exactamente uma vez** neste ficheiro (a própria derivação). **Ler em conjunto com o `<baseline_measurements>` deste plano, que corrige a contagem de `user.ativo !== false` de 4 para 1.**
    - `web/scripts/verify-limite-utilizadores-indicator.mjs` — a asserção `contagem-estrita` (`:114-121`) e o helper `stripComments()` (`:34-49`). Ler **antes** de alterar o código, para saber exactamente qual asserção vai ficar vermelha e porquê. **Não alterar o gate nesta task** — é a Task 2 que o faz.
  </read_first>

  <behavior>
    - Com `tenant_utilizadores_ativos = 5` e `tenant_limite_utilizadores = null` → label `"5 utilizadores"`, `atUserLimit` falso, contador em cinzento, botão activo.
    - Com `tenant_utilizadores_ativos = 5` e `tenant_limite_utilizadores = 7` → label `"5/7 utilizadores"`, `atUserLimit` falso, contador em cinzento, botão activo.
    - Com `tenant_utilizadores_ativos = 5` e `tenant_limite_utilizadores = 5` → label `"5/5 utilizadores · limite atingido"`, `atUserLimit` verdadeiro, contador em vermelho/semibold, botão desativado, tooltip de span-wrapper activo.
    - Com o campo ausente ou `undefined` (resposta antiga em cache, ou `useMe()` ainda a carregar) → o contador lê `0`, nunca `NaN` nem vazio. Fail-safe: `?? 0`, não `?? null` — um `null` propagaria para a interpolação de template e mostraria `"null utilizadores"`.
    - Regressão que **não** pode acontecer: nenhuma das 3 strings acima muda um único carácter, e nenhuma classe CSS do contador ou do botão muda.
  </behavior>

  <action>
    Duas alterações de uma linha e uma reescrita de comentário. Nada mais.

    **1. `web/src/types/auth.ts`.** Acrescentar um campo à interface `MeResponse`, como última propriedade, imediatamente depois de `tenant_limite_utilizadores?: number | null;`. Nome: `tenant_utilizadores_ativos` (ou o nome confirmado por `124-01-SUMMARY.md`, se divergir). Forma exacta: opcional (`?`), tipo `number | null`. Seguir a convenção do irmão numérico imediatamente acima (`| null` explícito), **não** a dos irmãos mais antigos `tenant_nome`/`tenant_logo_data_url` (que não têm `| null`). O `| null` explícito é obrigatório porque o backend devolve `null` quando o tenant não é encontrado (provado pelo caso de teste 3 do Plano 01), e porque a asserção nova do gate (Task 2) procura a string com `| null` incluído. Não reordenar nem alterar nenhuma propriedade existente.

    **2. `web/src/app/(dashboard)/settings/page.tsx` — a linha de derivação.** Substituir a atribuição de `activeUserCount` por uma leitura de `meData`, na mesma forma de optional-chaining-com-fallback que a linha imediatamente seguinte já usa para o limite, mas com fallback `0` em vez de `null`. Remover completamente a chamada `.filter(...)` sobre `users` — não a deixar como código morto, não a deixar comentada, não a deixar numa variável não usada.

    **Não tocar** em `const { data: meData } = useMe();` (a linha acima) — em particular **não renomear a binding `meData`**: a Fase 121 renomeou-a deliberadamente de `me` para `meData` para desambiguar de uma binding homónima em `RbacTab` cujas asserções em `verify-bloqueio-rbac.mjs` casam `me.isFetched` por substring exacta. Renomear de volta quebraria esse gate vizinho.

    **Não tocar** em `tenantUserLimit`, `atUserLimit` nem `userCountLabel` — as três expressões ficam literalmente iguais. É isso que garante o D-03 ("byte-a-byte idênticos") sem depender de verificação visual: se as entradas produzem os mesmos números, o ternário produz as mesmas strings.

    **Não tocar** em `useAdminUsers()` nem na variável `users` — a lista completa continua necessária para a tabela de gestão logo abaixo. Só a origem do número do indicador muda.

    **3. `web/src/app/(dashboard)/settings/page.tsx` — o bloco de comentário.** O comentário de 5 linhas acima da derivação explica hoje *porque* é que o cliente recalcula a contagem com igualdade estrita. Depois desta alteração o cliente já não recalcula nada, e deixar o comentário como está documentaria um raciocínio que já não descreve o código. Reescrevê-lo para dizer, em prosa: (a) que `activeUserCount` vem agora directamente do campo novo de `GET /auth/me`, calculado no backend pela função de contagem única da Fase 117, fechando a duplicação que a auditoria do marco v2.16 encontrou (Fase 124); (b) que `useMe()` continua a ser um acerto de cache na chave partilhada `["auth","me"]`, não um segundo pedido de rede; (c) que a lista de `useAdminUsers()` se mantém apenas para a tabela de gestão abaixo; (d) que a convenção de exibição do badge "Ativo" da tabela é separada e permanece inalterada.

    **Restrição literal obrigatória no comentário:** não escrever `ativo === true` no comentário novo. Duas razões concretas: o critério de aceitação desta task exige `grep -cF 'ativo === true'` = `0` sobre o **ficheiro inteiro** (a construção de grep que excluiria comentários está comprovadamente quebrada neste shell — ver `<baseline_measurements>` quirk 1), logo escrevê-lo no comentário reprova a task; e escrevê-lo num comentário também **não** satisfaria nenhum predicado do gate `.mjs` (o `stripComments()` remove linhas `//` antes de qualquer predicado correr), pelo que não é atalho para nada. Manter o comentário sem acentos, no mesmo estilo dos comentários vizinhos deste ficheiro.

    **Verificar o estado RED esperado.** Correr `cd web && pnpm verify:limite-utilizadores`. **Esperado: exit 1, com exactamente uma linha `FAIL` e essa linha a ser `FAIL contagem-estrita`.** As outras 7 asserções têm de imprimir `PASS`. Este é o sinal RED desta task e é a prova de que o gate da Fase 118 é preciso — falha exactamente na asserção que codificava a forma antiga, e em nenhuma outra. Se falhar mais do que uma asserção, **parar e investigar**: as asserções `copy-contract`, `span-wrapper-tooltip`, `layout-stack` e `use-me-auto-fetch` só podem falhar se JSX, copy, CSS ou a chamada a `useMe()` tiverem sido alterados, o que esta task proíbe. Se falhar zero asserções, **parar e investigar**: significa que o filtro não foi realmente removido.

    Correr depois `cd web && pnpm lint` e `cd web && pnpm build`, ambos têm de ficar limpos. `pnpm`, nunca `npm` — o `pnpm-lock.yaml` é a fonte autoritativa e existe um `package-lock.json` obsoleto em `web/` a ignorar.

    Proibições: não alterar nenhum ficheiro sob `web/src/components/`; não alterar `web/src/hooks/use-me.ts` nem `web/src/hooks/use-admin.ts` (a invalidação de `["auth","me"]` já existe); não alterar nada sob `web/scripts/` nesta task; não alterar `web/package.json` (a entrada `verify:limite-utilizadores` já está registada); não instalar nada; não alterar o JSX do `CardHeader`, do `<span>` do contador, do `Tooltip` ou do `Button`.
  </action>

  <verify>
    <automated>cd web && [ "$(grep -cF 'tenant_utilizadores_ativos?: number | null;' src/types/auth.ts)" -eq 1 ] && [ "$(grep -cF 'meData?.tenant_utilizadores_ativos' "src/app/(dashboard)/settings/page.tsx")" -eq 1 ] && [ "$(grep -cF 'ativo === true' "src/app/(dashboard)/settings/page.tsx")" -eq 0 ] && [ "$(grep -cF 'user.ativo !== false' "src/app/(dashboard)/settings/page.tsx")" -eq 1 ] && [ "$(grep -cF 'useAdminUsers' "src/app/(dashboard)/settings/page.tsx")" -eq 2 ] && [ "$(grep -cF 'const { data: meData } = useMe();' "src/app/(dashboard)/settings/page.tsx")" -eq 1 ] && [ "$(pnpm -s verify:limite-utilizadores | grep -c '^FAIL ')" -eq 1 ] && pnpm -s verify:limite-utilizadores | grep -q '^FAIL contagem-estrita' && [ "$(pnpm -s verify:limite-utilizadores | grep -c '^PASS ')" -eq 7 ] && pnpm lint && pnpm build</automated>
  </verify>

  <acceptance_criteria>
    - `grep -cF 'tenant_utilizadores_ativos?: number | null;' web/src/types/auth.ts` devolve exactamente `1` (com o `| null` explícito).
    - `grep -cF 'meData?.tenant_utilizadores_ativos' "web/src/app/(dashboard)/settings/page.tsx"` devolve exactamente `1`.
    - `grep -cF 'ativo === true' "web/src/app/(dashboard)/settings/page.tsx"` devolve `0` — o filtro client-side desapareceu do ficheiro inteiro, incluindo comentários. Se devolver `1`, a causa quase certa é o comentário novo tê-lo reproduzido — corrigir o comentário, não o gate.
    - `grep -cF 'user.ativo !== false' "web/src/app/(dashboard)/settings/page.tsx"` devolve `1` — inalterado face à linha-base; a convenção separada do badge da tabela (linha 511) está intocada. **A linha-base é 1, não 4** — ver a correcção ao PATTERNS.md em `<baseline_measurements>`.
    - `grep -cF 'useAdminUsers' "web/src/app/(dashboard)/settings/page.tsx"` devolve `2` (inalterado — a lista continua a alimentar a tabela) e `grep -cF 'const { data: meData } = useMe();'` devolve `1` (binding não renomeada).
    - As 3 expressões `tenantUserLimit`, `atUserLimit` e `userCountLabel` estão inalteradas: `git diff "web/src/app/(dashboard)/settings/page.tsx"` não mostra nenhuma linha adicionada ou removida que contenha `tenantUserLimit =`, `atUserLimit =`, `userCountLabel =`, `· limite atingido`, `flex flex-col items-end gap-2`, `tabIndex={0}` ou `TooltipTrigger`.
    - `git diff --stat` para `settings/page.tsx` mostra uma alteração pequena e localizada (ordem de grandeza: 1 linha de código trocada + o bloco de comentário de ~5 linhas reescrito) — não uma reformatação do ficheiro.
    - `cd web && pnpm verify:limite-utilizadores` termina com exit code **1**, o output contém exactamente **uma** linha começada por `FAIL ` e essa linha é `FAIL contagem-estrita`, e contém exactamente **7** linhas `PASS `. (Este RED é intencional e é resolvido pela Task 2.)
    - `cd web && pnpm lint` termina com exit code 0, sem erros novos.
    - `cd web && pnpm build` termina com exit code 0.
    - O diff desta task lista **exclusivamente** `web/src/types/auth.ts` e `web/src/app/(dashboard)/settings/page.tsx`. Em particular **não** lista `web/src/hooks/use-me.ts`, `web/src/hooks/use-admin.ts`, `web/package.json` nem nada sob `web/scripts/` ou `web/src/components/`.
  </acceptance_criteria>

  <done>O indicador lê a contagem do backend; a segunda implementação client-side deixou de existir no ficheiro; o comentário descreve o código real; lint e build estão limpos; e o gate da Fase 118 falha em exactamente uma asserção — a que a Task 2 vai reescrever.</done>
</task>

<task type="auto">
  <name>Task 2: Reescrever a asserção de contagem do gate para a fonte nova, com guarda negativa, e provar a sua precisão</name>

  <files>
    web/scripts/verify-limite-utilizadores-indicator.mjs
  </files>

  <read_first>
    - `web/scripts/verify-limite-utilizadores-indicator.mjs` — ficheiro completo (182 linhas). Ler em particular: o comentário de cabeçalho (`:1-9`), o helper `stripComments()` (`:34-49`) e o que ele remove, o helper `findTooltipBlockContaining()` (`:57-65`), o array de 8 asserções na íntegra (`:79-158`) para conhecer todos os ids e a ordem, a asserção `contagem-estrita` (`:114-121`) que é o alvo da reescrita, a asserção `types-auth-tenant-limite` (`:86-92`) que é a forma a copiar para a asserção nova, e o harness PASS/FAIL (`:160-179`) que **não** é alterado.
    - `web/src/app/(dashboard)/settings/page.tsx` — o bloco de derivação **depois** da Task 1, para confirmar por leitura a string exacta que o predicado novo tem de procurar. Os predicados casam por substring (`String.prototype.includes`), não por regex — um espaço a mais ou a menos faz falhar.
    - `web/src/types/auth.ts` — depois da Task 1, para confirmar a string exacta do campo novo, incluindo o `| null` e o `;` final.
    - `.planning/phases/LEXCV-124-eliminar-duplica-o-da-contagem-de-utilizadores-ativos-no-ind/124-CONTEXT.md` — a decisão D-04, que é a autoridade desta task: actualizar o gate para reflectir o campo novo, nunca contorná-lo.
    - `.planning/phases/LEXCV-124-eliminar-duplica-o-da-contagem-de-utilizadores-ativos-no-ind/124-PATTERNS.md` — secção 8, que identifica esta como a única alteração *de significado* de uma asserção existente nesta fase (todas as outras são adições puras).
  </read_first>

  <action>
    Duas alterações ao array `assertions`. O harness, os helpers e as outras 7 asserções ficam intocados.

    **1. Reescrever a asserção `contagem-estrita`.** Alterar os três campos (`id`, `descricao`, `predicate`) da entrada na posição actual dela no array (entre `use-me-auto-fetch` e `copy-contract`) — não a mover, não a duplicar, não a apagar.

    Novo `id`: `contagem-da-fonte-unica`. O id muda porque o significado da asserção muda: já não verifica uma igualdade estrita local, verifica que a contagem vem da fonte única do backend.

    Novo `predicate`: uma conjunção de **três** condições sobre `settingsPage`:
    - positiva — inclui a substring exacta da nova derivação (`meData?.tenant_utilizadores_ativos`), provando que o indicador lê o campo do backend;
    - **negativa** — **não** inclui `.ativo === true`, provando que o filtro client-side não voltou. Esta é a metade mais valiosa da asserção reescrita: é a guarda permanente contra o regresso da dívida técnica que esta fase fecha, e converte o gate de "exige a forma antiga" em "proíbe a forma antiga";
    - positiva, inalterada — continua a incluir `user.ativo !== false`, a convenção separada de exibição do badge da tabela, que esta fase não toca e que tem de continuar a existir.

    Nova `descricao`: descrever as três condições em prosa, sem acentos, no estilo das outras 8 descrições, e nomear a Fase 124 como origem da alteração.

    **2. Acrescentar uma asserção nova para o tipo.** Novo `id`: `types-auth-tenant-utilizadores-ativos`. Inseri-la imediatamente **depois** de `types-auth-tenant-limite`, mantendo o agrupamento do array (asserções sobre `auth.ts` primeiro, asserções sobre `settings/page.tsx` depois). `predicate`: `authTypes` inclui a string exacta do campo novo com o `| null` e o `;` finais. `descricao`: copiar a forma da descrição de `types-auth-tenant-limite`, adaptada.

    O array passa de 8 para 9 asserções.

    **Não alterar** o comentário de cabeçalho do ficheiro para lá do mínimo necessário (se for actualizado para mencionar a Fase 124, mantê-lo curto), nem `stripComments()`, nem `findTooltipBlockContaining()`, nem a constante `TOOLTIP_PHRASE`, nem o harness `for`/`process.exit`, nem qualquer uma das outras 7 asserções.

    **Correr o gate.** `cd web && pnpm verify:limite-utilizadores`. Esperado: exit 0, **9** linhas `PASS`, **zero** linhas `FAIL`.

    **Prova negativa da precisão do gate.** Seguir a técnica já usada e registada nas Fases 121 e 122 (regressões mínimas de uma linha, para isolar exactamente uma asserção em vez de um sinal cego tudo-ou-nada). Executar duas provas, uma de cada vez, revertendo entre elas:

    - **Prova A:** em `settings/page.tsx`, reverter temporariamente a linha de derivação para a forma antiga (`users?.filter((u) => u.ativo === true).length ?? 0`). Correr o gate. **Esperado: exactamente 1 `FAIL`, e é `contagem-da-fonte-unica`** — as duas metades falham em conjunto (perde-se a substring nova e reaparece a antiga), provando que a guarda negativa funciona. Reverter para o estado correcto e reconfirmar 9 `PASS`.
    - **Prova B:** em `types/auth.ts`, remover temporariamente o `| null` do campo novo (deixando `tenant_utilizadores_ativos?: number;`). Correr o gate. **Esperado: exactamente 1 `FAIL`, e é `types-auth-tenant-utilizadores-ativos`** — e em particular `types-auth-tenant-limite` continua `PASS`, provando que a asserção nova não está a casar por acidente a string do irmão. Reverter e reconfirmar 9 `PASS`.

    Se qualquer uma das provas fizer falhar mais do que uma asserção, ou fizer falhar uma asserção diferente da esperada, o gate não é preciso — **parar, investigar e corrigir o predicado**, não aceitar o sinal. Registar no SUMMARY o output real de cada prova negativa.

    **Não-regressão dos gates vizinhos.** `web/scripts/verify-bloqueio-rbac.mjs` (Fase 121) lê o **mesmo** `settings/page.tsx` para uma asserção de gating de RBAC não relacionada. Já foi confirmado por grep que não referencia `ativo`, `activeUserCount`, `tenant_limite` nem `tenant_utilizadores` em nenhuma forma, mas as suas asserções A03/A04 casam `me.isFetched` por substring exacta. Correr `cd web && pnpm verify:bloqueio-rbac` e confirmar exit 0. Correr também `cd web && pnpm verify:consola-tenants` e `cd web && pnpm verify:relatorio-utilizacao` — nenhum dos dois lê `settings/page.tsx`, logo esperam-se verdes sem surpresa; é uma confirmação barata de que nenhum gate do marco v2.16 ficou vermelho.

    Aplicar os 4 quirks de ambiente listados em `<baseline_measurements>` a qualquer grep de verificação escrito nesta task, em vez de repetir o mesmo engano.

    Proibições: não alterar nenhum ficheiro sob `web/src/` nesta task (as provas negativas são temporárias e revertidas — o diff final desta task tem de mostrar apenas o `.mjs`); não apagar nenhuma asserção; não relaxar nenhum predicado existente para o fazer passar; não acrescentar entradas novas a `web/package.json`; não instalar nada.
  </action>

  <verify>
    <automated>cd web && [ "$(pnpm -s verify:limite-utilizadores | grep -c '^PASS ')" -eq 9 ] && [ "$(pnpm -s verify:limite-utilizadores | grep -c '^FAIL ')" -eq 0 ] && pnpm -s verify:limite-utilizadores | grep -q '^PASS contagem-da-fonte-unica' && pnpm -s verify:limite-utilizadores | grep -q '^PASS types-auth-tenant-utilizadores-ativos' && [ "$(grep -cF 'id: "' scripts/verify-limite-utilizadores-indicator.mjs)" -eq 9 ] && [ "$(grep -cF 'contagem-estrita' scripts/verify-limite-utilizadores-indicator.mjs)" -eq 0 ] && [ "$(grep -cF '!settingsPage.includes(".ativo === true")' scripts/verify-limite-utilizadores-indicator.mjs)" -eq 1 ] && [ "$(git status --porcelain src/ | wc -l)" -eq 0 ] && pnpm -s verify:bloqueio-rbac && pnpm -s verify:consola-tenants && pnpm -s verify:relatorio-utilizacao && pnpm lint && pnpm build</automated>
  </verify>

  <acceptance_criteria>
    - `cd web && pnpm verify:limite-utilizadores` termina com exit code 0, imprime **9** linhas `PASS ` e **0** linhas `FAIL `.
    - Entre as 9 linhas `PASS` estão `PASS contagem-da-fonte-unica` e `PASS types-auth-tenant-utilizadores-ativos`.
    - `grep -cF 'id: "' web/scripts/verify-limite-utilizadores-indicator.mjs` devolve `9` (era 8); `grep -cF 'contagem-estrita' web/scripts/verify-limite-utilizadores-indicator.mjs` devolve `0` (a asserção foi reescrita, não duplicada nem mantida em paralelo).
    - Os 7 ids inalterados continuam presentes no ficheiro: `types-auth-tenant-plano`, `types-auth-tenant-limite`, `toast-prefix-generico`, `use-me-auto-fetch`, `copy-contract`, `span-wrapper-tooltip`, `layout-stack`.
    - A guarda negativa existe literalmente: `grep -cF '!settingsPage.includes(".ativo === true")' web/scripts/verify-limite-utilizadores-indicator.mjs` devolve `1`.
    - `git diff` mostra que `stripComments`, `findTooltipBlockContaining`, `TOOLTIP_PHRASE` e o bloco `for`/`process.exit` não foram alterados.
    - Prova negativa A registada no SUMMARY: com a derivação antiga restaurada temporariamente, o gate imprimiu exactamente 1 `FAIL` e era `contagem-da-fonte-unica`.
    - Prova negativa B registada no SUMMARY: com o `| null` removido temporariamente do tipo, o gate imprimiu exactamente 1 `FAIL` e era `types-auth-tenant-utilizadores-ativos`, com `types-auth-tenant-limite` ainda `PASS`.
    - `cd web && pnpm verify:bloqueio-rbac`, `pnpm verify:consola-tenants` e `pnpm verify:relatorio-utilizacao` terminam todos com exit code 0.
    - `cd web && pnpm lint` e `cd web && pnpm build` terminam com exit code 0.
    - `git status --porcelain web/src/` não devolve nenhuma linha — as duas provas negativas foram integralmente revertidas. O diff desta task lista **exclusivamente** `web/scripts/verify-limite-utilizadores-indicator.mjs`.
  </acceptance_criteria>

  <done>O gate da Fase 118 está verde com 9 asserções e passou a proibir o filtro client-side em vez de o exigir; a sua precisão está provada por duas regressões mínimas que isolaram exactamente uma asserção cada; e os outros 3 gates do marco v2.16 continuam verdes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Resposta de `GET /auth/me` → estado do componente `UserManagementTab` | O contador passa a confiar num número vindo do servidor em vez de o derivar de uma segunda resposta do servidor. Ambas as origens são o mesmo backend, atrás do mesmo cookie JWT — a fronteira de confiança não se alarga, apenas muda de resposta |
| UI (espelho) ↔ Backend (autoritário) | O indicador e o botão desativado continuam a ser UX; o `409` de `AdminController.limiteUtilizadoresExcedido` continua a ser a única aplicação real do limite. Nada nesta fase promove a UI a gate |
| Gate de origem (`verify-limite-utilizadores-indicator.mjs`) ↔ código de produção | O gate é o mecanismo que impede o regresso da duplicação. Alterá-lo é a alteração mais sensível deste plano: relaxá-lo por conveniência apagaria a única prova permanente do fecho desta dívida |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-124-05 | Tampering | O gate de origem da Fase 118, ao ser reescrito | mitigate | O risco real desta fase não é no produto, é no gate: apagar a asserção, comentá-la, ou relaxar o predicado para "passa sempre" fecharia a fase com um sinal falso. Mitigado em três camadas: (a) `124-CONTEXT.md` D-04 proíbe explicitamente contornar o gate; (b) a Task 2 exige contagem literal de asserções (`grep -cF 'id: "'` = 9, `grep -cF 'contagem-estrita'` = 0, e presença literal da guarda negativa = 1) — apagar em vez de reescrever reprova; (c) duas provas negativas de uma linha cada têm de isolar exactamente 1 `FAIL` no id esperado, o que um predicado sempre-verdadeiro não consegue satisfazer |
| T-124-06 | Elevation of Privilege | Botão "Novo utilizador" desactivado com base num número vindo do servidor | accept | Um cliente que adultere a resposta de `/auth/me` no browser consegue reactivar o botão — exactamente como já conseguia antes desta fase adulterando a lista de `/admin/users`. A superfície não muda. O `POST /api/v1/admin/users` continua a devolver `409` via `limiteUtilizadoresExcedido` (Fase 117), que não é tocado nem por este plano nem pelo Plano 01. Herda a disposição de T-118-02 |
| T-124-07 | Information Disclosure | Contagem renderizada no separador "Utilizadores" | accept | O número é a ocupação do próprio tenant do utilizador autenticado, e o separador que o renderiza já está gated por `hasUsersManage` (`users:manage`/ADMIN) no frontend e pelo `@PreAuthorize` de `AdminController` no backend para a lista que a tabela mostra. O campo vem de `/auth/me`, servido apenas ao próprio principal. Nenhum dado novo se torna visível a quem já não via o mesmo número calculado a partir da lista completa — se alguma coisa, a origem passa a ser mais estreita (um inteiro em vez da lista inteira de utilizadores) |
| T-124-08 | Tampering | Estado obsoleto do contador depois de criar/editar/reactivar um utilizador | accept | `useAdminSaveUser.onSuccess` já invalida `["auth","me"]` além de `["admin","users"]` (`use-admin.ts:30-35`), logo o contador novo refresca-se nos mesmos eventos que o antigo. `useAdminDeleteUser` não invalida `["auth","me"]`, mas `handleDeleteClick` faz um recarregamento completo da página depois do toast, o que reconstrói toda a cache. `staleTime: 60_000` em `useMe()` significa que, no pior caso sem mutação, o contador pode estar até 60 s desactualizado — o mesmo comportamento que já existia, e sem consequência de segurança porque o gate autoritário é o `409` |
| T-124-SC | Tampering | Instalações npm/pnpm | n/a | Esta fase não instala nada — `web/package.json` não é tocado (a entrada `verify:limite-utilizadores` já está registada desde a Fase 118) e nem `dependencies` nem `devDependencies` mudam. Gate: o diff da fase não lista `web/package.json` nem `pnpm-lock.yaml`. O Package Legitimacy Gate não se aplica |
</threat_model>

<verification>
1. `cd web && pnpm verify:limite-utilizadores` — exit 0, 9 `PASS`, 0 `FAIL`, incluindo `contagem-da-fonte-unica` e `types-auth-tenant-utilizadores-ativos`.
2. `cd web && pnpm verify:bloqueio-rbac` — exit 0 (lê o mesmo `settings/page.tsx`, não-regressão da Fase 121).
3. `cd web && pnpm verify:consola-tenants` e `pnpm verify:relatorio-utilizacao` — exit 0 (nenhum gate do marco v2.16 vermelho).
4. `cd web && pnpm lint` — exit 0. `cd web && pnpm build` — exit 0.
5. Contagens de origem (`grep -cF`, ficheiro inteiro — ver `<baseline_measurements>` para os quirks de grep deste ambiente): em `settings/page.tsx`, `ativo === true` = 0, `user.ativo !== false` = 1, `meData?.tenant_utilizadores_ativos` = 1, `useAdminUsers` = 2; em `types/auth.ts`, `tenant_utilizadores_ativos?: number | null;` = 1.
6. `git diff` de `settings/page.tsx` não mostra alterações a `tenantUserLimit`, `atUserLimit`, `userCountLabel`, `· limite atingido`, `flex flex-col items-end gap-2`, `tabIndex={0}` nem `TooltipTrigger` — prova estrutural do Critério de Sucesso 3 sem depender de inspecção visual.

<human-check>
Confirmação visual ao vivo dos 3 estados do indicador (Critério de Sucesso 3 do ROADMAP), para o fluxo de UAT de fim de fase. A prova estrutural do ponto 6 acima já cobre o essencial (as strings e classes não podem ter mudado se as linhas que as produzem não foram tocadas), pelo que esta confirmação é de reforço, não bloqueante — e `STATE.md` regista que a verificação por browser esteve indisponível por falha de tooling durante a Fase 122.

Com a app a correr (`backend`: `mvn spring-boot:run`; `web`: `pnpm dev`), autenticado como ADMIN, em `/settings` → separador "Gestão de Utilizadores", canto superior direito do cartão:

1. **Sem limite** — com `t_tenant.limite_utilizadores = NULL` para o tenant: o contador mostra `"N utilizadores"` (sem barra), em cinzento, e o botão "Novo utilizador" está activo. Confirmar que `N` iguala o número de utilizadores com `ativo = true` na tabela abaixo.
2. **Dentro do limite** — com `limite_utilizadores` acima da contagem actual: mostra `"N/M utilizadores"`, em cinzento, botão activo.
3. **No limite** — com `limite_utilizadores` igual à contagem actual: mostra `"N/M utilizadores · limite atingido"` em vermelho/semibold, o botão "Novo utilizador" está desactivado, e passar o rato (ou focar por Tab) sobre ele mostra o tooltip "Limite de utilizadores atingido. Desative um utilizador para libertar uma vaga.".
4. **Reactividade** — desactivar um utilizador na tabela e confirmar que o contador desce sem recarregar a página (a invalidação de `["auth","me"]` já existente).
</human-check>
</verification>

<success_criteria>
- O indicador "X/Y utilizadores" consome `tenant_utilizadores_ativos` de `GET /auth/me` — **Critério de Sucesso 2 do ROADMAP**.
- `.ativo === true` já não existe em `settings/page.tsx`; a convenção separada `user.ativo !== false` mantém a sua ocorrência intacta.
- As 3 strings de estado e todas as classes CSS do contador, do botão e do tooltip estão provadamente inalteradas por diff — **Critério de Sucesso 3 do ROADMAP**, com confirmação visual de reforço deixada ao UAT de fim de fase.
- `pnpm verify:limite-utilizadores` está verde com 9 asserções e passou a **proibir** o filtro client-side em vez de o exigir; os outros 3 gates do marco, o lint e o build continuam verdes — **Critério de Sucesso 4 do ROADMAP, metade frontend**.
- A precisão do gate reescrito está provada por duas regressões mínimas que isolaram exactamente uma asserção cada.
</success_criteria>

<output>
Criar `.planning/phases/LEXCV-124-eliminar-duplica-o-da-contagem-de-utilizadores-ativos-no-ind/124-02-SUMMARY.md` quando concluído.

Registar no SUMMARY, obrigatoriamente:
- O output real das duas provas negativas (A e B), incluindo a linha `FAIL` exacta de cada uma.
- A confirmação de que o RED intermédio da Task 1 foi exactamente `FAIL contagem-estrita` e mais nada (7 `PASS`, 1 `FAIL`).
- As contagens finais de grep face à tabela de `<baseline_measurements>`.
- O texto final do bloco de comentário reescrito em `settings/page.tsx`.
- Que o Critério de Sucesso 3 foi provado estruturalmente por diff, e que a confirmação visual dos 3 estados fica para o UAT de fim de fase (com o `<human-check>` deste plano como guião).
</output>
