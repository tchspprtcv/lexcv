---
phase: "83"
plan: "02"
type: execute
wave: 2
depends_on: ["83-01"]
files_modified:
  - web/src/hooks/use-processos.ts
  - web/src/lib/processo-juizo-origem-mapping.ts
  - web/src/hooks/use-processos.round-trip.test.ts
  - web/scripts/verify-juizo-origem-roundtrip.mjs
autonomous: true
requirements: []  # Integration phase — supports PROC-01 to PROC-14 (ROADMAP Phase 83 goal); owns no dedicated requirement ID per ROADMAP/REQUIREMENTS.md traceability table

must_haves:
  truths:
    - "use-processos.ts expõe o quarteto list/create/update/delete para Decisão, Testemunha e Facto, seguindo a convenção de queryKey já usada (['processos', '<subresource>', id])"
    - "normalizeProcesso()/toProcessoApiPayload() mapeiam juizo/origem corretamente, delegando essa lógica a um módulo partilhado (processo-juizo-origem-mapping.ts), e ambas as funções estão exportadas para serem verificáveis diretamente"
    - "Um teste de round-trip automatizado e executável (não apenas tsc/build limpo) confirma que juizo/origem sobrevivem a um ciclo criar/atualizar → normalizar, chamando o MESMO módulo partilhado que use-processos.ts usa em runtime (não uma reimplementação), prevenindo a 4ª recorrência do bug de mapeamento (PITFALLS.md Pitfall 1)"
    - "useDeleteProcesso limpa também as caches das 3 novas subresources ao apagar um processo"
  artifacts:
    - path: "web/src/lib/processo-juizo-origem-mapping.ts"
      provides: "mapJuizoOrigemFromApi/mapJuizoOrigemToPayload — módulo partilhado, sem imports de path-alias (@/...), consumido tanto por use-processos.ts (runtime) como pelo script de verificação (node puro)"
      contains: "export function mapJuizoOrigemFromApi"
    - path: "web/src/hooks/use-processos.ts"
      provides: "useDecisoes/useAddDecisao/useUpdateDecisao/useDeleteDecisao, useTestemunhas/useAddTestemunha/useUpdateTestemunha/useDeleteTestemunha, useFactos/useAddFacto/useUpdateFacto/useDeleteFacto; normalizeProcesso/toProcessoApiPayload exportados, delegando o mapeamento de juizo/origem a processo-juizo-origem-mapping.ts"
      contains: "export function normalizeProcesso"
    - path: "web/src/hooks/use-processos.round-trip.test.ts"
      provides: "spec vitest durável do round-trip juizo/origem (não executável nesta repo — sem test runner instalado)"
    - path: "web/scripts/verify-juizo-origem-roundtrip.mjs"
      provides: "prova automatizada e executável (node puro, import direto de processo-juizo-origem-mapping.ts) do round-trip juizo/origem, sem reimplementação"
  key_links:
    - from: "web/src/hooks/use-processos.ts (useAddDecisao)"
      to: "POST /processos/{id}/decisoes"
      via: "FormData com campos literais file/data/tipo/resumo (multipart, não JSON.stringify)"
      pattern: "form\\.set\\(\"data\""
    - from: "web/src/hooks/use-processos.ts (toProcessoApiPayload)"
      to: "web/src/lib/processo-juizo-origem-mapping.ts (mapJuizoOrigemToPayload)"
      via: "toProcessoApiPayload delega o campo juizo/origem a mapJuizoOrigemToPayload, que aplica o narrowing `'origem' in payload` — prova em tempo de compilação que PUT nunca envia origem"
      pattern: "mapJuizoOrigemToPayload\\(payload\\)"
    - from: "web/scripts/verify-juizo-origem-roundtrip.mjs"
      to: "web/src/lib/processo-juizo-origem-mapping.ts"
      via: "import relativo direto (\"../src/lib/processo-juizo-origem-mapping.ts\") das MESMAS funções mapJuizoOrigemFromApi/mapJuizoOrigemToPayload usadas em runtime por use-processos.ts — node executa .ts com type-stripping nativo, sem bundler; nenhuma reimplementação"
      pattern: "processo-juizo-origem-mapping"
---

<objective>
Ligar `juizo`/`origem` ao pipeline de mapeamento camelCase existente (`normalizeProcesso`/`toProcessoApiPayload`) via um novo módulo partilhado e sem path-aliases (`web/src/lib/processo-juizo-origem-mapping.ts`), e adicionar os 12 hooks CRUD (quarteto list/create/update/delete × Decisão/Testemunha/Facto) em `web/src/hooks/use-processos.ts`, usando os tipos e schemas definidos na Plan 83-01. Fechar com uma verificação de round-trip real que importa e executa esse MESMO módulo partilhado (não uma reimplementação), per decisão explícita de `83-CONTEXT.md`.

Purpose: `PITFALLS.md` Pitfall 1 documenta que este exato bug (campo adicionado ao formulário/tipo mas esquecido em `normalizeProcesso()`/`toProcessoApiPayload()`) já aconteceu 3 vezes neste projeto, e que nem `tsc` nem `pnpm build` o apanham — só um teste de round-trip real apanha. Uma verificação que reimplementa (copia) a lógica de mapeamento em vez de a importar reproduz exatamente essa classe de bug (a cópia diverge silenciosamente do código real), por isso esta plan centraliza o mapeamento juizo/origem num módulo partilhado importado por ambos os lados. `83-PATTERNS.md` confirma que os 12 endpoints novos são 100% consistentes em camelCase (sem ambiguidade snake_case), ao contrário de `ProcessoParte`/`ProcessoFase`, que já têm o bug hoje.

Output: um novo módulo partilhado (`web/src/lib/processo-juizo-origem-mapping.ts`, sem imports `@/...`) com `mapJuizoOrigemFromApi`/`mapJuizoOrigemToPayload`; `use-processos.ts` com os 12 hooks novos + `normalizeProcesso`/`toProcessoApiPayload` exportados, delegando o mapeamento juizo/origem a esse módulo; um spec vitest durável (`use-processos.round-trip.test.ts`, não executável nesta repo — sem test runner instalado, precedente confirmado em `82-VERIFICATION.md`); e um script Node (`verify-juizo-origem-roundtrip.mjs`) que importa diretamente o módulo partilhado e prova o round-trip de forma real e automatizada, sem exigir a instalação de um novo pacote e sem reimplementar nenhuma linha.
</objective>

<execution_context>
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/LEXCV-83-frontend-tipos-schemas-e-hooks/83-CONTEXT.md
@.planning/phases/LEXCV-83-frontend-tipos-schemas-e-hooks/83-PATTERNS.md
@.planning/research/PITFALLS.md
@.planning/milestones/v2.8-phases/LEXCV-74-enum-documento-tipo-bi-nif-restri-o-por-tipo/74-02-SUMMARY.md
</context>

<interfaces>
<!-- Contratos definidos pela Plan 83-01 que esta plan consome diretamente (executor NÃO precisa re-explorar
     o codebase para estes shapes — já resolvidos). -->

De `web/src/types/processos.ts` (Plan 83-01, Task 1) — tipos a importar nesta plan:
```typescript
export type TipoDecisao = "DESPACHO" | "DECISAO_INTERLOCUTORIA" | "SENTENCA" | "ACORDAO";
export type TipoTestemunha = "AUTOR" | "REU";
export type OrigemProcesso = "PETICAO_INICIAL" | "NOTIFICACOES_AVULSAS";

export interface Decisao { id: number; processoId: string; data: string; tipo: TipoDecisao; resumo?: string; documentoId?: string; }
export interface DecisaoCreateRequest { file?: File; data: string; tipo: TipoDecisao; resumo?: string; }
export interface DecisaoUpdateRequest { data: string; tipo: TipoDecisao; resumo?: string; }

export interface Testemunha { id: number; processoId: string; nome: string; contacto?: string; tipo?: TipoTestemunha; notas?: string; }
export interface TestemunhaCreateRequest { nome: string; tipo?: TipoTestemunha; contacto?: string; notas?: string; }
export interface TestemunhaUpdateRequest { nome: string; tipo?: TipoTestemunha; contacto?: string; notas?: string; }

export interface Facto { id: number; processoId: string; descricao: string; data?: string; ordem: number; }
export interface FactoCreateRequest { descricao: string; data?: string; }
export interface FactoUpdateRequest { descricao: string; data?: string; ordem: number; }

// Processo ganhou juizo?: string; origem?: OrigemProcesso;
// ProcessoCreateRequest ganhou juizo?: string; origem?: OrigemProcesso;
// ProcessoUpdateRequest ganhou APENAS juizo?: string; (sem origem)
```

Padrão exato a replicar 3× para list/create/update (`web/src/hooks/use-processos.ts`, `useProcessoFases`/`useAddProcessoFase`/`useUpdateProcessoFaseStatus`, linhas 258-303):
```typescript
export function useProcessoFases(id: string) {
  const enabled = typeof window !== "undefined" && Boolean(id);
  return useQuery({
    queryKey: ["processos", "fases", id],
    queryFn: () => apiFetch<ProcessoFase[]>(`/processos/${encodeURIComponent(id)}/fases`),
    enabled,
    staleTime: 15_000,
  });
}

export function useAddProcessoFase(id: string) {
  const queryClient = useQueryClient();
  return useMutation({
    mutationFn: (payload: ProcessoFaseCreateRequest) =>
      apiFetch<ProcessoFase>(`/processos/${encodeURIComponent(id)}/fases`, {
        method: "POST",
        body: JSON.stringify(payload satisfies ProcessoFaseCreateRequest),
      }),
    onSuccess: async () => {
      await queryClient.invalidateQueries({ queryKey: ["processos", "fases", id] });
    },
  });
}

export function useUpdateProcessoFaseStatus(id: string) {
  const queryClient = useQueryClient();
  return useMutation({
    mutationFn: (args: { faseId: number; payload: ProcessoFaseUpdateRequest }) =>
      apiFetch<ProcessoFase>(
        `/processos/${encodeURIComponent(id)}/fases/${encodeURIComponent(String(args.faseId))}`,
        { method: "PUT", body: JSON.stringify(args.payload satisfies ProcessoFaseUpdateRequest) },
      ),
    onSuccess: async (updated) => {
      queryClient.setQueryData<ProcessoFase[] | undefined>(["processos", "fases", id], (current) => {
        if (!current) return current;
        return current.map((item) => (item.id === updated.id ? updated : item));
      });
    },
  });
}
```

Padrão exato de delete (`web/src/hooks/use-documentos.ts#useDeleteDocumento`, linhas 74-89):
```typescript
export function useDeleteDocumento(id: string) {
  const queryClient = useQueryClient();
  return useMutation({
    mutationFn: () => apiFetch<void>(`/documentos/${encodeURIComponent(id)}`, { method: "DELETE" }),
    onSuccess: async () => {
      await Promise.all([
        queryClient.invalidateQueries({ queryKey: ["documentos", "list"] }),
        queryClient.removeQueries({ queryKey: ["documentos", "detail", id] }),
      ]);
    },
  });
}
```

Padrão exato de multipart POST (`web/src/hooks/use-documentos.ts#useUploadDocumento`, linhas 49-72) — `apiFetch` já ignora `Content-Type` quando `body instanceof FormData` (`web/src/lib/api.ts` linha 16):
```typescript
export function useUploadDocumento() {
  const queryClient = useQueryClient();
  return useMutation({
    mutationFn: (payload: DocumentoUploadPayload) => {
      const form = new FormData();
      form.set("file", payload.file);
      if (payload.nome?.trim()) form.set("nome", payload.nome.trim());
      return apiFetch<DocumentoUploadResponse>("/documentos/upload", { method: "POST", body: form });
    },
    onSuccess: async () => {
      await queryClient.invalidateQueries({ queryKey: ["documentos", "list"] });
    },
  });
}
```

Bloco atual de `normalizeProcesso`/`toProcessoApiPayload` a editar (`web/src/hooks/use-processos.ts`, linhas 80-115 — funções hoje NÃO exportadas):
```typescript
function normalizeProcesso(api: ProcessoApi): Processo {
  return {
    id: api.id,
    tenant_id: api.tenant_id ?? api.tenantId ?? "",
    // ... (demais campos já existentes, inalterados)
    created_at: api.created_at ?? api.createdAt ?? "",
    updated_at: api.updated_at ?? api.updatedAt,
  };
}

function toProcessoApiPayload(payload: ProcessoCreateRequest | ProcessoUpdateRequest): ProcessoApiPayload {
  return {
    clienteId: payload.cliente_id,
    // ... (demais campos já existentes, inalterados)
    dataFim: payload.data_fim,
  };
}
```

Novo módulo partilhado a criar nesta plan (`web/src/lib/processo-juizo-origem-mapping.ts`) — SEM nenhum import `@/...`, só imports relativos ou nenhum; esta é a forma exata esperada:
```typescript
// Módulo partilhado, sem dependências de path-alias, para o mapeamento juizo/origem.
// Consumido tanto por src/hooks/use-processos.ts (runtime) como por
// scripts/verify-juizo-origem-roundtrip.mjs (prova de round-trip executável) —
// ambos chamam EXATAMENTE as mesmas funções. Ver PITFALLS.md Pitfall 1.
import type { OrigemProcesso } from "../types/processos";

export interface ApiJuizoOrigem {
  juizo?: string;
  origem?: OrigemProcesso;
}

export interface CreateJuizoOrigem {
  juizo?: string;
  origem?: OrigemProcesso;
}

export interface UpdateJuizoOrigem {
  juizo?: string;
}

export function mapJuizoOrigemFromApi(api: ApiJuizoOrigem): { juizo?: string; origem?: OrigemProcesso } {
  return { juizo: api.juizo, origem: api.origem };
}

export function mapJuizoOrigemToPayload(
  payload: CreateJuizoOrigem | UpdateJuizoOrigem,
): { juizo?: string; origem?: OrigemProcesso } {
  return {
    juizo: payload.juizo,
    origem: "origem" in payload ? payload.origem : undefined,
  };
}
```

Cleanup atual de `useDeleteProcesso` a estender (linhas 220-228):
```typescript
onSuccess: async () => {
  await Promise.all([
    queryClient.invalidateQueries({ queryKey: ["processos", "list"] }),
    queryClient.removeQueries({ queryKey: ["processos", "detail", id] }),
    queryClient.removeQueries({ queryKey: ["processos", "partes", id] }),
    queryClient.removeQueries({ queryKey: ["processos", "fases", id] }),
    queryClient.removeQueries({ queryKey: ["processos", "movimentacoes", id] }),
  ]);
},
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Hooks CRUD Decisão/Testemunha/Facto + módulo partilhado de mapeamento juizo/origem</name>
  <files>
    web/src/hooks/use-processos.ts
    web/src/lib/processo-juizo-origem-mapping.ts
  </files>
  <read_first>
    - web/src/hooks/use-processos.ts (ficheiro completo, já lido — 568 linhas)
    - web/src/hooks/use-documentos.ts (padrão multipart/delete, já lido)
    - web/src/lib/api.ts (comportamento de `apiFetch` com `FormData` vs JSON, já lido)
    - Este plan's `<interfaces>` acima (contratos exatos da Plan 83-01 + padrões a replicar + shape exato do novo módulo partilhado)
    - .planning/phases/LEXCV-83-frontend-tipos-schemas-e-hooks/83-PATTERNS.md secções "web/src/hooks/use-processos.ts (hook, CRUD)" e "juizo/origem" (nomes exatos de query key, campos de FormData, comportamento 409 do Facto)
  </read_first>
  <behavior>
    - mapJuizoOrigemFromApi(api) com `api.juizo`/`api.origem` definidos → devolve `{ juizo, origem }` idênticos aos valores de entrada (sem fallback `??`, já que não há ambiguidade snake_case para estes dois campos)
    - mapJuizoOrigemToPayload(CreateJuizoOrigem com juizo+origem definidos) → devolve `{ juizo, origem }` ambos definidos
    - mapJuizoOrigemToPayload(UpdateJuizoOrigem com juizo definido, sem propriedade origem) → devolve `juizo` definido e `origem` estritamente `undefined`
    - normalizeProcesso(api) com `api.juizo`/`api.origem` definidos → `Processo.juizo`/`Processo.origem` idênticos aos valores de entrada, via delegação a mapJuizoOrigemFromApi
    - toProcessoApiPayload(ProcessoCreateRequest com juizo+origem definidos) → payload resultante com `juizo` e `origem` ambos definidos, via delegação a mapJuizoOrigemToPayload
    - toProcessoApiPayload(ProcessoUpdateRequest com juizo definido, sem propriedade origem) → payload resultante com `juizo` definido e `origem` estritamente `undefined`, via delegação a mapJuizoOrigemToPayload
    - useAddDecisao(id).mutate com `{ data, tipo, resumo }` (sem file) → corpo do POST é um `FormData` contendo as chaves `data`/`tipo`/`resumo`, sem chave `file`
    - useUpdateFacto(id).mutate que resulte num 409 do backend → nenhuma lógica de catch/supressão adicional impede o toast automático de `apiFetch` de aparecer
  </behavior>
  <action>
    Criar `web/src/lib/processo-juizo-origem-mapping.ts` PRIMEIRO (antes de editar `use-processos.ts`) — um módulo novo, pequeno, sem dependências e SEM nenhum import com path-alias (`@/...`); usar apenas `import type { OrigemProcesso } from "../types/processos"` (import relativo, type-only — elidido em runtime pelo type-stripping do Node) ou nenhum import. Este módulo é o único local onde a lógica de mapeamento juizo/origem existe — tanto `use-processos.ts` (em runtime) como `web/scripts/verify-juizo-origem-roundtrip.mjs` (Task 2, via `node` puro) vão importar e chamar as MESMAS funções daqui, fechando definitivamente a classe de bug de PITFALLS.md Pitfall 1 (nenhuma cópia hand-maintained em lado nenhum). Copiar exatamente o shape do bloco `<interfaces>` acima ("Novo módulo partilhado a criar nesta plan"): exportar `ApiJuizoOrigem`, `CreateJuizoOrigem`, `UpdateJuizoOrigem`, `mapJuizoOrigemFromApi`, `mapJuizoOrigemToPayload`.

    Depois, em `web/src/hooks/use-processos.ts`:

    1. No bloco de import `import type {...} from "@/types/processos"`, adicionar: `Decisao, DecisaoCreateRequest, DecisaoUpdateRequest, Testemunha, TestemunhaCreateRequest, TestemunhaUpdateRequest, Facto, FactoCreateRequest, FactoUpdateRequest, OrigemProcesso`. Adicionar também `import { mapJuizoOrigemFromApi, mapJuizoOrigemToPayload } from "@/lib/processo-juizo-origem-mapping";` (este ficheiro já usa `@/lib/...` para outros imports — manter a mesma convenção de path-alias aqui).

    2. No tipo local `ProcessoApi`, adicionar `juizo?: string;` e `origem?: OrigemProcesso;` — sem chave snake_case alternativa (per 83-PATTERNS.md, `juizo`/`origem` não têm ambiguidade de case no wire).

    3. No tipo local `ProcessoApiPayload`, adicionar `juizo?: string;` e `origem?: OrigemProcesso;`.

    4. Em `normalizeProcesso`, adicionar `...mapJuizoOrigemFromApi(api),` ao objeto devolvido — NÃO mapear `juizo`/`origem` diretamente dentro de `normalizeProcesso`; delegar sempre ao módulo partilhado criado acima.

    5. Em `toProcessoApiPayload`, adicionar `...mapJuizoOrigemToPayload(payload),` ao objeto devolvido — NÃO reimplementar o narrowing `"origem" in payload` diretamente dentro de `toProcessoApiPayload`; delegar sempre a `mapJuizoOrigemToPayload`, que já aplica esse narrowing e prova em tempo de compilação que chamadas PUT (`ProcessoUpdateRequest`, sem a propriedade `origem`) nunca enviam `origem`, enquanto chamadas POST (`ProcessoCreateRequest`, que tem `origem`) deixam o valor passar quando presente.

    6. Mudar as duas funções de `function normalizeProcesso(...)`/`function toProcessoApiPayload(...)` para `export function normalizeProcesso(...)`/`export function toProcessoApiPayload(...)` — continua necessário para o spec vitest durável da Task 2 (`use-processos.round-trip.test.ts`) importar as funções reais, não uma reimplementação. A prova executável real do round-trip (o script Node da Task 2) importa diretamente `processo-juizo-origem-mapping.ts`, não `use-processos.ts`.

    7. Adicionar os 12 hooks novos, colocados depois da função `useAddProcessoMovimentacao` existente e antes de `useCreateIntake`, seguindo estritamente o padrão estrutural do bloco `<interfaces>` acima:

       **Decisão** (queryKey `["processos", "decisoes", id]`):
       - `useDecisoes(id)`: GET `/processos/${id}/decisoes` → `Decisao[]`, `staleTime: 15_000`, mesmo guard `enabled` que `useProcessoFases`.
       - `useAddDecisao(id)`: `mutationFn(payload: DecisaoCreateRequest)` constrói um `FormData` com `form.set("data", payload.data)`, `form.set("tipo", payload.tipo)`, `if (payload.resumo?.trim()) form.set("resumo", payload.resumo.trim())`, `if (payload.file) form.set("file", payload.file)` — nomes de campo literais `file`/`data`/`tipo`/`resumo` (nomes `@RequestParam` do multipart do backend, não transformados). POST para `/processos/${id}/decisoes` com `body: form` (sem `JSON.stringify`, sem header manual — `apiFetch` já ignora `Content-Type` quando o body é `FormData`). `onSuccess(created)`: sempre `invalidateQueries(["processos", "decisoes", id])`; adicionalmente `invalidateQueries(["documentos", "list"])` quando `created.documentoId` for truthy (o backend criou um novo `Documento` transacionalmente quando um ficheiro foi anexado).
       - `useUpdateDecisao(id)`: `mutationFn(args: { decisaoId: number; payload: DecisaoUpdateRequest })` faz PUT `JSON.stringify(args.payload satisfies DecisaoUpdateRequest)` para `/processos/${id}/decisoes/${args.decisaoId}` → `Decisao`. `onSuccess(updated)`: `setQueryData` em `["processos", "decisoes", id]` substituindo o item cujo `id` coincide, espelhando exatamente `useUpdateProcessoFaseStatus`.
       - `useDeleteDecisao(id)`: `mutationFn(decisaoId: number)` faz DELETE `/processos/${id}/decisoes/${decisaoId}`. `onSuccess`: `invalidateQueries(["processos", "decisoes", id])` E `invalidateQueries(["documentos", "list"])` incondicionalmente (o backend faz cascade-delete do `Documento` ligado sempre que `documentoId` estava definido).

       **Testemunha** (queryKey `["processos", "testemunhas", id]`):
       - `useTestemunhas(id)`: GET `/processos/${id}/testemunhas` → `Testemunha[]`.
       - `useAddTestemunha(id)`: `mutationFn(payload: TestemunhaCreateRequest)` POST `JSON.stringify(payload satisfies TestemunhaCreateRequest)` → `Testemunha`. `onSuccess`: `invalidateQueries(["processos", "testemunhas", id])`.
       - `useUpdateTestemunha(id)`: `mutationFn(args: { testemunhaId: number; payload: TestemunhaUpdateRequest })` PUT `JSON.stringify(args.payload satisfies TestemunhaUpdateRequest)` → `Testemunha`. `onSuccess`: `setQueryData` substituindo o item por `id` (mesmo padrão de `useUpdateDecisao`).
       - `useDeleteTestemunha(id)`: `mutationFn(testemunhaId: number)` DELETE `/processos/${id}/testemunhas/${testemunhaId}`. `onSuccess`: `invalidateQueries(["processos", "testemunhas", id])`.

       **Facto** (queryKey `["processos", "factos", id]`):
       - `useFactos(id)`: GET `/processos/${id}/factos` → `Facto[]` (já vem ordenado por `ordem` ascendente do backend, sem necessidade de sort no cliente).
       - `useAddFacto(id)`: `mutationFn(payload: FactoCreateRequest)` POST `JSON.stringify(payload satisfies FactoCreateRequest)` → `Facto` (`ordem` é calculado no servidor; o tipo `FactoCreateRequest` já não tem essa propriedade). `onSuccess`: `invalidateQueries(["processos", "factos", id])`.
       - `useUpdateFacto(id)`: `mutationFn(args: { factoId: number; payload: FactoUpdateRequest })` PUT `JSON.stringify(args.payload satisfies FactoUpdateRequest)` → `Facto`. `onSuccess`: `setQueryData` substituindo por `id`. NÃO adicionar nenhum catch/try especial para o 409 de conflito de `ordem` — o comportamento por omissão de `apiFetch` (linha 43 de `web/src/lib/api.ts`) já mostra um toast para qualquer status não-401/403, incluindo o 409 "Conflito ao atribuir ordem"; adicionar um catch aqui só arriscaria suprimir esse comportamento já existente.
       - `useDeleteFacto(id)`: `mutationFn(factoId: number)` DELETE `/processos/${id}/factos/${factoId}`. `onSuccess`: `invalidateQueries(["processos", "factos", id])`.

    8. No `onSuccess` existente de `useDeleteProcesso` (dentro do array `Promise.all([...])`), adicionar três novas chamadas `queryClient.removeQueries({ queryKey: [...] })` para `["processos", "decisoes", id]`, `["processos", "testemunhas", id]`, `["processos", "factos", id]` — mesmo padrão já aplicado a partes/fases/movimentacoes.
  </action>
  <acceptance_criteria>
    - `web/src/lib/processo-juizo-origem-mapping.ts` existe, contém `export function mapJuizoOrigemFromApi` e `export function mapJuizoOrigemToPayload`, e NÃO contém a substring `"@/` (confirma zero imports de path-alias)
    - `grep -c "mapJuizoOrigemFromApi\|mapJuizoOrigemToPayload" web/src/hooks/use-processos.ts` retorna >= 3 (1 import + 1 chamada em normalizeProcesso + 1 chamada em toProcessoApiPayload)
    - `grep -c "export function normalizeProcesso\|export function toProcessoApiPayload" web/src/hooks/use-processos.ts` retorna 2
    - `grep -c "export function useDecisoes\|export function useAddDecisao\|export function useUpdateDecisao\|export function useDeleteDecisao\|export function useTestemunhas\|export function useAddTestemunha\|export function useUpdateTestemunha\|export function useDeleteTestemunha\|export function useFactos\|export function useAddFacto\|export function useUpdateFacto\|export function useDeleteFacto" web/src/hooks/use-processos.ts` retorna 12
    - `grep -c '\["processos", "decisoes", id\]\|\["processos", "testemunhas", id\]\|\["processos", "factos", id\]' web/src/hooks/use-processos.ts` é >= 3 (aparecem em múltiplos hooks cada)
    - `awk '/export function useAddDecisao/,/^}/' web/src/hooks/use-processos.ts | grep -c 'form.set("data"\|form.set("tipo"\|form.set("file"'` retorna 3
    - `awk '/export function useDeleteProcesso/,/^}/' web/src/hooks/use-processos.ts | grep -c '"decisoes"\|"testemunhas"\|"factos"'` retorna 3
    - `cd web && npx tsc --noEmit 2>&1 | grep -v "cliente-documento-tipo.test.ts\|clientes.legacy-documento-tipo.test.ts\|use-processos.round-trip.test.ts"` não produz nenhuma linha de erro nova
  </acceptance_criteria>
  <verify>
    <automated>cd web && npx tsc --noEmit 2>&1 | grep -v "cliente-documento-tipo.test.ts\|clientes.legacy-documento-tipo.test.ts\|use-processos.round-trip.test.ts"</automated>
  </verify>
  <done>web/src/lib/processo-juizo-origem-mapping.ts existe (sem imports @/...) e exporta mapJuizoOrigemFromApi/mapJuizoOrigemToPayload; use-processos.ts exporta normalizeProcesso/toProcessoApiPayload, que delegam o mapeamento juizo/origem a esse módulo partilhado, e ganha os 12 hooks novos (quarteto × 3 entidades), seguindo exatamente a convenção de queryKey e estrutura já usada por fases/partes/documentos; useDeleteProcesso limpa as 3 novas caches.</done>
</task>

<task type="auto">
  <name>Task 2: Verificação de round-trip juizo/origem (spec vitest durável + script Node executável que importa o módulo partilhado)</name>
  <files>
    web/src/hooks/use-processos.round-trip.test.ts
    web/scripts/verify-juizo-origem-roundtrip.mjs
  </files>
  <read_first>
    - web/src/hooks/use-processos.ts (após a Task 1 desta plan — `normalizeProcesso`/`toProcessoApiPayload` já exportados e a delegar juizo/origem a `processo-juizo-origem-mapping.ts`)
    - web/src/lib/processo-juizo-origem-mapping.ts (após a Task 1 desta plan — módulo partilhado sem imports `@/...`, com `mapJuizoOrigemFromApi`/`mapJuizoOrigemToPayload`; é este ficheiro que o script Node desta task importa diretamente)
    - .planning/research/PITFALLS.md Pitfall 1 (o bug exato de mapeamento que esta verificação deve provar que não acontece)
    - .planning/phases/LEXCV-83-frontend-tipos-schemas-e-hooks/83-CONTEXT.md (decisão explícita: "critério de aceitação... deve incluir um teste de round-trip... não apenas tsc/build limpo")
    - .planning/milestones/v2.8-phases/LEXCV-74-enum-documento-tipo-bi-nif-restri-o-por-tipo/74-02-SUMMARY.md secção "Deviations from Plan" (precedente estabelecido: sem test runner instalado neste repo — `web/package.json` não tem `vitest`/`jest`; confirmado ainda válido em `82-VERIFICATION.md`, linha `Cannot find module 'vitest'` como erro pré-existente aceite)
  </read_first>
  <action>
    Criar `web/src/hooks/use-processos.round-trip.test.ts` usando sintaxe `describe`/`it`/`expect` do vitest (este ficheiro não vai executar nesta repo hoje — não há test runner instalado, confirmado ainda verdadeiro na verificação da Phase 82; o ficheiro existe como o spec de comportamento durável e preciso para quando um runner for eventualmente adicionado, seguindo exatamente o precedente já estabelecido por `web/src/lib/cliente-documento-tipo.test.ts` na Phase 74). Importar `normalizeProcesso`, `toProcessoApiPayload` de `"./use-processos"` — têm de ser as funções reais exportadas na Task 1, não reimplementações. Cobrir no mínimo:
    (a) `normalizeProcesso` mapeia `juizo`/`origem` de um objeto simulado com shape `ProcessoApi` (resposta camelCase de entidade, ex.: `{ id, tenantId, clienteId, juizo: "1º Juízo Cível", origem: "PETICAO_INICIAL", createdAt }`) para os campos `juizo`/`origem` do `Processo` devolvido, inalterados;
    (b) `toProcessoApiPayload` sobre um `ProcessoCreateRequest` com `juizo` e `origem` definidos produz um payload com ambos definidos com os mesmos valores;
    (c) `toProcessoApiPayload` sobre um `ProcessoUpdateRequest` com `juizo` definido (sem propriedade `origem`, per Plan 83-01) produz um payload com `juizo` definido e `origem` estritamente `undefined`;
    (d) um round-trip completo: `normalizeProcesso` sobre uma resposta simulada com `juizo`/`origem` definidos → usar os valores do `Processo` resultante para construir um `ProcessoCreateRequest` → `toProcessoApiPayload` → confirmar que ambos os valores sobrevivem inalterados de ponta a ponta.

    Como o vitest não pode executar nesta repo, criar TAMBÉM `web/scripts/verify-juizo-origem-roundtrip.mjs`: um script Node ESM sem dependências que reverifica de forma independente os mesmos 4 casos acima, mas desta vez importando DIRETAMENTE o módulo partilhado criado na Task 1 — `import { mapJuizoOrigemFromApi, mapJuizoOrigemToPayload } from "../src/lib/processo-juizo-origem-mapping.ts";` (import relativo com extensão `.ts` explícita; Node ≥22 faz type-stripping nativo de `.ts` sem flags nem dependências, confirmado empiricamente nesta máquina em Node v24.15.0 — o único obstáculo real era o path-alias `@/...`, já eliminado porque o módulo partilhado não usa nenhum). NÃO reimplementar nem copiar as linhas de `juizo`/`origem` dentro do script — chamar as funções importadas diretamente sobre os mesmos 4 casos de teste. Usar apenas `node:assert` (nativo) para as asserções. Adicionar um comentário no topo do script explicitando que este é o MESMO módulo usado por `use-processos.ts` em runtime — não uma duplicação — fechando definitivamente PITFALLS.md Pitfall 1 (se o mapeamento real quebrar, este script quebra também, porque é literalmente o mesmo código). O script deve imprimir `PASS` e terminar com código de saída 0 em caso de sucesso, e lançar um `AssertionError` não apanhado (código de saída não-zero) em caso de falha.

    Executar `node web/scripts/verify-juizo-origem-roundtrip.mjs` e confirmar que imprime `PASS` com código de saída 0 — esta é a prova automatizada real exigida pelo critério de aceitação de `83-CONTEXT.md` ("não apenas por build limpo"), e agora exerce genuinamente o código de produção partilhado, não uma cópia hand-maintained.
  </action>
  <acceptance_criteria>
    - `web/src/hooks/use-processos.round-trip.test.ts` existe, contém `describe(`, `it(`, e importa `normalizeProcesso`/`toProcessoApiPayload` de `"./use-processos"`
    - `web/scripts/verify-juizo-origem-roundtrip.mjs` existe e contém `from "../src/lib/processo-juizo-origem-mapping.ts"` (import direto do módulo partilhado, não uma cópia)
    - `grep -c "function mapJuizoOrigemFromApi\|function mapJuizoOrigemToPayload" web/scripts/verify-juizo-origem-roundtrip.mjs` retorna 0 (confirma que o script NÃO reimplementa as funções — só as importa e chama)
    - `node web/scripts/verify-juizo-origem-roundtrip.mjs` termina com código de saída 0 e a saída contém a string `PASS`
    - `cd web && npx tsc --noEmit 2>&1 | grep -v "cliente-documento-tipo.test.ts\|clientes.legacy-documento-tipo.test.ts\|use-processos.round-trip.test.ts"` não produz nenhuma linha de erro nova (o novo `.test.ts` desta task falha a resolver `vitest` exatamente como os 2 ficheiros de teste pré-existentes — comportamento esperado e já documentado, não uma regressão)
  </acceptance_criteria>
  <verify>
    <automated>node web/scripts/verify-juizo-origem-roundtrip.mjs</automated>
  </verify>
  <done>Um teste de round-trip real e executável (script Node) importa diretamente o módulo partilhado `processo-juizo-origem-mapping.ts` — o MESMO código usado por use-processos.ts em runtime — e confirma que juizo/origem sobrevivem ao ciclo mapJuizoOrigemFromApi/mapJuizoOrigemToPayload em ambas as direções (create com origem, update sem origem); o spec vitest correspondente existe como contrato durável para quando um test runner for instalado.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Browser (hooks React Query, consumidos por formulários da Phase 84) → Next.js proxy → API Spring Boot já hardened (Phase 81 RBAC + tenant/ownership checks) | Corpo de request (JSON ou multipart) construído a partir de tipos definidos na Plan 83-01, nunca objetos livres |
| `useAddDecisao` → `POST /processos/{id}/decisoes` (multipart) | Único hook novo desta fase que envia um ficheiro binário — o `file` é opcional e só é incluído no `FormData` quando presente, nunca reconstruído a partir de outra fonte |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-83-03 | Tampering | `useAddDecisao` FormData | mitigate | Campos do `FormData` construídos individualmente a partir de `DecisaoCreateRequest` tipado (`data`/`tipo`/`resumo`/`file`), nunca por spread de um objeto livre — impede que uma chave extra inesperada seja enviada; o backend ignora chaves desconhecidas via `@RequestParam` nomeado, mas o tipo TS já impede a tentativa do lado do cliente |
| T-83-04 | Repudiation | `useUpdateFacto` — 409 de conflito de `ordem` | mitigate | Nenhuma lógica de catch/supressão à volta desta mutation — o toast automático de `apiFetch` (não-401/403) garante que o utilizador vê sempre o 409, nunca fica a pensar que uma reordenação silenciosamente falhou |
| T-83-05 | Tampering | `toProcessoApiPayload` — exclusão de `origem` em updates | mitigate | O narrowing `"origem" in payload` sobre um tipo `ProcessoUpdateRequest` que não tem essa propriedade, agora centralizado em `mapJuizoOrigemToPayload`, prova em tempo de compilação que nenhuma chamada PUT desta fase pode enviar `origem` — fecha o vetor descrito em PITFALLS.md Pitfall 2 do lado do cliente (o backend já ignora `origem` em `updateProcesso`, per Phase 81, mas esta camada garante que o cliente nem tenta) |
| T-83-SC | Supply chain | npm/pnpm installs | mitigate | Zero novos pacotes instalados nesta plan — a verificação de round-trip usa apenas `node:assert` (nativo) em vez de instalar `vitest`, seguindo o precedente já aceite da Phase 74 (`74-02-SUMMARY.md`); confirmado por ausência de diff em `package.json`/`pnpm-lock.yaml` |
| T-83-06 | Information Disclosure | `useTestemunhas` — `contacto`/`notas` (PII leve) | accept | Sem exposição nova: `GET /processos/{id}/testemunhas` já exige `processos:view` (`@PreAuthorize`, Phase 81), inalterado por esta fase; esta plan só tipa e consome um endpoint já protegido |
| T-83-07 | Tampering | `verify-juizo-origem-roundtrip.mjs` — risco de divergência silenciosa (PITFALLS.md Pitfall 1) | mitigate | O script importa diretamente `web/src/lib/processo-juizo-origem-mapping.ts` (mesmo módulo usado em runtime por `use-processos.ts`), em vez de reimplementar as linhas de mapeamento — uma regressão real em `juizo`/`origem` faz o script falhar (exit != 0), não apenas a cópia divergir silenciosamente sem detetar o problema |

Nenhuma ameaça neste registo é alta severidade — esta plan não introduz novos endpoints nem lógica de autorização; consome exclusivamente endpoints já protegidos pela Phase 81.
</threat_model>

<verification>
1. `cd web && npx tsc --noEmit` não produz nenhum erro novo além do baseline pré-existente (2 erros `vitest` de Phase 74 + 1 erro `vitest` novo, esperado, do `.test.ts` criado na Task 2 desta plan).
2. `node web/scripts/verify-juizo-origem-roundtrip.mjs` termina com código de saída 0 e imprime `PASS`, importando diretamente `web/src/lib/processo-juizo-origem-mapping.ts` (mesmo código usado em runtime por `use-processos.ts` — sem reimplementação).
3. `web/src/hooks/use-processos.ts` exporta `normalizeProcesso`/`toProcessoApiPayload` (delegando juizo/origem ao módulo partilhado) e os 12 hooks novos (list/create/update/delete × Decisão/Testemunha/Facto).
</verification>

<success_criteria>
`use-processos.ts` ganha o quarteto de hooks (list/create/update/delete) para cada entidade nova, seguindo a convenção `queryKey` já usada (ROADMAP Phase 83 success criterion 3). `normalizeProcesso()`/`toProcessoApiPayload()` mapeiam `juizo`/`origem` corretamente, delegando a um módulo partilhado — verificado por um teste de round-trip real e executável que importa esse MESMO módulo, não apenas por build limpo (ROADMAP Phase 83 success criterion 4, PITFALLS.md Pitfall 1, 4ª recorrência prevenida).
</success_criteria>

<output>
Create `.planning/phases/LEXCV-83-frontend-tipos-schemas-e-hooks/83-02-SUMMARY.md` when done.
</output>
</output>
