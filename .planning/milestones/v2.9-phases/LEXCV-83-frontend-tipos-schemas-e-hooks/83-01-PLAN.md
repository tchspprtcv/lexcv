---
phase: "83"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/types/processos.ts
  - web/src/schemas/processos.ts
  - web/src/lib/tipo-decisao.ts
  - web/src/lib/tipo-testemunha.ts
  - web/src/lib/origem-processo.ts
autonomous: true
requirements: []  # Integration phase — supports PROC-01 to PROC-14 (ROADMAP Phase 83 goal); owns no dedicated requirement ID per ROADMAP/REQUIREMENTS.md traceability table

must_haves:
  truths:
    - "types/processos.ts exporta Decisao, Facto, Testemunha, TipoDecisao, TipoTestemunha, OrigemProcesso com o shape exato devolvido pelos 12 endpoints da Phase 81"
    - "Processo (e ProcessoCreateRequest) ganham juizo?/origem? tipados; ProcessoUpdateRequest ganha juizo mas explicitamente NÃO origem (imutável após intake)"
    - "schemas/processos.ts exporta decisaoFormSchema, factoFormSchema, testemunhaFormSchema e um schema de intake dedicado onde origem é um z.enum obrigatório, não optionalTrimmedString"
    - "Labels em português para TipoDecisao/TipoTestemunha/OrigemProcesso existem numa camada de apresentação separada dos valores wire (ASCII, estáveis)"
  artifacts:
    - path: "web/src/types/processos.ts"
      provides: "Decisao/DecisaoCreateRequest/DecisaoUpdateRequest, Testemunha/*, Facto/*, TipoDecisao, TipoTestemunha, OrigemProcesso, Processo.juizo/origem"
      contains: "export interface Decisao"
    - path: "web/src/schemas/processos.ts"
      provides: "decisaoFormSchema, factoFormSchema, testemunhaFormSchema, origemProcessoSchema, processoIntakeFormSchema"
      contains: "export const decisaoFormSchema"
    - path: "web/src/lib/tipo-decisao.ts"
      provides: "tipoDecisaoToLabel(tipo) — PT labels for TipoDecisao"
    - path: "web/src/lib/tipo-testemunha.ts"
      provides: "tipoTestemunhaToLabel(tipo) — PT labels for TipoTestemunha"
    - path: "web/src/lib/origem-processo.ts"
      provides: "origemProcessoToLabel(origem) — PT labels for OrigemProcesso"
  key_links:
    - from: "web/src/schemas/processos.ts (processoIntakeFormSchema)"
      to: "web/src/schemas/processos.ts (origemProcessoSchema)"
      via: "processoFormSchema.extend({ origem: origemProcessoSchema })"
      pattern: "processoIntakeFormSchema\\s*=\\s*processoFormSchema\\.extend"
    - from: "web/src/lib/tipo-decisao.ts"
      to: "web/src/types/processos.ts (TipoDecisao)"
      via: "import type"
      pattern: "import type \\{ TipoDecisao \\} from \"@/types/processos\""
---

<objective>
Adicionar os contratos de dados (tipos TypeScript + schemas Zod + label maps de apresentação) para `juizo`/`origem` em `Processo` e para as três entidades novas — Decisão, Facto, Testemunha — construídas na Phase 80/81, usando o shape exato de request/response documentado em `83-PATTERNS.md` (extraído diretamente do `ResourceController.java` vivo, não da investigação de arquitetura de alto nível).

Purpose: Esta é a camada de contratos ("blueprints") que a Plan 83-02 (hooks) e a Phase 84 (UI) vão consumir sem precisar explorar o backend por conta própria. `83-PATTERNS.md` já identificou que os tipos `ProcessoParte`/`ProcessoFase` existentes são exemplos vivos do bug de mapeamento que esta fase deve evitar (campos snake_case que nunca existiram no wire) — os tipos novos desta plan devem seguir o shape real (camelCase puro, sem ambiguidade) confirmado por essa investigação, não o padrão desatualizado dos tipos vizinhos.

Output: `web/src/types/processos.ts` com os novos tipos/interfaces; `web/src/schemas/processos.ts` com os novos schemas Zod (incluindo `origem` promovido a `z.enum` obrigatório apenas no fluxo de intake); três novos ficheiros `web/src/lib/{tipo-decisao,tipo-testemunha,origem-processo}.ts` com mapas de label PT, seguindo o padrão já estabelecido em `web/src/lib/conflict-check.ts`.
</objective>

<execution_context>
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/LEXCV-83-frontend-tipos-schemas-e-hooks/83-CONTEXT.md
@.planning/phases/LEXCV-83-frontend-tipos-schemas-e-hooks/83-PATTERNS.md
@.planning/research/PITFALLS.md
</context>

<interfaces>
<!-- Exact current source of the two files this plan edits, verified 2026-07-07. Executor should edit these
     directly — no further codebase exploration needed for field shapes (83-PATTERNS.md already did that
     extraction against the live backend). -->

Current `web/src/types/processos.ts` (relevant excerpt, lines 9-110 — `Processo`/`ProcessoCreateRequest`/`ProcessoUpdateRequest`/`ProcessoParte`/`ProcessoFase`/`ProcessoMovimentacao`):
```typescript
export interface Processo {
  id: string;
  tenant_id: string;
  cliente_id: string;
  numero?: string;
  titulo?: string;
  tipo_processo?: string;
  area_juridica?: string;
  tribunal?: string;
  descricao?: string;
  estado?: string;
  data_inicio?: string;
  data_fim?: string;
  responsavel_id?: string;
  responsavel_nome?: string;
  risco_mais_critico?: PrazoRisco;
  tem_prazo_escalonado?: boolean;
  legal_hold?: boolean;
  data_retencao?: string;
  created_at: string;
  updated_at?: string;
}

export interface ProcessoCreateRequest {
  cliente_id: string;
  numero?: string;
  titulo?: string;
  tipo_processo?: string;
  area_juridica?: string;
  tribunal?: string;
  descricao?: string;
  estado?: string;
  data_inicio?: string;
  data_fim?: string;
}

export interface ProcessoUpdateRequest {
  cliente_id?: string;
  numero?: string;
  titulo?: string;
  tipo_processo?: string;
  area_juridica?: string;
  tribunal?: string;
  descricao?: string;
  estado?: string;
  data_inicio?: string;
  data_fim?: string;
  legal_hold?: boolean;
  data_retencao?: string;
}
```
(`ProcessoMovimentacao` block ends around line 110 — new types for this plan go immediately after it, before the `ConflictNivel` section.)

Current `web/src/schemas/processos.ts` (full file, 99 lines — already read in full; key blocks to extend: `processoFormSchema` lines 9-22, `processoFaseStatusSchema`/`conflictNivelEnum` enum pattern lines 34/50-55, `processoParteFormSchema` lines 26-30 as the simple-required-field pattern).

`web/src/lib/conflict-check.ts` (full file, 32 lines) — the exact single-flat-`Record`-plus-function label-map pattern to replicate 3×:
```typescript
export function conflictNivelToLabel(nivel: ConflictNivel): string {
  const map: Record<ConflictNivel, string> = {
    sem_conflito: "SEM CONFLITO",
    potencial: "POTENCIAL",
    sanavel: "SANÁVEL",
    impeditivo: "IMPEDITIVO",
  };
  return map[nivel] ?? "POTENCIAL";
}
```

`web/src/schemas/documentos.ts` (full file, 31 lines) — the `fileListSchema` custom-validator pattern to replicate (optional variant, no `.refine(length === 1)`):
```typescript
const fileListSchema = z.custom<FileList>((value) => value instanceof FileList, {
  message: "O ficheiro é obrigatório",
});

export const documentoUploadFormSchema = z.object({
  file: fileListSchema.refine((files) => files.length === 1, "O ficheiro é obrigatório"),
  // ...
});
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Tipos TypeScript — Processo.juizo/origem + Decisao/Facto/Testemunha</name>
  <files>
    web/src/types/processos.ts
  </files>
  <read_first>
    - web/src/types/processos.ts (ficheiro completo, já lido — 217 linhas)
    - .planning/phases/LEXCV-83-frontend-tipos-schemas-e-hooks/83-PATTERNS.md secções "1. Decisao", "2. Testemunha", "3. Facto", "4. juizo/origem on Processo" (shape exato campo-a-campo, incluindo os "Suggested TS type" blocks)
    - .planning/research/PITFALLS.md Pitfall 1 (razão pela qual `tenant_id`/`created_at`/snake_case NÃO devem ser copiados dos tipos `ProcessoParte`/`ProcessoFase` vizinhos)
  </read_first>
  <action>
    Em `web/src/types/processos.ts`:

    1. Adicionar três novos type aliases exportados: `TipoDecisao` = união `"DESPACHO" | "DECISAO_INTERLOCUTORIA" | "SENTENCA" | "ACORDAO"`; `TipoTestemunha` = `"AUTOR" | "REU"`; `OrigemProcesso` = `"PETICAO_INICIAL" | "NOTIFICACOES_AVULSAS"`.

    2. No `Processo` existente, adicionar `juizo?: string;` e `origem?: OrigemProcesso;` como novos campos opcionais, sem remover/renomear nenhum campo existente.

    3. No `ProcessoCreateRequest` existente, adicionar `juizo?: string;` e `origem?: OrigemProcesso;` (ambos opcionais ao nível de tipo — a obrigatoriedade de `origem` no fluxo de intake é imposta pelo schema Zod na Task 2, não por esta interface TS).

    4. No `ProcessoUpdateRequest` existente, adicionar APENAS `juizo?: string;` — NÃO adicionar `origem` a este tipo. Esta omissão é deliberada: `origem` é imutável após o intake (o backend `updateProcesso` ignora silenciosamente qualquer `origem` no payload, per 83-PATTERNS.md secção 4), e a ausência da propriedade no tipo é o que vai permitir à Plan 83-02 usar o operador `in` do TypeScript para provar em tempo de compilação que chamadas PUT nunca enviam `origem`.

    5. Imediatamente a seguir ao bloco `ProcessoMovimentacao`/`ProcessoMovimentacaoCreateRequest` existente (antes da secção `ConflictNivel`), adicionar os três grupos de interfaces seguintes, copiando os campos exatamente como estão nos "Suggested TS type" de `83-PATTERNS.md`:

       - `Decisao { id: number; processoId: string; data: string; tipo: TipoDecisao; resumo?: string; documentoId?: string; }`
       - `DecisaoCreateRequest { file?: File; data: string; tipo: TipoDecisao; resumo?: string; }`
       - `DecisaoUpdateRequest { data: string; tipo: TipoDecisao; resumo?: string; }` — NÃO incluir `documentoId` nem `file` aqui: o handler PUT do backend lê um `Map<String,Object>` que só reconhece as chaves `data`/`tipo`/`resumo` (fix WR-03 da segunda revisão da Phase 81); o anexo só pode ser definido na criação.
       - `Testemunha { id: number; processoId: string; nome: string; contacto?: string; tipo?: TipoTestemunha; notas?: string; }`
       - `TestemunhaCreateRequest { nome: string; tipo?: TipoTestemunha; contacto?: string; notas?: string; }`
       - `TestemunhaUpdateRequest { nome: string; tipo?: TipoTestemunha; contacto?: string; notas?: string; }` (shape idêntico ao create — o backend usa o mesmo `Map<String,Object>` com a mesma validação em ambos os verbos)
       - `Facto { id: number; processoId: string; descricao: string; data?: string; ordem: number; }`
       - `FactoCreateRequest { descricao: string; data?: string; }` — `ordem` intencionalmente omitido: é calculado no servidor na criação, qualquer valor enviado pelo cliente é ignorado.
       - `FactoUpdateRequest { descricao: string; data?: string; ordem: number; }` — `ordem` É obrigatório aqui, ao contrário do create; o handler PUT do backend devolve 400 se `payload.getOrdem()` for null.
  </action>
  <acceptance_criteria>
    - `grep -c "export interface Decisao\b\|export interface Facto\b\|export interface Testemunha\b" web/src/types/processos.ts` retorna 3
    - `grep -c "export type TipoDecisao\|export type TipoTestemunha\|export type OrigemProcesso" web/src/types/processos.ts` retorna 3
    - `awk '/export interface Processo \{/,/^}/' web/src/types/processos.ts` contém `juizo?:` e `origem?:`
    - `awk '/export interface ProcessoUpdateRequest \{/,/^}/' web/src/types/processos.ts` contém `juizo?:` e NÃO contém a string `origem` (confirma a omissão deliberada — `grep -c "origem" ` nesse bloco isolado retorna 0)
    - `awk '/export interface DecisaoUpdateRequest \{/,/^}/' web/src/types/processos.ts | grep -c "documentoId\|file"` retorna 0
    - `cd web && npx tsc --noEmit 2>&1 | grep -v "cliente-documento-tipo.test.ts\|clientes.legacy-documento-tipo.test.ts"` não produz nenhuma linha de erro nova (baseline pré-existente: só os 2 erros `Cannot find module 'vitest'` nesses dois ficheiros de teste, confirmados como pré-existentes em `82-VERIFICATION.md`)
  </acceptance_criteria>
  <verify>
    <automated>cd web && npx tsc --noEmit 2>&1 | grep -v "cliente-documento-tipo.test.ts\|clientes.legacy-documento-tipo.test.ts"</automated>
  </verify>
  <done>web/src/types/processos.ts exporta Decisao/DecisaoCreateRequest/DecisaoUpdateRequest, Testemunha/TestemunhaCreateRequest/TestemunhaUpdateRequest, Facto/FactoCreateRequest/FactoUpdateRequest, TipoDecisao, TipoTestemunha, OrigemProcesso; Processo e ProcessoCreateRequest ganham juizo?/origem?; ProcessoUpdateRequest ganha apenas juizo?; tsc --noEmit não introduz nenhum erro novo.</done>
</task>

<task type="auto">
  <name>Task 2: Schemas Zod (decisaoFormSchema/factoFormSchema/testemunhaFormSchema/origem obrigatório) + label maps PT</name>
  <files>
    web/src/schemas/processos.ts
    web/src/lib/tipo-decisao.ts
    web/src/lib/tipo-testemunha.ts
    web/src/lib/origem-processo.ts
  </files>
  <read_first>
    - web/src/schemas/processos.ts (ficheiro completo, já lido — 99 linhas)
    - web/src/schemas/documentos.ts (padrão `fileListSchema` — ficheiro completo, já lido)
    - web/src/lib/conflict-check.ts (padrão de label map de enum plano — ficheiro completo, já lido)
    - web/src/types/processos.ts (após Task 1 — TipoDecisao/TipoTestemunha/OrigemProcesso já devem existir)
    - .planning/phases/LEXCV-83-frontend-tipos-schemas-e-hooks/83-CONTEXT.md secção "Claude's Discretion" (labels PT explícitos para origem; padrão enum-com-label)
    - .planning/phases/LEXCV-83-frontend-tipos-schemas-e-hooks/83-PATTERNS.md secção "Enum-with-label pattern" (labels sugeridos para TipoDecisao/TipoTestemunha)
  </read_first>
  <action>
    Em `web/src/schemas/processos.ts`:

    1. Adicionar três novos schemas de enum exportados, próximo do bloco existente `processoFaseStatusSchema`/`conflictNivelEnum`: `tipoDecisaoSchema = z.enum(["DESPACHO", "DECISAO_INTERLOCUTORIA", "SENTENCA", "ACORDAO"])`; `tipoTestemunhaSchema = z.enum(["AUTOR", "REU"])`; `origemProcessoSchema = z.enum(["PETICAO_INICIAL", "NOTIFICACOES_AVULSAS"])`. Apenas os valores ASCII de wire — sem labels acentuados dentro do schema.

    2. Dentro do `processoFormSchema` existente, adicionar o campo `juizo: optionalTrimmedString,` (juizo é editável tanto na criação como na edição, per backend). NÃO adicionar `origem` a este schema.

    3. Imediatamente a seguir a `processoFormSchema`/`ProcessoFormValues`, adicionar `export const processoIntakeFormSchema = processoFormSchema.extend({ origem: origemProcessoSchema });` e `export type ProcessoIntakeFormValues = z.infer<typeof processoIntakeFormSchema>;` — este é o schema que o passo 1 do wizard de intake (Phase 84) deve usar em vez do `processoFormSchema` nu, tornando `origem` um enum obrigatório apenas no intake (PROC-03/PROC-04), sem quebrar o formulário de edição (que continua a usar `processoFormSchema`, onde `origem` não é um campo — consistente com o backend, que ignora `origem` em `updateProcesso`).

    4. Adicionar um `fileListSchema` local (idêntico ao de `web/src/schemas/documentos.ts` — `z.custom<FileList>((value) => value instanceof FileList, { message: "O ficheiro é obrigatório" })`), seguindo o padrão já usado neste projeto de cada ficheiro `schemas/*.ts` ser autocontido (não importar entre ficheiros de schema).

    5. Adicionar `export const decisaoFormSchema = z.object({ data: z.string().trim().min(1, "A data é obrigatória"), tipo: tipoDecisaoSchema, resumo: optionalTrimmedString, file: fileListSchema.optional() })` e `export type DecisaoFormValues = z.infer<typeof decisaoFormSchema>;` — ao contrário de `documentoUploadFormSchema`, `file` aqui é opcional (sem `.refine(length === 1)`), porque o anexo da Decisão é opcional per 83-PATTERNS.md.

    6. Adicionar `export const testemunhaFormSchema = z.object({ nome: z.string().trim().min(1, "O nome é obrigatório"), tipo: tipoTestemunhaSchema.optional(), contacto: optionalTrimmedString, notas: optionalTrimmedString })` e `export type TestemunhaFormValues = z.infer<typeof testemunhaFormSchema>;`.

    7. Adicionar `export const factoFormSchema = z.object({ descricao: z.string().trim().min(1, "A descrição é obrigatória"), data: optionalTrimmedString })` e `export type FactoFormValues = z.infer<typeof factoFormSchema>;` — `ordem` fica deliberadamente fora deste schema (é calculado no servidor na criação; qualquer UI de reordenação na Phase 84 vai chamar o hook de update diretamente com um `ordem` já calculado, sem passar por este schema de formulário).

    Depois, criar três novos ficheiros em `web/src/lib/`, cada um seguindo exatamente o padrão de função única + `Record` plano já usado em `conflict-check.ts#conflictNivelToLabel` (não o padrão multi-tipo de `cliente-documento-tipo.ts`, já que estes três enums são planos, sem filtragem por outro campo):

    - `web/src/lib/tipo-decisao.ts`: `import type { TipoDecisao } from "@/types/processos";` e `export function tipoDecisaoToLabel(tipo: TipoDecisao): string` com um `Record<TipoDecisao, string>` mapeando `DESPACHO`→"Despacho", `DECISAO_INTERLOCUTORIA`→"Decisão Interlocutória", `SENTENCA`→"Sentença", `ACORDAO`→"Acórdão".
    - `web/src/lib/tipo-testemunha.ts`: `import type { TipoTestemunha } from "@/types/processos";` e `export function tipoTestemunhaToLabel(tipo: TipoTestemunha): string` com `AUTOR`→"Autor", `REU`→"Réu".
    - `web/src/lib/origem-processo.ts`: `import type { OrigemProcesso } from "@/types/processos";` e `export function origemProcessoToLabel(origem: OrigemProcesso): string` com `PETICAO_INICIAL`→"Petição Inicial", `NOTIFICACOES_AVULSAS`→"Notificações Avulsas" (labels citados textualmente em 83-CONTEXT.md — devem corresponder exatamente para a Phase 84).
  </action>
  <acceptance_criteria>
    - `grep -c "export const decisaoFormSchema\|export const factoFormSchema\|export const testemunhaFormSchema" web/src/schemas/processos.ts` retorna 3
    - `grep -n "export const origemProcessoSchema = z.enum(" web/src/schemas/processos.ts` produz 1 resultado
    - `awk '/export const processoIntakeFormSchema/,0' web/src/schemas/processos.ts | head -1` contém `origem: origemProcessoSchema` (obrigatório, sem `.optional()`)
    - `awk '/export const processoFormSchema = z.object/,/^\\}\\);/' web/src/schemas/processos.ts | grep -c "^\\s*origem:"` retorna 0 (confirma que `origem` não está no schema geral usado pela edição)
    - `web/src/lib/tipo-decisao.ts`, `web/src/lib/tipo-testemunha.ts`, `web/src/lib/origem-processo.ts` existem e cada um contém `export function` com o nome `tipoDecisaoToLabel`/`tipoTestemunhaToLabel`/`origemProcessoToLabel` respetivamente
    - `cd web && npx tsc --noEmit 2>&1 | grep -v "cliente-documento-tipo.test.ts\|clientes.legacy-documento-tipo.test.ts"` não produz nenhuma linha de erro nova
  </acceptance_criteria>
  <verify>
    <automated>cd web && npx tsc --noEmit 2>&1 | grep -v "cliente-documento-tipo.test.ts\|clientes.legacy-documento-tipo.test.ts"</automated>
  </verify>
  <done>schemas/processos.ts exporta decisaoFormSchema/factoFormSchema/testemunhaFormSchema, origemProcessoSchema (z.enum obrigatório usado por processoIntakeFormSchema), e o processoFormSchema geral ganha juizo sem ganhar origem; três novos ficheiros de label PT existem em web/src/lib/.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Formulários (Phase 84, ainda não construídos) → Zod schema desta plan → hooks (Plan 83-02) → backend já hardened (Phase 81) | Esta plan só define contratos de tipo/validação estáticos; não introduz nenhum novo endpoint nem lógica de runtime que processe input não confiável — é puramente camada de dados/tipagem |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-83-01 | Tampering | `processoIntakeFormSchema`/`decisaoFormSchema`/etc. | mitigate | `origem`/`tipo` são `z.enum(...)` fechados (não `z.string()` livre) — um valor fora do conjunto permitido nunca passa da validação client-side; a fronteira de segurança real continua a ser a validação `@Enumerated(STRING)` + checks explícitos do backend (Phase 81), esta camada é defesa em profundidade/UX |
| T-83-02 | Tampering | `ProcessoUpdateRequest` (tipo TS) | mitigate | `origem` é omitido do tipo `ProcessoUpdateRequest` (não apenas opcional) — torna impossível, ao nível do compilador, que um caller de update tente enviar `origem`, fechando o vetor descrito em PITFALLS.md Pitfall 2 do lado do tipo, antes mesmo de chegar à Plan 83-02 |
| T-83-SC | Supply chain | npm/pnpm installs | mitigate | Zero novos pacotes instalados nesta plan (usa apenas `zod`, já existente) — nada a auditar em `Package Legitimacy Audit` |

Nenhuma ameaça neste registo é alta severidade — esta plan é puramente tipos/validação estática, sem novo endpoint nem lógica de runtime que processe dados não confiáveis.
</threat_model>

<verification>
1. `cd web && npx tsc --noEmit` não produz nenhum erro novo além dos 2 erros pré-existentes de `vitest` não instalado (confirmados como baseline em `82-VERIFICATION.md`).
2. `web/src/types/processos.ts` exporta `Decisao`, `Facto`, `Testemunha`, `TipoDecisao`, `TipoTestemunha`, `OrigemProcesso`; `Processo`/`ProcessoCreateRequest` ganham `juizo`/`origem`; `ProcessoUpdateRequest` ganha apenas `juizo`.
3. `web/src/schemas/processos.ts` exporta `decisaoFormSchema`, `factoFormSchema`, `testemunhaFormSchema`, `origemProcessoSchema` (enum obrigatório), `processoIntakeFormSchema`.
4. Três novos ficheiros de label PT existem em `web/src/lib/`.
</verification>

<success_criteria>
`types/processos.ts` inclui `Processo.juizo`/`origem` e os tipos `Decisao`/`Facto`/`Testemunha` (ROADMAP Phase 83 success criterion 1). `schemas/processos.ts` inclui `decisaoFormSchema`/`factoFormSchema`/`testemunhaFormSchema`, e `origem` é um `z.enum(...)` obrigatório no fluxo de intake, não mais `optionalTrimmedString` (ROADMAP Phase 83 success criterion 2).
</success_criteria>

<output>
Create `.planning/phases/LEXCV-83-frontend-tipos-schemas-e-hooks/83-01-SUMMARY.md` when done.
</output>
