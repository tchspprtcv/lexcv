---
phase: 84
plan: 04
type: execute
wave: 2
depends_on: ["84-03"]
files_modified:
  - web/src/app/(dashboard)/processos/[id]/page.tsx
autonomous: true
requirements: [PROC-08, PROC-12]

must_haves:
  truths:
    - "A aba Decisões lista as decisões do processo e permite criar (com anexo opcional num único passo), editar e remover, tudo via Dialog"
    - "A aba Testemunhas lista as testemunhas do processo e permite criar, editar e remover, tudo via Dialog"
    - "Ambas as abas só mostram os botões Adicionar/Editar/Remover a utilizadores com processos:edit"
    - "Remover uma Decisão ou Testemunha exige confirmação (window.confirm) antes de chamar a API"
  artifacts:
    - path: "web/src/app/(dashboard)/processos/[id]/page.tsx"
      provides: "Corpo das abas Decisões e Testemunhas, com Dialogs Adicionar/Editar"
  key_links:
    - from: "web/src/app/(dashboard)/processos/[id]/page.tsx"
      to: "useAddDecisao(id) (web/src/hooks/use-processos.ts)"
      via: "FormData multipart incluindo file opcional"
      pattern: "addDecisao\\.mutateAsync"
    - from: "web/src/app/(dashboard)/processos/[id]/page.tsx"
      to: "useUpdateTestemunha(id) (web/src/hooks/use-processos.ts)"
      via: "PUT JSON, Dialog Editar pré-preenchido via testemunhaForm.reset(rowData)"
      pattern: "updateTestemunha\\.mutateAsync"
---

<objective>
Build the Decisões and Testemunhas tab bodies inside `processos/[id]/page.tsx`, replacing the `null` placeholders left by Plan 84-03. Both follow the same list-Card + Dialog "Adicionar X" + per-row "Editar" (Dialog, pre-populated) + per-row "✕ Apagar" pattern documented in 84-PATTERNS.md's "Dialog Adicionar pattern to replicate exactly" section, reusing the 8 already-built hooks from Phase 83 (`useDecisoes`/`useAddDecisao`/`useUpdateDecisao`/`useDeleteDecisao`, `useTestemunhas`/`useAddTestemunha`/`useUpdateTestemunha`/`useDeleteTestemunha`) without modification. Decisão's create form additionally carries a native `&lt;input type="file"&gt;` for the optional anexo, submitted as multipart in a single POST — this is intentionally NOT the `FileDropZone` component used later for the Documentos tab (two different upload UIs coexist by design, per CONTEXT.md).

Purpose: deliver PROC-08 (list/edit/remove Decisões) and PROC-12 (list/edit/remove Testemunhas) via the Dialog pattern mandated by CONTEXT.md/UI-SPEC.
Output: `processos/[id]/page.tsx` with fully working Decisões and Testemunhas tab bodies.
</objective>

<execution_context>
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/LEXCV-84-frontend-ui-intake-dados-sub-sec-es-documentos-termo-de-hono/84-CONTEXT.md
@.planning/phases/LEXCV-84-frontend-ui-intake-dados-sub-sec-es-documentos-termo-de-hono/84-UI-SPEC.md
@.planning/phases/LEXCV-84-frontend-ui-intake-dados-sub-sec-es-documentos-termo-de-hono/84-PATTERNS.md
@.planning/research/PITFALLS.md

<interfaces>
From web/src/hooks/use-processos.ts (already built, Phase 83 — do not modify, consume only):
useDecisoes(id) → useQuery&lt;Decisao[]&gt;, queryKey ["processos","decisoes",id]
useAddDecisao(id) → useMutation, payload `DecisaoCreateRequest = { file?: File; data: string; tipo: TipoDecisao; resumo?: string }`, builds FormData internally (data/tipo/resumo/file fields), invalidates decisoes list + documentos list if `created.documentoId` is set.
useUpdateDecisao(id) → useMutation, args `{ decisaoId: number; payload: DecisaoUpdateRequest }` where `DecisaoUpdateRequest = { data: string; tipo: TipoDecisao; resumo?: string }` — NO file/documentoId field, matches the deliberate Phase 81 decision that anexo can only be set at creation time.
useDeleteDecisao(id) → useMutation, arg `decisaoId: number`, invalidates decisoes + documentos lists.
useTestemunhas(id) → useQuery&lt;Testemunha[]&gt;, queryKey ["processos","testemunhas",id]
useAddTestemunha(id) / useUpdateTestemunha(id) (args `{ testemunhaId: number; payload: TestemunhaUpdateRequest }`) / useDeleteTestemunha(id) (arg `testemunhaId: number`) — `TestemunhaCreateRequest`/`TestemunhaUpdateRequest` both shape `{ nome: string; tipo?: TipoTestemunha; contacto?: string; notas?: string }` (symmetric, unlike Decisão/Facto).

From web/src/schemas/processos.ts (already built, Phase 83):
decisaoFormSchema = { data: string (required), tipo: tipoDecisaoSchema (required), resumo: optionalTrimmedString, file: fileListSchema.optional() } → type DecisaoFormValues
tipoDecisaoSchema = z.enum(["DESPACHO","DECISAO_INTERLOCUTORIA","SENTENCA","ACORDAO"])
testemunhaFormSchema = { nome: string (required), tipo: tipoTestemunhaSchema.optional(), contacto: optionalTrimmedString, notas: optionalTrimmedString } → type TestemunhaFormValues
tipoTestemunhaSchema = z.enum(["AUTOR","REU"])

From web/src/lib/tipo-decisao.ts: tipoDecisaoToLabel(tipo: TipoDecisao): string
From web/src/lib/tipo-testemunha.ts: tipoTestemunhaToLabel(tipo: TipoTestemunha): string

From web/src/types/processos.ts:
Decisao { id: number; processoId: string; data: string; tipo: TipoDecisao; resumo?: string; documentoId?: string }
Testemunha { id: number; processoId: string; nome: string; contacto?: string; tipo?: TipoTestemunha; notas?: string }

Existing local helper already in this file: `formatDate(v: string | undefined)` (line 108) — reuse for the Decisões table's Data column.

Copywriting (from 84-UI-SPEC.md, use verbatim):
Decisões: tab label "Decisões", CTA "Adicionar Decisão", Dialog titles "Adicionar Decisão"/"Editar Decisão", empty "Nenhuma decisão registada.", list error "Não foi possível carregar as decisões deste processo.", create/update error fallback "Erro ao adicionar decisão"/"Erro ao atualizar decisão", success toast "Decisão adicionada com sucesso."/"Decisão atualizada com sucesso.", delete confirm "Apagar esta decisão?", delete success "Decisão apagada com sucesso.", delete aria-label "Apagar decisão".
Testemunhas: tab label "Testemunhas", CTA "Adicionar Testemunha", Dialog titles "Adicionar Testemunha"/"Editar Testemunha", empty "Nenhuma testemunha registada.", list error "Não foi possível carregar as testemunhas deste processo.", create/update error fallback "Erro ao adicionar testemunha"/"Erro ao atualizar testemunha", success toast "Testemunha adicionada com sucesso."/"Testemunha atualizada com sucesso.", delete confirm "Apagar esta testemunha?", delete success "Testemunha apagada com sucesso.", delete aria-label "Apagar testemunha".
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Decisões tab — list, Adicionar (multipart w/ file), Editar, Apagar</name>
  <files>web/src/app/(dashboard)/processos/[id]/page.tsx</files>
  <read_first>
    web/src/app/(dashboard)/processos/[id]/page.tsx
    web/src/hooks/use-processos.ts
    web/src/schemas/processos.ts
    .planning/phases/LEXCV-84-frontend-ui-intake-dados-sub-sec-es-documentos-termo-de-hono/84-PATTERNS.md (sections "Dialog Adicionar pattern to replicate exactly", "Decisão file-input registration")
  </read_first>
  <action>
    Add to the `@/hooks/use-processos` import block: `useDecisoes, useAddDecisao, useUpdateDecisao, useDeleteDecisao`. Add to the `@/schemas/processos` import block: `decisaoFormSchema, type DecisaoFormValues`. Add `import { tipoDecisaoToLabel } from "@/lib/tipo-decisao";`. Add `type DecisaoUpdateRequest` to the `@/types/processos` type-only import list (used with `satisfies`).

    Near the top of `ProcessoDetailContent`, alongside the other tab hooks (`partes`, `fases`), declare `const decisoes = useDecisoes(id);`, `const addDecisao = useAddDecisao(id);`, `const updateDecisao = useUpdateDecisao(id);`, `const deleteDecisao = useDeleteDecisao(id);`. Declare `const [addDecisaoModal, setAddDecisaoModal] = React.useState(false);`, `const [editingDecisaoId, setEditingDecisaoId] = React.useState&lt;number | null&gt;(null);`, `const [decisaoServerError, setDecisaoServerError] = React.useState&lt;string | null&gt;(null);`. Declare `const decisaoForm = useForm&lt;DecisaoFormValues&gt;({ resolver: zodResolver(decisaoFormSchema), defaultValues: { data: "", tipo: undefined, resumo: undefined, file: undefined } });`.

    Write two handlers: `onOpenAddDecisao = () =&gt; { decisaoForm.reset({ data: "", tipo: undefined, resumo: undefined, file: undefined }); setEditingDecisaoId(null); setDecisaoServerError(null); setAddDecisaoModal(true); }` and `onOpenEditDecisao = (d: Decisao) =&gt; { decisaoForm.reset({ data: d.data, tipo: d.tipo, resumo: d.resumo, file: undefined }); setEditingDecisaoId(d.id); setDecisaoServerError(null); setAddDecisaoModal(true); }`. Write `onSubmitDecisao = async (values: DecisaoFormValues) =&gt; { setDecisaoServerError(null); try { if (editingDecisaoId !== null) { await updateDecisao.mutateAsync({ decisaoId: editingDecisaoId, payload: { data: values.data, tipo: values.tipo, resumo: values.resumo } satisfies DecisaoUpdateRequest }); toast.success("Decisão atualizada com sucesso."); } else { await addDecisao.mutateAsync({ data: values.data, tipo: values.tipo, resumo: values.resumo, file: values.file?.[0] }); toast.success("Decisão adicionada com sucesso."); } setAddDecisaoModal(false); } catch (e) { const msg = e instanceof Error ? e.message : editingDecisaoId !== null ? "Erro ao atualizar decisão" : "Erro ao adicionar decisão"; setDecisaoServerError(msg); toast.error(msg); } }`. Write `onDeleteDecisao = async (decisaoId: number) =&gt; { const ok = window.confirm("Apagar esta decisão?"); if (!ok) return; try { await deleteDecisao.mutateAsync(decisaoId); toast.success("Decisão apagada com sucesso."); } catch (e) { toast.error(e instanceof Error ? e.message : "Erro ao apagar decisão"); } }`.

    Replace the `tab === "decisoes" ? null` placeholder from Plan 84-03 with: a `Card` whose `CardHeader` has `CardTitle` "Decisões" and, `canEditProcessos ? (...) : null`, a `Dialog open={addDecisaoModal} onOpenChange={setAddDecisaoModal}` with `DialogTrigger asChild` → `Button type="button" variant="outline" size="sm" onClick={onOpenAddDecisao}` labelled "Adicionar Decisão" (`onClick` calls `onOpenAddDecisao` even though the trigger already opens the dialog via `asChild`/Radix state — this resets the form/editing-id first, so the click handler must run before the dialog opens; keep both the `Dialog`'s `open`/`onOpenChange` controlled state AND the trigger's `onClick` reset). `DialogContent` → `DialogHeader`/`DialogTitle` reading `editingDecisaoId === null ? "Adicionar Decisão" : "Editar Decisão"`; a `&lt;form onSubmit={decisaoForm.handleSubmit(onSubmitDecisao)}&gt;` with: a "Data" field (`&lt;input type="date"&gt;`, `{...decisaoForm.register("data")}`, inline Zod error), a "Tipo" field (native `&lt;select&gt;`, `{...decisaoForm.register("tipo")}`, one `&lt;option value=""&gt;`Selecionar tipo`&lt;/option&gt;` plus one `&lt;option&gt;` per `tipoDecisaoSchema` value labelled via `tipoDecisaoToLabel`, inline Zod error), a "Resumo" `&lt;textarea&gt;` (`{...decisaoForm.register("resumo")}`), and — only when `editingDecisaoId === null` — a native `&lt;input id="decisao_file" type="file" {...decisaoForm.register("file")} /&gt;` under a "Anexo (opcional)" Label (omit this field entirely from the JSX when editing, since `DecisaoUpdateRequest` has no file slot); `decisaoServerError` rendered as inline red text; `DialogFooter` with Cancelar (`onClick={() =&gt; setAddDecisaoModal(false)}`) and `Button type="submit" disabled={decisaoForm.formState.isSubmitting || addDecisao.isPending || updateDecisao.isPending}` labelled "Confirmar".

    Below the header, `CardContent` renders the list-state ternary (loading/error/empty/table) exactly matching the pattern in 84-PATTERNS.md's "Error/loading list-state ternary" — loading text "A carregar...", error text "Não foi possível carregar as decisões deste processo.", empty text "Nenhuma decisão registada.", else a raw `&lt;table&gt;` (same density/classes as the existing Partes/Fases tables in this file) with columns "Data" | "Tipo" | "Resumo" | "Ações", one `&lt;tr&gt;` per `decisoes.data` item: Data via `formatDate(d.data)`, Tipo via `tipoDecisaoToLabel(d.tipo)`, Resumo via `d.resumo ?? "—"`, and an "Ações" cell containing (when `canEditProcessos`) a text link `&lt;button type="button" className="text-blue-600 hover:underline text-xs" onClick={() =&gt; onOpenEditDecisao(d)}&gt;Editar&lt;/button&gt;` followed by `&lt;button type="button" className="text-neutral-500 hover:text-red-600" onClick={() =&gt; onDeleteDecisao(d.id)} aria-label="Apagar decisão"&gt;✕&lt;/button&gt;`.
  </action>
  <acceptance_criteria>
    - Source: `useDecisoes`, `useAddDecisao`, `useUpdateDecisao`, `useDeleteDecisao` are imported and called exactly once each inside `ProcessoDetailContent`.
    - Source: the file input `{...decisaoForm.register("file")}` is conditionally rendered only when `editingDecisaoId === null` (grep confirms the file input JSX is inside a conditional referencing `editingDecisaoId`).
    - Source: `onSubmitDecisao` branches on `editingDecisaoId !== null` and never includes a `file`/`documentoId` key in the `updateDecisao.mutateAsync` payload.
    - Source: the delete button has `aria-label="Apagar decisão"` and is icon-only (`✕`, no visible text sibling).
    - Source: `window.confirm("Apagar esta decisão?")` guards the delete call.
    - Behavior: opening "Adicionar Decisão" after previously editing a Decisão shows a blank form (not the previous edit's stale values) — proven by `onOpenAddDecisao` resetting the form and `editingDecisaoId` before opening.
  </acceptance_criteria>
  <verify>
    <automated>cd "C:/Users/francisco.horta/Documents/projects/personal/lexcv/web" && pnpm exec tsc --noEmit</automated>
  </verify>
  <done>Decisões tab lists, creates (with optional multipart anexo), edits (no file), and deletes decisões, gated by canEditProcessos, with the exact UI-SPEC copy.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Testemunhas tab — list, Adicionar, Editar, Apagar</name>
  <files>web/src/app/(dashboard)/processos/[id]/page.tsx</files>
  <read_first>
    web/src/app/(dashboard)/processos/[id]/page.tsx
    web/src/hooks/use-processos.ts
    web/src/schemas/processos.ts
  </read_first>
  <action>
    Add to the `@/hooks/use-processos` import block: `useTestemunhas, useAddTestemunha, useUpdateTestemunha, useDeleteTestemunha`. Add to the `@/schemas/processos` import block: `testemunhaFormSchema, type TestemunhaFormValues`. Add `import { tipoTestemunhaToLabel } from "@/lib/tipo-testemunha";`. Add `type TestemunhaCreateRequest, TestemunhaUpdateRequest` to the `@/types/processos` type-only import list.

    Follow the identical structural pattern as Task 1's Decisões tab, adapted for Testemunha's symmetric create/update shape (no file, no ordem — `TestemunhaCreateRequest` and `TestemunhaUpdateRequest` have the exact same 4 fields, so the edit form does not need to omit any field the way Decisão's file input was omitted): declare `testemunhas = useTestemunhas(id)`, `addTestemunha = useAddTestemunha(id)`, `updateTestemunha = useUpdateTestemunha(id)`, `deleteTestemunha = useDeleteTestemunha(id)`; `addTestemunhaModal`/`setAddTestemunhaModal`, `editingTestemunhaId`/`setEditingTestemunhaId&lt;number|null&gt;`, `testemunhaServerError`/`setTestemunhaServerError`; `testemunhaForm = useForm&lt;TestemunhaFormValues&gt;({ resolver: zodResolver(testemunhaFormSchema), defaultValues: { nome: "", tipo: undefined, contacto: undefined, notas: undefined } })`.

    `onOpenAddTestemunha` resets the form to blank defaults and `editingTestemunhaId(null)` before opening; `onOpenEditTestemunha(t: Testemunha)` calls `testemunhaForm.reset({ nome: t.nome, tipo: t.tipo, contacto: t.contacto, notas: t.notas })` and sets `editingTestemunhaId(t.id)` before opening. `onSubmitTestemunha` branches on `editingTestemunhaId !== null`: update path calls `updateTestemunha.mutateAsync({ testemunhaId: editingTestemunhaId, payload: values satisfies TestemunhaUpdateRequest })` with success toast "Testemunha atualizada com sucesso."; create path calls `addTestemunha.mutateAsync(values satisfies TestemunhaCreateRequest)` with success toast "Testemunha adicionada com sucesso."; error fallback "Erro ao atualizar testemunha"/"Erro ao adicionar testemunha" respectively. `onDeleteTestemunha(testemunhaId: number)` confirms with `window.confirm("Apagar esta testemunha?")`, calls `deleteTestemunha.mutateAsync(testemunhaId)`, success toast "Testemunha apagada com sucesso.", error fallback "Erro ao apagar testemunha".

    Replace the `tab === "testemunhas" ? null` placeholder with a `Card` (`CardTitle` "Testemunhas", Dialog trigger labelled "Adicionar Testemunha" gated by `canEditProcessos`, `DialogTitle` `editingTestemunhaId === null ? "Adicionar Testemunha" : "Editar Testemunha"`). Dialog form fields: "Nome" (`Input`, required, inline error), "Tipo" (native `&lt;select&gt;` with `&lt;option value=""&gt;`Selecionar tipo`&lt;/option&gt;` plus `AUTOR`/`REU` options labelled via `tipoTestemunhaToLabel`, since `tipo` is optional the placeholder is a legitimate empty selection, not a validation error state), "Contacto" (`Input`), "Notas" (`&lt;textarea&gt;`). `CardContent` list ternary: loading "A carregar...", error "Não foi possível carregar as testemunhas deste processo.", empty "Nenhuma testemunha registada.", else a raw `&lt;table&gt;` with columns "Nome" | "Tipo" | "Contacto" | "Ações" — Tipo cell renders `t.tipo ? tipoTestemunhaToLabel(t.tipo) : "—"`, Ações cell has the same "Editar" text-link + "✕" `aria-label="Apagar testemunha"` icon-button pair as Task 1, gated by `canEditProcessos`.
  </action>
  <acceptance_criteria>
    - Source: `useTestemunhas`, `useAddTestemunha`, `useUpdateTestemunha`, `useDeleteTestemunha` imported and each called exactly once inside `ProcessoDetailContent`.
    - Source: the Testemunhas Tipo `&lt;select&gt;` has exactly 3 `&lt;option&gt;` elements (empty placeholder + AUTOR + REU).
    - Source: delete button has `aria-label="Apagar testemunha"`, guarded by `window.confirm("Apagar esta testemunha?")`.
    - Source: both Dialog triggers (Decisões from Task 1, Testemunhas from this task) and both tables' Ações cells are gated by `canEditProcessos`.
    - Behavior: `pnpm --dir web run build` succeeds with both new tabs wired into the ternary chain (no leftover `null` placeholder for `decisoes`/`testemunhas`).
  </acceptance_criteria>
  <verify>
    <automated>cd "C:/Users/francisco.horta/Documents/projects/personal/lexcv/web" && pnpm run build</automated>
  </verify>
  <done>Testemunhas tab lists, creates, edits, and deletes testemunhas, gated by canEditProcessos, with the exact UI-SPEC copy; production build passes with both Decisões and Testemunhas fully wired.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → Next.js rewrite → Spring Boot `/api/v1/processos/{id}/decisoes|testemunhas` | New CRUD surfaces consuming the Phase 81 endpoints, which already double-check tenant + processoId ownership server-side (Pitfall 3) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-84-09 | Elevation of Privilege | Decisão/Testemunha Adicionar/Editar/Apagar affordances | mitigate | Every Dialog trigger and every row's Editar/Apagar action is wrapped in `canEditProcessos ? (...) : null`, matching the Partes/Fases convention exactly — a user with only `processos:view` sees the lists but no mutating controls. |
| T-84-10 | Tampering | Native `&lt;input type="file"&gt;` on Decisão create (no client-side type/size restriction) | accept | The backend's `/decisoes` multipart endpoint (Phase 81) performs the same validation as the generic Documento upload path; this phase does not add new client-side file validation, consistent with how the Documentos tab (Plan 84-05) also defers validation to the backend. |
| T-84-11 | Information Disclosure / IDOR | Decisão/Testemunha edit-Dialog pre-population (`onOpenEditDecisao`/`onOpenEditTestemunha`) | accept | The row object passed to these handlers always comes from this processo's own list query (`["processos","decisoes",id]` / `["processos","testemunhas",id]`) — there is no user-controlled ID input anywhere in this UI; the ownership double-check (parent tenant + child processoId) that prevents cross-processo IDOR on PUT/DELETE is already enforced server-side (Phase 81, Pitfall 3), and this frontend never lets a user type or paste an arbitrary decisaoId/testemunhaId. |
</threat_model>

<verification>
- `pnpm --dir web exec tsc --noEmit` passes after Task 1.
- `pnpm --dir web run build` passes after Task 2.
- Manual/human-check (deferred to phase-level UAT): create a Decisão with an attached PDF, confirm it appears in the Documentos tab too (once Plan 84-05 lands) via the shared `documentoId` linkage; edit a Testemunha and confirm the Dialog pre-fills the existing values.
</verification>

<success_criteria>
- Decisões and Testemunhas tabs are fully functional (list/create/edit/delete) via the Dialog pattern, permission-gated identically to Partes/Fases.
</success_criteria>

<output>
Create `.planning/phases/LEXCV-84-frontend-ui-intake-dados-sub-sec-es-documentos-termo-de-hono/84-04-SUMMARY.md` when done
</output>
