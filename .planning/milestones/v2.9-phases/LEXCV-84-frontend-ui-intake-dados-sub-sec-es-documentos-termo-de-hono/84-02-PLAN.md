---
phase: 84
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/app/(dashboard)/processos/[id]/termo-honorarios/page.tsx
autonomous: true
requirements: [PROC-15, PROC-16]

must_haves:
  truths:
    - "Navegar para /processos/{id}/termo-honorarios sem permissão processos:view mostra AccessDeniedState, não o documento"
    - "Se o Honorário do processo tem valorTotal null, o botão Imprimir fica desativado com uma mensagem explicando o motivo, e nenhum valor em branco é impresso disfarçado de zero"
    - "Se não existir nenhum Honorário associado ao processo, é mostrada uma mensagem de erro explícita em vez de um documento com todos os campos em branco"
    - "O documento combina dados de Cliente, Processo e Honorário só depois de todos os três pedidos terem resolvido com sucesso — nunca renderiza com dados parciais de um dos três"
  artifacts:
    - path: "web/src/app/(dashboard)/processos/[id]/termo-honorarios/page.tsx"
      provides: "Rota imprimível do Termo de Honorários, clone do padrão CSS-print de clientes/[id]/ficha"
      min_lines: 150
  key_links:
    - from: "web/src/app/(dashboard)/processos/[id]/termo-honorarios/page.tsx"
      to: "useProcesso / useCliente / useHonorarios"
      via: "3 hooks combinados, todos gatekeeping isLoading/isError antes de renderizar o corpo do documento"
      pattern: "useHonorarios\\(\\{ processoId: id \\}\\)"
    - from: "web/src/app/(dashboard)/processos/[id]/termo-honorarios/page.tsx"
      to: "honorario.valorTotal === null"
      via: "disabled na Button Imprimir + mensagem inline com Link para /financeiro/{honorarioId}"
      pattern: "disabled=\\{isBlocked\\}"
---

<objective>
Create the new printable route `/processos/{id}/termo-honorarios` — a direct architectural clone of `clientes/[id]/ficha/page.tsx`'s `window.print()` + `PRINT_CSS` + `BLANK`/`fmt()` pattern, combining `Cliente` + `Processo` + `Honorario` data. Implements PROC-15 (generate/print the Termo de Honorários) and PROC-16 (hard-block printing, not just warn, when `Honorario.valorTotal` is `null`). This is the single highest-severity UI surface flagged by PITFALLS.md Pitfall 6 (3-hook loading/error gating) — the page must never render a partial/stale document if one of the three source hooks (Processo, Cliente, Honorario) is still loading, errored, or 404s.

Purpose: give the office a printable engagement-fee document generated from already-live data, without inventing a PDF/docx dependency (explicitly rejected per PROJECT.md Key Decisions).
Output: `web/src/app/(dashboard)/processos/[id]/termo-honorarios/page.tsx` (new file).
</objective>

<execution_context>
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/LEXCV-84-frontend-ui-intake-dados-sub-sec-es-documentos-termo-de-hono/84-CONTEXT.md
@.planning/phases/LEXCV-84-frontend-ui-intake-dados-sub-sec-es-documentos-termo-de-hono/84-UI-SPEC.md
@.planning/phases/LEXCV-84-frontend-ui-intake-dados-sub-sec-es-documentos-termo-de-hono/84-PATTERNS.md
@.planning/research/PITFALLS.md
@web/src/app/(dashboard)/clientes/[id]/ficha/page.tsx

<interfaces>
Analog to clone (full file already read, 239 lines): web/src/app/(dashboard)/clientes/[id]/ficha/page.tsx
- `PRINT_CSS` (media-print rules hiding aside/header/[data-print-hide]/.bottom-nav/.ficha-print-btn, @page A4 2cm margin) — copy verbatim.
- `BLANK = "___________"`, `fmt(value)` null-guard helper — copy verbatim for text fields.
- Page shell: outer default-export component does the `usePermissions()`/`AccessDeniedState` gate, delegates to an inner `*Content` component that owns all data hooks.
- `SectionTitle({ children })` and `Field({ label, value })` helper components — copy verbatim.
- Signature block markup (two-column `border-b border-black` blocks under "Data e Assinaturas") — copy verbatim, only relabel "A Advogada"/"O Cliente" if needed (Claude's Discretion, UI-SPEC).

From web/src/hooks/use-processos.ts:
useProcesso(id: string) → { data?: Processo, isLoading, isError, error }

From web/src/hooks/use-clientes.ts:
useCliente(id: string) → { data?: Cliente, isLoading, isError, error } — Cliente fields used: nome, nif, morada (flat columns since v2.7/v2.8, no dados_tipo).

From web/src/hooks/use-financeiro.ts:
useHonorarios(args?: { processoId?: string }) → { data?: Honorario[], isLoading, isError, error } — query key ["honorarios","list",processoId].
useHonorario(id: number) is also available for a single-record re-fetch but is NOT needed here — useHonorarios({ processoId: id }) already returns the full array; index [0] is the one auto-created Honorario per processo (Phase 82 invariant, at most one row).

From web/src/types/financeiro.ts:
interface Honorario { id: number; processoId: string; valorTotal: number | null; totalPago: number; descricao?: string; dataAcordo?: string; }

From web/src/hooks/use-me.ts: useMe() → { data?: { tenant_nome?: string } } — used for the letterhead tenant name, same as Ficha Cliente.

From web/src/app/(dashboard)/financeiro/[id]/page.tsx (route confirmed to exist — `financeiro/[id]/page.tsx`):
`formatMoneyCVE(v: number | null | undefined)` local pattern: `v.toLocaleString("pt-CV", { style: "currency", currency: "CVE" })` — reuse this exact `toLocaleString` call (not a shared export, define a local equivalent in the new file) for a currency-safe field, since the generic `fmt()` helper does a raw `String(value)` cast which is wrong for a currency amount on a legal document.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Page shell — permission gate, 3-hook data sourcing, loading/error/blocked gating, print toolbar</name>
  <files>web/src/app/(dashboard)/processos/[id]/termo-honorarios/page.tsx</files>
  <read_first>
    web/src/app/(dashboard)/clientes/[id]/ficha/page.tsx
    web/src/hooks/use-processos.ts
    web/src/hooks/use-clientes.ts
    web/src/hooks/use-financeiro.ts
    .planning/phases/LEXCV-84-frontend-ui-intake-dados-sub-sec-es-documentos-termo-de-hono/84-PATTERNS.md (section "processos/[id]/termo-honorarios/page.tsx (new)")
  </read_first>
  <action>
    Create `web/src/app/(dashboard)/processos/[id]/termo-honorarios/page.tsx` as a new `"use client"` file. Default-exported `TermoHonorariosPage({ params }: { params: Promise&lt;{ id: string }&gt; })` unwraps `id` via `React.use(params)`, calls `usePermissions()`, computes `canViewProcessos = permissions.can.view("processos")`, and returns `AccessDeniedState` with `description="Não tem permissão para consultar este processo."` and `backHref="/processos"` when `!permissions.isLoading && !canViewProcessos` — otherwise delegates to an inner `TermoHonorariosContent({ id })`.

    `TermoHonorariosContent` calls three hooks: `const processo = useProcesso(id);`, `const cliente = useCliente(processo.data?.cliente_id ?? "");`, `const honorarios = useHonorarios({ processoId: id });`. Derive `const honorario = honorarios.data?.[0];` (first/only result per the Phase 82 one-Honorario-per-processo invariant). Derive `const isLoading = processo.isLoading || cliente.isLoading || honorarios.isLoading;` and `const isError = processo.isError || cliente.isError || honorarios.isError;` — both must combine ALL THREE hooks, not just the primary one (Pitfall 6). Derive `const isBlocked = !honorario || honorario.valorTotal === null;`.

    Render, in order: (1) the `PRINT_CSS` `&lt;style dangerouslySetInnerHTML&gt;` tag (copy verbatim from the Ficha analog, including the `eslint-disable-next-line react/no-danger` comment); (2) a `data-print-hide` toolbar row containing a "&larr; Voltar ao processo" `Link` to `/processos/${encodeURIComponent(id)}` and an "Imprimir" `Button` (`className="ficha-print-btn"`, `data-print-hide`, `disabled={isBlocked}`, `onClick={() =&gt; window.print()}`, containing `&lt;Printer className="h-4 w-4 mr-2" /&gt;Imprimir`); (3) directly beneath the Imprimir button, when `isBlocked &amp;&amp; honorario` (i.e. an Honorário exists but its valorTotal is null — distinct from "no Honorário at all"), render the exact inline message from UI-SPEC: "O valor dos honorários ainda não foi preenchido. Preencha o valor em " followed by a `Link` to `/financeiro/${honorario.id}` with link text "Financeiro" and `className="text-blue-600 hover:underline"`, followed by " antes de gerar o termo.", wrapped in `&lt;p className="text-sm text-red-600 mt-2"&gt;`.

    Below the toolbar, a conditional render chain: `isLoading` → `"A carregar..."` (same style as Ficha Cliente); else `isError` → `"Erro ao carregar o termo."` in red; else `!honorario` → the UI-SPEC error-state copy "Não foi possível gerar o termo: nenhum honorário associado a este processo." in red; else (all three hooks resolved AND honorario exists, regardless of whether valorTotal is null) → render the document body component `&lt;TermoHonorarios processo={processo.data!} cliente={cliente.data!} honorario={honorario} tenantNome={...} /&gt;` (the document body itself is built in Task 2 — for this task, stub `TermoHonorarios` as a component that returns `null` if it does not exist yet, OR implement Task 2 in the same pass if context budget allows; if stubbing, Task 2 in this same plan replaces the stub before the plan is considered done). Pull `tenantNome` from `useMe().data?.tenant_nome ?? "LexCV"`, matching the Ficha Cliente pattern exactly (import `useMe` from `@/hooks/use-me`).
  </action>
  <acceptance_criteria>
    - Source: `isLoading` and `isError` are both boolean-OR combinations of all three hooks (`processo`, `cliente`, `honorarios`) — grep confirms `processo.isLoading || cliente.isLoading || honorarios.isLoading` (or equivalent all-three composition) exists.
    - Source: the Imprimir `Button` has `disabled={isBlocked}` where `isBlocked` is `!honorario || honorario.valorTotal === null`.
    - Source: the blocked-message `Link` points to `` `/financeiro/${honorario.id}` `` with visible text `Financeiro`.
    - Source: the "nenhum honorário associado" error copy renders only when `!honorario` (distinct code path from the null-valorTotal blocked state).
    - Behavior: if `honorarios.data` is an empty array (no Honorário for this processo), the page shows the "não foi possível gerar o termo" message, not a blank/undefined document.
  </acceptance_criteria>
  <verify>
    <automated>cd "C:/Users/francisco.horta/Documents/projects/personal/lexcv/web" && pnpm exec tsc --noEmit</automated>
  </verify>
  <done>The route file exists, is permission-gated, sources all three entities, and never renders the document body unless all three hooks resolved successfully and an Honorário exists.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Document body — SectionTitle/Field components, 4 sections, currency-safe valorTotal rendering</name>
  <files>web/src/app/(dashboard)/processos/[id]/termo-honorarios/page.tsx</files>
  <read_first>
    web/src/app/(dashboard)/clientes/[id]/ficha/page.tsx
    web/src/types/processos.ts
    web/src/types/clientes.ts
    web/src/types/financeiro.ts
  </read_first>
  <action>
    In the same file, add `SectionTitle({ children })` and `Field({ label, value })` components copied verbatim from the Ficha Cliente analog (uppercase bordered `h2` / two-column `dt`-style row with `font-mono underline` styling when the value equals `BLANK`). Add a `fmtMoney(value: number | null | undefined)` local helper: returns `BLANK` when `value` is `null`/`undefined`, otherwise `value.toLocaleString("pt-CV", { style: "currency", currency: "CVE" })` — this is the currency-safe variant Pitfall 6 requires (do not reuse the generic text `fmt()` for `valorTotal`, since a raw `String(number)` cast would print an unformatted digit string on a legal document).

    Implement the `TermoHonorarios({ processo, cliente, honorario, tenantNome })` component (props typed with `Processo`, `Cliente`, `Honorario` from their respective `@/types/*` modules) returning a `&lt;div className="max-w-[210mm] mx-auto p-8 bg-white text-black"&gt;` wrapper, matching the Ficha Cliente layout: a centered letterhead block (`tenantNome`, then uppercase title "Termo de Honorários", then today's date via `new Date().toLocaleDateString("pt-CV", { day: "2-digit", month: "2-digit", year: "numeric" })`), followed by four `SectionTitle` + `Field` groups exactly matching the UI-SPEC heading list and order:
    1. "Identificação do Cliente" — Fields: "Nome" (`fmt(cliente.nome)`), "NIF" (`fmt(cliente.nif)`), "Morada" (`fmt(cliente.morada)`).
    2. "Identificação do Processo" — Fields: "Número" (`fmt(processo.numero)`), "Tipo" (`fmt(processo.tipo_processo)`), "Tribunal" (`fmt(processo.tribunal)`), "Juízo" (`fmt(processo.juizo)`), "Origem" (`fmt(processo.origem ? origemProcessoToLabel(processo.origem) : undefined)` — import `origemProcessoToLabel` from `@/lib/origem-processo`).
    3. "Honorários" — Fields: "Valor Total" (`fmtMoney(honorario.valorTotal)`), "Total Pago" (`fmtMoney(honorario.totalPago)`), "Data do Acordo" (`fmt(honorario.dataAcordo)`), "Descrição" (`fmt(honorario.descricao)`).
    4. "Data e Assinaturas" — copy the Ficha Cliente signature block verbatim (two-column `border-b border-black` blank-line blocks), relabelling the two signature captions "O Advogado" / "O Cliente" (Claude's Discretion per UI-SPEC — "A Advogada"/"O Advogado" wording is not semantically pinned, pick a neutral "O Advogado" since the signer role is unknown at render time, not gendered from any known field).

    Wire this component into Task 1's `TermoHonorariosContent` render (replace the Task-1 stub if one was left) so the full page compiles and renders end-to-end.
  </action>
  <acceptance_criteria>
    - Source: the file exports/defines exactly four `SectionTitle` invocations with the literal texts "Identificação do Cliente", "Identificação do Processo", "Honorários", "Data e Assinaturas" (grep for each string).
    - Source: `fmtMoney` is used for `honorario.valorTotal` (not the generic `fmt`).
    - Source: `honorario.totalPago`, `honorario.dataAcordo`, `honorario.descricao`, `cliente.nome`, `cliente.nif`, `cliente.morada`, `processo.numero`, `processo.tribunal`, `processo.juizo`, `processo.origem` are all referenced somewhere in the file.
    - Behavior: `pnpm --dir web run build` succeeds (full production build, proves the route compiles and is statically analyzable by Next.js — this is the plan-level gate for a brand-new route file, since `tsc --noEmit` alone does not catch Next.js App Router file-convention errors).
  </acceptance_criteria>
  <verify>
    <automated>cd "C:/Users/francisco.horta/Documents/projects/personal/lexcv/web" && pnpm run build</automated>
  </verify>
  <done>The Termo de Honorários prints Cliente + Processo + Honorário data across the four UI-SPEC sections; valorTotal renders via a currency-safe formatter; the production build succeeds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → Next.js rewrite → Spring Boot `/api/v1` | Three independent tenant-scoped reads (Processo, Cliente, Honorario) converge on one printable page |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-84-04 | Information Disclosure | `TermoHonorariosContent` render gating | mitigate | `isLoading`/`isError` are both computed as the OR of all three hooks (processo, cliente, honorarios), not just the primary one — the document body never renders unless all three have resolved successfully, preventing a partial/stale cross-entity render (Pitfall 6). |
| T-84-05 | Information Disclosure | Printing with `valorTotal === null` | mitigate | PROC-16 hard block: Imprimir button is `disabled` and the currency field uses `fmtMoney` → `BLANK` (`___________`) rather than printing an empty string that could read as "0" or "free of charge". |
| T-84-06 | Elevation of Privilege | Route-level access | mitigate | Top-level `canViewProcessos` gate (`permissions.can.view("processos")`) mirrors `clientes/[id]/ficha/page.tsx`'s `canViewClientes` gate — unauthorized users see `AccessDeniedState`, never the document shell. |
| T-84-15 | Information Disclosure | Cross-tenant processo/cliente/honorario mismatch (e.g. a processo whose cliente_id or honorario belongs to another tenant due to a hypothetical backend bug) | accept | All three underlying hooks call tenant-scoped backend endpoints (`getTenantId()` checks already enforced server-side per Phase 79/81/82); this frontend route has no mechanism to detect or correct a server-side tenant-isolation bug — that risk is owned by the backend endpoints, already covered by existing `@PreAuthorize`/tenant-check patterns. |
</threat_model>

<verification>
- `pnpm --dir web exec tsc --noEmit` passes after Task 1.
- `pnpm --dir web run build` passes after Task 2 (proves the new App Router page compiles end-to-end).
- Manual/human-check (deferred to phase-level UAT): open `/processos/{id}/termo-honorarios` for a processo with a null-`valorTotal` Honorário — Imprimir is disabled and the red message + Financeiro link show; set a valorTotal and reload — Imprimir becomes enabled and the currency prints formatted (e.g. "150 000,00 CVE"), not a raw number.
</verification>

<success_criteria>
- Route exists, is permission-gated, sources Cliente+Processo+Honorário via three independently-checked hooks, and hard-blocks printing when valorTotal is null per PROC-16.
</success_criteria>

<output>
Create `.planning/phases/LEXCV-84-frontend-ui-intake-dados-sub-sec-es-documentos-termo-de-hono/84-02-SUMMARY.md` when done
</output>
