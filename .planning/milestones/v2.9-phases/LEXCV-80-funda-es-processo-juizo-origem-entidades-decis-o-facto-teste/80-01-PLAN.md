---
phase: "80"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/main/java/com/lexcv/models/OrigemProcesso.java
  - backend/src/main/java/com/lexcv/models/TipoDecisao.java
  - backend/src/main/java/com/lexcv/models/TipoTestemunha.java
  - backend/src/main/java/com/lexcv/models/Processo.java
  - backend/src/main/java/com/lexcv/models/Decisao.java
  - backend/src/main/java/com/lexcv/models/Facto.java
  - backend/src/main/java/com/lexcv/models/Testemunha.java
  - backend/src/main/java/com/lexcv/repositories/DecisaoRepository.java
  - backend/src/main/java/com/lexcv/repositories/FactoRepository.java
  - backend/src/main/java/com/lexcv/repositories/TestemunhaRepository.java
autonomous: true
requirements: [PROC-01, PROC-06, PROC-09, PROC-11]

must_haves:
  truths:
    - "Processo entity persists a free-text juizo column and an OrigemProcesso-enum-backed origem column on t_processo"
    - "OrigemProcesso, TipoDecisao and TipoTestemunha enums exist with the exact confirmed constant values (PETICAO_INICIAL/NOTIFICACOES_AVULSAS; DESPACHO/DECISAO_INTERLOCUTORIA/SENTENCA/ACORDAO; AUTOR/REU)"
    - "Decisao, Facto and Testemunha are JPA @Entity classes with an Integer IDENTITY id, a processo_id FK, and explicitly NO tenant_id column (tenant isolation is transitive via parent Processo, enforced in Phase 81)"
    - "Each of Decisao, Facto, Testemunha has a Spring Data JPA repository extending JpaRepository<Entity, Integer> with findByProcessoId(UUID)"
    - "The backend compiles and starts cleanly against the existing PostgreSQL database with ddl-auto=update, applying the new columns/tables without any Hibernate schema error and without breaking existing Processo persistence"
  artifacts:
    - path: "backend/src/main/java/com/lexcv/models/OrigemProcesso.java"
      provides: "Enum PETICAO_INICIAL | NOTIFICACOES_AVULSAS"
    - path: "backend/src/main/java/com/lexcv/models/TipoDecisao.java"
      provides: "Enum DESPACHO | DECISAO_INTERLOCUTORIA | SENTENCA | ACORDAO"
    - path: "backend/src/main/java/com/lexcv/models/TipoTestemunha.java"
      provides: "Enum AUTOR | REU"
    - path: "backend/src/main/java/com/lexcv/models/Processo.java"
      provides: "juizo (String) and origem (OrigemProcesso, EnumType.STRING) columns added to existing entity"
    - path: "backend/src/main/java/com/lexcv/models/Decisao.java"
      provides: "Entity: Integer id, processoId FK, data, tipo (TipoDecisao), resumo, documentoId (nullable FK to t_documento)"
    - path: "backend/src/main/java/com/lexcv/models/Facto.java"
      provides: "Entity: Integer id, processoId FK, descricao, data, ordem (Integer, scoped per processo_id)"
    - path: "backend/src/main/java/com/lexcv/models/Testemunha.java"
      provides: "Entity: Integer id, processoId FK, nome, contacto, tipo (TipoTestemunha), notas"
    - path: "backend/src/main/java/com/lexcv/repositories/DecisaoRepository.java"
      provides: "JpaRepository<Decisao, Integer> with findByProcessoId(UUID)"
    - path: "backend/src/main/java/com/lexcv/repositories/FactoRepository.java"
      provides: "JpaRepository<Facto, Integer> with findByProcessoId(UUID) and findByProcessoIdOrderByOrdemAsc(UUID)"
    - path: "backend/src/main/java/com/lexcv/repositories/TestemunhaRepository.java"
      provides: "JpaRepository<Testemunha, Integer> with findByProcessoId(UUID)"
  key_links:
    - from: "Processo.java"
      to: "OrigemProcesso.java"
      via: "@Enumerated(EnumType.STRING) @Column(name = \"origem\") private OrigemProcesso origem;"
    - from: "Decisao.java"
      to: "TipoDecisao.java"
      via: "@Enumerated(EnumType.STRING) @Column(nullable = false) private TipoDecisao tipo;"
    - from: "Testemunha.java"
      to: "TipoTestemunha.java"
      via: "@Enumerated(EnumType.STRING) private TipoTestemunha tipo;"
    - from: "Decisao.java"
      to: "Documento (t_documento)"
      via: "@Column(name = \"documento_id\") private UUID documentoId; -- plain nullable FK column, no JPA relationship, no ownership validation yet (deferred to Phase 81 per PITFALLS.md Pitfall 7)"
    - from: "DecisaoRepository.java / FactoRepository.java / TestemunhaRepository.java"
      to: "t_decisao / t_facto / t_testemunha"
      via: "extends JpaRepository<Entity, Integer>, derived-query findByProcessoId(UUID)"
---

<objective>
Establish the data layer for Phase 80: add `juizo`/`origem` columns to the existing `Processo` entity, create three new lean child entities (`Decisao`, `Facto`, `Testemunha`) mirroring `Parte.java`'s shape (Integer IDENTITY id, `processo_id` FK, no own `tenant_id`), their three supporting enums (`OrigemProcesso`, `TipoDecisao`, `TipoTestemunha`), and their three Spring Data JPA repositories.

Purpose: Every later phase in v2.9 (81 backend CRUD, 82 Honorário auto-creation, 83 frontend types/hooks, 84 frontend UI) depends on these shapes being final. This phase has zero UI-visible change — it is pure data-layer foundation work, matching the v2.8 Phase 74→75 lesson that changing entity/enum shapes after downstream work exists cascades expensively.

Output: 3 new enum files, 3 new entity files, 3 new repository files, 1 modified entity file (`Processo.java`). Backend compiles, and the application starts cleanly against the local PostgreSQL instance with `ddl-auto=update` applying the new schema without error.
</objective>

<execution_context>
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/LEXCV-80-funda-es-processo-juizo-origem-entidades-decis-o-facto-teste/80-CONTEXT.md
@.planning/phases/LEXCV-80-funda-es-processo-juizo-origem-entidades-decis-o-facto-teste/80-PATTERNS.md
</context>

<interfaces>
<!-- Exact analogs already verified against current repository source (2026-07-07). Executor should copy these
     patterns directly -- no further codebase exploration needed for this plan. -->

From `backend/src/main/java/com/lexcv/models/DocumentoTipo.java` (enum pattern, whole file):
```java
package com.lexcv.models;

public enum DocumentoTipo {
    BI,
    CNI,
    PASSAPORTE,
    REG_COMERCIAL
}
```

From `backend/src/main/java/com/lexcv/models/Parte.java` (lean child-entity shape, whole file -- the exact template for Decisao/Facto/Testemunha):
```java
package com.lexcv.models;

import jakarta.persistence.*;
import lombok.*;
import java.util.UUID;

@Entity
@Table(name = "t_parte")
@Getter
@Setter
@NoArgsConstructor
@AllArgsConstructor
@Builder
public class Parte {
    @Id
    @GeneratedValue(strategy = GenerationType.IDENTITY)
    private Integer id;

    @Column(name = "processo_id", nullable = false)
    private UUID processoId;

    @Column(nullable = false)
    private String nome;

    private String tipo;
}
```

From `backend/src/main/java/com/lexcv/repositories/ParteRepository.java` (repository pattern, whole file):
```java
package com.lexcv.repositories;

import com.lexcv.models.Parte;
import org.springframework.data.jpa.repository.JpaRepository;
import java.util.List;
import java.util.UUID;

public interface ParteRepository extends JpaRepository<Parte, Integer> {
    List<Parte> findByProcessoId(UUID processoId);
}
```

Current `backend/src/main/java/com/lexcv/models/Processo.java` (full file, as of 2026-07-07 -- the exact file Task 1 modifies):
```java
package com.lexcv.models;

import jakarta.persistence.*;
import lombok.*;
import java.time.LocalDate;
import java.time.LocalDateTime;
import java.util.UUID;

@Entity
@Table(name = "t_processo")
@Getter
@Setter
@NoArgsConstructor
@AllArgsConstructor
@Builder
public class Processo {
    @Id
    @GeneratedValue(strategy = GenerationType.UUID)
    private UUID id;

    @Column(name = "tenant_id", nullable = false)
    private UUID tenantId;

    @Column(name = "cliente_id", nullable = false)
    private UUID clienteId;

    @Column(name = "responsavel_id")
    private UUID responsavelId;

    @Column(name = "numero_processo")
    private String numeroProcesso;

    @Column(name = "tipo_processo")
    private String tipoProcesso;

    @Column(name = "area_juridica")
    private String areaJuridica;

    private String tribunal;
    private String estado;

    @Column(name = "data_inicio")
    private LocalDate dataInicio;

    @Column(name = "data_fim")
    private LocalDate dataFim;

    private String descricao;

    @Column(name = "legal_hold")
    @Builder.Default
    private Boolean legalHold = false;

    @Column(name = "data_retencao")
    private LocalDate dataRetencao;

    @Column(name = "created_at", updatable = false)
    private LocalDateTime createdAt;

    @PrePersist
    protected void onCreate() {
        this.createdAt = LocalDateTime.now();
    }
}
```

`Cliente.java`'s enum-backed field (the only existing precedent for `@Enumerated(EnumType.STRING)` on an entity in this codebase):
```java
@Enumerated(EnumType.STRING)
@Column(name = "documento_tipo")
private DocumentoTipo documentoTipo;
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create OrigemProcesso/TipoDecisao/TipoTestemunha enums and extend Processo with juizo/origem</name>
  <files>
    backend/src/main/java/com/lexcv/models/OrigemProcesso.java
    backend/src/main/java/com/lexcv/models/TipoDecisao.java
    backend/src/main/java/com/lexcv/models/TipoTestemunha.java
    backend/src/main/java/com/lexcv/models/Processo.java
  </files>
  <read_first>
    - backend/src/main/java/com/lexcv/models/DocumentoTipo.java (enum pattern to replicate verbatim -- plain enum, ALL_CAPS_SNAKE constants, no annotations)
    - backend/src/main/java/com/lexcv/models/Processo.java (MUST read current state before editing -- full file already reproduced in this plan's interfaces section above)
    - backend/src/main/java/com/lexcv/models/Cliente.java lines 46-48 (the documentoTipo field -- only existing @Enumerated(EnumType.STRING) precedent in the codebase)
  </read_first>
  <action>
    Create three plain Java enums in package com.lexcv.models, no annotations on the enum itself (mirror DocumentoTipo.java exactly):

    OrigemProcesso.java: constants PETICAO_INICIAL, NOTIFICACOES_AVULSAS (locked in 80-CONTEXT.md, ROADMAP success criterion 1).

    TipoDecisao.java: constants DESPACHO, DECISAO_INTERLOCUTORIA, SENTENCA, ACORDAO (confirmed PT/BR judicial taxonomy per milestone research, locked in 80-CONTEXT.md).

    TipoTestemunha.java: constants AUTOR, REU (closed enum, explicit user decision over free text, locked in 80-CONTEXT.md).

    Store stable ASCII codes only -- do not store accented Portuguese labels ("Petição Inicial", "Decisão Interlocutória") as the enum constant names; those are frontend-only display labels for a later phase (per 80-PATTERNS.md Pitfall 8 guidance).

    Edit Processo.java -- add two new fields immediately after the existing `private String estado;` line (which itself follows `private String tribunal;`), before the blank line preceding `dataInicio`:

    1. `private String juizo;` -- bare unannotated String field, exactly like tribunal/estado (Hibernate's default naming strategy already maps the single-word field to an identically-named juizo column, no @Column needed).
    2. `@Enumerated(EnumType.STRING) @Column(name = "origem") private OrigemProcesso origem;` -- mirrors Cliente.documentoTipo's exact annotation shape.

    No new imports are needed in Processo.java -- `jakarta.persistence.*` is already wildcard-imported and covers @Enumerated/EnumType.

    Do NOT add @Column(updatable = false) to origem at the entity level -- Phase 81's updateProcesso controller logic (not this phase) is responsible for excluding origem from the update field-copy list, matching how estado is already excluded there.
  </action>
  <verify>
    <automated>cd backend &amp;&amp; mvn -DskipTests package -q 2&gt;&amp;1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - OrigemProcesso.java exists with exactly two constants: PETICAO_INICIAL, NOTIFICACOES_AVULSAS
    - TipoDecisao.java exists with exactly four constants: DESPACHO, DECISAO_INTERLOCUTORIA, SENTENCA, ACORDAO
    - TipoTestemunha.java exists with exactly two constants: AUTOR, REU
    - Processo.java has a bare `private String juizo;` field and a `@Enumerated(EnumType.STRING)`-annotated `private OrigemProcesso origem;` field
    - grep -c "PETICAO_INICIAL" backend/src/main/java/com/lexcv/models/OrigemProcesso.java returns 1
    - grep -c "private String juizo" backend/src/main/java/com/lexcv/models/Processo.java returns 1
    - mvn -DskipTests package exits 0 with no compilation errors
  </acceptance_criteria>
  <done>Three enum files exist with the exact locked constant values; Processo.java carries juizo (String) and origem (OrigemProcesso, EnumType.STRING) fields; full Maven package build succeeds</done>
</task>

<task type="auto">
  <name>Task 2: Create Decisao, Facto and Testemunha entities mirroring Parte.java's lean shape</name>
  <files>
    backend/src/main/java/com/lexcv/models/Decisao.java
    backend/src/main/java/com/lexcv/models/Facto.java
    backend/src/main/java/com/lexcv/models/Testemunha.java
  </files>
  <read_first>
    - backend/src/main/java/com/lexcv/models/Parte.java (the exact structural template -- already reproduced in full in this plan's interfaces section above)
    - backend/src/main/java/com/lexcv/models/Documento.java (processo_id/cliente_id plain-FK-column convention, no JPA relationship annotations -- the pattern to copy for Decisao.documentoId)
  </read_first>
  <action>
    Create three JPA @Entity classes in package com.lexcv.models, each using the Lombok stack @Getter @Setter @NoArgsConstructor @AllArgsConstructor @Builder (verbatim combo used by Parte.java -- do NOT use @Data), `@Id @GeneratedValue(strategy = GenerationType.IDENTITY) private Integer id;`, and `@Column(name = "processo_id", nullable = false) private UUID processoId;`. None of the three carry a tenant_id column -- tenant isolation is transitive via the parent Processo, enforced at the controller layer in Phase 81, not here.

    Decisao.java -- @Table(name = "t_decisao"). Fields beyond id/processoId:
    - `@Column(nullable = false) private LocalDate data;`
    - `@Enumerated(EnumType.STRING) @Column(nullable = false) private TipoDecisao tipo;`
    - `private String resumo;` (bare, optional)
    - `@Column(name = "documento_id") private UUID documentoId;` (nullable, plain FK column pointing at an existing t_documento row -- mirrors Documento.processoId/Documento.clienteId's plain-column convention, no @ManyToOne/@JoinColumn relationship mapping, no ownership validation at this layer -- that validation is a Phase 81 controller concern per 80-PATTERNS.md/PITFALLS.md Pitfall 7)

    Facto.java -- @Table(name = "t_facto"). Fields beyond id/processoId:
    - `@Column(nullable = false) private String descricao;`
    - `private LocalDate data;` (bare, optional)
    - `@Column(nullable = false) private Integer ordem;` (scoped per processo_id -- max-lookup/reindex logic is a Phase 81 controller concern, this phase only defines the column)

    Testemunha.java -- @Table(name = "t_testemunha"). Fields beyond id/processoId:
    - `@Column(nullable = false) private String nome;`
    - `private String contacto;` (bare, optional)
    - `@Enumerated(EnumType.STRING) private TipoTestemunha tipo;` (bare, optional -- nullable, not `nullable = false`, since witnesses may be registered before their side is confirmed)
    - `private String notas;` (bare, optional)

    Required imports for each: jakarta.persistence.*, lombok.*, java.util.UUID; Decisao.java and Facto.java additionally need java.time.LocalDate.
  </action>
  <verify>
    <automated>cd backend &amp;&amp; mvn -DskipTests package -q 2&gt;&amp;1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - Decisao.java, Facto.java, Testemunha.java each declare `@Id @GeneratedValue(strategy = GenerationType.IDENTITY) private Integer id;` and `@Column(name = "processo_id", nullable = false) private UUID processoId;`
    - None of the three files contain the string "tenantId" or "tenant_id" (grep -c "tenant" returns 0 for all three files)
    - Decisao.java has a TipoDecisao tipo field annotated @Enumerated(EnumType.STRING) and a nullable documentoId field
    - Facto.java has a `nullable = false` Integer ordem field
    - Testemunha.java has a TipoTestemunha tipo field annotated @Enumerated(EnumType.STRING)
    - mvn -DskipTests package exits 0 with no compilation errors
  </acceptance_criteria>
  <done>Decisao.java, Facto.java, Testemunha.java exist, each compiling cleanly, matching Parte.java's lean shape with zero tenant_id column and the entity-specific fields listed above</done>
</task>

<task type="auto">
  <name>Task 3: Create the three repositories and verify the app starts cleanly with the new schema applied</name>
  <files>
    backend/src/main/java/com/lexcv/repositories/DecisaoRepository.java
    backend/src/main/java/com/lexcv/repositories/FactoRepository.java
    backend/src/main/java/com/lexcv/repositories/TestemunhaRepository.java
  </files>
  <read_first>
    - backend/src/main/java/com/lexcv/repositories/ParteRepository.java (minimal repository pattern -- already reproduced in full in this plan's interfaces section above)
    - backend/src/main/java/com/lexcv/repositories/ProcessoFaseRepository.java (second confirming example of the same Integer-id child-repository shape)
    - backend/.env.example (confirms which env vars the local dev stack needs; do NOT edit backend/.env -- pass missing values as process-level env vars only, per this task's verify command)
  </read_first>
  <action>
    Create three minimal Spring Data JPA repository interfaces in package com.lexcv.repositories, matching ParteRepository.java's exact shape -- no custom @Query, no service layer:

    DecisaoRepository.java: extends JpaRepository&lt;Decisao, Integer&gt; with one method, `List&lt;Decisao&gt; findByProcessoId(UUID processoId);`

    FactoRepository.java: extends JpaRepository&lt;Facto, Integer&gt; with two methods, `List&lt;Facto&gt; findByProcessoId(UUID processoId);` and `List&lt;Facto&gt; findByProcessoIdOrderByOrdemAsc(UUID processoId);` -- the ordered variant is added now for Phase 81's controller convenience (Facto listing needs deterministic order-by-ordem).

    TestemunhaRepository.java: extends JpaRepository&lt;Testemunha, Integer&gt; with one method, `List&lt;Testemunha&gt; findByProcessoId(UUID processoId);`

    Required imports for each: com.lexcv.models.&lt;Entity&gt;, org.springframework.data.jpa.repository.JpaRepository, java.util.List, java.util.UUID.

    After creating all three files, run the verification below to confirm the full backend (all 10 files from this plan combined) both compiles and starts cleanly against the local PostgreSQL instance, with Hibernate's ddl-auto=update applying the new t_decisao/t_facto/t_testemunha tables and the juizo/origem columns on t_processo without any schema error, and without breaking existing bean wiring. The local backend/.env on this machine is missing the MINIO_* variables (a pre-existing environment gap unrelated to this phase's scope) -- do not edit backend/.env; instead supply the missing variables as process-level environment variables only for this one-off verification run (confirmed working values below, matching backend/.env.example's defaults). If the verification command reports APP_START_FAILED for a reason unrelated to this phase's files (e.g. local Postgres/MinIO not reachable in the execution environment), fall back to `mvn -DskipTests package` alone as the acceptance gate and note the skipped live-startup check explicitly in the plan SUMMARY.
  </action>
  <verify>
    <automated>cd backend &amp;&amp; mvn -DskipTests package -q 2&gt;&amp;1 | tail -40 &amp;&amp; MINIO_ENDPOINT=http://localhost:9000 MINIO_ACCESS_KEY=minioadmin MINIO_SECRET_KEY=minioadmin MINIO_BUCKET_NAME=lexcv-documentos MINIO_PUBLIC_ENDPOINT=http://localhost:9000 SEED_ENABLED=false timeout 60 mvn spring-boot:run -q &gt; ../scratchpad-phase80-startup.log 2&gt;&amp;1; grep -q "Started BackendApplication" ../scratchpad-phase80-startup.log &amp;&amp; echo "APP_STARTED_OK" || echo "APP_START_FAILED"; grep -icE "SchemaManagementException|PropertyAccessException|could not execute statement|Unknown column|MappingException" ../scratchpad-phase80-startup.log</automated>
  </verify>
  <acceptance_criteria>
    - DecisaoRepository.java, FactoRepository.java, TestemunhaRepository.java each extend JpaRepository parameterized with Integer as the id type
    - grep -c "findByProcessoId" on each of the three repository files returns at least 1
    - FactoRepository.java additionally contains findByProcessoIdOrderByOrdemAsc
    - mvn -DskipTests package exits 0 with no compilation errors across all 10 files from this plan
    - The startup verification log contains the line "Started BackendApplication" (or the plan SUMMARY explicitly documents that the live-startup check was skipped because local Postgres/MinIO were unreachable in the execution environment, with mvn -DskipTests package success cited as the fallback acceptance gate)
    - The startup verification log's count of SchemaManagementException/PropertyAccessException/could not execute statement/Unknown column/MappingException occurrences is 0 (when the live-startup check runs)
  </acceptance_criteria>
  <done>DecisaoRepository.java, FactoRepository.java, TestemunhaRepository.java exist and compile; the full 10-file changeset from this plan builds successfully and (when local Postgres/MinIO are reachable) the application starts cleanly with Hibernate applying the new t_decisao/t_facto/t_testemunha tables and t_processo's new juizo/origem columns via ddl-auto=update, with zero schema-related errors in the startup log</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| None yet (data layer only) | This phase introduces no new HTTP endpoints, no new user input path, and no new authentication/authorization surface. The new entities are inert POJOs/JPA mappings until Phase 81 wires controller endpoints on top of them. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-80-01 | Tampering | Decisao/Facto/Testemunha (no own tenant_id column) | accept | Documented, deliberate design choice matching Parte/ProcessoFase/Movimentacao -- these entities expose zero endpoints in this phase, so there is no exploitable surface yet. Tenant isolation MUST be enforced transitively via the parent Processo (parent tenant check + child processoId re-check, matching the ProcessoFase PUT pattern) when Phase 81 adds controller endpoints -- flagged explicitly as a hard requirement for that phase, not silently assumed. |
| T-80-02 | Information Disclosure | OrigemProcesso/TipoDecisao/TipoTestemunha enum constants | accept | Enum constants carry no PII and no secret; stored as stable ASCII codes, not user-controlled strings. Zero risk. |
| T-80-03 | Tampering | Decisao.documentoId (raw nullable UUID FK, no relationship/ownership validation at this layer) | mitigate (deferred, tracked) | This phase only defines the column -- it performs no writes to it (no endpoint exists yet). Phase 81's endpoint that sets this field MUST validate the referenced Documento belongs to the same tenant AND the same processo before persisting the link (re-applying the Phase 79 ownership-validation fix), per 80-PATTERNS.md/PITFALLS.md Pitfall 7. Recorded here so it is not silently missed when Phase 81 is planned. |
| T-80-SC | Supply Chain | Maven dependencies | accept | No new dependency introduced by this phase -- confirmed by milestone research (SUMMARY.md: "no new dependency required"). Nothing to audit. |

No threat in this register is high severity (no live endpoint, no user-reachable input path exists yet in this phase) -- nothing blocks this plan under the ASVS Level 1 gate.
</threat_model>

<verification>
1. `cd backend && mvn -DskipTests package -q` exits 0 -- all 10 files (3 enums, 3 entities, 3 repositories, 1 modified entity) compile and package cleanly together.
2. `grep -rc "tenant" backend/src/main/java/com/lexcv/models/Decisao.java backend/src/main/java/com/lexcv/models/Facto.java backend/src/main/java/com/lexcv/models/Testemunha.java` returns 0 for each file -- confirms no tenant_id column was accidentally added to the new child entities.
3. Live-startup check (Task 3's automated verify): the application starts cleanly against the local PostgreSQL instance with `ddl-auto=update`, applying `t_decisao`/`t_facto`/`t_testemunha` and the new `t_processo` columns with zero schema-related errors in the log. If local Postgres/MinIO are not reachable in the execution environment, this step is explicitly documented as skipped in the plan SUMMARY, with `mvn -DskipTests package` success as the fallback acceptance evidence.
4. No existing file outside this plan's `files_modified` list is touched -- `git status` after execution shows changes scoped to exactly the 10 files listed in frontmatter.
</verification>

<success_criteria>
`Processo` has `juizo` (String) and `origem` (OrigemProcesso, EnumType.STRING) columns. `Decisao`, `Facto`, `Testemunha` exist as lean JPA entities (Integer IDENTITY id, `processo_id` FK, no `tenant_id`) with their own Spring Data JPA repositories exposing `findByProcessoId`. The full backend module compiles cleanly as a single changeset, and the new schema applies via `ddl-auto=update` without breaking any existing Processo persistence path -- ready for Phase 81 to build controller endpoints on top of these exact shapes.
</success_criteria>

<output>
Create `.planning/phases/LEXCV-80-funda-es-processo-juizo-origem-entidades-decis-o-facto-teste/80-01-SUMMARY.md` when done.
</output>
