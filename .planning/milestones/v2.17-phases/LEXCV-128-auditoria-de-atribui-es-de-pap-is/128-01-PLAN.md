---
phase: 128-auditoria-de-atribui-es-de-pap-is
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/main/java/com/lexcv/models/AuditLog.java
  - backend/src/main/java/com/lexcv/repositories/AuditLogRepository.java
  - backend/migrations/128-add-audit-log-detalhe.sql
  - backend/migrations/README.md
  - DEPLOYMENT.md
  - backend/src/test/java/com/lexcv/repositories/AuditLogImutabilidadeTest.java
autonomous: true
requirements: [AUDT-01, AUDT-02, AUDT-04]

must_haves:
  truths:
    - "t_audit_log has a nullable detalhe column, created by ddl-auto on fresh installs and by migration 128 on existing ones"
    - "AuditLogRepository exposes no delete, saveAll, saveAndFlush or update method: calling one does not compile"
    - "Hibernate never emits an UPDATE for a loaded AuditLog (@Immutable)"
    - "A structural test fails if a write endpoint over the audit log, a delete/update statement against t_audit_log, or a widening of the repository appears"
    - "The ten existing audit write sites and GET /processos/{id}/audit keep compiling and behaving unchanged"
  artifacts:
    - path: "backend/src/main/java/com/lexcv/models/AuditLog.java"
      provides: "detalhe column, @Immutable, RBAC vocabulary comments"
      contains: "@Immutable"
    - path: "backend/src/main/java/com/lexcv/repositories/AuditLogRepository.java"
      provides: "Narrowed append-only repository with paginated RBAC finder"
      contains: "extends Repository<AuditLog, Long>"
    - path: "backend/migrations/128-add-audit-log-detalhe.sql"
      provides: "Idempotent ADD COLUMN IF NOT EXISTS detalhe"
      contains: "ADD COLUMN IF NOT EXISTS detalhe"
    - path: "backend/src/test/java/com/lexcv/repositories/AuditLogImutabilidadeTest.java"
      provides: "AUDT-04 structural gate"
      min_lines: 60
  key_links:
    - from: "AuditLogRepository.buscarEventosRbac"
      to: "t_audit_log.tenant_id"
      via: "native query WHERE a.tenant_id = :tenantId"
      pattern: "a.tenant_id = :tenantId"
    - from: "backend/migrations/README.md"
      to: "128-add-audit-log-detalhe.sql"
      via: "Path B row 17, re-run safety row, execution status row, Path A redundant list"
      pattern: "128-add-audit-log-detalhe"
---

<objective>
Lay the storage foundation for RBAC auditing: extend the existing `AuditLog` entity (Decision 1: reuse `t_audit_log`, no second table) with the nullable `detalhe` column (Decision 2), make the entity and repository immutable by construction (Decision 6), declare the paginated tenant-scoped RBAC finder (Decision 5), ship manual migration 128 with its README inventory rows, and add the structural gate that proves AUDT-04.

Purpose: Every later plan writes to or reads from this contract. Narrowing the repository first means no later plan can accidentally use `delete*`/`saveAll`.
Output: modified `AuditLog.java` and `AuditLogRepository.java`, `128-add-audit-log-detalhe.sql`, README/DEPLOYMENT updates, and `AuditLogImutabilidadeTest`.
</objective>

<execution_context>
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/LEXCV-128-auditoria-de-atribui-es-de-pap-is/128-CONTEXT.md
@.planning/phases/LEXCV-128-auditoria-de-atribui-es-de-pap-is/128-PATTERNS.md
@backend/migrations/README.md
@CLAUDE.md

<interfaces>
Shared vocabulary for the whole phase. Every later plan uses these exact strings. Defined here, in the entity's comments, per Decision 1.

entidade_tipo (new values):
- `papel_escritorio`: entidade_id = TenantRole.id (UUID string). Role lifecycle and permission events.
- `atribuicao_papel`: entidade_id = target User.id (UUID string). Assignment events.

acao (new values):
- `papel_criar`, `papel_renomear`, `papel_apagar`, `papel_permissoes_alterar` (entidade_tipo papel_escritorio)
- `papel_atribuir`, `papel_retirar` (entidade_tipo atribuicao_papel)

detalhe: JSON object serialized to text (written only by AuditoriaRbacService, Plan 02). Keys, null keys omitted:
- `autorNome` (User.nome of the actor at event time; absent for system events)
- `papelId`, `papelNome` (always present on RBAC events)
- `alvoNome` (User.nome of the target at event time; atribuicao_papel only)
- `nomeAntigo`, `nomeNovo` (papel_renomear only)
- `permissoesAdicionadas`, `permissoesRemovidas` (arrays of permission KEYS, sorted)
- `motivo`: `utilizador_eliminado` | `provisionamento` (atribuicao_papel only, optional)
Never an email, phone or any other personal field (revised Decision 2).

Current AuditLogRepository (to be narrowed):
  public interface AuditLogRepository extends JpaRepository<AuditLog, Long> {
      List<AuditLog> findByTenantIdAndProcessoIdOrderByTimestampDesc(UUID tenantId, UUID processoId);
  }
Callers (PATTERNS.md §2, verified): 5 x save in ParecerController, 5 x save in ResourceController,
ResourceController.getAuditLog uses findByTenantIdAndProcessoIdOrderByTimestampDesc, and 4 test files
stub `lenient().when(auditLogRepository.save(any())).thenReturn(null)`. No other inherited method is used anywhere.

Pagination precedent: NotificacaoRepository.buscarPorFiltros (native @Query + countQuery,
`CAST(:param AS text) IS NULL OR ...` for optional filters, returns Page<T>).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend AuditLog, narrow AuditLogRepository, add migration 128 and its inventory rows</name>
  <files>backend/src/main/java/com/lexcv/models/AuditLog.java, backend/src/main/java/com/lexcv/repositories/AuditLogRepository.java, backend/migrations/128-add-audit-log-detalhe.sql, backend/migrations/README.md, DEPLOYMENT.md</files>
  <read_first>
    - backend/src/main/java/com/lexcv/models/AuditLog.java (full, 51 lines)
    - backend/src/main/java/com/lexcv/repositories/AuditLogRepository.java (full)
    - backend/src/main/java/com/lexcv/repositories/NotificacaoRepository.java (buscarPorFiltros: the native query + countQuery idiom to copy)
    - backend/src/main/java/com/lexcv/repositories/ParecerSolicitacaoRepository.java (import style for Spring Data annotations)
    - backend/migrations/124-add-permission-catalogo-columns.sql (header-comment convention, idempotent ADD COLUMN IF NOT EXISTS)
    - backend/migrations/README.md (full: Path A skip list, Path B table currently 16 rows, Re-run safety "6 of 16", Known execution status "8 scripts")
    - DEPLOYMENT.md (the "Phase 126 — conversão de papéis de escritório" subsection, as placement reference)
  </read_first>
  <action>
AuditLog.java (Decision 1, 2, 6):
- Add class-level `@Immutable` (import `org.hibernate.annotations.Immutable`). Add a short comment explaining that Hibernate dirty-checking never issues an UPDATE for a loaded row, that `@PrePersist` still fires on first insert, and that this is half of AUDT-04's by-construction guarantee (the other half is the narrowed repository).
- Add field `detalhe` of type String with `@Column(name = "detalhe", columnDefinition = "text")`, nullable (no `nullable = false`). Comment: JSON written only by `AuditoriaRbacService`; the ten processo/parecer write sites never set it, so it stays null for them; stores only display names (User.nome), role names and permission keys, never email or other personal data (revised Decision 2), because `AdminController.deleteUser` is a hard delete and read-time resolution would lose the actor and target.
- Extend the two "Values:" comments on `acao` and `entidadeTipo` with the new vocabulary in the interfaces block (keep the existing values; also add the existing values PATTERNS.md found in use but not listed there: `processo_atribuir`, `parecer_criar`, `parecer_solicitacao`, if the executor confirms them by grepping `.acao("` and `.entidadeTipo("` across `backend/src/main/java`).

AuditLogRepository.java (Decision 6):
- Change the base interface from `JpaRepository<AuditLog, Long>` to `org.springframework.data.repository.Repository<AuditLog, Long>`.
- Declare exactly three methods: `AuditLog save(AuditLog auditLog)`, the existing `findByTenantIdAndProcessoIdOrderByTimestampDesc(UUID tenantId, UUID processoId)`, and a new `Page<AuditLog> buscarEventosRbac(UUID tenantId, String utilizadorAlvoId, String papelId, Pageable pageable)`.
- `buscarEventosRbac` is a native `@Query` with a paired `countQuery`, same idiom as `NotificacaoRepository.buscarPorFiltros`. WHERE clause: `a.tenant_id = :tenantId` AND `a.entidade_tipo IN ('papel_escritorio', 'atribuicao_papel')` AND optional target filter `(CAST(:utilizadorAlvoId AS text) IS NULL OR (a.entidade_tipo = 'atribuicao_papel' AND a.entidade_id = CAST(:utilizadorAlvoId AS text)))` AND optional role filter `(CAST(:papelId AS text) IS NULL OR (a.entidade_tipo = 'papel_escritorio' AND a.entidade_id = CAST(:papelId AS text)) OR (a.entidade_tipo = 'atribuicao_papel' AND CAST(a.detalhe AS jsonb) ->> 'papelId' = CAST(:papelId AS text)))`. The select query orders `ORDER BY a.timestamp DESC, a.id DESC`; the countQuery repeats the same WHERE without ORDER BY. Use `@Param` on each parameter. The role filter matches by role ID, not name, so events about a role that was later renamed are still found (this reconciles the UI-SPEC's name-based assumption; see Plan 08).
- Header comment: why `Repository` and not `JpaRepository` (delete/saveAll/saveAndFlush must not exist in the compiled API, not merely go uncalled); the full caller inventory from the interfaces block; that the method set is pinned by `AuditLogImutabilidadeTest` and that adding a method requires updating that test deliberately; and that this is the second use of Pageable in the codebase after NotificacaoRepository.
- Do NOT touch ParecerController, ResourceController or the four test files. They must compile unchanged.

backend/migrations/128-add-audit-log-detalhe.sql:
- Header comment in the 124 convention: Phase 128 (AUDT-01/AUDT-02) adds `t_audit_log.detalhe`. Why: installs running `SPRING_JPA_HIBERNATE_DDL_AUTO=validate` refuse to start without it. Provenance note: `t_audit_log` itself has NO creation script in this directory; it was created by `ddl-auto: update` (Phase 87 era) and exists on every install because every environment runs `update` today (CLAUDE.md). This script only adds a column to that existing table. Nullable, no backfill: existing rows are processo/parecer events that never had detail. Idempotent.
- Single statement: `ALTER TABLE t_audit_log ADD COLUMN IF NOT EXISTS detalhe TEXT;`

backend/migrations/README.md (inventory, same commit):
- Path A: add `128-add-audit-log-detalhe.sql` to the redundant-but-harmless group (next to the 126/127 bullets): ddl-auto already creates the column and `ADD COLUMN IF NOT EXISTS` makes a run a no-op.
- Path B table: add row 17 for `128-add-audit-log-detalhe.sql`. "What it does": adds nullable `t_audit_log.detalhe` (Phase 128, RBAC audit), no backfill, and states that `t_audit_log` has no creation script in this directory because `ddl-auto: update` created it. "Re-runnable?": **Yes**, `ADD COLUMN IF NOT EXISTS`.
- Re-run safety: change "Only **6 of 16** scripts" to "Only **7 of 17** scripts" and add a table row for `128-add-audit-log-detalhe`. "The other **10** must not be re-run" stays 10.
- Known execution status: change "**8 scripts are outstanding**" to "**9 scripts are outstanding**" and add a row `128-add-audit-log-detalhe.sql` | Pending, new in this phase.

DEPLOYMENT.md: add a short subsection "### Phase 128 — auditoria de papéis" right after the Phase 126 subsection. In 3-5 sentences: run `backend/migrations/128-add-audit-log-detalhe.sql` before the deploy on `validate` installs; redundant but harmless on `update`; no data conversion; link to `backend/migrations/README.md` as the authoritative inventory (do not restate the list).
  </action>
  <verify>
    <automated>JAVA_HOME="/c/Program Files/Java/jdk-23" mvn -q -f backend/pom.xml test-compile</automated>
  </verify>
  <acceptance_criteria>
    - `grep -F "@Immutable" backend/src/main/java/com/lexcv/models/AuditLog.java` matches
    - `grep -F "name = \"detalhe\"" backend/src/main/java/com/lexcv/models/AuditLog.java` matches and the same line has no `nullable = false`
    - `grep -F "papel_permissoes_alterar" backend/src/main/java/com/lexcv/models/AuditLog.java` and `grep -F "atribuicao_papel" backend/src/main/java/com/lexcv/models/AuditLog.java` both match
    - `grep -F "extends Repository<AuditLog, Long>" backend/src/main/java/com/lexcv/repositories/AuditLogRepository.java` matches; `grep -F "import org.springframework.data.jpa.repository.JpaRepository;" backend/src/main/java/com/lexcv/repositories/AuditLogRepository.java` returns nothing
    - `grep -F "buscarEventosRbac" backend/src/main/java/com/lexcv/repositories/AuditLogRepository.java` matches and `grep -F "a.tenant_id = :tenantId" backend/src/main/java/com/lexcv/repositories/AuditLogRepository.java` matches at least twice (query and countQuery)
    - `grep -F "ADD COLUMN IF NOT EXISTS detalhe" backend/migrations/128-add-audit-log-detalhe.sql` matches
    - `grep -F "7 of 17" backend/migrations/README.md` matches; `grep -F "6 of 16" backend/migrations/README.md` returns nothing
    - `grep -F "9 scripts are outstanding" backend/migrations/README.md` matches
    - `grep -c -F "128-add-audit-log-detalhe" backend/migrations/README.md` is at least 4 (Path A, Path B, re-run, status)
    - `grep -F "128-add-audit-log-detalhe" DEPLOYMENT.md` matches
    - `JAVA_HOME="/c/Program Files/Java/jdk-23" mvn -q -f backend/pom.xml test-compile` exits 0 (proves the ten save sites, getAuditLog and the four test stubs compile against the narrowed interface)
  </acceptance_criteria>
  <done>Entity carries detalhe and @Immutable; repository is append-only with three declared methods; migration 128 exists and is inventoried in all four README locations plus a DEPLOYMENT pointer; backend compiles including tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Structural immutability gate (AUDT-04)</name>
  <files>backend/src/test/java/com/lexcv/repositories/AuditLogImutabilidadeTest.java</files>
  <read_first>
    - backend/src/main/java/com/lexcv/repositories/AuditLogRepository.java (as modified by Task 1)
    - backend/src/main/java/com/lexcv/models/AuditLog.java (as modified by Task 1)
    - backend/src/test/java/com/lexcv/controllers/OfficeRolesControllerTest.java (header and JUnit 5 style used in this codebase)
    - .planning/phases/LEXCV-128-auditoria-de-atribui-es-de-pap-is/128-PATTERNS.md ("No Analog Found" table: this test has no precedent)
  </read_first>
  <behavior>
    - Test 1: AuditLogRepository is assignable to org.springframework.data.repository.Repository and NOT assignable to CrudRepository, ListCrudRepository, PagingAndSortingRepository or JpaRepository
    - Test 2: the set of method names from AuditLogRepository.class.getMethods() equals exactly {save, findByTenantIdAndProcessoIdOrderByTimestampDesc, buscarEventosRbac}; no method carries @Modifying
    - Test 3: no method name starts with delete, remove, update, saveAll or saveAndFlush
    - Test 4: AuditLog carries org.hibernate.annotations.Immutable
    - Test 5: no @RestController class under com.lexcv.controllers has a @PostMapping, @PutMapping, @PatchMapping or @DeleteMapping handler (or a @RequestMapping whose method includes POST/PUT/PATCH/DELETE) whose combined class+method path contains "audit" (case-insensitive, so "auditoria" is covered too)
    - Test 6: no .java file under src/main/java contains, case-insensitively, "delete from t_audit_log", "update t_audit_log", "delete from auditlog" or "update auditlog "
  </behavior>
  <action>
Create a plain JUnit 5 test (no Spring context) named `AuditLogImutabilidadeTest` in package `com.lexcv.repositories`. Class-level Javadoc in Portuguese: this is the Decision 6 structural gate, with no precedent in the codebase (PATTERNS.md "No Analog Found"). It fails when anyone widens the repository, reintroduces a delete/update path, or adds a write endpoint over the audit log. When the test fails because a legitimate read method was added, the fix is to add that name to the expected set deliberately, never to loosen the check.

Implementation guidance:
- Tests 1-4: plain reflection on `AuditLogRepository.class` and `AuditLog.class`. Check @Modifying with `org.springframework.data.jpa.repository.Modifying`.
- Test 5: use `ClassPathScanningCandidateComponentProvider(false)` with `AnnotationTypeFilter(RestController.class)` over base package `com.lexcv.controllers`. For each class, read the class-level `@RequestMapping` path(s) and, for each declared method, the path(s) of `@PostMapping/@PutMapping/@PatchMapping/@DeleteMapping` (value and path attributes), plus `@RequestMapping` with a mutating `RequestMethod`. Concatenate the class and method paths, lower-case the result, and fail with the offending class#method if it contains "audit". The existing `GET /api/v1/processos/{id}/audit` is a GET and must pass. Assert that the scan found at least 5 controllers, so an empty scan cannot pass vacuously.
- Test 6: `Files.walk(Path.of("src/main/java"))` (surefire's working directory is the backend module basedir). Filter `.java` files, read each file, lower-case it, and check it contains none of the four forbidden fragments. Assert that the walk found more than 50 files, so a wrong working directory cannot pass vacuously.
  </action>
  <verify>
    <automated>JAVA_HOME="/c/Program Files/Java/jdk-23" mvn -q -f backend/pom.xml test -Dtest=AuditLogImutabilidadeTest</automated>
  </verify>
  <acceptance_criteria>
    - File exists and `grep -F "ClassPathScanningCandidateComponentProvider" backend/src/test/java/com/lexcv/repositories/AuditLogImutabilidadeTest.java` matches
    - `grep -F "buscarEventosRbac" backend/src/test/java/com/lexcv/repositories/AuditLogImutabilidadeTest.java` matches (pinned method set)
    - `JAVA_HOME="/c/Program Files/Java/jdk-23" mvn -q -f backend/pom.xml test -Dtest=AuditLogImutabilidadeTest` exits 0 with 6 tests run
    - Mutation check (executor runs once and reverts, and records the result in SUMMARY): temporarily changing the repository base back to `JpaRepository` makes Tests 1-3 fail
  </acceptance_criteria>
  <done>AUDT-04 gate green; proven to go red when the repository is widened.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| application code → t_audit_log | Any future code path in the backend could try to mutate or delete audit rows |
| operator → production DB | Manual migration 128 is run by a human against a specific database |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-128-01 | Tampering | AuditLogRepository | mitigate | Base narrowed to `Repository<AuditLog, Long>`: delete*/saveAll/saveAndFlush absent from the compiled API; pinned by AuditLogImutabilidadeTest Tests 1-3 |
| T-128-02 | Tampering | AuditLog entity (dirty-checking) | mitigate | `@Immutable`: Hibernate never emits UPDATE for a loaded row; asserted by Test 4 |
| T-128-03 | Repudiation / Tampering | Future write endpoint or native SQL over the log | mitigate | Test 5 (no mutating mapping path containing "audit") and Test 6 (no DELETE/UPDATE statement against t_audit_log in main sources) |
| T-128-04 | Information Disclosure | buscarEventosRbac | mitigate | `a.tenant_id = :tenantId` in both query and countQuery; the tenant comes only from the principal (Plan 07); SQL-level two-tenant IT in Plan 07 |
| T-128-05 | Tampering | Direct DB access (psql, DBA) | accept | Out of the application's control; a DB trigger was considered and rejected because ddl-auto would not create it on fresh installs (inconsistent guarantee) and Decision 2 asks for the smallest possible migration. AUDT-04 scopes the guarantee to "superfície da aplicação" |
| T-128-06 | Denial of Service | Migration 128 on a table with many rows | accept | `ADD COLUMN ... TEXT` without default is a metadata-only change in PostgreSQL 11+; idempotent |
| T-128-07 | Information Disclosure | detalhe column content | mitigate | Vocabulary restricts detalhe to User.nome, role names and permission keys (revised Decision 2); enforced at the only writer (Plan 02 tests assert no email key) |
</threat_model>

<verification>
- `JAVA_HOME="/c/Program Files/Java/jdk-23" mvn -q -f backend/pom.xml test -Dtest=AuditLogImutabilidadeTest` green
- `JAVA_HOME="/c/Program Files/Java/jdk-23" mvn -q -f backend/pom.xml test -Dtest=ParecerControllerEntregaProveniencaTest,ResourceControllerUploadDocumentoTest` green (existing audit writers unaffected)
</verification>

<success_criteria>
- Narrowed repository compiles against all existing callers without touching them
- Migration 128 exists, idempotent, inventoried consistently (7 of 17 re-runnable, 9 outstanding, Path B row 17)
- Structural gate green and shown to fail on widening
</success_criteria>

<output>
Create `.planning/phases/LEXCV-128-auditoria-de-atribui-es-de-pap-is/128-01-SUMMARY.md` when done
</output>
