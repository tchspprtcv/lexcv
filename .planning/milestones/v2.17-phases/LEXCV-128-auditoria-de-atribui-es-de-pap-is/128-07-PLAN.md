---
phase: 128-auditoria-de-atribui-es-de-pap-is
plan: 07
type: execute
wave: 3
depends_on: ["128-02"]
files_modified:
  - backend/src/main/java/com/lexcv/controllers/AuditoriaRbacController.java
  - backend/src/test/java/com/lexcv/controllers/AuditoriaRbacControllerTest.java
  - backend/src/test/java/com/lexcv/repositories/AuditLogRepositoryIT.java
autonomous: true
requirements: [AUDT-03, AUDT-04]

must_haves:
  truths:
    - "An office administrator with rbac:manage can page through their own office's RBAC audit events, newest first"
    - "They can filter by target user and by role (by id, so renamed roles still match)"
    - "They never see another office's events, and never see processo/parecer audit rows"
    - "A caller without rbac:manage (including one holding only users:manage) is refused"
    - "The endpoint exposes only GET"
  artifacts:
    - path: "backend/src/main/java/com/lexcv/controllers/AuditoriaRbacController.java"
      provides: "GET /api/v1/admin/rbac/auditoria"
      contains: "hasAuthority('rbac:manage')"
    - path: "backend/src/test/java/com/lexcv/controllers/AuditoriaRbacControllerTest.java"
      provides: "Gate proof via real @PreAuthorize proxy + two-tenant isolation at controller level"
      min_lines: 100
    - path: "backend/src/test/java/com/lexcv/repositories/AuditLogRepositoryIT.java"
      provides: "SQL-level two-tenant isolation and filter proof for buscarEventosRbac"
      min_lines: 80
  key_links:
    - from: "AuditoriaRbacController.listar"
      to: "AuditoriaRbacService.listar"
      via: "tenantId from UserPrincipal, never a request parameter"
      pattern: "principal.getTenantId\\(\\)"
---

<objective>
Expose the RBAC audit log for consultation (Decision 5): `GET /api/v1/admin/rbac/auditoria?utilizadorAlvoId&papelId&page&size`, tenant-scoped, gated by `hasAuthority('rbac:manage')`, paginated with the `NotificacaoController.listar` envelope. Prove AUDT-03 with a two-tenant test at the controller level (runs locally) and at the SQL level (Testcontainers IT, compile-only locally).

Purpose: AUDT-03, plus the read-only half of AUDT-04 (a dedicated class with only a GET handler).
Output: new controller, unit test, repository IT.
</objective>

<execution_context>
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/LEXCV-128-auditoria-de-atribui-es-de-pap-is/128-CONTEXT.md
@.planning/phases/LEXCV-128-auditoria-de-atribui-es-de-pap-is/128-PATTERNS.md
@.planning/phases/LEXCV-128-auditoria-de-atribui-es-de-pap-is/128-UI-SPEC.md
@.planning/phases/LEXCV-128-auditoria-de-atribui-es-de-pap-is/128-02-PLAN.md

<interfaces>
From Plan 02:
  Page<AuditoriaRbacEntradaDto> AuditoriaRbacService.listar(UUID tenantId, UUID utilizadorAlvoId, UUID papelId, Pageable pageable)
From Plan 01:
  Page<AuditLog> AuditLogRepository.buscarEventosRbac(UUID tenantId, String utilizadorAlvoId, String papelId, Pageable pageable)

Response contract (consumed by Plan 08 frontend), built inline like NotificacaoController.listar:
  { "content": AuditoriaRbacEntradaDto[], "totalElements": long, "totalPages": int, "page": int, "size": int }
  AuditoriaRbacEntradaDto JSON: id, timestamp (ISO), acao, categoria ("papel"|"atribuicao"), autorNome, alvoId, alvoNome,
    papelId, papelNome, nomeAntigo, nomeNovo, permissoesAdicionadas, permissoesRemovidas, motivo
Route: /api/v1/admin/rbac/auditoria. It does not collide with /api/v1/admin/rbac (AdminController) or /api/v1/admin/rbac/roles (OfficeRolesController).
Bounds: page >= 0, 1 <= size <= 100, else 400 {"message": "page deve ser >= 0 e size deve estar entre 1 e 100"} (verbatim from NotificacaoController).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: AuditoriaRbacController with gate, bounds and tenant-from-principal</name>
  <files>backend/src/main/java/com/lexcv/controllers/AuditoriaRbacController.java, backend/src/test/java/com/lexcv/controllers/AuditoriaRbacControllerTest.java</files>
  <read_first>
    - backend/src/main/java/com/lexcv/controllers/NotificacaoController.java (listar: bounds check, PageRequest.of, Map envelope)
    - backend/src/main/java/com/lexcv/controllers/OfficeRolesController.java (class-level gate rationale: "uma classe, uma autoridade")
    - backend/src/test/java/com/lexcv/controllers/OfficeRolesControllerTest.java (novoProxyComMethodSecurity, autenticarComoPrincipalDoTenant, OUTRO_TENANT_ID: real @PreAuthorize proxy technique)
    - backend/src/main/java/com/lexcv/services/AuditoriaRbacService.java (Plan 02)
  </read_first>
  <behavior>
    - Real @PreAuthorize proxy: a principal with rbac:manage → 200; a principal with only users:manage → AccessDeniedException; a principal with no authorities → AccessDeniedException
    - Principal of TENANT_A → service.listar called with TENANT_A; never called with OUTRO_TENANT_ID (verify never). No request parameter can carry a tenant (the handler has no tenant parameter; asserted by reflection over the handler's parameters, which contain no parameter named tenantId or tenant)
    - utilizadorAlvoId and papelId are passed through to the service unchanged (UUID or null)
    - page=-1 → 400; size=0 → 400; size=101 → 400; the service is not called in these cases
    - Default page=0, size=20 → PageRequest.of(0, 20) (captured Pageable)
    - Response body map has exactly the keys content, totalElements, totalPages, page, size
    - Reflection: the class declares only @GetMapping handlers (no Post/Put/Patch/Delete mapping)
  </behavior>
  <action>
Create `AuditoriaRbacController` in `com.lexcv.controllers`: `@RestController`, `@RequestMapping("/api/v1/admin/rbac/auditoria")`, class-level `@PreAuthorize("hasAuthority('rbac:manage')")`, `@RequiredArgsConstructor`, one dependency `AuditoriaRbacService`.

Class Javadoc (Portuguese):
- Decision 5: the same authority that governs RBAC writes;
- a class of its own, for the same reason OfficeRolesController is one: a class-level gate cannot be forgotten on a new handler, whereas AdminController's class gate is users:manage;
- the read-only guarantee (AUDT-04): this class has and must only have GET, pinned by AuditLogImutabilidadeTest and by this class's own test;
- the tenant always comes from the principal (AUDT-03);
- RBAC events only; processo events keep their own view at GET /processos/{id}/audit;
- the role filter matches by id so renamed roles still match; the target-user filter matches entidade_id of atribuicao_papel events.

Single handler `@GetMapping("")` `listar(@RequestParam(required = false) UUID utilizadorAlvoId, @RequestParam(required = false) UUID papelId, @RequestParam(defaultValue = "0") int page, @RequestParam(defaultValue = "20") int size)`. Copy the bounds check and message verbatim from `NotificacaoController.listar`. Resolve `UserPrincipal` from `SecurityContextHolder`. Call `auditoriaRbacService.listar(principal.getTenantId(), utilizadorAlvoId, papelId, PageRequest.of(page, size))`. Return `ResponseEntity.ok(Map.of("content", ..., "totalElements", ..., "totalPages", ..., "page", ..., "size", ...))`. A malformed UUID parameter is rejected by Spring's type conversion with 400 before the handler runs; no custom handling is needed.

Test `AuditoriaRbacControllerTest`: build the real method-security proxy the same way `OfficeRolesControllerTest.novoProxyComMethodSecurity` does, and a principal helper for two tenants. Stub `service.listar` with a `PageImpl` holding one DTO. One test per behavior bullet.
  </action>
  <verify>
    <automated>JAVA_HOME="/c/Program Files/Java/jdk-23" mvn -q -f backend/pom.xml test -Dtest=AuditoriaRbacControllerTest,AuditLogImutabilidadeTest</automated>
  </verify>
  <acceptance_criteria>
    - `grep -F "@PreAuthorize(\"hasAuthority('rbac:manage')\")" backend/src/main/java/com/lexcv/controllers/AuditoriaRbacController.java` matches
    - `grep -F "/api/v1/admin/rbac/auditoria" backend/src/main/java/com/lexcv/controllers/AuditoriaRbacController.java` matches
    - `grep -F "principal.getTenantId()" backend/src/main/java/com/lexcv/controllers/AuditoriaRbacController.java` matches
    - `grep -F "PostMapping" backend/src/main/java/com/lexcv/controllers/AuditoriaRbacController.java`, and the same for `PutMapping`, `PatchMapping`, `DeleteMapping`: each returns nothing
    - `JAVA_HOME="/c/Program Files/Java/jdk-23" mvn -q -f backend/pom.xml test -Dtest=AuditoriaRbacControllerTest,AuditLogImutabilidadeTest` exits 0 (the gate still passes with the new controller on the classpath)
  </acceptance_criteria>
  <done>Endpoint live behind rbac:manage, tenant from principal, bounds enforced, GET-only; proven by a real proxy and two-tenant verification.</done>
</task>

<task type="auto">
  <name>Task 2: SQL-level two-tenant isolation and filter IT for buscarEventosRbac</name>
  <files>backend/src/test/java/com/lexcv/repositories/AuditLogRepositoryIT.java</files>
  <read_first>
    - backend/src/test/java/com/lexcv/repositories/NotificacaoRepositoryIT.java (Testcontainers @DataJpaTest idiom with the paginated native query)
    - backend/src/test/java/com/lexcv/repositories/UserRepositoryContagemPapeisIT.java (container declaration style)
    - backend/src/main/java/com/lexcv/repositories/AuditLogRepository.java (Plan 01)
  </read_first>
  <action>
Create `AuditLogRepositoryIT` following the NotificacaoRepositoryIT idiom exactly: `@DataJpaTest`, `@AutoConfigureTestDatabase(replace = NONE)`, `@Testcontainers`, a PostgreSQL container with `@ServiceConnection`. Insert rows through `auditLogRepository.save` with hand-written detalhe JSON strings:
- tenant A: a papel_escritorio papel_criar for role R1; a papel_atribuir (atribuicao_papel) for target U1 and role R1; a papel_retirar for target U2 and role R2; one processo event (acao transicao_estado, entidade_tipo processo, detalhe null);
- tenant B: a papel_atribuir for target U1 and role R1 (same ids as tenant A, to prove isolation is by tenant and not by id).

Assertions (@Test each):
(1) `buscarEventosRbac(A, null, null, PageRequest.of(0, 20))` returns exactly tenant A's 3 RBAC rows, newest first, with totalElements 3 (the processo row and tenant B's row are excluded);
(2) the target filter U1 on A returns only A's papel_atribuir;
(3) the role filter R1 on A returns the papel_criar and the papel_atribuir (both the entidade_id match and the detalhe papelId match);
(4) tenant B with no filters returns exactly its 1 row;
(5) page size 2 on A gives totalPages 2.

Class Javadoc: this is the SQL-level AUDT-03 proof. It cannot run locally (Docker npipe blocker) and runs wherever Testcontainers is available; locally only `test-compile` is gated.
  </action>
  <verify>
    <automated>JAVA_HOME="/c/Program Files/Java/jdk-23" mvn -q -f backend/pom.xml test-compile</automated>
  </verify>
  <acceptance_criteria>
    - File exists; `grep -F "@Testcontainers" backend/src/test/java/com/lexcv/repositories/AuditLogRepositoryIT.java` matches
    - `grep -F "buscarEventosRbac" backend/src/test/java/com/lexcv/repositories/AuditLogRepositoryIT.java` matches at least 5 times (`grep -c -F`)
    - `JAVA_HOME="/c/Program Files/Java/jdk-23" mvn -q -f backend/pom.xml test-compile` exits 0
    - SUMMARY states explicitly that the IT was compiled but not executed locally
  </acceptance_criteria>
  <done>SQL-level isolation and filter proof exists and compiles; execution deferred to a Docker-capable environment, stated honestly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → GET /api/v1/admin/rbac/auditoria | Untrusted query params (utilizadorAlvoId, papelId, page, size) |
| tenant A admin → tenant B data | Primary isolation boundary (AUDT-03) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-128-32 | Information Disclosure | cross-tenant read | mitigate | Tenant only from UserPrincipal; there is no tenant parameter (reflection test); SQL `a.tenant_id = :tenantId` in query and count (IT) |
| T-128-33 | Elevation of Privilege | gate | mitigate | Class-level hasAuthority('rbac:manage'); proven by a real AuthorizationManagerBeforeMethodInterceptor proxy that users:manage alone is denied |
| T-128-34 | Information Disclosure | enumeration via filters | accept | Filters only narrow inside the caller's own tenant; a foreign user or role id simply matches nothing (no 403/404 oracle) |
| T-128-35 | Denial of Service | unbounded page size | mitigate | size clamped to 1..100 (400 otherwise), same as NotificacaoController |
| T-128-36 | Tampering | write surface on the log | mitigate | GET-only class; AuditLogImutabilidadeTest Test 5 scans all controllers |
| T-128-37 | Injection | native query params | mitigate | Bound parameters only (Spring Data @Param); UUIDs parsed by Spring before reaching SQL |
</threat_model>

<verification>
`JAVA_HOME="/c/Program Files/Java/jdk-23" mvn -q -f backend/pom.xml test -Dtest=AuditoriaRbacControllerTest,AuditoriaRbacServiceTest,AuditLogImutabilidadeTest` exits 0 and `test-compile` exits 0.
</verification>

<success_criteria>
- AUDT-03 proven at the controller level (local) and the SQL level (IT)
- Response envelope matches the contract Plan 08 consumes
</success_criteria>

<output>
Create `.planning/phases/LEXCV-128-auditoria-de-atribui-es-de-pap-is/128-07-SUMMARY.md` when done
</output>
