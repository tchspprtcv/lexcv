---
phase: 126-migracao-de-papeis-existentes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/main/java/com/lexcv/models/User.java
  - backend/src/test/java/com/lexcv/models/UserAssociacaoTenantRoleTest.java
  - backend/migrations/127-add-user-tenant-role-table.sql
  - backend/migrations/README.md
  - DEPLOYMENT.md
autonomous: true
requirements: [MIGR-01, MIGR-03]

must_haves:
  truths:
    - "Um utilizador pode estar associado a papéis de escritório (TenantRole) sem perder a sua associação a papéis globais (Role)"
    - "A associação nova vive numa tabela própria, t_user_tenant_role — t_user_role continua a existir e povoada (migração reversível)"
    - "Existe um script manual numerado 127, idempotente, que cria a tabela nova numa base de dados a correr ddl-auto=validate"
    - "backend/migrations/README.md cataloga o script 127 nas quatro localizações que exige, com as contagens de prosa avançadas e não obsoletas"
    - "A secção de arranque em duas fases do DEPLOYMENT.md diz onde o 127 entra e o que significa um arranque abortado por deriva detectada"
  artifacts:
    - path: "backend/src/main/java/com/lexcv/models/User.java"
      provides: "Associação User -> TenantRole (t_user_tenant_role), EAGER, coexistindo com roles"
      contains: "t_user_tenant_role"
    - path: "backend/migrations/127-add-user-tenant-role-table.sql"
      provides: "Script manual idempotente que cria t_user_tenant_role"
      contains: "IF NOT EXISTS"
    - path: "backend/src/test/java/com/lexcv/models/UserAssociacaoTenantRoleTest.java"
      provides: "Prova estrutural de que a associação nova não reutiliza t_user_role e não substitui roles"
    - path: "backend/migrations/README.md"
      provides: "Inventário do script 127 e contagens de prosa avançadas"
      contains: "127-add-user-tenant-role-table.sql"
    - path: "DEPLOYMENT.md"
      provides: "Lugar do 127 no arranque em duas fases e leitura de um arranque abortado por deriva"
      contains: "127-add-user-tenant-role-table.sql"
  key_links:
    - from: "backend/src/main/java/com/lexcv/models/User.java"
      to: "t_user_tenant_role"
      via: "@ManyToMany + @JoinTable"
      pattern: "name = \"t_user_tenant_role\""
    - from: "backend/migrations/127-add-user-tenant-role-table.sql"
      to: "t_user_tenant_role"
      via: "CREATE TABLE IF NOT EXISTS"
      pattern: "CREATE TABLE IF NOT EXISTS t_user_tenant_role"
---

<objective>
Criar o único esquema novo de que esta fase precisa: a associação entre um utilizador e os papéis do seu próprio escritório, numa tabela **nova** que coexiste com `t_user_role` em vez de a substituir. Mais o script manual `127` e a documentação de deploy (MIGR-03).

Purpose: a Decisão 4 do `126-CONTEXT.md` é que a migração seja reversível — `t_user_role.role_id` mantém-se povoado depois da conversão, e reverter é voltar a apontar as leituras, não restaurar uma cópia de segurança. Isso obriga a uma tabela nova, não a uma alteração de `t_user_role`. Este é o único plano da fase que toca no esquema.

Output: 1 campo novo em `User`, 1 teste estrutural, 1 script de migração `127`, `README.md` actualizado em 4 localizações, `DEPLOYMENT.md` numa.

**Nada neste plano altera resolução de autoridade.** `JwtAuthenticationFilter`, `UserPrincipal`, `AuthController`, `AdminController`, `ParecerController` e `ResourceController` não são tocados. O campo novo nasce vazio e DORMENTE — o cutover é dos planos 04 e 05.
</objective>

<execution_context>
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/LEXCV-126-migra-o-de-pap-is-existentes/126-CONTEXT.md
@.planning/phases/LEXCV-126-migra-o-de-pap-is-existentes/126-PATTERNS.md
@.planning/phases/LEXCV-125-moldes-da-plataforma-e-provisionamento/125-01-SUMMARY.md
@CLAUDE.md
</context>

<build_environment>
O `java`/`mvn` por omissão no PATH resolvem para **JDK 26**, que NÃO compila este projeto (Lombok morre com `ExceptionInInitializerError: TypeTag :: UNKNOWN`). Todo o comando Maven deste plano corre a partir de `backend/` com `JAVA_HOME="/c/Program Files/Java/jdk-23"` prefixado, em shell **Bash** (não PowerShell). Sem isso, qualquer critério de aceitação Maven é inválido.
</build_environment>

<interfaces>
<!-- Contratos já extraídos. O executor não precisa de explorar o codebase para nenhum destes. -->

**`backend/src/main/java/com/lexcv/models/User.java` (estado actual, 79 linhas).** `@Entity @Table(name = "t_user")`, `@Getter @Setter @NoArgsConstructor @AllArgsConstructor @Builder`. A associação cuja forma se copia está nas linhas 54-61:

- `@ManyToMany(fetch = FetchType.EAGER)`
- `@JoinTable(name = "t_user_role", joinColumns = @JoinColumn(name = "user_id"), inverseJoinColumns = @JoinColumn(name = "role_id"))`
- `@Builder.Default`
- `private Set<Role> roles = new HashSet<>();`

Logo a seguir, linhas 63-67, a colecção de permissões directas — `@ElementCollection(fetch = FetchType.EAGER)`, `@CollectionTable(name = "t_user_permission", joinColumns = @JoinColumn(name = "user_id"))`, `@Column(name = "permission")`, `@Builder.Default private Set<String> permissions = new HashSet<>();`. **Não tocar**: é a parcela 2 da união de permissões efectivas.

**`backend/src/main/java/com/lexcv/models/TenantRole.java` (criado na Phase 125).** `@Entity @Table(name = "t_tenant_role", uniqueConstraints = @UniqueConstraint(columnNames = {"tenant_id", "nome"}))`. Campos: `UUID id` (`GenerationType.UUID`), `UUID tenantId` (`@Column(name = "tenant_id", nullable = false)`), `String nome`, `Integer moldeId` (`@Column(name = "molde_id")`, proveniência histórica, nunca navegável), `Boolean sistema`, `Set<Permission> permissions` (`@ManyToMany(EAGER)` sobre `t_tenant_role_permission`).

**`backend/migrations/126-add-tenant-role-tables.sql` (55 linhas, Phase 125) — o análogo exacto de header.** Estrutura: (a) linha de título `-- Phase NNN (REQ-ID): <o que faz>`; (b) bloco `IMPORTANT: This is a REQUIRED manual production migration script...` nomeando o ficheiro Java exacto; (c) parágrafo `Why:` ligando `ddl-auto: update` a `SPRING_JPA_HIBERNATE_DDL_AUTO=validate` e à ausência de migration runner; (d) parágrafo do que o script deliberadamente NÃO faz; (e) declaração explícita de idempotência. SQL: `ALTER TABLE ... ADD COLUMN IF NOT EXISTS`, `CREATE TABLE IF NOT EXISTS`, `CREATE INDEX IF NOT EXISTS`. A tabela de junção `t_tenant_role_permission` desse ficheiro é a forma exacta a copiar para a tabela nova deste plano (duas colunas, duas `REFERENCES`, `PRIMARY KEY` composta).

**Armadilha documentada em `125-01-SUMMARY.md` (Deviation 2), que este plano herda:** o header do `126` teve de ser reescrito porque a prosa continha as strings literais que os gates de `grep -c` contavam (`FOREIGN KEY`, `IF NOT EXISTS`), inflacionando as contagens. Os critérios deste plano usam por isso `grep -v '^--' | grep -c`, contando só instruções SQL, nunca prosa. Escreve a prosa que fizer sentido — os gates estão desenhados para não serem auto-invalidados por ela.

**`backend/migrations/README.md` — as 4 localizações que uma migração nova obriga a tocar** (a própria secção `## Adding a new migration` diz: "Add a row to the tables in this file in the same commit. The inventory lives here and nowhere else."):

1. **`## Path A — fresh install`**, lista de bullets "skip on a fresh install". O bullet do `126` fecha a secção e diz `redundant, but unlike the group above it will not error`. O `127` pertence ao mesmo balde (redundante mas inofensivo), **não** ao balde `FORBIDDEN` do `125`.
2. **`## Path B — existing database`**, tabela `| Order | File | What it does | Re-runnable? |`. Última linha existente: `| 15 | \`126-add-tenant-role-tables.sql\` | ... |`. Acrescentar a linha `16` imediatamente depois, em ordem numérica estritamente ascendente.
3. **`## Re-run safety`**. A prosa diz hoje `Only **5 of the 15** scripts tolerate being run twice`, `The other **10 must not be re-run**` e `Nine of them fail loudly (duplicate table / column / constraint / index)`. Com o `127` (re-executável, só `CREATE TABLE IF NOT EXISTS`) **apenas a primeira muda**: passa a `6 of 16`. O `127` entra no grupo TOLERANTE, logo o grupo não-tolerante continua com `10 must not be re-run` e `Nine of them fail loudly` -- acrescentar um script seguro não pode encolher o conjunto dos inseguros (16 - 6 = 10, igual a 15 - 5 = 10). Acrescentar também uma linha à tabela `| Safe to re-run | Why |`.
4. **`## Known execution status`**. Prosa `**7 scripts are outstanding**` → `**8 scripts are outstanding**`; acrescentar `| \`127-add-user-tenant-role-table.sql\` | Pending — new in this phase. |` imediatamente depois da linha do `126`.

**`DEPLOYMENT.md` — secção `## Database Schema — Two-Stage Boot`** (começa na linha 71). Sub-secções existentes, nesta ordem: `### Where these two settings actually live`, `### Stage 1 — First boot: create the schema, seed the data`, `### Stage 2 — Steady state: validate, never mutate`, `#### Confirm the setting took effect — do this before trusting the boot`. Factos dessa secção que a documentação nova não pode contradizer: nenhum ficheiro compose define `SPRING_PROFILES_ACTIVE`, logo `application-prod.yml` nunca carrega; `SPRING_JPA_HIBERNATE_DDL_AUTO` em `.env` é hoje a única coisa que põe uma instalação em `validate`; `DatabaseSeeder.seedRbac()` corre incondicionalmente na primeira instrução de `run()`, antes do gate `seedEnabled`, logo **também em produção com `SEED_ENABLED=false`**.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Associação User -> TenantRole em tabela nova, coexistindo com t_user_role</name>
  <read_first>
    - `backend/src/main/java/com/lexcv/models/User.java` (completo, 79 linhas — o idioma a copiar está nas linhas 54-61)
    - `backend/src/main/java/com/lexcv/models/TenantRole.java` (completo — o tipo do outro lado da associação)
    - `.planning/phases/LEXCV-126-migra-o-de-pap-is-existentes/126-CONTEXT.md` (Decisão 4 — porque a tabela é nova em vez de `t_user_role` ser reapontada)
    - `backend/src/test/java/com/lexcv/seed/DatabaseSeederInstanciabilidadeMoldesTest.java` (o precedente de asserção estrutural por reflexão sobre campos declarados, estabelecido pela Phase 125 — o 5.º caso desse ficheiro é o modelo)
  </read_first>
  <files>backend/src/main/java/com/lexcv/models/User.java, backend/src/test/java/com/lexcv/models/UserAssociacaoTenantRoleTest.java</files>
  <action>
Acrescentar a `User` um campo `tenantRoles` de tipo `Set<TenantRole>`, com exactamente o mesmo idioma da associação `roles` já existente: `@ManyToMany(fetch = FetchType.EAGER)`, `@JoinTable` com `name = "t_user_tenant_role"`, `joinColumns = @JoinColumn(name = "user_id")`, `inverseJoinColumns = @JoinColumn(name = "tenant_role_id")`, `@Builder.Default` e inicialização a `new HashSet&lt;&gt;()`.

`FetchType.EAGER` não é conveniência e o comentário de código tem de dizer porquê: `JwtAuthenticationFilter` faz deliberadamente UMA query por PK por pedido autenticado, sem qualquer memorização entre pedidos (ver o bloco de comentário em `JwtAuthenticationFilter:50-58`, que explica que o requisito é "imediato", não "no próximo login"). Se `tenantRoles` fosse LAZY, o plano 04 seria forçado a acrescentar uma query extra por pedido ou a introduzir memorização — exactamente o que aquele comentário proíbe. Com EAGER, o `userRepository.findById` que já existe traz a associação.

O campo `roles` (54-61) e a colecção `permissions` (63-67) ficam **intocados**, byte a byte. Não substituir, não deprecar, não anotar. Um doc-comment no campo novo tem de registar que `t_user_role` continua povoado por desenho (D-04: migração reversível — reverter é voltar a apontar as leituras, não restaurar backup) e que a remoção de `t_user_role.role_id` é explicitamente diferida para depois do marco fechar.

Criar `backend/src/test/java/com/lexcv/models/UserAssociacaoTenantRoleTest.java` — teste estrutural puro (JUnit 5, sem Mockito, sem contexto Spring), por reflexão sobre `User.class`, provando quatro factos que uma regressão futura tornaria silenciosa:
(a) existe um campo declarado `tenantRoles` cujo `@JoinTable.name()` é `t_user_tenant_role`, com `joinColumns` a `user_id` e `inverseJoinColumns` a `tenant_role_id`;
(b) o campo `roles` continua a existir e o seu `@JoinTable.name()` continua a ser `t_user_role` — esta é a asserção de reversibilidade, a que falha se alguém "simplificar" a associação antiga;
(c) as duas associações não partilham nome de `@JoinTable`;
(d) `tenantRoles` é `FetchType.EAGER` — a asserção que protege a invariante de "uma query por pedido" do filtro.
  </action>
  <verify>
    <automated>cd backend &amp;&amp; JAVA_HOME="/c/Program Files/Java/jdk-23" mvn -q test -Dtest=UserAssociacaoTenantRoleTest</automated>
  </verify>
  <acceptance_criteria>
    - `cd backend &amp;&amp; JAVA_HOME="/c/Program Files/Java/jdk-23" mvn test -Dtest=UserAssociacaoTenantRoleTest` termina em `BUILD SUCCESS`, 4 testes, 0 falhas.
    - `grep -c 'name = "t_user_tenant_role"' backend/src/main/java/com/lexcv/models/User.java` devolve `1`.
    - `grep -c 'name = "t_user_role"' backend/src/main/java/com/lexcv/models/User.java` devolve `1` — a associação antiga não foi removida nem renomeada.
    - `grep -c 'private Set<Role> roles = new HashSet<>();' backend/src/main/java/com/lexcv/models/User.java` devolve `1`.
    - `grep -c 'private Set<String> permissions = new HashSet<>();' backend/src/main/java/com/lexcv/models/User.java` devolve `1` — a parcela 2 da união intacta.
    - `git diff --name-only` não inclui `JwtAuthenticationFilter.java`, `UserPrincipal.java`, `AuthController.java`, `AdminController.java`, `ParecerController.java` nem `ResourceController.java`.
    - `cd backend &amp;&amp; JAVA_HOME="/c/Program Files/Java/jdk-23" mvn test` mantém a suite completa verde (baseline herdado da Phase 125: 209 testes; esperado 213 com os 4 novos, 0 falhas).
  </acceptance_criteria>
  <done>`User` tem duas associações de papel a coexistir — a nova sobre `t_user_tenant_role`, a antiga sobre `t_user_role` — com um teste estrutural que falha se a antiga for removida ou a nova deixar de ser EAGER. Zero ficheiros de resolução de autoridade tocados.</done>
</task>

<task type="auto">
  <name>Task 2: Script manual 127 e inventário do README nas quatro localizações</name>
  <read_first>
    - `backend/migrations/126-add-tenant-role-tables.sql` (completo — o análogo de header e de disciplina de idempotência)
    - `backend/migrations/README.md` (completo — as 4 secções a tocar estão enumeradas no bloco `<interfaces>` deste plano)
    - `backend/src/main/java/com/lexcv/models/User.java` (o estado depois da Task 1 — o script tem de corresponder ao mapeamento que a Task 1 escreveu)
    - `.planning/phases/LEXCV-125-moldes-da-plataforma-e-provisionamento/125-01-SUMMARY.md` (secção "Deviations from Plan" — a armadilha prosa-vs-gate, e a nota sobre a crase na célula da tabela do README)
  </read_first>
  <files>backend/migrations/127-add-user-tenant-role-table.sql, backend/migrations/README.md</files>
  <action>
Criar `backend/migrations/127-add-user-tenant-role-table.sql`. Uma única tabela de junção: `t_user_tenant_role` com `user_id UUID NOT NULL REFERENCES t_user(id)`, `tenant_role_id UUID NOT NULL REFERENCES t_tenant_role(id)` e `PRIMARY KEY (user_id, tenant_role_id)` — a mesma forma que `t_tenant_role_permission` tem no script `126`. Aqui as duas foreign keys são legítimas e desejadas, ao contrário de `molde_id` (que o `126` deixou deliberadamente sem constraint por ser proveniência histórica): ambos os lados desta associação são linhas vivas que a aplicação navega.

Header seguindo a estrutura do `126`: título `-- Phase 126 (MIGR-01/MIGR-03): ...`; bloco `IMPORTANT` nomeando `backend/src/main/java/com/lexcv/models/User.java` e o campo `tenantRoles` como o mapeamento que exige a tabela; parágrafo `Why:` repetindo a razão real (`ddl-auto: update` cria-a sozinho em dev; uma base a correr `SPRING_JPA_HIBERNATE_DDL_AUTO=validate` recusa arrancar sem ela; não há migration runner neste repositório).

**Dois parágrafos que este header tem de ter e que o `126` não tinha, porque são o que distingue esta migração:**

(a) **Deliberadamente SEM backfill de dados.** A conversão — instanciar moldes em cada tenant existente e apontar cada utilizador ao papel de escritório equivalente — não é feita em SQL, é feita em Java por um serviço convergente que corre no arranque seguinte, o mesmo idioma que o script `124` já usa para `DatabaseSeeder.seedRbac()` povoar `rotulo`/`descricao`/`modulo`/`ordem`. A razão é reutilização, não preferência: `SetupService.instanciarMoldes` já é o mecanismo provado de instanciação (Phase 125) e reimplementá-lo em SQL criaria uma segunda definição da mesma regra, capaz de divergir. O header deve nomear a classe que fará a conversão (`MigracaoPapeisEscritorioService`, plano 03) para que um operador que leia só este ficheiro saiba onde a conversão vive.

(b) **`t_user_role` não é tocado** por este script nem por essa conversão. A associação nova coexiste com a antiga; reverter esta migração é voltar a apontar as leituras, não restaurar uma cópia de segurança. Não existe `DROP COLUMN`, `DELETE` nem `TRUNCATE` em nenhum ponto desta fase.

Declaração de idempotência explícita: `CREATE TABLE IF NOT EXISTS` — seguro a correr duas vezes.

Actualizar `backend/migrations/README.md` nas **quatro** localizações enumeradas no bloco `<interfaces>`. As contagens de prosa são a parte que o `126-CONTEXT.md` avisa ficar obsoleta: `5 of the 15` → `6 of 16`; `**7 scripts are outstanding**` → `**8 scripts are outstanding**`. **Deixar intactas** `The other **10 must not be re-run**` e `Nine of them fail loudly` -- o `127` é idempotente e entra no grupo tolerante, pelo que o conjunto não-tolerante não muda de tamanho nem de composição. Acrescentar também a linha do `127` à tabela `Safe to re-run`.
  </action>
  <verify>
    <automated>cd backend &amp;&amp; test -f migrations/127-add-user-tenant-role-table.sql &amp;&amp; test "$(grep -v '^--' migrations/127-add-user-tenant-role-table.sql | grep -c 'IF NOT EXISTS')" = "1" &amp;&amp; test "$(grep -v '^--' migrations/127-add-user-tenant-role-table.sql | grep -cE 'DROP |DELETE |TRUNCATE ')" = "0" &amp;&amp; grep -q '127-add-user-tenant-role-table.sql' migrations/README.md &amp;&amp; grep -q '6 of 16' migrations/README.md &amp;&amp; grep -q '8 scripts are outstanding' migrations/README.md &amp;&amp; echo GATES_OK</automated>
  </verify>
  <acceptance_criteria>
    - O comando de `<verify>` imprime `GATES_OK`.
    - `grep -v '^--' backend/migrations/127-add-user-tenant-role-table.sql | grep -c 'CREATE TABLE IF NOT EXISTS t_user_tenant_role'` devolve `1`.
    - `grep -v '^--' backend/migrations/127-add-user-tenant-role-table.sql | grep -c 'REFERENCES'` devolve `2` (uma FK para `t_user`, uma para `t_tenant_role`).
    - `grep -v '^--' backend/migrations/127-add-user-tenant-role-table.sql | grep -ci 't_user_role'` devolve `0` — nenhuma instrução SQL toca na tabela antiga. (Contagem com `grep -v '^--'` de propósito: a prosa do header FALA de `t_user_role`, e deve.)
    - `ls backend/migrations/ | grep -c '^127'` devolve `1` — nenhum número duplicado.
    - `grep -n '126-add-tenant-role-tables.sql\` |' backend/migrations/README.md` e `grep -n '127-add-user-tenant-role-table.sql\` |' backend/migrations/README.md` mostram o `127` numa linha de número **superior** à do `126` na tabela Path B — ordem numérica ascendente preservada.
    - `grep -c '5 of the 15' backend/migrations/README.md` devolve `0` — a contagem obsoleta desapareceu.
    - `grep -c '7 scripts are outstanding' backend/migrations/README.md` devolve `0`.
    - `grep -c 'Nine of them fail loudly' backend/migrations/README.md` devolve `1` -- esta linha descreve o conjunto NÃO-tolerante, que o `127` não altera; alterá-la seria introduzir um número falso.
    - `grep -c '10 must not be re-run' backend/migrations/README.md` devolve `1` -- idem.
    - `grep -c '127-add-user-tenant-role-table.sql' backend/migrations/README.md` devolve `>= 4` (Path A, Path B, Re-run safety, Known execution status).
  </acceptance_criteria>
  <done>Script `127` existe, é idempotente, não contém nenhuma instrução destrutiva, declara explicitamente que a conversão de dados é feita em Java e que `t_user_role` não é tocado. O README cataloga-o nas 4 localizações e nenhuma contagem de prosa ficou obsoleta.</done>
</task>

<task type="auto">
  <name>Task 3: MIGR-03 — lugar do 127 no arranque em duas fases do DEPLOYMENT.md</name>
  <read_first>
    - `DEPLOYMENT.md`, secção `## Database Schema — Two-Stage Boot` (linhas 71 a ~150) e o ponto 4 da checklist pré-deploy (~linha 282)
    - `backend/migrations/README.md` (o estado depois da Task 2 — o `DEPLOYMENT.md` refere-se a ele como checklist autoritativa e não pode divergir dele)
    - `backend/migrations/127-add-user-tenant-role-table.sql` (o estado depois da Task 2)
    - `.planning/phases/LEXCV-126-migra-o-de-pap-is-existentes/126-CONTEXT.md` (Decisão 1 e a secção `<specifics>` — o que a verificação de deriva zero faz e porque falha alto)
  </read_first>
  <files>DEPLOYMENT.md</files>
  <action>
Acrescentar à secção `## Database Schema — Two-Stage Boot` uma sub-secção nova, `### Phase 126 — conversão de papéis de escritório e verificação de deriva zero`, colocada **depois** de `#### Confirm the setting took effect` e antes do que vier a seguir nessa secção. Tem de responder a quatro perguntas que um operador faz às 3 da manhã, e nada mais:

1. **O que correr e quando.** `backend/migrations/127-add-user-tenant-role-table.sql` corre **antes** de a instalação apanhar o deploy que introduz o campo `User.tenantRoles`, pela mesma razão de todos os outros scripts desta pasta: uma instalação em `validate` recusa arrancar sem a tabela. Numa instalação em stage 1 (`ddl-auto=update`) o script é redundante mas inofensivo.

2. **Onde a conversão de dados acontece.** Não no script. No arranque seguinte, em Java, por um serviço convergente e idempotente, na mesma categoria de trabalho que `DatabaseSeeder.seedRbac()` — que, como esta secção já documenta noutro ponto, corre incondicionalmente **também em produção com `SEED_ENABLED=false`**. Consequência prática a escrever explicitamente: **um operador não "corre a migração" de papéis; ela corre-se a si mesma no primeiro arranque depois do deploy.** Correr o mesmo arranque duas vezes não duplica nada.

3. **O que significa um arranque que falha depois deste deploy.** Esta é a parte que só existe nesta fase: a verificação de deriva zero (MIGR-02) compara, utilizador a utilizador, o conjunto de permissões efectivas antes e depois da conversão e **aborta o arranque** se divergirem, nomeando no log o email do utilizador e as permissões em divergência. Um backend que não arranca depois deste deploy, com essa mensagem no log, não é uma avaria de infraestrutura — é a rede de segurança a fazer o seu trabalho, e o estado correcto é **não forçar o arranque**: a conversão corre dentro de uma transacção, por isso uma falha não deixa dados meio-convertidos.

4. **Como reverter.** `t_user_role` continua povoado (nada nesta fase o apaga ou altera). Reverter é fazer rollback do deploy do código, que volta a ler papéis globais; as linhas de `t_user_tenant_role` ficam órfãs e inofensivas. **Não** é necessário restaurar cópia de segurança, e **não** existe nenhum `DROP COLUMN` a desfazer.

Não duplicar a tabela de inventário do README dentro do `DEPLOYMENT.md` — este remete para aquele como checklist autoritativa, e é essa a divisão de responsabilidade que já existe no ficheiro. Uma frase no ponto 4 da checklist pré-deploy (~linha 282, "You know which stage the target install is in") a apontar para a sub-secção nova é suficiente; não reescrever essa checklist.
  </action>
  <verify>
    <automated>grep -q 'Phase 126' DEPLOYMENT.md &amp;&amp; grep -q '127-add-user-tenant-role-table.sql' DEPLOYMENT.md &amp;&amp; grep -qi 'deriva' DEPLOYMENT.md &amp;&amp; grep -q 't_user_role' DEPLOYMENT.md &amp;&amp; echo DOCS_OK</automated>
  </verify>
  <acceptance_criteria>
    - O comando de `<verify>` imprime `DOCS_OK`.
    - `grep -n 'Phase 126' DEPLOYMENT.md` devolve uma linha cujo número está **entre** a linha de `## Database Schema — Two-Stage Boot` e a do cabeçalho `##` seguinte — a sub-secção está de facto dentro da secção de arranque em duas fases, não colada no fim do ficheiro. (Obter os dois limites com `grep -n '^## ' DEPLOYMENT.md`.)
    - `grep -c 'SEED_ENABLED=false' DEPLOYMENT.md` é `>= 1` e a sub-secção nova refere esse facto ao explicar que a conversão corre também em produção.
    - A sub-secção nova contém, em texto, as quatro respostas: script/quando, conversão-em-Java-no-arranque, arranque-abortado-é-a-rede-de-segurança, e reversão por rollback de código sem restauro de backup. Verificável por leitura: `sed -n '/### Phase 126/,/^#\{1,3\} /p' DEPLOYMENT.md` mostra os quatro pontos.
    - `grep -c 'DROP COLUMN' DEPLOYMENT.md` não aumentou em relação ao baseline (`git diff DEPLOYMENT.md | grep -c '^+.*DROP COLUMN'` devolve `0`, a não ser na frase que diz explicitamente que não existe nenhum a desfazer).
    - `git diff --stat DEPLOYMENT.md backend/migrations/README.md` mostra alterações nos dois ficheiros no mesmo commit.
  </acceptance_criteria>
  <done>Um operador que leia apenas o `DEPLOYMENT.md` sabe quando correr o `127`, que a conversão de dados corre sozinha no arranque seguinte inclusive em produção, o que significa um arranque abortado por deriva detectada, e que reverter é rollback de código sem restauro de backup.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operador → base de dados de produção | execução manual de SQL, sem migration runner, sem revisão automática; sete scripts já pendentes |
| mapeamento JPA → esquema físico | `ddl-auto` em dev cria o que o script cria em prod; divergência entre os dois é silenciosa em dev e fatal em `validate` |
| documento de deploy → operador | prosa obsoleta num checklist é indistinguível de prosa correcta no momento em que é lida |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-126-01 | Tampering | `t_user_role` (dados de autorização existentes) | mitigate | Nenhum `DROP`/`DELETE`/`TRUNCATE`/`ALTER` sobre `t_user_role` em nenhum artefacto desta fase; gate de `grep -v '^--' \| grep -cE 'DROP \|DELETE \|TRUNCATE '` a `0` na Task 2 |
| T-126-02 | Denial of Service | arranque do backend em `validate` | mitigate | O script `127` cria a tabela que `User.tenantRoles` exige; sem ele o arranque falha. Documentado no README (4 localizações) e no `DEPLOYMENT.md` (Task 3) como pré-requisito do deploy, não como follow-up opcional |
| T-126-03 | Repudiation | inventário de migrações | mitigate | As contagens de prosa do README são avançadas na mesma tarefa que acrescenta a linha, com gates negativos (`grep -c '5 of the 15'` = `0`) que falham se alguém só acrescentar a linha, e gates positivos nas contagens que NÃO devem mudar, que falham se alguém as corrigir por arrastamento |
| T-126-04 | Elevation of Privilege | associação nova sobre a tabela errada | mitigate | Teste estrutural `UserAssociacaoTenantRoleTest` assere que `tenantRoles` usa `t_user_tenant_role` e que `roles` continua em `t_user_role` — reutilizar `t_user_role` para a associação nova quebraria a reversibilidade e misturaria papéis globais com papéis de escritório numa só tabela |
| T-126-05 | Information Disclosure | permissões directas (`t_user_permission`) | accept | Este plano não toca nessa colecção; o gate de `grep -c 'private Set<String> permissions'` prova-o. A parcela 2 da união é lida (nunca alterada) pelos planos 02-05 |
| T-126-SC | Tampering | instalações de npm/pip/cargo | accept | Zero dependências novas nesta fase — sem `pom.xml`/`package.json` alterados, não há superfície de supply-chain. `git diff --stat backend/pom.xml web/package.json` deve ficar vazio |
</threat_model>

<verification>
1. `cd backend && JAVA_HOME="/c/Program Files/Java/jdk-23" mvn test` — suite completa verde (esperado 213, baseline 209 + 4 novos).
2. `cd backend && JAVA_HOME="/c/Program Files/Java/jdk-23" mvn spotbugs:check` — sem achados novos.
3. `git diff --name-only` limitado aos 5 ficheiros de `files_modified` — nenhum ficheiro de resolução de autoridade tocado neste plano.
4. `git diff --stat backend/pom.xml web/package.json` vazio — zero dependências novas.
</verification>

<success_criteria>
- `User` tem a associação `tenantRoles` sobre `t_user_tenant_role`, EAGER, e mantém `roles` sobre `t_user_role` intacta.
- `UserAssociacaoTenantRoleTest` passa com 4 casos e falharia se a associação antiga fosse removida ou a nova deixasse de ser EAGER.
- `backend/migrations/127-add-user-tenant-role-table.sql` existe, é idempotente, tem zero instruções destrutivas e zero referências SQL a `t_user_role`.
- `backend/migrations/README.md` cataloga o `127` em 4 secções e as contagens de prosa que o `127` realmente altera (`5 of the 15`, `7 scripts are outstanding`) foram avançadas, e as que ele não altera (`10 must not be re-run`, `Nine of them fail loudly`) ficaram intactas.
- A secção de arranque em duas fases do `DEPLOYMENT.md` responde às quatro perguntas de operador (MIGR-03 satisfeito).
- Suite completa verde, SpotBugs sem achados novos.
</success_criteria>

<output>
Criar `.planning/phases/LEXCV-126-migra-o-de-pap-is-existentes/126-01-SUMMARY.md` quando concluído.
</output>
