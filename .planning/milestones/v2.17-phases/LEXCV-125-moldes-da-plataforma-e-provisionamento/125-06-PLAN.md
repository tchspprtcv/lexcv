---
phase: 125-moldes-da-plataforma-e-provisionamento
plan: 06
type: execute
wave: 5
depends_on: [125-05]
files_modified:
  - web/src/schemas/moldes.ts
  - web/src/app/(dashboard)/plataforma/moldes/criar-molde-panel.tsx
  - web/src/app/(dashboard)/plataforma/moldes/page.tsx
  - web/scripts/verify-consola-moldes.mjs
  - web/package.json
autonomous: false
requirements: [MOLD-01, MOLD-02, MOLD-03, MOLD-04]

must_haves:
  truths:
    - "PLATAFORMA_ADMIN cria um molde novo num painel inline, escolhendo o nome e as permissões iniciais"
    - "O nome PLATAFORMA_ADMIN é recusado no formulário com a mensagem exacta do contrato de desenho"
    - "Um molde criado aparece imediatamente como coluna nova da matriz"
    - "Existe um gate estrutural que reprova se o aviso de não-propagação em três camadas ou os atributos de acessibilidade da matriz forem removidos por um refactor futuro"
    - "Um humano confirma ao vivo que provisionar um escritório novo lhe cria papéis instanciados e que editar um molde depois disso não os altera"
  artifacts:
    - path: "web/src/app/(dashboard)/plataforma/moldes/criar-molde-panel.tsx"
      provides: "Painel inline de criação de molde, orientado por props"
      contains: "zodResolver"
    - path: "web/src/schemas/moldes.ts"
      provides: "Schema Zod com normalização para maiúsculas e recusa do nome reservado"
      contains: "PLATAFORMA_ADMIN"
    - path: "web/scripts/verify-consola-moldes.mjs"
      provides: "Gate estrutural Node-only do aviso em três camadas e da acessibilidade da matriz"
      contains: "assertions"
  key_links:
    - from: "web/src/app/(dashboard)/plataforma/moldes/page.tsx"
      to: "CriarMoldePanel"
      via: "troca isFormOpen do Card da matriz pelo painel"
      pattern: "CriarMoldePanel"
    - from: "web/package.json"
      to: "web/scripts/verify-consola-moldes.mjs"
      via: "entrada verify:consola-moldes"
      pattern: "verify:consola-moldes"
---

<objective>
Fechar a fase: criação de moldes novos, um gate estrutural que impede um refactor futuro de apagar silenciosamente o aviso de não-propagação e a acessibilidade da matriz, e a confirmação humana ao vivo dos quatro critérios de sucesso da fase.

Purpose: MOLD-04 só existe quando um molde novo pode nascer da consola e ficar disponível para o próximo escritório provisionado. O gate estrutural existe porque o aviso em três camadas e os atributos de acessibilidade são exactamente o tipo de coisa que um refactor bem-intencionado remove sem se dar conta — e nenhum teste unitário os cobre.
Output: 1 schema, 1 painel, 1 wiring, 1 gate estrutural com entrada em `package.json`, 1 verificação humana.
</objective>

<execution_context>
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/LEXCV-125-moldes-da-plataforma-e-provisionamento/125-UI-SPEC.md
@.planning/phases/LEXCV-125-moldes-da-plataforma-e-provisionamento/125-PATTERNS.md
@.planning/phases/LEXCV-125-moldes-da-plataforma-e-provisionamento/125-05-SUMMARY.md
@.planning/phases/LEXCV-125-moldes-da-plataforma-e-provisionamento/125-02-SUMMARY.md
@CLAUDE.md
@web/AGENTS.md
</context>

<build_environment>
`web/node_modules` pode não existir neste worktree. O primeiro comando é `cd web && pnpm install`. Se a saída de um comando de verificação contiver `command not found`, `ERR_PNPM` ou uma falha de resolução de pacote, o critério **não** está cumprido, independentemente do código de saída.

Backend (para o checkpoint humano): `mvn` requer `JAVA_HOME="/c/Program Files/Java/jdk-23"` — o JDK 26 por omissão não compila este projecto.
</build_environment>

<interfaces>
<!-- Contratos já extraídos. O executor não precisa de explorar o codebase. -->

Analogia completa do painel — `web/src/app/(dashboard)/plataforma/criar-tenant-panel.tsx` (241 linhas): `"use client"`, `useForm` + `zodResolver` do `@hookform/resolvers/zod`, schema importado de `@/schemas/setup` (`setupSchema`, `type SetupFormValues`), layout `Card`+`CardHeader`+`CardContent`+`CardFooter`, botão de fecho `X` (ghost, `aria-label="Fechar"`). O componente é **puramente apresentação e validação**: o chamador (`page.tsx`) detém a mutação, os toasts e o fecho, via props `onCancel`/`onSubmit`/`isSubmitting` — manter essa divisão exacta.

Padrão de troca inline no pai — `web/src/app/(dashboard)/plataforma/page.tsx`: um booleano `isFormOpen` que substitui o `Card` principal pelo painel de criação (não um `Dialog`).

Lista de permissões em coluna única já existente — `web/src/app/(dashboard)/settings/page.tsx:697-724`: `<div className="grid gap-3 grid-cols-1 md:grid-cols-2 max-h-56 overflow-y-auto p-3 border border-slate-200 dark:border-slate-800 rounded-md bg-slate-50/30 dark:bg-slate-950/10">` com uma linha clicável por permissão, `<input type="checkbox" checked=... className="mt-0.5 ... rounded h-3.5 w-3.5 pointer-events-none" />` e um bloco de texto com `perm.nome` (`text-xs font-semibold`) e `perm.descricao` (`text-[10px]`). **Atenção:** esse precedente usa âmbar para o estado selecionado; o UI-SPEC reserva âmbar para três slots apenas (banner, badge de contagem, botão de confirmação), pelo que neste painel o estado marcado usa o azul de acento (`text-blue-600`, coerente com a matriz), não âmbar.

Do Plan 04: `useCreateMolde()` (mutação POST, invalida a listagem), tipo `MoldeCreateRequest = { nome: string; permissoes: string[] }`, `MoldePermissao` (`key`/`nome`/`descricao`/`modulo`).
Do Plan 05: `web/src/app/(dashboard)/plataforma/moldes/page.tsx` com guarda de página, cabeçalho, `Card` "Matriz de Moldes", banner âmbar, matriz acessível e fluxo de gravação com `AlertDialog`. O `CardHeader` tem hoje apenas o botão azul "Guardar Alterações".

Convenção de schema Zod — `web/src/schemas/setup.ts` (827 bytes): schema exportado + `type XFormValues = z.infer<typeof schema>`. Transformações no submit seguem a convenção de `adminEmail.toLowerCase()` já usada ali.

Analogia completa do gate estrutural — `web/scripts/verify-relatorio-utilizacao.mjs` (331 linhas) e `web/scripts/verify-consola-tenants.mjs` (281 linhas): scripts Node de zero dependências que leem os ficheiros-alvo como texto cru (sem import, sem type-stripping), com os helpers `stripComments(source)` (linhas 53-68), `sliceBetweenMarkers(source, startMarker, endMarker)` (linhas 75-81), um array de `{ id, descricao, predicate }` e um runner que imprime `PASS <id>` / `FAIL <id> — <motivo>` e termina em `process.exit(failures === 0 ? 0 : 1)` (linhas 310-328). Caminhos montados com `fileURLToPath(import.meta.url)` + `path.join` (linhas 26-45). Cada script tem um bloco de cabeçalho a declarar **o que não consegue provar** (linhas 16-24) — convenção a copiar na estrutura.

`web/package.json` tem hoje, no bloco `scripts`, as entradas `verify:juizo-origem`, `verify:limite-utilizadores`, `verify:consola-tenants`, `verify:bloqueio-rbac`, `verify:relatorio-utilizacao`.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Painel inline de criação de molde e o seu wiring na consola</name>
  <files>
    web/src/schemas/moldes.ts
    web/src/app/(dashboard)/plataforma/moldes/criar-molde-panel.tsx
    web/src/app/(dashboard)/plataforma/moldes/page.tsx
  </files>
  <read_first>
    web/src/app/(dashboard)/plataforma/criar-tenant-panel.tsx
    web/src/app/(dashboard)/plataforma/page.tsx
    web/src/schemas/setup.ts
    web/src/app/(dashboard)/settings/page.tsx
    web/src/app/(dashboard)/plataforma/moldes/page.tsx
    web/src/hooks/use-platform-moldes.ts
    .planning/phases/LEXCV-125-moldes-da-plataforma-e-provisionamento/125-UI-SPEC.md
  </read_first>
  <action>
Criar `web/src/schemas/moldes.ts` na convenção de `schemas/setup.ts`: um `criarMoldeSchema` com `nome` (string, obrigatório, `trim`, não vazio, transformado para maiúsculas na submissão — a convenção transform-on-submit de `adminEmail.toLowerCase()`) e `permissoes` (array de strings, pode ser vazio). O `nome` rejeita explicitamente o valor literal `PLATAFORMA_ADMIN` **sem distinção de caixa**, com a mensagem exacta do UI-SPEC: "Este nome está reservado à plataforma e não pode ser usado como molde." Exportar também `type CriarMoldeFormValues = z.infer<typeof criarMoldeSchema>`. Comentar que esta validação é espelho de UX da guarda do backend (Plan 03, `PAPEL_RESERVADO`), não a fronteira de autoridade.

Criar `web/src/app/(dashboard)/plataforma/moldes/criar-molde-panel.tsx` exportando `CriarMoldePanel`, orientado por props (`onCancel`, `onSubmit`, `isSubmitting`, e a lista de `MoldePermissao` do catálogo), **sem** mutação, toast nem fecho próprios — o pai detém tudo isso, exactamente como `CriarTenantPanel`. Estrutura (secção 7 do UI-SPEC):
  - `CardHeader` em `flex-row justify-between`: `CardTitle` "Criar Molde", `CardDescription` "Registe um novo molde de papel disponível para escritórios provisionados a partir de agora.", e um botão de fecho `X` ghost `rounded-full` com `aria-label="Fechar"` (markup idêntico ao de `criar-tenant-panel.tsx`).
  - `CardContent` com `space-y-6`. Primeiro campo: `Label` "Nome do Molde", `Input` com `placeholder="Ex.: SUPERVISOR"`, texto de ajuda "Maiúsculas, sem espaços — como os moldes existentes (ADMIN, ADVOGADO, TECNICO, ASSISTENTE).", e o erro de validação em `text-red-600` (convenção já usada em `criar-tenant-panel.tsx`).
  - Segundo campo: `Label` "Permissões Iniciais" e uma lista de checkboxes **agrupada por módulo**, em **coluna única**, scrollável (`max-h-72 overflow-y-auto`), com a mesma estrutura de linha da checklist já existente em `settings/page.tsx:697-724` mas com o estado marcado em azul de acento (`text-blue-600`), não âmbar — âmbar está reservado pelo UI-SPEC aos três slots de cautela. Cada checkbox leva `aria-label={permissao.nome}` (o texto visível da linha não está associado por `<label for>`), pela mesma razão dada para a matriz.
  - `CardFooter` com `border-t`, `justify-end`, `gap-3`: "Cancelar" (outline, `type="button"`) e "Criar Molde" (azul primário, desactivado enquanto `isSubmitting`, com label "A criar..." enquanto pendente — convenção de `criar-tenant-panel.tsx`).
  - **Nenhum aviso de não-propagação neste painel**: um molde que ainda não existe tem, por definição, zero escritórios instanciados. O UI-SPEC é explícito em não sobrecarregar a criação com uma ressalva que não se lhe aplica.

Ligar na `page.tsx` do Plan 05:
  - Acrescentar estado `isFormOpen` e um botão **"Criar Molde"** (outline, ícone `Plus`) no `CardHeader`, **antes** do botão azul "Guardar Alterações".
  - Quando `isFormOpen`, substituir o `Card` da matriz por `<CriarMoldePanel ... />` (troca inline, não `Dialog` — mesmo padrão de `PlataformaPageContent`).
  - Acrescentar também um botão "Criar Molde" dentro do `EmptyContent` do estado vazio defensivo.
  - Submissão: chamar `useCreateMolde()`; em sucesso, `toast.success('Molde "{NOME}" criado com sucesso. Fica disponível para escritórios provisionados a partir de agora.')` com o nome normalizado, fechar o painel, e deixar a invalidação do hook trazer a matriz com a coluna nova. Em falha (ex. nome duplicado), **manter o painel aberto com o input intacto** — o toast do `apiFetch` já informa; mesma convenção de recuperação de `CriarTenantPanel`.
  - Não alterar nada do que o Plan 05 construiu: banner, matriz, atributos de acessibilidade, `AlertDialog` de gravação, guarda de página e `colSpan` derivado ficam como estão. Em particular, o botão âmbar continua a existir **apenas** dentro do `AlertDialog`.

Zero primitivos novos em `components/ui/`, zero dependências novas, zero registos shadcn de terceiros, nenhum bloco `Sidebar`, nenhum `NavigationMenu`.
  </action>
  <acceptance_criteria>
    - `cd web && pnpm install && pnpm exec tsc --noEmit` exit 0 (sem `command not found`/`ERR_PNPM`), `cd web && pnpm lint` exit 0, `cd web && pnpm build` exit 0.
    - `grep -n 'Este nome está reservado à plataforma e não pode ser usado como molde.' web/src/schemas/moldes.ts` devolve exactamente 1 linha.
    - `grep -cE 'toUpperCase|PLATAFORMA_ADMIN' web/src/schemas/moldes.ts` devolve >= 2, e a comparação com o nome reservado é insensível à caixa (verificável por leitura: `toUpperCase()` aplicado antes da comparação, ou flag equivalente).
    - `grep -c 'zodResolver' 'web/src/app/(dashboard)/plataforma/moldes/criar-molde-panel.tsx'` devolve >= 1.
    - `grep -cE 'useCreateMolde|useMutation|toast\.' 'web/src/app/(dashboard)/plataforma/moldes/criar-molde-panel.tsx'` devolve `0` — o painel não detém mutação nem toasts.
    - `grep -n 'aria-label="Fechar"' 'web/src/app/(dashboard)/plataforma/moldes/criar-molde-panel.tsx'` devolve exactamente 1 linha.
    - `grep -c 'max-h-72' 'web/src/app/(dashboard)/plataforma/moldes/criar-molde-panel.tsx'` devolve `1`, e `grep -c 'amber' 'web/src/app/(dashboard)/plataforma/moldes/criar-molde-panel.tsx'` devolve `0`.
    - `grep -c 'CriarMoldePanel' 'web/src/app/(dashboard)/plataforma/moldes/page.tsx'` devolve >= 2 (import + uso), e `grep -c 'Criar Molde' 'web/src/app/(dashboard)/plataforma/moldes/page.tsx'` devolve >= 2 (`CardHeader` + estado vazio).
    - `grep -n 'Fica disponível para escritórios provisionados a partir de agora.' 'web/src/app/(dashboard)/plataforma/moldes/page.tsx'` devolve exactamente 1 linha.
    - `grep -c 'bg-amber-600' 'web/src/app/(dashboard)/plataforma/moldes/page.tsx'` continua a devolver `1` (só dentro do `AlertDialog`).
    - `grep -c 'Dialog' 'web/src/app/(dashboard)/plataforma/moldes/criar-molde-panel.tsx'` devolve `0` (painel inline, não modal).
    - `git diff --stat web/package.json` mostra alterações **apenas** no bloco `scripts` (Task 2) ou vazio nesta task; nenhuma dependência nova. `git diff --name-only web/src/components/ui/` vazio.
  </acceptance_criteria>
  <verify>
    <automated>cd web && pnpm install && pnpm exec tsc --noEmit && pnpm lint && pnpm build</automated>
  </verify>
  <done>Um molde novo pode ser criado a partir da consola num painel inline com nome normalizado e permissões iniciais, o nome reservado é recusado no formulário com a mensagem do contrato, e a matriz ganha a coluna nova por invalidação da query.</done>
</task>

<task type="auto">
  <name>Task 2: Gate estrutural do aviso em três camadas e da acessibilidade da matriz</name>
  <files>
    web/scripts/verify-consola-moldes.mjs
    web/package.json
  </files>
  <read_first>
    web/scripts/verify-relatorio-utilizacao.mjs
    web/scripts/verify-consola-tenants.mjs
    web/package.json
    web/src/app/(dashboard)/plataforma/moldes/page.tsx
    web/src/app/(dashboard)/plataforma/moldes/criar-molde-panel.tsx
    web/src/app/(dashboard)/plataforma/page.tsx
    .planning/phases/LEXCV-125-moldes-da-plataforma-e-provisionamento/125-UI-SPEC.md
  </read_first>
  <action>
Criar `web/scripts/verify-consola-moldes.mjs` como script Node de zero dependências, copiando a estrutura de `verify-relatorio-utilizacao.mjs`: montagem de caminhos com `fileURLToPath(import.meta.url)` + `path.join` apontando a `src/app/(dashboard)/plataforma/page.tsx`, `src/app/(dashboard)/plataforma/moldes/page.tsx` e `src/app/(dashboard)/plataforma/moldes/criar-molde-panel.tsx`; helpers `stripComments` e `sliceBetweenMarkers` copiados; array de `{ id, descricao, predicate }`; runner que imprime `PASS`/`FAIL` e sai com `process.exit(failures === 0 ? 0 : 1)`.

**Todas as predicates correm sobre a fonte com comentários removidos** (`stripComments`) — sem isso, prosa de cabeçalho a mencionar um token fá-lo-ia passar a si próprio, que é exactamente o modo de falha que este tipo de gate tem de evitar.

Assertions obrigatórias, agrupadas:
  - *Ponto de entrada*: `plataforma/page.tsx` contém `href="/plataforma/moldes"` e o label "Gerir Moldes", e essa ocorrência aparece **antes** de `href="/plataforma/relatorio"` (ordem do UI-SPEC).
  - *Guarda de página*: `moldes/page.tsx` contém `!me.isFetched` e `AccessDeniedState`, e o índice de `!me.isFetched` é **inferior** ao da verificação de `"PLATAFORMA_ADMIN"` (WR-03, Phase 120).
  - *Aviso, camada 1*: existe um bloco com `bg-amber-50` e `TriangleAlert`, contendo o texto "Alterar um molde não atualiza escritórios já criados.", e **não** existe `ShieldAlert` no ficheiro (registo visual distinto do `RbacTab`).
  - *Aviso, camada 2*: existe um `Badge` cuja variante deriva de `escritoriosInstanciados` (procurar a co-ocorrência de `escritoriosInstanciados` com `"amber"` e `"gray"`), e o token `escritoriosInstanciados` aparece dentro da fatia do `<thead>`/cabeçalho de coluna.
  - *Aviso, camada 3*: existem `AlertDialog`, `AlertDialogAction` e `AlertDialogCancel`; o texto "Confirmar e Gravar" existe; `bg-amber-600` aparece **exactamente uma vez** e dentro da fatia do `AlertDialog` (o botão âmbar nunca no `CardHeader`); e a mutação de gravação não é invocada fora do bloco do `AlertDialog`.
  - *Acessibilidade da matriz*: `scope="col"` presente; `scope="row"` presente; a linha/atributos da célula promovida a `<th scope="row">` contêm `text-left`; existe um `aria-label` template-string que referencia simultaneamente o rótulo da permissão e o nome do molde; `colSpan={5}` **ausente** e existe um `colSpan` derivado do número de moldes.
  - *Não-regressões*: `isAdminRow` ausente; "Gerido pela Plataforma" ausente; nenhum import de `@/server/mock-db`; nenhum import de `NavigationMenu` nem de um bloco `Sidebar`.
  - *Painel de criação*: `criar-molde-panel.tsx` contém `zodResolver`, `aria-label="Fechar"` e `max-h-72`; e **não** contém `useCreateMolde`, `useMutation`, `toast.` nem `Dialog` (painel inline orientado por props, sem mutação própria).

Incluir um bloco de cabeçalho a declarar explicitamente **o que este gate não consegue provar**, na mesma estrutura dos dois scripts análogos: que a rota resolve no browser, que um clique num checkbox persiste, que a contagem de escritórios está numericamente correcta, que o `AlertDialog` abre de facto, e que um utilizador sem `PLATAFORMA_ADMIN` recebe 403 do backend ao vivo. Isso é o âmbito da Task 3 e dos testes do Plan 03.

Acrescentar a `web/package.json`, no bloco `scripts`, exactamente uma linha nova a seguir a `verify:relatorio-utilizacao`: `"verify:consola-moldes": "node scripts/verify-consola-moldes.mjs"`. Não tocar em `dependencies` nem `devDependencies`.
  </action>
  <acceptance_criteria>
    - `cd web && pnpm run verify:consola-moldes` exit 0, e a saída contém pelo menos 15 linhas `PASS ` e zero linhas `FAIL `.
    - `grep -n '"verify:consola-moldes"' web/package.json` devolve exactamente 1 linha, e o número dessa linha é posterior ao de `"verify:relatorio-utilizacao"`.
    - `git diff web/package.json` não mostra nenhuma alteração fora do bloco `scripts` (nenhuma dependência nova).
    - `grep -c 'stripComments' web/scripts/verify-consola-moldes.mjs` devolve >= 2 (definição + uso), e nenhuma predicate corre sobre a fonte não filtrada (verificação por leitura).
    - `grep -cE "from ['\"]" web/scripts/verify-consola-moldes.mjs` conta apenas imports de `node:` (zero dependências de terceiros).
    - Prova de que o gate é capaz de reprovar: remover temporariamente o atributo `scope="col"` de `moldes/page.tsx`, correr `pnpm run verify:consola-moldes`, confirmar exit != 0 com uma linha `FAIL`; restaurar e confirmar exit 0. Repetir o mesmo exercício removendo o texto do banner. Registar as quatro saídas no SUMMARY — um gate que nunca se viu reprovar não é um gate.
    - `cd web && pnpm exec tsc --noEmit` exit 0 e `cd web && pnpm lint` exit 0 (o script `.mjs` não pode quebrar nenhum dos dois).
  </acceptance_criteria>
  <verify>
    <automated>cd web && pnpm run verify:consola-moldes</automated>
  </verify>
  <done>Existe um gate Node-only que reprova se o banner, a contagem por coluna, o `AlertDialog` âmbar ou qualquer dos três atributos de acessibilidade da matriz forem removidos, ligado a `pnpm run verify:consola-moldes`, com capacidade de reprovar demonstrada.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verificação humana ao vivo dos quatro critérios de sucesso da fase</name>
  <files>—</files>
  <read_first>
    .planning/phases/LEXCV-125-moldes-da-plataforma-e-provisionamento/125-01-SUMMARY.md
    .planning/phases/LEXCV-125-moldes-da-plataforma-e-provisionamento/125-02-SUMMARY.md
    .planning/phases/LEXCV-125-moldes-da-plataforma-e-provisionamento/125-03-SUMMARY.md
    .planning/phases/LEXCV-125-moldes-da-plataforma-e-provisionamento/125-05-SUMMARY.md
    .planning/ROADMAP.md
  </read_first>
  <what-built>
    Esquema de moldes (`t_role.instanciavel`) e de papéis de escritório (`t_tenant_role`, `t_tenant_role_permission`), instanciação automática dentro de `SetupService.provisionTenant`, três endpoints `/api/v1/platform/moldes` sob o gate `PLATAFORMA_ADMIN` de classe, e a consola `/plataforma/moldes` com aviso de não-propagação em três camadas, matriz acessível e criação inline de moldes.

    Já automatizado e confirmado antes deste checkpoint: suite Mockito completa verde (`mvn test`), `mvn spotbugs:check` sem novos achados, prova por proxy AOP real de que um ADMIN de escritório é recusado nos três handlers novos, prova por reversão genuína de que o snapshot é independente do molde, `pnpm exec tsc --noEmit` / `pnpm lint` / `pnpm build` verdes, e `pnpm run verify:consola-moldes` verde com capacidade de reprovar demonstrada.
  </what-built>
  <how-to-verify>
    Pré-requisitos: `backend/.env` com `DB_*`/`MINIO_*` válidos e PostgreSQL acessível; o script `backend/migrations/126-add-tenant-role-tables.sql` **não** é necessário em dev (`ddl-auto: update` cria o esquema), mas fica pendente para produção.

    1. Arrancar o backend a partir de `backend/` com `JAVA_HOME="/c/Program Files/Java/jdk-23" mvn spring-boot:run`. Confirmar arranque sem erro de validação de esquema.
    2. Arrancar o frontend a partir de `web/` com `pnpm dev` e entrar em `http://localhost:3000` como o utilizador `PLATAFORMA_ADMIN`.
    3. Em `/plataforma`, confirmar que o cabeçalho de "Tenants Registados" mostra três botões pela ordem `Gerir Moldes` → `Ver Relatório` → `Criar Tenant`. Clicar em "Gerir Moldes".
    4. **(Critério 1 do ROADMAP / MOLD-02)** Confirmar que `/plataforma/moldes` mostra a matriz com os 4 moldes (ADMIN, ADVOGADO, TECNICO, ASSISTENTE) como colunas, que `PLATAFORMA_ADMIN` **não** aparece como coluna, e que as 20 permissões aparecem agrupadas por módulo com rótulo legível (nenhuma chave técnica crua como `clientes:view`).
    5. Confirmar que o banner âmbar com "Alterar um molde não atualiza escritórios já criados." está visível **sem** ser preciso clicar em nada.
    6. Confirmar que cada coluna mostra um badge com a contagem de escritórios (cinzento a "0 escritórios" se nenhum escritório existente tiver papéis instanciados — o esperado antes de qualquer provisionamento novo).
    7. Confirmar que "Guardar Alterações" está **desactivado** antes de qualquer alteração. Alterar uma checkbox de um molde e confirmar que passa a activo.
    8. Clicar "Guardar Alterações": confirmar que **não grava logo** — abre um diálogo cujo título é "Confirmar alterações aos moldes", que lista **apenas** o molde alterado (não os quatro), e cujo botão de confirmação é âmbar com o texto "Confirmar e Gravar". Cancelar e confirmar que nada foi gravado (recarregar a página e verificar que a checkbox voltou ao valor original).
    9. Repetir e confirmar: gravar mostra o toast "Moldes atualizados com sucesso." e a matriz reflecte a alteração após recarregar.
    10. **(Critério 2 / MOLD-04)** Clicar "Criar Molde", preencher o nome em minúsculas (ex. `supervisor`), marcar 2 ou 3 permissões, submeter. Confirmar: o toast de sucesso nomeia o molde em MAIÚSCULAS, o painel fecha, e a matriz passa a ter uma **quinta coluna** com esse nome e badge "0 escritórios".
    11. Tentar criar um molde com o nome `plataforma_admin` (minúsculas). Confirmar que o formulário recusa com a mensagem "Este nome está reservado à plataforma e não pode ser usado como molde." e que nada é criado.
    12. **(Critério 3 / MOLD-01)** Voltar a `/plataforma`, criar um tenant novo ("Criar Tenant"). Depois, na base de dados, confirmar que o escritório novo recebeu uma linha de papel instanciado por cada molde — incluindo o molde criado no passo 10: `SELECT nome, molde_id, sistema FROM t_tenant_role WHERE tenant_id = '<id do tenant novo>' ORDER BY nome;` deve devolver 5 linhas, todas com `sistema = true` e `molde_id` preenchido. Confirmar também que a tenant reservada "ALCv" **não** tem nenhuma linha em `t_tenant_role`.
    13. Confirmar que o badge de contagem em `/plataforma/moldes` passou a mostrar "1 escritório" (singular) em cada coluna, após recarregar.
    14. **(Critério 4 / MOLD-03 — o mais importante)** Em `/plataforma/moldes`, remover uma permissão de um molde e gravar (confirmando o diálogo, que agora deve dizer "1 escritório(s) já têm uma cópia própria deste molde. Nenhum deles é alterado por esta gravação."). Depois, na base de dados, confirmar que as permissões do papel instanciado do escritório criado no passo 12 **não mudaram**: `SELECT COUNT(*) FROM t_tenant_role_permission trp JOIN t_tenant_role tr ON tr.id = trp.tenant_role_id WHERE tr.tenant_id = '<id do tenant novo>' AND tr.nome = '<molde editado>';` tem de devolver o mesmo número de antes da edição.
    15. Testar o gate de autoridade: entrar como `admin@alcv.cv` (ADMIN de escritório) e navegar directamente para `http://localhost:3000/plataforma/moldes`. Confirmar o estado de acesso negado, e confirmar no separador de rede do browser que qualquer pedido a `/api/v1/platform/moldes` devolve 403 (não 200 com dados).
    16. Verificar acessibilidade da matriz com o inspector: confirmar que os `<th>` de coluna têm `scope="col"`, que a célula de rótulo de cada linha é um `<th scope="row">` com texto **alinhado à esquerda** (não centrado), e que um checkbox qualquer tem um `aria-label` da forma "Ver Clientes — ADVOGADO".
  </how-to-verify>
  <acceptance_criteria>
    - Os 16 pontos acima recebem um veredicto explícito (`CONFIRMADO` ou `FALHOU` com descrição), registados em `.planning/phases/LEXCV-125-moldes-da-plataforma-e-provisionamento/125-HUMAN-UAT.md`.
    - Em particular, os pontos 12 e 14 (MOLD-01 e MOLD-03) são confirmados com a saída real das consultas SQL colada no ficheiro de UAT — não por inspecção visual apenas.
    - O ponto 15 é confirmado com o código de estado HTTP observado, não por dedução.
    - Se algum ponto falhar, não marcar a fase como concluída: registar o achado e corrigir antes de nova aprovação.
  </acceptance_criteria>
  <verify>
    <human-check>Os 16 pontos confirmados e registados em `125-HUMAN-UAT.md`, com as saídas SQL dos pontos 12 e 14 e o código HTTP do ponto 15 colados.</human-check>
    <automated>bash -c 'test -f ".planning/phases/LEXCV-125-moldes-da-plataforma-e-provisionamento/125-HUMAN-UAT.md" && grep -c "CONFIRMADO" ".planning/phases/LEXCV-125-moldes-da-plataforma-e-provisionamento/125-HUMAN-UAT.md"'</automated>
  </verify>
  <resume-signal>Escreva "aprovado", ou descreva o que aparece errado (coluna a mais/a menos, badge com contagem errada, diálogo a listar moldes não alterados, permissões do papel instanciado alteradas pela edição do molde, ausência de 403) para correcção antes de nova aprovação.</resume-signal>
  <done>Os quatro critérios de sucesso da fase têm confirmação humana ao vivo, com evidência SQL para a instanciação e para a invariante de snapshot, e evidência de HTTP 403 para o gate de autoridade.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| formulário do browser → `POST /api/v1/platform/moldes` | um nome de papel e um conjunto de permissões vindos de input humano entram no catálogo de papéis globais |
| refactor futuro → garantias de desenho | o aviso em três camadas e os atributos de acessibilidade não têm cobertura de teste unitário; o gate estrutural é a única barreira |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-125-37 | Elevation of Privilege | criação de um molde chamado `PLATAFORMA_ADMIN` | mitigate | Duas camadas: validação Zod insensível à caixa no formulário (espelho de UX, com a mensagem exacta do UI-SPEC) e a guarda `PAPEL_RESERVADO` no backend (Plan 03), que é a fronteira real. Confirmado ao vivo no ponto 11 do checkpoint |
| T-125-38 | Tampering | permissão desconhecida ou reservada num molde novo | mitigate | O painel só oferece as permissões do catálogo filtrado servido por `GET /platform/moldes`; o backend revalida cada chave contra `findAllByReservadaPlataformaFalse()` e recusa nomeando a chave |
| T-125-39 | Tampering | remoção silenciosa do aviso de não-propagação | mitigate | `verify-consola-moldes.mjs` reprova se o banner, o badge de contagem ou o `AlertDialog` âmbar desaparecerem; capacidade de reprovar demonstrada por remoção temporária |
| T-125-40 | Information Disclosure | acessibilidade como superfície de erro | mitigate | O gate pina `scope="col"`, `<th scope="row">` com `text-left` e o `aria-label` por checkbox — as três correcções à lacuna do `RbacTab` que um refactor descartaria como "igual ao RbacTab" |
| T-125-41 | Elevation of Privilege | consola alcançável por ADMIN de escritório | mitigate | Guarda de página (Plan 05) + gate de classe no backend (Plan 03, provado por proxy AOP); confirmado ao vivo com código HTTP no ponto 15 do checkpoint |
| T-125-42 | Repudiation | quem criou que molde | accept | Sem trilha de auditoria nesta fase; âmbito da Phase 128 |
| T-125-SC | Tampering | instalação de pacotes / primitivos de terceiros | mitigate | Zero pacotes novos: `git diff web/package.json` restrito ao bloco `scripts`; `git diff --name-only web/src/components/ui/` vazio; o gate estrutural verifica ausência de `NavigationMenu` e de bloco `Sidebar`; script de verificação com zero dependências de terceiros |
</threat_model>

<verification>
- `cd web && pnpm install && pnpm exec tsc --noEmit && pnpm lint && pnpm build && pnpm run verify:consola-moldes` — todos exit 0.
- `cd web && pnpm run verify:consola-tenants && pnpm run verify:relatorio-utilizacao && pnpm run verify:bloqueio-rbac` — não-regressão dos gates existentes.
- `cd backend && JAVA_HOME="/c/Program Files/Java/jdk-23" mvn test` verde.
- `git diff web/package.json` restrito ao bloco `scripts`; `git diff --name-only web/src/components/ui/` vazio.
- `125-HUMAN-UAT.md` existe com veredicto explícito nos 16 pontos.
</verification>

<success_criteria>
- Um molde novo pode ser criado da consola e passa a ser instanciado nos escritórios provisionados a partir daí (MOLD-04).
- O nome reservado é recusado no formulário com a mensagem exacta do contrato de desenho.
- `pnpm run verify:consola-moldes` pina o aviso em três camadas e os três atributos de acessibilidade, com capacidade de reprovar demonstrada.
- Os quatro critérios de sucesso da fase têm confirmação humana com evidência SQL e HTTP.
</success_criteria>

<output>
Create `.planning/phases/LEXCV-125-moldes-da-plataforma-e-provisionamento/125-06-SUMMARY.md` when done, mais `.planning/phases/LEXCV-125-moldes-da-plataforma-e-provisionamento/125-HUMAN-UAT.md` com os veredictos dos 16 pontos.

Registar no SUMMARY as quatro saídas da demonstração de capacidade de reprovar do gate estrutural, e repetir a nota de que `backend/migrations/126-add-tenant-role-tables.sql` continua pendente de execução manual em produção.
</output>
