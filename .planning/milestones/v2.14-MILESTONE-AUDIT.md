---
milestone: v2.14
audited: 2026-07-22T06:15:00Z
reaudited: 2026-07-22T13:10:00Z
status: tech_debt
scores:
  requirements: 15/15 (+3 informal AREA-1/2/3 items from inserted Phase 115.1)
  phases: 6/6
  integration: 6/6
  flows: 2/2
gaps:
  requirements: []
  integration: []
  flows: []
tech_debt:
  - phase: "111-backend-pesquisa-global-cross-entity-api"
    items:
      - "IN-01 (111-REVIEW.md): PesquisaController logs an exception description twice (inline + trailing Throwable stack trace) — cosmetic, explicitly accepted/deferred at phase close"
  - phase: "112-frontend-pesquisa-global-paleta-de-comando"
    items:
      - "Accent-folding gap: 'Ver Todos os Clientes'/'Ver Todos os Documentos' don't find the exact same accented-term matches the search palette found (narrow edge case; the higher-priority structured-ID case is fixed) — accepted override, 112-VERIFICATION.md"
      - "No automated regression tests for highlight-match.tsx or the open-redirect guard reuse — vitest is not installed anywhere in web/package.json (pre-existing infra gap since Phase 74/83, not phase-112-specific) — accepted override, 112-VERIFICATION.md"
      - "useGlobalSearch's staleTime: 30_000 could serve a stale result list for ~30s if a user searches, creates a matching record (e.g. via Phase 115.1's new Área-1 flow) elsewhere, then re-searches the identical query within the window. Backend itself has zero caching. Generic frontend cache-window nuance, not a broken wire — found by the milestone-wide integration re-check (2026-07-22)."
  - phase: "114-linguagem-visual-cantos-arredondados-radius"
    items:
      - "RAD-01 live visual QA partially closed: Dashboard (light), Clientes (both themes), Processos (dark), Login (light) confirmed rendering correctly via incidental screenshots during Phase 115's own live checkpoint (114-VERIFICATION.md Post-Verification Addendum). Still unconfirmed: mobile bottom-sheet Dialog at both breakpoints, the Ctrl+K/⌘K search palette's own dialog chrome, mobile hamburger Sheet, /settings, and the separate webpage/ landing page. Re-checked 2026-07-22: no v2.14 phase since (including 115.1) re-opened or touched this gap; mechanical proof (grep rounded-none) still reproduces clean."
  - phase: "115-linguagem-visual-icones-em-todos-os-botoes-filtros-icone-only"
    items:
      - "WR-01 (115-REVIEW.md): Clientes mobile list 'Editar' quick-action opens the same read-only view as 'Ver detalhes' instead of entering edit mode — pre-existing, predates this phase. Spawned as background task task_48c8e2d9 (in progress, started by user in a separate session)."
      - "WR-02 (115-REVIEW.md): Settings user-management search-clear button has no aria-label — pre-existing, predates this phase. Spawned as background task task_482b4a64 (in progress, started by user in a separate session)."
      - "6 pre-existing react-hooks/ESLint errors (set-state-in-effect x5, refs x1) across clientes/[id]/page.tsx, documentos/novo/page.tsx, processos/[id]/page.tsx, dashboard-shell.tsx — confirmed pre-existing via git hunk-range analysis, also independently corroborated by Phase 112's and Phase 114's own verification passes on 2026-07-21 (same fingerprint, before Phase 115 touched anything). Spawned as background task task_d6ca37c9 (in progress, started by user in a separate session)."
      - "IN-02 (115-REVIEW.md): processos/[id]/page.tsx prazo-toggle button's accessible name comes from title only, not this app's dominant aria-label convention — functions correctly, explicitly out of phase scope, no fix required."
  - phase: "115.1-melhorias-t-cnicas-ficha-de-cliente-login-do-webpage-e-filtr"
    items:
      - "AREA-1/AREA-2/AREA-3 (this phase's own requirement tags) were never added to .planning/REQUIREMENTS.md's traceability table — Phase 115.1 was inserted after the original v2.14 roadmap closed, without a formal REQUIREMENTS.md entry. Functionally wired and live-UAT-verified (115.1-HUMAN-UAT.md); this is a traceability/process gap only, confirmed independently by 3 separate SUMMARY.md files each noting requirements.mark-complete returned not_found."
      - "Non-privileged-role live verification (RBAC button absence for a non-processos:create/pareceres:create user; non-ADMIN advogado-name graceful degradation) blocked by a stale seeded test credential (assistente@lexcv.cv's DatabaseSeeder.java password no longer matches this long-lived dev DB after 115+ phases of manual testing) — both items remain source-verified only (two independent passes: code review + phase verifier), not a doubt in the implementation. See 115.1-HUMAN-UAT.md items 1 (partial) and 6."
      - "UI-REVIEW.md (21/24): raw UUID exposed unstyled in the 'Advogado Responsável' column for non-admin viewers when the name can't resolve (clientes/[id]/page.tsx:1278) — no visual treatment. Same underlying 'advogado list unavailable' condition worded 3 different ways across 3 surfaces this phase shipped — inconsistent product voice for one root cause. Both advisory/non-blocking, not re-litigated here (see 115.1-UI-REVIEW.md for full detail)."
  - phase: "milestone-wide (found by integration checker)"
    items:
      - "ResultadoPesquisaDto.subtitulo is a non-optional String but PesquisaController.montarSubtituloCliente() can return null in its fallback branch; frontend types it string | undefined, not | null. Currently harmless (every render site uses a falsy check treating null/undefined identically) but a latent type-contract looseness worth tightening."
      - "Nyquist test-validation coverage: 0/6 phases have a VALIDATION.md (never run for this milestone, including 115.1) — discovery only, not a blocker; consistent with this project's deliberate research_enabled=false convention (VALIDATION.md is only ever generated from a RESEARCH.md's Validation Architecture section, and research is disabled project-wide, not just for this milestone)."
  - phase: "out of scope (pre-existing, not v2.14 work)"
    items:
      - "backend/.../JwtAuthenticationFilter.java and JwtTokenProvider.java carry uncommitted, unrelated logging additions present since before this milestone's work began — not in any Phase 111-115.1 file list, flagged by the integration checker only for visibility, not as v2.14 debt"
      - "Re-checked 2026-07-22: backend/.../seed/DatabaseSeeder.java (673 changed lines, uncommitted) and web/src/server/mock-db.ts also carry unrelated uncommitted changes not attributable to any v2.14 phase. The RBAC scope strings this audit and Phase 115.1 depend on are present and correct in the current working-tree copy, so audit conclusions are unaffected — flagged for the user's own awareness/cleanup, not GSD-actionable."
      - "task_1add5d3b (Processos 'próximas audiências' missing lower-bound date check) — pre-existing bug found in passing during Phase 113, spawned as a separate background task at that time, in progress in a separate session"
---

# Milestone v2.14 Audit: UI/UX Melhorias

## Summary

**Re-audited 2026-07-22 to incorporate Phase 115.1**, an urgent decimal phase inserted after the original 5-phase milestone (111-115) had already fully closed and been audited once (see the original 06:15 audit below, preserved). Phase 115.1 addressed 3 user-reported gaps discovered right after v2.14's functional completion: creating Processo/Parecer directly from a cliente's ficha, a webpage "Entrar" login-redirect bug, and Pareceres filter visual alignment. It went through 3 code-review iterations (1 Critical + 3 Warnings fixed), a code-level verification (15/15 must-haves), and — notably — a **live human-UAT pass** (backend + web + webpage run together) that found and fixed a 4th real bug no static pass had caught: a recovery-message-never-renders defect in the CR-01/WR-01 fix chain, rooted in `watch()` on an uncontrolled react-hook-form field never observing a browser-only visual fallback. This is exactly the failure mode `human_needed` routing exists to catch, and it worked as designed.

All 6 phases (111-115.1) now pass individual goal-backward verification. 5/6 (111, 112, 113, 115, 115.1) closed with status `passed`; Phase 114 remains `human_needed` — every code-level must-have verified, but 2 of its 3 ROADMAP success criteria are rendering claims that static analysis alone cannot fully discharge. That gap was already substantively (not fully) narrowed by Phase 115's own incidental live checkpoint (see `114-VERIFICATION.md`'s Post-Verification Addendum); re-checked this session — no phase since has re-opened, regressed, or further closed it.

Cross-phase integration is clean across all 6 phases. A dedicated re-check specifically traced the 7 highest-risk integration seams introduced by inserting 115.1 after the fact (search-seeding wiring into all 4 list pages, Estado-filter/icon-only-button non-collision, radius-token survival, icon-language consistency for the new cliente-ficha buttons, and — highest risk — Phase 115.1's own Pareceres primitive swap landing on the *same file* Phase 115's own icon pass touched) and found zero regressions, zero broken flows, 9/9 traced links WIRED.

**No blockers.** Accumulated tech debt is real but narrow and mostly pre-existing/already-tracked: two small pre-existing UI bugs and one pre-existing lint-rule violation set were found in passing during Phase 115's own code review and spawned as independent background tasks (already running, started by the user); Phase 114's visual QA is substantively but not completely closed; Nyquist test-validation was never run for any of the 6 phases (a milestone-wide, project-wide gap, not phase-specific); Phase 115.1's own AREA-1/2/3 tags were never backfilled into REQUIREMENTS.md (a traceability gap, not a functional one); and 2 non-privileged-role UAT items remain source-verified-only, blocked by a stale test credential rather than any doubt in the code. None of this blocks completing the milestone.

## Requirements Coverage (3-Source Cross-Reference)

| REQ-ID | Phase | REQUIREMENTS.md | VERIFICATION.md | SUMMARY frontmatter | Final Status |
|--------|-------|------------------|------------------|----------------------|---------------|
| SRCH-01 | 111 | ✓ [x] | SATISFIED | listed (111-02) | **satisfied** |
| SRCH-02 | 111 | ✓ [x] | SATISFIED | listed (111-01) | **satisfied** |
| SRCH-03 | 112 | ✓ [x] | SATISFIED | listed (112-01, 112-02) | **satisfied** |
| SRCH-04 | 112 | ✓ [x] | SATISFIED | listed (112-02) | **satisfied** |
| SRCH-05 | 112 | ✓ [x] | SATISFIED | listed (112-02, 112-03) | **satisfied** |
| SRCH-06 | 111 | ✓ [x] | SATISFIED | listed (111-02) | **satisfied** |
| SRCH-07 | 111 | ✓ [x] | SATISFIED | listed (111-01) | **satisfied** |
| SRCH-08 | 112 | ✓ [x] | SATISFIED | listed (112-02) | **satisfied** |
| SRCH-09 | 112 | ✓ [x] | SATISFIED | listed (112-02, 112-04, 112-05) | **satisfied** |
| SRCH-10 | 112 | ✓ [x] | SATISFIED | listed (112-01, 112-02) | **satisfied** |
| SRCH-11 | 112 | ✓ [x] | SATISFIED | listed (112-01, 112-02) | **satisfied** |
| PEST-01 | 113 | ✓ [x] | SATISFIED | listed (113-01) | **satisfied** |
| ICON-01 | 115 | ✓ [x] | SATISFIED | listed (115-01..10) | **satisfied** |
| RAD-01 | 114 | ✓ [x] | SATISFIED (code-level) | listed (114-01) | **satisfied** (visual QA partial — see tech debt) |
| FICO-01 | 115 | ✓ [x] | SATISFIED | listed (115-02,04,05,06,07,08,11) | **satisfied** |
| AREA-1 (informal) | 115.1 | not registered | SATISFIED | listed (115.1-01) | **satisfied** (traceability gap only — see tech debt) |
| AREA-2 (informal) | 115.1 | not registered | SATISFIED | listed (115.1-02) | **satisfied** (traceability gap only — see tech debt) |
| AREA-3 (informal) | 115.1 | not registered | SATISFIED | listed (115.1-03) | **satisfied** (traceability gap only — see tech debt) |

**No orphaned requirements** among the 15 REQUIREMENTS.md-registered IDs — every one is backed by a phase VERIFICATION.md with a SATISFIED requirements-coverage row and a matching SUMMARY frontmatter entry. Phase 115.1's 3 informal AREA-* tags are functionally wired and verified but were never registered in REQUIREMENTS.md in the first place (an ad hoc phase inserted after milestone close) — this is a traceability gap, not an orphan in the strict sense (nothing was promised and dropped).

**FAIL gate:** Not triggered — no requirement is `unsatisfied`. RAD-01 carries a documented, narrowing visual-confirmation caveat but is not orphaned or unsatisfied.

## Phase Verification Status

| Phase | Status | Score | Notes |
|-------|--------|-------|-------|
| 111. Backend — Pesquisa Global Cross-Entity (API) | passed | 4/4 | 11/11 + 84/84 backend tests personally re-run; Testcontainers IT compiles, execution CI-gated (Docker unavailable in sandbox, pre-existing since Phase 91) |
| 112. Frontend — Pesquisa Global (Paleta de Comando) | passed | 23/23 (5 SC + 18 plan truths) | 2 accepted overrides (accent-folding edge case, deferred vitest coverage) |
| 113. Processos — Filtro por Estado | passed | 3/3 | Clean; pre-existing `proximasAudiencias` bug found in passing, spawned separately (task_1add5d3b) |
| 114. Linguagem Visual — Cantos Arredondados | human_needed | 7/7 code-level | 2 ROADMAP SC are rendering claims; substantively de-risked by Phase 115's incidental live QA; unchanged by this re-audit |
| 115. Linguagem Visual — Ícones + Filtros Ícone-Only | passed | 4/4 | Live human-verify checkpoint completed (11/11 checklist items confirmed against a running app); 3 pre-existing issues found by code review, spawned separately |
| 115.1. Melhorias Técnicas — Ficha de Cliente, Login do Webpage e Filtros de Pareceres (INSERTED) | passed | 15/15 code-level + 4/6 human_verification items live-confirmed | 3 code-review iterations (1 Critical + 3 Warnings fixed); live UAT found+fixed a 4th bug (recovery message never rendering, commit `ccd1b80`); 2 items blocked by stale test credential, not code doubt; UI-REVIEW 21/24 (2 advisory findings, non-blocking) |

## Cross-Phase Integration

Original check (2026-07-22 06:15): spawned `gsd-integration-checker` against Phases 111-115, with special attention to heavy file overlap (`processos/page.tsx`, `clientes/page.tsx`, `dashboard-shell.tsx`, `globals.css`). Result: 0 regressions, 0 gaps, 15/15 requirements wired clean.

**Re-check (2026-07-22 13:10, this audit)**: spawned a second `gsd-integration-checker` pass scoped to all 6 phases, specifically targeting the 7 integration seams Phase 115.1's insertion could plausibly have broken:

1. Phase 112's `?q=` seeding into all 4 list pages (Clientes/Processos/Documentos/Pareceres) — still wired byte-level in all 4 files, unaffected by 115.1.
2. Phase 113's Estado filter vs. Phase 115's icon-only Aplicar/Limpar in the same `processos/page.tsx` — disjoint JSX regions confirmed, no collision (unchanged from original audit; 115.1 never touched this file).
3. Phase 114's `--radius` token survival — fresh `grep -rn "rounded-none"` still returns only the same 6 documented exception lines; 115.1 introduced zero new occurrences.
4. Search trigger (Phase 112) and Estado filter (Phase 113) icon treatment (Phase 115) — untouched by 115.1, confirmed intact.
5. **Phase 115.1's new "Novo Processo"/"Novo Parecer" buttons match Phase 115's icon language exactly** — byte-identical `variant="outline" size="sm"` + `Plus` icon to the pre-existing "Adicionar" pattern, not a regression to pre-115 style.
6. **Highest risk, confirmed clean:** Phase 115.1's Área 3 (Pareceres `Select`→`NativeSelect` swap) landed on the exact same file (`pareceres/page.tsx`) Phase 115's own plan (115-09) touched for ICON-01. Current file has zero orphaned `Select` references **and** still renders every icon Phase 115 added at the same call sites — independently corroborated by both `115.1-VERIFICATION.md` and a live UAT filter round-trip (Estado=Concluído correctly narrowed 2→1 rows).
7. RBAC scope-string consistency between Phase 111's search endpoint and Phase 115.1's new `processos:create`/`pareceres:create` gates — same shared `permissions.ts`/`hasAuthority` mechanism, no divergence.

**Result: 0 regressions, 0 broken flows, 9/9 traced links WIRED.** Two new non-blocking observations surfaced (staleTime cache window; REQUIREMENTS.md traceability gap for AREA-1/2/3) — both recorded in tech debt, neither a wiring defect. Full E2E trace re-confirmed end to end: Ctrl+K search → click Processo result → detail page (rounded corners + icons present) → cliente ficha → "Novo Processo" (RBAC-gated, icon-consistent) → locked-cliente create form (live-UAT-confirmed) → Processos list → Estado filter still functional with icon-only Aplicar/Limpar → Pareceres list → filters now `NativeSelect`, icons still present, filtering still correct.

## Nyquist Compliance Discovery

`workflow.nyquist_validation` is enabled (`true`). Discovery only — no `/gsd:validate-phase` calls made.

| Phase | VALIDATION.md | Compliant | Action |
|-------|---------------|-----------|--------|
| 111 | missing | — | `/gsd:validate-phase 111` (optional) |
| 112 | missing | — | `/gsd:validate-phase 112` (optional) |
| 113 | missing | — | `/gsd:validate-phase 113` (optional) |
| 114 | missing | — | `/gsd:validate-phase 114` (optional) |
| 115 | missing | — | `/gsd:validate-phase 115` (optional) |
| 115.1 | missing | — | `/gsd:validate-phase 115.1` (optional) |

**Overall:** missing (6/6). Not phase-specific — no phase in this milestone (nor, as far as this audit can tell, in this project's history) has run Nyquist validation, consistent with `research_enabled=false` project-wide (VALIDATION.md is only ever generated from a RESEARCH.md section that never exists here). Recorded as tech debt, not a blocker.

## Tech Debt Summary

17 items across 7 groupings (5 phase-specific incl. 115.1, 1 milestone-wide, 1 explicitly out-of-scope) — see YAML frontmatter `tech_debt` for full detail. None are blockers. 3 items already have independent background-task fixes in progress (`task_48c8e2d9`, `task_482b4a64`, `task_d6ca37c9`), started by the user in separate sessions during this same milestone close-out. Phase 115.1 contributed 3 new items, all non-blocking (REQUIREMENTS.md traceability gap, 2 UAT items blocked by test credentials, 2 advisory UI-REVIEW findings) — notably, it also **closed** one issue class entirely by finding and fixing a real bug live rather than leaving it as debt.

---
*Audited: 2026-07-22T06:15:00Z (Phases 111-115)*
*Re-audited: 2026-07-22T13:10:00Z (added Phase 115.1, re-verified cross-phase integration)*
*Auditor: Claude (milestone audit)*
