---
phase: 99-webpage-nova-app-next-js-de-landing
plan: 02
type: execute
wave: 2
depends_on: [99-01]
files_modified:
  - webpage/src/types/setup.ts
  - webpage/src/lib/setup.ts
  - webpage/proxy.ts
  - webpage/src/types/branding.ts
  - webpage/src/lib/branding.ts
  - webpage/src/components/brand-mark.tsx
autonomous: true
requirements: [LP-04, LP-05, LP-06]

must_haves:
  truths:
    - "proxy.ts contém APENAS o ramo não-inicializado→/setup; zero verificação de access_token/useMe/autenticação — um visitante (autenticado ou anónimo) nunca é redirecionado para /login nem /dashboard (LP-04)"
    - "Quando o sistema não está inicializado, proxy.ts redireciona qualquer rota (exceto /setup) para /setup (LP-05)"
    - "Um erro transitório do backend no proxy faz fail-open (NextResponse.next()), nunca bloqueia a landing"
    - "fetchBranding() chama BACKEND_API_ORIGIN/api/v1/public/branding server-to-server (nunca no browser → zero CORS), com fail-open para {nome:'LexCV', logoDataUrl:null} em 404/erro/timeout (LP-06)"
    - "BrandMark renderiza o logo só quando logoDataUrl é um data:image/ URL; caso contrário o ícone Building2 + nome (fallback idêntico a dashboard-shell)"
  artifacts:
    - path: "webpage/proxy.ts"
      provides: "Edge proxy (Next.js 16) com só o gate de setup-status não-inicializado→/setup, fail-open, matcher que exclui landing-static"
      contains: "/setup"
    - path: "webpage/src/lib/setup.ts"
      provides: "fetchSetupStatus() cópia verbatim de web/ (URL relativa NEXT_PUBLIC_API_BASE_PATH, cache no-store)"
      contains: "no-store"
    - path: "webpage/src/lib/branding.ts"
      provides: "fetchBranding() server-to-server contra BACKEND_API_ORIGIN, cache no-store, fail-open para {nome:'LexCV', logoDataUrl:null}"
      contains: "BACKEND_API_ORIGIN"
    - path: "webpage/src/components/brand-mark.tsx"
      provides: "componente de marca (logo data:image/ com guard, fallback Building2 + nome)"
      contains: "Building2"
    - path: "webpage/src/types/branding.ts"
      provides: "tipo BrandingResponse {nome: string; logoDataUrl: string | null} (shape real da Phase 98)"
      contains: "logoDataUrl"
  key_links:
    - from: "webpage/proxy.ts"
      to: "fetchSetupStatus → /setup"
      via: "redirect quando !status.initialized"
      pattern: "SETUP_PATH"
    - from: "webpage/src/lib/branding.ts"
      to: "BACKEND_API_ORIGIN/api/v1/public/branding"
      via: "fetch server-to-server, cache no-store"
      pattern: "public/branding"
    - from: "webpage/src/components/brand-mark.tsx"
      to: "logoDataUrl / Building2"
      via: "guard data:image/ com fallback"
      pattern: "data:image/"
---

<objective>
Construir a camada de gate de routing e de dados de branding da `webpage/`: um `proxy.ts` (Edge middleware do Next.js 16) que replica APENAS o ramo "sistema não inicializado → /setup" do `web/proxy.ts` — removendo por completo o ramo de autenticação (`access_token`/redirect para /dashboard ou /login) — mais um `fetchBranding()` server-to-server contra o endpoint da Phase 98, e um componente `BrandMark` de logo+nome com fallback.

Purpose: LP-04 exige que a landing apareça SEMPRE (visitante autenticado ou não); copiar o `web/proxy.ts` na íntegra auto-redirecionaria todo o visitante anónimo para `/login` antes da landing renderizar — o oposto do objetivo. Este plano isola essa lógica sensível (o gate) e a obtenção de branding server-side (que fecha definitivamente a preocupação de CORS WR-01, porque o browser nunca faz a chamada).

Output: `proxy.ts` (só setup-gate, fail-open), `lib/setup.ts` (verbatim de web/), `lib/branding.ts` (server-to-server, fail-open), `types/setup.ts` + `types/branding.ts`, e `components/brand-mark.tsx` (consumido por header/hero/footer no plano 99-03/99-04).

Cobre: LP-04 (nunca auto-redirecionar por sessão), LP-05 (não inicializado → /setup), LP-06 (obter nome+logo via endpoint da Phase 98).
</objective>

<execution_context>
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/milestones/v2.12-phases/LEXCV-99-webpage-nova-app-next-js-de-landing/99-CONTEXT.md
@.planning/milestones/v2.12-phases/LEXCV-98-backend-endpoint-publico-de-branding/98-01-SUMMARY.md

<!-- Ficheiros-fonte a adaptar (o executor DEVE lê-los antes de tocar em código) -->
@web/proxy.ts
@web/src/lib/setup.ts
@web/src/types/setup.ts
@web/src/components/shared/dashboard-shell.tsx

<!-- Convenção Next.js 16 (confirmada nos docs locais, per web/AGENTS.md):
     - proxy.ts (NÃO middleware.ts) na raiz de webpage/, mesmo nível de src/app.
     - Exporta função `proxy(request: NextRequest)` (nomeada ou default) + `config` com `matcher`.
     - Ref: web/node_modules/next/dist/docs/01-app/03-api-reference/03-file-conventions/proxy.md
     - Nota do doc getting-started/16-proxy.md: fetch com options.cache no proxy "has no effect" — irrelevante aqui,
       o fetchSetupStatus usa cache:'no-store' apenas por paridade com web/; o comportamento é sempre fetch fresco. -->

<interfaces>
<!-- Shape REAL do endpoint da Phase 98 (de 98-01-SUMMARY.md): 200 {nome, logoDataUrl} quando existe tenant
     (logoDataUrl pode ser null explícito), 404 {"message":"Sistema não inicializado."} quando não inicializado. -->

types/setup.ts — copiar verbatim de web/src/types/setup.ts (só o SetupStatusResponse é necessário; os outros tipos são inofensivos se copiados, mas o mínimo é):
```ts
export type SetupStatusResponse = {
  initialized: boolean;
};
```

lib/setup.ts — copiar VERBATIM de web/src/lib/setup.ts (fetch mínimo, URL relativa via NEXT_PUBLIC_API_BASE_PATH, cache no-store; NÃO o wrapper apiFetch):
```ts
import type { SetupStatusResponse } from "@/types/setup";

const apiBasePath = process.env.NEXT_PUBLIC_API_BASE_PATH;

if (!apiBasePath) {
  throw new Error("NEXT_PUBLIC_API_BASE_PATH is required");
}

const setupStatusUrl = `${apiBasePath}/setup/status`;

export async function fetchSetupStatus(init?: RequestInit): Promise<SetupStatusResponse> {
  const response = await fetch(setupStatusUrl, {
    ...init,
    cache: "no-store",
    headers: { Accept: "application/json", ...(init?.headers ?? {}) },
  });
  if (!response.ok) {
    throw new Error(`Setup status failed with ${response.status}`);
  }
  return (await response.json()) as SetupStatusResponse;
}
```

proxy.ts — ADAPTADO de web/proxy.ts. CRÍTICO: manter SÓ o ramo não-inicializado→/setup. REMOVER por completo: as constantes LOGIN_PATH/DASHBOARD_PATH, o ramo `status.initialized && pathname === SETUP_PATH`, e toda a leitura de `request.cookies.get("access_token")`. Acrescentar `landing-static` ao matcher (o assetPrefix desta app) para o proxy não correr — nem fazer fetch de setup-status — em cada pedido de asset:
```ts
import { NextResponse, type NextRequest } from "next/server";

import { fetchSetupStatus } from "./src/lib/setup";

const SETUP_PATH = "/setup";

export async function proxy(request: NextRequest) {
  const { pathname } = request.nextUrl;

  try {
    const status = await fetchSetupStatus();

    if (!status.initialized && pathname !== SETUP_PATH) {
      return NextResponse.redirect(new URL(SETUP_PATH, request.url));
    }
  } catch {
    // fail open — um erro transitório do backend nunca deve bloquear a landing pública
    return NextResponse.next();
  }

  return NextResponse.next();
}

export const config = {
  matcher: ["/((?!api|_next/static|_next/image|landing-static|favicon.ico|.*\\..*).*)"],
};
```

types/branding.ts — shape REAL da Phase 98:
```ts
export type BrandingResponse = {
  nome: string;
  logoDataUrl: string | null;
};
```

lib/branding.ts — NOVO. Fetch server-to-server contra o ORIGIN absoluto do backend (nunca URL relativa: um Server Component/lib não tem origin para resolver relativos; e o origin explícito evita o "hairpin" para fora via domínio público). cache no-store (CONTEXT.md). Fail-open silencioso → fallback "LexCV":
```ts
import type { BrandingResponse } from "@/types/branding";

const backendOrigin = process.env.BACKEND_API_ORIGIN;

if (!backendOrigin) {
  throw new Error("BACKEND_API_ORIGIN is required");
}

const FALLBACK: BrandingResponse = { nome: "LexCV", logoDataUrl: null };

export async function fetchBranding(): Promise<BrandingResponse> {
  try {
    const response = await fetch(`${backendOrigin}/api/v1/public/branding`, {
      cache: "no-store",
      headers: { Accept: "application/json" },
    });
    // 404 (sistema não inicializado — o proxy já terá redirecionado) ou qualquer !ok → fail open
    if (!response.ok) {
      return FALLBACK;
    }
    const data = (await response.json()) as BrandingResponse;
    return { nome: data.nome || "LexCV", logoDataUrl: data.logoDataUrl ?? null };
  } catch {
    // rede/timeout server-side → fail open, nunca crash para um visitante anónimo
    return FALLBACK;
  }
}
```

brand-mark.tsx — NOVO. Server Component (SEM "use client" — sem hooks). Replica o padrão de fallback de dashboard-shell.tsx:262-276 (`<img>` com fallback para Building2 + nome), com um guard extra `data:image/` (endurecimento XSS — só coloca em `src` um data-URL de imagem; caso contrário, fallback). React auto-escapa `{nome}`; ZERO dangerouslySetInnerHTML:
```tsx
import { Building2 } from "lucide-react";

import { cn } from "@/lib/utils";
import type { BrandingResponse } from "@/types/branding";

export function BrandMark({
  branding,
  className,
}: {
  branding: BrandingResponse;
  className?: string;
}) {
  const { nome, logoDataUrl } = branding;
  const hasLogo = typeof logoDataUrl === "string" && logoDataUrl.startsWith("data:image/");

  return (
    <span
      className={cn(
        "inline-flex items-center gap-2 text-sm font-semibold text-slate-900 dark:text-slate-100",
        className,
      )}
    >
      {hasLogo ? (
        <img src={logoDataUrl!} alt="" className="h-5 w-5 rounded-sm object-contain" />
      ) : (
        <Building2 className="h-4 w-4 text-slate-400" />
      )}
      <span>{nome || "LexCV"}</span>
    </span>
  );
}
```
Padrão de fallback de dashboard-shell.tsx (linhas 262-276), a espelhar:
```tsx
{me.data?.tenant_logo_data_url ? (
  <img src={me.data.tenant_logo_data_url} alt="" className="h-5 w-5 object-contain rounded-sm" />
) : (
  <Building2 className="h-4 w-4 text-slate-400" />
)}
{me.data?.tenant_nome ?? "LexCV"}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Setup-status gate (proxy.ts sem ramo de autenticação)</name>
  <files>
    webpage/src/types/setup.ts (novo),
    webpage/src/lib/setup.ts (novo),
    webpage/proxy.ts (novo)
  </files>
  <read_first>
    - web/proxy.ts (FONTE A ADAPTAR — estudar exatamente quais linhas REMOVER: LOGIN_PATH, DASHBOARD_PATH, o ramo initialized&&===SETUP e o access_token)
    - web/src/lib/setup.ts (copiar verbatim)
    - web/src/types/setup.ts (fonte do SetupStatusResponse)
    - web/node_modules/next/dist/docs/01-app/03-api-reference/03-file-conventions/proxy.md (convenção Next.js 16 proxy.ts: função proxy + config.matcher; runtime Node.js; NÃO middleware.ts)
    - 99-CONTEXT.md (secção "Setup Gate & Navegação": o proxy é próprio, NUNCA copiado literalmente; só o ramo não-inicializado→/setup)
  </read_first>
  <action>
    Criar `webpage/src/types/setup.ts` com `export type SetupStatusResponse = { initialized: boolean };` (só isto é necessário).

    Copiar `webpage/src/lib/setup.ts` VERBATIM de web/src/lib/setup.ts — `fetchSetupStatus()`, URL relativa `${NEXT_PUBLIC_API_BASE_PATH}/setup/status`, cache no-store, fail-fast na env var. Este é o padrão exato que web/proxy.ts já usa em produção; mantê-lo idêntico garante que o mesmo mecanismo comprovado se aplica (o rewrite `/api/v1/:path*` do next.config.ts do plano 99-01 encaminha o fetch relativo para o backend em dev).

    Criar `webpage/proxy.ts` como o bloco `<interfaces>` — na RAIZ de webpage/ (mesmo nível de src/, convenção Next.js 16, ficheiro `proxy.ts` NÃO `middleware.ts`). CRÍTICO, o núcleo deste plano (LP-04): manter APENAS `if (!status.initialized && pathname !== SETUP_PATH) return NextResponse.redirect('/setup')`. REMOVER por completo — não deve existir em lado nenhum do ficheiro: as constantes `LOGIN_PATH`/`DASHBOARD_PATH`, o ramo `status.initialized && pathname === SETUP_PATH`, e qualquer `request.cookies.get("access_token")`. O `catch` faz `NextResponse.next()` (fail-open). No `matcher`, acrescentar `landing-static` ao negative-lookahead (o assetPrefix desta app) para o proxy não correr em pedidos de asset.

    Nota (LP-05 cross-zone, não bloqueante nesta fase): o alvo `/setup` não existe na tabela de rotas de webpage/ (pertence a web/) — tal como `/login`, só resolve cross-zone atrás do Caddy (Phase 100). Em `pnpm dev` isolado com backend inicializado (o caso normal deste dev), o redirect nunca dispara e a landing renderiza; não tentar criar uma rota /setup nem /login em webpage/.
  </action>
  <acceptance_criteria>
    - `webpage/proxy.ts` existe na raiz de webpage/ (não em src/), exporta `proxy` + `config` com `matcher`.
    - proxy.ts contém o redirect para `/setup` quando `!status.initialized`, e um `catch` com `NextResponse.next()` (fail-open).
    - proxy.ts NÃO contém `access_token`, NÃO contém `DASHBOARD`, NÃO contém `LOGIN`, NÃO contém `dashboard`/`login` (nenhum ramo de autenticação) — este é o critério central de LP-04.
    - o `matcher` inclui `landing-static` no negative-lookahead.
    - `lib/setup.ts` é byte-for-byte idêntico a web/src/lib/setup.ts.
    - `pnpm build` em webpage/ continua a passar com o proxy.ts presente.
  </acceptance_criteria>
  <verify>
    <automated>cd webpage && grep -Fq 'SETUP_PATH' proxy.ts && grep -Fq 'NextResponse.next' proxy.ts && grep -Fq 'landing-static' proxy.ts && ! grep -Fiq 'access_token' proxy.ts && ! grep -Fq 'DASHBOARD' proxy.ts && ! grep -Fiq 'login' proxy.ts && diff -q src/lib/setup.ts ../web/src/lib/setup.ts && pnpm build</automated>
  </verify>
  <done>
    O gate de setup-status vive num proxy.ts próprio com SÓ o ramo não-inicializado→/setup e fail-open; zero lógica de autenticação (nenhum access_token/login/dashboard); o build continua verde.
  </done>
</task>

<task type="auto">
  <name>Task 2: Branding fetch server-to-server + componente BrandMark</name>
  <files>
    webpage/src/types/branding.ts (novo),
    webpage/src/lib/branding.ts (novo),
    webpage/src/components/brand-mark.tsx (novo)
  </files>
  <read_first>
    - .planning/milestones/v2.12-phases/LEXCV-98-backend-endpoint-publico-de-branding/98-01-SUMMARY.md (shape real: 200 {nome, logoDataUrl}; 404 {"message":"Sistema não inicializado."})
    - web/src/lib/setup.ts (padrão de fetch mínimo cache:no-store que o branding espelha em estilo)
    - web/src/components/shared/dashboard-shell.tsx (linhas ~262-276: padrão de fallback de logo <img>→Building2+nome a replicar)
    - webpage/src/lib/utils.ts (cn, criado no plano 99-01 — dependência do BrandMark)
    - 99-CONTEXT.md (secção "Fetch de Branding & Estados de Erro": server-side, cache no-store, fail-open "LexCV"; fallback de logo null reutiliza dashboard-shell)
  </read_first>
  <action>
    Criar `webpage/src/types/branding.ts` com `export type BrandingResponse = { nome: string; logoDataUrl: string | null };` — o shape exato devolvido pela Phase 98.

    Criar `webpage/src/lib/branding.ts` como o bloco `<interfaces>`: `fetchBranding()` faz `fetch(\`${BACKEND_API_ORIGIN}/api/v1/public/branding\`, { cache: "no-store", headers: { Accept: "application/json" } })`. Usar o ORIGIN ABSOLUTO do backend (env `BACKEND_API_ORIGIN`), NUNCA uma URL relativa — isto é um fetch genuinamente server-to-server (o processo servidor da webpage chama o backend diretamente): (a) evita CORS por completo (o browser nunca faz a chamada — fecha WR-01), (b) evita o erro de parse de URL relativa num Server Component/lib, (c) usa um origin explícito, evitando o "hairpin" para fora via domínio público. Fail-open: 404/`!response.ok` → devolve `{ nome: "LexCV", logoDataUrl: null }`; `catch` de rede/timeout → o mesmo fallback. Nunca lançar para o chamador. Fail-fast na env var em falta (paridade com web/).

    Criar `webpage/src/components/brand-mark.tsx` como o bloco `<interfaces>` — um Server Component (SEM "use client", sem hooks). Recebe `{ branding: BrandingResponse; className?: string }`. Renderiza: se `logoDataUrl` for uma string que começa por `data:image/` → `<img src={logoDataUrl} alt="" className="h-5 w-5 rounded-sm object-contain" />`; caso contrário → `<Building2 className="h-4 w-4 text-slate-400" />`; seguido de `<span>{nome || "LexCV"}</span>`. O guard `startsWith("data:image/")` é endurecimento XSS (nunca colocar um URL não-imagem/`javascript:` em `src`). Wrapper `inline-flex items-center gap-2 text-sm font-semibold text-slate-900 dark:text-slate-100` + `cn(...)` para aceitar className. ZERO dangerouslySetInnerHTML.
  </action>
  <acceptance_criteria>
    - `lib/branding.ts` faz fetch a `BACKEND_API_ORIGIN` + `/api/v1/public/branding` com `cache: "no-store"`, e devolve `{ nome: "LexCV", logoDataUrl: null }` em `!ok` e em `catch` (fail-open, dois caminhos).
    - `brand-mark.tsx` só renderiza `<img>` quando `logoDataUrl` começa por `data:image/`; caso contrário renderiza `<Building2 />`; e nunca usa `dangerouslySetInnerHTML`.
    - `types/branding.ts` define `BrandingResponse` com `nome: string` e `logoDataUrl: string | null`.
    - brand-mark.tsx NÃO tem `"use client"` (é Server Component).
    - `pnpm build` em webpage/ continua a passar.
  </acceptance_criteria>
  <verify>
    <automated>cd webpage && grep -Fq 'BACKEND_API_ORIGIN' src/lib/branding.ts && grep -Fq 'public/branding' src/lib/branding.ts && grep -Fq 'no-store' src/lib/branding.ts && grep -Fq 'data:image/' src/components/brand-mark.tsx && grep -Fq 'Building2' src/components/brand-mark.tsx && ! grep -Fq 'dangerouslySetInnerHTML' src/components/brand-mark.tsx && ! grep -Fq 'use client' src/components/brand-mark.tsx && pnpm build</automated>
    <human-check>Com o backend a correr e sistema inicializado (nome/logo definidos via /setup): num Server Component temporário ou no plano 99-04, `fetchBranding()` devolve o nome real da tenant; com o backend desligado, devolve {nome:"LexCV", logoDataUrl:null} sem crash. Confirmação de integração, fora do gate automatizado.</human-check>
  </verify>
  <done>
    fetchBranding() obtém {nome, logoDataUrl} server-to-server do endpoint da Phase 98 com fail-open "LexCV"; BrandMark mostra logo (data:image/ guarded) ou Building2+nome; zero CORS, zero dangerouslySetInnerHTML.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Visitante (qualquer) → proxy.ts | O Edge proxy corre antes de cada rota renderizar. Decide redirects; deliberadamente SEM qualquer decisão de autenticação (app pública). |
| webpage (servidor) → backend GET /api/v1/public/branding | Fetch server-to-server (origin explícito BACKEND_API_ORIGIN); o browser nunca atravessa esta fronteira (zero CORS). |
| Dados de branding da tenant (nome, logoDataUrl) → DOM renderizado | nome/logoDataUrl vêm do registo da tenant (definido pelo admin no /setup, não pelo visitante anónimo), mas são dados dinâmicos que chegam ao HTML. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-99-02 | Tampering / Information Disclosure (XSS) | brand-mark.tsx (render de nome + logoDataUrl) | mitigate | `nome` é renderizado como text child de React (`{nome}`) → auto-escapado, sem injeção de HTML. `logoDataUrl` só entra em `<img src>` quando passa o guard `startsWith("data:image/")` — um `javascript:`/`data:text/html`/URL arbitrário cai no fallback Building2. ZERO `dangerouslySetInnerHTML` em toda a app. Reforçado pelo CSP `img-src 'self' data:` (T-99-01, plano 99-01). Risco residual baixo: os dados são definidos pelo admin no /setup, não pelo visitante anónimo. |
| T-99-03 | Denial of Service / Availability | proxy.ts fail-open + branding fail-open | accept | **Tradeoff deliberado disponibilidade-sobre-rigor (decisão CONTEXT.md), nomeado explicitamente como risco aceite, não introduzido em silêncio:** um erro/timeout do backend no proxy faz `NextResponse.next()` e no branding faz fallback "LexCV" — a landing pública nunca falha/crasha para um visitante anónimo. O reverso aceite: se o backend estiver em baixo durante um genuíno primeiro-arranque (não inicializado), o redirect para /setup não dispara e a landing aparece com branding de fallback. Aceitável para uma página de marketing pública anónima; a alternativa (fail-closed) bloquearia visitantes por um soluço transitório do backend. |
| T-99-04 | Information Disclosure (SSRF) | lib/branding.ts destino do fetch | accept | O destino do fetch é a env var FIXA `BACKEND_API_ORIGIN` + o path literal `/api/v1/public/branding` — nunca derivado de input do visitante (sem query params, sem headers do request). Não há vetor de SSRF (o visitante não controla o destino). |
| T-99-05 | Elevation of Privilege (ausência deliberada de auth) | proxy.ts | accept | Esta app NÃO tem autenticação nem rotas protegidas, POR DESENHO (landing pública). O proxy NÃO tem gate de auth de propósito (LP-04). Nomeado para deixar claro que a ausência é intencional, não uma omissão: não existem recursos protegidos nesta app a que o gate pudesse aplicar-se. O endpoint de branding consumido é público e não devolve dados sensíveis (garantido pela Phase 98: só nome+logoDataUrl). |
</threat_model>

<verification>
Verificação geral do plano (gate + branding):

1. `grep -Fq 'SETUP_PATH' webpage/proxy.ts` e `! grep -Fiq 'access_token' webpage/proxy.ts` e `! grep -Fiq 'login' webpage/proxy.ts` e `! grep -Fq 'DASHBOARD' webpage/proxy.ts` — o proxy tem SÓ o ramo de setup, zero autenticação (LP-04).
2. `grep -Fq 'landing-static' webpage/proxy.ts` — matcher exclui o assetPrefix.
3. `diff -q webpage/src/lib/setup.ts web/src/lib/setup.ts` — fetchSetupStatus verbatim (mecanismo comprovado de web/).
4. `grep -Fq 'BACKEND_API_ORIGIN' webpage/src/lib/branding.ts` + `grep -Fq 'no-store'` + fail-open em `!ok` e `catch` — server-to-server, zero CORS (LP-06).
5. `grep -Fq 'data:image/' webpage/src/components/brand-mark.tsx` + `! grep -Fq 'dangerouslySetInnerHTML'` — render de branding endurecido (T-99-02).
6. `cd webpage && pnpm build` continua verde.
</verification>

<success_criteria>
- [ ] LP-04: proxy.ts contém SÓ o ramo não-inicializado→/setup; zero access_token/login/dashboard — visitante autenticado ou anónimo nunca é auto-redirecionado.
- [ ] LP-05: sistema não inicializado → redirect para /setup (o alvo resolve cross-zone na Phase 100).
- [ ] LP-06: fetchBranding() obtém {nome, logoDataUrl} do endpoint público da Phase 98, server-to-server, com fail-open "LexCV".
- [ ] proxy.ts fail-open (catch → NextResponse.next()); branding fail-open (404/erro → fallback) — tradeoff de disponibilidade nomeado (T-99-03).
- [ ] BrandMark: logo só para data:image/ (guard XSS), fallback Building2+nome; sem dangerouslySetInnerHTML; Server Component.
- [ ] `pnpm build` verde com o gate + branding presentes.
</success_criteria>

<output>
Criar `.planning/milestones/v2.12-phases/LEXCV-99-webpage-nova-app-next-js-de-landing/99-02-SUMMARY.md` quando concluído.
</output>
