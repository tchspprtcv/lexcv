---
phase: 99-webpage-nova-app-next-js-de-landing
plan: 03
type: execute
wave: 3
depends_on: [99-01, 99-02]
files_modified:
  - webpage/src/components/site-header.tsx
  - webpage/src/components/hero-section.tsx
  - webpage/src/components/site-footer.tsx
autonomous: true
requirements: [LP-07, LP-11]

must_haves:
  truths:
    - "O header sticky mostra a marca (BrandMark), navegação por âncoras (#funcionalidades/#confianca/#contacto, escondida abaixo de md), ThemeToggle e o CTA 'Entrar'"
    - "O Hero mostra eyebrow + H1 Display 'Gestão jurídica completa para a sua instituição' + subtítulo (ambos verbatim de CONTEXT.md) + a marca da tenant (logo+nome) no Hero (LP-06)"
    - "O CTA 'Entrar' é um <a href=\"/login\"> real via Button asChild/Slot (NUNCA next/link/<Link>) e aparece no header (topo) e no footer (fundo) — LP-11"
    - "O footer mostra a marca, o CTA 'Entrar' grande (size lg + ArrowRight) e a linha © {ano atual} LexCV"
    - "As cores/tokens seguem o UI-SPEC: acento blue-600/blue-400 só no hover da nav e na hairline do Hero; botões com rounded-none; eyebrow text-sm uppercase tracking-[0.2em]"
  artifacts:
    - path: "webpage/src/components/site-header.tsx"
      provides: "header sticky (h-16, bg-white/80 backdrop-blur), BrandMark, nav âncoras hidden<md, ThemeToggle, Entrar secondary"
      contains: "/login"
    - path: "webpage/src/components/hero-section.tsx"
      provides: "Hero: BrandMark, hairline de acento, eyebrow, H1 text-5xl, subtítulo, CTA Entrar secondary + ghost Ver Funcionalidades"
      contains: "Gestão jurídica completa para a sua instituição"
    - path: "webpage/src/components/site-footer.tsx"
      provides: "footer: BrandMark, Entrar size lg + ArrowRight, © {getFullYear} LexCV"
      contains: "/login"
  key_links:
    - from: "webpage/src/components/site-header.tsx"
      to: "<a href=/login> (Button asChild)"
      via: "Slot/asChild mantém o DOM como <a> real, nunca <Link>"
      pattern: "/login"
    - from: "webpage/src/components/hero-section.tsx"
      to: "BrandMark(branding)"
      via: "personalização da tenant no Hero (LP-06)"
      pattern: "BrandMark"
    - from: "webpage/src/components/site-footer.tsx"
      to: "<a href=/login> (Button asChild size lg)"
      via: "CTA final grande, cross-zone"
      pattern: "/login"
---

<objective>
Construir a "chrome" da landing — o header sticky, a secção Hero, e o footer — os três consumindo `BrandMark`/`branding` (do plano 99-02) e carregando o CTA principal "Entrar" como um `<a href="/login">` real (via o padrão `asChild`/`Slot` do `Button`), presente no topo (header) e no fundo (footer).

Purpose: Isolar a superfície onde o requisito crítico LP-11 vive (o "Entrar" tem de ser `<a>`, nunca `<Link>`, no topo E no fundo) e onde a personalização da tenant aparece (Hero, LP-06), para verificação focada. O Hero é o ponto focal da página (H1 Display 48px), pelo que o seu tratamento tipográfico/espaçamento segue o UI-SPEC ao detalhe.

Output: `site-header.tsx`, `hero-section.tsx`, `site-footer.tsx` — componentes de apresentação (Server Components) que recebem `branding: BrandingResponse` e são compostos por `page.tsx` no plano 99-04.

Cobre: LP-07 (secção Hero com título/subtítulo/proposta de valor) e LP-11 (CTA "Entrar" como <a href="/login">, topo e fundo).
</objective>

<execution_context>
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/milestones/v2.12-phases/LEXCV-99-webpage-nova-app-next-js-de-landing/99-CONTEXT.md
@.planning/milestones/v2.12-phases/LEXCV-99-webpage-nova-app-next-js-de-landing/99-UI-SPEC.md

<!-- Componentes/padrões criados nos planos anteriores (o executor DEVE lê-los antes de compor) -->
@webpage/src/components/brand-mark.tsx
@webpage/src/components/theme-toggle.tsx
@webpage/src/components/ui/button.tsx
@webpage/src/types/branding.ts

<!-- Referência de padrões visuais existentes em web/ (eyebrow/tracking, header sticky) -->
@web/src/app/setup/page.tsx
@web/src/components/shared/dashboard-shell.tsx

<interfaces>
<!-- Contratos e classes EXATAS do UI-SPEC. Usar diretamente — sem re-inventar cores/tipografia/espaçamento. -->

Button (de webpage/src/components/ui/button.tsx, plano 99-01): CVA + asChild/Slot. Base já traz `rounded-md font-medium text-sm`. Variantes: default (primário escuro), secondary, outline, ghost, link. Tamanhos: default (h-9 px-4), sm (h-8 text-xs — NÃO USAR, força text-xs), lg (h-10 px-8), icon.
UI-SPEC (Typography exceção 2): usar `size="default"` + `variant="secondary"` para o CTA pequeno do header; `size="lg"` + `variant="default"` para o CTA grande do footer; NUNCA `size="sm"`. TODOS os Button desta página levam `className="rounded-none"` (override do rounded-md interno).

BrandMark (de webpage/src/components/brand-mark.tsx, plano 99-02): `<BrandMark branding={branding} className?={...} />` — logo data:image/ com fallback Building2 + nome.

Padrão CTA "Entrar" (UI-SPEC Copywriting) — asChild mantém o DOM como <a> real (LP-11):
```tsx
// Header (pequeno/secundário):
<Button asChild variant="secondary" className="rounded-none">
  <a href="/login">Entrar</a>
</Button>
// Footer (grande/primário, CTA final):
<Button asChild size="lg" className="rounded-none">
  <a href="/login">Entrar<ArrowRight className="h-4 w-4" /></a>
</Button>
```

Tokens de cor/tipografia/espaçamento (UI-SPEC — não re-inventar):
- Eyebrow: `inline-flex items-center border border-slate-300 px-3 py-1 text-sm font-semibold uppercase tracking-[0.2em] text-slate-500 dark:border-slate-700 dark:text-slate-400` (Label 14px; padrão de setup/page.tsx standardizado para text-sm).
- H1 (Display 48px, plano/ponto focal): `text-5xl font-semibold tracking-tight text-slate-900 dark:text-slate-50` — FLAT em todos os breakpoints (sem step-down responsivo).
- Subtítulo / body: `text-base text-slate-600 dark:text-slate-300`.
- Hairline de acento do Hero (o ÚNICO uso de acento no Hero): `h-px w-12 bg-blue-600 dark:bg-blue-400`.
- Nav links (Label + hover de acento): `text-sm font-semibold text-slate-600 hover:text-blue-600 dark:text-slate-300 dark:hover:text-blue-400`.
- Header bar: `sticky top-0 z-10 h-16 border-b border-slate-200 bg-white/80 backdrop-blur-md dark:border-slate-800 dark:bg-[#020617]/80` (espelha dashboard-shell.tsx).
- Container: `mx-auto max-w-7xl px-6` (header/footer); Hero `mx-auto max-w-3xl px-6`.
- Padding vertical de secção: `py-12 md:py-16 lg:py-16` (48→64→64, held flat; tokens declarados).
- Neutros novos: usar SÓ `slate-*` (nunca `neutral-*`, que é exceção interna de Button/Badge).
- Border radius: `rounded-none` em toda a página (Cards já são; Buttons precisam de override por call-site).

site-header.tsx — Server Component (renderiza ThemeToggle, que é client — OK, um Server Component pode renderizar um filho client):
```tsx
import { BrandMark } from "@/components/brand-mark";
import { ThemeToggle } from "@/components/theme-toggle";
import { Button } from "@/components/ui/button";
import type { BrandingResponse } from "@/types/branding";

export function SiteHeader({ branding }: { branding: BrandingResponse }) {
  return (
    <header className="sticky top-0 z-10 h-16 border-b border-slate-200 bg-white/80 backdrop-blur-md dark:border-slate-800 dark:bg-[#020617]/80">
      <div className="mx-auto flex h-full max-w-7xl items-center gap-4 px-6">
        <BrandMark branding={branding} />
        <div className="ml-auto flex items-center gap-6">
          <nav className="hidden items-center gap-6 md:flex">
            <a href="#funcionalidades" className="text-sm font-semibold text-slate-600 hover:text-blue-600 dark:text-slate-300 dark:hover:text-blue-400">Funcionalidades</a>
            <a href="#confianca" className="text-sm font-semibold text-slate-600 hover:text-blue-600 dark:text-slate-300 dark:hover:text-blue-400">Confiança</a>
            <a href="#contacto" className="text-sm font-semibold text-slate-600 hover:text-blue-600 dark:text-slate-300 dark:hover:text-blue-400">Contacto</a>
          </nav>
          <div className="flex items-center gap-3">
            <ThemeToggle />
            <Button asChild variant="secondary" className="rounded-none">
              <a href="/login">Entrar</a>
            </Button>
          </div>
        </div>
      </div>
    </header>
  );
}
```

hero-section.tsx — Server Component. O BrandMark no Hero satisfaz LP-06/ROADMAP SC2 ("nome+logo exibidos no Hero"); fica SUBORDINADO ao H1 (pequeno, acima do eyebrow) para o H1 continuar o único ponto focal (UI-SPEC):
```tsx
import { ArrowRight } from "lucide-react";
import { BrandMark } from "@/components/brand-mark";
import { Button } from "@/components/ui/button";
import type { BrandingResponse } from "@/types/branding";

export function HeroSection({ branding }: { branding: BrandingResponse }) {
  return (
    <section className="border-b border-slate-200 py-12 dark:border-slate-800 md:py-16 lg:py-16">
      <div className="mx-auto max-w-3xl px-6">
        <BrandMark branding={branding} className="mb-6" />
        <div className="mb-4 h-px w-12 bg-blue-600 dark:bg-blue-400" />
        <span className="inline-flex items-center border border-slate-300 px-3 py-1 text-sm font-semibold uppercase tracking-[0.2em] text-slate-500 dark:border-slate-700 dark:text-slate-400">
          PLATAFORMA DE GESTÃO JURÍDICA
        </span>
        <h1 className="mt-6 text-5xl font-semibold tracking-tight text-slate-900 dark:text-slate-50">
          Gestão jurídica completa para a sua instituição
        </h1>
        <p className="mt-6 text-base text-slate-600 dark:text-slate-300">
          Clientes, processos, prazos e documentos — tudo num único painel, com isolamento total por tenant.
        </p>
        <div className="mt-8 flex flex-wrap items-center gap-4">
          <Button asChild variant="secondary" className="rounded-none">
            <a href="/login">Entrar</a>
          </Button>
          <Button asChild variant="ghost" className="rounded-none">
            <a href="#funcionalidades">Ver Funcionalidades<ArrowRight className="h-4 w-4" /></a>
          </Button>
        </div>
      </div>
    </section>
  );
}
```

site-footer.tsx — Server Component. Ano corrente computado server-side:
```tsx
import { ArrowRight } from "lucide-react";
import { BrandMark } from "@/components/brand-mark";
import { Button } from "@/components/ui/button";
import type { BrandingResponse } from "@/types/branding";

export function SiteFooter({ branding }: { branding: BrandingResponse }) {
  const ano = new Date().getFullYear();
  return (
    <footer className="border-t border-slate-200 py-12 dark:border-slate-800 md:py-16 lg:py-16">
      <div className="mx-auto flex max-w-7xl flex-col items-center gap-6 px-6 text-center">
        <BrandMark branding={branding} />
        <Button asChild size="lg" className="rounded-none">
          <a href="/login">Entrar<ArrowRight className="h-4 w-4" /></a>
        </Button>
        <p className="text-sm text-slate-500 dark:text-slate-400">
          © {ano} LexCV. Plataforma institucional de gestão jurídica.
        </p>
      </div>
    </footer>
  );
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: site-header.tsx (header sticky + nav âncoras + Entrar secondary)</name>
  <files>webpage/src/components/site-header.tsx (novo)</files>
  <read_first>
    - webpage/src/components/brand-mark.tsx (BrandMark, do plano 99-02)
    - webpage/src/components/theme-toggle.tsx (ThemeToggle, do plano 99-01)
    - webpage/src/components/ui/button.tsx (variantes/asChild)
    - web/src/components/shared/dashboard-shell.tsx (linha ~243: padrão de header sticky h-16 bg-white/80 backdrop-blur-md border-b — espelhar)
    - 99-UI-SPEC.md (Page Structure ponto 1 "Sticky header"; Copywriting "Sticky header" nav labels + Primary CTA)
  </read_first>
  <action>
    Criar `webpage/src/components/site-header.tsx` exatamente como o bloco `<interfaces>`. É um Server Component (pode renderizar `<ThemeToggle />`, que é o único filho client). Estrutura: header sticky `h-16` com `bg-white/80 dark:bg-[#020617]/80 backdrop-blur-md` + `border-b` + `sticky top-0 z-10` (espelha dashboard-shell.tsx); container `mx-auto max-w-7xl px-6`; à esquerda `<BrandMark branding={branding} />`; à direita (agrupado com `ml-auto`) a `<nav>` com 3 âncoras — "Funcionalidades" (#funcionalidades), "Confiança" (#confianca), "Contacto" (#contacto), `hidden md:flex` (escondida abaixo de 768px, sem hamburger para 3 âncoras) e hover de acento `hover:text-blue-600 dark:hover:text-blue-400` — seguida de `<ThemeToggle />` e do CTA "Entrar".

    CTA "Entrar" do header (LP-11, instância do TOPO): `<Button asChild variant="secondary" className="rounded-none"><a href="/login">Entrar</a></Button>`. CRÍTICO: é um `<a href="/login">` real via `asChild`/`Slot` — NUNCA `next/link`/`<Link>` (não importar next/link em lado nenhum). Usar `variant="secondary"` + `size` default (NUNCA `size="sm"`, que forçaria text-xs — UI-SPEC exceção 2).
  </action>
  <acceptance_criteria>
    - site-header.tsx contém `<a href="/login">Entrar</a>` dentro de um `Button asChild variant="secondary"` com `rounded-none`.
    - Contém as 3 âncoras `#funcionalidades`/`#confianca`/`#contacto` com labels "Funcionalidades"/"Confiança"/"Contacto", numa `<nav>` com `hidden md:flex`.
    - Contém `<BrandMark` e `<ThemeToggle`.
    - Header tem `sticky top-0 z-10`, `h-16`, `backdrop-blur-md`.
    - NÃO importa `next/link` nem usa `<Link`; NÃO usa `size="sm"`.
    - `pnpm build` continua a passar.
  </acceptance_criteria>
  <verify>
    <automated>cd webpage && grep -Fq 'href="/login"' src/components/site-header.tsx && grep -Fq '#funcionalidades' src/components/site-header.tsx && grep -Fq 'hidden md:flex' src/components/site-header.tsx && grep -Fq 'sticky top-0' src/components/site-header.tsx && ! grep -Fq 'next/link' src/components/site-header.tsx && ! grep -Fq 'size="sm"' src/components/site-header.tsx && pnpm build</automated>
  </verify>
  <done>
    Header sticky com marca, 3 âncoras (hidden<md), ThemeToggle e "Entrar" como <a href=/login> secondary rounded-none; zero next/link, zero size sm.
  </done>
</task>

<task type="auto">
  <name>Task 2: hero-section.tsx (eyebrow + H1 Display + subtítulo + CTAs + branding)</name>
  <files>webpage/src/components/hero-section.tsx (novo)</files>
  <read_first>
    - webpage/src/components/brand-mark.tsx (BrandMark para a personalização da tenant no Hero)
    - webpage/src/components/ui/button.tsx (variantes secondary/ghost + asChild)
    - web/src/app/setup/page.tsx (linhas ~108-131: padrão de eyebrow bordered uppercase tracking + H1/heading numa página full-bleed institucional)
    - 99-UI-SPEC.md (Typography role assignment Display/Heading/Body/Label; Color "Accent" hairline; Copywriting "Hero"; Page Structure ponto 2 "Hero é o ponto focal")
    - 99-CONTEXT.md (Conteúdo & Tom: H1/subtítulo LOCKED verbatim)
  </read_first>
  <action>
    Criar `webpage/src/components/hero-section.tsx` exatamente como o bloco `<interfaces>` — Server Component. Ordem vertical: `<BrandMark branding={branding} className="mb-6" />` (marca da tenant no Hero, LP-06/ROADMAP SC2 — pequena, subordinada ao H1) → hairline de acento `<div className="mb-4 h-px w-12 bg-blue-600 dark:bg-blue-400" />` (o ÚNICO acento do Hero, UI-SPEC) → eyebrow `PLATAFORMA DE GESTÃO JURÍDICA` (bordered, `text-sm font-semibold uppercase tracking-[0.2em]`) → H1 `text-5xl font-semibold tracking-tight` com o texto LOCKED **"Gestão jurídica completa para a sua instituição"** (verbatim CONTEXT.md; Display 48px FLAT, sem step-down responsivo) → subtítulo `text-base` LOCKED **"Clientes, processos, prazos e documentos — tudo num único painel, com isolamento total por tenant."** → linha de CTAs.

    CTAs do Hero: "Entrar" secondary (`<Button asChild variant="secondary" className="rounded-none"><a href="/login">Entrar</a></Button>`) + um segundo link ghost para scroll in-page **"Ver Funcionalidades"** → `<a href="#funcionalidades">` (UI-SPEC: um link secundário/ghost de scroll, NÃO um segundo CTA de marca a competir). Ambos via asChild (o "Ver Funcionalidades" é âncora interna; o "Entrar" é cross-zone). Container `mx-auto max-w-3xl px-6`; secção `py-12 md:py-16 lg:py-16` + `border-b`. Usar SÓ tokens de espaçamento declarados (mt-6=24/lg, mt-8=32/xl).
  </action>
  <acceptance_criteria>
    - hero-section.tsx contém o H1 exato "Gestão jurídica completa para a sua instituição" com classe `text-5xl`.
    - Contém o subtítulo exato "Clientes, processos, prazos e documentos — tudo num único painel, com isolamento total por tenant.".
    - Contém o eyebrow "PLATAFORMA DE GESTÃO JURÍDICA" (uppercase, tracking-[0.2em]).
    - Contém a hairline de acento `bg-blue-600 dark:bg-blue-400` e `<BrandMark` (personalização LP-06 no Hero).
    - Contém `<a href="/login">Entrar` e `<a href="#funcionalidades">` (Ver Funcionalidades).
    - NÃO importa `next/link`; H1 é flat (sem `sm:text-`/`md:text-`/`lg:text-` no H1).
    - `pnpm build` continua a passar.
  </acceptance_criteria>
  <verify>
    <automated>cd webpage && grep -Fq 'Gestão jurídica completa para a sua instituição' src/components/hero-section.tsx && grep -Fq 'text-5xl' src/components/hero-section.tsx && grep -Fq 'PLATAFORMA DE GESTÃO JURÍDICA' src/components/hero-section.tsx && grep -Fq 'href="/login"' src/components/hero-section.tsx && grep -Fq 'href="#funcionalidades"' src/components/hero-section.tsx && grep -Fq 'bg-blue-600' src/components/hero-section.tsx && ! grep -Fq 'next/link' src/components/hero-section.tsx && pnpm build</automated>
    <human-check>Visualmente (plano 99-04): o H1 é o maior elemento da página, o eyebrow/hairline lê como institucional, a marca da tenant aparece no Hero, dark/light com paridade. Confirmação visual, fora do gate automatizado.</human-check>
  </verify>
  <done>
    Hero com eyebrow + H1 Display (texto locked) + subtítulo (texto locked) + hairline de acento + BrandMark da tenant + "Entrar" secondary e "Ver Funcionalidades" ghost; H1 flat; zero next/link.
  </done>
</task>

<task type="auto">
  <name>Task 3: site-footer.tsx (marca + Entrar grande + © {ano} LexCV)</name>
  <files>webpage/src/components/site-footer.tsx (novo)</files>
  <read_first>
    - webpage/src/components/brand-mark.tsx (BrandMark)
    - webpage/src/components/ui/button.tsx (size lg + variant default + asChild)
    - 99-UI-SPEC.md (Copywriting "Footer"; Primary CTA instância grande/primária footer com ArrowRight; Page Structure ponto 6)
  </read_first>
  <action>
    Criar `webpage/src/components/site-footer.tsx` exatamente como o bloco `<interfaces>` — Server Component. Conteúdo centrado (`flex flex-col items-center gap-6 ... text-center`, container `mx-auto max-w-7xl px-6`), secção `border-t border-slate-200 dark:border-slate-800 py-12 md:py-16 lg:py-16`: `<BrandMark branding={branding} />` (escala pequena) → o CTA "Entrar" GRANDE/primário (instância do FUNDO, LP-11): `<Button asChild size="lg" className="rounded-none"><a href="/login">Entrar<ArrowRight className="h-4 w-4" /></a></Button>` (size lg, variant default primário, com `ArrowRight` a espelhar o submit de setup/page.tsx) → linha de fine print `text-sm text-slate-500 dark:text-slate-400`: **"© {ano} LexCV. Plataforma institucional de gestão jurídica."** com `ano = new Date().getFullYear()` (computado server-side, não hardcoded).

    CRÍTICO (LP-11): o "Entrar" é `<a href="/login">` via asChild — NUNCA next/link/<Link>. Esta é a segunda das duas instâncias exigidas (topo no header, fundo aqui).
  </action>
  <acceptance_criteria>
    - site-footer.tsx contém `<a href="/login">Entrar` dentro de um `Button asChild size="lg"` com `rounded-none` e um `ArrowRight`.
    - Contém a fine print "© {ano} LexCV. Plataforma institucional de gestão jurídica." usando `new Date().getFullYear()` (não um ano hardcoded).
    - Contém `<BrandMark`.
    - NÃO importa `next/link`.
    - `pnpm build` continua a passar.
  </acceptance_criteria>
  <verify>
    <automated>cd webpage && grep -Fq 'href="/login"' src/components/site-footer.tsx && grep -Fq 'size="lg"' src/components/site-footer.tsx && grep -Fq 'getFullYear' src/components/site-footer.tsx && grep -Fq 'Plataforma institucional de gestão jurídica' src/components/site-footer.tsx && ! grep -Fq 'next/link' src/components/site-footer.tsx && pnpm build</automated>
  </verify>
  <done>
    Footer com marca, "Entrar" grande (size lg + ArrowRight, <a href=/login>) e © {ano corrente} LexCV; zero next/link. Segunda instância do CTA (fundo) para LP-11.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Dados de branding (nome/logoDataUrl) → header/hero/footer | Os três componentes recebem `branding` e renderizam-no via `BrandMark`. |
| Visitante → links de saída (/login, #âncoras, mailto no 99-04) | O único destino externo/cross-zone é `/login` (relativo, mesmo domínio); as âncoras são in-page. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-99-02 (herdado) | Tampering / Information Disclosure (XSS) | render de branding em header/hero/footer | mitigate | Estes componentes renderizam a marca EXCLUSIVAMENTE através do `BrandMark` (plano 99-02), que já aplica o guard `data:image/` e o auto-escape de React em `{nome}`. Nenhum destes ficheiros introduz `dangerouslySetInnerHTML` nem coloca dados dinâmicos noutro sítio que não o BrandMark. Zero superfície nova. |
| T-99-06 | Tampering (integridade de navegação cross-zone) | CTA "Entrar" | mitigate | O "Entrar" é `<a href="/login">` estático e literal (nunca derivado de input), via `asChild`/`Slot` — mantém-se um `<a>` real no DOM, resolvido cross-zone pelo Caddy na Phase 100. Proibir `next/link`/`<Link>` (verificado por grep) impede um 404 client-side em produção (o `/login` não existe na tabela de rotas de webpage/). Superfície de segurança: nenhuma (link estático mesmo-domínio). |
| — | (sem input, sem forms) | header/hero/footer | accept | Componentes 100% de apresentação estática: zero inputs, zero forms, zero mutations, zero fetch (o fetch vive em page.tsx no 99-04). Nenhuma fronteira de confiança nova é atravessada; nada a mitigar além do render de branding (T-99-02). |
| T-99-SC | Tampering (supply chain) | — | accept | Não se aplica a este plano: zero instalação de pacotes; usa `lucide-react` (ArrowRight) já instalado no plano 99-01. |
</threat_model>

<verification>
Verificação geral do plano (chrome + Hero):

1. `grep -Fq 'href="/login"'` em site-header.tsx E em site-footer.tsx — "Entrar" no topo E no fundo (LP-11).
2. `! grep -rq 'next/link' webpage/src/components/site-header.tsx webpage/src/components/hero-section.tsx webpage/src/components/site-footer.tsx` — nenhum <Link> (LP-11).
3. `grep -Fq 'Gestão jurídica completa para a sua instituição'` + `grep -Fq 'text-5xl'` no hero — H1 Display locked (LP-07).
4. `grep -Fq 'PLATAFORMA DE GESTÃO JURÍDICA'` + subtítulo locked no hero (LP-07).
5. `grep -Fq 'getFullYear'` no footer — ano dinâmico, não hardcoded.
6. `! grep -Fq 'size="sm"'` no header — respeita a exceção 2 do UI-SPEC.
7. `cd webpage && pnpm build` verde.
</verification>

<success_criteria>
- [ ] LP-07: Hero com eyebrow + H1 Display (texto locked) + subtítulo (proposta de valor, locked) — o ponto focal da página.
- [ ] LP-11: "Entrar" como `<a href="/login">` real (Button asChild/Slot), no header (topo) E no footer (fundo); zero `next/link`/`<Link>`.
- [ ] LP-06 (render): a marca da tenant (BrandMark) aparece no header e no Hero.
- [ ] Cores/tipografia/espaçamento seguem o UI-SPEC: acento blue só na hairline do Hero + hover da nav; botões rounded-none; eyebrow text-sm uppercase tracking-[0.2em]; H1 text-5xl flat.
- [ ] Header sticky h-16 backdrop-blur; footer com © {ano corrente} dinâmico.
- [ ] `pnpm build` verde.
</success_criteria>

<output>
Criar `.planning/milestones/v2.12-phases/LEXCV-99-webpage-nova-app-next-js-de-landing/99-03-SUMMARY.md` quando concluído.
</output>
