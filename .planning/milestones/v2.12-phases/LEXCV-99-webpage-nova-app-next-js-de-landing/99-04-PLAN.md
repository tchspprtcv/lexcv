---
phase: 99-webpage-nova-app-next-js-de-landing
plan: 04
type: execute
wave: 4
depends_on: [99-01, 99-02, 99-03]
files_modified:
  - webpage/src/components/features-section.tsx
  - webpage/src/components/trust-section.tsx
  - webpage/src/components/contact-section.tsx
  - webpage/src/app/page.tsx
autonomous: true
requirements: [LP-06, LP-08, LP-09, LP-10]

must_haves:
  truths:
    - "A secção Funcionalidades (id=funcionalidades) mostra os 6 módulos — Clientes, Processos, Agenda/Prazos, Documentos, Financeiro, Notificações — cada um num Card com ícone azul de acento (LP-08)"
    - "A secção Confiança (id=confianca) mostra EXATAMENTE 4 cards verificáveis (Isolamento Multi-Tenant, RBAC Granular, Trilha de Auditoria, Ecossistema Cabo Verde), sem testemunhos/estatísticas fabricadas; ícones em slate (NÃO acento) (LP-09)"
    - "A secção Contacto (id=contacto) tem um CTA mailto:contacto@lexcv.cv com assunto pré-preenchido — nunca sourced de Tenant.email/telefone (LP-10)"
    - "page.tsx é um Server Component async que faz fetchBranding() uma vez (dynamic force-dynamic) e compõe header/hero/funcionalidades/confiança/contacto/footer, passando branding a header/hero/footer (LP-06)"
    - "page.tsx não contém lógica de redirect (vive só no proxy.ts) — página 'burra'"
    - "As 10 instâncias de CardTitle têm className text-2xl e as 10 de CardDescription têm text-base (UI-SPEC)"
  artifacts:
    - path: "webpage/src/components/features-section.tsx"
      provides: "Funcionalidades: id=funcionalidades, 6 module cards, ícones text-blue-600 dark:text-blue-400, grid 1→md:2→lg:3"
      contains: "funcionalidades"
    - path: "webpage/src/components/trust-section.tsx"
      provides: "Confiança: id=confianca, 4 cards, ícones slate (não acento), grid 1→sm:2→lg:4"
      contains: "confianca"
    - path: "webpage/src/components/contact-section.tsx"
      provides: "Contacto: id=contacto, H2 + body + CTA mailto:contacto@lexcv.cv (Mail icon)"
      contains: "mailto:contacto@lexcv.cv"
    - path: "webpage/src/app/page.tsx"
      provides: "Server Component async: fetchBranding + composição das 6 secções; export const dynamic force-dynamic"
      contains: "fetchBranding"
  key_links:
    - from: "webpage/src/app/page.tsx"
      to: "fetchBranding() → branding para SiteHeader/HeroSection/SiteFooter"
      via: "único fetch server-side, passado por props"
      pattern: "fetchBranding"
    - from: "webpage/src/components/site-header.tsx nav"
      to: "secções id=funcionalidades/confianca/contacto"
      via: "âncoras in-page correspondem aos ids das secções"
      pattern: "id=\"funcionalidades\""
    - from: "webpage/src/components/contact-section.tsx"
      to: "mailto:contacto@lexcv.cv"
      via: "link estático fixo, nunca Tenant.email"
      pattern: "mailto:contacto@lexcv.cv"
---

<objective>
Completar a landing: as três secções de conteúdo (Funcionalidades/Módulos, Confiança Institucional, Contacto) e a montagem final em `page.tsx` — um Server Component async que faz `fetchBranding()` uma única vez e compõe header + hero + as três secções + footer, passando o branding às peças que o exibem.

Purpose: Estas secções entregam o miolo informativo (LP-08/09/10) e o `page.tsx` fecha a integração (LP-06 render + wiring). As âncoras da nav do header (99-03) resolvem contra os `id` destas secções. O `page.tsx` é o único ponto de fetch (CONTEXT.md) e mantém-se "burro" (zero redirect — isso vive no proxy.ts).

Output: `features-section.tsx`, `trust-section.tsx`, `contact-section.tsx`, e o `page.tsx` real (substitui o placeholder do plano 99-01). Com isto a fase está funcionalmente completa e verificável isoladamente por `pnpm dev`/build.

Cobre: LP-06 (page renderiza branding no Hero/header via wiring), LP-08 (Funcionalidades), LP-09 (Confiança), LP-10 (Contacto mailto).
</objective>

<execution_context>
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/milestones/v2.12-phases/LEXCV-99-webpage-nova-app-next-js-de-landing/99-CONTEXT.md
@.planning/milestones/v2.12-phases/LEXCV-99-webpage-nova-app-next-js-de-landing/99-UI-SPEC.md

<!-- Peças a compor (o executor DEVE lê-las antes de montar page.tsx) -->
@webpage/src/components/site-header.tsx
@webpage/src/components/hero-section.tsx
@webpage/src/components/site-footer.tsx
@webpage/src/components/ui/card.tsx
@webpage/src/lib/branding.ts

<interfaces>
<!-- Copy e classes EXATAS do UI-SPEC. Transcrever, não re-inventar. -->

Card (webpage/src/components/ui/card.tsx, plano 99-01): CardHeader tem `flex flex-col gap-1.5 p-6` (o gap-1.5 interno é herdado como-está). CardTitle base `font-semibold leading-none tracking-tight` (SEM tamanho — precisa de `className="text-2xl"` no call-site). CardDescription base `text-sm text-neutral-500 dark:text-neutral-400` (precisa de `className="text-base"` no call-site). Card é `rounded-none` por defeito. UI-SPEC: aplicar `text-2xl` em TODAS as 10 instâncias de CardTitle (6 módulos + 4 confiança) e `text-base` em TODAS as 10 de CardDescription.

Eyebrow/H2 (mesmos tokens do plano 99-03): eyebrow `inline-flex items-center border border-slate-300 px-3 py-1 text-sm font-semibold uppercase tracking-[0.2em] text-slate-500 dark:border-slate-700 dark:text-slate-400`; H2 `text-2xl font-semibold text-slate-900 dark:text-slate-50`. Secção `py-12 md:py-16 lg:py-16`; container `mx-auto max-w-7xl px-6`; grid `gap-6`; espaço eyebrow/heading→grid via `mt-8`.

ACENTO (regra crítica do UI-SPEC — não trocar): os ícones dos 6 cards de MÓDULOS levam acento `text-blue-600 dark:text-blue-400`. Os ícones dos 4 cards de CONFIANÇA NÃO são acento — usam `text-slate-900 dark:text-slate-100` (acento reservado a: ícones de módulo, hover da nav, hairline do Hero — nada mais).

features-section.tsx — Server Component. id="funcionalidades" (alvo da âncora do header). Ícones lucide já usados em web/: Users, Scale, Calendar, FileText, Wallet, Bell. Copy verbatim do UI-SPEC:
```tsx
import { Bell, Calendar, FileText, Scale, Users, Wallet } from "lucide-react";
import { Card, CardDescription, CardHeader, CardTitle } from "@/components/ui/card";

const MODULOS = [
  { icon: Users, title: "Clientes", desc: "Gestão completa de clientes, particulares e empresas, com ficha, procuração e conta corrente." },
  { icon: Scale, title: "Processos", desc: "Processos com partes, fases, decisões, factos e testemunhas, com timeline completa." },
  { icon: Calendar, title: "Agenda/Prazos", desc: "Calendário unificado com deteção automática de prazos críticos e eventos recorrentes." },
  { icon: FileText, title: "Documentos", desc: "Upload, download e organização de documentos por cliente e por processo." },
  { icon: Wallet, title: "Financeiro", desc: "Honorários, pagamentos e conta corrente, com termo de honorários imprimível." },
  { icon: Bell, title: "Notificações", desc: "Alertas automáticos de prazos, novas fases, documentos e atribuições, direto no sino." },
];

export function FeaturesSection() {
  return (
    <section id="funcionalidades" className="py-12 md:py-16 lg:py-16">
      <div className="mx-auto max-w-7xl px-6">
        <span className="inline-flex items-center border border-slate-300 px-3 py-1 text-sm font-semibold uppercase tracking-[0.2em] text-slate-500 dark:border-slate-700 dark:text-slate-400">
          MÓDULOS
        </span>
        <h2 className="mt-4 text-2xl font-semibold text-slate-900 dark:text-slate-50">
          Tudo o que a sua instituição precisa, num só lugar
        </h2>
        <div className="mt-8 grid grid-cols-1 gap-6 md:grid-cols-2 lg:grid-cols-3">
          {MODULOS.map(({ icon: Icon, title, desc }) => (
            <Card key={title}>
              <CardHeader>
                <Icon className="h-6 w-6 text-blue-600 dark:text-blue-400" />
                <CardTitle className="text-2xl">{title}</CardTitle>
                <CardDescription className="text-base">{desc}</CardDescription>
              </CardHeader>
            </Card>
          ))}
        </div>
      </div>
    </section>
  );
}
```

trust-section.tsx — Server Component. id="confianca". EXATAMENTE 4 cards (nunca contadores/estatísticas/testemunhos fabricados — LP-09/CONTEXT.md). Ícones: Lock, ShieldCheck, History, Building2 (todos lucide já instalados). Ícones em SLATE (não acento). Copy verbatim do UI-SPEC:
```tsx
import { Building2, History, Lock, ShieldCheck } from "lucide-react";
import { Card, CardDescription, CardHeader, CardTitle } from "@/components/ui/card";

const CONFIANCA = [
  { icon: Lock, title: "Isolamento Multi-Tenant", desc: "Cada instituição opera em ambiente de dados totalmente isolado, sem partilha entre clientes da plataforma." },
  { icon: ShieldCheck, title: "RBAC Granular", desc: "Perfis Admin, Advogado, Técnico e Assistente com permissões específicas por módulo e ação." },
  { icon: History, title: "Trilha de Auditoria", desc: "Alterações relevantes — fases, atribuições, documentos — ficam registadas e rastreáveis." },
  { icon: Building2, title: "Ecossistema Cabo Verde", desc: "Desenhado para a realidade institucional cabo-verdiana, alinhado ao ecossistema NOSi." },
];

export function TrustSection() {
  return (
    <section id="confianca" className="border-t border-slate-200 py-12 dark:border-slate-800 md:py-16 lg:py-16">
      <div className="mx-auto max-w-7xl px-6">
        <span className="inline-flex items-center border border-slate-300 px-3 py-1 text-sm font-semibold uppercase tracking-[0.2em] text-slate-500 dark:border-slate-700 dark:text-slate-400">
          CONFIANÇA INSTITUCIONAL
        </span>
        <h2 className="mt-4 text-2xl font-semibold text-slate-900 dark:text-slate-50">
          Construído para instituições exigentes
        </h2>
        <div className="mt-8 grid grid-cols-1 gap-6 sm:grid-cols-2 lg:grid-cols-4">
          {CONFIANCA.map(({ icon: Icon, title, desc }) => (
            <Card key={title}>
              <CardHeader>
                <Icon className="h-6 w-6 text-slate-900 dark:text-slate-100" />
                <CardTitle className="text-2xl">{title}</CardTitle>
                <CardDescription className="text-base">{desc}</CardDescription>
              </CardHeader>
            </Card>
          ))}
        </div>
      </div>
    </section>
  );
}
```

contact-section.tsx — Server Component. id="contacto". CTA mailto LOCKED (CONTEXT.md/UI-SPEC), URL-encoded exato; NUNCA sourced de Tenant.email/telefone (LP-10). Ícone Mail:
```tsx
import { Mail } from "lucide-react";
import { Button } from "@/components/ui/button";

export function ContactSection() {
  return (
    <section id="contacto" className="border-t border-slate-200 py-12 dark:border-slate-800 md:py-16 lg:py-16">
      <div className="mx-auto max-w-3xl px-6 text-center">
        <span className="inline-flex items-center border border-slate-300 px-3 py-1 text-sm font-semibold uppercase tracking-[0.2em] text-slate-500 dark:border-slate-700 dark:text-slate-400">
          CONTACTO
        </span>
        <h2 className="mt-4 text-2xl font-semibold text-slate-900 dark:text-slate-50">Pronto para começar?</h2>
        <p className="mt-6 text-base text-slate-600 dark:text-slate-300">
          Fale com a nossa equipa e conheça a plataforma em detalhe.
        </p>
        <div className="mt-8 flex justify-center">
          <Button asChild className="rounded-none">
            <a href="mailto:contacto@lexcv.cv?subject=Pedido%20de%20Demonstra%C3%A7%C3%A3o%20%E2%80%94%20LexCV">
              Pedir Demonstração<Mail className="h-4 w-4" />
            </a>
          </Button>
        </div>
      </div>
    </section>
  );
}
```

page.tsx — Server Component async (SUBSTITUI o placeholder do plano 99-01). `export const dynamic = "force-dynamic"` garante que o build NUNCA chama o backend (o fetch corre por-request). Único fetchBranding(), passado a header/hero/footer; features/trust/contact não precisam de branding. ZERO lógica de redirect (vive no proxy.ts):
```tsx
import { ContactSection } from "@/components/contact-section";
import { FeaturesSection } from "@/components/features-section";
import { HeroSection } from "@/components/hero-section";
import { SiteFooter } from "@/components/site-footer";
import { SiteHeader } from "@/components/site-header";
import { TrustSection } from "@/components/trust-section";
import { fetchBranding } from "@/lib/branding";

export const dynamic = "force-dynamic";

export default async function Home() {
  const branding = await fetchBranding();

  return (
    <>
      <SiteHeader branding={branding} />
      <main>
        <HeroSection branding={branding} />
        <FeaturesSection />
        <TrustSection />
        <ContactSection />
      </main>
      <SiteFooter branding={branding} />
    </>
  );
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: features-section.tsx (Funcionalidades — 6 módulos, ícones de acento)</name>
  <files>webpage/src/components/features-section.tsx (novo)</files>
  <read_first>
    - webpage/src/components/ui/card.tsx (Card/CardHeader/CardTitle/CardDescription — precisam de text-2xl/text-base no call-site)
    - 99-UI-SPEC.md (Copywriting "Funcionalidades/Módulos": eyebrow/H2/6 one-liners + grid; Color "Accent": ícones de módulo em blue)
    - 99-CONTEXT.md (Conteúdo & Tom: módulos refletem capacidades reais validadas, nunca inventadas)
  </read_first>
  <action>
    Criar `webpage/src/components/features-section.tsx` exatamente como o bloco `<interfaces>`. Server Component, `<section id="funcionalidades">` (o `id` é o alvo da âncora "Funcionalidades" do header). Eyebrow "MÓDULOS" + H2 "Tudo o que a sua instituição precisa, num só lugar". Grid `grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6` com 6 `Card` (um por módulo), na ordem e com a copy VERBATIM do UI-SPEC: Clientes (Users), Processos (Scale), Agenda/Prazos (Calendar), Documentos (FileText), Financeiro (Wallet), Notificações (Bell).

    Cada card: `<CardHeader>` com o ícone `<Icon className="h-6 w-6 text-blue-600 dark:text-blue-400" />` (ACENTO — só os ícones de módulo o levam), `<CardTitle className="text-2xl">{title}</CardTitle>` e `<CardDescription className="text-base">{desc}</CardDescription>`. Não sobrepor o `gap-1.5`/`p-6` internos do CardHeader (herdados como-está).
  </action>
  <acceptance_criteria>
    - features-section.tsx tem `<section id="funcionalidades">` e os 6 títulos: Clientes, Processos, Agenda/Prazos, Documentos, Financeiro, Notificações.
    - Os 6 ícones de módulo usam `text-blue-600 dark:text-blue-400` (acento).
    - As 6 instâncias de CardTitle têm `text-2xl`; as 6 de CardDescription têm `text-base`.
    - Grid `grid-cols-1 md:grid-cols-2 lg:grid-cols-3`.
    - Eyebrow "MÓDULOS" + H2 "Tudo o que a sua instituição precisa, num só lugar".
    - `pnpm build` continua a passar.
  </acceptance_criteria>
  <verify>
    <automated>cd webpage && grep -Fq 'id="funcionalidades"' src/components/features-section.tsx && grep -Fq 'text-blue-600' src/components/features-section.tsx && grep -Fq 'lg:grid-cols-3' src/components/features-section.tsx && grep -Fq 'termo de honorários imprimível' src/components/features-section.tsx && grep -Fq 'Clientes' src/components/features-section.tsx && grep -Fq 'Processos' src/components/features-section.tsx && grep -Fq 'Agenda/Prazos' src/components/features-section.tsx && grep -Fq 'Documentos' src/components/features-section.tsx && grep -Fq 'Financeiro' src/components/features-section.tsx && grep -Fq 'Notificações' src/components/features-section.tsx && grep -Fq 'CardTitle className="text-2xl"' src/components/features-section.tsx && grep -Fq 'CardDescription className="text-base"' src/components/features-section.tsx && pnpm build</automated>
  </verify>
  <done>
    Secção Funcionalidades com id=funcionalidades e os 6 módulos (copy verbatim), ícones azuis de acento, CardTitle text-2xl + CardDescription text-base, grid 1→md:2→lg:3.
  </done>
</task>

<task type="auto">
  <name>Task 2: trust-section.tsx (Confiança — 4 cards) + contact-section.tsx (mailto)</name>
  <files>
    webpage/src/components/trust-section.tsx (novo),
    webpage/src/components/contact-section.tsx (novo)
  </files>
  <read_first>
    - webpage/src/components/ui/card.tsx (mesmos primitivos)
    - webpage/src/components/ui/button.tsx (Button asChild para o mailto)
    - 99-UI-SPEC.md (Copywriting "Confiança Institucional" 4 tópicos + "Contacto" mailto; Color: ícones de confiança NÃO são acento)
    - 99-CONTEXT.md (Prova Social: EXATAMENTE 4 bullets verificáveis, nunca fabricados; Contacto: mailto:contacto@lexcv.cv assunto "Pedido de Demonstração — LexCV")
  </read_first>
  <action>
    Criar `webpage/src/components/trust-section.tsx` exatamente como o bloco `<interfaces>`. Server Component, `<section id="confianca">` (alvo da âncora "Confiança"), `border-t`. Eyebrow "CONFIANÇA INSTITUCIONAL" + H2 "Construído para instituições exigentes". Grid `grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6` com EXATAMENTE 4 Card (LP-09 — nunca contadores/estatísticas/testemunhos fabricados), copy VERBATIM: Isolamento Multi-Tenant (Lock), RBAC Granular (ShieldCheck), Trilha de Auditoria (History), Ecossistema Cabo Verde (Building2). CRÍTICO — os ícones de confiança usam `text-slate-900 dark:text-slate-100` (NÃO `text-blue-*`; o acento é reservado aos ícones de módulo). CardTitle `text-2xl`, CardDescription `text-base`.

    Criar `webpage/src/components/contact-section.tsx` exatamente como o bloco `<interfaces>`. Server Component, `<section id="contacto">` (alvo da âncora "Contacto"), `border-t`, container `max-w-3xl text-center`. Eyebrow "CONTACTO" + H2 "Pronto para começar?" + body "Fale com a nossa equipa e conheça a plataforma em detalhe." + CTA: `<Button asChild className="rounded-none"><a href="mailto:contacto@lexcv.cv?subject=Pedido%20de%20Demonstra%C3%A7%C3%A3o%20%E2%80%94%20LexCV">Pedir Demonstração<Mail className="h-4 w-4" /></a></Button>`. O mailto é LOCKED e FIXO — nunca sourced de `Tenant.email`/`telefone` (LP-10); manter a query string URL-encoded exata.
  </action>
  <acceptance_criteria>
    - trust-section.tsx tem `<section id="confianca">` e os 4 títulos: Isolamento Multi-Tenant, RBAC Granular, Trilha de Auditoria, Ecossistema Cabo Verde (exatamente 4 cards).
    - Os ícones de confiança usam `text-slate-900 dark:text-slate-100` e NÃO `text-blue-600` (não-acento).
    - Grid `grid-cols-1 sm:grid-cols-2 lg:grid-cols-4`; CardTitle text-2xl + CardDescription text-base.
    - contact-section.tsx tem `<section id="contacto">` e o CTA `mailto:contacto@lexcv.cv?subject=Pedido%20de%20Demonstra%C3%A7%C3%A3o%20%E2%80%94%20LexCV`.
    - contact-section.tsx NÃO referencia `Tenant.email`/`telefone`/branding (link estático).
    - `pnpm build` continua a passar.
  </acceptance_criteria>
  <verify>
    <automated>cd webpage && grep -Fq 'id="confianca"' src/components/trust-section.tsx && grep -Fq 'Isolamento Multi-Tenant' src/components/trust-section.tsx && grep -Fq 'RBAC Granular' src/components/trust-section.tsx && grep -Fq 'Trilha de Auditoria' src/components/trust-section.tsx && grep -Fq 'Ecossistema Cabo Verde' src/components/trust-section.tsx && grep -Fq 'lg:grid-cols-4' src/components/trust-section.tsx && ! grep -Fq 'text-blue-600' src/components/trust-section.tsx && grep -Fq 'CardTitle className="text-2xl"' src/components/trust-section.tsx && grep -Fq 'CardDescription className="text-base"' src/components/trust-section.tsx && grep -Fq 'id="contacto"' src/components/contact-section.tsx && grep -Fq 'mailto:contacto@lexcv.cv' src/components/contact-section.tsx && pnpm build</automated>
  </verify>
  <done>
    Confiança com 4 cards verificáveis (ícones slate, não acento) + Contacto com CTA mailto fixo URL-encoded (nunca Tenant.email); grids e tokens do UI-SPEC.
  </done>
</task>

<task type="auto">
  <name>Task 3: page.tsx — montagem final (fetchBranding + composição das 6 secções)</name>
  <files>webpage/src/app/page.tsx (substitui o placeholder do plano 99-01)</files>
  <read_first>
    - webpage/src/lib/branding.ts (fetchBranding, do plano 99-02)
    - webpage/src/components/site-header.tsx (do plano 99-03 — recebe branding)
    - webpage/src/components/hero-section.tsx (do plano 99-03 — recebe branding)
    - webpage/src/components/site-footer.tsx (do plano 99-03 — recebe branding)
    - webpage/src/components/features-section.tsx, trust-section.tsx, contact-section.tsx (Tasks 1-2 deste plano)
    - 99-CONTEXT.md (Fetch de Branding: server-side no Server Component async de page.tsx; page.tsx mantém-se "burra" — redirect só no proxy.ts)
  </read_first>
  <action>
    Substituir `webpage/src/app/page.tsx` (o placeholder do plano 99-01) pelo bloco `<interfaces>`: um Server Component async `Home()` com `export const dynamic = "force-dynamic"` (garante que o build não chama o backend — o fetch é por-request). Fazer `const branding = await fetchBranding();` UMA vez e compor, por esta ordem: `<SiteHeader branding={branding} />`, depois um `<main>` com `<HeroSection branding={branding} />` + `<FeaturesSection />` + `<TrustSection />` + `<ContactSection />`, depois `<SiteFooter branding={branding} />`. As secções features/trust/contact não recebem props (conteúdo estático). page.tsx NÃO contém nenhuma lógica de redirect/gate (isso vive exclusivamente no proxy.ts — CONTEXT.md "page.tsx burra").

    Após escrever, correr `pnpm build` (tem de passar SEM backend, graças ao force-dynamic) e `pnpm dev -p 3001` para a verificação visual (human-check abaixo).
  </action>
  <acceptance_criteria>
    - page.tsx é async, tem `export const dynamic = "force-dynamic"`, e faz `await fetchBranding()` exatamente uma vez.
    - Renderiza SiteHeader, HeroSection, FeaturesSection, TrustSection, ContactSection, SiteFooter (as 6 peças), passando `branding` a header/hero/footer.
    - page.tsx NÃO contém `redirect`, `NextResponse`, `access_token`, nem qualquer lógica de gate (é "burra").
    - `pnpm build` passa SEM o backend a correr (prova do force-dynamic).
    - No repositório inteiro webpage/src, nenhum ficheiro importa `next/link` (LP-11 global).
  </acceptance_criteria>
  <verify>
    <automated>cd webpage && grep -Fq 'force-dynamic' src/app/page.tsx && grep -Fq 'fetchBranding' src/app/page.tsx && grep -Fq 'SiteHeader' src/app/page.tsx && grep -Fq 'HeroSection' src/app/page.tsx && grep -Fq 'FeaturesSection' src/app/page.tsx && grep -Fq 'TrustSection' src/app/page.tsx && grep -Fq 'ContactSection' src/app/page.tsx && grep -Fq 'SiteFooter' src/app/page.tsx && ! grep -Fiq 'redirect' src/app/page.tsx && ! grep -rq 'next/link' src/ && pnpm build</automated>
    <human-check>
      Verificação visual da landing completa (CONTEXT.md: "verificada isoladamente via pnpm dev/build próprios"). Com o backend a correr + sistema inicializado (ou aceitar o fallback "LexCV" se o backend estiver desligado):
      1. `cd webpage && pnpm dev` → abrir http://localhost:3001/ .
      2. Hero: H1 grande + eyebrow + subtítulo + marca da tenant (logo/nome real, ou Building2+"LexCV" no fallback).
      3. Funcionalidades: 6 cards (Clientes/Processos/Agenda-Prazos/Documentos/Financeiro/Notificações), ícones azuis.
      4. Confiança: 4 cards (ícones slate, não azuis); Contacto: botão "Pedir Demonstração" abre o cliente de email com o assunto pré-preenchido.
      5. Footer: "Entrar" grande + © do ano corrente.
      6. Alternar dark/light no toggle do header — paridade total, sem cores partidas.
      7. Redimensionar para <768px: as âncoras da nav escondem-se, os grids colapsam para 1 coluna, layout mantém-se.
      8. Clicar "Entrar" (topo e fundo): confirmar que é `<a href="/login">` (em dev isolado dá 404 — ESPERADO; resolve cross-zone só atrás do Caddy na Phase 100).
      9. Clicar as âncoras da nav → scroll para as secções corretas.
      Responder "aprovado" ou descrever problemas. (Segue o padrão do 98-01: human-check embutido numa task auto, não um checkpoint bloqueante.)
    </human-check>
  </verify>
  <done>
    page.tsx (Server Component async, force-dynamic) faz um único fetchBranding() e compõe as 6 secções passando branding a header/hero/footer; zero lógica de redirect; build verde sem backend; landing completa servida em pnpm dev; zero next/link em toda a webpage/src.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| webpage (servidor) → backend (fetchBranding em page.tsx) | Fetch server-to-server no render de page.tsx (por-request, force-dynamic); herda a fronteira/mitigações do plano 99-02. |
| Copy estática das secções → DOM | Funcionalidades/Confiança/Contacto renderizam SÓ strings autoradas hardcoded; zero dados de tenant/visitante. |
| Visitante → mailto:contacto@lexcv.cv | Link estático fixo; abre o cliente de email do visitante. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-99-07 | Tampering / Information Disclosure (XSS) | features/trust/contact + page.tsx | mitigate | As três secções renderizam EXCLUSIVAMENTE copy estática autorada (arrays MODULOS/CONFIANCA hardcoded, headings/body literais) — zero dados de tenant ou de visitante. O único dado dinâmico (branding) flui de page.tsx para o `BrandMark` (guarded, T-99-02) via header/hero/footer, nunca para estas secções. Zero `dangerouslySetInnerHTML` em toda a página. |
| T-99-08 | Information Disclosure | contact-section.tsx (mailto) | mitigate | O CTA de contacto é um `mailto:contacto@lexcv.cv` FIXO e hardcoded — NUNCA sourced de `Tenant.email`/`Tenant.telefone` (LP-10). Impossível vazar o contacto real da tenant por construção (o componente nem recebe branding). Verificado por grep (mailto literal presente; sem referência a branding/email/telefone). |
| T-99-09 | Denial of Service / Availability | page.tsx (force-dynamic + fetchBranding) | accept | `export const dynamic = "force-dynamic"` + o fail-open de `fetchBranding` (T-99-03) garantem que: (a) o build NUNCA depende do backend (não há fetch em build-time), e (b) em runtime, backend em baixo → landing renderiza com fallback "LexCV", nunca crash. Tradeoff de disponibilidade deliberado (herdado do 99-02), aceite. |
| T-99-SC | Tampering (supply chain) | — | accept | Não se aplica: zero instalação de pacotes; usa `lucide-react` (Users/Scale/.../Mail) já instalado no plano 99-01. |
</threat_model>

<verification>
Verificação geral do plano (secções + montagem) e da FASE completa:

1. Funcionalidades (LP-08): `grep -Fq 'id="funcionalidades"'` + os 6 títulos de módulo + `text-blue-600` + `lg:grid-cols-3`.
2. Confiança (LP-09): `grep -Fq 'id="confianca"'` + 4 títulos + `! grep -Fq 'text-blue-600'` (ícones não-acento) + `lg:grid-cols-4`.
3. Contacto (LP-10): `grep -Fq 'mailto:contacto@lexcv.cv'` + `id="contacto"`, sem referência a Tenant.email/telefone.
4. Montagem (LP-06): `grep -Fq 'fetchBranding'` + `force-dynamic` + as 6 secções em page.tsx; branding passado a header/hero/footer.
5. page.tsx "burra": `! grep -Fiq 'redirect' src/app/page.tsx` (redirect só no proxy.ts).
6. LP-11 global: `! grep -rq 'next/link' webpage/src/` (nenhum <Link> em toda a app).
7. `cd webpage && pnpm build` passa SEM backend (force-dynamic).
8. (human-check) `pnpm dev` → landing completa, dark/light, responsivo, "Entrar" ×2, âncoras, mailto — critérios de sucesso do ROADMAP Phase 99.
</verification>

<success_criteria>
- [ ] LP-08: secção Funcionalidades (id=funcionalidades) com os 6 módulos (Clientes/Processos/Agenda-Prazos/Documentos/Financeiro/Notificações), ícones de acento, grid 1→md:2→lg:3.
- [ ] LP-09: secção Confiança (id=confianca) com EXATAMENTE 4 cards verificáveis (isolamento/RBAC/auditoria/NOSi-CV), ícones slate não-acento, sem fabricação.
- [ ] LP-10: secção Contacto (id=contacto) com CTA mailto:contacto@lexcv.cv fixo (assunto pré-preenchido), nunca de Tenant.email/telefone.
- [ ] LP-06 (wiring): page.tsx faz fetchBranding() uma vez e passa branding a header/hero/footer; render da personalização confirmado.
- [ ] page.tsx "burra" (zero redirect; force-dynamic; build sem backend); zero next/link em toda a webpage/src.
- [ ] 10× CardTitle text-2xl + 10× CardDescription text-base (UI-SPEC); acento só em ícones de módulo/nav-hover/hero-hairline.
- [ ] `pnpm build` verde + landing completa servida em `pnpm dev` (human-check dark/light + responsivo + Entrar ×2 + âncoras + mailto).
</success_criteria>

<output>
Criar `.planning/milestones/v2.12-phases/LEXCV-99-webpage-nova-app-next-js-de-landing/99-04-SUMMARY.md` quando concluído.
</output>
