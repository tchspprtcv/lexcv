---
phase: 99-webpage-nova-app-next-js-de-landing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - webpage/package.json
  - webpage/tsconfig.json
  - webpage/postcss.config.mjs
  - webpage/eslint.config.mjs
  - webpage/next.config.ts
  - webpage/.env.example
  - webpage/.gitignore
  - webpage/src/app/globals.css
  - webpage/src/app/providers.tsx
  - webpage/src/app/layout.tsx
  - webpage/src/app/page.tsx
  - webpage/src/lib/utils.ts
  - webpage/src/components/ui/button.tsx
  - webpage/src/components/ui/card.tsx
  - webpage/src/components/theme-toggle.tsx
autonomous: true
requirements: [LP-03, LP-12]

must_haves:
  truths:
    - "webpage/ existe como app Next.js 16 standalone com o seu próprio package.json (sem pnpm workspace) e `pnpm install` resolve as dependências"
    - "`pnpm build` na webpage/ compila com sucesso (com BACKEND_API_ORIGIN definido)"
    - "A app suporta dark/light via next-themes portado em modo theme-only (sem QueryClientProvider)"
    - "next.config.ts define assetPrefix '/landing-static' e output 'standalone'"
    - "Os primitivos Button/Card e o ThemeToggle são cópias byte-for-byte de web/, com zero novas dependências de UI (LP-12)"
  artifacts:
    - path: "webpage/package.json"
      provides: "manifesto standalone com subconjunto pinado das deps de web/ (lucide-react, next-themes, cva, clsx, tailwind-merge, @radix-ui/react-slot); scripts dev/start na porta 3001"
      contains: "next-themes"
    - path: "webpage/next.config.ts"
      provides: "assetPrefix '/landing-static' (LP-13 estrutural), output 'standalone', rewrite /api/v1, security headers com img-src data:, fail-fast em BACKEND_API_ORIGIN"
      contains: "landing-static"
    - path: "webpage/src/app/providers.tsx"
      provides: "NextThemesProvider theme-only (sem QueryClientProvider)"
      contains: "NextThemesProvider"
    - path: "webpage/src/app/layout.tsx"
      provides: "root layout com fontes Geist + Providers, body scrollável (sem overflow-hidden), sem Toaster"
      contains: "Providers"
    - path: "webpage/src/components/ui/button.tsx"
      provides: "primitivo Button (CVA + asChild/Slot) copiado byte-for-byte de web/"
      contains: "buttonVariants"
    - path: "webpage/src/components/ui/card.tsx"
      provides: "primitivos Card/CardHeader/CardTitle/CardDescription/CardContent copiados byte-for-byte de web/"
      contains: "CardDescription"
    - path: "webpage/src/components/theme-toggle.tsx"
      provides: "toggle dark/light copiado byte-for-byte de web/"
      contains: "useTheme"
  key_links:
    - from: "webpage/src/app/layout.tsx"
      to: "webpage/src/app/providers.tsx"
      via: "<Providers> envolve children"
      pattern: "Providers"
    - from: "webpage/src/app/providers.tsx"
      to: "next-themes"
      via: "NextThemesProvider attribute=class defaultTheme=system"
      pattern: "NextThemesProvider"
    - from: "webpage/next.config.ts"
      to: "assetPrefix"
      via: "config Multi-Zones para _next/static próprio"
      pattern: "landing-static"
---

<objective>
Criar o esqueleto completo e compilável da nova app Next.js 16 standalone `webpage/` (sibling de `web/`/`backend/` na raiz do monorepo): configuração (package.json com subconjunto pinado das deps de `web/`, tsconfig, postcss, eslint, next.config.ts), a shell de tema (globals.css, providers theme-only, layout), os primitivos de UI reutilizados (`Button`, `Card`, `ThemeToggle`, `cn`) copiados de `web/`, e uma `page.tsx` placeholder mínima para tornar o build verificável.

Purpose: Estabelecer uma fundação buildável e com dark/light funcional sobre a qual os planos seguintes constroem o gate de setup (99-02), o header/hero/footer (99-03) e as secções de conteúdo (99-04). Zero novas dependências de UI (LP-12) — tudo é subconjunto verbatim do que `web/` já usa em produção.

Output: A árvore `webpage/` arranca com `pnpm install` + `pnpm build`, serve uma página placeholder com `ThemeToggle` funcional, e tem `assetPrefix: '/landing-static'` + `output: 'standalone'` configurados (fundação estrutural para Multi-Zones da Phase 100).

Cobre: LP-03 (nova app Next.js standalone `webpage/`) e LP-12 (dark/light + reutilização de primitivos shadcn/Tailwind de `web/`, sem novas deps de UI).
</objective>

<execution_context>
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/milestones/v2.12-phases/LEXCV-99-webpage-nova-app-next-js-de-landing/99-CONTEXT.md
@.planning/milestones/v2.12-phases/LEXCV-99-webpage-nova-app-next-js-de-landing/99-UI-SPEC.md

<!-- Ficheiros-fonte a copiar/adaptar (o executor DEVE lê-los antes de tocar em código). CLAUDE.md: usar SEMPRE pnpm (nunca npm); o package-lock.json em web/ é obsoleto — ignorar. -->
@web/package.json
@web/tsconfig.json
@web/postcss.config.mjs
@web/eslint.config.mjs
@web/next.config.ts
@web/.env.example
@web/.gitignore
@web/src/app/globals.css
@web/src/app/providers.tsx
@web/src/app/layout.tsx
@web/src/lib/utils.ts
@web/src/components/ui/button.tsx
@web/src/components/ui/card.tsx
@web/src/components/theme-toggle.tsx

<!-- AVISO Next.js 16 (web/AGENTS.md): esta versão tem breaking changes vs. training data. Antes de escrever next.config.ts/layout/routing, confirmar convenções em web/node_modules/next/dist/docs/ em vez de assumir. Confirmado nesta fase: proxy.ts (não middleware.ts) é a convenção — mas proxy.ts é do plano 99-02, não deste. -->

<interfaces>
<!-- Conteúdos-alvo dos ficheiros ADAPTADOS (diferem da fonte web/). Os ficheiros marcados "byte-for-byte" copiam-se sem alteração da fonte referenciada acima. -->

package.json — subconjunto CURADO das deps de web/package.json. EXCLUIR explicitamente (decisão CONTEXT.md): @tanstack/react-query, react-hook-form, zod, @hookform/resolvers, e todos os @radix-ui/* EXCETO react-slot (não há forms/dialogs/popovers nesta app). Copiar as version strings EXATAS de web/package.json (nunca "latest"):
```json
{
  "name": "webpage",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev -p 3001",
    "build": "next build",
    "start": "next start -p 3001",
    "lint": "eslint"
  },
  "dependencies": {
    "@radix-ui/react-slot": "^1.2.3",
    "class-variance-authority": "^0.7.1",
    "clsx": "^2.1.1",
    "lucide-react": "^0.543.0",
    "next": "16.2.6",
    "next-themes": "^0.4.6",
    "react": "19.2.4",
    "react-dom": "19.2.4",
    "tailwind-merge": "^3.3.1",
    "tailwindcss-animate": "^1.0.7"
  },
  "devDependencies": {
    "@tailwindcss/postcss": "^4",
    "@types/node": "^20",
    "@types/react": "^19",
    "@types/react-dom": "^19",
    "eslint": "^9",
    "eslint-config-next": "16.2.6",
    "tailwindcss": "^4",
    "typescript": "^5"
  }
}
```

next.config.ts — ADAPTADO de web/next.config.ts: acrescenta `assetPrefix: '/landing-static'` (LP-13), mantém `output: 'standalone'`, o rewrite `/api/v1/:path*` (necessário para o fetch relativo de setup-status do proxy.ts no plano 99-02) e o bloco de security headers VERBATIM (o CSP `img-src 'self' data:` é obrigatório para o logo data-URL). Mantém o fail-fast em BACKEND_API_ORIGIN:
```ts
import type { NextConfig } from "next";

const backendOrigin = process.env.BACKEND_API_ORIGIN;
if (!backendOrigin) {
  throw new Error("BACKEND_API_ORIGIN is required");
}

const nextConfig: NextConfig = {
  output: "standalone",
  assetPrefix: "/landing-static",
  async rewrites() {
    return [
      { source: "/api/v1/:path*", destination: `${backendOrigin}/api/v1/:path*` },
    ];
  },
  async headers() {
    return [
      {
        source: "/(.*)",
        headers: [
          { key: "X-Content-Type-Options", value: "nosniff" },
          { key: "X-Frame-Options", value: "DENY" },
          { key: "X-XSS-Protection", value: "1; mode=block" },
          { key: "Strict-Transport-Security", value: "max-age=31536000; includeSubDomains" },
          { key: "Content-Security-Policy", value: "default-src 'self'; script-src 'self' 'unsafe-inline' 'unsafe-eval'; style-src 'self' 'unsafe-inline'; img-src 'self' data:; connect-src 'self';" },
        ],
      },
    ];
  },
};

export default nextConfig;
```

providers.tsx — ADAPTADO (theme-only slice de web/src/app/providers.tsx; REMOVER QueryClient/QueryClientProvider por completo, per CONTEXT.md):
```tsx
"use client";

import { ThemeProvider as NextThemesProvider } from "next-themes";
import * as React from "react";

export function Providers({ children }: { children: React.ReactNode }) {
  return (
    <NextThemesProvider
      attribute="class"
      defaultTheme="system"
      enableSystem
      disableTransitionOnChange
    >
      {children}
    </NextThemesProvider>
  );
}
```

layout.tsx — ADAPTADO de web/src/app/layout.tsx: mantém as fontes Geist/Geist_Mono + variáveis CSS + <Providers>; REMOVE o import e render de <Toaster> (não há toasts) e REMOVE `h-full flex flex-col overflow-hidden` do body (a landing tem de fazer scroll natural) → body `min-h-screen bg-background text-foreground`. Mantém `suppressHydrationWarning` (next-themes exige-o). metadata atualizada para a landing:
```tsx
import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";
import { Providers } from "./providers";

const geistSans = Geist({ variable: "--font-geist-sans", subsets: ["latin"] });
const geistMono = Geist_Mono({ variable: "--font-geist-mono", subsets: ["latin"] });

export const metadata: Metadata = {
  title: "LexCV — Plataforma de Gestão Jurídica",
  description:
    "Gestão jurídica completa para a sua instituição — clientes, processos, prazos e documentos num único painel, com isolamento total por tenant.",
};

export default function RootLayout({ children }: Readonly<{ children: React.ReactNode }>) {
  return (
    <html
      lang="pt-CV"
      className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      suppressHydrationWarning
    >
      <body className="min-h-screen bg-background text-foreground">
        <Providers>{children}</Providers>
      </body>
    </html>
  );
}
```

page.tsx — PLACEHOLDER mínimo (será SUBSTITUÍDO integralmente pelo plano 99-04). Serve só para tornar `pnpm build`/`pnpm dev` verificáveis e provar que a shell de tema + um primitivo (Button via ThemeToggle) renderizam com dark/light:
```tsx
import { ThemeToggle } from "@/components/theme-toggle";

export default function Home() {
  return (
    <main className="flex min-h-screen flex-col items-center justify-center gap-4">
      <ThemeToggle />
      <p className="text-base text-slate-500 dark:text-slate-400">
        LexCV — landing em construção (placeholder, substituído no plano 99-04)
      </p>
    </main>
  );
}
```

tsconfig.json — copiar de web/tsconfig.json mas simplificar o `exclude` para `["node_modules"]` (webpage/ não tem `src/app/_api-backup`). Manter o alias `"@/*": ["./src/*"]` e todas as compilerOptions.

globals.css, lib/utils.ts, components/ui/button.tsx, components/ui/card.tsx, components/theme-toggle.tsx — copiar BYTE-FOR-BYTE das fontes web/ referenciadas em <context>. Não alterar uma linha. (button.tsx usa @radix-ui/react-slot; card.tsx já é rounded-none por defeito; theme-toggle.tsx é "use client" + useTheme.)

postcss.config.mjs, eslint.config.mjs, .env.example, .gitignore — copiar BYTE-FOR-BYTE de web/. (.gitignore ignora node_modules/.next/.env* mas NÃO o pnpm-lock.yaml — que é committed, autoritativo per CLAUDE.md.)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffolding e configuração da app standalone webpage/</name>
  <files>
    webpage/package.json (novo),
    webpage/tsconfig.json (novo),
    webpage/postcss.config.mjs (novo),
    webpage/eslint.config.mjs (novo),
    webpage/next.config.ts (novo),
    webpage/.env.example (novo),
    webpage/.gitignore (novo)
  </files>
  <read_first>
    - web/package.json (fonte das version strings EXATAS das deps; copiar só o subconjunto curado)
    - web/tsconfig.json (copiar, simplificar exclude)
    - web/postcss.config.mjs (copiar byte-for-byte)
    - web/eslint.config.mjs (copiar byte-for-byte)
    - web/next.config.ts (fonte a adaptar: acrescentar assetPrefix, manter rewrite/headers/output/fail-fast)
    - web/.env.example (copiar byte-for-byte)
    - web/.gitignore (copiar byte-for-byte)
    - CLAUDE.md (secção Frontend: usar SEMPRE pnpm; pnpm-lock.yaml é autoritativo; ignorar package-lock.json)
  </read_first>
  <action>
    Criar a diretoria `webpage/` na raiz do monorepo (sibling de `backend/`/`web/`; não existe pnpm workspace raiz — cada app é standalone com o seu próprio node_modules/lockfile).

    Criar `webpage/package.json` exatamente como o bloco `<interfaces>` acima: name `"webpage"`, scripts com porta **3001** (`next dev -p 3001` / `next start -p 3001`, para coexistir com o `web/` na 3000), e as deps/devDeps do subconjunto curado com as version strings COPIADAS DE web/package.json (nunca resolver "latest" — ver threat model T-99-SC). EXCLUIR @tanstack/react-query, react-hook-form, zod, @hookform/resolvers e todos os @radix-ui exceto react-slot.

    Criar `webpage/next.config.ts` como o bloco `<interfaces>`: `output: "standalone"` + `assetPrefix: "/landing-static"` (LP-13) + o rewrite `/api/v1/:path*` → BACKEND_API_ORIGIN + o bloco de security headers VERBATIM de web/ (crítico: CSP com `img-src 'self' data:` para o logo data-URL) + fail-fast `if (!backendOrigin) throw`.

    Copiar `webpage/tsconfig.json` de web/ (simplificar `exclude` para `["node_modules"]`, manter o alias `@/*`). Copiar `webpage/postcss.config.mjs`, `webpage/eslint.config.mjs`, `webpage/.env.example`, `webpage/.gitignore` byte-for-byte de web/.

    Correr `pnpm install` DENTRO de `webpage/` (gera `webpage/pnpm-lock.yaml` — committed, autoritativo; NÃO criar package-lock.json). Criar também `webpage/.env.local` (gitignored, não committed) com `BACKEND_API_ORIGIN=http://localhost:8080` e `NEXT_PUBLIC_API_BASE_PATH=/api/v1` para tornar o build/dev locais verificáveis (o next.config.ts faz throw sem BACKEND_API_ORIGIN).
  </action>
  <acceptance_criteria>
    - `webpage/package.json` existe, tem `"name": "webpage"`, scripts com `-p 3001`, e NÃO contém `@tanstack/react-query`, `react-hook-form` nem `zod`.
    - `webpage/package.json` contém as deps `next-themes`, `lucide-react`, `class-variance-authority`, `clsx`, `tailwind-merge`, `@radix-ui/react-slot` com as MESMAS version strings de web/package.json.
    - `webpage/next.config.ts` contém `assetPrefix: "/landing-static"`, `output: "standalone"`, o rewrite `/api/v1/:path*` e `img-src 'self' data:` no CSP.
    - `pnpm install` em webpage/ conclui sem erros e gera `webpage/pnpm-lock.yaml`.
    - `webpage/.gitignore` ignora `.env*` (exceto `.env.example`) e NÃO ignora `pnpm-lock.yaml`.
  </acceptance_criteria>
  <verify>
    <automated>cd webpage && pnpm install && test -f pnpm-lock.yaml && grep -Fq 'landing-static' next.config.ts && grep -Fq 'next-themes' package.json && ! grep -Fq 'react-query' package.json && ! grep -Fq 'react-hook-form' package.json</automated>
  </verify>
  <done>
    A árvore webpage/ tem toda a configuração standalone; `pnpm install` resolve o subconjunto curado (zero novas deps de UI vs. web/) e gera o lockfile; next.config.ts tem assetPrefix + standalone + CSP data:.
  </done>
</task>

<task type="auto">
  <name>Task 2: Shell de tema + primitivos reutilizados + page placeholder</name>
  <files>
    webpage/src/app/globals.css (novo),
    webpage/src/app/providers.tsx (novo),
    webpage/src/app/layout.tsx (novo),
    webpage/src/app/page.tsx (novo, placeholder),
    webpage/src/lib/utils.ts (novo),
    webpage/src/components/ui/button.tsx (novo),
    webpage/src/components/ui/card.tsx (novo),
    webpage/src/components/theme-toggle.tsx (novo)
  </files>
  <read_first>
    - web/src/app/globals.css (Tailwind v4 CSS-based: @import tailwindcss, @plugin tailwindcss-animate, @custom-variant dark, tokens --background/--foreground; copiar byte-for-byte)
    - web/src/app/providers.tsx (fonte a adaptar: manter só o NextThemesProvider, remover QueryClient)
    - web/src/app/layout.tsx (fonte a adaptar: manter Geist + Providers, remover Toaster e overflow-hidden)
    - web/src/lib/utils.ts (cn() clsx+tailwind-merge; copiar byte-for-byte)
    - web/src/components/ui/button.tsx (CVA + asChild/Slot; copiar byte-for-byte — NÃO modificar; variantes/tamanhos usados nos planos seguintes)
    - web/src/components/ui/card.tsx (Card/CardHeader/CardTitle/CardDescription/CardContent, já rounded-none; copiar byte-for-byte)
    - web/src/components/theme-toggle.tsx ("use client" + useTheme + Sun/Moon; copiar byte-for-byte)
    - 99-UI-SPEC.md (Design System / Dark mode: confirmar que providers/theme-toggle são portados exatamente)
  </read_first>
  <action>
    Copiar BYTE-FOR-BYTE (sem alterar uma linha) para dentro de webpage/: `src/app/globals.css`, `src/lib/utils.ts`, `src/components/ui/button.tsx`, `src/components/ui/card.tsx`, `src/components/theme-toggle.tsx` — a partir das fontes web/ homónimas. (O alias `@/*` do tsconfig faz os imports `@/lib/utils`/`@/components/...` resolverem sem alteração.) NÃO criar badge.tsx — o UI-SPEC marca-o opcional e nenhum requisito o exige.

    Criar `src/app/providers.tsx` como o bloco `<interfaces>`: `"use client"` + só o `NextThemesProvider` (attribute="class", defaultTheme="system", enableSystem, disableTransitionOnChange). REMOVER por completo QueryClient/QueryClientProvider (TanStack Query está excluída, CONTEXT.md).

    Criar `src/app/layout.tsx` como o bloco `<interfaces>`: fontes Geist/Geist_Mono via next/font/google com as variáveis `--font-geist-sans`/`--font-geist-mono`, `<Providers>` a envolver children, `suppressHydrationWarning` no <html>. CRÍTICO — divergências da fonte web/: (1) NÃO importar nem renderizar `<Toaster>`; (2) body é `min-h-screen bg-background text-foreground` (NUNCA `h-full flex flex-col overflow-hidden`, que impediria o scroll da landing). metadata title/description da landing (ver bloco).

    Criar `src/app/page.tsx` como o PLACEHOLDER do bloco `<interfaces>` (renderiza `<ThemeToggle />` + uma linha de texto). Este ficheiro será SUBSTITUÍDO integralmente pelo plano 99-04 — existe agora apenas para tornar `pnpm build`/`pnpm dev` verificáveis e provar dark/light.

    Correr `pnpm build` em webpage/ (com o `.env.local` criado na Task 1). O build tem de passar sem backend a correr (o placeholder não faz fetch).
  </action>
  <acceptance_criteria>
    - `pnpm build` em webpage/ conclui com sucesso (exit 0), com `.env.local` presente (BACKEND_API_ORIGIN definido).
    - `providers.tsx` contém `NextThemesProvider` e NÃO contém `QueryClientProvider` nem `@tanstack/react-query`.
    - `layout.tsx` contém `Geist` + `Providers` e NÃO contém `Toaster` nem `overflow-hidden`.
    - `globals.css`, `lib/utils.ts`, `ui/button.tsx`, `ui/card.tsx`, `theme-toggle.tsx` são idênticos byte-a-byte às fontes web/ homónimas.
    - `page.tsx` renderiza `<ThemeToggle />` (placeholder) e não faz nenhum fetch.
    - `pnpm dev -p 3001` serve `/` e o toggle dark/light alterna a classe `.dark` no <html>.
  </acceptance_criteria>
  <verify>
    <automated>cd webpage && pnpm build && grep -Fq 'NextThemesProvider' src/app/providers.tsx && ! grep -Fq 'QueryClientProvider' src/app/providers.tsx && ! grep -Fq 'Toaster' src/app/layout.tsx && ! grep -Fq 'overflow-hidden' src/app/layout.tsx && diff -q src/lib/utils.ts ../web/src/lib/utils.ts && diff -q src/components/ui/button.tsx ../web/src/components/ui/button.tsx && diff -q src/components/ui/card.tsx ../web/src/components/ui/card.tsx</automated>
    <human-check>`cd webpage && pnpm dev` → abrir http://localhost:3001/ : a página placeholder renderiza, o `ThemeToggle` alterna dark/light (fundo #f8fafc ↔ #020617) sem flash de hidratação. Confirmação visual leve; não corre no gate automatizado.</human-check>
  </verify>
  <done>
    webpage/ compila e serve uma página placeholder com dark/light funcional via next-themes portado; Button/Card/ThemeToggle/cn/globals são cópias verbatim de web/ (zero deps novas); layout sem Toaster e com body scrollável.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pnpm registry → webpage/node_modules | Instalação de dependências: superfície de supply-chain. Nesta fase é um subconjunto pinado de deps já em produção em web/. |
| Visitante anónimo → app webpage (headers HTTP) | A app pública responde a qualquer visitante; os security headers (CSP/X-Frame-Options/nosniff/HSTS) definidos no next.config.ts atravessam esta fronteira. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-99-SC | Tampering (supply chain) | webpage/package.json + `pnpm install` | mitigate | As dependências de `webpage/` são um subconjunto PINADO e VERBATIM das deps já presentes, vetadas e em produção em `web/package.json` — zero pacotes novos (LP-12/CONTEXT.md "sem novas dependências de UI"). O executor copia as version strings EXATAS de web/package.json, nunca resolve "latest". Como não é introduzido nenhum pacote novo/desconhecido, a proveniência é o lockfile existente e em produção — não se aplica o gate de legitimidade de pacotes por-pacote (não há [ASSUMED]/[SUS] a verificar). Como não existe RESEARCH.md (research desativado no projeto), esta nota inline estabelece a proveniência em vez de uma tabela de audit. Verificável: cada linha de deps do webpage/package.json corresponde a uma linha de web/package.json. |
| T-99-01 | Information Disclosure / Tampering | next.config.ts security headers | mitigate | O bloco `headers()` é copiado verbatim de web/: `X-Frame-Options: DENY` (anti-clickjacking numa página pública), `X-Content-Type-Options: nosniff`, HSTS, e um CSP `default-src 'self'` com `img-src 'self' data:` — este último autoriza o logo data-URL da tenant (planos 99-02/03) SEM abrir img-src a origens externas. Reduz a superfície XSS/injeção antes de qualquer conteúdo dinâmico ser adicionado. |
| T-99-02 | Denial of Service (build-time) | next.config.ts fail-fast | accept | O `throw new Error("BACKEND_API_ORIGIN is required")` no module load é um fail-fast deliberado (mesmo padrão de web/) — o build/arranque falha alto e cedo se a env var faltar, em vez de arrancar mal-configurado. Aceite: é o comportamento pretendido, não um defeito. |
</threat_model>

<verification>
Verificação geral do plano (fundação da fase):

1. `cd webpage && pnpm install` conclui e gera `pnpm-lock.yaml` (subconjunto curado, zero deps novas de UI).
2. `cd webpage && pnpm build` passa sem backend a correr (placeholder não faz fetch), com `.env.local` presente.
3. `grep -Fq 'landing-static' webpage/next.config.ts` e `grep -Fq 'standalone' webpage/next.config.ts` — assetPrefix + output configurados (LP-13 estrutural / fundação Phase 100).
4. `providers.tsx` tem NextThemesProvider e não tem QueryClientProvider; `layout.tsx` não tem Toaster nem overflow-hidden.
5. `diff -q` confirma que utils.ts/button.tsx/card.tsx/theme-toggle.tsx/globals.css são byte-for-byte idênticos às fontes web/ (LP-12, zero deps novas).
6. (human-check) `pnpm dev -p 3001` → http://localhost:3001/ serve o placeholder e o ThemeToggle alterna dark/light.
</verification>

<success_criteria>
- [ ] LP-03: existe uma app Next.js 16 standalone `webpage/` com package.json/tsconfig/next.config próprios, buildável isoladamente (`pnpm install` + `pnpm build`).
- [ ] LP-12 (fundação): dark/light via next-themes portado (theme-only); Button/Card/ThemeToggle/cn/globals copiados byte-for-byte de web/; zero novas dependências de UI.
- [ ] next.config.ts define `assetPrefix: '/landing-static'` + `output: 'standalone'` (fundação estrutural Multi-Zones para Phase 100).
- [ ] providers.tsx NÃO tem QueryClientProvider; package.json NÃO tem @tanstack/react-query/react-hook-form/zod.
- [ ] layout.tsx sem Toaster e com body scrollável (sem overflow-hidden); page.tsx placeholder com ThemeToggle.
- [ ] `pnpm-lock.yaml` gerado e committed; sem package-lock.json.
</success_criteria>

<output>
Criar `.planning/milestones/v2.12-phases/LEXCV-99-webpage-nova-app-next-js-de-landing/99-01-SUMMARY.md` quando concluído.
</output>
