---
phase: 116-reposicionamento-institucional-fim-das-referencias-a-nosi
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - webpage/src/components/trust-section.tsx
  - .trae/documents/SPEC.md
  - backend/src/main/java/com/lexcv/seed/DatabaseSeeder.java
  - .planning/PROJECT.md   # conditional — only if the SIJ-01 assertion in Task 3 fails
autonomous: true
requirements: [SIJ-01, SIJ-02, SIJ-03, SIJ-04]

must_haves:
  truths:
    - "Um visitante anonimo da landing publica le, no card 'Ecossistema Cabo Verde', uma descricao que referencia o SIJ (Sistema Judicial de Cabo Verde) e nunca a NOSi"
    - "A seccao 'What This Is' de .planning/PROJECT.md descreve o LexCV como alinhado ao ecossistema do SIJ, sem mencionar NOSi"
    - "O paragrafo 1 de .trae/documents/SPEC.md descreve o produto com foco no ecossistema do SIJ, sem mencionar NOSi"
    - "Uma instalacao nova que corra o seed cria um tenant de demonstracao com identidade generica (nome/NIF/email/telefone), sem nenhum campo associado a NOSi — incluindo o nome servido publicamente por GET /api/v1/branding"
    - "Nenhuma superficie viva do produto (backend/src, webpage/src, web/src excluindo o mock legado, .trae/documents) contem a string 'nosi' em qualquer capitalizacao"
    - "As tres formulacoes institucionais (PROJECT.md, trust-section.tsx, SPEC.md) usam a mesma expressao canonica 'ecossistema do SIJ (Sistema Judicial de Cabo Verde)'"
    - "Os ficheiros fora de ambito (web/src/server/mock-db.ts, .figma/) permanecem por tocar"
  artifacts:
    - path: "webpage/src/components/trust-section.tsx"
      provides: "Copy do card institucional da landing publica, entrada Building2 do array CONFIANCA"
      contains: "alinhado ao ecossistema do SIJ (Sistema Judicial de Cabo Verde)."
    - path: ".trae/documents/SPEC.md"
      provides: "Descricao do produto na seccao 1 (Visao Geral e Requisitos)"
      contains: "com foco no ecossistema do SIJ (Sistema Judicial de Cabo Verde)."
    - path: "backend/src/main/java/com/lexcv/seed/DatabaseSeeder.java"
      provides: "Identidade do tenant de demonstracao seedado (bloco Tenant.builder())"
      contains: ".tipoEntidade(\"PRIVADO\")"
    - path: ".planning/PROJECT.md"
      provides: "Descricao institucional do produto na seccao 'What This Is'"
      contains: "ecossistema do SIJ (Sistema Judicial de Cabo Verde)"
  key_links:
    - from: "backend/src/main/java/com/lexcv/seed/DatabaseSeeder.java"
      to: "GET /api/v1/branding (PublicController)"
      via: "Tenant.nome copiado para TenantPublicInfoResponse.nome, servido sem autenticacao"
      pattern: "\\.nome\\(\"Gabinete Jur.+dico Demonstra.+o\"\\)"
    - from: "webpage/src/components/trust-section.tsx (array CONFIANCA)"
      to: "card renderizado <CardDescription>{desc}</CardDescription>"
      via: "CONFIANCA.map dentro do proprio componente TrustSection"
      pattern: "ecossistema do SIJ \\(Sistema Judicial de Cabo Verde\\)"
    - from: ".planning/PROJECT.md 'What This Is'"
      to: ".trae/documents/SPEC.md §1 e webpage trust-section.tsx"
      via: "expressao canonica identica nos 3 documentos"
      pattern: "ecossistema do SIJ \\(Sistema Judicial de Cabo Verde\\)"
---

<objective>
Remover todas as referencias vivas a "NOSi" da superficie do produto e substituir pelo enquadramento correto (ecossistema do SIJ — Sistema Judicial de Cabo Verde), em 3 ficheiros de conteudo/dados, e fechar formalmente SIJ-01 verificando a edicao ja aplicada em `.planning/PROJECT.md`.

Purpose: O LexCV descreve-se hoje, na landing publica, na documentacao tecnica e nos dados de demonstracao, como ligado a NOSi (agencia de TI do governo cabo-verdiano) — uma afiliacao que nao existe. O tenant seedado chega a publicar o nome "NOSi (Demonstracao)" a visitantes anonimos via `GET /api/v1/branding`.
Output: 3 ficheiros editados (copy da landing, SPEC.md, seeder do tenant de demonstracao) + verificacao formal de SIJ-01 + varrimento residual que prova zero ocorrencias de "nosi" nas superficies vivas.

Correcao de posicionamento/copy pura — sem nova funcionalidade, sem endpoint novo, sem integracao tecnica real com o SIJ, sem migracao de schema, sem alteracao de dependencias.
</objective>

<execution_context>
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/LEXCV-116-reposicionamento-institucional-fim-das-refer-ncias-a-nosi/116-CONTEXT.md
@CLAUDE.md

<interfaces>
<!-- Contratos relevantes ja verificados durante o planeamento. Usar diretamente, sem exploracao adicional. -->

backend/src/main/java/com/lexcv/models/Tenant.java (linhas 20-29) — `nome` e `@Column(nullable = false)`; `nif`, `email`, `telefone` sao `String` simples;
`tipoEntidade` e `private String tipoEntidade;` com `@Column(name = "tipo_entidade")` e SEM `@Enumerated` / sem enum / sem check constraint.
Consequencia: alterar o valor seedado de `tipoEntidade` nao exige migracao de schema nem script SQL manual.

backend/src/main/java/com/lexcv/controllers/PublicController.java (linhas 51-62) — endpoint publico nao autenticado; le
`tenantRepository.findFirstByOrderByCreatedAtAsc()` e devolve `TenantPublicInfoResponse` com EXCLUSIVAMENTE `.nome(t.getNome())` e
`.logoDataUrl(t.getLogoDataUrl())`. `nif`, `email` e `telefone` do tenant NAO sao expostos publicamente.

backend/src/main/java/com/lexcv/seed/DatabaseSeeder.java (linhas 39-56) — `run()` faz early-return excepto se
`app.seed.enabled == true` E `SystemSetting.initialized == true` E `tenantRepository.count() == 0 && userRepository.count() == 0 && clienteRepository.count() == 0`.
O seeder so faz INSERT numa base de dados virgem; nunca faz UPDATE de um tenant existente.

Consumidores de `tipoEntidade` / `tipo_entidade` em todo o repositorio: apenas a declaracao em `Tenant.java:25-26` e a atribuicao no seeder.
Zero leituras em `@PreAuthorize`, queries, DTOs, `web/src` ou `webpage/src`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Copy institucional — landing publica e SPEC.md</name>

  <files>webpage/src/components/trust-section.tsx, .trae/documents/SPEC.md</files>

  <read_first>
    - webpage/src/components/trust-section.tsx (ficheiro completo, 35 linhas — ver o array CONFIANCA e o render CONFIANCA.map antes de editar)
    - .trae/documents/SPEC.md (linhas 1-10 — o paragrafo alvo e a linha 4)
    - .planning/PROJECT.md (linha 5, seccao "What This Is") — fonte da formulacao canonica ja aplicada; as 3 formulacoes tem de ficar identicas
    - .planning/phases/LEXCV-116-reposicionamento-institucional-fim-das-refer-ncias-a-nosi/116-CONTEXT.md (seccao "Texto de Substituicao Institucional")
  </read_first>

  <action>
    Duas substituicoes literais, uma por ficheiro. Nao reformatar, nao reordenar, nao reindentar, nao alterar mais nenhum caractere em nenhum dos 2 ficheiros.

    (1) `webpage/src/components/trust-section.tsx`, linha 8 — objeto do array `CONFIANCA` cujo `icon` e `Building2`. O valor da propriedade
    `desc` passa da string atual `Desenhado para a realidade institucional cabo-verdiana, alinhado ao ecossistema NOSi.` para exatamente
    `Desenhado para a realidade institucional cabo-verdiana, alinhado ao ecossistema do SIJ (Sistema Judicial de Cabo Verde).`
    Reparar na diferenca de preposicao: o texto atual diz "ao ecossistema NOSi", o novo diz "ao ecossistema do SIJ". O `title` mantem-se
    `"Ecossistema Cabo Verde"`, o `icon` mantem-se `Building2`, a posicao no array mantem-se (4o elemento), e as 3 outras entradas do array
    (`Lock`/Isolamento Multi-Tenant, `ShieldCheck`/RBAC Granular, `History`/Trilha de Auditoria) ficam byte-a-byte intactas. Nao mexer nos imports,
    no JSX, nem em nenhuma className.

    (2) `.trae/documents/SPEC.md`, linha 4 (primeiro paragrafo da seccao "## 1. Visao Geral e Requisitos"). Substituir a substring
    `com foco no ecossistema do NOSi.` por exatamente `com foco no ecossistema do SIJ (Sistema Judicial de Cabo Verde).`
    Tudo o resto da linha 4 fica inalterado, incluindo `A **LexCV** e uma plataforma institucional de gestao juridica desenvolvida pela Speed Tech`,
    o resto da frase apos o ponto final (`A plataforma centraliza a gestao de clientes...`) e o espaco em branco existente no fim da linha.
    Nenhuma outra linha do SPEC.md e tocada.

    Ambos os ficheiros ja contem caracteres acentuados (`cabo-verdiana`, `gestao juridica`, `Visao Geral`). Preservar a codificacao UTF-8
    existente — a edicao nao pode re-codificar acentos nem introduzir BOM.
  </action>

  <verify>
    <automated>cd webpage && pnpm lint</automated>
    <automated>grep -cF "alinhado ao ecossistema do SIJ (Sistema Judicial de Cabo Verde)." webpage/src/components/trust-section.tsx</automated>
    <automated>grep -iF "nosi" webpage/src/components/trust-section.tsx || echo CLEAN</automated>
    <automated>grep -cF "com foco no ecossistema do SIJ (Sistema Judicial de Cabo Verde)." .trae/documents/SPEC.md</automated>
    <automated>grep -iF "nosi" .trae/documents/SPEC.md || echo CLEAN</automated>
    <automated>git diff --numstat -- webpage/src/components/trust-section.tsx .trae/documents/SPEC.md</automated>
  </verify>

  <acceptance_criteria>
    - `cd webpage && pnpm lint` termina com exit code 0 e sem erros novos
    - `grep -cF "alinhado ao ecossistema do SIJ (Sistema Judicial de Cabo Verde)." webpage/src/components/trust-section.tsx` imprime `1`
    - `grep -iF "nosi" webpage/src/components/trust-section.tsx || echo CLEAN` imprime `CLEAN` (zero ocorrencias, qualquer capitalizacao)
    - `grep -cF "\"Ecossistema Cabo Verde\"" webpage/src/components/trust-section.tsx` imprime `1` (titulo do card preservado)
    - `grep -cF "Trilha de Auditoria" webpage/src/components/trust-section.tsx` imprime `1` (restantes entradas do array intactas)
    - `grep -cF "com foco no ecossistema do SIJ (Sistema Judicial de Cabo Verde)." .trae/documents/SPEC.md` imprime `1`
    - `grep -iF "nosi" .trae/documents/SPEC.md || echo CLEAN` imprime `CLEAN`
    - `grep -cF "desenvolvida pela Speed Tech" .trae/documents/SPEC.md` imprime `1` (resto da frase preservado)
    - `git diff --numstat -- webpage/src/components/trust-section.tsx .trae/documents/SPEC.md` devolve 1 insercao e 1 delecao (primeiras duas colunas: 1 e 1) para CADA um dos 2 ficheiros — prova de que nenhum outro texto foi alterado
    - `git diff -- webpage/src/components/trust-section.tsx .trae/documents/SPEC.md` nao mostra nenhuma linha alterada com acentos corrompidos (ex.: `Ã§`, `Ã£`, `ï»¿`)
  </acceptance_criteria>

  <done>Os 2 ficheiros de copy referenciam o SIJ com a formulacao canonica, zero ocorrencias de "nosi" em qualquer capitalizacao, exatamente 1 linha alterada por ficheiro, `pnpm lint` verde. SIJ-02 e SIJ-03 satisfeitos.</done>
</task>

<task type="auto">
  <name>Task 2: Identidade generica do tenant de demonstracao seedado</name>

  <files>backend/src/main/java/com/lexcv/seed/DatabaseSeeder.java</files>

  <read_first>
    - backend/src/main/java/com/lexcv/seed/DatabaseSeeder.java (linhas 39-95 — as 3 guardas de early-return em `run()`, o bloco `Tenant.builder()` das linhas 64-70, e os utilizadores seedados imediatamente a seguir cujos emails NAO mudam)
    - backend/src/main/java/com/lexcv/models/Tenant.java (ficheiro completo, 42 linhas — confirma que `tipoEntidade` e `String` livre sem `@Enumerated`, logo sem migracao de schema)
    - backend/src/main/java/com/lexcv/controllers/PublicController.java (linhas 40-65 — confirma que `Tenant.nome` e servido a visitantes anonimos via `GET /api/v1/branding`, e que `nif`/`email`/`telefone` nao sao)
    - .planning/phases/LEXCV-116-reposicionamento-institucional-fim-das-refer-ncias-a-nosi/116-CONTEXT.md (seccao "Identidade do Tenant de Demonstracao (DatabaseSeeder.java)")
  </read_first>

  <action>
    No bloco `Tenant tenant = Tenant.builder()...build();` (linhas 64-70), substituir os valores literais de 5 campos. Valores exatos, sem reinterpretacao:

    - `.nome("NOSi (Demonstração)")` passa a `.nome("Gabinete Jurídico Demonstração")`
    - `.nif("500100200")` passa a `.nif("000000000")` (placeholder claramente ficticio)
    - `.tipoEntidade("PUBLICO")` passa a `.tipoEntidade("PRIVADO")` (campo `String` livre em `Tenant.java:26`, sem enum — nao exige migracao)
    - `.email("contacto@nosi.cv")` passa a `.email("contacto@lexcv.cv")` (mesmo dominio dos utilizadores seedados)
    - `.telefone("+238 2607900")` passa a `.telefone("+238 200 0000")`

    Nao alterar: a ordem das chamadas do builder, o `.build()`, a linha `tenant = tenantRepository.save(tenant);`, a extracao `UUID tenantId = tenant.getId();`,
    nem qualquer outro campo do tenant (`id`, `createdAt`, `logoDataUrl`). Nao adicionar campos novos ao builder. Nao criar nenhum script de migracao em
    `backend/migrations/` — esta alteracao nao mexe no schema.

    Nao tocar em mais nenhuma entidade seedada no ficheiro: os utilizadores `admin@lexcv.cv` e `assistente@lexcv.cv` mantem emails, nomes, passwords e roles;
    clientes, processos, eventos e honorarios seedados ficam intactos. Nao alterar as guardas de `run()` (linhas 39-56).

    O ficheiro ja contem caracteres acentuados (`Demonstração`, `João Andrade`, `🌱` na linha 58). Preservar a codificacao UTF-8 existente — a edicao
    nao pode re-codificar acentos nem introduzir BOM (Java compila com `-encoding UTF-8`; acentos corrompidos passam a compilar mas produzem dados errados).

    FORA DE AMBITO, nao editar: `web/src/server/mock-db.ts` linha 190 (`nome: "NOSi (Demonstração)"`) — mock legado pre-backend, marcado
    "ignore unless migrating" em CLAUDE.md e listado explicitamente em REQUIREMENTS.md Out of Scope.
  </action>

  <verify>
    <automated>cd backend && mvn -q -DskipTests compile</automated>
    <automated>grep -cE '\.nome\("Gabinete Jur.+dico Demonstra.+o"\)' backend/src/main/java/com/lexcv/seed/DatabaseSeeder.java</automated>
    <automated>grep -cF '.nif("000000000")' backend/src/main/java/com/lexcv/seed/DatabaseSeeder.java</automated>
    <automated>grep -cF '.tipoEntidade("PRIVADO")' backend/src/main/java/com/lexcv/seed/DatabaseSeeder.java</automated>
    <automated>grep -cF '.email("contacto@lexcv.cv")' backend/src/main/java/com/lexcv/seed/DatabaseSeeder.java</automated>
    <automated>grep -cF '.telefone("+238 200 0000")' backend/src/main/java/com/lexcv/seed/DatabaseSeeder.java</automated>
    <automated>grep -iF "nosi" backend/src/main/java/com/lexcv/seed/DatabaseSeeder.java || echo CLEAN</automated>
    <automated>git diff --numstat -- backend/src/main/java/com/lexcv/seed/DatabaseSeeder.java</automated>
  </verify>

  <acceptance_criteria>
    - `cd backend && mvn -q -DskipTests compile` termina com exit code 0
    - `grep -cE '\.nome\("Gabinete Jur.+dico Demonstra.+o"\)' backend/src/main/java/com/lexcv/seed/DatabaseSeeder.java` imprime `1`
    - `grep -cF '.nif("000000000")' ...DatabaseSeeder.java` imprime `1`
    - `grep -cF '.tipoEntidade("PRIVADO")' ...DatabaseSeeder.java` imprime `1`
    - `grep -cF '.email("contacto@lexcv.cv")' ...DatabaseSeeder.java` imprime `1`
    - `grep -cF '.telefone("+238 200 0000")' ...DatabaseSeeder.java` imprime `1`
    - `grep -iF "nosi" ...DatabaseSeeder.java || echo CLEAN` imprime `CLEAN`
    - `grep -cF '.nif("500100200")' ...DatabaseSeeder.java` imprime `0` e `grep -cF '.telefone("+238 2607900")' ...DatabaseSeeder.java` imprime `0` (valores antigos removidos)
    - `grep -cF 'admin@lexcv.cv' ...DatabaseSeeder.java` imprime `1` e `grep -cF 'assistente@lexcv.cv' ...DatabaseSeeder.java` imprime `1` (utilizadores seedados intactos)
    - `git diff --numstat -- backend/src/main/java/com/lexcv/seed/DatabaseSeeder.java` devolve 5 insercoes e 5 delecoes (primeiras duas colunas: 5 e 5)
    - `git status --porcelain backend/migrations/` nao devolve nada (nenhum script de migracao criado)
    - `git status --porcelain web/src/server/mock-db.ts` nao devolve nada (ficheiro fora de ambito nao tocado)
    - `git diff -- backend/src/main/java/com/lexcv/seed/DatabaseSeeder.java` nao mostra acentos corrompidos (`Ã§`, `Ã£`, `Ã­`) em nenhuma linha
  </acceptance_criteria>

  <done>O tenant de demonstracao seedado tem identidade generica (nome, NIF, tipo, email, telefone), zero ocorrencias de "nosi", backend compila, 5 linhas alteradas e mais nenhuma, nenhum script de migracao criado. SIJ-04 satisfeito.</done>
</task>

<task type="auto">
  <name>Task 3: Fecho formal de SIJ-01 e varrimento residual de "nosi"</name>

  <files>.planning/PROJECT.md (edicao condicional — so se a assercao de SIJ-01 falhar; caso contrario esta tarefa e read-only)</files>

  <read_first>
    - .planning/PROJECT.md (linhas 1-20 — a seccao "## What This Is" ate ao inicio de "## Core Value")
    - .planning/REQUIREMENTS.md (seccao "Out of Scope" — confirma os 4 ficheiros/areas deliberadamente excluidos do varrimento)
    - .planning/ROADMAP.md (Phase 116, os 4 Success Criteria)
  </read_first>

  <action>
    Tarefa de verificacao com um unico ramo de escrita condicional. SIJ-01 foi aplicado na abertura da milestone (fora desta fase);
    o trabalho aqui e prova-lo formalmente e provar que nao ficou nenhum residuo noutro sitio.

    (1) Assercao SIJ-01, delimitada a seccao: extrair o bloco entre `## What This Is` e `## Core Value` de `.planning/PROJECT.md` e confirmar
    que (a) nao contem "nosi" em nenhuma capitalizacao e (b) contem a expressao canonica `ecossistema do SIJ (Sistema Judicial de Cabo Verde)`.
    A delimitacao por seccao e obrigatoria, e um `grep` ao ficheiro inteiro daria um falso positivo: `.planning/PROJECT.md` contem hoje a string
    "NOSi" em 9 linhas, TODAS legitimas — descrevem esta propria milestone/requisito em vez de descreverem o produto. Nenhuma e residuo e
    nenhuma pode ser removida por esta tarefa:
      - linha 13 — a frase `**Goal:**` da milestone v2.15 ("remover todas as referencias vivas a 'NOSi'...");
      - linhas 16-19 — os 4 bullets de `**Target features:**`, que nomeiam os 4 ficheiros a corrigir;
      - linhas 110-113 — os 4 itens do checklist `### Active` (os requisitos SIJ-01..04 ainda por fechar).
    Pela mesma razao, a expressao canonica aparece 2 vezes no ficheiro e nao 1: linha 5 (a formulacao institucional de "What This Is", que E o
    alvo de SIJ-01) e linha 13 (a frase Goal, que repete a mesma expressao canonica ao descrever a correcao). Ambas sao esperadas.

    (2) So se (1) falhar: aplicar na seccao "What This Is" a mesma formulacao canonica ja usada em `.planning/PROJECT.md` linha 5
    (`alinhada ao ecossistema do SIJ (Sistema Judicial de Cabo Verde)`), sem alterar mais nada no ficheiro, e repetir a assercao.
    Se (1) passar a primeira, nao editar `.planning/PROJECT.md` de todo e registar no SUMMARY que SIJ-01 estava ja satisfeito e foi verificado.

    (3) Varrimento residual sobre as superficies vivas do produto: procurar "nosi" (case-insensitive) em `backend/src`, `webpage/src`,
    `web/src` e `.trae/documents`, excluindo explicitamente `web/src/server/mock-db.ts`. O resultado tem de ser vazio.
    Se aparecer qualquer ocorrencia nao antecipada, NAO improvisar texto de substituicao — parar e reportar a ocorrencia ao orquestrador,
    porque o texto de substituicao de cada superficie e uma decisao ja fixada em 116-CONTEXT.md e nao pode ser inventada pelo executor.

    Areas deliberadamente FORA do varrimento, por decisao registada em REQUIREMENTS.md Out of Scope e 116-CONTEXT.md — nao editar nenhuma:
    `web/src/server/mock-db.ts` (mock legado), `.figma/` (dump de referencia de design, nao importado pela aplicacao),
    `.planning/milestones/` e `.planning/RETROSPECTIVE.md` (registo historico), e as 9 linhas de `.planning/PROJECT.md` que descrevem a propria
    milestone (linha 13, linhas 16-19, linhas 110-113) — a seccao "What This Is" (linha 5) e a UNICA parte de PROJECT.md em ambito.
    Nao esta em ambito o comentario de hostname `RAI-GV-NOSI-194` dentro de `deploy_key.pub` — nao e copy do produto, e um comentario de chave SSH;
    reportar como observacao no SUMMARY, sem editar.
  </action>

  <verify>
    <automated>awk '/^## What This Is/{f=1} /^## Core Value/{f=0} f' .planning/PROJECT.md | grep -iF "nosi" || echo CLEAN</automated>
    <automated>awk '/^## What This Is/{f=1} /^## Core Value/{f=0} f' .planning/PROJECT.md | grep -cF "ecossistema do SIJ (Sistema Judicial de Cabo Verde)"</automated>
    <automated>grep -cF "NOSi" .planning/PROJECT.md</automated>
    <automated>grep -cF "ecossistema do SIJ (Sistema Judicial de Cabo Verde)" .planning/PROJECT.md</automated>
    <automated>grep -rniE "nosi" backend/src webpage/src web/src .trae/documents | grep -v "web/src/server/mock-db.ts" || echo CLEAN</automated>
    <automated>grep -cF 'nome: "NOSi (Demonstração)"' web/src/server/mock-db.ts</automated>
    <automated>git status --porcelain .figma/ .planning/milestones/ .planning/RETROSPECTIVE.md</automated>
  </verify>

  <acceptance_criteria>
    - `awk '/^## What This Is/{f=1} /^## Core Value/{f=0} f' .planning/PROJECT.md | grep -iF "nosi" || echo CLEAN` imprime `CLEAN`
    - `awk '/^## What This Is/{f=1} /^## Core Value/{f=0} f' .planning/PROJECT.md | grep -cF "ecossistema do SIJ (Sistema Judicial de Cabo Verde)"` imprime `1`
    - `grep -rniE "nosi" backend/src webpage/src web/src .trae/documents | grep -v "web/src/server/mock-db.ts" || echo CLEAN` imprime `CLEAN` — zero residuos nas superficies vivas
    - `grep -cF 'nome: "NOSi (Demonstração)"' web/src/server/mock-db.ts` imprime `1` — o ficheiro fora de ambito continua intacto (assercao de nao-regressao de ambito)
    - `git status --porcelain .figma/ .planning/milestones/ .planning/RETROSPECTIVE.md` nao devolve nada — areas historicas/de referencia por tocar
    - `grep -cF "NOSi" .planning/PROJECT.md` imprime `9`, nao `1` — `grep -c` conta linhas, e as 9 linhas em causa descrevem a milestone/requisito, nao o produto (linha 13 = frase `**Goal:**` da v2.15; linhas 16-19 = bullets `**Target features:**`; linhas 110-113 = checklist `### Active`). Nenhuma tarefa deste plano toca nessas linhas, logo `9` e o valor esperado antes E depois da execucao; um valor abaixo de 9 significa que a Task 3 saiu de ambito e apagou descricao de milestone
    - `grep -cF "ecossistema do SIJ (Sistema Judicial de Cabo Verde)" .planning/PROJECT.md` imprime `2`, nao `1` — linha 5 (a formulacao de "What This Is", alvo de SIJ-01) e linha 13 (a frase Goal, que repete a mesma expressao canonica ao descrever a correcao); nao e duplicacao acidental
    - O SUMMARY declara explicitamente se `.planning/PROJECT.md` foi editado por esta tarefa (esperado: nao foi — SIJ-01 ja estava satisfeito) e regista a observacao sobre `deploy_key.pub`
  </acceptance_criteria>

  <done>SIJ-01 verificado formalmente com assercao delimitada a seccao, varrimento residual limpo em todas as superficies vivas, ficheiros fora de ambito provadamente por tocar.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Internet anonima -> landing publica (`webpage/src/components/trust-section.tsx`) | A copy editada na Task 1 e servida a visitantes nao autenticados. Fluxo unidirecional para fora: nenhum input do utilizador atravessa esta fronteira para dentro nesta fase |
| Internet anonima -> `GET /api/v1/branding` (`PublicController`) | Leitura nao autenticada do `nome` do tenant seedado. O valor alterado na Task 2 atravessa esta fronteira para fora |
| Arranque da aplicacao -> base de dados virgem (`DatabaseSeeder`) | Os valores seedados tornam-se linhas persistidas. Triplo guard de early-return; so INSERT, nunca UPDATE |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-116-01 | Information Disclosure | `DatabaseSeeder.java` (tenant seedado) -> `GET /api/v1/branding` | mitigate | Esta fase E a mitigacao. O seed atual publica a identidade de um terceiro real: nome "NOSi", NIF com aspeto real `500100200`, dominio `nosi.cv`, telefone institucional `+238 2607900` — e o `nome` chega a visitantes anonimos (verificado: `PublicController:58-62` copia `nome`+`logoDataUrl` para `TenantPublicInfoResponse`). Substituido por valores provadamente ficticios (NIF `000000000`, email no proprio dominio, nome generico), fixados verbatim em 116-CONTEXT.md e verificados por `grep -cF` — nenhum valor autorado pelo executor chega a producao |
| T-116-02 | Information Disclosure | `trust-section.tsx` (copy publica) | mitigate | Texto publicado a internet anonima. Risco real: prosa livre do executor a afirmar factos institucionais errados ou a expor detalhe interno. Mitigado por a string alvo estar fixada literalmente no `<action>` e ser validada por `grep -cF` exato mais `git diff --numstat` = `1 1`, que reprova qualquer texto adicional nao previsto |
| T-116-03 | Tampering | `DatabaseSeeder.run()` contra base de dados ja povoada | accept | Nenhuma mitigacao necessaria — verificado em `DatabaseSeeder.java:39-56`: `run()` faz early-return excepto se `app.seed.enabled` E `SystemSetting.initialized` E `tenantRepository.count()==0 && userRepository.count()==0 && clienteRepository.count()==0`. O seeder so faz INSERT numa BD virgem; nunca faz UPDATE de um tenant existente, logo nenhuma identidade de tenant em producao pode ser sobrescrita por esta alteracao |
| T-116-04 | Elevation of Privilege | `Tenant.tipoEntidade` "PUBLICO" -> "PRIVADO" | accept | Verificado zero consumidores: `grep -rn "tipoEntidade\|tipo_entidade"` sobre `backend/src`, `web/src` e `webpage/src` devolve apenas a declaracao (`Tenant.java:25-26`) e a atribuicao no seeder. Nenhum `@PreAuthorize`, filtro de query, DTO ou ramo de UI le o campo, e e `String` livre sem `@Enumerated` nem check constraint -> a alteracao e provadamente neutra para autorizacao e comportamento, e nao exige migracao de schema |
| T-116-05 | Tampering (XSS) | `trust-section.tsx` -> `<CardDescription>{desc}</CardDescription>` | accept | O valor editado e um literal de compilacao renderizado como filho JSX (auto-escaping do React). Sem `dangerouslySetInnerHTML`, sem interpolacao, sem input de utilizador neste caminho. Acrescentar parenteses e acentos ao literal nao introduz nenhum sink novo |
| T-116-SC | Tampering (supply chain) | instalacoes npm/pnpm/maven | accept (n/a) | Esta fase nao adiciona nem atualiza nenhuma dependencia: zero `pnpm add`, zero alteracao a `pom.xml` ou `pnpm-lock.yaml`. O Package Legitimacy Gate nao se aplica e nao ha tabela de auditoria a exigir. `pnpm lint` e `mvn compile` correm contra o lockfile/`pom.xml` existentes. Se algum executor se encontrar a instalar um pacote, o ambito da fase foi violado — parar e escalar em vez de instalar |

**Avaliacao ASVS L1:** nenhuma superficie de autenticacao, autorizacao, validacao de input, sessao, criptografia ou upload e criada ou modificada.
Nenhum endpoint novo, nenhum `@PreAuthorize` alterado, nenhuma query alterada, nenhuma fronteira de tenant tocada. O unico movimento liquido de risco
e uma reducao (T-116-01): deixa de se publicar a identidade de um terceiro real nos dados de demonstracao.
</threat_model>

<verification>
Verificacao ao nivel da fase, apos as 3 tarefas (comandos a correr da raiz do repositorio):

1. Varrimento residual global das superficies vivas:
   `grep -rniE "nosi" backend/src webpage/src web/src .trae/documents | grep -v "web/src/server/mock-db.ts" || echo CLEAN` -> `CLEAN`
2. Consistencia da formulacao canonica nos 3 documentos — contagens esperadas POR FICHEIRO, nao uniformes:
   `grep -cF "ecossistema do SIJ (Sistema Judicial de Cabo Verde)" .trae/documents/SPEC.md webpage/src/components/trust-section.tsx .planning/PROJECT.md`
   -> uma linha por ficheiro: `.trae/documents/SPEC.md:1`, `webpage/src/components/trust-section.tsx:1`, `.planning/PROJECT.md:2`
   O `2` de PROJECT.md e esperado e nao e residuo nem duplicacao: linha 5 (seccao "What This Is", o alvo de SIJ-01) e linha 13 (a frase `**Goal:**` da milestone v2.15, que repete a mesma expressao canonica ao descrever a correcao)
3. Backend compila: `cd backend && mvn -q -DskipTests compile` -> exit 0
4. Frontend publico passa o lint: `cd webpage && pnpm lint` -> exit 0
5. Ambito nao excedido: `git status --porcelain` lista no maximo os 3 (ou 4, com PROJECT.md) ficheiros declarados em `files_modified` mais os artefactos de planeamento; `.figma/`, `.planning/milestones/`, `web/src/server/mock-db.ts` e `backend/migrations/` sem alteracoes
6. Volume da alteracao: `git diff --numstat` -> 1 insercao/1 delecao para trust-section.tsx, 1 insercao/1 delecao para SPEC.md, 5 insercoes/5 delecoes para DatabaseSeeder.java
</verification>

<success_criteria>
Os 4 Success Criteria da Phase 116 (ROADMAP.md) satisfeitos e verificaveis por comando:

1. **SIJ-01** — a seccao "What This Is" de `.planning/PROJECT.md` nao contem "NOSi" e contem `ecossistema do SIJ (Sistema Judicial de Cabo Verde)` (assercao delimitada por `awk`, nao ao ficheiro inteiro)
2. **SIJ-02** — `webpage/src/components/trust-section.tsx` nao contem "nosi" em nenhuma capitalizacao; o card "Ecossistema Cabo Verde" mantem o titulo e passa a descrever o alinhamento ao ecossistema do SIJ
3. **SIJ-03** — `.trae/documents/SPEC.md` nao contem "nosi" em nenhuma capitalizacao; a linha 4 diz `com foco no ecossistema do SIJ (Sistema Judicial de Cabo Verde).`
4. **SIJ-04** — `backend/src/main/java/com/lexcv/seed/DatabaseSeeder.java` nao contem "nosi" em nenhuma capitalizacao; nome, NIF, tipo de entidade, email e telefone do tenant de demonstracao sao genericos e ficticios; o backend compila; nenhum script de migracao foi criado

Alem disso: zero ficheiros fora de ambito alterados, zero dependencias adicionadas, zero acentos corrompidos.
</success_criteria>

<output>
Create `.planning/phases/LEXCV-116-reposicionamento-institucional-fim-das-refer-ncias-a-nosi/116-01-SUMMARY.md` when done.

Registar no SUMMARY: se `.planning/PROJECT.md` foi ou nao editado pela Task 3 (esperado: nao — SIJ-01 ja estava satisfeito, apenas verificado),
o resultado do varrimento residual, e a observacao nao acionavel sobre `deploy_key.pub` (`RAI-GV-NOSI-194`, comentario de hostname numa chave SSH, fora de ambito).
</output>
