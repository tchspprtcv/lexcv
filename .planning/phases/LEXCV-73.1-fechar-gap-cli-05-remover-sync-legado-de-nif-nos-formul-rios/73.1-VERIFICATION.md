---
phase: 73.1-fechar-gap-cli-05
verified: 2026-07-02T10:20:00Z
status: passed
score: 4/4 must-haves verified
overrides_applied: 0
---

# Phase 73.1: Fechar gap CLI-05 (remover sync legado de NIF) Verification Report

**Phase Goal:** Fechar o gap CLI-05 identificado no audit da milestone v2.7 — remover a lógica legada de sincronização de NIF (frontend E backend) que sobrescreve o campo nif validado, e adicionar validação server-side ao campo nif
**Verified:** 2026-07-02T10:20:00Z
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | The dedicated Zod-validated `nif` field is the sole source of `payload.nif` in both create and edit forms — the legacy documento_tipo/documento_numero selection never overwrites it | VERIFIED | Read both `novo/page.tsx` and `editar/page.tsx` in full. `payload` is built via `{ ...values, ... }` with no post-build `payload.nif = values.documento_numero` assignment. `grep -rn "Sincronizar NIF\|payload.nif = values.documento_numero"` across both files returns zero matches (exit 1). |
| 2 | A direct API caller (non-browser) that POSTs or PUTs a Cliente with a missing or non-9-digit NIF receives HTTP 400, not a persisted invalid record | VERIFIED | `Cliente.java:38-39` carries `@NotBlank(message = "NIF é obrigatório")` + `@Pattern(regexp = "^\\d{9}$", ...)`. `ResourceController.java:220` (`createCliente`) and `:256` (`updateCliente`) both declare `@Valid @RequestBody Cliente`. No global `@ControllerAdvice`/`MethodArgumentNotValidException` handler exists in the codebase (confirmed absent), so Spring Boot's default behavior applies: `@Valid` failures return HTTP 400 automatically. |
| 3 | The backend never re-derives `nif` from `documentoNumero` on create or update — the request-body `nif` field is persisted as-is after validation | VERIFIED | `createCliente` (lines 220-242) contains no `setNif` call at all — the validated `cliente` entity is persisted directly via `clienteRepository.save(cliente)`, so `nif` flows from the request body untouched. `updateCliente` line 281: `cliente.setNif(payload.getNif());` — unconditional, no `getDocumentoNumero()` branch. Repo-wide grep for `setNif.*getDocumentoNumero` and `nif = .*documento_numero` returns zero matches. |
| 4 | Existing seed data (3 clientes with valid 9-digit NIFs) still loads without validation failure | VERIFIED | `DatabaseSeeder.java:66,100,116` — NIFs `"500100200"`, `"123456789"`, `"512345678"`, all exactly 9 numeric digits, satisfy `^\d{9}$`. Independent `mvn -DskipTests clean package` succeeded, confirming the entity/annotations compile; seeding is not itself invoked by the package goal but the values were inspected directly and match the pattern. |

**Score:** 4/4 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `backend/src/main/java/com/lexcv/models/Cliente.java` | `@NotBlank` + `@Pattern(^\d{9}$)` Bean Validation on `nif` field | VERIFIED | Lines 5-6 import `jakarta.validation.constraints.NotBlank` and `.Pattern`; lines 38-39 annotate `private String nif;` with both. |
| `backend/src/main/java/com/lexcv/controllers/ResourceController.java` | `@Valid` on createCliente and updateCliente `@RequestBody Cliente` params; NIF-sync blocks removed | VERIFIED | Line 220: `createCliente(@Valid @RequestBody Cliente cliente)`. Line 256: `updateCliente(@PathVariable UUID id, @Valid @RequestBody Cliente payload)`. `grep -c "@Valid @RequestBody Cliente"` = 2. No `setNif(...getDocumentoNumero())` pattern remains anywhere in the file. |
| `web/src/app/(dashboard)/clientes/novo/page.tsx` | No post-payload NIF-sync override | VERIFIED | Full file read (356 lines); `onSubmit` (lines 83-111) builds payload via spread of `values` only, no override block. |
| `web/src/app/(dashboard)/clientes/[id]/editar/page.tsx` | No post-payload NIF-sync override | VERIFIED | Full file read (700 lines); `onSubmit` (lines 178-207) builds payload via spread of `values` only, no override block. |

### Key Link Verification

| From | To | Via | Status | Details |
|------|-----|-----|--------|---------|
| `web/.../clientes/novo/page.tsx` | `payload.nif` | Spread of Zod-validated `values.nif` only | WIRED | `payload: ClienteCreateRequest = { ...values, ... }` — no downstream overwrite. `values` is the react-hook-form `ClienteFormValues` validated by `zodResolver(clienteFormSchema)`, whose `nif` field enforces `nifPattern = /^\d{9}$/` client-side. |
| `web/.../clientes/[id]/editar/page.tsx` | `payload.nif` | Spread of Zod-validated `values.nif` only | WIRED | Same pattern as novo/page.tsx; confirmed no override in `onSubmit`. |
| `ResourceController.createCliente` | `Cliente.nif` `@Pattern` | `@Valid` triggers Bean Validation → Spring default 400 | WIRED | `@Valid @RequestBody Cliente cliente` at controller boundary; no custom exception handler exists to intercept/bypass the default `MethodArgumentNotValidException` → 400 response. |
| `ResourceController.updateCliente` | `Cliente.nif` `@Pattern` | `@Valid` triggers Bean Validation → Spring default 400; `cliente.setNif(payload.getNif())` applies validated value unconditionally | WIRED | `@Valid @RequestBody Cliente payload` at controller boundary; line 281 applies `payload.getNif()` directly with no re-derivation branch. |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Backend compiles cleanly with new Jakarta Bean Validation annotations and `@Valid` usages | `cd backend && mvn -DskipTests clean package` | `BUILD SUCCESS`, 94 source files compiled, jar repackaged, 26.5s | PASS |
| Frontend typechecks cleanly with payload types intact after NIF-sync removal | `cd web && pnpm exec tsc --noEmit` | Exit with no output (0 errors) | PASS |
| No debt markers introduced in modified files | `grep -n "TBD\|FIXME\|XXX\|TODO\|HACK\|PLACEHOLDER"` across all 4 modified files | No matches (exit 1) | PASS |
| Git commits e990ca9 / 9387f98 exist and match claimed diffs | `git show --stat <hash>` | e990ca9: -10 lines across both frontend files; 9387f98: +7/-10 across Cliente.java + ResourceController.java | PASS |

Note: Full runtime spot-checks (actually POSTing an invalid NIF to a running server and observing the HTTP 400 body) were not performed — this would require starting the Spring Boot app + PostgreSQL, which is out of scope for static/build-level verification per the "no starting servers" constraint. The `@Valid` + Bean Validation wiring is a well-established Spring Boot pattern (mirrored from the existing `@Valid @RequestBody PrazoRequest` precedent at line 1396) and its default-400 behavior is a framework guarantee, not custom code that could silently fail.

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|-------------|--------------|--------|----------|
| CLI-05 | 73.1-01-PLAN.md | Utilizador deve fornecer um NIF válido (exatamente 9 dígitos numéricos, obrigatório) para criar ou editar qualquer cliente | SATISFIED | Both audit BLOCKER 1 (frontend overwrite) and BLOCKER 2 (missing server-side validation) are closed. NIF is now validated client-side (Zod, pre-existing from Phase 71) AND server-side (Bean Validation, this phase) with a single source of truth (dedicated `nif` field) in both layers. |

### Anti-Patterns Found

None. Scanned all 4 modified files for TBD/FIXME/XXX/TODO/HACK/PLACEHOLDER markers, empty implementations, and hardcoded stub returns — zero matches. Repo-wide grep for any remaining NIF-sync pattern (`setNif.*getDocumentoNumero`, `nif = .*documento_numero`) returns zero matches outside the removed blocks.

### Human Verification Required

None. All must-haves are verifiable via static code inspection, grep, and independent build/typecheck execution — no visual, real-time, or external-service behavior is involved in this gap-closure phase.

### Gaps Summary

No gaps. Both BLOCKER-level findings from `.planning/v2.7-MILESTONE-AUDIT.md` are closed:
- BLOCKER 1 (frontend silent NIF overwrite): legacy sync blocks fully deleted from both `novo/page.tsx` and `editar/page.tsx`; independently confirmed via full file reads and grep.
- BLOCKER 2 (no server-side NIF enforcement): `@NotBlank` + `@Pattern(^\d{9}$)` added to `Cliente.nif`; `@Valid` added to both `createCliente` and `updateCliente`; server-side NIF-sync twin removed from both endpoints; independently confirmed via file reads, grep, and a clean `mvn -DskipTests clean package` build.

Seed data compatibility confirmed (all 3 seeded NIFs are valid 9-digit values). Frontend typecheck passes cleanly. Both task commits (`e990ca9`, `9387f98`) exist in git history with diffs matching the SUMMARY.md's stated changes.

---

_Verified: 2026-07-02T10:20:00Z_
_Verifier: Claude (gsd-verifier)_
