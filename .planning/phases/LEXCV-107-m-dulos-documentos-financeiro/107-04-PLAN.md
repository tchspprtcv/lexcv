---
phase: 107-m-dulos-documentos-financeiro
plan: 04
type: execute
wave: 2
depends_on: [107-01]
files_modified:
  - web/src/app/(dashboard)/documentos/page.tsx
autonomous: true
requirements: [DOF-02]
must_haves:
  truths:
    - "The Documentos list Processo filter is a searchable closed-list Combobox (pick an existing processo by número/título), replacing the free-text UUID Input"
    - "The Documentos list Cliente filter is a searchable closed-list Combobox (pick an existing cliente by nome), replacing the free-text UUID Input"
    - "Both filters are wired via react-hook-form Controller against processo_id / cliente_id with no schema change"
    - "The documentos list RBAC gate uses permissions.isFetched"
  artifacts:
    - path: "web/src/app/(dashboard)/documentos/page.tsx"
      provides: "2 closed-list Combobox filters (Controller) + isFetched gate"
      contains: "Combobox"
  key_links:
    - from: "web/src/app/(dashboard)/documentos/page.tsx"
      to: "web/src/components/shared/combobox.tsx"
      via: "import Combobox, wired inside Controller render"
      pattern: "from \"@/components/shared/combobox\""
    - from: "web/src/app/(dashboard)/documentos/page.tsx"
      to: "processos.data / clientes.data"
      via: "options mapped from already-fetched useProcessos()/useClientes({})"
      pattern: "options={"
---

<objective>
Upgrade the two free-text UUID `Input` filters on the Documentos list (`Processo ID`, `Cliente ID`) to the shared closed-list searchable `Combobox` built in Plan 01, and bundle the `isFetched` RBAC-race fix for this page.

Purpose: DOF-02 (searchable Select-family control for the Documentos list filters). Options come from `useProcessos()`/`useClientes({})` which are already fetched on this page and already mapped into `processoById`/`clienteNomeById` — reuse the same label logic, do NOT add a second fetch. The submit model is unchanged (user still clicks "Filtrar").
Output: one migrated route file consuming the Wave-1 Combobox.
</objective>

<execution_context>
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/LEXCV-107-m-dulos-documentos-financeiro/107-CONTEXT.md
@.planning/phases/LEXCV-107-m-dulos-documentos-financeiro/107-PATTERNS.md
@.planning/phases/LEXCV-107-m-dulos-documentos-financeiro/107-UI-SPEC.md
@web/src/schemas/documentos.ts

<interfaces>
<!-- Combobox contract (from Plan 107-01, LOCKED). Import from @/components/shared/combobox -->
<!-- Combobox({ value, onChange, options, placeholder?, searchPlaceholder?, emptyMessage?, creatable?, createLabel?, triggerClassName?, id? }) -->
<!-- ComboboxOption = { value: string; label: string } -->
<!-- Closed-list mode = omit `creatable` (default false). Combobox displays options.find(o=>o.value===value)?.label, or placeholder when unset. -->

<!-- RHF Controller precedent (Phase 106 DatePickerField), from agenda/novo/page.tsx lines 185-197:
     <Controller control={form.control} name="dataInicio"
       render={({ field }) => <DatePickerField value={field.value} onChange={field.onChange} .../>} /> -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: isFetched gate + Processo filter to closed-list Combobox (Controller)</name>
  <files>web/src/app/(dashboard)/documentos/page.tsx</files>
  <read_first>
    - web/src/app/(dashboard)/documentos/page.tsx (the file being modified — RBAC gate line 35; existing data `useProcessos()` line 64 / `useClientes({})` line 65; existing label maps `processoById`/`clienteNomeById` lines 67-74; the Processo filter `<Input>` lines 112-118 inside `<form className="grid gap-4 sm:grid-cols-3">` at line 111; `form.reset({ processo_id: "", cliente_id: "" })` line 85)
    - web/src/components/shared/combobox.tsx (the Plan-01 component being consumed — closed-list mode: pass `options`, omit `creatable`)
    - web/src/schemas/documentos.ts (lines 9-12 — `documentosFiltersFormSchema` already types processo_id/cliente_id as plain optional strings; NO schema change needed)
    - web/src/app/(dashboard)/agenda/novo/page.tsx (lines 185-197 — the `Controller` + composed-field wiring shape to replicate)
    - .planning/phases/LEXCV-107-m-dulos-documentos-financeiro/107-UI-SPEC.md (Combobox closed-list mode table + Copywriting Contract for exact placeholders/empty copy)
  </read_first>
  <action>
    Two changes in `documentos/page.tsx`:
    (1) RBAC gate line 35: `if (!permissions.isLoading && !canViewDocumentos) {` → `if (permissions.isFetched && !canViewDocumentos) {`.
    (2) Replace the Processo filter `<Input id="processo_id" {...form.register("processo_id")} placeholder="Ex.: 7c8b..." />` (lines 112-118) with a `<Controller control={form.control} name="processo_id" render={({ field }) => (<Combobox id="processo_id" value={field.value} onChange={field.onChange} options={processoOptions} placeholder="Todos os processos" searchPlaceholder="Pesquisar processo por número ou título..." emptyMessage="Nenhum processo encontrado." />)} />`. Build `processoOptions` from the already-fetched `processos.data` as `(processos.data ?? []).map((p) => ({ value: p.id, label: p.numero ?? p.titulo ?? p.id }))` — reuse the exact same label logic already used by `processoById`/the page's mobile-card renderer; do NOT add a second fetch. Keep the surrounding `<div className="space-y-2"><Label htmlFor="processo_id">Processo ID</Label>…</div>` and the error-paragraph block unchanged. Import `Combobox` from `@/components/shared/combobox` and `Controller` from `react-hook-form` (if not already imported). Do NOT modify `documentosFiltersFormSchema`.
  </action>
  <verify>
    <automated>grep -q 'permissions.isFetched && !canViewDocumentos' "web/src/app/(dashboard)/documentos/page.tsx" && grep -q 'from "@/components/shared/combobox"' "web/src/app/(dashboard)/documentos/page.tsx" && grep -q 'name="processo_id"' "web/src/app/(dashboard)/documentos/page.tsx" && grep -q 'Pesquisar processo por número ou título' "web/src/app/(dashboard)/documentos/page.tsx" && ! grep -q 'id="processo_id" {...form.register' "web/src/app/(dashboard)/documentos/page.tsx"</automated>
  </verify>
  <done>
    The Processo filter is a `Controller`-wrapped closed-list `Combobox` sourced from `processos.data`; the RBAC gate uses `isFetched`; the schema is untouched; the free-text `<Input>` for processo_id is gone.
  </done>
  <acceptance_criteria>
    - Source assertion: `permissions.isFetched && !canViewDocumentos` present.
    - Source assertion: `Combobox` imported from `@/components/shared/combobox`; a `Controller` with `name="processo_id"` renders it; the old `<Input id="processo_id" {...form.register}>` is gone.
    - Source assertion: `processoOptions` maps `processos.data` to `{value: p.id, label: p.numero ?? p.titulo ?? p.id}`; no new fetch hook added.
    - Behavior: selecting a processo in the Combobox sets `processo_id` to that id; clicking "Filtrar" narrows the list to that processo (submit model unchanged).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Cliente filter to closed-list Combobox (Controller)</name>
  <files>web/src/app/(dashboard)/documentos/page.tsx</files>
  <read_first>
    - web/src/app/(dashboard)/documentos/page.tsx (the Cliente filter `<Input>` lines 120-126; the `clientes = useClientes({})` at line 65 and `clienteNomeById` map lines 67-74; the Processo filter just migrated in Task 1 — replicate its exact shape)
    - web/src/components/shared/combobox.tsx (closed-list mode)
    - .planning/phases/LEXCV-107-m-dulos-documentos-financeiro/107-UI-SPEC.md (Combobox closed-list mode: Cliente placeholders/empty copy)
  </read_first>
  <action>
    Replace the Cliente filter `<Input id="cliente_id" {...form.register("cliente_id")} placeholder="Ex.: 1a2b..." />` (lines 120-126) with a `<Controller control={form.control} name="cliente_id" render={({ field }) => (<Combobox id="cliente_id" value={field.value} onChange={field.onChange} options={clienteOptions} placeholder="Todos os clientes" searchPlaceholder="Pesquisar cliente por nome..." emptyMessage="Nenhum cliente encontrado." />)} />`, mirroring Task 1's Processo shape exactly. Build `clienteOptions` from the already-fetched `clientes.data` as `(clientes.data ?? []).map((c) => ({ value: c.id, label: c.nome }))` — reuse the same label logic already used by `clienteNomeById`; no second fetch. Keep the surrounding `<div className="space-y-2"><Label htmlFor="cliente_id">Cliente ID</Label>…</div>` and error-paragraph block unchanged.
  </action>
  <verify>
    <automated>grep -q 'name="cliente_id"' "web/src/app/(dashboard)/documentos/page.tsx" && grep -q 'Pesquisar cliente por nome' "web/src/app/(dashboard)/documentos/page.tsx" && grep -q 'Nenhum cliente encontrado' "web/src/app/(dashboard)/documentos/page.tsx" && ! grep -q 'id="cliente_id" {...form.register' "web/src/app/(dashboard)/documentos/page.tsx" && cd web && pnpm exec tsc --noEmit</automated>
  </verify>
  <done>
    The Cliente filter is a `Controller`-wrapped closed-list `Combobox` sourced from `clientes.data`; the free-text `<Input>` for cliente_id is gone; `pnpm exec tsc --noEmit` passes (both filters + Combobox consumption typecheck clean).
  </done>
  <acceptance_criteria>
    - Source assertion: a `Controller` with `name="cliente_id"` renders a `Combobox` with the Cliente placeholders/empty copy; the old `<Input id="cliente_id" {...form.register}>` is gone.
    - Source assertion: `clienteOptions` maps `clientes.data` to `{value: c.id, label: c.nome}`.
    - CLI: `cd web && pnpm exec tsc --noEmit` exits 0.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → Next rewrite → Spring API | Documentos list read + filter. The filter values (`processo_id`/`cliente_id`) are already sent as query params to the existing endpoint — now sourced from a picked id instead of free-typed UUID text. Client RBAC gate is defense-in-depth; server `@PreAuthorize` unchanged. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-107-07 | Tampering | filter value source (free UUID → picked id) | mitigate | The Combobox constrains the value to an id from the user's own authorized processo/cliente list, strictly narrowing the input surface vs. the prior free-text UUID field. Server tenant-scoping of the list query is unchanged and remains authoritative. |
| T-107-08 | Elevation of Privilege | client RBAC gate on documentos list | mitigate | Switch to `permissions.isFetched && !canViewDocumentos`, closing the pre-resolve render race. Server `@PreAuthorize` unchanged. |
| T-107-SC | Tampering | npm/pnpm installs | accept | Zero new dependencies (UI-SPEC Registry Safety); consumes the Plan-01 Combobox composed from already-installed primitives. |
</threat_model>

<verification>
- Both filters are `Controller`-wrapped `Combobox` instances sourced from already-fetched `processos.data`/`clientes.data`; no free-text UUID `<Input>` remains for either.
- `documentosFiltersFormSchema` unchanged; `pnpm exec tsc --noEmit` passes.
- RBAC gate uses `isFetched`.
</verification>

<success_criteria>
- DOF-02 satisfied for the Documentos list filters (searchable closed-list Combobox for Processo + Cliente).
- The 6th? No — this page's RBAC gate is the 1st of the 6 bundled gates numerically but fixed here in Wave 2; combined with Plans 02/03 all 6 gates are fixed across the phase.
</success_criteria>

<output>
Create `.planning/phases/LEXCV-107-m-dulos-documentos-financeiro/107-04-SUMMARY.md` when done.
</output>
