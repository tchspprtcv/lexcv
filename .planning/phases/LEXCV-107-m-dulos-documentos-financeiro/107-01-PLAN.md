---
phase: 107-m-dulos-documentos-financeiro
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/components/shared/combobox.tsx
autonomous: true
requirements: [DOF-02]
must_haves:
  truths:
    - "A shared Combobox component exists rendering a Popover+Command dropdown behind an outline trigger button"
    - "In closed mode, the trigger shows the label of the option matching the current value, or a muted placeholder when unset"
    - "In creatable mode, a typed value with no exact match can be committed via a 'Usar \"{query}\"' item and is displayed in the trigger"
    - "Selecting any item closes the popover, clears the query, and calls onChange with the chosen value"
    - "Filtering is manual (shouldFilter={false}) so the creatable item always renders regardless of cmdk match logic"
  artifacts:
    - path: "web/src/components/shared/combobox.tsx"
      provides: "Shared Combobox composition (Popover + Command), first Command consumer in the project"
      exports: ["Combobox"]
      min_lines: 60
  key_links:
    - from: "web/src/components/shared/combobox.tsx"
      to: "@/components/ui/command"
      via: "import CommandInput/CommandList/CommandGroup/CommandItem/CommandEmpty"
      pattern: "from \"@/components/ui/command\""
    - from: "web/src/components/shared/combobox.tsx"
      to: "@/components/ui/popover"
      via: "import Popover/PopoverTrigger/PopoverContent"
      pattern: "from \"@/components/ui/popover\""
---

<objective>
Build the shared `Combobox` composition (`Popover` + `Command`) — the first `Command` consumer in the project and the foundational interface that the four Wave-2 call sites (Documentos list Processo/Cliente filters, and the `Documento.tipo` field in the Processo and Cliente document tabs) will consume.

Purpose: DOF-02 expands `Documento.tipo` from a `datalist` to a proper "closed-list + typeable new value" control. No existing `Command`-based analog exists; `DatePickerField` (Phase 106) is the structural precedent for composing a new field from `Popover` primitives with plain `value`/`onChange` props (Controller-compatible). Defining this contract first prevents the Wave-2 executors from a scavenger hunt.
Output: `web/src/components/shared/combobox.tsx` exporting a `Combobox` component supporting both closed-searchable and creatable modes.
</objective>

<execution_context>
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/LEXCV-107-m-dulos-documentos-financeiro/107-CONTEXT.md
@.planning/phases/LEXCV-107-m-dulos-documentos-financeiro/107-PATTERNS.md
@.planning/phases/LEXCV-107-m-dulos-documentos-financeiro/107-UI-SPEC.md

<interfaces>
<!-- LOCKED prop signature for this component. Wave-2 plans 04 and 05 are written against exactly these names. -->
<!-- ComboboxOption = { value: string; label: string } -->
<!-- Combobox props:
       value: string | undefined
       onChange: (v: string) => void
       options: ComboboxOption[]
       placeholder?: string          (trigger text when nothing selected)
       searchPlaceholder?: string    (CommandInput placeholder)
       emptyMessage?: string         (CommandEmpty text; default "Nenhum resultado encontrado.")
       creatable?: boolean           (default false)
       createLabel?: (query: string) => string   (default: q => `Usar "${q}"`)
       triggerClassName?: string     (extra classes merged onto the trigger Button)
       id?: string                   (forwarded to the trigger Button for <Label htmlFor>)
-->

Primitive contracts already read in full (see 107-PATTERNS.md for exact line refs):
- @web/src/components/ui/command.tsx  (CommandInput pre-wraps SearchIcon + forces h-8; CommandItem auto-renders trailing CheckIcon gated on data-checked)
- @web/src/components/ui/popover.tsx  (thin radix-ui re-export; default align="end" sideOffset=8 — override align="start")
- @web/src/components/shared/date-picker-field.tsx  (structural precedent: open-state + PopoverTrigger asChild Button pattern)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the shared Combobox component (closed + creatable modes)</name>
  <files>web/src/components/shared/combobox.tsx</files>
  <read_first>
    - web/src/components/shared/date-picker-field.tsx (structural precedent — copy the "use client", open-state useState, and `Popover open/onOpenChange` + `PopoverTrigger asChild` + outline `Button` shape verbatim, only the popover content differs)
    - web/src/components/ui/command.tsx (full primitive contract: CommandInput at lines 65-86 pre-wraps SearchIcon and forces h-8; CommandItem at lines 146-164 auto-renders a trailing CheckIcon gated on `data-checked`; CommandEmpty at lines 104-115)
    - web/src/components/ui/popover.tsx (default align="end" — must override align="start")
    - web/src/components/ui/button.tsx (trigger uses variant="outline"; buttonVariants default weight is font-medium, must be overridden to font-normal)
    - .planning/phases/LEXCV-107-m-dulos-documentos-financeiro/107-UI-SPEC.md (Component Inventory → "Combobox (Popover + Command)": the binding visual/interaction contract, both mode tables)
    - .planning/phases/LEXCV-107-m-dulos-documentos-financeiro/107-PATTERNS.md (combobox.tsx section: import deltas, trigger anatomy, findings #4/#5/#7)
  </read_first>
  <behavior>
    - Trigger: `Button type="button" variant="outline" role="combobox" aria-expanded={open}` with `className` merged from a base `"w-full justify-between font-normal"` plus the caller's `triggerClassName`, and the caller's `id` forwarded. Trailing icon `ChevronsUpDown` (lucide-react, first use) `className="ml-2 h-4 w-4 shrink-0 opacity-50"`.
    - Trigger label logic: compute `selected = options.find(o => o.value === value)`. Display text = `selected?.label` if matched; ELSE, when `creatable` and `value` is a non-empty string, display the raw `value` (a freshly-typed tipo that is not in `options` must still show, not fall back to placeholder); ELSE the `placeholder`. Apply `text-muted-foreground` only when the displayed text is the placeholder (i.e. no selected label and no creatable raw value).
    - PopoverContent: `className="w-80 p-0" align="start"` (fixed w-80 per finding #7, p-0 because Command supplies its own p-1).
    - `<Command shouldFilter={false}>` — manual filtering. Maintain a `query` state; `filtered = options.filter(o => o.label.toLowerCase().includes(query.trim().toLowerCase()))`.
    - `CommandInput` receives `placeholder={searchPlaceholder}` `value={query}` `onValueChange={setQuery}` — do NOT pass any height/className override (SearchIcon + h-8 already baked in per finding #5).
    - Creatable "create" item: when `creatable` and `query.trim()` is non-empty AND no option matches `query.trim()` case-insensitively, render a single `CommandItem` (value bound to the query) ABOVE the filtered group, labelled by `createLabel(query.trim())` (default `Usar "{query}"`); selecting it calls `commit(query.trim())`.
    - Each option renders a `CommandItem` with `data-checked={option.value === value}`, `value={option.label}` (so cmdk keyboard nav works on visible text), and children = the label text ONLY (do NOT render a manual `<Check>` — CommandItem auto-renders the checkmark; a manual one double-renders per finding #4). `onSelect` calls `commit(option.value)`.
    - `commit(v)` helper: `onChange(v); setOpen(false); setQuery("");` (matches DatePickerField's immediate close-on-select UX; no separate Aplicar step).
    - Empty state: `CommandEmpty` with `emptyMessage` (default "Nenhum resultado encontrado."). In creatable mode the create item guarantees a forward path once the user types, so CommandEmpty is only visible when both `query` is empty and there are zero options.
  </behavior>
  <action>
    Create `web/src/components/shared/combobox.tsx` as a `"use client"` component exporting `function Combobox(...)` and a `ComboboxOption` type (`{ value: string; label: string }`), per the LOCKED prop signature in this plan's `<interfaces>` block. Reuse the exact `useState(open)` + `Popover open={open} onOpenChange={setOpen}` + `PopoverTrigger asChild` + outline `Button` skeleton from `date-picker-field.tsx`, swapping the leading `CalendarIcon` for a TRAILING `ChevronsUpDown` and the `Calendar` content for the `Command` composition described in `<behavior>`. Import `Button` from `@/components/ui/button`; `Popover`/`PopoverTrigger`/`PopoverContent` from `@/components/ui/popover`; `Command`/`CommandInput`/`CommandList`/`CommandGroup`/`CommandItem`/`CommandEmpty` from `@/components/ui/command`; `cn` from `@/lib/utils`; `ChevronsUpDown` from `lucide-react`. Do NOT add a manual `Check` icon inside options (finding #4). Do NOT override `CommandInput` height (finding #5). Use `align="start"` on `PopoverContent` (overriding popover.tsx's `align="end"` default). Keep the trigger `font-normal` (overriding buttonVariants' `font-medium`), matching the `DatePickerField` precedent for controls that display a data value rather than an action label.
  </action>
  <verify>
    <automated>test -f web/src/components/shared/combobox.tsx && grep -q 'export function Combobox' web/src/components/shared/combobox.tsx && grep -q 'shouldFilter={false}' web/src/components/shared/combobox.tsx && grep -q 'ChevronsUpDown' web/src/components/shared/combobox.tsx && grep -q 'role="combobox"' web/src/components/shared/combobox.tsx && grep -q 'data-checked' web/src/components/shared/combobox.tsx && ! grep -q '<Check' web/src/components/shared/combobox.tsx && cd web && pnpm exec tsc --noEmit</automated>
  </verify>
  <done>
    combobox.tsx exists, exports `Combobox` + `ComboboxOption`, uses `shouldFilter={false}` with manual filtering, a trailing `ChevronsUpDown`, `role="combobox"`/`aria-expanded`, `data-checked` on items, NO manual `<Check>` child, and `pnpm exec tsc --noEmit` passes with zero errors.
  </done>
  <acceptance_criteria>
    - Source assertion: file exports `Combobox` and type `ComboboxOption`; imports come from `@/components/ui/command` and `@/components/ui/popover`.
    - Source assertion: `<Command shouldFilter={false}>` present; no manual `<Check` child inside options; `CommandInput` carries no height override.
    - Source assertion: trigger `Button` has `variant="outline"`, `role="combobox"`, `aria-expanded={open}`, `font-normal`, and a trailing `ChevronsUpDown`.
    - Source assertion: creatable branch renders a `CommandItem` whose label uses `createLabel`/`Usar "` only when the trimmed query has no case-insensitive exact match.
    - CLI: `cd web && pnpm exec tsc --noEmit` exits 0.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client component → (none) | Pure client-side UI composition; no network, no new server input. Selected values feed existing, unchanged consumers (existing list filter query params / existing free-text upload `tipo`). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-107-01 | Tampering | Combobox creatable free-text `value` | accept | `tipo` is already unconstrained free text server-side (no enum in any layer); this component narrows, not widens, input surface. Server upload endpoint validation unchanged and out of scope. |
| T-107-02 | Information Disclosure | option labels rendered client-side | accept | Options are sourced by consumers from data the user is already authorized to see (RBAC-gated pages); Combobox renders only what it is handed. |
| T-107-SC | Tampering | npm/pnpm installs | accept | Zero new dependencies this phase (UI-SPEC Registry Safety). `cmdk` behind `Command` was vetted + installed in Phase 101; this is its first consumer, not a new install. |
</threat_model>

<verification>
- `cd web && pnpm exec tsc --noEmit` passes (component compiles with no consumers yet).
- `grep` confirms the binding contract markers: `shouldFilter={false}`, `role="combobox"`, `data-checked`, trailing `ChevronsUpDown`, and no manual `<Check>` child.
</verification>

<success_criteria>
- `web/src/components/shared/combobox.tsx` exists, exports `Combobox` + `ComboboxOption`, and implements both closed-searchable and creatable modes per the UI-SPEC binding contract.
- Typecheck passes; the LOCKED prop signature matches what plans 04 and 05 consume.
</success_criteria>

<output>
Create `.planning/phases/LEXCV-107-m-dulos-documentos-financeiro/107-01-SUMMARY.md` when done.
</output>
