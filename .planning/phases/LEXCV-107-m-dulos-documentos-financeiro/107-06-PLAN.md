---
phase: 107-m-dulos-documentos-financeiro
plan: 06
type: execute
wave: 3
depends_on: [107-01, 107-02, 107-03, 107-04, 107-05]
files_modified: []
autonomous: false
requirements: [DOF-01, DOF-02]
must_haves:
  truths:
    - "pnpm build and pnpm lint both pass with zero new errors across the whole web app"
    - "Zero hand-rolled bg-blue-600 upload progress bars remain in any of the 3 upload sites"
    - "Zero native <datalist>-based Documento.tipo inputs remain in the 2 ficha tabs"
    - "Zero '!permissions.isLoading && !can' access gates remain in the 6 migrated files, while the 3 legitimate isLoading submit-guards are intact"
    - "A human has visually confirmed Progress bars, both Combobox modes, Select filters, and NativeSelect fields in light and dark themes"
  artifacts: []
  key_links: []
---

<objective>
Final holistic quality gate for Phase 107: run the full build/lint pipeline, assert via regression greps that every migration landed and no legacy pattern survives across all 8 modified source files, then a blocking human visual checkpoint confirms the visual/interaction contract in both themes.

Purpose: this phase touches two large ficha files and 6 route files across two waves; a single end-of-phase gate catches integration regressions (import gaps, leftover legacy markup, sentinel-logic mistakes) that per-file greps cannot, and the human check validates the pieces the automated gate cannot (the new Combobox composition's real dropdown behavior, Progress bar tokenization, dark mode).
Output: green build/lint + regression assertions + recorded human sign-off.
</objective>

<execution_context>
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/LEXCV-107-m-dulos-documentos-financeiro/107-CONTEXT.md
@.planning/phases/LEXCV-107-m-dulos-documentos-financeiro/107-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Holistic build/lint gate + regression greps</name>
  <files>web/src/app/(dashboard)/documentos/page.tsx, web/src/app/(dashboard)/documentos/novo/page.tsx, web/src/app/(dashboard)/documentos/[id]/page.tsx, web/src/app/(dashboard)/financeiro/page.tsx, web/src/app/(dashboard)/financeiro/novo/page.tsx, web/src/app/(dashboard)/financeiro/[id]/page.tsx, web/src/app/(dashboard)/processos/[id]/page.tsx, web/src/app/(dashboard)/clientes/[id]/page.tsx</files>
  <read_first>
    - .planning/phases/LEXCV-107-m-dulos-documentos-financeiro/107-UI-SPEC.md (the binding contract being verified — Progress/Select/NativeSelect/Combobox inventories)
    - .planning/phases/LEXCV-107-m-dulos-documentos-financeiro/107-PATTERNS.md (Shared Patterns section — the 6 RBAC gates and the 3 do-not-touch submit-guards)
  </read_first>
  <action>
    Run `cd web && pnpm build` and `cd web && pnpm lint`; both must exit 0 with no new errors. Then confirm the regression assertions below (all encoded in the automated verify as an &&-chain that exits 0 only when every assertion holds). If build/lint fails or any assertion fails, fix the offending file (this gate plan may edit any of the 8 listed source files to close a regression) and re-run until green. Regression assertions:
    (a) No hand-rolled progress fill survives: `bg-blue-600 transition-all` appears ZERO times in documentos/novo, processos/[id], clientes/[id].
    (b) All 3 Progress swaps landed: `<Progress value={progresso ?? 0}` appears in each of those 3 files.
    (c) No `<datalist>` tipo input survives: `<datalist` appears ZERO times in processos/[id] and clientes/[id].
    (d) All 6 access gates migrated: `!permissions.isLoading && !can` appears ZERO times across the 6 files (documentos page/novo/[id], financeiro page/novo/[id]).
    (e) The 3 submit-guards intact: `permissions.isLoading || !canCreateDocumentos` (documentos/novo), `permissions.isLoading || !canCreateFinanceiro` (financeiro/novo), `permissions.isLoading || !canEditFinanceiro` (financeiro/[id]) each still present.
    (f) Financeiro filters migrated: `<Select value={filtroProcesso}` and `<Select value={filtroStatus}` present in financeiro/page; `const selectClassName` absent in financeiro/novo.
    (g) Combobox adopted at all 4 sites: `from "@/components/shared/combobox"` present in documentos/page, processos/[id], clientes/[id].
  </action>
  <verify>
    <automated>cd web && pnpm build && pnpm lint && cd .. && ! grep -rq 'bg-blue-600 transition-all' "web/src/app/(dashboard)/documentos/novo/page.tsx" "web/src/app/(dashboard)/processos/[id]/page.tsx" "web/src/app/(dashboard)/clientes/[id]/page.tsx" && grep -q '<Progress value={progresso ?? 0}' "web/src/app/(dashboard)/documentos/novo/page.tsx" && grep -q '<Progress value={progresso ?? 0}' "web/src/app/(dashboard)/processos/[id]/page.tsx" && grep -q '<Progress value={progresso ?? 0}' "web/src/app/(dashboard)/clientes/[id]/page.tsx" && ! grep -rq '<datalist' "web/src/app/(dashboard)/processos/[id]/page.tsx" "web/src/app/(dashboard)/clientes/[id]/page.tsx" && ! grep -rq '!permissions.isLoading && !can' "web/src/app/(dashboard)/documentos/page.tsx" "web/src/app/(dashboard)/documentos/novo/page.tsx" "web/src/app/(dashboard)/documentos/[id]/page.tsx" "web/src/app/(dashboard)/financeiro/page.tsx" "web/src/app/(dashboard)/financeiro/novo/page.tsx" "web/src/app/(dashboard)/financeiro/[id]/page.tsx" && grep -q 'permissions.isLoading || !canCreateDocumentos' "web/src/app/(dashboard)/documentos/novo/page.tsx" && grep -q 'permissions.isLoading || !canCreateFinanceiro' "web/src/app/(dashboard)/financeiro/novo/page.tsx" && grep -q 'permissions.isLoading || !canEditFinanceiro' "web/src/app/(dashboard)/financeiro/[id]/page.tsx" && ! grep -q 'const selectClassName' "web/src/app/(dashboard)/financeiro/novo/page.tsx" && grep -q '<Select value={filtroProcesso}' "web/src/app/(dashboard)/financeiro/page.tsx" && grep -q '<Select value={filtroStatus}' "web/src/app/(dashboard)/financeiro/page.tsx" && grep -q 'from "@/components/shared/combobox"' "web/src/app/(dashboard)/documentos/page.tsx" && grep -q 'from "@/components/shared/combobox"' "web/src/app/(dashboard)/processos/[id]/page.tsx" && grep -q 'from "@/components/shared/combobox"' "web/src/app/(dashboard)/clientes/[id]/page.tsx"</automated>
  </verify>
  <done>
    `pnpm build` and `pnpm lint` pass; assertions (a)-(g) all hold: no legacy progress bar / datalist / isLoading-access-gate survives, all Progress/Select/NativeSelect/Combobox migrations are present, the 3 submit-guards remain, and `selectClassName` is gone.
  </done>
  <acceptance_criteria>
    - CLI: `cd web && pnpm build` exits 0; `cd web && pnpm lint` exits 0.
    - CLI: the full &&-chain in the automated verify exits 0 (every presence/absence assertion holds).
    - CLI: `grep -rq 'bg-blue-600 transition-all'` over the 3 upload files returns no matches; `<datalist` returns no matches in the 2 ficha files.
    - CLI: `!permissions.isLoading && !can` returns no matches over the 6 migrated files; the 3 submit-guard strings are each still present.
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human visual checkpoint — Progress, Combobox (both modes), Select, NativeSelect (light + dark)</name>
  <read_first>
    - .planning/phases/LEXCV-107-m-dulos-documentos-financeiro/107-UI-SPEC.md (Component Inventory — Progress/Select/NativeSelect/Combobox contracts + Copywriting Contract placeholders)
    - .planning/phases/LEXCV-107-m-dulos-documentos-financeiro/107-CONTEXT.md (locked scope boundary — tipo in documentos/novo stays plain Input; Pagamento.metodo untouched; the bundled RBAC isFetched fix intent)
  </read_first>
  <action>
    After Task 1's gate is green, start the dev server (`cd web && pnpm dev`) with the backend reachable so RBAC/permission and processo/cliente data load, present the checklist in `<how-to-verify>`, and PAUSE for the human verdict. This is a blocking human-verify checkpoint — never auto-approve (`workflow.auto_advance` is ignored, and `parallelization.skip_checkpoints` does not apply since this plan runs solo in Wave 3). Record the human's concrete evidence (themes exercised, the creatable Combobox "Usar …" commit observed, the dark-mode Progress fill + popover elevation seen) and the approved/issues verdict in 107-06-SUMMARY.md; if issues are reported, enumerate them for a `/gsd:plan-phase 107 --gaps` closure plan rather than silently closing the phase.
  </action>
  <what-built>
    Phase 107 migrated: the upload Progress bar at 3 sites (documentos/novo, ProcessoDocumentosTab, ClienteDocumentosEntreguesTab) to the official `Progress` (bg-primary, theme-aware); `Documento.confidencialidade` and honorário `processoId` to `NativeSelect`; the Financeiro Processo/Estado list filters to Radix `Select` (with a `"todos"` sentinel); and a brand-new shared `Combobox` at 4 sites — creatable mode for `Documento.tipo` in the 2 ficha upload dialogs, and closed-list searchable mode for the Processo/Cliente filters on the Documentos list. The 6 RBAC gates were switched to `permissions.isFetched`.
  </what-built>
  <how-to-verify>
    Run `cd web && pnpm dev` and, in BOTH light and dark themes:
    1. `/documentos` — the Processo and Cliente filters are now dropdown buttons ("Todos os processos"/"Todos os clientes"). Open each: a search box filters by número/título (Processo) / nome (Cliente); pick one, click "Filtrar" — the list narrows. "Limpar" resets. Confirm the checkmark shows on the selected row (single check, not doubled).
    2. `/documentos/novo` — upload a file: the progress bar animates as a solid `Progress` bar (no visual glitch). Confirm `Confidencialidade` is a native select with the 4 options. Confirm `Tipo` is still a plain free-text input (NOT a Combobox — locked exclusion).
    3. Open a processo `/processos/{id}` → aba Documentos → "Adicionar Documento": the `Tipo` field is now a Combobox — pick an existing tipo, then type a brand-new tipo and confirm the `Usar "…"` item appears and commits + displays in the trigger. Upload and confirm the progress bar renders via `Progress`.
    4. Open a cliente `/clientes/{id}` → aba Documentos Entregues → repeat step 3 (byte-identical sibling).
    5. `/financeiro` — Processo and Estado filters are Radix `Select`; changing each narrows the list; default ("Todos") shows the full list. Confirm the honorário-create form (`/financeiro/novo`) `Processo` field is a NativeSelect.
    6. In dark mode specifically, confirm the Progress bar fill uses the theme primary (blue-500) and the Combobox/Select dropdown surfaces have proper popover elevation (no flat/invisible panel).
  </how-to-verify>
  <acceptance_criteria>
    - Both Documentos-list filter Comboboxes open, search, select, and narrow the list on "Filtrar"; the selected row shows exactly one checkmark (no double-check bug).
    - The 2 ficha `Documento.tipo` Comboboxes support both picking an existing tipo and committing a freshly-typed new tipo via the `Usar "…"` item.
    - Progress bars render via the official `Progress` at all 3 upload sites; in dark mode the fill uses the theme primary.
    - Financeiro Processo/Estado filters are Radix `Select` with a working "Todos" default; `documentos/novo` Tipo remains a plain Input.
    - No access-denied flash for unauthorized roles on the Documentos/Financeiro routes.
  </acceptance_criteria>
  <resume-signal>Type "approved" if all 6 checks pass in both themes, or describe the issues found.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| verification only | This plan runs build/lint/grep and a human visual check; it introduces no new runtime code path or server input. Any fix it applies is bounded to the 8 already-in-scope files. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-107-11 | Elevation of Privilege | final RBAC-gate regression sweep | mitigate | Assertion (d) proves zero `!permissions.isLoading && !can` gates survive and (e) proves the 3 submit-guards are intact — closes the risk that a wave-parallel edit reverted or missed a gate. Server `@PreAuthorize` remains authoritative. |
| T-107-SC | Tampering | npm/pnpm installs | accept | Zero new dependencies this phase; `pnpm build` confirms the lockfile is unchanged. |
</threat_model>

<verification>
- `pnpm build` + `pnpm lint` green.
- Regression greps (a)-(g) all hold.
- Human visual checkpoint APPROVED for all 6 checks in light and dark themes.
</verification>

<success_criteria>
- DOF-01 and DOF-02 verified end-to-end across all 8 modified files; no legacy pattern survives; human sign-off recorded.
- Phase 107 ready to mark complete (DOF-01, DOF-02 → Complete in REQUIREMENTS.md).
</success_criteria>

<output>
Create `.planning/phases/LEXCV-107-m-dulos-documentos-financeiro/107-06-SUMMARY.md` when done.
</output>
