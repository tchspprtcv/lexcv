---
phase: 125-moldes-da-plataforma-e-provisionamento
plan: 05
type: execute
wave: 4
depends_on: [125-04]
files_modified:
  - web/src/app/(dashboard)/plataforma/moldes/page.tsx
autonomous: true
requirements: [MOLD-02, MOLD-03]

must_haves:
  truths:
    - "PLATAFORMA_ADMIN vê e edita as permissões de cada molde numa matriz permissão × molde em /plataforma/moldes"
    - "Um utilizador sem PLATAFORMA_ADMIN vê o estado de acesso negado, nunca a matriz"
    - "O aviso de não-propagação é visível permanentemente acima da matriz, sem depender de o operador tentar gravar"
    - "Cada coluna de molde mostra, sempre, quantos escritórios já instanciaram aquele molde"
    - "Gravar exige confirmação explícita num diálogo que lista apenas os moldes alterados e diz que nenhum escritório existente é afectado"
    - "A matriz é navegável por leitor de ecrã: cabeçalhos com scope e um nome acessível por cada checkbox"
  artifacts:
    - path: "web/src/app/(dashboard)/plataforma/moldes/page.tsx"
      provides: "Rota /plataforma/moldes: guarda de página, banner âmbar, matriz acessível, fluxo de gravação com AlertDialog"
      min_lines: 250
      contains: "TriangleAlert"
  key_links:
    - from: "web/src/app/(dashboard)/plataforma/moldes/page.tsx"
      to: "useMoldes"
      via: "hook TanStack Query do Plan 04"
      pattern: "useMoldes\\(\\)"
    - from: "web/src/app/(dashboard)/plataforma/moldes/page.tsx"
      to: "useUpdateMoldes"
      via: "mutação disparada apenas pelo AlertDialogAction"
      pattern: "useUpdateMoldes\\(\\)"
    - from: "web/src/app/(dashboard)/plataforma/moldes/page.tsx"
      to: "aria-label por checkbox"
      via: "`${permissao.nome} — ${molde.nome}`"
      pattern: "aria-label=\\{`\\$\\{"
---

<objective>
Construir o ecrã `/plataforma/moldes`: guarda de página, banner de não-propagação, matriz acessível permissão × molde, e o fluxo de gravação com confirmação obrigatória.

Purpose: o aviso de não-propagação em três camadas (banner permanente + contagem por coluna + `AlertDialog` obrigatório na gravação) é a peça de desenho mais importante do ecrã. É o único mecanismo que impede a decisão de snapshot de se tornar uma surpresa desagradável: quem administra a plataforma vai assumir propagação — é a expectativa por omissão de qualquer pessoa — e sem este aviso só descobriria o contrário quando um cliente reclamasse.
Output: 1 rota nova, completa, sem placeholders.

**O UI-SPEC é binding e passou por duas rondas de BLOCK.** Não re-litigar as suas decisões. Em particular: o `<strong>` sem classe de peso no banner é um alinhamento deliberado à convenção do próprio banner do `RbacTab` — não "corrigir". O `p-3` de célula é uma excepção declarada e grid-alinhada — não passar a 16px. O botão âmbar só pode existir dentro do `AlertDialog`, nunca no `CardHeader`.

O botão "Criar Molde" **não** é adicionado neste plano — vem no Plan 06, junto do painel que abre. Não criar um botão desactivado nem qualquer placeholder no seu lugar.
</objective>

<execution_context>
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/LEXCV-125-moldes-da-plataforma-e-provisionamento/125-UI-SPEC.md
@.planning/phases/LEXCV-125-moldes-da-plataforma-e-provisionamento/125-PATTERNS.md
@.planning/phases/LEXCV-125-moldes-da-plataforma-e-provisionamento/125-04-SUMMARY.md
@CLAUDE.md
@web/AGENTS.md
</context>

<build_environment>
`web/node_modules` pode não existir neste worktree. O primeiro comando é `cd web && pnpm install`. Se a saída de qualquer comando de verificação contiver `command not found`, `ERR_PNPM` ou uma falha de resolução de pacote, o critério **não** está cumprido, independentemente do código de saída (lição registada: "rtk tsc false-clean in fresh worktree").

Next.js 16: consultar `web/node_modules/next/dist/docs/` antes de escrever código de routing/framework — esta versão tem quebras face a convenções mais antigas (`web/AGENTS.md`).
</build_environment>

<interfaces>
<!-- Contratos já extraídos. O executor não precisa de explorar o codebase. -->

Do Plan 04, `web/src/types/platform-moldes.ts`:
`MoldePermissao = { key: string; nome: string; descricao: string | null; modulo: string }` (`key` = chave técnica `scope:action`, `nome` = rótulo legível — a inversão é herdada do contrato da Phase 124);
`MoldeSummary = { id: number; nome: string; permissoes: string[]; escritoriosInstanciados: number }`;
`MoldesConsola = { permissoes: MoldePermissao[]; moldes: MoldeSummary[] }`;
`MoldesUpdateRequest = { moldes: { id: number; permissoes: string[] }[] }`.
Do Plan 04, `web/src/hooks/use-platform-moldes.ts`: `useMoldes()` (query), `useUpdateMoldes()` (mutação PUT em lote, devolve o payload actualizado), `useCreateMolde()` (não usado neste plano).

Guarda de página a copiar verbatim de `web/src/app/(dashboard)/plataforma/relatorio/page.tsx:28-51` — a ordem **não** é negociável (WR-03, revisão da Phase 120): componente exportado chama `const me = useMe();`, depois `if (!me.isFetched) { return null; }`, e só depois `if (!me.data?.roles?.includes("PLATAFORMA_ADMIN")) { return <AccessDeniedState description="..." backHref="/dashboard" />; }`, e por fim renderiza um componente de conteúdo separado. Nunca combinar as duas condições numa só.

Imports já usados por `relatorio/page.tsx` (mesmos caminhos): `{ AccessDeniedState } from "@/components/shared/access-denied-state"`, `{ Badge } from "@/components/ui/badge"`, `{ Button } from "@/components/ui/button"`, `{ Card, CardContent, CardDescription, CardHeader, CardTitle } from "@/components/ui/card"`, `{ useMe } from "@/hooks/use-me"`, `Link from "next/link"`, `{ ArrowLeft } from "lucide-react"`.

Cabeçalho com back-arrow a copiar (`relatorio/page.tsx:67-87`): `<div className="flex items-center gap-4">` com `<Button asChild variant="ghost" className="h-9 w-9 p-0 text-slate-500 hover:text-slate-900 dark:hover:text-white"><Link href="/plataforma" aria-label="Voltar"><ArrowLeft className="h-4 w-4" /></Link></Button>` seguido de `<div>` com `h1` e `p`.
Classes do `Card` a copiar: `border-slate-200 dark:border-slate-800 bg-white/50 dark:bg-slate-900/50 backdrop-blur-sm rounded-xl`.

Matriz a copiar de `RbacTab` (`web/src/app/(dashboard)/settings/page.tsx:919-990`), estado actual relevante:
- wrapper `<div className="overflow-x-auto border border-slate-200 dark:border-slate-800 rounded-md">`, `<table className="w-full text-sm text-left border-collapse">`;
- `<thead className="bg-slate-100/80 dark:bg-slate-950/80 border-b border-slate-200 dark:border-slate-800">`, primeira `<th className="p-3 font-semibold text-slate-600 dark:text-slate-400 min-w-[280px]">Módulo / Permissão</th>`, e uma `<th className="p-3 font-bold text-center text-slate-700 dark:text-slate-300 text-xs tracking-wider">` por papel;
- `<tbody className="divide-y divide-slate-200 dark:divide-slate-800">`; agrupamento por módulo com `const modules = Array.from(new Set(systemPermissions.map((p) => p.modulo)));`; linha separadora `<tr className="bg-slate-50 dark:bg-slate-900/50"><td colSpan={5} className="px-3 py-2 text-xs font-bold text-slate-500 uppercase tracking-widest">{mod}</td></tr>` — **o `colSpan={5}` está hardcoded e tem de passar a `moldes.length + 1`**;
- linha de permissão `<tr className="hover:bg-slate-50/30 dark:hover:bg-slate-900/10 transition-colors">`, célula de nome `<td className="p-3"><div className="font-medium text-slate-900 dark:text-slate-100">{perm.nome}</div><div className="text-[11px] text-slate-500 dark:text-slate-400 mt-0.5">{perm.descricao}</div></td>`;
- célula de checkbox `<td className="p-3 text-center"><label className="inline-flex items-center justify-center cursor-pointer p-2"><input type="checkbox" checked={...} onChange={...} className="h-4 w-4 text-blue-600 focus:ring-blue-500 border-slate-300 dark:border-slate-800 rounded transition-all cursor-pointer" /></label></td>`;
- banner informativo do `RbacTab` (`settings/page.tsx:903-917`), o slot visual directamente análogo: `<div className="p-4 bg-blue-500/5 ... text-xs border ... rounded-md flex items-start gap-2.5">` com `<ShieldAlert className="h-4 w-4 mt-0.5 ... flex-shrink-0" />` e `<div><strong>Nota Importante:</strong> ...</div>`.

`Badge` (`web/src/components/ui/badge.tsx`) tem já as variantes `amber` e `gray` — nenhum primitivo novo é necessário.
`AlertDialog` existe em `web/src/components/ui/alert-dialog.tsx`; `Empty`/`EmptyHeader`/`EmptyTitle`/`EmptyDescription`/`EmptyContent` existem em `web/src/components/ui/empty.tsx`.
`toast` vem de `sonner` (já em uso em `settings/page.tsx` e `plataforma/page.tsx`).

Convenção de erro na gravação, já estabelecida no `AlertDialog` de estado do tenant em `plataforma/page.tsx`: em caso de falha **manter o `AlertDialog` aberto** (o toast do `apiFetch` já informa), para o operador poder tentar de novo sem refazer o diff.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Rota, guarda de página, banner de não-propagação e matriz acessível</name>
  <files>
    web/src/app/(dashboard)/plataforma/moldes/page.tsx
  </files>
  <read_first>
    web/src/app/(dashboard)/plataforma/relatorio/page.tsx
    web/src/app/(dashboard)/settings/page.tsx
    web/src/components/ui/badge.tsx
    web/src/components/ui/empty.tsx
    web/src/types/platform-moldes.ts
    web/src/hooks/use-platform-moldes.ts
    .planning/phases/LEXCV-125-moldes-da-plataforma-e-provisionamento/125-UI-SPEC.md
  </read_first>
  <action>
Criar `web/src/app/(dashboard)/plataforma/moldes/page.tsx` como client component (`"use client";`), com duas partes: o componente exportado por omissão (só guarda de página) e um componente de conteúdo interno (todo o resto), exactamente a divisão de `relatorio/page.tsx`.

**Guarda de página** (secção 2 do UI-SPEC): `useMe()`, depois `if (!me.isFetched) return null;`, depois `if (!me.data?.roles?.includes("PLATAFORMA_ADMIN")) return <AccessDeniedState description="Não tem permissão para aceder à gestão de moldes de papel." backHref="/dashboard" />;`. Copiar também o comentário de `relatorio/page.tsx` que explica porquê a ordem não é negociável (WR-03, Phase 120) e que esta guarda é espelho de UX — a fronteira autoritativa é o `@PreAuthorize` de classe do `PlatformAdminController`.

**Cabeçalho**: back-arrow para `/plataforma` (markup idêntico ao de `relatorio`), `h1` com `text-3xl font-semibold tracking-tight` e o texto "Moldes de Papel da Plataforma" (peso `font-semibold` como em `relatorio`, o seu irmão estrutural — **não** `font-bold` como a página pai `/plataforma`), e subtítulo `text-sm text-slate-500 dark:text-slate-400`: "Defina os conjuntos de permissões que cada escritório novo recebe automaticamente ao ser provisionado."

**Card principal** com as classes de `relatorio` (`border-slate-200 dark:border-slate-800 bg-white/50 dark:bg-slate-900/50 backdrop-blur-sm rounded-xl`). `CardHeader` em `flex-row` com `justify-between`: `CardTitle` "Matriz de Moldes" (`text-xl font-semibold`) e `CardDescription` "Permissões atribuídas a cada molde. Escritórios já provisionados não são afetados por alterações feitas aqui." À direita, neste plano, apenas o slot para o botão de gravação (o botão em si é da Task 2). **Não** acrescentar "Criar Molde" — é do Plan 06.

**Estados de carregamento/erro/vazio** (recipe idêntica ao `RbacTab`, linhas 774-803): carregamento = `Loader2` centrado num contentor `h-48`; erro = `AlertCircle` vermelho centrado + "Não foi possível carregar os moldes de papel." + botão outline "Tentar novamente" a chamar `refetch()`; vazio (defensivo) = `Empty`/`EmptyHeader`/`EmptyTitle`/`EmptyDescription` com título "Nenhum molde definido" e descrição "Crie o primeiro molde para que os próximos escritórios provisionados nasçam com papéis prontos a atribuir." — usar o `EmptyTitle` como está, sem sobrepor o seu peso 500/18px (o UI-SPEC declara essa excepção explicitamente).

**Banner de não-propagação** (secção 4, camada 1 de 3), no topo do `CardContent`, **permanentemente visível** e não condicionado a nenhum estado de gravação: `<div className="p-4 bg-amber-50 dark:bg-amber-500/10 text-slate-700 dark:text-slate-300 text-xs border border-amber-200 dark:border-amber-500/30 rounded-md flex items-start gap-2.5">` com `<TriangleAlert className="h-4 w-4 mt-0.5 text-amber-600 dark:text-amber-400 flex-shrink-0" />` e um `<div>` contendo `<strong>Alterar um molde não atualiza escritórios já criados.</strong>` seguido do texto: "Cada escritório recebe, no momento em que é provisionado, uma cópia própria e independente de cada molde (snapshot) — editar o molde depois disso não muda essa cópia. Só os escritórios provisionados a partir de agora recebem o conjunto de permissões que gravar aqui." Usar `TriangleAlert` (não `ShieldAlert`, que o `RbacTab` já usa para o seu registo neutro). O `<strong>` fica **sem** classe de peso — alinhamento deliberado ao banner do `RbacTab`, declarado no UI-SPEC; não acrescentar `font-semibold`.

**Matriz** (secção 5): copiar a estrutura do `RbacTab` com estas diferenças obrigatórias, cada uma delas uma decisão explícita do UI-SPEC:
  - Colunas **dinâmicas** a partir de `data.moldes` (não o array hardcoded de 4 papéis), porque um molde criado no Plan 06 tem de aparecer aqui.
  - Filtrar do lado do cliente qualquer molde chamado `PLATAFORMA_ADMIN` — defesa em profundidade sobre a garantia do backend, espelhando a prática do `RbacTab` de defender no UI mesmo quando o servidor já bloqueia.
  - **Não** portar a lógica `isAdminRow` do `RbacTab`: nenhum molde é imutável nem pré-marcado-e-trancado. Um molde chamado "ADMIN" é apenas o modelo de administrador de um escritório, não o papel de plataforma.
  - **Não** portar o branch de leitura-apenas nem o badge "Gerido pela Plataforma": esta página só é alcançada por `PLATAFORMA_ADMIN`, logo todos os visitantes podem editar.
  - `colSpan` da linha separadora de módulo passa a `moldes.length + 1` (o `5` hardcoded do `RbacTab` é um bug latente por copiar).
  - Cabeçalho de coluna: `<th key={molde.id} scope="col" className="p-3 font-bold text-center text-slate-700 dark:text-slate-300 text-xs tracking-wider">` contendo `<div className="flex flex-col items-center gap-1">` com `<span>{molde.nome}</span>` e um `Badge` (camada 2 de 3 do aviso) com `variant={molde.escritoriosInstanciados > 0 ? "amber" : "gray"}` e `className="text-[10px] font-semibold"`, mostrando `${n} escritório` / `${n} escritórios` com pluralização correcta para 1, e `"0 escritórios"` quando zero. Esta contagem é **ambiente e sempre visível** — pô-la só dentro do diálogo faria o operador aprender o âmbito depois de já ter decidido mudar algo.
  - A célula de rótulo de cada linha passa de `<td>` a `<th scope="row">` e **tem de levar `text-left` explícito** na className, além do `p-3`: um `<th>` herda `text-align: center` do browser (ao contrário do `<td>`), e os `<div>` internos não declaram alinhamento — sem `text-left` o nome e a descrição da permissão renderizariam centrados, quebrando a exigência de a matriz ler célula-a-célula igual ao `RbacTab`. O peso não precisa de reset: o `font-medium` dos filhos já sobrepõe o bold por omissão do `<th>`.
  - Cada `<input type="checkbox">` leva `aria-label={`${permissao.nome} — ${molde.nome}`}` (ex. "Ver Clientes — ADVOGADO"). O `<label>` envolvente mantém-se pelo alvo de clique maior, mas não tem texto visível, pelo que o `aria-label` no input é o único nome acessível e não pode ser omitido. Estes três pontos (`scope="col"`, `<th scope="row">` com `text-left`, `aria-label` por checkbox) corrigem uma lacuna real de acessibilidade do `RbacTab` e **não** podem ser descartados como "igual ao RbacTab".
  - Sem roving-tabindex nem widget de navegação de grelha — os checkboxes ficam na ordem de tabulação nativa.
  - Sem caixa de pesquisa/filtro (nota de discrição 5 do UI-SPEC: a lista de moldes é pequena e cresce devagar).

**Estado local de edição**: um `Record<number, Set<string>>` (ou estrutura equivalente) inicializado a partir de `data.moldes` e actualizado por um handler de célula que alterna a chave da permissão para aquele molde, exactamente o padrão `localRolePermissions` vs. estado obtido do `RbacTab` (linhas 808-826), sem a guarda `isAdminRow`. Quando a query devolve dados novos, o estado local tem de reflectir o payload fresco sem apagar silenciosamente edições não gravadas — usar o padrão de ajuste de estado em render já estabelecido neste codebase (comparar uma referência do payload aplicado e reinicializar quando mudou), não `useEffect` com `setState`, que o ESLint deste projecto rejeita (`react-hooks/set-state-in-effect`).

Agrupar permissões por módulo preservando a ordem de chegada de `data.permissoes` (o backend já ordena por `ordem`) — `Array.from(new Set(permissoes.map((p) => p.modulo)))`, igual ao `RbacTab`.

Nenhum primitivo novo em `components/ui/`. Nenhum registo shadcn de terceiros, nenhum bloco `Sidebar`, nenhum `NavigationMenu` — proibições permanentes do projecto. A matriz e os checkboxes são markup `<table>`/`<input type="checkbox">` à mão, como o precedente do `RbacTab`.
  </action>
  <acceptance_criteria>
    - `cd web && pnpm install && pnpm exec tsc --noEmit` exit 0 (saída sem `command not found`/`ERR_PNPM`), e `cd web && pnpm lint` exit 0.
    - `ls 'web/src/app/(dashboard)/plataforma/moldes/page.tsx'` existe e tem >= 250 linhas (`wc -l`).
    - `grep -c 'if (!me.isFetched)' 'web/src/app/(dashboard)/plataforma/moldes/page.tsx'` devolve `1`, e o seu número de linha é **menor** que o de `grep -n 'PLATAFORMA_ADMIN")' `-> ocorrência da verificação de papel (guarda de carregamento antes da de papel).
    - `grep -c 'AccessDeniedState' 'web/src/app/(dashboard)/plataforma/moldes/page.tsx'` devolve >= 2 (import + uso), e `grep -n 'Não tem permissão para aceder à gestão de moldes de papel.' ...` devolve exactamente 1 linha.
    - `grep -c 'TriangleAlert' 'web/src/app/(dashboard)/plataforma/moldes/page.tsx'` devolve >= 2 (import + uso), e `grep -c 'ShieldAlert' ...` devolve `0`.
    - `grep -n 'bg-amber-50 dark:bg-amber-500/10' ...` devolve exactamente 1 linha, e o banner **não** está dentro de nenhum bloco condicional de gravação (verificar por leitura: está no topo do `CardContent`, sem `&&` nem ternário à sua volta).
    - `grep -n 'Alterar um molde não atualiza escritórios já criados.' ...` devolve exactamente 1 linha, e `grep -c '<strong className' ...` devolve `0` (o `<strong>` do banner não leva classe de peso).
    - `grep -c 'scope="col"' ...` devolve >= 1 e `grep -c 'scope="row"' ...` devolve >= 1.
    - `grep -n 'th scope="row"' ...` e a mesma linha (ou a sua className) contém `text-left` — verificar com `grep -A2 'scope="row"' ... | grep -c 'text-left'` >= 1.
    - `grep -c 'aria-label={`' ...` devolve >= 1, e a expressão do `aria-label` do checkbox contém tanto o rótulo da permissão como o nome do molde (verificação por leitura da linha devolvida).
    - `grep -c 'colSpan={5}' ...` devolve `0`, e `grep -c 'colSpan={' ...` devolve >= 1 com expressão derivada do número de moldes.
    - `grep -c 'isAdminRow' ...` devolve `0` e `grep -c 'Gerido pela Plataforma' ...` devolve `0`.
    - `grep -c 'escritoriosInstanciados' ...` devolve >= 2 (badge de coluna, e lógica de variante/pluralização).
    - `grep -cE 'variant="amber"|"amber" : "gray"' ...` devolve >= 1.
    - `grep -c 'Criar Molde' ...` devolve `0` (esse botão é do Plan 06; nenhum placeholder desactivado).
    - `git diff --stat web/package.json` vazio e `git diff --name-only web/src/components/ui/` vazio.
  </acceptance_criteria>
  <verify>
    <automated>cd web && pnpm install && pnpm exec tsc --noEmit && pnpm lint</automated>
  </verify>
  <done>A rota `/plataforma/moldes` existe com guarda de página na ordem correcta, banner âmbar permanente, matriz dinâmica com contagem por coluna e as três correcções de acessibilidade (`scope="col"`, `<th scope="row">` com `text-left`, `aria-label` por checkbox), sem lógica de linha imutável nem branch de leitura-apenas.</done>
</task>

<task type="auto">
  <name>Task 2: Fluxo de gravação com confirmação obrigatória e diff apenas dos moldes alterados</name>
  <files>
    web/src/app/(dashboard)/plataforma/moldes/page.tsx
  </files>
  <read_first>
    web/src/app/(dashboard)/plataforma/moldes/page.tsx
    web/src/app/(dashboard)/plataforma/page.tsx
    web/src/app/(dashboard)/settings/page.tsx
    web/src/components/ui/alert-dialog.tsx
    web/src/hooks/use-platform-moldes.ts
    .planning/phases/LEXCV-125-moldes-da-plataforma-e-provisionamento/125-UI-SPEC.md
  </read_first>
  <action>
Acrescentar ao `CardHeader` o botão primário **"Guardar Alterações"** (azul, `bg-blue-600 hover:bg-blue-700 text-white`, ícone `Save` de `lucide-react`), com `disabled` quando não existe diff local face ao payload obtido — um operador não pode abrir um diálogo de confirmação consequente sobre uma gravação que não muda nada. O botão **não** chama a mutação: abre um `AlertDialog`.

Calcular o diff entre o estado local e o último payload obtido, por molde, comparando conjuntos de chaves (não ordem de array). Expor duas coisas derivadas: um booleano "existe diff" (que controla o `disabled`) e a lista dos moldes alterados (que alimenta o diálogo). Só moldes **alterados** entram na lista — quem só tocou no `ADVOGADO` não deve ter de ler sobre o `ASSISTENTE`.

**`AlertDialog`** (camada 3 de 3 do aviso, secção 6 do UI-SPEC):
  - `AlertDialogHeader` com título "Confirmar alterações aos moldes" e descrição "Vai gravar alterações de permissões nos moldes abaixo. Esta gravação não altera nenhum papel já copiado para um escritório existente — só afeta escritórios provisionados a partir de agora."
  - Uma `<ul>` **fora** do `AlertDialogDescription` (que renderiza um `<p>`; uma lista dentro dele seria markup inválido), com uma linha por molde alterado. Para `escritoriosInstanciados > 0`: "{NOME} — {N} escritório(s) já têm uma cópia própria deste molde. Nenhum deles é alterado por esta gravação." Para `=== 0`: "{NOME} — 0 escritórios instanciaram este molde ainda. Esta alteração não tem impacto em nenhum escritório existente." O caso `> 0` renderiza em texto âmbar (com um `TriangleAlert` pequeno ou marcador), o caso `0` em texto slate/neutro — o caso mais tranquilo tem de ler visivelmente mais calmo, não apenas semanticamente.
  - `AlertDialogFooter` com `AlertDialogCancel` outline "Cancelar" e `AlertDialogAction` "Confirmar e Gravar" em **âmbar** (`bg-amber-600 hover:bg-amber-700 text-white`) — âmbar e não azul porque é exactamente o ponto de decisão para o qual o aviso existe; âmbar e não vermelho porque nada é apagado nem perdido, só o modelo mental do operador corre risco de estar errado. Este botão âmbar só pode existir **dentro** do diálogo, nunca no `CardHeader` ao lado do azul que o abre (watch item 1 do UI-SPEC).

Ligar `AlertDialogAction` a `useUpdateMoldes()`, enviando `{ moldes: [...] }` apenas com os moldes alterados (o backend aceita um lote parcial). Em sucesso: `toast.success("Moldes atualizados com sucesso.")`, fechar o diálogo, e deixar a invalidação da query (já feita pelo hook) trazer as contagens `escritoriosInstanciados` frescas. Em falha: **manter o `AlertDialog` aberto** — o `apiFetch` já mostra a mensagem do backend em toast, e fechar obrigaria o operador a refazer o diff; é a convenção já estabelecida no `AlertDialog` de estado de tenant em `plataforma/page.tsx`. Enquanto a mutação está pendente, desactivar ambos os botões do footer e mostrar um label de progresso no de confirmação.

Não alterar nada do que a Task 1 construiu: banner, matriz, atributos de acessibilidade, estados de carregamento/erro/vazio e guarda de página ficam como estão. Não introduzir nenhum primitivo novo em `components/ui/`. Não acrescentar "Criar Molde" (Plan 06).
  </action>
  <acceptance_criteria>
    - `cd web && pnpm exec tsc --noEmit` exit 0 e `cd web && pnpm lint` exit 0.
    - `cd web && pnpm build` exit 0 (a rota `/plataforma/moldes` aparece na listagem de rotas da saída do build).
    - `grep -c 'AlertDialog' 'web/src/app/(dashboard)/plataforma/moldes/page.tsx'` devolve >= 6 (import + Trigger/Content/Header/Footer/Action/Cancel).
    - `grep -n 'Confirmar alterações aos moldes' ...` devolve exactamente 1 linha; `grep -n 'Confirmar e Gravar' ...` devolve exactamente 1 linha; `grep -n 'Guardar Alterações' ...` devolve exactamente 1 linha.
    - `grep -n 'bg-amber-600' ...` devolve exactamente 1 linha, e essa linha está **dentro** do bloco do `AlertDialog` (verificar que o número de linha é posterior ao de `AlertDialogFooter` ou dentro de `AlertDialogAction`); `grep -c 'bg-amber-600' ...` é `1` — o âmbar não aparece em nenhum botão do `CardHeader`.
    - `grep -n 'Moldes atualizados com sucesso.' ...` devolve exactamente 1 linha.
    - `grep -n 'disabled' ...` mostra o botão "Guardar Alterações" desactivado por uma expressão derivada do diff (verificação por leitura da linha).
    - `grep -c 'useUpdateMoldes' ...` devolve >= 2 (import + uso), e o `mutate`/`mutateAsync` é invocado **apenas** dentro do handler do `AlertDialogAction` (verificação por leitura: nenhuma invocação no `onClick` do botão do `CardHeader`).
    - `grep -c 'não altera nenhum papel já copiado para um escritório existente' ...` devolve `1`.
    - `grep -cE 'escritório\(s\) já têm uma cópia própria deste molde|0 escritórios instanciaram este molde ainda' ...` devolve `2` (as duas variantes de linha do diálogo).
    - `grep -c 'Criar Molde' ...` devolve `0`.
    - `git diff --stat web/package.json` vazio e `git diff --name-only web/src/components/ui/` vazio.
  </acceptance_criteria>
  <verify>
    <automated>cd web && pnpm exec tsc --noEmit && pnpm lint && pnpm build</automated>
  </verify>
  <done>Gravar exige passar por um `AlertDialog` que lista apenas os moldes alterados com a sua contagem de escritórios e a garantia de não-propagação, com acção de confirmação âmbar existente só dentro do diálogo, e o botão que o abre está desactivado quando não há diff.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → `/api/v1/platform/moldes` | o ecrã é o único cliente desta superfície cross-tenant; o gate real é o `@PreAuthorize` de classe do backend |
| modelo mental do operador → dados de todos os escritórios | a fronteira "perigosa" desta fase não é técnica: é a suposição por omissão de que editar um molde propaga. O aviso em três camadas é a mitigação |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-125-31 | Elevation of Privilege | guarda de página de `/plataforma/moldes` | mitigate | `!me.isFetched` resolve antes da verificação de papel (WR-03, Phase 120), impedindo que o conteúdo e o pedido à API rendam durante a janela anterior ao primeiro resolve; a autoridade real permanece no backend (provado no Plan 03) |
| T-125-32 | Elevation of Privilege | `PLATAFORMA_ADMIN` como coluna da matriz | mitigate | Filtro por nome literal do lado do cliente, defesa em profundidade sobre a exclusão do backend; gate de grep confirma zero lógica `isAdminRow` importada |
| T-125-33 | Tampering | gravação acidental com modelo mental errado | mitigate | Três camadas: banner permanente (não condicional), badge de contagem por coluna sempre visível, `AlertDialog` obrigatório listando só os moldes alterados. O botão que o abre está desactivado sem diff |
| T-125-34 | Tampering | perda silenciosa de edições locais | mitigate | Reinicialização do estado local por comparação de referência do payload aplicado (padrão de ajuste em render já estabelecido), nunca `setState` em `useEffect`; falha na mutação mantém o diálogo aberto e o diff intacto |
| T-125-35 | Information Disclosure | contagem de escritórios exibida | accept | `escritoriosInstanciados` é um agregado sem identificação de escritório, exibido exclusivamente a `PLATAFORMA_ADMIN` atrás do gate mais restritivo do sistema |
| T-125-36 | Repudiation | quem confirmou a gravação | accept | Sem trilha de auditoria nesta fase; âmbito da Phase 128 |
| T-125-SC | Tampering | instalação de pacotes / primitivos de terceiros | mitigate | Zero pacotes novos e zero primitivos novos: `git diff --stat web/package.json` e `git diff --name-only web/src/components/ui/` vazios. Sem registos shadcn de terceiros, sem bloco `Sidebar`, sem `NavigationMenu` |
</threat_model>

<verification>
- `cd web && pnpm install && pnpm exec tsc --noEmit && pnpm lint && pnpm build` — todos exit 0, sem `command not found`/`ERR_PNPM`.
- `git diff --stat web/package.json` vazio; `git diff --name-only web/src/components/ui/` vazio.
- `git diff --name-only` limitado ao ficheiro declarado em `files_modified`.
</verification>

<success_criteria>
- `/plataforma/moldes` existe, protegida, e mostra a matriz permissão × molde com colunas dinâmicas.
- O aviso de não-propagação existe nas três camadas exigidas pelo UI-SPEC.
- A matriz cumpre o contrato de acessibilidade (`scope="col"`, `<th scope="row">` com `text-left`, `aria-label` por checkbox).
- Gravar passa obrigatoriamente por um `AlertDialog` com diff apenas dos moldes alterados e acção de confirmação âmbar.
- Zero dependências novas, zero primitivos novos.
</success_criteria>

<output>
Create `.planning/phases/LEXCV-125-moldes-da-plataforma-e-provisionamento/125-05-SUMMARY.md` when done.

Registar no SUMMARY que o botão "Criar Molde" e o painel de criação são deliberadamente do Plan 06, e que nenhum placeholder foi deixado no seu lugar.
</output>
