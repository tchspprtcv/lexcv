---
phase: 125-moldes-da-plataforma-e-provisionamento
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/main/java/com/lexcv/models/Role.java
  - backend/src/main/java/com/lexcv/models/TenantRole.java
  - backend/src/main/java/com/lexcv/repositories/RoleRepository.java
  - backend/src/main/java/com/lexcv/repositories/TenantRoleRepository.java
  - backend/src/main/java/com/lexcv/seed/DatabaseSeeder.java
  - backend/src/test/java/com/lexcv/seed/DatabaseSeederInstanciabilidadeMoldesTest.java
  - backend/migrations/126-add-tenant-role-tables.sql
  - backend/migrations/README.md
autonomous: true
requirements: [MOLD-01, MOLD-03]

must_haves:
  truths:
    - "Cada papel global declara explicitamente se é um molde, através de Role.instanciavel"
    - "PLATAFORMA_ADMIN nunca é instanciável (instanciavel = false), em cada arranque, sem depender de disciplina de código no chamador"
    - "Existe uma tabela t_tenant_role onde um papel é único por (tenant_id, nome), permitindo que dois escritórios tenham ambos um papel com o mesmo nome"
    - "Um papel instanciado guarda a proveniência num molde_id que não é uma associação JPA navegável"
    - "A leitura de moldes é fechada ao nível de SQL por uma query derivada (findAllByInstanciavelTrue), não por filtro Java pós-leitura"
    - "Existe um script manual de migração numerado 126 e catalogado no README, e nenhum número de migração foi reutilizado"
  artifacts:
    - path: "backend/src/main/java/com/lexcv/models/TenantRole.java"
      provides: "Entidade do papel de escritório (snapshot de um molde), única por (tenant_id, nome)"
      contains: "uniqueConstraints"
    - path: "backend/src/main/java/com/lexcv/repositories/TenantRoleRepository.java"
      provides: "findByTenantId, findByTenantIdAndNome, countByMoldeId"
      contains: "countByMoldeId"
    - path: "backend/src/main/java/com/lexcv/models/Role.java"
      provides: "Coluna instanciavel com default fechado"
      contains: "instanciavel"
    - path: "backend/migrations/126-add-tenant-role-tables.sql"
      provides: "Script manual idempotente que cria t_tenant_role, t_tenant_role_permission e t_role.instanciavel"
      contains: "IF NOT EXISTS"
    - path: "backend/src/test/java/com/lexcv/seed/DatabaseSeederInstanciabilidadeMoldesTest.java"
      provides: "Prova Mockito de que o seeder converge instanciavel nos 5 papéis sem apagar linhas"
  key_links:
    - from: "backend/src/main/java/com/lexcv/seed/DatabaseSeeder.java"
      to: "Role.instanciavel"
      via: "upsertRolePermissions com terceiro argumento booleano"
      pattern: "upsertRolePermissions\\(\"PLATAFORMA_ADMIN\", Collections.emptyList\\(\\), false\\)"
    - from: "backend/src/main/java/com/lexcv/repositories/RoleRepository.java"
      to: "t_role.instanciavel"
      via: "query derivada"
      pattern: "findAllByInstanciavelTrue"
---

<objective>
Criar o esquema de dados de que todo o marco v2.17 depende: a marca de instanciabilidade nos papéis globais (quais são moldes), e as tabelas `t_tenant_role` / `t_tenant_role_permission` onde vivem as cópias por escritório.

Purpose: sem esta base, nada pode ser instanciado nem editado. É também aqui que a guarda mais importante da fase nasce — `PLATAFORMA_ADMIN` fica marcado como NÃO instanciável, com default fechado (false) para que um papel novo nunca se torne molde por esquecimento.
Output: 2 entidades/colunas novas, 2 repositórios, seeder convergente, script de migração 126 catalogado, 1 classe de teste Mockito.

**Nada nesta fase altera resolução de autoridade.** `t_user_role`, `JwtAuthenticationFilter`, `UserPrincipal` e `PUT /admin/rbac` não são tocados. As linhas de `t_tenant_role` nascem correctas e DORMENTES.
</objective>

<execution_context>
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/LEXCV-125-moldes-da-plataforma-e-provisionamento/125-CONTEXT.md
@.planning/phases/LEXCV-125-moldes-da-plataforma-e-provisionamento/125-PATTERNS.md
@.planning/phases/LEXCV-124-cat-logo-de-permiss-es-em-base-de-dados/124-01-SUMMARY.md
@CLAUDE.md
</context>

<build_environment>
O `java`/`mvn` por omissão no PATH resolvem para JDK 26, que NÃO compila este projeto (Lombok morre com `ExceptionInInitializerError: TypeTag :: UNKNOWN`). Todo o comando Maven deste plano corre a partir de `backend/` com `JAVA_HOME="/c/Program Files/Java/jdk-23"` prefixado. Sem isso, qualquer critério de aceitação Maven é inválido.
</build_environment>

<interfaces>
<!-- Contratos que o executor recebe já extraídos. Não é necessário explorar o codebase. -->

Estado actual de `backend/src/main/java/com/lexcv/models/Role.java` (ficheiro completo, 32 linhas): `@Entity @Table(name = "t_role")`, `@Getter @Setter @NoArgsConstructor @AllArgsConstructor @Builder @EqualsAndHashCode(of = "nome")`; campos `Integer id` (`GenerationType.IDENTITY`), `String nome` (`@Column(nullable = false, unique = true)`), `Set<Permission> permissions` (`@ManyToMany(fetch = FetchType.EAGER)` + `@JoinTable(name = "t_role_permission", joinColumns = @JoinColumn(name = "role_id"), inverseJoinColumns = @JoinColumn(name = "permission_id"))` + `@Builder.Default new HashSet<>()`).

Idioma de coluna-bandeira com default seguro, estabelecido pela Phase 124 em `Permission.java:61-63` — copiar a forma exacta (`@Column(name = "...", nullable = false, columnDefinition = "boolean not null default <valor>")` + `@Builder.Default`). O `columnDefinition` garante que `ddl-auto: update` cria a coluna já com o default certo; o `@Builder.Default` é obrigatório, senão qualquer `Role.builder()...build()` que não fixe o campo obtém `null`.

Constraint única por tenant, idioma de `Cliente.java:12-19`: `@Table(name = "...", uniqueConstraints = @UniqueConstraint(columnNames = {"tenant_id", "nome"}))`.
Coluna de tenant, idioma de `User.java:23-24` / `Cliente.java:30-31`: `@Column(name = "tenant_id", nullable = false) private UUID tenantId;`.
Estratégia de id para entidades criadas em runtime (`Cliente.id`, `User.id`): `@GeneratedValue(strategy = GenerationType.UUID)` — NÃO `IDENTITY` (esse é o de `Role`/`Permission`, entidades de catálogo).

`RoleRepository` hoje: `public interface RoleRepository extends JpaRepository<Role, Integer> { Optional<Role> findByNome(String nome); }`.
`ClienteRepository` (analogia de finders por tenant): `List<Cliente> findByTenantId(UUID tenantId);` / `Optional<Cliente> findByTenantIdAndDocumentoNumero(UUID, String);`.
Precedente de exclusão ao nível de SQL (Phase 124): `PermissionRepository.findAllByReservadaPlataformaFalse()`.

`DatabaseSeeder.upsertRolePermissions` actual (linhas 482-490), o único ponto onde papéis são criados/actualizados:
`Role role = roleRepository.findByNome(roleName).orElseGet(() -> roleRepository.save(Role.builder().nome(roleName).build())); boolean changed = role.getPermissions().addAll(permissions); if (changed) { roleRepository.save(role); }`
Chamadores (dentro de `seedRbac()`): linha 436 `"ADMIN"`, 438 `"ASSISTENTE"`, 447 `"TECNICO"`, 457 `"ADVOGADO"`, 479 `"PLATAFORMA_ADMIN"` com `Collections.emptyList()`.

Andaime do teste de seeder (`DatabaseSeederCatalogoPermissoesTest`): `@ExtendWith(MockitoExtension.class)`, `@Mock` por repositório, `@InjectMocks DatabaseSeeder`, `ReflectionTestUtils` para pôr `seedEnabled = false` (cenário mais barato: `run()` executa `seedRbac()` + `seedTenantPlataforma()` e regressa antes de tocar em qualquer outro repositório), `ArgumentCaptor` para inspeccionar o que foi persistido, `lenient()` onde necessário.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Marca de instanciabilidade em Role e entidade/repositório do papel de escritório</name>
  <files>
    backend/src/main/java/com/lexcv/models/Role.java
    backend/src/main/java/com/lexcv/models/TenantRole.java
    backend/src/main/java/com/lexcv/repositories/RoleRepository.java
    backend/src/main/java/com/lexcv/repositories/TenantRoleRepository.java
  </files>
  <read_first>
    backend/src/main/java/com/lexcv/models/Role.java
    backend/src/main/java/com/lexcv/models/Permission.java
    backend/src/main/java/com/lexcv/models/Cliente.java
    backend/src/main/java/com/lexcv/models/User.java
    backend/src/main/java/com/lexcv/repositories/RoleRepository.java
    backend/src/main/java/com/lexcv/repositories/ClienteRepository.java
    backend/src/main/java/com/lexcv/repositories/PermissionRepository.java
    .planning/phases/LEXCV-125-moldes-da-plataforma-e-provisionamento/125-PATTERNS.md
  </read_first>
  <action>
Acrescentar a `Role` uma coluna `instanciavel` (`Boolean`, `@Column(name = "instanciavel", nullable = false, columnDefinition = "boolean not null default false")` + `@Builder.Default private Boolean instanciavel = false;`), copiando literalmente o idioma de `Permission.reservadaPlataforma`. O default é **false — falha fechada**: um papel só se torna molde se alguém o declarar explicitamente, e o `DatabaseSeeder` (Task 2) é quem declara. Documentar essa escolha num doc-comment sobre o campo, no mesmo registo do doc-comment de `Permission.reservadaPlataforma`, dizendo porquê o default é fechado (um papel novo nunca deve ser instanciado em todos os escritórios por esquecimento) e que `PLATAFORMA_ADMIN` é o caso que este default protege. Não alterar mais nada em `Role` — `nome`, `id`, `permissions` e o `@EqualsAndHashCode(of = "nome")` ficam byte-a-byte iguais.

Acrescentar a `RoleRepository` a query derivada `List<Role> findAllByInstanciavelTrue();`, com um comentário de uma linha a dizer que esta é a única porta de leitura de moldes e que fecha a exclusão de não-instanciáveis ao nível de SQL (mesmo padrão que `PermissionRepository.findAllByReservadaPlataformaFalse()`, Phase 124) — nenhum chamador futuro pode esquecer-se do filtro. Não remover nem alterar `findByNome`.

Criar `TenantRole` como entidade nova: `@Entity`, `@Table(name = "t_tenant_role", uniqueConstraints = @UniqueConstraint(columnNames = {"tenant_id", "nome"}))`, Lombok `@Getter @Setter @NoArgsConstructor @AllArgsConstructor @Builder`. Campos: `UUID id` com `@GeneratedValue(strategy = GenerationType.UUID)`; `UUID tenantId` em `@Column(name = "tenant_id", nullable = false)`; `String nome` em `@Column(nullable = false)`; `Integer moldeId` em `@Column(name = "molde_id")` (nullable); `Boolean sistema` em `@Column(nullable = false, columnDefinition = "boolean not null default false")` com `@Builder.Default = false`; `Set<Permission> permissions` com `@ManyToMany(fetch = FetchType.EAGER)` + `@JoinTable(name = "t_tenant_role_permission", joinColumns = @JoinColumn(name = "tenant_role_id"), inverseJoinColumns = @JoinColumn(name = "permission_id"))` + `@Builder.Default new HashSet<>()`.

`moldeId` é **deliberadamente um `Integer` cru e não um `@ManyToOne Role`** (D: "Snapshot, not live reference"). Escrever isso no doc-comment do campo, explicando as duas consequências: (a) editar o molde não pode chegar a esta linha por nenhum caminho de navegação JPA, e (b) não existe FK na base de dados, para que um molde possa vir a ser apagado sem tornar linhas históricas irremovíveis. Não acrescentar `@ManyToOne`, `@JoinColumn(name = "molde_id")` sobre um `Role`, nem `cascade` de qualquer espécie. Não acrescentar `@EqualsAndHashCode` (a entidade é identificada por id gerado).

Criar `TenantRoleRepository extends JpaRepository<TenantRole, UUID>` com exactamente três métodos derivados: `List<TenantRole> findByTenantId(UUID tenantId);`, `Optional<TenantRole> findByTenantIdAndNome(UUID tenantId, String nome);` e `long countByMoldeId(Integer moldeId);`. O `countByMoldeId` é o número que a consola de moldes precisa para o aviso de não-propagação (quantos escritórios já instanciaram aquele molde) — anotar isso num comentário de uma linha.
  </action>
  <acceptance_criteria>
    - `cd backend && JAVA_HOME="/c/Program Files/Java/jdk-23" mvn -DskipTests compile` termina com exit 0 e sem `BUILD FAILURE`.
    - `grep -c 'instanciavel' backend/src/main/java/com/lexcv/models/Role.java` devolve um valor >= 2 (coluna + campo), e `grep -n 'columnDefinition = "boolean not null default false"' backend/src/main/java/com/lexcv/models/Role.java` devolve exactamente 1 linha.
    - `grep -n 'findAllByInstanciavelTrue' backend/src/main/java/com/lexcv/repositories/RoleRepository.java` devolve exactamente 1 linha.
    - `grep -n 'uniqueConstraints = @UniqueConstraint(columnNames = {"tenant_id", "nome"})' backend/src/main/java/com/lexcv/models/TenantRole.java` devolve exactamente 1 linha.
    - `grep -n 't_tenant_role_permission' backend/src/main/java/com/lexcv/models/TenantRole.java` devolve exactamente 1 linha.
    - `grep -v '^\s*\*' backend/src/main/java/com/lexcv/models/TenantRole.java | grep -v '^\s*//' | grep -cE '@ManyToOne|@OneToOne|cascade'` devolve `0` — `moldeId` não é uma associação navegável.
    - `grep -cE 'findByTenantId\(|findByTenantIdAndNome\(|countByMoldeId\(' backend/src/main/java/com/lexcv/repositories/TenantRoleRepository.java` devolve `3`.
    - `git diff --name-only` NÃO inclui `backend/src/main/java/com/lexcv/config/JwtAuthenticationFilter.java`, `backend/src/main/java/com/lexcv/config/UserPrincipal.java`, `backend/src/main/java/com/lexcv/models/User.java` nem `backend/src/main/java/com/lexcv/controllers/AdminController.java`.
  </acceptance_criteria>
  <verify>
    <automated>cd backend && JAVA_HOME="/c/Program Files/Java/jdk-23" mvn -DskipTests compile</automated>
  </verify>
  <done>`Role` declara instanciabilidade com default fechado, `RoleRepository` fecha a leitura de moldes por SQL, `TenantRole`/`TenantRoleRepository` existem com unicidade por tenant e `molde_id` como coluna histórica não navegável, e o backend compila.</done>
</task>

<task type="auto">
  <name>Task 2: DatabaseSeeder declara quais papéis são moldes, convergindo em cada arranque</name>
  <files>
    backend/src/main/java/com/lexcv/seed/DatabaseSeeder.java
    backend/src/test/java/com/lexcv/seed/DatabaseSeederInstanciabilidadeMoldesTest.java
  </files>
  <read_first>
    backend/src/main/java/com/lexcv/seed/DatabaseSeeder.java
    backend/src/test/java/com/lexcv/seed/DatabaseSeederCatalogoPermissoesTest.java
    backend/src/main/java/com/lexcv/models/Role.java
    .planning/phases/LEXCV-124-cat-logo-de-permiss-es-em-base-de-dados/124-01-SUMMARY.md
  </read_first>
  <action>
Alterar a assinatura de `DatabaseSeeder.upsertRolePermissions` para `private void upsertRolePermissions(String roleName, Collection<Permission> permissions, boolean instanciavel)`. Dentro do método: manter o `findByNome(...).orElseGet(...)` existente, mas com o builder a fixar já a bandeira (`Role.builder().nome(roleName).instanciavel(instanciavel).build()`); depois do `boolean changed = role.getPermissions().addAll(permissions);`, calcular se a bandeira divergiu do valor declarado (comparar com `Boolean.valueOf(instanciavel)`, tolerando `getInstanciavel() == null` numa base de dados onde a coluna acabou de ser criada) e, se sim, chamar `role.setInstanciavel(instanciavel)`; gravar quando `changed` OU a bandeira divergiu. Isto faz o seeder **convergir** em cada arranque, exactamente como `seedRbac()` já põe `reservadaPlataforma = false` nas 20 entradas do catálogo em cada arranque (precedente da Phase 124) — é por isso que o script de migração da Task 3 não precisa de nenhum `UPDATE` de backfill.

Actualizar os 5 chamadores dentro de `seedRbac()`: `"ADMIN"`, `"ASSISTENTE"`, `"TECNICO"` e `"ADVOGADO"` passam `true`; `"PLATAFORMA_ADMIN"` passa `false`. Deixar um comentário no chamador de `"PLATAFORMA_ADMIN"` a dizer que este `false` é a guarda de nível de dados que impede o papel de plataforma de se tornar instanciável, editável como molde ou atribuível a partir de superfície de escritório — a continuação das guardas das Phases 119 e 121, não uma reinvenção. Não alterar mais nada em `seedRbac()`: o catálogo de 20 permissões, a ordem de chamadas, `seedTenantPlataforma()` e `seedUtilizadorPlataforma()` ficam intactos.

Não tocar em `seedTenantPlataforma()`: a tenant reservada "ALCv" continua a nascer sem nenhum papel instanciado, porque não passa por `provisionTenant` (D: Phase 119 — opera exclusivamente por `PLATAFORMA_ADMIN`, fora do RBAC scoped por tenant). Não injectar `TenantRoleRepository` no `DatabaseSeeder`.

Criar `DatabaseSeederInstanciabilidadeMoldesTest` seguindo o andaime de `DatabaseSeederCatalogoPermissoesTest` (mesmos mocks, `@InjectMocks`, `ReflectionTestUtils` para `seedEnabled = false`), com um doc-comment de classe a citar a Phase 125 e MOLD-01. Casos:
  1. Num primeiro arranque (nenhum papel existe), os 5 papéis são gravados e a captura por `ArgumentCaptor<Role>` mostra `instanciavel = true` para ADMIN/ASSISTENTE/TECNICO/ADVOGADO e `false` para PLATAFORMA_ADMIN.
  2. Numa base de dados já existente onde `PLATAFORMA_ADMIN` foi carregado com `instanciavel = true` (cenário de deriva), o seeder grava-o de novo com `false` — convergência, não só criação.
  3. Numa base de dados onde a coluna acabou de ser criada e `getInstanciavel()` devolve `null` nos 4 papéis de escritório, o seeder grava-os com `true` (não deixa `null` numa coluna `NOT NULL`).
  4. O seeder nunca invoca nenhuma operação de remoção sobre `roleRepository` (`verify(roleRepository, never()).delete(any())` e `deleteAll()`), e o `id`/`nome` de um papel pré-existente sobrevive intacto.
  5. Nenhum `TenantRole` é criado pelo seeder: não existe nenhuma interacção com um repositório de papéis de escritório (provado estruturalmente — o `DatabaseSeeder` não o declara como colaborador).
  </action>
  <acceptance_criteria>
    - `cd backend && JAVA_HOME="/c/Program Files/Java/jdk-23" mvn test -Dtest=DatabaseSeederInstanciabilidadeMoldesTest` termina com exit 0, `Failures: 0, Errors: 0` e pelo menos 5 testes executados.
    - `cd backend && JAVA_HOME="/c/Program Files/Java/jdk-23" mvn test -Dtest=DatabaseSeederCatalogoPermissoesTest,DatabaseSeederPlataformaAdminTest` termina com exit 0 (não-regressão dos testes de seeder existentes; se `DatabaseSeederPlataformaAdminTest` não existir com esse nome, correr apenas o primeiro e registar a divergência no SUMMARY).
    - `grep -n 'upsertRolePermissions("PLATAFORMA_ADMIN", Collections.emptyList(), false)' backend/src/main/java/com/lexcv/seed/DatabaseSeeder.java` devolve exactamente 1 linha.
    - `grep -vE '^\s*(//|\*|/\*)' backend/src/main/java/com/lexcv/seed/DatabaseSeeder.java | grep -c 'upsertRolePermissions(' ` devolve `6` (1 definição + 5 chamadas).
    - `grep -vE '^\s*(//|\*|/\*)' backend/src/main/java/com/lexcv/seed/DatabaseSeeder.java | grep -c 'TenantRole'` devolve `0` — o seeder não instancia papéis de escritório.
    - `cd backend && JAVA_HOME="/c/Program Files/Java/jdk-23" mvn test` termina com exit 0 e sem `BUILD FAILURE` (suite completa verde).
  </acceptance_criteria>
  <verify>
    <automated>cd backend && JAVA_HOME="/c/Program Files/Java/jdk-23" mvn test -Dtest=DatabaseSeederInstanciabilidadeMoldesTest</automated>
  </verify>
  <done>Cada arranque deixa os 4 papéis de escritório marcados como moldes e `PLATAFORMA_ADMIN` marcado como não-molde, provado por 5 casos Mockito, e nenhuma linha de papel é apagada no processo.</done>
</task>

<task type="auto">
  <name>Task 3: Script de migração 126 e inventário do README</name>
  <files>
    backend/migrations/126-add-tenant-role-tables.sql
    backend/migrations/README.md
  </files>
  <read_first>
    backend/migrations/README.md
    backend/migrations/124-add-permission-catalogo-columns.sql
    backend/src/main/java/com/lexcv/models/TenantRole.java
    backend/src/main/java/com/lexcv/models/Permission.java
  </read_first>
  <action>
**O número é 126, não 125.** `backend/migrations/125-convert-tenant-logo-data-url-to-text.sql` já existe de trabalho anterior não relacionado, e a Phase 124 acabou de ocupar o `124`. A sequência existente é `74, 81, 82, 86, 88, 91, 93, 96, 111, 117, 120, 124, 125` — o próximo inteiro livre é 126. Confirmar com `ls backend/migrations` antes de criar o ficheiro; se `126-*.sql` já existir, parar e reportar em vez de sobrescrever.

Criar `backend/migrations/126-add-tenant-role-tables.sql` com o cabeçalho na convenção de `124-add-permission-catalogo-columns.sql` (o quê, porquê, o que rebenta sem ele, e se é idempotente), citando "Phase 125 (MOLD-01/MOLD-03)". O corpo, por esta ordem:
  1. `ALTER TABLE t_role ADD COLUMN IF NOT EXISTS instanciavel BOOLEAN NOT NULL DEFAULT FALSE;`
  2. `CREATE TABLE IF NOT EXISTS t_tenant_role` com `id UUID PRIMARY KEY`, `tenant_id UUID NOT NULL`, `nome VARCHAR(255) NOT NULL`, `molde_id INTEGER`, `sistema BOOLEAN NOT NULL DEFAULT FALSE`, e `CONSTRAINT uk_tenant_role_tenant_nome UNIQUE (tenant_id, nome)`.
  3. `CREATE TABLE IF NOT EXISTS t_tenant_role_permission` com `tenant_role_id UUID NOT NULL REFERENCES t_tenant_role(id)`, `permission_id INTEGER NOT NULL REFERENCES t_permission(id)` e `PRIMARY KEY (tenant_role_id, permission_id)`.
  4. `CREATE INDEX IF NOT EXISTS idx_tenant_role_molde ON t_tenant_role(molde_id);` — suporta o `countByMoldeId` que a consola de moldes chama por cada molde listado.

No cabeçalho, declarar explicitamente três coisas: (a) que **não há `FOREIGN KEY` de `molde_id` para `t_role`**, deliberadamente, porque a proveniência é histórica e uma FK tornaria um molde irremovível no futuro; (b) que **não há nenhum `UPDATE` de backfill** de `instanciavel` — `DatabaseSeeder.seedRbac()` converge os 5 papéis no arranque seguinte, antes de a app aceitar pedidos, exactamente como o script `124` já faz para `rotulo`/`descricao`/`modulo`/`ordem`; (c) que todas as instruções usam `IF NOT EXISTS`, pelo que o script é seguro a correr duas vezes.

Actualizar `backend/migrations/README.md` em **quatro** sítios, no mesmo commit (a regra da secção "Adding a new migration": o inventário vive aqui e em mais nenhum lado):
  1. **Path A — fresh install** (a lista de "skip"): acrescentar um bullet próprio para `126`, dizendo que é redundante numa base de dados nova porque `ddl-auto` já criou as duas tabelas e a coluna, e que — ao contrário dos scripts do bullet acima — não dá erro de objecto duplicado, simplesmente não muda nada (todas as instruções são `IF NOT EXISTS`).
  2. **Path B — existing database**: acrescentar a linha **15**, estritamente depois da linha 14 (`125-convert-tenant-logo-data-url-to-text.sql`) — ordem numérica ascendente por nome de ficheiro, não por número de fase do roadmap. Colunas: Order `15`, File `126-add-tenant-role-tables.sql`, What it does (cria `t_tenant_role` + `t_tenant_role_permission` e acrescenta `t_role.instanciavel`; Phase 125, moldes e papéis de escritório; sem backfill — o seeder converge no arranque seguinte), Re-runnable? `**Yes**` — todas as instruções usam `IF NOT EXISTS`.
  3. **Re-run safety**: a frase `Only **4 of the 14** scripts tolerate being run twice:` passa a `Only **5 of the 15** scripts tolerate being run twice:`, e a tabela ganha a linha `126-add-tenant-role-tables` com a razão ("every statement uses `IF NOT EXISTS`"). A frase seguinte ("The other **10** must not be re-run. Nine of them fail loudly…") mantém-se numericamente correcta (15 − 5 = 10) e **não deve ser alterada** — confirmar a aritmética antes de a deixar como está.
  4. **Known execution status**: a frase `**6 scripts are outstanding**` passa a `**7 scripts are outstanding**`, e a tabela ganha a linha `126-add-tenant-role-tables.sql` com status `Pending — new in this phase.`

Não alterar o banner de STOP nem nada relativo ao script `125` — esse aviso é sobre um script destrutivo diferente e não tem relação com este.
  </action>
  <acceptance_criteria>
    - `ls backend/migrations/126-add-tenant-role-tables.sql` existe, e `ls backend/migrations | sed 's/^\([0-9]*\)-.*/\1/' | sort -n | uniq -d` não devolve nada (nenhum número duplicado).
    - `grep -c 'IF NOT EXISTS' backend/migrations/126-add-tenant-role-tables.sql` devolve `4`.
    - `grep -cE 'FOREIGN KEY|REFERENCES t_role' backend/migrations/126-add-tenant-role-tables.sql` devolve `0` (nenhuma FK de `molde_id` para `t_role`).
    - `grep -cE '^\s*UPDATE ' backend/migrations/126-add-tenant-role-tables.sql` devolve `0` (sem backfill).
    - `grep -n 'uk_tenant_role_tenant_nome' backend/migrations/126-add-tenant-role-tables.sql` devolve exactamente 1 linha.
    - `grep -c '126-add-tenant-role-tables' backend/migrations/README.md` devolve `4` (Path A, Path B, re-run safety, known execution status).
    - `grep -n 'Only \*\*5 of the 15\*\* scripts' backend/migrations/README.md` devolve 1 linha e `grep -c 'Only \*\*4 of the 14\*\*' backend/migrations/README.md` devolve `0`.
    - `grep -n '\*\*7 scripts are outstanding\*\*' backend/migrations/README.md` devolve 1 linha e `grep -c '\*\*6 scripts are outstanding\*\*' backend/migrations/README.md` devolve `0`.
    - A linha do `126` na tabela Path B aparece DEPOIS da linha do `125` no ficheiro: `grep -n '125-convert-tenant-logo-data-url-to-text.sql | Converts' backend/migrations/README.md` e a linha `| 15 | \`126-add-tenant-role-tables.sql\`` confirmam número de linha crescente.
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'test "$(grep -c "126-add-tenant-role-tables" backend/migrations/README.md)" = "4" && test "$(grep -c "IF NOT EXISTS" backend/migrations/126-add-tenant-role-tables.sql)" = "4" && echo OK'</automated>
  </verify>
  <done>Existe um script manual idempotente numerado 126 (não 125), sem FK de proveniência e sem backfill, e as quatro tabelas/contagens do README foram actualizadas sem deixar nenhum número obsoleto.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| arranque da aplicação → `t_role` | `DatabaseSeeder` decide, sem pedido HTTP nenhum, quais papéis são instanciáveis em todos os escritórios futuros |
| esquema de base de dados → autoridade futura | `t_tenant_role`/`t_tenant_role_permission` são as tabelas que as Phases 126/127 vão usar para decidir o que cada utilizador pode fazer; um default errado aqui propaga-se a todos os escritórios |
| operador de produção → base de dados | o script de migração é executado à mão, sem runner, contra dados reais |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-125-01 | Elevation of Privilege | `Role.instanciavel` (default da coluna) | mitigate | Default **false** em `columnDefinition` + `@Builder.Default`; um papel novo (incluindo `PLATAFORMA_ADMIN`) nunca se torna molde por omissão — só por declaração explícita no seeder (Task 1, Task 2) |
| T-125-02 | Elevation of Privilege | `DatabaseSeeder.seedRbac()` → `PLATAFORMA_ADMIN` | mitigate | Chamador explícito com `false` + teste de convergência que reprova se uma base de dados derivada o tiver a `true` (Task 2, casos 1 e 2) |
| T-125-03 | Tampering | `TenantRole.moldeId` | mitigate | Coluna `Integer` crua, sem `@ManyToOne`/`cascade` e sem FK no SQL — não existe caminho de navegação por onde editar um molde possa alcançar uma linha já instanciada (Task 1, Task 3; gate de grep a `0` ocorrências de `@ManyToOne`) |
| T-125-04 | Tampering | unicidade de `t_tenant_role` | mitigate | `UNIQUE (tenant_id, nome)` na entidade E no script SQL — impede colisão de nomes dentro de um escritório e permite deliberadamente o mesmo nome em escritórios diferentes (isolamento por tenant, convenção `(tenant_id, …)` do projecto) |
| T-125-05 | Elevation of Privilege | resolução de autoridade existente | mitigate | Nenhum ficheiro de autenticação/autorização é tocado: gate de aceitação da Task 1 verifica que `git diff --name-only` não inclui `JwtAuthenticationFilter`, `UserPrincipal`, `User` nem `AdminController`. As linhas novas nascem DORMENTES |
| T-125-06 | Information Disclosure | leitura de moldes | mitigate | Única porta de leitura é `findAllByInstanciavelTrue()` (exclusão por SQL, não por filtro Java) — padrão herdado do `findAllByReservadaPlataformaFalse()` da Phase 124 |
| T-125-07 | Denial of Service | script de migração re-executado | mitigate | Todas as 4 instruções usam `IF NOT EXISTS`; o script entra no grupo "safe to re-run" do README e a contagem é actualizada para 5 de 15 |
| T-125-08 | Repudiation | quem mudou a instanciabilidade de um papel | accept | Não há trilha de auditoria nesta fase; a marca é definida por código semeado, não por API. Auditoria é o âmbito da Phase 128 |
| T-125-SC | Tampering | instalação de pacotes (npm/pip/cargo/maven) | mitigate | Esta fase instala **zero** pacotes novos. Critério: `git diff --stat backend/pom.xml web/package.json` vazio nos 3 tasks. Nenhum pacote `[ASSUMED]`/`[SUS]` existe porque nenhum pacote é adicionado |
</threat_model>

<verification>
- `cd backend && JAVA_HOME="/c/Program Files/Java/jdk-23" mvn test` verde (suite completa).
- `cd backend && JAVA_HOME="/c/Program Files/Java/jdk-23" mvn spotbugs:check` sem novos achados.
- `git diff --stat backend/pom.xml` vazio (nenhuma dependência nova).
- `git diff --name-only` limitado aos 8 ficheiros declarados em `files_modified`.
</verification>

<success_criteria>
- `t_role` tem uma coluna de instanciabilidade com default fechado, e o seeder converge os 5 papéis em cada arranque (4 moldes + `PLATAFORMA_ADMIN` não-molde).
- `t_tenant_role` e `t_tenant_role_permission` existem como entidades JPA, com unicidade `(tenant_id, nome)` e `molde_id` como coluna histórica não navegável.
- `TenantRoleRepository` expõe `findByTenantId`, `findByTenantIdAndNome` e `countByMoldeId`.
- Existe `backend/migrations/126-add-tenant-role-tables.sql`, idempotente, catalogado nas 4 localizações do README, sem contagens obsoletas.
- Zero alterações em resolução de autoridade.
</success_criteria>

<output>
Create `.planning/phases/LEXCV-125-moldes-da-plataforma-e-provisionamento/125-01-SUMMARY.md` when done.

Registar no SUMMARY, em "User Setup Required", que `backend/migrations/126-add-tenant-role-tables.sql` é um script de execução manual pendente para produção (não há migration runner neste repositório) — a juntar aos já pendentes `74`, `117`, `120`, `124`.
</output>
