---
phase: 125-moldes-da-plataforma-e-provisionamento
plan: 03
type: execute
wave: 2
depends_on: [125-01]
files_modified:
  - backend/src/main/java/com/lexcv/dtos/MoldesConsolaResponse.java
  - backend/src/main/java/com/lexcv/dtos/MoldesUpdateRequest.java
  - backend/src/main/java/com/lexcv/dtos/MoldeCreateRequest.java
  - backend/src/main/java/com/lexcv/dtos/MoldeProvisionResponse.java
  - backend/src/main/java/com/lexcv/controllers/PlatformAdminController.java
  - backend/src/test/java/com/lexcv/controllers/PlatformAdminControllerTest.java
  - backend/src/test/java/com/lexcv/controllers/PlatformAdminControllerMoldesTest.java
autonomous: true
requirements: [MOLD-02, MOLD-03, MOLD-04]

must_haves:
  truths:
    - "GET /api/v1/platform/moldes devolve, num só pedido, o catálogo de permissões e a lista de moldes com o número de escritórios que já instanciaram cada um"
    - "PUT /api/v1/platform/moldes grava o conjunto de permissões de um ou mais moldes e nunca toca em nenhuma linha de t_tenant_role"
    - "POST /api/v1/platform/moldes cria um molde novo, já marcado como instanciável, que passa a ser instanciado nos escritórios provisionados a partir daí"
    - "PLATAFORMA_ADMIN nunca é listado, nunca é editável e nunca é criável como molde"
    - "Um ADMIN de escritório é recusado nos três handlers novos, provado por um proxy AOP real e não por leitura de anotação"
    - "Permissões reservadas à plataforma nunca aparecem no catálogo servido a esta consola"
  artifacts:
    - path: "backend/src/main/java/com/lexcv/dtos/MoldesConsolaResponse.java"
      provides: "Payload único da consola: permissoes (catálogo) + moldes (com escritoriosInstanciados)"
      contains: "escritoriosInstanciados"
    - path: "backend/src/main/java/com/lexcv/controllers/PlatformAdminController.java"
      provides: "GET/PUT/POST /platform/moldes sob o gate de classe herdado"
      contains: "/moldes"
    - path: "backend/src/test/java/com/lexcv/controllers/PlatformAdminControllerMoldesTest.java"
      provides: "Grupo A (chamada directa) + Grupo B (proxy AOP de method security) para os 3 handlers novos"
      contains: "AuthorizationManagerBeforeMethodInterceptor"
  key_links:
    - from: "backend/src/main/java/com/lexcv/controllers/PlatformAdminController.java"
      to: "TenantRoleRepository.countByMoldeId"
      via: "projecção do DTO de molde"
      pattern: "countByMoldeId"
    - from: "backend/src/main/java/com/lexcv/controllers/PlatformAdminController.java"
      to: "PermissionRepository.findAllByReservadaPlataformaFalse"
      via: "catálogo servido à consola"
      pattern: "findAllByReservadaPlataformaFalse"
    - from: "backend/src/main/java/com/lexcv/controllers/PlatformAdminController.java"
      to: "RoleRepository.findAllByInstanciavelTrue"
      via: "listagem de moldes"
      pattern: "findAllByInstanciavelTrue"
---

<objective>
Expor os moldes como superfície de plataforma: três handlers em `PlatformAdminController` que listam, editam e criam moldes, sob o gate de classe `@PreAuthorize("hasRole('PLATAFORMA_ADMIN')")` que já lá está e é herdado automaticamente.

Purpose: é o contrato que a consola `/plataforma/moldes` consome. O `GET` tem de trazer o número de escritórios que já instanciaram cada molde — é o número load-bearing de todo o aviso de não-propagação do UI-SPEC, sem o qual o ecrã não consegue cumprir o requisito de "impossível de ler mal".
Output: 4 DTOs, 3 handlers, 1 classe de teste nova, 1 teste existente adaptado ao construtor.

**Fronteira intransponível:** `PUT /admin/rbac` não muda nesta fase (CATL-04 e a autoridade `rbac:manage` são da Phase 127). Editar um molde não pode tocar em nenhuma linha de `t_tenant_role` — isso é provado, não afirmado.
</objective>

<execution_context>
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/LEXCV-125-moldes-da-plataforma-e-provisionamento/125-CONTEXT.md
@.planning/phases/LEXCV-125-moldes-da-plataforma-e-provisionamento/125-PATTERNS.md
@.planning/phases/LEXCV-125-moldes-da-plataforma-e-provisionamento/125-UI-SPEC.md
@.planning/phases/LEXCV-125-moldes-da-plataforma-e-provisionamento/125-01-SUMMARY.md
@.planning/phases/LEXCV-124-cat-logo-de-permiss-es-em-base-de-dados/124-02-SUMMARY.md
@CLAUDE.md
</context>

<build_environment>
`java`/`mvn` por omissão resolvem para JDK 26, que não compila este projeto (Lombok: `ExceptionInInitializerError: TypeTag :: UNKNOWN`). Todo o comando Maven corre a partir de `backend/` com `JAVA_HOME="/c/Program Files/Java/jdk-23"` prefixado.
</build_environment>

<interfaces>
<!-- Contratos já extraídos. O executor não precisa de explorar o codebase. -->

`PlatformAdminController` hoje: `@RestController @RequestMapping("/api/v1/platform") @PreAuthorize("hasRole('PLATAFORMA_ADMIN')") @RequiredArgsConstructor`. O gate é **de classe** e cobre automaticamente qualquer handler novo — não acrescentar `@PreAuthorize` por método (convenção documentada pela Phase 119/120 no próprio doc-comment da classe). Constante `private static final String TENANT_RESERVADO = "ALCv";`. Colaboradores actuais, nesta ordem: `SetupService setupService`, `TenantRepository tenantRepository`, `UserRepository userRepository`. Handlers existentes: `POST /tenants` (`createTenant`), `GET /tenants` (`listTenants`), `PUT /tenants/{id}` (`updateTenant`), `PATCH /tenants/{id}/ativo` (`setTenantAtivo`), helper privado `toSummary(Tenant)`.

Idiomas a copiar de handlers existentes:
- Listagem com projecção: `listTenants` → `repository.findAll().stream().map(this::toSummary).sorted(Comparator.comparing(..., String.CASE_INSENSITIVE_ORDER)).collect(Collectors.toList())` → `ResponseEntity.ok(...)`.
- Contagem viva dentro da projecção: `toSummary` já usa `userRepository.countByTenantIdAndAtivoTrue(tenant.getId())`.
- Guarda de 404: `updateTenant` → `findById(id).orElse(null)`; se nulo, `ResponseEntity.status(HttpStatus.NOT_FOUND).body(Map.of("message", "..."))`.
- Guarda de validação: `ResponseEntity.badRequest().body(Map.of("message", "..."))`.
- Guarda de entidade reservada por nome literal: `setTenantAtivo` → `if (TENANT_RESERVADO.equals(tenant.getNome()) && ...) return ResponseEntity.badRequest().body(Map.of("message", "..."));`.
- Criação: `createTenant` → `try { ... return ResponseEntity.status(HttpStatus.CREATED).body(response); } catch (IllegalArgumentException ex) { badRequest } catch (IllegalStateException ex) { FORBIDDEN } catch (DataIntegrityViolationException ex) { badRequest com mensagem própria }`.
- `@Transactional` em método de controller é precedente estabelecido neste codebase (`ParecerController`, `ResourceController`).

DTOs existentes como analogia de forma:
- Resposta: `TenantAdminSummaryResponse` → `@Data @Builder @NoArgsConstructor @AllArgsConstructor`, projecção plana.
- Pedido: `TenantUpdateRequest` → `@Getter @Setter` apenas (corpo mutável, desserializado pelo Jackson), sem `@Builder`.

Contratos consumidos, criados noutros planos/fases:
- Plan 01: `RoleRepository.findAllByInstanciavelTrue()` → `List<Role>`; `TenantRoleRepository.countByMoldeId(Integer)` → `long`; `Role.getInstanciavel()`/`setInstanciavel(Boolean)`.
- Phase 124: `PermissionRepository.findAllByReservadaPlataformaFalse()` → `List<Permission>`; `Permission` tem `nome` (chave técnica, ex. `clientes:view`), `rotulo`, `descricao`, `modulo`, `ordem` (Integer, nullable), `reservadaPlataforma`.
- `Role` tem `id` (Integer), `nome`, `permissions` (`Set<Permission>`), e agora `instanciavel`.

Andaime do teste de controller (`PlatformAdminControllerTest`): `@ExtendWith(MockitoExtension.class)`, `@Mock` por colaborador, `@AfterEach` a limpar `SecurityContextHolder`, helper `novoController()` (linha 103) que instancia o controller com os mocks — **esta linha tem de ganhar os colaboradores novos**. Grupo B usa proxy AOP real:
`ProxyFactory factory = new ProxyFactory(novoController()); factory.setProxyTargetClass(true); factory.addAdvisor(AuthorizationManagerBeforeMethodInterceptor.preAuthorize()); return (PlatformAdminController) factory.getProxy();`
e `autenticarComoRoles(String... roles)` que povoa `SecurityContextHolder` com `UsernamePasswordAuthenticationToken(null, null, List<SimpleGrantedAuthority>)`. Uma chamada Java directa **nunca** avalia `@PreAuthorize` — o proxy é obrigatório, ler a anotação por reflexão não é prova (convenção estabelecida pela Phase 119).

Campo imposto pelo UI-SPEC (binding, secção 5): o payload de cada molde tem de trazer **`escritoriosInstanciados`** (número). `125-PATTERNS.md` sugeria `tenantsInstanciados`; onde os dois documentos divergem, **o UI-SPEC prevalece** — o nome do campo é `escritoriosInstanciados`, no DTO e no tipo TypeScript.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: DTOs da consola de moldes e GET /platform/moldes</name>
  <files>
    backend/src/main/java/com/lexcv/dtos/MoldesConsolaResponse.java
    backend/src/main/java/com/lexcv/dtos/MoldesUpdateRequest.java
    backend/src/main/java/com/lexcv/dtos/MoldeCreateRequest.java
    backend/src/main/java/com/lexcv/dtos/MoldeProvisionResponse.java
    backend/src/main/java/com/lexcv/controllers/PlatformAdminController.java
    backend/src/test/java/com/lexcv/controllers/PlatformAdminControllerTest.java
  </files>
  <read_first>
    backend/src/main/java/com/lexcv/controllers/PlatformAdminController.java
    backend/src/main/java/com/lexcv/dtos/TenantAdminSummaryResponse.java
    backend/src/main/java/com/lexcv/dtos/TenantUpdateRequest.java
    backend/src/main/java/com/lexcv/dtos/RbacResponse.java
    backend/src/main/java/com/lexcv/controllers/AdminController.java
    backend/src/main/java/com/lexcv/models/Permission.java
    backend/src/main/java/com/lexcv/models/Role.java
    backend/src/test/java/com/lexcv/controllers/PlatformAdminControllerTest.java
    .planning/phases/LEXCV-125-moldes-da-plataforma-e-provisionamento/125-UI-SPEC.md
  </read_first>
  <action>
Criar os 4 DTOs em `backend/src/main/java/com/lexcv/dtos/`:

`MoldesConsolaResponse` (`@Data @Builder @NoArgsConstructor @AllArgsConstructor`), com dois campos: `List<PermissaoDto> permissoes` e `List<MoldeDto> moldes`. Duas classes estáticas aninhadas, no mesmo estilo de `RbacResponse.PermissionDefDto`: `PermissaoDto` com `String key` (a chave técnica, `Permission.nome`), `String nome` (o rótulo legível, `Permission.rotulo`), `String descricao`, `String modulo`; `MoldeDto` com `Integer id`, `String nome`, `List<String> permissoes` (chaves técnicas), `long escritoriosInstanciados`. Declarar `PermissaoDto` aqui de forma independente e **não** reutilizar `RbacResponse.PermissionDefDto` — a consola de plataforma não deve depender do DTO da superfície de administração de escritório, que a Phase 127 vai alterar. Documentar essa escolha num doc-comment de classe, junto com a razão do payload único (um só pedido sob um só gate de classe, em vez de a consola ir buscar o catálogo a `GET /admin/rbac`, cujo gate é `ADMIN` **ou** `PLATAFORMA_ADMIN` e portanto mais largo do que esta consola).

`MoldesUpdateRequest` (`@Getter @Setter`, sem `@Builder`): um campo `List<MoldePermissoesEntry> moldes`, com a classe estática aninhada `MoldePermissoesEntry` (`@Getter @Setter`) contendo `Integer id` e `List<String> permissoes`. O `PUT` é **em lote** por decisão de UI: o UI-SPEC (secção 6) grava num só `AlertDialog` todos os moldes alterados, com um só botão "Confirmar e Gravar" — e o caminho do CONTEXT.md é `PUT /platform/moldes`, sem `{id}`. Escrever isso no doc-comment.

`MoldeCreateRequest` (`@Getter @Setter`): `String nome`, `List<String> permissoes`.

`MoldeProvisionResponse` (`@Data @Builder @NoArgsConstructor @AllArgsConstructor`): `Integer id`, `String nome` — projecção mínima de 201, mesma forma que `TenantProvisionResponse`.

Acrescentar a `PlatformAdminController` três colaboradores `private final`, **no fim** da lista existente (a ordem importa para o construtor gerado e para os testes): `RoleRepository roleRepository`, `PermissionRepository permissionRepository`, `TenantRoleRepository tenantRoleRepository`. Acrescentar também a constante `private static final String PAPEL_RESERVADO = "PLATAFORMA_ADMIN";`, no mesmo registo de `TENANT_RESERVADO`, com um comentário a dizer que este papel nunca é instanciável, nunca aparece como molde editável e nunca é atribuível a partir de superfície de escritório — a continuação das guardas das Phases 119 e 121, não uma reinvenção.

Implementar `GET /moldes` (`@GetMapping("/moldes")`, método `listMoldes()`), **sem** `@PreAuthorize` próprio (o gate de classe cobre). Corpo:
  - Catálogo: `permissionRepository.findAllByReservadaPlataformaFalse()`, depois filtrar em Java as permissões sem rótulo utilizável (nulo ou em branco após `trim`) — mesma disciplina que a Phase 124 aplicou em `AdminController.getRbac()`, e requisito directo do UI-SPEC, cujo `aria-label` de cada checkbox é `${permissao.rotulo} — ${molde.nome}` e fica sem sentido com um rótulo em branco. Ordenar de forma estável por `Permission.ordem` com `Comparator.nullsLast(Comparator.naturalOrder())` e desempate por `nome` (tolera uma base de dados onde a migração correu mas o seeder ainda não populou `ordem`). Mapear cada entidade por um helper privado `toPermissaoDto(Permission)` usando `builder()` com **campos nomeados** — `key` ← `nome`, `nome` ← `rotulo` — nunca um construtor posicional, para evitar a troca silenciosa identificada como armadilha na Phase 124.
  - Moldes: `roleRepository.findAllByInstanciavelTrue()`, com uma exclusão defensiva por nome literal de `PAPEL_RESERVADO` (defesa em profundidade sobre o filtro SQL), ordenados por nome com `String.CASE_INSENSITIVE_ORDER`, mapeados por um helper privado `toMoldeDto(Role)` que preenche `id`, `nome`, `permissoes` (as chaves técnicas de `role.getPermissions()`, ordenadas de forma estável) e `escritoriosInstanciados` via `tenantRoleRepository.countByMoldeId(role.getId())` — a contagem viva, calculada por molde, exactamente como `toSummary` já faz com `countByTenantIdAndAtivoTrue`.
  - Devolver `ResponseEntity.ok(MoldesConsolaResponse.builder()...build())`.

Actualizar `PlatformAdminControllerTest`: acrescentar `@Mock` para os três repositórios novos e passá-los ao construtor no helper `novoController()` (linha 103). Onde algum teste existente falhe por stubbing desnecessário, usar `lenient()`. Não apagar nem reescrever nenhum caso existente — os testes de Grupo B (proxy AOP) dos 4 handlers de tenants têm de continuar a passar exactamente como estão.
  </action>
  <acceptance_criteria>
    - `cd backend && JAVA_HOME="/c/Program Files/Java/jdk-23" mvn -DskipTests compile` exit 0.
    - `cd backend && JAVA_HOME="/c/Program Files/Java/jdk-23" mvn test -Dtest=PlatformAdminControllerTest` exit 0, `Failures: 0, Errors: 0`, com o mesmo número de testes de antes.
    - `grep -n '@GetMapping("/moldes")' backend/src/main/java/com/lexcv/controllers/PlatformAdminController.java` devolve exactamente 1 linha.
    - `grep -vE '^\s*(//|\*|/\*)' backend/src/main/java/com/lexcv/controllers/PlatformAdminController.java | grep -c '@PreAuthorize'` devolve `1` — só o gate de classe, zero anotações por método.
    - `grep -n 'findAllByReservadaPlataformaFalse' backend/src/main/java/com/lexcv/controllers/PlatformAdminController.java` devolve exactamente 1 linha, e `grep -vE '^\s*(//|\*|/\*)' .../PlatformAdminController.java | grep -c 'permissionRepository.findAll()'` devolve `0`.
    - `grep -n 'countByMoldeId' backend/src/main/java/com/lexcv/controllers/PlatformAdminController.java` devolve exactamente 1 linha.
    - `grep -n 'escritoriosInstanciados' backend/src/main/java/com/lexcv/dtos/MoldesConsolaResponse.java` devolve >= 1 linha, e `grep -c 'tenantsInstanciados' backend/src/main/java/com/lexcv/dtos/MoldesConsolaResponse.java` devolve `0`.
    - `grep -c 'RbacResponse' backend/src/main/java/com/lexcv/dtos/MoldesConsolaResponse.java` devolve `0` (DTO independente).
    - `git diff --name-only backend/src/main/java/com/lexcv/controllers/AdminController.java` vazio — `PUT /admin/rbac` não muda nesta fase.
  </acceptance_criteria>
  <verify>
    <automated>cd backend && JAVA_HOME="/c/Program Files/Java/jdk-23" mvn test -Dtest=PlatformAdminControllerTest</automated>
  </verify>
  <done>Os 4 DTOs existem, `GET /api/v1/platform/moldes` devolve catálogo filtrado e ordenado mais a lista de moldes com `escritoriosInstanciados` calculado ao vivo, sob o gate de classe herdado, e a suite de testes existente do controller continua verde.</done>
</task>

<task type="auto">
  <name>Task 2: PUT e POST /platform/moldes com as guardas do papel reservado</name>
  <files>
    backend/src/main/java/com/lexcv/controllers/PlatformAdminController.java
  </files>
  <read_first>
    backend/src/main/java/com/lexcv/controllers/PlatformAdminController.java
    backend/src/main/java/com/lexcv/dtos/MoldesUpdateRequest.java
    backend/src/main/java/com/lexcv/dtos/MoldeCreateRequest.java
    backend/src/main/java/com/lexcv/models/Role.java
    backend/src/main/java/com/lexcv/repositories/PermissionRepository.java
    .planning/phases/LEXCV-125-moldes-da-plataforma-e-provisionamento/125-CONTEXT.md
    .planning/phases/LEXCV-125-moldes-da-plataforma-e-provisionamento/125-UI-SPEC.md
  </read_first>
  <action>
Implementar `PUT /moldes` (`@PutMapping("/moldes")`, método `updateMoldes(@RequestBody MoldesUpdateRequest request)`), com `@Transactional` (precedente de `ParecerController`/`ResourceController`), sem `@PreAuthorize` próprio. **Validar tudo antes de gravar qualquer coisa** — validate-then-write, para que um pedido inválido não deixe metade dos moldes alterados:
  - `request` ou `request.getMoldes()` nulo/vazio → 400 com `Map.of("message", ...)`.
  - Por cada entrada: `id` nulo → 400; `roleRepository.findById(id)` vazio → 404 ("Molde não encontrado."); o papel encontrado não instanciável (`Boolean.TRUE.equals(getInstanciavel())` falso) → 400 ("Este papel não é um molde da plataforma."); nome igual a `PAPEL_RESERVADO` → 400 ("O papel PLATAFORMA_ADMIN não pode ser editado como molde."); `permissoes` nulo → 400. Aplicar o idioma literal-name de `setTenantAtivo` para as duas últimas guardas.
  - Resolver as chaves técnicas pedidas em entidades `Permission` lendo por `permissionRepository.findAllByReservadaPlataformaFalse()` (uma leitura, reutilizada para todas as entradas) e construindo um mapa chave→entidade; qualquer chave desconhecida ou reservada → 400 nomeando a chave rejeitada. Esta é a razão pela qual a resolução passa pela leitura filtrada e não por `findById` sobre ids crus: uma permissão reservada à plataforma nunca pode entrar num molde por via de um corpo de pedido.
  - Só depois de todas as entradas passarem, aplicar: `role.setPermissions(new HashSet<>(permissoesResolvidas))` por molde e `roleRepository.save(role)`.
  - Devolver `ResponseEntity.ok(...)` com um `MoldesConsolaResponse` reprojectado (reutilizar os helpers `toPermissaoDto`/`toMoldeDto` da Task 1), para que o cliente receba as contagens `escritoriosInstanciados` actualizadas sem um segundo pedido.
  - **Nunca ler nem escrever `tenantRoleRepository` neste handler**, a não ser a contagem de leitura dentro de `toMoldeDto`. Escrever um comentário de bloco a dizer porquê: o papel instanciado é um snapshot (MOLD-03) — propagar aqui contradiria a decisão bloqueada da fase, e é isso que o aviso de não-propagação do ecrã promete ao operador.

Implementar `POST /moldes` (`@PostMapping("/moldes")`, método `createMolde(@RequestBody MoldeCreateRequest request)`), no formato de `createTenant`:
  - `nome` nulo ou em branco após `trim` → 400 ("O nome do molde é obrigatório.").
  - Normalizar para maiúsculas com `trim` (`toUpperCase(Locale.ROOT)`), coerente com os 4 moldes semeados e com a transformação de submissão que o UI-SPEC especifica.
  - Nome normalizado igual a `PAPEL_RESERVADO` → 400 com a mensagem exacta do UI-SPEC: `"Este nome está reservado à plataforma e não pode ser usado como molde."`.
  - `roleRepository.findByNome(nomeNormalizado)` já presente → 400 ("Já existe um papel com este nome.").
  - Resolver `permissoes` pelo mesmo mapa filtrado do `PUT` (chave desconhecida ou reservada → 400); uma lista vazia ou nula é aceite (um molde pode nascer sem permissões).
  - Gravar `Role.builder().nome(nomeNormalizado).instanciavel(true).permissions(new HashSet<>(...)).build()` e devolver `ResponseEntity.status(HttpStatus.CREATED).body(MoldeProvisionResponse.builder().id(...).nome(...).build())`.
  - Envolver a gravação num `catch (DataIntegrityViolationException ex)` → 400 ("Já existe um papel com este nome."), para o caso de duplicação concorrente que a constraint única de `Role.nome` apanha na base de dados.

Não alterar nenhum dos 4 handlers de tenants, nem `toSummary`, nem `TENANT_RESERVADO`, nem a anotação de classe. Não acrescentar `@PreAuthorize` por método em nenhum dos dois handlers novos.
  </action>
  <acceptance_criteria>
    - `cd backend && JAVA_HOME="/c/Program Files/Java/jdk-23" mvn -DskipTests compile` exit 0.
    - `grep -n '@PutMapping("/moldes")' backend/src/main/java/com/lexcv/controllers/PlatformAdminController.java` devolve exactamente 1 linha, e `grep -n '@PostMapping("/moldes")' ...` devolve exactamente 1 linha.
    - `grep -vE '^\s*(//|\*|/\*)' backend/src/main/java/com/lexcv/controllers/PlatformAdminController.java | grep -c '@PreAuthorize'` continua a devolver `1`.
    - `grep -c 'PAPEL_RESERVADO' backend/src/main/java/com/lexcv/controllers/PlatformAdminController.java` devolve >= 3 (declaração + guarda no PUT + guarda no POST).
    - `grep -n 'Este nome está reservado à plataforma e não pode ser usado como molde.' backend/src/main/java/com/lexcv/controllers/PlatformAdminController.java` devolve exactamente 1 linha (mensagem exacta do UI-SPEC).
    - `grep -n 'instanciavel(true)' backend/src/main/java/com/lexcv/controllers/PlatformAdminController.java` devolve exactamente 1 linha.
    - `grep -vE '^\s*(//|\*|/\*)' backend/src/main/java/com/lexcv/controllers/PlatformAdminController.java | grep -c 'tenantRoleRepository\.\(save\|delete\|deleteAll\)'` devolve `0` — nenhum handler escreve em `t_tenant_role`.
    - `grep -vE '^\s*(//|\*|/\*)' backend/src/main/java/com/lexcv/controllers/PlatformAdminController.java | grep -c 'tenantRoleRepository\.'` devolve `1` — a única utilização é o `countByMoldeId` de leitura.
    - `git diff --name-only backend/src/main/java/com/lexcv/controllers/AdminController.java` vazio.
  </acceptance_criteria>
  <verify>
    <automated>cd backend && JAVA_HOME="/c/Program Files/Java/jdk-23" mvn -DskipTests compile</automated>
  </verify>
  <done>`PUT /api/v1/platform/moldes` grava em lote com validação prévia completa e sem tocar em papéis instanciados; `POST /api/v1/platform/moldes` cria um molde já instanciável com o nome normalizado, recusando `PLATAFORMA_ADMIN` com a mensagem exacta do UI-SPEC.</done>
</task>

<task type="auto">
  <name>Task 3: Testes dos 3 handlers, incluindo recusa de ADMIN de escritório por proxy AOP real</name>
  <files>
    backend/src/test/java/com/lexcv/controllers/PlatformAdminControllerMoldesTest.java
  </files>
  <read_first>
    backend/src/test/java/com/lexcv/controllers/PlatformAdminControllerTest.java
    backend/src/main/java/com/lexcv/controllers/PlatformAdminController.java
    backend/src/main/java/com/lexcv/dtos/MoldesConsolaResponse.java
    backend/src/main/java/com/lexcv/dtos/MoldesUpdateRequest.java
    backend/src/main/java/com/lexcv/dtos/MoldeCreateRequest.java
    .planning/phases/LEXCV-125-moldes-da-plataforma-e-provisionamento/125-UI-SPEC.md
  </read_first>
  <action>
Criar `PlatformAdminControllerMoldesTest` no andaime exacto de `PlatformAdminControllerTest` (Mockito puro, `@AfterEach` a limpar `SecurityContextHolder`, helpers `novoController()` e `novoProxyComMethodSecurity()` e `autenticarComoRoles(...)` copiados). Doc-comment de classe a explicar a divisão em dois grupos e a citar Phase 119: uma chamada Java directa nunca avalia `@PreAuthorize`, portanto o Grupo B monta um `ProxyFactory` CGLIB (`setProxyTargetClass(true)`, obrigatório porque o controller não implementa interface) com o mesmo `AuthorizationManagerBeforeMethodInterceptor.preAuthorize()` que o Spring Security usa em produção. **Ler a anotação por reflexão não é prova aceitável.**

Grupo A — comportamento (chamada directa):
  1. `listMoldes` devolve o catálogo já filtrado e ordenado: stubar `findAllByReservadaPlataformaFalse()` com permissões em `ordem` desordenada, uma com `rotulo` nulo e outra com `rotulo` em branco; afirmar que as duas sem rótulo são excluídas e que as restantes saem por `ordem` ascendente com `null` no fim.
  2. `listMoldes` devolve `escritoriosInstanciados` por molde a partir de `countByMoldeId`: stubar contagens diferentes por id (por exemplo 0 e 3) e afirmar que cada `MoldeDto` recebe a sua, e que o repositório é consultado uma vez por molde.
  3. `listMoldes` nunca lista `PLATAFORMA_ADMIN`: stubar `findAllByInstanciavelTrue()` a devolver, numa regressão hipotética do filtro SQL, um papel com esse nome, e afirmar que ele não aparece no payload.
  4. `updateMoldes` grava o conjunto pedido: `ArgumentCaptor<Role>` confirma que `getPermissions()` do papel gravado contém exactamente as chaves pedidas.
  5. `updateMoldes` **nunca escreve em `t_tenant_role`** (MOLD-03, o caso mais importante do ficheiro): `verify(tenantRoleRepository, never()).save(any())`, `never()).delete(any())`, `never()).deleteAll()`, e nenhuma chamada a `findByTenantId`. As únicas interacções permitidas são `countByMoldeId`.
  6. `updateMoldes` recusa: id inexistente → 404; papel não instanciável → 400; papel chamado `PLATAFORMA_ADMIN` → 400; chave de permissão desconhecida → 400 nomeando a chave; lista de moldes vazia → 400. Em **todos** estes casos, afirmar `verify(roleRepository, never()).save(any())` — validate-then-write, nenhuma gravação parcial.
  7. `createMolde` normaliza o nome para maiúsculas com `trim` e grava com `instanciavel = true` e as permissões pedidas; resposta 201 com `id` e `nome`.
  8. `createMolde` recusa `PLATAFORMA_ADMIN` (em qualquer caixa, ex. `plataforma_admin`) com 400 e a mensagem exacta `"Este nome está reservado à plataforma e não pode ser usado como molde."`, sem gravar nada.
  9. `createMolde` recusa nome duplicado (via `findByNome` presente) e converte `DataIntegrityViolationException` em 400 (duplicação concorrente).

Grupo B — autoridade (proxy AOP):
  10. Um utilizador autenticado apenas com `ROLE_ADMIN` recebe `AccessDeniedException` nos **três** handlers novos (`listMoldes`, `updateMoldes`, `createMolde`) — um teste por handler, nunca só num deles. Este é o teste que prova que a superfície cross-tenant permanece inalcançável a um ADMIN de escritório.
  11. Um utilizador autenticado com `ROLE_PLATAFORMA_ADMIN` atravessa o gate nos três handlers (o proxy não lança; o resultado é o comportamento normal).
  12. Um utilizador sem autenticação nenhuma (contexto vazio) é recusado nos três handlers.

Escrever um comentário a marcar o caso 5 e o caso 10 como os dois testes que não podem ser apagados por nenhum refactor futuro: o 5 é a prova de MOLD-03 do lado do servidor, o 10 é a prova do gate cross-tenant.
  </action>
  <acceptance_criteria>
    - `cd backend && JAVA_HOME="/c/Program Files/Java/jdk-23" mvn test -Dtest=PlatformAdminControllerMoldesTest` exit 0, `Failures: 0, Errors: 0`, com pelo menos 12 testes executados.
    - `grep -c 'AuthorizationManagerBeforeMethodInterceptor' backend/src/test/java/com/lexcv/controllers/PlatformAdminControllerMoldesTest.java` devolve >= 1.
    - `grep -c 'AccessDeniedException' backend/src/test/java/com/lexcv/controllers/PlatformAdminControllerMoldesTest.java` devolve >= 3 (um por handler novo).
    - `grep -c 'tenantRoleRepository, never())' backend/src/test/java/com/lexcv/controllers/PlatformAdminControllerMoldesTest.java` devolve >= 2.
    - `grep -c 'roleRepository, never()).save' backend/src/test/java/com/lexcv/controllers/PlatformAdminControllerMoldesTest.java` devolve >= 1.
    - Prova de RED genuíno do gate cross-tenant: comentar temporariamente o `@PreAuthorize` de classe em `PlatformAdminController`, correr `mvn test -Dtest=PlatformAdminControllerMoldesTest`, confirmar que os 3 casos do Grupo B **falham**, restaurar a anotação e confirmar que voltam a passar. Registar as duas saídas no SUMMARY.
    - `cd backend && JAVA_HOME="/c/Program Files/Java/jdk-23" mvn test` exit 0 (suite completa verde).
    - `cd backend && JAVA_HOME="/c/Program Files/Java/jdk-23" mvn spotbugs:check` sem novos achados.
  </acceptance_criteria>
  <verify>
    <automated>cd backend && JAVA_HOME="/c/Program Files/Java/jdk-23" mvn test -Dtest=PlatformAdminControllerMoldesTest</automated>
  </verify>
  <done>Os três handlers novos têm prova comportamental de listagem/edição/criação, prova de que a edição nunca toca papéis instanciados, e prova por proxy AOP real de que um ADMIN de escritório é recusado nos três — com demonstração de que o teste do gate é capaz de reprovar.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → `/api/v1/platform/moldes` | entrada não confiável que atravessa para uma superfície **cross-tenant por desenho**: os moldes afectam todos os escritórios provisionados a partir daí |
| corpo do pedido → `t_role.permissions` | chaves de permissão vindas de JSON decidem que autoridade um molde confere |
| corpo do pedido → `t_role` (criação) | um nome de papel novo vindo de JSON entra no catálogo de papéis globais |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-125-17 | Elevation of Privilege | `GET/PUT/POST /platform/moldes` | mitigate | Gate de classe `@PreAuthorize("hasRole('PLATAFORMA_ADMIN')")` herdado, zero anotações por método (gate de grep a `1` ocorrência); provado por proxy AOP real nos 3 handlers (Task 3, caso 10) com demonstração de RED genuíno |
| T-125-18 | Elevation of Privilege | `PLATAFORMA_ADMIN` como molde | mitigate | Constante `PAPEL_RESERVADO` com guarda literal-name no `PUT` e no `POST` (idioma de `TENANT_RESERVADO`), exclusão defensiva na listagem, e `instanciavel = false` semeado (Plan 01). Três camadas, nenhuma delas suficiente sozinha |
| T-125-19 | Tampering | corpo do `PUT` → papéis instanciados | mitigate | Handler nunca escreve em `tenantRoleRepository` (gate de grep a `0` gravações); `verify(never())` nos testes (Task 3, caso 5) |
| T-125-20 | Elevation of Privilege | permissões reservadas num molde | mitigate | Resolução de chaves exclusivamente pelo mapa de `findAllByReservadaPlataformaFalse()`; chave desconhecida ou reservada → 400 nomeando a chave. Uma permissão reservada não pode entrar num molde por via de JSON |
| T-125-21 | Tampering | gravação parcial de um lote | mitigate | Validate-then-write: todas as entradas validadas antes de qualquer `save`, `@Transactional` no handler; testes afirmam `roleRepository, never()).save` em cada caso de recusa |
| T-125-22 | Tampering | nome de molde duplicado em concorrência | mitigate | Constraint única de `Role.nome` na base de dados + `catch (DataIntegrityViolationException)` → 400, idioma de `createTenant` |
| T-125-23 | Information Disclosure | catálogo servido à consola | mitigate | `findAllByReservadaPlataformaFalse()` (exclusão por SQL) + filtro de rótulo em branco em Java, com a divergência herdada da Phase 124 mantida inerte porque **esta fase não marca nenhuma permissão como reservada** |
| T-125-24 | Elevation of Privilege | `PUT /admin/rbac` | mitigate | Não é tocado nesta fase (gate de `git diff --name-only` vazio em `AdminController.java`); CATL-04 e `rbac:manage` são da Phase 127 |
| T-125-25 | Denial of Service | criação ilimitada de moldes | accept | Superfície interna de um único operador de plataforma, atrás do gate mais restritivo do sistema; nenhum consumo desproporcionado por pedido |
| T-125-26 | Repudiation | quem editou que molde | accept | Sem trilha de auditoria nesta fase; âmbito da Phase 128 |
| T-125-SC | Tampering | instalação de pacotes | mitigate | Zero pacotes novos; `git diff --stat backend/pom.xml` vazio |
</threat_model>

<verification>
- `cd backend && JAVA_HOME="/c/Program Files/Java/jdk-23" mvn test` verde (suite completa).
- `cd backend && JAVA_HOME="/c/Program Files/Java/jdk-23" mvn spotbugs:check` sem novos achados.
- `git diff --name-only` limitado aos 7 ficheiros declarados em `files_modified` (em particular, `AdminController.java` fora).
- `git diff --stat backend/pom.xml` vazio.
</verification>

<success_criteria>
- `GET /api/v1/platform/moldes` devolve num só pedido o catálogo filtrado/ordenado e a lista de moldes com `escritoriosInstanciados`.
- `PUT /api/v1/platform/moldes` grava em lote, valida antes de gravar, e nunca escreve em `t_tenant_role`.
- `POST /api/v1/platform/moldes` cria um molde instanciável com nome normalizado e recusa `PLATAFORMA_ADMIN` com a mensagem exacta do UI-SPEC.
- Um ADMIN de escritório é recusado nos três handlers, provado por proxy AOP real capaz de reprovar.
- `PUT /admin/rbac` inalterado.
</success_criteria>

<output>
Create `.planning/phases/LEXCV-125-moldes-da-plataforma-e-provisionamento/125-03-SUMMARY.md` when done.

Registar no SUMMARY: (a) as duas saídas da demonstração de RED genuíno do gate de classe; (b) que o nome do campo é `escritoriosInstanciados` por o UI-SPEC prevalecer sobre a sugestão `tenantsInstanciados` de `125-PATTERNS.md`; (c) que nenhuma permissão do catálogo foi marcada como reservada nesta fase, pelo que a divergência `rolePermissions`/JWT herdada da Phase 124 continua inerte.
</output>
