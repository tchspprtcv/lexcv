---
phase: 58-formulario-dinamico
plan: 02
type: execute
wave: 2
depends_on: [58-01]
files_modified:
  - web/src/app/(dashboard)/clientes/page.tsx
  - web/src/app/(dashboard)/clientes/[id]/page.tsx
autonomous: true
requirements: [PERF-02, EMP-02]

must_haves:
  truths:
    - "Listing shows CLI-XXXX badge (blue, monospace) next to client name when numero_cliente is set"
    - "Listing shows green Avençado badge next to name when avencado is true"
    - "Listing stat cards count Particulares and Empresas (not Singulares/Coletivas)"
    - "Listing filter dropdown shows Particular and Empresa options (not Singular/Coletiva)"
    - "Detail page header shows CLI-XXXX badge and Avençado badge alongside client name"
  artifacts:
    - path: "web/src/app/(dashboard)/clientes/page.tsx"
      provides: "Updated listing with badges and corrected enum values"
      contains: "PARTICULAR"
    - path: "web/src/app/(dashboard)/clientes/[id]/page.tsx"
      provides: "Updated detail header with numero_cliente and avencado badges"
      contains: "numero_cliente"
  key_links:
    - from: "web/src/app/(dashboard)/clientes/page.tsx"
      to: "web/src/components/ui/badge.tsx"
      via: "Badge variant=blue for numero_cliente, variant=green for avencado"
    - from: "web/src/app/(dashboard)/clientes/[id]/page.tsx"
      to: "web/src/components/ui/badge.tsx"
      via: "Badge import added (not currently imported)"
---

<objective>
Update the clientes listing page and detail page to display numero_cliente and avencado badges, and fix the legacy SINGULAR/COLETIVA enum values to PARTICULAR/EMPRESA.

Purpose: Per D-04, D-06, D-07 — users must see the CLI-XXXX number and Avençado status at a glance in listing rows and in the detail header. The enum fix corrects a data mismatch introduced by Phase 57.
Output: Updated page.tsx and [id]/page.tsx with badge display and corrected enum references.
</objective>

<execution_context>
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/LEXCV-58-formulario-dinamico/58-CONTEXT.md
@.planning/phases/LEXCV-58-formulario-dinamico/58-PATTERNS.md
@.planning/phases/LEXCV-58-formulario-dinamico/58-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix enum values and add badges to listing page</name>
  <files>web/src/app/(dashboard)/clientes/page.tsx</files>
  <read_first>
    - web/src/app/(dashboard)/clientes/page.tsx (read all lines — find stat card filter logic ~line 68, filter dropdown ~line 309, mobile card badge block ~line 405, ClienteRow component badgeVariant ~line 496, desktop name cell ~line 512)
    - web/src/components/ui/badge.tsx (confirm variant names: blue, green are available)
    - .planning/phases/LEXCV-58-formulario-dinamico/58-PATTERNS.md (exact replacement snippets for all four change points)
  </read_first>
  <action>
    Make four targeted changes to web/src/app/(dashboard)/clientes/page.tsx:

    1. Stat card counters (near line 68): Replace filter comparisons from "SINGULAR" to "PARTICULAR" and "COLETIVA" to "EMPRESA". Rename variables from totalSingulares/totalColetivas to totalParticulares/totalEmpresas. Update any JSX that renders these variable names or their labels (e.g. "Singulares" → "Particulares", "Coletivas" → "Empresas").

    2. Filter dropdown options (near line 309): Replace option value="SINGULAR" with value="PARTICULAR" and label "Singular" with "Particular". Replace value="COLETIVA" with value="EMPRESA" and label "Coletiva" with "Empresa".

    3. ClienteRow badgeVariant (near line 496): Replace the ternary that checks tipo === "SINGULAR" with tipo === "PARTICULAR", and tipo === "COLETIVA" with tipo === "EMPRESA".

    4. Desktop TableRow name cell (near line 512): After the existing ID div (text-[11px] font-medium tracking-wider), add a conditional block: if cliente.numero_cliente exists, render a div with flex items-center gap-1 containing a Badge variant="blue" className="rounded-none font-mono font-bold text-[10px]" showing {cliente.numero_cliente}, and if cliente.avencado is also true, add a Badge variant="green" className="rounded-none font-bold text-[10px]" showing "Avençado".

    5. Mobile card badge block (near line 405): After the existing ativo badge, add the same two conditional badges as step 4 — numero_cliente blue badge and avencado green badge — with flex-shrink-0 added to className.

    Badge is already imported in this file — no new import needed.
  </action>
  <verify>
    <automated>cd web && pnpm build 2>&1 | grep -E "error TS|Type error" | head -20</automated>
  </verify>
  <acceptance_criteria>
    - web/src/app/(dashboard)/clientes/page.tsx contains "PARTICULAR" (replacing "SINGULAR")
    - web/src/app/(dashboard)/clientes/page.tsx contains "EMPRESA" (replacing "COLETIVA")
    - web/src/app/(dashboard)/clientes/page.tsx does NOT contain the string "SINGULAR" (grep returns 0 matches)
    - web/src/app/(dashboard)/clientes/page.tsx does NOT contain the string "COLETIVA" (grep returns 0 matches)
    - web/src/app/(dashboard)/clientes/page.tsx contains "numero_cliente"
    - web/src/app/(dashboard)/clientes/page.tsx contains variant="green"
    - pnpm build exits 0
  </acceptance_criteria>
  <done>Listing shows correct enum labels; CLI-XXXX and Avençado badges render in mobile cards and desktop rows.</done>
</task>

<task type="auto">
  <name>Task 2: Add numero_cliente and avencado badges to detail page header</name>
  <files>web/src/app/(dashboard)/clientes/[id]/page.tsx</files>
  <read_first>
    - web/src/app/(dashboard)/clientes/[id]/page.tsx (read all lines — find import block lines 6-26, data card Nome row ~line 105)
    - web/src/components/ui/badge.tsx (confirm Badge import path and variant names)
    - .planning/phases/LEXCV-58-formulario-dinamico/58-PATTERNS.md (exact replacement snippet for the Nome row in the dl element)
  </read_first>
  <action>
    In web/src/app/(dashboard)/clientes/[id]/page.tsx:

    1. Add Badge to the import block at the top of the file. It is not currently imported — add: import { Badge } from "@/components/ui/badge" alongside the existing component imports.

    2. Find the dl grid that shows client data. Locate the Nome row (dt contains "Nome", dd contains {cliente.data.nome}). Replace the dd element to include flex layout: change dd className to include "flex items-center gap-2 flex-wrap" and after {cliente.data.nome} add two conditional badges:
       - If cliente.data.numero_cliente exists: Badge variant="blue" className="rounded-none font-mono font-bold text-[10px]" containing {cliente.data.numero_cliente}
       - If cliente.data.avencado is true: Badge variant="green" className="rounded-none font-bold text-[10px]" containing "Avençado"

    Per D-04 and D-07, the badges appear in the detail header alongside the name. The numero_cliente is hidden when null (no placeholder text per D-04: "hidden when null").
  </action>
  <verify>
    <automated>cd web && pnpm build 2>&1 | grep -E "error TS|Type error" | head -20</automated>
  </verify>
  <acceptance_criteria>
    - web/src/app/(dashboard)/clientes/[id]/page.tsx contains import { Badge } from "@/components/ui/badge"
    - web/src/app/(dashboard)/clientes/[id]/page.tsx contains "numero_cliente"
    - web/src/app/(dashboard)/clientes/[id]/page.tsx contains variant="green"
    - web/src/app/(dashboard)/clientes/[id]/page.tsx contains variant="blue"
    - pnpm build exits 0
  </acceptance_criteria>
  <done>Detail page header shows CLI-XXXX badge and Avençado badge alongside the client name.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API response → listing render | numero_cliente and avencado come from backend; rendered as display-only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-58-02-01 | Information Disclosure | numero_cliente display | accept | numero_cliente is a non-sensitive sequential ID (CLI-0001); displaying it to authorized users is by design; all listing/detail pages already require clientes:view permission |
</threat_model>

<verification>
Run from web/ directory: pnpm build — must exit 0. Then manually verify in browser: listing shows CLI-XXXX badge and Avençado badge in rows; stat cards show "Particulares" and "Empresas"; detail page header shows badges next to name.
</verification>

<success_criteria>
- Listing page contains PARTICULAR/EMPRESA enum values, no SINGULAR/COLETIVA strings
- Listing rows show numero_cliente badge (blue) and avencado badge (green) when fields are set
- Detail page imports Badge and shows both badges in the Nome row
- pnpm build exits 0
</success_criteria>

<output>
Create `.planning/phases/LEXCV-58-formulario-dinamico/58-02-SUMMARY.md` when done
</output>
