---
phase: 58-formulario-dinamico
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/components/ui/radio-group.tsx
  - web/src/components/ui/switch.tsx
  - web/src/types/clientes.ts
  - web/src/schemas/clientes.ts
autonomous: true
requirements: [PERF-02, PERF-03, PERF-04, EMP-02]

must_haves:
  truths:
    - "RadioGroup component renders Particular/Empresa options in the browser"
    - "Switch component toggles a boolean value via Radix primitives"
    - "Cliente type includes numero_cliente, avencado, dados_tipo, and tipo as PARTICULAR | EMPRESA"
    - "ClienteCreateRequest and ClienteUpdateRequest include avencado and dados_tipo but NOT numero_cliente"
    - "Zod schema validates dados_tipo.nome_comercial and dados_tipo.representante_legal as required when tipo === EMPRESA"
  artifacts:
    - path: "web/src/components/ui/radio-group.tsx"
      provides: "RadioGroup + RadioGroupItem shadcn wrappers over @radix-ui/react-radio-group"
      exports: ["RadioGroup", "RadioGroupItem"]
    - path: "web/src/components/ui/switch.tsx"
      provides: "Switch shadcn wrapper over @radix-ui/react-switch"
      exports: ["Switch"]
    - path: "web/src/types/clientes.ts"
      provides: "Extended Cliente interfaces with Phase 57 fields"
      contains: "numero_cliente"
    - path: "web/src/schemas/clientes.ts"
      provides: "Extended Zod schema with tipo enum and dados_tipo object"
      contains: "PARTICULAR"
  key_links:
    - from: "web/src/app/(dashboard)/clientes/novo/page.tsx"
      to: "web/src/components/ui/radio-group.tsx"
      via: "import RadioGroup, RadioGroupItem"
    - from: "web/src/app/(dashboard)/clientes/novo/page.tsx"
      to: "web/src/components/ui/switch.tsx"
      via: "import Switch"
    - from: "web/src/schemas/clientes.ts"
      to: "web/src/types/clientes.ts"
      via: "schema output must align with ClienteCreateRequest shape"
---

<objective>
Install the two missing Radix UI primitives, scaffold their shadcn wrappers, and extend the type/schema layer to include all Phase 57 backend fields.

Purpose: All downstream plans (form refactors, listing/detail badge updates) depend on these components and type definitions. Nothing in Wave 2 can compile without them.
Output: radio-group.tsx, switch.tsx, updated types/clientes.ts, updated schemas/clientes.ts.
</objective>

<execution_context>
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/LEXCV-58-formulario-dinamico/58-CONTEXT.md
@.planning/phases/LEXCV-58-formulario-dinamico/58-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Radix primitives and scaffold UI components</name>
  <files>web/src/components/ui/radio-group.tsx, web/src/components/ui/switch.tsx</files>
  <read_first>
    - web/src/components/ui/dialog.tsx (shadcn wrapper pattern: "use client", namespace import, named function exports, data-slot, cn())
    - web/package.json (confirm @radix-ui/react-radio-group and @radix-ui/react-switch are absent before installing)
    - web/pnpm-lock.yaml (pnpm is the package manager — use pnpm add, not npm install)
  </read_first>
  <action>
    From the web/ directory, run: pnpm add @radix-ui/react-radio-group @radix-ui/react-switch

    Create web/src/components/ui/radio-group.tsx following the dialog.tsx shadcn pattern exactly:
    - "use client" directive at top
    - Import * as RadioGroupPrimitive from "@radix-ui/react-radio-group"
    - Import cn from "@/lib/utils"
    - Export named function RadioGroup wrapping RadioGroupPrimitive.Root with data-slot="radio-group" and className={cn("grid gap-2", className)}
    - Export named function RadioGroupItem wrapping RadioGroupPrimitive.Item with data-slot="radio-group-item"; className applies focus-visible ring, disabled opacity, data-[state=checked] border and bg using neutral-900 (light) / neutral-50 (dark); inside the Item render RadioGroupPrimitive.Indicator containing a span h-2 w-2 rounded-full bg-white dark:bg-neutral-950

    Create web/src/components/ui/switch.tsx following the same pattern:
    - "use client" directive at top
    - Import * as SwitchPrimitive from "@radix-ui/react-switch"
    - Import cn from "@/lib/utils"
    - Export named function Switch wrapping SwitchPrimitive.Root with data-slot="switch"; className: peer inline-flex h-5 w-9 shrink-0 cursor-pointer items-center rounded-full border-2 border-transparent transition-colors, focus-visible ring styles, disabled styles, data-[state=checked]:bg-neutral-900 data-[state=unchecked]:bg-neutral-200, dark equivalents
    - Inside Switch render SwitchPrimitive.Thumb with data-slot="switch-thumb"; className: pointer-events-none block h-4 w-4 rounded-full bg-white shadow-lg ring-0 transition-transform data-[state=checked]:translate-x-4 data-[state=unchecked]:translate-x-0 dark:bg-neutral-950

    The complete implementations are specified in 58-PATTERNS.md — use those exact className strings verbatim to avoid dark mode regressions.
  </action>
  <verify>
    <automated>cd web && pnpm build 2>&1 | grep -E "error|Error" | head -20</automated>
  </verify>
  <acceptance_criteria>
    - web/package.json contains "@radix-ui/react-radio-group" and "@radix-ui/react-switch" as dependencies
    - web/src/components/ui/radio-group.tsx exists and exports RadioGroup and RadioGroupItem
    - web/src/components/ui/switch.tsx exists and exports Switch
    - Both files start with "use client"
    - Both files import cn from "@/lib/utils"
    - pnpm build (from web/) exits 0 with no TypeScript errors in these two files
  </acceptance_criteria>
  <done>Two new component files committed; Radix packages in package.json; build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Extend types/clientes.ts and schemas/clientes.ts for Phase 57 fields</name>
  <files>web/src/types/clientes.ts, web/src/schemas/clientes.ts</files>
  <read_first>
    - web/src/types/clientes.ts (current interfaces: Cliente, ClienteCreateRequest, ClienteUpdateRequest, ClientesListFilters — read all lines)
    - web/src/schemas/clientes.ts (current Zod schema, optionalTrimmedString helper, superRefine block — read all lines)
    - .planning/phases/LEXCV-58-formulario-dinamico/58-PATTERNS.md (exact field additions, superRefine extension, Zod v4 notes)
  </read_first>
  <action>
    In web/src/types/clientes.ts:
    - Add two new interfaces at the top of the file (before the Cliente interface): DadosTipoParticular with optional fields idade?: number, sexo?: string, nacionalidade?: string; and DadosTipoEmpresa with optional fields nome_comercial?: string, sede?: string, representante_legal?: string, cargo?: string.
    - In the Cliente interface: change tipo?: string to tipo?: "PARTICULAR" | "EMPRESA"; add numero_cliente?: string (read-only, backend-generated); add avencado?: boolean; add dados_tipo?: DadosTipoParticular | DadosTipoEmpresa.
    - In ClienteCreateRequest: change tipo?: string to tipo?: "PARTICULAR" | "EMPRESA"; add avencado?: boolean; add dados_tipo?: DadosTipoParticular | DadosTipoEmpresa. Do NOT add numero_cliente — it is backend-generated and must never appear in create or update payloads.
    - In ClienteUpdateRequest: apply the same additions as ClienteCreateRequest.
    - In ClientesListFilters: the tipo?: string field stays as string (it is a query param filter value; callers will pass "PARTICULAR" or "EMPRESA" strings).

    In web/src/schemas/clientes.ts:
    - Replace the existing tipo field (currently optionalTrimmedString or similar) with: tipo: z.enum(["PARTICULAR", "EMPRESA"]).optional()
    - Add avencado: z.boolean().default(false)
    - Add dados_tipo as an optional z.object containing: idade (z.number().int().positive().optional()), sexo (optionalTrimmedString), nacionalidade (optionalTrimmedString), nome_comercial (optionalTrimmedString), sede (optionalTrimmedString), representante_legal (optionalTrimmedString), cargo (optionalTrimmedString) — then .optional() on the whole object.
    - Extend the existing superRefine block (do NOT replace it) to add two new checks after the existing documento_tipo check: when data.tipo === "EMPRESA", add issues for dados_tipo.nome_comercial and dados_tipo.representante_legal if either is missing or empty after trim, using path ["dados_tipo", "nome_comercial"] and ["dados_tipo", "representante_legal"] respectively.
  </action>
  <verify>
    <automated>cd web && pnpm build 2>&1 | grep -E "error TS|Type error" | head -20</automated>
  </verify>
  <acceptance_criteria>
    - web/src/types/clientes.ts contains interface DadosTipoParticular and interface DadosTipoEmpresa
    - web/src/types/clientes.ts Cliente interface contains numero_cliente?: string
    - web/src/types/clientes.ts ClienteCreateRequest does NOT contain numero_cliente (grep returns 0 matches in that interface)
    - web/src/schemas/clientes.ts contains z.enum(["PARTICULAR", "EMPRESA"])
    - web/src/schemas/clientes.ts contains dados_tipo
    - web/src/schemas/clientes.ts superRefine block contains data.tipo === "EMPRESA" conditional
    - pnpm build exits 0
  </acceptance_criteria>
  <done>Types and schema extended; all Phase 57 fields typed; no numero_cliente in request types; build passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Form → API | ClienteCreateRequest must never carry numero_cliente (backend assigns it) |
| npm registry | New Radix packages installed from npm registry |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-58-01 | Tampering | ClienteCreateRequest / ClienteUpdateRequest | mitigate | Enforce at type level: numero_cliente must not appear in create/update types; verified by acceptance criteria grep check |
| T-58-SC | Tampering | pnpm add @radix-ui/react-radio-group @radix-ui/react-switch | accept | Both packages are official Radix UI scoped packages published under @radix-ui org; verified by npmjs.com listing |
</threat_model>

<verification>
Run from web/ directory: pnpm build — must exit 0 with zero TypeScript errors in radio-group.tsx, switch.tsx, types/clientes.ts, schemas/clientes.ts.
</verification>

<success_criteria>
- @radix-ui/react-radio-group and @radix-ui/react-switch present in web/package.json
- radio-group.tsx and switch.tsx exist with correct exports and "use client" directive
- Cliente type has numero_cliente, avencado, dados_tipo, tipo as literal union
- No request type contains numero_cliente
- Zod schema validates EMPRESA required fields via superRefine
- pnpm build exits 0
</success_criteria>

<output>
Create `.planning/phases/LEXCV-58-formulario-dinamico/58-01-SUMMARY.md` when done
</output>
