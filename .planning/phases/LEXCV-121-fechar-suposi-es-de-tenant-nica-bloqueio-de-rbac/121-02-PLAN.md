---
phase: 121-fechar-suposi-es-de-tenant-nica-bloqueio-de-rbac
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/app/(dashboard)/settings/page.tsx
  - web/scripts/verify-bloqueio-rbac.mjs
  - web/package.json
autonomous: true
requirements: [ISOL-03]

must_haves:
  truths:
    - "Um ADMIN de escritorio abre Definicoes > Controlo de Acesso (RBAC) e nao ve nenhum botao Guardar Regras"
    - "No lugar do botao aparece um Badge neutro com cadeado e o texto Gerido pela Plataforma, legivel sem passar o rato"
    - "Passar o rato ou focar por teclado sobre esse Badge revela a explicacao completa num Tooltip"
    - "Durante a janela de carregamento do useMe(), o estado apresentado e o Badge — nunca um flash do botao"
    - "O separador RBAC continua visivel e a matriz de permissoes continua a renderizar e a alternar exatamente como antes"
  artifacts:
    - path: "web/src/app/(dashboard)/settings/page.tsx"
      provides: "RbacTab com o cabecalho condicional (Button para PLATAFORMA_ADMIN, Badge+Tooltip para todos os outros)"
      contains: "Gerido pela Plataforma"
    - path: "web/scripts/verify-bloqueio-rbac.mjs"
      provides: "Gate executavel de origem com 11 assercoes sobre RbacTab, incluindo as guardas de nao-regressao"
      min_lines: 120
      contains: "stripComments"
  key_links:
    - from: "web/src/app/(dashboard)/settings/page.tsx (RbacTab)"
      to: "hook useMe (cache partilhada [\"auth\",\"me\"])"
      via: "self-fetch dentro do sub-componente, como ja faz UserManagementTab"
      pattern: "useMe\\(\\)"
    - from: "web/package.json"
      to: "web/scripts/verify-bloqueio-rbac.mjs"
      via: "script npm verify:bloqueio-rbac"
      pattern: "verify:bloqueio-rbac"
---

<objective>
Retirar a acao de gravar RBAC da interface de qualquer `ADMIN` de escritorio, substituindo-a por um marcador estatico e auto-explicativo — satisfazendo o Criterio de Sucesso 4 da Phase 121 ("deixa de expor a acao de gravar... evitando um `403` confuso na UI").

Purpose: o Plan 01 fecha a fronteira real no backend. Sem esta alteracao, o botao "Guardar Regras" continuaria a existir e a produzir um `403` sem qualquer explicacao para o utilizador. O criterio esta escrito inteiramente como uma **remocao** — nao pede um ecra de edicao de RBAC para `PLATAFORMA_ADMIN`, e este plano deliberadamente nao cria nenhum (ver Deferred Ideas em `121-CONTEXT.md`).

Output: uma troca de render condicional dentro de `RbacTab`, zero imports novos, zero instalacoes shadcn novas, mais um gate executavel `pnpm verify:bloqueio-rbac` na linha dos ja existentes das Phases 118 e 120.
</objective>

<execution_context>
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@web/AGENTS.md
@.planning/phases/LEXCV-121-fechar-suposi-es-de-tenant-nica-bloqueio-de-rbac/121-UI-SPEC.md
@.planning/phases/LEXCV-121-fechar-suposi-es-de-tenant-nica-bloqueio-de-rbac/121-CONTEXT.md
@.planning/phases/LEXCV-121-fechar-suposi-es-de-tenant-nica-bloqueio-de-rbac/121-PATTERNS.md
</context>

<interfaces>
<!-- Contratos ja confirmados no planeamento por leitura direta do codigo. NAO redescobrir. -->

Fronteiras de funcao em `web/src/app/(dashboard)/settings/page.tsx` (1072 linhas no total, confirmado):
`export default function SettingsPage()` :51 · `function UserManagementTab(...)` :180 · `function RbacTab()` :760 · `function NotificationPreferencesTab()` :967

Estado atual do topo de `RbacTab` (`:760-772`) — repare que os **early returns comecam na linha 776**, pelo que qualquer hook novo tem de ficar acima deles:

```tsx
function RbacTab() {
  const { data: rbac, isLoading, isError, refetch } = useAdminRbac();
  type RolePermissionsMap = Record<MockRole, MockPermission[]>;

  const [localRolePermissions, setLocalRolePermissions] = React.useState<RolePermissionsMap | null>(null);
  const [saving, setSaving] = React.useState(false);
  const [success, setSuccess] = React.useState<string | null>(null);
  const [error, setError] = React.useState<string | null>(null);

  const effectiveRolePermissions =
    localRolePermissions ?? (rbac?.rolePermissions as RolePermissionsMap | undefined);

  if (isLoading) { /* ... */ }
```

Sitio exato da troca (`:865-876`, dentro do `CardHeader` `flex flex-col sm:flex-row sm:items-center sm:justify-between`):

```tsx
        <Button
          onClick={handleSave}
          disabled={saving}
          className="bg-blue-600 hover:bg-blue-700 text-white shadow-sm flex items-center gap-2 self-start sm:self-auto"
        >
          {saving ? (
            <Loader2 className="h-4 w-4 animate-spin" />
          ) : (
            <Save className="h-4 w-4" />
          )}
          Guardar Regras
        </Button>
```

Forma alvo (contrato literal de `121-UI-SPEC.md`, Interaction Note 3 — o `<Button>` em si nao muda, so passa a estar dentro de uma condicao):

```tsx
{isPlatformAdmin ? (
  <Button
    onClick={handleSave}
    disabled={saving}
    className="bg-blue-600 hover:bg-blue-700 text-white shadow-sm flex items-center gap-2 self-start sm:self-auto"
  >
    {saving ? <Loader2 className="h-4 w-4 animate-spin" /> : <Save className="h-4 w-4" />}
    Guardar Regras
  </Button>
) : (
  <Tooltip>
    <TooltipTrigger asChild>
      <span tabIndex={0} className="self-start sm:self-auto">
        <Badge variant="outline" className="gap-1">
          <Lock className="h-3 w-3" />
          Gerido pela Plataforma
        </Badge>
      </span>
    </TooltipTrigger>
    <TooltipContent>
      As regras de acesso por perfil (RBAC) passaram a ser uma configuração fixa e comum a
      toda a plataforma LexCV — já não podem ser alteradas a partir de um escritório individual.
    </TooltipContent>
  </Tooltip>
)}
```

Imports ja presentes no topo do ficheiro — **zero imports novos** (confirmado): `Lock` :6, `Save` :18, `Loader2` :14 (linha do bloco lucide-react `:4-22`); `useMe` :26; `Badge` :44; `Tooltip, TooltipContent, TooltipTrigger` :45.

`TooltipProvider` (`delayDuration={700}`) ja esta montado uma unica vez na raiz da app (`web/src/app/providers.tsx`) — nao adicionar outro.

`Role` (`web/src/types/auth.ts:2`) ja inclui `"PLATAFORMA_ADMIN"` desde a Phase 120 — nenhuma alteracao de tipos nesta fase.

`useMe()` (`web/src/hooks/use-me.ts`) devolve um resultado TanStack Query com `queryKey: ["auth","me"]` e `staleTime: 60_000` — chamar dentro de `RbacTab` faz dedupe pela cache partilhada, nao e um segundo pedido de rede. Precedente no mesmo ficheiro: `UserManagementTab` (`:196-201`) ja faz exatamente isto.

Idiomas de papel confirmados: `dashboard-shell.tsx:91` (`me.data?.roles?.includes("PLATAFORMA_ADMIN") ?? false`) e `plataforma/page.tsx:76-89` (guarda `if (!me.isFetched) return null;` **antes** da verificacao de papel, correcao WR-03 da Phase 120 contra fail-open na janela de carregamento).

`<RbacTab />` e invocado sem props (`settings/page.tsx:163`), gated pelo pai por `activeTab === "rbac" && hasRbacManage` — nao ha call site a alterar.

Convencao de gate executavel ja estabelecida (`web/scripts/verify-limite-utilizadores-indicator.mjs` Phase 118, `web/scripts/verify-consola-tenants.mjs` Phase 120): Node puro, zero dependencias, `stripComments()` obrigatorio antes de qualquer assercao, localizacao de bloco por marcador, `console.log("PASS <id>")` / `console.log("FAIL <id> — <motivo>")` por assercao, `process.exit(failures === 0 ? 0 : 1)`.

Comandos (CLAUDE.md): `cd web && pnpm lint`, `pnpm build`, `pnpm verify:<nome>`. **pnpm, nunca npm** — `pnpm-lock.yaml` e a fonte de verdade; ignorar o `package-lock.json` obsoleto.

Next.js 16: antes de escrever qualquer codigo de framework (routing/data APIs), consultar `web/node_modules/next/dist/docs/`. Esta alteracao e puramente de componente cliente (`"use client"` ja no topo do ficheiro), pelo que nao toca em APIs de framework.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Trocar o botao Guardar Regras por Badge + Tooltip para quem nao e PLATAFORMA_ADMIN</name>

  <files>
    web/src/app/(dashboard)/settings/page.tsx
  </files>

  <read_first>
    - `web/src/app/(dashboard)/settings/page.tsx` — o bloco de imports (`:1-46`), `UserManagementTab`'s `useMe()` self-fetch e o comentario que o justifica (`:196-201`), e `RbacTab` inteiro (`:760-965`), com especial atencao aos tres early returns (`isLoading`, `isError`, `!effectiveRolePermissions`) que comecam na linha 776.
    - `.planning/phases/LEXCV-121-fechar-suposi-es-de-tenant-nica-bloqueio-de-rbac/121-UI-SPEC.md` — Interaction Notes 1 a 8 (a expressao condicional, o self-fetch, o JSX alvo literal, a obrigatoriedade do wrapper `<span tabIndex={0}>`, os imports zero, os non-goals) e a seccao Copywriting Contract (as duas frases exatas).
    - `web/src/app/(dashboard)/plataforma/columns.tsx` linhas 61-82 e 139-143 — a composicao `Tooltip > TooltipTrigger asChild > <span tabIndex={0}>` e o `<Badge variant="outline">` que servem de precedente direto.
    - `web/src/components/shared/dashboard-shell.tsx` linhas 85-91 — o idioma de verificacao de papel e o comentario que enquadra este tipo de gate como espelho de UX, nunca como fronteira de autorizacao.
    - `web/src/app/(dashboard)/plataforma/page.tsx` linhas 76-89 — a guarda `!me.isFetched` (correcao WR-03).
  </read_first>

  <action>
    Dentro de `RbacTab` (e so ai), acrescentar `const me = useMe();` **imediatamente a seguir** a linha `const { data: rbac, isLoading, isError, refetch } = useAdminRbac();`, e logo abaixo `const isPlatformAdmin = me.isFetched && (me.data?.roles?.includes("PLATAFORMA_ADMIN") ?? false);`.

    A posicao e obrigatoria, nao estetica: `RbacTab` tem tres early returns (`isLoading`, `isError`, `!effectiveRolePermissions`) e um hook chamado depois de qualquer um deles viola as Rules of Hooks e quebra a ordem de hooks entre renders. `useMe()` tem de ficar acima do primeiro `if (isLoading)`.

    Nomear a variavel `me` (nao desestruturar para `{ data: me }` como faz `UserManagementTab`), porque a expressao precisa tambem de `me.isFetched`. Nao ha colisao — `RbacTab` nao tem hoje nenhuma variavel `me`.

    Envolver o `<Button onClick={handleSave} ...>` existente do `CardHeader` numa expressao ternaria sobre `isPlatformAdmin`, exatamente na forma dada em `<interfaces>` (contrato literal do UI-SPEC). O `<Button>` mantem-se identico: mesmo `onClick`, mesmo `disabled={saving}`, mesmas classes, mesmo conteudo. A unica liberdade e colapsar o ternario interno `{saving ? ... : ...}` para uma linha, como o UI-SPEC mostra.

    O ramo alternativo e um `Tooltip` cujo `TooltipTrigger asChild` envolve `<span tabIndex={0} className="self-start sm:self-auto">` a conter um `<Badge variant="outline" className="gap-1">` com `<Lock className="h-3 w-3" />` e o texto `Gerido pela Plataforma`. O `TooltipContent` leva a frase exata da tabela de Copywriting do UI-SPEC, sem reformular uma unica palavra.

    O wrapper `<span tabIndex={0}>` **nao e opcional**: um `Badge` renderiza um `<span>` sem tab stop nativo, logo sem o wrapper o Tooltip so abriria com rato e nunca com teclado. E a mesma tecnica das Phases 118/120 por uma razao diferente (la contornava `disabled:pointer-events-none` de um `<Button>` desativado; aqui fornece a acessibilidade por teclado que nao existe de raiz). O `className="self-start sm:self-auto"` vem do `<Button>` para o alinhamento em `flex-col` no mobile ficar identico.

    Non-goals explicitos — **nao tocar**:
    - `hasRbacManage` (`:58`, `can.manage("rbac") || isAdmin`) fica exatamente igual: o `ADMIN` de escritorio continua a ver o separador e a matriz em modo leitura. Alargar a visibilidade a `PLATAFORMA_ADMIN` esta fora de ambito por decisao explicita.
    - `handleCheckboxChange` e o seu `if (role === "ADMIN") return;` ficam iguais — nenhum `disabled` novo e adicionado a matriz, mesmo sabendo que um `ADMIN` de escritorio ja nao consegue persistir um toggle (limitacao conhecida e aceite, UI-SPEC Interaction Note 8).
    - `handleSave`, `saving`, `success`, `error` e os banners `{success && ...}` / `{error && ...}` ficam iguais — passam a ser inalcancaveis pela UI para um `ADMIN` de escritorio, o que e o efeito pretendido; nao apagar nem reescrever.
    - `useAdminRbac()` e o caminho de leitura `GET /admin/rbac` ficam iguais.
    - O banner "Nota Importante" (`ShieldAlert`, imutabilidade da linha ADMIN) fica igual — e outro assunto; nao fundir a explicacao nova nele.
    - Nenhum import novo, nenhum componente shadcn novo instalado, nenhum `TooltipProvider` novo.

    Acrescentar um comentario curto acima da derivacao de `isPlatformAdmin` enquadrando-a como espelho de UX (a fronteira real e a anotacao do backend, Plan 01), na linha do comentario de `dashboard-shell.tsx:85-90`. **Higiene de gate:** esse comentario nao pode conter as strings literais `Gerido pela Plataforma` nem `isPlatformAdmin ?` — o gate da Task 2 remove comentarios antes de asserir, mas manter a disciplina evita ambiguidade em qualquer grep manual.

    Correr `cd web && pnpm lint` e `pnpm build`; ambos tem de terminar limpos.
  </action>

  <verify>
    <automated>cd web && pnpm lint && pnpm build && grep -q 'const isPlatformAdmin = me.isFetched' "src/app/(dashboard)/settings/page.tsx" && grep -q 'Gerido pela Plataforma' "src/app/(dashboard)/settings/page.tsx" && grep -q 'const hasRbacManage = can.manage("rbac") || isAdmin;' "src/app/(dashboard)/settings/page.tsx"</automated>
  </verify>

  <acceptance_criteria>
    - Em `RbacTab`, `const me = useMe();` aparece **antes** de `if (isLoading)` — nenhum hook e chamado depois de um early return.
    - Existe exatamente uma linha `const isPlatformAdmin = me.isFetched && (me.data?.roles?.includes("PLATAFORMA_ADMIN") ?? false);`.
    - O texto `Guardar Regras` aparece no ficheiro **depois** da primeira ocorrencia de `isPlatformAdmin ?` — ou seja, o botao esta dentro do ramo verdadeiro do ternario.
    - O ramo falso contem, por esta ordem: `<Tooltip>`, `<TooltipTrigger asChild>`, `<span tabIndex={0}`, `<Badge variant="outline"`, `<Lock className="h-3 w-3" />` e o texto `Gerido pela Plataforma`.
    - O `TooltipContent` contem a frase exata do UI-SPEC (comparada apos normalizacao de espacos): "As regras de acesso por perfil (RBAC) passaram a ser uma configuração fixa e comum a toda a plataforma LexCV — já não podem ser alteradas a partir de um escritório individual."
    - `git diff` de `settings/page.tsx` nao mostra alteracoes fora de `RbacTab` — em particular, a linha 58 (`hasRbacManage`) e o bloco de imports (`:1-46`) ficam com zero alteracoes.
    - `handleSave`, `saving`, `success`, `error`, os dois banners e o `if (role === "ADMIN") return;` de `handleCheckboxChange` continuam presentes e inalterados.
    - `cd web && pnpm lint` e `cd web && pnpm build` terminam ambos com exit 0.
  </acceptance_criteria>

  <done>Um `ADMIN` de escritorio deixa de ter qualquer botao de gravar no separador RBAC e passa a ver um Badge neutro explicado por Tooltip; tudo o resto do separador esta bit-a-bit na mesma.</done>
</task>

<task type="auto">
  <name>Task 2: Gate executavel pnpm verify:bloqueio-rbac com 11 assercoes de origem</name>

  <files>
    web/scripts/verify-bloqueio-rbac.mjs (novo), web/package.json
  </files>

  <read_first>
    - `web/scripts/verify-consola-tenants.mjs` — o cabecalho que declara explicitamente **o que o gate nao consegue provar**, a implementacao de `stripComments()`, a tecnica de localizacao de bloco por marcador, a forma da lista `assertions` (`{ id, descricao, predicate }`) e o loop final de impressao/exit.
    - `web/scripts/verify-limite-utilizadores-indicator.mjs` — a variante mais curta do mesmo padrao (Phase 118), util para calibrar o tamanho.
    - `web/package.json` — o bloco `scripts` com as tres entradas `verify:*` ja existentes, para acrescentar a quarta com a mesma forma.
    - `web/src/app/(dashboard)/settings/page.tsx` — o resultado da Task 1, que este gate afere.
  </read_first>

  <action>
    Criar `web/scripts/verify-bloqueio-rbac.mjs` seguindo verbatim o estilo dos dois gates existentes: Node puro (`node:fs/promises`, `node:path`, `node:url`), zero dependencias, sem import de modulo TypeScript, sem type-stripping — le o ficheiro como texto.

    Cabecalho do ficheiro: comentario a declarar que prova as assercoes de origem do bloqueio de RBAC (Phase 121, ISOL-03, Plan 02) e, explicitamente, **o que nao consegue provar** e fica para o checkpoint humano do Plan 04: que o Tooltip realmente aparece ao passar o rato e ao focar por teclado, que o Badge renderiza com as cores neutras certas no browser, e que o backend devolve mesmo `403` a um `ADMIN` de escritorio.

    Copiar `stripComments()` de `verify-consola-tenants.mjs` sem alteracoes — sem ele, o proprio cabecalho ou qualquer comentario do codigo-fonte que mencione um dos tokens procurados tornaria o gate auto-invalidante.

    Extrair o bloco de `RbacTab`: indice de `function RbacTab()` no ficheiro ja sem comentarios, ate ao proximo `\nfunction ` a seguir a esse indice (na pratica `function NotificationPreferencesTab()`); falhar com mensagem clara se qualquer um dos dois marcadores nao for encontrado.

    Definir estas 11 assercoes, cada uma com `id`, `descricao` e `predicate`:

    - `A01-usemme-dentro-do-rbactab` — o bloco de `RbacTab` contem `useMe()`.
    - `A02-hook-antes-do-primeiro-early-return` — no bloco, o indice de `useMe()` e menor que o indice de `if (isLoading)`. (Regra dos Hooks: a violacao mais provavel desta alteracao.)
    - `A03-isplatformadmin-com-isfetched-e-papel` — existe uma linha que comeca por `const isPlatformAdmin` e que contem simultaneamente `me.isFetched` e `roles?.includes("PLATAFORMA_ADMIN")`.
    - `A04-isfetched-antes-do-papel` — nessa mesma linha, o indice de `me.isFetched` e menor que o indice de `PLATAFORMA_ADMIN` (fail-closed durante o carregamento: o estado por omissao e o Badge, nunca o botao).
    - `A05-guardar-regras-sob-condicao` — no bloco, o indice de `isPlatformAdmin ?` e menor que o indice de `Guardar Regras`.
    - `A06-badge-outline-com-rotulo` — no bloco, `variant="outline"` e `Gerido pela Plataforma` existem ambos, e o indice de `Gerido pela Plataforma` e maior que o de `isPlatformAdmin ?`.
    - `A07-span-tabindex-dentro-do-tooltiptrigger` — no bloco, os indices de `TooltipTrigger asChild`, `<span tabIndex={0}` e `<Badge` aparecem por essa ordem estrita.
    - `A08-texto-exato-do-tooltip` — o bloco, com espacos normalizados (`replace(/\s+/g, " ")`), contem a frase exata do UI-SPEC, tambem ela normalizada, declarada como constante no topo do script.
    - `A09-hasrbacmanage-inalterado` — o ficheiro completo (sem comentarios) contem `const hasRbacManage = can.manage("rbac") || isAdmin;`. Guarda de nao-regressao para o non-goal mais facil de violar por "coerencia".
    - `A10-handlesave-e-leitura-intactos` — o bloco contem `apiFetch("/admin/rbac"`, `method: "PUT"` e `useAdminRbac()`.
    - `A11-matriz-admin-imutavel-inalterada` — o bloco contem `if (role === "ADMIN") return;`.

    Registar o script em `web/package.json` como `"verify:bloqueio-rbac": "node scripts/verify-bloqueio-rbac.mjs"`, imediatamente a seguir a `verify:consola-tenants`, mantendo a formatacao existente do bloco `scripts`. Nao alterar mais nada no `package.json` — zero dependencias novas.

    Correr `cd web && pnpm verify:bloqueio-rbac` e confirmar 11 linhas `PASS` e exit 0.

    Prova de que o gate tem dentes (nao substituivel por inspecao visual): reverter temporariamente **uma** alteracao da Task 1 — trocar `{isPlatformAdmin ? (` por um render incondicional do `<Button>` — correr o gate, confirmar que falha em `A05` (e provavelmente noutras), e repor de imediato o estado correto. Registar por escrito o resultado dessa negativa no SUMMARY. Terminar com `git diff` a mostrar apenas as alteracoes pretendidas.
  </action>

  <verify>
    <automated>cd web && pnpm verify:bloqueio-rbac && test $(pnpm -s verify:bloqueio-rbac | grep -c '^PASS ') -eq 11 && pnpm lint</automated>
  </verify>

  <acceptance_criteria>
    - `web/scripts/verify-bloqueio-rbac.mjs` existe, nao importa nenhuma dependencia externa (`grep -c "from \"" ` so encontra imports `node:`), e contem `stripComments`.
    - `cd web && pnpm verify:bloqueio-rbac` imprime exatamente 11 linhas comecadas por `PASS ` e nenhuma `FAIL `, com exit code 0.
    - Os ids impressos sao `A01-` a `A11-` conforme a lista da `<action>`, cada um com a `descricao` correspondente disponivel na mensagem de falha.
    - `web/package.json` ganhou exatamente uma linha nova no bloco `scripts` (`verify:bloqueio-rbac`); as seccoes `dependencies` e `devDependencies` estao inalteradas (`git diff web/package.json` mostra 1 linha adicionada, 0 removidas fora da virgula de formatacao).
    - A prova negativa foi executada: com o render do botao tornado incondicional, o gate falha em `A05`; o estado correto foi reposto e o gate volta a 11 `PASS`. O resultado esta registado no SUMMARY.
    - `cd web && pnpm lint` termina com exit 0.
    - `git status --porcelain` lista apenas `web/src/app/(dashboard)/settings/page.tsx`, `web/scripts/verify-bloqueio-rbac.mjs` e `web/package.json` (mais ficheiros `.planning/`).
  </acceptance_criteria>

  <done>Existe um gate executavel de 11 assercoes que falha se o botao voltar a ser incondicional, se o hook subir para depois de um early return, se o texto do Badge/Tooltip divergir do UI-SPEC, ou se qualquer um dos tres non-goals for violado.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser (`RbacTab`) -> `PUT /api/v1/admin/rbac` | O gate de papel adicionado aqui vive no cliente e e trivialmente contornavel (curl, devtools) — nunca e a fronteira de autorizacao |
| `GET /auth/me` -> `me.roles` no cliente | Dados de papel ja servidos hoje a todos os utilizadores autenticados; esta alteracao apenas os le |
| Janela de carregamento do `useMe()` -> estado renderizado | Uma condicao mal escrita pode falhar aberta durante essa janela |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-121-07 | Elevation of Privilege | Verificacao de papel no cliente ser confundida com autorizacao | mitigate | A fronteira real e a anotacao de metodo do Plan 01 em `AdminController.updateRbac`; um `PUT` direto por curl continua a receber `403` mesmo com o botao escondido. O comentario exigido pela `<action>` enquadra explicitamente esta condicao como espelho de UX, na linha do comentario ja existente em `dashboard-shell.tsx:85-90`, e o Plan 04 confirma o `403` ao vivo |
| T-121-08 | Information Disclosure | Leitura de `me.roles` no browser | accept | `GET /auth/me` ja serve `roles` a todos os utilizadores autenticados desde antes desta milestone; zero campos novos, zero pedidos novos (dedupe pela cache `["auth","me"]`) |
| T-121-09 | Elevation of Privilege | Fail-open durante o carregamento de `useMe()` — flash do botao antes de `me` resolver | mitigate | `me.isFetched &&` como primeiro operando torna o estado por omissao o Badge; assercao `A04` do gate verifica a ordem dos operandos. Mesmo padrao da correcao WR-03 da Phase 120 em `plataforma/page.tsx` |
| T-121-10 | Tampering | "Coerencia" levar a alargar `hasRbacManage` ou a desativar a matriz, saindo do ambito bloqueado por `121-CONTEXT.md` | mitigate | Non-goals explicitos na `<action>` e assercoes `A09`/`A11` do gate a falhar se `hasRbacManage` ou a guarda `role === "ADMIN"` da matriz mudarem |
| T-121-11 | Repudiation | Declarar o gate verde quando as assercoes nao testam nada de util | mitigate | Prova negativa obrigatoria (tornar o botao incondicional -> `A05` falha), registada no SUMMARY; `stripComments()` impede que texto de comentario satisfaca qualquer assercao |
| T-121-12 | Denial of Service | Hook adicionado depois de um early return partir `RbacTab` em runtime com erro de ordem de hooks | mitigate | Posicao obrigatoria declarada na `<action>`, assercao `A02` do gate, e `pnpm build` como verificacao de compilacao |
| T-121-SC | Tampering | Instalacoes npm/pnpm | n/a | Zero dependencias novas e zero instalacoes shadcn: `Badge`, `Tooltip*`, `Lock` e `useMe` ja estao importados no ficheiro (UI-SPEC Interaction Note 5). Apenas uma linha de `scripts` e acrescentada ao `package.json`. O Package Legitimacy Gate nao se aplica |
</threat_model>

<verification>
1. `cd web && pnpm verify:bloqueio-rbac` — 11 `PASS`, exit 0.
2. `cd web && pnpm lint` — exit 0.
3. `cd web && pnpm build` — exit 0.
4. `grep -q 'const hasRbacManage = can.manage("rbac") || isAdmin;' "web/src/app/(dashboard)/settings/page.tsx"` — non-goal preservado.
5. `git diff web/package.json` — apenas a linha `verify:bloqueio-rbac`.
6. Prova negativa do gate registada (botao incondicional -> `A05` falha).
</verification>

<success_criteria>
- ROADMAP Phase 121, Criterio de Sucesso 4 (o separador RBAC deixa de expor a acao de gravar a um `ADMIN` de tenant, evitando um `403` confuso) esta satisfeito ao nivel do codigo, com um marcador visivel a explicar porque — nao com um espaco vazio nem com um botao desativado.
- Nenhum ecra novo de edicao de RBAC para `PLATAFORMA_ADMIN` foi criado, e a visibilidade do separador nao foi alargada — os dois itens explicitamente diferidos por `121-CONTEXT.md`.
- O contrato visual de `121-UI-SPEC.md` (Badge `outline` neutro, `Lock` `h-3 w-3`, `gap-1`, wrapper `<span tabIndex={0}>`, as duas frases exatas) e verificavel por comando, nao por leitura.
</success_criteria>

<output>
Criar `.planning/phases/LEXCV-121-fechar-suposi-es-de-tenant-nica-bloqueio-de-rbac/121-02-SUMMARY.md` quando terminar, incluindo o resultado da prova negativa do gate e a limitacao conhecida e aceite (um `ADMIN` de escritorio ainda consegue alternar checkboxes em estado local sem forma de os persistir — UI-SPEC Interaction Note 8, deliberadamente fora de ambito).
</output>
