---
phase: 121-fechar-suposi-es-de-tenant-nica-bloqueio-de-rbac
plan: 03
type: execute
wave: 2
depends_on: [121-01]
files_modified:
  - .planning/phases/LEXCV-121-fechar-suposi-es-de-tenant-nica-bloqueio-de-rbac/121-ISOL-AUDIT.md
autonomous: true
requirements: [ISOL-01, ISOL-02]

must_haves:
  truths:
    - "GET /api/v1/public/branding devolve sempre a marca generica LexCV, sem ler qualquer Tenant, confirmado por teste verde e nao apenas por leitura"
    - "findFirstByOrderByCreatedAtAsc nao existe em nenhum codigo vivo do repositorio — os unicos vestigios sao texto de comentario"
    - "Uma varredura reproduzivel por comando cobre todos os padroes de resolucao-de-tenant-por-heuristica e atribui um veredito explicito a cada superficie"
    - "O padrao adjacente findByXxxId-sem-tenantId fica registado como contexto de fundo, explicitamente NAO como achado de ISOL-02, com dono nomeado (Phase 123)"
    - "A Phase 123 (ISOL-04) consegue citar este ficheiro em vez de repetir a varredura de raiz"
  artifacts:
    - path: ".planning/phases/LEXCV-121-fechar-suposi-es-de-tenant-nica-bloqueio-de-rbac/121-ISOL-AUDIT.md"
      provides: "Registo de auditoria ISOL-01/ISOL-02 com tabela de veredito por superficie e comandos de reproducao"
      min_lines: 60
      contains: "COVERED"
  key_links:
    - from: ".planning/phases/LEXCV-121-fechar-suposi-es-de-tenant-nica-bloqueio-de-rbac/121-ISOL-AUDIT.md"
      to: ".planning/milestones/v2.11-phases/LEXCV-97-auditoria-de-milestone-d-vida-t-cnica-e-uat-pendente/97-01-SUMMARY.md"
      via: "reutilizacao do formato de tabela de veredito da AUD-01"
      pattern: "Query"
    - from: ".planning/phases/LEXCV-121-fechar-suposi-es-de-tenant-nica-bloqueio-de-rbac/121-ISOL-AUDIT.md"
      to: "backend/src/main/java/com/lexcv/controllers/AdminController.java"
      via: "linha de veredito sobre Role/Permission a citar o gate de metodo do Plan 01"
      pattern: "PLATAFORMA_ADMIN"
---

<objective>
Fechar ISOL-01 e ISOL-02 pela unica via que ambos precisam: evidencia registada, nao codigo novo. ISOL-01 ja foi satisfeito como efeito colateral da correcao CR-02 da Phase 119; ISOL-02 foi varrido por pesquisa dedicada que encontrou zero lacunas. O que falta e o **registo auditavel** de ambos.

Purpose: o Criterio de Sucesso 2 desta fase exige literalmente "uma pesquisa dedicada ao codigo de producao... confirma que nenhum outro caminho resolve 'a' tenant por heuristica". Uma afirmacao dessas so vale se for reproduzivel: a Phase 123 (ISOL-04) tem por mandato auditar exatamente estas superficies e deve poder citar este ficheiro, tal como o fecho de milestone da v2.11 cita hoje `97-01-SUMMARY.md`. Este plano re-corre a varredura com comandos explicitos (nao copia conclusoes de `121-CONTEXT.md`) e produz o artefacto.

Output: `121-ISOL-AUDIT.md`, com veredito por superficie no formato de tabela ja estabelecido pela AUD-01, os comandos de reproducao, e o registo explicito de dois itens de contexto que **nao** sao achados desta fase.
</objective>

<execution_context>
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/LEXCV-121-fechar-suposi-es-de-tenant-nica-bloqueio-de-rbac/121-CONTEXT.md
@.planning/phases/LEXCV-121-fechar-suposi-es-de-tenant-nica-bloqueio-de-rbac/121-PATTERNS.md
@.planning/phases/LEXCV-121-fechar-suposi-es-de-tenant-nica-bloqueio-de-rbac/121-01-SUMMARY.md
@.planning/milestones/v2.11-phases/LEXCV-97-auditoria-de-milestone-d-vida-t-cnica-e-uat-pendente/97-01-SUMMARY.md
@.planning/research/PITFALLS.md
</context>

<interfaces>
<!-- Factos ja confirmados no planeamento. Reconfirmar por comando, nunca assumir. -->

Estado atual de `backend/src/main/java/com/lexcv/controllers/PublicController.java` (ficheiro completo, 42 linhas): classe sem construtor, sem campos, sem `@RequiredArgsConstructor`, sem nenhuma referencia a repositorio. O unico handler devolve `TenantPublicInfoResponse.builder().nome("LexCV").logoDataUrl(null).build()`. O docblock de classe ja diz por palavras suas *"CR-02 (119-REVIEW.md) / ISOL-01 (Phase 121, ja satisfeito por esta correcao)"*.

`backend/src/test/java/com/lexcv/controllers/PublicControllerTest.java` (53 linhas): 2 testes, `getBranding_devolveSempreAMarcaGenericaLexCV` e `getBranding_devolveSempreAMesmaRespostaIndependentementeDoEstado`, nenhum deles a mockar qualquer repositorio.

`SecurityConfig.authorizeHttpRequests` mantem `/api/v1/public/branding` em `permitAll()` — endpoint publico por design, e o motivo pelo qual ISOL-01 importa.

Formato de tabela de veredito a reutilizar (`97-01-SUMMARY.md:71-90`, AUD-01 "Tenant-Isolation Audit of Notification Surfaces"):

```markdown
| Query / Guard | Scope confirmed | Verdict |
|---|---|---|
| `<simbolo ou ficheiro:linha>` | `<como o ambito e garantido>` | COVERED (`<porque>`) |
```

Frontmatter a adaptar (mesma forma de `97-01-SUMMARY.md:1-39`):

```yaml
---
phase: 121-fechar-suposi-es-de-tenant-nica-bloqueio-de-rbac
plan: 03
subsystem: api
tags: [tenant-isolation, security-audit, rbac]
requirements-completed: [ISOL-01, ISOL-02]
---
```

Item de fundo a citar sem corrigir (`.planning/research/PITFALLS.md`, Pitfall 1): `ProcessoRepository.findByClienteId(UUID)` e assinaturas `findByXxxId`-sem-`tenantId` equivalentes (~11 metodos) sao seguras hoje apenas porque cada call site revalida separadamente o tenant da entidade-pai. Concern real, IDOR-adjacente, anterior a v2.16 por varias milestones, e **nao** newly risky por causa de um 2o tenant pagante — ao contrario de ISOL-03. Dono: Phase 123 (ISOL-04).

Assimetria observada a registar (consequencia intencional da decisao de `121-CONTEXT.md` de nao tocar no `GET`): apos o Plan 01, um `PLATAFORMA_ADMIN` **passa** o gate de `PUT /api/v1/admin/rbac` mas continua a receber `403` em `GET /api/v1/admin/rbac` (que so tem o gate de classe `hasRole('ADMIN')`). Nao e defeito nem achado — e o resultado literal dos criterios de sucesso, que nomeiam so o `PUT`. Registar como facto para a Phase 123 nao o redescobrir como surpresa.

Comandos (CLAUDE.md): `cd backend && mvn test -Dtest=PublicControllerTest`.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Confirmar ISOL-01 por execucao, nao por leitura</name>

  <files>
    (nenhum ficheiro alterado — recolha de evidencia)
  </files>

  <read_first>
    - `backend/src/main/java/com/lexcv/controllers/PublicController.java` — ficheiro completo (42 linhas), incluindo o docblock de classe.
    - `backend/src/test/java/com/lexcv/controllers/PublicControllerTest.java` — ficheiro completo (53 linhas), os 2 testes e o que cada um afirma.
    - `backend/src/main/java/com/lexcv/config/SecurityConfig.java` — o bloco `authorizeHttpRequests`, para confirmar que `/api/v1/public/branding` continua em `permitAll()`.
    - `.planning/phases/LEXCV-121-fechar-suposi-es-de-tenant-nica-bloqueio-de-rbac/121-PATTERNS.md` — Pattern Assignment 4, que descreve esta task como confirmacao pura, com zero diff esperado.
  </read_first>

  <action>
    Recolher quatro pecas de evidencia independentes para ISOL-01, cada uma por comando e com o output registado literalmente (nao parafraseado):

    1. Correr `cd backend && mvn test -Dtest=PublicControllerTest`. Registar o numero de testes corridos e o resultado. Esperado: 2 testes, 0 falhas.
    2. Correr uma pesquisa em todo o repositorio por `findFirstByOrderByCreatedAtAsc`. Esperado: exatamente 2 ocorrencias, ambas em texto de comentario/Javadoc (`PublicController.java` e `PublicControllerTest.java`), zero em codigo vivo. Registar ficheiro e linha de cada ocorrencia e classificar cada uma como comentario ou codigo.
    3. Confirmar por pesquisa que `PublicController.java` nao contem nenhuma das strings `Repository`, `@RequiredArgsConstructor` ou `private final` fora de comentarios — isto e, que a classe nao tem nenhuma dependencia injetada capaz de resolver uma tenant.
    4. Confirmar que `TenantRepository.java` nao declara nenhum metodo `findFirstBy`/`findTopBy` — a assinatura removida nao voltou por outro nome.

    Se qualquer uma das quatro divergir do esperado, **parar e reportar** em vez de corrigir: ISOL-01 esta declarado como ja fechado; uma divergencia significa que a premissa desta fase esta errada e o orquestrador tem de decidir, nao este plano.

    Proibicoes: NAO alterar `PublicController.java` nem `PublicControllerTest.java`. NAO acrescentar testes novos. NAO reescrever o docblock. O diff esperado desta task e literalmente vazio no codigo.
  </action>

  <verify>
    <automated>cd backend && mvn test -Dtest=PublicControllerTest && test $(grep -rn 'findFirstByOrderByCreatedAtAsc' --include=*.java src | wc -l) -eq 2 && test $(grep -c 'Repository' src/main/java/com/lexcv/controllers/PublicController.java) -eq 0 && test $(grep -cE 'findFirstBy|findTopBy' src/main/java/com/lexcv/repositories/TenantRepository.java) -eq 0</automated>
  </verify>

  <acceptance_criteria>
    - `cd backend && mvn test -Dtest=PublicControllerTest` termina com exit 0 e `Tests run: 2, Failures: 0, Errors: 0`.
    - A pesquisa por `findFirstByOrderByCreatedAtAsc` em `backend/src` devolve exatamente 2 linhas, ambas identificadas por ficheiro:linha e classificadas explicitamente como texto de comentario.
    - `grep -c 'Repository' backend/src/main/java/com/lexcv/controllers/PublicController.java` devolve `0`.
    - `grep -cE 'findFirstBy|findTopBy' backend/src/main/java/com/lexcv/repositories/TenantRepository.java` devolve `0`.
    - `git status --porcelain` nao lista nenhum ficheiro sob `backend/` — esta task nao altera codigo.
    - Os quatro outputs estao registados literalmente para reutilizacao na Task 2.
  </acceptance_criteria>

  <done>ISOL-01 esta confirmado por execucao de teste e por quatro pesquisas independentes, com output registado — nao por leitura de um docblock que se auto-declara satisfeito.</done>
</task>

<task type="auto">
  <name>Task 2: Re-correr a varredura ISOL-02 e escrever 121-ISOL-AUDIT.md</name>

  <files>
    .planning/phases/LEXCV-121-fechar-suposi-es-de-tenant-nica-bloqueio-de-rbac/121-ISOL-AUDIT.md (novo)
  </files>

  <read_first>
    - `.planning/milestones/v2.11-phases/LEXCV-97-auditoria-de-milestone-d-vida-t-cnica-e-uat-pendente/97-01-SUMMARY.md` — o frontmatter (`:1-39`) e as tabelas de veredito (`:71-90`); e este o formato a reproduzir, nao a reinventar.
    - `.planning/research/PITFALLS.md`, Pitfall 1 completo — para citar com exatidao o que fica de fora e porque.
    - `.planning/phases/LEXCV-121-fechar-suposi-es-de-tenant-nica-bloqueio-de-rbac/121-01-SUMMARY.md` — o estado final do gate de metodo, para a linha de veredito sobre `Role`/`Permission` poder afirmar o bloqueio como facto de origem observado e nao como intencao.
    - `backend/src/main/java/com/lexcv/jobs/AlertasDiariosJob.java` — o docblock de classe (`:31-37`), que ja explica porque a iteracao cross-tenant deste job e por design.
    - `backend/src/main/java/com/lexcv/controllers/PlatformAdminController.java` — o cabecalho e a anotacao de classe, para citar o gate correto.
  </read_first>

  <action>
    Re-correr a varredura de ISOL-02 com comandos explicitos sobre `backend/src/main/java` e `web/src` — **nao** copiar as conclusoes de `121-CONTEXT.md`; o valor deste artefacto e ser reproduzivel. No minimo estes padroes, cada um com o output completo examinado linha a linha:

    - `findFirstBy` / `findTopBy` (resolucao "a mais antiga/primeira")
    - `.findAll()` (iteracao cross-tenant)
    - `.get(0)` e `stream().findFirst()` (primeiro elemento de uma lista como se fosse "a" tenant)
    - a string literal `"LexCV"` (referencia a tenant reservada)
    - `findFirstByOrderByCreatedAtAsc` no repositorio inteiro (ja recolhido na Task 1, reutilizar)

    Para **cada** ocorrencia devolvida, atribuir um dos tres desfechos e nunca deixar nenhuma sem desfecho: (a) resolucao legitima da tenant reservada por nome exato; (b) tabela/entidade global de plataforma por design; (c) iteracao cross-tenant deliberada e ja revista, com o `tenantId` como parametro explicito de ciclo e nunca adivinhado. Qualquer ocorrencia que nao caiba em nenhum dos tres e um achado real — nesse caso **parar e reportar ao orquestrador**, porque significaria que a premissa "zero lacunas" de `121-CONTEXT.md` esta errada e este plano nao tem mandato para escrever codigo.

    Escrever `.planning/phases/LEXCV-121-fechar-suposi-es-de-tenant-nica-bloqueio-de-rbac/121-ISOL-AUDIT.md` com esta estrutura:

    - Frontmatter na forma dada em `<interfaces>` (`requirements-completed: [ISOL-01, ISOL-02]`).
    - Seccao **ISOL-01 — confirmacao de regressao**: as quatro evidencias da Task 1, cada uma com o comando corrido e o output resumido, mais a conclusao `COVERED (fechado pela correcao CR-02 da Phase 119; esta fase confirma, nao reimplementa)`.
    - Seccao **ISOL-02 — veredito por superficie**: uma tabela `| Query / Guard | Scope confirmed | Verdict |` com **no minimo 8 linhas**, uma por simbolo/superficie tracada, cada `Scope confirmed` a citar ficheiro:linha concreto. Cobrir obrigatoriamente: `TenantRepository.findFirstByOrderByCreatedAtAsc` (inexistente), `PublicController.getBranding`, a iteracao cross-tenant de `PlatformAdminController`, o ciclo `findAll()` de `AlertasDiariosJob`, os literais `"LexCV"` no backend (`DatabaseSeeder`/`TenantRepository.findByNome`) e no frontend (`plataforma/columns.tsx`), `Role`/`Permission` como tabelas globais sem `tenant_id`, e o caminho de resolucao de tenant da app autenticada (sempre `UserPrincipal.getTenantId()` a partir do JWT, nunca inferido). O veredito de todas as linhas e `COVERED` — se alguma fosse `FIXED`, este plano estaria fora de ambito e teria de parar.
    - Seccao **Comandos de reproducao**: a lista literal dos comandos corridos, para a Phase 123 poder re-executar a varredura sem a redesenhar.
    - Seccao **Registado mas explicitamente fora de ambito para ISOL-02**: o padrao `findByXxxId`-sem-`tenantId` de `PITFALLS.md` Pitfall 1, com a justificacao completa (real, pre-existente, IDOR-adjacente, anterior a v2.16 por varias milestones, **nao** newly risky por causa de um 2o tenant pagante — ao contrario de ISOL-03, que a Phase 120 tornou explorável) e o dono nomeado: Phase 123 (ISOL-04) decide se merece fase de correcao propria. Deixar explicito que **nao** e um achado desta auditoria.
    - Seccao **Assimetria observada em `/admin/rbac`**: apos o Plan 01, um `PLATAFORMA_ADMIN` passa o gate do `PUT` mas continua a receber `403` no `GET` (que so tem gate de classe). Registar como consequencia intencional e literal dos criterios de sucesso (que nomeiam so o `PUT`), com nota de que um ecra de edicao de RBAC para plataforma esta explicitamente diferido por `121-CONTEXT.md`.

    Escrever em portugues, na linha do resto de `.planning/`. Nao inflacionar: uma linha de tabela por superficie realmente tracada, sem preenchimento.

    Proibicoes: NAO alterar nenhum ficheiro de codigo. NAO "corrigir" o padrao de `PITFALLS.md`. NAO alterar o comportamento nem a autorizacao de `GET /admin/rbac`.
  </action>

  <verify>
    <automated>test -f ".planning/phases/LEXCV-121-fechar-suposi-es-de-tenant-nica-bloqueio-de-rbac/121-ISOL-AUDIT.md" && test $(grep -c '| COVERED' ".planning/phases/LEXCV-121-fechar-suposi-es-de-tenant-nica-bloqueio-de-rbac/121-ISOL-AUDIT.md") -ge 8 && test $(grep -c '| FIXED' ".planning/phases/LEXCV-121-fechar-suposi-es-de-tenant-nica-bloqueio-de-rbac/121-ISOL-AUDIT.md") -eq 0 && grep -q 'PITFALLS.md' ".planning/phases/LEXCV-121-fechar-suposi-es-de-tenant-nica-bloqueio-de-rbac/121-ISOL-AUDIT.md" && grep -q 'Comandos de reproducao' ".planning/phases/LEXCV-121-fechar-suposi-es-de-tenant-nica-bloqueio-de-rbac/121-ISOL-AUDIT.md" && test -z "$(git status --porcelain -- backend web)"</automated>
  </verify>

  <acceptance_criteria>
    - `121-ISOL-AUDIT.md` existe, com frontmatter que inclui `requirements-completed: [ISOL-01, ISOL-02]`.
    - A tabela de veredito de ISOL-02 tem no minimo 8 linhas cujo campo de veredito comeca por `COVERED` (`grep -c '| COVERED'` >= 8) e **zero** linhas `FIXED`.
    - Cada linha da tabela cita pelo menos um `ficheiro:linha` ou um simbolo qualificado concreto no campo `Scope confirmed` — nenhuma linha se limita a "confirmado".
    - As 5 familias de padrao (`findFirstBy`/`findTopBy`, `.findAll()`, `.get(0)`/`findFirst()`, literal `"LexCV"`, `findFirstByOrderByCreatedAtAsc`) aparecem todas na seccao de comandos de reproducao, com os comandos exatos que foram corridos.
    - A seccao de ISOL-01 inclui o resultado do `mvn test -Dtest=PublicControllerTest` (2 testes, 0 falhas) e as tres pesquisas da Task 1.
    - Existe a seccao que declara o padrao `findByXxxId`-sem-`tenantId` como fora de ambito, cita `PITFALLS.md` Pitfall 1, e nomeia a Phase 123 (ISOL-04) como dono.
    - Existe a seccao sobre a assimetria `GET`/`PUT` de `/admin/rbac`, descrita como consequencia intencional.
    - `git status --porcelain -- backend web` esta vazio — zero alteracoes de codigo neste plano.
  </acceptance_criteria>

  <done>Existe um registo de auditoria reproduzivel, no formato ja usado pela AUD-01, que fecha ISOL-01 e ISOL-02 com veredito por superficie e entrega a Phase 123 uma base citavel em vez de uma varredura por refazer.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `GET /api/v1/public/branding` (nao autenticado) -> dados de tenant | Endpoint `permitAll()` em `SecurityConfig`; qualquer leitura de tenant aqui seria exposta a internet publica sem credenciais |
| Codigo que resolve "a" tenant por heuristica -> dados de outro tenant | Com mais de um tenant real, qualquer `findFirst`/`get(0)` sobre tenants passa a devolver dados de uma organizacao arbitraria |
| Registo de auditoria -> decisao futura (Phase 123) | Um veredito errado ou nao reproduzivel aqui propaga-se como falsa confianca para a auditoria que autoriza o 2o tenant pagante real |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-121-13 | Information Disclosure | `GET /api/v1/public/branding` a devolver nome/logo de um escritorio real a um chamador anonimo | mitigate | Ja fechado pela correcao CR-02 da Phase 119 (resposta constante, controller sem dependencias). Esta fase confirma por execucao de teste e por 3 pesquisas independentes, incluindo a ausencia de qualquer `findFirstBy`/`findTopBy` em `TenantRepository` — nao apenas por leitura do docblock |
| T-121-14 | Information Disclosure | Qualquer outro caminho a resolver "a" tenant por heuristica | mitigate | Varredura re-executada por comando sobre `backend/src/main/java` e `web/src` com 5 familias de padrao; cada ocorrencia recebe um de tres desfechos; qualquer ocorrencia sem desfecho para o plano e vai ao orquestrador |
| T-121-15 | Repudiation | Auditoria que declara COVERED sem evidencia por superficie, criando falsa confianca para a Phase 123 | mitigate | Formato de tabela por superficie herdado da AUD-01, com `ficheiro:linha` obrigatorio em cada linha, minimo de 8 linhas verificado por comando, e seccao de comandos de reproducao para re-execucao independente |
| T-121-16 | Tampering | Um plano de auditoria a "aproveitar" para corrigir o padrao de `PITFALLS.md` e alargar ambito silenciosamente | mitigate | Proibicao explicita na `<action>`, dono nomeado (Phase 123), e `git status --porcelain -- backend web` vazio como criterio de aceitacao |
| T-121-17 | Information Disclosure | Auditoria a incluir segredos, credenciais ou conteudo de `.env` | mitigate | O artefacto so cita simbolos, ficheiros e linhas de codigo-fonte; nenhum comando corrido le `.env` ou a base de dados |
| T-121-SC | Tampering | Instalacoes npm/pip/cargo/Maven | n/a | Este plano nao instala nada e nao altera codigo — so executa testes e pesquisas ja existentes e escreve documentacao. O Package Legitimacy Gate nao se aplica |
</threat_model>

<verification>
1. `cd backend && mvn test -Dtest=PublicControllerTest` — 2 testes, 0 falhas.
2. `grep -rn 'findFirstByOrderByCreatedAtAsc' --include=*.java backend/src | wc -l` devolve `2`, ambas em comentario.
3. `grep -c 'Repository' backend/src/main/java/com/lexcv/controllers/PublicController.java` devolve `0`.
4. `grep -cE 'findFirstBy|findTopBy' backend/src/main/java/com/lexcv/repositories/TenantRepository.java` devolve `0`.
5. `grep -c '| COVERED' .planning/phases/LEXCV-121-*/121-ISOL-AUDIT.md` >= 8; `grep -c '| FIXED'` = 0.
6. O ficheiro de auditoria contem a seccao de comandos de reproducao e a referencia a `PITFALLS.md`.
7. `git status --porcelain -- backend web` vazio.
</verification>

<success_criteria>
- ROADMAP Phase 121, Criterio de Sucesso 1 (branding sem dependencia de `findFirstByOrderByCreatedAtAsc`) confirmado por teste verde e por ausencia comprovada do simbolo em codigo vivo.
- ROADMAP Phase 121, Criterio de Sucesso 2 (pesquisa dedicada ao codigo de producao) satisfeito por uma varredura re-executada, com veredito por superficie e comandos reproduziveis — nao por uma afirmacao de que a pesquisa foi feita.
- A Phase 123 (ISOL-04) herda uma base citavel, incluindo dois itens de contexto explicitamente marcados como nao-achados desta fase (o padrao de `PITFALLS.md` e a assimetria `GET`/`PUT` de `/admin/rbac`).
</success_criteria>

<output>
Criar `.planning/phases/LEXCV-121-fechar-suposi-es-de-tenant-nica-bloqueio-de-rbac/121-ISOL-AUDIT.md` (o artefacto de auditoria) e `.planning/phases/LEXCV-121-fechar-suposi-es-de-tenant-nica-bloqueio-de-rbac/121-03-SUMMARY.md` quando terminar.
</output>
