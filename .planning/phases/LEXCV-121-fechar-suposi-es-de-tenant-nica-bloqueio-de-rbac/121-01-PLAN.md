---
phase: 121-fechar-suposi-es-de-tenant-nica-bloqueio-de-rbac
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/main/java/com/lexcv/controllers/AdminController.java
  - backend/src/test/java/com/lexcv/controllers/AdminControllerRbacAutorizacaoTest.java
autonomous: true
requirements: [ISOL-03]

must_haves:
  truths:
    - "Um chamador que satisfaz genuinamente o gate de classe hasRole('ADMIN') (autoridade ROLE_ADMIN) e recusado com AccessDeniedException ao invocar updateRbac atraves de um proxy real de method security"
    - "Um chamador que NAO satisfaz hasRole('ADMIN') mas detem ROLE_PLATAFORMA_ADMIN atravessa o gate e o handler corre ate ao fim"
    - "getRbac continua sem qualquer anotacao de metodo — a leitura da matriz por um ADMIN de tenant nao e afetada"
    - "A anotacao de classe de AdminController continua a ser exatamente hasRole('ADMIN'), sem alteracao"
    - "Os Casos 6/7/8 de AdminControllerPlataformaAdminContencaoTest continuam verdes com o ficheiro literalmente inalterado"
  artifacts:
    - path: "backend/src/test/java/com/lexcv/controllers/AdminControllerRbacAutorizacaoTest.java"
      provides: "Prova comportamental de que a anotacao de metodo substitui (nao soma a) a de classe, via ProxyFactory + AuthorizationManagerBeforeMethodInterceptor"
      min_lines: 90
      contains: "AuthorizationManagerBeforeMethodInterceptor"
    - path: "backend/src/main/java/com/lexcv/controllers/AdminController.java"
      provides: "Gate de metodo hasRole('PLATAFORMA_ADMIN') exclusivo do handler PUT /rbac"
      contains: "PLATAFORMA_ADMIN"
  key_links:
    - from: "backend/src/main/java/com/lexcv/controllers/AdminController.java"
      to: "handler updateRbac (PUT /api/v1/admin/rbac)"
      via: "anotacao de metodo imediatamente acima de @PutMapping(\"/rbac\")"
      pattern: "@PreAuthorize\\(\"hasRole\\('PLATAFORMA_ADMIN'\\)\"\\)"
    - from: "backend/src/test/java/com/lexcv/controllers/AdminControllerRbacAutorizacaoTest.java"
      to: "AdminController.updateRbac"
      via: "proxy CGLIB com o advisor de method security do Spring Security"
      pattern: "ProxyFactory"
---

<objective>
Fechar o unico gap genuinamente exploravel desta fase: `PUT /api/v1/admin/rbac` deixa de aceitar chamadas de um `ADMIN` de escritorio e passa a exigir `PLATAFORMA_ADMIN`, atraves de uma anotacao **de metodo** que substitui — para esse handler apenas — o gate de classe `hasRole('ADMIN')` de `AdminController`.

Purpose: `Role` e `Permission` sao tabelas globais de plataforma, sem coluna `tenant_id` (confirmado por leitura direta de ambas as entidades). Hoje, qualquer `ADMIN` de qualquer escritorio que grave a matriz RBAC reescreve exatamente as mesmas linhas `t_role`/`t_role_permission` de que dependem os utilizadores `TECNICO`/`ADVOGADO`/`ASSISTENTE` de **todos** os outros escritorios. A Phase 120 tornou possivel criar um 2o tenant real, pelo que este e o item de maior risco identificado pela propria proposta (ROADMAP Phase 121, nota de Risco).

Esta combinacao — anotacao de classe permissiva + anotacao de metodo restritiva na mesma classe — nao existe em mais nenhum sitio deste codebase (confirmado por grep de todos os `@PreAuthorize` em `controllers/`). Por isso a semantica "a mais especifica ganha" e provada **comportamentalmente**, com RED antes de GREEN, e nunca assumida por analogia.

Output: uma linha de anotacao em `AdminController.java` e um ficheiro de teste dedicado com 5 casos, dos quais 3 falham antes da alteracao e passam depois.
</objective>

<execution_context>
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/LEXCV-121-fechar-suposi-es-de-tenant-nica-bloqueio-de-rbac/121-CONTEXT.md
@.planning/phases/LEXCV-121-fechar-suposi-es-de-tenant-nica-bloqueio-de-rbac/121-PATTERNS.md
@backend/src/main/java/com/lexcv/controllers/AdminController.java
@backend/src/test/java/com/lexcv/controllers/PlatformAdminControllerTest.java
@backend/src/test/java/com/lexcv/controllers/AdminControllerPlataformaAdminContencaoTest.java
</context>

<interfaces>
<!-- Contratos ja confirmados no planeamento por leitura direta do codigo. NAO redescobrir. -->

Estado atual de `AdminController.java` (confirmado):

```java
// linhas 26-30 — gate de classe, MANTER exatamente como esta
@RestController
@RequestMapping("/api/v1/admin")
@PreAuthorize("hasRole('ADMIN')")
@RequiredArgsConstructor
public class AdminController {
```

```java
// linha 346 — getRbac, imediatamente antes de updateRbac, DELIBERADAMENTE nao tocado
@GetMapping("/rbac")
public ResponseEntity<?> getRbac() { ... }

// linha 391 — updateRbac, estado atual (corpo NAO muda nesta fase)
@PutMapping("/rbac")
public ResponseEntity<?> updateRbac(@RequestBody Map<String, Object> body) { ... }
```

`org.springframework.security.access.prepost.PreAuthorize` ja esta importado em `AdminController.java:17` — **zero imports novos** no ficheiro de producao.

Convencao de ordenacao confirmada (`NotificacaoController.java:69-70`, `ResourceController.java:169-170`): o `@PreAuthorize` de metodo vai sempre **acima** da anotacao de mapeamento, nunca abaixo.

Construtor de `AdminController` (5 argumentos, ja usado verbatim em `AdminControllerPlataformaAdminContencaoTest.java:98-100`):

```java
private AdminController novoController() {
    return new AdminController(userRepository, roleRepository, permissionRepository, passwordEncoder, tenantRepository);
}
```

Harness de proxy AOP a reutilizar (`PlatformAdminControllerTest.java:111-116`) — o unico mecanismo neste codebase que avalia mesmo um `@PreAuthorize` (nao existe `MockMvc`/`@SpringBootTest`/`@WebMvcTest` em lado nenhum da suite):

```java
private AdminController novoProxyComMethodSecurity() {
    ProxyFactory factory = new ProxyFactory(novoController());
    factory.setProxyTargetClass(true);   // CGLIB obrigatorio: a classe alvo nao implementa interface
    factory.addAdvisor(AuthorizationManagerBeforeMethodInterceptor.preAuthorize());
    return (AdminController) factory.getProxy();
}
```

**Armadilha confirmada — nao copiar `PlatformAdminControllerTest.autenticarComoRoles`.** Esse helper (`:118-124`) constroi `SimpleGrantedAuthority("ADMIN")` **sem** o prefixo `"ROLE_"` que o Spring exige para `hasRole(...)`. Para os testes negativos daquele ficheiro e inofensivo; aqui destruiria a prova, porque a recusa passaria a ser trivialmente verdadeira pelo motivo errado (a autoridade nunca satisfaria nenhum `hasRole`, logo o teste nao distinguiria "o override funciona" de "o contexto de seguranca estava simplesmente vazio"). Usar sempre a forma prefixada, como em `PlatformAdminControllerTest.autenticarComoPlataformaAdmin` (`:126-134`, que usa literalmente `"ROLE_PLATAFORMA_ADMIN"`):

```java
private void autenticarComoAuthorities(String... authorities) {
    List<SimpleGrantedAuthority> autoridades = Arrays.stream(authorities)
            .map(SimpleGrantedAuthority::new)
            .toList();
    SecurityContextHolder.getContext()
            .setAuthentication(new UsernamePasswordAuthenticationToken(null, null, autoridades));
}
```

`updateRbac` nunca le `SecurityContextHolder` no seu corpo, por isso uma lista de autoridades sem `UserPrincipal` e suficiente (mais simples que `autenticarComoPlataformaAdmin`).

Imports necessarios no ficheiro de teste novo (todos ja provados em `PlatformAdminControllerTest.java`):

```java
import org.springframework.aop.framework.ProxyFactory;
import org.springframework.security.access.AccessDeniedException;
import org.springframework.security.access.prepost.PreAuthorize;
import org.springframework.security.authentication.UsernamePasswordAuthenticationToken;
import org.springframework.security.authorization.method.AuthorizationManagerBeforeMethodInterceptor;
import org.springframework.security.core.authority.SimpleGrantedAuthority;
import org.springframework.security.core.context.SecurityContextHolder;
import java.lang.reflect.Method;
import java.util.Arrays;
```

`SecurityConfig` (confirmado): `authorizeHttpRequests` so declara `permitAll()` para 6 paths publicos e `anyRequest().authenticated()` — **nao existe nenhuma regra de papel ao nivel do filtro para `/api/v1/admin/**`**. Toda a autorizacao destes endpoints vem exclusivamente das anotacoes, e `@EnableMethodSecurity` esta ativo em `SecurityConfig`. Consequencia intencional e aceite: apos esta alteracao, um `PLATAFORMA_ADMIN` **passa** no `PUT /rbac` mas continua a receber `403` no `GET /rbac` (que so tem o gate de classe). Isto e coerente com a decisao registada em `121-CONTEXT.md` (so o `PUT` e nomeado pelos criterios de sucesso) — nao "corrigir".

Comandos (CLAUDE.md): `cd backend && mvn test -Dtest=<Classe>`, `mvn test`, `mvn spotbugs:check`. Requer `backend/.env` com todas as variaveis `DB_*`/`MINIO_*` — a suite de testes usa Mockito puro e nao precisa de base de dados, mas o build precisa de compilar.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Escrever a prova comportamental primeiro (RED) — AdminControllerRbacAutorizacaoTest</name>

  <files>
    backend/src/test/java/com/lexcv/controllers/AdminControllerRbacAutorizacaoTest.java (novo)
  </files>

  <read_first>
    - `backend/src/test/java/com/lexcv/controllers/PlatformAdminControllerTest.java` — ler o Javadoc de classe (`:53-77`, explica **porque** um proxy e necessario), o helper `novoProxyComMethodSecurity()` (`:111-116`), `autenticarComoRoles` (`:118-124`, o helper que NAO se deve copiar) e `autenticarComoPlataformaAdmin` (`:126-134`, a prefixagem correta a seguir), mais o teste de reflexao `anotacaoDeClasseTemValorExatoHasRolePlataformaAdmin` (`:233-238`).
    - `backend/src/test/java/com/lexcv/controllers/AdminControllerPlataformaAdminContencaoTest.java` — os 5 `@Mock`, o `novoController()` de 5 argumentos (`:98-100`), o `@AfterEach limparSecurityContext()`, e os Casos 7/8 (`:226-256`) cujo corpo de pedido serve de modelo para o `body` dos novos testes. Este ficheiro NAO e alterado por este plano.
    - `backend/src/main/java/com/lexcv/controllers/AdminController.java` — o corpo de `updateRbac` (`:391-424`), para saber exatamente que colaboradores sao chamados no caminho feliz (`roleRepository.findByNome`, `permissionRepository.findByNome`, `roleRepository.save`).
    - `.planning/phases/LEXCV-121-fechar-suposi-es-de-tenant-nica-bloqueio-de-rbac/121-PATTERNS.md` — Pattern Assignment 2, em particular o bloco de aviso sobre o prefixo `ROLE_`.
  </read_first>

  <action>
    Criar `AdminControllerRbacAutorizacaoTest.java` em `backend/src/test/java/com/lexcv/controllers/`, package `com.lexcv.controllers`, anotado com `@ExtendWith(MockitoExtension.class)`. O nome segue a convencao ja estabelecida neste diretorio (`AdminControllerLimiteUtilizadoresTest`, `AdminControllerPlataformaAdminContencaoTest`, `AuthControllerLoginLockoutTest`): sempre `<Controller><FraseDeConcern>Test`.

    Declarar os mesmos 5 `@Mock` de `AdminControllerPlataformaAdminContencaoTest` (`userRepository`, `roleRepository`, `permissionRepository`, `passwordEncoder`, `tenantRepository`), o `@AfterEach` que chama `SecurityContextHolder.clearContext()`, o helper `novoController()` de 5 argumentos, o helper `novoProxyComMethodSecurity()` (copia exata do de `PlatformAdminControllerTest`, so mudando o tipo de retorno para `AdminController`) e o helper `autenticarComoAuthorities(String...)` com autoridades **ja prefixadas** — ver `<interfaces>`.

    Escrever exatamente 5 casos de teste:

    1. `updateRbac_comRoleAdminDeTenantNormalERecusadoMesmoSatisfazendoOGateDeClasse` — autentica com a autoridade unica `"ROLE_ADMIN"` (prefixada, satisfaz genuinamente o gate de classe `hasRole('ADMIN')`), monta o proxy, e afirma `assertThrows(AccessDeniedException.class, () -> proxy.updateRbac(body))` mais `verify(roleRepository, never()).save(any())`. O `body` e `Map.of("rolePermissions", Map.of("ASSISTENTE", List.of("clientes:view")))`. **Este teste nao pode ter nenhum stub** — o handler nunca corre no estado GREEN, e um stub nao usado dispararia `UnnecessaryStubbingException` do modo estrito do Mockito.

    2. `updateRbac_comRolePlataformaAdminPassaOGateMesmoSemHasRoleAdmin` — autentica **apenas** com `"ROLE_PLATAFORMA_ADMIN"` (deliberadamente sem `"ROLE_ADMIN"`), stub de `roleRepository.findByNome("ASSISTENTE")` a devolver `Optional.of(Role.builder().id(4).nome("ASSISTENTE").build())` e de `permissionRepository.findByNome("clientes:view")` a devolver `Optional.of(Permission.builder().id(1).nome("clientes:view").build())`, monta o proxy, e afirma `assertDoesNotThrow`, `HttpStatus.OK` e `verify(roleRepository, times(1)).save(any())`. Este e o teste que prova que as duas anotacoes **nao** sao combinadas com E logico — se fossem, este chamador seria recusado.

    3. `updateRbac_temAnotacaoDeMetodoComValorExatoHasRolePlataformaAdmin` — por reflexao, obtem `AdminController.class.getMethod("updateRbac", Map.class)`, le a anotacao `PreAuthorize` do metodo, afirma `assertNotNull` e `assertEquals("hasRole('PLATAFORMA_ADMIN')", anotacao.value())`. Espelha `anotacaoDeClasseTemValorExatoHasRolePlataformaAdmin` de `PlatformAdminControllerTest`.

    4. `getRbac_continuaSemAnotacaoDeMetodo` — por reflexao, `AdminController.class.getMethod("getRbac")`, afirma que `getAnnotation(PreAuthorize.class)` e `null`. Guarda de regressao para a decisao explicita de `121-CONTEXT.md` de nao tocar no `GET`.

    5. `anotacaoDeClasseContinuaHasRoleAdmin` — por reflexao, `AdminController.class.getAnnotation(PreAuthorize.class)`, afirma `assertEquals("hasRole('ADMIN')", anotacao.value())`. Guarda de regressao para os restantes handlers da classe (utilizadores, etc.), que continuam governados so por este gate.

    Escrever um Javadoc de classe que explique, em duas ou tres frases, porque este ficheiro existe separado do de contencao: prova o **gate de autorizacao** (que so um proxy AOP avalia), enquanto o de contencao prova **guardas de corpo de handler** por chamada Java direta. Referir que esta e a primeira classe deste codebase a combinar anotacao de classe com anotacao de metodo, e que por isso o mecanismo e provado por comportamento e nao por analogia.

    Correr `cd backend && mvn test -Dtest=AdminControllerRbacAutorizacaoTest` e **confirmar que falha**. No estado atual (sem a anotacao de metodo), os casos 1, 2 e 3 tem de falhar: o caso 1 porque `ROLE_ADMIN` ainda satisfaz o unico gate existente e nenhuma excecao e lancada; o caso 2 porque um chamador sem `ROLE_ADMIN` e recusado pelo gate de classe; o caso 3 porque a anotacao de metodo ainda nao existe. Os casos 4 e 5 tem de passar ja. Registar por escrito o resultado exato do RED (quais falharam e com que erro) — vai ser citado no SUMMARY.

    Nota antecipada para o RED: no estado atual o caso 2 pode falhar por `AccessDeniedException` na assercao **ou** por `UnnecessaryStubbingException` (os stubs ficam por usar quando o gate de classe recusa antes do corpo). As duas formas sao evidencia RED valida — nao "arranjar" com `lenient()`, porque no estado GREEN os stubs passam a ser usados.

    Proibicoes: NAO alterar `AdminController.java` nesta task (pertence a Task 2). NAO alterar `AdminControllerPlataformaAdminContencaoTest.java` — o objetivo declarado e que esse ficheiro fique literalmente intacto. NAO introduzir `MockMvc`, `@SpringBootTest` ou `@WebMvcTest` — este codebase nao tem nenhum harness desse tipo e esta fase nao e o sitio para o estrear.

    Commit desta task isoladamente, no estilo ja usado pela Phase 117 (`test(121-01): ...`), para que o historico git mostre a prova genuinamente a falhar antes da correcao.
  </action>

  <verify>
    <automated>test -f backend/src/test/java/com/lexcv/controllers/AdminControllerRbacAutorizacaoTest.java && grep -q 'AuthorizationManagerBeforeMethodInterceptor' backend/src/test/java/com/lexcv/controllers/AdminControllerRbacAutorizacaoTest.java && grep -q 'ROLE_ADMIN' backend/src/test/java/com/lexcv/controllers/AdminControllerRbacAutorizacaoTest.java && cd backend && ! mvn -q test -Dtest=AdminControllerRbacAutorizacaoTest</automated>
  </verify>

  <acceptance_criteria>
    - `backend/src/test/java/com/lexcv/controllers/AdminControllerRbacAutorizacaoTest.java` existe e contem exatamente 5 metodos anotados com `@Test` (`grep -c '@Test'` devolve 5).
    - O ficheiro contem literalmente `AuthorizationManagerBeforeMethodInterceptor.preAuthorize()`, `factory.setProxyTargetClass(true)` e `ProxyFactory`.
    - O ficheiro contem as autoridades **prefixadas** `"ROLE_ADMIN"` e `"ROLE_PLATAFORMA_ADMIN"`; `grep -c '"ADMIN"'` sobre linhas de codigo (nao comentarios) nao encontra nenhuma autoridade nao prefixada passada a `SimpleGrantedAuthority`.
    - `cd backend && mvn test -Dtest=AdminControllerRbacAutorizacaoTest` **falha** (exit code != 0) neste ponto, e o output nomeia os casos 1, 2 e 3 como falhados enquanto os casos 4 e 5 passam. O comando `<automated>` inverte esse exit code, pelo que passa exatamente quando o RED e genuino.
    - `git diff --name-only` nao lista `backend/src/main/java/com/lexcv/controllers/AdminController.java` nem `backend/src/test/java/com/lexcv/controllers/AdminControllerPlataformaAdminContencaoTest.java`.
    - O resultado do RED (nomes dos casos falhados + tipo de erro de cada um) esta registado por escrito para o SUMMARY.
  </acceptance_criteria>

  <done>Existe uma prova comportamental de 5 casos que falha em 3 deles exatamente pela razao certa — o gate ainda nao existe — e o commit de teste esta separado do commit de correcao.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Adicionar o gate de metodo a updateRbac (GREEN) e confirmar zero regressao</name>

  <files>
    backend/src/main/java/com/lexcv/controllers/AdminController.java
  </files>

  <read_first>
    - `backend/src/main/java/com/lexcv/controllers/AdminController.java` — linhas 26-30 (gate de classe a preservar byte a byte), 32-51 (o docblock de `PAPEL_PLATAFORMA`, cujas duas ultimas linhas prometem literalmente esta alteracao: *"A Phase 121 (ISOL-03) ira depois fechar o endpoint PUT /rbac por inteiro a papeis de plataforma"*), 346 (`getRbac`, a nao tocar) e 391-424 (`updateRbac`).
    - `backend/src/main/java/com/lexcv/controllers/NotificacaoController.java` linhas 69-70 e `backend/src/main/java/com/lexcv/controllers/ResourceController.java` linhas 169-170 — a convencao confirmada de ordenacao (`@PreAuthorize` acima da anotacao de mapeamento).
    - `.planning/STATE.md`, entradas de decisao das Phases 119/120 sobre comentarios auto-referenciais que fazem disparar gates baseados em grep (`119-04`, `120-01`, `120-02`) — a mesma armadilha aplica-se ao comentario que esta task reescreve.
  </read_first>

  <behavior>
    Apos esta task, os 5 casos de `AdminControllerRbacAutorizacaoTest` passam:
    - Caso 1: chamador com `ROLE_ADMIN` invoca `updateRbac` pelo proxy e recebe `AccessDeniedException`; `roleRepository.save` nunca e chamado.
    - Caso 2: chamador apenas com `ROLE_PLATAFORMA_ADMIN` invoca `updateRbac` pelo proxy, recebe `200 OK` e `roleRepository.save` e chamado uma vez.
    - Caso 3: `AdminController.updateRbac` tem uma anotacao `@PreAuthorize` de metodo com o valor exato `hasRole('PLATAFORMA_ADMIN')`.
    - Caso 4: `AdminController.getRbac` continua sem qualquer anotacao `@PreAuthorize` de metodo.
    - Caso 5: a anotacao de classe de `AdminController` continua com o valor exato `hasRole('ADMIN')`.
    E os Casos 6/7/8 de `AdminControllerPlataformaAdminContencaoTest` continuam verdes sem esse ficheiro ter sido tocado (chamam `novoController().updateRbac(...)` diretamente; `@PreAuthorize` e metadados inertes sem proxy).
  </behavior>

  <action>
    Acrescentar uma unica linha de anotacao a `AdminController.java`: `@PreAuthorize("hasRole('PLATAFORMA_ADMIN')")` imediatamente **acima** de `@PutMapping("/rbac")`, seguindo a convencao de ordenacao ja usada em `NotificacaoController` e `ResourceController`. Zero imports novos (`PreAuthorize` ja esta importado na linha 17). O corpo de `updateRbac` nao muda em nada — nenhuma linha, nenhum reordenamento, nenhuma extracao de metodo: os Casos 7 e 8 do teste de contencao afirmam sobre esse corpo e tem de continuar a afirmar exatamente o mesmo.

    NAO tocar em `getRbac` (linha 346): continua sem anotacao de metodo, herdando so o gate de classe, para que um `ADMIN` de escritorio mantenha a leitura da matriz e nao apanhe um `403` inesperado no separador de Definicoes.

    NAO tocar na anotacao de classe (linha 28): `hasRole('ADMIN')` continua a governar todos os outros handlers desta classe (utilizadores, etc.).

    Acrescentar um comentario curto imediatamente acima da nova anotacao explicando o mecanismo: uma anotacao de metodo **substitui** a de classe para esse handler (a mais especifica ganha; nunca sao combinadas com E logico), que `Role`/`Permission` sao tabelas globais sem `tenant_id`, e que por isso permitir a escrita a um ADMIN de escritorio significaria reescrever as permissoes de todos os outros escritorios. Referenciar ISOL-03/Phase 121.

    Reescrever as duas ultimas linhas do docblock de `PAPEL_PLATAFORMA` (linhas 49-51), que hoje descrevem esta alteracao como futura ("ira depois fechar"), para a descreverem como concretizada — mantendo o resto do docblock intacto. Este codebase ja tem esse habito documentado (ver `PublicController.java`, cujo docblock foi atualizado no proprio sitio da correcao).

    **Higiene de gate (precedente repetido das Phases 119/120):** nem o comentario novo nem o docblock reescrito podem conter a string literal `hasRole('PLATAFORMA_ADMIN')`, nem comecar uma linha com `@PreAuthorize`. Descrever o gate por palavras ("exige o papel de plataforma", "anotacao propria de metodo"), nunca reproduzindo o literal — caso contrario as assercoes de contagem abaixo tornam-se auto-invalidantes. Pela mesma razao, nao colocar nenhum comentario que mencione `PreAuthorize` na linha imediatamente anterior a `@GetMapping("/rbac")`.

    Correr, por esta ordem: `mvn test -Dtest=AdminControllerRbacAutorizacaoTest` (5/5 verdes), `mvn test -Dtest=AdminControllerPlataformaAdminContencaoTest` (todos verdes, ficheiro intacto), `mvn test` (suite completa sem novas falhas) e `mvn spotbugs:check` (limpo, gate SAST estabelecido pela Phase 90).

    Commit separado do commit de teste da Task 1, no estilo `feat(121-01): ...`.
  </action>

  <verify>
    <automated>test $(grep -cE '^[[:space:]]*@PreAuthorize' backend/src/main/java/com/lexcv/controllers/AdminController.java) -eq 2 && grep -A1 "@PreAuthorize(\"hasRole('PLATAFORMA_ADMIN')\")" backend/src/main/java/com/lexcv/controllers/AdminController.java | grep -q '@PutMapping("/rbac")' && test $(grep -B1 '@GetMapping("/rbac")' backend/src/main/java/com/lexcv/controllers/AdminController.java | grep -c 'PreAuthorize') -eq 0 && cd backend && mvn test && mvn spotbugs:check</automated>
  </verify>

  <acceptance_criteria>
    - `grep -cE '^[[:space:]]*@PreAuthorize' backend/src/main/java/com/lexcv/controllers/AdminController.java` devolve exatamente `2` — uma de classe, uma de metodo, nenhuma outra.
    - A linha imediatamente seguinte a `@PreAuthorize("hasRole('PLATAFORMA_ADMIN')")` e `@PutMapping("/rbac")` (verificado com `grep -A1`).
    - `grep -B1 '@GetMapping("/rbac")' ... | grep -c 'PreAuthorize'` devolve `0` — `getRbac` continua sem gate proprio.
    - A anotacao de classe continua exatamente `@PreAuthorize("hasRole('ADMIN')")`: `grep -B3 'public class AdminController' ...` mostra-a inalterada.
    - `git diff` de `AdminController.java` mostra **uma** linha de anotacao adicionada, um comentario novo, a reescrita das 2 linhas do docblock de `PAPEL_PLATAFORMA` — e **zero** alteracoes dentro do corpo de `updateRbac` ou de `getRbac`.
    - `git status --porcelain` nao lista `backend/src/test/java/com/lexcv/controllers/AdminControllerPlataformaAdminContencaoTest.java` como modificado.
    - `cd backend && mvn test -Dtest=AdminControllerRbacAutorizacaoTest` passa 5/5.
    - `cd backend && mvn test` termina com exit 0 e sem novas falhas relativamente ao estado anterior a esta fase.
    - `cd backend && mvn spotbugs:check` termina com exit 0.
    - O comentario novo e o docblock reescrito **nao** contem a string literal `hasRole('PLATAFORMA_ADMIN')`.
  </acceptance_criteria>

  <done>`PUT /api/v1/admin/rbac` exige `PLATAFORMA_ADMIN`, provado por 5 casos verdes que antes falhavam em 3; `getRbac`, o gate de classe e o teste de contencao ficaram intactos; suite completa e SpotBugs verdes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ADMIN de escritorio (tenant) -> `PUT /api/v1/admin/rbac` | Entrada autenticada mas nao confiavel que hoje escreve tabelas globais de plataforma (`t_role`, `t_role_permission`), sem `tenant_id`, partilhadas por todos os escritorios |
| Anotacao de metodo -> anotacao de classe (`AdminController`) | Primeira vez neste codebase que a resolucao "a mais especifica ganha" do Spring Security e usada; o resultado da resolucao **e** a fronteira de autorizacao |
| Suite de testes -> proxy AOP | Uma chamada Java direta nunca avalia `@PreAuthorize`; so o proxy prova a fronteira |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-121-01 | Elevation of Privilege | `AdminController.updateRbac` (`PUT /api/v1/admin/rbac`) | mitigate | Anotacao de metodo `hasRole('PLATAFORMA_ADMIN')` acima de `@PutMapping("/rbac")`; um `ADMIN` de escritorio deixa de poder alterar permissoes de papel. `SecurityConfig` nao tem regra de papel ao nivel do filtro para `/api/v1/admin/**`, pelo que esta anotacao **e** a unica fronteira — dai a prova comportamental obrigatoria (Caso 1) |
| T-121-02 | Tampering | Linhas globais `t_role`/`t_role_permission` (sem coluna `tenant_id`) | mitigate | Mesmo gate. Complementarmente, as guardas de corpo ja existentes (Phase 119, Caso 7) continuam a ignorar entradas `ADMIN`/`PLATAFORMA_ADMIN` — a Task 2 proibe explicitamente qualquer alteracao ao corpo de `updateRbac` para nao as enfraquecer |
| T-121-03 | Spoofing | Prova de autorizacao construida com autoridade nao prefixada (`"ADMIN"` em vez de `"ROLE_ADMIN"`) | mitigate | `121-PATTERNS.md` identifica esta armadilha no helper reutilizavel; a Task 1 obriga a autoridades prefixadas nas duas direcoes e o criterio de aceitacao verifica-o. Sem isto, a recusa seria trivialmente verdadeira pelo motivo errado e o gate poderia estar ausente sem o teste dar por isso |
| T-121-04 | Denial of Service | Anotar por engano a classe, ou anotar tambem `getRbac`, tirando a leitura da matriz a todos os `ADMIN` de escritorio | mitigate | Casos 4 e 5 (reflexao) mais tres assercoes de origem no `<verify>`: contagem de exatamente 2 `@PreAuthorize`, adjacencia obrigatoria a `@PutMapping("/rbac")`, e zero `PreAuthorize` na linha anterior a `@GetMapping("/rbac")` |
| T-121-05 | Repudiation | Declarar o gate provado quando o teste passaria mesmo sem a alteracao | mitigate | Sequencia RED->GREEN em duas tasks e dois commits; o `<automated>` da Task 1 exige explicitamente exit code de falha, e o resultado do RED e registado por escrito no SUMMARY |
| T-121-06 | Tampering | Refatoracao inadvertida do corpo de `updateRbac` a invalidar silenciosamente os Casos 7/8 ja existentes | mitigate | Proibicao explicita na `<action>`, criterio de aceitacao sobre o `git diff`, e `git status` a confirmar que `AdminControllerPlataformaAdminContencaoTest.java` fica literalmente intacto |
| T-121-SC | Tampering | Instalacoes npm/pip/cargo/Maven | n/a | Zero dependencias novas: a anotacao reutiliza um import existente e o harness de teste usa `spring-aop`/`spring-security-core`, ja no classpath e ja usados por `PlatformAdminControllerTest`. O Package Legitimacy Gate nao se aplica |
</threat_model>

<verification>
1. `cd backend && mvn test -Dtest=AdminControllerRbacAutorizacaoTest` — falha em 3 casos antes da Task 2, passa 5/5 depois.
2. `grep -cE '^[[:space:]]*@PreAuthorize' backend/src/main/java/com/lexcv/controllers/AdminController.java` devolve `2`.
3. `grep -A1 "@PreAuthorize(\"hasRole('PLATAFORMA_ADMIN')\")" ... | grep -q '@PutMapping("/rbac")'` — adjacencia confirmada.
4. `grep -B1 '@GetMapping("/rbac")' ... | grep -c 'PreAuthorize'` devolve `0`.
5. `cd backend && mvn test -Dtest=AdminControllerPlataformaAdminContencaoTest` — verde, com `git status` a mostrar o ficheiro intacto.
6. `cd backend && mvn test` — suite completa verde.
7. `cd backend && mvn spotbugs:check` — exit 0.
</verification>

<success_criteria>
- ROADMAP Phase 121, Criterio de Sucesso 3 (`PUT /api/v1/admin/rbac` deixa de aceitar chamadas de um `ADMIN` de tenant) esta satisfeito ao nivel do codigo e provado por um proxy real de method security, nao por leitura da anotacao.
- A semantica "anotacao de metodo substitui a de classe" fica demonstrada nas duas direcoes, estreando de forma verificada um mecanismo que este codebase nunca tinha exercido.
- `GET /api/v1/admin/rbac`, o gate de classe e os testes de contencao da Phase 119 ficam inalterados.
</success_criteria>

<output>
Criar `.planning/phases/LEXCV-121-fechar-suposi-es-de-tenant-nica-bloqueio-de-rbac/121-01-SUMMARY.md` quando terminar, incluindo o resultado literal do RED (casos falhados e tipo de erro) e a nota de que um `PLATAFORMA_ADMIN` passa a poder chamar `PUT /rbac` mas continua a receber `403` no `GET /rbac` — consequencia intencional da decisao de nao tocar no `GET`.
</output>
