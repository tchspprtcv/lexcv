---
phase: 71-frontend-types-schema-api-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/types/clientes.ts
  - web/src/schemas/clientes.ts
autonomous: true
requirements: [CLI-05, CLI-06]

must_haves:
  truths:
    - "The client TypeScript types contain no reference to dados_tipo, DadosTipoParticular, or DadosTipoEmpresa"
    - "A shared DocumentoTipo union type exists and includes REG_COMERCIAL"
    - "The Zod client form schema requires a nif field validated as exactly 9 numeric digits, unconditionally (both PARTICULAR and EMPRESA)"
  artifacts:
    - path: "web/src/types/clientes.ts"
      provides: "Flattened client DTO types + DocumentoTipo union"
      contains: "export type DocumentoTipo"
    - path: "web/src/schemas/clientes.ts"
      provides: "Client form schema with mandatory 9-digit NIF validation"
      contains: "nifPattern"
  key_links:
    - from: "web/src/schemas/clientes.ts"
      to: "nifPattern regex"
      via: "z.string().trim().regex(nifPattern, ...)"
      pattern: "regex\\(nifPattern"
---

<objective>
Flatten the client TypeScript DTO types (remove the `dados_tipo` JSON blob and its
`DadosTipoParticular`/`DadosTipoEmpresa` shapes), introduce a shared `DocumentoTipo`
union that includes `REG_COMERCIAL`, and make the Zod client form schema require a
`nif` field validated as exactly 9 numeric digits for every client type.

Purpose: Align the frontend contract with the flattened backend model delivered in
Phase 70 (CLI-06) and enforce mandatory NIF validation at the schema layer (CLI-05).
Output: Updated `web/src/types/clientes.ts` and `web/src/schemas/clientes.ts`.
</objective>

<execution_context>
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/LEXCV-71-frontend-types-schema-api-integration/71-CONTEXT.md
@.planning/phases/LEXCV-71-frontend-types-schema-api-integration/71-PATTERNS.md

<interfaces>
<!-- Contracts to reuse. Do not explore the codebase to rediscover these. -->

Established regex-constant + required-field pattern from web/src/schemas/setup.ts:
```typescript
export const strongPasswordPattern =
  /^(?=.*[a-z])(?=.*[A-Z])(?=.*\d)(?=.*[@$!%*?&])[A-Za-z\d@$!%*?&]{8,}$/;

adminPassword: z
  .string()
  .regex(strongPasswordPattern, "A password deve ter 8+ caracteres, ...")
```

Current dual snake_case + camelCase convention already in web/src/types/clientes.ts
(preserve it for changed fields):
```typescript
documento_tipo?: string;
documentoTipo?: string;
```

Backend DocumentoTipo enum (Phase 70): NIF | CNI | PASSAPORTE | REG_COMERCIAL
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Flatten client DTO types and add DocumentoTipo union</name>
  <files>web/src/types/clientes.ts</files>
  <read_first>
    - web/src/types/clientes.ts (full file — you edit it in place)
    - .planning/phases/LEXCV-71-frontend-types-schema-api-integration/71-PATTERNS.md (section "web/src/types/clientes.ts")
  </read_first>
  <action>
Edit web/src/types/clientes.ts exactly as follows:

1. DELETE the `DadosTipoParticular` interface (lines 1-5) and the `DadosTipoEmpresa`
   interface (lines 7-12) entirely.

2. ADD a shared union type near the top of the file (after the remaining leading
   interfaces, before `export interface Cliente`):
   `export type DocumentoTipo = "NIF" | "CNI" | "PASSAPORTE" | "REG_COMERCIAL";`

3. In `interface Cliente`: remove the `dados_tipo?: DadosTipoParticular | DadosTipoEmpresa;`
   field (line 49). Change `documento_tipo?: string;` -> `documento_tipo?: DocumentoTipo;`
   and `documentoTipo?: string;` -> `documentoTipo?: DocumentoTipo;`. Change
   `nif?: string;` -> `nif: string;` (make required, mirroring how `nome: string` is
   required here — per CLI-05). KEEP the existing flat `idade?`, `sexo?`,
   `nacionalidade?` fields (they are the flattened Particular replacement).

4. In `interface ClienteCreateRequest`: remove the `dados_tipo?: ...` field (line 80).
   Change both `documento_tipo?: string;` and `documentoTipo?: string;` to use
   `DocumentoTipo`. Change `nif?: string;` -> `nif: string;` (required, mirroring
   `nome: string` in this interface — per CLI-05).

5. In `interface ClienteUpdateRequest`: remove the `dados_tipo?: ...` field (line 101).
   Change both `documento_tipo?: string;` and `documentoTipo?: string;` to use
   `DocumentoTipo`. KEEP `nif?: string;` OPTIONAL here (partial-update / PATCH
   semantics, consistent with `nome?: string` staying optional in this interface).

Do NOT add flat Empresa sub-fields (nome_comercial/sede/representante_legal/cargo) —
CLI-06 only mandates removing `dados_tipo`, and CONTEXT.md gives discretion to drop
those Empresa sub-fields. They are not re-added in this phase.
  </action>
  <verify>
    <automated>cd web && node -e "const s=require('fs').readFileSync('src/types/clientes.ts','utf8'); const bad=/dados_tipo|DadosTipoParticular|DadosTipoEmpresa/.test(s); const hasUnion=/export type DocumentoTipo =[^;]*REG_COMERCIAL/.test(s); if(bad){console.error('FAIL: dados_tipo remnant found');process.exit(1)} if(!hasUnion){console.error('FAIL: DocumentoTipo union with REG_COMERCIAL missing');process.exit(1)} console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E "dados_tipo|DadosTipoParticular|DadosTipoEmpresa" web/src/types/clientes.ts` returns zero matches.
    - `web/src/types/clientes.ts` contains `export type DocumentoTipo = "NIF" | "CNI" | "PASSAPORTE" | "REG_COMERCIAL";`.
    - `Cliente.nif` and `ClienteCreateRequest.nif` are declared without `?` (required); `ClienteUpdateRequest.nif` stays optional.
    - Both `documento_tipo`/`documentoTipo` fields in all three interfaces are typed `DocumentoTipo` (not `string`).
  </acceptance_criteria>
  <done>Client DTO types are flattened, `dados_tipo` fully removed, `DocumentoTipo` union present with `REG_COMERCIAL`, and NIF required on Cliente/Create but optional on Update.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Remove dados_tipo API coupling from Zod schema and add mandatory 9-digit NIF</name>
  <files>web/src/schemas/clientes.ts</files>
  <read_first>
    - web/src/schemas/clientes.ts (full file — you edit it in place)
    - web/src/schemas/setup.ts (regex-constant + required-field pattern to mirror)
    - .planning/phases/LEXCV-71-frontend-types-schema-api-integration/71-PATTERNS.md (section "web/src/schemas/clientes.ts")
  </read_first>
  <behavior>
    - `nifPattern.test("123456789")` is true (exactly 9 digits).
    - `nifPattern.test("12345678")` is false (8 digits).
    - `nifPattern.test("1234567890")` is false (10 digits).
    - `nifPattern.test("12345678a")` is false (non-digit).
    - `clienteFormSchema.safeParse({ tipo:"PARTICULAR", nome:"X", nif:"123456789" }).success` is true.
    - `clienteFormSchema.safeParse({ tipo:"PARTICULAR", nome:"X", nif:"" }).success` is false (NIF required).
    - `clienteFormSchema.safeParse({ tipo:"EMPRESA", nome:"X", nif:"12345678" }).success` is false (9-digit rule applies to EMPRESA too).
    - Schema parse result contains no `dados_tipo` requirement failures for EMPRESA missing nome_comercial (that rule is removed).
  </behavior>
  <action>
Edit web/src/schemas/clientes.ts exactly as follows:

1. ADD a top-level named regex constant after the imports:
   `export const nifPattern = /^\d{9}$/;`

2. DELETE the entire `dados_tipo` object field (lines 22-34: the
   `dados_tipo: z.object({ ... }).optional(),` block).

3. CHANGE the `nif` field from `nif: optionalTrimmedString,` (line 36) to a mandatory
   validated field:
   `nif: z.string().trim().regex(nifPattern, "NIF deve conter exatamente 9 dígitos numéricos"),`

4. In the `.superRefine((data, ctx) => { ... })` block:
   - KEEP the first `if (data.documento_tipo && !data.documento_numero)` block
     (documento_tipo/documento_numero pairing rule — still valid, orthogonal).
   - KEEP the existing `if (data.documento_tipo === "NIF" && data.documento_numero)`
     digit check on `documento_numero` (leave it as-is; it validates the generic
     document-number field, which is separate from the dedicated `nif` field).
   - DELETE the entire `if (data.tipo === "EMPRESA") { ... }` block (lines 78-93) that
     references `data.dados_tipo?.nome_comercial` / `data.dados_tipo?.representante_legal`.
     Those Empresa-required rules depended on `dados_tipo` and are dropped in this phase
     (form-level Empresa validation is redesigned in Phase 72).

Use Portuguese error messages throughout, matching the file's existing convention.
Do NOT introduce a conditional "only if documento_tipo === NIF" branch for the new
`nif` field — CLI-05 makes NIF unconditionally required for both client types.
  </action>
  <verify>
    <automated>cd web && node -e "const s=require('fs').readFileSync('src/schemas/clientes.ts','utf8'); if(/dados_tipo/.test(s)){console.error('FAIL: dados_tipo remnant in schema');process.exit(1)} if(!/export const nifPattern = \/\^\\\\d\{9\}\$\//.test(s)){console.error('FAIL: nifPattern constant missing/incorrect');process.exit(1)} if(!/regex\(nifPattern/.test(s)){console.error('FAIL: nif field not wired to nifPattern');process.exit(1)} console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c dados_tipo web/src/schemas/clientes.ts` returns 0.
    - File contains `export const nifPattern = /^\d{9}$/;`.
    - The `nif` field uses `z.string().trim().regex(nifPattern, ...)` (required, not `optionalTrimmedString`).
    - The `if (data.tipo === "EMPRESA")` superRefine block referencing `dados_tipo` is removed.
    - The `documento_tipo`/`documento_numero` pairing check and the `documento_numero` NIF-digit check both remain intact.
  </acceptance_criteria>
  <done>The client form schema requires NIF as exactly 9 numeric digits for all client types, has no `dados_tipo` object or `dados_tipo`-based Empresa validation, and preserves the orthogonal documento_tipo/documento_numero rules.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser form -> Zod schema | User-entered NIF/document strings validated client-side before submit |
| frontend -> backend API | Payload crosses to Spring Boot; backend is the authoritative validator |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-71-01 | Tampering | `nifPattern` client-side NIF validation in `web/src/schemas/clientes.ts` | accept | Client-side regex `/^\d{9}$/` improves UX but is bypassable (attacker can skip the SPA and POST directly). Backend does NOT yet enforce NIF format — this is a PRE-EXISTING gap not introduced by this phase. Recorded as a backend follow-up; no data-integrity claim is made on the client regex alone. |
| T-71-02 | Information Disclosure | Flattened DTO types (`Cliente`, `Create/UpdateRequest`) | accept | Type flattening changes shape only, adds no new field to responses; no additional PII surfaced. |
| T-71-SC | Tampering | npm/pnpm installs | mitigate | This plan adds NO new dependencies (only edits two existing TS files). No install step, so the package-legitimacy checkpoint is not applicable to this plan. |
</threat_model>

<verification>
- `cd web && pnpm exec tsc --noEmit` may still fail ONLY due to the 5 consumer pages
  (fixed in plan 71-02). The two files this plan owns must themselves be internally
  consistent: `web/src/types/clientes.ts` and `web/src/schemas/clientes.ts` contain no
  `dados_tipo` references and export `DocumentoTipo` / `nifPattern` respectively.
- Run both per-task `<automated>` node checks — both print `OK`.
</verification>

<success_criteria>
- `dados_tipo`, `DadosTipoParticular`, `DadosTipoEmpresa` no longer appear in
  `web/src/types/clientes.ts` or `web/src/schemas/clientes.ts`.
- `DocumentoTipo` union (with `REG_COMERCIAL`) is exported from the types file.
- `nifPattern` (`/^\d{9}$/`) is exported and wired to a required `nif` field in the schema.
- NIF validation applies to both PARTICULAR and EMPRESA (unconditional).
</success_criteria>

<output>
Create `.planning/phases/LEXCV-71-frontend-types-schema-api-integration/71-01-SUMMARY.md` when done.
</output>
