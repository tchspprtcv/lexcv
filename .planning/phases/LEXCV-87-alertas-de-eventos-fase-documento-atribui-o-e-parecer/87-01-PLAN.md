---
phase: 87-alertas-de-eventos-fase-documento-atribui-o-e-parecer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/main/java/com/lexcv/services/NotificacaoService.java
  - backend/src/test/java/com/lexcv/services/NotificacaoServiceTest.java
autonomous: true
requirements: [NOTF-15, NOTF-16, NOTF-18, NOTF-19]

must_haves:
  truths:
    - "Chamar notificarFaseEntrada/notificarProcessoAtribuido com responsavelId nulo notifica os ADMIN e NUNCA lança exceção"
    - "notificarAdmins com excluirUserId não gera linha para o utilizador excluído (ator)"
    - "notificarDocumentoNovo e notificarParecerAtribuido excluem o ator do destinatário primário E do fan-out ADMIN"
    - "As 4 novas notificar* compõem criar(...) + notificarAdmins(...) — nenhuma chama notificacaoRepository.save diretamente"
  artifacts:
    - path: "backend/src/main/java/com/lexcv/services/NotificacaoService.java"
      provides: "4 métodos públicos notificar* + overload notificarAdmins de 8 args (actor exclusion)"
      contains: "public void notificarFaseEntrada"
    - path: "backend/src/test/java/com/lexcv/services/NotificacaoServiceTest.java"
      provides: "Testes Mockito das 4 wrappers + actor exclusion + null-guard"
      contains: "notificarFaseEntrada"
  key_links:
    - from: "NotificacaoService.notificarFaseEntrada"
      to: "NotificacaoService.criar / notificarAdmins"
      via: "composição interna"
      pattern: "notificarAdmins\\("
---

<objective>
Estender `NotificacaoService` (construído na Phase 86 antecipando exatamente esta fase — ver o comentário nas linhas 76-80 do ficheiro) com os 4 métodos públicos de conveniência que os gatilhos de controller da Phase 87 vão chamar, mais um overload de `notificarAdmins` que permite excluir o ator do fan-out.

Purpose: Concentrar toda a lógica de resolução de destinatário (+ADMIN, null-guard do responsável, exclusão do ator) num único sítio, mantendo os 6 call sites de controller (Plans 87-02/87-03) reduzidos a uma linha cada, em vez de reimplementar fan-out inline em cada gatilho. É a fundação obrigatória de que os Plans 87-02 e 87-03 dependem para compilar.
Output: `NotificacaoService.java` com `notificarFaseEntrada`, `notificarDocumentoNovo`, `notificarProcessoAtribuido`, `notificarParecerAtribuido` + overload de 8 args de `notificarAdmins`; `NotificacaoServiceTest.java` com os testes que provam o comportamento.
</objective>

<execution_context>
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/LEXCV-87-alertas-de-eventos-fase-documento-atribui-o-e-parecer/87-CONTEXT.md
@.planning/phases/LEXCV-87-alertas-de-eventos-fase-documento-atribui-o-e-parecer/87-PATTERNS.md
@backend/src/main/java/com/lexcv/services/NotificacaoService.java
@backend/src/test/java/com/lexcv/services/NotificacaoServiceTest.java
@backend/src/main/java/com/lexcv/models/Notificacao.java

<interfaces>
<!-- Assinaturas já existentes em NotificacaoService.java que as novas wrappers compõem. -->
<!-- O executor usa estas diretamente — não precisa de explorar o código. -->

Já existente (Phase 86), NÃO alterar a assinatura pública de criar:
```java
// Único ponto de escrita. Lança IllegalArgumentException se tenantId/destinatarioId forem null,
// se o destinatario não pertencer ao tenant, ou se algum campo obrigatório for branco/exceder 255.
public Notificacao criar(UUID tenantId, UUID destinatarioId, String categoria, String titulo,
                         String mensagem, String entidadeTipo, String entidadeId, String linkUrl);

// Fan-out atual (7 args). Uma linha por ADMIN do tenant. Chamado pelo teste existente (linha 146).
@Transactional
void notificarAdmins(UUID tenantId, String categoria, String titulo, String mensagem,
                     String entidadeTipo, String entidadeId, String linkUrl);
```

Valores de `categoria` (fixados por Notificacao.java:26) e `entidadeTipo` (convenção AuditLog) a usar:
| Wrapper | categoria | entidadeTipo |
|---|---|---|
| notificarFaseEntrada | "FASE_ENTRADA" | "processo" |
| notificarDocumentoNovo | "DOCUMENTO_NOVO" | "documento" |
| notificarProcessoAtribuido | "PROCESSO_ATRIBUIDO" | "processo" |
| notificarParecerAtribuido | "PARECER_ATRIBUIDO" | "parecer_solicitacao" |
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Overload notificarAdmins (actor exclusion) + wrappers do lado processo (FASE_ENTRADA, PROCESSO_ATRIBUIDO)</name>
  <files>backend/src/main/java/com/lexcv/services/NotificacaoService.java, backend/src/test/java/com/lexcv/services/NotificacaoServiceTest.java</files>
  <read_first>
    - backend/src/main/java/com/lexcv/services/NotificacaoService.java (o ficheiro a modificar; ler o comentário 76-80 que descreve exatamente estes métodos, e a assinatura de criar/notificarAdmins)
    - backend/src/test/java/com/lexcv/services/NotificacaoServiceTest.java (padrão de teste Mockito a replicar — em especial o setup de stubs `when(userRepository.findById(...))` por cada destinatário esperado, linhas 49-53 e 137-143)
    - backend/src/main/java/com/lexcv/models/Notificacao.java (getters getDestinatarioId/getCategoria/getEntidadeTipo/getLida usados nas asserções)
  </read_first>
  <behavior>
    - notificarAdmins de 8 args com excluirUserId igual ao id de um dos 2 ADMINs → apenas o OUTRO admin recebe linha (save chamado 1x, e o destinatário salvo != excluirUserId)
    - notificarAdmins de 7 args (existente) continua a notificar TODOS os admins (o teste existente na linha ~146 continua a passar sem alteração)
    - notificarFaseEntrada com responsavelId != null e 1 ADMIN → 2 linhas (responsável + admin), ambas categoria "FASE_ENTRADA", entidadeTipo "processo"
    - notificarFaseEntrada com responsavelId == null e 1 ADMIN → 1 linha (só admin), SEM lançar exceção (null-guard antes de criar)
    - notificarProcessoAtribuido com responsavelId != null e 1 ADMIN → 2 linhas; a linha do responsável tem mensagem começada por "Foi-lhe atribuído o processo "
  </behavior>
  <action>
    Escrever primeiro os testes (RED) em `NotificacaoServiceTest.java`, depois implementar (GREEN). Como Java exige compilação, adicionar as assinaturas dos métodos junto com os testes é aceitável — o RED é a falha de asserção, não apenas de compilação.

    Em `NotificacaoService.java`:
    1. Adicionar overload `void notificarAdmins(UUID tenantId, String categoria, String titulo, String mensagem, String entidadeTipo, String entidadeId, String linkUrl, UUID excluirUserId)` anotado `@Transactional`. Itera `userRepository.findByTenantIdAndRoleName(tenantId, "ADMIN")`; para cada admin, `if (excluirUserId != null && excluirUserId.equals(admin.getId())) continue;` senão chama `criar(...)`. Refatorar o `notificarAdmins` de 7 args existente para delegar no de 8 args com `excluirUserId = null` (manter a assinatura de 7 args intacta — o teste na linha ~146 depende dela).
    2. Adicionar `public void notificarFaseEntrada(UUID tenantId, UUID processoId, UUID responsavelId, String numeroProcesso, String nomeFase, String linkUrl)`. Construir `String numeroTexto = numeroProcesso != null ? numeroProcesso : "(sem número)";`, `titulo = "Nova fase"`, `mensagem = "O processo " + numeroTexto + " entrou na fase " + nomeFase`. Se `responsavelId != null`, chamar `criar(tenantId, responsavelId, "FASE_ENTRADA", titulo, mensagem, "processo", processoId.toString(), linkUrl)`. Depois SEMPRE `notificarAdmins(tenantId, "FASE_ENTRADA", titulo, mensagem, "processo", processoId.toString(), linkUrl)` (7 args — sem exclusão de ator, CONTEXT.md não a exige para FASE_ENTRADA).
    3. Adicionar `public void notificarProcessoAtribuido(UUID tenantId, UUID processoId, UUID responsavelId, String numeroProcesso, String linkUrl)`. `numeroTexto` como acima, `titulo = "Processo atribuído"`. Mensagem do designado (2ª pessoa, texto travado pela CONTEXT.md): `mensagemDest = "Foi-lhe atribuído o processo " + numeroTexto + "."`. Mensagem para ADMIN (3ª pessoa — copy à discrição, sem nome do ator per CONTEXT.md): `mensagemAdmin = "O processo " + numeroTexto + " foi atribuído a um novo responsável."`. Se `responsavelId != null`, `criar(tenantId, responsavelId, "PROCESSO_ATRIBUIDO", titulo, mensagemDest, "processo", processoId.toString(), linkUrl)`. Depois SEMPRE `notificarAdmins(tenantId, "PROCESSO_ATRIBUIDO", titulo, mensagemAdmin, "processo", processoId.toString(), linkUrl)` (sem exclusão de ator).

    Nos testes, replicar o padrão existente: `@Mock` dos 2 repositórios, `when(userRepository.findById(<id>)).thenReturn(Optional.of(User.builder().id(<id>).tenantId(TENANT_ID).build()))` por cada destinatário esperado, `when(userRepository.findByTenantIdAndRoleName(TENANT_ID, "ADMIN")).thenReturn(List.of(...))`, `when(notificacaoRepository.save(any())).thenAnswer(inv -> inv.getArgument(0))`, e `ArgumentCaptor<Notificacao>` para asserir destinatários e mensagens. Para o caso null-guard usar `verify(notificacaoRepository, times(1)).save(...)` e confirmar que NÃO foi lançada exceção.
  </action>
  <verify>
    <automated>mvn -f backend/pom.xml -q test -Dtest=NotificacaoServiceTest</automated>
  </verify>
  <acceptance_criteria>
    - `mvn -f backend/pom.xml -q test -Dtest=NotificacaoServiceTest` passa (inclui os testes antigos + os novos)
    - `NotificacaoService.java` contém `notificarFaseEntrada`, `notificarProcessoAtribuido` e um `notificarAdmins` com parâmetro `excluirUserId`
    - O `notificarAdmins` de 7 args continua a existir e delega no de 8 args (grep encontra ambas as assinaturas)
    - Teste prova: excluirUserId salta o admin correspondente; responsavelId null → só admin, sem exceção
  </acceptance_criteria>
  <done>Os métodos do lado processo existem, testados, e os testes pré-existentes da Phase 86 continuam verdes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wrappers com exclusão de ator (DOCUMENTO_NOVO, PARECER_ATRIBUIDO)</name>
  <files>backend/src/main/java/com/lexcv/services/NotificacaoService.java, backend/src/test/java/com/lexcv/services/NotificacaoServiceTest.java</files>
  <read_first>
    - backend/src/main/java/com/lexcv/services/NotificacaoService.java (agora já com o overload de 8 args da Task 1 — reutilizá-lo)
    - backend/src/test/java/com/lexcv/services/NotificacaoServiceTest.java (mesmos padrões de stub/captor da Task 1)
  </read_first>
  <behavior>
    - notificarDocumentoNovo com destinatarios=[userX, atorId] e 1 ADMIN (admin != ator) → 2 linhas: userX e admin; atorId NUNCA recebe linha
    - notificarDocumentoNovo com destinatarios=[userX, userX] (duplicado) e 0 ADMIN → 1 única linha (dedup por Set)
    - notificarDocumentoNovo em que o ADMIN == atorId → esse admin é excluído do fan-out
    - notificarParecerAtribuido com advogadoId != atorId e 1 ADMIN → 2 linhas (advogado + admin)
    - notificarParecerAtribuido com advogadoId == atorId (auto-atribuição) e 1 ADMIN (admin != ator) → 1 linha (só admin); o próprio ator/advogado é excluído do destinatário primário
  </behavior>
  <action>
    Escrever testes primeiro (RED), depois implementar (GREEN), reutilizando o overload de 8 args da Task 1 para o fan-out ADMIN com exclusão do ator.

    Em `NotificacaoService.java`:
    1. Adicionar `public void notificarDocumentoNovo(UUID tenantId, String documentoId, java.util.Collection<UUID> destinatarios, String nomeDocumento, String linkUrl, UUID atorId)`. `titulo = "Novo documento"`, `mensagem = "Foi adicionado o documento \"" + nomeDocumento + "\"."`. Deduplicar destinatários com `new java.util.LinkedHashSet<>(destinatarios == null ? java.util.List.of() : destinatarios)`; para cada `dest` do set: `if (dest != null && !dest.equals(atorId)) criar(tenantId, dest, "DOCUMENTO_NOVO", titulo, mensagem, "documento", documentoId, linkUrl);`. Depois `notificarAdmins(tenantId, "DOCUMENTO_NOVO", titulo, mensagem, "documento", documentoId, linkUrl, atorId)` (overload de 8 args — exclui o ator). Mensagem única (evento em 3ª pessoa serve responsável e admins igualmente).
    2. Adicionar `public void notificarParecerAtribuido(UUID tenantId, String solicitacaoId, UUID advogadoId, String linkUrl, UUID atorId)`. `titulo = "Parecer atribuído"`, `mensagemDest = "Foi-lhe atribuído um parecer jurídico."`, `mensagemAdmin = "Um parecer jurídico foi atribuído a um advogado."`. Se `advogadoId != null && !advogadoId.equals(atorId)`, `criar(tenantId, advogadoId, "PARECER_ATRIBUIDO", titulo, mensagemDest, "parecer_solicitacao", solicitacaoId, linkUrl)`. Depois `notificarAdmins(tenantId, "PARECER_ATRIBUIDO", titulo, mensagemAdmin, "parecer_solicitacao", solicitacaoId, linkUrl, atorId)`.

    Nota de design (registar no SUMMARY): NÃO há dedup entre o destinatário primário e o fan-out ADMIN — se o responsável/advogado também for ADMIN recebe 2 linhas. Isto preserva o comportamento fixado na Phase 86 (`notificarAdmins` é independente) e não deve ser "corrigido" aqui.

    Nos testes, adicionar `private static final UUID ATOR = UUID.randomUUID();` e stubs `findById` por cada destinatário efetivamente salvo (NÃO stub para o ator quando ele é excluído — senão fica um stub Mockito não usado; usar `lenient()` só se necessário). Asserir via `ArgumentCaptor` a lista exata de `getDestinatarioId()` e que o ator nunca aparece.
  </action>
  <verify>
    <automated>mvn -f backend/pom.xml -q test -Dtest=NotificacaoServiceTest</automated>
  </verify>
  <acceptance_criteria>
    - `mvn -f backend/pom.xml -q test -Dtest=NotificacaoServiceTest` passa
    - `NotificacaoService.java` contém `notificarDocumentoNovo` e `notificarParecerAtribuido`, ambos com parâmetro `atorId` e ambos a chamar o `notificarAdmins` de 8 args
    - Teste prova: ator excluído do primário E do fan-out; duplicados no `Collection` geram uma só linha
    - `mvn -f backend/pom.xml -q -DskipTests compile` compila todo o backend (garante que nenhum call site existente foi quebrado)
  </acceptance_criteria>
  <done>Os 4 métodos notificar* existem e estão testados; o backend compila; toda a resolução de destinatário está centralizada em NotificacaoService.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| controller→NotificacaoService | Os controllers (Plans 87-02/03) passam ids já validados; o serviço é o choke point de escrita de notificações |
| NotificacaoService→t_notificacao | Toda a criação de linha passa por `criar(...)`, que revalida o destinatário contra o tenant |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-87-01-01 | Information Disclosure | wrappers notificar* | mitigate | Todas passam o `tenantId` recebido a `criar(...)`, que valida `userRepository.findById(dest).filter(u -> tenantId.equals(u.getTenantId()))` e lança se falhar; `notificarAdmins` usa `findByTenantIdAndRoleName(tenantId, "ADMIN")` — só admins do próprio tenant. Impossível endereçar destinatário de outro tenant |
| T-87-01-02 | Denial of Service | notificarFaseEntrada/notificarProcessoAtribuido | mitigate | `criar()` lança `IllegalArgumentException` em destinatarioId null; wrappers fazem null-guard de `responsavelId` ANTES de `criar` para não rebentar a transação do controller pai quando um processo ainda não tem responsável (estado comum pré-Phase-87). Testado |
| T-87-01-03 | Repudiation/Info Disclosure | actor exclusion | mitigate | DOCUMENTO_NOVO e PARECER_ATRIBUIDO excluem o `atorId` do primário e do fan-out via overload de 8 args — o ator não é notificado da sua própria ação, conforme CONTEXT.md. Testado |
| T-87-01-SC | Tampering (supply chain) | dependências | accept | Nenhum package instalado nesta fase; usa apenas JUnit 5 + Mockito já presentes em `spring-boot-starter-test`. Sem alteração de `pom.xml` |
</threat_model>

<verification>
- `mvn -f backend/pom.xml -q test -Dtest=NotificacaoServiceTest` verde (testes antigos + novos)
- `mvn -f backend/pom.xml -q -DskipTests compile` compila
- grep confirma as 4 assinaturas públicas `notificar*` e o overload `excluirUserId`
</verification>

<success_criteria>
NotificacaoService expõe `notificarFaseEntrada`, `notificarDocumentoNovo`, `notificarProcessoAtribuido`, `notificarParecerAtribuido` e o overload de 8 args de `notificarAdmins`; testes Mockito provam fan-out, exclusão de ator, null-guard e dedup; o backend inteiro continua a compilar; nenhuma classe fora de NotificacaoService escreve notificações.
</success_criteria>

<output>
Create `.planning/phases/LEXCV-87-alertas-de-eventos-fase-documento-atribui-o-e-parecer/87-01-SUMMARY.md` when done
</output>
