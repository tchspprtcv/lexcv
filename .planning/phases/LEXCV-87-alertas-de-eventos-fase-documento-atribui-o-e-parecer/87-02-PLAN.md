---
phase: 87-alertas-de-eventos-fase-documento-atribui-o-e-parecer
plan: 02
type: execute
wave: 2
depends_on: [87-01]
files_modified:
  - backend/src/main/java/com/lexcv/controllers/ResourceController.java
autonomous: true
requirements: [NOTF-15, NOTF-16, NOTF-17, NOTF-18]

must_haves:
  truths:
    - "Adicionar uma fase a um processo cria uma notificação FASE_ENTRADA para o responsável (se houver) + ADMIN, com link para a aba Fases"
    - "Fazer upload de um documento novo ligado a um processo notifica o responsável do processo (+ADMIN); ligado só a um cliente notifica a equipa advogados/administrativos (+ADMIN); ligado a ambos notifica só o responsável do processo; o ator (uploader) é sempre excluído"
    - "Existe o endpoint PUT /processos/{id}/atribuir gated por processos:manage que valida que o novo responsavelId pertence ao tenant e notifica o novo responsável (+ADMIN) de imediato"
    - "Criar um processo com responsavelId definido notifica esse responsável (+ADMIN)"
  artifacts:
    - path: "backend/src/main/java/com/lexcv/controllers/ResourceController.java"
      provides: "3 gatilhos de notificação + endpoint de reatribuição + injeção de NotificacaoService"
      contains: "atribuirResponsavel"
  key_links:
    - from: "ResourceController.createProcessoFase"
      to: "notificacaoService.notificarFaseEntrada"
      via: "chamada após save"
      pattern: "notificarFaseEntrada\\("
    - from: "ResourceController.atribuirResponsavel"
      to: "responsavelId tenant validation"
      via: "userRepository.findById + tenant check (cópia de createProcesso)"
      pattern: "não pertence a este tenant"
---

<objective>
Ligar 3 dos 4 gatilhos de notificação da Phase 87 em `ResourceController` e criar o novo endpoint dedicado de reatribuição de responsável (NOTF-17), que também dispara o alerta de atribuição (NOTF-18).

Purpose: Tornar observáveis os requisitos NOTF-15 (entrada de fase), NOTF-16 (documento novo) e a metade backend de NOTF-17/NOTF-18 (reatribuição + alerta). Cada gatilho é uma chamada fina a um método `notificar*` de NotificacaoService (Plan 87-01) no ponto onde a entidade já é gravada hoje. O endpoint de reatribuição é a primeira forma de definir `Processo.responsavelId` fora de `createProcesso`.
Output: `ResourceController.java` com o campo `notificacaoService` injetado, gatilhos em `createProcessoFase`, `uploadDocumento`, `createProcesso`, e o novo `PUT /processos/{id}/atribuir`.
</objective>

<execution_context>
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/LEXCV-87-alertas-de-eventos-fase-documento-atribui-o-e-parecer/87-CONTEXT.md
@.planning/phases/LEXCV-87-alertas-de-eventos-fase-documento-atribui-o-e-parecer/87-PATTERNS.md
@backend/src/main/java/com/lexcv/controllers/ResourceController.java
@backend/src/main/java/com/lexcv/models/Processo.java

<interfaces>
<!-- Contratos criados no Plan 87-01 (já existem quando este plano corre). Usar diretamente. -->
```java
// NotificacaoService — métodos a chamar (Plan 87-01):
public void notificarFaseEntrada(UUID tenantId, UUID processoId, UUID responsavelId,
                                 String numeroProcesso, String nomeFase, String linkUrl); // responsavelId nullable OK
public void notificarDocumentoNovo(UUID tenantId, String documentoId, java.util.Collection<UUID> destinatarios,
                                   String nomeDocumento, String linkUrl, UUID atorId); // exclui atorId, dedup interno
public void notificarProcessoAtribuido(UUID tenantId, UUID processoId, UUID responsavelId,
                                       String numeroProcesso, String linkUrl); // responsavelId nullable OK, sem exclusão de ator
```

```java
// Repositórios de equipa do cliente — JÁ injetados em ResourceController (campos linhas 69-70), tenant-scoped:
clienteAdvogadoRepository.findByClienteIdAndTenantId(UUID clienteId, UUID tenantId);       // List<ClienteAdvogado>, .getUserId()
clienteAdministrativoRepository.findByClienteIdAndTenantId(UUID clienteId, UUID tenantId); // List<ClienteAdministrativo>, .getUserId()
```

```java
// Extração do ator (padrão já usado neste ficheiro e em ParecerController:154-155):
Authentication auth = SecurityContextHolder.getContext().getAuthentication();
UserPrincipal principal = (UserPrincipal) auth.getPrincipal();
UUID atorId = principal.getUserId();
// Processo getters relevantes: getResponsavelId() (nullable), getNumeroProcesso() (nullable), getId(), getTenantId()
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Injetar NotificacaoService + gatilho FASE_ENTRADA em createProcessoFase (NOTF-15)</name>
  <files>backend/src/main/java/com/lexcv/controllers/ResourceController.java</files>
  <read_first>
    - backend/src/main/java/com/lexcv/controllers/ResourceController.java (imports 1-43; lista de campos injetados 50-73; getTenantId 117-120; createProcessoFase 1594-1617)
    - backend/src/main/java/com/lexcv/services/NotificacaoService.java (assinatura de notificarFaseEntrada — Plan 87-01)
  </read_first>
  <action>
    1. Adicionar o import `import com.lexcv.services.NotificacaoService;` junto do import já existente `import com.lexcv.services.RiscoPrazoService;` (linha 7).
    2. Adicionar o campo `private final NotificacaoService notificacaoService;` à lista de dependências `@RequiredArgsConstructor` (a seguir a `private final RiscoPrazoService riscoPrazoService;`, linha 68). Lombok gera o construtor — não escrever construtor manual.
    3. Em `createProcessoFase` (1594-1617): a última linha hoje é `return ResponseEntity.status(HttpStatus.CREATED).body(processoFaseRepository.save(pf));`. Substituir por: capturar `ProcessoFase saved = processoFaseRepository.save(pf);`, depois chamar `notificacaoService.notificarFaseEntrada(processo.getTenantId(), id, processo.getResponsavelId(), processo.getNumeroProcesso(), faseNome, "/processos/" + id + "?tab=fases");`, depois `return ResponseEntity.status(HttpStatus.CREATED).body(saved);`. Usar a variável local `faseNome` já existente (linha 1601) para o nome da fase e o path var `id` para o processoId. O `linkUrl` "?tab=fases" é copy backend correta; a navegação real por aba é da Phase 89 (não wire frontend aqui — a aba é estado React local hoje, ver 87-UI-SPEC.md).
    NÃO tentar corrigir o bug pré-existente de `createProcessoFase` não desativar fases anteriores (Deferred em CONTEXT.md) — apenas disparar a notificação sempre que o método é chamado.
  </action>
  <verify>
    <automated>mvn -f backend/pom.xml -q -DskipTests compile</automated>
  </verify>
  <acceptance_criteria>
    - `mvn -f backend/pom.xml -q -DskipTests compile` compila
    - grep em ResourceController.java encontra `private final NotificacaoService notificacaoService;` e `notificarFaseEntrada(`
    - O `linkUrl` passado contém `?tab=fases`
    - Nenhuma alteração ao comportamento de `ativa`/desativação de fases (o diff toca apenas no save + notificação)
  </acceptance_criteria>
  <done>NotificacaoService injetado; entrada de fase dispara FASE_ENTRADA para responsável (se houver) + ADMIN.</done>
</task>

<task type="auto">
  <name>Task 2: Gatilho DOCUMENTO_NOVO em uploadDocumento com branching processo/cliente + exclusão de ator (NOTF-16)</name>
  <files>backend/src/main/java/com/lexcv/controllers/ResourceController.java</files>
  <read_first>
    - backend/src/main/java/com/lexcv/controllers/ResourceController.java (uploadDocumento completo 2395-2483 — em especial validação de tenant 2414-2427, ramo replace vs novo 2433-2472, save 2474)
    - backend/src/main/java/com/lexcv/services/NotificacaoService.java (assinatura de notificarDocumentoNovo — Plan 87-01)
    - .planning/phases/LEXCV-87-alertas-de-eventos-fase-documento-atribui-o-e-parecer/87-CONTEXT.md (secção "Notificação de Novo Documento" — regra de precedência processo>cliente e exclusão de ator)
  </read_first>
  <action>
    Em `uploadDocumento` (2395-2483), inserir o gatilho DENTRO do bloco `try`, imediatamente após `Documento saved = documentoRepository.save(documento);` (linha 2474) e ANTES do `return` (2475). Disparar APENAS quando `replaceId == null` (upload de documento genuinamente novo — NOTF-16 diz "novo documento adicionado"; uma substituição de versão não é um documento novo). Registar esta decisão de discrição no SUMMARY.

    Bloco a inserir (dentro de `if (replaceId == null) { ... }`):
    1. Extrair o ator: `Authentication auth = SecurityContextHolder.getContext().getAuthentication(); UserPrincipal principal = (UserPrincipal) auth.getPrincipal(); UUID atorId = principal.getUserId();` (imports Authentication/SecurityContextHolder/UserPrincipal já presentes — linhas 3,26,27).
    2. `UUID tenantId = getTenantId();`
    3. Precedência processo > cliente:
       - Se `saved.getProcessoId() != null`: `Processo proc = processoRepository.findById(saved.getProcessoId()).orElse(null); UUID resp = proc != null ? proc.getResponsavelId() : null; java.util.List<UUID> dests = resp != null ? java.util.List.of(resp) : java.util.List.of();` e chamar `notificacaoService.notificarDocumentoNovo(tenantId, saved.getId().toString(), dests, saved.getNome(), "/processos/" + saved.getProcessoId(), atorId);`.
       - Senão, se `saved.getClienteId() != null`: construir `java.util.List<UUID> dests = new java.util.ArrayList<>();`, `clienteAdvogadoRepository.findByClienteIdAndTenantId(saved.getClienteId(), tenantId).forEach(a -> dests.add(a.getUserId()));`, `clienteAdministrativoRepository.findByClienteIdAndTenantId(saved.getClienteId(), tenantId).forEach(a -> dests.add(a.getUserId()));` e chamar `notificacaoService.notificarDocumentoNovo(tenantId, saved.getId().toString(), dests, saved.getNome(), "/clientes/" + saved.getClienteId(), atorId);`.
       O uso de `if / else if` garante a regra de precedência: um documento ligado a AMBOS notifica só o responsável do processo, nunca também a equipa do cliente. O dedup de destinatários e o fan-out ADMIN (com exclusão do ator) são feitos dentro de `notificarDocumentoNovo` (Plan 87-01).
    NÃO alterar a lógica de storage/replace nem as validações de tenant existentes (2414-2427) — o diff é puramente aditivo após o save.
  </action>
  <verify>
    <automated>mvn -f backend/pom.xml -q -DskipTests compile</automated>
  </verify>
  <acceptance_criteria>
    - `mvn -f backend/pom.xml -q -DskipTests compile` compila
    - O gatilho está dentro de `if (replaceId == null)` (grep confirma que `notificarDocumentoNovo` aparece após a condição de replaceId null)
    - Ambos os ramos (`getProcessoId() != null` e `getClienteId() != null`) chamam `notificarDocumentoNovo`, em `if/else if` (precedência processo)
    - Os lookups de equipa usam `findByClienteIdAndTenantId(..., tenantId)` (tenant-scoped) — nunca uma variante sem tenant
    - `mvn -f backend/pom.xml -q spotbugs:check` não introduz novos findings de segurança neste método
  </acceptance_criteria>
  <done>Upload de documento novo notifica o destinatário correto (responsável do processo, ou equipa do cliente quando não há processo), sempre + ADMIN, sempre excluindo o uploader.</done>
</task>

<task type="auto">
  <name>Task 3: Gatilho PROCESSO_ATRIBUIDO em createProcesso + novo endpoint PUT /processos/{id}/atribuir (NOTF-17, NOTF-18)</name>
  <files>backend/src/main/java/com/lexcv/controllers/ResourceController.java</files>
  <read_first>
    - backend/src/main/java/com/lexcv/controllers/ResourceController.java (createProcesso 965-979 — bloco de validação de tenant do responsavelId 970-976; getProcesso 981-987 para padrão de 404)
    - backend/src/main/java/com/lexcv/controllers/ParecerController.java (atribuirAdvogado 235-289 — analog estrutural exato: parse UUID do body, 404 se entidade não pertence ao tenant, save, resposta ok)
    - backend/src/main/java/com/lexcv/services/NotificacaoService.java (assinatura de notificarProcessoAtribuido — Plan 87-01)
  </read_first>
  <action>
    1. Gatilho de atribuição inicial em `createProcesso` (965-979): após `Processo saved = processoRepository.save(processo);` (linha 977) e antes do `return`, inserir `if (saved.getResponsavelId() != null) { notificacaoService.notificarProcessoAtribuido(tenantId, saved.getId(), saved.getResponsavelId(), saved.getNumeroProcesso(), "/processos/" + saved.getId()); }`. Sem exclusão de ator (CONTEXT.md não a exige para PROCESSO_ATRIBUIDO). `tenantId` já é variável local (linha 968).

    2. Novo endpoint de reatribuição, colocado imediatamente a seguir a `createProcesso` (após linha 979). Espelhar `ParecerController.atribuirAdvogado` (mesma forma: `Map<String,String>` + `UUID.fromString`), mudando o scope para `manage`:
       - Anotações: `@PreAuthorize("hasAuthority('processos:manage')")` (CONTEXT.md trava isto em `manage`, NÃO `edit`), `@PutMapping("/processos/{id}/atribuir")`, `@Transactional`.
       - Assinatura: `public ResponseEntity<?> atribuirResponsavel(@PathVariable UUID id, @RequestBody Map<String, String> body)`.
       - Corpo:
         a. `UUID tenantId = getTenantId();`
         b. `String responsavelIdRaw = body.get("responsavelId");` — se null ou blank → `ResponseEntity.status(HttpStatus.BAD_REQUEST).body(Map.of("message", "responsavelId é obrigatório"))`.
         c. Parse: `UUID responsavelId; try { responsavelId = UUID.fromString(responsavelIdRaw); } catch (IllegalArgumentException e) { return 400 com message "responsavelId inválido"; }`.
         d. Carregar processo: `Processo processo = processoRepository.findById(id).orElse(null); if (processo == null || !processo.getTenantId().equals(tenantId)) return ResponseEntity.status(HttpStatus.NOT_FOUND).body(Map.of("message", "Processo não encontrado"));`.
         e. Validar posse do novo responsável — CÓPIA VERBATIM do bloco de createProcesso (970-976): `User responsavel = userRepository.findById(responsavelId).orElse(null); if (responsavel == null || !tenantId.equals(responsavel.getTenantId())) return ResponseEntity.status(HttpStatus.BAD_REQUEST).body(Map.of("message", "responsavelId não pertence a este tenant"));`. Manter esta string de erro exata — o frontend (Plan 87-04) exibe a `message` do backend tal como vem.
         f. `processo.setResponsavelId(responsavelId); Processo saved = processoRepository.save(processo);`.
         g. `notificacaoService.notificarProcessoAtribuido(tenantId, saved.getId(), saved.getResponsavelId(), saved.getNumeroProcesso(), "/processos/" + saved.getId());`.
         h. `return ResponseEntity.ok(saved);` — devolver a entidade `Processo` crua, exatamente como `formalizar`/`transicao` já devolvem, para que o `normalizeProcesso()` do frontend a trate sem alterações.
       NÃO notificar o responsável anterior (Out of Scope em REQUIREMENTS.md). NÃO permitir responsavelId null/vazio (sem caminho de "desatribuir" nesta fase).
  </action>
  <verify>
    <automated>mvn -f backend/pom.xml -q -DskipTests compile</automated>
  </verify>
  <acceptance_criteria>
    - `mvn -f backend/pom.xml -q -DskipTests compile` compila
    - grep encontra `@PutMapping("/processos/{id}/atribuir")` e `hasAuthority('processos:manage')` no mesmo método `atribuirResponsavel`
    - grep encontra a string exata `responsavelId não pertence a este tenant` dentro de `atribuirResponsavel` (validação de tenant presente)
    - O endpoint devolve `ResponseEntity.ok(saved)` com o `Processo` cru (mesma forma que `formalizar`)
    - `createProcesso` chama `notificarProcessoAtribuido` só quando `saved.getResponsavelId() != null`
    - `mvn -f backend/pom.xml -q spotbugs:check` sem novos findings de alta severidade no endpoint
  </acceptance_criteria>
  <done>Existe um endpoint de reatribuição seguro (manage-gated, tenant-validado) que notifica o novo responsável + ADMIN; a criação de processo com responsável também notifica.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cliente→API (HTTP) | `responsavelId`, `processoId`, `clienteId`, ficheiro e path vars são input não confiável |
| API→NotificacaoService | ids resolvidos e validados antes de chegarem ao serviço |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-87-02-01 | Elevation of Privilege | PUT /processos/{id}/atribuir | mitigate | `@PreAuthorize("hasAuthority('processos:manage')")` — reatribuir é ação de gestão, gate mais estrito que os outros writes de /processos (que usam `edit`); alinhado com createProcesso/formalizar |
| T-87-02-02 | Tampering (IDOR) | atribuirResponsavel | mitigate | Dupla verificação: (a) processo carregado e `processo.getTenantId().equals(tenantId)` senão 404; (b) novo `responsavelId` validado `userRepository.findById(...)` + `tenantId.equals(responsavel.getTenantId())` senão 400 — cópia verbatim do bloco já auditado de createProcesso. Impossível atribuir a utilizador de outro tenant ou reatribuir processo alheio |
| T-87-02-03 | Information Disclosure | uploadDocumento branch cliente-sem-processo | mitigate | A equipa é resolvida SÓ via `findByClienteIdAndTenantId(clienteId, tenantId)` (tenant-scoped nas duas junction tables); o `clienteId` já foi validado contra o tenant (2414-2419) antes; `criar()` revalida cada destinatário contra o tenant. Nenhum caminho expõe utilizadores/clientes de outro tenant |
| T-87-02-04 | Information Disclosure | conteúdo/linkUrl da notificação | mitigate | Mensagens contêm apenas numeroProcesso/nomeFase/nome do documento de entidades do próprio tenant; destinatários são todos tenant-scoped. Sem dados sensíveis cross-tenant no titulo/mensagem/linkUrl |
| T-87-02-05 | Repudiation | uploadDocumento actor exclusion | mitigate | `atorId = principal.getUserId()` e passado a `notificarDocumentoNovo`, que exclui o ator do primário e do fan-out — o uploader não se auto-notifica |
| T-87-02-SC | Tampering (supply chain) | dependências | accept | Sem instalação de packages; sem alteração de `pom.xml`; usa apenas beans/repos já existentes |
</threat_model>

<verification>
- `mvn -f backend/pom.xml -q -DskipTests compile` compila
- `mvn -f backend/pom.xml -q spotbugs:check` (FindSecBugs) sem novos findings de alta severidade
- grep confirma: campo `notificacaoService`, chamadas `notificarFaseEntrada`/`notificarDocumentoNovo`/`notificarProcessoAtribuido`, endpoint `atribuirResponsavel` com gate `processos:manage` e validação de tenant
- Live HTTP round-trip (reatribuir + confirmar notificação criada) fica para UAT humano — consistente com o padrão do projeto (constrangimento de credenciais/lockout registado em STATE.md)
</verification>

<success_criteria>
Os 3 gatilhos (fase, documento, criação/atribuição de processo) disparam a notificação certa via NotificacaoService, e o endpoint `PUT /processos/{id}/atribuir` existe, é manage-gated, valida o tenant do novo responsável e notifica-o de imediato — sem regressão nas validações de tenant/storage existentes.
</success_criteria>

<output>
Create `.planning/phases/LEXCV-87-alertas-de-eventos-fase-documento-atribui-o-e-parecer/87-02-SUMMARY.md` when done
</output>
