---
phase: 87-alertas-de-eventos-fase-documento-atribui-o-e-parecer
plan: 03
type: execute
wave: 2
depends_on: [87-01]
files_modified:
  - backend/src/main/java/com/lexcv/controllers/ParecerController.java
autonomous: true
requirements: [NOTF-19]

must_haves:
  truths:
    - "Criar uma solicitação de parecer com advogadoId definido notifica esse advogado (+ADMIN), excluindo o ator"
    - "Reatribuir o advogado de um parecer via PUT .../{id}/atribuir notifica o novo advogado (+ADMIN), excluindo o ator, e NÃO notifica o advogado anterior"
  artifacts:
    - path: "backend/src/main/java/com/lexcv/controllers/ParecerController.java"
      provides: "Campo NotificacaoService + 2 gatilhos PARECER_ATRIBUIDO"
      contains: "notificarParecerAtribuido"
  key_links:
    - from: "ParecerController.atribuirAdvogado"
      to: "notificacaoService.notificarParecerAtribuido"
      via: "chamada após save + AuditLog"
      pattern: "notificarParecerAtribuido\\("
---

<objective>
Ligar o 4º gatilho de notificação da Phase 87 — PARECER_ATRIBUIDO — nos dois pontos onde um advogado passa a estar atribuído a uma solicitação de parecer: na criação (quando `advogadoId` já vem no corpo) e na reatribuição posterior (`atribuirAdvogado`). Fecha o gap NOTF-05/06/07 que foi deferido na v2.6 exatamente por falta desta infraestrutura de notificações.

Purpose: Tornar observável o requisito NOTF-19 (advogado alertado quando lhe é atribuído um parecer). Cada gatilho é uma chamada fina a `notificarParecerAtribuido` (Plan 87-01) no ponto onde a entidade já é gravada, reutilizando o `principal` que ambos os métodos já extraem para o AuditLog.
Output: `ParecerController.java` com o campo `notificacaoService` injetado e a chamada de notificação em `createSolicitacao` e `atribuirAdvogado`.
</objective>

<execution_context>
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/LEXCV-87-alertas-de-eventos-fase-documento-atribui-o-e-parecer/87-CONTEXT.md
@.planning/phases/LEXCV-87-alertas-de-eventos-fase-documento-atribui-o-e-parecer/87-PATTERNS.md
@backend/src/main/java/com/lexcv/controllers/ParecerController.java

<interfaces>
<!-- Contrato criado no Plan 87-01 (já existe quando este plano corre). Usar diretamente. -->
```java
// NotificacaoService (Plan 87-01):
public void notificarParecerAtribuido(UUID tenantId, String solicitacaoId, UUID advogadoId,
                                      String linkUrl, UUID atorId);
// - exclui atorId do destinatário primário E do fan-out ADMIN
// - null-guarda advogadoId internamente
// - categoria "PARECER_ATRIBUIDO", entidadeTipo "parecer_solicitacao"
```
Base path do controller: `@RequestMapping("/api/v1/pareceres/solicitacoes")`.
`principal.getUserId()` já é obtido em createSolicitacao (linhas 154-155) e em atribuirAdvogado (277-278) para o AuditLog — reutilizar.
`ParecerSolicitacao`: getId() (UUID), getAdvogadoId() (UUID, nullable).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Injetar NotificacaoService + gatilho na criação (createSolicitacao)</name>
  <files>backend/src/main/java/com/lexcv/controllers/ParecerController.java</files>
  <read_first>
    - backend/src/main/java/com/lexcv/controllers/ParecerController.java (imports 1-32; campos 40-46; createSolicitacao 100-166, com o ramo advogado-na-criação 141-149, save 151 e extração de principal 154-155)
    - backend/src/main/java/com/lexcv/services/NotificacaoService.java (assinatura de notificarParecerAtribuido — Plan 87-01)
  </read_first>
  <action>
    1. Adicionar o import `import com.lexcv.services.NotificacaoService;` junto do import já existente `import com.lexcv.services.StorageService;` (linha 15).
    2. Adicionar o campo `private final NotificacaoService notificacaoService;` à lista `@RequiredArgsConstructor` (a seguir a `private final AuditLogRepository auditLogRepository;`, linha 46). Lombok gera o construtor.
    3. Em `createSolicitacao`: depois do bloco AuditLog que grava usando `principal` (após a linha 163, onde `principal` já está em scope, dentro do mesmo método), inserir `if (saved.getAdvogadoId() != null) { notificacaoService.notificarParecerAtribuido(tenantId, saved.getId().toString(), saved.getAdvogadoId(), "/pareceres/" + saved.getId(), principal.getUserId()); }`. O guard `saved.getAdvogadoId() != null` reflete exatamente o ramo em que um advogado foi atribuído na criação (141-149). `tenantId` já é variável local (linha 103).
  </action>
  <verify>
    <automated>mvn -f backend/pom.xml -q -DskipTests compile</automated>
  </verify>
  <acceptance_criteria>
    - `mvn -f backend/pom.xml -q -DskipTests compile` compila
    - grep encontra `private final NotificacaoService notificacaoService;` em ParecerController.java
    - grep encontra `notificarParecerAtribuido(` dentro de createSolicitacao, guardado por `saved.getAdvogadoId() != null`
    - O `atorId` passado é `principal.getUserId()` (reuso da extração existente, sem nova extração duplicada)
  </acceptance_criteria>
  <done>Criar solicitação com advogado atribuído notifica esse advogado + ADMIN, excluindo o criador.</done>
</task>

<task type="auto">
  <name>Task 2: Gatilho na reatribuição (atribuirAdvogado)</name>
  <files>backend/src/main/java/com/lexcv/controllers/ParecerController.java</files>
  <read_first>
    - backend/src/main/java/com/lexcv/controllers/ParecerController.java (atribuirAdvogado 235-289 — save em 274, extração de principal em 277-278, AuditLog em 279-286, return em 288)
    - backend/src/main/java/com/lexcv/services/NotificacaoService.java (assinatura de notificarParecerAtribuido)
  </read_first>
  <action>
    Em `atribuirAdvogado` (235-289): após o bloco AuditLog (linha 286) e antes de `return ResponseEntity.ok(saved);` (288), inserir `notificacaoService.notificarParecerAtribuido(tenantId, saved.getId().toString(), saved.getAdvogadoId(), "/pareceres/" + saved.getId(), principal.getUserId());`. `tenantId` (239) e `principal` (277-278) já estão em scope. Sem guard adicional — `advogadoId` já foi validado não-null e como ADVOGADO do tenant (265-269).
    NÃO ler nem notificar o `advogadoId` anterior: `atribuirAdvogado` sobrescreve `advogadoId` sem o ler antes (271) — manter assim (CONTEXT.md e REQUIREMENTS.md Out of Scope: só o novo advogado é notificado).
  </action>
  <verify>
    <automated>mvn -f backend/pom.xml -q -DskipTests compile</automated>
  </verify>
  <acceptance_criteria>
    - `mvn -f backend/pom.xml -q -DskipTests compile` compila
    - grep encontra `notificarParecerAtribuido(` dentro de atribuirAdvogado, após o AuditLog e antes do return
    - Não é introduzida nenhuma leitura do advogadoId anterior (o método continua a não notificar o advogado anterior)
    - `mvn -f backend/pom.xml -q spotbugs:check` sem novos findings de alta severidade
  </acceptance_criteria>
  <done>Reatribuir o advogado de um parecer notifica o novo advogado + ADMIN, excluindo o ator; o advogado anterior nunca é notificado.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cliente→API (HTTP) | `advogadoId` do corpo e path var `id` são input não confiável |
| API→NotificacaoService | advogadoId já validado (tenant + role ADVOGADO) antes de notificar |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-87-03-01 | Information Disclosure | notificarParecerAtribuido | mitigate | `advogadoId` é validado por `validateAdvogado(advogadoId, tenantId)` (tenant + papel ADVOGADO) em ambos os métodos antes de ser usado; `criar()` revalida o destinatário contra o tenant. Impossível notificar/expor advogado de outro tenant |
| T-87-03-02 | Repudiation | actor exclusion | mitigate | `atorId = principal.getUserId()` passado a `notificarParecerAtribuido`, que exclui o ator do primário e do fan-out — o próprio (raro) auto-atribuidor não se notifica; o fan-out ADMIN nunca notifica o ator |
| T-87-03-03 | Information Disclosure | advogado anterior | mitigate | O método não lê o advogadoId anterior e não o notifica — nenhuma informação de reatribuição vaza para o responsável anterior (consistente com Out of Scope) |
| T-87-03-SC | Tampering (supply chain) | dependências | accept | Sem instalação de packages; sem alteração de `pom.xml` |
</threat_model>

<verification>
- `mvn -f backend/pom.xml -q -DskipTests compile` compila
- `mvn -f backend/pom.xml -q spotbugs:check` sem novos findings de alta severidade
- grep confirma o campo `notificacaoService` e as 2 chamadas `notificarParecerAtribuido`
- Live HTTP round-trip fica para UAT humano (constrangimento de credenciais registado em STATE.md)
</verification>

<success_criteria>
Ambos os pontos de atribuição de advogado (criação e reatribuição) disparam PARECER_ATRIBUIDO para o novo advogado + ADMIN, excluindo o ator, sem notificar o advogado anterior — fechando o gap NOTF-19.
</success_criteria>

<output>
Create `.planning/phases/LEXCV-87-alertas-de-eventos-fase-documento-atribui-o-e-parecer/87-03-SUMMARY.md` when done
</output>
