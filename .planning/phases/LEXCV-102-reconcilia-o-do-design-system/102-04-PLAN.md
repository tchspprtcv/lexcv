---
phase: 102-reconcilia-o-do-design-system
plan: 04
type: execute
wave: 2
depends_on: [102-01, 102-02, 102-03]
files_modified: []
autonomous: false
requirements: [DSR-01, DSR-02, DSR-03]
must_haves:
  truths:
    - "The whole app builds and typechecks after all three Wave-1 reconciliation plans are merged"
    - "The 3 badge gray call sites (notificacoes/dashboard/processos) still compile and render gray"
    - "No reconciled surface file still contains the flat dark:bg-[#020617]/dark:bg-slate-950 magic hex or a stray rounded-none"
    - "No scoped @radix-ui/react-* import was reintroduced anywhere in ui/"
    - "A human has visually signed off, in BOTH light and dark mode, that: (a) cards/dialogs/popovers now show dark-mode elevation contrast (Rule B, expected), (b) default buttons are still neutral-900 (NOT blue), (c) badges keep all colors incl gray, (d) icon-only buttons show a ~700ms-delayed tooltip"
  artifacts:
    - path: ".planning/phases/LEXCV-102-reconcilia-o-do-design-system/102-04-SUMMARY.md"
      provides: "Recorded human sign-off + final verification results"
  key_links:
    - from: "reconciled ui/* surfaces"
      to: "globals.css --card/--popover tokens"
      via: "rendered getComputedStyle in dark mode"
      pattern: "dark:bg-(card|popover)"
---

<objective>
Close Phase 102 with the mandatory holistic verification: (1) an automated final gate proving the whole app still builds/typechecks after all three Wave-1 plans merge and that no reconciliation regression leaked (badge gray call sites, magic-hex removal, no scoped-radix reintroduction), and (2) the mandatory human visual checkpoint (light+dark, real browser + getComputedStyle) that 102-CONTEXT.md requires because this phase changes rendered pixels on heavily-used surfaces.

Purpose: Per 102-CONTEXT.md and the Phase-101 CSS-cascade-bug lesson, a build passing is NOT sufficient — the dangerous failure mode is a file that compiles but silently drops a variant or ships the wrong surface color. This plan is the goal-backward gate for the whole phase. It is NOT autonomous: it pauses for a human to confirm the Rule-B dark-mode elevation change is the intended improvement (not a bug) and that nothing regressed.
Output: 102-04-SUMMARY.md recording the automated results + the human verdict.
</objective>

<execution_context>
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/LEXCV-102-reconcilia-o-do-design-system/102-UI-SPEC.md
@.planning/phases/LEXCV-102-reconcilia-o-do-design-system/102-CONTEXT.md
@.planning/phases/LEXCV-102-reconcilia-o-do-design-system/102-01-SUMMARY.md
@.planning/phases/LEXCV-102-reconcilia-o-do-design-system/102-02-SUMMARY.md
@.planning/phases/LEXCV-102-reconcilia-o-do-design-system/102-03-SUMMARY.md

<interfaces>
<!-- Expected rendered-value checks (getComputedStyle) in DARK mode after Rule B: -->
<!--   card / dialog / alert-dialog / popover background should resolve to oklch(0.205 0 0) (a lighter gray), -->
<!--   NOT #020617 / rgb(2,6,23) (the flat page background). If they still equal the page bg, Rule B did not apply. -->
<!-- Rule C invariants (must remain true): default Button = neutral-900 (dark near-black), NOT #2563eb/#3b82f6 blue. -->
<!--   Badge gray = neutral-100 bg / neutral-700 text (light). All 9 badge variants render distinct colors. -->
<!-- badge gray call sites to re-grep: web/src/app/(dashboard)/{notificacoes,dashboard,processos}/page.tsx -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Final holistic automated gate (build + regression greps)</name>
  <files>web/src/components/ui/badge.tsx, web/src/components/ui/card.tsx, web/src/components/ui/dialog.tsx, web/src/components/ui/alert-dialog.tsx, web/src/components/ui/popover.tsx, web/src/app/providers.tsx (all read-only — verification task, no edits)</files>
  <read_first>
    - .planning/phases/LEXCV-102-reconcilia-o-do-design-system/102-01-SUMMARY.md (what Rule-C files changed)
    - .planning/phases/LEXCV-102-reconcilia-o-do-design-system/102-02-SUMMARY.md (which Rule-B/optional substitutions were applied — table muted, sheet dark surface)
    - .planning/phases/LEXCV-102-reconcilia-o-do-design-system/102-03-SUMMARY.md (tooltip rollout coverage)
    - web/src/components/ui/badge.tsx (confirm gray survived)
  </read_first>
  <action>
    From the merged web/ tree, run the final automated gate. NOTE: if operating in an isolated worktree, run `pnpm install` first — worktree node_modules do NOT sync from the main checkout (Phase 101 learning). This task makes NO code edits — it verifies only.

    1. `pnpm build` — must exit 0 (proves the 93 imports across 38 consumer files + the tooltip nesting all compile/typecheck together after the three plans merge).
    2. Badge gray call sites still compile+present: grep `variant=` for `gray` in web/src/app/(dashboard)/notificacoes/page.tsx, dashboard/page.tsx, processos/page.tsx — all 3 must still reference the gray variant (build already proves they typecheck).
    3. Magic-hex removal: grep the reconciled surface files (card/dialog/alert-dialog/popover, and sheet per its chosen substitution) — confirm NO remaining `bg-[#020617]` and no `dark:bg-slate-950`; they must reference `dark:bg-card`/`dark:bg-popover`.
    4. Stray radius: grep card/dialog/alert-dialog for `rounded-none` — must be absent (replaced by rounded-lg).
    5. No scoped-radix regression: grep `web/src/components/ui/*.tsx` for `@radix-ui/react-` — must return nothing (unified radix-ui only).
    6. sr-only labels intact: grep `Fechar` present in dialog.tsx and sheet.tsx.
    7. Single tooltip provider: grep confirms exactly one `TooltipProvider` mount with `delayDuration={700}` in providers.tsx.

    Record every command's pass/fail in the summary. If any check fails, STOP and report — do not proceed to the human checkpoint with a red gate.
  </action>
  <verify>
    <automated>cd web && pnpm build && ! grep -RIl "bg-\[#020617\]\|dark:bg-slate-950" src/components/ui/ && ! grep -RIl "@radix-ui/react-" src/components/ui/ && grep -q "delayDuration={700}" src/app/providers.tsx && echo GATE_GREEN</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm build` exits 0
    - grep across web/src/components/ui/ returns NO `bg-[#020617]` and NO `dark:bg-slate-950`
    - grep across web/src/components/ui/ returns NO scoped `@radix-ui/react-*` import
    - card.tsx/dialog.tsx/alert-dialog.tsx contain no `rounded-none`
    - badge.tsx still has the `gray` variant; the 3 gray call sites still reference it
    - providers.tsx has exactly one `TooltipProvider` with `delayDuration={700}`; `Fechar` present in dialog.tsx + sheet.tsx
  </acceptance_criteria>
  <done>Automated gate is green across build, badge-gray, magic-hex removal, radius, radix-unification, and tooltip mount.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Mandatory human visual sign-off (light + dark) — Rule-B elevation, Rule-C identity, DSR-03 tooltips</name>
  <files>web/src/components/ui/* and web/src/app/(dashboard)/* (surfaces under visual review — this task makes NO code edits)</files>
  <read_first>
    - .planning/phases/LEXCV-102-reconcilia-o-do-design-system/102-CONTEXT.md (the mandatory human visual checkpoint decision, same protocol as Phase 101 — browser + getComputedStyle, not grep)
    - .planning/phases/LEXCV-102-reconcilia-o-do-design-system/102-UI-SPEC.md (Color section: Rule B is an expected change; Rule C invariants; Tooltip copy + 700ms delay)
    - .planning/phases/LEXCV-102-reconcilia-o-do-design-system/102-02-SUMMARY.md (which optional substitutions were applied — table muted / sheet dark surface — so the reviewer knows what to look at)
  </read_first>
  <action>
    Human checkpoint (NOT autonomous — pause and present the &lt;human-check&gt; steps below; make no code changes). Context of what was built: 102-01 reconciled the Rule-C identity primitives (button/badge/form) with zero color change + closed inherited dedup debt; 102-02 applied the Rule-A/B surface substitutions (the one intentional visible change: dark-mode elevation on card/dialog/alert-dialog/popover via --card/--popover); 102-03 mounted TooltipProvider (700ms) and wrapped the icon-only buttons. First run the automated backstop (&lt;automated&gt;), then present the manual steps. On completion, wait for the user's resume signal ("approved" or a description of issues).
  </action>
  <verify>
    <automated>cd web && pnpm build</automated>
    <human-check>
      Start the dev server: `cd web && pnpm dev` (http://localhost:3000). Log in as admin@lexcv.cv / admin123. Verify in BOTH light and dark mode (use the topbar theme toggle):
      1. DARK MODE — Rule B elevation (the expected change): open /dashboard (KPI cards), open an AlertDialog (e.g. a delete confirmation on /clientes), and open a popover. Confirm the card/dialog/popover surface is a visibly LIGHTER gray than the page background behind it (it now looks elevated, not flat). Optional precise check: DevTools → inspect the surface → Computed → background-color ≈ oklch(0.205 0 0) / rgb ~ (37,37,37), NOT rgb(2,6,23). Confirm it reads as an intended improvement, not a bug.
      2. LIGHT MODE — no regression: the same surfaces look as before (white cards/dialogs/popovers, sharp corners).
      3. Rule C — buttons NOT blue: on /clientes/novo, confirm the primary "Guardar"/"Criar" button is still dark neutral-900 (near-black), NOT institutional blue; outline/ghost/secondary buttons unchanged.
      4. Rule C — badges: on /processos and /notificacoes, confirm status/tipo badges keep their colors (blue/green/amber/red/purple/gray) — nothing turned monochrome.
      5. Sharp corners preserved: cards/dialogs/inputs still have sharp (zero-radius) corners in both themes.
      6. DSR-03 tooltips: on /clientes, hover (and Tab-focus) the row-action icon buttons (eye/print/edit/delete) — after ~0.7s a Portuguese tooltip appears ("Ver detalhes"/"Imprimir"/"Editar"/"Eliminar"). Hover the sidebar logout icon — "Terminar sessão" appears. Confirm the delay feels like a short pause (~700ms), not instant.
      7. Icon-only a11y: with a screen reader or the DevTools accessibility pane, confirm the icon buttons now expose an accessible name (aria-label).
    </human-check>
  </verify>
  <acceptance_criteria>
    - Dark mode: card/dialog/alert-dialog/popover surfaces are visibly elevated (lighter than page bg) and the user confirms it as intended
    - Light mode: no visual regression on the same surfaces
    - Default button remains neutral-900 (not blue); outline/ghost/secondary unchanged
    - All badge colors incl gray render distinctly
    - Sharp (zero-radius) corners preserved on cards/dialogs/inputs in both themes
    - Icon-only buttons show a ~700ms-delayed Portuguese tooltip and expose an aria-label
    - The user's verdict ("approved" or the described issues) is recorded verbatim in 102-04-SUMMARY.md
  </acceptance_criteria>
  <done>User types "approved" after confirming, in both themes, the Rule-B elevation (expected), Rule-C button/badge identity (unchanged), sharp corners, and DSR-03 tooltip delay + a11y name — or describes the issues to fix before the phase closes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| rendered DOM → human reviewer | The checkpoint is the trust boundary that catches "compiles but silently wrong" (dropped variant / wrong surface color) that a build gate cannot |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-102-09 | Tampering | a component compiles but silently ships a dropped variant or wrong surface color | mitigate | Mandatory human visual checkpoint (light+dark, getComputedStyle) in addition to `pnpm build` — the build alone cannot catch a silent visual drop (Phase 101 CSS-cascade lesson) |
| T-102-10 | Repudiation | Rule-B pixel change applied without recorded approval | mitigate | Blocking human-verify checkpoint; verdict recorded verbatim in 102-04-SUMMARY.md |
| T-102-SC | Supply Chain | npm/pnpm installs | accept | Verification-only plan; no packages installed. No package-legitimacy checkpoint required. |
</threat_model>

<verification>
- Task 1 automated gate green (build + all regression greps) BEFORE the human checkpoint runs.
- Human sign-off recorded for both light and dark mode covering: Rule-B elevation (expected), Rule-C button/badge identity (unchanged), sharp corners, and DSR-03 tooltip delay + a11y name.
- Any issue the human reports is captured as a gap for a follow-up fix plan (do not close the phase with an unresolved visual regression).
</verification>

<success_criteria>
- Whole-app `pnpm build` green after all three Wave-1 plans merge.
- Zero reconciliation regressions (badge gray intact, magic-hex gone, radius tokenized, radix unified, Fechar labels intact).
- Human has approved the Rule-B dark-mode elevation change and confirmed no Rule-C identity regression and working DSR-03 tooltips, in both themes.
</success_criteria>

<output>
Create `.planning/phases/LEXCV-102-reconcilia-o-do-design-system/102-04-SUMMARY.md` when done — include the automated gate results and the verbatim human verdict.
</output>
