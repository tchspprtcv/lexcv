# Plan 22-01: Processos — Pesquisa/Filtros e UX/Performance

**Phase:** 22
**Goal:** Pesquisa multi-campo, filtros avançados e sorting na listagem de processos (produtividade + UX).

<steps>
1. Atualizar `useProcessos()` para aceitar filtros e sorting (querystring + queryKey).
2. Atualizar mock `api-backup/v1/processos` para suportar `q/estado/tribunal/area_juridica/cliente_id/sortBy/sortDir`.
3. Atualizar backend `GET /processos` para suportar os mesmos query params.
4. Atualizar UI de listagem de processos: pesquisa com debounce + painel “Filtros” com selects/inputs.
5. Mostrar dados reais de `tribunal` e `area_juridica` na tabela (remover placeholders).
6. Verificar comportamento com dataset e confirmar que filtros são aplicados.
</steps>

