---
phase: 63-aprova-o-e-entrega
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/main/java/com/lexcv/models/ParecerVersao.java
  - backend/src/main/java/com/lexcv/models/ParecerSolicitacao.java
  - backend/src/main/java/com/lexcv/controllers/ParecerController.java
autonomous: true
requirements: [PARC-07, PARC-08, PARC-09]

must_haves:
  truths:
    - "ADMIN (pareceres:manage) pode marcar uma versão específica como aprovada internamente"
    - "Aprovar uma versão de uma solicitação PENDENTE/EM_ELABORACAO transiciona o status para EM_REVISAO"
    - "Advogado responsável ou ADMIN pode marcar uma versão como final e entregar, mudando o status para CONCLUIDO"
    - "Solicitação concluída regista a versaoFinalId apontando para a versão entregue"
    - "Qualquer escrita (aprovar/entregar) sobre uma solicitação já CONCLUIDO é rejeitada"
    - "Parecer entregue permanece consultável/descarregável via os endpoints GET existentes das Fases 61/62"
  artifacts:
    - path: "backend/src/main/java/com/lexcv/models/ParecerVersao.java"
      provides: "Campos aprovado/aprovadoPorId/aprovadoEm"
      contains: "private Boolean aprovado"
    - path: "backend/src/main/java/com/lexcv/models/ParecerSolicitacao.java"
      provides: "Campo versaoFinalId"
      contains: "private UUID versaoFinalId"
    - path: "backend/src/main/java/com/lexcv/controllers/ParecerController.java"
      provides: "Endpoints /aprovar e /entregar"
      contains: "aprovarVersao"
  key_links:
    - from: "ParecerController.aprovarVersao"
      to: "ParecerVersao.setAprovado/setAprovadoPorId/setAprovadoEm"
      via: "parecerVersaoRepository.save"
      pattern: "setAprovado\\(true\\)"
    - from: "ParecerController.entregarSolicitacao"
      to: "ParecerSolicitacao.setVersaoFinalId/setStatus"
      via: "parecerSolicitacaoRepository.save"
      pattern: "setStatus\\(\"CONCLUIDO\"\\)"
---

<objective>
Adicionar o fluxo de aprovação interna opcional e entrega final de pareceres no backend.
Cobre três campos de estado novos (aprovação na versão, versão final na solicitação) e dois
endpoints de transição de estado no ParecerController existente.

Purpose: PARC-07 (aprovação interna ADMIN), PARC-08 (entrega final → CONCLUIDO), PARC-09
(parecer entregue continua consultável via GETs existentes — sem novo endpoint público).
Output: ParecerVersao + ParecerSolicitacao com campos novos; ParecerController com /aprovar e /entregar.
</objective>

<execution_context>
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/LEXCV-63-aprova-o-e-entrega/63-CONTEXT.md
@.planning/phases/LEXCV-63-aprova-o-e-entrega/63-PATTERNS.md

<interfaces>
<!-- Contratos existentes que o executor usa diretamente — sem exploração de codebase necessária. -->

UserPrincipal (com.lexcv.config.UserPrincipal) — já usado no controller:
- principal.getUserId(): UUID            // id do utilizador autenticado
- principal.getRoles(): Set/List<String> // contém nomes de roles, ex. "ADMIN"
- principal.getTenantId(): UUID

ParecerSolicitacao (campos relevantes existentes):
- getId(): UUID, getTenantId(): UUID, getAdvogadoId(): UUID (nullable)
- getStatus(): String  // "PENDENTE" | "EM_ELABORACAO" | "EM_REVISAO" | "CONCLUIDO"
- setStatus(String)

ParecerVersao (campos relevantes existentes):
- getId(): UUID, getSolicitacaoId(): UUID, getTenantId(): UUID

Repositórios (JpaRepository — findById/save herdados):
- parecerSolicitacaoRepository.findById(UUID), .save(entity)
- parecerVersaoRepository.findById(UUID), .save(entity)

ParecerController helpers existentes:
- getTenantId(): UUID  // lê tenant do SecurityContext
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Adicionar campos de aprovação e versão final às entidades</name>
  <files>
    backend/src/main/java/com/lexcv/models/ParecerVersao.java,
    backend/src/main/java/com/lexcv/models/ParecerSolicitacao.java
  </files>
  <read_first>
    backend/src/main/java/com/lexcv/models/ParecerVersao.java,
    backend/src/main/java/com/lexcv/models/ParecerSolicitacao.java,
    .planning/phases/LEXCV-63-aprova-o-e-entrega/63-PATTERNS.md
  </read_first>
  <action>
    Em ParecerVersao.java adicionar três campos novos seguindo o padrão dos campos de auditoria existentes (criadoPorId/createdAt, linhas 36-40): um campo `aprovado` do tipo Boolean anotado `@Column(nullable = false)` + `@Builder.Default` com valor inicial false; um campo `aprovadoPorId` do tipo UUID anotado `@Column(name = "aprovado_por_id")` (nullable, sem default); um campo `aprovadoEm` do tipo LocalDateTime anotado `@Column(name = "aprovado_em")` (nullable, sem default). NÃO alterar o método onCreate()/@PrePersist — aprovadoEm fica null até o endpoint /aprovar o definir explicitamente. O import java.time.LocalDateTime já existe (linha 5); UUID já importado (linha 6). Colocar os campos após createdAt e antes do bloco @PrePersist.

    Em ParecerSolicitacao.java adicionar um campo `versaoFinalId` do tipo UUID anotado `@Column(name = "versao_final_id")` (nullable, sem FK, sem default), seguindo exatamente o padrão de advogadoId (linhas 33-34). Colocar após advogadoId e antes do campo prioridade. UUID já importado (linha 7).

    Não adicionar getters/setters manuais — Lombok @Getter/@Setter (já presente em ambas as classes) gera setAprovado/setAprovadoPorId/setAprovadoEm e setVersaoFinalId automaticamente. ddl-auto=update no dev cria as colunas no arranque.
  </action>
  <verify>
    <automated>cd backend && mvn -DskipTests compile -q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - ParecerVersao.java contém `private Boolean aprovado` com `@Builder.Default` e valor inicial false
    - ParecerVersao.java contém `@Column(name = "aprovado_por_id")` sobre `private UUID aprovadoPorId`
    - ParecerVersao.java contém `@Column(name = "aprovado_em")` sobre `private LocalDateTime aprovadoEm`
    - O método onCreate()/@PrePersist em ParecerVersao.java permanece inalterado (não define aprovadoEm)
    - ParecerSolicitacao.java contém `@Column(name = "versao_final_id")` sobre `private UUID versaoFinalId`
    - `mvn -DskipTests compile` termina com BUILD SUCCESS
  </acceptance_criteria>
  <done>Ambas as entidades compilam com os campos novos; Lombok gera os setters usados na Task 2.</done>
</task>

<task type="auto">
  <name>Task 2: Adicionar endpoints /aprovar e /entregar ao ParecerController</name>
  <files>backend/src/main/java/com/lexcv/controllers/ParecerController.java</files>
  <read_first>
    backend/src/main/java/com/lexcv/controllers/ParecerController.java,
    backend/src/main/java/com/lexcv/models/ParecerVersao.java,
    backend/src/main/java/com/lexcv/models/ParecerSolicitacao.java,
    .planning/phases/LEXCV-63-aprova-o-e-entrega/63-PATTERNS.md
  </read_first>
  <action>
    Adicionar `import java.time.LocalDateTime;` ao bloco de imports (o controller ainda não importa java.time).

    Adicionar o método aprovarVersao: anotado `@PreAuthorize("hasAuthority('pareceres:manage')")` (ADMIN-only por PARC-07; este scope é seedado só para ADMIN desde a Fase 61) e `@PutMapping("/{id}/versoes/{versaoId}/aprovar")`, assinatura `public ResponseEntity<?> aprovarVersao(@PathVariable UUID id, @PathVariable UUID versaoId)`. Corpo: obter tenantId via getTenantId(); fazer lookup da solicitação por id com a guarda null + tenantId (mesma forma das linhas 212-215, retornar 404 com message "Solicitação não encontrada"); fazer lookup da versão por versaoId validando que versao não é null e versao.getSolicitacaoId().equals(id) (mesma forma das linhas 253-256, retornar 404 com message "Versão não encontrada"); guarda CONCLUIDO (se solicitacao.getStatus() == "CONCLUIDO" retornar 400 com message "Não é possível aprovar uma versão de um parecer concluído"); obter o actor via Authentication/UserPrincipal como nas linhas 273-274; chamar versao.setAprovado(true), versao.setAprovadoPorId(principal.getUserId()), versao.setAprovadoEm(LocalDateTime.now()) e parecerVersaoRepository.save(versao); se o status atual for "PENDENTE" ou "EM_ELABORACAO", chamar solicitacao.setStatus("EM_REVISAO") e parecerSolicitacaoRepository.save(solicitacao); retornar ResponseEntity.ok(versao).

    Adicionar o método entregarSolicitacao: anotado `@PreAuthorize("hasAuthority('pareceres:manage')")` e `@PutMapping("/{id}/entregar")`, assinatura `public ResponseEntity<?> entregarSolicitacao(@PathVariable UUID id, @RequestParam UUID versaoFinalId)`. Corpo: getTenantId(); lookup da solicitação com guarda null + tenantId (404 "Solicitação não encontrada"); lookup da versão por versaoFinalId validando versao não null e versao.getSolicitacaoId().equals(id) (404 "Versão não encontrada"); guarda CONCLUIDO (400 com message "Parecer já foi entregue"); autorização advogado-responsável-ou-ADMIN copiando o padrão das linhas 273-281 (isAdmin = principal.getRoles().contains("ADMIN"); isResponsavel = solicitacao.getAdvogadoId() != null && solicitacao.getAdvogadoId().equals(principal.getUserId()); se nenhum, 403 com message "Apenas o advogado responsável ou ADMIN pode entregar o parecer"); chamar solicitacao.setVersaoFinalId(versaoFinalId), solicitacao.setStatus("CONCLUIDO"); retornar ResponseEntity.ok(parecerSolicitacaoRepository.save(solicitacao)).

    Em ambos os métodos: os campos de estado (aprovado/aprovadoPorId/aprovadoEm/versaoFinalId/status) são definidos programaticamente pelo controller — nunca vinculados a um @RequestBody (evita mass-assignment, consistente com a guarda já existente em updateSolicitacao). versaoFinalId chega como @RequestParam UUID validado, não via body DTO. Colocar os dois métodos novos após atribuirAdvogado (a seguir à linha 232), antes dos endpoints /versoes.
  </action>
  <verify>
    <automated>cd backend && mvn -DskipTests compile -q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - ParecerController.java contém `import java.time.LocalDateTime;`
    - Existe um método `aprovarVersao` anotado `@PutMapping("/{id}/versoes/{versaoId}/aprovar")` e `@PreAuthorize("hasAuthority('pareceres:manage')")`
    - aprovarVersao chama setAprovado(true), setAprovadoPorId(principal.getUserId()) e setAprovadoEm(LocalDateTime.now())
    - aprovarVersao transiciona status para "EM_REVISAO" apenas quando o status atual é "PENDENTE" ou "EM_ELABORACAO"
    - Existe um método `entregarSolicitacao` anotado `@PutMapping("/{id}/entregar")` recebendo `@RequestParam UUID versaoFinalId`
    - entregarSolicitacao rejeita com 403 quando o actor não é ADMIN nem advogado responsável
    - Ambos os métodos rejeitam com 400 quando solicitacao.getStatus() == "CONCLUIDO" (guarda contém grep de `CONCLUIDO`)
    - Ambos os métodos fazem lookup com guarda tenantId + 404 antes de qualquer mutação
    - `mvn -DskipTests compile` termina com BUILD SUCCESS
  </acceptance_criteria>
  <done>Os dois endpoints compilam e seguem os padrões de tenant-scoping, guarda CONCLUIDO e autorização já estabelecidos no controller.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→API (/aprovar, /entregar) | Input HTTP autenticado (cookie JWT) cruza para os novos endpoints de transição de estado |
| API→DB | Escritas de estado (aprovado, status, versaoFinalId) persistidas em t_parecer_versao / t_parecer_solicitacao |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-63-01 | Elevation of Privilege | POST/PUT /aprovar | mitigate | `@PreAuthorize("hasAuthority('pareceres:manage')")` — scope seedado só para ADMIN desde a Fase 61 |
| T-63-02 | Elevation of Privilege | PUT /entregar | mitigate | Check in-method advogado-responsável-ou-ADMIN (isResponsavel/isAdmin) → 403 se nenhum |
| T-63-03 | Tampering (mass-assignment) | ambos os endpoints | mitigate | Campos de estado definidos programaticamente; nunca vinculados a @RequestBody; versaoFinalId via @RequestParam UUID validado |
| T-63-04 | Information Disclosure (cross-tenant) | lookups de solicitação/versão | mitigate | Guarda tenantId.equals + validação versao.getSolicitacaoId().equals(id) antes de qualquer leitura/escrita; 404 caso falhe |
| T-63-05 | Tampering (entrega de versão alheia) | /entregar versaoFinalId | mitigate | versaoFinalId validado como pertencente à solicitação (getSolicitacaoId().equals(id)) antes de set |
| T-63-06 | Tampering (escrita pós-entrega) | ambos os endpoints | mitigate | Guarda CONCLUIDO rejeita com 400 — entrega é irreversível |
</threat_model>

<verification>
- `cd backend && mvn -DskipTests compile` → BUILD SUCCESS
- `grep -n "aprovarVersao\|entregarSolicitacao" backend/src/main/java/com/lexcv/controllers/ParecerController.java` → ambos presentes
- `grep -n "pareceres:manage" backend/src/main/java/com/lexcv/controllers/ParecerController.java` → 2 ocorrências (aprovar + entregar)
- `grep -n "private UUID versaoFinalId" backend/src/main/java/com/lexcv/models/ParecerSolicitacao.java` → presente
- `grep -n "private Boolean aprovado" backend/src/main/java/com/lexcv/models/ParecerVersao.java` → presente
</verification>

<success_criteria>
- PARC-07: ADMIN consegue aprovar uma versão; aprovação regista aprovadoPorId/aprovadoEm e transiciona PENDENTE/EM_ELABORACAO → EM_REVISAO
- PARC-08: advogado responsável ou ADMIN consegue entregar a versão final, registando versaoFinalId e mudando o status para CONCLUIDO
- PARC-09: parecer entregue continua consultável/descarregável via os GETs existentes (/{id}, /{id}/versoes, download de anexo) das Fases 61/62 — sem novo endpoint necessário
- Entrega é irreversível: escritas sobre CONCLUIDO rejeitadas
</success_criteria>

<output>
Create `.planning/phases/LEXCV-63-aprova-o-e-entrega/63-01-SUMMARY.md` when done
</output>
