---
phase: 45-filtros-edit-delete-ui
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/app/(dashboard)/financeiro/[id]/page.tsx
  - web/src/schemas/financeiro.ts
autonomous: true
requirements:
  - FIN-14
  - FIN-15
  - FIN-16

must_haves:
  truths:
    - "Utilizador com financeiro:edit vê botão Editar no header do detalhe e pode alterar valorTotal, descricao e dataAcordo via dialog modal"
    - "Utilizador com financeiro:manage vê botão Apagar no header e pode eliminar o honorário após confirmação; backend 409 mostra erro inline"
    - "Após apagar honorário com sucesso, utilizador é redirecionado para /financeiro"
    - "Utilizador com financeiro:manage vê botão Apagar em cada linha de pagamento; após confirmação o pagamento é eliminado e a tabela atualiza"
    - "Utilizadores sem as permissões necessárias não veem os botões"
  artifacts:
    - path: "web/src/schemas/financeiro.ts"
      provides: "honorarioUpdateSchema e HonorarioUpdateFormValues"
      contains: "honorarioUpdateSchema"
    - path: "web/src/app/(dashboard)/financeiro/[id]/page.tsx"
      provides: "Editar honorário (Dialog), Apagar honorário (AlertDialog), Apagar pagamento (AlertDialog por linha)"
      contains: "canManageFinanceiro"
  key_links:
    - from: "EditarHonorarioDialog"
      to: "useUpdateHonorario()"
      via: "mutateAsync({ id, data })"
      pattern: "useUpdateHonorario"
    - from: "AlertDialog apagar honorário"
      to: "useDeleteHonorario()"
      via: "mutateAsync(honorarioId) + router.push('/financeiro')"
      pattern: "useDeleteHonorario"
    - from: "AlertDialog apagar pagamento (por linha)"
      to: "useDeletePagamento()"
      via: "mutateAsync({ pagamentoId, honorarioId })"
      pattern: "useDeletePagamento"
---

<objective>
Adicionar ações de edição e eliminação à página de detalhe do honorário em `[id]/page.tsx`, e o schema de atualização em `schemas/financeiro.ts`.

Purpose: Cumpre FIN-14 (editar honorário), FIN-15 (apagar honorário), FIN-16 (apagar pagamento), tal como decidido em CONTEXT.md.

Output:
- `schemas/financeiro.ts` com `honorarioUpdateSchema` adicionado
- `[id]/page.tsx` com Dialog de edição, AlertDialog de eliminação de honorário e AlertDialog de eliminação de pagamento por linha
</objective>

<execution_context>
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/45-filtros-edit-delete-ui/45-CONTEXT.md

<interfaces>
<!-- Contratos existentes relevantes para este plano. -->

De web/src/types/financeiro.ts:
```typescript
export interface Honorario {
  id: number;
  processoId: string;
  valorTotal: number;
  totalPago: number;
  descricao?: string;
  dataAcordo?: string; // "YYYY-MM-DD"
}

export interface HonorarioUpdateRequest {
  valorTotal: number;
  descricao?: string;
  dataAcordo?: string;
}

export interface Pagamento {
  id: number;
  honorarioId: number;
  valorPago: number;
  dataPagamento: string;
  metodo?: string;
}
```

De web/src/hooks/use-financeiro.ts:
```typescript
export function useUpdateHonorario(): UseMutationResult<
  Honorario,
  Error,
  { id: number; data: HonorarioUpdateRequest }
>

export function useDeleteHonorario(): UseMutationResult<void, Error, number>

export function useDeletePagamento(): UseMutationResult<
  void,
  Error,
  { pagamentoId: number; honorarioId: number }
>
```

De web/src/schemas/financeiro.ts (estado atual — honorarioUpdateSchema NÃO existe):
```typescript
export const honorarioFormSchema   // schema de criação (inclui processoId)
export const pagamentoFormSchema
```

De web/src/components/ui/dialog.tsx — existe:
```typescript
export { Dialog, DialogContent, DialogHeader, DialogTitle, DialogFooter, DialogClose, DialogTrigger }
```

alert-dialog.tsx NÃO existe ainda — deve ser instalado com:
  pnpm dlx shadcn@latest add alert-dialog
antes de o importar.

Padrão de permissões (CLAUDE.md):
  permissions.can.manage("financeiro")  →  canManageFinanceiro
  permissions.can.edit("financeiro")    →  canEditFinanceiro  (já existe em [id]/page.tsx)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Adicionar honorarioUpdateSchema em schemas/financeiro.ts e instalar alert-dialog</name>
  <files>web/src/schemas/financeiro.ts</files>
  <action>
Duas sub-ações nesta task:

**1. Instalar alert-dialog (componente shadcn que ainda não existe):**

Executar no diretório `web/`:
```
pnpm dlx shadcn@latest add alert-dialog
```

Verificar que `web/src/components/ui/alert-dialog.tsx` foi criado.

**2. Adicionar `honorarioUpdateSchema` em `schemas/financeiro.ts`:**

Adicionar, após `export const honorarioFormSchema` e antes de `export const pagamentoFormSchema`, o seguinte schema de atualização:

```typescript
export const honorarioUpdateSchema = z.object({
  valorTotal: moneyString,
  descricao: optionalTrimmedString,
  dataAcordo: optionalDateString,
});

export type HonorarioUpdateFormValues = z.infer<typeof honorarioUpdateSchema>;
```

As primitivas `moneyString`, `optionalTrimmedString` e `optionalDateString` já existem no ficheiro — reutilizá-las diretamente sem redeclarar.
  </action>
  <verify>
    <automated>cd C:/Users/francisco.horta/Documents/projects/personal/lexcv/web && pnpm build 2>&1 | tail -20</automated>
  </verify>
  <done>
- `web/src/components/ui/alert-dialog.tsx` existe
- `schemas/financeiro.ts` exporta `honorarioUpdateSchema` e `HonorarioUpdateFormValues`
- `pnpm build` passa sem erros
  </done>
</task>

<task type="auto">
  <name>Task 2: Adicionar Edit Dialog, Delete AlertDialog (honorário) e Delete AlertDialog (pagamento) em [id]/page.tsx</name>
  <files>web/src/app/(dashboard)/financeiro/[id]/page.tsx</files>
  <action>
Modificar `[id]/page.tsx` para adicionar as três ações de mutação. As alterações concentram-se em `HonorarioDetailContent`.

**Novos imports a adicionar no topo do ficheiro:**

```typescript
import { useRouter } from "next/navigation";
import {
  AlertDialog,
  AlertDialogAction,
  AlertDialogCancel,
  AlertDialogContent,
  AlertDialogDescription,
  AlertDialogFooter,
  AlertDialogHeader,
  AlertDialogTitle,
  AlertDialogTrigger,
} from "@/components/ui/alert-dialog";
import {
  Dialog,
  DialogClose,
  DialogContent,
  DialogFooter,
  DialogHeader,
  DialogTitle,
  DialogTrigger,
} from "@/components/ui/dialog";
import {
  useDeleteHonorario,
  useDeletePagamento,
  useUpdateHonorario,
} from "@/hooks/use-financeiro";
import { honorarioUpdateSchema, type HonorarioUpdateFormValues } from "@/schemas/financeiro";
import type { HonorarioUpdateRequest } from "@/types/financeiro";
```

Nota: `useCreatePagamento`, `useHonorario`, `useHonorarioPagamentos` já estão importados — não duplicar.

**Alterações em `HonorarioDetailPage` (componente de página raiz):**

1. Declarar `canManageFinanceiro` junto a `canEditFinanceiro`:
   ```typescript
   const canManageFinanceiro = permissions.can.manage("financeiro");
   ```

2. Passar `canManageFinanceiro` como prop para `HonorarioDetailContent` (adicionar ao tipo e à chamada).

**Alterações na interface de `HonorarioDetailContent`:**

Adicionar `canManageFinanceiro: boolean` ao tipo de props.

**Alterações dentro de `HonorarioDetailContent`:**

1. Adicionar hooks de mutação após os existentes:
   ```typescript
   const router = useRouter();
   const updateHonorario = useUpdateHonorario();
   const deleteHonorario = useDeleteHonorario();
   const deletePagamento = useDeletePagamento();
   ```

2. Adicionar estado para o Dialog de edição e erros de servidor do honorário:
   ```typescript
   const [editOpen, setEditOpen] = React.useState(false);
   const [deleteHonorarioError, setDeleteHonorarioError] = React.useState<string | null>(null);
   ```

3. Criar form de edição com react-hook-form + zod, pré-preenchido com os valores atuais do honorário. Usar `React.useEffect` para repor o form quando `honorario.data` muda:
   ```typescript
   const editForm = useForm<HonorarioUpdateFormValues>({
     resolver: zodResolver(honorarioUpdateSchema),
     defaultValues: {
       valorTotal: honorario.data ? String(honorario.data.valorTotal) : "",
       descricao: honorario.data?.descricao ?? "",
       dataAcordo: honorario.data?.dataAcordo ?? "",
     },
   });

   React.useEffect(() => {
     if (honorario.data) {
       editForm.reset({
         valorTotal: String(honorario.data.valorTotal),
         descricao: honorario.data.descricao ?? "",
         dataAcordo: honorario.data.dataAcordo ?? "",
       });
     }
   }, [honorario.data, editForm]);
   ```

4. Handler de submissão do form de edição:
   ```typescript
   const onSubmitEdit = async (values: HonorarioUpdateFormValues) => {
     try {
       const payload: HonorarioUpdateRequest = {
         valorTotal: Number(values.valorTotal),
         descricao: values.descricao,
         dataAcordo: values.dataAcordo,
       };
       await updateHonorario.mutateAsync({ id: honorarioId, data: payload });
       setEditOpen(false);
     } catch (e) {
       editForm.setError("root", {
         message: e instanceof Error ? e.message : "Erro ao guardar alterações",
       });
     }
   };
   ```

5. Handler de eliminação do honorário:
   ```typescript
   const onDeleteHonorario = async () => {
     setDeleteHonorarioError(null);
     try {
       await deleteHonorario.mutateAsync(honorarioId);
       router.push("/financeiro");
     } catch (e) {
       const msg = e instanceof Error ? e.message : "Erro ao apagar honorário";
       setDeleteHonorarioError(msg);
     }
   };
   ```

**No JSX — header (div com botões no topo):**

Após os botões "Voltar" e "Ver processo" existentes, adicionar condicionalmente:

- Botão Editar (só se `canEditFinanceiro && honorario.data`):
  Usar `Dialog` com `open={editOpen}` e `onOpenChange={setEditOpen}`. O trigger é um `<Button variant="outline">Editar</Button>`. O conteúdo do Dialog contém um `<form>` com três campos (valorTotal, descricao, dataAcordo) usando `<Input>` e `<Label>`, `editForm.register(...)`, mensagens de erro por campo (`editForm.formState.errors.*`), e `editForm.formState.errors.root`. O footer tem `<DialogClose asChild><Button variant="outline">Cancelar</Button></DialogClose>` e um botão de submissão.

- Botão Apagar honorário (só se `canManageFinanceiro && honorario.data`):
  Usar `AlertDialog`. O trigger é um `<Button variant="destructive">Apagar</Button>`. O `AlertDialogContent` tem title "Apagar honorário?" e description "Esta ação é irreversível. Se o honorário tiver pagamentos registados, a operação será rejeitada." O footer tem `AlertDialogCancel` e `AlertDialogAction` com `onClick={onDeleteHonorario}`. Se `deleteHonorarioError` não for null, mostrar `<p className="text-sm text-red-600 mt-2">{deleteHonorarioError}</p>` após o AlertDialog (não dentro, pois ele fecha automaticamente).

  Para mostrar o erro inline sem o AlertDialog fechar: envolver o AlertDialog e a mensagem de erro em um fragment, e adicionar `deleteHonorarioError &&` antes de um `<p>` fora do AlertDialog.

**Na tabela de pagamentos — coluna de ações (FIN-16):**

1. Adicionar cabeçalho `<th>` vazio (coluna de ações) no `<thead>`.

2. Em cada `<tr>` de pagamento, adicionar `<td className="py-2 pl-4">`:
   - Condicionalmente (só se `canManageFinanceiro`): um `AlertDialog` com trigger `<Button variant="ghost" size="sm" className="text-red-600 hover:text-red-700">Apagar</Button>`, title "Apagar pagamento?" e description "O valor pago será revertido na conta-corrente do cliente.", e `AlertDialogAction` com `onClick={() => deletePagamento.mutate({ pagamentoId: p.id, honorarioId })}`.
  </action>
  <verify>
    <automated>cd C:/Users/francisco.horta/Documents/projects/personal/lexcv/web && pnpm build 2>&1 | tail -20</automated>
  </verify>
  <done>
- `pnpm build` termina sem erros de TypeScript nem de compilação
- `canManageFinanceiro` declarado e passado como prop
- Dialog de edição presente no header, gated por `canEditFinanceiro`
- AlertDialog de eliminação de honorário presente no header, gated por `canManageFinanceiro`
- AlertDialog de eliminação de pagamento presente por linha na tabela, gated por `canManageFinanceiro`
- Após delete de honorário bem-sucedido, `router.push('/financeiro')` é chamado
- Erros de servidor (incluindo 409) são apresentados ao utilizador
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Descrição |
|----------|-----------|
| Browser → API (PUT /honorarios/:id) | Input do utilizador validado pelo Zod antes de enviar; backend valida também |
| Browser → API (DELETE /honorarios/:id) | Requer `financeiro:manage`; backend rejeita com 409 se tiver pagamentos |
| Browser → API (DELETE /pagamentos/:id) | Requer `financeiro:manage`; backend reverte a conta-corrente do cliente |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-45-02-01 | Tampering | PUT /honorarios/:id | mitigate | `honorarioUpdateSchema` valida valorTotal > 0 antes de enviar; backend tem validação própria |
| T-45-02-02 | Elevation of Privilege | Botões de edição/eliminação | mitigate | Gated por `canEditFinanceiro` / `canManageFinanceiro` no frontend; backend tem `@PreAuthorize` para ambos |
| T-45-02-03 | Denial of Service | DELETE com honorário com pagamentos | accept | Backend responde 409; frontend mostra mensagem clara; sem retry automático |
| T-45-02-SC | Tampering | pnpm dlx shadcn@latest add alert-dialog | mitigate | shadcn é CLI oficial do projeto; verificar que o ficheiro gerado existe antes de importar |
</threat_model>

<verification>
- `pnpm build` sem erros de TypeScript
- Sem imports duplicados de hooks já existentes
- `useForm` para edição usa `zodResolver(honorarioUpdateSchema)` — não `honorarioFormSchema`
- `router` importado de `"next/navigation"` (não de `"next/router"`)
- `deletePagamento.mutate(...)` (não `mutateAsync`) na linha da tabela para evitar unhandled promise rejection
</verification>

<success_criteria>
- FIN-14: Botão "Editar" aparece a utilizadores com `financeiro:edit`; form pré-preenchido; guardar chama PUT e fecha o dialog
- FIN-15: Botão "Apagar" aparece a utilizadores com `financeiro:manage`; confirmação obrigatória; 409 mostra mensagem de erro inline; sucesso redireciona para /financeiro
- FIN-16: Botão "Apagar" por linha de pagamento aparece a utilizadores com `financeiro:manage`; confirmação obrigatória; sucesso atualiza a tabela via cache invalidation
- Build passa sem erros
</success_criteria>

<output>
Criar `.planning/phases/45-filtros-edit-delete-ui/45-02-SUMMARY.md` quando concluído.
</output>
