---
phase: "44-status-kpis"
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/types/financeiro.ts
  - web/src/app/(dashboard)/financeiro/page.tsx
autonomous: true
requirements:
  - FIN-07
  - FIN-08
  - FIN-09
  - FIN-10

must_haves:
  truths:
    - "Cada linha de honorário na tabela tem uma coluna 'Estado' com badge colorido"
    - "Badge exibe 'Pendente' (amber) quando totalPago == 0"
    - "Badge exibe 'Parcialmente Pago' (azul) quando 0 < totalPago < valorTotal"
    - "Badge exibe 'Pago' (verde) quando totalPago >= valorTotal"
    - "Quatro cards KPI no topo: Total Faturado, Total Recebido, Em Dívida, Receita do Mês"
    - "Os valores dos KPI cards são derivados de honorarios.data — sem fetch adicional"
  artifacts:
    - path: "web/src/types/financeiro.ts"
      provides: "Interface Honorario com campo totalPago: number"
      contains: "totalPago"
    - path: "web/src/app/(dashboard)/financeiro/page.tsx"
      provides: "KPI cards + coluna Estado com badge na tabela"
      contains: "totalPago"
  key_links:
    - from: "web/src/app/(dashboard)/financeiro/page.tsx"
      to: "web/src/types/financeiro.ts"
      via: "import Honorario"
      pattern: "totalPago"
---

<objective>
Adicionar `totalPago` ao tipo `Honorario`, calcular o estado de cada honorário no frontend, exibir badge de estado na tabela e renderizar quatro cards KPI no topo da página financeiro.

Purpose: Cumprir FIN-07 a FIN-10 — o utilizador vê imediatamente o estado de cada honorário e um resumo financeiro global, tudo derivado dos dados já carregados por `useHonorarios()`.
Output: `financeiro.ts` com `totalPago`; `page.tsx` com KPI cards e coluna Estado.
</objective>

<execution_context>
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/44-status-kpis/44-CONTEXT.md

<interfaces>
<!-- web/src/types/financeiro.ts — estado actual -->
<!--
export interface Honorario {
  id: number;
  processoId: string;
  valorTotal: number;
  descricao?: string;
  dataAcordo?: string;
}
// HonorarioCreateRequest e HonorarioUpdateRequest não incluem totalPago
-->

<!-- web/src/app/(dashboard)/financeiro/page.tsx — estrutura actual relevante -->
<!--
function formatMoneyCVE(v: number): string — já existe, reutilizar
Card, CardContent, CardHeader, CardTitle — já importados
useHonorarios() devolve Honorario[]; honorarios.data é o array
Tabela tem colunas: Honorário, Processo, Cliente, Total, Data do acordo
-->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Adicionar totalPago ao tipo Honorario</name>
  <files>web/src/types/financeiro.ts</files>
  <behavior>
    - Honorario tem campo totalPago: number (obrigatório — backend garante 0 quando sem pagamentos)
    - HonorarioCreateRequest NÃO tem totalPago
    - HonorarioUpdateRequest NÃO tem totalPago
  </behavior>
  <action>
Em `web/src/types/financeiro.ts`, adicionar `totalPago: number` à interface `Honorario` imediatamente após `valorTotal`:

```
export interface Honorario {
  id: number;
  processoId: string;
  valorTotal: number;
  totalPago: number;      // campo computado pelo backend (soma de pagamentos)
  descricao?: string;
  dataAcordo?: string;
}
```

Não alterar `HonorarioCreateRequest` nem `HonorarioUpdateRequest` — `totalPago` é read-only do servidor.

Após a edição, verificar que o TypeScript compila: `pnpm --filter web tsc --noEmit`.
  </action>
  <verify>
    <automated>cd C:/Users/francisco.horta/Documents/projects/personal/lexcv/web && pnpm tsc --noEmit 2>&1 | tail -10</automated>
  </verify>
  <done>Interface Honorario tem totalPago: number; tsc --noEmit passa sem erros relacionados a financeiro.ts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: KPI cards e badge de estado na página financeiro</name>
  <files>web/src/app/(dashboard)/financeiro/page.tsx</files>
  <behavior>
    - calcHonorarioStatus(totalPago, valorTotal): "Pendente" | "Parcialmente Pago" | "Pago"
      - totalPago == 0 → "Pendente"
      - 0 < totalPago < valorTotal → "Parcialmente Pago"
      - totalPago >= valorTotal → "Pago"
    - KPI faturado = sum(valorTotal) de todos honorários
    - KPI recebido = sum(totalPago) de todos honorários
    - KPI divida = faturado - recebido
    - KPI mesCorrente = sum(totalPago) de honorários onde dataAcordo está no ano+mês corrente; honorários sem dataAcordo são excluídos
  </behavior>
  <action>
Modificar `web/src/app/(dashboard)/financeiro/page.tsx` com as seguintes alterações:

**1. Função helper de status** — adicionar antes do componente `FinanceiroContent`:

```typescript
type HonorarioStatus = "Pendente" | "Parcialmente Pago" | "Pago";

function calcHonorarioStatus(totalPago: number, valorTotal: number): HonorarioStatus {
  if (totalPago <= 0) return "Pendente";
  if (totalPago < valorTotal) return "Parcialmente Pago";
  return "Pago";
}
```

**2. Mapeamento de cores do badge** — adicionar a seguir à função acima:

```typescript
const statusBadgeClass: Record<HonorarioStatus, string> = {
  Pendente: "inline-flex items-center rounded-full px-2 py-0.5 text-xs font-medium bg-amber-100 text-amber-800 dark:bg-amber-900/30 dark:text-amber-400",
  "Parcialmente Pago": "inline-flex items-center rounded-full px-2 py-0.5 text-xs font-medium bg-blue-100 text-blue-800 dark:bg-blue-900/30 dark:text-blue-400",
  Pago: "inline-flex items-center rounded-full px-2 py-0.5 text-xs font-medium bg-green-100 text-green-800 dark:bg-green-900/30 dark:text-green-400",
};
```

**3. Cálculo dos KPIs** — dentro de `FinanceiroContent`, após a declaração de `isLoading`/`isError`, adicionar:

```typescript
const list = honorarios.data ?? [];
const now = new Date();
const currentYearMonth = `${now.getFullYear()}-${String(now.getMonth() + 1).padStart(2, "0")}`;

const kpiFaturado = list.reduce((acc, h) => acc + h.valorTotal, 0);
const kpiRecebido = list.reduce((acc, h) => acc + h.totalPago, 0);
const kpiDivida = kpiFaturado - kpiRecebido;
const kpiMes = list
  .filter((h) => h.dataAcordo?.startsWith(currentYearMonth))
  .reduce((acc, h) => acc + h.totalPago, 0);
```

**4. Grid de KPI cards** — inserir ANTES do `<Card>` que contém a tabela de honorários (ou seja, logo após o `<div className="flex items-start justify-between...">` do cabeçalho):

```tsx
<div className="grid grid-cols-2 gap-4 sm:grid-cols-4">
  {[
    { label: "Total Faturado", value: kpiFaturado },
    { label: "Total Recebido", value: kpiRecebido },
    { label: "Em Dívida", value: kpiDivida },
    { label: "Receita do Mês", value: kpiMes },
  ].map(({ label, value }) => (
    <Card key={label}>
      <CardHeader className="pb-1 pt-4 px-4">
        <CardTitle className="text-xs font-medium text-neutral-500 dark:text-neutral-400">
          {label}
        </CardTitle>
      </CardHeader>
      <CardContent className="px-4 pb-4">
        <p className="text-xl font-semibold">{formatMoneyCVE(value)}</p>
      </CardContent>
    </Card>
  ))}
</div>
```

Nota: os KPI cards devem ser renderizados independentemente do estado de loading — usar `list` (que é `[]` enquanto carrega). Os cards aparecem com zeros durante o loading, o que é aceitável.

**5. Coluna Estado na tabela** — no `<thead>`, adicionar antes do `</tr>` de fecho:
```tsx
<th className="py-2 pr-4 font-medium">Estado</th>
```

Em cada `<tr>` do `tbody`, adicionar antes do `</tr>` de fecho (após a célula `formatDate(h.dataAcordo)`):
```tsx
<td className="py-2 pr-4">
  {(() => {
    const status = calcHonorarioStatus(h.totalPago, h.valorTotal);
    return <span className={statusBadgeClass[status]}>{status}</span>;
  })()}
</td>
```
  </action>
  <verify>
    <automated>cd C:/Users/francisco.horta/Documents/projects/personal/lexcv/web && pnpm tsc --noEmit 2>&1 | tail -10</automated>
  </verify>
  <done>
    - tsc --noEmit sem erros
    - page.tsx contém calcHonorarioStatus, statusBadgeClass, kpiFaturado, kpiRecebido, kpiDivida, kpiMes
    - Grid de 4 cards e coluna Estado presentes no JSX
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Descrição |
|----------|-----------|
| Backend JSON → Frontend | totalPago vem do servidor; frontend não calcula totais de pagamentos — só exibe |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-44-03 | Tampering | totalPago no JSON de resposta | accept | Campo é calculado no backend via @Formula SQL — não pode ser forjado pelo cliente pois é ignorado em POST/PUT (READ_ONLY Jackson) |
| T-44-04 | Information Disclosure | KPI cards expõem totais financeiros | accept | Os cards só aparecem para utilizadores com `financeiro:view` (gate já existente no componente) |
</threat_model>

<verification>
1. `pnpm tsc --noEmit` passa sem erros em `web/`
2. `pnpm build` em `web/` compila sem erros de tipo em financeiro
3. Inspecção visual: 4 cards KPI visíveis no topo, coluna Estado com badges coloridos na tabela
</verification>

<success_criteria>
- Interface `Honorario` em `financeiro.ts` tem `totalPago: number`
- `page.tsx` exibe 4 KPI cards (Faturado, Recebido, Dívida, Mês) calculados de `honorarios.data`
- Tabela de honorários tem coluna Estado com badge amber/azul/verde conforme status calculado
- Zero pedidos HTTP adicionais (tudo derivado de `useHonorarios()`)
- TypeScript compila sem erros
</success_criteria>

<output>
Criar `.planning/phases/44-status-kpis/44-02-SUMMARY.md` quando concluído.
</output>
