---
phase: 97-auditoria-de-milestone
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/lib/cliente-documento-tipo.ts
  - web/src/lib/cliente-documento-tipo.test.ts
  - web/src/app/(dashboard)/clientes/[id]/page.tsx
  - web/src/app/(dashboard)/clientes/[id]/ficha/page.tsx
  - backend/src/test/java/com/lexcv/models/ClienteNifValidationTest.java
autonomous: true
requirements: [AUD-03]

must_haves:
  truths:
    - "The client detail page renders 'Registo Comercial' (not the raw string 'REG_COMERCIAL') for a company client's Tipo de Documento"
    - "The printable ficha renders the translated Portuguese DocumentoTipo label, not the raw enum value"
    - "The 4 NIF validation scenarios from Phase 73.1 have automated backend test coverage"
  artifacts:
    - path: "web/src/lib/cliente-documento-tipo.ts"
      provides: "getDocumentoTipoLabel translating every DocumentoTipo enum value to its Portuguese label"
      contains: "getDocumentoTipoLabel"
    - path: "backend/src/test/java/com/lexcv/models/ClienteNifValidationTest.java"
      provides: "Bean Validation coverage for Cliente.nif (@NotBlank + @Pattern ^\\d{9}$)"
      contains: "class ClienteNifValidationTest"
  key_links:
    - from: "web/src/app/(dashboard)/clientes/[id]/page.tsx"
      to: "getDocumentoTipoLabel"
      via: "label lookup on the Tipo de Documento read-only row"
      pattern: "getDocumentoTipoLabel"
    - from: "web/src/app/(dashboard)/clientes/[id]/ficha/page.tsx"
      to: "getDocumentoTipoLabel"
      via: "label lookup on the Tipo Doc. field"
      pattern: "getDocumentoTipoLabel"
    - from: "backend/src/test/java/com/lexcv/models/ClienteNifValidationTest.java"
      to: "Cliente.nif constraints"
      via: "jakarta.validation.Validator.validate"
      pattern: "validator\\.validate"
---

<objective>
Close two known, tracked debt items (STATE.md Pending Todos, AUD-03):
1. `DocumentoTipo` enum values (notably `REG_COMERCIAL`) render as raw strings instead of
   translated Portuguese labels on the client detail page and the printable ficha.
2. The 4 NIF validation scenarios introduced in Phase 73.1 (`Cliente.nif` = `@NotBlank` +
   `@Pattern("^\\d{9}$")`) have no automated backend test coverage.

Purpose: Satisfy AUD-03 — user-facing correctness (Portuguese domain language per CLAUDE.md) and
regression protection for a validation rule closed in a prior gap-closure phase.
Output: A shared label helper used by both render sites, matching tests, and a backend
Bean-Validation test proving all 4 NIF scenarios.
</objective>

<execution_context>
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/LEXCV-97-auditoria-de-milestone-d-vida-t-cnica-e-uat-pendente/97-CONTEXT.md

<interfaces>
<!-- Existing contracts the executor builds against. Extracted from codebase — use directly. -->

From backend DocumentoTipo.java (the 4 enum values needing labels):
- BI, CNI, PASSAPORTE, REG_COMERCIAL

From web/src/lib/cliente-documento-tipo.ts (existing helper — extend it, do not create a new module):
- interface DocumentoTipoOption { value: DocumentoTipo; label: string }
- getDocumentoTipoOptions(tipo): DocumentoTipoOption[]   // PARTICULAR → CNI/BI/Passaporte, EMPRESA → Registo Comercial
- toDocumentoTipo(value, tipo): DocumentoTipo | undefined
  Canonical labels already defined here: CNI→"CNI", BI→"BI", PASSAPORTE→"Passaporte", REG_COMERCIAL→"Registo Comercial"

Current RAW-render sites to fix:
- web/src/app/(dashboard)/clientes/[id]/page.tsx (~line 676):
  <dd className="col-span-2">{cliente.data.documento_tipo ?? cliente.data.documentoTipo ?? "—"}</dd>
- web/src/app/(dashboard)/clientes/[id]/ficha/page.tsx (~line 184):
  <Field label="Tipo Doc." value={fmt(cliente.documento_tipo ?? cliente.documentoTipo)} />
  // fmt(value) returns BLANK ("___________") for null/undefined/empty

From backend Cliente.java (the rule under test):
- @NotBlank(message = "NIF é obrigatório") @Pattern(regexp = "^\\d{9}$", message = "NIF deve conter exatamente 9 dígitos numéricos") private String nif;

Backend test convention (see NotificacaoServiceTest / AlertasDiariosJobTest): plain JUnit 5, no
@SpringBootTest / no MockMvc harness anywhere in this codebase. Bean Validation is tested with a
standalone jakarta Validator from Validation.buildDefaultValidatorFactory().getValidator().
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add getDocumentoTipoLabel and apply it at both render sites</name>
  <files>web/src/lib/cliente-documento-tipo.ts, web/src/lib/cliente-documento-tipo.test.ts, web/src/app/(dashboard)/clientes/[id]/page.tsx, web/src/app/(dashboard)/clientes/[id]/ficha/page.tsx</files>
  <read_first>
    - web/src/lib/cliente-documento-tipo.ts (whole file — extend it)
    - web/src/lib/cliente-documento-tipo.test.ts (existing tests — mirror their style for the new cases)
    - web/src/app/(dashboard)/clientes/[id]/page.tsx (the Dados read-only grid around line 676; note it already imports from @/lib/cliente-documento-tipo at line 64)
    - web/src/app/(dashboard)/clientes/[id]/ficha/page.tsx (the Field rows around line 184; fmt() defined at line 39)
    - web/src/types/clientes.ts (the DocumentoTipo type)
  </read_first>
  <behavior>
    - getDocumentoTipoLabel("REG_COMERCIAL") returns "Registo Comercial"
    - getDocumentoTipoLabel("PASSAPORTE") returns "Passaporte"
    - getDocumentoTipoLabel("BI") returns "BI"; getDocumentoTipoLabel("CNI") returns "CNI"
    - getDocumentoTipoLabel(null) and getDocumentoTipoLabel(undefined) and getDocumentoTipoLabel("") return undefined (so callers' existing "—"/BLANK fallback still applies)
    - getDocumentoTipoLabel("LEGACY_UNKNOWN") returns "LEGACY_UNKNOWN" verbatim (never hide a stored legacy value)
  </behavior>
  <action>
    In `web/src/lib/cliente-documento-tipo.ts` add an exported `getDocumentoTipoLabel(value:
    string | null | undefined): string | undefined` backed by a single canonical
    `Record<DocumentoTipo, string>` label map (BI→"BI", CNI→"CNI", PASSAPORTE→"Passaporte",
    REG_COMERCIAL→"Registo Comercial") reused by `OPTIONS_BY_TIPO` so labels stay
    single-sourced. Return `undefined` for null/empty, the mapped label for a known value, and
    the raw value verbatim for an unknown/legacy value. In the detail page (~line 676) replace
    the raw `{cliente.data.documento_tipo ?? cliente.data.documentoTipo ?? "—"}` with
    `{getDocumentoTipoLabel(cliente.data.documento_tipo ?? cliente.data.documentoTipo) ?? "—"}`,
    adding the import to the existing `@/lib/cliente-documento-tipo` import group. In the ficha
    page (~line 184) replace the `Tipo Doc.` value with
    `fmt(getDocumentoTipoLabel(cliente.documento_tipo ?? cliente.documentoTipo))`, importing
    `getDocumentoTipoLabel` from `@/lib/cliente-documento-tipo`. Add the behavior cases above to
    `cliente-documento-tipo.test.ts`.
  </action>
  <verify>
    <automated>cd web && pnpm test -- cliente-documento-tipo && pnpm exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Test-command: `pnpm test -- cliente-documento-tipo` passes including the 6 new getDocumentoTipoLabel cases.
    - Source assertion: both page.tsx (~676) and ficha/page.tsx (~184) call getDocumentoTipoLabel; neither renders documento_tipo/documentoTipo directly anymore.
    - CLI: `pnpm exec tsc --noEmit` reports 0 errors.
    - Behavior assertion: for a company client, the detail page and ficha show "Registo Comercial" instead of "REG_COMERCIAL".
  </acceptance_criteria>
  <done>A single-sourced label helper exists and both render sites use it; a company client's document type shows "Registo Comercial"; tests + typecheck green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add backend Bean-Validation tests for the 4 NIF scenarios</name>
  <files>backend/src/test/java/com/lexcv/models/ClienteNifValidationTest.java</files>
  <read_first>
    - backend/src/main/java/com/lexcv/models/Cliente.java (the nif field constraints and messages)
    - backend/src/test/java/com/lexcv/services/NotificacaoServiceTest.java (plain-JUnit-5, no-Spring-context convention to mirror)
    - backend/pom.xml (confirm spring-boot-starter-validation / hibernate-validator is on the test classpath; it is transitively via the app dependency)
  </read_first>
  <behavior>
    - A Cliente with nif="123456789" (exactly 9 digits) produces ZERO violations on the nif property.
    - A Cliente with nif=null (or "" / "   ") produces a @NotBlank violation ("NIF é obrigatório").
    - A Cliente with nif="12345" (too short) or "1234567890" (too long) produces a @Pattern violation ("NIF deve conter exatamente 9 dígitos numéricos").
    - A Cliente with nif="12345678A" (non-numeric) produces a @Pattern violation.
  </behavior>
  <action>
    Create `backend/src/test/java/com/lexcv/models/ClienteNifValidationTest.java` — a plain JUnit 5
    test (no `@SpringBootTest`, matching the codebase convention) that builds a standalone
    `jakarta.validation.Validator` via `Validation.buildDefaultValidatorFactory().getValidator()`
    and asserts `validator.validate(cliente)` on a `Cliente` populated only enough to isolate the
    `nif` property (set other required-for-persistence fields as needed, but assert specifically
    on the `nif` violations subset). Cover the 4 scenarios from the `<behavior>` block: valid
    9-digit passes; blank/null trips `@NotBlank`; wrong length trips `@Pattern`; non-numeric trips
    `@Pattern`. Assert on the constraint message strings so a future message change is caught.
  </action>
  <verify>
    <automated>cd backend && mvn -q test -Dtest=ClienteNifValidationTest</automated>
  </verify>
  <acceptance_criteria>
    - Test-command: `mvn -q test -Dtest=ClienteNifValidationTest` passes with all 4 scenarios green.
    - Source assertion: the test uses jakarta.validation Validator (validator.validate) and no @SpringBootTest/MockMvc.
    - Behavior assertion: the blank case asserts the "NIF é obrigatório" message and the malformed cases assert the "NIF deve conter exatamente 9 dígitos numéricos" message.
  </acceptance_criteria>
  <done>ClienteNifValidationTest exists and passes; the previously-uncovered NIF validation rule now has regression protection.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → createCliente/updateCliente | Untrusted NIF input crosses here; server-side Bean Validation is the enforcement layer (Phase 73.1) |
| stored data → rendered UI | Enum values rendered to the user; labels are presentation-only (no injection risk, values are a closed enum) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-97-02-01 | Tampering | Cliente.nif input | mitigate | Add regression tests proving @NotBlank + @Pattern reject malformed NIF at the API boundary (Task 2) |
| T-97-02-02 | Information Disclosure | DocumentoTipo render | accept | Labels map a closed enum to static Portuguese strings; no user-controlled content, no injection surface |
| T-97-02-SC | Tampering | package installs | accept | No npm/pip/cargo installs — reuses existing helper + existing hibernate-validator on test classpath |
</threat_model>

<verification>
- Frontend: `pnpm test -- cliente-documento-tipo` green; `pnpm exec tsc --noEmit` 0 errors; `pnpm lint` clean.
- Backend: `mvn -q test -Dtest=ClienteNifValidationTest` green.
- Manual/visual (optional, live env available): open a company client's detail page and printable
  ficha at localhost:3000 and confirm "Registo Comercial" is shown.
</verification>

<success_criteria>
AUD-03 satisfied: DocumentoTipo labels are translated on both the client detail page and the
printable ficha via a single shared helper, and the 4 Phase 73.1 NIF validation scenarios have
passing automated backend coverage.
</success_criteria>

<output>
Create `.planning/phases/LEXCV-97-auditoria-de-milestone-d-vida-t-cnica-e-uat-pendente/97-02-SUMMARY.md` when done.
</output>
