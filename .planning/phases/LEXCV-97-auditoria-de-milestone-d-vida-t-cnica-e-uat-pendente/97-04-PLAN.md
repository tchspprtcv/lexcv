---
phase: 97-auditoria-de-milestone
plan: 04
type: execute
wave: 2
depends_on: [97-01, 97-02, 97-03]
files_modified:
  - .planning/STATE.md
  - .planning/PROJECT.md
autonomous: true
requirements: [AUD-04, AUD-05]

must_haves:
  truths:
    - "A fresh code audit (not a re-run of the already-known debt list) was performed over this milestone's changed surfaces and adjacent code, with every finding either fixed in-session or recorded as tracked debt"
    - "The recurring MINIO_ENDPOINT blocker is documented as RESOLVED for this environment and marked closed in STATE.md/PROJECT.md"
    - "STATE.md Pending Todos + Deferred Items + Blockers reflect the true post-audit state: AUD-01/02/03/05 outcomes marked closed vs. still-open, ready for the milestone-audit skill to read"
  artifacts:
    - path: ".planning/STATE.md"
      provides: "Updated Pending Todos / Blockers / Deferred Items reflecting AUD outcomes"
      contains: "MINIO_ENDPOINT"
    - path: ".planning/PROJECT.md"
      provides: "MinIO resolution + any newly recorded technical debt in Key Decisions / concerns"
      contains: "MINIO"
  key_links:
    - from: "97-04 consolidation"
      to: "97-01-SUMMARY.md / 97-02-SUMMARY.md / 97-UAT.md"
      via: "reads wave-1 outcomes and folds them into STATE.md"
      pattern: "97-0[123]"
    - from: "STATE.md Blockers"
      to: "MINIO_ENDPOINT resolution"
      via: "blocker marked closed with the documented resolution"
      pattern: "MINIO_ENDPOINT"
---

<objective>
Close the milestone: (1) run a genuinely FRESH code-discovery audit (AUD-05) — not a re-run of
any already-known list — over this milestone's changed surfaces and their neighbors, fixing what
is trivially fixable in-session and recording the rest as tracked debt; (2) document the
MINIO_ENDPOINT resolution (AUD-04) — genuinely resolved this session via real MinIO env vars in
`backend/.env`, confirmed working — and mark the long-standing blocker closed; (3) consolidate the
wave-1 audit outcomes (97-01 tenant isolation, 97-02 debt fixes, 97-03 UAT closure) into STATE.md
and PROJECT.md so the milestone-audit / complete-milestone / cleanup skills read an accurate final
state.

This plan runs in Wave 2 so it sees the milestone's FINAL integration shape (all wave-1 fixes
landed) and is the SOLE writer of STATE.md/PROJECT.md this phase (avoids write contention).

Purpose: Satisfy AUD-04 + AUD-05 and leave the repo ready for milestone close-out.
Output: Fresh-audit findings, MinIO resolution documented, STATE.md/PROJECT.md updated.
</objective>

<execution_context>
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/LEXCV-97-auditoria-de-milestone-d-vida-t-cnica-e-uat-pendente/97-CONTEXT.md

<wave1_outcomes>
<!-- Read these completed wave-1 outputs before consolidating. -->
@.planning/phases/LEXCV-97-auditoria-de-milestone-d-vida-t-cnica-e-uat-pendente/97-01-SUMMARY.md
@.planning/phases/LEXCV-97-auditoria-de-milestone-d-vida-t-cnica-e-uat-pendente/97-02-SUMMARY.md
@.planning/phases/LEXCV-97-auditoria-de-milestone-d-vida-t-cnica-e-uat-pendente/97-03-SUMMARY.md
@.planning/phases/LEXCV-97-auditoria-de-milestone-d-vida-t-cnica-e-uat-pendente/97-UAT.md
</wave1_outcomes>

<minio_resolution_facts>
<!-- From 97-CONTEXT.md — the resolution to document, already TRUE for this environment. -->
- backend/.env (gitignored) now sets MINIO_ENDPOINT=http://localhost:9000 + credentials, pointing at a running lexcv_minio Docker container.
- Backend boots fully (Spring context starts, MinioConfig.s3Client() no longer throws the "Illegal character ... ${MINIO_ENDPOINT}" IllegalArgumentException).
- This is the blocker recorded in STATE.md "Blockers/Concerns" and across 87/89-HUMAN-UAT gaps — the same env issue, now genuinely resolved for local dev (not merely worked around).
- The fix is environment/config, not code: backend/.env.example already documents the required MINIO_* vars.
</minio_resolution_facts>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fresh code-discovery audit (AUD-05) over the milestone's final shape</name>
  <files>backend/src/main/java/com/lexcv/services/NotificacaoService.java, backend/src/main/java/com/lexcv/controllers/NotificacaoController.java, backend/src/main/java/com/lexcv/jobs/AlertasDiariosJob.java</files>
  <read_first>
    - .planning/STATE.md (Pending Todos + Deferred Items — so this audit does NOT just re-list them)
    - backend/src/main/java/com/lexcv/services/NotificacaoService.java (final post-milestone shape)
    - backend/src/main/java/com/lexcv/controllers/NotificacaoController.java (confirm every new endpoint has an @PreAuthorize scope)
    - backend/src/main/java/com/lexcv/jobs/AlertasDiariosJob.java (error isolation per tenant/entity)
    - web/src/hooks (new notification/preference/snooze hooks added this milestone)
  </read_first>
  <action>
    Perform a bounded, FRESH pass (target: the notification/preference/snooze/team surfaces added
    in Phases 90-96 plus immediately adjacent code) looking for gaps NOT already tracked in
    STATE.md. Concretely check for: (a) new controller endpoints missing an `@PreAuthorize`
    scope:action guard or missing tenant scoping; (b) unvalidated request inputs on new
    endpoints; (c) `TODO`/`FIXME`/`XXX` markers introduced this milestone (grep the changed
    files); (d) N+1 query risks in new list/fan-out paths; (e) swallowed exceptions or missing
    error isolation in the daily job; (f) dead code left behind (e.g., the removed
    /eventos/upcoming). For each finding, classify: FIX-NOW (trivial, low-risk — apply inline; a
    source fix is safe here because Wave 1 has completed) or RECORD-AS-DEBT (non-trivial — capture
    a precise description, file, and rationale for Task 3 to write into STATE.md). If the pass
    finds nothing new, state that explicitly with the surfaces covered — a clean result is a valid
    outcome, but the coverage must be enumerated so it is auditable. Do NOT re-audit the tenant
    isolation covered by 97-01 or the debt closed by 97-02.
  </action>
  <verify>
    <automated>cd backend && grep -rn "TODO\|FIXME\|XXX" src/main/java/com/lexcv/services/NotificacaoService.java src/main/java/com/lexcv/controllers/NotificacaoController.java src/main/java/com/lexcv/jobs/AlertasDiariosJob.java | grep -v '^#' | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - Behavior assertion: the SUMMARY enumerates the surfaces audited and lists each finding classified FIX-NOW or RECORD-AS-DEBT (or states "no new gaps" with the covered surfaces named).
    - Source assertion: every new NotificacaoController endpoint is confirmed to carry an @PreAuthorize guard (or a finding is raised if not).
    - Test-command: if any FIX-NOW change was applied to backend, `mvn -q -DskipTests package` succeeds; if frontend, `pnpm exec tsc --noEmit` reports 0 errors.
  </acceptance_criteria>
  <done>A fresh, enumerated audit pass is complete; trivial gaps fixed, non-trivial gaps captured for Task 3, coverage documented.</done>
</task>

<task type="auto">
  <name>Task 2: Document the MINIO_ENDPOINT resolution (AUD-04) and mark the blocker closed</name>
  <files>.planning/STATE.md, .planning/PROJECT.md</files>
  <read_first>
    - .planning/STATE.md (the "Blockers/Concerns" section and the Deferred Items rows mentioning MINIO)
    - .planning/PROJECT.md (Key Decisions / concerns section — where the resolution note belongs)
    - backend/.env.example (confirm the MINIO_* vars are documented for future environments)
    - backend/src/main/java/com/lexcv/config/MinioConfig.java (the s3Client() bean that previously threw)
  </read_first>
  <action>
    In STATE.md, edit the "Blockers/Concerns" section so the MINIO_ENDPOINT entry is marked
    RESOLVED (2026-07-14): state that `backend/.env` now supplies a real `MINIO_ENDPOINT`
    (http://localhost:9000) + credentials against a running `lexcv_minio` container, the Spring
    context boots fully, and `backend/.env.example` documents the required MINIO_* vars for any
    future environment — clarifying this is an environment/config resolution, not a code change.
    In PROJECT.md, add a short note under Key Decisions (or the equivalent concerns/blockers area)
    recording that the recurring MinIO env blocker that gated live UAT across v2.8/v2.9/v2.10 is
    closed as of v2.11 Phase 97, with the resolution. Do not fabricate a code fix — the resolution
    is that the env is now correctly provisioned locally.
  </action>
  <verify>
    <automated>grep -c "RESOLVED\|resolvido" .planning/STATE.md && grep -c "MINIO\|MinIO" .planning/PROJECT.md</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: STATE.md "Blockers/Concerns" MINIO_ENDPOINT entry is marked RESOLVED with the resolution (env vars in backend/.env, container running, context boots).
    - Source assertion: PROJECT.md records the MinIO blocker closure with a date/phase reference.
    - Behavior assertion: the note states this is an environment/config resolution, and that .env.example documents the vars for future environments.
  </acceptance_criteria>
  <done>The MINIO_ENDPOINT blocker is documented as resolved and marked closed in both STATE.md and PROJECT.md.</done>
</task>

<task type="auto">
  <name>Task 3: Consolidate wave-1 outcomes into STATE.md / PROJECT.md for milestone close-out</name>
  <files>.planning/STATE.md, .planning/PROJECT.md</files>
  <read_first>
    - .planning/phases/LEXCV-97-auditoria-de-milestone-d-vida-t-cnica-e-uat-pendente/97-01-SUMMARY.md (AUD-01 tenant verdict)
    - .planning/phases/LEXCV-97-auditoria-de-milestone-d-vida-t-cnica-e-uat-pendente/97-02-SUMMARY.md (AUD-03 fixes)
    - .planning/phases/LEXCV-97-auditoria-de-milestone-d-vida-t-cnica-e-uat-pendente/97-UAT.md (AUD-02 UAT verdicts)
    - .planning/STATE.md (Pending Todos + Deferred Items tables to update)
  </read_first>
  <action>
    Update STATE.md so its "Pending Todos" and "Deferred Items" reflect the true post-audit state:
    mark the DocumentoTipo raw-label todo and the missing-NIF-tests todo as CLOSED by 97-02
    (AUD-03); update the "UAT pending (75/76/79/81/82/84/85/89)" carried item to reflect the
    97-UAT.md verdicts (which phases are live-verified, which are code-verified, which remain
    open-with-reason — e.g., the Phase 85 product decision carried forward); note the AUD-01
    tenant-isolation confirmation result from 97-01. Add any RECORD-AS-DEBT findings from Task 1 as
    new, precisely-worded Pending Todos (file + rationale). Do NOT mark the milestone itself
    complete and do NOT edit ROADMAP.md phase-completion tables — that is the close-out skill's job;
    this task only leaves STATE.md/PROJECT.md accurate. Keep the STATE.md size discipline (the file
    documents its own size constraint) — prefer editing existing rows over appending duplicates.
  </action>
  <verify>
    <automated>grep -c "AUD-01\|AUD-02\|AUD-03\|97-UAT\|CLOSED\|fechado" .planning/STATE.md</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: STATE.md Pending Todos no longer lists the DocumentoTipo-labels and NIF-tests items as open (marked closed by 97-02 / AUD-03).
    - Source assertion: the UAT carried item reflects 97-UAT.md's per-phase verdicts, and any open item (incl. the Phase 85 product decision) is explicitly stated with its reason.
    - Behavior assertion: any Task 1 RECORD-AS-DEBT finding appears as a new Pending Todo with file + rationale; the AUD-01 verdict is noted.
    - Source assertion: ROADMAP.md phase-completion tables are NOT modified by this task.
  </acceptance_criteria>
  <done>STATE.md and PROJECT.md accurately reflect what this milestone closed vs. left open, ready for the milestone-audit skill to consume.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| new endpoints → auth/RBAC | Fresh audit confirms new controller surfaces enforce @PreAuthorize scope:action + tenant scoping |
| planning docs → close-out skills | STATE.md/PROJECT.md are the source of truth the milestone-audit skill reads; inaccuracies here mislead close-out |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-97-04-01 | Elevation of Privilege | new NotificacaoController endpoints | mitigate | Fresh audit confirms every new endpoint carries an @PreAuthorize scope guard (Task 1) |
| T-97-04-02 | Denial of Service | AlertasDiariosJob error isolation | mitigate | Confirm per-tenant/per-entity exception isolation still intact; raise a finding if not (Task 1) |
| T-97-04-03 | Repudiation | milestone close-out state | mitigate | STATE.md/PROJECT.md updated to an accurate, attributable post-audit state (Task 2/3) |
| T-97-04-SC | Tampering | package installs | accept | No npm/pip/cargo installs — audit + documentation only |
</threat_model>

<verification>
- If any FIX-NOW code change was applied: backend `mvn -q -DskipTests package` compiles / frontend `pnpm exec tsc --noEmit` clean.
- STATE.md shows the MINIO blocker RESOLVED and the AUD-03 todos closed.
- PROJECT.md records the MinIO closure.
- ROADMAP.md phase tables untouched (close-out skill owns those).
</verification>

<success_criteria>
AUD-04 + AUD-05 satisfied: a fresh code audit ran with every finding fixed-or-recorded, the
MINIO_ENDPOINT blocker is documented resolved and closed, and STATE.md/PROJECT.md present an
accurate final state for milestone v2.11 close-out.
</success_criteria>

<output>
Create `.planning/phases/LEXCV-97-auditoria-de-milestone-d-vida-t-cnica-e-uat-pendente/97-04-SUMMARY.md` when done.
</output>
