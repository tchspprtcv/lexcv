---
phase: 122-relat-rio-de-utiliza-o-por-tenant
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/test/java/com/lexcv/controllers/PlatformAdminControllerTest.java
autonomous: true
requirements: [UTIL-01]

must_haves:
  truths:
    - "Existe um teste automatizado que prova que GET /api/v1/platform/tenants devolve um tenant com ativo=false no corpo da resposta"
    - "O teste passa contra a implementacao atual, sem qualquer alteracao a codigo de producao — e uma guarda de regressao, nao uma correcao"
    - "Se uma refatoracao futura acrescentar um filtro de estado a listTenants(), a suite falha em vez de deixar passar"
    - "Nenhum ficheiro de producao do backend foi tocado por esta fase"
  artifacts:
    - path: "backend/src/test/java/com/lexcv/controllers/PlatformAdminControllerTest.java"
      provides: "Teste listTenants_incluiTenantSuspensoComEstadoAtivoFalseNaResposta no Grupo A, provando o criterio de sucesso 3 no lado do servidor"
      contains: "listTenants_incluiTenantSuspensoComEstadoAtivoFalseNaResposta"
  key_links:
    - from: "backend/src/test/java/com/lexcv/controllers/PlatformAdminControllerTest.java"
      to: "PlatformAdminController.listTenants()"
      via: "chamada Java direta a novoController().listTenants() com um fixture .ativo(false)"
      pattern: "\\.ativo\\(false\\)"
    - from: "PlatformAdminController.toSummary(Tenant)"
      to: "UserRepository.countByTenantIdAndAtivoTrue"
      via: "stub Mockito unico, confirmando que a contagem de utilizadores ativos continua a ter uma so implementacao"
      pattern: "countByTenantIdAndAtivoTrue"
---

<objective>
Fechar o unico gap genuinamente real que a investigacao desta fase encontrou no backend: `GET /api/v1/platform/tenants` **ja** devolve tenants suspensos (nao existe nenhum filtro no pipeline, confirmado por leitura direta), mas nao existe nenhum teste que o prove.

Purpose: O criterio de sucesso 3 da Phase 122 diz que "tenants suspensos continuam visiveis no relatorio com o seu estado identificado, em vez de desaparecerem da lista". No frontend isso e garantido pelo Plan 01 (nenhum filtro no memo de pesquisa, Badge de estado). No backend, e garantido hoje apenas por ausencia: nao ha `filter` no stream de `listTenants()`. Os 3 testes `listTenants_*` existentes constroem **todos** os seus fixtures com `.ativo(true)` — confirmado por leitura do ficheiro completo. Uma refatoracao futura que acrescentasse um filtro de estado (por exemplo, para "limpar" a consola) passaria a suite inteira e quebraria silenciosamente o relatorio de faturacao. Este plano acrescenta a unica assercao que falta.

Este plano **nao altera codigo de producao**. Nao e uma correcao de bug — e uma guarda de regressao sobre comportamento correto que hoje esta sem prova. Espera-se que passe imediatamente contra a implementacao atual; se falhar, isso e um achado a escalar, nao a "arranjar" com uma alteracao ao controlador.

Output: um metodo de teste novo dentro de um ficheiro de teste existente. Zero ficheiros novos, zero ficheiros de producao tocados.
</objective>

<execution_context>
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/LEXCV-122-relat-rio-de-utiliza-o-por-tenant/122-CONTEXT.md
@.planning/phases/LEXCV-122-relat-rio-de-utiliza-o-por-tenant/122-PATTERNS.md
@backend/src/test/java/com/lexcv/controllers/PlatformAdminControllerTest.java
@backend/src/main/java/com/lexcv/controllers/PlatformAdminController.java
@backend/src/main/java/com/lexcv/dtos/TenantAdminSummaryResponse.java
</context>

<interfaces>
<!-- Contratos ja confirmados no planeamento por leitura direta do codigo. NAO redescobrir por exploracao. -->

Implementacao sob teste (`PlatformAdminController.java:106-113`) — **nao alterar**. Confirmado: nenhum `filter` no pipeline, so `map` + `sorted`:

```java
@GetMapping("/tenants")
public ResponseEntity<?> listTenants() {
    List<TenantAdminSummaryResponse> tenants = tenantRepository.findAll().stream()
            .map(this::toSummary)
            .sorted(Comparator.comparing(TenantAdminSummaryResponse::getNome, String.CASE_INSENSITIVE_ORDER))
            .collect(Collectors.toList());
    return ResponseEntity.ok(tenants);
}
```

Projecao (`PlatformAdminController.java:191-200`) — a unica implementacao de contagem de utilizadores ativos em todo o codebase passa por aqui:

```java
private TenantAdminSummaryResponse toSummary(Tenant tenant) {
    return TenantAdminSummaryResponse.builder()
            .id(tenant.getId())
            .nome(tenant.getNome())
            .plano(tenant.getPlano())
            .limiteUtilizadores(tenant.getLimiteUtilizadores())
            .ativo(tenant.getAtivo())
            .utilizadoresAtivos(userRepository.countByTenantIdAndAtivoTrue(tenant.getId()))
            .build();
}
```

Campo do DTO (`TenantAdminSummaryResponse.java`) — `ativo` e `Boolean` (wrapper) com `@Data` do Lombok, logo o getter e `getAtivo()`, **nao** `isAtivo()`:

```java
private UUID id;
private String nome;
private TenantPlano plano;
private Integer limiteUtilizadores;
private Boolean ativo;
private long utilizadoresAtivos;
```

Helper de instanciacao ja existente no ficheiro de teste (`:103-105`) — o Grupo A usa instanciacao direta, sem proxy AOP:

```java
private PlatformAdminController novoController() {
    return new PlatformAdminController(setupService, tenantRepository, userRepository);
}
```

Teste-modelo a clonar estruturalmente (`PlatformAdminControllerTest.java:259-282`) — a forma arrange/act/assert com 2 tenants e o idioma `orElseThrow()`-como-assercao:

```java
@Test
void listTenants_devolve200ComUmResumoPorTenantComAContagemDoSeuProprioId() {
    UUID tenantAId = UUID.randomUUID();
    UUID tenantBId = UUID.randomUUID();
    Tenant tenantA = Tenant.builder().id(tenantAId).nome("Escritorio A").plano(TenantPlano.STARTER)
            .limiteUtilizadores(5).ativo(true).build();
    Tenant tenantB = Tenant.builder().id(tenantBId).nome("Escritorio B").plano(TenantPlano.STANDARD)
            .limiteUtilizadores(null).ativo(true).build();
    when(tenantRepository.findAll()).thenReturn(List.of(tenantA, tenantB));
    when(userRepository.countByTenantIdAndAtivoTrue(tenantAId)).thenReturn(3L);
    when(userRepository.countByTenantIdAndAtivoTrue(tenantBId)).thenReturn(7L);

    ResponseEntity<?> response = novoController().listTenants();

    assertEquals(HttpStatus.OK, response.getStatusCode());
    @SuppressWarnings("unchecked")
    List<TenantAdminSummaryResponse> corpo = (List<TenantAdminSummaryResponse>) response.getBody();
    assertNotNull(corpo);
    assertEquals(2, corpo.size());
    TenantAdminSummaryResponse resumoA = corpo.stream().filter(r -> r.getId().equals(tenantAId)).findFirst().orElseThrow();
    TenantAdminSummaryResponse resumoB = corpo.stream().filter(r -> r.getId().equals(tenantBId)).findFirst().orElseThrow();
    assertEquals(3L, resumoA.getUtilizadoresAtivos());
    assertEquals(7L, resumoB.getUtilizadoresAtivos());
}
```

Ponto de insercao exato: o Grupo A comeca no marcador de comentario `// ---- Grupo A: comportamento de listTenants/updateTenant/setTenantAtivo (Phase 120 Plan 02) ----` (`:257`). O teste novo vai **depois** de `listTenants_devolveOrdenadoPorNomeCaseInsensitiveMesmoQuandoFindAllDevolveForaDeOrdem`, que fecha em `:319`, e **antes** de `updateTenant_comPlanoELimiteValidosDevolve200EGravaComAtivoInalterado`, que abre em `:321`. Mesmo grupo, mesma seccao — isto e um teste comportamental, nao um teste de autorizacao do Grupo B.

Imports e static-imports necessarios: **todos ja presentes** no ficheiro (`assertEquals`, `assertNotNull`, `when`, `any`, `HttpStatus`, `ResponseEntity`, `List`, `UUID`, `Tenant`, `TenantPlano`, `TenantAdminSummaryResponse`). Confirmado por leitura das linhas 1-51. Zero imports novos.

Modo estrito do Mockito: o `MockitoExtension` deste projeto e estrito. Um `when(...)` que nunca seja usado dispara `UnnecessaryStubbingException`. Como o teste novo tem 2 tenants e nao afirma contagens especificas, usar **um** stub com matcher (`countByTenantIdAndAtivoTrue(any())`) em vez de dois stubs por id — exatamente o que `listTenants_nuncaDevolveEntidadesCruas` (`:285-296`) e `listTenants_devolveOrdenado...` (`:299-319`) ja fazem.

Comandos (CLAUDE.md): `cd backend && mvn test -Dtest=PlatformAdminControllerTest`, `mvn test`, `mvn spotbugs:check`. Sem wrapper Maven commitado — usar o `mvn` do sistema. Requer `backend/.env` (existente) para o build compilar; a suite usa Mockito puro e nao precisa de base de dados nem de MinIO.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Acrescentar a guarda de regressao de tenant suspenso ao Grupo A de PlatformAdminControllerTest</name>

  <files>
    backend/src/test/java/com/lexcv/controllers/PlatformAdminControllerTest.java
  </files>

  <read_first>
    - `backend/src/test/java/com/lexcv/controllers/PlatformAdminControllerTest.java` (ficheiro completo) — o ficheiro a alterar. Ler em particular: os imports e static-imports (`:1-51`, para confirmar que **nenhum** import novo e necessario), o Javadoc de classe (`:53-77`, que explica a divisao Grupo A / Grupo B), os `@Mock` e o helper `novoController()` (`:81-105`), o marcador de inicio do Grupo A (`:257`), os 3 testes `listTenants_*` existentes (`:259-319`, todos com fixtures de estado ativo — o gap que esta task fecha) e o inicio de `updateTenant_comPlanoELimiteValidosDevolve200EGravaComAtivoInalterado` (`:321`, o limite inferior do ponto de insercao).
    - `backend/src/main/java/com/lexcv/controllers/PlatformAdminController.java` — `listTenants()` (`:106-113`) e `toSummary(Tenant)` (`:191-200`), para confirmar por leitura direta que nao existe filtro de estado no pipeline **antes** de escrever a assercao. Este ficheiro **nao** e alterado por esta task.
    - `backend/src/main/java/com/lexcv/dtos/TenantAdminSummaryResponse.java` — confirmar que `ativo` e do tipo wrapper `Boolean`, logo o getter gerado pelo Lombok e a forma `get` e nao a forma `is`.
    - `.planning/phases/LEXCV-122-relat-rio-de-utiliza-o-por-tenant/122-PATTERNS.md` — a seccao sobre `PlatformAdminControllerTest.java`, que fixa o nome do teste, o ponto de insercao exato e o idioma `orElseThrow()`-como-assercao.
  </read_first>

  <behavior>
    - `listTenants()` chamado com `tenantRepository.findAll()` a devolver dois tenants, um com `.ativo(true)` e outro com `.ativo(false)`, devolve `HttpStatus.OK`.
    - O corpo da resposta tem 2 elementos — o tenant suspenso **nao** e removido.
    - O elemento cujo `getId()` iguala o id do tenant suspenso existe (provado pelo `orElseThrow()` do stream filtrado, que lanca `NoSuchElementException` se estiver ausente).
    - Esse elemento tem `getAtivo()` igual a `false` — o estado e transportado ao cliente, nao normalizado nem omitido.
    - Caso de fronteira coberto pelo mesmo teste: os dois tenants tem `limiteUtilizadores` deixado a `null` (o contrato "sem limite" da Phase 117), pelo que a projecao nao depende de um limite estar definido para incluir a linha.
  </behavior>

  <action>
    Abrir `backend/src/test/java/com/lexcv/controllers/PlatformAdminControllerTest.java` e inserir **um** metodo de teste novo, chamado `listTenants_incluiTenantSuspensoComEstadoAtivoFalseNaResposta`, no ponto exato descrito em `<interfaces>`: dentro do bloco do Grupo A, imediatamente depois de `listTenants_devolveOrdenadoPorNomeCaseInsensitiveMesmoQuandoFindAllDevolveForaDeOrdem` e imediatamente antes de `updateTenant_comPlanoELimiteValidosDevolve200EGravaComAtivoInalterado`. Nao reordenar nem reformatar nenhum metodo existente.

    Arrange: dois `UUID.randomUUID()` (um para o tenant ativo, um para o suspenso). Dois `Tenant.builder()`: o primeiro com `.nome("Escritorio Ativo")`, `.plano(TenantPlano.STARTER)`, `.ativo(true)`; o segundo com `.nome("Escritorio Suspenso")`, `.plano(TenantPlano.STANDARD)`, `.ativo(false)`. Seguir a convencao de nomes de fixture ja usada no ficheiro (`Escritorio A`/`B`/`C`/`D`, `Alfa`/`bravo`/`Charlie Advogados`) — nomes descritivos em portugues sem acentos, consistentes com o resto do ficheiro. Stub de `tenantRepository.findAll()` a devolver `List.of(tenantAtivo, tenantSuspenso)` e **um unico** stub `when(userRepository.countByTenantIdAndAtivoTrue(any())).thenReturn(0L)` — dois stubs por id seriam desnecessarios e o modo estrito do Mockito reclamaria de qualquer um que nao fosse consumido.

    Act: `novoController().listTenants()`. Instanciacao direta, sem `novoProxyComMethodSecurity()` — este e um teste de comportamento, nao de autorizacao; o Grupo B ja cobre o gate de papel para os 4 handlers.

    Assert, nesta ordem: `HttpStatus.OK`; cast do corpo para `List<TenantAdminSummaryResponse>` com `@SuppressWarnings("unchecked")` (mesma tecnica dos 3 testes vizinhos); `assertNotNull(corpo)`; `assertEquals(2, corpo.size())`; localizar o resumo do tenant suspenso com `corpo.stream().filter(r -> r.getId().equals(tenantSuspensoId)).findFirst().orElseThrow()` — o `orElseThrow()` **e** parte da assercao (falha com `NoSuchElementException` se a linha tiver sido filtrada, uma prova mais forte do que so contar o tamanho); e por fim `assertEquals(false, resumoSuspenso.getAtivo())`. Usar `getAtivo()`, nao `isAtivo()`: o campo e `Boolean` (wrapper), logo o Lombok gera a forma `get`.

    Escrever um Javadoc curto sobre o metodo a explicar tres coisas, em prosa: (a) que este teste guarda o criterio de sucesso 3 da Phase 122 — o relatorio de utilizacao depende de tenants suspensos continuarem na lista, porque um escritorio suspenso continua a ter de ser faturado ou reconciliado; (b) que a implementacao atual ja satisfaz isto por ausencia de filtro, e por isso o teste passa de imediato — o seu valor e impedir que uma refatoracao futura acrescente esse filtro em silencio; (c) que os 3 testes `listTenants_*` anteriores usavam todos fixtures com estado ativo, o que deixava esta propriedade sem prova. Escrever essa prosa **sem** reproduzir literalmente a expressao de filtro que se quer impedir e sem repetir o nome do proprio metodo de teste dentro do comentario — as Phases 119/120/121 tem tres incidentes registados em STATE.md de comentarios auto-referenciais a disparar gates baseados em grep.

    Correr `cd backend && mvn test -Dtest=PlatformAdminControllerTest` e confirmar que **todos** os testes da classe passam, incluindo o novo. Esperado: verde imediato — nao existe filtro no pipeline. Se o teste novo falhar, **parar e escalar**: isso significaria que existe um filtro que a investigacao desta fase nao encontrou, e a decisao de o remover e de produto/arquitetura, nao desta task. Nunca alterar `PlatformAdminController.java` para fazer o teste passar.

    Depois, correr a suite completa `cd backend && mvn test` para confirmar zero regressoes noutras classes.

    Proibicoes: nao criar ficheiros novos; nao tocar em nenhum ficheiro sob `backend/src/main/`; nao alterar nem reformatar nenhum dos testes existentes; nao introduzir `MockMvc`, `@SpringBootTest` ou `@WebMvcTest` (este codebase nao tem nenhum harness desse tipo e esta fase nao e o sitio para o estrear); nao acrescentar dependencias ao `pom.xml`.
  </action>

  <verify>
    <automated>cd backend && grep -q 'listTenants_incluiTenantSuspensoComEstadoAtivoFalseNaResposta' src/test/java/com/lexcv/controllers/PlatformAdminControllerTest.java && [ "$(grep -v '^[[:space:]]*//' src/test/java/com/lexcv/controllers/PlatformAdminControllerTest.java | grep -v '^[[:space:]]*\*' | grep -c '\.ativo(false)')" -ge 1 ] && mvn -q test -Dtest=PlatformAdminControllerTest && mvn -q test</automated>
  </verify>

  <acceptance_criteria>
    - `backend/src/test/java/com/lexcv/controllers/PlatformAdminControllerTest.java` contem um metodo `listTenants_incluiTenantSuspensoComEstadoAtivoFalseNaResposta` anotado com `@Test`.
    - Sobre linhas de codigo (excluindo `//` e continuacoes `*`), `grep -c '\.ativo(false)'` no ficheiro devolve pelo menos 1 — antes desta task devolvia 0 nos fixtures de `listTenants_*`.
    - O corpo do teste novo contem `orElseThrow()` e `assertEquals(false, ` aplicado a `getAtivo()`.
    - O teste novo esta posicionado entre `listTenants_devolveOrdenadoPorNomeCaseInsensitiveMesmoQuandoFindAllDevolveForaDeOrdem` e `updateTenant_comPlanoELimiteValidosDevolve200EGravaComAtivoInalterado` (verificavel comparando os numeros de linha dos tres nomes).
    - `cd backend && mvn test -Dtest=PlatformAdminControllerTest` termina com exit code 0 e o output reporta 0 falhas e 0 erros.
    - `cd backend && mvn test` (suite completa) termina com exit code 0.
    - `git diff --name-only` lista **exclusivamente** `backend/src/test/java/com/lexcv/controllers/PlatformAdminControllerTest.java`. Em particular nao lista `backend/src/main/java/com/lexcv/controllers/PlatformAdminController.java` nem `backend/pom.xml`.
    - `git diff --stat` mostra apenas linhas acrescentadas nesse ficheiro (nenhuma linha removida de testes existentes).
  </acceptance_criteria>

  <done>O comportamento "um tenant suspenso continua na resposta de `listTenants()` com `ativo=false`" tem, pela primeira vez, uma prova automatizada; a suite backend inteira esta verde; e nenhum ficheiro de producao foi tocado.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `GET /api/v1/platform/tenants` -> cliente | Fronteira de autorizacao inalterada: gate de classe `hasRole('PLATAFORMA_ADMIN')` em `PlatformAdminController` (Phase 119). Esta task nao acrescenta, remove nem enfraquece nenhuma anotacao |
| `listTenants()` -> conjunto de linhas devolvidas | A decisao de **quais** tenants entram na resposta e uma propriedade de correcao de faturacao; hoje esta correta mas sem prova |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-122-07 | Repudiation | `listTenants()` a passar a filtrar tenants suspensos numa refatoracao futura | mitigate | Teste novo com fixture `.ativo(false)` a afirmar presenca da linha (via `orElseThrow()`) e `getAtivo()` igual a `false`. Sem ele, um escritorio suspenso desapareceria da base factual de faturacao sem nenhum sinal na suite |
| T-122-08 | Tampering | Alterar codigo de producao para fazer o teste passar | mitigate | Proibicao explicita na `<action>` (parar e escalar em vez de alterar o controlador) mais criterio de aceitacao sobre `git diff --name-only`, que so pode listar o ficheiro de teste |
| T-122-09 | Information Disclosure | Teste a contornar o gate de autorizacao e a normalizar isso como padrao aceitavel | accept | O Grupo A instancia o controlador diretamente por convencao ja estabelecida em todo este codebase; uma chamada Java direta nunca avalia `@PreAuthorize`, e o gate de papel dos 4 handlers ja tem prova comportamental propria no Grupo B (proxy AOP real). Este teste nao toca nessa fronteira |
| T-122-10 | Information Disclosure | Fixtures de teste com credenciais ou dados reais | accept | Fixtures usam apenas `UUID.randomUUID()`, nomes de escritorio ficticios e repositorios mockados; zero credenciais, zero dados reais, zero acesso a base de dados |
| T-122-SC | Tampering | Instalacoes Maven/npm | n/a | Zero dependencias novas: todos os imports e static-imports necessarios ja existem no ficheiro (confirmado por leitura de `:1-51`), e `pom.xml` nao e tocado. O Package Legitimacy Gate nao se aplica |
</threat_model>

<verification>
1. `cd backend && mvn test -Dtest=PlatformAdminControllerTest` — exit 0, 0 falhas, 0 erros; o output nomeia o teste novo entre os executados.
2. `cd backend && mvn test` — exit 0 na suite completa (zero regressoes noutras classes).
3. `cd backend && mvn spotbugs:check` — exit 0 (o SAST deste projeto tem de continuar limpo; um ficheiro de teste novo nao deve introduzir achados, mas confirma-se em vez de se assumir).
4. `git diff --name-only` lista exatamente 1 ficheiro, e esse ficheiro esta sob `backend/src/test/`.
5. `grep -n 'listTenants_' backend/src/test/java/com/lexcv/controllers/PlatformAdminControllerTest.java` mostra 4 testes `listTenants_*` (eram 3 antes desta task).
</verification>

<success_criteria>
- Existe um 4o teste `listTenants_*` na classe, com um fixture `.ativo(false)`, a afirmar presenca da linha e `ativo` igual a `false` no corpo da resposta.
- A suite backend completa esta verde e o SpotBugs limpo.
- Zero ficheiros de producao alterados, zero ficheiros novos, zero dependencias novas.
- O SUMMARY registra explicitamente que o teste passou na primeira execucao (comportamento ja correto, guarda de regressao acrescentada) e nao como resultado de uma alteracao ao controlador.

**Nota de rastreabilidade (nao ignorar):** este plano lista `UTIL-01` no frontmatter para rastreabilidade, mas **NAO** deve correr `requirements mark-complete` para `UTIL-01`. `REQUIREMENTS.md` exige um relatorio interno alcancavel a mostrar 4 campos por tenant — um teste de backend nao entrega nenhum ecra. `UTIL-01` fecha no Plan 03, que e o plano que torna o relatorio alcancavel.
</success_criteria>

<output>
Criar `.planning/phases/LEXCV-122-relat-rio-de-utiliza-o-por-tenant/122-02-SUMMARY.md` no final, registando: o nome exato do teste acrescentado, o numero de linha onde foi inserido, a contagem de testes `listTenants_*` antes/depois (3 -> 4), o resultado da primeira execucao (esperado: verde imediato, sem alteracao de producao), e o resultado da suite completa mais do `spotbugs:check`.
</output>
