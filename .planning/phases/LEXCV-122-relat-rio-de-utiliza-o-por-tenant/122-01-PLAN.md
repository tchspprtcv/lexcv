---
phase: 122-relat-rio-de-utiliza-o-por-tenant
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/app/(dashboard)/plataforma/relatorio/columns.tsx
  - web/src/app/(dashboard)/plataforma/relatorio/page.tsx
autonomous: true
requirements: [UTIL-01]

must_haves:
  truths:
    - "Existe uma rota /plataforma/relatorio que mostra, por tenant, nome, plano, limite de utilizadores contratado e utilizadores ativos agora"
    - "Um utilizador autenticado sem o papel PLATAFORMA_ADMIN que navegue para /plataforma/relatorio ve AccessDeniedState, nunca a lista de tenants"
    - "Antes do primeiro resolve de useMe() a pagina nao renderiza nada nem dispara GET /platform/tenants — o guard falha fechado, nao aberto"
    - "O numero de utilizadores ativos vem diretamente do campo utilizadoresAtivos da resposta da API, sem qualquer recalculo no cliente"
    - "Um tenant suspenso continua a ter linha no relatorio, com um Badge a ler Suspenso — nenhum filtro o remove"
    - "O numero de utilizadores ativos tambem e visivel em viewports mobile (abaixo de md), nao apenas na DataTable de desktop"
    - "O ecra nao tem nenhuma acao de criar/editar/suspender/eliminar — zero mutacoes"
  artifacts:
    - path: "web/src/app/(dashboard)/plataforma/relatorio/columns.tsx"
      provides: "relatorioColumns — 4 column defs (nome, plano, utilizadores, estado) num array estatico, sem factory e sem coluna de acoes"
      min_lines: 90
      exports: ["relatorioColumns"]
      contains: "utilizadoresAtivos"
    - path: "web/src/app/(dashboard)/plataforma/relatorio/page.tsx"
      provides: "Rota do relatorio: guard PLATAFORMA_ADMIN, header satelite, Card com pesquisa, cards mobile e DataTable de desktop"
      min_lines: 150
      contains: "useTenantsAdmin"
  key_links:
    - from: "web/src/app/(dashboard)/plataforma/relatorio/page.tsx"
      to: "useTenantsAdmin (GET /api/v1/platform/tenants)"
      via: "import de @/hooks/use-platform-admin, sem modificacao do hook"
      pattern: "useTenantsAdmin\\(\\)"
    - from: "web/src/app/(dashboard)/plataforma/relatorio/page.tsx"
      to: "relatorioColumns"
      via: "import de ./columns, passado a DataTable sem useMemo (nao e factory)"
      pattern: "columns=\\{relatorioColumns\\}"
    - from: "web/src/app/(dashboard)/plataforma/relatorio/columns.tsx"
      to: "TENANT_RESERVADO"
      via: "import de ../columns (nao redeclarado)"
      pattern: "from \"\\.\\./columns\""
    - from: "web/src/app/(dashboard)/plataforma/relatorio/columns.tsx"
      to: "tenant.utilizadoresAtivos"
      via: "leitura direta do campo da resposta, sem agregacao no cliente"
      pattern: "tenant\\.utilizadoresAtivos"
---

<objective>
Construir o ecra do relatorio de utilizacao — a substancia desta fase inteira — como uma rota nova, 100% de leitura, em `web/src/app/(dashboard)/plataforma/relatorio/`.

Purpose: O criterio de sucesso 1 da Phase 122 exige um ecra que **mostre na interface**, por tenant, nome/plano/limite contratado/utilizadores ativos, acessivel so a `PLATAFORMA_ADMIN`. O criterio 2 exige que o numero de utilizadores ativos venha da mesma unica fonte de verdade da Phase 117 (`UserRepository.countByTenantIdAndAtivoTrue`) e nunca de um calculo paralelo. O criterio 3 exige que tenants suspensos continuem visiveis com o estado identificado. Os tres resolvem-se sem uma unica linha de backend novo: `GET /api/v1/platform/tenants` (Phase 120) ja devolve exatamente os 6 campos necessarios, para **todos** os tenants sem filtro de `ativo`, e o proprio doc-comment de `TenantAdminSummaryResponse` ja nomeia esta fase por numero como reutilizadora desses campos.

Este plano e deliberadamente um exercicio de "reutilizar e cortar", nao de desenho novo: 4 dos 5 cell renderers de `plataforma/columns.tsx` sao copiados verbatim, o guard de pagina e copiado estruturalmente de `plataforma/page.tsx`, e o header satelite vem de `processos/dashboard/page.tsx`. As duas divergencias reais estao registadas abaixo e sao **obrigatorias**, nao opcionais.

Output: dois ficheiros novos, zero ficheiros existentes alterados, zero dependencias novas.
</objective>

<execution_context>
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@web/AGENTS.md
@.planning/phases/LEXCV-122-relat-rio-de-utiliza-o-por-tenant/122-CONTEXT.md
@.planning/phases/LEXCV-122-relat-rio-de-utiliza-o-por-tenant/122-PATTERNS.md
@.planning/phases/LEXCV-122-relat-rio-de-utiliza-o-por-tenant/122-UI-SPEC.md
@web/src/app/(dashboard)/plataforma/page.tsx
@web/src/app/(dashboard)/plataforma/columns.tsx
@web/src/app/(dashboard)/processos/dashboard/page.tsx
</context>

<interfaces>
<!-- Contratos ja confirmados no planeamento por leitura direta do codigo. NAO redescobrir por exploracao. -->

Tipo consumido (`web/src/types/platform-admin.ts:10-19`, **zero alteracoes**):

```ts
export type TenantPlano = "STARTER" | "STANDARD" | "ENTERPRISE";

export type TenantAdminSummary = {
  id: string;
  nome: string;
  plano: TenantPlano;
  limiteUtilizadores: number | null;
  ativo: boolean;
  utilizadoresAtivos: number;
};
```

Hook consumido (`web/src/hooks/use-platform-admin.ts:21-30`, **zero alteracoes** — e o unico dos 4 hooks do ficheiro que esta fase toca):

```ts
export function useTenantsAdmin() {
  const enabled = typeof window !== "undefined";
  return useQuery({
    queryKey: TENANTS_LIST_KEY,           // ["platform","tenants","list"]
    queryFn: () => apiFetch<TenantAdminSummary[]>("/platform/tenants"),
    enabled,
    staleTime: 30_000,
  });
}
```

Constante a importar, nunca a redeclarar (`plataforma/columns.tsx:18`):

```ts
export const TENANT_RESERVADO = "LexCV";
```

Constante privada em `plataforma/columns.tsx:20-24` — **nao e exportada**, tem de ser redeclarada byte-identica no ficheiro novo:

```ts
const PLANO_BADGE_VARIANT: Record<TenantPlano, "gray" | "purple" | "amber"> = {
  STARTER: "gray",
  STANDARD: "purple",
  ENTERPRISE: "amber",
};
```

Props de `AccessDeniedState` (`web/src/components/shared/access-denied-state.tsx`, confirmado — todas opcionais, nada a alterar):

```ts
type AccessDeniedStateProps = { title?: string; description?: string; backHref?: string; backLabel?: string };
```

Assinatura da `DataTable` partilhada (`web/src/components/shared/data-table/data-table.tsx`, confirmada): recebe `columns`, `data`, `getRowId`; ja tem fallback interno "Sem resultados para os filtros aplicados." em `data-table.tsx:117` — nao reimplementar no ramo de desktop.

Guard de pagina a copiar estruturalmente (`plataforma/page.tsx:76-91`) — o early-return `!me.isFetched` **antes** do teste de papel nao e boilerplate opcional: e a correcao do bug WR-03 da code review da Phase 120 (sem ele, `useTenantsAdmin()` dispara `GET /platform/tenants` para qualquer utilizador autenticado durante a janela anterior ao primeiro resolve de `useMe()`, ou seja o guard falha **aberto**):

```tsx
const me = useMe();
if (!me.isFetched) { return null; }
if (!me.data?.roles?.includes("PLATAFORMA_ADMIN")) {
  return <AccessDeniedState description="..." backHref="/dashboard" />;
}
return <RelatorioUtilizacaoContent />;
```

Memo de pesquisa a reutilizar byte-identico (`plataforma/page.tsx:106-111`) — mesmo campo, mesma fonte de dados; **filtra so por `nome`**, e e por isso que um tenant suspenso nunca desaparece:

```tsx
const tenantsFiltrados = React.useMemo(() => {
  const termo = searchTerm.trim().toLowerCase();
  const lista = tenants.data ?? [];
  if (!termo) return lista;
  return lista.filter((t) => t.nome.toLowerCase().includes(termo));
}, [tenants.data, searchTerm]);
```

Header satelite a seguir (`processos/dashboard/page.tsx:29-39`), com as **2 divergencias obrigatorias** da UI-SPEC ja aplicadas (`aria-label="Voltar"`, que o precedente nao tem; e `font-semibold` no `<h1>`, nao `font-bold`):

```tsx
<div className="flex items-center gap-4">
  <Button asChild variant="ghost" className="h-9 w-9 p-0 text-slate-500 hover:text-slate-900 dark:hover:text-white">
    <Link href="/plataforma" aria-label="Voltar">
      <ArrowLeft className="h-4 w-4" />
    </Link>
  </Button>
  <div>
    <h1 className="text-3xl font-semibold text-slate-900 dark:text-white tracking-tight">Relatório de Utilização</h1>
    <p className="text-sm text-slate-500 dark:text-slate-400 mt-1">Utilizadores ativos, plano e limite contratado por tenant — base para faturação manual</p>
  </div>
</div>
```

Bloco `utilizadores` a copiar verbatim (`plataforma/columns.tsx:163-194`) — le `tenant.utilizadoresAtivos` diretamente, sem `reduce`/`filter`/`length`, e e exatamente isso que satisfaz o criterio 2:

```tsx
{
  id: "utilizadores",
  accessorFn: (tenant) => tenant.utilizadoresAtivos,
  meta: { label: "Utilizadores" },
  header: ({ column }) => <DataTableColumnHeader column={column} title="Utilizadores" />,
  cell: ({ row }) => {
    const tenant = row.original;
    const atingiuLimite =
      tenant.limiteUtilizadores !== null && tenant.utilizadoresAtivos >= tenant.limiteUtilizadores;

    return (
      <div className="flex flex-col">
        <span
          className={
            atingiuLimite
              ? "font-semibold text-red-600 dark:text-red-400"
              : "text-slate-800 dark:text-slate-200"
          }
        >
          {tenant.limiteUtilizadores !== null
            ? `${tenant.utilizadoresAtivos}/${tenant.limiteUtilizadores}`
            : `${tenant.utilizadoresAtivos} · sem limite`}
        </span>
        {atingiuLimite ? (
          <span className="text-[12px] uppercase tracking-wide text-red-600 dark:text-red-400">
            limite atingido
          </span>
        ) : null}
      </div>
    );
  },
},
```

Linha nova dos cards mobile (UI-SPEC, secao "Mobile cards") — substitui a fila de icones de acao que e removida; `pl-[52px]` = 40px do avatar + 12px do gap, exatamente o indent que `plataforma/page.tsx:237` ja usa:

```tsx
<div className="mt-2 pl-[52px]">
  <span className={atingiuLimite ? "text-sm font-semibold text-red-600 dark:text-red-400" : "text-sm text-slate-600 dark:text-slate-300"}>
    {tenant.limiteUtilizadores !== null
      ? `${tenant.utilizadoresAtivos}/${tenant.limiteUtilizadores} utilizadores`
      : `${tenant.utilizadoresAtivos} utilizadores · sem limite`}
  </span>
  {atingiuLimite ? (
    <span className="ml-2 text-[12px] uppercase tracking-wide text-red-600 dark:text-red-400">limite atingido</span>
  ) : null}
</div>
```

Comandos (CLAUDE.md, `web/package.json` confirmado): `cd web && pnpm lint`, `pnpm exec tsc --noEmit`, `pnpm build`. `pnpm` e autoritativo (`pnpm-lock.yaml`); ignorar o `package-lock.json` obsoleto. `web/.env.local` existe (confirmado), pelo que `pnpm build` corre — `next.config.ts` lanca sem `BACKEND_API_ORIGIN`. `tsconfig.json` ja tem `noEmit: true`, pelo que `pnpm exec tsc --noEmit` funciona sem flags extra. `next.config.ts` **nao** ativa `typedRoutes`, por isso um `href` de rota inexistente nao e erro de compilacao — a existencia da rota prova-se pela tabela de rotas do `pnpm build`, nao pelo type-check.

`ArrowLeft` esta confirmado presente em `lucide-react@0.543.0` instalado — **zero bumps de versao, zero instalacoes**.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Criar relatorio/columns.tsx — 4 column defs num array estatico, sem factory e sem acoes</name>

  <files>
    web/src/app/(dashboard)/plataforma/relatorio/columns.tsx (novo)
  </files>

  <read_first>
    - `web/src/app/(dashboard)/plataforma/columns.tsx` (ficheiro completo, 221 linhas) — a fonte de tudo o que esta task copia. Ler em particular: `TENANT_RESERVADO` (`:18`), `PLANO_BADGE_VARIANT` (`:20-24`), `TenantAcoesCell` (`:32-107`, o componente a **nao** trazer), a assinatura de factory `columns({ onEdit, onToggleAtivo })` (`:114-120`, a forma a **nao** copiar) e as 4 column defs `nome`/`plano`/`utilizadores`/`estado` (`:122-204`, as unicas a copiar).
    - `.planning/phases/LEXCV-122-relat-rio-de-utiliza-o-por-tenant/122-PATTERNS.md` — secao sobre `relatorio/columns.tsx` e a tabela "No Analog Found", que explica porque a forma de array estatico e a primeira do seu genero neste codebase.
    - `.planning/phases/LEXCV-122-relat-rio-de-utiliza-o-por-tenant/122-UI-SPEC.md` — secao "Report table — columns", que fixa a ordem das 4 colunas e a ausencia da 5a.
    - `web/src/types/platform-admin.ts` — os tipos `TenantAdminSummary` e `TenantPlano` que as column defs parametrizam.
  </read_first>

  <action>
    Criar o diretorio `web/src/app/(dashboard)/plataforma/relatorio/` (confirmado inexistente hoje — sem colisao de nomes) e dentro dele `columns.tsx`, com `"use client"` na primeira linha.

    Exportar **uma constante, nao uma funcao**: `export const relatorioColumns: ColumnDef<TenantAdminSummary>[]` inicializada com um array literal de 4 elementos. Este ficheiro e o primeiro `columns.tsx` deste codebase que nao e factory — os outros 6 (`clientes`, `processos`, `pareceres`, `financeiro`, `documentos`, `plataforma`) sao todos factory porque todos precisam de receber pelo menos um callback de linha ou um mapa de lookup. Um ecra so-de-leitura nao tem nenhum, logo a forma de factory seria uma indirecao vazia. Nao "corrigir" para factory por analogia com os outros ficheiros.

    Redeclarar `PLANO_BADGE_VARIANT` byte-identica a `plataforma/columns.tsx:20-24` (ver `<interfaces>`) — e uma const privada, nao exportada, no ficheiro de origem, por isso nao ha nada para importar. **Nao** redeclarar `TENANT_RESERVADO`: importar de `"../columns"` (resolve para `plataforma/columns.tsx`), para o literal do nome reservado ter uma unica definicao no codebase.

    Incluir exatamente 4 column defs, nesta ordem, todas com o `cell` copiado verbatim de `plataforma/columns.tsx`:

    1. `nome` — `accessorKey: "nome"`, `enableHiding: false`, `meta: { label: "Nome" }`, cell de `:128-147` (avatar de iniciais `rounded-md` + nome em `font-bold` + Badge `outline` com o rotulo "Plataforma" apenas na linha cujo `nome` iguala `TENANT_RESERVADO`). Texto simples em `span`, nunca `Link` — nao existe pagina de detalhe de tenant em lado nenhum desta app.
    2. `plano` — `accessorKey: "plano"`, cell de `:154-161` (Badge com `variant={PLANO_BADGE_VARIANT[plano]}` e `className="font-bold tracking-wide"`, valor de enum em maiusculas cru). O `font-bold` aqui e deliberado e ja foi disclosed e force-approved na UI-SPEC (secao Typography, "Disclosed third weight") — **nao** baixar para `font-semibold`: e o mesmo Badge, com o mesmo dado, a um clique de distancia em `/plataforma`, e divergir tornaria o mesmo badge visualmente diferente em dois ecras.
    3. `utilizadores` — `accessorFn: (tenant) => tenant.utilizadoresAtivos`, cell de `:163-193`, copiado verbatim (ver `<interfaces>`). Le o campo `utilizadoresAtivos` da resposta diretamente. **Proibida** qualquer chamada a `reduce`, `filter`, `length` ou contagem derivada no cliente: o criterio de sucesso 2 exige uma unica fonte de verdade (`UserRepository.countByTenantIdAndAtivoTrue`, Phase 117), e um recalculo aqui criaria a segunda implementacao que o criterio proibe.
    4. `estado` — `accessorKey: "ativo"`, cell de `:196-204`: um Badge com `variant={ativo ? "green" : "red"}` e o texto `{ativo ? "Ativo" : "Suspenso"}`. E este Badge que torna o criterio 3 concreto — o tenant suspenso mantem a linha e o estado fica identificado.

    Nao incluir a 5a column def de acoes nem o componente `TenantAcoesCell`. Consequencia: os imports `Button`, `Tooltip`, `TooltipContent`, `TooltipTrigger`, `Lock`, `Pencil`, `Unlock` e `cn` **nao** entram neste ficheiro — sem call site, seriam codigo morto e o ESLint marca-os.

    Lista de imports exata (nada mais, nada menos): `ColumnDef` de `@tanstack/react-table`; `Badge` de `@/components/ui/badge`; `DataTableColumnHeader` de `@/components/shared/data-table/data-table-column-header`; `tenantInitials` de `@/lib/tenant-initials`; os tipos `TenantAdminSummary` e `TenantPlano` de `@/types/platform-admin`; `TENANT_RESERVADO` de `"../columns"`.

    Escrever um doc-comment de topo, em prosa, que diga tres coisas: (a) que este ficheiro serve o relatorio so-de-leitura de `/plataforma/relatorio` e por isso exporta um array estatico em vez de uma factory, sendo o primeiro do genero neste codebase; (b) que as 4 celulas sao copias verbatim das do ficheiro de colunas da consola, de proposito, para o mesmo dado nunca renderizar diferente nos dois ecras; (c) que a coluna de utilizadores le o campo da resposta sem recalculo, por exigencia do criterio 2 da fase. Escrever esse comentario **em prosa, sem reproduzir literalmente tokens que os gates de origem do Plan 03 procuram** — as Phases 119/120/121 tiveram tres incidentes registados em STATE.md de comentarios auto-referenciais a disparar gates baseados em grep; o script do Plan 03 usa `stripComments`, mas a margem custa zero.

    Correr `cd web && pnpm lint` e `pnpm exec tsc --noEmit`, ambos limpos, antes de commitar.
  </action>

  <verify>
    <automated>cd web && test -f "src/app/(dashboard)/plataforma/relatorio/columns.tsx" && grep -q 'export const relatorioColumns' "src/app/(dashboard)/plataforma/relatorio/columns.tsx" && [ "$(grep -v '^[[:space:]]*//' "src/app/(dashboard)/plataforma/relatorio/columns.tsx" | grep -v '^[[:space:]]*\*' | grep -c 'export function columns')" = "0" ] && [ "$(grep -v '^[[:space:]]*//' "src/app/(dashboard)/plataforma/relatorio/columns.tsx" | grep -v '^[[:space:]]*\*' | grep -cE 'id: "(nome|plano|utilizadores|estado)"')" = "4" ] && [ "$(grep -v '^[[:space:]]*//' "src/app/(dashboard)/plataforma/relatorio/columns.tsx" | grep -v '^[[:space:]]*\*' | grep -c 'id: "acoes"')" = "0" ] && grep -q 'from "\.\./columns"' "src/app/(dashboard)/plataforma/relatorio/columns.tsx" && pnpm lint && pnpm exec tsc --noEmit</automated>
  </verify>

  <acceptance_criteria>
    - `web/src/app/(dashboard)/plataforma/relatorio/columns.tsx` existe e exporta `relatorioColumns` tipado `ColumnDef<TenantAdminSummary>[]`. Sobre linhas de codigo (excluindo `//` e continuacoes `*`), `grep -c 'export function columns'` devolve 0.
    - Sobre linhas de codigo: exatamente 4 ocorrencias de `id: "nome"`/`id: "plano"`/`id: "utilizadores"`/`id: "estado"` somadas, e 0 ocorrencias de `id: "acoes"`.
    - `grep -c 'TenantAcoesCell'` devolve 0. Sobre linhas de codigo, `grep -cE '\b(TooltipTrigger|TooltipContent|Pencil|Unlock)\b'` devolve 0 — zero imports mortos da celula de acoes.
    - O ficheiro importa `TENANT_RESERVADO` de `"../columns"` e **nao** contem a atribuicao literal do nome reservado: sobre linhas de codigo, `grep -c '= "LexCV"'` devolve 0.
    - O bloco da column def `utilizadores` contem `tenant.utilizadoresAtivos` e, dentro desse bloco, `grep -cE '\.(reduce|filter)\(|\.length'` devolve 0 — nenhuma agregacao no cliente.
    - O bloco da column def `estado` contem literalmente `"Suspenso"` e `variant={ativo ? "green" : "red"}`.
    - `PLANO_BADGE_VARIANT` esta declarada com as 3 chaves `STARTER`/`STANDARD`/`ENTERPRISE` mapeadas a `"gray"`/`"purple"`/`"amber"`.
    - `cd web && pnpm lint` e `cd web && pnpm exec tsc --noEmit` terminam ambos com exit code 0.
  </acceptance_criteria>

  <done>Existe um ficheiro de column defs so-de-leitura, com as 4 colunas do relatorio, forma de array estatico, zero callbacks, zero coluna de acoes e zero imports mortos — e o type-check e o lint passam.</done>
</task>

<task type="auto">
  <name>Task 2: Criar relatorio/page.tsx — rota com guard PLATAFORMA_ADMIN, pesquisa, cards mobile e DataTable</name>

  <files>
    web/src/app/(dashboard)/plataforma/relatorio/page.tsx (novo)
  </files>

  <read_first>
    - `web/src/app/(dashboard)/plataforma/page.tsx` (ficheiro completo) — a fonte estrutural. Ler em particular: o comentario de WR-03 mais o guard (`:60-91`), o memo `tenantsFiltrados` (`:106-111`), a forma do Card/pesquisa/loading/erro (`:161-190`) e o bloco de cards mobile (`:191-309`), incluindo a fila de icones de acao (`:237-304`) que **e removida** e o indent `pl-[52px]` que se mantem.
    - `web/src/app/(dashboard)/plataforma/relatorio/columns.tsx` — o ficheiro criado na Task 1, para confirmar o nome exportado e o facto de nao ser factory (nao precisa de `useMemo`).
    - `web/src/app/(dashboard)/processos/dashboard/page.tsx` linhas 29-39 — a forma do header satelite (seta de voltar + h1 + subtitulo, sem breadcrumb).
    - `.planning/phases/LEXCV-122-relat-rio-de-utiliza-o-por-tenant/122-UI-SPEC.md` — secoes "Copywriting Contract" (as strings sao contrato, nao sugestao), a secao sobre `relatorio/page.tsx` e "Mobile cards — one deliberate divergence".
    - `web/src/components/shared/data-table/data-table.tsx` — confirmar a assinatura de props e o fallback interno "Sem resultados para os filtros aplicados." (`:117`), para nao o duplicar no ramo de desktop.
    - `web/AGENTS.md` — esta e a versao 16.2.6 do Next; convencoes de routing confirmam-se em `web/node_modules/next/dist/docs/`, nunca por memoria.
  </read_first>

  <action>
    Criar `web/src/app/(dashboard)/plataforma/relatorio/page.tsx` com `"use client"` na primeira linha. Estruturar em dois componentes, como `plataforma/page.tsx` faz: `export default function RelatorioUtilizacaoPage()` (so o guard) e `function RelatorioUtilizacaoContent()` (o conteudo). A separacao nao e estetica: e o que garante que `useTenantsAdmin()` nunca e chamado antes do guard passar.

    **Guard, ordem obrigatoria.** Chamar `useMe()`, depois um early-return que devolve `null` quando `!me.isFetched`, e **so depois** o teste de papel que devolve `AccessDeniedState` quando `me.data?.roles?.includes("PLATAFORMA_ADMIN")` for falso, terminando com o render de `RelatorioUtilizacaoContent`. Props exatas do `AccessDeniedState`: `description="Não tem permissão para aceder ao relatório de utilização de tenants."` e `backHref="/dashboard"`. Inverter esta ordem, ou juntar as duas condicoes numa so (o padrao `isFetched && !canX` usado nas outras 25 paginas do dashboard), reintroduz o bug WR-03 da code review da Phase 120: o conteudo renderizaria para qualquer utilizador autenticado durante a janela anterior ao primeiro resolve de `useMe()`, disparando `GET /platform/tenants` antes de o papel do chamador ser conhecido. Escrever um comentario curto acima do guard a dizer que a ordem e deliberada e porque, referindo WR-03 por nome.

    **Header.** Copiar a forma de `processos/dashboard/page.tsx:29-39` com as 2 divergencias da UI-SPEC ja aplicadas (ver `<interfaces>`): `aria-label="Voltar"` no `Link` para `/plataforma` (o precedente nao tem `aria-label`; acrescentar e uma melhoria de acessibilidade que este codebase ja fez antes, ex. Phase 115.1) e `font-semibold` no `h1`, **nunca** `font-bold`. O `font-semibold` no `h1` e a excecao de Dimension 4 explicitamente disclosed e force-approved na UI-SPEC — nao "corrigir" para `font-bold` por consistencia com os outros titulos da app. Copy exata: titulo "Relatório de Utilização"; subtitulo (`text-sm text-slate-500 dark:text-slate-400 mt-1`) "Utilizadores ativos, plano e limite contratado por tenant — base para faturação manual", sem ponto final. **Sem breadcrumb** — este ecra segue a forma satelite de `/processos/dashboard`, nao a forma com breadcrumb de `/plataforma`.

    **Estado e dados.** Um unico `React.useState("")` para `searchTerm`, uma chamada a `useTenantsAdmin()`, e o memo `tenantsFiltrados` copiado byte-identico de `plataforma/page.tsx:106-111` (ver `<interfaces>`). O memo filtra **so** por `nome`; nao acrescentar nenhuma condicao sobre `ativo`, `plano` ou limite — o criterio de sucesso 3 exige explicitamente que tenants suspensos continuem na lista com o estado identificado, em vez de desaparecerem.

    **Card.** Um `Card` com `className="border-slate-200 dark:border-slate-800 bg-white/50 dark:bg-slate-900/50 backdrop-blur-sm rounded-xl"` (classes identicas as do Card de `/plataforma`), renderizado **incondicionalmente** — nao existe painel de criacao, logo nao existe o ternario `isFormOpen` da origem. `CardHeader` sem override de className (nao ha botao para colocar em frente ao titulo neste Card, ao contrario de `/plataforma`), com `CardTitle className="text-xl font-semibold"` a ler "Utilização por Tenant" e `CardDescription` a ler "Estado atual de todos os tenants registados na plataforma." `CardContent className="space-y-4"` com um `Input` de `placeholder="Pesquisar tenant por nome..."` e `className="bg-slate-50 dark:bg-slate-950"` (placeholder reutilizado caracter a caracter de `/plataforma`), seguido do ternario loading/erro/conteudo copiado verbatim de `:183-189`: "A carregar..." em `p-6 text-sm text-slate-500`; erro em `p-6 text-sm text-red-600` com o mesmo fallback "Erro ao carregar tenants."

    **Cards mobile** (`md:hidden divide-y divide-slate-200 dark:divide-slate-800`). Copiar `plataforma/page.tsx:192-236` verbatim: verificacao de lista vazia com a string "Sem resultados para os filtros aplicados.", avatar de iniciais, nome, Badge "Plataforma" quando o nome iguala `TENANT_RESERVADO`, Badge de plano com o ternario de 3 ramos e `text-[10px]`, Badge de estado verde/vermelho. Remover a fila de icones de acao inteira (`:237-304`) e, no seu lugar, colocar o bloco de utilizadores da UI-SPEC (ver `<interfaces>`), calculando `atingiuLimite` localmente do mesmo modo que a column def o faz. Esta substituicao e obrigatoria, nao cosmetica: o card mobile de `/plataforma` **nunca mostra a contagem de utilizadores** (depende da coluna de desktop), e copiar esse bloco tal e qual violaria silenciosamente o criterio de sucesso 1 em qualquer viewport abaixo de `md`. Nao trazer `Tooltip`, `Lock`, `Unlock`, `Pencil` nem `cn` — sem acoes, seriam codigo morto.

    **DataTable de desktop** (`hidden md:block`). Passar `columns={relatorioColumns}`, `data={tenantsFiltrados}` e `getRowId={(t) => t.id}`. Passar `relatorioColumns` importado diretamente, **sem** `React.useMemo` e **sem** `React.useCallback` — nao e factory e nao ha callbacks, logo memoizar seria ruido.

    **Proibicoes explicitas.** Nao importar nem chamar `useCreateTenant`, `useUpdateTenant` ou `useSetTenantAtivo`; nao usar `Dialog`, `AlertDialog`, `toast` ou `mutateAsync`; nao adicionar exportacao CSV, nem linha de KPIs agregados, nem `window.location.reload`. Os tres primeiros grupos sao mutacoes (este ecra e 100% de leitura); a exportacao CSV e a linha de KPIs estao registadas como Deferred Ideas em `122-CONTEXT.md` e explicitamente excluidas pela UI-SPEC. Nao tocar em `dashboard-shell.tsx`: a decisao registada (precedente da Phase 89, v2.10) e que este ecra secundario e alcancado apenas por link em contexto, nunca por um segundo item permanente de navegacao lateral.

    Lista de imports exata: `React`; `Link` de `next/link`; `ArrowLeft` de `lucide-react`; `AccessDeniedState`; `Badge`; `Button`; `Card`/`CardContent`/`CardDescription`/`CardHeader`/`CardTitle`; `Input`; `DataTable`; `useMe`; `useTenantsAdmin`; `tenantInitials`; `TENANT_RESERVADO` de `"../columns"`; `relatorioColumns` de `"./columns"`.

    Este ficheiro e um Client Component de rota simples, exatamente como as outras 25 paginas do grupo `(dashboard)` — nao introduzir `generateMetadata`, `params`/`searchParams` assincronos, nem qualquer API de servidor.

    Correr `cd web && pnpm lint`, `pnpm exec tsc --noEmit` e `pnpm build` — os tres limpos. O `pnpm build` e o que prova que o App Router resolve mesmo a rota nova: `/plataforma/relatorio` tem de aparecer na tabela de rotas do output.
  </action>

  <verify>
    <automated>cd web && test -f "src/app/(dashboard)/plataforma/relatorio/page.tsx" && grep -q 'useTenantsAdmin' "src/app/(dashboard)/plataforma/relatorio/page.tsx" && grep -q 'columns={relatorioColumns}' "src/app/(dashboard)/plataforma/relatorio/page.tsx" && [ "$(grep -v '^[[:space:]]*//' "src/app/(dashboard)/plataforma/relatorio/page.tsx" | grep -v '^[[:space:]]*\*' | grep -cE 'useCreateTenant|useUpdateTenant|useSetTenantAtivo|mutateAsync|AlertDialog')" = "0" ] && pnpm lint && pnpm exec tsc --noEmit && pnpm build > ../scratchpad/122-01-build.log 2>&1 && grep -q 'plataforma/relatorio' ../scratchpad/122-01-build.log</automated>
  </verify>

  <acceptance_criteria>
    - `web/src/app/(dashboard)/plataforma/relatorio/page.tsx` existe, chama `useTenantsAdmin()` e passa `columns={relatorioColumns}` a `DataTable` sem envolver em `useMemo`.
    - No corpo do componente exportado por omissao, o indice do early-return de `!me.isFetched` **precede** o indice da primeira ocorrencia de `AccessDeniedState`, e o ficheiro contem `roles?.includes("PLATAFORMA_ADMIN")` — mesma assercao de ordem que `web/scripts/verify-consola-tenants.mjs` ja aplica a `/plataforma`.
    - A tag de abertura do `h1` contem `font-semibold` e **nao** contem `font-bold`.
    - O `Link` de voltar aponta para `/plataforma` e tem `aria-label="Voltar"`.
    - O bloco `md:hidden` contem `utilizadoresAtivos` — a contagem esta visivel em mobile, nao apenas na DataTable de desktop.
    - O callback do memo `tenantsFiltrados` referencia apenas `nome`; `grep` do corpo do memo por `ativo` devolve 0 ocorrencias.
    - Sobre linhas de codigo (excluindo comentarios), `grep -cE 'useCreateTenant|useUpdateTenant|useSetTenantAtivo|mutateAsync|AlertDialog'` devolve 0, e `grep -icE 'csv|window\.location\.reload'` devolve 0.
    - As 6 strings de copy da UI-SPEC aparecem byte-identicas: "Relatório de Utilização", "Utilizadores ativos, plano e limite contratado por tenant — base para faturação manual", "Utilização por Tenant", "Estado atual de todos os tenants registados na plataforma.", "Pesquisar tenant por nome...", "Não tem permissão para aceder ao relatório de utilização de tenants."
    - `cd web && pnpm lint`, `pnpm exec tsc --noEmit` e `pnpm build` terminam todos com exit code 0, e o output do `pnpm build` lista a rota `/plataforma/relatorio`.
  </acceptance_criteria>

  <done>A rota `/plataforma/relatorio` existe, compila, aparece na tabela de rotas do build, esta fechada a quem nao e `PLATAFORMA_ADMIN` (incluindo durante o loading de `useMe()`), mostra as 4 colunas em desktop e a contagem de utilizadores tambem em mobile, e nao tem uma unica mutacao.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser -> rewrite do Next -> `GET /api/v1/platform/tenants` | Fronteira autoritativa: o gate de classe `hasRole('PLATAFORMA_ADMIN')` de `PlatformAdminController` (Phase 119, inalterado). Esta fase nao acrescenta nenhum endpoint nem altera nenhum gate |
| Componente de rota `/plataforma/relatorio` -> `useTenantsAdmin()` | Fronteira de UX: o guard de pagina decide se o hook chega a ser chamado. Dados cross-tenant (todos os tenants da plataforma) atravessam aqui |
| Input de pesquisa -> filtro no cliente | Entrada do utilizador; nunca sai do browser, nunca entra numa query nem num URL |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-122-01 | Information Disclosure | Guard de `relatorio/page.tsx` durante a janela de loading de `useMe()` | mitigate | Early-return `null` para `!me.isFetched` **antes** do teste de papel, e nao a condicao unica `isFetched && !canX` usada nas outras 25 paginas. Sem isto o guard falha aberto e `GET /platform/tenants` dispara para qualquer utilizador autenticado (bug WR-03, code review da Phase 120). Criterio de aceitacao verifica a ordem por indice |
| T-122-02 | Information Disclosure | Lista cross-tenant servida a um ecra de dashboard | mitigate | Reutiliza exclusivamente `GET /api/v1/platform/tenants`, ja gated a `PLATAFORMA_ADMIN` no backend; nenhum endpoint tenant-scoped e usado para servir dados cross-tenant. O `TenantAdminSummaryResponse` omite deliberadamente logotipo Base64, NIF, email, telefone e data de criacao — menos superficie de exposicao do que a entidade crua |
| T-122-03 | Information Disclosure | Campos sensiveis a mais na projecao | accept | Zero alteracoes ao DTO do backend. Nada de novo e transportado por esta fase |
| T-122-04 | Tampering (XSS) | `searchTerm` -> `tenantsFiltrados` | accept | O valor so e comparado com comparacao de substring em minusculas e renderizado como valor do `Input`; nunca e injetado em HTML, URL, header ou query. Zero `dangerouslySetInnerHTML` nos dois ficheiros novos (verificavel por grep) e o React escapa nos de texto por omissao |
| T-122-05 | Elevation of Privilege | Ecra so-de-leitura a ganhar caminhos de escrita por copia inadvertida | mitigate | Proibicao explicita e verificada por grep de `useCreateTenant`/`useUpdateTenant`/`useSetTenantAtivo`/`mutateAsync`/`AlertDialog`; a coluna de acoes e o `TenantAcoesCell` sao deliberadamente deixados de fora, nao desativados |
| T-122-06 | Repudiation | Contagem de utilizadores divergente da usada na faturacao | mitigate | A coluna le `tenant.utilizadoresAtivos` da resposta sem agregacao no cliente (grep proibe `reduce`/`filter`/`length` no bloco), mantendo `UserRepository.countByTenantIdAndAtivoTrue` como unica fonte de verdade — criterio de sucesso 2 |
| T-122-SC | Tampering | Instalacoes npm/pnpm | n/a | Zero dependencias novas. `ArrowLeft` ja existe no `lucide-react@0.543.0` instalado e todos os primitivos shadcn necessarios (`Card`, `Button`, `Input`, `Badge`, `DataTable`) estao instalados desde as Phases 101/104/120. Nenhuma task deste plano corre um gestor de pacotes, por isso o Package Legitimacy Gate nao se aplica |
</threat_model>

<verification>
1. `cd web && pnpm lint` — exit 0, zero erros e zero avisos novos.
2. `cd web && pnpm exec tsc --noEmit` — exit 0.
3. `cd web && pnpm build` — exit 0, e a tabela de rotas do output inclui `/plataforma/relatorio`.
4. `git status` confirma que apenas os 2 ficheiros novos foram criados: nenhum ficheiro existente aparece como modificado (em particular `plataforma/page.tsx`, `plataforma/columns.tsx`, `dashboard-shell.tsx`, `use-platform-admin.ts`, `types/platform-admin.ts` e qualquer ficheiro de `backend/` ficam intactos).
5. `grep -rn 'plataforma/relatorio' web/src/components/shared/dashboard-shell.tsx` devolve 0 linhas — nenhum item permanente de navegacao lateral foi acrescentado.
</verification>

<success_criteria>
- Existem exatamente 2 ficheiros novos, ambos em `web/src/app/(dashboard)/plataforma/relatorio/`, e zero ficheiros existentes alterados.
- `relatorioColumns` e um array estatico de 4 column defs; nao existe coluna de acoes nem nenhum import morto da celula de acoes.
- A rota aparece na tabela de rotas do `pnpm build`.
- O guard falha fechado durante o loading de `useMe()` (ordem verificada por indice) e devolve `AccessDeniedState` com a copy exacta da UI-SPEC para quem nao e `PLATAFORMA_ADMIN`.
- A contagem de utilizadores ativos aparece em desktop (coluna) e em mobile (linha indentada `pl-[52px]`), sempre lida do campo da resposta sem recalculo.
- Nenhuma mutacao, nenhuma exportacao CSV, nenhuma linha de KPIs, nenhum item de navegacao lateral novo.
- `pnpm lint` e `pnpm exec tsc --noEmit` limpos.

**Nota de rastreabilidade (nao ignorar):** este plano lista `UTIL-01` no frontmatter para rastreabilidade, mas **NAO** deve correr `requirements mark-complete` para `UTIL-01`. `REQUIREMENTS.md` exige um relatorio interno alcancavel; o ecra so fica alcancavel quando o Plan 03 acrescenta o link de entrada em `/plataforma`. Fechar `UTIL-01` aqui repetiria exatamente a regressao de fecho prematuro que as Phases 120-02/120-03/120-04 documentaram e que o commit `cd45fcf9` teve de corrigir.
</success_criteria>

<output>
Criar `.planning/phases/LEXCV-122-relat-rio-de-utiliza-o-por-tenant/122-01-SUMMARY.md` no final, registando: a lista exata de ficheiros criados, o output da tabela de rotas do `pnpm build` na linha de `/plataforma/relatorio`, confirmacao de que a forma de array estatico foi usada em vez de factory (e porque), e qualquer desvio da UI-SPEC com justificacao (o esperado e zero desvios).
</output>
