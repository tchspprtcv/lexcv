---
phase: 122-relat-rio-de-utiliza-o-por-tenant
plan: 04
type: execute
wave: 3
depends_on: [122-03]
files_modified:
  - .planning/phases/LEXCV-122-relat-rio-de-utiliza-o-por-tenant/122-HUMAN-UAT.md
autonomous: false
requirements: [UTIL-01]

must_haves:
  truths:
    - "GET /api/v1/platform/tenants respondeu 200 a uma sessao real de PLATAFORMA_ADMIN e 403 a uma sessao real de ADMIN de escritorio, provado por HTTP e nao por leitura de codigo"
    - "A rota /plataforma/relatorio renderiza no browser e mostra os 4 campos por tenant"
    - "O numero de utilizadores ativos no relatorio e igual ao mostrado em /plataforma para o mesmo tenant"
    - "Um tenant suspenso ao vivo continua a ter linha no relatorio, com Badge Suspenso"
    - "Abaixo de 768px a contagem de utilizadores continua visivel no card mobile"
    - "A navegacao lateral do PLATAFORMA_ADMIN continua a ter apenas uma entrada de plataforma"
  artifacts:
    - path: ".planning/phases/LEXCV-122-relat-rio-de-utiliza-o-por-tenant/122-HUMAN-UAT.md"
      provides: "Registo datado dos vereditos por cenario, com valores observados e nao apenas PASS/FAIL"
      min_lines: 40
  key_links:
    - from: "sessao real de PLATAFORMA_ADMIN"
      to: "GET /api/v1/platform/tenants"
      via: "cookie httpOnly obtido por POST /api/v1/auth/login, reutilizado em curl"
      pattern: "platform/tenants"
    - from: "browser em /plataforma/relatorio"
      to: "utilizadoresAtivos por tenant"
      via: "render da coluna Utilizadores e da linha equivalente no card mobile"
      pattern: "utilizadores"
---

<objective>
Fechar a unica classe de afirmacao que nenhum gate desta fase consegue provar: **renderizacao e alcancabilidade reais**.

Purpose: O criterio de sucesso 1 da Phase 122 diz que o ecra "mostra na interface" 4 campos por tenant. Um gate de origem (Plan 03) prova que o codigo diz isso; nao prova que o Next resolve a rota, que o clique navega, que os Badges renderizam, nem que os numeros correspondem a utilizadores reais na base de dados. O criterio 1 tambem exige "acessivel so a `PLATAFORMA_ADMIN`" — uma afirmacao de autorizacao que so uma resposta HTTP real de uma sessao real prova.

Este plano e **deliberadamente mais leve** do que os equivalentes das Phases 120 e 121, e isso e uma decisao consciente, nao um descuido. Esta fase nao acrescenta nenhum endpoint, nenhuma fronteira de autorizacao nova, nenhuma mutacao e nenhuma alteracao a codigo de producao no backend: reutiliza o mesmo `GET /api/v1/platform/tenants` que a UAT ao vivo da Phase 120 (`120-HUMAN-UAT.md`) ja exercitou, e o mesmo padrao de guard de pagina que ja foi verificado ao vivo nessa fase. Por isso a UAT desta fase cobre 6 cenarios focados, em vez dos ~11 pontos das fases anteriores — e os que podem ser automatizados **sao** automatizados antes de chegarem a uma pessoa.

Output: `122-HUMAN-UAT.md` com veredito e valores observados por cenario.
</objective>

<execution_context>
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/LEXCV-122-relat-rio-de-utiliza-o-por-tenant/122-CONTEXT.md
@.planning/phases/LEXCV-122-relat-rio-de-utiliza-o-por-tenant/122-UI-SPEC.md
@.planning/phases/LEXCV-122-relat-rio-de-utiliza-o-por-tenant/122-01-SUMMARY.md
@.planning/phases/LEXCV-122-relat-rio-de-utiliza-o-por-tenant/122-02-SUMMARY.md
@.planning/phases/LEXCV-122-relat-rio-de-utiliza-o-por-tenant/122-03-SUMMARY.md
@.planning/phases/LEXCV-120-frontend-consola-de-administra-o-de-tenants/120-HUMAN-UAT.md
</context>

<interfaces>
<!-- Contratos e condicoes de ambiente ja confirmados no planeamento. -->

Contas semeadas (`backend/.../seed/DatabaseSeeder.java`, Phase 119): `plataforma@lexcv.cv` com papel `PLATAFORMA_ADMIN` (semeada apenas quando `app.seed.enabled` esta ativo) e `admin@lexcv.cv` com papel `ADMIN` de escritorio. Password documentada em `CLAUDE.md`: `Pa$$w0rd`.

Contrato de login (`backend/.../dtos/LoginRequest.java`, confirmado — dois campos, nada mais): `email` e `password`. `POST /api/v1/auth/login` devolve os cookies httpOnly `access_token` e `refresh_token`; nao existem bearer tokens em JS. Um `curl` com jar de cookies (`-c` para gravar, `-b` para reenviar) reproduz exatamente o que o browser faz.

Portas: backend em `8080` (`mvn spring-boot:run` a partir de `backend/`), frontend em `3000` (`pnpm dev` a partir de `web/`). O browser fala sempre com `3000` — `next.config.ts` faz rewrite de `/api/v1/:path*` para `BACKEND_API_ORIGIN`, por isso o `curl` pode ir diretamente a `8080` ou passar por `3000`; ir a `8080` isola o backend e e a forma preferida para os cenarios de autorizacao.

Prerequisitos de ambiente: `backend/.env` (todas as variaveis `DB_*` e `MINIO_*` sao obrigatorias, sem defaults), PostgreSQL acessivel, e o container MinIO a correr. `web/.env.local` existe. O bloqueador historico de `MINIO_ENDPOINT` esta resolvido desde 2026-07-14 (Phase 97 AUD-04) — se o contexto Spring nao arrancar, verificar primeiro `backend/.env` antes de assumir defeito de codigo.

**Dois avisos de ambiente conhecidos, para nao serem reportados como defeitos desta fase:**
1. `backend/migrations/120b-backfill-tenant-plano.sql` **nao** correu nesta base de dados de desenvolvimento (registado em STATE.md como pendente). Qualquer tenant legado com `plano = NULL` renderiza um Badge de plano incorreto — cosmetico, pre-existente, e ja impedido para tenants novos pelo `@Builder.Default` de `Tenant.plano`. Se isso aparecer, registar como observacao de ambiente, nao como falha do cenario.
2. Esta base de dados de desenvolvimento e de longa duracao e ja teve, na Phase 115.1, um caso de password semeada desatualizada para contas de teste. Se o login de `plataforma@lexcv.cv` ou `admin@lexcv.cv` falhar com `401`, isso e ambiente (conta antiga na base de dados), nao codigo — resolver criando ou reiniciando a conta, e registar o desvio.

Aparencia esperada da resposta de `GET /api/v1/platform/tenants` (6 campos por tenant, ordenados por nome case-insensitive): `id`, `nome`, `plano`, `limiteUtilizadores` (`null` significa sem limite), `ativo`, `utilizadoresAtivos`.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Pre-flight automatizado — provar por HTTP real os 2 cenarios de autorizacao e o de tenant suspenso</name>

  <files>
    (nenhum ficheiro de codigo — esta task produz apenas evidencia para o registo de UAT)
  </files>

  <read_first>
    - `.planning/phases/LEXCV-122-relat-rio-de-utiliza-o-por-tenant/122-03-SUMMARY.md` — confirmar que os 15 `PASS` do gate estao registados e que `UTIL-01` foi fechado, antes de gastar uma sessao ao vivo.
    - `.planning/phases/LEXCV-120-frontend-consola-de-administra-o-de-tenants/120-HUMAN-UAT.md` — o formato de registo por cenario ja usado nesta milestone (veredito + valores observados, nao apenas `PASS`).
    - `CLAUDE.md` — comandos de arranque de backend e frontend, e as credenciais documentadas.
    - `backend/src/main/java/com/lexcv/seed/DatabaseSeeder.java` — confirmar que contas existem e sob que condicoes de `app.seed.enabled`, antes de assumir que um `401` e defeito.
  </read_first>

  <action>
    Automatizar tudo o que nao exige olhos humanos, para o checkpoint da Task 2 receber apenas afirmacoes genuinamente visuais ou interativas.

    Arrancar a pilha: `cd backend && mvn spring-boot:run` num processo, e `cd web && pnpm dev` noutro. Confirmar que o contexto Spring arranca por inteiro (procurar a linha de porta 8080 no log) e que o Next serve em 3000. Se o contexto Spring falhar, diagnosticar `backend/.env` primeiro.

    Cenario A1 (autorizacao positiva). `POST http://localhost:8080/api/v1/auth/login` com corpo JSON contendo `email` igual a `plataforma@lexcv.cv` e `password` igual a `Pa$$w0rd`, gravando os cookies num jar. Depois `GET http://localhost:8080/api/v1/platform/tenants` reenviando esse jar. Registar: codigo de estado (esperado `200`), numero de tenants devolvidos, e o valor dos 6 campos de **cada** tenant devolvido, na ordem em que vierem. Este e o snapshot factual contra o qual o browser vai ser comparado na Task 2.

    Cenario A2 (autorizacao negativa — a prova ASVS L1 desta fase). Novo login, agora com `admin@lexcv.cv` e a mesma password, num jar de cookies **separado** (nunca reutilizar o jar do A1, ou o resultado nao prova nada). `GET http://localhost:8080/api/v1/platform/tenants` com esse jar. Registar o codigo de estado, que tem de ser `403`. Um `500` seria uma regressao do `GlobalExceptionHandler` da Phase 119 (que mapeia `AccessDeniedException` para `403` globalmente) e um `200` seria uma falha critica de isolamento — em qualquer desses casos, parar e escalar antes de continuar para o checkpoint.

    Cenario A3 (tenant suspenso presente na resposta). Identificar, na resposta do A1, um tenant que **nao** seja o reservado `LexCV` (o reservado nao pode ser suspenso por desenho, guard da Phase 120). Se nao existir nenhum outro tenant, criar um por `POST http://localhost:8080/api/v1/platform/tenants` com o jar do A1, e registar que foi criado para este teste. Suspender esse tenant com `PATCH http://localhost:8080/api/v1/platform/tenants/{id}/ativo` e corpo `{"ativo":false}`. Repetir o `GET` do A1 e registar: que o id do tenant suspenso **continua** presente na lista, e que o seu campo `ativo` vem `false`. **Nao reativar ainda** — a Task 2 precisa do tenant suspenso para o cenario visual H3; a reativacao acontece no fim da Task 2.

    Registar os tres resultados (comandos exatos, codigos de estado e corpos relevantes) em texto, para serem colados no `122-HUMAN-UAT.md`. Nao inventar nem arredondar valores: os numeros de `utilizadoresAtivos` do A1 sao a base da comparacao cruzada do cenario H2.

    Proibicoes: nao alterar nenhum ficheiro de codigo nesta task; nao correr `SEED_ENABLED=true` contra uma base de dados de producao; nao apagar nenhum tenant.
  </action>

  <verify>
    <automated>curl -s -o /dev/null -w '%{http_code}' -X POST http://localhost:8080/api/v1/auth/login -H 'Content-Type: application/json' -c ../scratchpad/122-plat.jar -d '{"email":"plataforma@lexcv.cv","password":"Pa$$w0rd"}' | grep -q 200 && curl -s -o ../scratchpad/122-tenants.json -w '%{http_code}' -b ../scratchpad/122-plat.jar http://localhost:8080/api/v1/platform/tenants | grep -q 200 && grep -q '"utilizadoresAtivos"' ../scratchpad/122-tenants.json && grep -q '"ativo":false' ../scratchpad/122-tenants.json && curl -s -o /dev/null -w '%{http_code}' -X POST http://localhost:8080/api/v1/auth/login -H 'Content-Type: application/json' -c ../scratchpad/122-admin.jar -d '{"email":"admin@lexcv.cv","password":"Pa$$w0rd"}' | grep -q 200 && curl -s -o /dev/null -w '%{http_code}' -b ../scratchpad/122-admin.jar http://localhost:8080/api/v1/platform/tenants | grep -q 403</automated>
  </verify>

  <acceptance_criteria>
    - O login de `plataforma@lexcv.cv` devolveu `200` e gravou cookies; `GET /api/v1/platform/tenants` com esses cookies devolveu `200`.
    - O corpo da resposta contem, para cada tenant, os 6 campos `id`, `nome`, `plano`, `limiteUtilizadores`, `ativo`, `utilizadoresAtivos`, e os valores estao registados por escrito (nao resumidos).
    - O login de `admin@lexcv.cv` devolveu `200` (a conta autentica-se normalmente) e `GET /api/v1/platform/tenants` com **esse** jar de cookies devolveu exatamente `403` — nem `200` nem `500`.
    - Os dois jars de cookies sao ficheiros distintos e nenhum pedido reutilizou o jar errado.
    - Existe pelo menos um tenant com `"ativo":false` no corpo da resposta apos o `PATCH`, e o seu `id` e o mesmo que foi suspenso.
    - O tenant suspenso continua a aparecer na lista depois da suspensao (comparar a contagem de tenants antes e depois do `PATCH`: tem de ser igual).
    - Nenhum ficheiro de codigo foi alterado (`git diff --name-only` vazio para `web/` e `backend/`).
  </acceptance_criteria>

  <done>Os dois cenarios de autorizacao e o cenario de tenant suspenso estao provados por HTTP real, com valores registados, e o ambiente esta preparado (tenant suspenso ainda por reativar) para os cenarios visuais da Task 2.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verificacao humana ao vivo — 6 cenarios visuais e de navegacao</name>

  <files>
    .planning/phases/LEXCV-122-relat-rio-de-utiliza-o-por-tenant/122-HUMAN-UAT.md (novo)
  </files>

  <read_first>
    - `.planning/phases/LEXCV-122-relat-rio-de-utiliza-o-por-tenant/122-UI-SPEC.md` — as secoes "Copywriting Contract" e "Screen Layout & Component Reuse Map" fixam exatamente o que tem de aparecer no ecra; a secao Typography explica porque o titulo desta pagina e deliberadamente mais leve do que os outros da app.
    - O registo textual produzido pela Task 1 — os valores de `utilizadoresAtivos` por tenant, necessarios para o cenario H2.
    - `.planning/phases/LEXCV-120-frontend-consola-de-administra-o-de-tenants/120-HUMAN-UAT.md` — formato de registo a seguir.
  </read_first>

  <action>
    Conduzir a sessao de verificacao humana e produzir o registo. O executor nao "faz" os cenarios sozinho: apresenta-os ao utilizador pela ordem H1 a H6 (ver `<how-to-verify>`, que e o guiao literal), recolhe o veredito e os valores observados de cada um, e transcreve-os. A pilha ja esta a correr desde a Task 1 e existe um tenant suspenso a espera do cenario H3.

    Antes de apresentar os cenarios, colar no inicio do registo o resultado textual dos 3 cenarios automatizados da Task 1 (A1: `200` e os 6 campos por tenant; A2: `403` para o `ADMIN` de escritorio; A3: tenant suspenso presente com `ativo` a `false`) — sao evidencia ja produzida e o utilizador nao os deve repetir manualmente.

    Escrever `.planning/phases/LEXCV-122-relat-rio-de-utiliza-o-por-tenant/122-HUMAN-UAT.md` com uma seccao por cenario (A1-A3 e H1-H6), cada uma com tres partes: veredito explicito, valores observados (numeros, textos de badge, URLs, texto de copy — nunca so `PASS`), e desvios. Datar o ficheiro no topo. Seguir o formato de `120-HUMAN-UAT.md`.

    Registar as duas observacoes esperadas como conformes e nao como defeitos: o peso de letra mais leve do titulo desta pagina (excecao documentada e aprovada na UI-SPEC) e um eventual badge de plano errado em tenants legados (migracao `120b-backfill-tenant-plano.sql` pendente nesta base de dados, item de ambiente pre-existente registado em STATE.md).

    No fim, reativar pela consola o tenant que a Task 1 suspendeu e confirmar por escrito que o badge dele voltou a "Ativo" nos dois ecras. Se a Task 1 criou um tenant so para o teste, identifica-lo explicitamente no registo para o utilizador decidir se o mantem.

    Se algum cenario falhar, registar o valor real observado (nao uma descricao vaga) e devolver isso ao orquestrador como gap — nao tentar corrigir codigo dentro deste plano, e nao voltar a correr `requirements mark-complete` (o `UTIL-01` foi fechado pelo Plan 03).

    Proibicoes: nao alterar nenhum ficheiro sob `web/` ou `backend/` nesta task; nao apagar tenants; nao deixar nenhum tenant suspenso no fim.
  </action>

  <what-built>
    - Uma rota nova `/plataforma/relatorio` (Plan 01), 100% de leitura, com guard de papel `PLATAFORMA_ADMIN` que falha fechado durante o carregamento de `useMe()`, header satelite com seta de voltar, pesquisa por nome, 4 colunas em desktop (Nome, Plano, Utilizadores, Estado) e cards empilhados em mobile com a contagem de utilizadores incluida.
    - Um botao `outline` "Ver Relatório" no `CardHeader` da consola `/plataforma` (Plan 03), a esquerda do "Criar Tenant" inalterado.
    - Uma guarda de regressao no backend a provar que `listTenants()` devolve tenants suspensos (Plan 02) — zero codigo de producao alterado.
    - Um gate de origem com 15 assercoes (`pnpm verify:relatorio-utilizacao`, Plan 03), que se viu falhar com precisao em 3 familias diferentes.
    - Ja provado automaticamente pela Task 1 deste plano, **nao repetir manualmente**: `200` para `PLATAFORMA_ADMIN` e `403` para `ADMIN` de escritorio no endpoint; e presenca do tenant suspenso na resposta com `ativo` a `false`.
  </what-built>

  <how-to-verify>
    A pilha ja esta a correr desde a Task 1 (backend em `8080`, frontend em `3000`) e existe um tenant suspenso por reativar. Percorrer os 6 cenarios pela ordem e registar, para cada um, o veredito **e os valores observados** — nao apenas `PASS`.

    **H1 — Alcancabilidade num clique.** Em `http://localhost:3000`, entrar como `plataforma@lexcv.cv` / `Pa$$w0rd`. Navegar para `/plataforma`. Confirmar que no cabecalho do cartao "Tenants Registados" aparecem **dois** botoes, por esta ordem da esquerda para a direita: "Ver Relatório" (borda neutra, com um icone de grafico) e "Criar Tenant" (azul, com um icone de mais). Clicar em "Ver Relatório". Registar: o URL final na barra de endereco (esperado `/plataforma/relatorio`), o titulo grande da pagina (esperado "Relatório de Utilização") e o subtitulo. Confirmar que existe uma seta de voltar a esquerda do titulo e que clicar nela devolve a `/plataforma`.

    **H2 — Os 4 campos e a coerencia dos numeros.** No relatorio, em janela larga (acima de 768px), registar para **cada** linha da tabela: o nome, o texto do badge de plano, o texto exato da celula de Utilizadores (formato `X/Y` quando existe limite, ou `X · sem limite` quando nao existe) e o texto do badge de Estado. Comparar cada valor de utilizadores com o snapshot da Task 1: **tem de coincidir exatamente**. Depois voltar a `/plataforma` e comparar a coluna Utilizadores desse ecra com a do relatorio para o mesmo tenant: os dois ecras tem de mostrar o mesmo numero. Esta comparacao cruzada e a verificacao ao vivo do criterio de sucesso 2 (uma unica fonte de verdade). Se algum tenant estiver no limite ou acima, confirmar que o numero aparece em vermelho com a legenda "limite atingido" por baixo.

    **H3 — Tenant suspenso continua visivel com estado identificado.** O tenant suspenso pela Task 1 tem de ter linha no relatorio, com um badge vermelho a ler "Suspenso". Registar o nome desse tenant e o texto do badge. Depois escrever o nome dele na caixa "Pesquisar tenant por nome..." e confirmar que continua a aparecer (a pesquisa filtra so por nome, nunca por estado). Este e o criterio de sucesso 3 ao vivo.

    **H4 — Viewport mobile.** Estreitar a janela do browser para menos de 768px (ou usar o modo de dispositivo das ferramentas de desenvolvimento). Confirmar que a tabela e substituida por cartoes empilhados e que **cada cartao continua a mostrar a contagem de utilizadores**, numa linha indentada por baixo do nome, alinhada com o texto do nome e nao com o avatar. Registar o texto exato dessa linha para um tenant. Este cenario existe porque o cartao mobile de `/plataforma` — a origem que foi copiada — nunca mostra essa contagem; se ela nao aparecer aqui, o criterio de sucesso 1 falha em mobile.

    **H5 — Acesso negado para papel de escritorio.** Terminar a sessao e entrar como `admin@lexcv.cv` / `Pa$$w0rd`. Escrever `/plataforma/relatorio` diretamente na barra de endereco. Registar exatamente o que aparece: esperado o cartao de acesso negado com o texto "Não tem permissão para aceder ao relatório de utilização de tenants." e um botao de voltar. Confirmar que **nenhum** nome de tenant, numero de utilizadores ou badge de plano aparece no ecra em nenhum momento, nem sequer por um instante durante o carregamento — se houver um flash de conteudo antes do cartao de acesso negado, isso e o bug de guard a falhar aberto e tem de ser registado como FAIL.

    **H6 — Sem item permanente de navegacao lateral.** De volta na sessao de `plataforma@lexcv.cv`, registar a lista completa de entradas da navegacao lateral. Tem de existir exatamente **uma** entrada relacionada com plataforma (a da consola, da Phase 120) e **nenhuma** entrada para o relatorio. Confirmar tambem no menu de navegacao inferior, em viewport mobile.

    **Observacao a registar, nao a tratar como defeito.** O titulo desta pagina usa deliberadamente um peso de letra mais leve do que os titulos dos outros ecras da app (600 em vez de 700). E uma excecao documentada e aprovada na UI-SPEC (secao Typography, "Disclosed third weight"), depois de 3 rondas de revisao. Registar como observado e conforme, nao como inconsistencia a corrigir. Do mesmo modo, se algum badge de plano aparecer errado ou vazio, verificar primeiro se esse tenant e legado com plano nulo — a migracao `120b-backfill-tenant-plano.sql` esta pendente nesta base de dados (STATE.md), e isso e um item de ambiente pre-existente, nao um defeito desta fase.

    **Limpeza obrigatoria no fim.** Reativar o tenant que a Task 1 suspendeu, atraves da propria consola `/plataforma` (botao de reativar), e confirmar que o badge dele volta a "Ativo" nos dois ecras. Se a Task 1 tiver criado um tenant novo so para o teste, registar isso explicitamente no `122-HUMAN-UAT.md` para o utilizador decidir se o quer manter.

    Escrever `122-HUMAN-UAT.md` com uma seccao por cenario (H1 a H6, mais um resumo dos 3 automatizados A1-A3 da Task 1), cada uma com: veredito, valores observados, e qualquer desvio. Datar o ficheiro. Se algum cenario falhar, registar o valor observado real em vez de o descrever vagamente — as Phases 120 e 121 tiveram de corrigir registos de UAT desatualizados com adendas datadas precisamente por falta desse detalhe.
  </how-to-verify>

  <verify>
    <automated>test -f .planning/phases/LEXCV-122-relat-rio-de-utiliza-o-por-tenant/122-HUMAN-UAT.md && [ "$(grep -cE '^#+ *(H[1-6]|A[1-3])' .planning/phases/LEXCV-122-relat-rio-de-utiliza-o-por-tenant/122-HUMAN-UAT.md)" -ge 9 ] && grep -qiE 'veredito|verdict' .planning/phases/LEXCV-122-relat-rio-de-utiliza-o-por-tenant/122-HUMAN-UAT.md && cd web && pnpm verify:relatorio-utilizacao</automated>
    <human-check>Os 6 cenarios H1-H6 foram percorridos ao vivo no browser, com valores observados registados, e o tenant suspenso foi reativado no fim.</human-check>
  </verify>

  <acceptance_criteria>
    - `122-HUMAN-UAT.md` existe, esta datado, e tem uma seccao por cada um dos 9 cenarios (A1-A3 automatizados + H1-H6 humanos), cada uma com veredito explicito e valores observados.
    - H1 registra o URL final `/plataforma/relatorio`, o titulo "Relatório de Utilização" e a ordem dos dois botoes no cabecalho da consola.
    - H2 registra, por linha da tabela, os 4 valores observados, e afirma explicitamente que cada numero de utilizadores coincide com o snapshot HTTP da Task 1 **e** com o mostrado em `/plataforma` para o mesmo tenant.
    - H3 nomeia o tenant suspenso e registra o texto do badge observado ("Suspenso"), incluindo apos filtrar pela caixa de pesquisa.
    - H4 registra o texto exato da linha de utilizadores num cartao mobile, abaixo de 768px.
    - H5 registra o texto do cartao de acesso negado e afirma explicitamente que nenhum dado de tenant apareceu, nem em flash durante o carregamento.
    - H6 registra a lista completa de entradas de navegacao lateral e confirma zero entradas para o relatorio, em desktop e em mobile.
    - A excecao de peso de letra do titulo e a possivel questao de plano nulo em tenants legados estao registadas como observacoes conformes/de ambiente, nao como defeitos.
    - O tenant suspenso foi reativado e o `122-HUMAN-UAT.md` confirma-o; se um tenant foi criado para o teste, esta explicitamente identificado.
    - `cd web && pnpm verify:relatorio-utilizacao` continua a devolver 15 `PASS` depois da sessao ao vivo (nenhuma alteracao acidental de codigo durante a UAT).
  </acceptance_criteria>

  <resume-signal>Escrever "aprovado" se os 6 cenarios passaram, ou descrever exatamente o que foi observado em cada cenario que falhou (valor real, nao descricao vaga)</resume-signal>

  <done>Existe um registo datado com veredito e valores observados para os 9 cenarios; a alcancabilidade, os 4 campos, a coerencia cruzada dos numeros, a visibilidade do tenant suspenso, o comportamento em mobile, a negacao de acesso a papel de escritorio e a ausencia de item de navegacao lateral estao todos confirmados ao vivo; e o ambiente ficou no estado em que estava.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Sessao real de `ADMIN` de escritorio -> `GET /api/v1/platform/tenants` | A fronteira que o criterio de sucesso 1 exige ("acessivel so a `PLATAFORMA_ADMIN`"); so uma resposta HTTP real a prova |
| Browser -> `/plataforma/relatorio` durante o carregamento de `useMe()` | A janela em que o guard pode falhar aberto (bug WR-03 da Phase 120); um flash de conteudo e observavel apenas ao vivo |
| Sessao de teste -> base de dados de desenvolvimento | A UAT suspende (e tem de reativar) um tenant real desta base de dados |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-122-16 | Elevation of Privilege | `GET /api/v1/platform/tenants` alcancado por papel de escritorio | mitigate | Cenario A2 automatizado com jar de cookies **separado** de uma sessao real de `admin@lexcv.cv`, a exigir exatamente `403`; `200` e falha critica de isolamento e `500` e regressao do `GlobalExceptionHandler` da Phase 119 — em ambos os casos a task para e escala em vez de prosseguir para o checkpoint |
| T-122-17 | Information Disclosure | Flash de dados de tenant antes do cartao de acesso negado | mitigate | Cenario H5 exige observacao explicita de que nenhum nome, numero ou badge aparece **nem por um instante**; e a unica forma de confirmar ao vivo que o early-return de `!me.isFetched` faz o que o gate de origem afirma estruturalmente |
| T-122-18 | Repudiation | Numeros do relatorio divergentes da contagem real de utilizadores | mitigate | Cenario H2 compara cada valor com o snapshot HTTP da Task 1 **e** com o mostrado em `/plataforma`, uma verificacao cruzada de tres pontos sobre a mesma unica fonte de verdade |
| T-122-19 | Repudiation | Registo de UAT desatualizado apos uma correcao posterior | mitigate | Formato de registo exige valores observados e nao apenas `PASS`, e o ficheiro e datado. As Phases 120 e 121 tiveram de corrigir registos com adendas datadas por falta deste detalhe — precedente explicitamente citado na `<how-to-verify>` |
| T-122-20 | Denial of Service | Tenant deixado suspenso no fim da sessao de teste, bloqueando utilizadores reais | mitigate | Limpeza obrigatoria: reativar pela consola e confirmar o badge nos dois ecras; criterio de aceitacao exige a confirmacao escrita no registo |
| T-122-21 | Spoofing | Credenciais de teste documentadas publicamente usadas contra producao | mitigate | Todos os cenarios correm contra `localhost:8080`/`localhost:3000` com a base de dados de desenvolvimento; a `<action>` proibe explicitamente `SEED_ENABLED=true` contra producao. `Pa$$w0rd` esta documentada em `CLAUDE.md`, o que e precisamente por que a Phase 119 decidiu nao semear a conta de plataforma sem gate |
| T-122-SC | Tampering | Instalacoes npm/pip/cargo | n/a | Este plano nao instala nada e nao altera nenhum ficheiro de codigo — so produz um documento de registo. O Package Legitimacy Gate nao se aplica |
</threat_model>

<verification>1. Os 3 cenarios automatizados (A1/A2/A3) devolveram `200`, `403` e presenca do tenant suspenso, respetivamente, com valores registados.
2. Os 6 cenarios humanos (H1-H6) tem veredito explicito e valores observados em `122-HUMAN-UAT.md`.
3. `cd web && pnpm verify:relatorio-utilizacao` — 15 `PASS`, exit 0, depois da sessao ao vivo.
4. `git diff --name-only` lista apenas `.planning/phases/LEXCV-122-relat-rio-de-utiliza-o-por-tenant/122-HUMAN-UAT.md` (e o SUMMARY) — nenhum ficheiro de `web/` ou `backend/` foi alterado durante a UAT.
5. O tenant suspenso para teste esta reativado e o badge dele le "Ativo" nos dois ecras.
</verification>

<success_criteria>
- `122-HUMAN-UAT.md` existe, datado, com 9 seccoes (A1-A3, H1-H6), cada uma com veredito e valores observados.
- A alcancabilidade num clique a partir de `/plataforma` esta confirmada ao vivo, incluindo a seta de voltar.
- Os numeros de utilizadores ativos coincidem em tres pontos: resposta HTTP, relatorio e consola.
- O tenant suspenso ficou visivel no relatorio com badge "Suspenso", inclusive sob pesquisa por nome.
- A contagem de utilizadores esta visivel abaixo de 768px.
- Um `ADMIN` de escritorio recebe `403` no endpoint e o cartao de acesso negado no ecra, sem flash de dados.
- A navegacao lateral continua com uma unica entrada de plataforma.
- A excecao de tipografia e a questao de plano nulo em tenants legados estao registadas como observacoes, nao como defeitos.
- O ambiente ficou limpo (tenant reativado).

**Nota:** `UTIL-01` ja foi fechado pelo Plan 03. Este plano **nao** volta a correr `requirements mark-complete` — se algum cenario falhar, o caminho correto e um plano de gap closure, nao reabrir e refechar o requisito.
</success_criteria>

<output>
Criar `.planning/phases/LEXCV-122-relat-rio-de-utiliza-o-por-tenant/122-HUMAN-UAT.md` (o registo por cenario) e `.planning/phases/LEXCV-122-relat-rio-de-utiliza-o-por-tenant/122-04-SUMMARY.md` (o resumo de execucao, com a contagem de cenarios passados/falhados, quaisquer observacoes de ambiente encontradas, e confirmacao da limpeza).
</output>
