---
phase: 122-relat-rio-de-utiliza-o-por-tenant
plan: 03
type: execute
wave: 2
depends_on: [122-01]
files_modified:
  - web/src/app/(dashboard)/plataforma/page.tsx
  - web/scripts/verify-relatorio-utilizacao.mjs
  - web/package.json
autonomous: true
requirements: [UTIL-01]

must_haves:
  truths:
    - "Um PLATAFORMA_ADMIN na consola /plataforma ve um botao Ver Relatorio no CardHeader e chega ao relatorio com um clique"
    - "O botao Ver Relatorio aparece a esquerda de Criar Tenant, em variante outline neutra, sem competir com o CTA acentuado"
    - "O botao Criar Tenant continua literalmente inalterado — mesmo handler, mesmas classes, mesmo icone"
    - "Nenhum item permanente de navegacao lateral foi acrescentado para o relatorio"
    - "Existe um gate automatizado, executavel por um comando de package.json, que prova por origem as 15 propriedades estruturais desta fase"
    - "O gate falha de forma precisa quando uma propriedade e quebrada, provado por uma injecao de regressao temporaria"
  artifacts:
    - path: "web/src/app/(dashboard)/plataforma/page.tsx"
      provides: "Ponto de entrada para /plataforma/relatorio no CardHeader da consola, com cluster de 2 botoes e wrap em viewports estreitos"
      contains: "/plataforma/relatorio"
    - path: "web/scripts/verify-relatorio-utilizacao.mjs"
      provides: "Gate de origem com 15 assercoes sobre os 4 ficheiros relevantes, no estilo de verify-consola-tenants.mjs"
      min_lines: 200
    - path: "web/package.json"
      provides: "Script verify:relatorio-utilizacao"
      contains: "verify:relatorio-utilizacao"
  key_links:
    - from: "web/src/app/(dashboard)/plataforma/page.tsx"
      to: "/plataforma/relatorio"
      via: "componente Link de next/link dentro do CardHeader, com Button asChild variant outline"
      pattern: "href=\"/plataforma/relatorio\""
    - from: "web/package.json"
      to: "web/scripts/verify-relatorio-utilizacao.mjs"
      via: "entrada de scripts verify:relatorio-utilizacao"
      pattern: "verify:relatorio-utilizacao"
    - from: "web/scripts/verify-relatorio-utilizacao.mjs"
      to: "web/src/components/shared/dashboard-shell.tsx"
      via: "assercao de nao-regressao: zero referencias a rota do relatorio no shell"
      pattern: "dashboard-shell"
---

<objective>
Tornar o relatorio **alcancavel** e blindar as suas propriedades estruturais com um gate automatizado duravel.

Purpose: O Plan 01 cria a rota, mas uma rota sem porta de entrada nao satisfaz `UTIL-01` — `REQUIREMENTS.md` pede um relatorio interno que o administrador de plataforma **consulta**, e `122-CONTEXT.md` fixou explicitamente como se chega la: um link em contexto no `CardHeader` da consola `/plataforma`, **nunca** um segundo item permanente de navegacao lateral (precedente da Phase 89, v2.10, registado nas Key Decisions de `PROJECT.md` — as notificacoes ganharam rota propria e deliberadamente nenhuma entrada de nav). O mecanismo para acrescentar esse item existe e esta a um `if` de distancia em `dashboard-shell.tsx`, usado ha uma fase para o proprio `/plataforma` — e e precisamente por isso que a proibicao precisa de uma assercao automatizada, nao apenas de uma frase num plano.

A segunda metade deste plano existe porque este codebase ja resolveu este problema quatro vezes: `verify:juizo-origem`, `verify:limite-utilizadores`, `verify:consola-tenants` e `verify:bloqueio-rbac` sao gates de origem em Node puro que provam propriedades estruturais que nenhum teste unitario cobre (nao existe infraestrutura de teste de componentes React neste projeto — as 3 unicas suites vitest testam `hooks`, `lib` e `schemas`, nunca JSX). O relatorio herda esse padrao.

Output: uma alteracao cirurgica a um `CardHeader` existente, um script de gate novo com 15 assercoes, e uma entrada de `scripts`. **Este e o plano que fecha `UTIL-01`.**
</objective>

<execution_context>
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@web/AGENTS.md
@.planning/phases/LEXCV-122-relat-rio-de-utiliza-o-por-tenant/122-CONTEXT.md
@.planning/phases/LEXCV-122-relat-rio-de-utiliza-o-por-tenant/122-PATTERNS.md
@.planning/phases/LEXCV-122-relat-rio-de-utiliza-o-por-tenant/122-UI-SPEC.md
@.planning/phases/LEXCV-122-relat-rio-de-utiliza-o-por-tenant/122-01-SUMMARY.md
@web/src/app/(dashboard)/plataforma/page.tsx
@web/scripts/verify-consola-tenants.mjs
</context>

<interfaces>
<!-- Contratos ja confirmados no planeamento por leitura direta do codigo. NAO redescobrir por exploracao. -->

Bloco exato a alterar (`plataforma/page.tsx:162-174`, estado atual — um unico botao, sem cluster):

```tsx
<CardHeader className="flex flex-row items-center justify-between space-y-0">
  <div>
    <CardTitle className="text-xl font-semibold">Tenants Registados</CardTitle>
    <CardDescription>Lista de organizações com acesso à plataforma LexCV.</CardDescription>
  </div>
  <Button
    onClick={() => setIsFormOpen(true)}
    className="bg-blue-600 hover:bg-blue-700 text-white flex items-center gap-1.5 shadow-sm text-xs py-1.5 px-3 h-auto"
  >
    <Plus className="h-4 w-4" />
    Criar Tenant
  </Button>
</CardHeader>
```

Forma-alvo (fixada pela UI-SPEC, linhas 119-141 — reproduzida aqui para diff exato):

```tsx
<CardHeader className="flex flex-row flex-wrap items-center justify-between gap-3 space-y-0">
  <div>
    <CardTitle className="text-xl font-semibold">Tenants Registados</CardTitle>
    <CardDescription>Lista de organizações com acesso à plataforma LexCV.</CardDescription>
  </div>
  <div className="flex items-center gap-2">
    <Button asChild variant="outline" className="text-xs py-1.5 px-3 h-auto flex items-center gap-1.5">
      <Link href="/plataforma/relatorio">
        <FileChartColumn className="h-4 w-4" />
        Ver Relatório
      </Link>
    </Button>
    <Button
      onClick={() => setIsFormOpen(true)}
      className="bg-blue-600 hover:bg-blue-700 text-white flex items-center gap-1.5 shadow-sm text-xs py-1.5 px-3 h-auto"
    >
      <Plus className="h-4 w-4" />
      Criar Tenant
    </Button>
  </div>
</CardHeader>
```

Tres diferencas deliberadas, todas obrigatorias: o `className` do `CardHeader` ganha `flex-wrap` e `gap-3` (consideracao nova — a Phase 120 so precisou de acomodar um botao); o `Button` de "Criar Tenant" passa a estar dentro de um `div` com `flex items-center gap-2`, **sem qualquer alteracao ao proprio botao**; e "Ver Relatório" renderiza primeiro (secundario antes de primario, exatamente a ordem de `processos/page.tsx:142-157`, que poe "Dashboard" em `outline` antes de "Novo Processo" acentuado).

Imports a alterar (`plataforma/page.tsx:3-4`, estado atual):

```tsx
import * as React from "react";
import { Lock, Pencil, Plus, Unlock } from "lucide-react";
```

Estado-alvo — `next/link` nao esta importado hoje neste ficheiro (confirmado: nunca navegou para nenhum lado, so alternou estado local e abriu Dialogs/AlertDialogs):

```tsx
import * as React from "react";
import Link from "next/link";
import { FileChartColumn, Lock, Pencil, Plus, Unlock } from "lucide-react";
```

`FileChartColumn` esta confirmado presente no `lucide-react@0.543.0` instalado (`web/node_modules/lucide-react/dist/lucide-react.d.ts`, exportado na linha 23486) — **zero bump de versao, zero instalacao**.

Condicao de visibilidade: nada a acrescentar. O `CardHeader` inteiro ja vive dentro do ramo `else` do ternario `{isFormOpen ? <CriarTenantPanel .../> : <Card>...}` (`:154`), que por sua vez so renderiza depois do guard de pagina `PLATAFORMA_ADMIN` (`:76-91`) passar. O botao novo e apenas um segundo filho de um `CardHeader` que ja e condicional.

Padrao de gate a seguir (`web/scripts/verify-consola-tenants.mjs`, ficheiro completo, 282 linhas) — Node puro, zero dependencias, `fs/promises` + `path` + `fileURLToPath`. Reutilizar **verbatim** os 3 helpers ja provados:
- `stripComments(source)` (`:61-76`) — remove blocos `{/* ... */}`, blocos `/* ... */` e linhas que comecam por `//` ou `*`. Obrigatorio: sem ele, um comentario que mencione um token procurado torna o gate auto-invalidante.
- `sliceBetweenMarkers(source, startMarker, endMarker)` (`:97-103`) — delimita um bloco por dois marcadores literais.
- O loop final de reporting (`:260-278`) — imprime `PASS <id>` ou `FAIL <id> — <motivo>` por assercao e faz `process.exit(failures === 0 ? 0 : 1)`.

Scripts existentes em `web/package.json` (confirmado) — a entrada nova segue exatamente esta forma:

```json
"verify:consola-tenants": "node scripts/verify-consola-tenants.mjs",
"verify:bloqueio-rbac": "node scripts/verify-bloqueio-rbac.mjs"
```

Comandos: `cd web && pnpm lint`, `pnpm exec tsc --noEmit`, `pnpm build`, `pnpm verify:relatorio-utilizacao`, `pnpm verify:consola-tenants` (nao-regressao do gate da Phase 120).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Acrescentar o botao Ver Relatorio ao CardHeader de /plataforma</name>

  <files>
    web/src/app/(dashboard)/plataforma/page.tsx
  </files>

  <read_first>
    - `web/src/app/(dashboard)/plataforma/page.tsx` — as linhas 3-4 (imports a alterar), 154 (o ternario `isFormOpen` que ja governa a visibilidade) e 161-174 (o `Card` e o `CardHeader` a alterar). Confirmar por leitura que `next/link` **nao** esta importado hoje.
    - `web/src/app/(dashboard)/processos/page.tsx` linhas 142-157 — o precedente exato de cluster de 2 botoes: `outline` secundario primeiro, acentuado primario depois, ambos com a mesma altura.
    - `.planning/phases/LEXCV-122-relat-rio-de-utiliza-o-por-tenant/122-UI-SPEC.md` linhas 115-145 — a forma-alvo e as adicoes de import, ja fixadas.
    - `.planning/phases/LEXCV-122-relat-rio-de-utiliza-o-por-tenant/122-01-SUMMARY.md` — confirmar que a rota `/plataforma/relatorio` existe e apareceu na tabela de rotas do build do Plan 01, antes de criar um link para ela.
  </read_first>

  <action>
    Editar `web/src/app/(dashboard)/plataforma/page.tsx` com exatamente tres alteracoes, e nenhuma mais.

    Primeira: acrescentar `import Link from "next/link";` imediatamente depois do import de React (linha 3), e acrescentar `FileChartColumn` a lista de icones do import de `lucide-react`, em ordem alfabetica, ficando a lista com 5 nomes.

    Segunda: no `className` do `CardHeader` (`:162`), acrescentar `flex-wrap` e `gap-3`, mantendo os tokens ja presentes (`flex`, `flex-row`, `items-center`, `justify-between`, `space-y-0`). Sem isto, em viewports estreitos tres elementos (bloco de titulo + 2 botoes) ficariam comprimidos numa linha indivisivel — consideracao que a Phase 120 nunca teve de resolver porque so tinha um botao aqui.

    Terceira: envolver o `Button` de "Criar Tenant" num `div` com `className="flex items-center gap-2"` e acrescentar, **antes** dele dentro desse mesmo `div`, um `Button asChild variant="outline" className="text-xs py-1.5 px-3 h-auto flex items-center gap-1.5"` cujo unico filho e um `Link href="/plataforma/relatorio"` contendo `<FileChartColumn className="h-4 w-4" />` e o texto `Ver Relatório` (com acento, exatamente esta copy da UI-SPEC). Ver a forma-alvo completa em `<interfaces>`.

    O botao "Criar Tenant" fica **literalmente inalterado**: mesmo `onClick={() => setIsFormOpen(true)}`, mesmas classes `bg-blue-600 hover:bg-blue-700 text-white flex items-center gap-1.5 shadow-sm text-xs py-1.5 px-3 h-auto`, mesmo `<Plus className="h-4 w-4" />`, mesmo texto. So muda de pai (passa a estar dentro do `div` novo).

    A variante do botao novo e `outline`, nao acentuada, e isso e uma decisao fechada da UI-SPEC (secao Color): a Phase 120 estabeleceu que "Criar Tenant" e o unico ponto focal acentuado deste ecra, e dois botoes azuis lado a lado no mesmo cabecalho diluiriam essa regra. Nao "melhorar" o botao novo para azul.

    Proibicoes: nao tocar em `web/src/components/shared/dashboard-shell.tsx` nem em `sidebar-nav.tsx` — a decisao registada e que este ecra secundario nao ganha item permanente de navegacao (precedente da Phase 89, v2.10; o mecanismo de splice existe em `dashboard-shell.tsx` e foi usado ha uma fase para o proprio `/plataforma`, o que torna a tentacao real). Nao acrescentar nenhuma condicao de visibilidade nova ao botao — o `CardHeader` ja e condicional duas vezes. Nao alterar nenhuma outra parte de `page.tsx` (guard, memo de pesquisa, cards mobile, DataTable, os dois dialogos, `EditarTenantForm`, `AlteracaoEstadoConteudo`).

    Correr `cd web && pnpm lint`, `pnpm exec tsc --noEmit` e `pnpm verify:consola-tenants` — este ultimo e o gate da Phase 120 sobre este mesmo ficheiro e tem de continuar a passar todas as 12 assercoes, provando que nada do que a Phase 120 garantiu foi quebrado por esta edicao.
  </action>

  <verify>
    <automated>cd web && grep -q 'href="/plataforma/relatorio"' "src/app/(dashboard)/plataforma/page.tsx" && grep -q 'FileChartColumn' "src/app/(dashboard)/plataforma/page.tsx" && grep -q 'import Link from "next/link"' "src/app/(dashboard)/plataforma/page.tsx" && grep -q 'flex-wrap' "src/app/(dashboard)/plataforma/page.tsx" && [ "$(grep -c 'setIsFormOpen(true)' "src/app/(dashboard)/plataforma/page.tsx")" = "1" ] && [ "$(grep -c 'plataforma/relatorio' src/components/shared/dashboard-shell.tsx)" = "0" ] && pnpm lint && pnpm exec tsc --noEmit && pnpm verify:consola-tenants</automated>
  </verify>

  <acceptance_criteria>
    - `web/src/app/(dashboard)/plataforma/page.tsx` contem `href="/plataforma/relatorio"` e o texto `Ver Relatório` (com acento).
    - O ficheiro contem `import Link from "next/link";` e o import de `lucide-react` lista 5 nomes incluindo `FileChartColumn`.
    - O `className` do `CardHeader` contem os 7 tokens `flex`, `flex-row`, `flex-wrap`, `items-center`, `justify-between`, `gap-3`, `space-y-0`.
    - O indice da primeira ocorrencia de `Ver Relatório` **precede** o indice de `Criar Tenant` (ordem secundario-antes-de-primario).
    - `grep -c 'setIsFormOpen(true)'` devolve exatamente 1 e o botao "Criar Tenant" conserva as classes `bg-blue-600 hover:bg-blue-700 text-white` e o icone `Plus` — inalterado.
    - O `Button` novo tem `variant="outline"` e **nao** contem `bg-blue-600`.
    - `grep -c 'plataforma/relatorio'` em `web/src/components/shared/dashboard-shell.tsx` e em `web/src/components/shared/sidebar-nav.tsx` devolve 0 em ambos.
    - `git diff --name-only` lista exclusivamente `web/src/app/(dashboard)/plataforma/page.tsx` para esta task.
    - `cd web && pnpm lint`, `pnpm exec tsc --noEmit` e `pnpm verify:consola-tenants` terminam todos com exit code 0; o ultimo imprime 12 linhas `PASS` e nenhuma `FAIL`.
  </acceptance_criteria>

  <done>Um `PLATAFORMA_ADMIN` na consola tem um botao neutro "Ver Relatório" a esquerda de "Criar Tenant" que leva a `/plataforma/relatorio`; "Criar Tenant" esta intacto; nenhum item de navegacao lateral foi criado; e o gate da Phase 120 sobre este mesmo ficheiro continua verde.</done>
</task>

<task type="auto">
  <name>Task 2: Criar o gate de origem verify-relatorio-utilizacao.mjs com 15 assercoes e provar que falha com precisao</name>

  <files>
    web/scripts/verify-relatorio-utilizacao.mjs (novo)
    web/package.json
  </files>

  <read_first>
    - `web/scripts/verify-consola-tenants.mjs` (ficheiro completo, 282 linhas) — o modelo. Ler o comentario de topo (incluindo a seccao explicita "O QUE ESTE GATE NAO CONSEGUE PROVAR", que este ficheiro tambem deve ter), `stripComments` (`:61-76`), `sliceBetweenMarkers` (`:97-103`), a forma dos objetos de assercao (`:129-257`) e o loop de reporting (`:260-278`).
    - `web/src/app/(dashboard)/plataforma/relatorio/columns.tsx` e `web/src/app/(dashboard)/plataforma/relatorio/page.tsx` — os ficheiros criados pelo Plan 01, contra os quais as assercoes A01-A11 correm. Ler para escrever predicados que casem com o codigo real, nao com o codigo assumido.
    - `web/src/app/(dashboard)/plataforma/page.tsx` — o ficheiro alterado na Task 1, alvo das assercoes A12-A13.
    - `web/package.json` linhas 5-15 — a forma exata das 4 entradas `verify:*` existentes.
    - `.planning/phases/LEXCV-122-relat-rio-de-utiliza-o-por-tenant/122-UI-SPEC.md` — as strings de copy que A07/A09 verificam.
  </read_first>

  <action>
    Criar `web/scripts/verify-relatorio-utilizacao.mjs` em Node puro, zero dependencias, seguindo `verify-consola-tenants.mjs` na estrutura: constantes de caminho no topo, os helpers `stripComments` e `sliceBetweenMarkers` copiados verbatim, um array `assertions` de objetos `{ id, descricao, predicate }`, e o loop de reporting que imprime `PASS <id>` / `FAIL <id> — <motivo>` e sai com codigo 1 se houver falhas.

    Quatro ficheiros-alvo, lidos como texto e passados por `stripComments` antes de qualquer assercao: `src/app/(dashboard)/plataforma/relatorio/columns.tsx`, `src/app/(dashboard)/plataforma/relatorio/page.tsx`, `src/app/(dashboard)/plataforma/page.tsx` e `src/components/shared/dashboard-shell.tsx` (este ultimo apenas para a assercao de nao-regressao).

    Implementar exatamente estas 15 assercoes, com estes ids literais:

    1. `colunas-array-estatico` — `relatorio/columns.tsx` casa `/export const relatorioColumns\s*:\s*ColumnDef<TenantAdminSummary>\[\]\s*=/` e **nao** contem `export function columns`. Prova a forma deliberada de array estatico (a primeira do genero neste codebase) e nao uma copia acidental da factory.
    2. `colunas-quatro-ids` — contagem de `/id: "(nome|plano|utilizadores|estado)"/g` igual a 4 **e** contagem de `id: "acoes"` igual a 0.
    3. `colunas-sem-celula-de-acoes` — nenhuma ocorrencia de `TenantAcoesCell`, `TooltipTrigger`, `TooltipContent`, `Pencil` ou `Unlock`. Prova ausencia de imports mortos e, por consequencia, ausencia de acoes num ecra so-de-leitura.
    4. `colunas-nome-reservado-importado` — casa `/from\s+"\.\.\/columns"/` **e** nao contem a atribuicao literal do nome reservado (`= "LexCV"`). Uma unica definicao do literal no codebase.
    5. `utilizadores-sem-recalculo` — delimitar o bloco da column def de utilizadores com `sliceBetweenMarkers(columnsSource, 'id: "utilizadores"', 'id: "estado"')`; afirmar que contem `tenant.utilizadoresAtivos` e que **nao** casa `/\.(reduce|filter|map)\(|\.length/`. Esta e a assercao do criterio de sucesso 2: uma unica fonte de verdade, sem calculo paralelo no cliente.
    6. `estado-badge-suspenso` — delimitar de `'id: "estado"'` ao fim do ficheiro; afirmar presenca de `"Suspenso"`, `"Ativo"` e `variant={ativo ? "green" : "red"}`. Criterio de sucesso 3, lado do render.
    7. `relatorio-guard-falha-fechado` — delimitar de `export default function` a `function RelatorioUtilizacaoContent` em `relatorio/page.tsx`; afirmar que o indice de `/if\s*\(\s*!me\.isFetched\s*\)/` e diferente de -1, que o indice de `AccessDeniedState` e diferente de -1, e que o primeiro **precede** o segundo. Mesma tecnica da assercao `guard-de-pagina-falha-fechado-durante-loading` de `verify-consola-tenants.mjs` (`:243-257`). Afirmar tambem presenca de `roles?.includes("PLATAFORMA_ADMIN")`.
    8. `relatorio-sem-filtro-de-estado` — delimitar o corpo do memo de `const tenantsFiltrados` a `}, [tenants.data, searchTerm]);`; afirmar presenca de `t.nome.toLowerCase().includes(termo)` e **ausencia** de `ativo` dentro desse bloco. Prova que nenhum filtro esconde tenants suspensos.
    9. `relatorio-h1-semibold` — extrair a tag de abertura do `h1` com `/<h1\b[^>]*>/`; afirmar que contem `font-semibold` e **nao** contem `font-bold`. Guarda a excecao de Dimension 4 explicitamente disclosed e force-approved na UI-SPEC, contra uma "correcao" bem-intencionada futura.
    10. `relatorio-mobile-mostra-utilizadores` — delimitar de `md:hidden` a `hidden md:block` em `relatorio/page.tsx`; afirmar presenca de `utilizadoresAtivos` e de `pl-[52px]`. Sem esta linha, o criterio de sucesso 1 falharia em qualquer viewport abaixo de `md`, porque o card mobile de `/plataforma` (a origem copiada) nunca mostra a contagem.
    11. `relatorio-sem-mutacoes` — `relatorio/page.tsx` nao contem nenhum de `useCreateTenant`, `useUpdateTenant`, `useSetTenantAtivo`, `mutateAsync`, `AlertDialog`, `window.location.reload`, nem (case-insensitive) `csv`.
    12. `entrada-ver-relatorio` — `plataforma/page.tsx` contem `href="/plataforma/relatorio"`, `Ver Relatório` e `variant="outline"`, e o `className` do `CardHeader` contem `flex-wrap`.
    13. `entrada-ordem-e-criar-tenant-intocado` — em `plataforma/page.tsx`, o indice de `Ver Relatório` precede o indice de `Criar Tenant`; a contagem de `setIsFormOpen(true)` e exatamente 1; e o ficheiro continua a conter `bg-blue-600 hover:bg-blue-700 text-white`. Prova a ordem secundario-antes-de-primario e a nao-alteracao do CTA.
    14. `sem-segundo-item-de-nav` — `dashboard-shell.tsx` contem 0 ocorrencias de `plataforma/relatorio`. Prova a decisao registada de `122-CONTEXT.md` (precedente da Phase 89) contra o mecanismo de splice que existe nesse mesmo ficheiro.
    15. `sem-export-csv-nem-kpis` — nem `relatorio/page.tsx` nem `relatorio/columns.tsx` contem (case-insensitive) `csv`, `download` ou `Blob`. Prova que as Deferred Ideas de `122-CONTEXT.md` continuam diferidas.

    Escrever um comentario de topo com a mesma disciplina do modelo, incluindo uma seccao explicita do que este gate **nao** consegue provar (fica para o checkpoint humano do Plan 04): que a rota resolve mesmo no browser; que o clique em "Ver Relatório" navega; que os badges e as cores renderizam; que um utilizador real sem `PLATAFORMA_ADMIN` recebe mesmo `403` do backend; e que os numeros mostrados correspondem aos utilizadores reais na base de dados. Um gate de origem prova estrutura, nao renderizacao.

    Acrescentar a `web/package.json`, na seccao `scripts`, a entrada `"verify:relatorio-utilizacao": "node scripts/verify-relatorio-utilizacao.mjs"`, depois de `verify:bloqueio-rbac`, seguindo exatamente a forma das 4 entradas existentes. **Nao** acrescentar, remover nem alterar nenhuma dependencia; nao correr `pnpm install`; nao tocar em `pnpm-lock.yaml`.

    Correr `cd web && pnpm verify:relatorio-utilizacao` e confirmar 15 linhas `PASS` e exit code 0.

    **Prova negativa obrigatoria.** Um gate que nunca se viu falhar nao e prova de nada. Escolher 3 assercoes de familias diferentes — `utilizadores-sem-recalculo` (A05), `relatorio-h1-semibold` (A09) e `sem-segundo-item-de-nav` (A14) — e, uma a uma, introduzir uma regressao **minima e cirurgica** no codigo-fonte, correr o gate, registar que **apenas** essa assercao passa a `FAIL` (as outras 14 continuam `PASS`), e reverter imediatamente com `git checkout -- <ficheiro>`. Seguir a tecnica da Phase 121: alterar a condicao ou acrescentar um token, nunca apagar blocos inteiros — uma remocao grosseira faria cair varias assercoes ao mesmo tempo e nao provaria precisao. Sugestoes concretas: para A05, acrescentar uma expressao com `.filter(` dentro do bloco da column def de utilizadores; para A09, trocar `font-semibold` por `font-bold` na tag do `h1`; para A14, acrescentar uma string com o caminho do relatorio a `dashboard-shell.tsx`. Depois das 3 reversoes, correr `git status` para confirmar arvore limpa e `pnpm verify:relatorio-utilizacao` para confirmar 15 `PASS` outra vez. Registar os 3 resultados no SUMMARY.

    Por fim, correr os 4 gates existentes (`pnpm verify:juizo-origem`, `pnpm verify:limite-utilizadores`, `pnpm verify:consola-tenants`, `pnpm verify:bloqueio-rbac`), mais `pnpm lint`, `pnpm exec tsc --noEmit`, `pnpm build` e `pnpm test` — todos verdes, para confirmar zero regressoes em qualquer gate ou suite pre-existente.

    Depois de todos os gates estarem verdes, **este e o plano que fecha `UTIL-01`**: correr o passo de `requirements mark-complete` para `UTIL-01`. Justificacao para o registo: `REQUIREMENTS.md` exige um relatorio interno alcancavel a mostrar 4 campos por tenant; os Plans 01 e 02 deliberadamente nao o fecharam (o Plan 01 entregou uma rota sem porta de entrada, o Plan 02 entregou apenas uma guarda de teste no backend), exatamente como os Plans 120-02/03/04 documentaram para `PROV-02`/`PROV-05` e como o commit `cd45fcf9` teve de corrigir por um fecho prematuro. Este plano entrega a porta de entrada, pelo que o fecho aqui e o correto e nao uma repeticao dessa regressao.
  </action>

  <verify>
    <automated>cd web && test -f scripts/verify-relatorio-utilizacao.mjs && grep -q 'verify:relatorio-utilizacao' package.json && [ "$(pnpm verify:relatorio-utilizacao | grep -c '^PASS ')" = "15" ] && [ "$(pnpm verify:relatorio-utilizacao | grep -c '^FAIL ')" = "0" ] && pnpm verify:consola-tenants && pnpm verify:bloqueio-rbac && pnpm verify:limite-utilizadores && pnpm verify:juizo-origem && pnpm lint && pnpm exec tsc --noEmit && pnpm test && pnpm build</automated>
  </verify>

  <acceptance_criteria>
    - `web/scripts/verify-relatorio-utilizacao.mjs` existe, e Node puro (zero `import` de pacotes fora de `node:fs/promises`, `node:path`, `node:url`) e define exatamente 15 objetos de assercao com os 15 ids literais listados na `<action>`.
    - O script contem os helpers `stripComments` e `sliceBetweenMarkers` (nenhuma assercao corre sobre codigo-fonte nao normalizado — sem isto o gate seria auto-invalidante por comentarios).
    - `cd web && pnpm verify:relatorio-utilizacao` imprime 15 linhas que comecam por `PASS `, 0 linhas que comecam por `FAIL `, e termina com exit code 0.
    - `web/package.json` tem a entrada `"verify:relatorio-utilizacao": "node scripts/verify-relatorio-utilizacao.mjs"`; `git diff web/package.json` mostra **apenas** essa linha acrescentada (nenhuma alteracao em `dependencies` ou `devDependencies`), e `git diff --name-only` nao lista `pnpm-lock.yaml`.
    - A prova negativa esta registada para as 3 assercoes escolhidas: para cada uma, o SUMMARY indica a regressao exata introduzida, que **so** aquela assercao passou a `FAIL` (14 continuaram `PASS`), e que a reversao devolveu o gate a 15 `PASS`. `git status` fica limpo no fim.
    - Os 4 gates pre-existentes (`verify:juizo-origem`, `verify:limite-utilizadores`, `verify:consola-tenants`, `verify:bloqueio-rbac`) terminam todos com exit code 0.
    - `pnpm lint`, `pnpm exec tsc --noEmit`, `pnpm test` e `pnpm build` terminam todos com exit code 0.
    - `UTIL-01` esta marcado como completo em `REQUIREMENTS.md` (tabela de rastreabilidade a ler `Complete` para `UTIL-01`), e o SUMMARY explica porque este plano e o que o fecha.
  </acceptance_criteria>

  <done>O relatorio e alcancavel a partir da consola, as 15 propriedades estruturais desta fase tem um gate automatizado que corre por comando de `package.json` e que se viu falhar com precisao em 3 familias diferentes, todos os gates e suites anteriores continuam verdes, e `UTIL-01` esta fechado.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Consola `/plataforma` -> `/plataforma/relatorio` | Navegacao entre dois ecras que sao ambos gated a `PLATAFORMA_ADMIN`; nenhum novo dado atravessa esta fronteira, so um `href` |
| Codigo-fonte -> gate de origem em Node | O script le ficheiros do repositorio como texto; nao executa nem importa nenhum deles |
| `package.json` -> gestor de pacotes | Uma entrada de `scripts` nova aponta para um ficheiro local; nenhuma dependencia e acrescentada |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-122-11 | Information Disclosure | Botao "Ver Relatório" visivel a quem nao e `PLATAFORMA_ADMIN` | mitigate | O botao vive dentro do `CardHeader` que so renderiza depois do guard de pagina de `/plataforma` passar (`:76-91`), e dentro do ramo `else` do ternario `isFormOpen`. Mesmo que o URL vaze, `/plataforma/relatorio` tem o seu proprio guard (Plan 01) e o gate de classe `hasRole('PLATAFORMA_ADMIN')` do backend continua a ser a fronteira autoritativa. Defesa em 3 camadas, nenhuma delas a depender da invisibilidade do link |
| T-122-12 | Elevation of Privilege | Acrescentar por engano o relatorio a navegacao lateral, expondo a existencia da consola de plataforma a todos os papeis | mitigate | Assercao A14 do gate (`sem-segundo-item-de-nav`): zero referencias a rota em `dashboard-shell.tsx`, verificada tambem no `<verify>` da Task 1. O mecanismo de splice por papel existe nesse ficheiro e foi usado ha uma fase para `/plataforma`, o que torna a proibicao facil de violar sem uma assercao |
| T-122-13 | Tampering | Regressao silenciosa das propriedades estruturais da fase numa refatoracao futura | mitigate | Gate de 15 assercoes com comando dedicado em `package.json`, no padrao dos 4 gates existentes; a prova negativa obrigatoria em 3 familias diferentes demonstra que o gate falha com precisao e nao de forma binaria |
| T-122-14 | Tampering | Gate auto-invalidante por comentarios que mencionem os tokens procurados | mitigate | `stripComments` aplicado a todos os 4 ficheiros antes de qualquer predicado, exatamente como no modelo `verify-consola-tenants.mjs`; as Phases 119/120/121 tem 3 incidentes registados deste tipo |
| T-122-15 | Tampering | Alteracao inadvertida ao botao "Criar Tenant" ou ao resto de `plataforma/page.tsx` | mitigate | Assercao A13 (`setIsFormOpen(true)` exatamente 1 vez, classes `bg-blue-600` preservadas), criterio de aceitacao sobre `git diff --name-only`, e re-execucao das 12 assercoes de `verify:consola-tenants` (o gate da Phase 120 sobre este mesmo ficheiro) |
| T-122-SC | Tampering | Instalacoes npm/pnpm | mitigate | Zero dependencias novas. `FileChartColumn` ja existe no `lucide-react@0.543.0` instalado (confirmado nas typings) e o gate e Node puro (`node:fs/promises`, `node:path`, `node:url`). A edicao de `package.json` acrescenta **apenas** uma entrada de `scripts`; criterio de aceitacao proibe alteracoes a `dependencies`/`devDependencies` e exige que `pnpm-lock.yaml` nao apareca no diff. Nenhuma task corre um gestor de pacotes em modo de instalacao, por isso o Package Legitimacy Gate nao e acionado |
</threat_model>

<verification>
1. `cd web && pnpm verify:relatorio-utilizacao` — 15 `PASS`, 0 `FAIL`, exit 0.
2. `cd web && pnpm verify:consola-tenants` — 12 `PASS`, 0 `FAIL`, exit 0 (nao-regressao do gate da Phase 120 sobre `plataforma/page.tsx`).
3. `cd web && pnpm verify:bloqueio-rbac && pnpm verify:limite-utilizadores && pnpm verify:juizo-origem` — exit 0 nos tres.
4. `cd web && pnpm lint && pnpm exec tsc --noEmit && pnpm test && pnpm build` — exit 0 nos quatro; a tabela de rotas do build continua a listar `/plataforma/relatorio`.
5. `git diff --name-only` lista exatamente 3 ficheiros: `web/src/app/(dashboard)/plataforma/page.tsx`, `web/scripts/verify-relatorio-utilizacao.mjs`, `web/package.json`. Nao lista `pnpm-lock.yaml`, `dashboard-shell.tsx`, `sidebar-nav.tsx` nem nada sob `backend/`.
6. `git status` limpo depois das 3 reversoes da prova negativa (nenhum ficheiro de regressao temporaria por reverter).
7. `REQUIREMENTS.md` mostra `UTIL-01` como `Complete` na tabela de rastreabilidade.
</verification>

<success_criteria>
- `/plataforma/relatorio` e alcancavel com um clique a partir de `/plataforma`, por um botao `outline` neutro chamado "Ver Relatório", posicionado antes de "Criar Tenant".
- "Criar Tenant" e o resto de `plataforma/page.tsx` estao inalterados; o gate da Phase 120 sobre esse ficheiro continua a passar as 12 assercoes.
- Nenhum item permanente de navegacao lateral foi criado para o relatorio, e existe uma assercao automatizada a garanti-lo.
- `pnpm verify:relatorio-utilizacao` existe, corre em Node puro e passa 15/15.
- O gate demonstrou falhar com precisao em 3 familias de assercao diferentes, com reversao completa e arvore limpa.
- Zero dependencias novas, `pnpm-lock.yaml` intocado.
- `UTIL-01` fechado em `REQUIREMENTS.md`, com a justificacao registada no SUMMARY.
</success_criteria>

<output>
Criar `.planning/phases/LEXCV-122-relat-rio-de-utiliza-o-por-tenant/122-03-SUMMARY.md` no final, registando: o diff conceptual do `CardHeader` (antes/depois), a lista dos 15 ids de assercao com o veredito de cada um, a tabela da prova negativa (assercao escolhida, regressao introduzida, quantas passaram a `FAIL`, confirmacao da reversao), o resultado dos 4 gates pre-existentes, e a justificacao explicita de porque este plano — e nao o 01 nem o 02 — e o que fecha `UTIL-01`.
</output>
