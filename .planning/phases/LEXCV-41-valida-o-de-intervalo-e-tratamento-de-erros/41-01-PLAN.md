---
phase: 41-valida-o-de-intervalo-e-tratamento-de-erros
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/schemas/eventos.ts
  - backend/src/main/java/com/lexcv/controllers/ResourceController.java
  - web/src/lib/api.ts
autonomous: true
requirements:
  - AGD-31-01
  - AGD-33-02
  - AGD-33-03
must_haves:
  truths:
    - "Zod schema enforces dataFim is not before dataInicio"
    - "Backend rejects POST/PUT event requests if dataFim is before dataInicio"
    - "Backend GET /eventos returns HTTP 400 if date query parameters are malformed"
    - "Frontend toast displays detailed server error messages correctly"
  artifacts:
    - path: "web/src/schemas/eventos.ts"
      provides: "Zod date range validation"
      contains: "dataFim"
    - path: "backend/src/main/java/com/lexcv/controllers/ResourceController.java"
      provides: "Backend date range and query param parsing checks"
      contains: "isBefore"
---

<objective>
Implement robust frontend and backend date validation rules, catch and handle date query parsing errors gracefully in Spring Boot, and display user-friendly toast messages.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Validar intervalo de datas no Zod schema do frontend</name>
  <files>web/src/schemas/eventos.ts</files>
  <action>
Modify `web/src/schemas/eventos.ts` to add a refine check on `eventoFormSchema` ensuring `dataFim` is greater than or equal to `dataInicio`.
  </action>
  <verify>
    <automated>grep -c "A data de fim não pode ser anterior" web/src/schemas/eventos.ts</automated>
  </verify>
  <acceptance_criteria>
    - Zod schema rejects invalid date ranges and sets the error on the dataFim field.
  </acceptance_criteria>
  <done>Zod schema refined for date range validation.</done>
</task>

<task type="auto">
  <name>Task 2: Adicionar validações de data e tratamento de erros no Backend</name>
  <files>backend/src/main/java/com/lexcv/controllers/ResourceController.java</files>
  <action>
Modify `backend/src/main/java/com/lexcv/controllers/ResourceController.java`:
1. In `listEventos`, catch invalid `dataInicio` and `dataFim` parameter parsing exceptions and return HTTP 400 with a detailed error.
2. In `createEvento` and `updateEvento`, reject payloads where `dataFim` is before `dataInicio` with HTTP 400.
  </action>
  <verify>
    <automated>grep -c "isBefore" backend/src/main/java/com/lexcv/controllers/ResourceController.java</automated>
  </verify>
  <acceptance_criteria>
    - Backend returns BAD_REQUEST on invalid date range inputs and query param parsing failures.
  </acceptance_criteria>
  <done>Backend date validation and parsing error handling implemented.</done>
</task>

<task type="auto">
  <name>Task 3: Melhorar tratamento de erros e exibição de Toasts no frontend</name>
  <files>web/src/lib/api.ts</files>
  <action>
Modify `apiFetch` in `web/src/lib/api.ts` to parse JSON error responses and extract `.message` or `.error` fields to display in toast notifications.
  </action>
  <verify>
    <automated>grep -c "JSON.parse" web/src/lib/api.ts</automated>
  </verify>
  <acceptance_criteria>
    - JSON error response messages are extracted and displayed in toasts.
  </acceptance_criteria>
  <done>API error toast mapping improved.</done>
</task>

</tasks>

<verification>
Verify build passes:
```bash
pnpm --filter web build
```
</verification>

<success_criteria>
- Frontend form rejects invalid date ranges with inline Zod errors
- Backend validates date ranges and format errors returning HTTP 400
- Frontend toast display is user-friendly and shows detailed backend errors
</success_criteria>
