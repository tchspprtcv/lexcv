---
phase: 109-notifica-es-settings-setup-wizard
plan: 03
type: execute
wave: 2
depends_on: [109-01, 109-02]
files_modified: []
autonomous: false
requirements: [NTF-28, NTF-29, NTF-30]

must_haves:
  truths:
    - "pnpm build passes with zero new type errors after both implementation plans land"
    - "pnpm lint passes with zero new errors (no orphaned imports left in dashboard-shell.tsx)"
    - "A human confirms the DropdownMenu user menu, the Badge counter, and the /setup Progress render and behave correctly in light and dark themes"
  artifacts: []
  key_links: []
---

<objective>
Close Phase 109 with the milestone's established module-phase gate: an automated `pnpm build` + `pnpm lint` regression gate, followed by a mandatory human visual/functional checkpoint covering all 3 NTF surfaces. This plan touches no source files — it only verifies the combined output of Plans 109-01 (UserMenu/DropdownMenu) and 109-02 (Badge counter + setup Progress).

Purpose: Prove the component-identity swaps compile clean and behave correctly (the user menu now opens instead of navigating; logout works from the menu; the bell counter and setup progress render as expected) before marking NTF-28/29/30 complete.
Output: A green build/lint gate and a human-approved sign-off (or a list of concrete issues to fix).
</objective>

<execution_context>
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/LEXCV-109-notifica-es-settings-setup-wizard/109-UI-SPEC.md

@web/src/components/shared/user-menu.tsx
@web/src/components/shared/dashboard-shell.tsx
@web/src/components/shared/notification-bell.tsx
@web/src/app/setup/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Holistic build + lint regression gate</name>
  <read_first>
    - .planning/phases/LEXCV-109-notifica-es-settings-setup-wizard/109-01-SUMMARY.md and 109-02-SUMMARY.md (what shipped, any deviations to double-check)
    - web/src/components/shared/dashboard-shell.tsx (confirm no orphaned Button/Tooltip/LogOut imports remain)
    - CLAUDE.md (frontend commands: `pnpm build`, `pnpm lint`, run from `web/`)
  </read_first>
  <files>web/ (no source changes — verification only)</files>
  <action>
    From `web/`, run `pnpm build` and `pnpm lint`. Both must complete with zero new errors. If either fails, diagnose against the two implementation plans (most likely cause: a now-unused import left behind in `dashboard-shell.tsx`, or a `cn`/`Progress` import missing) and fix minimally within the files owned by 109-01/109-02 — do NOT expand scope. Confirm via grep that exactly one `UserMenu` component definition exists and it is consumed 3× in `dashboard-shell.tsx`, that the bell counter is a `<Badge>` (no manual `<span>` counter), and that `<Progress` is rendered in `setup/page.tsx`.
  </action>
  <verify>
    <automated>cd web && pnpm build && pnpm lint && test "$(grep -c '<UserMenu' src/components/shared/dashboard-shell.tsx)" = "3" && grep -q '<Badge' src/components/shared/notification-bell.tsx && grep -q '<Progress value=' src/app/setup/page.tsx && echo GATE_OK</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm build` exits 0 with no new TypeScript/compile errors.
    - `pnpm lint` exits 0 with no new errors (no `no-unused-vars` for Button/Tooltip/LogOut in dashboard-shell.tsx).
    - `<UserMenu` count in dashboard-shell.tsx is exactly 3; bell counter is a `<Badge>`; `<Progress value=` present in setup/page.tsx.
    - `<verify>` prints `GATE_OK`.
  </acceptance_criteria>
  <done>Build and lint are green; all 3 NTF surfaces are structurally present.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human visual + functional checkpoint (3 NTF surfaces, both themes)</name>
  <read_first>
    - .planning/phases/LEXCV-109-notifica-es-settings-setup-wizard/109-UI-SPEC.md ("Component Inventory", "Copywriting Contract")
    - .planning/phases/LEXCV-109-notifica-es-settings-setup-wizard/109-01-SUMMARY.md and 109-02-SUMMARY.md
  </read_first>
  <action>
    Present this checkpoint to the user. Ensure the frontend dev server is running (`pnpm dev` in `web/`) and walk the human through the `<how-to-verify>` steps below in both light and dark themes. Do not auto-approve — this is a blocking human checkpoint. If the user reports issues, fix them minimally within the files owned by Plans 109-01/109-02 (do not expand scope), then re-present. On "approved", record the sign-off in the SUMMARY and mark NTF-28/29/30 Complete in REQUIREMENTS.md.
  </action>
  <what-built>
    NTF-28: a shared `UserMenu` DropdownMenu at 3 call sites (topbar header, desktop sidebar footer, mobile Sheet footer) — Perfil / Configurações / separator / Terminar sessão; the block no longer navigates directly to `/settings`, and the two footer logout buttons are folded into the menu.
    NTF-29: the notification-bell unread counter re-implemented with the official `Badge` (red normal / slate-400 on fetch error, `9+`/`!` cap), same position/size.
    NTF-30: the `/setup` wizard's Checklist card now shows a linear `Progress` bar (33% idle / 66% submitting / 100% success) with the 3 checklist lines as a legend.
  </what-built>
  <how-to-verify>
    Start the frontend (`pnpm dev` in `web/`, or the running dev server) and, in BOTH light and dark themes (toggle via the topbar ThemeToggle):
    1. Topbar user menu (desktop ≥sm width): click the avatar+name block in the header — a DropdownMenu opens (it does NOT navigate to /settings). Confirm items Perfil, Configurações (gear icon), a separator, Terminar sessão (logout icon). Click "Configurações" → lands on /settings. Reopen, click "Perfil" → lands on /profile. Reopen, click "Terminar sessão" → you are logged out and sent to /login.
    2. Desktop sidebar footer + mobile Sheet footer (open the hamburger drawer at <md width): the same menu opens from the avatar block; there is no longer a separate standalone logout icon button beside it. Menu opens upward/collision-adjusted, not clipped.
    3. Notification bell: with unread notifications present, the red counter badge shows the number (or `9+`); it sits in the same top-right corner of the bell as before with no layout shift. (Optional: if a fetch error can be induced, the badge turns slate-gray with `!`.) Opening the bell still shows the existing Popover list unchanged.
    4. /setup wizard: visit /setup (on a fresh/uninitialized install, or inspect the page) — the right column shows a "Progresso" (or "Checklist") card with a Progress bar at 33% and the 3 numbered lines below; on submit it advances to 66%, and to 100% on success.
    5. RBAC sanity (optional but recommended): log in as a non-ADMIN seeded role (e.g. teste.advogado@lexcv.cv) and confirm the user menu + bell render the same (no crash, no RBAC-denial flash) — none of these 3 surfaces are permission-gated.
  </how-to-verify>
  <acceptance_criteria>
    - The topbar/sidebar/Sheet user block opens a DropdownMenu (does not navigate directly); Perfil→/profile, Configurações→/settings, Terminar sessão→logout all work.
    - No standalone logout icon button remains in the sidebar/Sheet footers.
    - The bell counter renders as a red badge (number / `9+`) in the original position with no layout shift; the bell Popover list is unchanged.
    - The /setup Progress bar is visible from mount at 33% and advances 66%→100% through submit→success.
    - Everything renders correctly in both light and dark themes with no visual regression or clipping.
  </acceptance_criteria>
  <resume-signal>Type "approved" to close Phase 109, or describe any issues to fix.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| n/a (verification-only plan) | This plan writes no source; the only boundary is the CI gate protecting against regressions shipped by 109-01/109-02. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-109-05 | Tampering | build/lint regression gate | mitigate | `pnpm build` + `pnpm lint` must exit 0 before human sign-off, catching any type error or orphaned-import regression introduced by the two implementation plans. |
| T-109-SC | Tampering | npm installs | accept | Zero new dependencies across the whole phase; all primitives were vetted in Phase 101's package-legitimacy gate. |
</threat_model>

<verification>
- `pnpm build` and `pnpm lint` exit 0 with no new errors.
- Human checkpoint approves the DropdownMenu behavior, Badge counter, and setup Progress across both themes (or returns a concrete fix list).
</verification>

<success_criteria>
Phase 109 closes: NTF-28/29/30 verified both automatically (build/lint green, all 3 surfaces structurally present) and by human visual/functional sign-off; the notification-bell Popover confirmed untouched.
</success_criteria>

<output>
Create `.planning/phases/LEXCV-109-notifica-es-settings-setup-wizard/109-03-SUMMARY.md` when done.
</output>
