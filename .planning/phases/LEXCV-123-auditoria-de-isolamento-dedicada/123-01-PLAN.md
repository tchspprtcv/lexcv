---
phase: 123-auditoria-de-isolamento-dedicada
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/LEXCV-123-auditoria-de-isolamento-dedicada/123-ISOL-AUDIT.md
autonomous: true
requirements: [ISOL-04]

must_haves:
  truths:
    - "Um leitor de 123-ISOL-AUDIT.md vê um veredito explícito e nomeado para a consola de tenants (Phase 120) e para o relatório de utilização (Phase 122)"
    - "Cada linha de veredito do Critério 1 carrega o comando literal que a produziu e o valor observado, reproduzível por qualquer fase futura"
    - "Todos os caminhos backend que leem ou escrevem dados de Tenant têm uma linha de disposição — incluindo os dois que NÃO são gated por papel: os campos tenant_* de GET /auth/me (own-tenant) e a escrita de Tenant do público POST /setup/initialize (gate singleton)"
    - "A auditoria declara explicitamente que TenantAdminSummaryResponse é a única projeção de Tenant servida às duas superfícies e enumera o que ela deliberadamente omite"
  artifacts:
    - path: ".planning/phases/LEXCV-123-auditoria-de-isolamento-dedicada/123-ISOL-AUDIT.md"
      provides: "Cabeçalho da auditoria + veredito por superfície do Critério de Sucesso 1 + secção de comandos de reprodução"
      min_lines: 70
      contains: "Critério de Sucesso 1"
  key_links:
    - from: "123-ISOL-AUDIT.md"
      to: "backend/src/main/java/com/lexcv/controllers/PlatformAdminController.java"
      via: "linha de veredito citando o gate de classe e a contagem zero de gates de método"
      pattern: "PLATAFORMA_ADMIN"
    - from: "123-ISOL-AUDIT.md"
      to: "backend/src/main/java/com/lexcv/dtos/TenantAdminSummaryResponse.java"
      via: "linha de veredito sobre a projeção partilhada pelas 2 superfícies"
      pattern: "TenantAdminSummaryResponse"
    - from: "123-ISOL-AUDIT.md"
      to: "web/src/hooks/use-platform-admin.ts"
      via: "linha de veredito provando que as 4 mutações/queries só chamam /platform/tenants"
      pattern: "use-platform-admin"
---

<objective>
Reconfirmar, por execução de comando e leitura direta do código actual (nunca por citação das alegações das Fases 120/121/122), o **Critério de Sucesso 1** da Fase 123: a consola de administração de tenants (Phase 120) e o relatório de utilização (Phase 122) expõem dados exclusivamente através de endpoints gated a `PLATAFORMA_ADMIN`, nunca através de um endpoint tenant-scoped comum. Criar `123-ISOL-AUDIT.md` com o veredito por superfície.

Purpose: ISOL-04 exige um veredito auditável antes de o utilizador poder provisionar em segurança um 2º tenant pagante real. Esta é a última fase do marco v2.16 e a auditoria é a sua porta de saída. O valor da auditoria está em ser **independentemente re-derivada** — uma auditoria que apenas repete o que as fases anteriores afirmaram não prova nada de novo.
Output: `.planning/phases/LEXCV-123-auditoria-de-isolamento-dedicada/123-ISOL-AUDIT.md` (novo ficheiro), com cabeçalho + secção do Critério 1 + comandos de reprodução. **Zero alterações de código de produção esperadas** — esta é uma fase de confirmação e documentação, tal como a Fase 121 tratou ISOL-01/ISOL-02.
</objective>

<execution_context>
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/LEXCV-123-auditoria-de-isolamento-dedicada/123-CONTEXT.md

# Precedente de formato — reutilizar literalmente a tabela `Query / Guard | Scope confirmed | Verdict`,
# a secção "Comandos de reproducao", e o desfecho explícito COVERED / lista de fixes.
# NÃO inventar um formato novo.
@.planning/phases/LEXCV-121-fechar-suposi-es-de-tenant-nica-bloqueio-de-rbac/121-ISOL-AUDIT.md
</context>

<known_traps>
Este codebase tem um historial documentado (STATE.md: Fases 119-04, 120-01, 120-02, 121-01, 121-03, 122) de gates baseados em `grep` a colidirem com o texto dos próprios comentários. Aplicar as seguintes precauções ao escrever e ao correr comandos:

1. **Git Bash é o shell.** Em padrões `grep -E`, escrever parênteses como classe de caracteres — `[(]` — nunca `\(`. `\(` faz `grep: Unmatched ( or \(` neste ambiente (confirmado durante o planeamento).
2. **`grep -c` que devolve `0` sai com código 1** e aborta uma cadeia `&&`. Separar comandos de auditoria com `;`, nunca com `&&`.
3. **Comentários contam.** Um `grep -c` sobre um ficheiro `.java` conta linhas de Javadoc. Onde a contagem tem de refletir código vivo, filtrar primeiro (`grep -v '^\s*\*' | grep -v '^\s*//'`) ou confirmar cada ocorrência por leitura da linha.
4. Já existe um caso literal deste efeito nos testes desta fase: `grep -l "SpringBootTest"` lista `SetupControllerSingletonRegressaoTest.java`, mas esse ficheiro é um teste Mockito puro — a palavra aparece só num docblock que diz explicitamente que NÃO usa `@SpringBootTest`. Confirmar por `grep -n "@ExtendWith"` antes de classificar qualquer teste como lento.
5. **Divergências entre o valor esperado e o valor observado registam-se com transparência**, no espírito da "Nota de transparência" de `121-ISOL-AUDIT.md` — não se escondem, nem se escalam automaticamente. Se um comando devolver um valor diferente do esperado, ler a linha em causa, classificá-la (falso positivo vs. achado real) e escrever a classificação na auditoria.
</known_traps>

<tasks>

<task type="auto">
  <name>Task 1: Re-derivar por execução todos os caminhos de exposição de dados de Tenant e dar disposição a cada um</name>
  <files>(nenhum — task de auditoria por execução de comando e leitura direta; escreve zero ficheiros. As conclusões passam para a Task 2, que é quem cria o documento. `git status --porcelain -- backend web` tem de ficar vazio.)</files>

  <read_first>
    - backend/src/main/java/com/lexcv/controllers/PlatformAdminController.java (201 linhas — ler na íntegra: é o único endpoint das 2 superfícies)
    - backend/src/main/java/com/lexcv/dtos/TenantAdminSummaryResponse.java (a projeção partilhada)
    - backend/src/main/java/com/lexcv/dtos/TenantProvisionResponse.java (resposta 201 do POST /platform/tenants)
    - web/src/hooks/use-platform-admin.ts (76 linhas — as 4 queries/mutações que ambos os ecrãs consomem)
    - web/src/app/(dashboard)/plataforma/relatorio/page.tsx (192 linhas — o ecrã do relatório e o seu guard)
    - web/src/app/(dashboard)/plataforma/page.tsx — NÃO ler na íntegra (566 linhas). Ler apenas as regiões dos imports de hooks e do guard RBAC: `sed -n '30,110p'`
    - backend/src/main/java/com/lexcv/dtos/UserResponse.java (os 4 campos tenant_* servidos por GET /auth/me)
    - backend/src/main/java/com/lexcv/controllers/AuthController.java (ler a região de `getMe()`, ~linhas 195-270 — confirmar que o `Tenant` é resolvido a partir de `UserPrincipal.getTenantId()`, nunca do corpo do pedido)
    - backend/src/main/java/com/lexcv/services/SetupService.java (170 linhas — os 2 caminhos de escrita de Tenant: initializeSystem vs. provisionTenant)
    - backend/src/main/java/com/lexcv/controllers/SetupController.java
    - backend/src/main/java/com/lexcv/config/SecurityConfig.java (100 linhas — a lista permitAll)
  </read_first>

  <action>
Auditar, por execução de comando mais leitura das linhas devolvidas, as três perguntas abaixo. Registar em notas de trabalho (não escrever ainda o ficheiro de auditoria — isso é a Task 2) o comando literal, o valor observado, e a disposição de cada ocorrência.

**(A) Gate do lado do servidor.** Confirmar que `PlatformAdminController` tem exactamente um `@PreAuthorize` ao nível de classe, com o valor literal `hasRole('PLATAFORMA_ADMIN')`, e **zero** `@PreAuthorize` ao nível de método (ou seja, nenhum dos 4 handlers relaxa o gate herdado):

```
grep -n '^@PreAuthorize' backend/src/main/java/com/lexcv/controllers/PlatformAdminController.java
grep -nE '^[[:space:]]+@PreAuthorize' backend/src/main/java/com/lexcv/controllers/PlatformAdminController.java
grep -nE '@(Get|Post|Put|Patch|Delete)Mapping' backend/src/main/java/com/lexcv/controllers/PlatformAdminController.java
```
Valores esperados no momento do planeamento: 1 gate de classe na linha 53; 0 gates de método; 4 handlers (`POST /tenants`, `GET /tenants`, `PUT /tenants/{id}`, `PATCH /tenants/{id}/ativo`). Reforçar o gate por execução do teste que assere o valor literal da anotação, em vez de confiar só no grep:
```
cd backend && mvn -q test -Dtest=PlatformAdminControllerTest
```
(`PlatformAdminControllerTest#anotacaoDeClasseTemValorExatoHasRolePlataformaAdmin` compara a anotação com a string exacta; `createTenant_comRoleAdminDeTenantNormalERecusadoAntesDeOMetodoCorrer` prova a recusa com um proxy AOP real, não por reflexão.)

**(B) Trajeto frontend → HTTP das 2 superfícies.** Provar que ambos os ecrãs só alcançam dados de Tenant através de `/platform/tenants*`, e que nenhum deles chama qualquer outro endpoint para obter dados de tenant:
```
grep -c 'apiFetch' web/src/hooks/use-platform-admin.ts
grep -oE 'apiFetch<[^(]*[(][^,)]*' web/src/hooks/use-platform-admin.ts | grep -cv 'platform'
grep -rn '/platform' web/src --include=*.ts --include=*.tsx
```
Esperado: 5 ocorrências de `apiFetch` (1 import + 4 chamadas), **0** chamadas fora de `/platform`, e nenhuma referência a `/platform` fora de `use-platform-admin.ts` / `types/platform-admin.ts`. Confirmar por leitura que `plataforma/page.tsx` e `plataforma/relatorio/page.tsx` obtêm os dados exclusivamente via `useTenantsAdmin` (mais as 3 mutações na consola), e que ambos aplicam o guard `if (!me.isFetched) return null;` **antes** da verificação `roles.includes("PLATAFORMA_ADMIN")` (a correcção WR-03 da Phase 120 — falhar fechado durante o carregamento, nunca fail-open).

**(C) Superfície de dados e caminhos não-gated-por-papel.** Enumerar a projeção servida e dar disposição explícita a cada caminho backend que toca dados de `Tenant`:
```
grep -cE '^[[:space:]]+private ' backend/src/main/java/com/lexcv/dtos/TenantAdminSummaryResponse.java
grep -cE '^[[:space:]]+private .*(logoDataUrl|nif|email|telefone|createdAt)' backend/src/main/java/com/lexcv/dtos/TenantAdminSummaryResponse.java
grep -rn 'TenantAdminSummaryResponse|TenantProvisionResponse|TenantPublicInfoResponse' --include=*.java backend/src/main/java
grep -rnE 'tenantRepository[.](save|delete|findAll)' --include=*.java backend/src/main/java
```
Esperado: 6 campos; 0 campos sensíveis; os 2 DTOs de administração referenciados **apenas** em `PlatformAdminController` (`TenantPublicInfoResponse` só em `PublicController`, que devolve a marca genérica hardcoded — ISOL-01, já fechado na Phase 121).

Dois caminhos **não** são gated por papel e têm de receber uma disposição nomeada em vez de omissão silenciosa:

1. **`GET /api/v1/auth/me`** serve campos `tenant_*` (`tenant_id`, `tenant_nome`, `tenant_logo_data_url`, `tenant_plano`, `tenant_limite_utilizadores` em `UserResponse.java`) a qualquer utilizador autenticado — é um endpoint tenant-scoped comum que devolve dados de tenant. Confirmar por leitura de `AuthController.getMe` que o `Tenant` é resolvido a partir de `UserPrincipal.getTenantId()` (o JWT do próprio chamador) e nunca a partir de um id fornecido pelo cliente, e que não existe caminho para obter os campos de outro tenant. Disposição esperada: COVERED / own-tenant-only, não uma via de contorno das 2 superfícies (não expõe a lista de tenants nem `utilizadoresAtivos` de terceiros).
2. **`POST /api/v1/setup/initialize`** é público (consta da lista `permitAll` em `SecurityConfig`) e escreve um `Tenant` via `SetupService.initializeSystem`. Confirmar que o mecanismo de bloqueio é o gate **singleton** (a verificação `setupService.isInitialized()` em `SetupController` mais o `throw` sobre `settings.getInitialized()` dentro de `initializeSystem`, com `findByIdForUpdate` a serializar corridas), e não um gate de papel — e que `provisionTenant` (o caminho repetível, sem gate singleton) é invocado exclusivamente por `PlatformAdminController`. Provar por execução:
```
cd backend && mvn -q test -Dtest=SetupControllerSingletonRegressaoTest,SetupServiceProvisionTenantTest
```
Disposição esperada: COVERED, com o mecanismo nomeado (singleton, PROV-06) — porque um leitor que assuma "todo caminho de escrita de Tenant é gated a PLATAFORMA_ADMIN" estaria factualmente errado, e a auditoria tem de dizer a verdade literal.

Não alterar nenhum ficheiro sob `backend/` ou `web/` nesta task. Se algum comando revelar um gate em falta, uma projeção com campo sensível, ou um caminho de leitura cross-tenant não gated, **parar e reportar como achado** em vez de o corrigir em silêncio — esta fase não previa alterações de produção, pelo que um achado real é uma mudança material de âmbito que o orquestrador tem de decidir.
  </action>

  <acceptance_criteria>
    - `grep -n '^@PreAuthorize' PlatformAdminController.java` devolve exactamente 1 linha, e essa linha contém a string literal `hasRole('PLATAFORMA_ADMIN')`
    - `grep -nE '^[[:space:]]+@PreAuthorize' PlatformAdminController.java` devolve 0 linhas (nenhum handler relaxa o gate de classe)
    - `mvn -q test -Dtest=PlatformAdminControllerTest` termina em `BUILD SUCCESS` com 0 falhas e 0 erros
    - `grep -oE 'apiFetch<[^(]*[(][^,)]*' web/src/hooks/use-platform-admin.ts | grep -cv 'platform'` devolve `0`
    - `grep -cE '^[[:space:]]+private ' TenantAdminSummaryResponse.java` devolve `6`; `grep -cE '^[[:space:]]+private .*(logoDataUrl|nif|email|telefone|createdAt)'` devolve `0`
    - `grep -rn 'TenantAdminSummaryResponse|TenantProvisionResponse' --include=*.java backend/src/main/java` não devolve nenhuma ocorrência num ficheiro de controller diferente de `PlatformAdminController.java`
    - `mvn -q test -Dtest=SetupControllerSingletonRegressaoTest,SetupServiceProvisionTenantTest` termina em `BUILD SUCCESS`
    - Ambos os ecrãs (`plataforma/page.tsx`, `plataforma/relatorio/page.tsx`) confirmados por leitura a executar o branch `!me.isFetched` antes de qualquer verificação de papel
    - `git status --porcelain -- backend web` devolve vazio no fim da task
  </acceptance_criteria>

  <verify>
    <automated>(cd backend && mvn -q test -Dtest=PlatformAdminControllerTest,SetupControllerSingletonRegressaoTest,SetupServiceProvisionTenantTest) || exit 1; test -z "$(git status --porcelain -- backend web)" || exit 1; echo "ZERO-DIFF-OK"</automated>
  </verify>

  <done>Os 4 handlers de `PlatformAdminController` confirmados a herdar um único gate de classe `hasRole('PLATAFORMA_ADMIN')` sem excepções (por grep e por teste com proxy AOP real); as 4 chamadas HTTP das 2 superfícies confirmadas a apontar só para `/platform/tenants*`; a projeção `TenantAdminSummaryResponse` confirmada com 6 campos e zero campos sensíveis; e os 2 caminhos não-gated-por-papel (`/auth/me` own-tenant, `/setup/initialize` singleton) traçados com disposição nomeada. Zero ficheiros de produção alterados.</done>
</task>

<task type="auto">
  <name>Task 2: Criar 123-ISOL-AUDIT.md com o veredito por superfície do Critério de Sucesso 1</name>
  <files>.planning/phases/LEXCV-123-auditoria-de-isolamento-dedicada/123-ISOL-AUDIT.md (criar)</files>

  <read_first>
    - .planning/phases/LEXCV-121-fechar-suposi-es-de-tenant-nica-bloqueio-de-rbac/121-ISOL-AUDIT.md (116 linhas — o formato a reutilizar literalmente: tabela `Query / Guard | Scope confirmed | Verdict`, secção "Comandos de reproducao", veredito explícito por linha)
    - .planning/phases/LEXCV-123-auditoria-de-isolamento-dedicada/123-CONTEXT.md (as decisões trancadas desta fase)
    - .planning/ROADMAP.md — apenas a secção `#### Phase 123` (os 3 Critérios de Sucesso, para os citar textualmente)
  </read_first>

  <action>
Criar `.planning/phases/LEXCV-123-auditoria-de-isolamento-dedicada/123-ISOL-AUDIT.md`, reutilizando o formato de `121-ISOL-AUDIT.md` — **não inventar um formato novo** (decisão trancada em `123-CONTEXT.md`).

Estrutura a escrever nesta task (o Plano 02 anexa a seguir a secção do Critério 2, a decisão do Pitfall 1 e o veredito final — deixar o ficheiro terminado de forma a que um append posterior encaixe sem reescrever nada do que aqui vai):

1. **Frontmatter YAML** no mesmo estilo de `121-ISOL-AUDIT.md`: `phase: 123-auditoria-de-isolamento-dedicada`, `plan: 01`, `subsystem: api`, `tags: [tenant-isolation, security-audit, rbac, multi-tenancy]`. **Não** incluir `requirements-completed` aqui — ISOL-04 só fecha no Plano 02, Task 3 (o marco tem precedente explícito e repetido contra o fecho prematuro de requisitos: Phase 120 Planos 02/03/04 para PROV-02/PROV-05, Phase 122 Planos 01/02 para UTIL-01).

2. **Título e parágrafo de abertura em negrito**, no estilo do precedente: uma frase que declare o âmbito (as 3 superfícies novas da v2.16: consola de tenants, relatório de utilização, bloqueio de RBAC), o método (re-derivação independente por execução de comando, não citação das Fases 120/121/122), e que nenhum código de produção foi alterado.

3. **Nota de reutilização de formato**, citando `121-ISOL-AUDIT.md` e `97-01-SUMMARY.md` (AUD-01, v2.11) como os dois precedentes — o ROADMAP nomeia explicitamente a AUD-01 ("no espírito da AUD-01 da v2.11").

4. **Secção `## Critério de Sucesso 1 — Veredito por superfície`**, abrindo com a citação textual do critério tal como está no ROADMAP, seguida de uma tabela `| Query / Guard | Scope confirmed | Verdict |` com uma linha por item auditado na Task 1. Cobrir no mínimo:
   - o gate de classe de `PlatformAdminController` (valor literal + contagem zero de gates de método + os 4 handlers)
   - cada um dos 4 endpoints `/platform/tenants*` individualmente, com o método HTTP
   - `TenantAdminSummaryResponse` (6 campos; o que omite deliberadamente e porquê — logótipo `@Lob`, NIF, email, telefone, data de criação)
   - `TenantProvisionResponse` (resposta 201, nunca a entidade crua)
   - `PlatformAdminController.listTenants` `tenantRepository.findAll()` (iteração cross-tenant deliberada, gated)
   - `use-platform-admin.ts` (as 4 chamadas, todas `/platform/tenants*`)
   - o guard de `/plataforma` e o guard de `/plataforma/relatorio` (`!me.isFetched` antes da verificação de papel — WR-03)
   - `GET /auth/me` campos `tenant_*` — disposição own-tenant-only, com o mecanismo nomeado (`UserPrincipal.getTenantId()` do JWT)
   - `POST /setup/initialize` → `SetupService.initializeSystem` — disposição COVERED **via gate singleton, não via gate de papel**, com `SetupService.provisionTenant` contrastado como o caminho repetível gated a `PLATAFORMA_ADMIN`
   - `PublicController` / `TenantPublicInfoResponse` — marca genérica hardcoded, sem resolução de tenant (ISOL-01, fechado na Phase 121)

   Cada linha `Verdict` tem de ser `COVERED` ou uma descrição de fix aplicado — nunca uma frase subjetiva. Onde a disposição não for um simples COVERED (os casos `/auth/me` e `/setup/initialize`), acrescentar o mecanismo entre parênteses, à maneira das categorias `(a)/(b)/(c)` de `121-ISOL-AUDIT.md`.

5. **Secção `## Comandos de reproducao — Critério 1`**, com um bloco de código bash contendo os comandos literais corridos na Task 1 e, em comentário no fim de cada linha, o valor observado. Incluir os avisos de ambiente que se aplicam a quem os re-correr: `[(]` em vez de `\(` em padrões `-E` (Git Bash), e separar por `;` porque `grep -c` que devolve `0` sai com código 1.

6. **Secção `## Divergências entre valor esperado e valor observado`** — se a Task 1 não encontrou nenhuma, escrever explicitamente que não houve nenhuma (uma linha), em vez de omitir a secção. Se encontrou, seguir o formato da "Nota de transparência" de `121-ISOL-AUDIT.md`: comando, valor esperado, valor observado, a linha lida, e a classificação (falso positivo de comentário vs. achado real).

7. **Nota de fecho da secção**: contagem literal de fixes aplicados no Critério 1 (escrever o número de forma positiva, ex. `Fixes aplicados no Critério 1: 0`). **Não** escrever uma frase do tipo "zero linhas FIXED" e **não** criar nenhum gate de verificação que exija `grep -c FIXED == 0` sobre este documento — o próprio documento contém a palavra, pelo que um gate assim seria auto-invalidante (é exactamente a classe de erro que este codebase já registou 6 vezes em STATE.md).
  </action>

  <acceptance_criteria>
    - O ficheiro `.planning/phases/LEXCV-123-auditoria-de-isolamento-dedicada/123-ISOL-AUDIT.md` existe e tem pelo menos 70 linhas
    - `grep -c 'Critério de Sucesso 1' 123-ISOL-AUDIT.md` devolve pelo menos `1`
    - `grep -c '^| ' 123-ISOL-AUDIT.md` devolve pelo menos `11` (uma linha de tabela por item auditado, além do cabeçalho e do separador)
    - `grep -c 'COVERED' 123-ISOL-AUDIT.md` devolve pelo menos `11`
    - `grep -c 'Comandos de reproducao' 123-ISOL-AUDIT.md` devolve pelo menos `1`
    - Todos estes identificadores aparecem literalmente no documento: `PlatformAdminController`, `TenantAdminSummaryResponse`, `use-platform-admin`, `hasRole('PLATAFORMA_ADMIN')`, `auth/me`, `setup/initialize`, `isFetched`
    - `grep -c 'requirements-completed' 123-ISOL-AUDIT.md` devolve `0` (ISOL-04 fecha no Plano 02, não aqui)
    - O documento contém uma secção sobre divergências, mesmo que declare zero divergências
    - `git status --porcelain -- backend web` devolve vazio (nenhum ficheiro de produção tocado por esta fase)
  </acceptance_criteria>

  <verify>
    <automated>D=.planning/phases/LEXCV-123-auditoria-de-isolamento-dedicada/123-ISOL-AUDIT.md; test -f "$D" || exit 1; test "$(grep -c '' $D)" -ge 70 || exit 1; test "$(grep -c '^| ' $D)" -ge 11 || exit 1; test "$(grep -c 'COVERED' $D)" -ge 11 || exit 1; for t in "Critério de Sucesso 1" "Comandos de reproducao" "PlatformAdminController" "TenantAdminSummaryResponse" "use-platform-admin" "auth/me" "setup/initialize" "isFetched"; do grep -q "$t" $D || { echo "FALTA: $t"; exit 1; }; done; test "$(grep -c 'requirements-completed' $D)" -eq 0 || exit 1; test -z "$(git status --porcelain -- backend web)" || exit 1; echo "AUDIT-C1-OK"</automated>
  </verify>

  <done>`123-ISOL-AUDIT.md` existe com frontmatter, parágrafo de abertura, nota de reutilização de formato, tabela de veredito por superfície do Critério 1 com pelo menos 11 linhas todas com veredito explícito, secção de comandos de reprodução com os valores observados, e secção de divergências. Nenhum ficheiro de produção alterado. ISOL-04 deliberadamente ainda não fechado.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → Next.js (`/api/v1/*` rewrite) → Caddy → Spring | Único caminho por onde a consola e o relatório obtêm dados; o JWT em cookie httpOnly atravessa aqui |
| tenant ADMIN → `/api/v1/platform/*` | Fronteira de escalada de privilégio: um ADMIN de escritório não pode atravessar |
| público não autenticado → `POST /api/v1/setup/initialize` | Escrita de `Tenant` alcançável sem autenticação; a fronteira é o gate singleton, não um papel |
| esta auditoria → decisão de negócio "provisionar um 2º tenant pagante real" | Um veredito falso-limpo desbloqueia uma acção irreversível em produção |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-123-01 | Information Disclosure | `GET /api/v1/platform/tenants` + `TenantAdminSummaryResponse` | mitigate | Re-derivar o gate de classe por dois métodos independentes: `grep -n '^@PreAuthorize'` (valor literal) **e** execução de `PlatformAdminControllerTest` (que compara a anotação por reflexão e prova a recusa com um proxy AOP real); assere 0 `@PreAuthorize` de método e 6 campos no DTO com 0 campos sensíveis |
| T-123-02 | Elevation of Privilege | tenant ADMIN a alcançar qualquer dos 4 handlers `/platform/tenants*` | mitigate | `grep -nE '@(Get\|Post\|Put\|Patch\|Delete)Mapping'` enumera os 4 handlers e a contagem zero de gates de método prova que nenhum relaxa o gate herdado; `createTenant_comRoleAdminDeTenantNormalERecusadoAntesDeOMetodoCorrer` prova a recusa em runtime |
| T-123-03 | Information Disclosure | campos `tenant_*` de `GET /api/v1/auth/me` (endpoint tenant-scoped comum que devolve dados de tenant) | accept | Own-tenant-only: o `Tenant` é resolvido a partir de `UserPrincipal.getTenantId()` (JWT), nunca de um id do cliente; não expõe lista de tenants nem `utilizadoresAtivos` de terceiros. Registado como traçado-e-dispositionado na tabela de veredito, nunca omitido |
| T-123-04 | Elevation of Privilege | público `POST /api/v1/setup/initialize` → `SetupService.initializeSystem` escreve `Tenant` sem gate de papel | mitigate | Confirmar o gate singleton em dois níveis por execução de `SetupControllerSingletonRegressaoTest` (`isInitialized()` no controller + `throw` sobre `settings.getInitialized()` no serviço, com `findByIdForUpdate` a serializar corridas concorrentes); nomear o mecanismo explicitamente na auditoria em vez de deixar o leitor assumir gating por papel |
| T-123-05 | Spoofing | render fail-open de `/plataforma` e `/plataforma/relatorio` durante o carregamento de `useMe()` | mitigate | Confirmar por leitura que ambos os ecrãs executam `if (!me.isFetched) return null;` **antes** de `roles.includes("PLATAFORMA_ADMIN")` — a correcção WR-03 da Phase 120; o backend continua a recusar com 403 (defesa em profundidade) |
| T-123-06 | Repudiation | veredito de auditoria não reproduzível ou não auditável | mitigate | Cada linha de veredito carrega o comando literal e o valor observado numa secção de comandos de reprodução dedicada; divergências esperado-vs-observado escritas com o formato de "Nota de transparência" do precedente, nunca suavizadas |
| T-123-SC | Tampering | instalações npm/pip/cargo | accept | Zero instalações de pacotes neste plano — é auditoria e documentação. O gate de legitimidade de pacotes não é acionado; asserido por `git status --porcelain -- backend web` vazio (o que também cobre `package.json`/`pom.xml`) |
</threat_model>

<verification>
- `cd backend && mvn -q test -Dtest=PlatformAdminControllerTest,SetupControllerSingletonRegressaoTest,SetupServiceProvisionTenantTest` → `BUILD SUCCESS`
- `123-ISOL-AUDIT.md` existe, ≥70 linhas, com secção do Critério 1, ≥11 linhas de tabela, ≥11 vereditos `COVERED`, e secção de comandos de reprodução
- `git status --porcelain -- backend web` vazio antes e depois do plano — a auditoria não altera código de produção
- Nenhum item da tabela do Critério 1 tem veredito subjetivo; todos são `COVERED` ou um fix descrito
</verification>

<success_criteria>
1. O Critério de Sucesso 1 do ROADMAP (consola + relatório expõem dados só via endpoints gated a `PLATAFORMA_ADMIN`, nunca via endpoint tenant-scoped comum) está re-derivado por execução independente e tem veredito explícito por superfície escrito em `123-ISOL-AUDIT.md`.
2. Os dois caminhos que **não** são gated por papel (`/auth/me` own-tenant; `/setup/initialize` singleton) estão nomeados na auditoria com o seu mecanismo real, em vez de ficarem omitidos por serem "óbvios".
3. Zero alterações a ficheiros sob `backend/` ou `web/`.
4. O ficheiro está estruturado para o Plano 02 anexar o Critério 2, a decisão do Pitfall 1 e o veredito final sem reescrever nada.
</success_criteria>

<output>
Create `.planning/phases/LEXCV-123-auditoria-de-isolamento-dedicada/123-01-SUMMARY.md` when done
</output>
