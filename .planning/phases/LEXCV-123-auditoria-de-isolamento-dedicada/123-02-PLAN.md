---
phase: 123-auditoria-de-isolamento-dedicada
plan: 02
type: execute
wave: 2
depends_on: ["123-01"]
files_modified:
  - .planning/phases/LEXCV-123-auditoria-de-isolamento-dedicada/123-ISOL-AUDIT.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [ISOL-04]

must_haves:
  truths:
    - "A auditoria afirma, com prova executável, que AdminController.updateRbac é o único call site HTTP-alcançável em toda a base de código que escreve as tabelas globais Role/Permission"
    - "O handler morto web/src/app/_api-backup/v1/admin/rbac/route.ts aparece na auditoria como traçado-e-descartado, com provas independentes de inalcançabilidade, nunca omitido por ser óbvio"
    - "A escrita per-user de permissões em AdminController.updateUser está traçada e dispositionada como own-tenant-only, distinta de uma escrita da matriz global"
    - "A decisão sobre o Pitfall 1 (findByXxxId sem tenantId) está registada como risco residual conhecido e aceite, com as 4 razões e a recomendação de uma fase dedicada futura"
    - "A auditoria termina com um veredito nomeado para cada uma das 3 superfícies e uma declaração explícita da pré-condição de provisionar um 2º tenant pagante real"
    - "ISOL-04 está marcado como concluído em REQUIREMENTS.md, na checklist e na tabela de rastreabilidade"
    - "Nenhum ficheiro sob backend/ ou web/ foi alterado por toda a Fase 123"
  artifacts:
    - path: ".planning/phases/LEXCV-123-auditoria-de-isolamento-dedicada/123-ISOL-AUDIT.md"
      provides: "Veredito do Critério 2, achado do handler morto, decisão do Pitfall 1, evidência ao vivo citada, veredito final por superfície e declaração de go/no-go"
      min_lines: 150
      contains: "_api-backup"
    - path: ".planning/REQUIREMENTS.md"
      provides: "ISOL-04 fechado na checklist e na tabela de rastreabilidade"
      contains: "ISOL-04"
  key_links:
    - from: "123-ISOL-AUDIT.md"
      to: "backend/src/main/java/com/lexcv/controllers/AdminController.java"
      via: "linha de veredito citando updateRbac como único caminho de escrita HTTP-alcançável de Role/Permission"
      pattern: "updateRbac"
    - from: "123-ISOL-AUDIT.md"
      to: "web/.next/app-path-routes-manifest.json"
      via: "prova de que zero rotas de API constam do manifesto de rotas construído"
      pattern: "app-path-routes-manifest"
    - from: ".planning/REQUIREMENTS.md"
      to: "123-ISOL-AUDIT.md"
      via: "linha de rastreabilidade de ISOL-04 a apontar para o ficheiro de auditoria"
      pattern: "ISOL-04"
---

<objective>
Reconfirmar por execução o **Critério de Sucesso 2** da Fase 123 (o bloqueio de `PUT /api/v1/admin/rbac` não tem via de contorno — nenhum outro endpoint tenant-facing continua a permitir escrever `Role`/`Permission`), fechar a decisão pendente sobre o Pitfall 1 que a Fase 121 nomeou esta fase como dona, e escrever o veredito final por superfície que satisfaz o **Critério de Sucesso 3**. Fechar ISOL-04.

Purpose: Este é o último plano do marco v2.16. A sua saída é a porta que o utilizador atravessa para provisionar um 2º tenant pagante real com uma pré-condição documentada em vez de um pressentimento. Duas coisas nesta fase existem especificamente para não serem varridas para debaixo do tapete: o handler morto em `_api-backup` (parece uma via de contorno tenant-facing de escrita de RBAC à primeira vista) e a decisão sobre o Pitfall 1 (que a Fase 121 recusou tomar e passou explicitamente para aqui).
Output: `123-ISOL-AUDIT.md` completo (secções do Critério 2 anexadas às do Critério 1 do Plano 01) e `REQUIREMENTS.md` com ISOL-04 fechado. **Zero alterações de código de produção esperadas.**
</objective>

<execution_context>
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/LEXCV-123-auditoria-de-isolamento-dedicada/123-CONTEXT.md

# Saída do Plano 01 — este plano ANEXA-LHE, não a reescreve
@.planning/phases/LEXCV-123-auditoria-de-isolamento-dedicada/123-ISOL-AUDIT.md

# A Fase 121 nomeou esta fase como dona da decisão sobre o Pitfall 1 — ver a secção
# "Registado mas explicitamente fora de âmbito para ISOL-02" e a secção "Assimetria observada em /admin/rbac"
@.planning/phases/LEXCV-121-fechar-suposi-es-de-tenant-nica-bloqueio-de-rbac/121-ISOL-AUDIT.md
</context>

<known_traps>
Aplicam-se as mesmas precauções de ambiente registadas no Plano 01 (`123-01-PLAN.md`, secção `<known_traps>`) — em resumo: em padrões `grep -E` usar `[(]`, nunca `\(`; separar comandos por `;` porque `grep -c` que devolve `0` sai com código 1; contar comentários como possíveis falsos positivos e confirmar cada ocorrência por leitura da linha.

Precaução adicional específica deste plano: **não criar nenhum gate de verificação que exija a ausência de uma palavra que o próprio documento de auditoria contém.** Concretamente, `grep -c 'FIXED'` sobre `123-ISOL-AUDIT.md` NUNCA pode ser asserido como `0` — a auditoria discute vereditos e o precedente `121-ISOL-AUDIT.md` contém literalmente a frase "Zero linhas `FIXED`". Registar contagens de fixes de forma positiva (`Fixes aplicados: 0`). Este codebase já registou esta exacta classe de erro seis vezes em `STATE.md` (Fases 119-04, 120-01, 120-02, 121-01, 121-03, 122).
</known_traps>

<tasks>

<task type="auto">
  <name>Task 1: Provar por execução que a escrita da matriz RBAC não tem via de contorno HTTP-alcançável</name>
  <files>(nenhum — task de auditoria por execução de comando e leitura direta; escreve zero ficheiros. As conclusões passam para a Task 2. `git status --porcelain -- backend web` tem de ficar vazio.)</files>

  <read_first>
    - backend/src/main/java/com/lexcv/controllers/AdminController.java — NÃO ler as 444 linhas. Ler a região do RBAC (`sed -n '340,444p'`, que cobre `getRbac` e `updateRbac`), a região do override de permissões per-user (`sed -n '270,330p'`, que cobre `updateUser`), e o cabeçalho da classe (`sed -n '20,40p'`, o gate de classe)
    - web/src/app/_api-backup/v1/admin/rbac/route.ts (o handler morto — ler na íntegra, tem ~55 linhas)
    - web/src/lib/api.ts (55 linhas — como `apiFetch` constrói cada URL)
    - web/next.config.ts (o rewrite `/api/v1/:path*` → `BACKEND_API_ORIGIN`)
    - backend/src/main/java/com/lexcv/models/Role.java e backend/src/main/java/com/lexcv/models/Permission.java (confirmar ausência de coluna de tenant — tabelas globais de plataforma)
    - backend/src/main/java/com/lexcv/models/User.java — apenas os campos `tenantId` e `permissions` (`grep -nE 'tenantId|permissions'`)
  </read_first>

  <action>
Auditar as quatro perguntas abaixo por execução mais leitura das linhas devolvidas. Registar comando, valor observado e disposição em notas de trabalho (o ficheiro escreve-se na Task 2).

**(A) Enumerar TODA a escrita das tabelas globais `Role`/`Permission`.** Não procurar apenas `updateRbac` — enumerar exaustivamente e dar disposição a cada ocorrência:
```
grep -rnE 'roleRepository[.](save|delete|saveAll|deleteAll)|permissionRepository[.](save|delete|saveAll|deleteAll)' --include=*.java backend/src/main/java
grep -rnE '[.]setPermissions[(]' --include=*.java backend/src/main/java
```
Valores esperados no momento do planeamento: 3 ocorrências de `roleRepository.save` — `AdminController.java:438` (dentro de `updateRbac`) e `DatabaseSeeder.java:377,381`; zero ocorrências de qualquer escrita de `permissionRepository`; 2 ocorrências de `setPermissions` — `AdminController.java:320` e `:437`.

Dar disposição a cada uma:
- `AdminController:438` + `:437` (`role.setPermissions`) — dentro de `updateRbac`, gated por `@PreAuthorize("hasRole('PLATAFORMA_ADMIN')")` ao nível de método (ISOL-03, Phase 121). Confirmar o valor literal da anotação e que ela está adjacente a `updateRbac` e **não** a `getRbac`.
- `DatabaseSeeder:377,381` — código de arranque. Confirmar por leitura que `DatabaseSeeder` não tem nenhuma anotação de mapping HTTP e não é um `@RestController`/`@Controller` (`grep -nE '@RestController|@Controller|Mapping' DatabaseSeeder.java` deve devolver 0 mappings), pelo que não é alcançável por pedido.
- `AdminController:320` (`user.setPermissions`) — **este é um achado material que a auditoria tem de nomear.** Escreve `User.permissions` (um `Set<String>` na própria linha do utilizador), não a matriz global `Role`→`Permission`. Confirmar por leitura de `updateUser` que: (1) é tenant-scoped — `!user.getTenantId().equals(principal.getTenantId())` devolve 404 antes de qualquer mutação; (2) tem a guarda de contenção de `PLATAFORMA_ADMIN` da Phase 119 (CR-01), que devolve 403 antes de qualquer `userRepository.save`. Disposição esperada: COVERED / override per-user own-tenant, **não** uma via de contorno do bloqueio da matriz — mas registado explicitamente, porque um leitor que veja "um ADMIN de tenant ainda consegue escrever permissões" sem esta distinção concluiria erradamente que o bloqueio ficou incompleto.

Reforçar por execução dos testes que provam os gates de autorização em runtime (todos são testes Mockito puros — rápidos):
```
cd backend && mvn -q test -Dtest=AdminControllerRbacAutorizacaoTest,AdminControllerPlataformaAdminContencaoTest
```

**(B) Confirmar que `Role`/`Permission` são tabelas globais de plataforma, não per-tenant.** Ler `Role.java` e `Permission.java` na íntegra e confirmar ausência de qualquer campo/coluna de tenant. Isto é o que torna o bloqueio a `PLATAFORMA_ADMIN` a mitigação correcta (uma escrita por um tenant afetaria todos) — e é a razão pela qual `getRbac` foi alargado a `hasRole('ADMIN') or hasRole('PLATAFORMA_ADMIN')` pelo CR-01 da Phase 121. Confirmar o valor literal actual da anotação de `getRbac` e registá-lo: a assimetria `GET`/`PUT` que `121-ISOL-AUDIT.md` documentou foi subsequentemente alterada por esse CR-01, pelo que a auditoria desta fase tem de registar o **estado actual lido**, não o estado que o precedente descreve.

**(C) Traçar e descartar o handler morto de RBAC no frontend.** `web/src/app/_api-backup/v1/admin/rbac/route.ts` exporta `GET` e `PUT` que escrevem `mockDb.rolePermissions` verificando apenas `ctx.roles.includes("ADMIN")` — à primeira vista, exactamente a via de contorno tenant-facing que o Critério 2 exclui. Provar a inalcançabilidade por três evidências independentes, para que nenhuma delas seja o único ponto de falha do argumento:

1. **Manifesto de rotas construído.** O prefixo `_` faz o Next.js App Router tratar `_api-backup` como pasta privada e excluir toda a subárvore do routing. Provar pelo artefacto de build, não pela documentação:
```
node -e "const m=require('./web/.next/app-path-routes-manifest.json'); const k=Object.keys(m); console.log('rotas:',k.length,'| rotas de api:',k.filter(x=>x.includes('api')).length)"
grep -c '_api-backup' web/.next/app-path-routes-manifest.json
grep -c 'admin/rbac' web/.next/app-path-routes-manifest.json
```
Esperado: 36 rotas, **0** rotas de API, **0** ocorrências de `_api-backup`, **0** ocorrências de `admin/rbac`. Confirmar a proveniência do manifesto antes de o citar: `stat -c '%y %n' web/.next/app-path-routes-manifest.json web/src/app/_api-backup/v1/admin/rbac/route.ts` e `git log -1 --format='%h %ad %s' --date=short -- web/src/app/_api-backup`. Se o manifesto for mais antigo do que a última alteração a `_api-backup`, correr `cd web && pnpm build` para o regenerar antes de o usar como prova. (No momento do planeamento o manifesto era de 2026-07-30 e `_api-backup` não é tocado desde 2026-05-27, commit `40008dc8` — o manifesto reflete o código actual.)

2. **Nenhum cliente HTTP desta app alcança rotas internas do Next.js.** `web/src/lib/api.ts` constrói toda e qualquer URL como `${API_BASE}${path}` onde `API_BASE` é `NEXT_PUBLIC_API_BASE_PATH` (`/api/v1`), e `next.config.ts` reescreve `/api/v1/:path*` para `${BACKEND_API_ORIGIN}/api/v1/:path*`. Confirmar por leitura de ambos os ficheiros e por contagem:
```
grep -nE 'API_BASE|fetch[(]' web/src/lib/api.ts
grep -rn '_api-backup' web/src --include=*.ts --include=*.tsx
```
Esperado: **0** referências a `_api-backup` em qualquer ficheiro vivo sob `web/src` fora da própria subárvore `_api-backup`.

3. **Dependências mortas.** O handler importa `@/server/mock-db` e `@/server/request-auth` — a implementação simulada pré-backend que o `CLAUDE.md` deste projeto classifica como legado a ignorar. Confirmar que `mockDb.rolePermissions` não tem qualquer ligação à base de dados PostgreSQL nem às tabelas `Role`/`Permission` do Spring, pelo que mesmo uma execução hipotética deste handler não escreveria nada de real.

Registar o handler como **traçado e descartado** com as 3 provas, e acrescentar a recomendação de limpeza técnica opcional (remover a subárvore `_api-backup`, 33 ficheiros `route.ts`) como diferida — está explicitamente em `123-CONTEXT.md` > Deferred Ideas, pelo que **não** a executar nesta fase.

**(D) Varredura negativa por outras superfícies de escrita de RBAC.** Confirmar que `PlatformAdminController` não tem nenhum endpoint de RBAC próprio e que nenhum outro controller expõe escrita de papéis/permissões:
```
grep -rn 'rbac' --include=*.java backend/src/main/java
grep -rniE 'rolePermissions|systemPermissions' web/src --include=*.ts --include=*.tsx
```
Dar disposição a cada ocorrência devolvida. Esperado: as ocorrências backend concentradas em `AdminController` (`getRbac`/`updateRbac`); as ocorrências frontend em `RbacTab`/`use-admin.ts` (consumo de leitura mais o `handleSave` já condicionado a `PLATAFORMA_ADMIN` pela Phase 121, coberto pelo gate executável `pnpm verify:bloqueio-rbac`). Correr esse gate como evidência adicional de não-regressão:
```
cd web && pnpm verify:bloqueio-rbac
```

Não alterar nenhum ficheiro sob `backend/` ou `web/`. Se a varredura revelar um segundo caminho HTTP-alcançável de escrita de `Role`/`Permission`, **parar e reportar como achado** — seria uma falha real do Critério 2 e uma mudança material de âmbito que o orquestrador tem de decidir, não algo a corrigir em silêncio numa fase de auditoria.
  </action>

  <acceptance_criteria>
    - `grep -rnE 'roleRepository[.](save|delete|saveAll|deleteAll)|permissionRepository[.](save|delete|saveAll|deleteAll)' --include=*.java backend/src/main/java` devolve exactamente um ficheiro de controller, e é `AdminController.java`, e a linha está dentro do corpo de `updateRbac`
    - A anotação imediatamente adjacente a `updateRbac` contém a string literal `hasRole('PLATAFORMA_ADMIN')`; a anotação adjacente a `getRbac` é lida e o seu valor literal actual é registado
    - `grep -nE '@RestController|@Controller' backend/src/main/java/com/lexcv/seed/DatabaseSeeder.java` devolve 0 linhas (o seeder não é alcançável por HTTP)
    - `Role.java` e `Permission.java` confirmados por leitura integral sem qualquer campo de tenant
    - `AdminController.updateUser` confirmado por leitura a devolver 404 quando `!user.getTenantId().equals(principal.getTenantId())`, antes de qualquer mutação
    - `node -e "...app-path-routes-manifest.json..."` reporta 0 rotas de API; `grep -c '_api-backup'` e `grep -c 'admin/rbac'` sobre o manifesto devolvem ambos `0`
    - `grep -rn '_api-backup' web/src --include=*.ts --include=*.tsx` devolve 0 linhas fora da própria subárvore `_api-backup`
    - `mvn -q test -Dtest=AdminControllerRbacAutorizacaoTest,AdminControllerPlataformaAdminContencaoTest` termina em `BUILD SUCCESS`
    - `cd web && pnpm verify:bloqueio-rbac` termina com todas as asserções a passar
    - `git status --porcelain -- backend web` devolve vazio no fim da task
  </acceptance_criteria>

  <verify>
    <automated>cd backend && mvn -q test -Dtest=AdminControllerRbacAutorizacaoTest,AdminControllerPlataformaAdminContencaoTest ; cd ../web && pnpm verify:bloqueio-rbac ; cd .. ; node -e "const m=require('./web/.next/app-path-routes-manifest.json'); const n=Object.keys(m).filter(x=>x.includes('api')).length; if(n!==0){console.error('ROTAS DE API NO MANIFESTO:',n);process.exit(1)} console.log('MANIFEST-0-API-OK')" ; test -z "$(git status --porcelain -- backend web)" && echo "ZERO-DIFF-OK"</automated>
  </verify>

  <done>Enumeração exaustiva concluída: `AdminController.updateRbac` confirmado como o único call site HTTP-alcançável que escreve as tabelas globais `Role`/`Permission`, com o seu gate de método literal verificado; `DatabaseSeeder` confirmado inalcançável por HTTP; o override per-user de `AdminController.updateUser` traçado e dispositionado como own-tenant-only distinto da matriz global; o handler morto `_api-backup/v1/admin/rbac/route.ts` provado inalcançável por 3 evidências independentes com a proveniência do manifesto confirmada. Zero ficheiros de produção alterados.</done>
</task>

<task type="auto">
  <name>Task 2: Anexar o veredito do Critério 2, a decisão do Pitfall 1 e o veredito final por superfície</name>
  <files>.planning/phases/LEXCV-123-auditoria-de-isolamento-dedicada/123-ISOL-AUDIT.md (anexar — preservar intactas as secções do Critério 1 escritas pelo Plano 01)</files>

  <read_first>
    - .planning/phases/LEXCV-123-auditoria-de-isolamento-dedicada/123-ISOL-AUDIT.md (a saída do Plano 01 — anexar, não reescrever)
    - .planning/research/PITFALLS.md — apenas a região do Pitfall 1 (`sed -n '39,64p'`) e a linha do baseline sobre `Honorario` sem `tenant_id` (`sed -n '12,12p'`)
    - .planning/phases/LEXCV-121-fechar-suposi-es-de-tenant-nica-bloqueio-de-rbac/121-ISOL-AUDIT.md — as secções "Registado mas explicitamente fora de âmbito para ISOL-02" e "Assimetria observada em /admin/rbac"
    - .planning/phases/LEXCV-123-auditoria-de-isolamento-dedicada/123-CONTEXT.md — a decisão trancada sobre o Pitfall 1 (as 4 razões, para as reproduzir fielmente)
    - .planning/ROADMAP.md — apenas a secção `#### Phase 123` (os 3 Critérios de Sucesso, para citar textualmente)
  </read_first>

  <action>
Anexar a `123-ISOL-AUDIT.md` (sem alterar nada do que o Plano 01 escreveu) as seguintes secções, no mesmo formato do precedente `121-ISOL-AUDIT.md`:

1. **`## Critério de Sucesso 2 — Veredito por superfície`**, abrindo com a citação textual do critério do ROADMAP, seguida de uma tabela `| Query / Guard | Scope confirmed | Verdict |` com uma linha por item auditado na Task 1. Cobrir no mínimo:
   - `AdminController.updateRbac` (`PUT /api/v1/admin/rbac`) — o único caminho de escrita HTTP-alcançável, com o valor literal do gate de método e a linha do `roleRepository.save`
   - `AdminController.getRbac` (`GET /api/v1/admin/rbac`) — o valor literal **actual** da anotação lido nesta fase; registar explicitamente que o CR-01 da Phase 121 alargou este gate depois de `121-ISOL-AUDIT.md` ter documentado a assimetria `GET`/`PUT`, pelo que o precedente está desactualizado nesse ponto e esta auditoria substitui-o
   - `AdminController.updateUser` (`PUT /api/v1/admin/users/{id}`) `user.setPermissions` — override per-user, tenant-scoped por 404, com a guarda de contenção de `PLATAFORMA_ADMIN`; explicar por que **não** é uma via de contorno da matriz global
   - `DatabaseSeeder` `roleRepository.save` (2 linhas) — código de arranque, zero mappings HTTP
   - `Role` / `Permission` (entidades JPA) — tabelas globais de plataforma sem coluna de tenant, o que justifica o gate a `PLATAFORMA_ADMIN`
   - `PlatformAdminController` — confirmado sem nenhum endpoint de RBAC próprio
   - `RbacTab` / `use-admin.ts` no frontend — leitura mais `handleSave` condicionado a `PLATAFORMA_ADMIN`, com o resultado de `pnpm verify:bloqueio-rbac`

2. **`## Achado traçado e descartado — handler morto de RBAC em _api-backup`**. Esta secção é obrigatória e não pode ser reduzida a uma nota de rodapé (decisão trancada em `123-CONTEXT.md`: "deve entrar na auditoria como traçado e descartado... não silenciosamente ignorado por ser óbvio"). Escrever: o que o handler faz (exporta `GET` e `PUT`, escreve `mockDb.rolePermissions`, verifica apenas `ctx.roles.includes("ADMIN")` sem distinção de `PLATAFORMA_ADMIN`), por que parece uma via de contorno tenant-facing, e as **3 provas independentes** de inalcançabilidade com os comandos e valores observados: (1) o manifesto de rotas construído com 0 rotas de API e 0 ocorrências de `_api-backup`/`admin/rbac`, incluindo a nota de proveniência (data do manifesto vs. data da última alteração ao ficheiro); (2) `apiFetch` construir toda a URL a partir de `NEXT_PUBLIC_API_BASE_PATH` que `next.config.ts` reescreve para o backend Spring, mais 0 referências vivas a `_api-backup` em `web/src`; (3) as dependências `mockDb`/`request-auth` serem a implementação simulada pré-backend, sem qualquer ligação às tabelas PostgreSQL. Terminar com a recomendação de limpeza técnica **opcional e diferida** (remover a subárvore, 33 ficheiros `route.ts`), marcada como não executada nesta fase por decisão explícita.

3. **`## Decisão sobre o Pitfall 1 — findByXxxId sem tenantId (risco residual conhecido e aceite)`**. `121-ISOL-AUDIT.md` nomeou esta fase como dona desta decisão pendente; ela está tomada em `123-CONTEXT.md` e tem de ser reproduzida fielmente, não reinterpretada. Escrever: o padrão (vários métodos de repositório, ex. `ProcessoRepository.findByClienteId`, ~11 métodos, sem parâmetro `tenantId` na própria query; `Honorario` sem coluna `tenant_id` própria, ligado apenas por `processo_id`); o veredito — **aceite como risco residual conhecido**, com a dupla verificação em cada call site como mitigação suficiente por agora, explicitamente **não** "corrigido" e explicitamente **não** "fora de âmbito silenciosamente ignorado"; e as 4 razões tal como registadas em `123-CONTEXT.md`:
   (i) padrão pré-existente há várias milestones, não introduzido nem agravado por esta milestone;
   (ii) cada call site já confirmado a fazer a reverificação corretamente, zero indícios de omissão;
   (iii) corrigir por completo exigiria alterar a assinatura de ~11 métodos de repositório e todos os call sites — refactor grande com risco de regressão real, fora do âmbito desta milestone;
   (iv) não bloqueia a criação segura de um 2º tenant pagante real — o risco existe hoje independentemente do número de tenants, e nenhuma das 3 superfícies novas o agrava.
   Terminar com a recomendação clara de que a eliminação completa deste padrão exige uma fase dedicada de refactor de repositórios, matéria para uma milestone futura.

4. **`## Evidência ao vivo já reunida (citada, não repetida)`**. Justificar por escrito a decisão de não repetir UAT ao vivo, e citar a evidência HTTP real já recolhida nas fases das próprias superfícies: Phase 120 Plano 06 (`403` confirmado para um ADMIN de escritório nos 3 endpoints de plataforma, mais o corte de uma sessão já aberta ao suspender um tenant), Phase 121 Plano 04 (`403` real de um ADMIN de escritório no `PUT /admin/rbac` **e** `200` de um `PLATAFORMA_ADMIN` no mesmo endpoint — o contra-teste que exclui um bloqueio universal acidental), Phase 122 Plano 04 (`200`/`403` no relatório mais o tenant suspenso presente na resposta). Registar as duas razões pelas quais a re-verificação estática mais automatizada é suficiente aqui: (a) os gates estão sob cobertura de regressão automatizada permanente (`PlatformAdminControllerTest`, `AdminControllerRbacAutorizacaoTest`, `AdminControllerPlataformaAdminContencaoTest`, `JwtAuthenticationFilterTenantSuspensoTest`, `AuthControllerTenantSuspensoTest`), ao contrário de um `curl` de uma só vez; (b) precedente directo da AUD-01 (`97-01-SUMMARY.md`), que saltou a sonda cross-tenant ao vivo sob a mesma cláusula de discrição e foi aceite. Registar também, com honestidade, que a Fase 122 fechou com um override formal do utilizador para 6 cenários visuais bloqueados por uma falha de ferramenta (Browser MCP) — não uma falha do produto —, e que repetir esse trabalho aqui provavelmente reencontraria o mesmo bloqueio por zero informação nova.

5. **`## Veredito final por superfície`** — uma tabela final com exactamente uma linha por superfície nova da v2.16, cada uma com veredito nomeado: consola de administração de tenants (Phase 120), relatório de utilização (Phase 122), bloqueio de RBAC (Phase 121). Acrescentar a linha de `StorageService`, que o ROADMAP declara fora de âmbito (particionamento por `tenantId` já existente, sem alterações necessárias) — confirmar essa afirmação por um grep rápido de `tenantId` em `StorageService.java` em vez de a copiar por fé. Contagem de fixes escrita de forma positiva (`Fixes aplicados: 0`), nunca como uma asserção de ausência de palavra.

6. **`## Pré-condição para provisionar um 2º tenant pagante real`** — o Critério de Sucesso 3 exige que o veredito esteja "documentado antes de se considerar segura a criação de um 2º tenant pagante real fora de teste". Escrever uma declaração explícita de go/no-go que inclua: as pré-condições de deployment já registadas em `STATE.md` que continuam a aplicar-se (nomeadamente as migrações manuais pendentes `117-add-tenant-plano-limite-utilizadores.sql` e `120-add-tenant-ativo.sql`, mais o aviso já registado de que a `120b-backfill-tenant-plano.sql` foi corrida só nesta base de dados de desenvolvimento e que um deployment de produção que a salte devolve `500` na primeira suspensão de tenant contra dados pré-existentes), e a restrição de ordem do Risco da Phase 121 (não usar a consola da Phase 120 em produção para um 2º tenant real antes de a Phase 121 estar também em produção). Terminar com uma secção curta **`### O que esta auditoria NÃO cobriu`**, enumerando as fronteiras de âmbito (o padrão do Pitfall 1 em todo o codebase histórico; a segurança de infraestrutura/rede; a limpeza de `_api-backup`), para que nenhum leitor sobre-interprete o veredito.
  </action>

  <acceptance_criteria>
    - `123-ISOL-AUDIT.md` tem pelo menos 150 linhas e conserva intactas as secções do Critério 1 escritas pelo Plano 01 (a string `Critério de Sucesso 1` continua presente)
    - Todas estas 6 secções existem no documento: `Critério de Sucesso 2`, `_api-backup`, `Pitfall 1`, `Evidência ao vivo`, `Veredito final por superfície`, `Pré-condição`
    - `grep -c 'updateRbac' 123-ISOL-AUDIT.md` devolve pelo menos `2`
    - `grep -c 'app-path-routes-manifest' 123-ISOL-AUDIT.md` devolve pelo menos `1` (a prova do manifesto está citada, não parafraseada)
    - A secção do Pitfall 1 contém as 4 razões numeradas e a expressão literal `risco residual`
    - A tabela do veredito final tem uma linha nomeada para cada uma das 3 superfícies: as strings `Phase 120`, `Phase 121` e `Phase 122` aparecem todas nessa secção
    - `grep -c 'Fixes aplicados' 123-ISOL-AUDIT.md` devolve pelo menos `1`
    - O documento contém a secção `O que esta auditoria NÃO cobriu`
    - Nenhuma asserção de verificação criada por esta task exige `grep -c 'FIXED' == 0` sobre o documento
    - `git status --porcelain -- backend web` devolve vazio
  </acceptance_criteria>

  <verify>
    <automated>D=.planning/phases/LEXCV-123-auditoria-de-isolamento-dedicada/123-ISOL-AUDIT.md; test "$(grep -c '' $D)" -ge 150 || exit 1; for t in "Critério de Sucesso 1" "Critério de Sucesso 2" "_api-backup" "Pitfall 1" "risco residual" "Evidência ao vivo" "Veredito final por superfície" "Pré-condição" "app-path-routes-manifest" "Fixes aplicados" "NÃO cobriu" "Phase 120" "Phase 121" "Phase 122"; do grep -q "$t" $D || { echo "FALTA: $t"; exit 1; }; done; test "$(grep -c 'updateRbac' $D)" -ge 2 || exit 1; test -z "$(git status --porcelain -- backend web)" || exit 1; echo "AUDIT-C2-OK"</automated>
  </verify>

  <done>`123-ISOL-AUDIT.md` completo: veredito por superfície do Critério 2 com o `updateRbac` provado como único caminho de escrita HTTP-alcançável, o handler morto registado como traçado-e-descartado com 3 provas, a decisão do Pitfall 1 registada como risco residual aceite com as 4 razões e a recomendação de fase futura, a evidência ao vivo das Fases 120/121/122 citada com a justificação de não a repetir, o veredito final nomeado para as 3 superfícies, e a declaração de pré-condição de go/no-go com as fronteiras de âmbito. Zero ficheiros de produção alterados.</done>
</task>

<task type="auto">
  <name>Task 3: Fechar ISOL-04 e provar zero alterações de produção em toda a fase</name>
  <files>.planning/REQUIREMENTS.md (2 linhas alteradas: a checklist de ISOL-04 e a sua linha na tabela de rastreabilidade)</files>

  <read_first>
    - .planning/REQUIREMENTS.md — a linha da checklist de ISOL-04 (`grep -n 'ISOL-04'`, esperado nas linhas ~33 e ~82) e a tabela de rastreabilidade
    - .planning/phases/LEXCV-123-auditoria-de-isolamento-dedicada/123-ISOL-AUDIT.md (o documento final, para o cross-check)
    - .planning/ROADMAP.md — apenas a secção `#### Phase 123` (os 3 Critérios de Sucesso)
  </read_first>

  <action>
Três passos, nesta ordem — o fecho do requisito é o último, e só depois de o cross-check passar.

**(A) Cross-check de cobertura dos critérios de sucesso.** Confirmar que cada um dos 3 Critérios de Sucesso da Fase 123 tem uma linha de veredito correspondente em `123-ISOL-AUDIT.md`, não apenas uma menção em prosa:
- Critério 1 (consola + relatório só via endpoints gated a `PLATAFORMA_ADMIN`) → a tabela do Critério 1 do Plano 01
- Critério 2 (bloqueio de `PUT /admin/rbac` sem via de contorno) → a tabela do Critério 2 da Task 2
- Critério 3 (veredito explícito por superfície, documentado antes de se considerar segura a criação de um 2º tenant pagante real) → a tabela do veredito final mais a secção de pré-condição

Se algum critério não tiver linha de veredito, corrigir o documento nesta task antes de continuar. Este cross-check existe porque o marco tem precedente de um critério ficar coberto em prosa mas sem veredito nomeado.

**(B) Prova de zero diff de produção em toda a fase.** Esta fase declarou-se, à partida, sem alterações de código de produção. Provar, não afirmar:
```
git status --porcelain -- backend web
git diff --stat -- backend web
git diff --stat -- web/package.json backend/pom.xml
```
Os três têm de devolver vazio. Registar o resultado no SUMMARY. Se algum devolver conteúdo, **parar e reportar** — uma alteração de produção não planeada nesta fase é uma mudança material de âmbito, e o SUMMARY tem de a explicar em vez de a normalizar.

**(C) Fechar ISOL-04.** Este é o plano que legitimamente fecha ISOL-04 — o requisito exige a auditoria documentada com veredito explícito, que só existe depois da Task 2 (o mesmo padrão do Plano 05 da Phase 120 para PROV-02/PROV-05 e do Plano 03 da Phase 122 para UTIL-01: fechar apenas quando o artefacto que o requisito descreve existe de facto). Actualizar `.planning/REQUIREMENTS.md` em dois lugares:
1. A linha da checklist: `- [ ] **ISOL-04**: ...` → `- [x] **ISOL-04**: ...` (preservar o texto do requisito byte a byte)
2. A linha da tabela de rastreabilidade: `| ISOL-04 | Phase 123 | Pending |` → `| ISOL-04 | Phase 123 | Complete (auditoria de confirmação, zero alterações de código de produção — veredito por superfície em 123-ISOL-AUDIT.md; decisão sobre o Pitfall 1 registada como risco residual aceite) |`

Não tocar em nenhuma outra linha de `REQUIREMENTS.md`.
  </action>

  <acceptance_criteria>
    - `grep -c '^- \[x\] \*\*ISOL-04\*\*' .planning/REQUIREMENTS.md` devolve `1`
    - `grep -c '^- \[ \] \*\*ISOL-04\*\*' .planning/REQUIREMENTS.md` devolve `0`
    - A linha da tabela de rastreabilidade de ISOL-04 contém `Complete` e a string `123-ISOL-AUDIT.md`, e já não contém `Pending`
    - `git diff --numstat -- .planning/REQUIREMENTS.md` mostra no máximo 2 linhas adicionadas e 2 removidas (só as duas linhas de ISOL-04 alteradas)
    - `git status --porcelain -- backend web` devolve vazio; `git diff --stat -- web/package.json backend/pom.xml` devolve vazio
    - Cada um dos 3 Critérios de Sucesso da Fase 123 confirmado com uma linha de veredito correspondente em `123-ISOL-AUDIT.md`
  </acceptance_criteria>

  <verify>
    <automated>R=.planning/REQUIREMENTS.md; test "$(grep -c '^- \[x\] \*\*ISOL-04\*\*' $R)" -eq 1 || exit 1; test "$(grep -c '^- \[ \] \*\*ISOL-04\*\*' $R)" -eq 0 || exit 1; grep 'ISOL-04' $R | grep -q 'Complete' || exit 1; grep 'ISOL-04' $R | grep -q '123-ISOL-AUDIT.md' || exit 1; grep 'ISOL-04' $R | grep -q 'Pending' && { echo "AINDA PENDING"; exit 1; }; test -z "$(git status --porcelain -- backend web)" || exit 1; test -z "$(git diff --stat -- web/package.json backend/pom.xml)" || exit 1; echo "ISOL-04-FECHADO-OK"</automated>
  </verify>

  <done>Os 3 Critérios de Sucesso da Fase 123 confirmados com linha de veredito em `123-ISOL-AUDIT.md`; zero diff de produção provado por três comandos; ISOL-04 marcado `[x]` na checklist e `Complete` na tabela de rastreabilidade de `REQUIREMENTS.md`, com a referência ao ficheiro de auditoria e a nota de que a decisão do Pitfall 1 é um risco residual aceite. O marco v2.16 fica com 15/15 requisitos fechados.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tenant ADMIN → `PUT /api/v1/admin/rbac` | Fronteira central deste plano: a matriz `Role`→`Permission` é global (sem coluna de tenant), logo uma escrita por um tenant afetaria todos os outros |
| tenant ADMIN → `PUT /api/v1/admin/users/{id}` (`permissions`) | Escrita de permissões que **permanece** disponível ao ADMIN de escritório; a fronteira é `User.tenantId` mais a guarda de contenção de `PLATAFORMA_ADMIN` |
| browser → rotas internas Next.js (`web/src/app/**/route.ts`) | Fronteira que o handler morto em `_api-backup` atravessaria se fosse routável; hoje inexistente (0 rotas de API no manifesto construído) |
| esta auditoria → decisão irreversível de provisionar um 2º tenant pagante real em produção | Um veredito falso-limpo, ou sobre-interpretado, autoriza uma acção que não se desfaz |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-123-07 | Elevation of Privilege | qualquer escrita de `Role`/`Permission` alcançável por HTTP | mitigate | Enumeração exaustiva (não busca dirigida a `updateRbac`) de `roleRepository.*`/`permissionRepository.*`/`setPermissions` em todo `backend/src/main/java`, com disposição nomeada por ocorrência; gate de método de `updateRbac` confirmado pelo valor literal e por execução de `AdminControllerRbacAutorizacaoTest` |
| T-123-08 | Elevation of Privilege | `web/src/app/_api-backup/v1/admin/rbac/route.ts` — escreveria RBAC verificando apenas `roles.includes("ADMIN")`, sem distinção de `PLATAFORMA_ADMIN` | mitigate | Três provas independentes de inalcançabilidade, nenhuma como ponto único de falha: (1) `web/.next/app-path-routes-manifest.json` com 0 rotas de API e 0 ocorrências de `_api-backup`/`admin/rbac`, com proveniência do manifesto confirmada por `stat`+`git log` e rebuild se estiver desactualizado; (2) `apiFetch` constrói toda a URL a partir de `NEXT_PUBLIC_API_BASE_PATH` que `next.config.ts` reescreve para o backend Spring, mais 0 referências vivas em `web/src`; (3) as dependências `mockDb`/`request-auth` não têm ligação às tabelas PostgreSQL |
| T-123-09 | Elevation of Privilege | `AdminController.updateUser` `user.setPermissions` — via de escrita de permissões que continua aberta ao ADMIN de tenant | accept | Own-tenant-only por desenho: 404 quando `!user.getTenantId().equals(principal.getTenantId())` antes de qualquer mutação, mais a guarda de contenção de `PLATAFORMA_ADMIN` da Phase 119 (403 antes de `userRepository.save`); escreve `User.permissions`, nunca a matriz global. Registado explicitamente na tabela de veredito para que não seja redescoberto como surpresa |
| T-123-10 | Information Disclosure | métodos `findByXxxId` sem `tenantId` (PITFALLS Pitfall 1); `Honorario` sem coluna `tenant_id` própria | accept | Risco residual conhecido e aceite por decisão trancada em `123-CONTEXT.md`; mitigação vigente é a dupla verificação em cada call site, confirmada em investigações anteriores desta sessão; documentado com as 4 razões e a recomendação explícita de uma fase dedicada de refactor de repositórios numa milestone futura — nunca marcado como "corrigido" nem omitido |
| T-123-11 | Repudiation | veredito falso-limpo a autorizar o provisionamento de um 2º tenant pagante real | mitigate | O documento fecha com uma declaração de pré-condição de go/no-go que inclui as migrações manuais pendentes e a restrição de ordem do Risco da Phase 121, mais uma secção `O que esta auditoria NÃO cobriu` que delimita o âmbito — impedindo que o veredito seja lido como uma garantia mais ampla do que é |
| T-123-12 | Tampering | precedente desactualizado (`121-ISOL-AUDIT.md` documenta a assimetria `GET`/`PUT` de `/admin/rbac`, alterada depois pelo CR-01 da Phase 121) | mitigate | Registar o valor literal **actual** da anotação de `getRbac` lido nesta fase e declarar explicitamente que esta auditoria substitui o precedente nesse ponto — em vez de propagar uma afirmação obsoleta por citação |
| T-123-SC | Tampering | instalações npm/pip/cargo | accept | Zero instalações de pacotes neste plano — auditoria e documentação. Gate de legitimidade não acionado; asserido pelo requisito de `git diff --stat -- web/package.json backend/pom.xml` vazio na Task 3 |
</threat_model>

<verification>
- `cd backend && mvn -q test -Dtest=AdminControllerRbacAutorizacaoTest,AdminControllerPlataformaAdminContencaoTest` → `BUILD SUCCESS`
- `cd web && pnpm verify:bloqueio-rbac` → todas as asserções passam (não-regressão do bloqueio da Phase 121)
- `web/.next/app-path-routes-manifest.json` → 0 rotas de API, 0 ocorrências de `_api-backup`, 0 de `admin/rbac`
- `123-ISOL-AUDIT.md` ≥150 linhas, com as 6 secções obrigatórias do Critério 2 e o Critério 1 do Plano 01 intacto
- ISOL-04 marcado `[x]` e `Complete` em `REQUIREMENTS.md`, com no máximo 2 linhas alteradas
- `git status --porcelain -- backend web` e `git diff --stat -- web/package.json backend/pom.xml` ambos vazios
</verification>

<success_criteria>
1. `AdminController.updateRbac` está provado por enumeração exaustiva como o único call site HTTP-alcançável que escreve as tabelas globais `Role`/`Permission` — Critério de Sucesso 2 satisfeito com veredito explícito.
2. O handler morto de `_api-backup` está na auditoria como traçado-e-descartado com 3 provas independentes, e o override per-user de permissões está dispositionado como own-tenant-only distinto da matriz global.
3. A decisão sobre o Pitfall 1 está registada como risco residual conhecido e aceite, com as 4 razões e a recomendação de uma fase dedicada futura — fechando a decisão que a Fase 121 passou explicitamente para aqui.
4. A auditoria tem um veredito nomeado para cada uma das 3 superfícies novas mais uma declaração de pré-condição de go/no-go e uma delimitação explícita de âmbito — Critério de Sucesso 3 satisfeito.
5. ISOL-04 fechado em `REQUIREMENTS.md`; zero alterações a ficheiros sob `backend/` ou `web/` em toda a Fase 123, provado por comando.
</success_criteria>

<output>
Create `.planning/phases/LEXCV-123-auditoria-de-isolamento-dedicada/123-02-SUMMARY.md` when done
</output>
