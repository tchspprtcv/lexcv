---
phase: 92-agenda-riscoprazoservice-consolida-o
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/main/java/com/lexcv/models/Evento.java
  - backend/src/main/java/com/lexcv/controllers/ResourceController.java
autonomous: true
requirements: [AGD-35]

must_haves:
  truths:
    - "GET /eventos devolve um campo risco por evento (ok/proximo/vencido) calculado no backend via RiscoPrazoService.computeRiscoEvento"
    - "Cada instância recorrente expandida recebe o seu próprio risco calculado a partir da data de ocorrência dessa instância"
    - "O endpoint órfão GET /eventos/upcoming deixa de existir no backend"
  artifacts:
    - path: "backend/src/main/java/com/lexcv/models/Evento.java"
      provides: "Campo @Transient risco no modelo Evento (serializado por Jackson, ignorado por JPA)"
      contains: "private String risco"
    - path: "backend/src/main/java/com/lexcv/controllers/ResourceController.java"
      provides: "listEventos enriquecido com risco por evento; getUpcomingEventos removido"
      contains: "setRisco(riscoPrazoService.computeRiscoEvento"
  key_links:
    - from: "ResourceController.listEventos"
      to: "riscoPrazoService.computeRiscoEvento"
      via: "setRisco por evento expandido, usando dataInicio + prioridade"
      pattern: "setRisco\\(riscoPrazoService\\.computeRiscoEvento"
---

<objective>
Fazer `GET /eventos` (o endpoint que a Agenda usa) devolver um campo `risco` por evento, calculado no backend pelo mesmo `RiscoPrazoService` (Phase 85) já usado por Prazos, dashboard e job diário — e remover o endpoint órfão `GET /eventos/upcoming`, sem consumidores desde que a Phase 89 substituiu o sino de eventos pelo sino de `Notificacao` genérica (AGD-35, decisão travada em 92-CONTEXT.md).

Purpose: eliminar a única lacuna de dados que impede a Agenda de confiar inteiramente no `RiscoPrazoService` para Eventos (Prazos já devolvem `risco`), fechando a raiz da "5ª implementação divergente de prazo crítico".
Output: campo `risco` em cada evento de `GET /eventos`; método `getUpcomingEventos` eliminado.
</objective>

<execution_context>
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/LEXCV-92-agenda-riscoprazoservice-consolida-o/92-CONTEXT.md
@.planning/research/ARCHITECTURE.md
@.planning/research/PITFALLS.md

<interfaces>
<!-- RiscoPrazoService (Phase 85) — método já existente, reutilizar diretamente. Não hand-roll. -->
<!-- backend/src/main/java/com/lexcv/services/RiscoPrazoService.java -->
String computeRiscoEvento(LocalDateTime dataEvento, String prioridade)   // wrapper (hoje = LocalDate.now())
String computeRiscoEvento(LocalDateTime dataEvento, String prioridade, LocalDate hoje)  // 3-arg, testável
// Constantes: RiscoPrazoService.OK="ok", .PROXIMO="proximo", .VENCIDO="vencido"
// Regra: dataEvento null -> "ok"; passado -> "vencido"; <=7 dias (ALTA) ou <=3 dias (restante) -> "proximo"; senão "ok"

<!-- ResourceController já injecta o service (linha ~73): private final RiscoPrazoService riscoPrazoService; -->

<!-- Padrão de referência EXATO para expor risco — como Prazos já fazem (ResourceController listPrazos/listAllPrazos, linhas ~1640/1663): -->
<!-- risco é um campo string top-level direto no item devolvido ("risco": "proximo"), NÃO um objeto aninhado. -->

<!-- getUpcomingEventos (linha ~2482) já usa o método correto: computeRiscoEvento(e.getDataInicio(), e.getPrioridade()) -->
<!-- Esse método é o padrão a replicar em listEventos, POR DECISÃO TRAVADA (92-CONTEXT.md: "o mesmo método já usado em /eventos/upcoming"). -->

<!-- Evento model já tem 2 campos @Transient serializados por Jackson e consumidos pelo frontend -->
<!-- (isRecurrenceInstance, recurrenceInstanceDate, linhas 55-59) — provam que @Transient + getter Lombok serializa. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Adicionar campo risco ao payload de GET /eventos</name>
  <files>backend/src/main/java/com/lexcv/models/Evento.java, backend/src/main/java/com/lexcv/controllers/ResourceController.java</files>
  <read_first>
    - backend/src/main/java/com/lexcv/models/Evento.java (ficheiro inteiro — os campos @Transient existentes nas linhas 55-59 são o padrão exato a copiar)
    - backend/src/main/java/com/lexcv/controllers/ResourceController.java linhas 2355-2479 (listEventos: filtros, expansão de recorrência, `return ResponseEntity.ok(expanded)`)
    - backend/src/main/java/com/lexcv/controllers/ResourceController.java linhas 2496-2504 (getUpcomingEventos: a chamada `computeRiscoEvento(e.getDataInicio(), e.getPrioridade())` a replicar)
    - backend/src/main/java/com/lexcv/controllers/ResourceController.java linhas 1630-1644 (listPrazos: padrão de risco como campo string top-level)
    - backend/src/main/java/com/lexcv/services/RiscoPrazoService.java (ficheiro inteiro — assinaturas de computeRiscoEvento)
  </read_first>
  <action>
    (AGD-35) Expor `risco` por evento em `GET /eventos` reutilizando o `RiscoPrazoService` já injetado.

    1. Em `Evento.java`, adicionar um campo `@Transient private String risco;` junto dos campos `@Transient` existentes (`isRecurrenceInstance`, `recurrenceInstanceDate`, linhas 55-59). A classe já tem `@Getter @Setter` ao nível de classe, portanto Lombok gera `getRisco()`/`setRisco(...)` automaticamente e o Jackson serializa-o como `"risco"` — exatamente como já acontece com os outros dois campos `@Transient` consumidos pelo frontend. NÃO adicionar `@Column`; `@Transient` (jakarta.persistence.Transient, já importado via `jakarta.persistence.*`) garante que o JPA o ignora na persistência e no `ddl-auto=validate` de produção.

    2. Em `ResourceController.listEventos` (linha ~2356), imediatamente ANTES do `return ResponseEntity.ok(expanded);` (linha ~2478), iterar a lista `expanded` e, para cada `Evento e`, chamar `e.setRisco(riscoPrazoService.computeRiscoEvento(e.getDataInicio(), e.getPrioridade()));`. Usar o campo `riscoPrazoService` já injetado (linha ~73) e a MESMA sobrecarga `computeRiscoEvento(LocalDateTime, String)` já usada em `getUpcomingEventos` (decisão travada 92-CONTEXT.md: "o mesmo método já usado em /eventos/upcoming"). Calcular DEPOIS da expansão de recorrência garante que cada instância expandida (que já tem o seu `dataInicio` = data da ocorrência, definido no builder das linhas ~2444-2458) recebe o risco correto para a sua própria data — não o do master.

    3. `risco` é um campo string top-level direto no evento serializado ("risco": "proximo"), espelhando o padrão de Prazos (listPrazos/listAllPrazos, `m.put("risco", risco)`), NÃO um objeto aninhado (decisão de discrição 92-CONTEXT.md: "seguir o padrão já usado por Prazos na mesma resposta"). Como é um campo JSON novo e aditivo, não quebra nenhum consumidor existente do shape atual de `GET /eventos`.

    NOTA (registar no SUMMARY, não corrigir aqui): `getUpcomingEventos` e `listEventos` usam `getDataInicio()`, mas o KPI do dashboard (`isEventoCritico`, linha ~3114) usa `getDataFim()` para o mesmo `computeRiscoEvento`. Esta divergência de argumento data-início-vs-data-fim entre Agenda e KPI do dashboard é PRÉ-EXISTENTE e fica FORA do âmbito desta fase (92-CONTEXT.md defere auditoria de consistência Dashboard/Agenda para a Phase 97). Implementar `getDataInicio()` conforme a decisão travada; apenas documentar a discrepância no SUMMARY.
  </action>
  <verify>
    <automated>cd backend && node -e "const fs=require('fs'); const stripJava=s=>s.split('\n').filter(l=>!l.trim().startsWith('//')&&!l.trim().startsWith('*')&&!l.trim().startsWith('/*')).join('\n'); const ev=stripJava(fs.readFileSync('src/main/java/com/lexcv/models/Evento.java','utf8')); const rc=stripJava(fs.readFileSync('src/main/java/com/lexcv/controllers/ResourceController.java','utf8')); const ok=/@Transient\s+private\s+String\s+risco/.test(ev) && /setRisco\(riscoPrazoService\.computeRiscoEvento\(\s*e\.getDataInicio\(\)\s*,\s*e\.getPrioridade\(\)\s*\)\)/.test(rc); process.exit(ok?0:1)"</automated>
    <automated>cd backend && mvn -q -DskipTests compile</automated>
  </verify>
  <acceptance_criteria>
    - `Evento.java` tem `@Transient private String risco;` com getter/setter Lombok (campo serializável, não persistido)
    - `listEventos` chama `e.setRisco(riscoPrazoService.computeRiscoEvento(e.getDataInicio(), e.getPrioridade()))` para cada evento em `expanded` antes de devolver
    - Nenhuma nova cópia de tabela de limiares/lógica de risco introduzida — apenas o `RiscoPrazoService` partilhado é chamado
    - `cd backend && mvn -q -DskipTests compile` compila sem erros
  </acceptance_criteria>
  <done>GET /eventos devolve `risco` por evento (incluindo instâncias recorrentes), via RiscoPrazoService, como campo string direto; compila.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Remover o endpoint órfão GET /eventos/upcoming</name>
  <files>backend/src/main/java/com/lexcv/controllers/ResourceController.java</files>
  <read_first>
    - backend/src/main/java/com/lexcv/controllers/ResourceController.java linhas 2481-2506 (o método `getUpcomingEventos` completo a eliminar)
    - backend/src/main/java/com/lexcv/controllers/ResourceController.java linhas 3100-3123 (isEventoCritico/agendaUrgentesCount — outros consumidores de computeRiscoEvento que NÃO se tocam)
    - .planning/phases/LEXCV-92-agenda-riscoprazoservice-consolida-o/92-CONTEXT.md secção "Endpoint legado"
  </read_first>
  <action>
    (AGD-35, decisão travada) Eliminar por completo o método handler `getUpcomingEventos` de `ResourceController.java` — o bloco `@PreAuthorize("hasAuthority('agenda:view')")` + `@GetMapping("/eventos/upcoming")` + corpo do método (linhas ~2481-2506, terminando no `return ResponseEntity.ok(slimList);` e respetiva chaveta de fecho).

    Este endpoint ficou órfão desde a Phase 89 (v2.10), que substituiu o sino de eventos pelo sino de `Notificacao` genérica. O seu único consumidor frontend é o hook morto `useUpcomingEventos`, removido no plano 92-02. Confirmar via grep (comando de verificação abaixo) que nenhum outro código backend referencia `/eventos/upcoming` nem `getUpcomingEventos` antes de considerar a tarefa concluída.

    NÃO remover nem alterar mais nada: `computeRiscoEvento` continua a ser usado por `listEventos` (Task 1), por `isEventoCritico` (linha ~3114) e pelos KPIs do dashboard — apagar apenas este método. NÃO tocar em `getEvento` (linha ~2509), `createEvento`, `updateEvento` nem em qualquer outro handler de `/eventos`.
  </action>
  <verify>
    <automated>cd backend && node -e "const fs=require('fs'); const stripJava=s=>s.split('\n').filter(l=>!l.trim().startsWith('//')&&!l.trim().startsWith('*')&&!l.trim().startsWith('/*')).join('\n'); const rc=stripJava(fs.readFileSync('src/main/java/com/lexcv/controllers/ResourceController.java','utf8')); const ok=!/getUpcomingEventos/.test(rc) && !/eventos\/upcoming/.test(rc); process.exit(ok?0:1)"</automated>
    <automated>cd backend && mvn -q -DskipTests compile</automated>
  </verify>
  <acceptance_criteria>
    - `getUpcomingEventos` e a string de rota `eventos/upcoming` já não existem em `ResourceController.java` (fora de comentários)
    - `computeRiscoEvento` continua a ser referenciado (por `listEventos` e `isEventoCritico`) — não foi removido em cascata
    - `cd backend && mvn -q -DskipTests compile` compila sem erros
  </acceptance_criteria>
  <done>O endpoint `/eventos/upcoming` foi removido; o resto do controlador de eventos permanece intacto e compila.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → Next rewrite → Spring API | Pedido autenticado a `GET /eventos` (gated `agenda:view`); resposta ganha um campo derivado novo |
| tenant isolation | Todos os eventos devolvidos têm de pertencer ao tenant do chamador |

## STRIDE Threat Register (ASVS L1 — security_block_on: high)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-92-01 | Information Disclosure | campo `risco` novo em GET /eventos | mitigate | `risco` é derivado APENAS de `dataInicio`+`prioridade` do próprio evento já filtrado por `eventoRepository.findByTenantId(getTenantId())` — não introduz nova fonte de dados nem cruza fronteira de tenant. O scoping por tenant de `listEventos` mantém-se inalterado. |
| T-92-02 | Denial of Service | cálculo de risco por instância recorrente | accept | `computeRiscoEvento` é O(1) por instância; a expansão de recorrência já está limitada pela janela `effectiveStart/effectiveEnd` (±1 ano) pré-existente. Adicionar uma chamada O(1) por instância já expandida é negligenciável. |
| T-92-03 | Attack surface reduction | remoção de GET /eventos/upcoming | mitigate | Remover um endpoint órfão reduz superfície de ataque; confirmado via grep que não tem consumidores. Sem impacto em endpoints remanescentes. |
| T-92-SC | Tampering | npm/pip/cargo installs | mitigate | Sem novas dependências nesta fase (reutiliza RiscoPrazoService, Jackson, JPA já presentes). Sem tarefas de instalação; gate de legitimidade N/A. |
</threat_model>

<verification>
- `cd backend && mvn -q -DskipTests compile` compila com ambos os ficheiros alterados
- `GET /eventos` passa a incluir `"risco"` em cada evento (verificável em runtime com backend+DB; automatizado aqui via asserção de fonte + compilação, pois um teste HTTP full-context esbarraria no bloqueador MINIO_ENDPOINT — Pitfall 5)
- `getUpcomingEventos`/`eventos/upcoming` já não existem no backend
</verification>

<success_criteria>
1. `GET /eventos` devolve `risco` (ok/proximo/vencido) por evento, calculado via `RiscoPrazoService.computeRiscoEvento` (AGD-35)
2. Instâncias recorrentes recebem risco por ocorrência (não do master)
3. `GET /eventos/upcoming` removido; sem regressão nos restantes handlers de eventos e nos consumidores de `computeRiscoEvento` (dashboard KPIs)
</success_criteria>

<output>
Create `.planning/phases/LEXCV-92-agenda-riscoprazoservice-consolida-o/92-01-SUMMARY.md` when done
</output>
