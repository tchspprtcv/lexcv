---
phase: 48-recorrencia-de-eventos
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/main/java/com/lexcv/models/Evento.java
  - backend/src/main/java/com/lexcv/controllers/ResourceController.java
autonomous: true
requirements: [AGE-03, AGE-04]
must_haves:
  truths:
    - "An Evento with recurrenceRule != null persists its rule, end date and exceptions"
    - "GET /eventos returns expanded virtual instances instead of the recurring master"
    - "DELETE /eventos/{id}/instances?date=YYYY-MM-DD adds the date to the master's recurrenceExceptions without deleting rows"
  artifacts:
    - path: "backend/src/main/java/com/lexcv/models/Evento.java"
      provides: "recurrenceRule, recurrenceEndDate, recurrenceExceptions columns + transient isRecurrenceInstance, recurrenceInstanceDate"
      contains: "recurrenceRule"
    - path: "backend/src/main/java/com/lexcv/controllers/ResourceController.java"
      provides: "instance expansion in listEventos + deleteEventoInstance endpoint"
      contains: "instances"
  key_links:
    - from: "listEventos"
      to: "expanded virtual Evento list"
      via: "in-memory expansion loop over recurring masters"
      pattern: "recurrenceRule"
---

<objective>
Add recurrence support to the backend: three persisted columns plus two transient fields on the Evento entity, in-memory instance expansion in GET /eventos, and a new soft-delete-instance endpoint.

Purpose: Store recurrence rules and serve expanded virtual instances so the calendar shows every occurrence (AGE-03, AGE-04).
Output: Updated Evento entity and ResourceController.
</objective>

<execution_context>
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/LEXCV-48-recorr-ncia-de-eventos/48-CONTEXT.md
@CLAUDE.md

<interfaces>
From backend/src/main/java/com/lexcv/models/Evento.java — entity uses Lombok @Getter/@Setter/@Builder, @Table(name="t_evento"), Integer id (IDENTITY), UUID tenantId, LocalDateTime dataInicio/dataFim, String tipo/titulo/descricao/prioridade, Boolean concluido. @PrePersist sets createdAt.

From ResourceController.java listEventos (lines 1476-1531): loads eventoRepository.findByTenantId(tenantId), filters processoId/concluido via removeIf, parses dataInicio/dataFim with DateTimeFormatter.ISO_DATE_TIME into `start`/`end` LocalDateTime, removeIf events outside window, returns ResponseEntity.ok(eventos).

From ResourceController.java deleteEvento (lines 1606-1616): @PreAuthorize("hasAuthority('agenda:edit')"), finds by id, tenant check, eventoRepository.delete.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add recurrence fields to Evento entity</name>
  <read_first>backend/src/main/java/com/lexcv/models/Evento.java</read_first>
  <files>backend/src/main/java/com/lexcv/models/Evento.java</files>
  <action>Add import java.time.LocalDate. Add three persisted columns: `@Column(name = "recurrence_rule") private String recurrenceRule;` (nullable, values DAILY|WEEKLY|MONTHLY, null = normal event), `@Column(name = "recurrence_end_date") private LocalDate recurrenceEndDate;`, `@Column(name = "recurrence_exceptions") private String recurrenceExceptions;` (comma-separated excluded dates like 2026-07-04,2026-07-11). Add two transient fields excluded from JPA but serialized to JSON: `@Transient private Boolean isRecurrenceInstance;` and `@Transient private String recurrenceInstanceDate;`. Keep ddl-auto=update to auto-create the new columns on startup. Do NOT alter existing columns or @PrePersist.</action>
  <acceptance_criteria>
    - Evento.java declares recurrenceRule, recurrenceEndDate (LocalDate), recurrenceExceptions persisted columns
    - isRecurrenceInstance and recurrenceInstanceDate are annotated @Transient
    - `mvn -DskipTests compile` succeeds from backend/
  </acceptance_criteria>
  <verify>
    <automated>cd backend && mvn -DskipTests compile -q</automated>
  </verify>
  <done>Entity compiles with new columns and transient fields.</done>
</task>

<task type="auto">
  <name>Task 2: Expand recurring instances in GET /eventos</name>
  <read_first>backend/src/main/java/com/lexcv/controllers/ResourceController.java</read_first>
  <files>backend/src/main/java/com/lexcv/controllers/ResourceController.java</files>
  <action>In listEventos, after the existing processoId/concluido/start/end filtering and BEFORE `return ResponseEntity.ok(eventos)`, build an expanded list. If `start`/`end` are null, default the expansion window to today.minusYears(1) .. today.plusYears(1) (use LocalDateTime.now()). Iterate the filtered masters: for an evento with recurrenceRule == null, add it unchanged. For recurrenceRule != null, walk a cursor from evento.getDataInicio() while `cursor.toLocalDate()` is not after evento.getRecurrenceEndDate(); compute the step: DAILY = plusDays(1), WEEKLY = plusWeeks(1), MONTHLY = plusMonths(1). Parse recurrenceExceptions into a Set<String> of ISO date strings (split on comma, trim, ignore blanks). For each cursor date not in exceptions AND whose date is within [effectiveStart, effectiveEnd] window (compare on LocalDateTime), build a virtual copy via Evento.builder() copying every field (id, tenantId, processoId, tipo, titulo, descricao, prioridade, concluido, recurrenceRule, recurrenceEndDate, recurrenceExceptions) with dataInicio = cursor and dataFim = cursor.plus(duration between master dataInicio and dataFim) (use java.time.Duration.between; if master dataFim null, leave null). Set instance.setIsRecurrenceInstance(true) and instance.setRecurrenceInstanceDate(cursor.toLocalDate().toString()). Do NOT add the master itself to the expanded list. Return ResponseEntity.ok(expanded). The virtual instance keeps the master id (so detail/toggle still resolve the master).</action>
  <acceptance_criteria>
    - listEventos returns expanded virtual instances (each with isRecurrenceInstance=true, recurrenceInstanceDate set) for recurring masters, and the master is not present in the response
    - Non-recurring events pass through unchanged
    - Exception dates are skipped; expansion is bounded to ±1 year when no date filter is supplied
    - `cd backend && mvn -DskipTests compile -q` succeeds
  </acceptance_criteria>
  <verify>
    <automated>cd backend && mvn -DskipTests compile -q</automated>
  </verify>
  <done>GET /eventos serves expanded instances within the window.</done>
</task>

<task type="auto">
  <name>Task 3: Add DELETE /eventos/{id}/instances endpoint</name>
  <read_first>backend/src/main/java/com/lexcv/controllers/ResourceController.java</read_first>
  <files>backend/src/main/java/com/lexcv/controllers/ResourceController.java</files>
  <action>Add a new endpoint after deleteEvento: `@PreAuthorize("hasAuthority('agenda:edit')") @DeleteMapping("/eventos/{id}/instances")` taking `@PathVariable Integer id` and `@RequestParam String date`. Find the master via eventoRepository.findById(id); 404 with Map.of("message","Evento não encontrado") if null or tenantId mismatch. Validate `date` parses as LocalDate (DateTimeParseException -> 400 Map.of("message","Parâmetro 'date' inválido. Esperado YYYY-MM-DD")). Read the master recurrenceExceptions, split into a LinkedHashSet of trimmed non-blank tokens, add the new date string if absent, join back with comma, setRecurrenceExceptions, eventoRepository.save(master). Return ResponseEntity.ok(Map.of("message","Instância removida com sucesso!")). Do NOT delete the master row.</action>
  <acceptance_criteria>
    - DELETE /eventos/{id}/instances?date=YYYY-MM-DD appends the date to recurrenceExceptions and persists the master without deleting it
    - Returns 404 for unknown/foreign-tenant id, 400 for malformed date
    - gated with @PreAuthorize("hasAuthority('agenda:edit')")
    - `cd backend && mvn -DskipTests compile -q` succeeds
  </acceptance_criteria>
  <verify>
    <automated>cd backend && mvn -DskipTests compile -q</automated>
  </verify>
  <done>Instance soft-delete endpoint compiles and updates exceptions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser→/api/v1/eventos | authenticated user input (date params, path id) crosses here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-48-01 | Information Disclosure | listEventos / deleteEventoInstance | mitigate | every read/write scoped by getTenantId(); 404 on tenant mismatch (existing pattern preserved) |
| T-48-02 | Tampering | date / id request params | mitigate | parse LocalDate with try/catch -> 400; Integer path var; no string concatenation into queries |
| T-48-03 | Denial of Service | instance expansion loop | mitigate | expansion window capped at ±1 year and bounded by recurrenceEndDate; no unbounded recurrence allowed |
| T-48-04 | Elevation of Privilege | instance delete | mitigate | @PreAuthorize("hasAuthority('agenda:edit')") on both delete endpoints |
| T-48-SC | Tampering | npm/pip/cargo installs | accept | no new dependencies added in this plan |
</threat_model>

<verification>
- `cd backend && mvn -DskipTests compile` succeeds
- Manual smoke (optional): create a recurring evento, GET /eventos returns multiple instances with isRecurrenceInstance=true; DELETE one instance, that date disappears from subsequent GET.
</verification>

<success_criteria>
- Evento persists recurrence rule, end date, exceptions
- GET /eventos expands instances bounded to the requested window or ±1 year
- DELETE /eventos/{id}/instances soft-deletes a single occurrence via exceptions
</success_criteria>

<output>
Create `.planning/phases/LEXCV-48-recorr-ncia-de-eventos/48-01-SUMMARY.md` when done
</output>
