---
phase: 48-recorrencia-de-eventos
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/types/eventos.ts
  - web/src/hooks/use-eventos.ts
  - web/src/schemas/eventos.ts
  - web/src/app/(dashboard)/agenda/novo/page.tsx
  - web/src/app/(dashboard)/agenda/page.tsx
  - web/src/app/(dashboard)/agenda/[id]/page.tsx
autonomous: true
requirements: [AGE-03, AGE-05, AGE-06]
must_haves:
  truths:
    - "The new-event form has a Recorrência section (Nenhuma/Diária/Semanal/Mensal); selecting a rule makes data de fim da recorrência required"
    - "Recurring instances on the calendar show a ↻ indicator distinguishing them from normal events"
    - "Deleting a recurring event offers 'Apagar esta instância' and 'Apagar toda a série'; both reflect immediately"
  artifacts:
    - path: "web/src/schemas/eventos.ts"
      provides: "recurrenceRule + recurrenceEndDate fields with conditional superRefine"
      contains: "recurrenceRule"
    - path: "web/src/hooks/use-eventos.ts"
      provides: "useDeleteEvento + useDeleteEventoInstance mutations"
      contains: "useDeleteEventoInstance"
    - path: "web/src/app/(dashboard)/agenda/[id]/page.tsx"
      provides: "delete action with recurring two-option dialog"
      contains: "Apagar esta instância"
  key_links:
    - from: "agenda/[id]/page.tsx delete dialog"
      to: "/eventos/{id}/instances and /eventos/{id}"
      via: "useDeleteEventoInstance / useDeleteEvento"
      pattern: "useDeleteEventoInstance"
    - from: "agenda/page.tsx event pill"
      to: "isRecurrenceInstance flag"
      via: "↻ indicator render"
      pattern: "isRecurrenceInstance"
---

<objective>
Wire the frontend for recurrence: types, hooks, Zod schema, the Recorrência form section, the calendar instance indicator, and the delete dialog with per-instance vs. whole-series options.

Purpose: Let users create recurring events, see instances distinctly, and choose deletion scope (AGE-03, AGE-05, AGE-06).
Output: Updated types, hooks, schema, and three agenda pages.
</objective>

<execution_context>
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/LEXCV-48-recorr-ncia-de-eventos/48-CONTEXT.md
@CLAUDE.md
@web/AGENTS.md

<interfaces>
From web/src/types/eventos.ts — Evento { id:number; tenantId; processoId?; tipo?; titulo; descricao?; dataInicio; dataFim; prioridade; concluido }. EventoCreateRequest and EventoUpdateRequest mirror these as optional/partial.

From web/src/hooks/use-eventos.ts — apiFetch wrapper; useEventos(filters), useEvento(id), useCreateEvento, useUpdateEvento(id), useToggleEventoConcluido(id). Mutations invalidate queryKey ["eventos","list"]. No delete hook exists yet.

From web/src/schemas/eventos.ts — eventoFormSchema is a z.object(...).refine(dataFim>=dataInicio). Exports EventoFormValues. optionalTrimmedString helper trims+optionals.

From agenda/page.tsx — event pill renders `<span className="opacity-80 mr-1">{cat.shortLabel}:</span>{e.titulo}` inside a Link; events come from useEventos({}) mapped with isPrazo:false. getCategoria(e) classifies the pill.

From agenda/[id]/page.tsx — EventoDetailContent uses useEvento(id); has a button row (Voltar/Editar/Concluir). No delete button currently. Note: useEvento fetches the master by id, so evento.data.recurrenceRule is set for recurring events but isRecurrenceInstance is NOT set on the detail fetch.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Types, hooks, and Zod schema for recurrence</name>
  <read_first>web/src/types/eventos.ts, web/src/hooks/use-eventos.ts, web/src/schemas/eventos.ts</read_first>
  <files>web/src/types/eventos.ts, web/src/hooks/use-eventos.ts, web/src/schemas/eventos.ts</files>
  <behavior>
    - eventoFormSchema accepts recurrenceRule 'NONE' with no recurrenceEndDate (valid)
    - eventoFormSchema with recurrenceRule 'WEEKLY' and empty recurrenceEndDate produces an error on path ['recurrenceEndDate']
    - useDeleteEventoInstance(id).mutate({date}) issues DELETE /eventos/{id}/instances?date={date}
  </behavior>
  <action>In types/eventos.ts: add to Evento the optional fields `recurrenceRule?: 'DAILY' | 'WEEKLY' | 'MONTHLY'`, `recurrenceEndDate?: string`, `recurrenceExceptions?: string`, `isRecurrenceInstance?: boolean`, `recurrenceInstanceDate?: string`. Add to EventoCreateRequest and EventoUpdateRequest: `recurrenceRule?: string` and `recurrenceEndDate?: string`.
  In schemas/eventos.ts: add `recurrenceRule: z.enum(['NONE','DAILY','WEEKLY','MONTHLY']).default('NONE')` and `recurrenceEndDate: optionalTrimmedString` to the object, then add a `.superRefine((data, ctx) => {...})` (chained after the existing .refine) that, when `data.recurrenceRule !== 'NONE'` and `!data.recurrenceEndDate`, calls ctx.addIssue with code z.ZodIssueCode.custom, path ['recurrenceEndDate'], message "A data de fim da recorrência é obrigatória". Keep existing dataFim>=dataInicio refine.
  In hooks/use-eventos.ts: add `useDeleteEvento(id: number)` mutation calling apiFetch(`/eventos/${encodeURIComponent(String(id))}`, { method: 'DELETE' }) and invalidating ["eventos","list"] and ["eventos","upcoming"] onSuccess. Add `useDeleteEventoInstance(id: number)` mutation taking `(args: { date: string })`, calling apiFetch(`/eventos/${encodeURIComponent(String(id))}/instances?date=${encodeURIComponent(args.date)}`, { method: 'DELETE' }), invalidating the same query keys onSuccess.</action>
  <acceptance_criteria>
    - Evento/EventoCreateRequest/EventoUpdateRequest expose the new recurrence fields
    - eventoFormSchema flags missing recurrenceEndDate only when recurrenceRule !== 'NONE'
    - useDeleteEvento and useDeleteEventoInstance exported and invalidate the eventos list
    - `cd web && pnpm lint` passes for these files
  </acceptance_criteria>
  <verify>
    <automated>cd web && pnpm lint</automated>
  </verify>
  <done>Types, hooks, schema updated and lint-clean.</done>
</task>

<task type="auto">
  <name>Task 2: Recorrência section in new-event form + calendar indicator</name>
  <read_first>web/src/app/(dashboard)/agenda/novo/page.tsx, web/src/schemas/eventos.ts, web/src/app/(dashboard)/agenda/page.tsx</read_first>
  <files>web/src/app/(dashboard)/agenda/novo/page.tsx, web/src/app/(dashboard)/agenda/page.tsx</files>
  <action>In agenda/novo/page.tsx: add `recurrenceRule: 'NONE'` and `recurrenceEndDate: undefined` to defaultValues. Add a "Recorrência" section after the descrição field: a Label + select bound with form.register('recurrenceRule') offering options Nenhuma(NONE)/Diária(DAILY)/Semanal(WEEKLY)/Mensal(MONTHLY) using selectClassName. Watch the value via `const recurrenceRule = form.watch('recurrenceRule')`; when `recurrenceRule !== 'NONE'` render a Label "Fim da recorrência" + `<Input type="date" {...form.register('recurrenceEndDate')} />` and show form.formState.errors.recurrenceEndDate.message. In onSubmit, only add recurrenceRule and recurrenceEndDate to the payload when `values.recurrenceRule !== 'NONE'` (send the raw YYYY-MM-DD recurrenceEndDate string); otherwise omit both. Keep existing payload construction otherwise.
  In agenda/page.tsx: in the dayEvents pill render, before the title, add `{!e.isPrazo && (e as any).isRecurrenceInstance ? <span className="mr-1" title="Evento recorrente">↻</span> : null}`. Do not change eventosByDay or buildMonthGrid.</action>
  <acceptance_criteria>
    - New-event form shows a Recorrência select; choosing a non-Nenhuma value reveals a required date input
    - Submitting with Nenhuma omits recurrenceRule/recurrenceEndDate from the payload
    - Recurring instances on the calendar render a ↻ marker distinguishing them from normal events
    - `cd web && pnpm lint` passes
  </acceptance_criteria>
  <verify>
    <automated>cd web && pnpm lint</automated>
  </verify>
  <done>Form creates recurring events; calendar marks instances.</done>
</task>

<task type="auto">
  <name>Task 3: Delete action with recurring two-option dialog</name>
  <read_first>web/src/app/(dashboard)/agenda/[id]/page.tsx, web/src/hooks/use-eventos.ts</read_first>
  <files>web/src/app/(dashboard)/agenda/[id]/page.tsx</files>
  <action>Import useDeleteEvento and useDeleteEventoInstance from @/hooks/use-eventos and useRouter from next/navigation. In EventoDetailContent, instantiate `const router = useRouter()`, `const del = useDeleteEvento(id)`, `const delInstance = useDeleteEventoInstance(id)`, and a `const [confirmOpen, setConfirmOpen] = React.useState(false)`. Add an "Apagar" Button (variant destructive/secondary) in the button row, gated on canEditAgenda, that opens the confirm UI. When `evento.data?.recurrenceRule` is set (recurring), render a dialog/panel with two buttons: "Apagar esta instância" calling `delInstance.mutateAsync({ date: evento.data.dataInicio.slice(0,10) })` then `router.push('/agenda')`, and "Apagar toda a série" calling `del.mutateAsync()` then `router.push('/agenda')`. When recurrenceRule is null/undefined, render a single confirm ("Apagar evento") calling `del.mutateAsync()` then `router.push('/agenda')`. Use an existing AlertDialog primitive if present under components/ui (check by import path); otherwise render a simple inline confirm panel with the two/one buttons and a Cancelar button that closes it. Disable buttons while del.isPending || delInstance.isPending. Surface errors inline.</action>
  <acceptance_criteria>
    - Detail page shows an Apagar button (when canEditAgenda)
    - For a recurring event, two options appear: "Apagar esta instância" and "Apagar toda a série"; instance delete sends the master dataInicio date, series delete removes the master
    - For a non-recurring event, a single confirm deletes it
    - On success navigates to /agenda; `cd web && pnpm lint` passes
  </acceptance_criteria>
  <verify>
    <automated>cd web && pnpm lint</automated>
  </verify>
  <done>Delete dialog branches on recurrence and both actions reflect immediately.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser form→API | user-entered recurrence rule/date submitted to backend |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-48F-01 | Tampering | recurrence form inputs | mitigate | Zod superRefine enforces required end date; backend re-validates and is the authority |
| T-48F-02 | Elevation of Privilege | delete actions | mitigate | UI gated on canEditAgenda; backend @PreAuthorize("agenda:edit") is authoritative |
| T-48F-SC | Tampering | pnpm installs | accept | no new dependencies added in this plan |
</threat_model>

<verification>
- `cd web && pnpm lint` passes
- Manual smoke: create a WEEKLY event with an end date → calendar shows multiple ↻-marked pills; open one, Apagar → choose "Apagar esta instância" → that occurrence disappears; "Apagar toda a série" removes all.
</verification>

<success_criteria>
- Recorrência section with conditional required end date
- Calendar distinguishes recurring instances visually
- Delete dialog offers instance vs. series for recurring events
</success_criteria>

<output>
Create `.planning/phases/LEXCV-48-recorr-ncia-de-eventos/48-02-SUMMARY.md` when done
</output>
