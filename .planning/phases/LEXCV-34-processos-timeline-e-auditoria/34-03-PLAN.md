---
phase: 34-processos-timeline-e-auditoria
plan: "03"
type: execute
wave: 3
depends_on: ["34-02"]
files_modified:
  - web/src/app/(dashboard)/processos/[id]/page.tsx
autonomous: false
requirements: [PRC-28, AUD-02]

must_haves:
  truths:
    - "Detail page tab order is: Timeline, Partes, Fases — Movimentacoes tab is absent"
    - "Default active tab on page load is Timeline"
    - "Timeline tab renders a vertical dot-and-line feed of entries from useTimeline(id)"
    - "Each timeline entry shows a colored dot, a lucide icon, titulo (text-sm font-medium), timestamp (text-xs), optional descricao, optional autorNome"
    - "Five type-filter chips are rendered above the feed; toggling a chip hides/shows entries of that tipo"
    - "Two date inputs (De / Até) filter entries by timestamp range; filters apply client-side without re-fetching"
    - "Auditoria tab button is absent from DOM for users without processos:manage"
    - "Auditoria tab renders a flat list of AuditLogEntry rows with acao badge, entidadeTipo, entidadeId, autorNome, timestamp"
    - "Timeline shows loading skeleton of 3 pulse blocks while useTimeline is loading"
    - "Timeline shows empty state when useTimeline returns an empty array"
  artifacts:
    - path: "web/src/app/(dashboard)/processos/[id]/page.tsx"
      provides: "Restructured tab system with Timeline and Auditoria tabs"
      contains: "TabKey = \"timeline\""
  key_links:
    - from: "tab === 'timeline'"
      to: "useTimeline(id)"
      via: "filteredItems memo over timeline.data"
      pattern: "useTimeline"
    - from: "tab === 'auditoria' && canManageProcessos"
      to: "useAuditLog(id)"
      via: "auditLog.data array render"
      pattern: "useAuditLog"
    - from: "selectedTipos state"
      to: "filteredItems"
      via: "React.useMemo filter over timeline.data"
      pattern: "useMemo.*selectedTipos"
---

<objective>
Frontend UI: restructures the processo detail page tab system, adds the Timeline tab with dot-and-line feed and client-side filter bar, and adds the Auditoria tab with RBAC-gated visibility. Removes the Movimentacoes tab. All logic is contained in a single file — page.tsx.

Purpose: Delivers the user-facing feature. After this plan, users can navigate a unified chronological process history and (if authorized) view the compliance audit trail.
Output: One modified file — page.tsx. No new component files. No new dependencies.
</objective>

<execution_context>
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/LEXCV-34-processos-timeline-e-auditoria/34-UI-SPEC.md
@.planning/phases/LEXCV-34-processos-timeline-e-auditoria/34-RESEARCH.md
@.planning/phases/LEXCV-34-processos-timeline-e-auditoria/34-PATTERNS.md
@.planning/phases/LEXCV-33-processos-workflow-gates-e-prazos/33-03-SUMMARY.md

<interfaces>
<!-- Extracted from page.tsx per 34-PATTERNS.md lines 513-646 -->

Current TabKey and useState (lines 82, 130):
```typescript
type TabKey = "partes" | "fases" | "movimentacoes";
const [tab, setTab] = React.useState<TabKey>("partes");
```

Phase 34 change:
```typescript
type TabKey = "timeline" | "partes" | "fases" | "auditoria";
const [tab, setTab] = React.useState<TabKey>("timeline");
```

Existing imports to extend (lines 7-17 lucide, lines 37-53 hooks, lines 69-76 types):
```typescript
// lucide — ADD: FileText, GitBranch, Calendar, Paperclip, ShieldCheck (and optionally ArrowRightLeft)
// hooks — ADD: useTimeline, useAuditLog
// types — ADD: TimelineItem, AuditLogEntry (and TimelineItemType for filter state)
```

Tab button pattern (lines 851-872) — existing button style to replicate for Timeline tab:
```tsx
<Button
  type="button"
  variant={tab === "partes" ? "secondary" : "outline"}
  onClick={() => setTab("partes")}
>
  Partes
</Button>
```

RBAC conditional (lines 495-509) — exact pattern for Auditoria tab button:
```tsx
{canManageProcessos ? (
  <Button
    type="button"
    variant={tab === "auditoria" ? "secondary" : "outline"}
    onClick={() => setTab("auditoria")}
  >
    Auditoria
  </Button>
) : null}
```

Tab content ternary chain — current (lines 875-1134 approx):
```tsx
{tab === "partes" ? (
  <div className="grid gap-4 lg:grid-cols-2">
    {/* partes content — DO NOT MODIFY */}
  </div>
) : tab === "fases" ? (
  <div className="grid gap-4 lg:grid-cols-2">
    {/* fases content — DO NOT MODIFY */}
  </div>
) : /* tab === "movimentacoes" */ (
  <div className="grid gap-4 lg:grid-cols-2">
    {/* movimentacoes content — REMOVE this block */}
  </div>
)}
```

Phase 34 ternary chain:
```tsx
{tab === "timeline" ? (
  /* TimelineFeed with FilterBar — new */
) : tab === "partes" ? (
  /* unchanged partes content */
) : tab === "fases" ? (
  /* unchanged fases content */
) : tab === "auditoria" && canManageProcessos ? (
  /* AuditoriaList — new */
) : null}
```

Movimentacoes card list (lines 1109-1134) — reference for entry structure to replace:
```tsx
<div key={m.id} className="rounded-md border border-neutral-200 p-3 dark:border-neutral-800">
  <div className="flex flex-wrap items-baseline justify-between gap-2">
    <div className="font-medium">{m.titulo}</div>
    <div className="text-xs text-neutral-500 dark:text-neutral-400">{formatDateTime(m.data)}</div>
  </div>
  {m.descricao ? (
    <div className="mt-2 text-sm text-neutral-700 dark:text-neutral-200 whitespace-pre-wrap">
      {m.descricao}
    </div>
  ) : null}
</div>
```

Icon + dot color mapping (from UI-SPEC):
| tipo | Icon | Dot className |
|------|------|---------------|
| movimentacao | FileText | bg-emerald-500 |
| transicao | GitBranch | bg-blue-600 |
| evento | Calendar | bg-amber-500 |
| documento | Paperclip | bg-slate-400 |
| decisao | ShieldCheck | bg-purple-500 |

Acao badge variants for Auditoria tab (from UI-SPEC):
| acao | variant |
|------|---------|
| transicao_estado | blue |
| conflict_check_decisao | purple |
| documento_download | secondary |
| documento_eliminacao | red |
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend imports, TabKey, useState, and add client-side filter state + useTimeline/useAuditLog calls</name>
  <files>
    web/src/app/(dashboard)/processos/[id]/page.tsx
  </files>
  <read_first>
    - web/src/app/(dashboard)/processos/[id]/page.tsx (read FULLY — identify: lucide import block lines ~7-17; hook import block lines ~37-53; types import block lines ~69-76; TabKey type line ~82; useState("partes") line ~130; all existing hook calls in ProcessoDetailContent to understand current hook instantiation locations; formatDateTime helper usage for date formatting pattern)
  </read_first>
  <action>
    Make four changes to page.tsx — imports, TabKey, useState, and new state + data hooks.

    CHANGE 1 — Lucide imports: In the existing lucide-react import block, add FileText, GitBranch, Calendar, Paperclip, ShieldCheck to the destructured import. Do not remove any existing icons.

    CHANGE 2 — Hook imports: In the existing use-processos import block, add useTimeline and useAuditLog to the destructured import. Do not remove any existing hook imports.

    CHANGE 3 — Type imports: In the existing processos types import block, add TimelineItem, AuditLogEntry, and TimelineItemType to the destructured import. Do not remove any existing type imports.

    CHANGE 4 — TabKey and useState: Change the TabKey type definition from `"partes" | "fases" | "movimentacoes"` to `"timeline" | "partes" | "fases" | "auditoria"`. Change the initial useState value from "partes" to "timeline".

    CHANGE 5 — Add filter state and data hooks inside ProcessoDetailContent (or wherever the other hooks are called — the existing useWorkflow, usePrazos, etc. calls): Add the following React.useState declarations for filter state (after the existing useState declarations for tab, dialogs, etc.):
    - const [selectedTipos, setSelectedTipos] = React.useState<Set<TimelineItemType>>(new Set(["movimentacao", "transicao", "evento", "documento", "decisao"]))
    - const [dateFrom, setDateFrom] = React.useState<string>("")
    - const [dateTo, setDateTo] = React.useState<string>("")

    Add hook calls:
    - const timeline = useTimeline(processo.data?.id ?? "")
    - const auditLog = useAuditLog(processo.data?.id ?? "")

    Add filteredItems memo immediately after the hook calls:
    ```
    const filteredItems = React.useMemo(() => {
      return (timeline.data ?? []).filter((item) => {
        if (!selectedTipos.has(item.tipo)) return false;
        if (dateFrom && item.timestamp < dateFrom) return false;
        if (dateTo && item.timestamp > dateTo + "T23:59:59") return false;
        return true;
      });
    }, [timeline.data, selectedTipos, dateFrom, dateTo]);
    ```

    Do not yet modify the tab buttons section or the tab content section — those are handled in Task 2.
  </action>
  <verify>
    <automated>cd "C:/Users/francisco.horta/Documents/projects/personal/lexcv/web" && pnpm exec tsc --noEmit 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - tsc --noEmit exits 0 (no type errors)
    - page.tsx imports FileText, GitBranch, Calendar, Paperclip, ShieldCheck from lucide-react
    - page.tsx imports useTimeline and useAuditLog from @/hooks/use-processos
    - page.tsx imports TimelineItem, AuditLogEntry, TimelineItemType from @/types/processos
    - TabKey type contains "timeline" and "auditoria" and does NOT contain "movimentacoes"
    - useState initial value is "timeline" not "partes"
    - selectedTipos state initializes to a Set with all five TimelineItemType values
    - filteredItems is a React.useMemo that filters timeline.data by selectedTipos and date range
    - useTimeline and useAuditLog are called with processo.data?.id ?? ""
  </acceptance_criteria>
  <done>Imports, TabKey, useState, filter state, and hooks are in place. TypeScript compiles clean.</done>
</task>

<task type="auto">
  <name>Task 2: Tab buttons restructure + Timeline feed + Auditoria list + remove Movimentacoes tab</name>
  <files>
    web/src/app/(dashboard)/processos/[id]/page.tsx
  </files>
  <read_first>
    - web/src/app/(dashboard)/processos/[id]/page.tsx (read again focusing on: tab button group lines ~851-872; tab content ternary chain lines ~875-1134; movimentacoes content block to identify exact lines to remove; Card/CardHeader/CardContent import usage to understand existing card patterns; formatDateTime helper name and usage)
  </read_first>
  <action>
    Make three changes to the tab section of page.tsx.

    CHANGE 1 — Tab button group: Locate the div containing the tab buttons. Replace the button group so it contains exactly: Timeline button first (variant secondary when tab==="timeline"), then Partes button (unchanged), then Fases button (unchanged), then Auditoria button wrapped in {canManageProcessos ? (...) : null} (absent from DOM entirely for non-managers). Remove the Movimentacoes button. Apply existing button pattern: type="button", variant={tab === "X" ? "secondary" : "outline"}, onClick={() => setTab("X")}.

    CHANGE 2 — Tab content ternary chain: Refactor the ternary chain to add "timeline" as the first arm and "auditoria" as the last arm, remove the "movimentacoes" arm, and leave the "partes" and "fases" arms completely unchanged (copy them exactly as-is — do NOT modify any existing partes or fases JSX).

    The "timeline" arm content (single Card wrapping everything):
    - CardHeader with CardTitle "Timeline" and the filter bar below the title
    - Filter bar (a flex-wrap gap-2 items-end div): five tipo-filter chip buttons (labels: Movimentações, Transições, Eventos, Documentos, Decisões, corresponding to tipos movimentacao/transicao/evento/documento/decisao); two Input fields for De/Até with type="date" h-8 text-xs rounded-none; a "Limpar filtros" ghost Button that resets selectedTipos to the full set and dateFrom/dateTo to "". Chip active state: inline className — bg-slate-900 text-white dark:bg-slate-100 dark:text-slate-900 rounded-none h-8 px-3 text-xs. Chip inactive: variant="outline" h-8 px-3 text-xs rounded-none. Each chip button has aria-pressed={selectedTipos.has("movimentacao")} (etc.) and toggles the tipo in/out of selectedTipos via setSelectedTipos with a new Set.
    - CardContent with the feed:
      - Loading state (timeline.isLoading): render 3 skeleton blocks, each a div with animate-pulse bg-slate-100 dark:bg-slate-800 rounded h-12 mb-4
      - Error state (timeline.isError): render "Não foi possível carregar a timeline. Verifique a ligação e tente novamente." in text-red-600 text-sm py-4
      - Empty state (filteredItems.length === 0 and !timeline.isLoading): render centered div py-12 text-center with heading "Sem entradas na timeline" (text-sm font-medium text-slate-500) and body "As movimentações, eventos e documentos do processo aparecem aqui assim que forem registados." (text-xs text-slate-400 mt-1)
      - Feed (filteredItems.length > 0): map over filteredItems with a relative div. Per entry:
        - Outer div: relative flex gap-3 py-4
        - Left column: a div with relative flex flex-col items-center, containing: the dot (a span with h-2.5 w-2.5 rounded-full plus the tipo-specific bg color className — use a helper mapping or inline ternary), and below the dot the connector line (a div with absolute top-3 bottom-0 left-[5px] w-0.5 bg-slate-200 dark:bg-slate-700 — hidden on the last item via a check, or omitted for last entry)
        - Right column: icon (the tipo-specific lucide Icon at h-4 w-4 with matching color) and content block: titulo in text-sm font-medium text-slate-900 dark:text-white; timestamp formatted with a formatDateTime call or new Date(item.timestamp).toLocaleString("pt-PT") in text-xs text-slate-500 dark:text-slate-400; optional descricao in text-sm text-slate-600 dark:text-slate-400 mt-1 whitespace-pre-wrap; optional autorNome in text-xs text-slate-400 dark:text-slate-500 italic mt-0.5
        - Add aria-label on the icon wrapper: aria-label="{tipoLabel} em {formattedDate}"
        - Use React.Fragment or index-based key={item.tipo + item.id}

    The "auditoria" arm (tab === "auditoria" && canManageProcessos): a Card with CardHeader (CardTitle "Auditoria") and CardContent:
      - Loading state (auditLog.isLoading): "A carregar auditoria..." in text-sm text-slate-500 py-4
      - Error state (auditLog.isError): "Erro ao carregar a auditoria. Tente novamente." in text-red-600 text-sm py-4
      - Empty state (no entries): centered div with "Sem registos de auditoria" heading and "Os eventos sensíveis do processo (transições, decisões, downloads e eliminações) ficam registados aqui." body (same pattern as timeline empty state)
      - Entries list: div containing rows, each row a flex items-center gap-3 py-2 border-b border-slate-100 dark:border-slate-800 div with: timestamp in text-xs text-slate-500 w-36 shrink-0; acao Badge using variant mapped from acao value (transicao_estado→blue, conflict_check_decisao→purple, documento_download→secondary, documento_eliminacao→red) with className "rounded-none font-bold tracking-wide text-[11px]"; entidadeTipo in text-xs text-slate-500; entidadeId in text-xs text-slate-400 font-mono truncate max-w-[120px]; autorNome (if present) in text-xs text-slate-600 dark:text-slate-300.
      - Use key={entry.id} per row.

    CHANGE 3 — Remove Movimentacoes tab content block: The existing movimentacoes arm of the ternary (the final else arm that renders the movimentacoes list) must be removed entirely. The card that contains the "Adicionar movimentação" form and the movimentacoes list should be deleted. Do not remove useProcessoMovimentacoes imports or hooks — only the JSX rendering block for that tab arm.

    After all changes, ensure no dangling JSX or TypeScript errors. Check that canManageProcessos is defined in scope (it should already be — Phase 33 added it). Verify useProcessoMovimentacoes is still imported (it may be used programmatically) and that removing the JSX block does not create an unused variable error — if it does, remove the hook call variable assignment only if the hook result is truly unused after this change.
  </action>
  <verify>
    <automated>cd "C:/Users/francisco.horta/Documents/projects/personal/lexcv/web" && pnpm exec tsc --noEmit 2>&1 | tail -15 && pnpm lint 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - tsc --noEmit exits 0 with no errors
    - pnpm lint exits 0 (pre-existing img warnings acceptable; no new errors)
    - page.tsx contains `variant={tab === "timeline" ? "secondary" : "outline"}` tab button
    - page.tsx does NOT contain `variant={tab === "movimentacoes" ?` tab button
    - page.tsx contains `{canManageProcessos ? (` wrapping the Auditoria tab button
    - page.tsx contains `tab === "timeline" ?` as the first arm of the tab content ternary
    - page.tsx contains `tab === "auditoria" && canManageProcessos ?` arm
    - page.tsx contains `selectedTipos.has(` for tipo filter chip logic
    - page.tsx contains `aria-pressed=` on filter chip buttons
    - page.tsx contains five dot color classNames: bg-emerald-500, bg-blue-600, bg-amber-500, bg-slate-400, bg-purple-500 (one per timeline entry type)
    - page.tsx contains FileText, GitBranch, Calendar, Paperclip, ShieldCheck in timeline entry rendering
    - page.tsx contains the loading skeleton (animate-pulse) for the timeline
    - page.tsx contains "Sem entradas na timeline" empty state copy
    - page.tsx contains "Sem registos de auditoria" empty state copy
    - page.tsx does NOT contain the old movimentacoes card rendering block (the tab content for movimentacoes is gone)
    - React.useState initial value is "timeline" (confirmed from Task 1 — verify still in place after Task 2 edits)
  </acceptance_criteria>
  <done>Tab system fully restructured: Timeline is default tab with dot-and-line feed, tipo chips, and date filters. Auditoria tab is absent for non-managers. Movimentacoes tab content removed. TypeScript and lint pass clean.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Wave 3 complete — page.tsx has the full Timeline and Auditoria feature. Backend (Wave 1) and data layer (Wave 2) are also in place. Verify the full feature end-to-end with a running stack.
  </what-built>
  <how-to-verify>
    Prerequisites: `cd backend && mvn spring-boot:run` (with SEED_ENABLED=true), then `cd web && pnpm dev`.
    Log in as admin@lexcv.cv / admin123 (ADMIN role has all permissions including processos:manage).
    Open a processo that has at least one movimentacao, one evento, one documento, and a conflict-check decision.

    Verify Timeline tab:
    1. Navigate to /processos/{id} — the active tab should be "Timeline" (not "Partes").
    2. The Timeline tab shows a vertical dot-and-line feed. Each entry has a colored dot, a lucide icon, titulo, timestamp, and optional descricao/autorNome.
    3. State transition entries (tipo=transicao) show a blue dot and GitBranch icon; documento entries show a slate dot and Paperclip icon; evento entries show amber and Calendar; decisao shows purple and ShieldCheck; movimentacao shows emerald and FileText.
    4. Entries are ordered most-recent first.
    5. Click a tipo chip (e.g., "Documentos") to deselect it — documento entries disappear from the feed. Click again to re-show. Repeat for each type.
    6. Enter a date in the "De" field — entries before that date disappear. Clear the date — they return.
    7. Click "Limpar filtros" — all filters reset, all entries visible.
    8. The "Movimentações" tab button is gone from the tab row.

    Verify Auditoria tab:
    9. The "Auditoria" tab button is visible (admin has processos:manage).
    10. Click Auditoria — see a flat list of audit entries. Each row shows timestamp, acao badge (blue/purple/secondary/red), entidadeTipo, truncated entidadeId, and autorNome.
    11. Execute a state transition from the Workflow card — return to Auditoria tab and verify a new transicao_estado entry appeared.
    12. Download a documento — return to Auditoria tab and verify a documento_download entry appeared.

    Verify RBAC:
    13. Log in as a TECNICO user (processos:view, not processos:manage). Navigate to the same processo detail.
    14. "Auditoria" tab button is completely absent from the DOM (not just disabled — inspect element to confirm).
    15. Timeline tab is still visible and functional for TECNICO.

    Verify empty state:
    16. Navigate to a processo with no movimentacoes/eventos/documentos. Timeline tab shows "Sem entradas na timeline" empty state.
  </how-to-verify>
  <resume-signal>Type "approved" if all 16 checks pass. Describe any failures with step number and observed vs expected behavior.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI render → canManageProcessos | Tab button absent from DOM for non-managers; this is a UX measure only — backend enforces via @PreAuthorize |
| Client-side filter → filteredItems | Filter operates over already-fetched data in the browser; no additional security boundary crossed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-08 | Information Disclosure | Auditoria tab button conditional render | accept | Tab button absent from DOM when !canManageProcessos; a direct URL to /audit still returns 403 from the backend — the UI absence is UX-only, backend is the enforcement boundary |
| T-34-09 | Tampering | selectedTipos filter state | accept | Client-side filter only hides already-fetched data from view; no security boundary — all data was fetched from the authenticated endpoint |
| T-34-10 | Tampering | tab === "auditoria" && canManageProcessos content gate | accept | Even if a user manually sets tab state to "auditoria", the useAuditLog hook query would return a 403 error from the backend — the auditLog.data would be null/undefined and the empty/error state would render |
| T-34-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this wave; zero supply-chain risk |
</threat_model>

<verification>
After all tasks complete:

1. `cd web && pnpm exec tsc --noEmit` — must exit 0
2. `cd web && pnpm lint` — must exit 0 (pre-existing img warnings acceptable)
3. `grep -c "tab === \"timeline\"" web/src/app/(dashboard)/processos/\[id\]/page.tsx` — returns ≥ 2 (button + content arm)
4. `grep -c "movimentacoes" web/src/app/(dashboard)/processos/\[id\]/page.tsx` — returns only hook import references, not any JSX rendering of the movimentacoes tab content (the tab button and card list are gone)
5. `grep -c "canManageProcessos" web/src/app/(dashboard)/processos/\[id\]/page.tsx` — returns ≥ 2 (existing Phase 33 uses + new Auditoria tab gate)
6. Human checkpoint (task 3): visual end-to-end verification with running stack
</verification>

<success_criteria>
- page.tsx has TabKey = "timeline" | "partes" | "fases" | "auditoria" with "timeline" as default
- Timeline tab: dot-and-line feed, color-coded by tipo, sorted desc, with 5-tipo chip filter bar and De/Até date filters (client-side, no re-fetch)
- Auditoria tab: flat list with acao badges, absent from DOM for non-processos:manage users
- Movimentacoes tab content and button removed
- Loading/empty/error states present for both new tabs per UI-SPEC copy
- Dark mode classes present per UI-SPEC dark mode contract (border-slate-200 dark:border-slate-700, etc.)
- Accessibility: aria-pressed on chips, aria-label on icon wrappers, date inputs linked to labels via htmlFor/id
- tsc and lint pass clean
- Human checkpoint approved
</success_criteria>

<output>
Create `.planning/phases/LEXCV-34-processos-timeline-e-auditoria/34-03-SUMMARY.md` when done.
</output>
