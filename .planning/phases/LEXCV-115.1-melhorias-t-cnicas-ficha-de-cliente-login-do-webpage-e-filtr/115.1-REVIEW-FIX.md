---
phase: LEXCV-115.1-melhorias-t-cnicas-ficha-de-cliente-login-do-webpage-e-filtr
fixed_at: 2026-07-22T10:02:40Z
review_path: .planning/phases/LEXCV-115.1-melhorias-t-cnicas-ficha-de-cliente-login-do-webpage-e-filtr/115.1-REVIEW.md
iteration: 3
findings_in_scope: 1
fixed: 1
skipped: 0
status: all_fixed
---

# Phase LEXCV-115.1: Code Review Fix Report

**Fixed at:** 2026-07-22T10:02:40Z
**Source review:** .planning/phases/LEXCV-115.1-melhorias-t-cnicas-ficha-de-cliente-login-do-webpage-e-filtr/115.1-REVIEW.md
**Iteration:** 3

**Summary:**
- Findings in scope: 1
- Fixed: 1
- Skipped: 0

## Fixed Issues

### WR-01: "Cliente já não está disponível" recovery message never clears once the user picks a valid replacement

**Status:** fixed
**Files modified:** `web/src/app/(dashboard)/processos/novo/page.tsx`, `web/src/app/(dashboard)/pareceres/nova/page.tsx`
**Commit:** eda068f
**Applied fix:** Read both files at the cited line ranges and confirmed the code matched the review's description exactly — `lockedClienteMissing` (added in the prior fix pass, commit `4b13f96`) was being read directly by the red "cliente já não está disponível" message, with no way to become `false` again once the user picked a valid replacement from the now-enabled `<NativeSelect>` (an uncontrolled RHF field, so selecting an option never touches `lockedClienteId` or `clientes.data`, the only two live inputs `lockedClienteMissing` depends on). Applied the review's suggested fix as-is in both files: added a narrower derived value gated on whether the user has actually made a selection yet — `pareceres/nova/page.tsx` already computed `clienteIdValue = form.watch("clienteId")` for the Processo-dropdown filter, so `lockedClienteMissingUnresolved = lockedClienteMissing && !clienteIdValue` reuses it directly; `processos/novo/page.tsx` had no equivalent watch, so added `const currentClienteId = intakeForm.watch("cliente_id")` immediately followed by the same `lockedClienteMissingUnresolved` derivation, placed directly after the existing `lockedClienteMissing` block in both files. Updated the JSX in both files so the red recovery message now reads `lockedClienteMissingUnresolved` instead of `lockedClienteMissing`, and the muted "não pode ser alterado aqui" branch gained the review's prescribed `&& !lockedClienteMissing` guard so it can't reappear once the field has genuinely recovered. Left the `disabled={...}` expression on the `<NativeSelect>` completely untouched in both files — it still reads the original `lockedClienteMissing`, exactly as the review specified, so the field cannot re-lock itself the instant a valid replacement is chosen.

**Verification:** Since the worktree's `web/node_modules` doesn't exist (gitignored), temporarily linked it to the main repo's via a read-only NTFS junction (`New-Item -ItemType Junction`) to run real project tooling, then removed the junction afterward (confirmed the main repo's `node_modules` was left untouched). Ran `npx tsc --noEmit -p tsconfig.json`: 3 errors, all pre-existing `Cannot find module 'vitest'` in unrelated `*.test.ts` files (byte-identical to the baseline recorded in the prior fix report) — zero new errors, and neither modified file appears in the error list. Ran `npx eslint` on both modified files: 0 errors, 3 warnings — `pareceres/nova/page.tsx:70` (`react-hooks/incompatible-library` on the pre-existing `form.watch("clienteId")` call, unchanged by this fix) and `processos/novo/page.tsx:155` (`@typescript-eslint/no-unused-vars` on the pre-existing `estado: _estado` destructure) are both pre-existing and already logged in `115.1-01-deferred-items.md`; `processos/novo/page.tsx:134` (`react-hooks/incompatible-library` on the newly added `intakeForm.watch("cliente_id")` call) is a new instance of the exact same rule/pattern the review's own Fix section explicitly prescribed adding, mirroring the identical pre-accepted tradeoff already present in the sibling file — not a new class of problem. Also re-read both modified sections in full after editing to confirm the inserted `lockedClienteMissingUnresolved` derivation and the updated JSX conditions are present and the surrounding code (including the untouched `disabled` expression) is intact.

## Skipped Issues

None — the single in-scope finding (WR-01) was fixed. IN-01 (Info-level: missing `encodeURIComponent` on conflict-check match links, `processos/novo/page.tsx:581-590`, carried over unchanged across all three review iterations) and IN-02 (Info-level: missing `encodeURIComponent` on the post-create redirect in `pareceres/nova/page.tsx:114`, newly noted this iteration) were both excluded by `fix_scope: critical_warning` and not attempted — they remain open for a future `fix_scope: all` pass.

---

_Fixed: 2026-07-22T10:02:40Z_
_Fixer: Claude (gsd-code-fixer)_
_Iteration: 3_
