---
phase: LEXCV-115.1-melhorias-t-cnicas-ficha-de-cliente-login-do-webpage-e-filtr
verified: 2026-07-22T11:30:00Z
status: human_needed
score: 15/15 must-haves verified (code-level)
overrides_applied: 0
human_verification:
  - test: "Login as a user WITH processos:create + pareceres:create, open a cliente ficha, confirm both 'Novo Processo' (Processos tab) and 'Novo Parecer' (Pareceres tab) buttons render in the 'Adicionar'-style outline/sm/Plus visual (not the bold list-page CTA). Then repeat as a user WITHOUT those scopes and confirm both buttons are absent."
    expected: "Buttons visible only for scope holders; absent otherwise. Visual style matches ClienteDocumentosEntreguesTab's 'Adicionar' button exactly."
    why_human: "RBAC-gated visual rendering requires a live login session per role; code inspection confirms the conditional (`canCreateProcessos ? (...) : null`) and correct permission derivation, but not actual rendered output for real user accounts."
  - test: "Hard-reload a cliente ficha (cold cache), then click 'Novo Processo' and separately 'Novo Parecer'. Confirm the Cliente field on the destination form shows the correct client pre-selected (not the empty placeholder) despite being disabled, and Step 3's review screen (Processos) shows the correct client name, not '—'."
    expected: "Cliente field is disabled AND correctly shows the originating client, even when clientes.data was not yet loaded at mount time."
    why_human: "This is the exact CR-01 finding from 115.1-REVIEW.md ('cannot be caught by build/lint since it's a runtime data-timing issue'). The fix (React.useEffect re-sync via setValue) is present and correctly shaped in the current code, and was traced against react-hook-form's DOM-write behavior, but no iteration ever ran an actual browser session against a cold cache to confirm the race is closed."
  - test: "Navigate directly to /processos/novo?clienteId=<a non-existent or already-deleted client id> and to /pareceres/nova?clienteId=<same>. Confirm the Cliente select re-enables and shows the red 'cliente já não está disponível' message. Then pick a valid replacement client from the now-enabled dropdown and confirm the red message clears (and the muted 'não pode ser alterado' message does NOT reappear)."
    expected: "Field recovers (enabled + selectable) when the locked id can't be resolved; message clears once the user picks a valid replacement."
    why_human: "This exercises the WR-01 (iter2) recovery branch and the WR-01 (iter3) message-clearing fix together — a multi-step, data-dependent interaction (query settles as 'not found', then a user selection changes local form state) that static analysis traced logically but was never run in a browser."
  - test: "With webpage/ and web/ both running as separate local dev servers, set NEXT_PUBLIC_WEB_APP_URL in webpage/.env.local to web's origin (e.g. http://localhost:3000), rebuild/restart webpage's dev server, and click all 4 'Entrar' links (site-header desktop, site-header mobile sheet, hero section, footer). Confirm each navigates to {web origin}/login instead of 404ing on webpage's own (nonexistent) /login route."
    expected: "All 4 links land on the web app's real /login page when the var is set; this is the actual gap #2 behavior being closed."
    why_human: "This is the actual dev-local bug the phase was created to fix. Verified so far only via (a) unit-style logic checks of the trim/fallback string computation and (b) inspecting minified build output for the inlined literal — neither exercises two live dev servers and an actual cross-app navigation."
  - test: "Open Pareceres' 'Filtros' and 'Pesquisa Avançada' panels in both light and dark theme. Confirm the 6 dropdowns render as native OS `<select>` pickers (not the old custom popover), no border/background/text regression vs. Processos' equivalent filters, and that applying a filter / running Pesquisa Avançada still returns correctly filtered results."
    expected: "Visual parity with Processos' filter primitives in both themes; filtering/search still functionally correct."
    why_human: "Visual/theme rendering and end-to-end filter-query correctness require the running app; grep confirms the JSX/primitives/state wiring is structurally correct but not the rendered pixels or the live query round-trip."
  - test: "Login as a non-ADMIN user holding pareceres:view/pareceres:create (e.g. an ADVOGADO). On a cliente ficha's Pareceres tab, confirm the 'Advogado Responsável' column shows a raw id (not a blank '—') when the friendly-name map can't resolve. On /pareceres/nova, confirm the 'Advogado responsável (opcional)' dropdown shows the 'não foi possível carregar a lista de advogados' helper text instead of silently only offering 'Atribuir mais tarde'."
    expected: "Graceful degradation is visible to a real non-ADMIN session, not just present in the source."
    why_human: "This is the WR-01 (iter1) graceful-degradation fix, dependent on the /admin/users endpoint 403-ing for non-ADMIN roles — a live-role, live-network-response scenario that cannot be grepped."
---

# Phase 115.1: Melhorias Técnicas — Ficha de Cliente, Login do Webpage e Filtros de Pareceres Verification Report

**Phase Goal:** Fechar 3 gaps de UX/técnicos identificados após o fecho da v2.14: criação de artefactos a partir da ficha de cliente, redirect de autenticação a partir do webpage público, e consistência visual dos filtros em Pareceres.
**Verified:** 2026-07-22T11:30:00Z
**Status:** human_needed
**Re-verification:** No — initial verification

## Goal Achievement

This phase closes 3 unrelated, independently-scoped gaps (Área 1/2/3). All 3 plans' `must_haves` (15 truths, 9 artifacts, 9 key links) were checked directly against the current file contents on disk — not against SUMMARY.md prose. Every truth, artifact, and key link is code-level VERIFIED. Additionally, this phase went through 3 code-review iterations (1 Critical + 3 Warnings found and fixed); all 4 fixes were independently re-confirmed present in the current code by direct file inspection (not by trusting the REVIEW-FIX reports), and independently cross-checked with fresh `tsc --noEmit` / `eslint` runs and one reproduced behavioral test. No gaps were found. Because several of the fixed behaviors are runtime/data-timing or visual in nature (explicitly flagged as such by the phase's own code review), this report routes to **human_needed** rather than **passed** — see Human Verification Required below.

### Observable Truths

| #   | Truth | Status | Evidence |
| --- | ------- | ---------- | -------------- |
| 1 | [AREA-1] User holding `processos:create` sees "Novo Processo" button in cliente ficha's Processos tab | VERIFIED | `clientes/[id]/page.tsx:150,878,1107-1116` — `canCreateProcessos = permissions.can.create("processos")` derived once, passed as prop, gates a `<div className="flex items-center justify-end">` header row above the tab's `<Card>` |
| 2 | [AREA-1] User holding `pareceres:create` sees "Novo Parecer" button in cliente ficha's Pareceres tab | VERIFIED | `clientes/[id]/page.tsx:151,888,1226-1235` — identical pattern, `canCreatePareceres` |
| 3 | [AREA-1] Button is RBAC-gated only, NOT coupled to the ficha's `isEditing`/`editable` toggle | VERIFIED | Both header rows are gated solely by `canCreateProcessos`/`canCreatePareceres` ternaries (lines 1107, 1226) — no reference to `isEditing`/`editable` anywhere near either block, unlike the "Adicionar" button which is `editable && canEditDocumentos`-gated |
| 4 | [AREA-1] Clicking "Novo Processo" opens `/processos/novo?clienteId={id}` with cliente pre-selected + field disabled | VERIFIED | `clientes/[id]/page.tsx:1110`: `href={\`/processos/novo?clienteId=${encodeURIComponent(clienteId)}\`}`; `processos/novo/page.tsx:66,91,111-115,342-346`: `lockedClienteId` read, seeded into `defaultValues.cliente_id`, re-sync `useEffect`, `disabled` expression includes `Boolean(lockedClienteId)` |
| 5 | [AREA-1] Clicking "Novo Parecer" opens `/pareceres/nova?clienteId={id}` with cliente pre-selected + field disabled | VERIFIED | `clientes/[id]/page.tsx:1229`: same href pattern; `pareceres/nova/page.tsx:49,60,78-82,164-168`: identical treatment, direct `clienteId` field match |
| 6 | [AREA-1] Both create forms opened WITHOUT `?clienteId=` behave exactly as before (field enabled, empty default, no helper text) | VERIFIED | When `lockedClienteId` is `null`: `Boolean(null)` = `false`, so `disabled` reduces to the original `clientes.isPending \|\| clientes.isError`; `lockedClienteMissing` short-circuits to `false`; the helper-text ternary chain (`lockedClienteMissingUnresolved ? ... : lockedClienteId && !lockedClienteMissing ? ... : null`) resolves to `null` — traced in both `processos/novo/page.tsx:342-364` and `pareceres/nova/page.tsx:164-186` |
| 7 | [AREA-2] `getLoginUrl()` helper exists, returns `{NEXT_PUBLIC_WEB_APP_URL}/login` when set / `/login` otherwise, never throws | VERIFIED | `webpage/src/lib/get-login-url.ts:17-20` — reproduced the exact logic in an isolated Node test across 5 cases (unset, empty, bare origin, trailing slash, multiple trailing slashes) — all 5 pass; zero `throw` statements in the file (`! grep -q 'throw'` still true) |
| 8 | [AREA-2] All 4 "Entrar" call sites compute href via `getLoginUrl()` instead of hardcoded `/login` | VERIFIED | `grep -c 'href={getLoginUrl()}'` across the 3 files = 4 (site-header.tsx ×2, hero-section.tsx ×1, site-footer.tsx ×1); `grep -r 'href="/login"' webpage/src` = 0 matches |
| 9 | [AREA-2] With `NEXT_PUBLIC_WEB_APP_URL` unset (production case), all 4 links resolve to `/login`, byte-identical to pre-phase | VERIFIED | Logic-level guarantee from the ternary fallback (confirmed by the Node reproduction above); `git diff 1daafb3..HEAD` for all 3 component files shows only the import line + the `href=` line changed — `Button` variant/className/text/icon (`ArrowRight`, `onClick`) all byte-identical |
| 10 | [AREA-2] `NEXT_PUBLIC_WEB_APP_URL` documented in `webpage/.env.example` | VERIFIED | `webpage/.env.example:3` — `NEXT_PUBLIC_WEB_APP_URL=` (empty, matching the file's existing bare `KEY=value` style) |
| 11 | [AREA-3] All 6 filter `Select` fields (3 Filtros + 3 Pesquisa Avançada) render as `NativeSelect` | VERIFIED | `grep -c '<NativeSelect' pareceres/page.tsx` = 6; all 6 confirmed by direct read (Estado/Advogado/Cliente ×2 panels), each preserving its original bound state var + `"todos"` sentinel |
| 12 | [AREA-3] Pesquisar / Data Início / Data Fim raw `<input>`/`<label>` render via `Input`/`Label` primitives, classes preserved | VERIFIED | `pareceres/page.tsx:262-354` — `<Label htmlFor="pesquisa-texto" className="text-[11px] font-bold ...">`, `<Input id="pesquisa-texto" ... className="bg-white dark:bg-[#020617] border-slate-300 dark:border-slate-700 focus-visible:ring-blue-500">`; same pattern for both date fields |
| 13 | [AREA-3] Radix `Select` import fully removed (no partial/orphan usage) | VERIFIED | `grep -E '@/components/ui/select"|<SelectTrigger|<SelectItem|<SelectValue|<SelectContent' pareceres/page.tsx` = 0 matches |
| 14 | [AREA-3] Filter/search behavior unchanged — same bound state vars, same `"todos"` sentinel, same query fired | VERIFIED | `draftStatus`/`draftAdvogadoId`/`draftClienteId` (Filtros) and `pesquisaTexto`/`pesquisaClienteId`/`pesquisaAdvogadoId`/`pesquisaStatus`/`pesquisaDataInicio`/`pesquisaDataFim` (Pesquisa Avançada) all unchanged names; `onApply`/`onClear`/`onPesquisar`/`onLimparPesquisa` handlers untouched; `usePareceres(filters)`/`usePesquisarPareceres(pesquisaFilters, ...)` unchanged |
| 15 | [AREA-3] Aplicar/Limpar/Pesquisar/Limpar Filtros buttons keep text (no icon-only, no Tooltip) | VERIFIED | `pareceres/page.tsx:190-197,357-373` — all 4 buttons render `<Check>`/`<X>` icon + visible text (`Aplicar`, `Limpar`, `Pesquisar`/`A pesquisar...`, `Limpar Filtros`); no `Tooltip` import added |

**Score:** 15/15 truths verified (code level)

### Required Artifacts

| Artifact | Expected | Status | Details |
| -------- | ----------- | ------ | ------- |
| `web/src/app/(dashboard)/clientes/[id]/page.tsx` | RBAC derivation + 2 contextual buttons + prop-drilling | ✓ VERIFIED | Contains `Novo Processo`/`Novo Parecer` exactly once each; `canCreateProcessos`/`canCreatePareceres` derived once and prop-drilled |
| `web/src/app/(dashboard)/processos/novo/page.tsx` | `useSearchParams` read, seeded default, locked `NativeSelect` | ✓ VERIFIED | Contains `lockedClienteId`; also contains the CR-01/WR-01 review-fix additions (`lockedClienteMissing`, `lockedClienteMissingUnresolved`) |
| `web/src/app/(dashboard)/pareceres/nova/page.tsx` | Same treatment, direct field-name match | ✓ VERIFIED | Same as above; `clienteId` field maps 1:1 to the query param |
| `webpage/src/lib/get-login-url.ts` | Client-safe, no-throw login URL resolver | ✓ VERIFIED | Exports exactly one function `getLoginUrl(): string`; behavior reproduced and passed 5/5 test cases |
| `webpage/.env.example` | Documents `NEXT_PUBLIC_WEB_APP_URL` | ✓ VERIFIED | Line 3, empty value |
| `webpage/src/components/site-header.tsx` | Desktop + mobile "Entrar" via `getLoginUrl()` | ✓ VERIFIED | 2 occurrences of `href={getLoginUrl()}`; mobile `onClick={() => setOpen(false)}` preserved |
| `webpage/src/components/hero-section.tsx` | Hero "Entrar" via `getLoginUrl()` | ✓ VERIFIED | 1 occurrence |
| `webpage/src/components/site-footer.tsx` | Footer "Entrar" via `getLoginUrl()`, `ArrowRight` preserved | ✓ VERIFIED | 1 occurrence; `<ArrowRight className="h-4 w-4" />` still present after `Entrar` |
| `web/src/app/(dashboard)/pareceres/page.tsx` | Filter panels aligned to Processos/Clientes primitive set | ✓ VERIFIED | 6× `<NativeSelect`, `Input`/`Label` imports present, `@/components/ui/select` import removed |

### Key Link Verification

| From | To | Via | Status | Details |
| ---- | --- | --- | ------ | ------- |
| `ClienteProcessosTab` button | `/processos/novo?clienteId={id}` | `Link href` + `encodeURIComponent` | WIRED | `clientes/[id]/page.tsx:1110` |
| `ClienteParecerTab` button | `/pareceres/nova?clienteId={id}` | `Link href` + `encodeURIComponent` | WIRED | `clientes/[id]/page.tsx:1229` |
| `processos/novo/page.tsx` | intake form `cliente_id` default + disabled expr | `searchParams.get('clienteId')` → `lockedClienteId` | WIRED | Lines 66, 91, 342-346; `Boolean(lockedClienteId)` present in `disabled` |
| `pareceres/nova/page.tsx` | form `clienteId` default + disabled expr | `searchParams.get('clienteId')` → `lockedClienteId` | WIRED | Lines 49, 60, 164-168 |
| `site-header.tsx` (×2) / `hero-section.tsx` / `site-footer.tsx` | `get-login-url.ts` | `import { getLoginUrl }` + `href={getLoginUrl()}` | WIRED | 4/4 call sites confirmed, each file imports the helper |
| `get-login-url.ts` | `process.env.NEXT_PUBLIC_WEB_APP_URL` | read inside function body, no-throw fallback | WIRED | Line 18; read is inside the function, not module scope |
| `pareceres/page.tsx` filter/search state | `NativeSelect`/`Input` primitives | `onChange={(e) => setX(e.target.value)}` | WIRED | Confirmed on all 6 `NativeSelect` + 3 `Input` fields |
| `pareceres/page.tsx` imports | `@/components/ui/native-select`, `input`, `label` | import swap, `select` removed | WIRED | Confirmed via import block read + zero-match grep for the old import |
| `processos:create` / `pareceres:create` (frontend gate) | `@PreAuthorize("hasAuthority(...)")` (backend gate) | unchanged server-side enforcement | WIRED | `ResourceController.java:1240,1273` (`processos:create`), `ParecerController.java:105` (`pareceres:create`) — both layers agree, confirming the phase's threat-model claim that authorization wasn't weakened |

### Data-Flow Trace (Level 4)

This phase does not introduce new data sources. Área 1's buttons are pure navigation links (query-string only, no fetch). Área 2's helper is a pure synchronous string function (no data source at all — traced and behaviorally reproduced above). Área 3 is a primitive swap that explicitly preserves the pre-existing `usePareceres(filters)` / `usePesquisarPareceres(pesquisaFilters, ...)` data flow — confirmed unchanged by reading `onApply`/`onPesquisar` and the `rows`/`resultsLoading`/`resultsError` derivation (`pareceres/page.tsx:95-101`), which still reference the same query hooks feeding the same `filters`/`pesquisaFilters` state that the (now-`NativeSelect`-driven) draft/pesquisa state variables populate. No hollow/disconnected props found.

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
| -------- | ------- | ------ | ------ |
| `getLoginUrl()` trim/fallback logic (5 cases: unset, empty, bare origin, trailing slash, multi-trailing-slash) | Reproduced the function body in an isolated `node -e` script | 5/5 cases match expected output | ✓ PASS |
| No new TypeScript errors introduced across `web/` | `npx tsc --noEmit -p tsconfig.json` | 3 errors, all pre-existing `Cannot find module 'vitest'` in unrelated `*.test.ts` files; none in phase-touched files | ✓ PASS |
| No new ESLint errors/warnings on the 4 modified `web/` files | `npx eslint` on all 4 files | 3 errors + 4 warnings, all individually cross-referenced against `115.1-01-deferred-items.md` and `115.1-REVIEW*.md` as pre-existing/pre-accepted; `pareceres/page.tsx` (Área 3) shows 0 issues | ✓ PASS |
| No new ESLint issues on the 4 modified `webpage/` files | `npx eslint` on `get-login-url.ts` + 3 components | 0 issues | ✓ PASS |
| Diff scope matches claims exactly (9 files, 0 backend files) | `git diff --stat 1daafb3..HEAD -- web/ webpage/` and `-- backend/` | 9 files changed (4 `web/` + 5 `webpage/`); 0 backend files touched | ✓ PASS |
| All 11 commit hashes cited across the 3 SUMMARYs and 3 REVIEW-FIX reports exist in git history | `git cat-file -e <hash>` for each | 11/11 found | ✓ PASS |

Full `pnpm build` was not re-run by this verification (already run and confirmed multiple times by the executor/reviewer across all 3 iterations, including direct inspection of minified bundle output for Área 2; `tsc --noEmit` + `eslint` independently reproduce the same "clean" result here). Consistent with "keep verification fast — grep/file checks, not running the app."

### Requirements Coverage

No formal REQ-IDs exist for this ad hoc phase (inserted after v2.14 milestone completion, not part of `.planning/REQUIREMENTS.md`). Per the phase's own `115.1-CONTEXT.md` and all 3 plans' frontmatter, using the 3 `[AREA-1]`/`[AREA-2]`/`[AREA-3]` Phase Boundary items as the requirement set:

| Requirement | Source Plan | Description | Status | Evidence |
| ----------- | ---------- | ----------- | ------ | -------- |
| AREA-1 | 115.1-01-PLAN.md | Criar Processo/Parecer a partir da Ficha de Cliente | ✓ SATISFIED | Truths 1-6 above, all VERIFIED |
| AREA-2 | 115.1-02-PLAN.md | Redirect "Entrar" do Webpage → Login do Web | ✓ SATISFIED | Truths 7-10 above, all VERIFIED |
| AREA-3 | 115.1-03-PLAN.md | Alinhamento Visual dos Filtros de Pareceres | ✓ SATISFIED | Truths 11-15 above, all VERIFIED |

Confirmed via `grep -n "AREA-1\|AREA-2\|AREA-3" .planning/REQUIREMENTS.md` (0 matches) that these IDs are absent from REQUIREMENTS.md's traceability table — expected, not a gap, per this phase's own 3 SUMMARYs (all 3 independently ran `gsd-sdk query requirements.mark-complete` and got `not_found`) and per this verification's task instructions. `.planning/REQUIREMENTS.md` itself is untouched by this phase (`git diff --stat 1daafb3..HEAD -- .planning/REQUIREMENTS.md` produces no output).

### Anti-Patterns Found

None. Scanned all 9 phase-touched files for `TBD`/`FIXME`/`XXX`/`TODO`/`HACK`/`PLACEHOLDER`/"coming soon"/"not yet implemented" — all matches were false positives (the CSS utility class prefix `placeholder:`, the JSX `placeholder="..."` attribute, and the `"todos"` filter-sentinel string, none of which are debt markers). No empty implementations (`return null`/`=> {}`), no hardcoded-empty stub props, no console.log-only handlers found in any of the 9 files.

Two pre-existing, out-of-scope Info-level findings remain open in the codebase (both explicitly excluded from every fix pass by `fix_scope: critical_warning`, and both pre-dating this phase's diff — not gaps of this phase's must-haves):
- `processos/novo/page.tsx:581-590` — conflict-check "Ver registo" links omit `encodeURIComponent` (low risk: backend-generated UUIDs, not user input).
- `pareceres/nova/page.tsx:122` — post-create redirect `router.push(\`/pareceres/${created.id}\`)` omits `encodeURIComponent`, inconsistent with its `processos/novo` sibling (same low risk).

### Human Verification Required

See frontmatter `human_verification` for the structured list. Summary: 6 items, spanning (1) RBAC-gated button visibility across privileged/unprivileged accounts, (2) the CR-01 cold-cache locked-field pre-selection race fix, (3) the WR-01 missing-cliente recovery-and-clear flow, (4) the actual cross-dev-server local redirect fix that Área 2 exists to deliver, (5) Área 3's light/dark visual parity and live filter/search correctness, and (6) the non-ADMIN advogado graceful-degradation fix. All 6 were explicitly called out as "Manual (phase verifier / UAT)" in the 3 plans' own `<verification>` sections and/or as requiring a live browser session in `115.1-REVIEW.md`'s CR-01 finding ("pnpm build/pnpm lint cannot catch this class of bug since it's a runtime data-timing issue, not a type or lint error") — none of the 3 review iterations record an actual browser session, only code tracing and build/lint re-runs.

### Gaps Summary

No gaps found. All 15 must-have truths, 9 artifacts, and 9 key links across the 3 plans (Área 1/2/3) are verified present and correctly wired in the current codebase — not merely claimed by SUMMARY.md. All 4 code-review findings from 3 review iterations (1 Critical: CR-01 locked-field cold-cache desync; 3 Warnings: WR-01 blank advogado for non-ADMIN, WR-01 missing-cliente no recovery path, WR-01 recovery message never clears) were independently re-verified as present and correctly shaped in the current code, not just claimed fixed. Diff scope (9 files, 0 backend files touched) matches every plan's stated file list exactly. Independent `tsc`/`eslint` runs reproduce the "clean" state claimed by the executor/reviewer. This report routes to `human_needed` rather than `passed` solely because several of the fixed behaviors are runtime-timing or visual/RBAC-session dependent and were only ever verified via code tracing plus type/lint tooling, never an actual browser session — exactly the category the review process itself flagged as needing manual confirmation before shipping.

---

_Verified: 2026-07-22T11:30:00Z_
_Verifier: Claude (gsd-verifier)_
