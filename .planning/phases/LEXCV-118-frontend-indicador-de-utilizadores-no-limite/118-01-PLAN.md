---
phase: 118-frontend-indicador-de-utilizadores-no-limite
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/main/java/com/lexcv/dtos/UserResponse.java
  - backend/src/main/java/com/lexcv/controllers/AuthController.java
  - backend/src/test/java/com/lexcv/controllers/AuthControllerGetMeTenantPlanoTest.java
autonomous: true
requirements: [PLAN-03]

must_haves:
  truths:
    - "GET /api/v1/auth/me devolve tenant_plano (nome do enum TenantPlano como String) e tenant_limite_utilizadores (Integer ou null) a qualquer utilizador autenticado, seja qual for o papel"
    - "tenant_limite_utilizadores a null (plano sem limite) chega ao cliente como null explicito, nunca coagido a 0 nem omitido do contrato"
    - "Um tenant com plano a null nao provoca NullPointerException — tenant_plano vem null e o pedido devolve 200"
    - "tenant_nome e tenant_logo_data_url continuam a ser devolvidos exatamente como antes: os campos novos foram acrescentados DENTRO do bloco tenantRepository.findById(...).ifPresent(...) ja existente"
    - "GET /auth/me continua a fazer exatamente uma consulta ao TenantRepository por pedido — zero queries novas"
    - "O tenant lido e sempre o do principal autenticado (principal.getTenantId()); nenhum tenant id e alguma vez lido do pedido"
    - "A verificacao autoritaria do limite (AdminController.limiteUtilizadoresExcedido, Phase 117) permanece intacta — esta fase nao lhe toca e a sua suite continua verde"
  artifacts:
    - path: "backend/src/main/java/com/lexcv/dtos/UserResponse.java"
      provides: "Os 2 campos novos no contrato JSON de /auth/me, em snake_case como os irmaos tenant_*"
      contains: "private Integer tenant_limite_utilizadores;"
    - path: "backend/src/main/java/com/lexcv/controllers/AuthController.java"
      provides: "Mapeamento dos 2 campos dentro do bloco ifPresent existente do getMe"
      contains: "setTenant_limite_utilizadores"
    - path: "backend/src/test/java/com/lexcv/controllers/AuthControllerGetMeTenantPlanoTest.java"
      provides: "Prova automatizada dos 4 comportamentos (limite numerico, limite null, plano null, nao-regressao + query unica)"
      contains: "getTenant_limite_utilizadores"
      min_lines: 80
  key_links:
    - from: "backend/src/main/java/com/lexcv/controllers/AuthController.java"
      to: "backend/src/main/java/com/lexcv/models/Tenant.java"
      via: "leitura do limite do tenant do principal, dentro do ifPresent ja existente (zero queries novas)"
      pattern: "tenantRepository\\.findById\\(principal\\.getTenantId\\(\\)\\)"
    - from: "backend/src/main/java/com/lexcv/controllers/AuthController.java"
      to: "backend/src/main/java/com/lexcv/dtos/UserResponse.java"
      via: "setters Lombok dos 2 campos novos, com passagem direta de null"
      pattern: "setTenant_limite_utilizadores\\(t\\.getLimiteUtilizadores\\(\\)\\)"
---

<objective>
Expor `tenant_plano` e `tenant_limite_utilizadores` no `GET /api/v1/auth/me`, acrescentando-os ao `UserResponse` e ao bloco `tenantRepository.findById(...).ifPresent(...)` que ja existe no `AuthController.getMe`. Sem endpoint novo, sem query nova, sem superficie de autorizacao nova.

Purpose: este e o gap real que a Phase 117 (backend-only) nao cobriu e que bloqueia as proprias Success Criteria da Phase 118 — hoje nenhum endpoint expoe `limiteUtilizadores` ao frontend, pelo que o `Y` do indicador "X/Y utilizadores" nao existe do lado do cliente. Fecha-se aqui, minimamente, estendendo um endpoint existente.

Output: `UserResponse.java` com 2 campos novos, `AuthController.java` com 2 chamadas de setter dentro do lambda ja existente, e um teste Mockito novo com 4 casos. Nenhum outro ficheiro de backend e tocado.
</objective>

<execution_context>
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/LEXCV-118-frontend-indicador-de-utilizadores-no-limite/118-CONTEXT.md
@.planning/phases/LEXCV-118-frontend-indicador-de-utilizadores-no-limite/118-PATTERNS.md

# 118-PATTERNS.md tem o corpo integral do bloco a estender, o ficheiro completo do
# UserResponse, e os tipos-fonte confirmados em Tenant.java. Usa-o como fonte primaria
# em vez de reexplorar o codebase.
</context>

<interfaces>
<!-- Contratos ja existentes no codebase. O executor NAO precisa de os redescobrir. -->

`backend/src/main/java/com/lexcv/models/TenantPlano.java` (completo):
```java
public enum TenantPlano { STARTER, STANDARD, ENTERPRISE }
```

`backend/src/main/java/com/lexcv/models/Tenant.java` (getters relevantes, Lombok `@Getter`):
```java
private TenantPlano plano;          // getPlano()  — pode ser null
private Integer limiteUtilizadores; // getLimiteUtilizadores() — null = sem limite (Phase 117)
private String nome;                // getNome()
private String logoDataUrl;         // getLogoDataUrl()
```

`backend/src/main/java/com/lexcv/config/UserPrincipal.java` (Lombok `@Builder` + `@Getter`):
```java
UserPrincipal.builder().userId(UUID).tenantId(UUID).nome(String).email(String)
             .roles(Set<String>).permissions(Set<String>).authorities(...).build();
// getUsername() devolve email; getPassword() devolve null
```

`backend/src/main/java/com/lexcv/controllers/AuthController.java` — campos `final` por ordem de
declaracao (a assinatura gerada por `@RequiredArgsConstructor` segue exatamente esta ordem):
```java
private final UserRepository userRepository;
private final TenantRepository tenantRepository;
private final JwtTokenProvider tokenProvider;
private final PasswordEncoder passwordEncoder;
// => new AuthController(userRepository, tenantRepository, tokenProvider, passwordEncoder)
```

Bloco exato a estender (`getMe`, linhas 169-172 — NAO acrescentar um segundo `.ifPresent`):
```java
tenantRepository.findById(principal.getTenantId()).ifPresent(t -> {
    response.setTenant_nome(t.getNome());
    response.setTenant_logo_data_url(t.getLogoDataUrl());
});
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Expor tenant_plano e tenant_limite_utilizadores no GET /auth/me (teste primeiro)</name>

  <files>
    backend/src/test/java/com/lexcv/controllers/AuthControllerGetMeTenantPlanoTest.java (novo)
    backend/src/main/java/com/lexcv/dtos/UserResponse.java (modificar)
    backend/src/main/java/com/lexcv/controllers/AuthController.java (modificar)
  </files>

  <read_first>
    - `.planning/phases/LEXCV-118-frontend-indicador-de-utilizadores-no-limite/118-PATTERNS.md` — seccoes "AuthController.java" e "UserResponse.java": tem o bloco `ifPresent` integral a estender, o ficheiro completo do DTO, e a justificacao dos tipos (`.name()` para o enum, `Integer` direto para o limite).
    - `.planning/phases/LEXCV-118-frontend-indicador-de-utilizadores-no-limite/118-CONTEXT.md` — seccao `<decisions>`, bloco "Backend gap": a decisao de estender `/auth/me` em vez de criar endpoint novo, e o contrato `null = sem limite`.
    - `backend/src/main/java/com/lexcv/controllers/AuthController.java` — ler `getMe` por inteiro (linhas 146-175) antes de editar.
    - `backend/src/test/java/com/lexcv/controllers/AdminControllerLimiteUtilizadoresTest.java` — analog de teste mais proximo (Phase 117): `@ExtendWith(MockitoExtension.class)`, instanciacao direta do controller com mocks, `SecurityContextHolder` populado com `UsernamePasswordAuthenticationToken(UserPrincipal..., null, List.of())` e `@AfterEach` com `SecurityContextHolder.clearContext()`. Nao existe harness MockMvc/`@SpringBootTest` neste codebase — nao introduzir um.
    - `backend/src/test/java/com/lexcv/controllers/PublicControllerTest.java` — segundo analog, para o estilo de Javadoc de classe e de asserts (`assertEquals`/`assertNull`).
  </read_first>

  <behavior>
    Casos que `AuthControllerGetMeTenantPlanoTest` deve provar. Escrever os 4 ANTES de tocar em
    `UserResponse.java`/`AuthController.java` — devem falhar a compilar (os getters `getTenant_plano()`
    e `getTenant_limite_utilizadores()` ainda nao existem). Commitar o RED e o GREEN separadamente,
    como na Phase 117.

    - **Caso 1 — plano e limite numericos chegam ao contrato.** Tenant com `plano=TenantPlano.STANDARD`
      e `limiteUtilizadores=5`. `getMe()` devolve `HttpStatus.OK`; o corpo (cast para `UserResponse`)
      tem `getTenant_plano()` igual a `"STANDARD"` (String, nao o enum) e `getTenant_limite_utilizadores()`
      igual a `5`.
    - **Caso 2 — limite null passa como null, nunca 0.** Tenant com `plano=TenantPlano.ENTERPRISE` e
      `limiteUtilizadores=null`. `getMe()` devolve `OK`, `getTenant_plano()` e `"ENTERPRISE"`, e
      `getTenant_limite_utilizadores()` e `null` (usar `assertNull`, e nao `assertEquals(0, ...)`) —
      este e o contrato "sem limite" da Phase 117 a atravessar a fronteira HTTP intacto.
    - **Caso 3 — plano null nao rebenta.** Tenant com `plano=null` e `limiteUtilizadores=10`.
      `getMe()` devolve `OK` sem lancar `NullPointerException`; `getTenant_plano()` e `null` e
      `getTenant_limite_utilizadores()` e `10`. Prova a guarda ternaria a volta de `.name()`.
    - **Caso 4 — nao-regressao dos campos irmaos + query unica.** Tenant com `nome="Escritorio X"`,
      `logoDataUrl="data:image/png;base64,AAA"`, `plano=STARTER`, `limiteUtilizadores=3`. Asserir que
      `getTenant_nome()` e `getTenant_logo_data_url()` continuam preenchidos ao mesmo tempo que os 2
      campos novos, e asserir `verify(tenantRepository, times(1)).findById(TENANT_ID)` — prova que os
      setters novos ficaram DENTRO do lambda existente e que nenhuma segunda consulta foi introduzida
      (decisao explicita de CONTEXT.md: "zero extra query").

    Notas de montagem (todas confirmadas na leitura de planeamento):
    - Mocks `@Mock`: `UserRepository`, `TenantRepository`, `JwtTokenProvider`, `PasswordEncoder`.
      Instanciar com `new AuthController(userRepository, tenantRepository, tokenProvider, passwordEncoder)`.
    - `getMe()` chama sempre `userRepository.findById(principal.getUserId())` — sob `MockitoExtension`
      (strict stubs) esse stub e obrigatorio em todos os casos; devolver `Optional.empty()` chega
      (o `ifPresent` faz no-op e o builder ja preencheu nome/email a partir do principal).
    - `SecurityContextHolder` populado com `UserPrincipal.builder().userId(USER_ID).tenantId(TENANT_ID).build()`
      dentro de um helper privado, e limpo em `@AfterEach`. `roles`/`permissions`/`email` a null nao
      provocam NPE em `getMe` (verificado no planeamento).
    - `Tenant` constroi-se com `Tenant.builder()` (Lombok `@Builder` presente).
    - Javadoc de classe a explicar o que e provado e porque, no estilo de `PublicControllerTest`.
  </behavior>

  <action>
    Escrever primeiro `backend/src/test/java/com/lexcv/controllers/AuthControllerGetMeTenantPlanoTest.java`
    com os 4 casos do bloco `<behavior>`, correr `cd backend && mvn test -Dtest=AuthControllerGetMeTenantPlanoTest`
    e confirmar que falha (RED — erro de compilacao por getters inexistentes). Commitar o RED.

    Depois, `backend/src/main/java/com/lexcv/dtos/UserResponse.java`: acrescentar exatamente dois campos
    a seguir a `private String tenant_logo_data_url;` (ultimo campo actual), mantendo o snake_case dos
    irmaos `tenant_*` (Jackson serializa pelo nome do campo; nao existe `@JsonProperty` nesta classe e
    nao se deve introduzir nenhum):
    `private String tenant_plano;` e `private Integer tenant_limite_utilizadores;`.
    O tipo TEM de ser `Integer` (boxed) e nunca `int` — `int` coagiria "sem limite" a `0`, destruindo o
    contrato da Phase 117. As anotacoes Lombok `@Data`/`@Builder`/`@NoArgsConstructor`/`@AllArgsConstructor`
    ja presentes geram getters/setters/builder automaticamente; nao escrever boilerplate manual.

    Depois, `backend/src/main/java/com/lexcv/controllers/AuthController.java`: acrescentar exatamente
    duas linhas DENTRO do lambda `tenantRepository.findById(principal.getTenantId()).ifPresent(t -> {...})`
    que ja existe no `getMe` (linhas 169-172), a seguir a `setTenant_logo_data_url`:
    - `response.setTenant_plano(t.getPlano() != null ? t.getPlano().name() : null);` — a guarda ternaria
      e obrigatoria (`Tenant.plano` e nullable) e `.name()` da a String simples que o contrato
      `MeResponse.tenant_plano?: string` do frontend espera. Nao usar `String.valueOf(...)` (produziria
      a String literal `"null"`), nao usar `toString()`, nao introduzir serializador de enum nenhum.
    - `response.setTenant_limite_utilizadores(t.getLimiteUtilizadores());` — atribuicao direta, sem
      ternario e sem `orElse`, para o `null` atravessar intacto.

    Correr o teste outra vez e confirmar GREEN. Commitar o GREEN em separado do RED.

    Proibicoes explicitas:
    - NAO criar um endpoint novo, um controller novo nem um DTO novo — CONTEXT.md exclui-o explicitamente.
    - NAO acrescentar um segundo `tenantRepository.findById(...)` nem um segundo bloco `.ifPresent(...)`
      (o gate de origem reprova: a contagem tem de ficar em 1).
    - NAO acrescentar `@PreAuthorize` a `getMe` nem a qualquer metodo do `AuthController` — nenhum metodo
      desta classe tem `@PreAuthorize` hoje e esta fase nao introduz superficie de autorizacao nova.
      A restricao por papel vive no frontend (visibilidade do separador "Utilizadores"), nao aqui.
    - NAO tocar em `AdminController.java`, em `limiteUtilizadoresExcedido`, nem em qualquer teste da
      Phase 117 — a aplicacao autoritaria do limite fica exatamente como esta.
    - NAO alterar `login`, `refresh`, `updateMe`, `changePassword` nem qualquer outro metodo do
      `AuthController`.
    - NAO acrescentar dependencias ao `pom.xml`.
  </action>

  <verify>
    <automated>cd backend && mvn test -Dtest=AuthControllerGetMeTenantPlanoTest && grep -c 'private Integer tenant_limite_utilizadores;' src/main/java/com/lexcv/dtos/UserResponse.java && grep -c 'private String tenant_plano;' src/main/java/com/lexcv/dtos/UserResponse.java && grep -v '^[[:space:]]*[/*]' src/main/java/com/lexcv/controllers/AuthController.java | grep -c 'setTenant_limite_utilizadores(t.getLimiteUtilizadores())' && grep -v '^[[:space:]]*[/*]' src/main/java/com/lexcv/controllers/AuthController.java | grep -c 'tenantRepository.findById(principal.getTenantId())'</automated>
  </verify>

  <acceptance_criteria>
    - `cd backend && mvn test -Dtest=AuthControllerGetMeTenantPlanoTest` termina com exit code 0 e reporta 4 testes executados, 0 falhas, 0 erros.
    - O ficheiro de teste contem 4 metodos `@Test` distintos (`grep -c '@Test'` devolve 4), um por caso do bloco `<behavior>`.
    - O Caso 2 usa `assertNull(...)` sobre `getTenant_limite_utilizadores()` — `grep -c 'assertNull'` no ficheiro de teste devolve pelo menos 2 (limite null e plano null).
    - O Caso 4 contem `verify(tenantRepository, times(1)).findById` — prova de consulta unica.
    - Em `UserResponse.java`: `grep -c 'private Integer tenant_limite_utilizadores;'` devolve 1 e `grep -c 'private int tenant_limite_utilizadores'` devolve 0 (tipo boxed, nunca primitivo).
    - Em `UserResponse.java`: `grep -c '@JsonProperty'` devolve 0 — a convencao de serializacao por nome de campo mantem-se.
    - Em `AuthController.java`, filtrando comentarios: `setTenant_limite_utilizadores(t.getLimiteUtilizadores())` aparece 1 vez, `setTenant_plano(` aparece 1 vez, e `tenantRepository.findById(principal.getTenantId())` aparece **exatamente 1 vez** (zero queries novas, um so bloco `ifPresent`).
    - Em `AuthController.java`, filtrando comentarios, `grep -c 'String.valueOf(t.getPlano())\|t.getPlano().toString()'` devolve 0.
    - Em `AuthController.java`, `grep -c '@PreAuthorize'` devolve 0 — nenhuma anotacao de autorizacao nova.
    - `git diff --stat backend/` lista apenas os 3 ficheiros declarados em `files_modified`; `backend/pom.xml` e `AdminController.java` nao aparecem no diff.
  </acceptance_criteria>

  <done>`GET /api/v1/auth/me` devolve `tenant_plano` (String ou null) e `tenant_limite_utilizadores` (Integer ou null) para qualquer sessao autenticada, sem query adicional e sem quebrar `tenant_nome`/`tenant_logo_data_url` — provado por 4 testes automatizados verdes.</done>
</task>

<task type="auto">
  <name>Task 2: Gate de regressao, SAST e verificacao das mitigacoes STRIDE do backend</name>

  <files>
    backend/src/main/java/com/lexcv/controllers/AuthController.java (condicional — so se a suite ou o SAST exigirem correcao)
    backend/src/test/java/com/lexcv/controllers/AuthControllerGetMeTenantPlanoTest.java (condicional — idem)
  </files>

  <read_first>
    - `backend/src/main/java/com/lexcv/controllers/AuthController.java` — estado pos-Task 1, para as assercoes de origem das mitigacoes.
    - `backend/src/test/java/com/lexcv/controllers/AdminControllerLimiteUtilizadoresTest.java` — a suite da Phase 117 que TEM de continuar verde; e a prova de que a aplicacao autoritaria do 409 nao foi tocada.
    - `backend/spotbugs-exclude.xml` — ficheiro de exclusoes, para distinguir um achado novo de um ja excluido.
    - `CLAUDE.md` — seccao Commands (backend): `mvn test`, `mvn spotbugs:check`, `mvn -DskipTests package`.
    - `.planning/STATE.md` — Blockers/Concerns: os testes `*IT` (Testcontainers, corridos por `mvn verify`) tem um bloqueio ambiental conhecido de Docker nesta maquina e passam em CI; nao os usar como gate local.
  </read_first>

  <action>
    Correr a suite unitaria completa do backend (`mvn test`, Surefire, ficheiros `*Test.java`) e confirmar
    que fica verde. A alteracao desta fase e puramente aditiva (2 campos num DTO Lombok + 2 setters), pelo
    que a unica fonte plausivel de regressao seria um teste que assere a forma completa do `UserResponse`;
    se algum falhar, corrigir a causa nos ficheiros desta fase — nunca enfraquecer nem apagar uma assercao
    pre-existente para a suite passar.

    NAO usar `mvn verify` como gate local (testes `*IT` em Testcontainers, bloqueio ambiental de Docker
    conhecido — ver STATE.md). Esta fase nao acrescenta nenhum teste `*IT`.

    Correr `mvn spotbugs:check` (SpotBugs + FindSecBugs, gate SAST do projeto, ASVS L1, bloqueio em
    severidade `high`) e confirmar que passa sem achados novos. Se surgir um achado, corrigi-lo no codigo;
    so acrescentar entrada a `spotbugs-exclude.xml` se for falso positivo comprovado, documentando a
    justificacao no SUMMARY.

    Confirmar por assercao de origem que as mitigacoes do `<threat_model>` estao de facto no codigo
    entregue, e registar no SUMMARY um veredito explicito (CONFIRMADO / CORRIGIDO) por item, no espirito
    da auditoria AUD-01 da v2.11 — veredito por item, nao checklist generica:
    - **T-118-01 (Information Disclosure — exposicao dos campos novos a todos os papeis):** confirmar que
      os 2 campos novos sao metadados comerciais do proprio tenant do chamador (nome de plano + inteiro de
      capacidade), da mesma classe e com a mesma audiencia dos ja pre-existentes `tenant_nome`/
      `tenant_logo_data_url`, e que ambos sao lidos exclusivamente a partir de
      `tenantRepository.findById(principal.getTenantId())` — logo nenhum chamador consegue observar o plano
      de outro tenant. Nao e segredo, nao e PII, nao e credencial. Disposicao: `accept`, com precedente
      citado.
    - **T-118-02 (Elevation of Privilege — a UI nao pode virar o gate):** confirmar por diff que
      `AdminController.limiteUtilizadoresExcedido` e `createUser` nao foram tocados nesta fase e que
      `AdminControllerLimiteUtilizadoresTest` continua verde — a aplicacao autoritaria do `409` continua a
      ser a unica fonte de verdade do limite; o indicador do Plan 02 e espelho, nunca gate.
    - **T-118-03 (Tampering — leitura cross-tenant):** confirmar que nenhum tenant id e lido do pedido em
      `getMe` e que o unico `findById` usa `principal.getTenantId()`.
    - **T-118-SC (Tampering — cadeia de fornecimento):** confirmar que `backend/pom.xml` nao aparece no
      diff da fase — zero dependencias novas, Package Legitimacy Gate nao se aplica.

    Confirmar por fim que `mvn -DskipTests package` produz o artefacto sem erros e que `git status --porcelain`
    nao lista ficheiros de backend fora dos declarados em `files_modified` deste plano.
  </action>

  <verify>
    <automated>cd backend && mvn test && mvn spotbugs:check && mvn -q -DskipTests package && mvn test -Dtest=AdminControllerLimiteUtilizadoresTest && git diff --name-only -- pom.xml src/main/java/com/lexcv/controllers/AdminController.java | wc -l</automated>
  </verify>

  <acceptance_criteria>
    - `cd backend && mvn test` termina com exit code 0 (BUILD SUCCESS), 0 falhas e 0 erros; o numero total de testes executados e igual ao de antes desta fase mais 4.
    - `cd backend && mvn test -Dtest=AdminControllerLimiteUtilizadoresTest` continua a reportar 4 testes verdes — a aplicacao do limite da Phase 117 esta intacta (T-118-02).
    - `cd backend && mvn spotbugs:check` termina com exit code 0, sem achados novos. Qualquer entrada nova em `spotbugs-exclude.xml` esta justificada por escrito no SUMMARY.
    - `cd backend && mvn -q -DskipTests package` termina com exit code 0.
    - `git diff --name-only -- backend/pom.xml backend/src/main/java/com/lexcv/controllers/AdminController.java` devolve zero linhas (T-118-02 e T-118-SC).
    - Em `AuthController.java`, filtrando comentarios, `grep -c 'body.get("tenant_id")\|body.get("tenantId")\|containsKey("tenant_id")'` devolve 0 (T-118-03).
    - Em `AuthController.java`, filtrando comentarios, `grep -c 'tenantRepository.findById'` devolve 1 e `grep -c 'tenantRepository.findById(principal.getTenantId())'` devolve 1 — a unica leitura de tenant e a do principal (T-118-03).
    - O SUMMARY contem um veredito explicito (CONFIRMADO ou CORRIGIDO) para cada um dos 4 itens T-118-01/02/03/SC, incluindo a frase de precedente para T-118-01 (`tenant_nome` ja e servido a todos os papeis autenticados desde antes desta fase).
  </acceptance_criteria>

  <done>A suite unitaria do backend esta verde (incluindo a da Phase 117 inalterada), o SAST passa sem achados novos, o artefacto compila, e as 4 mitigacoes STRIDE tem veredito explicito registado no SUMMARY.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Sessao autenticada (qualquer papel) → `GET /api/v1/auth/me` | O corpo da resposta passa a transportar 2 campos tenant-scoped adicionais; o pedido em si nao traz input aplicacional (so o cookie JWT ja validado por `JwtAuthenticationFilter`) |
| Tenant A ↔ Tenant B | Os campos novos sao lidos com o tenant id do principal; nenhum caminho permite observar o plano/limite de outro tenant |
| UI (espelho) ↔ Backend (autoritario) | O indicador da Phase 118 e informativo; a unica aplicacao real do limite continua a ser `AdminController.limiteUtilizadoresExcedido` (Phase 117), fora do ambito de escrita desta fase |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-118-01 | Information Disclosure | `AuthController.getMe` — `tenant_plano` e `tenant_limite_utilizadores` devolvidos a TODOS os papeis autenticados, nao so a ADMIN | accept | **Nao e exposicao sensivel nova.** Os 2 campos sao metadados comerciais do proprio tenant do chamador: um nome de escalao (`STARTER`/`STANDARD`/`ENTERPRISE`) e um inteiro de capacidade contratada. Nao sao segredo, PII, credencial nem dado de negocio de clientes. Sao exatamente da mesma classe e da mesma audiencia que os pre-existentes `tenant_nome` e `tenant_logo_data_url`, servidos por este mesmo metodo a todos os papeis desde antes desta fase (precedente confirmado na v2.15, Phase 116: `/auth/me` serve o nome do tenant a qualquer utilizador autenticado). Nao ha leitura cross-tenant: os valores vem de `tenantRepository.findById(principal.getTenantId())`, chaveado pelo claim de tenant do proprio JWT. A renderizacao continua gated ao separador "Utilizadores" (`users:manage`/ADMIN) no frontend. ASVS L1 V8.1 satisfeito — nenhum dado sensivel acrescentado |
| T-118-02 | Elevation of Privilege | Confianca no limite exposto ao cliente | mitigate | O valor exposto e um espelho de UX, nunca o gate. `AdminController.limiteUtilizadoresExcedido` (Phase 117, ja implementado e testado, incluindo a correcao de bypass por reativacao) continua a ser a unica verificacao autoritaria e **nao e tocado por esta fase**. Um `POST /api/v1/admin/users` forjado com o limite adulterado no cliente continua a receber `409`. Task 2 reprova se `AdminController.java` aparecer no diff e re-corre a suite da Phase 117 |
| T-118-03 | Tampering | Origem do tenant id em `getMe` | mitigate | O unico `tenantRepository.findById` do ficheiro usa `principal.getTenantId()`, resolvido a partir do `SecurityContextHolder`. Nenhum `tenant_id` e lido do corpo ou de query params. Gate de origem na Task 2 reprova qualquer leitura de tenant a partir do pedido, e reprova tambem uma segunda chamada `findById` |
| T-118-04 | Denial of Service | Custo adicional por pedido de `/auth/me` | accept | Zero custo adicional: os 2 campos sao lidos da entidade `Tenant` **ja carregada** pelo `findById` existente. Nenhuma query nova, nenhuma coluna `@Lob` nova (o `logoDataUrl`, ja existente, e o unico campo pesado e mantem-se inalterado). Caso 4 do teste assere `times(1)` sobre `findById` |
| T-118-05 | Repudiation | Ausencia de auditoria da leitura do plano | accept | `/auth/me` e uma leitura de sessao de altissima frequencia (polling/refocus em toda a app); registar cada leitura do proprio plano do tenant geraria ruido sem valor forense. Nenhum requisito ASVS L1 exige logging de leitura de metadados nao sensiveis do proprio tenant |
| T-118-SC | Tampering | Instalacoes npm/pip/cargo/Maven | n/a | Esta fase nao instala nenhum pacote nem acrescenta dependencias — `backend/pom.xml` nao e tocado (gate na Task 2). O Package Legitimacy Gate nao se aplica |
</threat_model>

<verification>
1. `cd backend && mvn test -Dtest=AuthControllerGetMeTenantPlanoTest` — 4 testes, 0 falhas.
2. `cd backend && mvn test` — suite unitaria completa verde, incluindo `AdminControllerLimiteUtilizadoresTest` da Phase 117 inalterada.
3. `cd backend && mvn spotbugs:check` — sem achados novos.
4. `cd backend && mvn -q -DskipTests package` — exit 0.
5. Gates de origem: `tenantRepository.findById` aparece exatamente 1 vez; `@PreAuthorize` aparece 0 vezes; `pom.xml` e `AdminController.java` fora do diff.
6. `git status --porcelain` sem ficheiros de backend fora do ambito declarado.
</verification>

<success_criteria>
- `GET /api/v1/auth/me` devolve `tenant_plano` (String do nome do enum, ou `null`) e `tenant_limite_utilizadores` (`Integer`, ou `null` para "sem limite") a qualquer utilizador autenticado.
- O contrato `null = sem limite` da Phase 117 atravessa a fronteira HTTP intacto — nunca coagido a `0` nem omitido.
- `tenant_nome` e `tenant_logo_data_url` continuam a ser devolvidos como antes, a partir do mesmo e unico `ifPresent`.
- Nenhum endpoint novo, nenhum DTO novo, nenhuma query nova, nenhuma anotacao de autorizacao nova.
- A aplicacao autoritaria do limite (Phase 117) fica intacta e provada verde.
</success_criteria>

<output>
Criar `.planning/phases/LEXCV-118-frontend-indicador-de-utilizadores-no-limite/118-01-SUMMARY.md` quando terminar, incluindo o veredito explicito por mitigacao (T-118-01/02/03/SC) e a forma JSON exata que o Plan 02 vai consumir.
</output>
