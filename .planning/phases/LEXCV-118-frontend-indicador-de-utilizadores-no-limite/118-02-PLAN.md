---
phase: 118-frontend-indicador-de-utilizadores-no-limite
plan: 02
type: execute
wave: 2
depends_on: [118-01]
files_modified:
  - web/src/types/auth.ts
  - web/src/app/(dashboard)/settings/page.tsx
  - web/scripts/verify-limite-utilizadores-indicator.mjs
  - web/package.json
autonomous: true
requirements: [PLAN-03]

must_haves:
  truths:
    - "A aba 'Gestão de Utilizadores' mostra 'X/Y utilizadores' por cima do botão 'Novo Utilizador', com X = utilizadores ativos e Y = limite contratado do tenant"
    - "Quando o tenant nao tem limite (limite null ou ainda desconhecido), o mesmo sitio le 'X utilizadores' sem barra e o botao 'Novo Utilizador' NUNCA e desativado"
    - "Quando X >= Y, o botao 'Novo Utilizador' fica desativado e o contador le 'X/Y utilizadores · limite atingido' a vermelho e semibold — legivel sem passar o rato por cima"
    - "Passar o rato OU dar foco por teclado ao botao desativado mostra o tooltip 'Limite de utilizadores atingido. Desative um utilizador para libertar uma vaga.' — o tooltip dispara mesmo com o botao nativamente disabled"
    - "Um 409 devolvido pelo backend aparece como toast com a frase limpa do backend, sem o prefixo 'API 409: ', e a UI nao rebenta"
    - "O indicador consome a query partilhada ['auth','me'] via useMe() dentro do proprio UserManagementTab — sem prop-drilling e sem segundo pedido de rede"
    - "A contagem X usa ativo === true (semantica identica a countByTenantIdAndAtivoTrue do backend), enquanto o badge 'Ativo' da tabela mantem a sua propria convencao user.ativo !== false — as duas continuam deliberadamente diferentes"
  artifacts:
    - path: "web/src/types/auth.ts"
      provides: "Contrato TypeScript dos 2 campos novos de /auth/me"
      contains: "tenant_limite_utilizadores?: number | null;"
    - path: "web/src/app/(dashboard)/settings/page.tsx"
      provides: "Indicador X/Y, estado desativado do botao com tooltip explicativo, e toast de erro sem prefixo de status"
      contains: "limite atingido"
    - path: "web/scripts/verify-limite-utilizadores-indicator.mjs"
      provides: "Gate automatizado de regressao das 8 assercoes de origem (inclui a tecnica span-wrapper que impede o regresso do bug tooltip+disabled)"
      min_lines: 60
  key_links:
    - from: "web/src/app/(dashboard)/settings/page.tsx"
      to: "web/src/hooks/use-me.ts"
      via: "useMe() chamado dentro do UserManagementTab, deduplicado pela cache ['auth','me']"
      pattern: "useMe\\(\\)"
    - from: "web/src/app/(dashboard)/settings/page.tsx"
      to: "web/src/components/ui/tooltip.tsx"
      via: "TooltipTrigger asChild sobre um <span tabIndex={0}> que envolve o Button disabled"
      pattern: "<TooltipTrigger asChild>\\s*<span[^>]*tabIndex=\\{0\\}"
    - from: "web/src/app/(dashboard)/settings/page.tsx"
      to: "web/src/lib/api.ts"
      via: "strip generico do prefixo 'API NNN: ' lancado por apiFetch"
      pattern: "\\^API \\\\d\\{3\\}: "
---

<objective>
Fechar PLAN-03 na interface: a aba "Gestão de Utilizadores" passa a mostrar a ocupação do plano ("X/Y utilizadores"), desativa o botão "Novo Utilizador" quando o limite é atingido — com o motivo visível sem hover e um tooltip que **realmente dispara** sobre um botão desativado — e apresenta um eventual `409` do backend como toast limpo.

Purpose: entrega as 3 Success Criteria da Phase 118. É também a primeira correção efetiva do bug de composição `Tooltip` + `Button disabled` documentado como dívida desde a Phase 102 (v2.13) — daí o gate de origem estrutural que impede o seu regresso silencioso.

Output: `MeResponse` estendido, `UserManagementTab` com contador + estado desativado + tooltip + toast corrigido, e um script de verificação executável (`pnpm verify:limite-utilizadores`) que é o gate de regressão desta fase.
</objective>

<execution_context>
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@web/AGENTS.md
@.planning/phases/LEXCV-118-frontend-indicador-de-utilizadores-no-limite/118-CONTEXT.md
@.planning/phases/LEXCV-118-frontend-indicador-de-utilizadores-no-limite/118-UI-SPEC.md
@.planning/phases/LEXCV-118-frontend-indicador-de-utilizadores-no-limite/118-PATTERNS.md
@.planning/phases/LEXCV-118-frontend-indicador-de-utilizadores-no-limite/118-01-SUMMARY.md

# 118-UI-SPEC.md tem o contrato de copy verbatim, a tabela de cor/tipografia e as 6 Interaction Notes.
# 118-PATTERNS.md tem o JSX atual do CardHeader, os 2 analogs a combinar para o span-wrapper, e o
# bloco catch a corrigir. Usa-os como fonte primaria em vez de reexplorar o codebase.
# Este plano NAO toca em nenhuma API de routing/data-fetching do Next.js 16 — nao ha necessidade de
# consultar web/node_modules/next/dist/docs/ para esta alteracao (e um componente cliente ja existente).
</context>

<interfaces>
<!-- Contratos ja existentes ou entregues pelo Plan 01 (wave 1). Nao explorar o codebase para os redescobrir. -->

Forma JSON entregue pelo Plan 01 em `GET /api/v1/auth/me` (campos relevantes):
```json
{ "tenant_nome": "…", "tenant_logo_data_url": "…", "tenant_plano": "STANDARD", "tenant_limite_utilizadores": 5 }
```
`tenant_plano` e `null` quando o tenant nao tem plano; `tenant_limite_utilizadores` e `null` quando nao ha limite.

`web/src/types/auth.ts` — estado ATUAL de `MeResponse` (linhas 17-29), a estender no fim:
```typescript
export interface MeResponse {
  id: string; tenant_id: string; nome: string; email: string;
  roles: Role[]; avatar_url?: string; telefone?: string; ativo?: boolean;
  permissions: string[];
  tenant_nome?: string;
  tenant_logo_data_url?: string;
}
```

`web/src/hooks/use-me.ts` (completo — consumido, NAO modificado):
```typescript
export function useMe() {
  return useQuery({ queryKey: ["auth", "me"], queryFn: () => apiFetch<MeResponse>("/auth/me"),
                    enabled: typeof window !== "undefined", staleTime: 60_000 });
}
```
`data` e `MeResponse | undefined`. `queryKey` partilhada com o `usePermissions()` ja montado no
`SettingsPage` pai — chamar `useMe()` outra vez dentro do `UserManagementTab` e um hit de cache,
nao um segundo pedido de rede.

`web/src/server/mock-db.ts` — `MockUser.ativo` e `ativo?: boolean` (opcional). Por isso a contagem
tem de usar `=== true` e nao `!== false`.

`web/src/lib/api.ts:26-47` — formato de erro lancado por `apiFetch` (NAO modificar este ficheiro):
```typescript
if (res.status !== 401 && res.status !== 403) { toast.error(`Erro ${res.status}: ${errorMessage}`); }
throw new Error(`API ${res.status}: ${errorMessage}`);
```
`errorMessage` ja e o campo `message` do JSON do backend — para o `409` da Phase 117 e exatamente
`Limite de utilizadores atingido para o vosso plano.`

`web/src/app/(dashboard)/settings/page.tsx:351-365` — JSX ATUAL do `CardHeader` a alterar:
```tsx
<CardHeader className="flex flex-row items-center justify-between space-y-0">
  <div>
    <CardTitle className="text-xl font-semibold">Utilizadores Registados</CardTitle>
    <CardDescription>Lista de profissionais com credenciais de acesso ao sistema LexCV.</CardDescription>
  </div>
  <Button
    onClick={handleCreateClick}
    className="bg-blue-600 hover:bg-blue-700 text-white flex items-center gap-1.5 shadow-sm text-xs py-1.5 px-3 h-auto"
  >
    <Plus className="h-4 w-4" />
    Novo Utilizador
  </Button>
</CardHeader>
```

`web/src/app/(dashboard)/pareceres/[id]/page.tsx:302-311` — unico precedente da tecnica span-wrapper:
```tsx
<Tooltip>
  <TooltipTrigger asChild>
    <span tabIndex={0} aria-label={…} className="…" />
  </TooltipTrigger>
  <TooltipContent>{…}</TooltipContent>
</Tooltip>
```

`web/src/components/ui/button.tsx:8` — a razao pela qual o wrapper e obrigatorio:
```
"… disabled:pointer-events-none disabled:opacity-50 …"
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Contrato MeResponse + caminho de erro 409 limpo + gate de verificacao executavel</name>

  <files>
    web/scripts/verify-limite-utilizadores-indicator.mjs (novo)
    web/package.json (modificar — 1 entrada em scripts)
    web/src/types/auth.ts (modificar)
    web/src/app/(dashboard)/settings/page.tsx (modificar — apenas o bloco catch de handleFormSubmit)
  </files>

  <read_first>
    - `.planning/phases/LEXCV-118-frontend-indicador-de-utilizadores-no-limite/118-01-SUMMARY.md` — confirmar a forma JSON exata entregue pelo Plan 01 antes de escrever o tipo.
    - `.planning/phases/LEXCV-118-frontend-indicador-de-utilizadores-no-limite/118-PATTERNS.md` — seccoes "web/src/types/auth.ts" e "3. Toast prefix-strip generalization": tem o interface atual completo e o bloco catch exato a corrigir.
    - `.planning/phases/LEXCV-118-frontend-indicador-de-utilizadores-no-limite/118-UI-SPEC.md` — linha "Error state (409)" da Copywriting Contract e a Interaction Note 5.
    - `web/src/types/auth.ts` — ficheiro completo (29 linhas), para colocar os campos na ordem certa.
    - `web/src/app/(dashboard)/settings/page.tsx` — ler `handleFormSubmit` por inteiro (linhas 242-300) antes de editar.
    - `web/scripts/verify-juizo-origem-roundtrip.mjs` — analog do unico script de verificacao ja existente neste projeto: Node puro, sem dependencias, `import assert from "node:assert"`, registado em `package.json` sob um script `verify:*`.
    - `web/package.json` — bloco `scripts` atual (`dev`, `build`, `start`, `lint`, `verify:juizo-origem`).
  </read_first>

  <behavior>
    Assercoes A1-A3 que `web/scripts/verify-limite-utilizadores-indicator.mjs` deve provar. Escrever o
    script primeiro, correr, confirmar que falha (RED), e so depois aplicar as alteracoes de codigo.

    - **A1 `types-auth-tenant-plano`** — `web/src/types/auth.ts`, apos remocao de comentarios, contem a
      linha `tenant_plano?: string;` dentro do bloco `export interface MeResponse`.
    - **A2 `types-auth-tenant-limite`** — o mesmo ficheiro contem `tenant_limite_utilizadores?: number | null;`.
      A presenca literal de `| null` e obrigatoria: e o que permite distinguir "sem limite" de "campo
      ausente" sem coagir a `0`.
    - **A3 `toast-prefix-generico`** — `web/src/app/(dashboard)/settings/page.tsx`, apos remocao de
      comentarios, contem `replace(/^API \d{3}: /, "")` E contem **zero** ocorrencias da string literal
      `API 400: `. Sem isto, o `409` da Phase 117 renderizaria o toast local como `API 409: Limite de
      utilizadores atingido…` em vez da frase limpa do backend.

    O script imprime uma linha `PASS <id>` ou `FAIL <id> — <motivo>` por assercao, e termina com exit
    code `0` se todas passarem, `1` se alguma falhar.
  </behavior>

  <action>
    Criar `web/scripts/verify-limite-utilizadores-indicator.mjs` — Node puro, sem dependencias, no estilo
    de `verify-juizo-origem-roundtrip.mjs` (que ja existe e e o precedente do projeto para "prova
    executavel de origem quando nao ha framework de testes no frontend"; este repositorio nao tem jest
    nem vitest e esta fase NAO deve introduzir um).

    Estrutura do script: ler os dois ficheiros-alvo com `node:fs/promises` a partir de caminhos relativos
    resolvidos com `import.meta.url` (para correr tanto de `web/` como via `pnpm`); normalizar cada
    conteudo removendo comentarios antes de asserir — remover blocos `/* … */`, remover blocos JSX
    `{/* … */}`, e remover linhas cujo conteudo aparado comece por `//` ou por `*`. Esta normalizacao e
    obrigatoria: sem ela, um comentario que mencione um dos tokens procurados tornaria o gate
    auto-invalidante. Definir um array de assercoes, cada uma com `id`, `descricao` e uma funcao
    predicado; percorrer, imprimir `PASS`/`FAIL` por linha, e `process.exit(falhas === 0 ? 0 : 1)`.

    Implementar nesta task apenas as assercoes A1, A2 e A3 do bloco `<behavior>` (a Task 2 acrescenta
    A4-A8 ao mesmo ficheiro). Registar em `web/package.json`, no bloco `scripts`, a entrada
    `"verify:limite-utilizadores": "node scripts/verify-limite-utilizadores-indicator.mjs"`, imediatamente
    a seguir a `verify:juizo-origem`. Nao mexer em mais nada do `package.json` — nenhuma dependencia nova,
    nenhuma alteracao de versao.

    Correr `pnpm verify:limite-utilizadores` e confirmar que falha nas 3 assercoes (RED).

    Depois, `web/src/types/auth.ts`: acrescentar exatamente duas linhas ao fim do `interface MeResponse`,
    logo a seguir a `tenant_logo_data_url?: string;`, seguindo a mesma convencao de opcionalidade e o mesmo
    snake_case dos irmaos: `tenant_plano?: string;` e `tenant_limite_utilizadores?: number | null;`.
    Nao alterar mais nenhum tipo deste ficheiro e nao tocar em `web/src/hooks/use-me.ts` — o hook ja
    tipa a query como `apiFetch<MeResponse>` e herda os campos novos por tipagem estrutural.

    Depois, `web/src/app/(dashboard)/settings/page.tsx`, bloco `catch` de `handleFormSubmit` (linha 295):
    substituir `err.message.replace("API 400: ", "")` por `err.message.replace(/^API \d{3}: /, "")`,
    ancorado no inicio (`^`) para nunca mutilar uma mensagem de backend que por acaso contenha o padrao
    a meio. Nao alterar mais nada do bloco `catch`: o `setMessage(...)`, o `toast.error(...)` e o fallback
    `"Erro ao gravar dados."` ficam exatamente como estao.

    Correr `pnpm verify:limite-utilizadores` outra vez e confirmar exit 0 (GREEN), seguido de `pnpm lint`.

    Proibicoes explicitas:
    - NAO alterar `web/src/lib/api.ts` — o toast automatico (`Erro 409: …`) ja e generico e correto; o
      duplo-toast (automatico + local) e comportamento pre-existente para todos os erros e nao e alterado
      por esta fase.
    - NAO corrigir o bug identico em `web/src/components/profile/user-password-form.tsx:62` — esta
      explicitamente fora do ambito por CONTEXT.md; registar no SUMMARY como candidato a limpeza futura.
    - NAO instalar jest/vitest/@testing-library nem qualquer outra dependencia.
    - NAO mexer no `UserManagementTab` alem do bloco `catch` nesta task — o indicador e a Task 2.
  </action>

  <verify>
    <automated>cd web && pnpm verify:limite-utilizadores && pnpm lint</automated>
  </verify>

  <acceptance_criteria>
    - `cd web && pnpm verify:limite-utilizadores` termina com exit code 0 e imprime `PASS` para as 3 assercoes `types-auth-tenant-plano`, `types-auth-tenant-limite` e `toast-prefix-generico`.
    - `cd web && pnpm lint` termina com exit code 0, sem erros nem avisos novos.
    - `grep -c 'tenant_limite_utilizadores?: number | null;' web/src/types/auth.ts` devolve 1 e `grep -c 'tenant_plano?: string;' web/src/types/auth.ts` devolve 1.
    - `grep -c 'API 400: ' web/src/app/\(dashboard\)/settings/page.tsx` devolve 0.
    - `grep -c 'replace(/\^API ' web/src/app/\(dashboard\)/settings/page.tsx` devolve 1.
    - `git diff --name-only -- web/src/lib/api.ts web/src/hooks/use-me.ts web/src/components/profile/user-password-form.tsx` devolve zero linhas.
    - `git diff web/package.json` mostra exatamente uma linha acrescentada (a entrada `verify:limite-utilizadores`) e nenhuma alteracao nos blocos `dependencies`/`devDependencies`.
    - `git diff web/src/app/\(dashboard\)/settings/page.tsx` mostra exatamente 1 linha alterada nesta task (a do `replace`).
  </acceptance_criteria>

  <done>`MeResponse` transporta os 2 campos novos com `| null` explicito, o toast local de erro deixa de colar o prefixo `API NNN: ` a mensagem do backend (o `409` da Phase 117 le-se limpo), e existe um gate `pnpm verify:limite-utilizadores` verde com 3 assercoes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Indicador X/Y + botao desativado com tooltip que dispara (tecnica span-wrapper)</name>

  <files>
    web/scripts/verify-limite-utilizadores-indicator.mjs (estender — assercoes A4-A8)
    web/src/app/(dashboard)/settings/page.tsx (modificar — import de useMe, corpo do UserManagementTab, CardHeader)
  </files>

  <read_first>
    - `.planning/phases/LEXCV-118-frontend-indicador-de-utilizadores-no-limite/118-UI-SPEC.md` — na integra. As tabelas Copywriting/Color/Typography/Spacing e as Interaction Notes 1-6 sao o contrato visual verbatim desta task; as strings de copy nao podem ser reformuladas.
    - `.planning/phases/LEXCV-118-frontend-indicador-de-utilizadores-no-limite/118-PATTERNS.md` — seccao "4. web/src/app/(dashboard)/settings/page.tsx", sub-seccoes 1 e 2: tem o JSX atual do `CardHeader`, os dois analogs parciais a combinar (Analog A = forma do Tooltip ja neste ficheiro; Analog B = tecnica `<span tabIndex={0}>` em `pareceres/[id]/page.tsx`) e o JSX combinado ilustrativo.
    - `.planning/phases/LEXCV-118-frontend-indicador-de-utilizadores-no-limite/118-CONTEXT.md` — seccao "X/Y Indicator — Behavior": a semantica de `null`/`undefined` e a proibicao de tratar `0`/`null` como "zero vagas".
    - `web/src/app/(dashboard)/settings/page.tsx` — ler as linhas 179-200 (inicio do `UserManagementTab`, hooks ja auto-fetched), 320-370 (loading guard, `filteredUsers`, `CardHeader`) e 440-490 (linha da tabela com o badge `Ativo` e os 2 Tooltips existentes).
    - `web/src/components/ui/tooltip.tsx` — confirmar que `TooltipTrigger`/`TooltipContent` sao os wrappers Radix esperados e que `TooltipProvider` NAO precisa de ser montado aqui.
    - `web/src/app/providers.tsx` (linha 30) — confirmar que `TooltipProvider delayDuration={700}` ja esta montado na raiz da app.
    - `web/src/app/(dashboard)/pareceres/[id]/page.tsx` (linhas 296-315) — o precedente do span focavel como `TooltipTrigger asChild`.
  </read_first>

  <behavior>
    Assercoes A4-A8 a acrescentar ao script `verify-limite-utilizadores-indicator.mjs`. Escrever as 5
    assercoes primeiro, correr o script, confirmar que falham (RED), e so depois implementar o JSX.

    - **A4 `use-me-auto-fetch`** — `settings/page.tsx` (sem comentarios) importa `useMe` de `@/hooks/use-me`
      e contem uma chamada `useMe()`. Prova a Interaction Note 2 (auto-fetch dentro do sub-componente,
      deduplicado pela cache `["auth","me"]`), nao prop-drilling a partir do `SettingsPage`.
    - **A5 `contagem-estrita`** — o ficheiro contem `.ativo === true` **e** continua a conter
      `user.ativo !== false`. As duas convencoes coexistem de proposito: a contagem espelha
      `countByTenantIdAndAtivoTrue` do backend, o badge da tabela mantem o seu default de exibicao.
      Uma futura "harmonizacao" das duas quebra este gate — e isso e intencional.
    - **A6 `copy-contract`** — o ficheiro contem as 3 strings de copy verbatim do UI-SPEC:
      `utilizadores` no template do contador, o sufixo `· limite atingido` (com o separador meio-ponto
      `·`, nao um hifen), e a frase completa do tooltip
      `Limite de utilizadores atingido. Desative um utilizador para libertar uma vaga.`
    - **A7 `span-wrapper-tooltip`** (o gate central desta fase) — localizar a frase do tooltip, delimitar
      o bloco `<Tooltip>` … `</Tooltip>` que a contem, e asserir, dentro desse bloco: (i) existe
      `<TooltipTrigger asChild>` seguido (so com espacos/quebras de linha pelo meio) de um `<span` que
      carrega `tabIndex={0}`; (ii) o bloco contem o atributo `disabled`; (iii) o indice de `tabIndex={0}`
      e **anterior** ao indice de `disabled` — ou seja, o span focavel envolve o botao desativado, e nao
      o contrario. Isto impede o regresso do bug documentado desde a Phase 102: `buttonVariants` tem
      `disabled:pointer-events-none` embutido, pelo que um `TooltipTrigger asChild` diretamente sobre um
      `<Button disabled>` produz um tooltip morto, silenciosamente.
    - **A8 `layout-stack`** — o ficheiro contem `flex flex-col items-end gap-2` (o empilhamento
      contador-sobre-botao alinhado a direita, Interaction Note 6 / Spacing `sm`).
  </behavior>

  <action>
    Acrescentar primeiro as assercoes A4-A8 ao array do script criado na Task 1, correr
    `pnpm verify:limite-utilizadores` e confirmar que as 5 falham (RED) enquanto as 3 da Task 1 continuam
    a passar. Commitar o RED separado do GREEN, como na Phase 117.

    Depois implementar em `web/src/app/(dashboard)/settings/page.tsx`:

    1. **Import.** Acrescentar `import { useMe } from "@/hooks/use-me";` junto dos imports de hooks ja
       existentes (a seguir a `usePermissions`, linha 25). `usePermissions` ja e importado mas so e usado
       pelo `SettingsPage` pai — nao o reaproveitar aqui; `useMe()` e a leitura direta e minima.

    2. **Derivacoes no corpo do `UserManagementTab`**, junto dos hooks ja auto-fetched (a seguir a
       `useAdminRbac()`, ~linha 192). Chamar `useMe()` e destructurar so `data` para `me`. Derivar:
       - a contagem de ativos a partir do `users` ja destructurado de `useAdminUsers()`, filtrando por
         `u.ativo === true` (comparacao estrita obrigatoria — `MockUser.ativo` e `boolean | undefined`;
         `!== false` daria uma contagem diferente da do backend), com fallback `?? 0` para `users`
         indefinido;
       - o limite, com `me?.tenant_limite_utilizadores ?? null`. O optional chaining trata `me` ainda
         indefinido exatamente como "sem limite" — que e tambem o comportamento fail-safe correto: na
         duvida nunca bloquear o utilizador, porque o `409` do backend continua a ser a autoridade;
       - o booleano de limite atingido: verdadeiro **apenas** quando o limite nao e `null` e a contagem e
         maior ou igual ao limite. Nunca tratar `null` nem `0` como "zero vagas" (proibicao explicita de
         CONTEXT.md).
       Nao chamar `useAdminUsers()` outra vez, nao criar hook novo, nao criar ficheiro novo, e nao fazer
       prop-drilling a partir do `SettingsPage`. Nao acrescentar ramo de loading para o `me`: a aba
       "Utilizadores" so e renderizavel depois de `me` estar carregado (o botao do separador e gated por
       `hasUsersManage`, derivado de `me`) — Interaction Note 3.

    3. **Texto do contador**, com as 3 formas verbatim do UI-SPEC:
       - limite `null` → contagem seguida de ` utilizadores` (sem barra, sem cap implicito);
       - limite atingido → contagem, `/`, limite, ` utilizadores · limite atingido`;
       - abaixo do limite → contagem, `/`, limite, ` utilizadores`.
       Nao reformular, nao traduzir, nao acrescentar emoji nem icone (o UI-SPEC define este estado como
       text-only).

    4. **Estilo do contador.** Estado normal: `text-xs text-slate-500 dark:text-slate-400` (12px, peso
       400 — o mesmo `text-xs` ja usado pelo proprio botao "Novo Utilizador" e a mesma cor de meta-texto
       ja usada neste ficheiro). Estado no limite: `text-xs font-semibold text-red-600 dark:text-red-400`
       (peso 600 + destructive, a mesma convencao de erro que o banner ja existente deste ficheiro usa).
       A troca simultanea de peso e cor, mais o sufixo `· limite atingido`, e o sinal "porque esta
       desativado" sempre visivel exigido pela Success Criteria 2 — tem de ser legivel sem hover.
       Nao usar a cor de accent (`bg-blue-600`/`text-blue-600`) no contador: e informativo, nao acao.

    5. **Layout.** Substituir o `<Button onClick={handleCreateClick}>…</Button>` solto do `CardHeader`
       (linhas 358-364) por um `<div className="flex flex-col items-end gap-2">` contendo, por esta ordem,
       um `<span>` com o texto do contador e depois o botao. O `CardHeader` mantem exatamente as classes
       que ja tem (`flex flex-row items-center justify-between space-y-0`) — nao alterar o cabecalho nem
       o bloco `<div>` do titulo/descricao.

    6. **Botao + tooltip.** Renderizar condicionalmente: quando o limite esta atingido, o botao leva o
       atributo `disabled` e e envolvido por `<Tooltip><TooltipTrigger asChild><span tabIndex={0}>…</span>
       </TooltipTrigger><TooltipContent>…</TooltipContent></Tooltip>`, com o `TooltipContent` a conter a
       frase verbatim do UI-SPEC; caso contrario, renderizar o botao normal com `onClick={handleCreateClick}`
       e sem tooltip. Em ambos os ramos a `className` do `Button` mantem-se **exatamente**
       `bg-blue-600 hover:bg-blue-700 text-white flex items-center gap-1.5 shadow-sm text-xs py-1.5 px-3 h-auto`
       e o conteudo mantem-se o icone `<Plus className="h-4 w-4" />` seguido do texto `Novo Utilizador`.
       O `<span tabIndex={0}>` e o alvo real do `TooltipTrigger` porque `buttonVariants` tem
       `disabled:pointer-events-none` embutido — sem o wrapper o tooltip nunca dispararia. O `<Button>`
       fica nativamente `disabled` (a submissao continua bloqueada ao nivel do DOM, independentemente do
       tooltip); no ramo desativado o `onClick` e omitido. Nao usar `aria-disabled` em vez de `disabled`,
       nao remover `disabled:pointer-events-none` do `button.tsx`, e nao acrescentar atributos ARIA manuais
       ao span (o Radix Tooltip ja liga `aria-describedby` do trigger ao conteudo).
       `Tooltip`/`TooltipTrigger`/`TooltipContent` ja estao importados neste ficheiro (linha 44) e o
       `TooltipProvider` ja esta montado na raiz — nao acrescentar imports nem provider.

    Correr `pnpm verify:limite-utilizadores` (exit 0, 8 assercoes PASS), `pnpm lint` e `pnpm build`.

    Proibicoes explicitas:
    - NAO usar o valor do limite como gate de submissao logico alem do `disabled` visual — o `409` do
      backend (Phase 117) continua a ser a unica autoridade; nao acrescentar nenhuma validacao que impeca
      o `handleFormSubmit` de chegar a API.
    - NAO acrescentar o indicador fora do `UserManagementTab` (dashboard, app shell, `RbacTab`) — fora do
      ambito por CONTEXT.md (Deferred Ideas).
    - NAO acrescentar UI de edicao de `plano`/`limiteUtilizadores` — e a Phase 120 (PROV-04).
    - NAO instalar nenhum componente shadcn novo nem nenhuma dependencia (UI-SPEC: zero instalacoes).
    - NAO alterar o badge `Ativo` da tabela nem a sua condicao `user.ativo !== false`.
    - NAO alterar `handleCreateClick`, `handleEditClick`, `handleDeleteClick` nem o formulario.
  </action>

  <verify>
    <automated>cd web && pnpm verify:limite-utilizadores && pnpm lint && pnpm build</automated>
  </verify>

  <acceptance_criteria>
    - `cd web && pnpm verify:limite-utilizadores` termina com exit code 0 e imprime `PASS` para as 8 assercoes (`types-auth-tenant-plano`, `types-auth-tenant-limite`, `toast-prefix-generico`, `use-me-auto-fetch`, `contagem-estrita`, `copy-contract`, `span-wrapper-tooltip`, `layout-stack`).
    - A assercao `span-wrapper-tooltip` valida as 3 condicoes estruturais: `<TooltipTrigger asChild>` seguido de `<span … tabIndex={0}`, presenca de `disabled` no mesmo bloco `<Tooltip>`, e `tabIndex={0}` a aparecer ANTES de `disabled`.
    - `cd web && pnpm lint` termina com exit code 0, sem erros nem avisos novos.
    - `cd web && pnpm build` termina com exit code 0 (type-check do TypeScript incluido) — prova que `me?.tenant_limite_utilizadores` tipa contra o `MeResponse` estendido na Task 1.
    - `grep -c 'Limite de utilizadores atingido. Desative um utilizador para libertar uma vaga.' web/src/app/\(dashboard\)/settings/page.tsx` devolve 1.
    - `grep -c '· limite atingido' web/src/app/\(dashboard\)/settings/page.tsx` devolve 1.
    - `grep -c 'flex flex-col items-end gap-2' web/src/app/\(dashboard\)/settings/page.tsx` devolve 1.
    - `grep -c 'user.ativo !== false' web/src/app/\(dashboard\)/settings/page.tsx` devolve 1 — a convencao de exibicao da tabela ficou intacta.
    - `grep -c 'bg-blue-600 hover:bg-blue-700 text-white flex items-center gap-1.5 shadow-sm text-xs py-1.5 px-3 h-auto' web/src/app/\(dashboard\)/settings/page.tsx` devolve 2 — a classe do botao e identica nos dois ramos.
    - `git diff --name-only -- web/src/components/ui/button.tsx web/src/components/ui/tooltip.tsx web/src/app/providers.tsx web/src/hooks/use-me.ts` devolve zero linhas.
    - `git diff web/package.json` nao mostra alteracoes em `dependencies`/`devDependencies` (zero instalacoes).
  </acceptance_criteria>

  <done>A aba "Gestão de Utilizadores" mostra "X/Y utilizadores" nas 3 formas contratadas, desativa "Novo Utilizador" no limite com o motivo visivel sem hover, e o tooltip explicativo dispara sobre o botao desativado gracas ao wrapper `<span tabIndex={0}>` — tudo provado por 8 assercoes de origem verdes, lint e build limpos.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser (DOM manipulavel pelo utilizador) → `POST /api/v1/admin/users` | O estado `disabled` do botao vive no cliente e pode ser removido em devtools; o pedido resultante e input nao confiavel |
| Resposta de `/auth/me` → renderizacao na Settings | O valor do limite chega do backend e e renderizado como texto; a superficie de render esta gated pelo separador "Utilizadores" (`users:manage`/ADMIN) |
| Mensagem de erro do backend → toast Sonner | A frase do `409` vem do JSON do backend e e mostrada ao utilizador apos remocao do prefixo de status |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-118-06 | Elevation of Privilege | Botao "Novo Utilizador" desativado no cliente | mitigate | O `disabled` e espelho de UX, nunca gate. Remover o atributo em devtools apenas reabre o formulario local: `POST /api/v1/admin/users` continua a devolver `409` por `AdminController.limiteUtilizadoresExcedido` (Phase 117, nao tocado nesta fase — gate no Plan 01 Task 2), e o toast corrigido na Task 1 mostra essa rejeicao com clareza. Proibicao explicita na Task 2: nenhuma validacao nova impede o `handleFormSubmit` de chegar a API |
| T-118-07 | Information Disclosure | Renderizacao de `tenant_plano`/`tenant_limite_utilizadores` na UI | accept | **Nao e exposicao sensivel nova** — e um numero de capacidade contratada do proprio tenant do utilizador, consistente com o precedente `tenant_nome` (ja renderizado no app shell a todos os papeis desde antes desta fase). A superficie de render fica confinada ao `UserManagementTab`, so alcancavel pelo separador "Utilizadores" gated por `hasUsersManage` (`users:manage` ou papel ADMIN), espelhando o `@PreAuthorize("hasRole('ADMIN')")` do `AdminController`. Papeis nao privilegiados recebem o campo em `/auth/me` mas nao tem superficie que o mostre |
| T-118-08 | Injection (XSS) | Texto do contador, do tooltip e do toast de erro | accept | Todas as strings novas sao literais estaticas mais dois inteiros derivados de `users.length` e de um campo JSON numerico. React escapa por defeito e nao ha `dangerouslySetInnerHTML` em lado nenhum desta alteracao. O toast renderiza `err.message`, cuja origem e o campo `message` do JSON do backend — comportamento pre-existente do `apiFetch` (inalterado); o Sonner renderiza como texto, nao HTML. O `replace` novo e ancorado (`^`) e so remove um prefixo conhecido |
| T-118-09 | Tampering | Regressao silenciosa da tecnica span-wrapper | mitigate | Um refactor futuro que "limpe" o `<span tabIndex={0}>` e ponha `disabled` diretamente no `TooltipTrigger asChild` reintroduziria o bug de tooltip morto sem falhar lint nem build. Mitigado pela assercao estrutural `span-wrapper-tooltip` do script `verify-limite-utilizadores-indicator.mjs`, que reprova exatamente essa forma |
| T-118-10 | Spoofing | Estado do cliente desatualizado (duas abas abertas) | accept | Cenario explicitamente previsto pela Success Criteria 3: o indicador pode estar obsoleto (`staleTime: 60_000` no `useMe`, lista de utilizadores em cache). A consequencia maxima e um `409` do backend, agora apresentado como toast limpo. Nao se introduz polling nem invalidacao adicional — custo desproporcionado para um ecra administrativo de baixa frequencia |
| T-118-SC | Tampering | Instalacoes npm/pnpm | n/a | Esta fase nao instala nenhum pacote: `web/package.json` so recebe uma entrada em `scripts` (sem dependencia associada — Node puro), e o UI-SPEC confirma zero instalacoes shadcn. Gate nos criterios de aceitacao das duas tasks: `dependencies`/`devDependencies` inalterados. O Package Legitimacy Gate nao se aplica |
</threat_model>

<verification>
1. `cd web && pnpm verify:limite-utilizadores` — 8 assercoes PASS, exit 0.
2. `cd web && pnpm lint` — exit 0, sem erros ou avisos novos.
3. `cd web && pnpm build` — exit 0 (type-check incluido).
4. Gates de origem: `API 400: ` ausente; copy verbatim presente 1 vez cada; classe do botao identica nos 2 ramos; `user.ativo !== false` intacto.
5. `git diff --name-only` confirma que `api.ts`, `use-me.ts`, `button.tsx`, `tooltip.tsx`, `providers.tsx` e `user-password-form.tsx` ficaram fora do diff.
6. `git status --porcelain` sem ficheiros fora dos declarados em `files_modified`.
</verification>

<success_criteria>
- **Success Criteria 1 da fase:** o `UserManagementTab` mostra "X/Y utilizadores" com X = utilizadores `ativo === true` e Y = `tenant_limite_utilizadores` do tenant; sem limite, le "X utilizadores".
- **Success Criteria 2 da fase:** com X >= Y o botao "Novo Utilizador" fica desativado, o contador muda para vermelho/semibold com o sufixo `· limite atingido` (motivo visivel sem hover) e o tooltip explicativo dispara em hover e em foco por teclado.
- **Success Criteria 3 da fase:** um `409` do backend aparece como toast com a frase limpa do backend, sem prefixo `API 409: `, sem crash da UI — e nenhuma validacao de cliente substitui a verificacao autoritaria do backend.
- Zero dependencias novas, zero componentes shadcn novos, zero ficheiros fora do ambito declarado.
</success_criteria>

<output>
Criar `.planning/phases/LEXCV-118-frontend-indicador-de-utilizadores-no-limite/118-02-SUMMARY.md` quando terminar, registando: o veredito por mitigacao (T-118-06/07/08/09/10/SC), a forma final do JSX do `CardHeader`, e uma nota explicita de que a tecnica span-wrapper foi aplicada pela primeira vez neste codebase (fecha a divida documentada da Phase 102, v2.13) — incluindo o ponteiro para o bug identico ainda por corrigir em `web/src/components/profile/user-password-form.tsx:62`, deixado fora de ambito por CONTEXT.md.
</output>
