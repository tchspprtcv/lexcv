---
phase: 132-configuracao
plan: 01
wave: 1
status: complete
---

# Phase 132 Plan 01: Configuração

**Goal:** Os ficheiros de configuração e build do projeto (Maven, Docker Compose) referem "LexCV".

**Requirements:** CONFIG-01

## Tasks

1. `backend/pom.xml`: `<description>ALCv Legal System Backend...` → `<description>LexCV Legal
   System Backend...`
2. Varrer `docker-compose.yml`, `docker-compose.prod.yml`, `docker-compose.hostinger.yml` — só
   `docker-compose.hostinger.yml` continha "alcv" (`alcv.tech`/`www.alcv.tech`, domínio real de
   produção, preservado por decisão explícita desde a Phase 129) — sem alterações necessárias
3. `mvn -DskipTests package` limpo

## Files Modified

- `backend/pom.xml`
