---
phase: 119-backend-papel-de-administrador-de-plataforma-e-provisionamen
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/main/java/com/lexcv/repositories/TenantRepository.java
  - backend/src/main/java/com/lexcv/seed/DatabaseSeeder.java
  - backend/src/test/java/com/lexcv/seed/DatabaseSeederPlataformaAdminTest.java
autonomous: true
requirements: [PROV-01]

must_haves:
  truths:
    - "Existe um papel PLATAFORMA_ADMIN na base de dados depois de qualquer arranque da aplicacao, distinto de ADMIN, com um conjunto de permissoes VAZIO"
    - "Existe uma Tenant reservada com nome literal 'LexCV' depois de qualquer arranque da aplicacao"
    - "Existe um utilizador bootstrap plataforma@lexcv.cv, pertencente a Tenant 'LexCV', com o papel PLATAFORMA_ADMIN e apenas esse papel"
    - "O seed de plataforma corre incondicionalmente — nao e travado por app.seed.enabled=false, nem por SystemSetting.initialized=false, nem por a base de dados ja ter dados"
    - "Reiniciar a aplicacao N vezes nunca cria uma segunda Tenant 'LexCV' nem um segundo utilizador plataforma@lexcv.cv (find-or-create idempotente)"
    - "O bloco de dados demo continua a correr numa base de dados genuinamente vazia — as tres contagens que o protegem sao capturadas ANTES de o seed de plataforma inserir linhas"
    - "O papel PLATAFORMA_ADMIN nunca recebe permissoes scoped (clientes:*, processos:*, rbac:manage, users:manage) atraves do seed"
  artifacts:
    - path: "backend/src/main/java/com/lexcv/repositories/TenantRepository.java"
      provides: "Derived query findByNome para o lookup idempotente da tenant reservada"
      contains: "Optional<Tenant> findByNome(String nome)"
    - path: "backend/src/main/java/com/lexcv/seed/DatabaseSeeder.java"
      provides: "Papel PLATAFORMA_ADMIN em seedRbac() + metodo seedPlataforma() incondicional + gate do bloco demo hoisted"
      contains: "seedPlataforma"
      min_lines: 370
    - path: "backend/src/test/java/com/lexcv/seed/DatabaseSeederPlataformaAdminTest.java"
      provides: "Prova automatizada do seed incondicional, da idempotencia, das permissoes vazias e da nao-regressao do gate demo"
      contains: "PLATAFORMA_ADMIN"
      min_lines: 120
  key_links:
    - from: "backend/src/main/java/com/lexcv/seed/DatabaseSeeder.java"
      to: "backend/src/main/java/com/lexcv/repositories/TenantRepository.java"
      via: "lookup idempotente da tenant reservada por nome, antes de qualquer save"
      pattern: "tenantRepository\\.findByNome\\(\"LexCV\"\\)"
    - from: "backend/src/main/java/com/lexcv/seed/DatabaseSeeder.java"
      to: "backend/src/main/java/com/lexcv/models/Role.java"
      via: "upsert do papel de plataforma com coleccao de permissoes vazia"
      pattern: "upsertRolePermissions\\(\"PLATAFORMA_ADMIN\", Collections\\.emptyList\\(\\)\\)"
    - from: "backend/src/main/java/com/lexcv/seed/DatabaseSeeder.java"
      to: "backend/src/main/java/com/lexcv/repositories/UserRepository.java"
      via: "guarda find-or-create do utilizador bootstrap, por email"
      pattern: "userRepository\\.findByEmail\\(\"plataforma@lexcv\\.cv\"\\)"
---

<objective>
Seedar a infraestrutura de plataforma: o papel `PLATAFORMA_ADMIN` (com zero permissoes scoped), a `Tenant` reservada `"LexCV"`, e o utilizador bootstrap `plataforma@lexcv.cv` que lhe pertence — tudo incondicionalmente, em todos os arranques, de forma idempotente. Acrescentar o `TenantRepository.findByNome` que a idempotencia exige.

Purpose: fecha o Success Criterion 1 da Phase 119 (PROV-01). Sem estas linhas na base de dados, `hasRole('PLATAFORMA_ADMIN')` nunca e verdadeiro para ninguem e a capacidade de provisionamento dos Plans 02/04 e inatingivel. E infraestrutura nuclear, nao dados demo — por isso NAO herda os tres gates do bloco demo (`seedEnabled`, `initialized`, base de dados vazia).

Output: `TenantRepository.java` com um metodo derivado novo, `DatabaseSeeder.java` com uma linha nova em `seedRbac()` + um metodo `seedPlataforma()` novo + o gate do bloco demo reescrito para preservar a semantica original, e um teste Mockito novo.
</objective>

<execution_context>
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/LEXCV-119-backend-papel-de-administrador-de-plataforma-e-provisionamen/119-CONTEXT.md
@.planning/phases/LEXCV-119-backend-papel-de-administrador-de-plataforma-e-provisionamen/119-PATTERNS.md

# 119-PATTERNS.md tem o corpo integral de seedRbac()/upsertRolePermissions, do bloco demo
# Tenant/User, do metodo run() com os seus tres gates, e o ficheiro completo do RoleRepository
# (analog exato do findByNome). Usa-o como fonte primaria em vez de reexplorar o codebase.
</context>

<interfaces>
<!-- Contratos ja existentes no codebase. O executor NAO precisa de os redescobrir. -->

`backend/src/main/java/com/lexcv/repositories/TenantRepository.java` (ficheiro completo hoje, 21 linhas
— o Javadoc de `findFirstByOrderByCreatedAtAsc` e um aviso WR-01 pre-existente que NAO deve ser apagado
nem reescrito nesta fase; a Phase 121/ISOL-02 e que lhe toca):
```java
public interface TenantRepository extends JpaRepository<Tenant, UUID> {
    /** ... aviso WR-01 sobre a suposicao single-tenant ... */
    Optional<Tenant> findFirstByOrderByCreatedAtAsc();
}
```

`backend/src/main/java/com/lexcv/models/Tenant.java` (Lombok `@Builder`; so `nome` e `@Column(nullable = false)`):
```java
private UUID id;                    // @GeneratedValue(strategy = GenerationType.UUID)
private String nome;                // NOT NULL
private String nif;
private String tipoEntidade;
private String email;
private String telefone;
private String logoDataUrl;         // @Lob
private TenantPlano plano;          // nullable
private Integer limiteUtilizadores; // nullable = sem limite (Phase 117)
private LocalDateTime createdAt;    // preenchido por @PrePersist
```

`backend/src/main/java/com/lexcv/repositories/UserRepository.java` (metodos relevantes):
```java
Optional<User> findByEmail(String email);
long countByTenantIdAndAtivoTrue(UUID tenantId);
```

`backend/src/main/java/com/lexcv/seed/DatabaseSeeder.java` — helper ja existente, a reutilizar tal como
esta (nao alterar a sua assinatura nem o seu corpo):
```java
private void upsertRolePermissions(String roleName, Collection<Permission> permissions) {
    Role role = roleRepository.findByNome(roleName)
                    .orElseGet(() -> roleRepository.save(Role.builder().nome(roleName).build()));
    boolean changed = role.getPermissions().addAll(permissions);
    if (changed) {
            roleRepository.save(role);
    }
}
```

`DatabaseSeeder` ja tem `import java.util.*;` (linha 14) — `Collections`, `Set`, `UUID`, `Optional`
estao todos em scope. NAO acrescentar imports de `java.util`.

Ordem dos campos `final` de `DatabaseSeeder` (a assinatura gerada por `@RequiredArgsConstructor`
segue exatamente esta ordem — necessaria para o teste instanciar o seeder):
```java
tenantRepository, userRepository, roleRepository, permissionRepository, clienteRepository,
contaCorrenteRepository, processoRepository, parteRepository, faseProcessualRepository,
processoFaseRepository, eventoRepository, honorarioRepository, pagamentoRepository,
systemSettingRepository, passwordEncoder
```
`seedEnabled` NAO e construtor — e `@Value("${app.seed.enabled}")` num campo privado; no teste
define-se com `org.springframework.test.util.ReflectionTestUtils.setField(seeder, "seedEnabled", <bool>)`.

Corpo actual de `run()` (linhas 39-56) — os tres gates do bloco demo, na ordem exata:
```java
@Override
public void run(String... args) throws Exception {
        seedRbac();

        if (!seedEnabled) {
                return;
        }

        boolean initialized = systemSettingRepository.findById(SystemSetting.SINGLETON_ID)
                        .map(SystemSetting::getInitialized)
                        .orElse(false);
        if (!initialized) {
                return;
        }

        if (tenantRepository.count() > 0 || userRepository.count() > 0 || clienteRepository.count() > 0) {
                return;
        }
        // ... bloco de dados demo (linhas 58-291)
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: TenantRepository.findByNome + papel PLATAFORMA_ADMIN sem permissoes</name>

  <files>
    backend/src/main/java/com/lexcv/repositories/TenantRepository.java (modificar)
    backend/src/main/java/com/lexcv/seed/DatabaseSeeder.java (modificar — apenas seedRbac())
  </files>

  <read_first>
    - `.planning/phases/LEXCV-119-backend-papel-de-administrador-de-plataforma-e-provisionamen/119-PATTERNS.md` — seccao "TenantRepository.java" (analog `RoleRepository`, ficheiro completo) e seccao "DatabaseSeeder.java" Parte A (corpo integral de `seedRbac()` e de `upsertRolePermissions`).
    - `.planning/phases/LEXCV-119-backend-papel-de-administrador-de-plataforma-e-provisionamen/119-CONTEXT.md` — seccao `<decisions>`, bloco "Role and Reserved Tenant Seeding": a decisao explicita de `Collections.emptyList()` e de nao expor o papel no ecra RBAC.
    - `backend/src/main/java/com/lexcv/repositories/TenantRepository.java` — ficheiro completo (21 linhas) antes de editar; o Javadoc WR-01 existente fica intacto.
    - `backend/src/main/java/com/lexcv/seed/DatabaseSeeder.java` — ler `seedRbac()` (linhas 293-350) e `upsertRolePermissions` (linhas 352-360) por inteiro antes de editar.
  </read_first>

  <action>
    Em `backend/src/main/java/com/lexcv/repositories/TenantRepository.java`: acrescentar uma unica linha de
    metodo derivado, `Optional<Tenant> findByNome(String nome);`, a seguir a `findFirstByOrderByCreatedAtAsc()`.
    Os imports `Optional` e `UUID` ja existem — nao acrescentar imports. Acrescentar um Javadoc curto a
    explicar que serve o lookup idempotente da tenant reservada de plataforma (`"LexCV"`) em `DatabaseSeeder`.
    NAO apagar, encurtar nem reescrever o Javadoc WR-01 de `findFirstByOrderByCreatedAtAsc` — esse call site
    e propriedade da Phase 121 (ISOL-02), fora do ambito desta fase.

    Em `backend/src/main/java/com/lexcv/seed/DatabaseSeeder.java`, dentro de `seedRbac()`: acrescentar
    exatamente uma chamada a seguir a `upsertRolePermissions("ADVOGADO", ...)` (a ultima das quatro
    chamadas existentes, que termina na linha 349):
    `upsertRolePermissions("PLATAFORMA_ADMIN", Collections.emptyList());`
    A coleccao TEM de ser vazia. Acrescentar um comentario de uma a duas linhas a explicar porque: este papel
    opera exclusivamente atraves do seu proprio endpoint gated por `@PreAuthorize` (Plan 04) e nunca atraves
    do sistema RBAC scoped por tenant; dar-lhe qualquer `clientes:*`/`processos:*`/`rbac:manage`/`users:manage`
    contradiria a decisao bloqueada de CONTEXT.md.

    Proibicoes explicitas:
    - NAO acrescentar `"PLATAFORMA_ADMIN"` a lista `permKeys` (essa lista e de permissoes, nao de papeis).
    - NAO acrescentar nenhuma `Permission` nova ao `permissionMap`.
    - NAO alterar as quatro chamadas `upsertRolePermissions` existentes nem o corpo de `upsertRolePermissions`.
    - NAO tocar em `UserPrincipal.java` — o mapeamento papel -> `ROLE_<nome>` ja e generico (linhas 30-32) e o
      bloco `if (roles.contains("ADMIN"))` NAO ganha um equivalente para `PLATAFORMA_ADMIN`: este papel tem
      zero permissoes por desenho.
    - NAO tocar em `AdminController.java` neste plano (a contencao do papel no ecra RBAC e o Plan 03).
    - NAO acrescentar dependencias ao `pom.xml`.
  </action>

  <verify>
    <automated>cd backend && mvn -q -DskipTests compile && grep -c 'Optional<Tenant> findByNome(String nome);' src/main/java/com/lexcv/repositories/TenantRepository.java && grep -c 'findFirstByOrderByCreatedAtAsc' src/main/java/com/lexcv/repositories/TenantRepository.java && grep -v '^[[:space:]]*[/*]' src/main/java/com/lexcv/seed/DatabaseSeeder.java | grep -c 'upsertRolePermissions("PLATAFORMA_ADMIN", Collections.emptyList())'</automated>
  </verify>

  <acceptance_criteria>
    - `cd backend && mvn -q -DskipTests compile` termina com exit code 0.
    - Em `TenantRepository.java`: `grep -c 'Optional<Tenant> findByNome(String nome);'` devolve 1.
    - Em `TenantRepository.java`: `grep -c 'findFirstByOrderByCreatedAtAsc'` devolve 1 e `grep -c 'WR-01'` devolve 1 — o metodo e o aviso pre-existentes continuam la, intactos.
    - Em `TenantRepository.java`: `grep -c '^import'` devolve 3 (sem imports novos).
    - Em `DatabaseSeeder.java`, filtrando comentarios: `grep -c 'upsertRolePermissions("PLATAFORMA_ADMIN", Collections.emptyList())'` devolve 1.
    - Em `DatabaseSeeder.java`, filtrando comentarios: `grep -c 'upsertRolePermissions('` devolve 6 no total (4 chamadas pre-existentes + a nova + a declaracao do metodo helper).
    - Em `DatabaseSeeder.java`, filtrando comentarios: `grep -c '"PLATAFORMA_ADMIN"' | ` a mesma linha do `permKeys` nao aparece — concretamente, `grep -n 'permKeys' -A 12 src/main/java/com/lexcv/seed/DatabaseSeeder.java | grep -c 'PLATAFORMA_ADMIN'` devolve 0.
    - `git diff --name-only -- backend/src/main/java/com/lexcv/config/UserPrincipal.java backend/src/main/java/com/lexcv/controllers/AdminController.java backend/pom.xml` devolve zero linhas.
  </acceptance_criteria>

  <done>`TenantRepository` expoe `findByNome`, e qualquer arranque da aplicacao passa a garantir a existencia de um papel `PLATAFORMA_ADMIN` com conjunto de permissoes vazio, sem tocar nos quatro papeis de tenant existentes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Seed incondicional e idempotente da tenant reservada LexCV e do utilizador bootstrap</name>

  <files>
    backend/src/test/java/com/lexcv/seed/DatabaseSeederPlataformaAdminTest.java (novo)
    backend/src/main/java/com/lexcv/seed/DatabaseSeeder.java (modificar — run() e novo metodo seedPlataforma())
  </files>

  <read_first>
    - `.planning/phases/LEXCV-119-backend-papel-de-administrador-de-plataforma-e-provisionamen/119-PATTERNS.md` — seccao "DatabaseSeeder.java" Parte B (forma do bloco Tenant/User) e "Placement pattern" (a posicao exata do bloco novo, entre `seedRbac()` e o gate `seedEnabled`), e "Shared Patterns / Idempotent find-or-create seeding".
    - `.planning/phases/LEXCV-119-backend-papel-de-administrador-de-plataforma-e-provisionamen/119-CONTEXT.md` — seccao `<decisions>`: credenciais exatas (`plataforma@lexcv.cv` / `Pa$$w0rd`), idempotencia por `nome = "LexCV"`, e a justificacao de porque o bloco NAO e gated por `seedEnabled`.
    - `backend/src/main/java/com/lexcv/seed/DatabaseSeeder.java` — ler `run()` por inteiro (linhas 39-93) antes de editar, com atencao aos tres gates e ao bloco demo que eles protegem.
    - `backend/src/test/java/com/lexcv/controllers/AdminControllerLimiteUtilizadoresTest.java` — convencao de teste do codebase: `@ExtendWith(MockitoExtension.class)`, colaboradores `@Mock`, instanciacao direta, sem MockMvc/`@SpringBootTest`. Nao introduzir um harness novo.
    - `CLAUDE.md` — a nota de que `admin@lexcv.cv` / `Pa$$w0rd` ja e uma credencial de desenvolvimento conhecida e documentada.
  </read_first>

  <behavior>
    Casos que `DatabaseSeederPlataformaAdminTest` deve provar. Escrever ANTES de alterar `run()` — os
    primeiros casos devem falhar (o metodo `seedPlataforma` ainda nao existe / nada e criado). Commitar o
    RED e o GREEN em commits separados, como nas Phases 117 e 118.

    - **Caso 1 — seed de plataforma corre com `seedEnabled=false`.** `ReflectionTestUtils.setField(seeder,
      "seedEnabled", false)`. Depois de `run()`: `tenantRepository.save(...)` foi chamado com um `Tenant`
      cujo `getNome()` e exatamente `"LexCV"`, e `userRepository.save(...)` foi chamado com um `User` cujo
      `getEmail()` e `"plataforma@lexcv.cv"`. Usar `ArgumentCaptor` para inspeccionar os argumentos. Prova
      que o bloco e incondicional — nenhum dos tres gates do bloco demo o trava.
    - **Caso 2 — o utilizador bootstrap pertence a tenant reservada e so tem o papel de plataforma.** No
      mesmo cenario do Caso 1, o `User` capturado tem `getTenantId()` igual ao `getId()` do `Tenant`
      devolvido por `tenantRepository.save(...)`, `getAtivo()` a `true`, e `getRoles()` com tamanho
      exatamente 1 contendo um `Role` de nome `"PLATAFORMA_ADMIN"`. Asserir tambem que `getPasswordHash()`
      nao e nulo nem igual ao texto simples `"Pa$$w0rd"` (passou pelo `PasswordEncoder`).
    - **Caso 3 — idempotencia: nada e recriado num segundo arranque.** `tenantRepository.findByNome("LexCV")`
      devolve `Optional.of(tenantExistente)` e `userRepository.findByEmail("plataforma@lexcv.cv")` devolve
      `Optional.of(utilizadorExistente)`. Depois de `run()` (com `seedEnabled=false`, para isolar o bloco
      de plataforma): `verify(tenantRepository, never()).save(any())` e `verify(userRepository, never()).save(any())`.
    - **Caso 4 — as contagens que protegem o bloco demo sao observadas ANTES de o seed de plataforma
      inserir linhas.** Com `seedEnabled=false` e a base de dados vazia, usar `InOrder inOrder =
      inOrder(tenantRepository, userRepository, clienteRepository)` e asserir a sequencia:
      `inOrder.verify(tenantRepository).count()`, `inOrder.verify(userRepository).count()`,
      `inOrder.verify(clienteRepository).count()`, e so depois `inOrder.verify(tenantRepository).save(any())`.
      Este e o teste de nao-regressao critico: se as contagens forem lidas DEPOIS do seed de plataforma,
      elas passam a ser >= 1 para sempre e o bloco de dados demo deixa silenciosamente de correr em
      qualquer base de dados nova com `SEED_ENABLED=true`.

    Notas de montagem (todas confirmadas na leitura de planeamento):
    - 14 repositorios `@Mock` + `@Mock PasswordEncoder`, instanciados por `@InjectMocks DatabaseSeeder seeder`
      (`@RequiredArgsConstructor` gera o construtor pela ordem de campos listada em `<interfaces>`).
    - `seedRbac()` corre sempre antes do bloco de plataforma; para o manter inofensivo sob strict stubs,
      stubar `permissionRepository.findByNome(anyString())` para devolver `Optional.of(Permission.builder().nome("x").build())`
      e `roleRepository.findByNome(anyString())` para devolver `Optional.of(Role.builder().nome(...).permissions(new HashSet<>()).build())`.
      Usar `lenient()` onde um stub nao for exercitado por todos os casos, em vez de relaxar a extensao inteira.
    - O stub de `roleRepository.findByNome("PLATAFORMA_ADMIN")` tem de devolver um `Role` com esse nome —
      e o papel que sera anexado ao utilizador bootstrap.
    - `tenantRepository.save(any())` tem de devolver um `Tenant` com `id` nao nulo (usar
      `thenAnswer(inv -> { Tenant t = inv.getArgument(0); t.setId(UUID.randomUUID()); return t; })`),
      caso contrario o `getId()` usado no `tenantId` do utilizador rebenta com NPE.
    - `passwordEncoder.encode(anyString())` stubado para devolver uma String distinta do texto simples.
    - Javadoc de classe a explicar o que e provado e porque, no estilo de `PublicControllerTest`, incluindo
      uma frase explicita sobre o Caso 4 e a regressao que ele previne.
  </behavior>

  <action>
    Escrever primeiro `backend/src/test/java/com/lexcv/seed/DatabaseSeederPlataformaAdminTest.java` (a pasta
    `src/test/java/com/lexcv/seed/` ainda nao existe — criar) com os 4 casos do bloco `<behavior>`. Correr
    `cd backend && mvn test -Dtest=DatabaseSeederPlataformaAdminTest`, confirmar RED, commitar.

    Depois editar `backend/src/main/java/com/lexcv/seed/DatabaseSeeder.java`:

    **(a) Reescrever `run()` preservando a semantica exata do gate demo.** A ordem passa a ser: `seedRbac();`
    -> capturar as tres contagens numa variavel booleana local -> `seedPlataforma();` -> gate `seedEnabled`
    -> gate `initialized` -> gate da variavel capturada -> bloco demo. Concretamente, substituir o gate inline
    `if (tenantRepository.count() > 0 || userRepository.count() > 0 || clienteRepository.count() > 0) { return; }`
    por uma variavel calculada imediatamente a seguir a `seedRbac()` e ANTES de `seedPlataforma()`:
    `boolean bdVaziaAntesDoSeedPlataforma = tenantRepository.count() == 0 && userRepository.count() == 0 && clienteRepository.count() == 0;`
    e, na posicao onde o gate inline estava, `if (!bdVaziaAntesDoSeedPlataforma) { return; }`.
    Esta hoisting NAO e opcional nem cosmetica: sem ela, o seed de plataforma passa a inserir sempre 1 tenant
    e 1 utilizador antes do gate, as contagens ficam permanentemente > 0, e o bloco de dados demo deixa de
    correr em qualquer base de dados nova — uma regressao silenciosa de ambiente de desenvolvimento.
    Acrescentar um comentario a explicar exatamente isto no sitio da captura.

    **(b) Criar o metodo privado `seedPlataforma()`**, chamado a partir de `run()` na posicao acima (entre a
    captura das contagens e o gate `seedEnabled`), com este comportamento:
    - Resolver o papel: `roleRepository.findByNome("PLATAFORMA_ADMIN")` com `.orElseThrow(() -> new IllegalStateException(...))`
      e uma mensagem que diga que `seedRbac()` tem de correr primeiro. Nao usar `orElseGet(save)` aqui — o papel
      e propriedade do `seedRbac()` (Task 1) e duplicar a criacao esconderia uma falha de ordem.
    - Tenant reservada, find-or-create pelo nome literal:
      `Tenant lexcv = tenantRepository.findByNome("LexCV").orElseGet(() -> tenantRepository.save(Tenant.builder().nome("LexCV").build()));`
      Nao preencher `nif`, `tipoEntidade`, `email`, `telefone`, `logoDataUrl`, `plano` nem `limiteUtilizadores` —
      CONTEXT.md nao os pede e `nome` e a unica coluna NOT NULL.
    - Utilizador bootstrap, guardado por email: so criar se `userRepository.findByEmail("plataforma@lexcv.cv").isEmpty()`.
      `User.builder().tenantId(lexcv.getId()).nome("Administrador de Plataforma").email("plataforma@lexcv.cv")
      .passwordHash(passwordEncoder.encode("Pa$$w0rd")).ativo(true).roles(Set.of(plataformaAdminRole)).build()`.
    - No ramo em que o utilizador e efetivamente criado, emitir um aviso de arranque (mitigacao T-119-06) via
      `System.out.println` — o mesmo mecanismo que o resto de `DatabaseSeeder` ja usa — a dizer explicitamente
      que foi criada uma conta de administrador de plataforma com password por omissao e que ela tem de ser
      alterada antes de qualquer utilizacao fora de desenvolvimento. Este aviso NAO substitui a decisao
      bloqueada de CONTEXT.md (seed incondicional com `Pa$$w0rd`); e defesa em profundidade por cima dela.

    Correr `mvn test -Dtest=DatabaseSeederPlataformaAdminTest`, confirmar GREEN, commitar em separado do RED.

    Proibicoes explicitas:
    - NAO colocar `seedPlataforma()` dentro ou depois de qualquer um dos tres gates do bloco demo.
    - NAO gated o bloco de plataforma por `seedEnabled`, por `SystemSetting.initialized` nem por contagens.
    - NAO alterar nenhuma linha do bloco de dados demo (linhas 58-291) para alem do gate descrito em (a).
    - NAO alterar a semantica do gate demo: continua a exigir as tres contagens a zero; so muda o MOMENTO
      em que sao lidas.
    - NAO usar `tenantRepository.count()`/`findAll()` para decidir a existencia da tenant reservada — o
      lookup e por `findByNome("LexCV")`, o unico criterio idempotente correto quando existirem varios tenants.
    - NAO acrescentar `@Transactional` a `run()` nem a `seedPlataforma()` (nenhum metodo desta classe o tem hoje).
    - NAO acrescentar dependencias ao `pom.xml`.
  </action>

  <verify>
    <automated>cd backend && mvn test -Dtest=DatabaseSeederPlataformaAdminTest && grep -v '^[[:space:]]*[/*]' src/main/java/com/lexcv/seed/DatabaseSeeder.java | grep -c 'tenantRepository.findByNome("LexCV")' && grep -v '^[[:space:]]*[/*]' src/main/java/com/lexcv/seed/DatabaseSeeder.java | grep -c 'userRepository.findByEmail("plataforma@lexcv.cv")' && grep -v '^[[:space:]]*[/*]' src/main/java/com/lexcv/seed/DatabaseSeeder.java | grep -c 'tenantRepository.count() > 0' && test $(grep -n 'bdVaziaAntesDoSeedPlataforma =' src/main/java/com/lexcv/seed/DatabaseSeeder.java | head -1 | cut -d: -f1) -lt $(grep -n 'seedPlataforma();' src/main/java/com/lexcv/seed/DatabaseSeeder.java | head -1 | cut -d: -f1)</automated>
  </verify>

  <acceptance_criteria>
    - `cd backend && mvn test -Dtest=DatabaseSeederPlataformaAdminTest` termina com exit code 0 e reporta 4 testes, 0 falhas, 0 erros.
    - O ficheiro de teste contem 4 metodos `@Test` (`grep -c '@Test'` devolve 4) e usa `InOrder` (`grep -c 'InOrder'` >= 1) e `ArgumentCaptor` (`grep -c 'ArgumentCaptor'` >= 1).
    - Em `DatabaseSeeder.java`, filtrando comentarios: `tenantRepository.findByNome("LexCV")` aparece 1 vez e `userRepository.findByEmail("plataforma@lexcv.cv")` aparece 1 vez.
    - Em `DatabaseSeeder.java`, filtrando comentarios: `grep -c 'tenantRepository.count() > 0'` devolve **0** — o gate inline antigo foi substituido pela variavel hoisted.
    - Em `DatabaseSeeder.java`: o numero de linha da atribuicao `bdVaziaAntesDoSeedPlataforma =` e **inferior** ao numero de linha da chamada `seedPlataforma();` (comando de verificacao com `test ... -lt`).
    - Em `DatabaseSeeder.java`: o numero de linha da chamada `seedPlataforma();` e **inferior** ao numero de linha de `if (!seedEnabled)` — o bloco corre antes de todos os gates.
    - Em `DatabaseSeeder.java`, filtrando comentarios: `grep -c 'Tenant.builder().nome("LexCV")'` devolve 1 e `grep -c '\.plano(\|\.limiteUtilizadores('` devolve 0 dentro de `seedPlataforma` (a tenant reservada nao leva plano nem limite).
    - Em `DatabaseSeeder.java`, filtrando comentarios: `grep -c 'roleRepository.findByNome("PLATAFORMA_ADMIN")'` devolve 1 e essa linha usa `orElseThrow`, nao `orElseGet`.
    - O ramo de criacao do utilizador contem uma chamada `System.out.println` cuja string menciona a password por omissao (mitigacao T-119-06).
    - `cd backend && mvn test` (suite unitaria completa) termina com exit code 0 — nenhuma suite pre-existente regride.
    - `git diff --name-only -- backend/pom.xml backend/src/main/java/com/lexcv/controllers/ backend/src/main/java/com/lexcv/services/` devolve zero linhas.
  </acceptance_criteria>

  <done>Qualquer arranque da aplicacao garante a tenant reservada `"LexCV"`, o utilizador `plataforma@lexcv.cv` com o papel `PLATAFORMA_ADMIN`, e nunca duplica nenhum dos dois; o bloco de dados demo mantem a sua semantica original de "so em base de dados vazia", provado por um teste `InOrder`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Arranque da aplicacao -> base de dados (`DatabaseSeeder`) | Sem input externo nenhum (nao ha request, nao ha utilizador); mas e aqui que nasce a primeira credencial de altissimo privilegio da plataforma |
| Papel de plataforma <-> sistema RBAC scoped por tenant | Fronteira nova: `PLATAFORMA_ADMIN` tem de ficar permanentemente fora do modelo de permissoes por tenant, com conjunto de permissoes vazio |
| Tenant reservada `"LexCV"` <-> tenants de escritorios reais | A tenant de plataforma partilha a tabela `t_tenant` com os tenants pagantes; o seu unico identificador estavel e o nome literal |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-119-06 | Spoofing | Utilizador bootstrap `plataforma@lexcv.cv` / `Pa$$w0rd` seedado **incondicionalmente**, em todos os arranques, incluindo producao | accept (decisao bloqueada) + mitigacao parcial | CONTEXT.md fixa esta decisao explicitamente (seed incondicional, credenciais exatas, "core infrastructure"), pelo que e implementada tal como decidida. **Nota honesta de risco, escalada ao utilizador no retorno de planeamento:** esta credencial NAO esta na mesma categoria de `admin@lexcv.cv`, ao contrario do que a nota de seguranca da fase sugere — `admin@lexcv.cv` vive no bloco demo, protegido por tres gates (`seedEnabled` + `initialized` + base de dados vazia) e por isso nunca aparece num arranque de producao; esta aparece em todos. Mitigacao aplicada sem contradizer a decisao: aviso `System.out.println` no arranque, apenas no ramo em que a conta e realmente criada, a exigir mudanca imediata de password fora de desenvolvimento. Mitigacao adicional recomendada para a Phase 120 (fora do ambito desta): forcar mudanca de password no primeiro login do papel de plataforma |
| T-119-10 | Elevation of Privilege | `PLATAFORMA_ADMIN` receber permissoes scoped no seed | mitigate | `upsertRolePermissions("PLATAFORMA_ADMIN", Collections.emptyList())` — coleccao vazia, verificada por gate de origem na Task 1. `UserPrincipal.create` nao ganha nenhum ramo `if (roles.contains("PLATAFORMA_ADMIN"))`, ao contrario do ramo existente para `ADMIN`, pelo que este papel nunca acumula autoridades scoped. Nota: `upsertRolePermissions` so faz `addAll`, nunca remove — logo o seed **nao consegue reparar** permissoes injetadas por outra via; essa via e fechada pelo Plan 03 (T-119-02) |
| T-119-11 | Tampering | Criacao duplicada da tenant reservada / do utilizador bootstrap em arranques sucessivos | mitigate | Find-or-create idempotente: `tenantRepository.findByNome("LexCV")` antes de qualquer `save`, e `userRepository.findByEmail("plataforma@lexcv.cv").isEmpty()` como guarda do utilizador. Caso 3 do teste prova `never()).save(any())` num segundo arranque. Sem isto, cada reinicio criaria mais uma tenant `"LexCV"` e o lookup por nome ficaria ambiguo |
| T-119-12 | Denial of Service | Regressao silenciosa do seed de dados demo (o seed de plataforma envenena as contagens do gate) | mitigate | Contagens capturadas numa variavel local ANTES de `seedPlataforma()`; gate reescrito para a ler. Caso 4 do teste prova a ordem com `InOrder`, e um gate de origem compara os numeros de linha. Nao e um risco de seguranca classico, mas e uma quebra de integridade de ambiente que passaria despercebida sem prova explicita |
| T-119-13 | Repudiation | Ausencia de rasto da criacao da conta de plataforma | accept | O aviso de arranque (T-119-06) fica no log de arranque da aplicacao, que e o unico canal de observabilidade que `DatabaseSeeder` usa hoje. ASVS L1 nao exige auditoria estruturada de eventos de seed de arranque |
| T-119-SC | Tampering | Instalacoes npm/pip/cargo/Maven | n/a | Esta fase nao instala nenhum pacote nem acrescenta dependencias — `backend/pom.xml` fica fora do diff (gate nas duas tasks). Todas as classes usadas (`Collections`, `ReflectionTestUtils`, Mockito `InOrder`/`ArgumentCaptor`) ja estao disponiveis via `spring-boot-starter-test`. O Package Legitimacy Gate nao se aplica |
</threat_model>

<verification>
1. `cd backend && mvn -q -DskipTests compile` — exit 0.
2. `cd backend && mvn test -Dtest=DatabaseSeederPlataformaAdminTest` — 4 testes, 0 falhas.
3. `cd backend && mvn test` — suite unitaria completa verde (incluindo as suites das Phases 117 e 118, inalteradas).
4. Gates de origem: `upsertRolePermissions("PLATAFORMA_ADMIN", Collections.emptyList())` presente 1 vez; `tenantRepository.findByNome("LexCV")` presente 1 vez; `tenantRepository.count() > 0` presente 0 vezes; linha de `bdVaziaAntesDoSeedPlataforma =` anterior a linha de `seedPlataforma();`, que por sua vez e anterior a `if (!seedEnabled)`.
5. `git diff --name-only` nao lista `pom.xml`, `UserPrincipal.java`, `AdminController.java` nem nada em `controllers/`/`services/`.
</verification>

<success_criteria>
- Um arranque numa base de dados vazia produz: 1 papel `PLATAFORMA_ADMIN` com 0 permissoes, 1 tenant `"LexCV"`, 1 utilizador `plataforma@lexcv.cv` ativo com esse papel e so esse papel.
- Um segundo arranque nao produz linha nenhuma nova (idempotente).
- `SEED_ENABLED=false` nao impede nenhuma das tres linhas de existir.
- `SEED_ENABLED=true` numa base de dados vazia continua a produzir os dados demo, exatamente como antes desta fase.
- `TenantRepository.findByNome` existe e o Javadoc WR-01 de `findFirstByOrderByCreatedAtAsc` esta intacto.
</success_criteria>

<output>
Create `.planning/phases/LEXCV-119-backend-papel-de-administrador-de-plataforma-e-provisionamen/119-01-SUMMARY.md` when done
</output>
