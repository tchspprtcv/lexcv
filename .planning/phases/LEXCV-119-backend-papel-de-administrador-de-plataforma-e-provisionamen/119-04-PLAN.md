---
phase: 119-backend-papel-de-administrador-de-plataforma-e-provisionamen
plan: 04
type: execute
wave: 2
depends_on: [119-02]
files_modified:
  - backend/src/main/java/com/lexcv/controllers/PlatformAdminController.java
  - backend/src/main/java/com/lexcv/config/GlobalExceptionHandler.java
  - backend/src/test/java/com/lexcv/controllers/PlatformAdminControllerTest.java
  - backend/src/test/java/com/lexcv/controllers/SetupControllerSingletonRegressaoTest.java
autonomous: true
requirements: [PROV-06, PROV-01]

must_haves:
  truths:
    - "POST /api/v1/platform/tenants cria um tenant novo e devolve 201 com o id e o nome do tenant criado, nunca a entidade Tenant serializada"
    - "Um utilizador autenticado com o papel ADMIN de um tenant normal e recusado ao invocar POST /api/v1/platform/tenants — o gate @PreAuthorize dispara antes de o metodo do controller correr"
    - "Uma recusa de autorizacao chega ao cliente como 403, nao como 500"
    - "Um utilizador com o papel PLATAFORMA_ADMIN passa o mesmo gate e recebe 201"
    - "POST /api/v1/setup/initialize continua a devolver erro quando chamado uma segunda vez — o gate singleton esta intacto e SetupController.java nao foi modificado"
    - "/api/v1/platform nao esta na allowlist permitAll() do SecurityConfig — cai em anyRequest().authenticated()"
    - "PlatformAdminController nao le tenantId do SecurityContext: provisiona um tenant novo em vez de agir dentro do tenant do chamador"
  artifacts:
    - path: "backend/src/main/java/com/lexcv/controllers/PlatformAdminController.java"
      provides: "Endpoint POST /api/v1/platform/tenants gated a PLATAFORMA_ADMIN ao nivel da classe"
      contains: "hasRole('PLATAFORMA_ADMIN')"
      min_lines: 40
    - path: "backend/src/main/java/com/lexcv/config/GlobalExceptionHandler.java"
      provides: "Mapeamento AccessDeniedException -> 403, para que a recusa do @PreAuthorize nao seja engolida pelo catch-all de 500"
      contains: "AccessDeniedException"
    - path: "backend/src/test/java/com/lexcv/controllers/PlatformAdminControllerTest.java"
      provides: "Prova comportamental do gate de papel com proxy real de method security, do 201 e do DTO sem entidades cruas"
      contains: "AuthorizationManagerBeforeMethodInterceptor"
      min_lines: 150
    - path: "backend/src/test/java/com/lexcv/controllers/SetupControllerSingletonRegressaoTest.java"
      provides: "Prova de que o wizard publico continua singleton depois desta fase"
      contains: "isInitialized"
      min_lines: 60
  key_links:
    - from: "backend/src/main/java/com/lexcv/controllers/PlatformAdminController.java"
      to: "backend/src/main/java/com/lexcv/services/SetupService.java"
      via: "delegacao ao metodo de provisionamento do Plan 02, nunca a initializeSystem"
      pattern: "setupService\\.provisionTenant\\(request\\)"
    - from: "backend/src/main/java/com/lexcv/controllers/PlatformAdminController.java"
      to: "backend/src/main/java/com/lexcv/dtos/TenantProvisionResponse.java"
      via: "copia explicita getter-para-setter do id e do nome, sem serializar a entidade"
      pattern: "TenantProvisionResponse\\.builder\\(\\)"
    - from: "backend/src/main/java/com/lexcv/config/GlobalExceptionHandler.java"
      to: "backend/src/main/java/com/lexcv/config/SecurityConfig.java"
      via: "traducao da recusa de method security em resposta HTTP 403"
      pattern: "HttpStatus\\.FORBIDDEN"
---

<objective>
Expor a capacidade de provisionamento num controller novo, `POST /api/v1/platform/tenants`, gated a `PLATAFORMA_ADMIN` ao nivel da classe; garantir que uma recusa de autorizacao chega ao cliente como `403` e nao como `500`; e provar por teste que o wizard publico `/setup/initialize` continua singleton e intacto.

Purpose: fecha os Success Criteria 3 e 4 da Phase 119. O criterio 4 exige literalmente `403` — e a leitura de planeamento confirmou que hoje isso **nao acontece**: `GlobalExceptionHandler` tem um `@ExceptionHandler(Exception.class)` que devolve `500`, e nenhum handler de `AccessDeniedException` existe em todo o backend, pelo que a recusa do `@PreAuthorize` e engolida e devolvida como `500`. E exatamente o sintoma ja registado em STATE.md para `GET /api/v1/admin/users` ("For any non-ADMIN role this returns 500 (should be 403)"). Sem o mapeamento novo, o criterio 4 seria falso.

Output: um controller novo, um `@ExceptionHandler` novo, e dois testes novos.
</objective>

<execution_context>
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/LEXCV-119-backend-papel-de-administrador-de-plataforma-e-provisionamen/119-CONTEXT.md
@.planning/phases/LEXCV-119-backend-papel-de-administrador-de-plataforma-e-provisionamen/119-PATTERNS.md
@.planning/phases/LEXCV-119-backend-papel-de-administrador-de-plataforma-e-provisionamen/119-02-SUMMARY.md

# 119-PATTERNS.md tem o ficheiro completo do SetupController (analog estrutural), o cabecalho do
# AdminController (analog do gate de papel), o padrao de resposta 201 sem entidade crua, e a
# confirmacao de que SecurityConfig NAO precisa de alteracao nenhuma. Usa-o como fonte primaria.
</context>

<interfaces>
<!-- Contratos ja existentes / criados pelo Plan 02. O executor NAO precisa de os redescobrir. -->

Criado pelo Plan 02 (Wave 1) — assinatura exata a invocar:
```java
// com.lexcv.services.SetupService
@Transactional
public Tenant provisionTenant(SetupInitializeRequest request);
// lanca IllegalArgumentException (validacao / email duplicado)
// lanca IllegalStateException  (papel ADMIN ausente)
```
```java
// com.lexcv.dtos.TenantProvisionResponse
@Data @Builder @NoArgsConstructor @AllArgsConstructor
public class TenantProvisionResponse { private UUID id; private String nome; }
```

`backend/src/main/java/com/lexcv/dtos/SetupInitializeRequest.java` — reutilizado como corpo do pedido:
```java
@Getter @Setter
public class SetupInitializeRequest {
    private String clientName; private String logo;
    private String adminEmail; private String adminPassword;
}
```

`backend/src/main/java/com/lexcv/controllers/SetupController.java` — o analog estrutural (ficheiro
completo, 53 linhas). O mapeamento de excecoes a copiar e o do `initialize`:
```java
try {
    setupService.initializeSystem(request);
    return ResponseEntity.status(HttpStatus.CREATED).body(...);
} catch (IllegalArgumentException ex) {
    return ResponseEntity.badRequest().body(Map.of("message", ex.getMessage()));
} catch (IllegalStateException ex) {
    return ResponseEntity.status(HttpStatus.FORBIDDEN).body(Map.of("message", ex.getMessage()));
}
```
**Este ficheiro NAO e modificado por esta fase.** O `if (setupService.isInitialized())` que abre o
`initialize` e o gate singleton do Success Criterion 3 e fica exatamente como esta.

`backend/src/main/java/com/lexcv/controllers/AdminController.java` linhas 26-30 — o analog do gate:
```java
@RestController
@RequestMapping("/api/v1/admin")
@PreAuthorize("hasRole('ADMIN')")
@RequiredArgsConstructor
public class AdminController { ... }
```

`backend/src/main/java/com/lexcv/config/GlobalExceptionHandler.java` (ficheiro completo hoje, 50 linhas)
— `@RestControllerAdvice` com tres handlers: `MethodArgumentNotValidException` -> 400,
`ConstraintViolationException` -> 400, e `Exception.class` -> **500**. Nao existe handler de
`AccessDeniedException` em lado nenhum do backend (confirmado por grep em `src/main/java/`).

`backend/src/main/java/com/lexcv/config/SecurityConfig.java` — `@EnableMethodSecurity` esta ligado
(linha 25). A allowlist `permitAll()` (linhas 52-68) tem apenas `/api/v1/auth/login`, `/auth/refresh`,
`/auth/logout`, `/setup/status`, `/setup/initialize`, `/public/branding`; tudo o resto cai em
`.anyRequest().authenticated()` (linha 69). **`SecurityConfig.java` NAO e modificado por esta fase.**

Classes de Spring Security confirmadas disponiveis offline (spring-security-core 6.4.2, ja no
classpath via `spring-boot-starter-security`; `spring-security-test` 6.4.2 tambem ja e dependencia
de teste declarada no `pom.xml`):
```
org.springframework.security.access.AccessDeniedException
org.springframework.security.authorization.AuthorizationDeniedException   // extends AccessDeniedException
org.springframework.security.authorization.method.AuthorizationManagerBeforeMethodInterceptor
org.springframework.aop.framework.ProxyFactory                            // spring-aop
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: GlobalExceptionHandler mapeia AccessDeniedException para 403</name>

  <files>
    backend/src/main/java/com/lexcv/config/GlobalExceptionHandler.java (modificar)
  </files>

  <read_first>
    - `backend/src/main/java/com/lexcv/config/GlobalExceptionHandler.java` — ficheiro completo (50 linhas) antes de editar; reparar em que o `@ExceptionHandler(Exception.class)` faz `logger.error` e devolve `500` com um corpo `{"error", "message"}`.
    - `backend/src/main/java/com/lexcv/config/SecurityConfig.java` — linhas 23-27, para confirmar que `@EnableMethodSecurity` esta ligado (e por isso a recusa e lancada como excecao dentro do `DispatcherServlet`, onde o `@RestControllerAdvice` a apanha antes do `ExceptionTranslationFilter`).
    - `.planning/STATE.md` — Blockers/Concerns, entrada "(v2.13, Phase 108) Pareceres 'Advogado' picker": o registo do sintoma ja observado em producao (`500` em vez de `403` num endpoint gated por papel). Esta task fecha a metade backend desse sintoma.
  </read_first>

  <action>
    Em `backend/src/main/java/com/lexcv/config/GlobalExceptionHandler.java`, acrescentar um handler novo
    **antes** do `@ExceptionHandler(Exception.class)` existente (a ordem de declaracao nao decide a
    resolucao — o Spring escolhe sempre o handler mais especifico — mas manter o catch-all em ultimo torna
    a intencao legivel):

    `@ExceptionHandler(org.springframework.security.access.AccessDeniedException.class)` devolvendo
    `ResponseEntity.status(HttpStatus.FORBIDDEN)` com um corpo `Map<String, String>` no mesmo formato dos
    handlers vizinhos. Usar `logger.warn` (nao `logger.error`): uma recusa de autorizacao e um evento
    esperado, nao uma falha do servidor. A mensagem do corpo deve ser generica e em portugues (por exemplo
    "Acesso negado.") e **nao** deve ecoar `ex.getMessage()` — a mensagem interna do Spring Security pode
    revelar a expressao `@PreAuthorize` avaliada. O log pode conter o detalhe; a resposta ao cliente nao.

    Importar `org.springframework.security.access.AccessDeniedException`. Nota importante: capturar a classe
    **pai** `AccessDeniedException` e nao `AuthorizationDeniedException` — no Spring Security 6.4 a recusa
    de method security e lancada como `AuthorizationDeniedException`, que **estende** `AccessDeniedException`;
    capturar a pai cobre ambas e mantem-se estavel se o tipo concreto mudar de versao.

    Acrescentar um comentario de bloco curto a explicar porque este handler tem de existir: sem ele, a
    recusa de `@PreAuthorize` propaga-se ate ao `@ExceptionHandler(Exception.class)` e o cliente recebe
    `500`, o que (a) faz o `Success Criterion 4` da Phase 119 falhar literalmente, e (b) e o sintoma ja
    registado em STATE.md para `GET /api/v1/admin/users`.

    **Efeito lateral deliberado, a registar no SUMMARY:** este handler e global, por isso *todos* os
    endpoints ja gated por `@PreAuthorize` (`/api/v1/admin/**`, `/api/v1/notificacoes/**`, os metodos de
    `ResourceController`, etc.) passam a devolver `403` em vez de `500` numa recusa. Isto e a correcao do
    comportamento documentado como correto em STATE.md, e e seguro do lado do cliente: `web/src/lib/api.ts`
    suprime deliberadamente o toast de erro para `401`/`403` e apenas lanca (linha 43,
    `if (res.status !== 401 && res.status !== 403)`), sem qualquer redirect automatico — logo a mudanca
    remove o toast espurio "Erro 500: Access Denied" e nao introduz nenhum redirect novo. Registar esta
    analise no SUMMARY.

    Proibicoes explicitas:
    - NAO alterar nem remover os tres handlers existentes.
    - NAO acrescentar um `accessDeniedHandler` ao `SecurityConfig` — o problema esta na camada do
      `DispatcherServlet` (onde o `@RestControllerAdvice` intercepta primeiro), nao no filtro.
    - NAO ecoar `ex.getMessage()` no corpo da resposta.
    - NAO tocar em `SecurityConfig.java`.
    - NAO acrescentar dependencias ao `pom.xml`.
  </action>

  <verify>
    <automated>cd backend && mvn -q -DskipTests compile && grep -c 'AccessDeniedException' src/main/java/com/lexcv/config/GlobalExceptionHandler.java && grep -c '@ExceptionHandler(Exception.class)' src/main/java/com/lexcv/config/GlobalExceptionHandler.java && grep -v '^[[:space:]]*[/*]' src/main/java/com/lexcv/config/GlobalExceptionHandler.java | grep -c 'HttpStatus.FORBIDDEN' && test $(sed -n '/AccessDeniedException ex/,/^    }/p' src/main/java/com/lexcv/config/GlobalExceptionHandler.java | grep -c 'ex.getMessage()') -eq 0</automated>
  </verify>

  <acceptance_criteria>
    - `cd backend && mvn -q -DskipTests compile` termina com exit code 0.
    - Em `GlobalExceptionHandler.java`: `grep -c '@ExceptionHandler'` devolve 4 (os 3 pre-existentes + o novo).
    - Em `GlobalExceptionHandler.java`: `grep -c '@ExceptionHandler(Exception.class)'` devolve 1 e `grep -c 'MethodArgumentNotValidException'` e `grep -c 'ConstraintViolationException'` continuam a devolver os valores de antes desta fase.
    - Em `GlobalExceptionHandler.java`, filtrando comentarios: `grep -c 'HttpStatus.FORBIDDEN'` devolve 1.
    - Em `GlobalExceptionHandler.java`: o handler novo captura `AccessDeniedException` (a pai) e **nao** `AuthorizationDeniedException` — `grep -c 'AuthorizationDeniedException'` devolve 0.
    - **Extraindo apenas o corpo do handler novo** (`sed -n '/AccessDeniedException ex/,/^    }/p'`): `grep -c 'ex.getMessage()'` devolve **0** — a mensagem interna nao vaza para o cliente.
    - Extraindo apenas o corpo do handler novo: `grep -c 'logger.warn'` devolve 1 e `grep -c 'logger.error'` devolve 0.
    - `git diff --name-only -- backend/src/main/java/com/lexcv/config/SecurityConfig.java backend/pom.xml` devolve zero linhas.
  </acceptance_criteria>

  <done>Uma recusa de autorizacao de method security produz `403` com um corpo generico em portugues, em vez do `500` que o catch-all produzia — sem alterar nenhum dos handlers existentes nem o `SecurityConfig`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: PlatformAdminController com gate de papel provado por proxy real</name>

  <files>
    backend/src/test/java/com/lexcv/controllers/PlatformAdminControllerTest.java (novo)
    backend/src/main/java/com/lexcv/controllers/PlatformAdminController.java (novo)
  </files>

  <read_first>
    - `.planning/phases/LEXCV-119-backend-papel-de-administrador-de-plataforma-e-provisionamen/119-PATTERNS.md` — seccao "PlatformAdminController.java": imports + cabecalho de classe, o template de `initialize` a adaptar, o padrao de 201 sem entidade crua de `AdminController.createUser`, e a seccao "Shared Patterns / Authentication" que confirma que `SecurityConfig` nao precisa de alteracao.
    - `.planning/phases/LEXCV-119-backend-papel-de-administrador-de-plataforma-e-provisionamen/119-CONTEXT.md` — seccao `<decisions>`, bloco "New Endpoint — Fully Distinct Code Path", incluindo a discricao sobre o `GET` de listagem (ver proibicoes abaixo).
    - `.planning/phases/LEXCV-119-backend-papel-de-administrador-de-plataforma-e-provisionamen/119-02-SUMMARY.md` — a assinatura final entregue de `provisionTenant` e do `TenantProvisionResponse`, caso o Plan 02 tenha registado algum desvio.
    - `backend/src/main/java/com/lexcv/controllers/SetupController.java` — ficheiro completo, o analog estrutural.
    - `backend/src/test/java/com/lexcv/controllers/PublicControllerTest.java` — estilo de Javadoc de classe e de assercoes deste codebase.
  </read_first>

  <behavior>
    Casos que `PlatformAdminControllerTest` deve provar. Escrever ANTES de criar o controller — devem falhar
    a compilar. Commitar o RED e o GREEN em commits separados.

    O ficheiro tem dois grupos: assercoes funcionais sobre o metodo (chamada Java direta, como no resto do
    codebase) e assercoes de autorizacao sobre um **proxy real de method security** (necessario porque uma
    chamada Java direta ao controller nunca avalia o `@PreAuthorize`).

    Grupo A — comportamento do handler (instanciacao direta, sem proxy):
    - **Caso 1 — 201 com id e nome, sem entidade crua.** `setupService.provisionTenant(request)` devolve um
      `Tenant` com `id` e `nome = "Escritorio Novo"`. Asserir `HttpStatus.CREATED`; que o corpo e do tipo
      `TenantProvisionResponse` (nao `Tenant`) — usar `assertInstanceOf(TenantProvisionResponse.class, ...)`
      e `assertFalse(response.getBody() instanceof Tenant)`; e que `getId()`/`getNome()` correspondem aos do
      tenant devolvido pelo servico.
    - **Caso 2 — IllegalArgumentException -> 400.** O servico lanca
      `new IllegalArgumentException("O email do administrador é inválido.")`. Asserir
      `HttpStatus.BAD_REQUEST` e que o corpo e um `Map` com a chave `message` e essa mensagem.
    - **Caso 3 — IllegalStateException -> 403.** O servico lanca
      `new IllegalStateException("O papel ADMIN não está configurado.")`. Asserir `HttpStatus.FORBIDDEN`,
      mantendo o mesmo mapeamento que `SetupController.initialize` ja usa.
    - **Caso 4 — delega em provisionTenant, nunca em initializeSystem.** No cenario do Caso 1, asserir
      `verify(setupService, times(1)).provisionTenant(any())` e
      `verify(setupService, never()).initializeSystem(any())` e
      `verify(setupService, never()).isInitialized()`. Prova que o caminho novo nao reaproveita nem consulta
      o gate do wizard publico (Success Criterion 3).

    Grupo B — gate de autorizacao (proxy real de method security):
    - **Caso 5 — ROLE_ADMIN de um tenant normal e recusado.** Montar o proxy:
      `ProxyFactory factory = new ProxyFactory(controller); factory.setProxyTargetClass(true);
      factory.addAdvisor(AuthorizationManagerBeforeMethodInterceptor.preAuthorize());
      PlatformAdminController proxy = (PlatformAdminController) factory.getProxy();`
      Povoar o `SecurityContextHolder` com um `UsernamePasswordAuthenticationToken` cujas autoridades sao
      `List.of(new SimpleGrantedAuthority("ROLE_ADMIN"))`. Asserir
      `assertThrows(AccessDeniedException.class, () -> proxy.<metodo>(request))` **e**
      `verify(setupService, never()).provisionTenant(any())` — a recusa acontece antes de o metodo correr.
      Este e o Success Criterion 4 provado comportamentalmente, com a anotacao real a ser avaliada, e nao
      apenas lida por reflexao.
    - **Caso 6 — ROLE_PLATAFORMA_ADMIN passa o mesmo gate.** Mesmo proxy, autoridades
      `List.of(new SimpleGrantedAuthority("ROLE_PLATAFORMA_ADMIN"))`. Asserir que nao ha excecao e que a
      resposta e `HttpStatus.CREATED`. Sem este caso, o Caso 5 passaria mesmo com um gate partido que
      recusasse toda a gente.
    - **Caso 7 — assercao estrutural do valor exato da anotacao.** Por reflexao:
      `PlatformAdminController.class.getAnnotation(PreAuthorize.class).value()` e exatamente
      `hasRole('PLATAFORMA_ADMIN')`. Barato, e apanha uma gralha no nome do papel (que tornaria o gate
      inalcancavel) ou a remocao acidental da anotacao.
    - **Caso 8 — o handler nao le tenantId do SecurityContext.** No Caso 6, o `UserPrincipal` colocado no
      contexto tem `tenantId` da tenant reservada; asserir que o `SetupInitializeRequest` passado ao servico
      e exatamente o objeto recebido no parametro (`verify(setupService).provisionTenant(same(request))`).
      Prova que o controller nao injeta nem sobrepoe nenhum tenant do chamador — ao contrario dos endpoints
      tenant-scoped de `AdminController`, este cria um tenant **novo**.

    Notas de montagem (todas confirmadas na leitura de planeamento):
    - `AuthorizationManagerBeforeMethodInterceptor` implementa `PointcutAdvisor`, por isso `addAdvisor(...)`
      aceita-o diretamente. `preAuthorize()` e a fabrica estatica que ja o configura com o
      `PreAuthorizeAuthorizationManager`; `hasRole('X')` nao precisa de `ApplicationContext`.
    - `setProxyTargetClass(true)` e obrigatorio: `PlatformAdminController` nao implementa interfaces, logo
      o proxy tem de ser CGLIB. A classe e os metodos nao podem ser `final`.
    - Capturar `AccessDeniedException` (a pai), nao `AuthorizationDeniedException` — o Spring Security 6.4
      lanca a filha, que estende a pai; asserir a pai e estavel entre versoes.
    - `@AfterEach` com `SecurityContextHolder.clearContext()`, como em `AdminControllerLimiteUtilizadoresTest`.
    - Javadoc de classe a explicar porque existe um proxy AOP neste ficheiro quando o resto do codebase
      instancia controllers diretamente: uma chamada Java direta **nao** avalia `@PreAuthorize`, pelo que
      sem o proxy o Success Criterion 4 nao teria prova comportamental nenhuma. Referir tambem que isto nao
      introduz um harness `MockMvc`/`@SpringBootTest` (que continua a nao existir neste projeto).
  </behavior>

  <action>
    Escrever primeiro `backend/src/test/java/com/lexcv/controllers/PlatformAdminControllerTest.java` com os
    8 casos. Correr `cd backend && mvn test -Dtest=PlatformAdminControllerTest`, confirmar RED, commitar.

    Depois criar `backend/src/main/java/com/lexcv/controllers/PlatformAdminController.java`:

    Cabecalho de classe, replicando o padrao de `AdminController` com o papel trocado:
    `@RestController`, `@RequestMapping("/api/v1/platform")`, a anotacao `@PreAuthorize` com a expressao
    `hasRole('PLATAFORMA_ADMIN')` ao **nivel da classe** (nunca so no metodo — o gate de classe cobre
    automaticamente qualquer handler que a Phase 120 venha a acrescentar), `@RequiredArgsConstructor`,
    e um unico colaborador `private final SetupService setupService;`.

    Um unico handler: `@PostMapping("/tenants")`, recebendo `@RequestBody SetupInitializeRequest request` e
    devolvendo `ResponseEntity<?>`. Corpo, adaptado do template de `SetupController.initialize`:
    - `try { Tenant tenant = setupService.provisionTenant(request); }` e construir
      `TenantProvisionResponse.builder().id(tenant.getId()).nome(tenant.getNome()).build()`, devolvido com
      `ResponseEntity.status(HttpStatus.CREATED).body(response)`. Copia explicita getter-para-setter; nunca
      `body(tenant)`.
    - `catch (IllegalArgumentException ex) { return ResponseEntity.badRequest().body(Map.of("message", ex.getMessage())); }`
    - `catch (IllegalStateException ex) { return ResponseEntity.status(HttpStatus.FORBIDDEN).body(Map.of("message", ex.getMessage())); }`
    - **Sem** o pre-check `if (setupService.isInitialized())` — esse e o gate singleton do wizard publico e
      nao tem lugar neste caminho.

    Javadoc de classe a documentar: (a) que e a superficie de administracao de plataforma, gated ao papel
    `PLATAFORMA_ADMIN` e nunca acessivel a um `ADMIN` de escritorio; (b) que nao e tenant-scoped — ao
    contrario de `AdminController`, nao le `UserPrincipal.getTenantId()`, porque cria um tenant novo; (c)
    que `POST /api/v1/setup/initialize` continua a existir, publico e singleton, e que este controller nunca
    lhe chama.

    Correr `mvn test -Dtest=PlatformAdminControllerTest`, confirmar GREEN, commitar em separado do RED.

    Proibicoes explicitas:
    - NAO acrescentar `/api/v1/platform` a allowlist `permitAll()` do `SecurityConfig` — o caminho tem de
      cair em `.anyRequest().authenticated()`. `SecurityConfig.java` fica fora do diff.
    - NAO acrescentar um `GET /api/v1/platform/tenants` de listagem. CONTEXT.md deixa-o a discricao mas
      atribui a listagem a PROV-03, que e um requisito da **Phase 120**; esta fase entrega so o que
      PROV-01/PROV-06 exigem.
    - NAO ler `SecurityContextHolder`/`UserPrincipal` neste controller.
    - NAO chamar `setupService.initializeSystem(...)` nem `setupService.isInitialized()`.
    - NAO devolver `Tenant`, `User` ou `Role` no corpo da resposta.
    - NAO modificar `SetupController.java`, `SetupService.java`, `SecurityConfig.java` nem `pom.xml`.
    - NAO declarar a classe ou o metodo como `final` (impediria o proxy CGLIB do teste e o proxy real do
      Spring).
  </action>

  <verify>
    <automated>cd backend && mvn test -Dtest=PlatformAdminControllerTest && grep -c "hasRole('PLATAFORMA_ADMIN')" src/main/java/com/lexcv/controllers/PlatformAdminController.java && grep -c 'RequestMapping("/api/v1/platform")' src/main/java/com/lexcv/controllers/PlatformAdminController.java && grep -v '^[[:space:]]*[/*]' src/main/java/com/lexcv/controllers/PlatformAdminController.java | grep -c 'setupService.provisionTenant(request)' && test $(grep -v '^[[:space:]]*[/*]' src/main/java/com/lexcv/controllers/PlatformAdminController.java | grep -c 'isInitialized\|initializeSystem\|SecurityContextHolder\|UserPrincipal\|body(tenant)') -eq 0 && test $(grep -v '^[[:space:]]*[/*]' src/main/java/com/lexcv/config/SecurityConfig.java | grep -c '/api/v1/platform') -eq 0</automated>
  </verify>

  <acceptance_criteria>
    - `cd backend && mvn test -Dtest=PlatformAdminControllerTest` termina com exit code 0 e reporta 8 testes, 0 falhas, 0 erros.
    - O teste contem `AuthorizationManagerBeforeMethodInterceptor.preAuthorize()` e `setProxyTargetClass(true)` (`grep -c` >= 1 cada) — o gate e provado com a anotacao real a ser avaliada, nao so por reflexao.
    - O teste contem `assertThrows(AccessDeniedException.class` (Caso 5) e uma assercao de `HttpStatus.CREATED` sob `ROLE_PLATAFORMA_ADMIN` (Caso 6).
    - Em `PlatformAdminController.java`: `grep -c "hasRole('PLATAFORMA_ADMIN')"` devolve 1 e essa anotacao esta ao nivel da classe (numero de linha inferior ao de `public class PlatformAdminController`).
    - Em `PlatformAdminController.java`: `grep -c 'RequestMapping("/api/v1/platform")'` devolve 1 e `grep -c 'PostMapping("/tenants")'` devolve 1.
    - Em `PlatformAdminController.java`, filtrando comentarios: `grep -c 'isInitialized\|initializeSystem\|SecurityContextHolder\|UserPrincipal\|body(tenant)'` devolve **0**.
    - Em `PlatformAdminController.java`, filtrando comentarios: `grep -c '@GetMapping\|@PutMapping\|@DeleteMapping\|@PatchMapping'` devolve **0** — apenas o `POST` desta fase.
    - Em `PlatformAdminController.java`, filtrando comentarios: `grep -c 'TenantProvisionResponse.builder()'` devolve 1.
    - Em `SecurityConfig.java`, filtrando comentarios: `grep -c '/api/v1/platform'` devolve **0** — o caminho novo nao entrou na allowlist publica.
    - `git diff --name-only -- backend/src/main/java/com/lexcv/config/SecurityConfig.java backend/src/main/java/com/lexcv/controllers/SetupController.java backend/src/main/java/com/lexcv/services/SetupService.java backend/pom.xml` devolve zero linhas.
  </acceptance_criteria>

  <done>`POST /api/v1/platform/tenants` cria tenants e devolve `201` com um DTO de `id` + `nome`; um `ROLE_ADMIN` e recusado com `AccessDeniedException` (mapeada para `403` pela Task 1) antes de o metodo correr, e um `ROLE_PLATAFORMA_ADMIN` passa — tudo provado com a anotacao real a ser avaliada por um proxy de method security.</done>
</task>

<task type="auto">
  <name>Task 3: Regressao do gate singleton de /setup/initialize, suite completa e verificacao STRIDE</name>

  <files>
    backend/src/test/java/com/lexcv/controllers/SetupControllerSingletonRegressaoTest.java (novo)
  </files>

  <read_first>
    - `backend/src/main/java/com/lexcv/controllers/SetupController.java` — ficheiro completo (53 linhas); confirmar que continua exatamente como estava antes da fase.
    - `backend/src/test/java/com/lexcv/controllers/PublicControllerTest.java` — estilo de teste de controller a replicar.
    - `.planning/phases/LEXCV-119-backend-papel-de-administrador-de-plataforma-e-provisionamen/119-02-SUMMARY.md` — confirmar que o Plan 02 nao tocou em `initializeSystem`.
    - `backend/spotbugs-exclude.xml` — para distinguir um achado SAST novo de um ja excluido.
    - `.planning/STATE.md` — Blockers/Concerns: os testes `*IT` (Testcontainers) tem bloqueio ambiental de Docker nesta maquina; nao os usar como gate local.
  </read_first>

  <action>
    Criar `backend/src/test/java/com/lexcv/controllers/SetupControllerSingletonRegressaoTest.java`
    (`@ExtendWith(MockitoExtension.class)`, `@Mock SetupService setupService`, instanciacao direta
    `new SetupController(setupService)`), com tres casos que provam o Success Criterion 3:
    - **Sistema ja inicializado -> recusa.** `setupService.isInitialized()` devolve `true`. Asserir que
      `initialize(request)` devolve `HttpStatus.FORBIDDEN` com a mensagem
      `"O sistema já foi inicializado."` e `verify(setupService, never()).initializeSystem(any())` — a
      segunda chamada nunca chega ao servico.
    - **Primeira inicializacao -> 201.** `isInitialized()` devolve `false`. Asserir `HttpStatus.CREATED` e
      `verify(setupService, times(1)).initializeSystem(any())`.
    - **O caminho publico nunca toca no provisionamento de plataforma.** Em ambos os cenarios acima,
      asserir `verify(setupService, never()).provisionTenant(any())` — os dois caminhos sao irmaos
      independentes, nunca encadeados.

    Acrescentar um Javadoc de classe a dizer que este ficheiro existe como **teste de regressao** do Success
    Criterion 3: o endpoint publico de `/setup` continua singleton depois de a Phase 119 ter introduzido um
    segundo caminho de criacao de tenants, e o `SetupController` nao foi modificado por esta fase.

    Correr a suite unitaria completa (`mvn test`) e confirmar que fica verde, com atencao a
    `AdminControllerLimiteUtilizadoresTest` (Phase 117) e `AuthControllerGetMeTenantPlanoTest` (Phase 118).
    Se algum teste falhar por causa da alteracao global do `GlobalExceptionHandler` (Task 1), corrigir o
    teste apenas se ele asserir explicitamente `500` numa recusa de autorizacao — nesse caso o `403` e o
    comportamento correto e a assercao antiga estava a cimentar o defeito; documentar a alteracao no SUMMARY.
    Nunca enfraquecer uma assercao por conveniencia.

    NAO usar `mvn verify` como gate local (testes `*IT` em Testcontainers, bloqueio ambiental conhecido).

    Correr `mvn spotbugs:check` e confirmar que passa sem achados novos. Se surgir um achado, corrigi-lo no
    codigo; so acrescentar entrada a `spotbugs-exclude.xml` se for falso positivo comprovado, documentando
    a justificacao no SUMMARY.

    Registar no SUMMARY um veredito explicito (CONFIRMADO / CORRIGIDO) por item do `<threat_model>`, no
    espirito da auditoria AUD-01 da v2.11:
    - **T-119-04 (EoP — endpoint alcancavel por papel errado):** citar o resultado dos Casos 5/6/7 do teste
      do controller.
    - **T-119-05 (EoP — endpoint alcancavel sem autenticacao):** confirmar por assercao de origem que
      `/api/v1/platform` **nao** aparece em `SecurityConfig.java`, logo cai em `.anyRequest().authenticated()`.
    - **T-119-08 (Tampering — bypass do gate singleton):** citar os tres casos deste ficheiro novo e
      confirmar que `SetupController.java` e `SetupService.initializeSystem` estao fora do diff da fase.
    - **T-119-17 (Information Disclosure — entidade crua na resposta):** confirmar `body(tenant)` ausente e
      `TenantProvisionResponse.builder()` presente.
    - **T-119-21 (Information Disclosure — mensagem interna de autorizacao no corpo do 403):** confirmar que
      o handler novo nao ecoa `ex.getMessage()`.
    - **T-119-SC (cadeia de fornecimento):** confirmar `backend/pom.xml` fora do diff.

    Registar tambem no SUMMARY, como nota de impacto transversal, que o handler da Task 1 muda o codigo de
    estado de **todas** as recusas de `@PreAuthorize` do backend de `500` para `403`, incluindo o caso
    `GET /api/v1/admin/users` documentado em STATE.md, e que `web/src/lib/api.ts` ja trata `403` sem toast e
    sem redirect (linha 43) — pelo que o efeito visivel e a remocao de um toast espurio, sem regressao de
    navegacao.

    Confirmar por fim que `mvn -DskipTests package` produz o artefacto e que `git status --porcelain` nao
    lista ficheiros de backend fora dos declarados nos quatro planos desta fase.
  </action>

  <verify>
    <automated>cd backend && mvn test -Dtest=SetupControllerSingletonRegressaoTest && mvn test && mvn spotbugs:check && mvn -q -DskipTests package && git diff --name-only -- src/main/java/com/lexcv/controllers/SetupController.java src/main/java/com/lexcv/config/SecurityConfig.java pom.xml | wc -l | grep -qx 0</automated>
  </verify>

  <acceptance_criteria>
    - `cd backend && mvn test -Dtest=SetupControllerSingletonRegressaoTest` termina com exit code 0 e reporta 3 testes, 0 falhas, 0 erros.
    - O teste contem `verify(setupService, never()).initializeSystem` e `verify(setupService, never()).provisionTenant` (`grep -c` >= 1 cada).
    - `cd backend && mvn test` termina com exit code 0 (BUILD SUCCESS), 0 falhas e 0 erros; o total de testes executados e igual ao de antes desta fase mais a soma dos testes dos quatro planos.
    - `cd backend && mvn test -Dtest=AdminControllerLimiteUtilizadoresTest` e `cd backend && mvn test -Dtest=AuthControllerGetMeTenantPlanoTest` continuam verdes com o mesmo numero de testes de antes desta fase.
    - `cd backend && mvn spotbugs:check` termina com exit code 0, sem achados novos. Qualquer entrada nova em `spotbugs-exclude.xml` esta justificada por escrito no SUMMARY.
    - `cd backend && mvn -q -DskipTests package` termina com exit code 0.
    - `git diff --name-only -- backend/src/main/java/com/lexcv/controllers/SetupController.java backend/src/main/java/com/lexcv/config/SecurityConfig.java backend/pom.xml` devolve zero linhas.
    - O SUMMARY contem um veredito explicito (CONFIRMADO ou CORRIGIDO) para cada um dos 6 itens T-119-04/05/08/17/21/SC.
    - O SUMMARY contem a nota de impacto transversal do `403` global, incluindo a referencia a `web/src/lib/api.ts` linha 43 e ao caso `GET /api/v1/admin/users` de STATE.md.
  </acceptance_criteria>

  <done>O wizard publico continua provadamente singleton, a suite unitaria completa esta verde, o SAST passa sem achados novos, e as 6 mitigacoes STRIDE deste plano tem veredito explicito registado no SUMMARY.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Internet publica (nao autenticada) -> `POST /api/v1/setup/initialize` | Fronteira pre-existente, protegida pelo gate singleton `SystemSetting.initialized`. Esta fase NAO lhe toca e prova por teste que continua fechada |
| Sessao autenticada de um tenant normal -> `POST /api/v1/platform/tenants` | Fronteira nova de altissimo privilegio: criacao arbitraria de tenants. So `PLATAFORMA_ADMIN` a atravessa |
| Camada de method security -> resposta HTTP | Fronteira nova de traducao: a recusa lancada pelo `@PreAuthorize` tem de chegar ao cliente como `403`, sem revelar a expressao de autorizacao avaliada |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-119-04 | Elevation of Privilege | `POST /api/v1/platform/tenants` alcancavel por um papel errado (`ADMIN` de escritorio, `ADVOGADO`, etc.) | mitigate | `@PreAuthorize` com a expressao `hasRole('PLATAFORMA_ADMIN')` ao **nivel da classe** (cobre qualquer handler futuro da Phase 120 automaticamente). Provado comportamentalmente pelo Caso 5 — proxy AOP real com `AuthorizationManagerBeforeMethodInterceptor`, `ROLE_ADMIN` no contexto, `AccessDeniedException` lancada **antes** de `provisionTenant` correr (`never()).provisionTenant`). O Caso 6 impede que um gate partido que recusasse toda a gente passasse por bom; o Caso 7 fixa a string exata da expressao contra gralhas. **Depende do Plan 03:** este gate so vale enquanto um `ADMIN` de escritorio nao conseguir atribuir-se o papel (T-119-01) |
| T-119-05 | Elevation of Privilege | `POST /api/v1/platform/tenants` alcancavel sem autenticacao nenhuma | mitigate | `SecurityConfig.authorizeHttpRequests` termina em `.anyRequest().authenticated()`; `/api/v1/platform` **nao** e acrescentado a allowlist `permitAll()` (que so tem login/refresh/logout/setup-status/setup-initialize/public-branding). Gate de origem na Task 2 reprova qualquer ocorrencia de `/api/v1/platform` em `SecurityConfig.java`, e o ficheiro fica fora do diff da fase |
| T-119-08 | Tampering | Bypass ou afrouxamento do gate singleton de `/setup/initialize` | mitigate | O caminho novo e totalmente distinto: controller proprio, metodo de servico proprio, gate de seguranca proprio. `PlatformAdminController` nunca chama `initializeSystem` nem `isInitialized` (gate de origem reprova ambas as strings; Caso 4 prova `never()` nas duas). `SetupController.java` fica fora do diff e o seu comportamento singleton e re-provado pelos tres casos da Task 3 |
| T-119-21 | Information Disclosure | Corpo do `403` a revelar a expressao `@PreAuthorize` avaliada ou detalhe interno do Spring Security | mitigate | O handler novo devolve uma mensagem generica em portugues e **nao** ecoa `ex.getMessage()`; o detalhe fica so no `logger.warn` do lado do servidor. Gate de origem extrai o corpo do handler e reprova `ex.getMessage()`. `logger.warn` em vez de `logger.error` para nao poluir o canal de erros com recusas esperadas |
| T-119-17 | Information Disclosure | Serializacao da entidade `Tenant` (ou de `User`/`Role`) no corpo do 201 | mitigate | Copia explicita getter-para-setter para `TenantProvisionResponse` (`id` + `nome`), a disciplina "nunca entidade crua" documentada em `PublicController`. Caso 1 assere `assertInstanceOf(TenantProvisionResponse.class, ...)` e `assertFalse(body instanceof Tenant)`; gate de origem reprova `body(tenant)`. Em particular, o email e o hash de password do `ADMIN` inicial criado nunca entram na resposta |
| T-119-22 | Elevation of Privilege | Ampliacao silenciosa da superficie de plataforma (endpoints extra nao pedidos por esta fase) | mitigate | Apenas um `@PostMapping`; gate de origem reprova `@GetMapping`/`@PutMapping`/`@DeleteMapping`/`@PatchMapping` no ficheiro. A listagem de tenants (PROV-03) e explicitamente da Phase 120 e nao e antecipada aqui — cada endpoint novo e superficie de ataque nova a auditar na Phase 123 (ISOL-04) |
| T-119-23 | Tampering | Efeito colateral do `403` global sobre endpoints ja existentes | accept (efeito benefico, deliberado) | O handler e global e muda **todas** as recusas de `@PreAuthorize` de `500` para `403` — o comportamento que STATE.md ja documenta como o correto (`GET /api/v1/admin/users` devolve hoje `500` indevidamente). Analise de impacto no cliente feita no planeamento: `web/src/lib/api.ts` linha 43 suprime o toast para `401`/`403` e apenas lanca, sem redirect automatico — logo o efeito visivel e a remocao de um toast espurio "Erro 500: Access Denied", sem alteracao de navegacao. Nenhum teste existente assere `500` numa recusa de autorizacao (a suite completa e re-corrida na Task 3 para o confirmar) |
| T-119-SC | Tampering | Instalacoes npm/pip/cargo/Maven | n/a | Zero dependencias novas. Todas as classes usadas (`AccessDeniedException`, `AuthorizationManagerBeforeMethodInterceptor`, `ProxyFactory`) ja estao no classpath via `spring-boot-starter-security`/`spring-boot-starter-test`/`spring-aop`, e a presenca offline de `spring-security-core` 6.4.2 e `spring-security-test` 6.4.2 foi confirmada no planeamento. `backend/pom.xml` fica fora do diff (gate nas tres tasks). O Package Legitimacy Gate nao se aplica |
</threat_model>

<verification>
1. `cd backend && mvn -q -DskipTests compile` — exit 0.
2. `cd backend && mvn test -Dtest=PlatformAdminControllerTest` — 8 testes, 0 falhas (incluindo os 4 do gate de autorizacao com proxy real).
3. `cd backend && mvn test -Dtest=SetupControllerSingletonRegressaoTest` — 3 testes, 0 falhas.
4. `cd backend && mvn test` — suite unitaria completa verde, incluindo as suites das Phases 117 e 118.
5. `cd backend && mvn spotbugs:check` — sem achados novos.
6. Gates de origem: `hasRole('PLATAFORMA_ADMIN')` presente 1 vez ao nivel da classe; `isInitialized`/`initializeSystem`/`SecurityContextHolder`/`UserPrincipal`/`body(tenant)` ausentes do controller; `/api/v1/platform` ausente de `SecurityConfig.java`; `ex.getMessage()` ausente do corpo do handler de `403`; apenas um mapeamento HTTP no controller novo.
7. `SetupController.java`, `SecurityConfig.java` e `pom.xml` fora do diff.
</verification>

<success_criteria>
- `POST /api/v1/platform/tenants` responde `201` com `{id, nome}` a um chamador com `ROLE_PLATAFORMA_ADMIN`.
- O mesmo pedido feito por um `ADMIN` de escritorio e recusado pela camada de method security e chega ao cliente como `403` (nunca `500`, nunca `201`).
- `POST /api/v1/setup/initialize` continua a devolver erro numa segunda chamada, com `SetupController.java` byte-a-byte inalterado.
- Nenhuma entidade JPA e serializada na resposta do endpoint novo.
- `SecurityConfig.java` nao foi tocado e `/api/v1/platform` nao e publico.
</success_criteria>

<output>
Create `.planning/phases/LEXCV-119-backend-papel-de-administrador-de-plataforma-e-provisionamen/119-04-SUMMARY.md` when done
</output>
