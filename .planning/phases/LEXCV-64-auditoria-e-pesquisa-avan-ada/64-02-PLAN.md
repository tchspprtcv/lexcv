---
phase: 64-auditoria-e-pesquisa-avan-ada
plan: 02
type: execute
wave: 2
depends_on: [64-01]
files_modified:
  - backend/src/main/java/com/lexcv/repositories/ParecerSolicitacaoRepository.java
  - backend/src/main/java/com/lexcv/controllers/ParecerController.java
autonomous: true
requirements: [PARS-01, PARS-02]
must_haves:
  truths:
    - "Utilizador com pareceres:view pode pesquisar pareceres por texto livre no conteúdo da versão mais recente"
    - "Pesquisa combina texto com filtros clienteId, advogadoId, status, dataInicio, dataFim simultaneamente"
    - "A pesquisa é tenant-scoped — só retorna solicitações do tenant do utilizador"
    - "Texto só procura na versão mais recente de cada solicitação (sem duplicados)"
    - "Resultado vazio retorna 200 OK com lista vazia, não 404"
  artifacts:
    - path: "backend/src/main/java/com/lexcv/repositories/ParecerSolicitacaoRepository.java"
      provides: "Método pesquisar() com @Query optional-param + ILIKE + subquery MAX(numeroVersao)"
      contains: "pesquisar"
    - path: "backend/src/main/java/com/lexcv/controllers/ParecerController.java"
      provides: "Endpoint GET /api/v1/pareceres/pesquisa"
      contains: "pesquisa"
  key_links:
    - from: "ParecerController.java /pesquisa"
      to: "ParecerSolicitacaoRepository.pesquisar"
      via: "chamada tenant-scoped com params opcionais"
      pattern: "\\.pesquisar\\("
    - from: "ParecerSolicitacaoRepository.pesquisar"
      to: "ParecerVersao.conteudo"
      via: "JOIN + ILIKE na versão mais recente"
      pattern: "conteudo"
---

<objective>
Adicionar pesquisa textual avançada de pareceres: um método de query em ParecerSolicitacaoRepository (texto livre via ILIKE no conteúdo da versão mais recente + filtros opcionais combináveis) e um endpoint GET /api/v1/pareceres/pesquisa que o expõe, tenant-scoped, com scope pareceres:view.

Purpose: Satisfaz PARS-01 (pesquisa por texto livre) e PARS-02 (texto combinado com filtros cliente/advogado/status/data).
Output: novo método de repositório + novo endpoint.
</objective>

<execution_context>
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/LEXCV-64-auditoria-e-pesquisa-avan-ada/64-CONTEXT.md
@.planning/phases/LEXCV-64-auditoria-e-pesquisa-avan-ada/64-PATTERNS.md

<interfaces>
From ParecerSolicitacaoRepository (existing): findByTenantId(UUID), extends JpaRepository<ParecerSolicitacao, UUID>.
Entity ParecerSolicitacao fields: id, tenantId, clienteId, advogadoId, status (String), createdAt.

From ParecerVersaoRepository lines 13-14 (max-version aggregate pattern to reuse):
  @Query("SELECT MAX(v.numeroVersao) FROM ParecerVersao v WHERE v.solicitacaoId = :solicitacaoId")
  Optional<Integer> findMaxNumeroVersaoBySolicitacaoId(@Param("solicitacaoId") UUID solicitacaoId);
Entity ParecerVersao fields: id, solicitacaoId, numeroVersao (Integer), conteudo (String).

From UserRepository lines 16-20 (only existing JOIN @Query — establishes @Query + @Param + JPQL JOIN imports):
  org.springframework.data.jpa.repository.Query, org.springframework.data.repository.query.Param

Endpoint shape analog — listSolicitacoes (ParecerController lines 136-150): @PreAuthorize("hasAuthority('pareceres:view')"),
  @RequestParam(required = false) params, getTenantId(), ResponseEntity.ok(result).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Método pesquisar() em ParecerSolicitacaoRepository</name>
  <files>backend/src/main/java/com/lexcv/repositories/ParecerSolicitacaoRepository.java</files>
  <read_first>
    - backend/src/main/java/com/lexcv/repositories/ParecerSolicitacaoRepository.java (ficheiro a modificar — imports e métodos existentes)
    - backend/src/main/java/com/lexcv/repositories/ParecerVersaoRepository.java (linhas 13-14: padrão MAX(numeroVersao))
    - backend/src/main/java/com/lexcv/repositories/UserRepository.java (linhas 16-20: única @Query com JOIN existente — sintaxe e imports)
    - backend/src/main/java/com/lexcv/models/ParecerSolicitacao.java (nomes exatos dos campos: clienteId, advogadoId, status, createdAt, tenantId; tipo de createdAt)
    - backend/src/main/java/com/lexcv/models/ParecerVersao.java (campos solicitacaoId, numeroVersao, conteudo)
    - .planning/phases/LEXCV-64-auditoria-e-pesquisa-avan-ada/64-PATTERNS.md (secção "new search query method" — caveats de ILIKE nativo e subquery da versão mais recente)
  </read_first>
  <behavior>
    - pesquisar(tenantId, texto, clienteId, advogadoId, status, dataInicio, dataFim) retorna List<ParecerSolicitacao>
    - todos os params exceto tenantId são opcionais: quando null, o filtro correspondente é ignorado via idioma (:param IS NULL OR campo = :param)
    - texto: quando não-null, filtra por match ILIKE '%texto%' no conteudo APENAS da versão com MAX(numeroVersao) de cada solicitação
    - dataInicio/dataFim filtram por createdAt da solicitação (>= dataInicio, <= dataFim)
    - resultado sempre tenant-scoped (tenantId obrigatório no WHERE)
    - sem duplicados (uma linha por solicitação)
  </behavior>
  <action>
    Adicionar imports `org.springframework.data.jpa.repository.Query`, `org.springframework.data.repository.query.Param`, e `java.time.LocalDateTime` (ou o tipo exato de createdAt confirmado em ParecerSolicitacao.java — usar esse tipo).

    Criar método `pesquisar(...)` anotado com `@Query`. Como CONTEXT.md exige ILIKE nativo do Postgres (não portável em JPQL), usar `@Query(value = "...", nativeQuery = true)` com SQL nativo sobre as tabelas físicas `t_parecer_solicitacao` (alias s) e `t_parecer_versao` (alias v). Confirmar os nomes reais das tabelas/colunas via @Table/@Column nas entidades antes de escrever o SQL nativo (colunas provavelmente snake_case: tenant_id, cliente_id, advogado_id, created_at, solicitacao_id, numero_versao, conteudo).

    A cláusula de texto deve restringir o JOIN à versão mais recente: JOIN a t_parecer_versao onde v.numero_versao = (SELECT MAX(v2.numero_versao) FROM t_parecer_versao v2 WHERE v2.solicitacao_id = s.id), replicando o padrão de agregado MAX de findMaxNumeroVersaoBySolicitacaoId mas correlacionado. Isto garante uma linha por solicitação e evita duplicados.

    Aplicar o idioma de parâmetros opcionais a cada filtro: `(:clienteId IS NULL OR s.cliente_id = :clienteId)`, `(:advogadoId IS NULL OR s.advogado_id = :advogadoId)`, `(:status IS NULL OR s.status = :status)`, `(:dataInicio IS NULL OR s.created_at >= :dataInicio)`, `(:dataFim IS NULL OR s.created_at <= :dataFim)`, `(:texto IS NULL OR v.conteudo ILIKE '%' || :texto || '%')`. WHERE começa sempre com `s.tenant_id = :tenantId`.

    Como é nativeQuery, o tipo de retorno mapeia diretamente para a entidade ParecerSolicitacao (SELECT s.*). Anotar todos os params com @Param. Confirmar que o tipo Java de dataInicio/dataFim casa com o tipo de created_at (LocalDateTime vs LocalDate) — se created_at for timestamp, usar LocalDateTime.
  </action>
  <verify>
    <automated>cd backend && mvn -q -DskipTests compile 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "pesquisar" backend/src/main/java/com/lexcv/repositories/ParecerSolicitacaoRepository.java` >= 1
    - `grep -c "nativeQuery = true" backend/src/main/java/com/lexcv/repositories/ParecerSolicitacaoRepository.java` == 1
    - Query contém `ILIKE`, `MAX(` numero_versao subquery, e `:tenantId` no WHERE
    - Query contém os 6 filtros opcionais no idioma `(:param IS NULL OR ...)` (grep por "IS NULL OR" retorna >= 6)
    - `mvn -DskipTests compile` termina com BUILD SUCCESS
  </acceptance_criteria>
  <done>Método pesquisar() nativo compila, restringe à versão mais recente, tenant-scoped, com 6 filtros opcionais.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Endpoint GET /api/v1/pareceres/pesquisa no ParecerController</name>
  <files>backend/src/main/java/com/lexcv/controllers/ParecerController.java</files>
  <read_first>
    - backend/src/main/java/com/lexcv/controllers/ParecerController.java (ficheiro a modificar — listSolicitacoes linhas 136-150 como shape analog, @RequestMapping base path, getTenantId linhas 44-48)
    - backend/src/main/java/com/lexcv/repositories/ParecerSolicitacaoRepository.java (assinatura de pesquisar() criada na Task 1)
    - .planning/phases/LEXCV-64-auditoria-e-pesquisa-avan-ada/64-PATTERNS.md (secção "new /pesquisa endpoint" — nota sobre path absoluto que ignora o @RequestMapping base)
    - .planning/phases/LEXCV-64-auditoria-e-pesquisa-avan-ada/64-CONTEXT.md (decisões: scope pareceres:view, dataInicio/dataFim por createdAt, resultado vazio 200)
  </read_first>
  <behavior>
    - GET /api/v1/pareceres/pesquisa com params opcionais texto, clienteId, advogadoId, status, dataInicio, dataFim
    - requer authority pareceres:view
    - delega a parecerSolicitacaoRepository.pesquisar(tenantId, ...) — NÃO stream-filter
    - tenantId de getTenantId()
    - retorna ResponseEntity.ok(lista) mesmo quando vazia (sem 404)
  </behavior>
  <action>
    Adicionar um método handler anotado `@PreAuthorize("hasAuthority('pareceres:view')")` e `@GetMapping("/api/v1/pareceres/pesquisa")`. Como o base path da classe é `@RequestMapping("/api/v1/pareceres/solicitacoes")`, usar o path ABSOLUTO no @GetMapping para bypass do prefixo da classe (Spring permite paths absolutos em @GetMapping).

    Parâmetros: `@RequestParam(required = false) String texto`, `@RequestParam(required = false) UUID clienteId`, `@RequestParam(required = false) UUID advogadoId`, `@RequestParam(required = false) String status`, `@RequestParam(required = false) LocalDateTime dataInicio`, `@RequestParam(required = false) LocalDateTime dataFim` (usar o mesmo tipo de data escolhido na Task 1; adicionar `@DateTimeFormat(iso = ...)` se necessário para binding de query string).

    No corpo: obter `UUID tenantId = getTenantId();` e retornar `ResponseEntity.ok(parecerSolicitacaoRepository.pesquisar(tenantId, texto, clienteId, advogadoId, status, dataInicio, dataFim))`. Não adicionar branch de 404 — lista vazia retorna 200 conforme convenção de listSolicitacoes.

    Garantir imports necessários (LocalDateTime, @DateTimeFormat se usado, @GetMapping, @RequestParam já presentes). Não redeclarar campos já injetados.
  </action>
  <verify>
    <automated>cd backend && mvn -q -DskipTests compile 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "/api/v1/pareceres/pesquisa" backend/src/main/java/com/lexcv/controllers/ParecerController.java` >= 1
    - `grep -c "hasAuthority('pareceres:view')" backend/src/main/java/com/lexcv/controllers/ParecerController.java` aumentou em 1 face ao estado anterior (novo endpoint tem o scope)
    - Handler chama `parecerSolicitacaoRepository.pesquisar(` com tenantId como primeiro argumento
    - Sem branch de status 404 no handler; retorna ResponseEntity.ok
    - `mvn -DskipTests compile` termina com BUILD SUCCESS
  </acceptance_criteria>
  <done>Endpoint GET /pesquisa compila, gated por pareceres:view, delega ao repositório tenant-scoped, retorna 200 com lista (vazia ou não).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→/pesquisa | Query params não confiáveis (texto, IDs, datas) atravessam para uma query SQL nativa |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-64-04 | Tampering (SQL injection) | nativeQuery pesquisar() | mitigate | Todos os valores passam por bind params @Param (`:texto`, `:clienteId`...); nunca concatenar strings no SQL; ILIKE usa `'%' \|\| :texto \|\| '%'` com bind, não interpolação |
| T-64-05 | Information Disclosure | tenant isolation na pesquisa | mitigate | WHERE começa sempre por `s.tenant_id = :tenantId` de getTenantId(); nenhum caminho retorna solicitações de outro tenant |
| T-64-06 | Elevation of Privilege | acesso ao endpoint | mitigate | @PreAuthorize("hasAuthority('pareceres:view')") no handler, consistente com listSolicitacoes |
| T-64-07 | Denial of Service | pesquisa sem índice tsvector | accept | Volume baixo nesta fase (decisão CONTEXT.md); índice trigram/tsvector é melhoria futura documentada |
| T-64-SC | Tampering | npm/pip/cargo installs | accept | Nenhuma instalação de package nova neste plano |
</threat_model>

<verification>
- Backend compila (`mvn -DskipTests compile`)
- Método nativeQuery pesquisar() presente com 6 filtros opcionais + subquery MAX(numero_versao)
- Endpoint /api/v1/pareceres/pesquisa gated por pareceres:view, delega ao repositório
- Query usa exclusivamente bind params (sem concatenação de strings)
</verification>

<success_criteria>
Utilizador pode pesquisar pareceres por texto livre no conteúdo da versão mais recente, combinado com filtros de cliente, advogado, status e data, tenant-scoped, retornando 200 OK (lista possivelmente vazia).
</success_criteria>

<output>
Create `.planning/phases/LEXCV-64-auditoria-e-pesquisa-avan-ada/64-02-SUMMARY.md` when done
</output>
