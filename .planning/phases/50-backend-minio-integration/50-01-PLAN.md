---
phase: "50"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/pom.xml
  - backend/src/main/resources/application.yml
  - backend/src/main/java/com/lexcv/config/MinioProperties.java
  - backend/src/main/java/com/lexcv/config/MinioConfig.java
  - backend/src/main/java/com/lexcv/services/StorageService.java
  - backend/src/main/java/com/lexcv/exceptions/StorageUnavailableException.java
  - backend/.env.example
autonomous: true
requirements:
  - MIN-01
  - MIN-02
  - MIN-03
  - MIN-04

must_haves:
  truths:
    - "mvn compile -pl backend exits 0 with the new dependencies and classes"
    - "Spring context loads and MinioProperties binds MINIO_* env vars without errors"
    - "StorageService exposes upload(), presignedDownloadUrl(), delete() methods"
    - "Bucket is auto-created on startup if absent; warning is logged (not thrown) if MinIO unreachable"
    - "Object keys are always constructed as {tenantId}/{documentoId}/{filename}"
  artifacts:
    - path: backend/src/main/java/com/lexcv/config/MinioProperties.java
      provides: "@ConfigurationProperties(prefix = 'minio') class with endpoint, accessKey, secretKey, bucketName, presignedUrlExpiry fields"
    - path: backend/src/main/java/com/lexcv/config/MinioConfig.java
      provides: "@Bean S3Client and @Bean S3Presigner with pathStyleAccessEnabled(true)"
    - path: backend/src/main/java/com/lexcv/services/StorageService.java
      provides: "@Service with upload/presignedDownloadUrl/delete + ApplicationRunner bucket check"
    - path: backend/src/main/java/com/lexcv/exceptions/StorageUnavailableException.java
      provides: "RuntimeException wrapping SdkException for 503 propagation"
  key_links:
    - from: MinioConfig.java
      to: MinioProperties.java
      via: "@EnableConfigurationProperties(MinioProperties.class)"
    - from: StorageService.java
      to: S3Client / S3Presigner beans
      via: "@RequiredArgsConstructor injection"
    - from: StorageService.upload()
      to: objectKey
      via: "tenantId.toString() + '/' + documentoId.toString() + '/' + filename"
---

<objective>
Add the AWS SDK v2 S3 dependencies and create the MinIO infrastructure layer:
MinioProperties, MinioConfig (S3Client + S3Presigner beans), StorageService (upload / presignedDownloadUrl / delete), StorageUnavailableException, and the startup bucket-check ApplicationRunner.

Purpose: Establish the storage abstraction before ResourceController touches it. Plan 50-02 will consume StorageService; it cannot proceed until this plan is complete.

Output: Compilable Java classes; no endpoint changes yet.
</objective>

<execution_context>
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/50-backend-minio-integration/50-CONTEXT.md
@.planning/phases/50-backend-minio-integration/50-RESEARCH.md

<interfaces>
<!-- Contracts the executor needs to produce. No codebase exploration required. -->

MinioProperties (to create — backend/src/main/java/com/lexcv/config/MinioProperties.java):
  Annotation: @ConfigurationProperties(prefix = "minio") @Validated @Data
  Fields:
    @NotBlank String endpoint
    @NotBlank String accessKey
    @NotBlank String secretKey
    @NotBlank String bucketName
    long presignedUrlExpiry = 3600L

MinioConfig (to create — backend/src/main/java/com/lexcv/config/MinioConfig.java):
  @Configuration @EnableConfigurationProperties(MinioProperties.class)
  @Bean S3Client s3Client(MinioProperties props)
    — endpointOverride(URI.create(props.getEndpoint()))
    — region(Region.of("us-east-1"))
    — pathStyleAccessEnabled(true) [REQUIRED]
    — chunkedEncodingEnabled(false) [REQUIRED for MinIO upload]
    — httpClientBuilder(UrlConnectionHttpClient.builder())
  @Bean S3Presigner s3Presigner(MinioProperties props)
    — endpointOverride(URI.create(props.getEndpoint()))
    — region(Region.of("us-east-1"))
    — pathStyleAccessEnabled(true) [REQUIRED]
    — checksumValidationEnabled(false) [REQUIRED for browser-usable presigned URLs]

StorageService public API (to create — backend/src/main/java/com/lexcv/services/StorageService.java):
  String upload(UUID tenantId, UUID documentoId, String filename,
                InputStream inputStream, String contentType, long size)
    — objectKey = tenantId + "/" + documentoId + "/" + filename
    — s3Client.putObject(PutObjectRequest, RequestBody.fromInputStream(inputStream, size))
    — throws StorageUnavailableException on SdkException
    — returns objectKey

  String presignedDownloadUrl(String objectKey)
    — GetObjectPresignRequest with signatureDuration(Duration.ofSeconds(props.getPresignedUrlExpiry()))
    — returns s3Presigner.presignGetObject(req).url().toString()
    — throws StorageUnavailableException on SdkException

  void delete(String objectKey)
    — s3Client.deleteObject(DeleteObjectRequest)
    — throws StorageUnavailableException on SdkException

  implements ApplicationRunner — run() does headBucket; on NoSuchBucketException calls createBucket;
    on SdkException logs warning (log.warn) and does NOT throw

StorageUnavailableException (to create — backend/src/main/java/com/lexcv/exceptions/StorageUnavailableException.java):
  public class StorageUnavailableException extends RuntimeException
    constructor(String message, Throwable cause)

Existing pom.xml: no <dependencyManagement> block currently — must be added.
Existing application.yml ends at line 38 with JWT expiration; add minio: block after it.
backend/.env.example: read first to append MINIO_* vars without clobbering existing content.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add AWS SDK v2 dependencies and MinIO config classes</name>
  <files>
    backend/pom.xml,
    backend/src/main/resources/application.yml,
    backend/.env.example,
    backend/src/main/java/com/lexcv/config/MinioProperties.java,
    backend/src/main/java/com/lexcv/config/MinioConfig.java
  </files>
  <read_first>
    - backend/pom.xml (full file — determine where to insert dependencyManagement and dependencies)
    - backend/src/main/resources/application.yml (append minio block)
    - backend/.env.example (append MINIO_* vars without removing existing content)
  </read_first>
  <behavior>
    - After edit: mvn compile -pl backend resolves software.amazon.awssdk:s3 and url-connection-client
    - MinioProperties binds prefix "minio" and validates @NotBlank fields at startup
    - MinioConfig produces S3Client with pathStyleAccessEnabled(true) and chunkedEncodingEnabled(false)
    - MinioConfig produces S3Presigner with pathStyleAccessEnabled(true) and checksumValidationEnabled(false)
    - application.yml minio block uses ${MINIO_ENDPOINT}, ${MINIO_ACCESS_KEY}, ${MINIO_SECRET_KEY}, ${MINIO_BUCKET_NAME}, ${MINIO_PRESIGNED_EXPIRY:3600}
  </behavior>
  <action>
    In pom.xml: add a &lt;dependencyManagement&gt;&lt;dependencies&gt; block (before &lt;dependencies&gt;) importing the BOM:
      groupId=software.amazon.awssdk, artifactId=bom, version=2.46.14, type=pom, scope=import.
    In pom.xml &lt;dependencies&gt;: add software.amazon.awssdk:s3 (no version — BOM manages it),
      software.amazon.awssdk:url-connection-client (no version), and
      spring-boot-configuration-processor (optional=true, managed by Spring Boot parent).
    In application.yml: append a top-level "minio:" block with keys endpoint, access-key, secret-key,
      bucket-name, presigned-url-expiry using ${VAR} placeholders (presigned-url-expiry uses default :3600).
    In backend/.env.example: append four lines:
      MINIO_ENDPOINT=http://localhost:9000
      MINIO_ACCESS_KEY=minioadmin
      MINIO_SECRET_KEY=minioadmin
      MINIO_BUCKET_NAME=lexcv-documentos
    Create MinioProperties.java in com.lexcv.config package: @ConfigurationProperties(prefix="minio"),
      @Validated, @Data (Lombok). Fields: endpoint, accessKey, secretKey, bucketName (all @NotBlank String),
      presignedUrlExpiry (long, default 3600L). No-args constructor required for binding — @Data provides it.
    Create MinioConfig.java in com.lexcv.config package: @Configuration,
      @EnableConfigurationProperties(MinioProperties.class). Two @Bean methods as described in the interfaces
      block. Import URI, Region, AwsBasicCredentials, StaticCredentialsProvider, S3Client, S3Configuration,
      UrlConnectionHttpClient, S3Presigner from software.amazon.awssdk packages.
      Do NOT set region as a configurable env var — hardcode Region.of("us-east-1") as a placeholder
      (MinIO ignores region; this satisfies the SDK requirement).
  </action>
  <verify>
    <automated>cd backend && mvn compile -q 2>&amp;1 | tail -5 &amp;&amp; echo "COMPILE_OK"</automated>
  </verify>
  <acceptance_criteria>
    - pom.xml contains &lt;artifactId&gt;bom&lt;/artifactId&gt; inside a dependencyManagement block
    - pom.xml contains &lt;artifactId&gt;s3&lt;/artifactId&gt; and &lt;artifactId&gt;url-connection-client&lt;/artifactId&gt; in dependencies with no version tag
    - application.yml contains "minio:" key
    - backend/.env.example contains MINIO_ENDPOINT, MINIO_ACCESS_KEY, MINIO_SECRET_KEY, MINIO_BUCKET_NAME
    - MinioProperties.java exists at backend/src/main/java/com/lexcv/config/MinioProperties.java
    - MinioConfig.java exists at backend/src/main/java/com/lexcv/config/MinioConfig.java and contains pathStyleAccessEnabled(true) twice (once per bean)
    - mvn compile exits 0
  </acceptance_criteria>
  <done>pom.xml, application.yml, .env.example, MinioProperties, and MinioConfig all created and compile clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create StorageUnavailableException and StorageService</name>
  <files>
    backend/src/main/java/com/lexcv/exceptions/StorageUnavailableException.java,
    backend/src/main/java/com/lexcv/services/StorageService.java
  </files>
  <read_first>
    - backend/src/main/java/com/lexcv/config/MinioConfig.java (just created — confirm bean names)
    - backend/src/main/java/com/lexcv/config/MinioProperties.java (just created — confirm field getters)
  </read_first>
  <behavior>
    - StorageUnavailableException extends RuntimeException with (String message, Throwable cause) constructor
    - StorageService.upload() constructs objectKey as tenantId.toString()+"/"+documentoId.toString()+"/"+filename, calls s3Client.putObject, returns objectKey; throws StorageUnavailableException on SdkException
    - StorageService.presignedDownloadUrl() builds GetObjectPresignRequest with signatureDuration(Duration.ofSeconds(props.getPresignedUrlExpiry())), returns URL string; throws StorageUnavailableException on SdkException
    - StorageService.delete() calls s3Client.deleteObject; throws StorageUnavailableException on SdkException
    - StorageService.run() calls s3Client.headBucket; on NoSuchBucketException calls s3Client.createBucket; on SdkException calls log.warn(...) and returns (does NOT throw)
    - mvn compile exits 0 after both files created
  </behavior>
  <action>
    Create StorageUnavailableException.java in com.lexcv.exceptions package:
      public class StorageUnavailableException extends RuntimeException with one constructor(String message, Throwable cause) calling super(message, cause).
    Create StorageService.java in com.lexcv.services package: @Service, @RequiredArgsConstructor,
      @Slf4j (Lombok), implements ApplicationRunner.
    Inject via constructor: S3Client s3Client, S3Presigner s3Presigner, MinioProperties props.
    Implement upload(UUID tenantId, UUID documentoId, String filename, InputStream inputStream,
      String contentType, long size) — returns String objectKey.
      — Sanitise filename: strip any "/" or "\" characters from filename before inserting into key
        (use filename.replaceAll("[/\\\\]", "_")) to prevent path traversal (per security domain notes).
      — objectKey = tenantId.toString() + "/" + documentoId.toString() + "/" + sanitisedFilename
      — PutObjectRequest with bucket(props.getBucketName()), key(objectKey), contentType(contentType), contentLength(size)
      — RequestBody.fromInputStream(inputStream, size) — do NOT call file.getBytes()
      — Wrap the s3Client.putObject call in try/catch(SdkException e) throwing new StorageUnavailableException("Storage service unavailable", e)
      — Return objectKey
    Implement presignedDownloadUrl(String objectKey) — returns String.
      — GetObjectPresignRequest: signatureDuration(Duration.ofSeconds(props.getPresignedUrlExpiry())), GetObjectRequest with bucket and key
      — Catch SdkException, throw StorageUnavailableException
      — Return s3Presigner.presignGetObject(req).url().toString()
    Implement delete(String objectKey) — void.
      — DeleteObjectRequest with bucket and key
      — Catch SdkException, throw StorageUnavailableException
    Implement run(ApplicationArguments args) — void.
      — HeadBucketRequest.builder().bucket(props.getBucketName()).build()
      — Catch NoSuchBucketException: call s3Client.createBucket(CreateBucketRequest.builder().bucket(props.getBucketName()).build())
      — Catch SdkException (broader): log.warn("MinIO unavailable at startup: {}", e.getMessage()) and return
    Required imports from software.amazon.awssdk: s3.S3Client, s3.presigner.S3Presigner,
      s3.model.PutObjectRequest, s3.model.GetObjectRequest, s3.model.DeleteObjectRequest,
      s3.model.HeadBucketRequest, s3.model.CreateBucketRequest, s3.model.NoSuchBucketException,
      s3.presigner.model.GetObjectPresignRequest, core.sync.SdkClient (for RequestBody),
      core.exception.SdkException, awscore.sync.RequestBody.
      java.time.Duration, java.io.InputStream, java.util.UUID.
  </action>
  <verify>
    <automated>cd backend && mvn compile -q 2>&amp;1 | tail -5 &amp;&amp; echo "COMPILE_OK"</automated>
  </verify>
  <acceptance_criteria>
    - StorageUnavailableException.java exists in com/lexcv/exceptions/ and extends RuntimeException
    - StorageService.java exists in com/lexcv/services/ and implements ApplicationRunner
    - StorageService contains the string "tenantId.toString()" — confirming object key construction
    - StorageService contains "log.warn" inside the run() catch block
    - StorageService does NOT contain "getBytes()" — must use getInputStream pattern
    - mvn compile exits 0
  </acceptance_criteria>
  <done>StorageUnavailableException and StorageService created; mvn compile passes; infrastructure layer is fully wired and ready for Plan 50-02 to consume.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MinIO credentials in env | MINIO_ACCESS_KEY / MINIO_SECRET_KEY loaded from .env — never hardcoded in source |
| Object key construction | Filename from user input inserted into MinIO path — must be sanitised |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-50-01 | Tampering | StorageService.upload() filename | mitigate | Strip "/" and "\" from filename with replaceAll("[/\\\\]", "_") before inserting into objectKey |
| T-50-02 | Information Disclosure | MinioProperties fields | mitigate | Values come from env vars; never logged; @NotBlank ensures missing vars fail-fast at startup with clear error |
| T-50-03 | Denial of Service | startup bucket check | accept | SdkException caught and logged as warning — app starts regardless; MinIO may be slow in Docker Compose |
| T-50-SC | Tampering | Maven dependency install | accept | Both packages are official software.amazon.awssdk modules from aws/aws-sdk-java-v2 (Maven Central, verified in Package Legitimacy Audit in RESEARCH.md) |
</threat_model>

<verification>
After both tasks complete:
- mvn compile -pl backend exits 0
- MinioConfig.java contains pathStyleAccessEnabled(true) on both S3Client and S3Presigner beans
- MinioConfig.java contains chunkedEncodingEnabled(false) on the S3Client bean
- MinioConfig.java contains checksumValidationEnabled(false) on the S3Presigner bean
- StorageService.java contains "implements ApplicationRunner"
- StorageService.java does NOT contain "getBytes()"
- backend/.env.example contains all four MINIO_* variable stubs
</verification>

<success_criteria>
- mvn compile exits 0 with all new classes
- MinioProperties, MinioConfig, StorageService, StorageUnavailableException exist at the correct package paths
- All three MinIO compatibility flags are present in MinioConfig
- StorageService method signatures match those defined in the interfaces block exactly
- Filename sanitisation (strip path separators) is present in upload()
- Startup bucket check logs a warning on SdkException instead of throwing
</success_criteria>

<output>
Create `.planning/phases/50-backend-minio-integration/50-01-SUMMARY.md` when done.
</output>
