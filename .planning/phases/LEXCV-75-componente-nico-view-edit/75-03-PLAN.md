---
phase: 75-componente-nico-view-edit
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/app/(dashboard)/clientes/page.tsx
autonomous: true
requirements: [CLI-14]
must_haves:
  truths:
    - "The Editar pencil icon in the clientes list (both mobile card view and desktop row) links to /clientes/[id], not the removed /clientes/[id]/editar route"
  artifacts:
    - path: "web/src/app/(dashboard)/clientes/page.tsx"
      provides: "List page whose Editar pencil links point to the unified detail page"
  key_links:
    - from: "web/src/app/(dashboard)/clientes/page.tsx"
      to: "/clientes/[id]"
      via: "Link href on the two Editar pencil affordances"
      pattern: "clientes/\\$\\{encodeURIComponent"
---

<objective>
Repoint the two `Editar` pencil-icon `<Link href>` targets in the clientes list page from the removed `/clientes/[id]/editar` route to `/clientes/[id]`. These are the only two remaining internal links to the deleted route; they are in a different file from the merge and are easy to miss.

Purpose: Completes CLI-14 — with the `/editar` route deleted (plan 75-01), any remaining link to it would 404. Both list affordances must reach the unified detail page instead, where the user clicks the in-page Editar toggle.

Output: Both pencil-icon links in `clientes/page.tsx` point to `/clientes/[id]`.
</objective>

<execution_context>
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/LEXCV-75-componente-nico-view-edit/75-PATTERNS.md
@.planning/phases/LEXCV-75-componente-nico-view-edit/75-UI-SPEC.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Repoint the two Editar pencil links to /clientes/[id]</name>
  <read_first>
    - web/src/app/(dashboard)/clientes/page.tsx (the list page; the two link sites are around line 450 in the mobile/card list and line 597 in the desktop row)
    - .planning/phases/LEXCV-75-componente-nico-view-edit/75-PATTERNS.md ("Related Files Requiring Link Updates" table — both exact target lines)
    - .planning/phases/LEXCV-75-componente-nico-view-edit/75-UI-SPEC.md (Interaction Contract section 6 — links must point to /clientes/[id], not /editar)
  </read_first>
  <action>
    In web/src/app/(dashboard)/clientes/page.tsx, change the two Editar pencil-icon Link hrefs. Mobile/card list (approx line 450): change href={`/clientes/${encodeURIComponent(c.id)}/editar`} to href={`/clientes/${encodeURIComponent(c.id)}`}. Desktop row (approx line 597): change href={`/clientes/${encodeURIComponent(cliente.id)}/editar`} to href={`/clientes/${encodeURIComponent(cliente.id)}`}. Keep the Pencil icon, the surrounding canEditClientes guard, and all styling classes exactly as-is — only the `/editar` suffix is stripped. Do NOT remove the pencil affordances (CONTEXT.md/UI-SPEC.md do not prescribe removal; keep them as an additional entry point to the unified page).
  </action>
  <verify>
    <automated>cd web &amp;&amp; grep -rn "\[id\]/editar\|/editar\`" "src/app/(dashboard)/clientes/page.tsx"; echo "exit: $?"; pnpm tsc --noEmit 2>&amp;1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - source: `web/src/app/(dashboard)/clientes/page.tsx` contains ZERO occurrences of `/editar` in any Link href (grep for `editar` in this file returns no href matches).
    - source: both pencil-icon Links now point to `/clientes/${encodeURIComponent(...)}` with no `/editar` suffix.
    - source: the `Pencil` icon and `canEditClientes` guards around both affordances are preserved.
    - behavior: `cd web && pnpm tsc --noEmit` passes.
  </acceptance_criteria>
  <done>Both Editar pencil links in the clientes list resolve to /clientes/[id]; no /editar reference remains in the list page. tsc passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| list-page navigation → detail page | Link targets are static client-side routes; no untrusted input crosses a security boundary here (the id is already the row's own id, url-encoded as today). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-75-06 | Tampering | Link href construction in clientes/page.tsx | accept | Only the static route suffix `/editar` is removed; `encodeURIComponent` on the id is preserved unchanged, so no new injection/tampering surface is introduced. |
| T-75-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages installed by this plan; nothing to audit. |
</threat_model>

<verification>
- grep of `web/src/app/(dashboard)/clientes/page.tsx` for `/editar` returns no matches.
- `cd web && pnpm tsc --noEmit` passes.
- Manual (deferred to human_verify): clicking either pencil icon in the clientes list lands on the unified detail page (read mode), from which the in-page Editar toggle works.
</verification>

<success_criteria>
- CLI-14 (link portion): no internal link points to the removed `/clientes/[id]/editar` route; both list-page pencil affordances reach `/clientes/[id]`.
</success_criteria>

<output>
Create `.planning/phases/LEXCV-75-componente-nico-view-edit/75-03-SUMMARY.md` when done.
</output>
