---
phase: "56-dashboard-e-calendario"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/app/(dashboard)/dashboard/page.tsx
  - web/src/app/(dashboard)/agenda/page.tsx
autonomous: true
requirements:
  - DASH-01
  - CAL-01
must_haves:
  truths:
    - "Em mobile, os KPIs do dashboard aparecem em coluna única (1 por linha)"
    - "Em tablet (sm), os KPIs aparecem em 2 colunas"
    - "Em desktop (lg), os KPIs aparecem em 4 colunas"
    - "Em mobile, a agenda mostra os eventos de hoje (vista diária) por defeito"
    - "Em tablet/desktop (md+), a grade mensal continua visível e funcional"
  artifacts:
    - path: "web/src/app/(dashboard)/dashboard/page.tsx"
      provides: "KPI grid com grid-cols-1 sm:grid-cols-2 lg:grid-cols-4"
      contains: "grid-cols-1 sm:grid-cols-2 lg:grid-cols-4"
    - path: "web/src/app/(dashboard)/agenda/page.tsx"
      provides: "Vista diária mobile + calendário mensal oculto em mobile"
      contains: "hidden md:block"
  key_links:
    - from: "dashboard/page.tsx linha 218"
      to: "KPI grid div"
      via: "className Tailwind"
      pattern: "grid-cols-1 sm:grid-cols-2 lg:grid-cols-4"
    - from: "agenda/page.tsx Card do calendário mensal"
      to: "hidden md:block"
      via: "className Tailwind"
      pattern: "hidden md:block"
---

<objective>
Corrigir a grelha de KPIs do dashboard para 1→2→4 colunas em mobile/tablet/desktop (DASH-01) e implementar a vista diária como predefinição em mobile na agenda (CAL-01), ocultando o calendário mensal de 7 colunas em ecrãs pequenos e mostrando os eventos de hoje.

Purpose: Ambas as páginas são inutilizáveis em mobile no estado atual — a grelha de KPIs não tem breakpoint base e o calendário mensal de 7 colunas sobrepõe-se e fica ilegível em ecrãs estreitos.
Output: dashboard/page.tsx e agenda/page.tsx com layout mobile corrigido; `pnpm build` sem erros.
</objective>

<execution_context>
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Tarefa 1: DASH-01 — Adicionar grid-cols-1 à grelha de KPIs</name>
  <files>web/src/app/(dashboard)/dashboard/page.tsx</files>
  <action>
    Na linha 218 de dashboard/page.tsx existe atualmente:

      return &lt;div className="grid gap-4 sm:grid-cols-2 lg:grid-cols-4"&gt;{cards}&lt;/div&gt;;

    Alterar o className para:

      "grid gap-4 grid-cols-1 sm:grid-cols-2 lg:grid-cols-4"

    Apenas este className muda — nenhum outro código é alterado.
    O breakpoint base (sem prefixo) define 1 coluna em mobile.
    sm: (≥640px) passa para 2 colunas.
    lg: (≥1024px) passa para 4 colunas.
    (per DASH-01)
  </action>
  <verify>
    <automated>cd web && pnpm build 2>&amp;1 | tail -20</automated>
  </verify>
  <done>Build passa sem erros de TypeScript; className da div contém "grid-cols-1 sm:grid-cols-2 lg:grid-cols-4".</done>
</task>

<task type="auto" tdd="false">
  <name>Tarefa 2: CAL-01 — Vista diária mobile na agenda (ocultar calendário mensal em mobile)</name>
  <files>web/src/app/(dashboard)/agenda/page.tsx</files>
  <action>
    Existem dois blocos a modificar em agenda/page.tsx:

    1. O Card do calendário mensal (linha 328) não tem classe de visibilidade mobile. Adicionar "hidden md:block" ao wrapper Card:

       Antes: &lt;Card className="border-slate-200 dark:border-slate-800 rounded-none overflow-hidden shadow-sm"&gt;
       Depois: &lt;Card className="hidden md:block border-slate-200 dark:border-slate-800 rounded-none overflow-hidden shadow-sm"&gt;

    2. Após o bloco "Próximos Eventos" existente (que tem md:hidden, linhas 299-326), adicionar um novo bloco "Hoje" exclusivo para mobile. Este bloco deve:
       - Ter className="md:hidden space-y-4" para aparecer apenas em mobile
       - Calcular os eventos de hoje a partir de `eventosByDay` usando `dayKey(new Date())`
       - Mostrar cabeçalho com a data de hoje formatada em pt-CV (formato: weekday long + day + month long)
       - Se não houver eventos hoje, mostrar parágrafo "Nenhum evento hoje." em estilo slate-500
       - Se houver eventos, listar cada um com o mesmo padrão visual dos cards de "Próximos Eventos" (border, cat.borderClassName, hora, titulo, processoLabel)

    Para calcular a chave do dia de hoje usar a função `dayKey` já existente no ficheiro:
       const todayKey = dayKey(new Date());
       const todayEvents = eventosByDay.get(todayKey) ?? [];

    Inserir este bloco entre o bloco "Próximos Eventos" (linha ~326) e o Card do calendário mensal (linha ~328). O bloco "Próximos Eventos" existente não deve ser removido — continua visível em mobile mostrando eventos futuros. O novo bloco "Hoje" complementa com os eventos especificamente de hoje.

    Não criar novos componentes ou ficheiros — tudo inline no JSX existente.
    (per CAL-01)
  </action>
  <verify>
    <automated>cd web && pnpm build 2>&amp;1 | tail -30</automated>
  </verify>
  <done>Build passa sem erros; o Card do calendário mensal tem "hidden md:block" no className; existe um bloco md:hidden com cabeçalho de data de hoje e lista de eventos derivados de eventosByDay.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API → UI | Dados de eventos chegam via TanStack Query / apiFetch; já validados em fases anteriores |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-56-01 | Information Disclosure | agenda/page.tsx — eventos exibidos | accept | Dados já protegidos por RBAC backend (canViewAgenda guard existente); nenhuma mudança de superfície |
| T-56-SC | Tampering | npm/pnpm installs | accept | Nenhum novo pacote instalado neste plano |
</threat_model>

<verification>
1. `cd web && pnpm build` — sem erros de TypeScript ou compilação
2. Verificar manualmente em DevTools (Chrome, viewport 375px): KPIs aparecem em coluna única, calendário mensal oculto, bloco "Hoje" visível
3. Verificar em viewport 768px (md): calendário mensal visível, bloco "Hoje" oculto
</verification>

<success_criteria>
- `pnpm build` passa sem erros
- dashboard/page.tsx linha 218: className contém "grid-cols-1 sm:grid-cols-2 lg:grid-cols-4"
- agenda/page.tsx: Card do calendário mensal tem "hidden md:block"
- agenda/page.tsx: existe bloco md:hidden com data de hoje e eventos de `eventosByDay`
</success_criteria>

<output>
Criar `.planning/phases/56-dashboard-e-calendario/56-01-SUMMARY.md` quando concluído, seguindo o template em @C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/templates/summary.md
</output>
