---
phase: "54-listas-e-tabelas"
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/app/(dashboard)/processos/[id]/page.tsx
autonomous: true
requirements:
  - TAB-02

must_haves:
  truths:
    - "Em mobile, a tabela de Partes do Processo não provoca overflow horizontal silencioso — o utilizador pode fazer scroll para ver NIF"
    - "Em mobile, a tabela de Fases do Processo não provoca overflow horizontal — o utilizador pode fazer scroll para ver as ações"
    - "Em desktop, as tabelas de Partes e Fases têm o mesmo aspecto que antes"
  artifacts:
    - path: "web/src/app/(dashboard)/processos/[id]/page.tsx"
      provides: "Tabelas Partes e Fases com min-w e wrapper -mx-4 px-4 sm:mx-0 sm:px-0"
      contains: "min-w-\\[400px\\]"
  key_links:
    - from: "wrapper div overflow-x-auto"
      to: "table min-w-[400px]"
      via: "className na <table>"
      pattern: "min-w-\\[4[0-9][0-9]px\\]"
---

<objective>
Adicionar largura mínima às tabelas de Partes e Fases em `processos/[id]/page.tsx`, activando o scroll horizontal nativo no wrapper `overflow-x-auto` já existente.

Purpose: Em ecrãs estreitos, as tabelas de detalhe do processo ficam comprimidas e as colunas sobrepõem-se. O `overflow-x-auto` já existe — só falta `min-w-*` na `<table>` para o scroll funcionar.
Output: `processos/[id]/page.tsx` com `min-w-[400px]` na tabela Partes e `min-w-[480px]` + wrapper compensado na tabela Fases.
</objective>

<execution_context>
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/54-listas-e-tabelas/54-CONTEXT.md
@.planning/phases/54-listas-e-tabelas/54-PATTERNS.md
</context>

<interfaces>
<!-- Extraído de 54-PATTERNS.md — estrutura actual das tabelas a modificar -->

Tab "partes" — wrapper e tabela (processos/[id]/page.tsx ~linha 1253):
```tsx
<div className="overflow-x-auto">
  <table className="w-full text-sm">
    <thead ...>
      <tr ...>
        <th>Tipo</th>
        <th>Nome</th>
        <th>NIF</th>
      </tr>
    </thead>
    <tbody>
      {partes.data.map((p) => (
        <tr key={p.id} className="border-b border-neutral-200 last:border-b-0 dark:border-neutral-800">
          <td className="py-2 pr-4">{p.tipo ?? "—"}</td>
          <td className="py-2 pr-4 font-medium">{p.nome}</td>
          <td className="py-2 pr-4">{p.nif ?? "—"}</td>
        </tr>
      ))}
    </tbody>
  </table>
</div>
```

Tab "fases" — wrapper e tabela (processos/[id]/page.tsx ~linha 1331):
```tsx
<div className="overflow-x-auto">
  <table className="w-full text-sm">
    <thead ...>
      <tr ...>
        <th>Fase</th>
        <th>Status</th>
        <th>Ações</th>
      </tr>
    </thead>
    <tbody>
      {fases.data.map((f) => (
        <tr key={f.id} ...>
          <td className="py-2 pr-4 font-medium">{f.fase?.nome ?? f.fase_id}</td>
          <td className="py-2 pr-4"><select ...>...</select></td>
          <td className="py-2 pr-4"><Button ...>Guardar</Button></td>
        </tr>
      ))}
    </tbody>
  </table>
</div>
```
</interfaces>

<tasks>

<task type="auto">
  <name>Tarefa 1: Adicionar min-w à tabela Partes e ajustar wrapper</name>
  <files>web/src/app/(dashboard)/processos/[id]/page.tsx</files>
  <action>
No ficheiro `processos/[id]/page.tsx`, localizar a tab "partes" (procurar pela string "partes.data.map") e efectuar duas alterações cirúrgicas:

1. No wrapper da tabela Partes, alterar:
   `<div className="overflow-x-auto">`
   para:
   `<div className="overflow-x-auto -mx-4 px-4 sm:mx-0 sm:px-0">`

2. Na tag `<table>` da secção Partes, alterar:
   `<table className="w-full text-sm">`
   para:
   `<table className="w-full min-w-[400px] text-sm">`

Aplicar a mesma lógica à secção Fases (localizar via "fases.data.map"):

3. No wrapper da tabela Fases, alterar:
   `<div className="overflow-x-auto">`
   para:
   `<div className="overflow-x-auto -mx-4 px-4 sm:mx-0 sm:px-0">`

4. Na tag `<table>` da secção Fases, alterar:
   `<table className="w-full text-sm">`
   para:
   `<table className="w-full min-w-[480px] text-sm">`

Nota: Partes tem 3 colunas simples (400px suficiente). Fases tem um `<select>` e um `<Button>` na mesma linha (480px necessário). Não alterar nenhuma outra secção do ficheiro. Não tocar na tab "timeline" nem em Movimentações (renderizadas como timeline, não tabela — fora de scope do TAB-02 per 54-PATTERNS.md linha 309).
  </action>
  <verify>
    <automated>grep -n "min-w-\[400px\]\|min-w-\[480px\]" web/src/app/\(dashboard\)/processos/\[id\]/page.tsx</automated>
  </verify>
  <done>Grep retorna exactamente 2 linhas — uma com min-w-[400px] (Partes) e outra com min-w-[480px] (Fases). O ficheiro compila sem erros (`pnpm build` ou `pnpm lint` passa).</done>
</task>

<task type="auto">
  <name>Tarefa 2: Verificar build e criar SUMMARY</name>
  <files>.planning/phases/54-listas-e-tabelas/54-01-SUMMARY.md</files>
  <action>
Executar `pnpm --prefix web lint` para confirmar que não há erros de TypeScript/ESLint introduzidos pelas alterações.

Criar `.planning/phases/54-listas-e-tabelas/54-01-SUMMARY.md` com o template de SUMMARY seguindo `@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/templates/summary.md`. Incluir:
- Ficheiros modificados: `web/src/app/(dashboard)/processos/[id]/page.tsx`
- O que foi feito: adicionado `min-w-[400px]` à tabela Partes e `min-w-[480px]` à tabela Fases; wrapper de ambas actualizado para `-mx-4 px-4 sm:mx-0 sm:px-0`
- Padrão estabelecido: para tabelas complexas com `overflow-x-auto`, adicionar `min-w-[Xpx]` à `<table>` e compensar o padding do container pai com `-mx-4 px-4 sm:mx-0 sm:px-0` no wrapper
  </action>
  <verify>
    <automated>pnpm --prefix web lint 2>&1 | tail -5</automated>
  </verify>
  <done>lint termina sem erros. SUMMARY.md criado em `.planning/phases/54-listas-e-tabelas/54-01-SUMMARY.md`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → DOM | Alterações puramente de presentação — nenhum dado novo é exposto |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-54-01-SC | Tampering | npm/pip/cargo installs | accept | Nenhuma nova dependência instalada neste plano — alterações CSS puras |

Nenhum trust boundary novo é introduzido. As alterações são className-only em JSX existente.
</threat_model>

<verification>
1. `grep -c "min-w-\[400px\]" web/src/app/\(dashboard\)/processos/\[id\]/page.tsx` retorna `1`
2. `grep -c "min-w-\[480px\]" web/src/app/\(dashboard\)/processos/\[id\]/page.tsx` retorna `1`
3. `pnpm --prefix web lint` termina sem erros
4. Inspecção visual em mobile (375px): tab Partes mostra scrollbar horizontal; tab Fases mostra scrollbar horizontal
5. Inspecção visual em desktop (1280px): aspecto idêntico ao baseline
</verification>

<success_criteria>
- TAB-02 implementado: tabelas Partes e Fases em `/processos/[id]` têm scroll horizontal funcional em mobile
- Zero regressões: desktop sem alteração visual
- Build limpo: lint sem erros
</success_criteria>

<output>
Criar `.planning/phases/54-listas-e-tabelas/54-01-SUMMARY.md` quando concluído.
</output>
