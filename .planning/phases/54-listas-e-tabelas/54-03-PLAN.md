---
phase: "54-listas-e-tabelas"
plan: 03
type: execute
wave: 3
depends_on:
  - "54-02"
files_modified:
  - web/src/app/(dashboard)/documentos/page.tsx
  - web/src/app/(dashboard)/financeiro/page.tsx
autonomous: true
requirements:
  - TAB-01

must_haves:
  truths:
    - "Em mobile (<768px), /documentos mostra cards com nome do ficheiro, tipo, data, processo e botão de apagar"
    - "Em mobile (<768px), /financeiro mostra cards com processo, valor, Badge de estado e data"
    - "Os badges de estado em Financeiro usam o componente <Badge> de @/components/ui/badge em vez de <span> com classe inline"
    - "Em desktop (≥768px), ambas as tabelas permanecem sem alteração visual"
  artifacts:
    - path: "web/src/app/(dashboard)/documentos/page.tsx"
      provides: "Divisão hidden md:block (tabela) + md:hidden (cards)"
      contains: "md:hidden"
    - path: "web/src/app/(dashboard)/financeiro/page.tsx"
      provides: "Divisão hidden md:block (tabela) + md:hidden (cards) + Badge component"
      contains: "md:hidden"
  key_links:
    - from: "financeiro/page.tsx — statusBadgeClass span"
      to: "<Badge variant=...>"
      via: "substituição inline nos cards mobile"
      pattern: "Badge.*variant.*green\\|amber\\|blue"
---

<objective>
Adicionar cards mobile às páginas Documentos e Financeiro, completando TAB-01. Em Financeiro, migrar os badges de estado de `<span>` com classe inline para o componente `<Badge>` nos cards mobile.

Purpose: As páginas de Documentos (9 colunas) e Financeiro (6 colunas) são ilegíveis em mobile. O padrão estabelecido no Plano 02 (clientes) é aplicado aqui com adaptação dos campos essenciais por página.
Output: `documentos/page.tsx` e `financeiro/page.tsx` com padrão `hidden md:block` / `md:hidden` e cards inline.
</objective>

<execution_context>
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/54-listas-e-tabelas/54-CONTEXT.md
@.planning/phases/54-listas-e-tabelas/54-PATTERNS.md
@.planning/phases/54-listas-e-tabelas/54-02-SUMMARY.md
</context>

<interfaces>
<!-- Extraído de 54-PATTERNS.md -->

Estrutura actual da tabela em documentos/page.tsx (~linha 120):
```tsx
<Card>
  <CardContent>
    <div className="overflow-x-auto">
      <table className="w-full text-sm">
        <thead className="text-left text-neutral-500 dark:text-neutral-400">
          <tr className="border-b border-neutral-200 dark:border-neutral-800">
            <th>Nome</th><th>Tipo</th><th>Processo</th><th>Cliente</th>
            <th>Confid.</th><th>Ver.</th><th>Tamanho</th><th>Criado</th><th>Ações</th>
          </tr>
        </thead>
        <tbody>
          {list.data.map((d) => (<DocumentoRow key={d.id} ... />))}
        </tbody>
      </table>
    </div>
  </CardContent>
</Card>
```

Campos essenciais do card Documentos (de 54-CONTEXT.md):
- nome do ficheiro, tipo, data de upload, processo associado, botão download

Tipo DocumentoResponse (de 54-CONTEXT.md):
```ts
{ id, nome, tipo, dataUpload, processoId, processoNumero, clienteNome, confidencial, versao, tamanho }
```

Estrutura actual da tabela em financeiro/page.tsx (~linha 297):
```tsx
<Card>
  <CardHeader><CardTitle>Honorários</CardTitle></CardHeader>
  <CardContent>
    <div className="overflow-x-auto">
      <table className="w-full text-sm">
        <thead ...>
          <tr ...>
            <th>Honorário</th><th>Processo</th><th>Cliente</th>
            <th>Total</th><th>Data do acordo</th><th>Estado</th>
          </tr>
        </thead>
        <tbody>
          {filteredList.map((h) => (<tr key={h.id} ...>...</tr>))}
        </tbody>
      </table>
    </div>
  </CardContent>
</Card>
```

Badge de estado inline a substituir em financeiro/page.tsx (linha 355):
```tsx
// ANTES (nos cards mobile, usar Badge em vez disto):
<span className={statusBadgeClass[h.status]}>{h.status}</span>

// DEPOIS (apenas nos cards mobile):
<Badge
  variant={h.status === "Pago" ? "green" : h.status === "Parcialmente Pago" ? "blue" : "amber"}
  className="rounded-none font-bold text-[10px]"
>
  {h.status}
</Badge>
```

Tipo HonorarioResponse (de 54-CONTEXT.md):
```ts
{ id, processoId, processoNumero, valor, status, dataCriacao, clienteNome }
```

Padrão de visibilidade responsiva (estabelecido no Plano 02):
```tsx
<div className="hidden md:block">
  <div className="overflow-x-auto">
    <table ...>...</table>
  </div>
</div>
<div className="md:hidden divide-y divide-neutral-200 dark:divide-neutral-800">
  {data.map((item) => ( <div key={item.id} className="p-4">...</div> ))}
</div>
```
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Tarefa 1: Cards mobile em documentos/page.tsx</name>
  <files>web/src/app/(dashboard)/documentos/page.tsx</files>
  <action>
Ler `documentos/page.tsx` na íntegra. Localizar o `<div className="overflow-x-auto"><table ...>`.

1. Envolver esse `<div className="overflow-x-auto">` com `<div className="hidden md:block">`:
```tsx
<div className="hidden md:block">
  <div className="overflow-x-auto">
    <table ...>...</table>
  </div>
</div>
```

2. Imediatamente a seguir (ainda dentro de `<CardContent>`), adicionar o bloco de cards mobile. Os campos essenciais são: nome, tipo, data de upload, processo associado, botão de apagar (se `canEditDocumentos`):
```tsx
<div className="md:hidden divide-y divide-neutral-200 dark:divide-neutral-800">
  {list.data.map((d) => (
    <div key={d.id} className="p-4 space-y-2">
      <div className="flex items-start justify-between gap-2">
        <div className="font-bold text-slate-900 dark:text-white text-sm leading-tight">{d.nome}</div>
        {d.tipo && (
          <Badge variant="blue" className="rounded-none font-bold text-[10px] flex-shrink-0">{d.tipo}</Badge>
        )}
      </div>
      {d.processoNumero && (
        <div className="text-[11px] text-neutral-500 dark:text-neutral-400">
          Processo: <span className="font-medium text-neutral-700 dark:text-neutral-300">{d.processoNumero}</span>
        </div>
      )}
      <div className="text-[11px] text-neutral-500 dark:text-neutral-400">
        Enviado: {d.dataUpload ? new Date(d.dataUpload).toLocaleDateString("pt-CV") : "—"}
      </div>
      <div className="pt-1 flex gap-2 flex-wrap">
        <a
          href={`/api/v1/documentos/${d.id}/download`}
          target="_blank"
          rel="noreferrer"
          className="inline-flex items-center gap-1 text-xs font-medium text-blue-600 dark:text-blue-400 hover:underline min-h-[44px] px-2"
        >
          <Download className="h-4 w-4" /> Download
        </a>
        {canEditDocumentos && (
          <Button
            type="button"
            variant="outline"
            size="sm"
            onClick={() => { /* mesmo handler que DocumentoRow usa */ }}
            className="text-red-600 border-red-200 hover:bg-red-50 dark:text-red-400 dark:border-red-800 dark:hover:bg-red-900/20 min-h-[44px]"
          >
            Apagar
          </Button>
        )}
      </div>
    </div>
  ))}
</div>
```

Nota importante sobre o handler de apagar: o `DocumentoRow` tem o handler de delete encapsulado internamente. Para os cards mobile, verificar se há uma função de delete disponível no scope da página (ex: `handleDelete(id)` ou mutation directa). Se o componente `DocumentoRow` encapsula o delete e não há função disponível no scope, omitir o botão de apagar nos cards mobile e deixar apenas o botão na tabela desktop — documentar esta limitação no SUMMARY. Não criar lógica de delete duplicada; usar apenas o que já existe no scope.

Verificar também se `Badge` já está importado em `documentos/page.tsx`. Se não, adicionar:
```tsx
import { Badge } from "@/components/ui/badge";
```
  </action>
  <verify>
    <automated>grep -c "md:hidden" web/src/app/\(dashboard\)/documentos/page.tsx</automated>
  </verify>
  <done>grep retorna pelo menos 1. `hidden md:block` envolve a tabela. `md:hidden` contém os cards. Lint sem erros.</done>
</task>

<task type="auto" tdd="false">
  <name>Tarefa 2: Cards mobile em financeiro/page.tsx com Badge de estado</name>
  <files>web/src/app/(dashboard)/financeiro/page.tsx</files>
  <action>
Ler `financeiro/page.tsx` na íntegra. Localizar `<div className="overflow-x-auto"><table ...>` dentro de `<CardContent>`.

1. Envolver o `<div className="overflow-x-auto">` com `<div className="hidden md:block">`.

2. Imediatamente a seguir, adicionar o bloco de cards mobile. Os campos essenciais são: processo, valor (formatado), Badge de estado, data:
```tsx
<div className="md:hidden divide-y divide-neutral-200 dark:divide-neutral-800">
  {filteredList.map((h) => (
    <div key={h.id} className="p-4 space-y-2">
      <div className="flex items-start justify-between gap-2">
        <div className="font-bold text-slate-900 dark:text-white text-sm">
          {h.processoNumero ? `Proc. ${h.processoNumero}` : `Honorário #${h.id.slice(0, 6)}`}
        </div>
        <Badge
          variant={h.status === "Pago" ? "green" : h.status === "Parcialmente Pago" ? "blue" : "amber"}
          className="rounded-none font-bold text-[10px] flex-shrink-0"
        >
          {h.status}
        </Badge>
      </div>
      {h.clienteNome && (
        <div className="text-[11px] text-neutral-500 dark:text-neutral-400">
          {h.clienteNome}
        </div>
      )}
      <div className="flex items-center justify-between">
        <div className="font-bold text-slate-900 dark:text-white">
          {typeof h.valor === "number"
            ? h.valor.toLocaleString("pt-CV", { style: "currency", currency: "CVE" })
            : h.valor}
        </div>
        {h.dataCriacao && (
          <div className="text-[11px] text-neutral-500 dark:text-neutral-400">
            {new Date(h.dataCriacao).toLocaleDateString("pt-CV")}
          </div>
        )}
      </div>
    </div>
  ))}
</div>
```

3. Verificar se `Badge` já está importado em `financeiro/page.tsx`. Se não, adicionar:
```tsx
import { Badge } from "@/components/ui/badge";
```

4. NÃO alterar o `statusBadgeClass` existente nem os `<span>` na tabela desktop — deixar a tabela intacta. O `<Badge>` é usado apenas nos cards mobile.

Nota sobre `h.valor`: verificar o tipo real no `HonorarioResponse`. Se for string em vez de number, adaptar o formatter — se `typeof h.valor !== "number"`, renderizar directamente sem toLocaleString.
  </action>
  <verify>
    <automated>grep -c "md:hidden" web/src/app/\(dashboard\)/financeiro/page.tsx</automated>
  </verify>
  <done>grep retorna pelo menos 1. `hidden md:block` envolve a tabela. `md:hidden` contém os cards com Badge. Lint sem erros.</done>
</task>

<task type="auto">
  <name>Tarefa 3: Lint, audit de cobertura e SUMMARY final</name>
  <files>.planning/phases/54-listas-e-tabelas/54-03-SUMMARY.md</files>
  <action>
Executar `pnpm --prefix web lint` para confirmar ausência de erros nos dois ficheiros modificados.

Criar `.planning/phases/54-listas-e-tabelas/54-03-SUMMARY.md` com o template de SUMMARY. Incluir:
- Ficheiros modificados: `documentos/page.tsx`, `financeiro/page.tsx`
- Campos nos cards de Documentos: nome, tipo (Badge blue), processo, data de upload
- Campos nos cards de Financeiro: processo/ID, cliente, valor formatado, Badge de estado (green/blue/amber), data
- Nota sobre Badge: em Financeiro mobile os cards usam `<Badge>` componente; a tabela desktop mantém `<span>` com `statusBadgeClass` (não alterado)
- Auditoria TAB-01 completa: Clientes (Plano 02), Agenda (Plano 02), Documentos (Plano 03), Financeiro (Plano 03) — todos entregues
- Auditoria TAB-02 completa: Partes e Fases em Processos (Plano 01) — entregue
  </action>
  <verify>
    <automated>pnpm --prefix web lint 2>&1 | tail -5</automated>
  </verify>
  <done>lint termina sem erros. SUMMARY.md criado. Fase 54 completa: TAB-01 e TAB-02 entregues.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → DOM | Alterações puramente de presentação — nenhum dado novo é exposto |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-54-03-SC | Tampering | npm/pip/cargo installs | accept | Nenhuma nova dependência instalada — apenas classes CSS e componentes existentes |
| T-54-03-01 | Information Disclosure | cards mobile documentos | accept | Os cards exibem subconjunto dos campos da tabela; a visibilidade é controlada pelo mesmo sistema de permissões (`canEditDocumentos`) já presente na tabela |
| T-54-03-02 | Information Disclosure | cards mobile financeiro | accept | Os cards exibem subconjunto dos campos financeiros; dados de honorários são visíveis apenas a utilizadores com permissão `financeiro:view` já enforçada no backend |

Nenhum trust boundary novo é introduzido. Os dados exibidos nos cards são os mesmos que já aparecem na tabela desktop — não há exposição adicional.
</threat_model>

<verification>
1. `grep -c "hidden md:block" web/src/app/\(dashboard\)/documentos/page.tsx` retorna `1`
2. `grep -c "md:hidden" web/src/app/\(dashboard\)/documentos/page.tsx` retorna pelo menos `1`
3. `grep -c "hidden md:block" web/src/app/\(dashboard\)/financeiro/page.tsx` retorna `1`
4. `grep -c "md:hidden" web/src/app/\(dashboard\)/financeiro/page.tsx` retorna pelo menos `1`
5. `grep -c "Badge" web/src/app/\(dashboard\)/financeiro/page.tsx` retorna pelo menos `1` (componente Badge usado nos cards)
6. `pnpm --prefix web lint` sem erros
7. Inspecção visual em 375px: `/documentos` mostra cards; em 1280px mostra tabela 9 colunas
8. Inspecção visual em 375px: `/financeiro` mostra cards com badge colorido; em 1280px mostra tabela
</verification>

<success_criteria>
- TAB-01 completo: Documentos e Financeiro com cards mobile funcionais
- Badges de estado em Financeiro mobile usam componente `<Badge>` (green/blue/amber)
- TAB-01 cobertura total: Clientes + Agenda (Plano 02) + Documentos + Financeiro (Plano 03)
- Desktop sem regressão visual em ambas as páginas
- Build limpo — fase 54 concluída
</success_criteria>

<output>
Criar `.planning/phases/54-listas-e-tabelas/54-03-SUMMARY.md` quando concluído.
</output>
