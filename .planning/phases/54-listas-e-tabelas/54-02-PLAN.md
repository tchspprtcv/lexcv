---
phase: "54-listas-e-tabelas"
plan: 02
type: execute
wave: 2
depends_on:
  - "54-01"
files_modified:
  - web/src/app/(dashboard)/clientes/page.tsx
  - web/src/app/(dashboard)/agenda/page.tsx
autonomous: true
requirements:
  - TAB-01

must_haves:
  truths:
    - "Em mobile (<768px), a página /clientes mostra cards empilhados em vez da tabela"
    - "Cada card de cliente mostra: avatar com iniciais, nome, NIF/BI, telefone e botões Ver/Editar"
    - "Em desktop (≥768px), a tabela de clientes permanece inalterada"
    - "Em mobile, a secção de eventos da /agenda apresenta os cards já existentes com a divisão md:hidden correcta"
    - "Em desktop, a agenda permanece sem alteração visual"
  artifacts:
    - path: "web/src/app/(dashboard)/clientes/page.tsx"
      provides: "Divisão hidden md:block (tabela) + md:hidden (cards mobile)"
      contains: "md:hidden"
    - path: "web/src/app/(dashboard)/agenda/page.tsx"
      provides: "Cards de eventos com visibilidade responsiva confirmada"
      contains: "md:hidden"
  key_links:
    - from: "clientes/page.tsx — div md:hidden"
      to: "clientes.data.map → card inline"
      via: "JSX map no bloco md:hidden"
      pattern: "md:hidden.*space-y"
---

<objective>
Adicionar cards mobile à página de Clientes e confirmar/implementar visibilidade responsiva na página de Agenda.

Purpose: Em mobile, a tabela shadcn de Clientes (5 colunas) é ilegível. A Agenda já tem cards de evento mas sem a divisão `md:hidden` / `hidden md:block`. Ambas as páginas precisam do padrão de visibilidade responsiva definido em 54-CONTEXT.md.
Output: `clientes/page.tsx` com divisão tabela/cards. `agenda/page.tsx` com `md:hidden` aplicado à lista de eventos mobile.
</objective>

<execution_context>
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/54-listas-e-tabelas/54-CONTEXT.md
@.planning/phases/54-listas-e-tabelas/54-PATTERNS.md
</context>

<interfaces>
<!-- Extraído de 54-PATTERNS.md — estruturas actuais e padrões a aplicar -->

Wrapper actual da tabela em clientes/page.tsx (~linha 375):
```tsx
<Card className="border-slate-200 dark:border-slate-800 ...">
  <CardContent className="p-0">
    <div className="overflow-hidden">
      <Table>...</Table>
    </div>
  </CardContent>
</Card>
```

Padrão de avatar + nome já existente na tabela (ClienteRow ~linha 93):
```tsx
<div className="flex items-center gap-3">
  <div className="h-10 w-10 rounded-none bg-blue-50 dark:bg-blue-500/10 text-blue-700 dark:text-blue-400 border border-blue-100 dark:border-blue-500/20 flex items-center justify-center text-xs font-bold shadow-sm">
    {initials}
  </div>
  <div className="min-w-0">
    <Link href={`/clientes/${encodeURIComponent(cliente.id)}`} className="font-bold text-slate-900 dark:text-white hover:text-blue-600 dark:hover:text-blue-400 transition-colors">
      {cliente.nome}
    </Link>
  </div>
</div>
```

Padrão de card de item mobile (de 54-PATTERNS.md):
```tsx
<div className="rounded-none border border-slate-200 dark:border-slate-800 bg-white dark:bg-[#020617] p-4 shadow-sm">
  <div className="flex items-start justify-between gap-3">
    <div className="font-bold text-slate-900 dark:text-white text-sm">{nome}</div>
    <Badge variant="..." className="rounded-none font-bold">{estado}</Badge>
  </div>
  <div className="mt-2 space-y-1">
    <div className="text-[11px] text-slate-500 dark:text-slate-400">Campo: {valor}</div>
  </div>
  <div className="mt-3 flex items-center gap-2">
    <Button asChild size="sm" variant="ghost" className="h-8 w-8 p-0">
      <Link href={href}><Eye className="h-4 w-4" /></Link>
    </Button>
  </div>
</div>
```

Card de evento já existente em agenda/page.tsx (linhas 409-432):
```tsx
<div className={cn(
  "rounded-none border border-slate-200 dark:border-slate-800 bg-white dark:bg-[#020617] p-4 shadow-sm hover:shadow-md transition-all",
  cat.borderClassName
)}>
  <div className="flex items-start justify-between gap-3">
    <div className="text-[10px] font-bold tracking-wider uppercase" style={{ color: cat.titleColor }}>
      {cat.label}
    </div>
    <div className="text-xs font-bold text-slate-500 dark:text-slate-400 bg-slate-100 dark:bg-slate-800 px-2 py-0.5 rounded-sm">
      {hora}
    </div>
  </div>
  <div className="mt-2 text-sm font-bold text-slate-900 dark:text-white">{e.titulo}</div>
  <div className="mt-1 text-[11px] font-medium tracking-wider text-slate-500 dark:text-slate-400 uppercase">
    {processoLabel}
  </div>
</div>
```

Padrão de visibilidade responsiva a introduzir:
```tsx
{/* Desktop: tabela */}
<div className="hidden md:block">
  {/* tabela existente */}
</div>

{/* Mobile: cards */}
<div className="md:hidden space-y-3">
  {data.map((item) => (
    <div key={item.id} className="rounded-none border border-slate-200 dark:border-slate-800 bg-white dark:bg-[#020617] p-4 shadow-sm">
      ...
    </div>
  ))}
</div>
```

Campos essenciais por card (de 54-CONTEXT.md):
- Clientes: nome, NIF/BI, telefone, badge estado de conta
- Agenda (eventos): título, data/hora, categoria, status concluído
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Tarefa 1: Cards mobile em clientes/page.tsx</name>
  <files>web/src/app/(dashboard)/clientes/page.tsx</files>
  <action>
Ler `clientes/page.tsx` na íntegra para identificar a localização exacta do `<div className="overflow-hidden"><Table>...</Table></div>` dentro de `<CardContent className="p-0">`.

Envolver o bloco `<div className="overflow-hidden">` existente com `<div className="hidden md:block">`:
```tsx
<div className="hidden md:block">
  <div className="overflow-hidden">
    <Table>...</Table>
  </div>
</div>
```

Imediatamente a seguir (ainda dentro de `<CardContent className="p-0">`), adicionar o bloco de cards mobile:
```tsx
<div className="md:hidden divide-y divide-slate-200 dark:divide-slate-800">
  {clientes.data.map((c) => {
    const initials = c.nome.split(" ").filter(Boolean).slice(0, 2).map((w: string) => w[0]).join("").toUpperCase();
    return (
      <div key={c.id} className="p-4">
        <div className="flex items-center gap-3">
          <div className="h-10 w-10 flex-shrink-0 rounded-none bg-blue-50 dark:bg-blue-500/10 text-blue-700 dark:text-blue-400 border border-blue-100 dark:border-blue-500/20 flex items-center justify-center text-xs font-bold">
            {initials}
          </div>
          <div className="min-w-0 flex-1">
            <Link href={`/clientes/${encodeURIComponent(c.id)}`} className="font-bold text-slate-900 dark:text-white hover:text-blue-600 dark:hover:text-blue-400 transition-colors text-sm">
              {c.nome}
            </Link>
            {c.nif && (
              <div className="text-[11px] font-medium text-slate-500 dark:text-slate-400 mt-0.5">NIF: {c.nif}</div>
            )}
          </div>
          {c.estadoConta && (
            <Badge variant={c.estadoConta === "Activo" ? "green" : "gray"} className="rounded-none font-bold text-[10px] flex-shrink-0">
              {c.estadoConta}
            </Badge>
          )}
        </div>
        {c.telefone && (
          <div className="mt-2 text-[11px] text-slate-500 dark:text-slate-400 pl-[52px]">Tel: {c.telefone}</div>
        )}
        <div className="mt-3 pl-[52px] flex items-center gap-1">
          <Button asChild size="sm" variant="ghost" className="h-9 w-9 p-0 text-slate-500 hover:text-blue-600 dark:hover:text-blue-400 transition-colors">
            <Link href={`/clientes/${encodeURIComponent(c.id)}`}>
              <Eye className="h-4 w-4" />
            </Link>
          </Button>
          {canEditClientes && (
            <Button asChild size="sm" variant="ghost" className="h-9 w-9 p-0 text-slate-500 hover:text-emerald-600 dark:hover:text-emerald-400 transition-colors">
              <Link href={`/clientes/${encodeURIComponent(c.id)}/editar`}>
                <Pencil className="h-4 w-4" />
              </Link>
            </Button>
          )}
        </div>
      </div>
    );
  })}
</div>
```

Notas de implementação:
- `canEditClientes` já está declarado no scope da página — reutilizar directamente
- `Badge` já está importado — usar `variant="green"` para "Activo" e `variant="gray"` para outros estados
- `Eye`, `Pencil` já estão importados (verificar imports existentes; se ausentes, adicionar de `lucide-react`)
- `Link` de `next/link` já está importado
- Não eliminar nem alterar o componente `ClienteRow` — mantido para desktop
- A lógica de `estadoConta` pode não ter todos os valores mapeados — usar `"Activo" ? "green" : "gray"` como fallback seguro
  </action>
  <verify>
    <automated>grep -c "md:hidden" web/src/app/\(dashboard\)/clientes/page.tsx</automated>
  </verify>
  <done>grep retorna pelo menos 1. O bloco `md:hidden` existe no ficheiro. O bloco da tabela está envolvido em `hidden md:block`. Lint sem erros.</done>
</task>

<task type="auto" tdd="false">
  <name>Tarefa 2: Visibilidade responsiva em agenda/page.tsx</name>
  <files>web/src/app/(dashboard)/agenda/page.tsx</files>
  <action>
Ler `agenda/page.tsx` para localizar a secção "Próximos Eventos" (ou equivalente) que contém os cards de evento (~linhas 395–450). A agenda não tem tabela — tem uma lista de cards de eventos numa coluna lateral.

Objectivo: a lista de eventos lateral já tem o padrão de card correcto (linhas 409–432 do PATTERNS.md). O que falta é garantir que em mobile toda a área de calendário + lista lateral se adapta correctamente.

Verificar se a secção lateral de eventos tem algum wrapper com `hidden` ou `md:block`. Se não tiver:
- Envolver a lista de cards de eventos com `<div className="md:hidden space-y-3">` para que apareça como lista no topo em mobile (acima do calendário), OU confirmar que a estrutura de layout já torna os eventos visíveis em mobile.

Na prática, a agenda usa provavelmente um layout de duas colunas (`grid grid-cols-1 md:grid-cols-3` ou similar). Verificar o container pai da lista de eventos.

Se o layout for `grid` com colunas fixas em mobile (ex: `grid-cols-3`), ajustar para `grid-cols-1 md:grid-cols-3` para que mobile mostre colunas empilhadas.

Se a lista de eventos já aparece empilhada em mobile por conta do grid responsivo, adicionar apenas `md:hidden` ao título "Próximos Eventos" para evitar duplicação visual (o calendário grid 7-colunas já mostra eventos).

Não alterar o calendário mensal (grid 7 colunas com dias) — está fora de scope.

Garantir que `md:hidden` aparece pelo menos uma vez no ficheiro para a secção de eventos mobile.
  </action>
  <verify>
    <automated>grep -c "md:hidden\|md:block" web/src/app/\(dashboard\)/agenda/page.tsx</automated>
  </verify>
  <done>grep retorna pelo menos 1. A secção de eventos tem a divisão responsiva correcta. Lint sem erros.</done>
</task>

<task type="auto">
  <name>Tarefa 3: Lint e SUMMARY</name>
  <files>.planning/phases/54-listas-e-tabelas/54-02-SUMMARY.md</files>
  <action>
Executar `pnpm --prefix web lint` para confirmar ausência de erros de TypeScript/ESLint.

Criar `.planning/phases/54-listas-e-tabelas/54-02-SUMMARY.md` com o template de SUMMARY. Incluir:
- Ficheiros modificados: `clientes/page.tsx`, `agenda/page.tsx`
- O que foi feito: introdução do padrão `hidden md:block` (tabela) + `md:hidden` (cards) em Clientes; ajuste responsivo em Agenda
- Campos nos cards de Clientes: avatar com iniciais, nome, NIF, telefone, badge estadoConta, botões Ver/Editar
- Padrão estabelecido: `md:hidden divide-y divide-slate-200` para lista de cards inline (sem criar componente separado — YAGNI)
  </action>
  <verify>
    <automated>pnpm --prefix web lint 2>&1 | tail -5</automated>
  </verify>
  <done>lint termina sem erros. SUMMARY.md criado.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → DOM | Alterações puramente de presentação — nenhum dado novo é exposto |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-54-02-SC | Tampering | npm/pip/cargo installs | accept | Nenhuma nova dependência instalada — CSS classes e JSX existentes |
| T-54-02-01 | Information Disclosure | cards mobile clientes | accept | Os cards exibem os mesmos dados que a tabela desktop; a visibilidade é controlada pelo mesmo sistema de permissões (`canEditClientes`) já presente na tabela |

Nenhum trust boundary novo é introduzido.
</threat_model>

<verification>
1. `grep -c "hidden md:block" web/src/app/\(dashboard\)/clientes/page.tsx` retorna `1`
2. `grep -c "md:hidden" web/src/app/\(dashboard\)/clientes/page.tsx` retorna pelo menos `1`
3. `grep -c "md:hidden\|md:block" web/src/app/\(dashboard\)/agenda/page.tsx` retorna pelo menos `1`
4. `pnpm --prefix web lint` sem erros
5. Inspecção visual em 375px: `/clientes` mostra cards; em 1280px mostra tabela
6. Inspecção visual em 375px: `/agenda` mostra eventos empilhados correctamente
</verification>

<success_criteria>
- TAB-01 parcialmente implementado: Clientes com cards mobile funcionais e Agenda com layout responsivo
- Campos essenciais presentes nos cards de Clientes: nome, NIF, telefone, badge estado, Ver/Editar
- Desktop sem regressão visual em ambas as páginas
- Build limpo
</success_criteria>

<output>
Criar `.planning/phases/54-listas-e-tabelas/54-02-SUMMARY.md` quando concluído.
</output>
