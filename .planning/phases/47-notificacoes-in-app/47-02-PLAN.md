---
phase: "47-notificacoes-in-app"
plan: 2
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/components/ui/popover.tsx
  - web/src/hooks/use-eventos.ts
  - web/src/types/eventos.ts
  - web/src/components/shared/notification-bell.tsx
  - web/src/components/shared/dashboard-shell.tsx
autonomous: true
requirements: [AGE-01, AGE-02]

must_haves:
  truths:
    - "Header exibe badge com contagem de eventos próximos; badge oculto quando count=0 ou durante loading"
    - "Clicar no badge abre Popover com lista de até 10 eventos (título, data, tipo)"
    - "Cada item do Popover tem link para /processos/<processoId> quando processoId existe"
    - "Painel tem link 'Ver agenda' para /agenda no rodapé"
    - "Ao concluir evento (useToggleEventoConcluido / useSetEventoConcluido), cache upcoming é invalidado"
    - "pnpm build termina sem erros de TypeScript"
  artifacts:
    - path: "web/src/components/ui/popover.tsx"
      provides: "Componente Popover/PopoverTrigger/PopoverContent (shadcn pattern)"
      contains: "PopoverContent"
    - path: "web/src/hooks/use-eventos.ts"
      provides: "hook useUpcomingEventos()"
      contains: "useUpcomingEventos"
    - path: "web/src/types/eventos.ts"
      provides: "tipo UpcomingEvento"
      contains: "UpcomingEvento"
    - path: "web/src/components/shared/notification-bell.tsx"
      provides: "Componente NotificationBell com badge e Popover"
      contains: "NotificationBell"
    - path: "web/src/components/shared/dashboard-shell.tsx"
      provides: "Integração de NotificationBell no header"
      contains: "NotificationBell"
  key_links:
    - from: "NotificationBell"
      to: "useUpcomingEventos()"
      via: "import hook e consumir data/isLoading"
      pattern: "useUpcomingEventos"
    - from: "useUpcomingEventos()"
      to: "GET /api/v1/eventos/upcoming"
      via: "apiFetch('/eventos/upcoming?days=7')"
      pattern: "eventos.*upcoming"
    - from: "useToggleEventoConcluido / useSetEventoConcluido"
      to: "queryKey: [\"eventos\", \"upcoming\"]"
      via: "queryClient.invalidateQueries na onSuccess"
      pattern: "invalidateQueries.*upcoming"
---

<objective>
Implementar o componente NotificationBell com badge e painel Popover, integrá-lo no dashboard-shell, e adicionar o hook useUpcomingEventos.

Purpose: Dar ao utilizador visibilidade imediata dos eventos próximos sem sair da página atual.
Output: popover.tsx, hook useUpcomingEventos, NotificationBell component, integração no shell.
</objective>

<execution_context>
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.planning/PROJECT.md
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.planning/ROADMAP.md
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.planning/phases/47-notificacoes-in-app/47-CONTEXT.md

<interfaces>
<!-- web/src/types/eventos.ts — tipos existentes -->
export interface Evento {
  id: number; tenantId: string; processoId?: string; tipo?: string;
  titulo: string; descricao?: string; dataInicio: string; dataFim: string;
  prioridade: EventoPrioridade; concluido: boolean;
}

<!-- web/src/hooks/use-eventos.ts — hooks de mutação existentes (para adicionar invalidação) -->
export function useToggleEventoConcluido(id: number)  // onSuccess: invalida ["eventos","list"] e setQueryData
export function useSetEventoConcluido()                // onSuccess: invalida ["eventos","list"] e setQueryData

<!-- web/src/components/shared/dashboard-shell.tsx — ponto de integração linha ~165-169 -->
// Substituir este bloco:
<Button type="button" variant="ghost" className="h-9 w-9 p-0 ...">
  <Bell className="h-[1.1rem] w-[1.1rem]" />
</Button>
// Por: <NotificationBell />

<!-- Imports existentes em dashboard-shell.tsx (linha 8) -->
import { Bell, ... } from "lucide-react";  // Bell já importado
// apiFetch, useQuery NÃO estão em dashboard-shell — ficam no hook e no componente

<!-- Padrão de hook existente (use-eventos.ts) -->
return useQuery({
  queryKey: ["eventos", "list", query],
  queryFn: () => apiFetch<Evento[]>(`/eventos${query}`),
  enabled: typeof window !== "undefined",
  staleTime: 15_000,
});
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Instalar @radix-ui/react-popover e criar popover.tsx + tipo UpcomingEvento</name>
  <files>
    web/src/components/ui/popover.tsx
    web/src/types/eventos.ts
  </files>
  <action>
    1. Instalar a dependência: no diretório `web/`, executar `pnpm add @radix-ui/react-popover`. Confirmar que `web/package.json` e `web/pnpm-lock.yaml` são atualizados.

    2. Criar `web/src/components/ui/popover.tsx` seguindo o padrão shadcn dos outros componentes em `components/ui/`. O ficheiro deve exportar `Popover`, `PopoverTrigger` e `PopoverContent`. Usar `@radix-ui/react-popover` como base. `PopoverContent` deve usar `cn()` de `@/lib/utils` para classes, com `align="end"` como default prop e `sideOffset={8}`. Sem `PopoverArrow` — consistente com o estilo da UI. Estrutura típica shadcn:
       - `const Popover = PopoverPrimitive.Root`
       - `const PopoverTrigger = PopoverPrimitive.Trigger`
       - `const PopoverContent = React.forwardRef<...>` wrapping `PopoverPrimitive.Portal > PopoverPrimitive.Content`

    3. Adicionar o tipo `UpcomingEvento` a `web/src/types/eventos.ts` (append ao fim do ficheiro):
       ```
       export interface UpcomingEvento {
         id: number;
         titulo: string;
         dataInicio: string;
         processoId: string;  // string vazia quando null no backend
         tipo: string;        // string vazia quando null no backend
       }
       ```
  </action>
  <verify>
    <automated>cd C:/Users/francisco.horta/Documents/projects/personal/lexcv/web && pnpm tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>popover.tsx exporta Popover, PopoverTrigger, PopoverContent. UpcomingEvento existe em eventos.ts. TypeScript sem erros nos ficheiros novos.</done>
</task>

<task type="auto">
  <name>Task 2: Hook useUpcomingEventos + NotificationBell + integração no shell</name>
  <files>
    web/src/hooks/use-eventos.ts
    web/src/components/shared/notification-bell.tsx
    web/src/components/shared/dashboard-shell.tsx
  </files>
  <action>
    **A. Atualizar web/src/hooks/use-eventos.ts**

    1. Adicionar import de `UpcomingEvento` ao import de tipos no topo do ficheiro (onde já está `Evento, EventoCreateRequest, EventoUpdateRequest`).

    2. Adicionar o hook `useUpcomingEventos` ao fim do ficheiro (antes do fim, depois do último export):
       - queryKey: `["eventos", "upcoming"]`
       - queryFn: `() => apiFetch<UpcomingEvento[]>("/eventos/upcoming?days=7")`
       - enabled: `typeof window !== "undefined"`
       - staleTime: `60_000`
       - Retornar o resultado do useQuery diretamente.

    3. Nos hooks `useToggleEventoConcluido` e `useSetEventoConcluido`, adicionar na `onSuccess` a invalidação adicional:
       `queryClient.invalidateQueries({ queryKey: ["eventos", "upcoming"] })`
       Adicionar ao `Promise.all` já existente em cada hook — NÃO substituir as invalidações existentes.

    **B. Criar web/src/components/shared/notification-bell.tsx**

    Criar componente `NotificationBell` (export default e named export). Imports necessários:
    - `{ Bell }` de `"lucide-react"`
    - `Link` de `"next/link"`
    - `{ Button }` de `"@/components/ui/button"`
    - `{ Popover, PopoverTrigger, PopoverContent }` de `"@/components/ui/popover"`
    - `{ useUpcomingEventos }` de `"@/hooks/use-eventos"`

    Lógica do componente:
    - Chamar `useUpcomingEventos()`, desestruturar `{ data, isLoading }`.
    - `const count = data?.length ?? 0`
    - `const showBadge = !isLoading && count > 0`

    Renderizar:
    ```
    <Popover>
      <PopoverTrigger asChild>
        <Button type="button" variant="ghost" className="h-9 w-9 p-0 rounded-full text-slate-500 hover:text-slate-900 dark:text-slate-400 dark:hover:text-slate-100 hover:bg-slate-100 dark:hover:bg-slate-800 transition-colors relative">
          <Bell className="h-[1.1rem] w-[1.1rem]" />
          {showBadge && (
            <span className="absolute -top-0.5 -right-0.5 h-4 w-4 rounded-full bg-red-500 text-[10px] font-bold text-white flex items-center justify-center leading-none">
              {count > 9 ? "9+" : count}
            </span>
          )}
        </Button>
      </PopoverTrigger>
      <PopoverContent className="w-80 p-0" align="end">
        <div className="px-4 py-3 border-b border-slate-200 dark:border-slate-800">
          <p className="text-sm font-semibold text-slate-900 dark:text-slate-100">Próximos eventos</p>
        </div>
        {count === 0 ? (
          <p className="px-4 py-6 text-sm text-slate-500 dark:text-slate-400 text-center">Sem eventos nos próximos 7 dias</p>
        ) : (
          <ul className="divide-y divide-slate-100 dark:divide-slate-800 max-h-72 overflow-y-auto">
            {data!.map((ev) => (
              <li key={ev.id} className="px-4 py-3 hover:bg-slate-50 dark:hover:bg-slate-900 transition-colors">
                {ev.processoId ? (
                  <Link href={`/processos/${ev.processoId}`} className="block">
                    <NotificationItem ev={ev} />
                  </Link>
                ) : (
                  <NotificationItem ev={ev} />
                )}
              </li>
            ))}
          </ul>
        )}
        <div className="px-4 py-2 border-t border-slate-200 dark:border-slate-800">
          <Link href="/agenda" className="text-xs text-blue-600 dark:text-blue-400 hover:underline font-medium">
            Ver agenda
          </Link>
        </div>
      </PopoverContent>
    </Popover>
    ```

    Componente auxiliar interno `NotificationItem` (não exportado), recebe `ev: UpcomingEvento`:
    - Linha 1: `<p className="text-sm font-medium text-slate-900 dark:text-slate-100 truncate">{ev.titulo}</p>`
    - Linha 2: data formatada + tipo (se presente): `<p className="text-xs text-slate-500 dark:text-slate-400">{new Date(ev.dataInicio).toLocaleDateString("pt-PT", { day: "2-digit", month: "short" })}{ev.tipo ? ` · ${ev.tipo}` : ""}</p>`

    **C. Atualizar web/src/components/shared/dashboard-shell.tsx**

    1. Adicionar import no topo: `import { NotificationBell } from "@/components/shared/notification-bell";`
    2. Remover o `import { Bell }` de lucide-react se Bell não for mais usado noutro sítio no ficheiro — verificar antes de remover.
    3. Substituir o bloco Button/Bell estático nas linhas ~167-169 pelo componente: `<NotificationBell />`
    4. NÃO alterar mais nada no ficheiro.
  </action>
  <verify>
    <automated>cd C:/Users/francisco.horta/Documents/projects/personal/lexcv/web && pnpm build 2>&1 | tail -20</automated>
  </verify>
  <done>pnpm build termina sem erros. notification-bell.tsx exporta NotificationBell. use-eventos.ts exporta useUpcomingEventos. dashboard-shell.tsx importa e usa NotificationBell.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Descrição |
|----------|-----------|
| browser → /api/v1/eventos/upcoming | Resposta de API não confiável — tipar corretamente |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-47-03 | Information Disclosure | NotificationBell | mitigate | apiFetch usa credentials:"include" (cookie JWT) — sem token exposto em JS |
| T-47-04 | Tampering | Link href para /processos/processoId | mitigate | processoId vem do backend já scoped por tenant; não é input do utilizador |
| T-47-SC | Tampering | pnpm add @radix-ui/react-popover | mitigate | @radix-ui é pacote estabelecido do ecossistema shadcn — verificar npmjs.com/package/@radix-ui/react-popover antes de instalar |
</threat_model>

<verification>
1. `pnpm build` termina com BUILD SUCCESS / compiled successfully.
2. No browser: header exibe Bell; sem eventos próximos — badge não aparece; com eventos — badge mostra contagem.
3. Clicar Bell abre Popover com lista; cada item com processoId tem link /processos/<id>.
4. Rodapé do Popover tem link "Ver agenda" → /agenda.
</verification>

<success_criteria>
pnpm build sem erros TypeScript. NotificationBell renderiza badge apenas quando count > 0. Popover abre/fecha ao clicar. Cache ["eventos","upcoming"] é invalidado quando useToggleEventoConcluido ou useSetEventoConcluido são chamados com sucesso.
</success_criteria>

<output>
Criar `.planning/phases/47-notificacoes-in-app/47-02-SUMMARY.md` ao concluir.
</output>
