---
phase: 76-separadores-dados-contactos-e-notas
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/app/(dashboard)/clientes/[id]/page.tsx
autonomous: true
requirements: [CLI-15, CLI-18, CLI-19]

must_haves:
  truths:
    - "Utilizador vê 7 botões de separador na ficha do cliente, na ordem: Dados, Contactos e Notas, Processos, Pareceres, Documentos Entregues, Documentos a Tratar, Deslocações"
    - "Separador ativo por defeito ao abrir a ficha é 'Dados'"
    - "Utilizador clica em qualquer um dos 5 separadores ainda não implementados e vê um card 'Em breve' (sem spinner, sem chamada de API)"
    - "Separador 'Dados' apresenta NIF, tipo de documento e número de documento agrupados sob a sub-secção 'Identificação' dentro do card 'Dados'"
    - "Separador 'Contactos e Notas' apresenta os cards ClienteContactosCard e ClienteNotasCard, já não visíveis na página principal fora deste separador"
    - "Ao clicar 'Editar' estando noutro separador, a UI muda automaticamente para 'Dados'"
    - "Toggle view/edit (isEditing/editable) de todos os campos e cards movidos permanece inalterado"
  artifacts:
    - path: "web/src/app/(dashboard)/clientes/[id]/page.tsx"
      provides: "Tab shell (7 botões), conteúdo real para 'Dados' e 'Contactos e Notas', placeholder para os outros 5"
      contains: "type TabKey"
  key_links:
    - from: "web/src/app/(dashboard)/clientes/[id]/page.tsx (tab button row)"
      to: "setTab state setter"
      via: "onClick={() => setTab(<key>)}"
      pattern: "setTab\\(\"(dados|contactosNotas|processos|pareceres|documentosEntregues|documentosATratar|deslocacoes)\"\\)"
    - from: "web/src/app/(dashboard)/clientes/[id]/page.tsx (Editar button)"
      to: "tab state"
      via: "setTab(\"dados\") inside the Editar onClick handler"
      pattern: "setIsEditing\\(true\\); setTab\\(\"dados\"\\)"
    - from: "web/src/app/(dashboard)/clientes/[id]/page.tsx (Identificação sub-section)"
      to: "documento_tipo select + Número do Documento input"
      via: "relocated form.register bindings inside 'Dados' card"
      pattern: "form.register\\(\"documento_(tipo|numero)\"\\)"
---

<objective>
Reestruturar a ficha de cliente (`web/src/app/(dashboard)/clientes/[id]/page.tsx`) para organizar a informação em 7 separadores (tab shell estilo botões-toggle de processos), implementando conteúdo real apenas para "Dados" e "Contactos e Notas" e placeholders "Em breve" para os outros 5. A identificação (NIF, tipo de documento, número de documento) passa a viver dentro do card "Dados" numa sub-secção própria "Identificação" (CLI-19). Os cards Contactos e Notas mudam para o seu próprio separador (CLI-18).

Purpose: Fecha CLI-15 (7 separadores), CLI-18 (Contactos e Notas isolados) e CLI-19 (identificação no card Dados) — a fundação de navegação por separadores sobre a qual as Phases 77-79 constroem o conteúdo real dos restantes separadores.
Output: `web/src/app/(dashboard)/clientes/[id]/page.tsx` reestruturado in-place, sem alterar lógica de dados (hooks, mutations, sub-componentes) nem RBAC.
</objective>

<execution_context>
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/LEXCV-76-separadores-—-dados-contactos-e-notas/76-CONTEXT.md
@.planning/phases/LEXCV-76-separadores-—-dados-contactos-e-notas/76-PATTERNS.md
@.planning/phases/LEXCV-76-separadores-—-dados-contactos-e-notas/76-UI-SPEC.md

# Analog reference — copy the tab mechanism verbatim from this file:
@web/src/app/(dashboard)/processos/[id]/page.tsx

<interfaces>
<!-- Contracts the executor needs. Extracted from the target file. Do NOT explore the codebase — use these. -->

From web/src/app/(dashboard)/clientes/[id]/page.tsx (current, post-Phase 75):

ClienteDetailContent state (line ~111):
  const [isEditing, setIsEditing] = React.useState(false);
  const [serverError, setServerError] = React.useState<string | null>(null);
  const [legacyDocumentoTipo, setLegacyDocumentoTipo] = React.useState<string | null>(null);
  const clienteFormSchema = React.useMemo(() => buildClienteFormSchema(legacyDocumentoTipo ?? undefined), [legacyDocumentoTipo]);

Sub-components (defined lower in the same file, UNCHANGED by this phase):
  ClienteContactosCard({ clienteId, canEditClientes, editable, data, isLoading, isError })
  ClienteNotasCard({ clienteId, canEditClientes, editable, data, isLoading, isError })
  ProcuracaoCard({ clienteId, canEditClientes, editable, procuracaoKey })
  ResponsaveisCard({ title, clienteId, canEditClientes, editable, useList, useAdd, useRemove })

Helpers already imported (UNCHANGED):
  getDocumentoTipoOptions(tipo), toDocumentoTipo(value, tipo)  // from @/lib/cliente-documento-tipo (Phase 74)
  Card, CardContent, CardHeader, CardTitle  // from @/components/ui/card (already imported)

From web/src/app/(dashboard)/processos/[id]/page.tsx (analog, tab mechanism):
  type TabKey = "timeline" | "partes" | "fases" | "auditoria";      // line 93
  const [tab, setTab] = React.useState<TabKey>("timeline");          // line 142
  // tab button row: lines 910-941 (<div className="flex flex-wrap gap-2"> with <Button variant={tab === x ? "secondary" : "outline"}>)
  // conditional content chain: lines 943-1440 ({tab === "x" ? (...) : tab === "y" ? (...) : null})
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add 7-tab state machine + shell (button row, conditional chain, placeholder)</name>
  <files>web/src/app/(dashboard)/clientes/[id]/page.tsx</files>
  <read_first>
    - web/src/app/(dashboard)/clientes/[id]/page.tsx (the target file — read the full component, especially lines 69-120 for type/state placement, lines 340-363 for the header Editar button, lines 376-955 for the content region being wrapped)
    - web/src/app/(dashboard)/processos/[id]/page.tsx (analog — lines 93, 142, 910-941, 943-1440 for the exact tab mechanism to replicate)
    - .planning/phases/LEXCV-76-separadores-—-dados-contactos-e-notas/76-PATTERNS.md (sections 1, 2, 3, 5, 6)
    - .planning/phases/LEXCV-76-separadores-—-dados-contactos-e-notas/76-UI-SPEC.md (Interaction Contract sections 1, 4, 5, 6)
  </read_first>
  <action>
    Declare `type TabKey = "dados" | "contactosNotas" | "processos" | "pareceres" | "documentosEntregues" | "documentosATratar" | "deslocacoes"` after the existing `type PageProps` block (line 69-71) and before the `ClienteDetailPage` component. Initialize `const [tab, setTab] = React.useState<TabKey>("dados")` inside `ClienteDetailContent`, next to the existing `const [isEditing, setIsEditing]` at line ~111.

    Insert the tab button row as the first child inside the `cliente.data ? (<div className="space-y-4">` branch (line 377), immediately before the existing `<div className="grid gap-4 lg:grid-cols-2">` at line 378. Wrapper markup MUST be `<div className="overflow-x-auto"><div className="flex gap-2 w-max">` (NOT the analog's `flex flex-wrap` — this is the CONTEXT.md-sanctioned divergence for 7 buttons on mobile). Render exactly 7 `<Button type="button" variant={tab === <key> ? "secondary" : "outline"} onClick={() => setTab(<key>)}>` buttons, all always rendered, none `disabled`, in this exact label order: `Dados`, `Contactos e Notas`, `Processos`, `Pareceres`, `Documentos Entregues`, `Documentos a Tratar`, `Deslocações`.

    Wrap the existing content region (currently lines 378-954: the `grid lg:grid-cols-2` Dados/Conta-corrente/Informações block, the `isEditing ? Intake do Caso Card` block at 664-908, the Contactos/Notas grid at 910-927, and the Procuração/Responsáveis grid at 929-954) in a conditional-chain by tab. Use the same chained-ternary style as the analog (`tab === "dados" ? (...) : tab === "contactosNotas" ? (...) : ... : null`), NOT a switch/lookup. In THIS task, place ALL of the current 378-954 content under the `tab === "dados"` arm as a first pass (Task 2 will split out Contactos/Notas and fold in Identificação). The 5 not-yet-built arms (`processos`, `pareceres`, `documentosEntregues`, `documentosATratar`, `deslocacoes`) each render the identical placeholder card.

    Placeholder card markup (inline the same JSX in each of the 5 arms, or extract one local `PlaceholderEmBreve` component — executor's discretion): a `<Card>` containing `<CardContent className="py-12 text-center">` with `<p className="text-sm font-medium text-neutral-500 dark:text-neutral-400">Em breve</p>` and `<p className="text-xs text-neutral-400 dark:text-neutral-500 mt-1">Esta funcionalidade estará disponível brevemente.</p>`. No `CardHeader`, no `CardTitle`, no spinner, no `isLoading`/`isError`, no API call. Use `neutral-*` tokens (this file's convention), NOT the analog's `slate-*`.

    Add `setTab("dados")` to the existing Editar-button handler at line 359: change `onClick={() => setIsEditing(true)}` to `onClick={() => { setIsEditing(true); setTab("dados"); }}`. Do NOT touch the `Cancelar`/`Guardar` handlers (`onCancel`, `form.handleSubmit(onSubmit)`) — they must NOT force a tab switch.
  </action>
  <acceptance_criteria>
    - `grep -n 'type TabKey' web/src/app/(dashboard)/clientes/[id]/page.tsx` finds the 7-key union type.
    - `grep -n 'useState<TabKey>("dados")' web/src/app/(dashboard)/clientes/[id]/page.tsx` confirms default tab is "dados".
    - `grep -c 'setTab(' web/src/app/(dashboard)/clientes/[id]/page.tsx` returns >= 8 (7 tab buttons + Editar handler).
    - `grep -n 'overflow-x-auto' web/src/app/(dashboard)/clientes/[id]/page.tsx` confirms the scroll wrapper on the tab row; the file does NOT use `flex flex-wrap` for the tab row.
    - `grep -c 'Em breve' web/src/app/(dashboard)/clientes/[id]/page.tsx` returns >= 1 (placeholder present).
    - `grep -n 'setIsEditing(true); setTab("dados")' web/src/app/(dashboard)/clientes/[id]/page.tsx` confirms Editar triggers tab switch.
    - Behavior: all 7 tab labels (`Dados`, `Contactos e Notas`, `Processos`, `Pareceres`, `Documentos Entregues`, `Documentos a Tratar`, `Deslocações`) render; clicking a not-built tab shows "Em breve"; no `disabled` attribute on any tab button.
    - `cd web && pnpm build` succeeds (no TypeScript/JSX errors from the restructure).
  </acceptance_criteria>
  <verify>
    <automated>cd web && pnpm lint && pnpm build</automated>
  </verify>
  <done>The 7-tab shell renders with "Dados" active by default; the 5 not-yet-built tabs show the "Em breve" placeholder; clicking "Editar" from any tab switches to "Dados"; all existing content still renders (currently all under the "dados" arm); `pnpm build` passes.</done>
</task>

<task type="auto">
  <name>Task 2: Split Contactos e Notas into own tab + fold Identificação into Dados card</name>
  <files>web/src/app/(dashboard)/clientes/[id]/page.tsx</files>
  <read_first>
    - web/src/app/(dashboard)/clientes/[id]/page.tsx (post-Task-1 state — read lines 376-960 to locate the "dados" arm, the Dados card at ~378-523, the Informações Adicionais card at ~546-660, the Contactos/Notas grid at ~910-927)
    - .planning/phases/LEXCV-76-separadores-—-dados-contactos-e-notas/76-PATTERNS.md (sections 4, 7)
    - .planning/phases/LEXCV-76-separadores-—-dados-contactos-e-notas/76-UI-SPEC.md (Interaction Contract sections 2, 3; Copywriting Contract "Identificação" and "Intake do Caso" rows)
  </read_first>
  <action>
    Move the Contactos/Notas grid (the `<div className="grid gap-4 lg:grid-cols-2">` containing `<ClienteContactosCard .../>` and `<ClienteNotasCard .../>`, currently lines 910-927) OUT of the `tab === "dados"` arm and INTO the `tab === "contactosNotas"` arm of the conditional chain. Copy the JSX verbatim — same props, same `editable={isEditing}` wiring, no markup changes (CLI-18). After the move, the "dados" arm no longer renders these two cards.

    Fold identification into the "Dados" card (CLI-19). Create a new "Identificação" sub-section INSIDE the Dados card's `<CardContent>` (the card with `<CardTitle>Dados</CardTitle>` at line 381), as a distinct visually-separated block: `<div className="pt-4 border-t border-neutral-200 dark:border-neutral-800 space-y-4">` with heading `<h4 className="text-sm font-medium text-neutral-700 dark:text-neutral-300">Identificação</h4>`. This matches the existing "Honorários Propostos" sub-heading style already in this file. Add this sub-section for BOTH the `isEditing` (edit) and non-editing (view) branches of the Dados card.

    Into the Identificação sub-section, RELOCATE (do not duplicate — move from their current location):
    - NIF field. In edit mode: the `<Label htmlFor="nif">NIF</Label>` + `<Input id="nif" ... {...form.register("nif")} />` block currently at lines 394-402 moves down into the Identificação sub-section. In view mode: the `<dt>NIF</dt><dd>{cliente.data.nif ?? "—"}</dd>` pair currently at lines 497-498 moves into the Identificação sub-section.
    - Tipo de Documento + Número do Documento. In edit mode: the entire `<select id="documento_tipo">` block INCLUDING the `legacyDocumentoTipo` amber-warning paragraph (lines 569-581) and the `<Input id="documento_numero">` block — currently "Coluna 1" of the Informações Adicionais 2-col grid at lines 554-599 — moves verbatim into the Identificação sub-section. In view mode: the `<dt>Tipo de Documento</dt><dd>...</dd>` and `<dt>Número do Documento</dt><dd>...</dd>` pairs currently at lines 640-644 move into the Identificação sub-section.

    CRITICAL — do NOT alter the legacy-documento_tipo logic: the `legacyDocumentoTipo` state, `getDocumentoTipoOptions(form.watch("tipo"))`, `toDocumentoTipo`, the amber banner, and all `form.register("documento_tipo")` / `form.register("documento_numero")` / `form.register("nif")` bindings move UNCHANGED — only their JSX location changes. These are Phase 74 deliverables.

    After removing documento_tipo/documento_numero from the "Informações Adicionais" card, that card retains ONLY Ramo de Atividade + Detalhes Adicionais. Drop the now-empty "Coluna 1" and the `grid grid-cols-1 md:grid-cols-2` 2-column wrapper (lines 553-636) since only one column of fields remains — keep Ramo de Atividade + Detalhes Adicionais as a single-column `space-y-4` block. Apply the same reduction to the view-mode `dl` (remove the relocated `dt`/`dd` pairs, keep Ramo de Atividade + Detalhes Adicionais).

    Do NOT move: Intake do Caso card, Procuração card, Advogados/Administrativos (ResponsaveisCard) — these stay in the "dados" arm (no dedicated tab among the 7). Do NOT change any `isEditing`/`editable`/`canEditClientes && editable` gating.
  </action>
  <acceptance_criteria>
    - `grep -n 'tab === "contactosNotas"' web/src/app/(dashboard)/clientes/[id]/page.tsx` shows the arm rendering ClienteContactosCard + ClienteNotasCard; those two components appear exactly once in the file (moved, not duplicated).
    - `grep -c 'ClienteContactosCard' web/src/app/(dashboard)/clientes/[id]/page.tsx` — the JSX usage `<ClienteContactosCard` appears exactly once (function definition + one usage = 2 total matches for the identifier).
    - `grep -n 'Identificação' web/src/app/(dashboard)/clientes/[id]/page.tsx` finds the new sub-section heading inside the Dados card.
    - `grep -c 'form.register("nif")' web/src/app/(dashboard)/clientes/[id]/page.tsx` returns 1 (relocated, not duplicated).
    - `grep -c 'form.register("documento_tipo")' web/src/app/(dashboard)/clientes/[id]/page.tsx` returns 1; `grep -c 'form.register("documento_numero")' web/src/app/(dashboard)/clientes/[id]/page.tsx` returns 1 (relocated, not duplicated).
    - `grep -c 'legacyDocumentoTipo' web/src/app/(dashboard)/clientes/[id]/page.tsx` count is unchanged from before the edit (legacy logic preserved intact).
    - Behavior: NIF, Tipo de Documento, Número do Documento all appear under the "Identificação" heading inside the Dados card, in both view and edit mode; the "Informações Adicionais" card shows only Ramo de Atividade + Detalhes Adicionais; Contactos and Notas appear only under the "Contactos e Notas" tab.
    - `cd web && pnpm build` succeeds; no field/data is dropped (all fields still rendered somewhere).
  </acceptance_criteria>
  <verify>
    <automated>cd web && pnpm lint && pnpm build</automated>
  </verify>
  <done>Contactos e Notas render only under the "Contactos e Notas" tab; NIF + Tipo de Documento + Número do Documento are grouped under an "Identificação" sub-section inside the "Dados" card (view + edit); "Informações Adicionais" retains only Ramo de Atividade + Detalhes Adicionais; legacy documento_tipo logic and all view/edit gating are unchanged; `pnpm build` passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → Next.js page (client component) | User navigates tabs and toggles view/edit; all rendering is client-side over already-fetched `useCliente`/`useClienteContactos`/`useClienteNotas` data. No new data enters here. |
| existing mutations → backend API | `useUpdateCliente` and the Contactos/Notas/Procuração/Responsáveis mutations cross to the backend; UNCHANGED by this phase (pure JSX relocation), each already gated by `@PreAuthorize('clientes:*')` server-side and `canEditClientes && editable` client-side. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-76-01 | Information Disclosure | Fields relocated across tabs (NIF/documento_tipo/documento_numero into "Identificação"; Contactos/Notas into own tab) | mitigate | Acceptance criteria for Task 2 assert each relocated field appears exactly once (grep counts = 1) and is still rendered — guarantees no field is accidentally hidden or exposed to a state where it bypasses the existing `isEditing`/`canEditClientes` gate. No `@PreAuthorize` or RBAC change. |
| T-76-02 | Elevation of Privilege | view/edit gating (`canEditClientes && editable`) on moved cards/fields | mitigate | Explicit instruction: do NOT alter `isEditing`/`editable`/`canEditClientes && editable` logic; only JSX location changes. Task 2 acceptance criteria include "all view/edit gating unchanged". RBAC (`clientes:view`/`clientes:edit`) is enforced server-side per endpoint and unchanged. |
| T-76-03 | Tampering | `legacyDocumentoTipo` / `buildClienteFormSchema` / documento_tipo validation (Phase 74) | mitigate | "Do NOT alter the legacy-documento_tipo logic" instruction; acceptance criterion asserts `legacyDocumentoTipo` occurrence count unchanged and `form.register` bindings relocated verbatim — the Phase 74 per-tipo validation and legacy-value exemption stay intact, preventing regression of the documento_tipo integrity control. |
| T-76-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase (pure JSX reorganization within one existing file; zero new dependencies). Package Legitimacy Gate not triggered. |

No high-severity threats. All threats are contained to a single already-authorized page with no new trust boundary, no new data ingress, and no RBAC/authorization change (per planning context: "narrow threat surface — mostly ensuring no field/data becomes accidentally exposed or hidden across tab moves"). The relocation-integrity acceptance criteria (exact grep counts of 1 for each moved binding) are the primary mitigation.
</threat_model>

<verification>
Phase-level checks after both tasks complete:
- `cd web && pnpm lint && pnpm build` both pass.
- Tab shell: 7 buttons render in order (Dados, Contactos e Notas, Processos, Pareceres, Documentos Entregues, Documentos a Tratar, Deslocações); "Dados" active by default; none disabled.
- 5 not-built tabs each show the "Em breve" placeholder card (no spinner, no API call).
- "Identificação" sub-section inside the "Dados" card contains NIF + Tipo de Documento + Número do Documento in both view and edit mode.
- "Informações Adicionais" card shows only Ramo de Atividade + Detalhes Adicionais.
- "Contactos e Notas" tab shows ClienteContactosCard + ClienteNotasCard; they do not render on any other tab.
- Clicking "Editar" from a non-Dados tab switches to "Dados"; Cancelar/Guardar do not force a tab switch.
- No change to hooks, mutations, sub-component definitions, or RBAC.
</verification>

<success_criteria>
- CLI-15: Utilizador vê 7 separadores na ficha do cliente, na ordem definida.
- CLI-18: Separador "Contactos e Notas" apresenta os cards de Contactos e Notas, isolados no seu separador.
- CLI-19: Card "Dados" inclui identificação (NIF, tipo de documento, número de documento) como sub-secção "Identificação", respeitando o toggle view/edit.
- Nenhuma funcionalidade existente (isEditing/editable/mutations/legacy documento_tipo) quebra.
- `pnpm build` e `pnpm lint` passam.
</success_criteria>

<output>
Create `.planning/phases/LEXCV-76-separadores-—-dados-contactos-e-notas/76-01-SUMMARY.md` when done.
</output>
