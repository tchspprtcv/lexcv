---
phase: 120-frontend-consola-de-administra-o-de-tenants
plan: 02
type: execute
wave: 2
depends_on: [120-01]
files_modified:
  - backend/src/main/java/com/lexcv/dtos/TenantAdminSummaryResponse.java
  - backend/src/main/java/com/lexcv/dtos/TenantUpdateRequest.java
  - backend/src/main/java/com/lexcv/config/GlobalExceptionHandler.java
  - backend/src/main/java/com/lexcv/controllers/PlatformAdminController.java
  - backend/src/test/java/com/lexcv/controllers/PlatformAdminControllerTest.java
autonomous: true
requirements: [PROV-03, PROV-04, PROV-05]

must_haves:
  truths:
    - "GET /api/v1/platform/tenants devolve todos os tenants com nome, plano, limite e o numero de utilizadores ativos de cada um"
    - "PUT /api/v1/platform/tenants/{id} ajusta plano e limiteUtilizadores de qualquer tenant, aceitando null como sem limite"
    - "PATCH /api/v1/platform/tenants/{id}/ativo alterna o estado suspenso/ativo de um tenant"
    - "Suspender a tenant reservada LexCV e recusado com 400 e mensagem explicita — o unico PLATAFORMA_ADMIN nunca se pode trancar fora"
    - "Um ADMIN de um tenant normal recebe AccessDeniedException (403) em cada um dos 3 endpoints novos, tal como ja acontece no createTenant da Phase 119"
    - "A contagem de utilizadores ativos usa UserRepository.countByTenantIdAndAtivoTrue, sem nenhuma segunda implementacao"
  artifacts:
    - path: "backend/src/main/java/com/lexcv/dtos/TenantAdminSummaryResponse.java"
      provides: "Projecao minima por tenant para a consola (id, nome, plano, limiteUtilizadores, ativo, utilizadoresAtivos)"
      contains: "utilizadoresAtivos"
    - path: "backend/src/main/java/com/lexcv/dtos/TenantUpdateRequest.java"
      provides: "Corpo tipado do PUT, com TenantPlano tipado para o Jackson validar o enum"
      contains: "TenantPlano plano"
    - path: "backend/src/main/java/com/lexcv/controllers/PlatformAdminController.java"
      provides: "3 endpoints novos sob o gate de classe hasRole PLATAFORMA_ADMIN ja existente"
      contains: "PatchMapping"
  key_links:
    - from: "backend/src/main/java/com/lexcv/controllers/PlatformAdminController.java"
      to: "UserRepository.countByTenantIdAndAtivoTrue"
      via: "preenchimento de utilizadoresAtivos por tenant na listagem"
      pattern: "countByTenantIdAndAtivoTrue"
    - from: "backend/src/main/java/com/lexcv/controllers/PlatformAdminController.java"
      to: "Tenant.ativo (Plan 01)"
      via: "PATCH /tenants/{id}/ativo com guarda da tenant reservada"
      pattern: "setAtivo"
---

<objective>
Acrescentar a `PlatformAdminController` os 3 endpoints que a consola da Phase 120 consome: listar todos os tenants com utilizacao, ajustar plano/limite, e alternar suspenso/ativo — todos sob o gate de classe `hasRole('PLATAFORMA_ADMIN')` que a Phase 119 ja instalou.

Purpose: a Phase 119 entregou deliberadamente so `POST /platform/tenants` (criacao). Os Success Criteria 2, 3 e 4 do ROADMAP para a Phase 120 exigem listagem com utilizacao, edicao de plano/limite e alternancia de estado — capacidade de backend que ainda nao existe. Este e o ultimo plano de backend da fase: a partir daqui o frontend so consome.

Output: 2 DTOs novos, 3 handlers novos, um handler global para corpo JSON invalido, e cobertura de teste que prova tanto o comportamento como o gate de autorizacao de CADA handler novo.
</objective>

<execution_context>
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/LEXCV-120-frontend-consola-de-administra-o-de-tenants/120-CONTEXT.md
@.planning/phases/LEXCV-120-frontend-consola-de-administra-o-de-tenants/120-PATTERNS.md
@.planning/phases/LEXCV-120-frontend-consola-de-administra-o-de-tenants/120-01-SUMMARY.md
</context>

<interfaces>
<!-- Contratos ja confirmados no planeamento. NAO reexplorar o codebase para os redescobrir. -->

`PlatformAdminController.java` (71 linhas, estado pos-Phase-119): `@RestController`, `@RequestMapping("/api/v1/platform")`, `@PreAuthorize("hasRole('PLATAFORMA_ADMIN')")` **ao nivel da CLASSE**, `@RequiredArgsConstructor`, um unico campo `private final SetupService setupService;` e um unico handler `@PostMapping("/tenants") createTenant(@RequestBody SetupInitializeRequest)` com 3 catch (`IllegalArgumentException` -> 400, `IllegalStateException` -> 403, `DataIntegrityViolationException` -> 400). O gate de classe cobre automaticamente qualquer handler novo. O Javadoc de classe afirma explicitamente que este controller **nunca le `SecurityContextHolder`/`UserPrincipal`** — manter essa propriedade.

`UserRepository.countByTenantIdAndAtivoTrue(UUID tenantId)` devolve `long`. Doc-comment proprio: "UNICA fonte de verdade para utilizadores ativos de um tenant no codebase … as Phases 120 e 122 reutilizam este mesmo metodo — nao duplicar esta contagem noutro sitio."

`TenantRepository extends JpaRepository<Tenant, UUID>` — `findAll()`, `findById(UUID)`, `save(Tenant)`, mais `findFirstByNome(String)`.

`TenantPlano` (`backend/src/main/java/com/lexcv/models/TenantPlano.java`) — enum com exatamente 3 constantes: `STARTER`, `STANDARD`, `ENTERPRISE`.

`Tenant` apos o Plan 01 — campos: `id`, `nome`, `nif`, `tipoEntidade`, `email`, `telefone`, `logoDataUrl` (`@Lob`), `plano` (`TenantPlano`), `limiteUtilizadores` (`Integer`, `null` = sem limite), **`ativo` (`Boolean`, NOT NULL default true)**, `createdAt`.

`SetupInitializeRequest.java` (14 linhas) — o UNICO DTO de request tipado pre-existente no codebase; estilo `@Getter @Setter` simples, sem `@Builder`.

`TenantProvisionResponse.java` — DTO de resposta com `@Data @Builder @NoArgsConstructor @AllArgsConstructor` e doc-comment a dizer "nunca serializar a entidade Tenant diretamente".

`GlobalExceptionHandler.java` (`@RestControllerAdvice`) — handlers atuais: `MethodArgumentNotValidException` (400), `ConstraintViolationException` (400), `AccessDeniedException` (403, acrescentado pela Phase 119 com doc-comment a justificar o efeito global deliberado) e um catch-all `Exception` (500, corpo `{error, message}`). **Nao existe handler para `HttpMessageNotReadableException`** — hoje um enum invalido no corpo JSON cai no catch-all e devolve 500.

Guardas de rejeicao — precedentes exatos a copiar:
- `AdminController.deleteUser` (linhas 328-336): rejeita um alvo reservado com `ResponseEntity.badRequest().body(Map.of("message", "…"))` antes de qualquer escrita.
- `AdminController.updateUser` (linhas 250-269): valida um booleano vindo de `Map<String,Object>` com `if (!(body.get("ativo") instanceof Boolean novoAtivo)) { return 400 … }` e mensagem `"O campo ativo deve ser um valor booleano."`.

Convencao de verbos observada neste codebase: `@PatchMapping` para sub-recursos de estado estreitos e de proposito unico (`NotificacaoController` `/{id}/lida`, `/{id}/snooze`; `ResourceController` `/prazos/{prazoId}/concluido`); `@PutMapping` para atualizacao de campos do recurso inteiro (`AdminController` `/users/{id}`, `/rbac`).

Convencao de teste: `PlatformAdminControllerTest` ja existe (237 linhas, 9 testes) com dois grupos — Grupo A (instanciacao direta, sem proxy) e Grupo B (proxy CGLIB `ProxyFactory` com `setProxyTargetClass(true)` + `AuthorizationManagerBeforeMethodInterceptor.preAuthorize()` para avaliar `@PreAuthorize` a serio). Helpers privados ja disponiveis: `novoController()`, `novoProxyComMethodSecurity()`, `autenticarComoRoles(String...)`, `autenticarComoPlataformaAdmin(UUID)`, `pedidoValido()`, mais `@AfterEach limparSecurityContext()`. Nao existe MockMvc nem `@SpringBootTest` neste projeto.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: DTOs de contrato e handler global para corpo JSON invalido</name>

  <files>
    backend/src/main/java/com/lexcv/dtos/TenantAdminSummaryResponse.java (novo)
    backend/src/main/java/com/lexcv/dtos/TenantUpdateRequest.java (novo)
    backend/src/main/java/com/lexcv/config/GlobalExceptionHandler.java
  </files>

  <read_first>
    - `backend/src/main/java/com/lexcv/dtos/TenantProvisionResponse.java` (ficheiro completo, 27 linhas) — as anotacoes Lombok e a convencao de doc-comment "nunca a entidade crua" a replicar.
    - `backend/src/main/java/com/lexcv/dtos/UserSummaryResponse.java` (ficheiro completo, 26 linhas) — como este codebase documenta o que uma projecao minima EXCLUI deliberadamente.
    - `backend/src/main/java/com/lexcv/dtos/SetupInitializeRequest.java` (ficheiro completo, 14 linhas) — o estilo `@Getter @Setter` de um DTO de request.
    - `backend/src/main/java/com/lexcv/config/GlobalExceptionHandler.java` (ficheiro completo) — o doc-comment do handler de `AccessDeniedException` da Phase 119 e o modelo de como este projeto justifica um handler de efeito global.
  </read_first>

  <action>
    Criar `TenantAdminSummaryResponse` no package `com.lexcv.dtos`, com `@Data @Builder @NoArgsConstructor @AllArgsConstructor` (as mesmas 4 anotacoes de `TenantProvisionResponse`) e exatamente 6 campos: `UUID id`, `String nome`, `TenantPlano plano`, `Integer limiteUtilizadores`, `Boolean ativo`, `long utilizadoresAtivos`. Escrever um doc-comment de classe a registar: (a) que e a projecao consumida pela consola da Phase 120 e reutilizavel pelo relatorio da Phase 122; (b) que `limiteUtilizadores` a `null` significa "sem limite", semantica identica a de `Tenant.limiteUtilizadores`; (c) que a entidade `Tenant` nunca e serializada diretamente e que os campos `logoDataUrl` (`@Lob`, potencialmente megabytes de base64), `nif`, `email`, `telefone` e `createdAt` sao deliberadamente omitidos — a consola nao os mostra e transporta-los seria peso e superficie de exposicao gratuitos.

    Criar `TenantUpdateRequest` no mesmo package, estilo `SetupInitializeRequest` (`@Getter @Setter`, sem `@Builder`), com exatamente 2 campos: `TenantPlano plano` e `Integer limiteUtilizadores`. O campo `plano` e deliberadamente do tipo do enum, nao `String` — e isso que faz o Jackson recusar um valor invalido antes de o handler correr. NAO acrescentar `ativo` aqui: a alternancia de estado tem endpoint proprio (Task 2), e misturar as duas contraria a decisao explicita do UI-SPEC ("Never fold the suspend/reactivate toggle into this Dialog").

    Acrescentar a `GlobalExceptionHandler` um `@ExceptionHandler(HttpMessageNotReadableException.class)` (import `org.springframework.http.converter.HttpMessageNotReadableException`) que devolve `400` com corpo `Map.of("message", "Corpo do pedido inválido.")` e faz `logger.warn` com a excecao. Documentar em doc-comment, no mesmo estilo do handler de `AccessDeniedException` da Phase 119: sem este handler, um `plano` invalido no corpo do novo `PUT /platform/tenants/{id}` — ou qualquer JSON malformado em qualquer endpoint deste backend — cai no catch-all `Exception` e devolve `500` com o nome da classe da excecao no corpo; com ele, devolve `400` com a mesma forma `{"message": …}` que todos os outros endpoints usam, e deixa de ecoar detalhes internos de desserializacao ao cliente. Registar explicitamente que o efeito e global e deliberado — corrige um comportamento errado pre-existente, tal como o handler de `AccessDeniedException` corrigiu o 500-em-vez-de-403.

    Proibicoes: NAO usar `String` para `plano` em nenhum dos dois DTOs. NAO acrescentar `logoDataUrl` a `TenantAdminSummaryResponse`. NAO alterar nem remover nenhum handler existente de `GlobalExceptionHandler` — apenas acrescentar um.
  </action>

  <verify>
    <automated>cd backend && mvn -q -DskipTests compile && grep -q 'utilizadoresAtivos' src/main/java/com/lexcv/dtos/TenantAdminSummaryResponse.java && grep -q 'TenantPlano plano' src/main/java/com/lexcv/dtos/TenantUpdateRequest.java && ! grep -q 'logoDataUrl' src/main/java/com/lexcv/dtos/TenantAdminSummaryResponse.java && test $(grep -c '@ExceptionHandler' src/main/java/com/lexcv/config/GlobalExceptionHandler.java) -eq 5</automated>
  </verify>

  <acceptance_criteria>
    - `mvn -DskipTests compile` termina com exit code 0.
    - `TenantAdminSummaryResponse.java` declara exatamente 6 campos privados (`grep -c 'private '` devolve 6) e nenhum deles se chama `logoDataUrl`, `nif`, `email`, `telefone` ou `createdAt`.
    - `TenantAdminSummaryResponse.plano` e `TenantUpdateRequest.plano` sao ambos do tipo `TenantPlano` — `grep -c 'String plano'` nos dois ficheiros devolve 0.
    - `TenantUpdateRequest.java` declara exatamente 2 campos privados e nenhum se chama `ativo`.
    - `GlobalExceptionHandler.java` passa a ter 5 `@ExceptionHandler` (os 4 anteriores mais `HttpMessageNotReadableException`), e os 4 anteriores mantem-se textualmente inalterados — `git diff` do ficheiro mostra apenas linhas adicionadas, zero removidas fora da zona de imports.
    - Ambos os DTOs novos tem doc-comment de classe nao vazio.
  </acceptance_criteria>

  <done>Os contratos de entrada e saida dos 3 endpoints existem e compilam, e um corpo JSON invalido passa a devolver 400 com a forma `{"message": …}` em vez de 500.</done>
</task>

<task type="auto">
  <name>Task 2: 3 endpoints novos em PlatformAdminController com guarda da tenant reservada</name>

  <files>
    backend/src/main/java/com/lexcv/controllers/PlatformAdminController.java
  </files>

  <read_first>
    - `backend/src/main/java/com/lexcv/controllers/PlatformAdminController.java` (ficheiro completo, 71 linhas) — o Javadoc de classe (que afirma "nunca le SecurityContextHolder/UserPrincipal") e a forma do `createTenant` com os seus 3 catch.
    - `backend/src/main/java/com/lexcv/controllers/AdminController.java` linhas 68-97 (`listUsers`) — o padrao "buscar linhas, mapear cada uma para um DTO, `ResponseEntity.ok(responses)`".
    - `backend/src/main/java/com/lexcv/controllers/AdminController.java` linhas 240-280 e 320-340 (`updateUser` e `deleteUser`) — o guard `instanceof Boolean` e o padrao de rejeicao de um alvo reservado com 400 antes de qualquer escrita.
    - `backend/src/main/java/com/lexcv/repositories/UserRepository.java` — o doc-comment de `countByTenantIdAndAtivoTrue` que proibe explicitamente duplicar esta contagem.
    - `.planning/phases/LEXCV-120-frontend-consola-de-administra-o-de-tenants/120-UI-SPEC.md` seccao "Copywriting Contract", linha "Reserved-tenant guard tooltip" — a frase que o backend deve devolver e que o frontend vai espelhar.
  </read_first>

  <action>
    Acrescentar a `PlatformAdminController` os campos injetados `private final TenantRepository tenantRepository;` e `private final UserRepository userRepository;` (o `@RequiredArgsConstructor` gera o construtor de 3 argumentos; nao escrever construtor a mao). Declarar tambem `private static final String TENANT_RESERVADO = "LexCV";` — o literal so pode aparecer uma vez no ficheiro. Extrair um metodo privado `toSummary(Tenant tenant)` que constroi um `TenantAdminSummaryResponse` preenchendo `utilizadoresAtivos` com `userRepository.countByTenantIdAndAtivoTrue(tenant.getId())`, para os 3 handlers partilharem a mesma projecao.

    **Endpoint 1 — `@GetMapping("/tenants") listTenants()`**: `tenantRepository.findAll()`, mapear cada `Tenant` com `toSummary`, devolver `ResponseEntity.ok(lista)`. Sem `SecurityContextHolder`, sem filtro por tenant — este controller e deliberadamente cross-tenant e o gate de classe e que garante quem la chega. Ordenar a lista por `nome` com comparacao case-insensitive, para a consola ter ordem estavel entre chamadas (`findAll()` nao garante ordem nenhuma).

    **Endpoint 2 — `@PutMapping("/tenants/{id}") updateTenant(@PathVariable UUID id, @RequestBody TenantUpdateRequest request)`**: carregar o tenant; se ausente devolver `404` com `Map.of("message", "Tenant não encontrado.")`. Validar `plano` nao-nulo — `400` com `Map.of("message", "O plano é obrigatório.")` (um enum invalido ja e apanhado antes pelo Jackson + handler da Task 1, mas um campo em falta desserializa para `null` e chegaria aqui). Validar `limiteUtilizadores`: `null` e valido e significa "sem limite"; um valor presente tem de ser `>= 1`, caso contrario `400` com `Map.of("message", "O limite de utilizadores deve ser um número inteiro maior ou igual a 1, ou ficar vazio para não aplicar limite.")`. Aplicar `setPlano`/`setLimiteUtilizadores`, gravar com `tenantRepository.save(...)`, e devolver `200` com `toSummary(tenantGravado)` — assim o frontend pode usar a resposta diretamente. NUNCA tocar em `ativo` neste endpoint.

    **Endpoint 3 — `@PatchMapping("/tenants/{id}/ativo") setTenantAtivo(@PathVariable UUID id, @RequestBody Map<String, Object> body)`**: usar `Map` (nao DTO) porque e um sub-recurso de campo unico, exatamente como `AdminController.updateUser` ja faz para o mesmo campo. Validar com `if (!(body.get("ativo") instanceof Boolean novoAtivo))` -> `400` com `Map.of("message", "O campo ativo deve ser um valor booleano.")` (frase byte-identica a de `AdminController.updateUser`). Carregar o tenant; ausente -> `404` como acima. **Guarda da tenant reservada**: se `TENANT_RESERVADO.equals(tenant.getNome())` E `novoAtivo` for `false`, devolver `400` com `Map.of("message", "Não é possível suspender o tenant da plataforma (LexCV).")` — frase exata do UI-SPEC. A guarda so bloqueia a suspensao, nunca a reativacao (reativar uma tenant reservada e inofensivo e nao deve falhar). Caso contrario `setAtivo(novoAtivo)`, gravar, devolver `200` com `toSummary(tenantGravado)`.

    Escrever um Javadoc por handler. No do endpoint 3, registar explicitamente: (a) por que a guarda existe — suspender a tenant "LexCV" tranca fora o unico `PLATAFORMA_ADMIN` e nao ha nenhuma via de recuperacao pela aplicacao, so por SQL manual; (b) que esta e a camada autoritativa e que a desativacao do botao no frontend e apenas espelho de UX; (c) que a guarda usa comparacao literal de `nome` porque `t_tenant.nome` nao tem constraint `unique` (ver `TenantRepository.findFirstByNome`, WR-01 da Phase 119) — um segundo tenant chamado "LexCV" ficaria tambem nao-suspensivel, o que e o lado seguro do erro. No Javadoc de classe existente, actualizar a mencao a Phase 120 para registar que ela acrescentou 3 handlers, todos cobertos pelo mesmo gate de classe.

    Proibicoes: NAO acrescentar `@PreAuthorize` por metodo (o gate de classe ja cobre; duplicar so cria pontos de divergencia). NAO ler `SecurityContextHolder` nem `UserPrincipal` em nenhum handler — quebraria a propriedade documentada no Javadoc de classe e o teste `createTenant_naoLeTenantIdDoSecurityContext…` da Phase 119. NAO aceitar nem interpretar quaisquer campos de papel, permissao ou roles nestes endpoints — nenhuma das 3 assinaturas toca em `Role`/`Permission`, e e isso que garante que esta fase nao abre nenhuma via de escalonamento nova (complemento a contencao que a Phase 119 ja fez no `AdminController`). NAO devolver `Tenant` cru em nenhuma resposta. NAO reimplementar a contagem de utilizadores ativos com `findByTenantId(...).stream().filter(...)`. NAO marcar nenhum handler como `final`, `static` ou `private` — o proxy CGLIB dos testes de gate nao consegue intercetar metodos assim.
  </action>

  <verify>
    <automated>cd backend && mvn -q -DskipTests compile && grep -q 'GetMapping("/tenants")' src/main/java/com/lexcv/controllers/PlatformAdminController.java && grep -q 'PutMapping("/tenants/{id}")' src/main/java/com/lexcv/controllers/PlatformAdminController.java && grep -q 'PatchMapping("/tenants/{id}/ativo")' src/main/java/com/lexcv/controllers/PlatformAdminController.java && grep -q 'countByTenantIdAndAtivoTrue' src/main/java/com/lexcv/controllers/PlatformAdminController.java && test $(grep -c 'SecurityContextHolder' src/main/java/com/lexcv/controllers/PlatformAdminController.java) -eq 0 && test $(grep -c '@PreAuthorize' src/main/java/com/lexcv/controllers/PlatformAdminController.java) -eq 1</automated>
  </verify>

  <acceptance_criteria>
    - `mvn -DskipTests compile` termina com exit code 0.
    - O ficheiro tem exatamente 4 handlers (`grep -cE '@(Get|Post|Put|Patch|Delete)Mapping'` devolve 4) e exatamente 1 `@PreAuthorize` (o de classe).
    - `grep -c 'SecurityContextHolder'` e `grep -c 'UserPrincipal'` devolvem ambos 0.
    - `grep -c 'countByTenantIdAndAtivoTrue'` devolve pelo menos 1; `grep -c 'findByTenantId('` devolve 0 — nenhuma contagem paralela.
    - `grep -c '"LexCV"'` devolve 1 — o literal aparece so na constante `TENANT_RESERVADO`.
    - Nenhum handler declara parametro de tipo `Role` ou `Permission`: `grep -nE 'Role |Permission ' src/main/java/com/lexcv/controllers/PlatformAdminController.java` nao devolve nenhuma linha fora de comentarios.
    - A frase `Não é possível suspender o tenant da plataforma (LexCV).` aparece exatamente uma vez e e byte-identica a registada em `120-UI-SPEC.md`.
    - Nenhum handler devolve um objeto do tipo `Tenant` — todas as respostas de sucesso sao `TenantAdminSummaryResponse` ou `List<TenantAdminSummaryResponse>`.
  </acceptance_criteria>

  <done>Os 3 endpoints existem, compilam, reutilizam a contagem unica da Phase 117, protegem a tenant reservada contra suspensao, e nao introduzem nenhuma superficie de papeis/permissoes.</done>
</task>

<task type="auto">
  <name>Task 3: Cobertura de teste dos 3 endpoints, incluindo o gate de autorizacao de cada um</name>

  <files>
    backend/src/test/java/com/lexcv/controllers/PlatformAdminControllerTest.java
  </files>

  <read_first>
    - `backend/src/test/java/com/lexcv/controllers/PlatformAdminControllerTest.java` (ficheiro completo, 237 linhas) — o Javadoc de classe que explica os Grupos A e B, os helpers privados existentes e o `@AfterEach`. Os helpers `novoController()`/`novoProxyComMethodSecurity()` vao precisar de passar os 2 mocks novos ao construtor.
    - `backend/src/main/java/com/lexcv/controllers/PlatformAdminController.java` — a implementacao acabada de escrever na Task 2, para as assercoes baterem certo com as mensagens exatas.
    - `.planning/phases/LEXCV-119-backend-papel-de-administrador-de-plataforma-e-provisionamen/119-04-SUMMARY.md` — o que a Phase 119 ja provou sobre o gate, para nao duplicar prova e para reutilizar a mesma tecnica de proxy.
  </read_first>

  <action>
    Estender `PlatformAdminControllerTest` (nao criar ficheiro novo — o gate e a montagem do proxy sao os mesmos e devem viver juntos). Acrescentar `@Mock TenantRepository tenantRepository;` e `@Mock UserRepository userRepository;` e actualizar `novoController()` para os passar ao construtor de 3 argumentos.

    Grupo A (comportamento, instanciacao directa) — acrescentar os casos:
    1. `listTenants` devolve `200` e uma lista com um `TenantAdminSummaryResponse` por tenant, cada um com `utilizadoresAtivos` igual ao valor que `countByTenantIdAndAtivoTrue` devolve para o SEU proprio id. Usar 2 tenants com contagens diferentes, para provar que nao ha troca de valores entre linhas.
    2. `listTenants` nunca devolve entidades cruas — afirmar que nenhum elemento do corpo e `instanceof Tenant`.
    3. `listTenants` devolve a lista ordenada por `nome` mesmo quando `findAll()` devolve fora de ordem.
    4. `updateTenant` com `plano=STANDARD` e `limiteUtilizadores=10` devolve `200`, chama `save` uma vez, e o `Tenant` gravado (capturado com `ArgumentCaptor`) tem os dois valores novos e o `ativo` **inalterado**.
    5. `updateTenant` com `limiteUtilizadores=null` devolve `200` e grava `null` — "sem limite" e um valor legitimo, nao um erro.
    6. `updateTenant` com `limiteUtilizadores=0` devolve `400` e nunca chama `save`.
    7. `updateTenant` com `plano=null` devolve `400` e nunca chama `save`.
    8. `updateTenant` com id inexistente devolve `404`.
    9. `setTenantAtivo` com `{"ativo": false}` sobre um tenant normal devolve `200` e grava `ativo=false`.
    10. `setTenantAtivo` com `{"ativo": false}` sobre o tenant chamado `LexCV` devolve `400` com a mensagem exacta `Não é possível suspender o tenant da plataforma (LexCV).` e **nunca** chama `save`.
    11. `setTenantAtivo` com `{"ativo": true}` sobre o tenant chamado `LexCV` devolve `200` e grava — a guarda so bloqueia a suspensao.
    12. `setTenantAtivo` com `{"ativo": "sim"}` (String, nao Boolean) devolve `400` e nunca chama `save`.
    13. `setTenantAtivo` com id inexistente devolve `404`.

    Grupo B (gate de autorizacao real, via `novoProxyComMethodSecurity()`) — acrescentar, para CADA um dos 3 handlers novos, um caso que autentica com `autenticarComoRoles("ADMIN")` e afirma `assertThrows(AccessDeniedException.class, …)`, verificando adicionalmente que nem `tenantRepository` nem `userRepository` foram tocados (`verify(tenantRepository, never()).findAll()` e equivalentes). Sao 3 casos, e sao o ponto que impede esta fase de introduzir uma superficie nova alcancavel por um ADMIN de escritorio: o gate de classe cobre-os por heranca, mas so um teste por handler prova que nenhum ficou acidentalmente fora do proxy (por exemplo por se ter tornado `final`, `static` ou `private`).

    Acrescentar ainda um caso de contencao estrutural: afirmar por reflexao sobre `PlatformAdminController.class.getDeclaredMethods()` que nenhum metodo publico declara um parametro cujo tipo seja `com.lexcv.models.Role` ou `com.lexcv.models.Permission` — a prova de que esta fase nao acrescenta nenhuma via de escalonamento de papeis, complementando o que `AdminControllerPlataformaAdminContencaoTest` ja garante do lado do `AdminController`.

    Actualizar o Javadoc de classe do ficheiro de teste para registar que os Grupos A e B passaram a cobrir 4 handlers em vez de 1.

    Proibicoes: NAO introduzir MockMvc nem `@SpringBootTest`. NAO alterar nem apagar nenhum dos 9 testes existentes — so o helper `novoController()` muda, por causa da assinatura do construtor. NAO usar `lenient()` para calar stubs nao usados; ajustar os stubs ao caso concreto.
  </action>

  <verify>
    <automated>cd backend && mvn -q test -Dtest=PlatformAdminControllerTest && test $(grep -c '@Test' src/test/java/com/lexcv/controllers/PlatformAdminControllerTest.java) -ge 26 && test $(grep -c 'AccessDeniedException.class' src/test/java/com/lexcv/controllers/PlatformAdminControllerTest.java) -ge 4</automated>
  </verify>

  <acceptance_criteria>
    - `mvn test -Dtest=PlatformAdminControllerTest` corre pelo menos 26 testes (9 pre-existentes + 13 de comportamento + 3 de gate + 1 de contencao), 0 falhas, 0 erros.
    - Existem pelo menos 4 `assertThrows(AccessDeniedException.class, …)` — um por handler do controller.
    - Os 9 nomes de metodo de teste que ja existiam continuam presentes textualmente no ficheiro.
    - O caso 10 afirma a mensagem `Não é possível suspender o tenant da plataforma (LexCV).` por igualdade exacta de String, nao por `contains`.
    - O caso 4 usa `ArgumentCaptor<Tenant>` e afirma que o `ativo` do tenant capturado e igual ao valor que tinha antes da chamada.
    - `grep -c '@SpringBootTest\|MockMvc\|lenient()' src/test/java/com/lexcv/controllers/PlatformAdminControllerTest.java` devolve 0.
    - `mvn test` completo (suite inteira do backend) continua verde.
    - `mvn spotbugs:check` termina com exit code 0.
  </acceptance_criteria>

  <done>Os 3 endpoints novos tem prova de comportamento e prova de gate individual, e a ausencia de superficie de papeis/permissoes esta afirmada estruturalmente.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Utilizador autenticado (qualquer papel) -> `/api/v1/platform/**` | Fronteira de autorizacao: so `PLATAFORMA_ADMIN` pode atravessar; um ADMIN de escritorio nunca |
| `PLATAFORMA_ADMIN` -> dados de TODOS os tenants | Cross-tenant por desenho (nao e uma fuga); o risco e a audiencia errada, nao o alcance |
| Corpo JSON do cliente -> `TenantUpdateRequest` / `Map<String,Object>` | Entrada nao confiavel desserializada em estado persistido |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-120-08 | Information Disclosure | `GET /platform/tenants` devolve dados de todos os tenants | mitigate | Gate de classe `@PreAuthorize("hasRole('PLATAFORMA_ADMIN')")` herdado (Phase 119), mais um teste de proxy real por CADA handler novo (Task 3, Grupo B) que prova que um ADMIN de escritorio recebe `AccessDeniedException` e que nem sequer chega aos repositorios. O `GlobalExceptionHandler` da Phase 119 converte isso em `403` |
| T-120-09 | Information Disclosure | Serializacao acidental de campos sensiveis/pesados de `Tenant` (`logoDataUrl` `@Lob`, `nif`, `email`, `telefone`) | mitigate | `TenantAdminSummaryResponse` e uma projecao fechada de 6 campos; criterio de aceitacao verifica `grep -c 'private '` = 6 e a ausencia de `logoDataUrl`. Nenhum handler devolve `Tenant` cru |
| T-120-10 | Elevation of Privilege | Um endpoint novo aceitar campos de papel/permissao e tornar-se uma via de auto-promocao | mitigate | Nenhuma das 3 assinaturas aceita `Role`/`Permission`; criterio de aceitacao por `grep` na Task 2 e teste de contencao por reflexao na Task 3. Complementa a contencao que a Phase 119 ja fez no `AdminController` (fecha o vector T-119-01) |
| T-120-11 | Denial of Service | Suspender a tenant reservada "LexCV" tranca fora o unico `PLATAFORMA_ADMIN`, sem via de recuperacao pela aplicacao | mitigate | Guarda autoritativa no backend (`TENANT_RESERVADO.equals(nome)` + `novoAtivo == false` -> 400), com 2 casos de teste (bloqueia suspensao / permite reativacao). O espelho no frontend (Plan 04) e apenas UX |
| T-120-12 | Tampering | Corpo `{"ativo": "sim"}` ou enum invalido a corromper estado | mitigate | Guard `instanceof Boolean` copiado de `AdminController.updateUser` para o PATCH; tipo `TenantPlano` no DTO para o Jackson recusar enums invalidos; handler `HttpMessageNotReadableException` -> 400 com corpo consistente em vez de 500 |
| T-120-13 | Tampering | `limiteUtilizadores` a 0 ou negativo a bloquear silenciosamente a criacao de utilizadores num tenant | mitigate | Validacao explicita `>= 1` ou `null`, com mensagem propria e teste dedicado (caso 6). `null` continua a ser o unico contrato de "sem limite", tal como a Phase 117 fixou |
| T-120-14 | Repudiation | Ambiguidade sobre qual tenant foi suspenso e por quem | accept | Este projeto nao tem trilho de auditoria de accoes administrativas em nenhum modulo; introduzi-lo aqui seria ambito novo fora dos Success Criteria da fase. Registado como divida conhecida, nao como defeito desta fase |
| T-120-SC | Tampering | Instalacoes npm/pip/cargo/Maven | n/a | Este plano nao acrescenta nenhuma dependencia — zero entradas novas em `pom.xml`. O Package Legitimacy Gate nao se aplica |
</threat_model>

<verification>
1. `mvn -DskipTests compile` verde apos cada task.
2. `mvn test -Dtest=PlatformAdminControllerTest` — pelo menos 26 testes verdes.
3. `mvn test` (suite completa do backend) verde — zero regressoes.
4. `mvn spotbugs:check` exit code 0.
5. `grep -c 'SecurityContextHolder' backend/src/main/java/com/lexcv/controllers/PlatformAdminController.java` = 0.
6. `grep -c '"LexCV"' backend/src/main/java/com/lexcv/controllers/PlatformAdminController.java` = 1.
7. `grep -c '@ExceptionHandler' backend/src/main/java/com/lexcv/config/GlobalExceptionHandler.java` = 5.
</verification>

<success_criteria>
- Os Success Criteria 2, 3 e 4 da Phase 120 tem suporte de backend completo: listagem com utilizacao, ajuste de plano/limite, alternancia de estado.
- A contagem de utilizadores ativos continua a ter uma unica implementacao em todo o codebase.
- A tenant reservada nao pode ser suspensa, com prova de teste.
- Cada handler novo tem prova individual de que um ADMIN de escritorio nao lhe chega.
</success_criteria>

<output>
Criar `.planning/phases/LEXCV-120-frontend-consola-de-administra-o-de-tenants/120-02-SUMMARY.md` quando terminar.
</output>
