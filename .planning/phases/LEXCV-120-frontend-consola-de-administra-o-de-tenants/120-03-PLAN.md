---
phase: 120-frontend-consola-de-administra-o-de-tenants
plan: 03
type: execute
wave: 3
depends_on: [120-02]
files_modified:
  - web/src/types/auth.ts
  - web/src/types/platform-admin.ts
  - web/src/hooks/use-platform-admin.ts
  - web/src/components/shared/dashboard-shell.tsx
autonomous: true
requirements: [PROV-02, PROV-03, PROV-04, PROV-05]

must_haves:
  truths:
    - "O tipo Role do frontend inclui PLATAFORMA_ADMIN, pelo que me.data.roles.includes(PLATAFORMA_ADMIN) type-checka"
    - "Existe um hook de listagem e 3 hooks de mutacao (criar, atualizar plano/limite, alternar ativo) que falam com os 4 endpoints de /platform/tenants"
    - "Todas as mutacoes invalidam a query de listagem em vez de recarregar a pagina"
    - "Um utilizador com o papel PLATAFORMA_ADMIN ve um item de navegacao Plataforma na barra lateral, em desktop e em mobile"
    - "Um utilizador sem esse papel nao ve o item Plataforma em nenhuma das duas barras"
  artifacts:
    - path: "web/src/types/platform-admin.ts"
      provides: "Tipos TenantAdminSummary e TenantUpdateRequest espelhando os DTOs do Plan 02"
      contains: "utilizadoresAtivos"
    - path: "web/src/hooks/use-platform-admin.ts"
      provides: "1 query + 3 mutacoes contra /platform/tenants, com invalidateQueries"
      min_lines: 60
    - path: "web/src/components/shared/dashboard-shell.tsx"
      provides: "Item de navegacao Plataforma gated por papel, aplicado aos 2 call sites de SidebarNav"
      contains: "PLATAFORMA_ADMIN"
  key_links:
    - from: "web/src/hooks/use-platform-admin.ts"
      to: "backend GET/PUT/PATCH /api/v1/platform/tenants"
      via: "apiFetch com a chave de query [platform, tenants, list]"
      pattern: "/platform/tenants"
    - from: "web/src/components/shared/dashboard-shell.tsx"
      to: "web/src/components/shared/sidebar-nav.tsx"
      via: "array de navegacao derivado passado como prop nav aos 2 call sites"
      pattern: "nav=\\{navItems\\}"
---

<objective>
Construir a camada de dados do frontend para a consola de plataforma — tipos, hooks TanStack Query e o ponto de entrada na navegacao — antes de qualquer ecra existir.

Purpose: separa o contrato (que tem de bater exatamente com os DTOs do Plan 02) da composicao visual (Plans 04/05). Sem isto, o executor do ecra teria de inferir a forma da resposta a partir do backend enquanto escreve JSX, que e exatamente o padrao de "caca ao tesouro" que este projeto ja evita nas fases 83 e 89 (camada de dados primeiro, UI depois).

Output: `types/platform-admin.ts`, `hooks/use-platform-admin.ts`, a uniao `Role` alargada, e o item de navegacao "Plataforma" visivel so a `PLATAFORMA_ADMIN`.
</objective>

<execution_context>
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@web/AGENTS.md
@.planning/phases/LEXCV-120-frontend-consola-de-administra-o-de-tenants/120-UI-SPEC.md
@.planning/phases/LEXCV-120-frontend-consola-de-administra-o-de-tenants/120-PATTERNS.md
@.planning/phases/LEXCV-120-frontend-consola-de-administra-o-de-tenants/120-02-SUMMARY.md
</context>

<interfaces>
<!-- Contratos ja confirmados no planeamento. NAO reexplorar o codebase para os redescobrir. -->

Contrato HTTP entregue pelo Plan 02 (base `NEXT_PUBLIC_API_BASE_PATH` = `/api/v1`, portanto os caminhos passados a `apiFetch` comecam em `/platform/...`):
- `GET /platform/tenants` -> `200`, array de `{ id: UUID, nome: string, plano: "STARTER"|"STANDARD"|"ENTERPRISE", limiteUtilizadores: number|null, ativo: boolean, utilizadoresAtivos: number }`, ordenado por `nome`.
- `POST /platform/tenants` (Phase 119) -> `201`, `{ id, nome }`. Corpo: `SetupInitializeRequest` (`clientName`, `logo?`, `adminEmail`, `adminPassword`). Erros: `400` `{"message":"Já existe um utilizador com este email."}` entre outros.
- `PUT /platform/tenants/{id}` -> `200` com o mesmo objeto da listagem. Corpo: `{ plano, limiteUtilizadores }` (`limiteUtilizadores: null` = sem limite).
- `PATCH /platform/tenants/{id}/ativo` -> `200` com o mesmo objeto da listagem. Corpo: `{ ativo: boolean }`. `400` com `{"message":"Não é possível suspender o tenant da plataforma (LexCV)."}` ao tentar suspender a tenant reservada.

`web/src/lib/api.ts` (55 linhas) — `apiFetch<T>(path, init)`: junta `API_BASE` ao caminho, poe `Content-Type: application/json` quando ha `body` nao-FormData, envia sempre `credentials: "include"`, faz toast automatico de erros exceto `401`/`403`, e lanca `Error("API {status}: {message}")`. Nao ha nada a acrescentar aqui.

`web/src/types/auth.ts` (31 linhas) — `export type Role = "ADMIN" | "TECNICO" | "ADVOGADO" | "ASSISTENTE";` e `MeResponse` com `roles: Role[]`, `permissions: string[]`, `tenant_plano?: string | null`, `tenant_limite_utilizadores?: number | null`.

`web/src/types/setup.ts` (16 linhas) — ja exporta `SetupInitializeRequest` (`clientName`, `logo?: string | null`, `adminEmail`, `adminPassword`) e `SetupInitializeResponse`. **Reutilizar, nao duplicar.**

`web/src/hooks/use-clientes.ts` — padrao idiomatico a copiar: `useQuery({ queryKey: [...], queryFn: () => apiFetch<T>(path), enabled: typeof window !== "undefined", staleTime: 30_000 })` para leituras, e `useMutation({ mutationFn, onSuccess: async () => { await queryClient.invalidateQueries({ queryKey: [...] }); } })` para escritas.

`web/src/hooks/use-me.ts` — `useQuery({ queryKey: ["auth","me"], queryFn: () => apiFetch<MeResponse>("/auth/me"), staleTime: 60_000 })`. Partilhada por dedupe do TanStack Query em todo o app.

`web/src/components/shared/sidebar-nav.tsx` — exporta `type NavItem = { href: string; label: string; icon: React.ComponentType<{className?: string}>; requiredPermission?: string }` e `SidebarNav({ nav, pathname, permissions, onNavigate })`. Filtra internamente com `nav.filter((item) => hasPermission(permissions, item.requiredPermission))`. **NAO alterar este ficheiro** (instrucao explicita do UI-SPEC e do PATTERNS).

`web/src/components/shared/dashboard-shell.tsx`:
- linha 5: `import { SidebarNav, type NavItem } from "@/components/shared/sidebar-nav";`
- linha 10: `Building2` **ja esta importado** de `lucide-react` (usado nas linhas 187 e 196 para a identidade do tenant no topbar) — nao acrescentar import novo.
- linha 24: `import { useMe } from "@/hooks/use-me";`
- linhas 60-68: `const NAV: NavItem[] = [ Dashboard, Clientes, Processos, Agenda, Documentos, Financeiro, Pareceres ]` — os 6 ultimos com `requiredPermission`.
- linha 75: `const me = useMe();`
- **dois** call sites de `<SidebarNav nav={NAV} pathname={pathname} permissions={me.data?.permissions} onNavigate={...} />`: linhas 120-124 (`<aside>` desktop) e 140-144 (`<Sheet>` mobile). Os dois tem de receber o mesmo array derivado.

`web/src/app/(dashboard)/settings/page.tsx` linha 56 — o precedente exato de verificacao de papel: `const isAdmin = me?.roles?.includes("ADMIN");`

Nota Next.js 16 (`web/AGENTS.md`): esta versao tem breaking changes face a versoes anteriores. Nada neste plano toca em routing, proxy ou data APIs do framework — sao tipos, hooks de biblioteca (TanStack Query) e um componente cliente ja existente. Nao e preciso consultar `web/node_modules/next/dist/docs/` para este plano.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Tipos do dominio de plataforma e alargamento da uniao Role</name>

  <files>
    web/src/types/platform-admin.ts (novo)
    web/src/types/auth.ts
  </files>

  <read_first>
    - `web/src/types/setup.ts` (ficheiro completo, 16 linhas) — o estilo `export type X = { … }` a replicar e a confirmacao de que `SetupInitializeRequest` ja existe e nao deve ser duplicado.
    - `web/src/types/auth.ts` (ficheiro completo, 31 linhas) — a uniao `Role` e o `MeResponse` que a consome.
    - `.planning/phases/LEXCV-120-frontend-consola-de-administra-o-de-tenants/120-02-SUMMARY.md` — os nomes de campo exatos que os DTOs do backend acabaram por ter, para os tipos baterem certo com o JSON real.
  </read_first>

  <action>
    Criar `web/src/types/platform-admin.ts` (convencao deste projeto: um ficheiro por area de dominio em `web/src/types/`) com:
    - `export type TenantPlano = "STARTER" | "STANDARD" | "ENTERPRISE";` — uniao de literais espelhando o enum do backend, para o `NativeSelect` do Plan 05 poder ser type-safe.
    - `export type TenantAdminSummary = { id: string; nome: string; plano: TenantPlano; limiteUtilizadores: number | null; ativo: boolean; utilizadoresAtivos: number };` — nomes de campo byte-identicos aos do `TenantAdminSummaryResponse` do backend (camelCase; o backend serializa os campos Java tal como estao nomeados, sem estrategia de nomes global).
    - `export type TenantUpdateRequest = { plano: TenantPlano; limiteUtilizadores: number | null };`
    - `export type TenantAtivoRequest = { ativo: boolean };`
    Documentar num comentario de topo do ficheiro que `limiteUtilizadores: null` significa "sem limite" (mesmo contrato da Phase 117) e que `SetupInitializeRequest`/`SetupInitializeResponse` de `@/types/setup` sao reutilizados tal como estao para a criacao de tenant — nao redeclarar.

    Em `web/src/types/auth.ts`, alargar a uniao para `export type Role = "ADMIN" | "TECNICO" | "ADVOGADO" | "ASSISTENTE" | "PLATAFORMA_ADMIN";`. Sem esta alteracao, `me.data?.roles?.includes("PLATAFORMA_ADMIN")` nao type-checka (`Array<Role>.includes` exige o tipo do elemento) e `pnpm build` falharia nos Plans 03/04/05. Acrescentar um comentario curto de uma linha a registar que `PLATAFORMA_ADMIN` e o papel de plataforma da Phase 119, deliberadamente sem permissoes com scope. Nao alterar mais nada neste ficheiro.

    Proibicoes: NAO redeclarar `SetupInitializeRequest` em `platform-admin.ts`. NAO importar nada de `@/server/mock-db` (legado, ver CLAUDE.md). NAO usar `any` em nenhum dos tipos.
  </action>

  <verify>
    <automated>cd web && npx tsc --noEmit -p tsconfig.json && grep -q 'PLATAFORMA_ADMIN' src/types/auth.ts && grep -q 'utilizadoresAtivos: number' src/types/platform-admin.ts && ! grep -q 'SetupInitializeRequest' src/types/platform-admin.ts</automated>
  </verify>

  <acceptance_criteria>
    - `npx tsc --noEmit` termina com exit code 0.
    - `web/src/types/auth.ts` contem a uniao `Role` com exatamente 5 membros, incluindo `"PLATAFORMA_ADMIN"`, e o `MeResponse` mantem-se inalterado.
    - `web/src/types/platform-admin.ts` exporta exatamente 4 tipos (`TenantPlano`, `TenantAdminSummary`, `TenantUpdateRequest`, `TenantAtivoRequest`) — `grep -c '^export type'` devolve 4.
    - `TenantAdminSummary` tem exatamente 6 propriedades, com os mesmos nomes dos 6 campos de `TenantAdminSummaryResponse` no backend.
    - `grep -c 'any' src/types/platform-admin.ts` devolve 0.
    - `SetupInitializeRequest` nao e redeclarado — `grep -c 'SetupInitializeRequest' src/types/platform-admin.ts` devolve 0.
  </acceptance_criteria>

  <done>Os tipos do dominio de plataforma existem, batem certo com os DTOs do backend, e a uniao `Role` ja permite verificar o papel `PLATAFORMA_ADMIN` sem erro de tipo.</done>
</task>

<task type="auto">
  <name>Task 2: Hooks TanStack Query para os 4 endpoints de /platform/tenants</name>

  <files>
    web/src/hooks/use-platform-admin.ts (novo)
  </files>

  <read_first>
    - `web/src/hooks/use-clientes.ts` linhas 1-110 — o padrao idiomatico deste projeto: forma do `useQuery` (com `enabled` e `staleTime`), forma do `useMutation` com `onSuccess` a fazer `queryClient.invalidateQueries`, e o uso de `satisfies` no corpo serializado.
    - `web/src/lib/api.ts` (ficheiro completo, 55 linhas) — o que o `apiFetch` ja trata (credenciais, toast automatico, forma do erro lancado), para nao duplicar tratamento de erro no hook.
    - `web/src/hooks/use-admin.ts` (ficheiro completo) — **contra-exemplo**: e um analogo funcional da forma de mutacao, mas importa tipos de `@/server/mock-db` (legado, ver CLAUDE.md "Legacy / ignore"). Ler para reconhecer o que NAO copiar.
    - `web/src/types/platform-admin.ts` — os tipos acabados de criar na Task 1.
  </read_first>

  <action>
    Criar `web/src/hooks/use-platform-admin.ts` com uma unica chave de query partilhada, declarada como constante no topo do ficheiro (`const TENANTS_LIST_KEY = ["platform", "tenants", "list"] as const;`) para as 4 funcoes a referenciarem sem repetir literais:

    1. `useTenantsAdmin()` — `useQuery` sobre `apiFetch<TenantAdminSummary[]>("/platform/tenants")`, `queryKey: TENANTS_LIST_KEY`, `enabled: typeof window !== "undefined"`, `staleTime: 30_000` (mesmo valor de `useClientes`). Sem parametros de filtro: a pesquisa por nome e feita do lado do cliente pelo ecra (o `DataTable` partilhado nao configura row filtering por decisao de arquitetura da Phase 104, e o UI-SPEC especifica um unico campo de pesquisa por `nome`).
    2. `useCreateTenant()` — `useMutation` com `mutationFn: (payload: SetupInitializeRequest) => apiFetch<SetupInitializeResponse | { id: string; nome: string }>("/platform/tenants", { method: "POST", body: JSON.stringify(payload) })`, importando `SetupInitializeRequest` de `@/types/setup`. `onSuccess` invalida `TENANTS_LIST_KEY`. Tipar a resposta como `{ id: string; nome: string }` — e o que o `TenantProvisionResponse` da Phase 119 devolve.
    3. `useUpdateTenant()` — `useMutation` que recebe `{ id, payload }` (o id vai no argumento da mutacao, nao no argumento do hook, porque o ecra tem uma so instancia do hook e edita linhas diferentes), faz `PUT /platform/tenants/{encodeURIComponent(id)}` com `body: JSON.stringify(payload satisfies TenantUpdateRequest)` e devolve `TenantAdminSummary`. `onSuccess` invalida `TENANTS_LIST_KEY`.
    4. `useSetTenantAtivo()` — `useMutation` que recebe `{ id, ativo }`, faz `PATCH /platform/tenants/{encodeURIComponent(id)}/ativo` com `body: JSON.stringify({ ativo } satisfies TenantAtivoRequest)` e devolve `TenantAdminSummary`. `onSuccess` invalida `TENANTS_LIST_KEY`.

    Escrever um comentario de topo a registar: que todas as mutacoes invalidam a listagem em vez de recarregarem a pagina; que o `window.location.reload()` de `UserManagementTab` (`settings/page.tsx`) e um artefacto da era da base de dados simulada e esta explicitamente proibido pelo UI-SPEC desta fase; e que nao existe cache "detail" por tenant, pelo que nao ha `setQueryData` a fazer — a listagem inteira e refeita.

    Proibicoes: NAO importar nada de `@/server/mock-db`. NAO usar `window.location.reload()`. NAO chamar `toast` dentro do hook (o `apiFetch` ja faz o toast automatico de erro; o toast de sucesso pertence ao ecra, conforme o UI-SPEC fixa as frases). NAO usar `fetch` diretamente — sempre `apiFetch`. NAO adicionar `refetchInterval`/polling.
  </action>

  <verify>
    <automated>cd web && npx tsc --noEmit -p tsconfig.json && pnpm lint && test $(grep -c 'export function use' src/hooks/use-platform-admin.ts) -eq 4 && test $(grep -c 'invalidateQueries' src/hooks/use-platform-admin.ts) -eq 3 && test $(grep -c 'window.location.reload\|@/server/mock-db' src/hooks/use-platform-admin.ts) -eq 0</automated>
  </verify>

  <acceptance_criteria>
    - `npx tsc --noEmit` e `pnpm lint` terminam ambos com exit code 0.
    - O ficheiro exporta exatamente 4 hooks: `useTenantsAdmin`, `useCreateTenant`, `useUpdateTenant`, `useSetTenantAtivo`.
    - As 3 mutacoes chamam `queryClient.invalidateQueries` com a mesma constante `TENANTS_LIST_KEY` — `grep -c 'invalidateQueries'` devolve 3 e `grep -c '\["platform", "tenants", "list"\]'` devolve 1 (so a declaracao da constante).
    - `grep -c 'window.location.reload'` devolve 0 e `grep -c '@/server/mock-db'` devolve 0.
    - `grep -c 'apiFetch'` devolve pelo menos 4 e `grep -cE '(^|[^i])fetch\('` (fetch nativo direto) devolve 0.
    - Os caminhos usados sao exatamente `/platform/tenants`, `/platform/tenants/${…}` e `/platform/tenants/${…}/ativo` — sem prefixo `/api/v1` duplicado (o `API_BASE` ja o traz).
    - `grep -c 'toast'` devolve 0 — nenhum toast disparado a partir do hook.
  </acceptance_criteria>

  <done>Os 4 endpoints de plataforma tem hook proprio, com invalidacao de cache correta e zero heranca do padrao legado de recarregar a pagina.</done>
</task>

<task type="auto">
  <name>Task 3: Item de navegacao Plataforma gated por papel nos 2 call sites</name>

  <files>
    web/src/components/shared/dashboard-shell.tsx
  </files>

  <read_first>
    - `web/src/components/shared/dashboard-shell.tsx` (ficheiro completo) — o array `NAV`, o `const me = useMe();` e **os dois** call sites de `<SidebarNav nav={NAV} …/>` (desktop `<aside>` e mobile `<Sheet>`), mais os usos ja existentes de `Building2` no topbar.
    - `web/src/components/shared/sidebar-nav.tsx` (ficheiro completo) — o tipo `NavItem` e o filtro `hasPermission(permissions, item.requiredPermission)`, para perceber por que um item sem `requiredPermission` passa sempre pelo filtro (e portanto por que o gate tem de ser feito ANTES, na construcao do array).
    - `web/src/app/(dashboard)/settings/page.tsx` linhas 50-60 — o precedente `const isAdmin = me?.roles?.includes("ADMIN");`.
    - `.planning/phases/LEXCV-120-frontend-consola-de-administra-o-de-tenants/120-UI-SPEC.md` seccao "Route & navigation" — o contrato exato do item (`href`, `label`, `icon`) e a instrucao explicita de nao alterar `SidebarNav`.
  </read_first>

  <action>
    Dentro do corpo de `DashboardShell`, a seguir a `const me = useMe();`, derivar:
    - `const isPlatformAdmin = me.data?.roles?.includes("PLATAFORMA_ADMIN") ?? false;`
    - um `NavItem` com `href: "/plataforma"`, `label: "Plataforma"`, `icon: Building2` e **sem** `requiredPermission` (o papel `PLATAFORMA_ADMIN` nao tem permissoes com scope, por decisao da Phase 119 — por isso o filtro `hasPermission` do `SidebarNav` nao serve para o gatilhar e um item sem `requiredPermission` passaria sempre; e por isso que o gate tem de estar na construcao do array).
    - `const navItems = isPlatformAdmin ? [...NAV, platformNavItem] : NAV;` envolvido em `React.useMemo` com `[isPlatformAdmin]` como dependencia, para nao criar um array novo em cada render e forcar re-render do `SidebarNav`.

    Substituir `nav={NAV}` por `nav={navItems}` nos **dois** call sites (`<aside>` desktop e `<Sheet>` mobile). Nao ha aqui nenhuma decisao a tomar: se so um dos dois for alterado, o item aparece num tamanho de ecra e desaparece no outro. Definir o objeto `platformNavItem` uma unica vez (constante ao nivel de modulo, junto do `NAV`, ja que nao depende de nenhum valor de render) e usar essa referencia dentro do `useMemo`.

    Escrever um comentario curto por cima do `isPlatformAdmin` a registar: (a) que este e o unico item de navegacao gated por PAPEL em vez de por permissao com scope, e porque; (b) a consequencia emergente confirmada no planeamento — como um `PLATAFORMA_ADMIN` tem `permissions` vazio, os 6 itens com `requiredPermission` ja ficam ocultos para ele automaticamente pelo filtro existente do `SidebarNav`, sem codigo novo, restando-lhe tipicamente Dashboard (sem `requiredPermission`) e Plataforma; (c) que este gate e apenas espelho de UX — a fronteira real e o `@PreAuthorize` de classe do `PlatformAdminController`.

    Proibicoes: NAO alterar `web/src/components/shared/sidebar-nav.tsx` (instrucao explicita do UI-SPEC e do PATTERNS — `SidebarNav` nunca deve aprender o que e `PLATAFORMA_ADMIN`). NAO acrescentar `requiredPermission` ao item novo. NAO acrescentar import de `Building2` (ja existe na linha 10). NAO mexer nos 7 itens de `NAV` existentes nem na sua ordem — o item novo vai no fim, depois de Pareceres.
  </action>

  <verify>
    <automated>cd web && npx tsc --noEmit -p tsconfig.json && pnpm lint && test $(grep -c 'nav={navItems}' src/components/shared/dashboard-shell.tsx) -eq 2 && test $(grep -c 'nav={NAV}' src/components/shared/dashboard-shell.tsx) -eq 0 && grep -q 'PLATAFORMA_ADMIN' src/components/shared/dashboard-shell.tsx && git diff --quiet -- src/components/shared/sidebar-nav.tsx</automated>
  </verify>

  <acceptance_criteria>
    - `npx tsc --noEmit` e `pnpm lint` terminam ambos com exit code 0.
    - `grep -c 'nav={navItems}'` devolve exatamente 2 e `grep -c 'nav={NAV}'` devolve 0 — os dois call sites recebem o array derivado.
    - `web/src/components/shared/sidebar-nav.tsx` esta byte-identico ao estado anterior (`git diff --quiet` sobre esse ficheiro devolve exit code 0).
    - O item novo e `{ href: "/plataforma", label: "Plataforma", icon: Building2 }` e nao tem `requiredPermission` — `grep -A2 '"/plataforma"' src/components/shared/dashboard-shell.tsx` nao mostra `requiredPermission`.
    - O array `NAV` continua a ter 7 elementos, pela mesma ordem.
    - Nao ha nenhum `import { Building2 }` novo — o numero de linhas de import de `lucide-react` nao aumentou.
    - `pnpm build` termina com exit code 0.
  </acceptance_criteria>

  <done>Um `PLATAFORMA_ADMIN` ve o item "Plataforma" em desktop e mobile; qualquer outro papel nao o ve; `SidebarNav` ficou intocado.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser -> `/api/v1/platform/**` via `apiFetch` | Pedido com cookies httpOnly; a autorizacao e decidida no backend, nunca aqui |
| `me.data.roles` (resposta de `/auth/me`) -> visibilidade do item de navegacao | Dado vindo do servidor usado para decidir o que mostrar — decisao de UX, nunca de acesso |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-120-15 | Elevation of Privilege | Alguem confundir o gate de navegacao com um controlo de acesso e um utilizador manipular `roles` no cliente para ver o item | accept | A visibilidade do item nao concede nada: qualquer chamada a `/platform/**` e recusada com `403` pelo `@PreAuthorize` de classe (Plan 02, com teste de proxy por handler). O comentario obrigatorio no codigo regista explicitamente que este gate e espelho de UX |
| T-120-16 | Information Disclosure | Um utilizador sem papel de plataforma chegar a `/plataforma` escrevendo o URL e o ecra pedir dados | mitigate | O guard de pagina com `AccessDeniedState` chega no Plan 05; entretanto o backend responde `403` a qualquer pedido, o `apiFetch` suprime o toast para `403` e a listagem fica vazia. Nenhum dado atravessa |
| T-120-17 | Tampering | Hook a esquecer `invalidateQueries` e o ecra mostrar estado obsoleto apos suspender um tenant | mitigate | Criterio de aceitacao verifica `grep -c 'invalidateQueries'` = 3 (uma por mutacao), todas com a mesma constante de chave |
| T-120-18 | Denial of Service | Item de navegacao a aparecer so em desktop ou so em mobile por se alterar apenas um dos dois call sites | mitigate | Criterio de aceitacao exige `grep -c 'nav={navItems}'` = 2 e `grep -c 'nav={NAV}'` = 0 |
| T-120-SC | Tampering | Instalacoes npm/pnpm | n/a | Este plano nao acrescenta nenhuma dependencia — `web/package.json` `dependencies`/`devDependencies` ficam inalterados. `Building2`/lucide-react, TanStack Query e Zod ja estao instalados. O Package Legitimacy Gate nao se aplica |
</threat_model>

<verification>
1. `cd web && npx tsc --noEmit` exit code 0 apos cada task.
2. `cd web && pnpm lint` exit code 0.
3. `cd web && pnpm build` exit code 0.
4. `grep -c 'export function use' web/src/hooks/use-platform-admin.ts` = 4.
5. `grep -c 'nav={navItems}' web/src/components/shared/dashboard-shell.tsx` = 2.
6. `git diff --quiet -- web/src/components/shared/sidebar-nav.tsx` exit code 0 — ficheiro nao tocado.
7. `git diff --stat -- web/package.json` vazio — zero dependencias novas.
</verification>

<success_criteria>
- A camada de dados da consola existe e type-checka contra os DTOs reais do Plan 02.
- Todas as escritas invalidam a listagem; nenhuma recarrega a pagina.
- O ponto de entrada na navegacao existe e e visivel exclusivamente ao papel de plataforma, nos dois tamanhos de ecra.
- `SidebarNav` mantem-se generico, sem qualquer conhecimento de `PLATAFORMA_ADMIN`.
</success_criteria>

<output>
Criar `.planning/phases/LEXCV-120-frontend-consola-de-administra-o-de-tenants/120-03-SUMMARY.md` quando terminar.
</output>
