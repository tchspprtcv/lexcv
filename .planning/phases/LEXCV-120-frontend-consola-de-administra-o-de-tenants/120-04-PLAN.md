---
phase: 120-frontend-consola-de-administra-o-de-tenants
plan: 04
type: execute
wave: 4
depends_on: [120-03]
files_modified:
  - web/src/app/(dashboard)/plataforma/columns.tsx
  - web/src/app/(dashboard)/plataforma/criar-tenant-panel.tsx
autonomous: true
requirements: [PROV-02, PROV-03]

must_haves:
  truths:
    - "As colunas da tabela de tenants mostram nome com avatar de iniciais, plano em badge, ocupacao X/Y de utilizadores, estado Ativo/Suspenso e duas accoes por linha"
    - "A linha do tenant reservado LexCV mostra um badge Plataforma sob o nome e tem a accao de suspender desativada com tooltip explicativo"
    - "O tooltip sobre a accao desativada dispara por rato e por teclado, via a composicao span tabIndex={0} da Phase 118"
    - "O painel Criar Tenant reutiliza o schema Zod de /setup verbatim, com upload e pre-visualizacao de logo opcional"
    - "Nenhuma coluna nem o painel chamam window.location.reload"
  artifacts:
    - path: "web/src/app/(dashboard)/plataforma/columns.tsx"
      provides: "ColumnDef<TenantAdminSummary>[] com 5 colunas e celula de accoes Tooltip-wrapped"
      min_lines: 120
    - path: "web/src/app/(dashboard)/plataforma/criar-tenant-panel.tsx"
      provides: "Painel inline de criacao de tenant com react-hook-form + setupSchema"
      min_lines: 120
  key_links:
    - from: "web/src/app/(dashboard)/plataforma/criar-tenant-panel.tsx"
      to: "web/src/schemas/setup.ts"
      via: "import direto de setupSchema/SetupFormValues, sem re-derivar o regex de password"
      pattern: "@/schemas/setup"
    - from: "web/src/app/(dashboard)/plataforma/columns.tsx"
      to: "web/src/components/ui/tooltip.tsx"
      via: "TooltipTrigger asChild sobre span tabIndex={0} envolvendo o Button desativado"
      pattern: "tabIndex=\\{0\\}"
---

<objective>
Construir as duas pecas visuais independentes da consola — as definicoes de coluna da tabela de tenants e o painel de criacao de tenant — como ficheiros isolados, antes de o ecra que as compoe existir.

Purpose: `page.tsx` (Plan 05) e o ponto de montagem de tudo (guard de RBAC, Card de lista, pesquisa, DataTable, cards mobile, Dialog de edicao, AlertDialog de suspensao, 4 mutacoes, toasts). Escrever colunas e painel de criacao no mesmo passo faria esse ficheiro ultrapassar largamente o orcamento de contexto de um so executor. Separados, cada um cabe folgadamente e o Plan 05 limita-se a compor pecas com contratos ja fixados.

Output: `columns.tsx` e `criar-tenant-panel.tsx`, ambos com contratos de props explicitos que o Plan 05 consome sem ter de os inferir.
</objective>

<execution_context>
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@web/AGENTS.md
@.planning/phases/LEXCV-120-frontend-consola-de-administra-o-de-tenants/120-UI-SPEC.md
@.planning/phases/LEXCV-120-frontend-consola-de-administra-o-de-tenants/120-PATTERNS.md
@.planning/phases/LEXCV-120-frontend-consola-de-administra-o-de-tenants/120-03-SUMMARY.md
</context>

<interfaces>
<!-- Contratos ja confirmados no planeamento. NAO reexplorar o codebase para os redescobrir. -->

Tipos entregues pelo Plan 03 (`web/src/types/platform-admin.ts`):
`TenantPlano = "STARTER" | "STANDARD" | "ENTERPRISE"`;
`TenantAdminSummary = { id: string; nome: string; plano: TenantPlano; limiteUtilizadores: number | null; ativo: boolean; utilizadoresAtivos: number }`.

`web/src/schemas/setup.ts` (24 linhas, ficheiro completo) — **importar, nunca redefinir**:
```
export const strongPasswordPattern = /^(?=.*[a-z])(?=.*[A-Z])(?=.*\d)(?=.*[@$!%*?&])[A-Za-z\d@$!%*?&]{8,}$/;
export const setupSchema = z.object({
  clientName: z.string().trim().min(1, "O nome da empresa/cliente é obrigatório."),
  adminEmail: z.string().trim().email("Introduza um email válido."),
  adminPassword: z.string().regex(strongPasswordPattern, "A password deve ter 8+ caracteres, maiúscula, minúscula, número e símbolo."),
  confirmPassword: z.string().min(1, "Confirme a password."),
}).refine((v) => v.adminPassword === v.confirmPassword, { message: "As passwords não coincidem.", path: ["confirmPassword"] });
export type SetupFormValues = z.infer<typeof setupSchema>;
```
Nota: `setupSchema` NAO inclui `logo` — no wizard `/setup` o logo e estado `useState` separado do formulario. Fazer o mesmo aqui.

Primitivos ja instalados em `web/src/components/ui/` (zero instalacoes novas nesta fase): `card`, `button`, `input`, `label`, `badge`, `tooltip`, `dialog`, `alert-dialog`, `native-select`, `table`, `dropdown-menu`.
`Badge` (`web/src/components/ui/badge.tsx`) tem as variantes `default | secondary | outline | blue | green | amber | red | purple | gray` — todas as necessarias ja existem.

`web/src/components/shared/data-table/data-table-column-header.tsx` — exporta `DataTableColumnHeader({ column, title })`, usado no `header` de cada coluna ordenavel.

`web/src/app/(dashboard)/clientes/columns.tsx` (245 linhas) — analogo exato. Assinatura de export: `export function columns(canEditClientes: boolean): ColumnDef<Cliente>[]` (fabrica, nao constante). Celula de accoes num componente separado (`ClienteAcoesCell`) porque as column defs do TanStack sao objetos simples e nao podem chamar hooks. Classes do avatar de iniciais, a copiar verbatim: `h-10 w-10 rounded-md bg-blue-50 dark:bg-blue-500/10 text-blue-700 dark:text-blue-400 border border-blue-100 dark:border-blue-500/20 flex items-center justify-center text-xs font-bold shadow-sm`. Botoes de accao: `size="sm" variant="ghost"` com `className="h-9 w-9 p-0 text-slate-500 hover:text-… transition-colors"` e `aria-label` proprio, cada um envolvido em `<Tooltip><TooltipTrigger asChild>…</TooltipTrigger><TooltipContent>…</TooltipContent></Tooltip>`.

Composicao Tooltip + botao desativado (Phase 118, `settings/page.tsx` linhas 401-417) — obrigatoria porque `buttonVariants` embute `disabled:pointer-events-none`, o que impede um `TooltipTrigger asChild` colocado diretamente sobre um `Button disabled` de alguma vez disparar:
```
<Tooltip>
  <TooltipTrigger asChild>
    <span tabIndex={0}>
      <Button disabled …>…</Button>
    </span>
  </TooltipTrigger>
  <TooltipContent>…</TooltipContent>
</Tooltip>
```

`web/src/app/setup/page.tsx` — origem do tratamento de logo a replicar: `<input type="file" accept="image/*">`, leitura para data URL, pre-visualizacao, limite de 2MB, helper "PNG, JPG, WEBP até 2MB".

`web/src/app/(dashboard)/settings/page.tsx` (`UserManagementTab`) — origem da chrome do painel inline: `Card` com `className="border-slate-200 dark:border-slate-800 bg-white/50 dark:bg-slate-900/50 backdrop-blur-sm rounded-xl"`, `CardHeader className="flex flex-row items-center justify-between space-y-0"` com botao "X" (`variant="ghost"`, `className="h-8 w-8 p-0 rounded-full border border-slate-200 dark:border-slate-800"`, `aria-label="Fechar"`), `CardContent className="space-y-6"`, `CardFooter className="flex justify-end gap-3 pt-6 border-t border-slate-200 dark:border-slate-800"` com "Cancelar" (`variant="outline"`) e o botao de submissao (`className="bg-blue-600 hover:bg-blue-700 text-white shadow-sm"`).

Copy fixada pelo `120-UI-SPEC.md` (usar verbatim, nao parafrasear):
- Labels/placeholders do formulario: "Nome da Empresa/Cliente" (placeholder "Ex.: Sociedade Exemplo, Lda."), "Logo (opcional)" (helper "PNG, JPG, WEBP até 2MB"), "Email do Administrador" (placeholder "admin@empresa.cv"), "Password", "Confirmar Password". **Nao existe campo "Nome do Administrador"** — o backend fixa `nome = "Administrador"` (`SetupService.java:122`); acrescentar tal campo nao faria nada.
- Titulo/descricao do painel de criacao: seguir o par titulo+descricao de `UserManagementTab` na forma, com o proprio dominio de tenants.
- Botao de submissao do painel: "Criar Tenant" (mesmo rotulo do gatilho, sem inventar uma terceira frase).
- Tooltip da guarda do tenant reservado: "Não é possível suspender o tenant da plataforma (LexCV)."
- Tooltips das accoes de linha: "Editar", "Suspender", "Reativar".

Nota Next.js 16 (`web/AGENTS.md`): os dois ficheiros sao componentes de cliente (`"use client"` no topo, como `clientes/columns.tsx` e `settings/page.tsx`). Nenhum toca em routing, proxy ou data APIs do framework.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Definicoes de coluna da tabela de tenants</name>

  <files>
    web/src/app/(dashboard)/plataforma/columns.tsx (novo)
  </files>

  <read_first>
    - `web/src/app/(dashboard)/clientes/columns.tsx` (ficheiro completo, 245 linhas) — a assinatura de fabrica, o componente de celula de accoes separado, as classes do avatar de iniciais, o padrao de coluna com Badge (`tipo`) e a coluna `acoes` alinhada a direita.
    - `web/src/components/ui/badge.tsx` (ficheiro completo) — as 9 variantes disponiveis, para confirmar que `gray`/`purple`/`amber`/`green`/`red`/`outline` existem todas.
    - `web/src/app/(dashboard)/settings/page.tsx` linhas 390-430 — a composicao Tooltip + `<span tabIndex={0}>` + `Button disabled` tal como ficou depois da Phase 118.
    - `web/src/components/shared/data-table/data-table-column-header.tsx` (ficheiro completo) — a assinatura de `DataTableColumnHeader`.
    - `.planning/phases/LEXCV-120-frontend-consola-de-administra-o-de-tenants/120-UI-SPEC.md` seccao "DataTable columns" — o contrato coluna a coluna, incluindo variantes de badge por plano e o estado "limite atingido".
  </read_first>

  <action>
    Criar `web/src/app/(dashboard)/plataforma/columns.tsx` com `"use client"` no topo, exportando uma fabrica no mesmo estilo de `clientes/columns.tsx`:
    `export function columns({ onEdit, onToggleAtivo }: { onEdit: (tenant: TenantAdminSummary) => void; onToggleAtivo: (tenant: TenantAdminSummary) => void }): ColumnDef<TenantAdminSummary>[]`.
    Os dois callbacks sao obrigatorios e vem do `page.tsx` (Plan 05), que e onde vive o estado dos dialogos — as column defs nao abrem dialogos por si.
    Exportar tambem `export const TENANT_RESERVADO = "LexCV";` para o `page.tsx` reutilizar o mesmo literal nos cards mobile, em vez de o repetir.

    Cinco colunas, por esta ordem:
    1. `nome` — `enableHiding: false`, `header` com `DataTableColumnHeader title="Nome"`. Celula: avatar de iniciais (2 primeiras letras das 2 primeiras palavras, maiusculas) com as classes copiadas verbatim de `clientes/columns.tsx`, seguido do nome em `font-bold` como **texto simples, nao `<Link>`** (nao existe pagina de detalhe de tenant nesta fase). Quando `row.original.nome === TENANT_RESERVADO`, renderizar por baixo do nome um `<Badge variant="outline">` com o texto "Plataforma", para a accao desativada ficar auto-explicativa antes sequer de o rato passar.
    2. `plano` — `header` `DataTableColumnHeader title="Plano"`. Celula: `<Badge className="font-bold tracking-wide">` com o valor cru do enum (`STARTER`/`STANDARD`/`ENTERPRISE`, sem humanizar — mesma convencao da coluna `tipo` de Clientes) e `variant` mapeada por `STARTER` -> `"gray"`, `STANDARD` -> `"purple"`, `ENTERPRISE` -> `"amber"`.
    3. `utilizadores` — `accessorFn` que devolve `row.utilizadoresAtivos` (numero) para a ordenacao ser numerica; `header` `DataTableColumnHeader title="Utilizadores"`. Celula empilhada (`flex flex-col`): linha 1 e `"{utilizadoresAtivos}/{limiteUtilizadores}"` quando ha limite, ou `"{utilizadoresAtivos} · sem limite"` quando `limiteUtilizadores` e `null`. Quando ha limite e `utilizadoresAtivos >= limiteUtilizadores`, a linha 1 passa a `font-semibold` com cor destrutiva e aparece uma linha 2 com o texto "limite atingido" em maiusculas, tamanho 12px e cor destrutiva — mesma linguagem visual do indicador da Phase 118, reaproveitada para consistencia entre fases.
    4. `estado` — `header` `DataTableColumnHeader title="Estado"`. Celula: `<Badge variant={ativo ? "green" : "red"}>` com o texto humanizado em Title Case, "Ativo" ou "Suspenso" (deliberadamente diferente do enum cru da coluna `plano` — e um sinal de estado, nao um rotulo de escalao).
    5. `acoes` — `enableSorting: false`, `enableHiding: false`, header e celula alinhados a direita. Delegar num componente `TenantAcoesCell` declarado no mesmo ficheiro (as column defs sao objetos simples e nao podem conter hooks nem estado; alem disso o componente mantem a celula legivel). Duas accoes num `div className="inline-flex items-center gap-1"`, cada uma envolvida em `Tooltip`:
       - **Editar**: icone `Pencil`, `aria-label="Editar tenant"`, `h-9 w-9 p-0`, hover `text-emerald-600`, sempre activa (incluindo na linha "LexCV" — ajustar plano/limite da tenant de plataforma e inofensivo). `onClick` chama `onEdit(tenant)`. Tooltip "Editar".
       - **Suspender/Reativar**: um so lugar que alterna. Com `ativo === true`: icone `Lock`, `aria-label="Suspender tenant"`, hover `text-red-600`, tooltip "Suspender". Com `ativo === false`: icone `Unlock`, `aria-label="Reativar tenant"`, hover `text-emerald-600`, tooltip "Reativar". `onClick` chama `onToggleAtivo(tenant)`. **Quando `tenant.nome === TENANT_RESERVADO` e `tenant.ativo === true`**, o botao fica `disabled`, envolvido na composicao `<TooltipTrigger asChild><span tabIndex={0}><Button disabled …/></span></TooltipTrigger>`, e o `TooltipContent` passa a ser "Não é possível suspender o tenant da plataforma (LexCV)." — a composicao com `<span tabIndex={0}>` e obrigatoria porque `buttonVariants` embute `disabled:pointer-events-none` e um `TooltipTrigger` colocado diretamente sobre o `Button` desativado nunca dispararia. Quando esse mesmo tenant esta suspenso (estado alcancavel so por SQL manual), a accao de **Reativar** fica activa — a guarda so bloqueia a suspensao, exatamente como no backend.

    Nao existe accao "Eliminar" para tenants nesta fase — o UI-SPEC proibe-a explicitamente. Nao acrescentar.

    Proibicoes: NAO usar `<Link>` na coluna `nome`. NAO humanizar os valores de `plano`. NAO chamar hooks (`useMutation`, `useState`, `useRouter`) dentro de `TenantAcoesCell` — todas as accoes sobem por callback ao `page.tsx`; isto e uma divergencia deliberada face a `ClienteAcoesCell` (que tem o seu proprio `useDeleteCliente`) e existe para o Plan 05 conseguir mostrar um unico `AlertDialog` de confirmacao controlado, com a copy exata do UI-SPEC. NAO usar `any`. NAO acrescentar nenhuma coluna alem das 5.
  </action>

  <verify>
    <automated>cd web && npx tsc --noEmit -p tsconfig.json && pnpm lint && test $(grep -c 'tabIndex={0}' "src/app/(dashboard)/plataforma/columns.tsx") -ge 1 && test $(grep -c 'accessorKey\|accessorFn\|id: "' "src/app/(dashboard)/plataforma/columns.tsx") -ge 5 && test $(grep -c 'useState\|useMutation\|useQuery' "src/app/(dashboard)/plataforma/columns.tsx") -eq 0 && test $(grep -c 'window.location.reload' "src/app/(dashboard)/plataforma/columns.tsx") -eq 0</automated>
  </verify>

  <acceptance_criteria>
    - `npx tsc --noEmit` e `pnpm lint` terminam ambos com exit code 0.
    - `columns.tsx` exporta `columns` (fabrica que recebe `onEdit` e `onToggleAtivo`) e `TENANT_RESERVADO`.
    - O array devolvido tem exatamente 5 entradas, com os ids `nome`, `plano`, `utilizadores`, `estado`, `acoes`.
    - O bloco `disabled` da accao de suspender esta envolvido em `<span tabIndex={0}>` dentro de `<TooltipTrigger asChild>` — `grep -c 'tabIndex={0}'` devolve pelo menos 1, e o `<span>` aparece entre o `TooltipTrigger` e o `Button` (verificar por leitura do bloco, nao so por contagem).
    - A frase `Não é possível suspender o tenant da plataforma (LexCV).` aparece exatamente uma vez e e byte-identica a do backend (Plan 02) e a do `120-UI-SPEC.md`.
    - `grep -c 'useState\|useMutation\|useQuery\|useRouter'` devolve 0 — nenhum hook no ficheiro de colunas.
    - `grep -c '<Link'` devolve 0.
    - `grep -c 'Eliminar\|Trash'` devolve 0 — nenhuma accao de eliminacao.
    - As 3 variantes de badge de plano usadas sao `gray`, `purple` e `amber`, e as de estado sao `green` e `red`.
    - `grep -c 'any'` devolve 0.
  </acceptance_criteria>

  <done>A tabela de tenants tem as 5 colunas do UI-SPEC, com a linha reservada auto-identificada e a accao de suspender desativada com tooltip que dispara mesmo estando o botao `disabled`.</done>
</task>

<task type="auto">
  <name>Task 2: Painel inline de criacao de tenant</name>

  <files>
    web/src/app/(dashboard)/plataforma/criar-tenant-panel.tsx (novo)
  </files>

  <read_first>
    - `web/src/schemas/setup.ts` (ficheiro completo, 24 linhas) — o schema e o `strongPasswordPattern` a importar tal como estao.
    - `web/src/app/setup/page.tsx` (ficheiro completo) — o formulario equivalente ja existente: `react-hook-form` + `zodResolver(setupSchema)`, o tratamento do logo em `useState` fora do formulario, a leitura para data URL, a pre-visualizacao e o limite de 2MB.
    - `web/src/app/(dashboard)/settings/page.tsx` linhas 560-600 e 730-755 — a chrome do painel inline: `Card`, `CardHeader` com botao "X" `aria-label="Fechar"`, `CardContent`, `CardFooter` com "Cancelar" e o botao de submissao azul.
    - `.planning/phases/LEXCV-120-frontend-consola-de-administra-o-de-tenants/120-UI-SPEC.md` seccoes "Copywriting Contract" e "Criar Tenant (inline panel, not a modal)" — as frases verbatim e a justificacao para ser painel e nao modal.
  </read_first>

  <action>
    Criar `web/src/app/(dashboard)/plataforma/criar-tenant-panel.tsx` com `"use client"`, exportando
    `export function CriarTenantPanel({ onCancel, onSubmit, isSubmitting }: { onCancel: () => void; onSubmit: (payload: SetupInitializeRequest) => Promise<void>; isSubmitting: boolean })`.
    O componente e puramente de apresentacao e validacao: **nao chama nenhum hook de mutacao nem `apiFetch`** — recebe `onSubmit` do `page.tsx` (Plan 05), que detem `useCreateTenant`, os toasts e o fecho do painel. Isto mantem a copy dos toasts, a invalidacao de cache e o controlo de estado todos num so sitio.

    Formulario com `useForm<SetupFormValues>({ resolver: zodResolver(setupSchema) })`, importando `setupSchema` e `SetupFormValues` de `@/schemas/setup` — nunca re-derivar o regex de password nem reescrever as mensagens de validacao (o UI-SPEC diz literalmente "do not re-derive the regex"). Logo em `useState<string | null>` separado do formulario, tal como no wizard `/setup` (o `setupSchema` nao tem campo `logo`).

    Campos, por esta ordem, com a copy verbatim do UI-SPEC: "Nome da Empresa/Cliente" (`Input`, placeholder "Ex.: Sociedade Exemplo, Lda."), "Logo (opcional)" (`input type="file" accept="image/*"`, helper "PNG, JPG, WEBP até 2MB", rejeitar acima de 2MB com mensagem inline e nao submeter, pre-visualizar o data URL quando presente, permitir remover), "Email do Administrador" (`Input type="email"`, placeholder "admin@empresa.cv"), "Password" (`Input type="password"`), "Confirmar Password" (`Input type="password"`). **Nao acrescentar campo "Nome do Administrador"** — `SetupInitializeRequest` nao o tem e o backend fixa `nome = "Administrador"`; um campo desses seria silenciosamente ignorado.

    Chrome: `Card` com as classes de `UserManagementTab`, `CardHeader` `flex flex-row items-center justify-between space-y-0` com titulo + descricao a esquerda e botao "X" (`variant="ghost"`, `h-8 w-8 p-0 rounded-full border …`, `aria-label="Fechar"`, `type="button"`, chama `onCancel`) a direita; `CardContent className="space-y-6"` com os campos; `CardFooter className="flex justify-end gap-3 pt-6 border-t …"` com "Cancelar" (`type="button"`, `variant="outline"`, chama `onCancel`) e "Criar Tenant" (`type="submit"`, `className="bg-blue-600 hover:bg-blue-700 text-white shadow-sm"`, `disabled={isSubmitting}`, texto alternativo "A criar..." enquanto submete). O rotulo do botao de submissao e exatamente o mesmo do gatilho — nao inventar uma terceira frase.

    No `handleSubmit`, montar o payload `SetupInitializeRequest` a partir de `clientName`, `adminEmail`, `adminPassword` e do logo em estado (`logo: logoDataUrl ?? null`) e delegar em `onSubmit(payload)`. Nao apanhar o erro aqui — o `page.tsx` decide o que fazer (o `apiFetch` ja faz o toast automatico de erro do backend, incluindo a frase "Já existe um utilizador com este email.").

    Envolver tudo num `<form onSubmit={form.handleSubmit(…)}>` dentro do `Card` (mesma estrutura de `UserManagementTab`) e mostrar as mensagens de erro de cada campo por baixo do respetivo `Input`, com `aria-invalid` no campo em erro.

    Proibicoes: NAO usar `Dialog`/modal (decisao explicita do UI-SPEC — o upload de logo precisa de largura e nao existe precedente de "modal + upload" neste app). NAO chamar `useCreateTenant`, `apiFetch`, `toast` nem `queryClient` neste ficheiro. NAO usar `window.location.reload()`. NAO redefinir `strongPasswordPattern` nem nenhuma das 4 mensagens de validacao. NAO acrescentar campos alem dos 5 listados.
  </action>

  <verify>
    <automated>cd web && npx tsc --noEmit -p tsconfig.json && pnpm lint && grep -q '@/schemas/setup' "src/app/(dashboard)/plataforma/criar-tenant-panel.tsx" && test $(grep -c 'strongPasswordPattern *=' "src/app/(dashboard)/plataforma/criar-tenant-panel.tsx") -eq 0 && test $(grep -cE 'apiFetch|useCreateTenant|window.location.reload|<Dialog' "src/app/(dashboard)/plataforma/criar-tenant-panel.tsx") -eq 0 && test $(grep -c 'Nome do Administrador' "src/app/(dashboard)/plataforma/criar-tenant-panel.tsx") -eq 0</automated>
  </verify>

  <acceptance_criteria>
    - `npx tsc --noEmit` e `pnpm lint` terminam ambos com exit code 0.
    - O ficheiro importa `setupSchema` e `SetupFormValues` de `@/schemas/setup` e nao declara nenhum regex de password proprio (`grep -c 'strongPasswordPattern *='` devolve 0; `grep -c '(?=.\*'` devolve 0).
    - `grep -cE 'apiFetch|useCreateTenant|useMutation|toast|queryClient'` devolve 0 — o painel nao fala com a rede nem com a cache.
    - `grep -c '<Dialog'` devolve 0 — e um painel inline, nao um modal.
    - Existem exatamente 5 controlos de entrada (`grep -c '<Input\|type="file"'` devolve 5) e nenhum se chama "Nome do Administrador".
    - As 4 frases de copy do UI-SPEC aparecem verbatim: `Nome da Empresa/Cliente`, `Ex.: Sociedade Exemplo, Lda.`, `PNG, JPG, WEBP até 2MB`, `admin@empresa.cv`.
    - O botao "X" tem `aria-label="Fechar"` e `type="button"`.
    - O botao de submissao tem `type="submit"` e o rotulo `Criar Tenant`.
    - O limite de 2MB do logo esta implementado e impede a submissao — existe uma comparacao explicita contra `2 * 1024 * 1024` (ou equivalente literal) no ficheiro.
    - `grep -c 'any'` devolve 0.
    - `pnpm build` termina com exit code 0.
  </acceptance_criteria>

  <done>O painel de criacao de tenant existe como componente isolado, com as regras de validacao importadas do `/setup` sem duplicacao, upload de logo com limite, e zero acoplamento a rede ou cache.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Ficheiro escolhido pelo utilizador -> data URL do logo | Conteudo arbitrario lido no browser e enviado no corpo do pedido de criacao |
| Password escrita no formulario -> corpo do POST | Credencial em transito pela camada de apresentacao |
| `tenant.nome === "LexCV"` no cliente -> desativacao do botao | Decisao de UX espelhando uma guarda autoritativa do backend |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-120-19 | Elevation of Privilege | Confiar no `disabled` do botao como a guarda da tenant reservada | mitigate | O backend (Plan 02) rejeita com `400` qualquer `PATCH … /ativo` com `ativo=false` sobre "LexCV", independentemente do cliente. A `<action>` desta task obriga a registar que a desativacao no cliente e apenas espelho de UX, e o teste 10 do Plan 02 prova a camada autoritativa |
| T-120-20 | Denial of Service | Logo enorme convertido a data URL a fazer estourar o pedido ou a coluna `@Lob` | mitigate | Limite explicito de 2MB verificado antes da submissao, com mensagem inline e bloqueio do envio — mesmo limite ja praticado pelo wizard `/setup` |
| T-120-21 | Information Disclosure | Password do novo ADMIN a aparecer em texto claro ou a ficar em estado depois da submissao | mitigate | Os dois campos sao `type="password"`; o estado do formulario e `react-hook-form` local e desaparece quando o painel fecha (o `page.tsx` desmonta o componente). Nenhum log nem toast inclui o valor |
| T-120-22 | Spoofing | Regras de password divergirem entre o wizard `/setup` e esta consola, permitindo passwords fracas por um dos caminhos | mitigate | `setupSchema`/`strongPasswordPattern` sao importados do mesmo ficheiro, nunca reescritos (criterio de aceitacao por `grep`); o backend valida na mesma com o seu `STRONG_PASSWORD_PATTERN` em `SetupService.validateRequest` |
| T-120-23 | Tampering | Tooltip a nao disparar sobre o botao desativado e o operador nao perceber por que nao consegue suspender | mitigate | Composicao `<span tabIndex={0}>` obrigatoria (precedente Phase 118), com criterio de aceitacao proprio; confirmacao por rato e por teclado fica para o checkpoint humano do Plan 06 |
| T-120-SC | Tampering | Instalacoes npm/pnpm | n/a | Zero instalacoes: `Card`, `Button`, `Input`, `Label`, `Badge`, `Tooltip`, `NativeSelect`, `Table`, `Dialog`, `AlertDialog` ja existem em `web/src/components/ui/`; `react-hook-form`, `@hookform/resolvers`, `zod`, `lucide-react` e `@tanstack/react-table` ja estao em `web/package.json`. O UI-SPEC declara "Zero new shadcn installs". O Package Legitimacy Gate nao se aplica |
</threat_model>

<verification>
1. `cd web && npx tsc --noEmit` exit code 0 apos cada task.
2. `cd web && pnpm lint` exit code 0.
3. `cd web && pnpm build` exit code 0.
4. `columns.tsx` devolve 5 colunas com os ids esperados e zero hooks.
5. `grep -c 'tabIndex={0}' "web/src/app/(dashboard)/plataforma/columns.tsx"` >= 1.
6. `criar-tenant-panel.tsx` importa de `@/schemas/setup` e nao redefine o regex de password.
7. `git diff --stat -- web/package.json` vazio — zero dependencias novas.
</verification>

<success_criteria>
- As duas pecas visuais existem isoladas, com contratos de props explicitos que o Plan 05 consome sem inferencia.
- A guarda visual do tenant reservado usa a composicao de tooltip que a Phase 118 provou funcionar sobre um botao desativado.
- As regras de validacao de criacao de tenant sao literalmente as mesmas do wizard `/setup`, sem uma segunda copia a divergir com o tempo.
</success_criteria>

<output>
Criar `.planning/phases/LEXCV-120-frontend-consola-de-administra-o-de-tenants/120-04-SUMMARY.md` quando terminar.
</output>
