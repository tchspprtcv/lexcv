---
phase: 120-frontend-consola-de-administra-o-de-tenants
plan: 06
type: execute
wave: 6
depends_on: [120-05]
files_modified:
  - .planning/phases/LEXCV-120-frontend-consola-de-administra-o-de-tenants/120-HUMAN-UAT.md
autonomous: false
requirements: [PROV-02, PROV-03, PROV-04, PROV-05]

must_haves:
  truths:
    - "Contra backend e frontend a correr, o PLATAFORMA_ADMIN ve o item Plataforma na barra lateral e nao ve os modulos de tenant"
    - "Criar um 2o tenant real pela consola funciona e o novo tenant aparece na lista sem recarregar a pagina"
    - "Ajustar plano e limite de um tenant persiste e a ocupacao X/Y atualiza-se na lista"
    - "PROVA CENTRAL: com uma sessao de um utilizador do 2o tenant ABERTA numa janela, suspender esse tenant noutra janela faz com que o PROXIMO pedido da primeira sessao seja recusado, sem logout nem re-login"
    - "A accao de suspender esta desativada na linha LexCV, com tooltip que dispara por rato e por teclado"
    - "Reativar devolve o acesso de imediato e o ambiente fica reposto no fim"
  artifacts:
    - path: ".planning/phases/LEXCV-120-frontend-consola-de-administra-o-de-tenants/120-HUMAN-UAT.md"
      provides: "Veredito explicito por cenario, com o corte de sessao ao vivo registado separadamente"
      min_lines: 25
  key_links:
    - from: "web/src/app/(dashboard)/plataforma/page.tsx"
      to: "backend PATCH /api/v1/platform/tenants/{id}/ativo"
      via: "confirmacao de suspensao a atravessar a fronteira HTTP real"
      pattern: "ativo"
    - from: "backend/src/main/java/com/lexcv/config/JwtAuthenticationFilter.java"
      to: "sessao ja aberta do 2o tenant"
      via: "re-validacao por pedido a recusar um token ainda criptograficamente valido"
      pattern: "tenantRepository"
---

<objective>
Confirmar ao vivo, contra backend e frontend a correr, a unica afirmacao desta fase que nenhuma assercao de origem consegue provar: **suspender um tenant corta uma sessao ja aberta na requisicao imediatamente seguinte, sem logout nem re-login.**

Purpose: o Success Criterion 4 do ROADMAP e uma afirmacao de comportamento em tempo real entre dois processos de browser distintos. Este projeto tem historico documentado de fechar fases com afirmacoes deste tipo verificadas apenas estaticamente e depois acumular `verification_gap` (Phase 114 em STATE.md; a propria Phase 118 desta milestone precisou de um checkpoint ao vivo pelo mesmo motivo). Alem disso, esta e a primeira vez que este produto suspende o acesso de uma organizacao inteira — errar aqui significa ou nao cortar (falha de seguranca) ou cortar quem nao devia (indisponibilidade). Tambem e a primeira vez que se cria um 2o tenant real neste deployment, pelo que vale confirmar que o isolamento se mantem visivelmente.

Output: `120-HUMAN-UAT.md` com veredito explicito por cenario e o ambiente reposto no fim.
</objective>

<execution_context>
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/LEXCV-120-frontend-consola-de-administra-o-de-tenants/120-UI-SPEC.md
@.planning/phases/LEXCV-120-frontend-consola-de-administra-o-de-tenants/120-01-SUMMARY.md
@.planning/phases/LEXCV-120-frontend-consola-de-administra-o-de-tenants/120-02-SUMMARY.md
@.planning/phases/LEXCV-120-frontend-consola-de-administra-o-de-tenants/120-05-SUMMARY.md
</context>

<interfaces>
<!-- Factos de ambiente confirmados no planeamento. NAO redescobrir. -->

Comandos e portas (CLAUDE.md):
- backend: `cd backend && mvn spring-boot:run`, porta 8080. Requer `backend/.env` — **todas** as variaveis `DB_*` e `MINIO_*` sao obrigatorias, sem defaults.
- frontend: `cd web && pnpm dev`, porta 3000. Requer `web/.env.local` com `BACKEND_API_ORIGIN` e `NEXT_PUBLIC_API_BASE_PATH`.
- `next.config.ts` reescreve `/api/v1/:path*` para `${BACKEND_API_ORIGIN}/api/v1/:path*`, pelo que o browser ve a API na mesma origem.

Contas semeadas (`DatabaseSeeder`, Phase 119):
- `plataforma@lexcv.cv` / `Pa$$w0rd` — papel `PLATAFORMA_ADMIN`, pertence a tenant reservada "LexCV". Semeado apenas quando `SEED_ENABLED=true`; o papel e a tenant sao semeados incondicionalmente em todo o arranque.
- `admin@lexcv.cv` / `Pa$$w0rd` — ADMIN do tenant de demonstracao.
Se `plataforma@lexcv.cv` nao existir nesta base de dados de desenvolvimento, arrancar o backend uma vez com `SEED_ENABLED=true` — nao criar a conta a mao por SQL.

Migracao: `backend/migrations/120-add-tenant-ativo.sql` (Plan 01). Em dev o `ddl-auto: update` cria a coluna automaticamente; correr o script manualmente **so** se a coluna nao existir. Nao o correr duas vezes (`ALTER TABLE ADD COLUMN` falharia).

Esquema relevante: `t_tenant(id, nome, plano, limite_utilizadores, ativo)`; `t_user(id, tenant_id, email, ativo)`.

Autenticacao por CLI: `POST /api/v1/auth/login` com `{"email":…,"password":…}` devolve cookies httpOnly `access_token`/`refresh_token`. Guardar o ficheiro de cookies **fora do repositorio** (scratchpad da sessao).

Gate executavel do Plan 05: `cd web && pnpm verify:consola-tenants`.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Preparar o ambiente ao vivo e criar o 2o tenant real pela consola</name>

  <files>
    (nenhum ficheiro de codigo — operacoes de ambiente e de base de dados)
  </files>

  <read_first>
    - `.planning/phases/LEXCV-120-frontend-consola-de-administra-o-de-tenants/120-01-SUMMARY.md` — os 3 pontos de bloqueio implementados (login, refresh, filtro) e as mensagens exatas devolvidas por cada um, para as reconhecer nas respostas HTTP.
    - `.planning/phases/LEXCV-120-frontend-consola-de-administra-o-de-tenants/120-02-SUMMARY.md` — os caminhos e verbos finais dos 3 endpoints.
    - `CLAUDE.md` seccao Commands e os requisitos de `backend/.env` / `web/.env.local`.
    - `.planning/STATE.md` seccao Blockers/Concerns — bloqueios ambientais ja conhecidos (Docker/Testcontainers, MinIO) para os reconhecer em vez de os re-diagnosticar.
  </read_first>

  <action>
    Garantir que backend e frontend estao ambos a correr. Se algum nao arrancar por falta de `.env`/`.env.local` ou por a base de dados nao estar disponivel, NAO inventar workaround nem alterar codigo: registar o bloqueio ambiental e passar a Task 2 declarando explicitamente que cenarios ficam por confirmar.

    Correr `cd web && pnpm verify:consola-tenants` e confirmar as 10 assercoes `PASS` antes de qualquer verificacao ao vivo — se o gate estatico falhar, nao vale a pena gastar o tempo do operador.

    Registar o estado inicial da base de dados antes de mexer em nada: `SELECT id, nome, plano, limite_utilizadores, ativo FROM t_tenant ORDER BY nome;`. Guardar o resultado por escrito — a Task 2 termina por repor este estado.

    Autenticar por CLI como `plataforma@lexcv.cv`, guardando os cookies num ficheiro temporario **fora do repositorio**, e chamar `GET /api/v1/platform/tenants`. Confirmar que a resposta e `200` e que o JSON contem, por tenant, as chaves `id`, `nome`, `plano`, `limiteUtilizadores`, `ativo` e `utilizadoresAtivos` — esta e a unica prova ao vivo de que a serializacao Jackson atravessa mesmo a fronteira HTTP (os testes Mockito provam o metodo, nao a serializacao).

    Autenticar por CLI como `admin@lexcv.cv` (ADMIN de tenant normal) num segundo ficheiro de cookies e chamar os **3** endpoints novos (`GET /platform/tenants`, `PUT /platform/tenants/{id}`, `PATCH /platform/tenants/{id}/ativo`). Confirmar que os tres devolvem `403` — e a confirmacao ao vivo de que a fase nao abriu nenhuma superficie alcancavel por um administrador de escritorio, complementando os testes de proxy do Plan 02.

    Criar o 2o tenant **pela consola do browser**, nao por SQL nem por curl: entrar em `http://localhost:3000` como `plataforma@lexcv.cv`, ir a "Plataforma", usar "Criar Tenant" com nome `Escritorio Teste 120`, email `teste120@exemplo.cv` e uma password forte que satisfaca o padrao (registar a password usada nas notas da task — vai ser precisa na Task 2). Confirmar que o tenant aparece na lista sem recarregar a pagina.

    Confirmar por `SELECT` que o novo tenant existe com `ativo = true` e que existe um `t_user` com `tenant_id` do novo tenant.

    Proibicoes explicitas: NAO alterar nenhum ficheiro de codigo nesta task. NAO criar o 2o tenant por SQL (o objetivo e exercer o caminho real da UI). NAO suspender nenhum tenant nesta task — a suspensao pertence a Task 2, e tem de ser observada em direto. NAO correr `backend/migrations/120-*.sql` se a coluna `ativo` ja existir.
  </action>

  <verify>
    <automated>cd web && pnpm verify:consola-tenants && curl -s -o /dev/null -w '%{http_code}' -c "$COOKIE_JAR_PLAT" -H 'Content-Type: application/json' -d '{"email":"plataforma@lexcv.cv","password":"Pa$$w0rd"}' http://localhost:8080/api/v1/auth/login | grep -qx 200 && curl -s -b "$COOKIE_JAR_PLAT" http://localhost:8080/api/v1/platform/tenants | grep -q 'utilizadoresAtivos' && curl -s -o /dev/null -w '%{http_code}' -b "$COOKIE_JAR_ADMIN" http://localhost:8080/api/v1/platform/tenants | grep -qx 403</automated>
  </verify>

  <acceptance_criteria>
    - `pnpm verify:consola-tenants` imprime 10 `PASS` e sai com codigo 0.
    - `GET /api/v1/platform/tenants` com cookies de `plataforma@lexcv.cv` devolve `200` e um JSON que contem literalmente `utilizadoresAtivos`, `limiteUtilizadores` e `ativo`.
    - Os **3** endpoints novos devolvem `403` com os cookies de `admin@lexcv.cv` — cada um verificado individualmente e o codigo registado por escrito.
    - O tenant `Escritorio Teste 120` foi criado **pela UI** e um `SELECT` confirma a linha em `t_tenant` com `ativo = true`, mais um `t_user` associado.
    - O estado inicial de `t_tenant` (todas as linhas, com `plano`/`limite_utilizadores`/`ativo`) esta registado por escrito antes de qualquer alteracao.
    - A password usada na criacao esta registada nas notas da task (necessaria para o login da Task 2) e NAO em nenhum ficheiro dentro do repositorio.
    - `git status --porcelain` nao lista nenhum ficheiro de codigo alterado; os ficheiros de cookies estao fora do repositorio.
  </acceptance_criteria>

  <done>Ambiente ao vivo pronto, gate estatico verde, contrato HTTP confirmado, `403` confirmado para um ADMIN de escritorio nos 3 endpoints, e existe um 2o tenant real criado pela propria consola.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verificacao humana — corte de sessao ao vivo, edicao de plano/limite e guarda do tenant reservado</name>

  <files>
    .planning/phases/LEXCV-120-frontend-consola-de-administra-o-de-tenants/120-HUMAN-UAT.md (novo)
  </files>

  <read_first>
    - `.planning/phases/LEXCV-120-frontend-consola-de-administra-o-de-tenants/120-UI-SPEC.md` — a tabela de Copywriting (as frases exatas que o operador tem de reconhecer no ecra) e a seccao "Suspended tenant's own end users — no new UI needed", que explica por que o sintoma esperado e um redirect para o login e nao um banner dedicado.
    - `.planning/phases/LEXCV-120-frontend-consola-de-administra-o-de-tenants/120-05-SUMMARY.md` — a forma final do ecra entregue, para saber o que esperar visualmente.
    - Notas da Task 1 deste plano — o estado inicial de `t_tenant` a repor no fim, e a password do utilizador do 2o tenant.
  </read_first>

  <action>
    Apresentar ao operador o bloco `<what-built>` e a lista numerada de `<how-to-verify>`, e **parar** a espera do sinal de retoma. Nao prosseguir, nao auto-aprovar e nao substituir nenhum dos 10 pontos por analise estatica de codigo — a razao de existir deste plano e precisamente que os pontos 3 a 9 nao sao demonstraveis por leitura de origem.

    Assim que o operador responder, escrever `120-HUMAN-UAT.md` com uma linha por ponto (1 a 10), cada uma com veredito explicito: `CONFIRMADO`, `FALHOU — <descricao do que se viu>` ou `NAO VERIFICADO — <bloqueio ambiental concreto>`. Os pontos 5 (corte da sessao ja aberta) e 6 (bloqueio do novo login) levam vereditos separados, nunca fundidos: sao dois mecanismos distintos (filtro por pedido vs. gate de login) e um pode funcionar sem o outro. Os pontos 7 (tooltip por rato) e 8 (tooltip por teclado) tambem levam vereditos separados.

    Se algum ponto vier `FALHOU`, NAO corrigir codigo dentro deste plano: registar a causa provavel e o ficheiro/linha suspeitos no UAT e no SUMMARY, e devolver o resultado ao orquestrador para tratamento como gap. Um `FALHOU` no ponto 5 e **bloqueador de fase** — significa que o Success Criterion 4 do ROADMAP nao esta satisfeito e que a suspensao nao passa de um rotulo cosmetico.

    Confirmar por fim (ponto 10) que o ambiente ficou reposto, com um `SELECT` de leitura.
  </action>

  <what-built>
    - `Tenant.ativo` (`NOT NULL DEFAULT TRUE`) com migracao manual `backend/migrations/120-add-tenant-ativo.sql` (Plan 01).
    - Bloqueio de tenant suspenso nos **3** caminhos de acesso: `POST /auth/login` (403), `POST /auth/refresh` (401) e `JwtAuthenticationFilter` em **todas** as requisicoes autenticadas — provado por 13 testes Mockito (Plan 01).
    - 3 endpoints novos em `PlatformAdminController`, sob o mesmo gate de classe `hasRole('PLATAFORMA_ADMIN')` da Phase 119: listar com utilizacao, ajustar plano/limite, alternar ativo; com guarda a impedir a suspensao da tenant reservada "LexCV" (Plan 02, cobertura de teste incluindo gate por handler).
    - Tipos, 4 hooks TanStack Query com invalidacao de cache, e o item de navegacao "Plataforma" visivel so a `PLATAFORMA_ADMIN` (Plan 03).
    - Colunas da tabela de tenants e painel inline "Criar Tenant" reutilizando o schema Zod do `/setup` (Plan 04).
    - Ecra `/plataforma` completo: guard de RBAC, lista com pesquisa, cards mobile, Dialog "Editar Tenant" e AlertDialogs de Suspender/Reativar, mais o gate `pnpm verify:consola-tenants` com 10 assercoes verdes (Plan 05).
    - A Task 1 deste plano ja confirmou ao vivo o contrato HTTP, o `403` para um ADMIN de escritorio nos 3 endpoints, e criou um 2o tenant real (`Escritorio Teste 120`) pela propria consola.
  </what-built>

  <how-to-verify>
    Vai precisar de **duas janelas de browser separadas** (uma normal e uma anonima/privada, ou dois browsers diferentes) — nao dois separadores da mesma janela, porque partilhariam os mesmos cookies. Chame-lhes **Janela A** (administrador de plataforma) e **Janela B** (utilizador do 2o tenant).

    1. **Navegacao gated por papel.** Na Janela A, entrar em `http://localhost:3000` como `plataforma@lexcv.cv` / `Pa$$w0rd`. Confirmar que a barra lateral mostra o item **"Plataforma"** e que os modulos de tenant (Clientes, Processos, Agenda, Documentos, Financeiro, Pareceres) **nao** aparecem — o papel de plataforma nao tem permissoes com scope. Confirmar que o item aparece tambem em ecra estreito (encolher a janela abaixo de 768px e abrir o menu hamburguer).
    2. **Lista de tenants.** Clicar em "Plataforma". Confirmar o titulo "Administração de Tenants", o cartao "Tenants Registados" e a tabela com pelo menos 3 linhas: "LexCV" (com o badge "Plataforma" por baixo do nome), o tenant de demonstracao, e "Escritorio Teste 120". Confirmar que cada linha mostra o plano em badge, a ocupacao de utilizadores e o estado "Ativo" em verde. Escrever "Escritorio" no campo de pesquisa e confirmar que a lista filtra.
    3. **Ajustar plano e limite.** Na linha de "Escritorio Teste 120", clicar em Editar. Confirmar o dialogo "Editar Tenant" com os campos Plano e Limite de Utilizadores. Escolher `STANDARD`, escrever `1` no limite e Guardar. Confirmar o toast "Tenant atualizado com sucesso." e que a linha passa a mostrar `1/1` a vermelho com "limite atingido" por baixo (o tenant tem exatamente 1 utilizador). Voltar a abrir Editar, apagar o valor do limite (deixar vazio) e Guardar; confirmar que a linha passa a ler "1 · sem limite".
    4. **Sessao viva no 2o tenant.** Na **Janela B**, entrar em `http://localhost:3000` como `teste120@exemplo.cv` com a password registada na Task 1. Confirmar que entra normalmente e chega ao Dashboard. **Deixar esta janela aberta no Dashboard** — nao fechar, nao terminar sessao.
    5. **PROVA CENTRAL — corte da sessao ja aberta.** Voltar a **Janela A** e, na linha de "Escritorio Teste 120", clicar no icone de cadeado (Suspender). Confirmar o dialogo "Suspender Tenant" com a descricao que menciona "incluindo sessões já iniciadas". Confirmar. Confirmar o toast "Tenant suspenso com sucesso." e que a linha passa a "Suspenso" a vermelho.
       Ir **imediatamente** a **Janela B** (que continua aberta, com a sessao iniciada antes da suspensao) e clicar em qualquer item de navegacao, ou recarregar a pagina. **Confirmar que a sessao e recusada** — o utilizador e reencaminhado para o ecra de login — **sem** ter feito logout nem re-login. Este e o ponto mais importante de toda a fase: prova que a suspensao tem efeito imediato sobre uma sessao ja ativa, e nao apenas no proximo login. Registar quanto tempo demorou entre confirmar a suspensao e a Janela B ser recusada (deve ser o tempo do proximo pedido, nao minutos).
    6. **Bloqueio do novo login.** Ainda na Janela B, tentar entrar de novo como `teste120@exemplo.cv` com a password correta. Confirmar que o login e recusado com a mensagem sobre o acesso da organizacao estar suspenso — **nao** com "Credenciais inválidas" (a password esta certa; o motivo da recusa e outro).
    7. **Guarda do tenant reservado (rato).** Na Janela A, passar o rato sobre o icone de suspender da linha "LexCV" e esperar ~1 segundo (o `delayDuration` global e 700ms). Confirmar que o botao esta visivelmente desativado e que aparece o tooltip com o texto exato: `Não é possível suspender o tenant da plataforma (LexCV).`
    8. **Guarda do tenant reservado (teclado).** A partir do campo de pesquisa, navegar com `Tab` ate ao mesmo botao desativado e confirmar que o tooltip aparece so com o foco, sem rato. (Este e o mesmo mecanismo `<span tabIndex={0}>` que a Phase 118 estreou — confirmar que se manteve correto numa segunda utilizacao.)
    9. **Reativacao.** Na Janela A, clicar no icone de cadeado aberto da linha "Escritorio Teste 120". Confirmar o dialogo "Reativar Tenant" e a descricao "Os utilizadores de … recuperam o acesso de imediato.". Confirmar. Confirmar o toast "Tenant reativado com sucesso." e o estado de volta a "Ativo". Na Janela B, entrar de novo com as mesmas credenciais e confirmar que o acesso voltou.
    10. **Reposicao.** Repor o ambiente: apagar o tenant de teste e o seu utilizador por SQL (`DELETE FROM t_user WHERE tenant_id = '<id>'; DELETE FROM t_tenant WHERE id = '<id>';` — nao existe accao de eliminar tenant na UI, por decisao de ambito) e repor `plano`/`limite_utilizadores`/`ativo` dos tenants pre-existentes aos valores registados na Task 1. Confirmar com um `SELECT` que `t_tenant` voltou ao estado inicial.

    Nota: nao existe (nem deve existir) um banner do tipo "a sua organizacao foi suspensa" para o utilizador bloqueado — o UI-SPEC exclui-o deliberadamente, porque revelaria informacao a alguem que ja perdeu o acesso. O comportamento correto e exatamente o mesmo "sessao expirada" que ja existe para um utilizador desativado.
  </how-to-verify>

  <resume-signal>
    Escrever "aprovado" se os 10 pontos passarem, ou descrever o que falhou indicando o numero do ponto. Se o ponto 5 falhar (a Janela B continuar a funcionar depois da suspensao), isso e um bloqueador de fase — significa que a re-validacao por pedido do `JwtAuthenticationFilter` nao esta a apanhar o estado do tenant e que o Success Criterion 4 do ROADMAP nao esta satisfeito.
  </resume-signal>

  <verify>
    <automated>test -f .planning/phases/LEXCV-120-frontend-consola-de-administra-o-de-tenants/120-HUMAN-UAT.md && test $(grep -cE 'CONFIRMADO|FALHOU|NAO VERIFICADO' .planning/phases/LEXCV-120-frontend-consola-de-administra-o-de-tenants/120-HUMAN-UAT.md) -ge 10</automated>
  </verify>

  <acceptance_criteria>
    - Existe `.planning/phases/LEXCV-120-frontend-consola-de-administra-o-de-tenants/120-HUMAN-UAT.md` com uma linha por cada um dos 10 pontos, cada uma com veredito explicito `CONFIRMADO`, `FALHOU — <descricao>` ou `NAO VERIFICADO — <bloqueio ambiental>` — o `grep -cE` do bloco `<verify>` devolve pelo menos 10.
    - O ponto 5 (corte da sessao ja aberta) e o ponto 6 (bloqueio do novo login) tem vereditos individuais e separados, nunca agrupados num unico "suspensao OK".
    - Os pontos 7 e 8 (tooltip por rato e por teclado) tem vereditos individuais e separados.
    - O registo do ponto 5 indica explicitamente que a Janela B **nao** fez logout nem re-login entre o inicio de sessao e a recusa — sem essa nota, o cenario nao prova o que se propoe provar.
    - O ponto 6 confirma que a mensagem de recusa e sobre suspensao da organizacao, e nao "Credenciais inválidas".
    - O ponto 10 confirma por `SELECT` que `t_tenant` voltou ao estado registado na Task 1 e que o tenant de teste e o seu utilizador foram removidos.
    - Qualquer ponto `FALHOU` gera uma entrada correspondente no SUMMARY com a causa provavel e o ficheiro/linha suspeitos — nunca uma reformulacao do criterio para o fazer passar.
    - `git status --porcelain` lista apenas ficheiros dentro de `.planning/` — este plano nao altera codigo.
  </acceptance_criteria>

  <done>Os 10 pontos tem veredito explicito registado em `120-HUMAN-UAT.md`, com o corte de sessao ao vivo e o bloqueio de login confirmados separadamente, e o ambiente de desenvolvimento reposto ao estado inicial.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Sessao de desenvolvimento <-> base de dados de desenvolvimento | Este plano cria e depois apaga um tenant real e altera `plano`/`limite_utilizadores`/`ativo` de linhas existentes |
| Credenciais de teste <-> repositorio | Dois logins por CLI produzem ficheiros de cookies; a password do tenant de teste e escolhida nesta sessao |
| Janela A (plataforma) <-> Janela B (tenant suspenso) | O cenario central exige duas sessoes concorrentes com cookies isolados |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-120-30 | Tampering | Criacao e remocao de um tenant real na base de dados de desenvolvimento | mitigate | O estado inicial completo de `t_tenant` e registado por escrito antes de qualquer alteracao (Task 1) e reposto e confirmado por `SELECT` no ponto 10. O tenant de teste tem nome inequivoco (`Escritorio Teste 120`) e e apagado junto com o seu unico utilizador. Nenhum dado de cliente/processo/documento e tocado |
| T-120-31 | Information Disclosure | Ficheiros de cookies dos dois logins por CLI | mitigate | `access_token`/`refresh_token` escritos em ficheiros temporarios **fora do repositorio** (scratchpad da sessao), nunca dentro de `web/` ou `backend/`. Criterio de aceitacao da Task 1 verifica `git status --porcelain` limpo |
| T-120-32 | Spoofing | Password do tenant de teste escolhida nesta sessao acabar commitada | mitigate | A password fica apenas nas notas de execucao e no SUMMARY sob a forma de referencia, nunca em ficheiro versionado de codigo; o tenant e o utilizador sao apagados no ponto 10, tornando a credencial inutil |
| T-120-33 | Elevation of Privilege | Tentacao de "aprovar" o checkpoint sem verificacao humana real, sobretudo por o cenario de duas janelas dar trabalho | mitigate | `gate="blocking"` e `autonomous: false`; a `<action>` proibe explicitamente auto-aprovacao e substituicao dos pontos por analise estatica. `workflow.auto_advance` esta a `false` no `config.json`, pelo que o gate nao pode ser saltado silenciosamente |
| T-120-34 | Repudiation | Fundir "a suspensao funciona" num veredito unico e mascarar que so o login bloqueia e a sessao aberta continua viva | mitigate | Criterio de aceitacao exige vereditos separados para os pontos 5 e 6, e exige que o registo do ponto 5 declare explicitamente que nao houve logout nem re-login |
| T-120-35 | Denial of Service | Suspender por engano o tenant de demonstracao (ou a LexCV) durante o teste e ficar sem acesso | mitigate | O guiao so manda suspender `Escritorio Teste 120`, criado de proposito para isto; a tenant "LexCV" esta protegida pela guarda do backend (Plan 02) e o ponto 7 confirma que a UI nem sequer o permite tentar |
| T-120-SC | Tampering | Instalacoes npm/pip/cargo/Maven | n/a | Este plano nao instala nada e nao altera nenhum ficheiro de codigo — so documentacao de planeamento. O Package Legitimacy Gate nao se aplica |
</threat_model>

<verification>
1. `cd web && pnpm verify:consola-tenants` — 10 `PASS`, exit code 0 (Task 1).
2. `GET /api/v1/platform/tenants` ao vivo devolve `200` com `utilizadoresAtivos` no JSON (Task 1).
3. Os 3 endpoints novos devolvem `403` para `admin@lexcv.cv` (Task 1).
4. Os 10 pontos da Task 2 tem veredito explicito em `120-HUMAN-UAT.md`.
5. Pontos 5 e 6 (corte de sessao e bloqueio de login) confirmados separadamente.
6. Pontos 7 e 8 (tooltip por rato e por teclado) confirmados separadamente.
7. `SELECT id, nome, plano, limite_utilizadores, ativo FROM t_tenant ORDER BY nome;` confirma a reposicao ao estado inicial.
8. `git status --porcelain` lista apenas ficheiros `.planning/` — zero alteracoes de codigo neste plano.
</verification>

<success_criteria>
- O Success Criterion 4 do ROADMAP ("um tenant suspenso deixa imediatamente de conseguir autenticar-se ou continuar a usar uma sessao ja ativa") deixa de ser uma afirmacao verificada apenas estaticamente: foi vista a acontecer entre duas sessoes de browser reais.
- Os Success Criteria 1, 2 e 3 foram exercidos pelo caminho real da interface, incluindo a criacao de um 2o tenant real.
- A guarda da tenant reservada foi confirmada visualmente, por rato e por teclado.
- A base de dados de desenvolvimento fica no estado original, sem tenant de teste residual.
</success_criteria>

<output>
Criar `.planning/phases/LEXCV-120-frontend-consola-de-administra-o-de-tenants/120-HUMAN-UAT.md` (veredito por ponto) e `.planning/phases/LEXCV-120-frontend-consola-de-administra-o-de-tenants/120-06-SUMMARY.md` quando terminar.
</output>
