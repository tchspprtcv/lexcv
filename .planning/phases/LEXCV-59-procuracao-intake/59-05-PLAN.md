---
phase: "59"
plan: "05"
type: execute
wave: 4
depends_on: ["59-04"]
files_modified:
  - web/src/app/(dashboard)/clientes/[id]/page.tsx
autonomous: false
requirements: [PROC-01, PROC-02, INT-02, INT-03]

must_haves:
  truths:
    - "Detail page shows yellow badge 'Procuração em falta' when procuracaoKey is null (per D-01)"
    - "Detail page shows view link + 'Substituir' button when procuracaoKey is not null (per D-02, D-03)"
    - "Detail page has Advogados card with list of assigned advogados and Add/Remove actions (per D-06, D-09)"
    - "Detail page has Administrativos card with same pattern (per D-08, D-09)"
  artifacts:
    - path: web/src/app/(dashboard)/clientes/[id]/page.tsx
      provides: Procuração section + Advogados card + Administrativos card
  key_links:
    - from: page.tsx procuração section
      to: useUploadProcuracao, useDownloadProcuracao, useDeleteProcuracao
      via: "imported from use-clientes"
    - from: page.tsx advogados card
      to: useClienteAdvogados, useAddAdvogado, useRemoveAdvogado
      via: "imported from use-clientes; modal uses Dialog from @/components/ui/dialog"
---

<objective>
Add three new sections to the cliente detail page: a procuração card (upload, view, replace), an advogados card (list + add-via-modal + remove), and an administrativos card (same pattern).

Purpose: Delivers the interactive management of procuração and staff assignment in the read-only detail view, where per D-09 the assign/remove actions live.
Output: Updated clientes/[id]/page.tsx with three new cards.
</objective>

<execution_context>
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/LEXCV-59-procuracao-intake/59-CONTEXT.md
@.planning/phases/LEXCV-59-procuracao-intake/59-RESEARCH.md
@.planning/phases/LEXCV-59-procuracao-intake/59-04-SUMMARY.md

<interfaces>
<!-- Hook signatures from Plan 04 output -->
useUploadProcuracao(clienteId: string) → mutation.mutate(formData: FormData)
useDownloadProcuracao() → mutation.mutate(clienteId: string), returns { url: string }
useDeleteProcuracao(clienteId: string) → mutation.mutate()
useClienteAdvogados(clienteId: string) → { data: ClienteAdvogadoUser[] }
useAddAdvogado(clienteId: string) → mutation.mutate(userId: string)
useRemoveAdvogado(clienteId: string) → mutation.mutate(userId: string)
useClienteAdministrativos / useAddAdministrativo / useRemoveAdministrativo — same pattern

<!-- Dialog component API -->
From web/src/components/ui/dialog.tsx:
  Dialog, DialogTrigger, DialogContent, DialogHeader, DialogTitle, DialogFooter
  Standard Radix controlled: open={open} onOpenChange={setOpen}

<!-- FileDropZone component -->
From web/src/components/shared/file-drop-zone.tsx:
  FileDropZone: { onFileChange: (file: File) => void; accept?: string; disabled?: boolean }

<!-- Detail page current structure (must read before editing) -->
web/src/app/(dashboard)/clientes/[id]/page.tsx — card-based layout with contactos and notas sections
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add procuração section to detail page</name>
  <files>
    web/src/app/(dashboard)/clientes/[id]/page.tsx
  </files>
  <read_first>
    web/src/app/(dashboard)/clientes/[id]/page.tsx
    web/src/components/shared/file-drop-zone.tsx
    web/src/components/ui/dialog.tsx
  </read_first>
  <action>
    Read the full detail page to understand existing card layout and import patterns before editing.

    Add imports for: useUploadProcuracao, useDownloadProcuracao, useDeleteProcuracao from "@/hooks/use-clientes"; FileDropZone from "@/components/shared/file-drop-zone"; useState from "react".

    Add a Procuração card section after the client data card and before any existing sub-resource cards. Use the same card container style as existing sections.

    Card content logic (conditional on cliente.procuracao_key):
      When cliente.procuracao_key is null/undefined:
        Show a yellow badge or span with text "Procuração em falta" (use className with yellow/amber text color consistent with existing warning styles in the page, or use className="text-yellow-600 font-medium" if no existing warning class found)
        Show FileDropZone (accept="application/pdf") below the badge; on file selection call uploadMutation.mutate(formData) where formData has the file appended with key "file"

      When cliente.procuracao_key is not null:
        Show "Procuração carregada" label
        Show a "Ver procuração" button that calls downloadMutation.mutate(cliente.id) and then opens window.open(result.url, "_blank") on success
        Show a "Substituir" button that reveals a FileDropZone for replacement upload (same upload flow as missing case)
        Show a "Remover" button that calls deleteMutation.mutate() with a window.confirm("Tem a certeza que quer remover a procuração?") guard

    Replacement UX: add local state showReplace = useState(false); the "Substituir" button toggles it; when true renders FileDropZone below; on upload success setShowReplace(false).

    Do not block form submission — this is an independent section (per D-01), not inside the main form.
  </action>
  <verify>
    <automated>cd web && pnpm build 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - page.tsx imports useUploadProcuracao, useDownloadProcuracao, useDeleteProcuracao
    - page.tsx contains text "Procuração em falta" (the badge text)
    - page.tsx contains "Substituir" button text
    - page.tsx contains FileDropZone usage for procuração upload
    - pnpm build exits 0
  </acceptance_criteria>
  <done>Procuração card renders on detail page; pnpm build exits 0.</done>
</task>

<task type="auto">
  <name>Task 2: Add Advogados and Administrativos cards to detail page</name>
  <files>
    web/src/app/(dashboard)/clientes/[id]/page.tsx
  </files>
  <read_first>
    web/src/app/(dashboard)/clientes/[id]/page.tsx
    web/src/components/ui/dialog.tsx
  </read_first>
  <action>
    Read the current page.tsx (after Task 1 changes) before editing. Verify Dialog import is present or add it.

    Add imports: useClienteAdvogados, useAddAdvogado, useRemoveAdvogado, useClienteAdministrativos, useAddAdministrativo, useRemoveAdministrativo from "@/hooks/use-clientes"; Dialog, DialogTrigger, DialogContent, DialogHeader, DialogTitle, DialogFooter from "@/components/ui/dialog"; Button from "@/components/ui/button" if not already imported.

    Create a reusable inline function or inline JSX pattern (not a separate file) for the "user assignment card" used twice: once for advogados, once for administrativos.

    Advogados card, added after the Procuração section:
      Title: "Advogados"
      Uses useClienteAdvogados(cliente.id) to list assigned advogados.
      Each row shows: user.nome, user.numeroCedula (if set, formatted as "Cédula: {value}"), user.email or user.telefone, and an X (remove) button that calls removeAdvogado.mutate(user.id) with a window.confirm guard.
      "Adicionar Advogado" button opens a Dialog. Dialog contains:
        A controlled <select> (styled with the selectClassName or className="border rounded p-2 w-full") populated with users from a separate useQuery that fetches GET /api/v1/users?role=ADVOGADO — BUT since no such general users endpoint may exist, instead load the list from a helper hook or use a simple text input approach.
        IMPORTANT: The available ADVOGADO users must come from the backend. Add a new hook useUsuariosPorPapel(role: string) in use-clientes.ts that calls GET /api/v1/admin/utilizadores (the existing admin endpoint) filtered client-side by role, OR use UserRepository's findByTenantIdAndRoleName via a new lightweight GET /api/v1/admin/utilizadores?role=ADVOGADO query param if the admin endpoint supports it. Read the admin endpoint to determine the right path before implementing. If uncertain, fetch all users from the admin endpoint and filter by roles in JS.
        Select shows users by nome. On select change, store selectedUserId in local state.
        "Adicionar" button in DialogFooter calls addAdvogado.mutate(selectedUserId) then closes dialog.

    Administrativos card: identical structure but uses administrativos hooks and title "Administrativos".

    Keep both modals short (title + select + submit = 3 elements) per Risk 6 in RESEARCH.md.
  </action>
  <verify>
    <automated>cd web && pnpm build 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - page.tsx contains useClienteAdvogados and useClienteAdministrativos imports
    - page.tsx contains "Adicionar Advogado" button text
    - page.tsx contains DialogContent with a select element for advogados modal
    - page.tsx contains "Administrativos" section heading text
    - pnpm build exits 0
  </acceptance_criteria>
  <done>Both assignment cards render on detail page with add/remove modals; pnpm build exits 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → window.open(presigned URL) | URL from backend presigned response; opened in new tab |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-59-09 | Information Disclosure | Procuração presigned URL in browser | accept | Short-lived URL (3600s); same as existing documento download pattern |
| T-59-10 | Tampering | Remove advogado via UI without confirmation | mitigate | window.confirm guard before calling removeAdvogado.mutate |
| T-59-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed |
</threat_model>

<verification>
cd web && pnpm build
Expected: exit 0, no type errors

Manual: Start dev server (pnpm dev), visit /clientes/{id}:
- Expect yellow "Procuração em falta" badge when procuracao_key is null
- Expect "Advogados" and "Administrativos" cards with empty lists
- Expect "Adicionar Advogado" button opens modal with select element
</verification>

<success_criteria>
pnpm build exits 0. Detail page renders procuração card, advogados card, and administrativos card without runtime errors.
</success_criteria>

<output>
Create .planning/phases/LEXCV-59-procuracao-intake/59-05-SUMMARY.md when done.
</output>
