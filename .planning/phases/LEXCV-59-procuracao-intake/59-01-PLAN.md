---
phase: "59"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/main/java/com/lexcv/models/DocumentoEntregue.java
  - backend/src/main/java/com/lexcv/models/DocumentoATratar.java
  - backend/src/main/java/com/lexcv/models/Deslocacao.java
  - backend/src/main/java/com/lexcv/models/HonorariosPropostos.java
  - backend/src/main/java/com/lexcv/models/DocumentosEntreguesConverter.java
  - backend/src/main/java/com/lexcv/models/DocumentosATratarConverter.java
  - backend/src/main/java/com/lexcv/models/DeslocacoesConverter.java
  - backend/src/main/java/com/lexcv/models/HonorariosPropostosConverter.java
  - backend/src/main/java/com/lexcv/models/Cliente.java
autonomous: true
requirements: [INT-01, INT-04, INT-05, INT-06, INT-07, PROC-01]

must_haves:
  truths:
    - "Cliente entity compiles with procuracaoKey, descricaoCaso, and all four JSON columns"
    - "JSON columns serialize/deserialize to/from their respective POJO types via AttributeConverter"
    - "ddl-auto=update adds the new columns to t_cliente on startup without errors"
  artifacts:
    - path: backend/src/main/java/com/lexcv/models/DocumentoEntregue.java
      provides: POJO for documentos_entregues JSON column
    - path: backend/src/main/java/com/lexcv/models/HonorariosPropostos.java
      provides: POJO for honorarios_propostos JSON column (BigDecimal total)
    - path: backend/src/main/java/com/lexcv/models/Cliente.java
      provides: Extended entity with procuracaoKey, descricaoCaso, JSON columns
  key_links:
    - from: Cliente.java
      to: DocumentosEntreguesConverter.java
      via: "@Convert annotation on documentosEntregues field"
---

<objective>
Create the four POJO classes and four AttributeConverter implementations for the JSON intake columns, then extend Cliente.java with all Phase 59 fields: procuracaoKey, descricaoCaso, and the four JSON-backed fields.

Purpose: Establishes the persistence foundation that all Wave 2 endpoints depend on. Once these classes exist and the app starts, the t_cliente table will have the new columns.
Output: 8 new Java files + modified Cliente.java.
</objective>

<execution_context>
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/LEXCV-59-procuracao-intake/59-CONTEXT.md
@.planning/phases/LEXCV-59-procuracao-intake/59-RESEARCH.md

<interfaces>
<!-- DadosTipo pattern — replicate exactly for each new POJO -->
From backend/src/main/java/com/lexcv/models/DadosTipo.java:
  @Data @NoArgsConstructor @AllArgsConstructor @Builder
  @JsonInclude(JsonInclude.Include.NON_NULL)
  public class DadosTipo { ... }

From backend/src/main/java/com/lexcv/models/DadosTipoConverter.java:
  @Converter
  public class DadosTipoConverter implements AttributeConverter<DadosTipo, String> {
    private static final ObjectMapper MAPPER = new ObjectMapper();
    // convertToDatabaseColumn: null -> null, else MAPPER.writeValueAsString
    // convertToEntityAttribute: null/blank -> null, else MAPPER.readValue(dbData, DadosTipo.class)
  }

<!-- For list converters, change readValue call to: -->
  MAPPER.readValue(dbData, new TypeReference<List<DocumentoEntregue>>(){})
  (import com.fasterxml.jackson.core.type.TypeReference)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create POJO classes and AttributeConverters for JSON intake columns</name>
  <files>
    backend/src/main/java/com/lexcv/models/DocumentoEntregue.java,
    backend/src/main/java/com/lexcv/models/DocumentoATratar.java,
    backend/src/main/java/com/lexcv/models/Deslocacao.java,
    backend/src/main/java/com/lexcv/models/HonorariosPropostos.java,
    backend/src/main/java/com/lexcv/models/DocumentosEntreguesConverter.java,
    backend/src/main/java/com/lexcv/models/DocumentosATratarConverter.java,
    backend/src/main/java/com/lexcv/models/DeslocacoesConverter.java,
    backend/src/main/java/com/lexcv/models/HonorariosPropostosConverter.java
  </files>
  <read_first>
    backend/src/main/java/com/lexcv/models/DadosTipo.java
    backend/src/main/java/com/lexcv/models/DadosTipoConverter.java
  </read_first>
  <action>
    Create four POJO classes in package com.lexcv.models, each following the exact DadosTipo pattern (Lombok @Data, @NoArgsConstructor, @AllArgsConstructor, @Builder, @JsonInclude(JsonInclude.Include.NON_NULL)):

    DocumentoEntregue: fields String descricao, String data
    DocumentoATratar: field String descricao
    Deslocacao: fields String descricao, String local, String data
    HonorariosPropostos: fields BigDecimal total, String totalPorExtenso, String previsao (per planning decision: total is BigDecimal, not String)

    Create four AttributeConverter classes in the same package, each following DadosTipoConverter pattern:
    - DocumentosEntreguesConverter implements AttributeConverter<List<DocumentoEntregue>, String> — use MAPPER.readValue(dbData, new TypeReference<List<DocumentoEntregue>>(){}) in convertToEntityAttribute; handle null/blank returning null
    - DocumentosATratarConverter implements AttributeConverter<List<DocumentoATratar>, String> — same pattern
    - DeslocacoesConverter implements AttributeConverter<List<Deslocacao>, String> — same pattern
    - HonorariosPropostosConverter implements AttributeConverter<HonorariosPropostos, String> — single-object (not list), same as DadosTipoConverter structure

    Add import com.fasterxml.jackson.core.type.TypeReference to the three list converters.
    Add @Converter annotation to each converter class.
  </action>
  <verify>
    <automated>cd backend && mvn -DskipTests package -q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - DocumentoEntregue.java, DocumentoATratar.java, Deslocacao.java, HonorariosPropostos.java exist in backend/src/main/java/com/lexcv/models/
    - HonorariosPropostos.java contains "private BigDecimal total" (not String)
    - All four converter classes exist in backend/src/main/java/com/lexcv/models/
    - List converters contain "new TypeReference" in their convertToEntityAttribute method
    - mvn -DskipTests package exits 0
  </acceptance_criteria>
  <done>Eight new Java files compile cleanly with mvn -DskipTests package.</done>
</task>

<task type="auto">
  <name>Task 2: Extend Cliente.java with all Phase 59 fields</name>
  <files>
    backend/src/main/java/com/lexcv/models/Cliente.java
  </files>
  <read_first>
    backend/src/main/java/com/lexcv/models/Cliente.java
    backend/src/main/java/com/lexcv/models/DadosTipo.java
  </read_first>
  <action>
    Add the following fields to the Cliente entity class (package com.lexcv.models). All fields are nullable (no @Column(nullable = false)).

    Procuração field (per D-04):
      @Column(name = "procuracao_key")
      private String procuracaoKey;

    Intake plain-text field (per INT-01):
      @Column(name = "descricao_caso", columnDefinition = "TEXT")
      private String descricaoCaso;

    Four JSON intake fields (per D-12), each using @Convert with the corresponding converter:
      @Column(name = "documentos_entregues", columnDefinition = "TEXT")
      @Convert(converter = DocumentosEntreguesConverter.class)
      private List<DocumentoEntregue> documentosEntregues;

      @Column(name = "documentos_a_tratar", columnDefinition = "TEXT")
      @Convert(converter = DocumentosATratarConverter.class)
      private List<DocumentoATratar> documentosATratar;

      @Column(name = "deslocacoes", columnDefinition = "TEXT")
      @Convert(converter = DeslocacoesConverter.class)
      private List<Deslocacao> deslocacoes;

      @Column(name = "honorarios_propostos", columnDefinition = "TEXT")
      @Convert(converter = HonorariosPropostosConverter.class)
      private HonorariosPropostos honorariosPropostos;

    Do NOT add procuracaoKey to any PUT handler — that is handled by its own endpoint (Wave 2). Lombok @Data already on the class will generate getters/setters for all new fields automatically.

    Add necessary imports: jakarta.persistence.Convert, java.util.List, and the new POJO/converter class names.
  </action>
  <verify>
    <automated>cd backend && mvn -DskipTests package -q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Cliente.java contains "private String procuracaoKey"
    - Cliente.java contains "private String descricaoCaso"
    - Cliente.java contains "private List<DocumentoEntregue> documentosEntregues"
    - Cliente.java contains "private HonorariosPropostos honorariosPropostos"
    - Cliente.java contains "@Convert(converter = HonorariosPropostosConverter.class)"
    - mvn -DskipTests package exits 0
  </acceptance_criteria>
  <done>Cliente.java compiles with all six new fields; mvn -DskipTests package exits 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP request → Jackson deserialization | Untrusted JSON enters via PUT /clientes/{id} |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-59-01 | Tampering | Cliente.procuracaoKey via PUT body | mitigate | Wave 2: updateCliente handler must NOT call setProcuracaoKey(); field is write-protected via its own endpoint |
| T-59-SC | Tampering | npm/pip/cargo installs | accept | No new package installs in this plan |
</threat_model>

<verification>
Run: cd backend && mvn -DskipTests package
Expected: BUILD SUCCESS

Run: grep -l "procuracaoKey\|documentosEntregues\|honorariosPropostos" backend/src/main/java/com/lexcv/models/Cliente.java
Expected: file listed
</verification>

<success_criteria>
mvn -DskipTests package exits 0 with all 8 new Java files present and Cliente.java containing all 6 new fields.
</success_criteria>

<output>
Create .planning/phases/LEXCV-59-procuracao-intake/59-01-SUMMARY.md when done.
</output>
