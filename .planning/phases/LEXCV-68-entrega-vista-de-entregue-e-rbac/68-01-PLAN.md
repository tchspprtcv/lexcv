---
phase: 68-entrega-vista-de-entregue-e-rbac
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/hooks/use-pareceres.ts
  - web/src/app/(dashboard)/pareceres/[id]/page.tsx
  - web/src/app/(dashboard)/pareceres/nova/page.tsx
autonomous: true
requirements: [PARC-14, PARC-15, PARC-16]

must_haves:
  truths:
    - "Advogado responsável ou ADMIN vê o botão 'Entregar Parecer' apenas quando status != CONCLUIDO, e confirma via AlertDialog que enfatiza irreversibilidade"
    - "O diálogo de entrega obriga a seleccionar qual versão (de entre as versões desta solicitação já carregadas) é entregue como final"
    - "Após confirmar, a solicitação passa a CONCLUIDO e a UI reflecte isso sem reload (botão desaparece, bloco 'Parecer Entregue' aparece)"
    - "Uma solicitação CONCLUIDO expõe o bloco 'Parecer Entregue' com a versão final, autor e data (derivados da própria versão), e link de anexo"
    - "Uma solicitação CONCLUIDO é só-leitura na UI: sem botão de entrega e sem form de nova versão, independentemente do papel"
    - "Todos os CardTitle sob /pareceres têm text-lg font-bold; o marcador de timeline usa cor neutra (não accent)"
  artifacts:
    - path: "web/src/hooks/use-pareceres.ts"
      provides: "useEntregarParecer mutation hook (PUT query-param, no body, cascading invalidation)"
      contains: "useEntregarParecer"
    - path: "web/src/app/(dashboard)/pareceres/[id]/page.tsx"
      provides: "Entrega AlertDialog trigger + version selector + Parecer Entregue block + fixes"
      contains: "Entregar Parecer"
    - path: "web/src/app/(dashboard)/pareceres/nova/page.tsx"
      provides: "CardTitle typography fix"
      contains: "text-lg font-bold"
  key_links:
    - from: "web/src/app/(dashboard)/pareceres/[id]/page.tsx"
      to: "useEntregarParecer"
      via: "mutateAsync with versaoFinalId from local state"
      pattern: "useEntregarParecer"
    - from: "useEntregarParecer"
      to: "PUT /pareceres/solicitacoes/{id}/entregar"
      via: "apiFetch query param, no JSON body"
      pattern: "entregar\\?versaoFinalId="
    - from: "entrega trigger visibility"
      to: "backend isAdmin || isResponsavel"
      via: "canEditPareceres && isResponsavelOuAdmin && !isConcluido"
      pattern: "isResponsavelOuAdmin"
---

<objective>
Fechar o ciclo do parecer na UI: acção de entrega irreversível (PARC-14), vista dedicada "Parecer Entregue" após entrega (PARC-15, fecha o gap PARC-09 do v2.5), e auditoria RBAC/só-leitura em todas as acções de `/pareceres` (PARC-16). Inclui as duas correcções cross-cutting obrigatórias já flagged em 65/66/67: `text-lg font-bold` em todos os CardTitle do módulo e o marcador de timeline neutro (não accent).

Purpose: tornar o parecer utilizável de ponta a ponta pela aplicação e alinhar exactamente a visibilidade das acções da UI com os `@PreAuthorize` do backend.
Output: `useEntregarParecer` hook, secção de entrega + bloco "Parecer Entregue" no detalhe, correcções de tipografia/accent.
</objective>

<execution_context>
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/LEXCV-68-entrega-vista-de-entregue-e-rbac/68-CONTEXT.md
@.planning/phases/LEXCV-68-entrega-vista-de-entregue-e-rbac/68-PATTERNS.md
@.planning/phases/LEXCV-68-entrega-vista-de-entregue-e-rbac/68-UI-SPEC.md

<interfaces>
<!-- Backend contract (ParecerController.entregarSolicitacao, lines 353-384). versaoFinalId is @RequestParam UUID, NOT a JSON body field. -->
PUT /api/v1/pareceres/solicitacoes/{id}/entregar?versaoFinalId={uuid}
  @PreAuthorize("hasAuthority('pareceres:edit')") + instance check: isAdmin || isResponsavel
  400 if already CONCLUIDO; 404 if versão not belonging to this solicitação; 403 if not admin/responsavel
  200 OK -> updated ParecerSolicitacao (status=CONCLUIDO, versaoFinalId set)

<!-- Existing hooks in web/src/hooks/use-pareceres.ts -->
useParecer(id) -> query ["pareceres","detail",id] -> ParecerSolicitacao
useParecerVersoes(solicitacaoId) -> query ["pareceres","versoes",id] -> ParecerVersao[] (chronological ascending)
useCreateParecer() pattern: plain apiFetch mutation + single invalidate ["pareceres","list"]
apiFetch<T>(path, { method }) from "@/lib/api"

<!-- ParecerVersao fields used: id, numeroVersao, conteudo, caminhoAnexo, criadoPorId, createdAt -->
<!-- ParecerSolicitacao fields used: status, advogadoId, versaoFinalId. NO entregue-por/entregue-em fields exist. -->

<!-- Existing detail-page derivations (page.tsx lines 134-141) — reuse verbatim, do NOT re-derive: -->
me = permissions.data
canEditPareceres = permissions.can.edit("pareceres")
isResponsavelOuAdmin = me?.roles.includes("ADMIN") || (parecer.data?.advogadoId === me?.id)
isConcluido = parecer.data?.status === "CONCLUIDO"

<!-- AnexoLink component already in page.tsx (lines 81-110): <AnexoLink solicitacaoId versaoId caminhoAnexo /> -->
<!-- resolveUserNome(userId), formatDateTime(v), formatDate(v) already in page.tsx -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: useEntregarParecer hook</name>
  <files>web/src/hooks/use-pareceres.ts</files>
  <read_first>
    - web/src/hooks/use-pareceres.ts lines 71-84 (useCreateParecer — plain apiFetch mutation shape to copy)
    - web/src/hooks/use-pareceres.ts lines 1-5 (imports: apiFetch, ParecerSolicitacao already imported)
    - 68-PATTERNS.md lines 234-254 (composed useEntregarParecer guidance)
  </read_first>
  <action>
    Add exported `useEntregarParecer(solicitacaoId: string)` after the existing mutation hooks in use-pareceres.ts (PARC-14). It returns a `useMutation` whose `mutationFn` takes `{ versaoFinalId: string }` and calls `apiFetch<ParecerSolicitacao>` against `` `/pareceres/solicitacoes/${encodeURIComponent(solicitacaoId)}/entregar?versaoFinalId=${encodeURIComponent(payload.versaoFinalId)}` `` with `{ method: "PUT" }` — NO `body`, NO `JSON.stringify` (versaoFinalId is a query param per the backend `@RequestParam`, per D-CONTEXT entrega decision). `onSuccess` awaits `Promise.all` invalidating exactly `["pareceres","detail",solicitacaoId]` and `["pareceres","list"]` — do NOT invalidate the `["pareceres","versoes",...]` key (entrega mutates the solicitação status/versaoFinalId, not a version row, per CONTEXT.md). Reuse the already-imported `apiFetch`, `useMutation`, `useQueryClient`, and `ParecerSolicitacao` — add no new imports.
  </action>
  <verify>
    <automated>cd web && node -e "const s=require('fs').readFileSync('src/hooks/use-pareceres.ts','utf8'); const body=s.split('\n').filter(l=>!l.trim().startsWith('//')).join('\n'); process.exit((/export function useEntregarParecer/.test(body) && /entregar\?versaoFinalId=/.test(body) && !/JSON\.stringify[\s\S]*entregar/.test(body)) ? 0 : 1)"</automated>
  </verify>
  <acceptance_criteria>
    - `useEntregarParecer` exported; request is `PUT` with `versaoFinalId` in the query string and no JSON body
    - onSuccess invalidates `detail` + `list` keys only (not `versoes`)
    - `cd web && npx tsc --noEmit` passes
  </acceptance_criteria>
  <done>Hook exists, typechecks, matches backend query-param contract.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Entrega AlertDialog + version selector + Parecer Entregue block + RBAC/read-only</name>
  <files>web/src/app/(dashboard)/pareceres/[id]/page.tsx</files>
  <read_first>
    - web/src/app/(dashboard)/pareceres/[id]/page.tsx (full file — you edit ParecerDetailContent, lines 112-283)
    - web/src/app/(dashboard)/agenda/[id]/page.tsx lines 106-117 and 165-216 (AlertDialog + handler analog)
    - web/src/components/ui/alert-dialog.tsx (confirm exported members)
    - 68-PATTERNS.md lines 19-158 (entrega dialog + Parecer Entregue block guidance)
    - 68-UI-SPEC.md lines 52-102, 110-128 (typography mandate, accent rule, copy contract)
  </read_first>
  <action>
    Edit `pareceres/[id]/page.tsx` (PARC-14, PARC-15, PARC-16). All copy in Portuguese per UI-SPEC Copywriting Contract.

    1. Add an import block for `AlertDialog, AlertDialogAction, AlertDialogCancel, AlertDialogContent, AlertDialogDescription, AlertDialogFooter, AlertDialogHeader, AlertDialogTitle, AlertDialogTrigger` from `@/components/ui/alert-dialog`, and import `useEntregarParecer` from `@/hooks/use-pareceres`.

    2. Timeline-dot accent fix (line 221): change `bg-blue-600` to `bg-slate-400 dark:bg-slate-500` on the version dot `<span>`. Do NOT colour-code by status.

    3. CardTitle typography fix: change the "Dados" CardTitle (line 161) and "Versões" CardTitle (line 194) from `className="font-bold"` to `className="text-lg font-bold"`. Any new CardTitle you author in this task must also use `text-lg font-bold`. Do NOT touch `AlertDialogTitle` weight (it stays at the shared component default `font-semibold` per UI-SPEC grandfathered exception).

    4. Compute `showEntregarTrigger = !permissions.isLoading && canEditPareceres && isResponsavelOuAdmin && !isConcluido` alongside the existing `showNovaVersaoForm` (line 140), reusing the existing `isResponsavelOuAdmin`/`isConcluido`/`canEditPareceres` derivations verbatim — this mirrors the backend `isAdmin || isResponsavel` + `pareceres:edit` gate exactly (PARC-16).

    5. Entrega affordance: inside `ParecerDetailContent`, when `showEntregarTrigger`, render an `AlertDialog` controlled by local `React.useState(false)` (`confirmOpen`). Extract into a small component (e.g. `EntregarParecerDialog`) receiving `solicitacaoId={id}` and `versoes` data so `useEntregarParecer(id)` and the selector state live there. Trigger `<Button>` copy "Entregar Parecer" styled `variant="destructive"` (or `bg-destructive text-destructive-foreground hover:bg-destructive/90`) per UI-SPEC accent rule (accent blue is NOT spent here). Dialog: `AlertDialogTitle` "Entregar Parecer"; `AlertDialogDescription` exactly "Esta ação é irreversível. Depois de entregue, o parecer não pode receber novas versões nem ser reaberto."; then a version-selection control — prompt "Selecione a versão a entregar como final:" followed by a native `<select>` (or Label+radio) listing the passed `versoes.data` options as "Versão {numeroVersao} — {formatDateTime(createdAt)}", value = `versao.id`. Use controlled `React.useState<string | null>(selectedVersaoId)` defaulting to the most recent version (`versoes.data[versoes.data.length - 1]?.id`). The selector must ONLY list versions from this solicitação's already-fetched `versoes.data` — never a free-text/arbitrary ID field (versaoFinalId tampering mitigation).

    6. Entrega handler `handleEntregar`: guard `if (!selectedVersaoId) return;`, clear `entregaError`, `await entregar.mutateAsync({ versaoFinalId: selectedVersaoId })`, on success `toast.success("Parecer entregue com sucesso.")` and `setConfirmOpen(false)` (do NOT navigate away). On error set `entregaError` to the message and `toast.error(...)` (dual-channel) with fallback copy "Não foi possível entregar o parecer. Verifique a ligação e tente novamente." Render `{entregaError ? <p className="text-sm text-red-600 px-1">{entregaError}</p> : null}` above the footer. `AlertDialogCancel` "Cancelar" `disabled={entregar.isPending}`; `AlertDialogAction` `className="bg-destructive text-destructive-foreground hover:bg-destructive/90"`, `onClick={(e)=>{e.preventDefault(); void handleEntregar();}}`, label "Confirmar Entrega" / pending "A entregar...".

    7. "Parecer Entregue" block (PARC-15): replace the current placeholder `isConcluido` branch (lines 265-275, the "Parecer já entregue" text card) with a richer card. Look up `const versaoFinal = versoes.data?.find((v) => v.id === parecer.data?.versaoFinalId);` (no new fetch). Card with `CardTitle className="text-lg font-bold"` "Parecer Entregue", a `Badge variant="green"` as the ONLY colour accent, and fields: "Versão {numeroVersao}", "Elaborado por {resolveUserNome(versaoFinal.criadoPorId)} em {formatDateTime(versaoFinal.createdAt)}" (derived ONLY from the version's own fields — the backend has NO entregue-por/entregue-em field; do NOT fabricate one), and `<AnexoLink solicitacaoId={id} versaoId={versaoFinal.id} caminhoAnexo={versaoFinal.caminhoAnexo} />`. If `versaoFinal` is undefined (edge: versoes still loading), show a neutral "A carregar versão final..." line rather than crashing. This block is the read-only signal — do NOT add a second "apenas de leitura" banner.

    8. Read-only enforcement (PARC-16): the existing render precedence already gates the Nova Versão form behind `!isConcluido`; verify the entrega trigger is ALSO omitted when `isConcluido` (via `showEntregarTrigger`) and that after `mutateAsync` succeeds the cache invalidation flips `parecer.data.status` to CONCLUIDO, causing the trigger to disappear and the "Parecer Entregue" block to render without a manual reload. Omit the trigger entirely (no disabled button) when `!showEntregarTrigger` — "no dead buttons" convention.

    No fenced code in your edits beyond what the file already contains; follow the agenda AlertDialog structure and the existing page conventions.
  </action>
  <verify>
    <automated>cd web && node -e "const s=require('fs').readFileSync('src/app/(dashboard)/pareceres/[id]/page.tsx','utf8'); const ok = /useEntregarParecer/.test(s) && /Entregar Parecer/.test(s) && /Esta ação é irreversível/.test(s) && /Parecer Entregue/.test(s) && /bg-slate-400 dark:bg-slate-500/.test(s) && !/rounded-full shrink-0 bg-blue-600/.test(s) && (s.match(/CardTitle className=\"text-lg font-bold\"/g)||[]).length >= 3 && !/CardTitle className=\"font-bold\"/.test(s) && /versaoFinalId: selectedVersaoId/.test(s); process.exit(ok?0:1)"</automated>
  </verify>
  <acceptance_criteria>
    - Entrega AlertDialog present with irreversibility copy and a version `<select>` sourced only from `versoes.data`
    - `versaoFinalId` passed to the mutation from selector state (no free-text ID)
    - "Parecer Entregue" block renders final version data derived from the version's own fields (no fabricated entregue-por/em)
    - Timeline dot uses `bg-slate-400 dark:bg-slate-500`; no remaining `bg-blue-600` on the dot span
    - All CardTitle on this page use `text-lg font-bold` (≥3 instances: Dados, Versões, Parecer Entregue/entrega card); zero `CardTitle className="font-bold"` remaining
    - `cd web && npx tsc --noEmit` passes; `cd web && pnpm lint` reports no NEW errors referencing this file (pre-existing baseline unchanged)
  </acceptance_criteria>
  <done>Entrega flow, Parecer Entregue view, RBAC/read-only gating, and both mandated fixes all in place and typecheck-clean.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: CardTitle typography fix on nova page (module-wide completion)</name>
  <files>web/src/app/(dashboard)/pareceres/nova/page.tsx</files>
  <read_first>
    - web/src/app/(dashboard)/pareceres/nova/page.tsx line 121 (CardTitle "Dados da Solicitação", currently no className)
    - 68-UI-SPEC.md lines 60-64, 176-178 (total-module CardTitle mandate)
  </read_first>
  <action>
    Complete the module-wide CardTitle typography mandate (PARC-16 UI consistency, per UI-SPEC): change line 121 `<CardTitle>Dados da Solicitação</CardTitle>` to `<CardTitle className="text-lg font-bold">Dados da Solicitação</CardTitle>`. This is the last remaining unstyled CardTitle in `/pareceres/**` (the list page has no CardTitle — grep-confirmed; the detail page is fixed in Task 2). Do not touch anything else on this page.
  </action>
  <verify>
    <automated>cd web && node -e "const s=require('fs').readFileSync('src/app/(dashboard)/pareceres/nova/page.tsx','utf8'); process.exit((/CardTitle className=\"text-lg font-bold\">Dados da Solicitação/.test(s) && !/<CardTitle>Dados da Solicitação/.test(s)) ? 0 : 1)"</automated>
  </verify>
  <acceptance_criteria>
    - "Dados da Solicitação" CardTitle carries `text-lg font-bold`; no bare `<CardTitle>` for it remains
    - `cd web && npx tsc --noEmit` passes
    - grep across `web/src/app/(dashboard)/pareceres/**` finds zero CardTitle without `text-lg font-bold`
  </acceptance_criteria>
  <done>Every CardTitle in the /pareceres module carries the mandated typography; the 3-phase recurring defect is closed module-wide.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → Next rewrite → Spring API | Untrusted user input (versaoFinalId, action intent) crosses into the entrega endpoint |
| UI RBAC gate ↔ backend @PreAuthorize | The visible affordance must not misrepresent authorization; backend is the true enforcer |

## STRIDE Threat Register (ASVS L1 — security_block_on: high)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-68-01 | Elevation of Privilege | entrega trigger visibility (pareceres/[id]/page.tsx) | mitigate | Trigger rendered only when `canEditPareceres && isResponsavelOuAdmin && !isConcluido`, mirroring backend `hasAuthority('pareceres:edit')` + `isAdmin || isResponsavel`. Backend remains the enforcer (403 on mismatch); UI must not show a false affordance. High-severity: gate verified in Task 2. |
| T-68-02 | Tampering | versaoFinalId query param | mitigate | Dialog version selector is populated exclusively from this solicitação's already-fetched `versoes.data`; no free-text/arbitrary-ID input. Backend independently rejects (404) a versão not belonging to the solicitação — defense-in-depth. |
| T-68-03 | Tampering / Immutability | stale-state after entrega (status flip) | mitigate | `useEntregarParecer` onSuccess invalidates `detail` + `list`; the CONCLUIDO branch omits the entrega trigger AND the Nova Versão form. Verified the UI flips to read-only on cache refetch without manual reload (T-68 acceptance). |
| T-68-04 | Repudiation / Irreversibility UX | AlertDialog confirmation copy | mitigate | Mandatory explicit irreversibility statement in the dialog body; destructive-weight styling on confirm; no silent default — explicit version confirmation required before the one-way action. |
| T-68-05 | Information Disclosure | fabricated entrega metadata | accept/mitigate | "Parecer Entregue" block derives author/date only from the final version's own `criadoPorId`/`createdAt`; no fabricated entregue-por/em field (backend has none). Prevents misleading provenance claims. |
| T-68-SC | Tampering | npm/pip/cargo installs | mitigate | No new dependencies added this phase (confirmed: reuses existing alert-dialog, apiFetch, TanStack Query). No install tasks; legitimacy gate N/A. |
</threat_model>

<verification>
- `cd web && npx tsc --noEmit` passes with all three files changed
- `cd web && pnpm lint` reports no NEW problems referencing the touched files (Phase 67 baseline of 23 pre-existing problems unchanged — confirm via git stash if in doubt)
- Manual (human_verification, deferred per project convention): entrega button visible only for responsável/ADMIN pre-entrega; dialog forces version choice; after confirm the page flips to "Parecer Entregue" read-only without reload; CONCLUIDO parecer shows no entrega/nova-versão affordance for any role
</verification>

<success_criteria>
1. Responsável/ADMIN entrega a versão final via AlertDialog irreversível com selecção de versão (PARC-14)
2. Solicitação CONCLUIDO expõe bloco "Parecer Entregue" com versão final, autor/data derivados e anexo (PARC-15)
3. Solicitação CONCLUIDO é só-leitura na UI; todas as acções gated por scope + instance check espelhando o backend (PARC-16)
4. CardTitle module-wide e marcador de timeline neutro corrigidos (mandated cross-cutting fixes)
</success_criteria>

<output>
Create `.planning/phases/LEXCV-68-entrega-vista-de-entregue-e-rbac/68-01-SUMMARY.md` when done
</output>
