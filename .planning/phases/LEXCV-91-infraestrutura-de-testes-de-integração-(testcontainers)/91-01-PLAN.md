---
phase: 91-infraestrutura-de-testes-de-integracao
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/pom.xml
  - backend/src/test/resources/application.properties
  - backend/src/test/java/com/lexcv/repositories/NotificacaoRepositoryIT.java
autonomous: true
requirements: [TEST-01]

must_haves:
  truths:
    - "Running `mvn -B test -Dtest=NotificacaoRepositoryIT` starts a real postgres:16-alpine container and the test passes"
    - "buscarPorFiltros returns correctly tenant+destinatario scoped, categoria/lida filtered, paginated, created_at DESC ordered results against real PostgreSQL"
    - "The test runs with no MINIO_ENDPOINT / no production env var set (proves @DataJpaTest slice excludes MinioConfig/SecurityConfig)"
    - "The 3 pre-existing pure-unit tests (RiscoPrazoServiceTest, NotificacaoServiceTest, AlertasDiariosJobTest) still pass unchanged"
  artifacts:
    - path: "backend/pom.xml"
      provides: "Testcontainers test-scoped deps (BOM-managed, no explicit version) + maven-failsafe-plugin binding so *IT run under mvn verify"
      contains: "spring-boot-testcontainers"
    - path: "backend/src/test/resources/application.properties"
      provides: "Placeholder-free datasource override + ddl-auto=create-drop so the container schema builds from entity annotations, with zero dependency on .env"
      contains: "create-drop"
    - path: "backend/src/test/java/com/lexcv/repositories/NotificacaoRepositoryIT.java"
      provides: "@DataJpaTest + @Testcontainers + @ServiceConnection integration test for the native buscarPorFiltros query"
      min_lines: 60
  key_links:
    - from: "backend/src/test/java/com/lexcv/repositories/NotificacaoRepositoryIT.java"
      to: "PostgreSQLContainer(\"postgres:16-alpine\")"
      via: "@Container + @ServiceConnection static field auto-wiring the DataSource"
      pattern: "@ServiceConnection"
    - from: "backend/src/test/java/com/lexcv/repositories/NotificacaoRepositoryIT.java"
      to: "NotificacaoRepository.buscarPorFiltros"
      via: "@Autowired repository call under assertion"
      pattern: "buscarPorFiltros"
---

<objective>
Establish the backend's first-ever real-PostgreSQL integration-test infrastructure (Testcontainers) and use it to close the TEST-01 risk: `NotificacaoRepository.buscarPorFiltros` is the first `nativeQuery=true` + `Pageable` combination in this codebase and its Postgres-specific `CAST(:param AS text)` / `CAST(:param AS boolean)` null-guard idiom has never executed against a real Postgres engine.

Purpose: A native query built around a Postgres-specific type-inference quirk can pass against H2 for the wrong reason or fail for a reason unrelated to production. Only a real `postgres:16-alpine` container (the exact prod/dev image) gives a trustworthy result.
Output: 3 new/modified test-infra artifacts (pom deps + failsafe binding, shared test `application.properties`, `NotificacaoRepositoryIT`) that later plans (91-02) build on directly.
</objective>

<execution_context>
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/LEXCV-91-infraestrutura-de-testes-de-integração-(testcontainers)/91-CONTEXT.md
@.planning/research/STACK.md
@.planning/research/ARCHITECTURE.md
@.planning/research/PITFALLS.md
@backend/pom.xml
@backend/src/main/java/com/lexcv/repositories/NotificacaoRepository.java
@backend/src/main/java/com/lexcv/models/Notificacao.java
@backend/src/main/resources/application.yml

<interfaces>
<!-- Extracted from the codebase. Executor should use these directly — no exploration needed. -->

From backend/src/main/java/com/lexcv/repositories/NotificacaoRepository.java — the method under test (native query, tenant+destinatario mandatory, categoria/lida optional CAST-null-guarded, ORDER BY created_at DESC):
```
Page<Notificacao> buscarPorFiltros(UUID tenantId, UUID destinatarioId, String categoria, Boolean lida, Pageable pageable);
```

From backend/src/main/java/com/lexcv/models/Notificacao.java — @Entity @Table(name = "t_notificacao"), fields required NOT NULL for a valid save: tenantId (UUID), destinatarioId (UUID), categoria (String, e.g. "FASE_ENTRADA" / "DOCUMENTO_NOVO"), entidadeTipo (String, e.g. "processo"), entidadeId (String), titulo (String), mensagem (String, TEXT). Optional: linkUrl. lida defaults false (@Builder.Default). createdAt set by @PrePersist. Unique constraint uk_notificacao_dedup on (tenant_id, destinatario_id, entidade_tipo, entidade_id, categoria) — so distinct saved rows must differ in at least one of those columns.

From backend/pom.xml — parent is spring-boot-starter-parent:3.4.1 which transitively imports testcontainers-bom:1.20.4; maven-failsafe-plugin execution (goals integration-test + verify) is defined ONLY inside the parent's <pluginManagement>, so it does NOT run *IT classes unless the plugin is also declared in this pom's <build><plugins>.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Testcontainers test dependencies, failsafe binding, and shared test properties</name>
  <files>backend/pom.xml, backend/src/test/resources/application.properties</files>
  <read_first>
    - backend/pom.xml (current dependencies block lines 105-116, build/plugins lines 118-171)
    - backend/src/main/resources/application.yml (datasource placeholders lines 11-19, spring.config.import line 10)
    - .planning/research/STACK.md (Installation block lines 83-100; "Do NOT add testcontainers-bom" rationale line 49)
  </read_first>
  <action>
    In backend/pom.xml, inside the existing `<dependencies>` block, add THREE test-scoped dependencies WITHOUT any `<version>` tag (they resolve to 1.20.4 from the parent-managed testcontainers-bom, per STACK.md — do NOT add an explicit version and do NOT import testcontainers-bom at 2.0.x, which renames artifacts and breaks @ServiceConnection): `org.springframework.boot:spring-boot-testcontainers` (scope test), `org.testcontainers:junit-jupiter` (scope test), `org.testcontainers:postgresql` (scope test). Do NOT add H2 — it is explicitly rejected for this native query per CONTEXT.md.

    Also in backend/pom.xml, inside `<build><plugins>`, declare `org.apache.maven.plugins:maven-failsafe-plugin` with no version and an empty/minimal configuration. This activates the execution binding (goals integration-test + verify) that spring-boot-starter-parent defines only in `<pluginManagement>` — without this declaration, `*IT` classes are ignored by `mvn verify`. Surefire keeps running the existing `*Test` unit classes; failsafe runs the new `*IT` integration classes. This is the deliberate fast-unit / slow-container separation.

    Create backend/src/test/resources/application.properties (the directory does not exist yet). It must contain: `spring.datasource.url=jdbc:postgresql://localhost:5432/lexcv_test`, `spring.datasource.username=test`, `spring.datasource.password=test`, and `spring.jpa.hibernate.ddl-auto=create-drop`. Rationale to preserve: the main application.yml datasource URL uses `${DB_HOST}`/`${DB_PORT}`/`${DB_NAME}` placeholders with no defaults, which throw "could not resolve placeholder" when no .env exists (CI). These placeholder-free literals prevent that; @ServiceConnection then overrides them at runtime with the container's real JDBC URL/user/password (ConnectionDetails beans take precedence over spring.datasource.* properties). ddl-auto=create-drop is required because Hibernate defaults to `none` for a non-embedded datasource, and the container schema (including uk_notificacao_dedup and the t_parecer_versao unique constraint used by 91-02) must be created fresh from the @Entity annotations each run. Do NOT set MINIO/JWT/CORS properties — @DataJpaTest never instantiates those beans.
  </action>
  <verify>
    <automated>cd backend && mvn -q -DskipTests package 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `mvn -q -DskipTests package` in backend/ resolves the 3 new test dependencies with no "missing version" or "could not resolve" errors (BOM-managed 1.20.4).
    - `backend/src/test/resources/application.properties` exists and contains exactly the 4 keys above; `grep -c "create-drop" backend/src/test/resources/application.properties` returns 1.
    - `grep -c "spring-boot-testcontainers" backend/pom.xml` returns 1 and `grep -c "maven-failsafe-plugin" backend/pom.xml` returns 1.
    - No `<version>` tag appears on any of the 3 testcontainers/spring-boot-testcontainers dependency entries (verify by inspection of the added block).
    - No H2 dependency (`com.h2database`) was added anywhere in pom.xml.
  </acceptance_criteria>
  <done>Testcontainers test deps + failsafe binding are in pom.xml with no explicit versions, the shared test application.properties exists with placeholder-free datasource + create-drop, and the project still packages.</done>
</task>

<task type="auto">
  <name>Task 2: Write NotificacaoRepositoryIT against a real postgres:16-alpine container</name>
  <files>backend/src/test/java/com/lexcv/repositories/NotificacaoRepositoryIT.java</files>
  <read_first>
    - backend/src/main/java/com/lexcv/repositories/NotificacaoRepository.java (the full buscarPorFiltros native query + signature, lines 23-39)
    - backend/src/main/java/com/lexcv/models/Notificacao.java (NOT NULL fields + uk_notificacao_dedup, lines 13-64)
    - backend/src/test/java/com/lexcv/services/NotificacaoServiceTest.java (existing test conventions: JUnit 5, package layout, Portuguese method names)
    - .planning/research/ARCHITECTURE.md (Pattern A, lines 237-261; @AutoConfigureTestDatabase(Replace.NONE) requirement, line 261)
  </read_first>
  <action>
    Create `NotificacaoRepositoryIT` in package `com.lexcv.repositories` under backend/src/test/java. Annotate the class with `@DataJpaTest`, `@AutoConfigureTestDatabase(replace = AutoConfigureTestDatabase.Replace.NONE)` (MANDATORY — without it @DataJpaTest swaps in an embedded DB and silently ignores the container), and `@Testcontainers`. Declare a `static PostgreSQLContainer<?>` field annotated with both `@Container` and `@ServiceConnection`, constructed as `new PostgreSQLContainer<>("postgres:16-alpine")` (the exact prod/dev image tag). `@Autowired` the `NotificacaoRepository`.

    Write test methods (JUnit 5, plain `Assertions.*`, Portuguese names consistent with NotificacaoServiceTest) that persist a small fixture via `notificacaoRepository.save(...)` using the builder, then assert `buscarPorFiltros(...)` behaves correctly against REAL Postgres — this is the exact nativeQuery+Pageable combination that has never run against Postgres. Cover at minimum:
    1. Tenant + destinatario scoping: persist rows for two tenantIds and two destinatarioIds; assert a call for one (tenantId, destinatarioId) returns only that recipient's rows (never another tenant's or another recipient's). Vary entidadeId across rows so uk_notificacao_dedup is not tripped.
    2. Optional categoria filter with a bare null bind: call with `categoria = null` (must return all categorias for that scope — proves the `CAST(:categoria AS text) IS NULL` guard works on real Postgres) AND with a concrete categoria like "FASE_ENTRADA" (must return only matching rows).
    3. Optional lida filter: call with `lida = null` (all) and `lida = false`/`true` (proves the `CAST(:lida AS boolean)` guard).
    4. Pagination + ordering: persist enough rows (e.g. 3+) to request `PageRequest.of(0, 2)`; assert `getTotalElements()` equals the full scoped count, `getContent().size()` equals the page size, and ordering is created_at DESC.

    Use distinct fixture values so no two persisted rows collide on uk_notificacao_dedup (tenant_id, destinatario_id, entidade_tipo, entidade_id, categoria). Do NOT mock the repository — the whole point is exercising the real SQL. Do NOT use @SpringBootTest (would boot MinioConfig/SecurityConfig and hit the MINIO_ENDPOINT wall).
  </action>
  <verify>
    <automated>cd backend && mvn -B test -Dtest=NotificacaoRepositoryIT 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `mvn -B test -Dtest=NotificacaoRepositoryIT` reports BUILD SUCCESS with the test class run (Tests run: N, Failures: 0, Errors: 0) and the Testcontainers log shows a `postgres:16-alpine` container starting.
    - No `MINIO_ENDPOINT` / `could not resolve placeholder` / `IllegalStateException` about missing beans appears in the output (proves the @DataJpaTest slice excludes MinioConfig).
    - The test asserts pagination totalElements and DESC ordering explicitly (source assertion: `getTotalElements` and an ordering check appear in the file).
    - The test asserts the `categoria = null` and `lida = null` null-bind branches return all scoped rows (source assertion: at least one call passes null for those params).
    - Running with an unset MINIO_ENDPOINT env var still passes (run the command in a shell with no backend/.env loaded).
  </acceptance_criteria>
  <done>NotificacaoRepositoryIT passes against a real postgres:16-alpine container, exercising tenant/destinatario scoping, both CAST-null-guarded optional filters, and Pageable ordering — with no production env var required.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Maven build → dependency resolution | New test artifacts pulled from Maven Central; version provenance matters (supply chain) |
| Test JVM → ephemeral Postgres container | Disposable localhost container, credentials generated per run by Testcontainers |
| Test config ↔ production secrets | Test must never read/require prod DB, JWT, or MinIO secrets |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-91-01 | Tampering | spring-boot-testcontainers / org.testcontainers:* deps | mitigate | Versions BOM-managed by spring-boot-starter-parent:3.4.1 (testcontainers-bom 1.20.4); no explicit version pinning to an attacker-chosen release; official Spring/Testcontainers coordinates; 2.0.x line explicitly avoided |
| T-91-02 | Information disclosure | backend/src/test/resources/application.properties | mitigate | Placeholder-free dummy datasource values only; @ServiceConnection injects ephemeral container credentials at runtime; no prod secret referenced; @DataJpaTest slice never instantiates MinioProperties/JwtProperties |
| T-91-SC | Tampering | postgres:16-alpine container image | accept | Official Postgres image, identical tag to prod/dev docker-compose; ephemeral, localhost-only, torn down after run; not a package-manager (npm/pip/cargo) install so no blocking legitimacy checkpoint required |
</threat_model>

<verification>
- `mvn -B test -Dtest=NotificacaoRepositoryIT` passes with a real postgres:16-alpine container started by Testcontainers.
- `mvn -B test` (full unit slice) still shows the 3 pre-existing Mockito/JUnit tests green (no regression from the pom changes).
- No production environment variable is required for the test to pass.
</verification>

<success_criteria>
- Testcontainers infrastructure exists in the backend for the first time (deps + failsafe binding + shared test properties).
- TEST-01 is closed: `buscarPorFiltros` is proven correct (scoping, both CAST-null-guarded filters, Pageable ordering/totals) against real PostgreSQL, not H2.
- The @DataJpaTest slice provably sidesteps the recurring MINIO_ENDPOINT blocker.
</success_criteria>

<output>
Create `.planning/phases/LEXCV-91-infraestrutura-de-testes-de-integração-(testcontainers)/91-01-SUMMARY.md` when done.
</output>
