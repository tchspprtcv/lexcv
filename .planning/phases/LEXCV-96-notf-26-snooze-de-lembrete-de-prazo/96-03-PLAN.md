---
phase: 96-notf-26-snooze-de-lembrete-de-prazo
plan: 03
type: execute
wave: 2
depends_on: [96-01]
files_modified:
  - web/src/types/notificacoes.ts
  - web/src/hooks/use-notificacoes.ts
  - web/src/components/shared/notificacao-snooze-control.tsx
  - web/src/components/shared/notification-bell.tsx
  - web/src/app/(dashboard)/notificacoes/page.tsx
autonomous: true
requirements: [NOTF-26]

must_haves:
  truths:
    - "User can snooze a notification for 1, 3, or 7 days via a Popover + RadioGroup control (fixed presets, no date picker)"
    - "After snoozing, the bell unread badge drops and the item disappears from the bell dropdown (client-side snoozedUntil presentation filter)"
    - "The snoozed item stays visible on the /notificacoes history page, marked with an 'Adiado até DD/MM' badge"
    - "PRAZO_VENCIDO notifications never show a snooze control (reuses NOTIFICACAO_CATEGORIAS_NAO_SILENCIAVEIS)"
  artifacts:
    - path: "web/src/types/notificacoes.ts"
      provides: "snoozedUntil field on the Notificacao type"
      contains: "snoozedUntil"
    - path: "web/src/hooks/use-notificacoes.ts"
      provides: "useSnoozeNotificacao mutation (PATCH .../snooze, invalidates ['notificacoes'])"
      contains: "useSnoozeNotificacao"
    - path: "web/src/components/shared/notificacao-snooze-control.tsx"
      provides: "reusable Popover+RadioGroup snooze control, hidden for non-silenciável categories"
      contains: "RadioGroup"
    - path: "web/src/components/shared/notification-bell.tsx"
      provides: "snooze control wired into dropdown rows + client-side snooze presentation filter on the bell list"
      contains: "NotificacaoSnoozeControl"
    - path: "web/src/app/(dashboard)/notificacoes/page.tsx"
      provides: "snooze control + 'Adiado até' badge on each history row"
      contains: "NotificacaoSnoozeControl"
  key_links:
    - from: "NotificacaoSnoozeControl"
      to: "useSnoozeNotificacao"
      via: "mutate({ id, dias }) on preset confirm"
      pattern: "useSnoozeNotificacao"
    - from: "NotificacaoSnoozeControl"
      to: "NOTIFICACAO_CATEGORIAS_NAO_SILENCIAVEIS"
      via: "hide control for PRAZO_VENCIDO"
      pattern: "NOTIFICACAO_CATEGORIAS_NAO_SILENCIAVEIS"
    - from: "notification-bell dropdown list"
      to: "snoozedUntil"
      via: "client-side filter removing future-snoozed items from the bell preview"
      pattern: "snoozedUntil"
---

<objective>
Deliver the NOTF-26 snooze UX: a reusable Popover + RadioGroup control (fixed 1/3/7-day presets, no
date picker) wired into both the notification bell dropdown and the `/notificacoes` history page, a
TanStack Query mutation hook, and the `snoozedUntil` type field. Snoozing an item drops the bell badge
and removes the item from the bell preview, while the item stays visible on the history page with an
"Adiado até DD/MM" badge (the UX pitfall guard: never hide a snooze without surfacing it somewhere).
PRAZO_VENCIDO shows no snooze control, mirroring the backend block.

Depends on 96-01 (the PATCH endpoint + snoozedUntil field). Runs in Wave 2 in parallel with the
backend IT plan (96-02) — zero shared files.

Purpose: NOTF-26 success criteria 1 (UI control), 2 (disappears from bell, stays in history).
Output: new snooze-control component + hook + type field + two wired surfaces.
</objective>

<execution_context>
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/LEXCV-96-notf-26-snooze-de-lembrete-de-prazo/96-CONTEXT.md
@.planning/phases/LEXCV-96-notf-26-snooze-de-lembrete-de-prazo/96-01-SUMMARY.md
@web/AGENTS.md

<interfaces>
<!-- Existing frontend shapes to build on (read the files). -->

web/src/lib/api.ts — apiFetch(path, init): sets Content-Type application/json automatically when
  init.body is present and not FormData; sends credentials: "include". For a JSON PATCH:
  apiFetch<Notificacao>(path, { method: "PATCH", body: JSON.stringify({ dias }) }).

web/src/hooks/use-notificacoes.ts — existing mutation pattern to mirror (useMarcarNotificacaoLida):
  useMutation({ mutationFn: (id) => apiFetch(...,{method:"PATCH"}), onSuccess: invalidate ["notificacoes"] }).
  Query keys are prefixed with "notificacoes" (list + unread-count) so invalidating ["notificacoes"]
  refreshes both the badge and the lists.

web/src/lib/notificacao-categoria.ts — reuse (do NOT reinvent):
  NOTIFICACAO_CATEGORIAS_NAO_SILENCIAVEIS: readonly NotificacaoCategoria[]  // ["PRAZO_VENCIDO"]
  categoriaToLabel / categoriaToBadgeVariant / isInternalLinkUrl

web/src/components/ui/popover.tsx — Popover, PopoverTrigger, PopoverContent (Radix). Already used by the bell.
web/src/components/ui/radio-group.tsx — RadioGroup, RadioGroupItem (Radix). Both already installed — NO new deps.
web/src/components/ui/button.tsx — Button.

web/src/components/shared/notification-bell.tsx — NotificationBell: uses
  useNotificacoes({ size: 10 }, { poll: true }) for the dropdown list and useNotificacoesUnreadCount()
  for the badge. Rows render NotificacaoConteudo; a per-row Check button calls marcarLida.mutate(n.id).

web/src/app/(dashboard)/notificacoes/page.tsx — NotificacaoRow renders each history item with a
  categoria Badge and a "marcar lida" Check button. The page's useNotificacoes(...) query MUST remain
  unfiltered (snoozed items stay visible here — locked decision).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: snoozedUntil type + useSnoozeNotificacao hook + reusable snooze control</name>
  <files>web/src/types/notificacoes.ts, web/src/hooks/use-notificacoes.ts, web/src/components/shared/notificacao-snooze-control.tsx</files>
  <read_first>
    - web/src/types/notificacoes.ts (the Notificacao interface to extend)
    - web/src/hooks/use-notificacoes.ts (useMarcarNotificacaoLida as the mutation template + query-key prefix ["notificacoes"])
    - web/src/lib/notificacao-categoria.ts (NOTIFICACAO_CATEGORIAS_NAO_SILENCIAVEIS)
    - web/src/components/ui/popover.tsx and web/src/components/ui/radio-group.tsx (component APIs)
    - web/src/components/shared/notification-bell.tsx (how Popover is already used, for a consistent look)
  </read_first>
  <action>
    Type: add `snoozedUntil: string | null` to the `Notificacao` interface in
    web/src/types/notificacoes.ts (ISO string from the backend LocalDateTime, or null).

    Hook: add `useSnoozeNotificacao` to web/src/hooks/use-notificacoes.ts, mirroring
    useMarcarNotificacaoLida. mutationFn takes `{ id, dias }: { id: string; dias: 1 | 3 | 7 }` and calls
    `apiFetch<Notificacao>('/notificacoes/' + encodeURIComponent(id) + '/snooze', { method: "PATCH",
    body: JSON.stringify({ dias }) })`. onSuccess: `await queryClient.invalidateQueries({ queryKey:
    ["notificacoes"] })` so both the badge and the lists refresh (dropping the snoozed item from the
    unread count immediately, without waiting for polling).

    Component: create web/src/components/shared/notificacao-snooze-control.tsx — a `"use client"`
    reusable control `NotificacaoSnoozeControl({ notificacao }: { notificacao: Notificacao })`. If
    `NOTIFICACAO_CATEGORIAS_NAO_SILENCIAVEIS.includes(notificacao.categoria)`, render `null` (no snooze
    for PRAZO_VENCIDO — mirrors the backend 400). Otherwise render a Popover: the trigger is a small
    ghost Button (a clock/"Adiar" affordance, e.g. lucide `Clock` icon, with an aria-label like "Adiar
    notificação"); the PopoverContent holds a RadioGroup with exactly three RadioGroupItem options
    labelled "1 dia" / "3 dias" / "7 dias" (values "1"/"3"/"7"), plus a confirm Button ("Adiar") that
    calls `useSnoozeNotificacao().mutate({ id: notificacao.id, dias: Number(selected) as 1|3|7 })`,
    shows a success toast ("Notificação adiada por N dias."), and closes the Popover. Disable the confirm
    Button while the mutation isPending. Keep it self-contained so it drops into both a bell row and a
    history row identically (96-CONTEXT.md grants placement discretion).
  </action>
  <verify>
    <automated>cd web && pnpm lint && pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - Source: `Notificacao` in notificacoes.ts declares `snoozedUntil: string | null`.
    - Source: use-notificacoes.ts exports `useSnoozeNotificacao`, its mutationFn PATCHes `.../snooze` with a JSON `{ dias }` body, and onSuccess invalidates `["notificacoes"]`.
    - Source: notificacao-snooze-control.tsx returns `null` when the categoria is in NOTIFICACAO_CATEGORIAS_NAO_SILENCIAVEIS, and otherwise renders a Popover containing a RadioGroup with three preset options and a confirm Button that calls the snooze mutation.
    - Behavior: `pnpm lint` and `pnpm build` succeed.
  </acceptance_criteria>
  <done>The type carries snoozedUntil, a snooze mutation hook exists, and a reusable Popover+RadioGroup snooze control (hidden for PRAZO_VENCIDO) is ready to drop into both surfaces.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the snooze control into the bell + history page (with snoozed filter + badge)</name>
  <files>web/src/components/shared/notification-bell.tsx, web/src/app/(dashboard)/notificacoes/page.tsx</files>
  <read_first>
    - web/src/components/shared/notification-bell.tsx (the dropdown `<li>` row layout + per-row action button area)
    - web/src/app/(dashboard)/notificacoes/page.tsx (NotificacaoRow layout + the badge/action area)
    - web/src/components/shared/notificacao-snooze-control.tsx (the component built in Task 1)
    - web/src/lib/notificacao-categoria.ts (categoriaToBadgeVariant/label for the "Adiado até" badge styling reference)
  </read_first>
  <action>
    Bell (notification-bell.tsx): render `<NotificacaoSnoozeControl notificacao={n} />` in each dropdown
    row's action area, next to the existing marcar-lida Check button (both the linked and non-linked row
    branches). Additionally, apply a client-side presentation filter to the dropdown list ONLY: before
    mapping `list.data.content`, drop items where `n.snoozedUntil` is a future timestamp
    (`new Date(n.snoozedUntil) > new Date()`) so a just-snoozed item disappears from the bell preview
    (the bell shares the unfiltered GET /notificacoes endpoint, so this presentation filter — not a
    backend change — is what makes it vanish from the bell). This is a display-only filter; do not change
    the useNotificacoes call or the query. Keep the "slice(0, 10)" preview cap.

    History page (notificacoes/page.tsx): in `NotificacaoRow`, render `<NotificacaoSnoozeControl
    notificacao={notificacao} />` in the row's action area next to the existing Check button. Do NOT
    filter snoozed items here — the history list stays complete (locked decision). When
    `notificacao.snoozedUntil` is a future timestamp, also render a small badge reading `Adiado até
    {DD/MM}` (format the date with `toLocaleDateString("pt-CV", { day: "2-digit", month: "short" })`),
    so a snoozed item is visibly marked rather than silently missing from the bell (UX pitfall guard).
    Do not change the page's query/filters.
  </action>
  <verify>
    <automated>cd web && pnpm lint && pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - Source: notification-bell.tsx imports and renders `NotificacaoSnoozeControl` in the dropdown rows, and filters out future-`snoozedUntil` items from the displayed list (client-side) without altering the useNotificacoes call.
    - Source: notificacoes/page.tsx renders `NotificacaoSnoozeControl` per row AND shows an "Adiado até …" badge when snoozedUntil is in the future, with the page query/filters unchanged (no snoozed filtering on the history list).
    - Behavior: `pnpm lint` and `pnpm build` succeed.
  </acceptance_criteria>
  <done>Snoozing from the bell removes the item from the bell preview and drops the badge; the history page still shows every snoozed item, clearly marked "Adiado até DD/MM", with a working snooze control on non-PRAZO_VENCIDO rows.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser UI → PATCH /notificacoes/{id}/snooze | The hook sends only `{ dias }` and the notification `id` (the user's own, from a list already scoped to them by the backend); tenant/destinatario are never client-supplied — the server re-derives and re-validates ownership (enforced in 96-01) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-96-07 | Tampering | client submits an out-of-range `dias` or snoozes PRAZO_VENCIDO by manipulating the DOM | mitigate | The control only offers 1/3/7 and hides for PRAZO_VENCIDO, but the authoritative guard is server-side (96-01 rejects invalid dias / PRAZO_VENCIDO with 400) — the UI is convenience, not the security boundary |
| T-96-08 | Information Disclosure | XSS via a malicious linkUrl on a snoozed row | accept | Reuses the existing `isInternalLinkUrl` guard already applied to notification links; this plan adds no new link rendering path |
| T-96-SC | Tampering | npm/pip/cargo installs | accept | No new packages — Popover and RadioGroup (Radix) are already installed; TSX-only edits |
</threat_model>

<verification>
- `pnpm lint` and `pnpm build` pass with the new type field, hook, component, and wiring.
- Manual/visual confirmation is deferred to the Wave 3 human checkpoint (96-04).
</verification>

<success_criteria>
- A reusable snooze control (1/3/7-day presets) works on both the bell and the history page.
- Snoozing drops the bell badge and removes the item from the bell preview; the history page keeps it visible with an "Adiado até" badge.
- PRAZO_VENCIDO never offers a snooze control.
</success_criteria>

<output>
Create `.planning/phases/LEXCV-96-notf-26-snooze-de-lembrete-de-prazo/96-03-SUMMARY.md` when done
</output>
