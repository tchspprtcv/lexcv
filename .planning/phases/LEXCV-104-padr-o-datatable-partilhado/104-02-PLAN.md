---
phase: 104-padr-o-datatable-partilhado
plan: 02
type: execute
wave: 2
depends_on: [104-01]
files_modified:
  - web/package.json
  - web/pnpm-lock.yaml
  - web/src/components/ui/pagination.tsx
  - web/src/components/shared/data-table/data-table-column-header.tsx
  - web/src/components/shared/data-table/data-table-view-options.tsx
  - web/src/components/shared/data-table/data-table-pagination.tsx
  - web/src/components/shared/data-table/data-table.tsx
autonomous: true
requirements: [DTB-01, DTB-03]
must_haves:
  truths:
    - "@tanstack/react-table is installed in web/package.json and web/pnpm-lock.yaml"
    - "A shared generic DataTable exists under web/src/components/shared/data-table/ that sorts and paginates entirely client-side over already-fetched, already-server-filtered data — and NEVER filters (no getFilteredRowModel anywhere)"
    - "The DataTable renders through the reconciled Table/TableHeader/TableBody/TableRow/TableHead/TableCell primitives, never raw <table> tags"
    - "Sortable column headers render at 12px/600 uppercase in text-muted-foreground (the Label typography role), closing the pre-existing 10px/font-bold and raw-<th> 500-weight inconsistencies"
    - "The official shadcn Pagination primitive exists at web/src/components/ui/pagination.tsx"
    - "No row-selection column, no Checkbox import, no bulk-action UI is introduced anywhere in the pattern"
  artifacts:
    - path: "web/src/components/shared/data-table/data-table.tsx"
      provides: "Generic <DataTable columns data /> wrapper (useReactTable: core + sorted + paginated row models only)"
      min_lines: 40
    - path: "web/src/components/shared/data-table/data-table-column-header.tsx"
      provides: "Sortable header trigger (3-state cycle) with the mandated 12px/600 uppercase typography"
    - path: "web/src/components/shared/data-table/data-table-pagination.tsx"
      provides: "Client-side pagination footer: rows-per-page Select (10/20/50) + 'Página n de total' + Anterior/Seguinte"
    - path: "web/src/components/shared/data-table/data-table-view-options.tsx"
      provides: "Column-visibility DropdownMenu (checkbox items) behind an icon Button + Tooltip"
    - path: "web/src/components/ui/pagination.tsx"
      provides: "Official shadcn Pagination primitive (consumed by 104-05's /notificacoes swap)"
  key_links:
    - from: "web/src/components/shared/data-table/data-table.tsx"
      to: "@tanstack/react-table useReactTable"
      via: "getCoreRowModel + getSortedRowModel + getPaginationRowModel ONLY"
      pattern: "getCoreRowModel|getSortedRowModel|getPaginationRowModel"
    - from: "web/src/components/shared/data-table/data-table.tsx"
      to: "web/src/components/ui/table.tsx"
      via: "renders Table/TableHeader/TableBody/TableRow/TableHead/TableCell (never raw <table>)"
      pattern: "TableHeader|TableBody|TableRow|TableCell"
---

<objective>
Install `@tanstack/react-table` (now that 104-01 approved it), add the official shadcn `Pagination` primitive, and build the single reusable DataTable composition under `web/src/components/shared/data-table/`. This is the "build once, never per-module" pattern the 5 list screens (104-03/04/05) and the /notificacoes swap (104-05) consume. Covers DTB-01 and the primitive half of DTB-03.

Purpose: This repo has zero existing `useReactTable` composition (ARCHITECTURE.md Pattern 5 explicitly confirms `table.tsx` ≠ DataTable). Building it once, correctly — client-side sort/paginate only, server-side filtering left entirely to each screen's existing `use-*` hooks — prevents the pitfall of 5 divergent per-module reimplementations and the "client re-filters what the server already filtered" performance/correctness trap (PITFALLS.md).
Output: `@tanstack/react-table` in package.json, `web/src/components/ui/pagination.tsx`, and 4 files under `web/src/components/shared/data-table/`.
</objective>

<execution_context>
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/LEXCV-104-padr-o-datatable-partilhado/104-UI-SPEC.md
@.planning/phases/LEXCV-104-padr-o-datatable-partilhado/104-PATTERNS.md
@.planning/phases/LEXCV-104-padr-o-datatable-partilhado/104-01-SUMMARY.md
@web/AGENTS.md

<interfaces>
<!-- Contracts the executor composes against. Do NOT re-explore the codebase for these. -->

Table primitive to render through — web/src/components/ui/table.tsx (full file, 85 lines, exports):
  Table, TableHeader, TableBody, TableRow, TableHead, TableCell (all data-slot tagged; TableHead is h-12 px-4 text-left align-middle font-semibold; TableRow has hover:bg-muted/50). Render DataTable rows/headers THROUGH these — never emit a raw <table>/<thead>/<tr>/<th>/<td>.

Already-installed primitives to reuse as-is (do NOT add via CLI, do NOT modify):
  web/src/components/ui/select.tsx      → Select, SelectTrigger, SelectContent, SelectItem, SelectValue (for rows-per-page)
  web/src/components/ui/dropdown-menu.tsx → DropdownMenu, DropdownMenuTrigger, DropdownMenuContent, DropdownMenuCheckboxItem (for view options)
  web/src/components/ui/tooltip.tsx     → Tooltip, TooltipTrigger, TooltipContent (icon-only view-options trigger, per DSR-03)
  web/src/components/ui/button.tsx      → Button (variant "ghost"|"outline", size "sm")
  lucide-react icons: ArrowUp, ArrowDown, ChevronsUpDown (sort state), SlidersHorizontal (view options), ChevronLeft/ChevronRight (pagination — shadcn Pagination defaults)

Locked architecture constraint (104-CONTEXT.md Decisions): useReactTable is configured with getCoreRowModel(), getSortedRowModel(), getPaginationRowModel() ONLY. NEVER getFilteredRowModel(). Filtering stays 100% server-side in each screen's existing use-* hook + useState filters (untouched by this phase).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install @tanstack/react-table + add official Pagination primitive</name>
  <files>web/package.json, web/pnpm-lock.yaml, web/src/components/ui/pagination.tsx</files>
  <read_first>
    - .planning/phases/LEXCV-104-padr-o-datatable-partilhado/104-01-SUMMARY.md (confirm the legitimacy verdict is "approved" before installing; if it says rejected/blocked, STOP)
    - .planning/phases/LEXCV-104-padr-o-datatable-partilhado/104-UI-SPEC.md (Registry Safety + Scope note #5 — the exact `add pagination --diff` instruction and the "confirm no existing pagination.tsx collision" note)
    - web/package.json (confirm @tanstack/react-table absent, @tanstack/react-query present)
  </read_first>
  <action>
    Only after confirming 104-01-SUMMARY.md recorded an "approved" verdict: run `pnpm add @tanstack/react-table` in `web/` (this is the install 104-01 gated). Then add the official shadcn Pagination primitive with `pnpm dlx shadcn@latest add pagination --diff` in `web/` — this is the official registry (no npm dependency, no legitimacy gate), pure Tailwind/`cn()` composition; confirm there is no pre-existing `web/src/components/ui/pagination.tsx` to collide with (verified absent at planning time) and accept the clean add. Do NOT run any other `shadcn add`; do NOT touch any of the 14 reconciled primitives. Leave `pagination.tsx` exactly as the CLI generates it (untouched-after-add convention, same as tooltip.tsx/select.tsx from Phase 101).
  </action>
  <verify>
    <automated>grep -q '"@tanstack/react-table"' web/package.json && test -f web/src/components/ui/pagination.tsx && pnpm --dir web exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `web/package.json` dependencies contains a `@tanstack/react-table` entry and `web/pnpm-lock.yaml` is updated.
    - `web/src/components/ui/pagination.tsx` exists and exports Pagination, PaginationContent, PaginationItem, PaginationPrevious, PaginationNext (and PaginationLink) from the official registry.
    - `pnpm --dir web exec tsc --noEmit` passes with zero errors (no other file changed).
    - No other `web/src/components/ui/*.tsx` file was modified by the `add pagination` run (verify via git status — only pagination.tsx is new).
  </acceptance_criteria>
  <done>@tanstack/react-table is installed and pagination.tsx exists; typecheck is green; no reconciled primitive was disturbed.</done>
</task>

<task type="auto">
  <name>Task 2: Build the three DataTable leaf sub-components (column-header, view-options, pagination footer)</name>
  <files>web/src/components/shared/data-table/data-table-column-header.tsx, web/src/components/shared/data-table/data-table-view-options.tsx, web/src/components/shared/data-table/data-table-pagination.tsx</files>
  <read_first>
    - .planning/phases/LEXCV-104-padr-o-datatable-partilhado/104-PATTERNS.md (sections for data-table-column-header.tsx, data-table-view-options.tsx, data-table-pagination.tsx — the exact analogs, the mandated typography span, the notificacoes pager behavioral analog, the clientes footer spacing analog)
    - .planning/phases/LEXCV-104-padr-o-datatable-partilhado/104-UI-SPEC.md (Component Inventory, Typography, Pagination Contract, Copywriting Contract, Color rules)
    - web/src/components/ui/select.tsx (Select/SelectTrigger/SelectContent/SelectItem/SelectValue API for rows-per-page)
    - web/src/components/ui/dropdown-menu.tsx (DropdownMenuCheckboxItem API for view options)
    - web/src/components/ui/tooltip.tsx, web/src/components/ui/button.tsx
  </read_first>
  <action>
    Create three generic `<TData, TValue>` sub-components under `web/src/components/shared/data-table/`, all typed against `@tanstack/react-table`'s `Column`/`Table` types:

    (a) `data-table-column-header.tsx` — a `DataTableColumnHeader` taking `{ column, title }`. When `column.getCanSort()` is true, render a `Button variant="ghost" size="sm"` (reuse the row-action icon-button idiom from clientes/page.tsx) that calls `column.toggleSorting(column.getIsSorted() === "asc")` on click, with a trailing lucide icon: `ChevronsUpDown` when unsorted, `ArrowUp` when asc, `ArrowDown` when desc. Simple 3-state cycle, NO dropdown menu. When not sortable, render the title in a plain span, no button/icon. The title MUST render inside `<span className="text-xs font-semibold uppercase tracking-wider text-muted-foreground">` — this is the mandated Typography fix (12px/600, the Label role) that closes the pre-existing 3-way header inconsistency; do NOT reproduce the old `text-[10px] font-bold ... text-slate-500` treatment. Sort-state icons render in `text-muted-foreground` — NEVER `--primary`/accent blue (UI-SPEC Color rule).

    (b) `data-table-view-options.tsx` — a `DataTableViewOptions` taking `{ table }`. Trigger: an icon-only `Button variant="outline" size="sm"` with `SlidersHorizontal`, `aria-label="Colunas visíveis"`, wrapped in `Tooltip`/`TooltipTrigger asChild`/`TooltipContent` (per DSR-03's icon-only-button convention). Content: a `DropdownMenu` listing every column where `column.getCanHide()` is true as a `DropdownMenuCheckboxItem` bound to `column.getIsVisible()` / `column.toggleVisibility(value)`, labelled with the column's own header title.

    (c) `data-table-pagination.tsx` — a `DataTablePagination` taking `{ table }`. Reuse the clientes footer shell spacing exactly: `flex items-center justify-between px-6 py-4 border-t`. Left/leading: a rows-per-page `Select` (options 10/20/50, default 10) labelled **"Linhas por página"**, wired to `table.setPageSize(Number(value))` / `table.getState().pagination.pageSize`. Right/trailing: **"Página {table.getState().pagination.pageIndex + 1} de {table.getPageCount()}"** text plus **"Anterior"** (`table.previousPage()`, `disabled={!table.getCanPreviousPage()}`) and **"Seguinte"** (`table.nextPage()`, `disabled={!table.getCanNextPage()}`) as `Button variant="outline" size="sm"`. NO numbered page links (deliberate, UI-SPEC Color section). Copy strings must be exactly "Linhas por página" / "Página {n} de {total}" / "Anterior" / "Seguinte".

    Do NOT import Checkbox anywhere; do NOT add a row-selection column (REQUIREMENTS.md Out of Scope).
  </action>
  <verify>
    <automated>test -f web/src/components/shared/data-table/data-table-column-header.tsx && test -f web/src/components/shared/data-table/data-table-view-options.tsx && test -f web/src/components/shared/data-table/data-table-pagination.tsx && grep -rq 'text-xs font-semibold uppercase tracking-wider text-muted-foreground' web/src/components/shared/data-table/data-table-column-header.tsx && ! grep -riq 'checkbox\|getFilteredRowModel' web/src/components/shared/data-table/ && pnpm --dir web exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - All three files exist under web/src/components/shared/data-table/ and typecheck clean (`pnpm --dir web exec tsc --noEmit`).
    - `data-table-column-header.tsx` contains the exact class string `text-xs font-semibold uppercase tracking-wider text-muted-foreground` and does NOT contain `text-[10px]` or `font-bold`.
    - `data-table-pagination.tsx` contains the literal copy "Linhas por página", "Anterior", and "Seguinte", and contains no numbered page-link buttons.
    - Zero occurrences of `checkbox` (case-insensitive) or `getFilteredRowModel` anywhere under web/src/components/shared/data-table/.
    - Sort-state icons use text-muted-foreground, not any blue/primary accent class.
  </acceptance_criteria>
  <done>The three leaf sub-components compile, carry the mandated typography and copy, and contain no row-selection or client-filtering constructs.</done>
</task>

<task type="auto">
  <name>Task 3: Build the generic DataTable wrapper (useReactTable, no filtering)</name>
  <files>web/src/components/shared/data-table/data-table.tsx</files>
  <read_first>
    - .planning/phases/LEXCV-104-padr-o-datatable-partilhado/104-PATTERNS.md (data-table.tsx section — the mandatory getCoreRowModel/getSortedRowModel/getPaginationRowModel-only constraint, the empty-fallback copy, "render through primitives never raw <table>")
    - .planning/phases/LEXCV-104-padr-o-datatable-partilhado/104-UI-SPEC.md (Component Inventory row for data-table.tsx, Copywriting Contract empty-fallback)
    - web/src/components/ui/table.tsx (the primitives to render through)
    - web/src/components/shared/data-table/data-table-pagination.tsx, data-table-view-options.tsx (built in Task 2 — this wrapper mounts them)
  </read_first>
  <action>
    Create `data-table.tsx` exporting a generic `DataTable<TData, TValue>({ columns, data })`. Configure `useReactTable({ data, columns, getCoreRowModel: getCoreRowModel(), getSortedRowModel: getSortedRowModel(), getPaginationRowModel: getPaginationRowModel(), onSortingChange, onColumnVisibilityChange, state: { sorting, columnVisibility } })` with `sorting`/`columnVisibility` held in local `useState`. Do NOT configure `getFilteredRowModel()`, `onColumnFiltersChange`, or any `columnFilters` state — filtering is owned entirely by each screen's existing use-* hook (locked decision). Layout: a top toolbar row whose trailing edge holds `<DataTableViewOptions table={table} />`; then the `Table` primitive — `TableHeader` mapping `table.getHeaderGroups()` to `TableHead` cells via `flexRender(header.column.columnDef.header, ...)`, `TableBody` mapping `table.getRowModel().rows` to `TableRow`/`TableCell` via `flexRender(cell.column.columnDef.cell, ...)`; then `<DataTablePagination table={table} />` as the footer. Include the defensive zero-row branch: when `table.getRowModel().rows.length === 0`, render a single `<TableRow><TableCell colSpan={columns.length}>` with the copy **"Sem resultados para os filtros aplicados."** (rarely reached — each screen's outer guard intercepts the true-empty case first). Render exclusively through the Table primitives — never a raw `<table>` tag.
  </action>
  <verify>
    <automated>test -f web/src/components/shared/data-table/data-table.tsx && ! grep -q 'getFilteredRowModel' web/src/components/shared/data-table/data-table.tsx && grep -q 'getCoreRowModel' web/src/components/shared/data-table/data-table.tsx && grep -q 'getSortedRowModel' web/src/components/shared/data-table/data-table.tsx && grep -q 'getPaginationRowModel' web/src/components/shared/data-table/data-table.tsx && ! grep -Eq '<table[ >]' web/src/components/shared/data-table/data-table.tsx && grep -q 'Sem resultados para os filtros aplicados' web/src/components/shared/data-table/data-table.tsx && pnpm --dir web build</automated>
  </verify>
  <acceptance_criteria>
    - `data-table.tsx` configures exactly getCoreRowModel + getSortedRowModel + getPaginationRowModel and contains ZERO occurrences of `getFilteredRowModel`.
    - `data-table.tsx` contains no raw `<table` tag; it renders through TableHeader/TableBody/TableRow/TableHead/TableCell.
    - The empty fallback copy "Sem resultados para os filtros aplicados." is present.
    - `pnpm --dir web build` completes successfully (Next 16 full build + typecheck — not grep alone), proving the whole shared pattern compiles and is importable.
  </acceptance_criteria>
  <done>The generic DataTable compiles into a full `pnpm build`, sorts/paginates client-side only, renders through the Table primitives, and never configures client-side filtering.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev/CI ← npm registry | `@tanstack/react-table` package code enters the production build (install happens in this plan, gated by 104-01) |
| browser ← already-fetched data | The DataTable sorts/paginates data the existing use-* hooks already fetched (already tenant-scoped + server-filtered); it introduces no new network fetch and no new data exposure |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-104-SC | Tampering | `pnpm add @tanstack/react-table` (executed here) | mitigate | Install runs only after 104-01's blocking-human legitimacy approval is confirmed in 104-01-SUMMARY.md; task STOPs if verdict is rejected |
| T-104-ID | Information Disclosure | client-side sort/paginate over the full fetched array | accept | No new data crosses any boundary — the array was already fully fetched, tenant-scoped, and server-filtered by the existing hook; DataTable only reorders/slices what the browser already holds. getFilteredRowModel() is explicitly excluded so no filtering logic is duplicated where it could diverge from the server's authorization-aware filter |
| T-104-PAG | Tampering | official shadcn Pagination primitive add | accept | Official shadcn registry (already adopted Phase 101), pure Tailwind/`cn()` composition, no npm dependency, no third-party registry — no additional gate required |
</threat_model>

<verification>
- `@tanstack/react-table` present in package.json/lockfile; pagination.tsx exists.
- No `getFilteredRowModel`, no `checkbox`/row-selection, no raw `<table>` anywhere in shared/data-table/.
- Column headers carry the 12px/600 uppercase muted typography; pagination carries the exact Portuguese copy.
- `pnpm --dir web build` green.
</verification>

<success_criteria>
- The shared DataTable pattern is built once, compiles under a real Next build, sorts/paginates client-side only, and renders through the reconciled Table primitives.
- DTB-01 pattern artifacts and the DTB-03 Pagination primitive exist for the adoption plans (104-03/04/05) to consume.
</success_criteria>

<output>
Create `.planning/phases/LEXCV-104-padr-o-datatable-partilhado/104-02-SUMMARY.md` when done.
</output>
