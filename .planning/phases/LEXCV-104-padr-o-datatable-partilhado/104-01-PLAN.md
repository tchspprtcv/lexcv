---
phase: 104-padr-o-datatable-partilhado
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: false
requirements: [DTB-01]
must_haves:
  truths:
    - "@tanstack/react-table is confirmed on npmjs.com as the canonical TanStack package (publisher tanstack, high weekly downloads, real repo) BEFORE any pnpm add runs"
    - "The exact version to be installed resolves to a real published release, not a typosquat / hijacked name"
  artifacts:
    - path: ".planning/phases/LEXCV-104-padr-o-datatable-partilhado/104-01-SUMMARY.md"
      provides: "Recorded human legitimacy verdict (approved/rejected) for @tanstack/react-table, consumed by the downstream install plan"
  key_links:
    - from: "104-02 (install + pattern build)"
      to: "this gate's approval"
      via: "wave dependency — the pnpm add task is gated behind this human approval"
      pattern: "depends_on"
---

<objective>
Supply-chain gate for the one genuinely-new npm dependency Phase 104 introduces: `@tanstack/react-table`. No `## Package Legitimacy Audit` table exists in `.planning/research/` (verified absent in STACK.md / ARCHITECTURE.md / PITFALLS.md), so the GSD package-legitimacy fallback applies — the package is `[ASSUMED]` and must clear a blocking human verification before any `pnpm add` runs. This mirrors the Phase 101 gate (101-01) exactly.

Purpose: `@tanstack/react-table` is a headless data-grid library not currently in `web/package.json` (confirmed: only `@tanstack/react-query` exists today). It is a plain npm library addition, NOT a shadcn `--registry` third-party block, so the shadcn view/diff registry vetting gate does not apply — this npm-probe + human sign-off gate does. Stops a typosquat / hijacked package from entering the build of a production legal-practice platform.
Output: A recorded human approval (or rejection) for `@tanstack/react-table`, consumed by 104-02 as a wave-1 dependency.

Note: the official shadcn `Pagination` primitive that Phase 104 also adds (`pnpm dlx shadcn@latest add pagination`) does NOT pass through this gate — it is the official shadcn registry (already vetted/adopted in Phase 101), pure Tailwind/`cn()` composition, zero new npm dependency. Only `@tanstack/react-table` requires this sign-off.
</objective>

<execution_context>
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/LEXCV-104-padr-o-datatable-partilhado/104-UI-SPEC.md
@.planning/phases/LEXCV-104-padr-o-datatable-partilhado/104-CONTEXT.md

Precedent to mirror (pure blocking-human legitimacy gate, no install in this plan):
@.planning/phases/LEXCV-101-funda-o-cli-init-e-design-tokens/101-01-PLAN.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: @tanstack/react-table legitimacy verification (blocking, never auto-approvable)</name>
  <files>.planning/phases/LEXCV-104-padr-o-datatable-partilhado/104-01-SUMMARY.md</files>
  <read_first>
    - .planning/phases/LEXCV-104-padr-o-datatable-partilhado/104-UI-SPEC.md (Registry Safety section + Scope note #6 — confirm this is the npm gate, distinct from any shadcn registry vetting)
    - .planning/phases/LEXCV-101-funda-o-cli-init-e-design-tokens/101-01-PLAN.md (the exact gate protocol to replicate)
    - web/package.json (confirms only `@tanstack/react-query ^5.87.4` is present today; `@tanstack/react-table` is genuinely net-new)
  </read_first>
  <action>
    Run the automated npm-registry probe (see `<verify><automated>`) to confirm `@tanstack/react-table` resolves to a real, current release under the same `tanstack` npm org/publisher that already ships the trusted `@tanstack/react-query` dependency in web/package.json. Then present the package to the human for legitimacy sign-off against its npmjs.com page (publisher/org, weekly downloads, source repository link). This is a blocking-human gate: pause for the human verdict; never auto-approve (`workflow.auto_advance` is ignored for legitimacy checkpoints). Record the verdict (approved/rejected) in the plan SUMMARY. If rejected, flag 104-02's `pnpm add @tanstack/react-table` task as blocked rather than silently proceeding.
  </action>
  <what-built>
    Nothing is built or installed yet. This gate runs BEFORE the first (and only) new-npm-dependency install of Phase 104. The automated step queries the public npm registry to confirm the package name resolves to a real, current release; the human step confirms it is the canonical TanStack package (not a typosquat / hijacked name) before approving the downstream install in 104-02.
  </what-built>
  <how-to-verify>
    1. Run the automated registry probe (see `<verify><automated>`). Confirm `@tanstack/react-table` returns a version with no "not found" errors and that its maintainers/publisher is the `tanstack` org.
    2. Open https://www.npmjs.com/package/@tanstack/react-table and confirm: publisher is the TanStack org (same org as `@tanstack/react-query` already trusted in web/package.json), weekly downloads are very high (millions), and the repository links to github.com/TanStack/table.
    3. Confirm the package name is byte-exact `@tanstack/react-table` — not a character-off look-alike (e.g. `@tanstack/react-tables`, `react-table` unscoped, `@tanstack/table-react`).
    4. If the package looks illegitimate (wrong publisher, near-zero downloads, name off by a character, no source repo), REJECT it and record that 104-02's install task must be blocked.
  </how-to-verify>
  <verify>
    <automated>pnpm view @tanstack/react-table version dist-tags.latest maintainers 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - The automated probe prints a resolved version for `@tanstack/react-table` with zero "404 Not Found" / "ERR_PNPM_NO_MATCHING_VERSION" lines, and lists the `tanstack` org among maintainers.
    - A human has recorded, in 104-01-SUMMARY.md, an explicit approved/rejected verdict for `@tanstack/react-table` with the observed publisher + weekly-download evidence.
    - The package is NOT marked `[SLOP]`; if it cannot be confirmed legitimate it is REJECTED and 104-02's `pnpm add` task is flagged blocked (do not silently proceed).
  </acceptance_criteria>
  <resume-signal>Type "approved" to unblock 104-02's install, or state the rejection reason and that the install must be blocked.</resume-signal>
  <done>`@tanstack/react-table` has an explicit human legitimacy verdict recorded in 104-01-SUMMARY.md, the automated probe confirmed the name resolves under the tanstack org, and 104-02 is cleared to run its install (or the install is flagged blocked).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev/CI ← npm registry | Untrusted third-party package code (`@tanstack/react-table`) enters the build of a production legal-practice platform |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-104-SC | Tampering | npm install of `@tanstack/react-table` | mitigate | This blocking-human legitimacy gate verifies the package on npmjs.com (publisher, downloads, repo) before any install; never auto-approvable (`workflow.auto_advance` ignored) |
| T-104-SP | Spoofing | package-name typosquat (`@tanstack/react-table` vs a look-alike) | mitigate | Human confirms byte-exact name + that the publisher is the same `tanstack` org already trusted for `@tanstack/react-query` |
</threat_model>

<verification>
- Automated registry probe resolves `@tanstack/react-table` under the tanstack org.
- Human legitimacy verdict recorded; no `[SLOP]`, no unresolved `[ASSUMED]` proceeds to install.
</verification>

<success_criteria>
- `@tanstack/react-table` has an explicit human legitimacy verdict recorded before any install runs.
- 104-02 may proceed to `pnpm add @tanstack/react-table` only after this gate is approved.
</success_criteria>

<output>
Create `.planning/phases/LEXCV-104-padr-o-datatable-partilhado/104-01-SUMMARY.md` when done, recording the legitimacy verdict.
</output>
