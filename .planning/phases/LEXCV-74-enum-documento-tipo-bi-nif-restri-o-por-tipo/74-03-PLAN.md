---
phase: 74-enum-documento-tipo-bi-nif-restri-o-por-tipo
plan: 03
type: execute
wave: 2
depends_on: ["74-02"]
files_modified:
  - web/src/schemas/clientes.ts
  - web/src/app/(dashboard)/clientes/novo/page.tsx
  - web/src/app/(dashboard)/clientes/[id]/editar/page.tsx
autonomous: true
requirements: [CLI-22, CLI-23, CLI-24]
must_haves:
  truths:
    - "Creating a Particular cliente shows only Nenhum/CNI/BI/Passaporte in the document-type dropdown"
    - "Creating an Empresa cliente shows only Nenhum/Registo Comercial in the document-type dropdown"
    - "Zod rejects a documento_tipo that is not allowed for the selected tipo"
    - "Switching Particular<->Empresa clears documento_tipo/documento_numero when the selection becomes invalid"
    - "No DOCUMENTO_TIPOS/toDocumentoTipo duplication remains in either page — both import the shared module"
  artifacts:
    - path: "web/src/app/(dashboard)/clientes/novo/page.tsx"
      provides: "Filtered document-type dropdown driven by getDocumentoTipoOptions(tipo)"
      contains: "getDocumentoTipoOptions"
    - path: "web/src/app/(dashboard)/clientes/[id]/editar/page.tsx"
      provides: "Filtered document-type dropdown driven by getDocumentoTipoOptions(tipo)"
      contains: "getDocumentoTipoOptions"
    - path: "web/src/schemas/clientes.ts"
      provides: "superRefine validating documento_tipo against allowed set for tipo (NIF branch removed)"
      contains: "documento_tipo"
  key_links:
    - from: "web/src/app/(dashboard)/clientes/novo/page.tsx"
      to: "web/src/lib/cliente-documento-tipo.ts"
      via: "import getDocumentoTipoOptions/toDocumentoTipo"
      pattern: "cliente-documento-tipo"
    - from: "web/src/schemas/clientes.ts"
      to: "web/src/lib/cliente-documento-tipo.ts"
      via: "getDocumentoTipoOptions in superRefine"
      pattern: "getDocumentoTipoOptions"
---

<objective>
Wire both cliente form pages and the Zod schema to the shared `cliente-documento-tipo` module: filter the document-type dropdown by the selected `tipo`, validate the combination in Zod, and clear `documento_tipo`/`documento_numero` on an invalidating tipo switch. Remove the now-obsolete verbatim `DOCUMENTO_TIPOS`/`toDocumentoTipo` duplication and the NIF-specific `superRefine` branch.

Purpose: Delivers the frontend half of CLI-22/CLI-23 (filtered dropdown) and the frontend half of CLI-24 (Zod rejects invalid combinations), matching the backend rejection from Plan 01.

Output: Modified `web/src/schemas/clientes.ts`, `novo/page.tsx`, `[id]/editar/page.tsx`.
</objective>

<execution_context>
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/LEXCV-74-enum-documento-tipo-bi-nif-restri-o-por-tipo/74-CONTEXT.md
@.planning/phases/LEXCV-74-enum-documento-tipo-bi-nif-restri-o-por-tipo/74-PATTERNS.md
@.planning/phases/LEXCV-74-enum-documento-tipo-bi-nif-restri-o-por-tipo/74-UI-SPEC.md

<interfaces>
<!-- Shared module from Plan 02 (74-02). Import from @/lib/cliente-documento-tipo. -->

export interface DocumentoTipoOption { value: DocumentoTipo; label: string }
export function getDocumentoTipoOptions(tipo: "PARTICULAR" | "EMPRESA" | undefined): DocumentoTipoOption[]
export function toDocumentoTipo(value: string | undefined, tipo: "PARTICULAR" | "EMPRESA" | undefined): DocumentoTipo | undefined

Current schemas/clientes.ts superRefine block (lines 49-67) has two branches:
  1. documento_tipo set but no documento_numero → error (KEEP this branch).
  2. documento_tipo === "NIF" 9-digit check → REMOVE entirely (NIF is no longer a documento_tipo value; the dedicated `nif` field at line 25 handles that).

Duplicated local code to DELETE in both page files (novo/page.tsx lines 28-34, editar/page.tsx lines 30-34):
```typescript
const DOCUMENTO_TIPOS: readonly DocumentoTipo[] = ["NIF", "CNI", "PASSAPORTE", "REG_COMERCIAL"];
function toDocumentoTipo(value) { ... }
```

Existing dropdown block (hardcoded <option>s) and existing pendingTipo/confirmTipoChange dialog exist in both files. selectClassName styling constant stays unchanged.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Zod schema — remove NIF branch, add per-tipo documento_tipo validation</name>
  <read_first>
    - web/src/schemas/clientes.ts (full file — superRefine at lines 49-67, nif field ~line 25)
    - web/src/lib/cliente-documento-tipo.ts (shared module from Plan 02 — getDocumentoTipoOptions)
    - .planning/phases/LEXCV-74-enum-documento-tipo-bi-nif-restri-o-por-tipo/74-CONTEXT.md (Filtragem do Dropdown decisions)
  </read_first>
  <files>web/src/schemas/clientes.ts</files>
  <action>
    In the `superRefine` block: KEEP the existing branch that flags a missing `documento_numero` when `documento_tipo` is set (path `["documento_numero"]`). REMOVE the entire `data.documento_tipo === "NIF"` 9-digit branch — NIF is no longer a `documento_tipo` value and the dedicated `nif` field already validates the NIF. ADD a new branch: when `data.documento_tipo` is truthy, check it is in `getDocumentoTipoOptions(data.tipo).map(o => o.value)`; if not, `ctx.addIssue({ code: z.ZodIssueCode.custom, message: "Tipo de documento inválido para o tipo de cliente selecionado", path: ["documento_tipo"] })`. Import `getDocumentoTipoOptions` from `@/lib/cliente-documento-tipo`. Do not hardcode the allowed sets in the schema — call the shared module (single source of truth).
  </action>
  <acceptance_criteria>
    - No occurrence of `"NIF"` in the documento_tipo superRefine logic (`grep -c 'documento_tipo === "NIF"' web/src/schemas/clientes.ts` returns 0).
    - Schema imports and calls `getDocumentoTipoOptions`.
    - The documento_numero-required branch is preserved.
    - Typecheck/build passes.
  </acceptance_criteria>
  <verify>
    <automated>cd web && grep -q "getDocumentoTipoOptions" src/schemas/clientes.ts && ! grep -q 'documento_tipo === "NIF"' src/schemas/clientes.ts</automated>
  </verify>
  <done>Zod schema rejects out-of-set documento_tipo per tipo via the shared module; NIF-specific branch removed; documento_numero-required branch kept.</done>
</task>

<task type="auto">
  <name>Task 2: Wire novo/page.tsx to shared module — filtered dropdown + clear-on-switch</name>
  <read_first>
    - web/src/app/(dashboard)/clientes/novo/page.tsx (full file — DOCUMENTO_TIPOS lines 28-34, onTipoChange/confirmTipoChange lines 68-81, dropdown lines 234-250, dialog lines 337-352)
    - web/src/lib/cliente-documento-tipo.ts (shared module from Plan 02)
    - .planning/phases/LEXCV-74-enum-documento-tipo-bi-nif-restri-o-por-tipo/74-UI-SPEC.md (Interaction Contract — option filtering, auto-clear, undefined fallback)
  </read_first>
  <files>web/src/app/(dashboard)/clientes/novo/page.tsx</files>
  <action>
    Delete the local `DOCUMENTO_TIPOS` array and local `toDocumentoTipo` function (lines 28-34). Import `getDocumentoTipoOptions` and `toDocumentoTipo` from `@/lib/cliente-documento-tipo`. Replace the hardcoded `<option>` list in the documento_tipo `<select>` (lines 234-250) with: a fixed `<option value="">Nenhum</option>` first, then `getDocumentoTipoOptions(form.watch("tipo")).map(opt => <option key={opt.value} value={opt.value}>{opt.label}</option>)`. Preserve the existing `selectClassName`, the `id="documento_tipo"`, `form.register("documento_tipo")`, and the error-`<p>` rendering unchanged.
    Extend `confirmTipoChange` (lines 68-81 region): after setting the new tipo, if the current `form.getValues("documento_tipo")` is no longer valid for the new tipo (use `toDocumentoTipo(currentValue, pendingTipo)` returning undefined as the invalidity signal), call `form.setValue("documento_tipo", "")` and `form.setValue("documento_numero", "")`. Reuse the existing `pendingTipo` Dialog verbatim — no new dialog, no new copy (per UI-SPEC).
  </action>
  <acceptance_criteria>
    - `grep -c "DOCUMENTO_TIPOS" web/src/app/(dashboard)/clientes/novo/page.tsx` returns 0 (local array removed).
    - File imports getDocumentoTipoOptions from `@/lib/cliente-documento-tipo`.
    - The select renders a "Nenhum" option plus mapped options; no hardcoded `<option value="NIF">`.
    - confirmTipoChange clears documento_tipo/documento_numero when the value becomes invalid for the new tipo.
    - Behavior: with tipo=EMPRESA selected, the dropdown shows only Nenhum + Registo Comercial; with tipo=PARTICULAR, it shows Nenhum + CNI + BI + Passaporte.
  </acceptance_criteria>
  <verify>
    <automated>cd web && grep -q "getDocumentoTipoOptions" "src/app/(dashboard)/clientes/novo/page.tsx" && ! grep -q "DOCUMENTO_TIPOS" "src/app/(dashboard)/clientes/novo/page.tsx" && ! grep -q 'value="NIF"' "src/app/(dashboard)/clientes/novo/page.tsx"</automated>
  </verify>
  <done>novo/page.tsx renders the per-tipo filtered dropdown via the shared module, clears invalid selection on tipo switch, and no longer duplicates DOCUMENTO_TIPOS.</done>
</task>

<task type="auto">
  <name>Task 3: Wire [id]/editar/page.tsx to shared module — mirror novo/page.tsx</name>
  <read_first>
    - web/src/app/(dashboard)/clientes/[id]/editar/page.tsx (full file — DOCUMENTO_TIPOS lines 30-34, onTipoChange/confirmTipoChange lines 97-110, form reset dual-read line 141, dialog lines 683-698, dropdown block)
    - web/src/app/(dashboard)/clientes/novo/page.tsx (the sibling just modified in Task 2 — apply the identical refactor)
    - web/src/lib/cliente-documento-tipo.ts (shared module from Plan 02)
  </read_first>
  <files>web/src/app/(dashboard)/clientes/[id]/editar/page.tsx</files>
  <action>
    Apply the identical refactor as Task 2: delete the local `DOCUMENTO_TIPOS` array and local `toDocumentoTipo` (lines 30-34); import `getDocumentoTipoOptions` and `toDocumentoTipo` from `@/lib/cliente-documento-tipo`; replace the hardcoded documento_tipo `<option>` list with `<option value="">Nenhum</option>` + `getDocumentoTipoOptions(form.watch("tipo")).map(...)`; extend `confirmTipoChange` (lines 97-110 region) to clear `documento_tipo`/`documento_numero` via `form.setValue` when the current value is invalid for the new tipo (using `toDocumentoTipo(currentValue, pendingTipo) === undefined`). Reuse the existing dialog at lines 683-698 verbatim.
    PRESERVE the dual snake_case/camelCase form-reset read at line 141 (`documento_tipo: cliente.data.documento_tipo ?? cliente.data.documentoTipo ?? ""` and the documento_numero equivalent) exactly — it is unrelated to the enum change and must not regress.
  </action>
  <acceptance_criteria>
    - `grep -c "DOCUMENTO_TIPOS" web/src/app/(dashboard)/clientes/[id]/editar/page.tsx` returns 0.
    - File imports getDocumentoTipoOptions from `@/lib/cliente-documento-tipo`.
    - No hardcoded `<option value="NIF">` remains.
    - The dual-read `?? cliente.data.documentoTipo` reset pattern is still present.
    - Behavior: editing an EMPRESA cliente shows only Nenhum + Registo Comercial in the dropdown.
  </acceptance_criteria>
  <verify>
    <automated>cd web && grep -q "getDocumentoTipoOptions" "src/app/(dashboard)/clientes/[id]/editar/page.tsx" && ! grep -q "DOCUMENTO_TIPOS" "src/app/(dashboard)/clientes/[id]/editar/page.tsx" && grep -q "documentoTipo" "src/app/(dashboard)/clientes/[id]/editar/page.tsx" && pnpm build</automated>
  </verify>
  <done>editar/page.tsx mirrors novo/page.tsx (filtered dropdown, clear-on-switch, shared-module import), preserves the dual-read reset, and the web app builds clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user form → API | documento_tipo/documento_numero submitted from the browser; Zod is a UX pre-check, backend is authoritative |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-74-06 | Tampering | Client bypassing filtered dropdown to submit invalid combination | mitigate | Backend rejects at create/update (Plan 01, T-74-01). Frontend Zod + filtered dropdown are UX-layer only; do not rely on them for authorization. |
| T-74-07 | Denial of Service | Empty dropdown before tipo selected | mitigate | getDocumentoTipoOptions(undefined) falls back to PARTICULAR set (Plan 02), avoiding a broken/empty select. |
| T-74-SC | Tampering | npm/pnpm installs | accept | No new package installs — pure edits to existing files. |
</threat_model>

<verification>
- Neither page contains `DOCUMENTO_TIPOS` or a hardcoded `<option value="NIF">`.
- Both pages import `getDocumentoTipoOptions` from the shared module.
- Zod schema no longer branches on `documento_tipo === "NIF"` and calls `getDocumentoTipoOptions`.
- `pnpm build` succeeds.
- editar/page.tsx retains the dual snake_case/camelCase reset read.
</verification>

<success_criteria>
- Particular dropdown shows Nenhum/CNI/BI/Passaporte; Empresa dropdown shows Nenhum/Registo Comercial (CLI-22, CLI-23 frontend).
- Zod rejects out-of-set documento_tipo for the selected tipo (CLI-24 frontend), matching the backend rejection.
- Tipo switch clears an invalidated documento_tipo/documento_numero via the existing confirmation dialog.
- Duplication removed — both pages source options from the shared module.
</success_criteria>

<output>
Create `.planning/phases/LEXCV-74-enum-documento-tipo-bi-nif-restri-o-por-tipo/74-03-SUMMARY.md` when done.
</output>
