---
phase: 61-data-layer-backend-crud
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/main/java/com/lexcv/models/ParecerSolicitacao.java
  - backend/src/main/java/com/lexcv/repositories/ParecerSolicitacaoRepository.java
  - backend/src/main/java/com/lexcv/seed/DatabaseSeeder.java
autonomous: true
requirements: [PARC-04, PARC-10]
must_haves:
  truths:
    - "ParecerSolicitacao entity persists to t_parecer_solicitacao with UUID id, tenantId, clienteId, optional processoId/advogadoId, prioridade, status, prazo, createdAt"
    - "Repository can fetch all ParecerSolicitacao rows for a given tenantId"
    - "pareceres:view/create/edit/manage permissions are seeded; ADMIN gets all four, ADVOGADO gets view+create+edit, TECNICO/ASSISTENTE get view only"
  artifacts:
    - path: "backend/src/main/java/com/lexcv/models/ParecerSolicitacao.java"
      provides: "JPA entity for parecer requests"
      contains: "class ParecerSolicitacao"
    - path: "backend/src/main/java/com/lexcv/repositories/ParecerSolicitacaoRepository.java"
      provides: "tenant-scoped repository"
      contains: "findByTenantId"
    - path: "backend/src/main/java/com/lexcv/seed/DatabaseSeeder.java"
      provides: "pareceres RBAC scopes"
      contains: "pareceres:view"
  key_links:
    - from: "DatabaseSeeder.seedRbac"
      to: "ADVOGADO role"
      via: "upsertRolePermissions"
      pattern: "pareceres:create"
---

<objective>
Create the data layer foundation for the Parecer Jurídico module: the ParecerSolicitacao JPA entity, its tenant-scoped repository, and the `pareceres:*` RBAC scope seeding.

Purpose: Establishes the persistence contract and authorization scopes that the ParecerController (Plan 02) and all later phases (62-64) build on.
Output: One new entity, one new repository, and an extended seedRbac() method.
</objective>

<execution_context>
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/LEXCV-61-data-layer-backend-crud/61-CONTEXT.md
@.planning/phases/LEXCV-61-data-layer-backend-crud/61-PATTERNS.md

<interfaces>
Entity field set for ParecerSolicitacao (from 61-PATTERNS.md, sourced from Prazo.java/Processo.java/Evento.java analogs):
- id: UUID, @GeneratedValue(strategy = GenerationType.UUID)
- tenantId: UUID, @Column(name="tenant_id", nullable=false)
- clienteId: UUID, @Column(name="cliente_id", nullable=false)
- processoId: UUID, @Column(name="processo_id") — nullable
- advogadoId: UUID, @Column(name="advogado_id") — nullable
- prioridade: String, @Builder.Default = "MEDIA"
- status: String, @Builder.Default = "PENDENTE"
- prazo: LocalDate, nullable
- createdAt: LocalDateTime, @Column(name="created_at", updatable=false), set in @PrePersist

User.roles is a Set<Role> (ManyToMany EAGER); Role.nome holds the role name string (e.g. "ADVOGADO"). Relevant for Plan 02, not this plan.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ParecerSolicitacao entity and repository</name>
  <files>backend/src/main/java/com/lexcv/models/ParecerSolicitacao.java, backend/src/main/java/com/lexcv/repositories/ParecerSolicitacaoRepository.java</files>
  <read_first>
    - backend/src/main/java/com/lexcv/models/Prazo.java (primary analog: UUID id, prioridade default, @PrePersist timestamp)
    - backend/src/main/java/com/lexcv/models/Processo.java (required-FK clienteId pattern, nullable=false)
    - backend/src/main/java/com/lexcv/models/Evento.java (nullable processoId column pattern)
    - backend/src/main/java/com/lexcv/repositories/ProcessoRepository.java (repository analog)
  </read_first>
  <action>
    Create ParecerSolicitacao.java in package com.lexcv.models annotated @Entity, @Table(name = "t_parecer_solicitacao"), @Getter, @Setter, @NoArgsConstructor, @AllArgsConstructor, @Builder. Declare fields exactly as listed in the `<interfaces>` block: id (UUID, GenerationType.UUID), tenantId (column tenant_id, nullable=false), clienteId (column cliente_id, nullable=false), processoId (column processo_id, nullable), advogadoId (column advogado_id, nullable), prioridade (String, @Column(nullable=false), @Builder.Default = "MEDIA"), status (String, @Column(nullable=false), @Builder.Default = "PENDENTE"), prazo (LocalDate, no nullable constraint), createdAt (column created_at, updatable=false). Add a @PrePersist protected void onCreate() that sets createdAt = LocalDateTime.now(). Keep status and prioridade as free-form String fields — do NOT create Java enums (codebase convention per Prazo.prioridade / Processo.estado). Add a comment line listing the legal status values: PENDENTE | EM_ELABORACAO | EM_REVISAO | CONCLUIDO. Then create ParecerSolicitacaoRepository.java in package com.lexcv.repositories extending JpaRepository<ParecerSolicitacao, UUID> with one method: List<ParecerSolicitacao> findByTenantId(UUID tenantId). Import java.util.List and java.util.UUID.
  </action>
  <verify>
    <automated>cd backend && mvn -DskipTests -q compile</automated>
  </verify>
  <acceptance_criteria>
    - File backend/src/main/java/com/lexcv/models/ParecerSolicitacao.java exists and declares `class ParecerSolicitacao` with `@Table(name = "t_parecer_solicitacao")`
    - Entity has fields: id, tenantId, clienteId, processoId, advogadoId, prioridade, status, prazo, createdAt
    - clienteId column is `nullable = false`; processoId and advogadoId columns have no nullable=false constraint
    - prioridade has `@Builder.Default` and default value `"MEDIA"`; status has `@Builder.Default` and default value `"PENDENTE"`
    - File backend/src/main/java/com/lexcv/repositories/ParecerSolicitacaoRepository.java declares `findByTenantId(UUID tenantId)` returning `List<ParecerSolicitacao>`
    - `mvn -DskipTests compile` exits 0
  </acceptance_criteria>
  <done>Entity and repository compile; t_parecer_solicitacao table will be created by ddl-auto=update on next boot.</done>
</task>

<task type="auto">
  <name>Task 2: Seed pareceres RBAC scopes in DatabaseSeeder</name>
  <files>backend/src/main/java/com/lexcv/seed/DatabaseSeeder.java</files>
  <read_first>
    - backend/src/main/java/com/lexcv/seed/DatabaseSeeder.java (the seedRbac() method, ~lines 293-353, which this task extends)
  </read_first>
  <action>
    In seedRbac(), add the four new keys `"pareceres:view"`, `"pareceres:create"`, `"pareceres:edit"`, `"pareceres:manage"` to the permKeys Arrays.asList(...) literal. Do not modify the permissionMap build loop or the `upsertRolePermissions("ADMIN", permissionMap.values())` call — ADMIN automatically receives all four since it iterates permissionMap.values(). In the ASSISTENTE role list, add `permissionMap.get("pareceres:view")`. In the TECNICO role list, add `permissionMap.get("pareceres:view")`. In the ADVOGADO role list, add `permissionMap.get("pareceres:view")`, `permissionMap.get("pareceres:create")`, and `permissionMap.get("pareceres:edit")`. Do NOT assign `pareceres:manage` to ADVOGADO, TECNICO, or ASSISTENTE — it is reserved for Phase 63 approval/delivery and only reaches ADMIN via the blanket permissionMap.values() (per 61-CONTEXT.md RBAC decision).
  </action>
  <verify>
    <automated>cd backend && grep -v '^\s*//' src/main/java/com/lexcv/seed/DatabaseSeeder.java | grep -c 'pareceres:view'</automated>
  </verify>
  <acceptance_criteria>
    - permKeys list contains all four literals: "pareceres:view", "pareceres:create", "pareceres:edit", "pareceres:manage"
    - ADVOGADO role list references pareceres:view, pareceres:create, pareceres:edit (3 references)
    - TECNICO and ASSISTENTE role lists each reference pareceres:view only
    - No role list except ADMIN's blanket values() references pareceres:manage (grep `pareceres:manage` in non-comment lines appears only in permKeys, not in any upsertRolePermissions role-specific list)
    - `mvn -DskipTests compile` exits 0
  </acceptance_criteria>
  <done>All four pareceres scopes seeded with correct per-role assignment matching CONTEXT.md RBAC table.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DB schema | New t_parecer_solicitacao table introduces a new tenant-scoped data store |
| RBAC seed | seedRbac() defines authority strings enforced downstream by @PreAuthorize |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-61-01 | Elevation of Privilege | DatabaseSeeder role assignment | mitigate | pareceres:manage withheld from all non-ADMIN roles in seed; verified by acceptance criterion grep |
| T-61-02 | Information Disclosure | ParecerSolicitacao entity (no tenant filter at JPA level) | mitigate | tenantId is a not-null column; all tenant scoping enforced at controller layer (Plan 02). Entity itself stores no secrets |
| T-61-03 | Tampering | none (no package installs in this plan) | accept | No npm/maven dependency added; only new source files |
</threat_model>

<verification>
- `mvn -DskipTests compile` succeeds with both new files present.
- seedRbac() lists all four pareceres scopes and assigns them per the CONTEXT.md role table.
</verification>

<success_criteria>
- ParecerSolicitacao entity and repository compile and follow the Prazo/Processo field conventions.
- pareceres:view/create/edit/manage seeded; role assignment exactly matches CONTEXT.md (ADMIN all, ADVOGADO view+create+edit, TECNICO/ASSISTENTE view).
</success_criteria>

<output>
Create `.planning/phases/LEXCV-61-data-layer-backend-crud/61-01-SUMMARY.md` when done.
</output>
