---
phase: 61-data-layer-backend-crud
plan: 02
type: execute
wave: 2
depends_on: ["61-01"]
files_modified:
  - backend/src/main/java/com/lexcv/controllers/ParecerController.java
autonomous: true
requirements: [PARC-01, PARC-02, PARC-03, PARC-05, PARC-06, PARC-04]
must_haves:
  truths:
    - "User with pareceres:create can POST a solicitacao with clienteId, descricao, data, prazo, prioridade; clienteId is mandatory (400 if missing)"
    - "Solicitacao can carry an optional processoId"
    - "User can assign/reassign an advogado (User with role ADVOGADO) via PUT /{id}/atribuir; assignment is rejected if the User lacks role ADVOGADO or belongs to another tenant; rejected when status is CONCLUIDO"
    - "Assigning an advogado sets status to EM_ELABORACAO; unassigned solicitacao stays PENDENTE"
    - "User with pareceres:view can GET the tenant-scoped list and GET a single solicitacao by id"
    - "All endpoints scope strictly by tenantId from the security context"
  artifacts:
    - path: "backend/src/main/java/com/lexcv/controllers/ParecerController.java"
      provides: "CRUD + assignment endpoints under /api/v1/pareceres/solicitacoes"
      contains: "pareceres:create"
      min_lines: 90
  key_links:
    - from: "ParecerController.atribuir"
      to: "UserRepository + Role check"
      via: "advogado role validation"
      pattern: "ADVOGADO"
    - from: "ParecerController"
      to: "ParecerSolicitacaoRepository"
      via: "findByTenantId / save"
      pattern: "parecerSolicitacaoRepository\\.(findByTenantId|findById|save)"
---

<objective>
Build the dedicated ParecerController exposing CRUD and advogado-assignment endpoints for parecer requests, with strict tenant scoping, RBAC via @PreAuthorize, and ADVOGADO-role validation on assignment.

Purpose: Delivers the functional API for Phase 61 — create, list, view, update, and assign parecer solicitações.
Output: One new controller, ParecerController.java.
</objective>

<execution_context>
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/LEXCV-61-data-layer-backend-crud/61-CONTEXT.md
@.planning/phases/LEXCV-61-data-layer-backend-crud/61-PATTERNS.md
@.planning/phases/LEXCV-61-data-layer-backend-crud/61-01-SUMMARY.md

<interfaces>
From Plan 01 (61-01):
- ParecerSolicitacao entity: getId(), getTenantId()/setTenantId(), getClienteId(), getProcessoId(), getAdvogadoId()/setAdvogadoId(), getPrioridade(), getStatus()/setStatus(), getPrazo(), getCreatedAt(). Builder-default status="PENDENTE", prioridade="MEDIA".
- ParecerSolicitacaoRepository.findByTenantId(UUID), plus inherited JpaRepository save/findById.

Advogado role validation (sourced from User.java/Role.java, read in planning):
- User.getRoles() returns Set<Role> (ManyToMany, EAGER-loaded).
- Role.getNome() returns the role name String.
- Check: user.getRoles().stream().anyMatch(r -> "ADVOGADO".equals(r.getNome()))

Tenant + auth idiom (ResourceController.java):
- getTenantId(): reads (UserPrincipal) SecurityContextHolder.getContext().getAuthentication().getPrincipal(), returns principal.getTenantId().
- UserRepository.findById(UUID) returns Optional<User>.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ParecerController with create, list, get-by-id, and update endpoints</name>
  <files>backend/src/main/java/com/lexcv/controllers/ParecerController.java</files>
  <read_first>
    - backend/src/main/java/com/lexcv/controllers/ResourceController.java (lines 1-66 for imports/class header/getTenantId; lines 796-980 for Processo CRUD analog — createProcesso, getProcesso, listProcessos, updateProcesso)
    - backend/src/main/java/com/lexcv/config/UserPrincipal.java (getTenantId signature)
    - backend/src/main/java/com/lexcv/repositories/ParecerSolicitacaoRepository.java (from Plan 01)
    - backend/src/main/java/com/lexcv/models/ParecerSolicitacao.java (from Plan 01)
  </read_first>
  <action>
    Create ParecerController.java in package com.lexcv.controllers annotated @RestController, @RequestMapping("/api/v1/pareceres/solicitacoes"), @RequiredArgsConstructor. Inject `private final ParecerSolicitacaoRepository parecerSolicitacaoRepository;` and `private final UserRepository userRepository;`. Add the private getTenantId() helper verbatim from ResourceController (reads (UserPrincipal) SecurityContextHolder.getContext().getAuthentication().getPrincipal(), returns principal.getTenantId()).

    Implement four endpoints:
    1. POST "" with @PreAuthorize("hasAuthority('pareceres:create')") taking @RequestBody ParecerSolicitacao body. Set body.setTenantId(getTenantId()). If body.getClienteId() == null, return 400 with Map.of("message","clienteId é obrigatório"). If body.getAdvogadoId() != null, validate it via the same advogado-validation logic as the assign endpoint (Task 2 helper) — load the User, reject with 400 if null / wrong tenant / not ADVOGADO, and on success set status to "EM_ELABORACAO". If advogadoId is null, leave status at its default "PENDENTE". Save and return 201 CREATED with the saved entity.
    2. GET "" with @PreAuthorize("hasAuthority('pareceres:view')") returning ResponseEntity.ok(parecerSolicitacaoRepository.findByTenantId(getTenantId())) — simple tenant-scoped list, no pagination (per CONTEXT.md).
    3. GET "/{id}" with @PreAuthorize("hasAuthority('pareceres:view')"): findById, if null or tenantId mismatch return 404 Map.of("message","Solicitação não encontrada"), else ok(entity).
    4. PUT "/{id}" with @PreAuthorize("hasAuthority('pareceres:edit')") taking @RequestBody ParecerSolicitacao payload: load + tenant-check (404 idiom). Update only prazo, prioridade, clienteId, processoId from payload. Do NOT update status or advogadoId here — status is a state-machine field (mirrors the Processo.estado exclusion) and advogadoId goes through /atribuir. Save and return ok(saved).

    Because the advogado-validation logic is shared by POST and PUT /{id}/atribuir, extract it as a private helper now (see Task 2). Return entities directly, no DTO layer (CONTEXT.md).
  </action>
  <verify>
    <automated>cd backend && mvn -DskipTests -q compile</automated>
  </verify>
  <acceptance_criteria>
    - File backend/src/main/java/com/lexcv/controllers/ParecerController.java exists with @RequestMapping("/api/v1/pareceres/solicitacoes")
    - POST handler annotated @PreAuthorize("hasAuthority('pareceres:create')"); returns 400 when clienteId is null
    - POST always calls setTenantId(getTenantId()) and never trusts tenantId from the request body
    - GET list handler annotated pareceres:view and calls findByTenantId(getTenantId())
    - GET /{id} returns 404 when entity is null or tenantId mismatch
    - PUT /{id} annotated pareceres:edit and does NOT assign status or advogadoId from the payload
    - `mvn -DskipTests compile` exits 0
  </acceptance_criteria>
  <done>Create, list, get-by-id, and update endpoints compile, are tenant-scoped, and carry correct @PreAuthorize scopes.</done>
</task>

<task type="auto">
  <name>Task 2: Add advogado assignment endpoint with ADVOGADO-role validation</name>
  <files>backend/src/main/java/com/lexcv/controllers/ParecerController.java</files>
  <read_first>
    - backend/src/main/java/com/lexcv/controllers/ParecerController.java (the file created in Task 1, to extend it)
    - backend/src/main/java/com/lexcv/models/User.java (getRoles() returns Set<Role>)
    - backend/src/main/java/com/lexcv/models/Role.java (getNome() returns role name String)
    - backend/src/main/java/com/lexcv/controllers/ResourceController.java (lines 929-935, createProcesso responsavelId tenant-validation idiom)
  </read_first>
  <action>
    Add a private helper `validateAdvogado(UUID advogadoId, UUID tenantId)` returning the validated User or null on failure, and a PUT "/{id}/atribuir" endpoint annotated @PreAuthorize("hasAuthority('pareceres:edit')").

    Assignment endpoint accepts @RequestBody Map<String,String> body containing "advogadoId" (parse to UUID; if missing/blank return 400 Map.of("message","advogadoId é obrigatório")). Load the solicitacao by id, apply the tenant-scoped 404 idiom. If solicitacao.getStatus().equals("CONCLUIDO"), return 400 Map.of("message","Não é possível reatribuir um parecer concluído") (CONTEXT.md: reassignment blocked only in CONCLUIDO). Validate the advogado: load User via userRepository.findById(advogadoId); reject with 400 Map.of("message","advogadoId não pertence a este tenant ou não tem papel ADVOGADO") if the user is null, user.getTenantId() does not equal tenantId, OR user.getRoles().stream().noneMatch(r -> "ADVOGADO".equals(r.getNome())). On success set advogadoId, set status to "EM_ELABORACAO", save, return ok(saved).

    Refactor the POST advogado validation from Task 1 to call this same validateAdvogado helper so the role/tenant rule lives in one place. The ADVOGADO role check uses User.getRoles() (Set<Role>, EAGER) and Role.getNome() — there is no existing analog for FK role validation, so this is the canonical implementation for the module.
  </action>
  <verify>
    <automated>cd backend && grep -c 'ADVOGADO' src/main/java/com/lexcv/controllers/ParecerController.java && mvn -DskipTests -q compile</automated>
  </verify>
  <acceptance_criteria>
    - PUT /{id}/atribuir handler exists, annotated @PreAuthorize("hasAuthority('pareceres:edit')")
    - Assignment is rejected with 400 when the target User is null, belongs to another tenant, or has no Role with nome "ADVOGADO"
    - Assignment is rejected with 400 when the solicitacao status equals "CONCLUIDO"
    - On successful assignment, advogadoId is set and status becomes "EM_ELABORACAO"
    - The ADVOGADO check uses user.getRoles() and Role.getNome() (not a string field on User)
    - The same validateAdvogado helper is reused by the POST create handler
    - `mvn -DskipTests compile` exits 0
  </acceptance_criteria>
  <done>Assignment endpoint enforces tenant + ADVOGADO role + non-CONCLUIDO precondition, auto-transitions status, and shares validation with create.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → ParecerController | Untrusted JSON body (tenantId, advogadoId, status) crosses here |
| ParecerController → DB | Tenant-scoped reads/writes; cross-tenant access must be blocked |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-61-04 | Spoofing/Tampering | tenantId in request body | mitigate | Controller always overwrites with setTenantId(getTenantId()) on create; verifies tenantId match on every findById (404 idiom). Never trusts body tenantId |
| T-61-05 | Elevation of Privilege | advogadoId assignment | mitigate | validateAdvogado rejects users from other tenants or lacking Role "ADVOGADO" before accepting assignment (400) |
| T-61-06 | Elevation of Privilege | endpoint authority | mitigate | Every endpoint carries @PreAuthorize matching CONTEXT.md scope table (view/create/edit); pareceres:manage not used this phase |
| T-61-07 | Information Disclosure | cross-tenant read of solicitacao by id | mitigate | GET/PUT /{id} return tenant-scoped 404 when tenantId mismatches |
| T-61-08 | Tampering | status state-machine bypass via generic PUT | mitigate | PUT /{id} excludes status and advogadoId from payload-driven update; status only changes via assignment auto-transition |
| T-61-SC | Tampering | npm/maven installs | accept | No new dependency added in this plan — only a new source file using existing Spring/Lombok imports |
</threat_model>

<verification>
- `mvn -DskipTests compile` succeeds.
- ParecerController exposes POST/GET list/GET by-id/PUT update/PUT atribuir, all tenant-scoped and @PreAuthorize-gated.
- Assignment validates ADVOGADO role via User.getRoles()/Role.getNome().
</verification>

<success_criteria>
- All five endpoints functional and compile-clean.
- clienteId mandatory on create; advogadoId optional on create, validated when present.
- Assignment enforces ADVOGADO role + tenant + non-CONCLUIDO; auto-sets EM_ELABORACAO.
- No DTO layer, no pagination, entities returned directly (CONTEXT.md).
</success_criteria>

<output>
Create `.planning/phases/LEXCV-61-data-layer-backend-crud/61-02-SUMMARY.md` when done.
</output>
