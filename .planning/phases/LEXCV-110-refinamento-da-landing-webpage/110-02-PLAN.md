---
phase: 110-refinamento-da-landing-webpage
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - webpage/src/components/ui/badge.tsx
  - webpage/src/components/hero-section.tsx
  - webpage/src/components/contact-section.tsx
autonomous: true
requirements: [LDG-18]
user_setup: []

must_haves:
  truths:
    - "The Hero eyebrow renders as a Badge (variant=secondary) whose className overrides the base 12px/500 to preserve the existing text-sm font-semibold uppercase tracking-[0.2em] (14px/600) look"
    - "The Hero's Badge + <h1> + <p> + 2 CTA buttons sit inside a single Card (Card/CardHeader/CardContent), while BrandMark and the blue accent rule (h-px w-12) stay OUTSIDE/above the Card, unchanged"
    - "The Hero heading stays a raw <h1> tag with its existing text-5xl classes — it is NOT swapped to CardTitle (which renders <h3>)"
    - "The Contacto eyebrow renders as a Badge (variant=secondary, same className override as Hero) and its Badge + <h2> + <p> + mailto CTA sit inside a Card of the same composition as the Hero's"
    - "The Contacto heading stays a raw <h2> tag — it is NOT swapped to CardTitle"
  artifacts:
    - path: "webpage/src/components/ui/badge.tsx"
      provides: "Badge primitive (byte-identical copy of web/'s badge.tsx; defaultVariant=secondary)"
      exports: ["Badge"]
      min_lines: 35
    - path: "webpage/src/components/hero-section.tsx"
      provides: "Hero recomposed with Card + Badge, raw <h1> preserved"
      contains: "<Badge"
      min_lines: 30
    - path: "webpage/src/components/contact-section.tsx"
      provides: "Contacto recomposed with Card + Badge, raw <h2> preserved"
      contains: "<Badge"
      min_lines: 25
  key_links:
    - from: "webpage/src/components/hero-section.tsx"
      to: "webpage/src/components/ui/badge.tsx"
      via: "import Badge"
      pattern: "from \"@/components/ui/badge\""
    - from: "webpage/src/components/hero-section.tsx"
      to: "webpage/src/components/ui/card.tsx"
      via: "import Card/CardHeader/CardContent"
      pattern: "from \"@/components/ui/card\""
    - from: "webpage/src/components/contact-section.tsx"
      to: "webpage/src/components/ui/badge.tsx"
      via: "import Badge"
      pattern: "from \"@/components/ui/badge\""
---

<objective>
Recompose the landing's Hero and Contacto sections with `Card`/`Badge`, replicating the idiomatic `Card` composition already established by `TrustSection`, closing LDG-18. Today both sections use a hand-rolled bordered `<span>` eyebrow and free-floating content; this plan copies the `Badge` primitive into `webpage/` verbatim from `web/`, then wraps each section's content in a `Card` and swaps the eyebrow span for a `Badge`.

Purpose: bring Hero/Contacto onto the same idiomatic `Card` container pattern as `TrustSection`/`FeaturesSection`, using the official `Badge` primitive for the eyebrow, without any visual regression to headings.
Output: `webpage/src/components/ui/badge.tsx` (new, verbatim copy), a recomposed `webpage/src/components/hero-section.tsx`, and a recomposed `webpage/src/components/contact-section.tsx`.

Scope boundary: `webpage/` is a SEPARATE Next.js app from `web/`. All build/lint commands run in `webpage/`, never `web/`. Do NOT touch `webpage/src/components/ui/card.tsx` — its flat `rounded-none` / hardcoded-hex look is intentional and IS the target look (UI-SPEC Divergence Note §3). Do NOT migrate FeaturesSection/TrustSection eyebrows to Badge (out of scope — LDG-18 names only Hero + Contacto).
</objective>

<execution_context>
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/LEXCV-110-refinamento-da-landing-webpage/110-CONTEXT.md
@.planning/phases/LEXCV-110-refinamento-da-landing-webpage/110-UI-SPEC.md
@.planning/phases/LEXCV-110-refinamento-da-landing-webpage/110-PATTERNS.md

<interfaces>
<!-- Contracts the executor consumes. Extracted from source — no codebase exploration needed. -->

Badge primitive (from web/src/components/ui/badge.tsx, being copied verbatim):
  export function Badge({ className, variant, ...props }): props = span attrs + variant
  variants: default | secondary | outline | blue | green | amber | red | purple | gray
  defaultVariants.variant = "secondary"   (so variant may be omitted or passed explicitly)
  base classes: "inline-flex items-center rounded-full border px-2.5 py-0.5 text-xs font-medium"
    -> base is 12px/500; callers MUST override via className to keep the 14px/600 eyebrow look

Card primitive ALREADY in webpage/ (webpage/src/components/ui/card.tsx — do NOT modify):
  Card         -> <div> flat rounded-none border, bg-white / dark:bg-[#020617], shadow-sm
  CardHeader   -> <div> flex flex-col gap-1.5 p-6
  CardContent  -> <div> p-6 pt-0
  CardTitle    -> <h3>   (DO NOT use for Hero <h1> / Contacto <h2> — heading demotion)
  CardDescription -> <p>  (not needed here)
  CardFooter   -> <div>  (optional, executor discretion for CTA placement)

TrustSection Card composition (the analog to replicate — webpage/src/components/trust-section.tsx):
  <Card><CardHeader> icon + <CardTitle> + <CardDescription> </CardHeader></Card>
  (Hero/Contacto differ: they keep raw <h1>/<h2>, not CardTitle)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Copy the Badge primitive into webpage/ verbatim</name>
  <files>webpage/src/components/ui/badge.tsx</files>
  <read_first>
    - web/src/components/ui/badge.tsx (the source — copy this exactly)
    - webpage/src/lib/utils.ts (confirm `cn` resolves — it does)
    - webpage/package.json (confirm `class-variance-authority` is already a dependency — it is, ^0.7.1)
  </read_first>
  <action>
    Create `webpage/src/components/ui/badge.tsx` as a byte-identical copy of `web/src/components/ui/badge.tsx`. Use a shell copy (`cp web/src/components/ui/badge.tsx webpage/src/components/ui/badge.tsx`) to guarantee identical bytes. Make ZERO edits: imports (`class-variance-authority`'s `cva`/`VariantProps`, `@/lib/utils`'s `cn`) resolve unchanged in `webpage/`. The Badge keeps all 9 variants and `defaultVariants.variant: "secondary"`. The variants use hardcoded Tailwind color literals (neutral/blue/emerald/etc.) not CSS tokens, so there is zero token-mismatch risk in `webpage/` (UI-SPEC Divergence Note §5). Do NOT modify `globals.css`.
  </action>
  <verify>
    <automated>diff web/src/components/ui/badge.tsx webpage/src/components/ui/badge.tsx && grep -q 'export function Badge' webpage/src/components/ui/badge.tsx && grep -q 'variant: "secondary"' webpage/src/components/ui/badge.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `diff web/src/components/ui/badge.tsx webpage/src/components/ui/badge.tsx` prints nothing (byte-identical, exit 0)
    - `grep -q 'export function Badge' webpage/src/components/ui/badge.tsx` succeeds
    - `grep -q 'variant: "secondary"' webpage/src/components/ui/badge.tsx` succeeds (default variant preserved)
    - `globals.css` was NOT modified this task
  </acceptance_criteria>
  <done>webpage/src/components/ui/badge.tsx exists and is a byte-for-byte copy of web/'s badge.tsx.</done>
</task>

<task type="auto">
  <name>Task 2: Recompose HeroSection with Card + Badge</name>
  <files>webpage/src/components/hero-section.tsx</files>
  <read_first>
    - webpage/src/components/hero-section.tsx (current 32-line file — the exact base to modify)
    - webpage/src/components/trust-section.tsx (the Card composition analog to replicate)
    - webpage/src/components/ui/card.tsx (available exports: Card/CardHeader/CardContent/CardFooter — do NOT modify)
    - webpage/src/components/ui/badge.tsx (the primitive from Task 1)
    - .planning/phases/LEXCV-110-refinamento-da-landing-webpage/110-UI-SPEC.md (Typography + "Hero/Contacto Card composition boundary" sections)
  </read_first>
  <action>
    Recompose `webpage/src/components/hero-section.tsx` so its eyebrow becomes a `Badge` and its main content sits inside a `Card`, replicating TrustSection's `Card` composition. Concrete requirements:

    1. Add imports: `import { Badge } from "@/components/ui/badge"` and `import { Card, CardContent, CardHeader } from "@/components/ui/card"`. Keep `ArrowRight`, `BrandMark`, `Button`, `BrandingResponse` imports. Do NOT import `CardTitle`/`CardDescription` (not used here).

    2. Replace the manual eyebrow `<span className="inline-flex items-center border border-slate-300 px-3 py-1 text-sm font-semibold uppercase tracking-[0.2em] text-slate-500 dark:border-slate-700 dark:text-slate-400">PLATAFORMA DE GESTÃO JURÍDICA</span>` with `<Badge variant="secondary" className="text-sm font-semibold uppercase tracking-[0.2em]">PLATAFORMA DE GESTÃO JURÍDICA</Badge>`. The `className` override is MANDATORY: Badge's base is `text-xs font-medium` (12px/500) and must be overridden to the existing 14px/600 uppercase look — do not accept Badge's smaller default (UI-SPEC Typography).

    3. Wrap the Badge + the raw `<h1>` + the `<p>` + the 2 CTA `Button`s inside a single `<Card>`. Use `CardHeader` for the Badge and `CardContent` for the h1/p/CTAs (or place the CTAs in a CardFooter — Header-vs-Content-vs-Footer split is executor discretion per CONTEXT.md). The Card is a layout/spacing wrapper only.

    4. CRITICAL: keep the heading as a raw `<h1 className="... text-5xl font-semibold tracking-tight ...">` exactly as today, just relocated inside the Card. Do NOT swap `<h1>` for `CardTitle` — CardTitle renders an `<h3>` with baked-in `leading-none`, which would demote the page's H1 (real a11y/SEO regression) and break the 1.2 line-height target (UI-SPEC Typography heading-semantics note).

    5. Scope boundary: `BrandMark` (`<BrandMark branding={branding} className="mb-6" />`) and the standalone blue accent rule (`<div className="mb-4 h-px w-12 bg-blue-600 dark:bg-blue-400" />`) stay OUTSIDE/above the `Card`, unchanged — only Badge + h1 + p + 2 CTAs move inside. Keep the outer `<section>` and its `mx-auto max-w-3xl px-6` container.

    6. Do NOT change any CTA behavior/copy: the two Buttons remain `variant="secondary"` (Entrar → /login) and `variant="ghost"` (Ver Funcionalidades → #funcionalidades with ArrowRight), both `asChild` + `rounded-none`.
  </action>
  <verify>
    <automated>grep -q 'from "@/components/ui/badge"' webpage/src/components/hero-section.tsx && grep -q 'from "@/components/ui/card"' webpage/src/components/hero-section.tsx && grep -q '<Badge' webpage/src/components/hero-section.tsx && grep -q 'tracking-\[0.2em\]' webpage/src/components/hero-section.tsx && grep -q '<h1' webpage/src/components/hero-section.tsx && grep -q '<Card' webpage/src/components/hero-section.tsx && grep -q 'BrandMark' webpage/src/components/hero-section.tsx && ! grep -q 'CardTitle' webpage/src/components/hero-section.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'from "@/components/ui/badge"' webpage/src/components/hero-section.tsx` and `grep -q 'from "@/components/ui/card"' webpage/src/components/hero-section.tsx` both succeed
    - `grep -q '<Badge' webpage/src/components/hero-section.tsx` succeeds and the Badge line also contains `text-sm font-semibold uppercase tracking-[0.2em]` (grep for `tracking-\[0.2em\]` succeeds)
    - `grep -q '<h1' webpage/src/components/hero-section.tsx` succeeds (raw h1 preserved)
    - `! grep -q 'CardTitle' webpage/src/components/hero-section.tsx` — CardTitle is NOT used (no heading demotion)
    - `grep -q '<Card' webpage/src/components/hero-section.tsx` succeeds (content wrapped in a Card)
    - `grep -q 'BrandMark' webpage/src/components/hero-section.tsx` succeeds (BrandMark retained, outside the Card)
    - `! grep -q '<span className="inline-flex items-center border border-slate-300' webpage/src/components/hero-section.tsx` — the old manual eyebrow span is gone
  </acceptance_criteria>
  <done>Hero renders its eyebrow as a Badge and its Badge/h1/p/CTAs inside a Card, with BrandMark + accent rule still above the Card and the h1 unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: Recompose ContactSection with Card + Badge</name>
  <files>webpage/src/components/contact-section.tsx</files>
  <read_first>
    - webpage/src/components/contact-section.tsx (current 25-line file — the exact base to modify)
    - webpage/src/components/hero-section.tsx (post-Task-2 — copy the same Card/Badge shape for visual consistency, per CONTEXT.md's "no visual difference between Hero and Contacto Card" lock)
    - webpage/src/components/trust-section.tsx (Card composition analog)
    - webpage/src/components/ui/card.tsx (available exports — do NOT modify)
    - webpage/src/components/ui/badge.tsx (the primitive from Task 1)
    - .planning/phases/LEXCV-110-refinamento-da-landing-webpage/110-UI-SPEC.md (Typography heading-semantics note)
  </read_first>
  <action>
    Recompose `webpage/src/components/contact-section.tsx` using the SAME Card/Badge composition as the Hero (Task 2), so the two Cards are visually identical (only inner content differs). Concrete requirements:

    1. Add imports: `import { Badge } from "@/components/ui/badge"` and `import { Card, CardContent, CardHeader } from "@/components/ui/card"`. Keep `Mail` and `Button` imports. Do NOT import `CardTitle`/`CardDescription`.

    2. Replace the manual eyebrow `<span className="inline-flex items-center border border-slate-300 px-3 py-1 text-sm font-semibold uppercase tracking-[0.2em] text-slate-500 dark:border-slate-700 dark:text-slate-400">CONTACTO</span>` with `<Badge variant="secondary" className="text-sm font-semibold uppercase tracking-[0.2em]">CONTACTO</Badge>` — identical variant + className override to the Hero (consistency lock).

    3. Wrap the Badge + the raw `<h2>` + the `<p>` + the single mailto CTA `Button` inside a single `<Card>`, using the same CardHeader/CardContent split chosen in Task 2 for the Hero.

    4. CRITICAL: keep the heading as a raw `<h2 className="mt-4 text-2xl font-semibold ...">` exactly as today, just relocated inside the Card. Do NOT swap `<h2>` for `CardTitle` (would render `<h3>` and break heading semantics — UI-SPEC Typography note).

    5. Keep the outer `<section id="contacto" className="border-t ... py-12 ... md:py-16 lg:py-16">` and its `mx-auto max-w-3xl px-6 text-center` container. Keep the mailto CTA behavior/copy unchanged: `<Button asChild className="rounded-none"><a href="mailto:contacto@lexcv.cv?subject=...">Pedir Demonstração<Mail className="h-4 w-4" /></a></Button>`.
  </action>
  <verify>
    <automated>cd webpage && pnpm build && grep -q 'from "@/components/ui/badge"' src/components/contact-section.tsx && grep -q 'from "@/components/ui/card"' src/components/contact-section.tsx && grep -q '<Badge' src/components/contact-section.tsx && grep -q 'tracking-\[0.2em\]' src/components/contact-section.tsx && grep -q '<h2' src/components/contact-section.tsx && grep -q '<Card' src/components/contact-section.tsx && ! grep -q 'CardTitle' src/components/contact-section.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm build` in `webpage/` completes with zero type errors (holistic typecheck of all 3 modified files + the 2 new primitives)
    - `grep -q 'from "@/components/ui/badge"' webpage/src/components/contact-section.tsx` and `grep -q 'from "@/components/ui/card"' webpage/src/components/contact-section.tsx` both succeed
    - `grep -q '<Badge' webpage/src/components/contact-section.tsx` succeeds and the Badge line contains `text-sm font-semibold uppercase tracking-[0.2em]`
    - `grep -q '<h2' webpage/src/components/contact-section.tsx` succeeds (raw h2 preserved)
    - `! grep -q 'CardTitle' webpage/src/components/contact-section.tsx` — CardTitle not used
    - `grep -q '<Card' webpage/src/components/contact-section.tsx` succeeds
    - `! grep -q '<span className="inline-flex items-center border border-slate-300' webpage/src/components/contact-section.tsx` — old manual eyebrow span gone
  </acceptance_criteria>
  <done>Contacto renders its eyebrow as a Badge and its Badge/h2/p/CTA inside a Card matching the Hero's; build passes across all touched files.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser (client) rendering | Static marketing content recomposition. No server input, no network calls, no auth, no user-supplied data introduced by this plan. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-110-03 | Information Disclosure | hero-section.tsx `branding` prop rendered via BrandMark | accept | `branding` is fetched upstream and rendered by the unchanged `BrandMark` component; this plan does not change how branding data is escaped/rendered (pre-existing behavior, out of scope). |
| T-110-04 | Tampering | contact-section.tsx `mailto:` CTA href | accept | The mailto href is a static, hardcoded string literal — no dynamic/user input, no injection surface. Unchanged by this plan. |
| T-110-SC | Tampering (supply chain) | new file webpage/src/components/ui/badge.tsx | accept | No npm/pip/cargo install this phase. `badge.tsx` is a byte-identical in-repo copy of a file already vetted through Phase 101's package-legitimacy gate; `class-variance-authority` is already a webpage/ dependency. No registry fetch, no new package. |
</threat_model>

<verification>
- `cd webpage && pnpm build` passes (typecheck of all 3 modified files + 2 new primitives).
- `webpage/src/components/ui/badge.tsx` is byte-identical to `web/src/components/ui/badge.tsx`.
- Hero and Contacto each render a `Badge` eyebrow (14px/600 uppercase preserved) inside a `Card`, with raw `<h1>`/`<h2>` retained (no CardTitle).
- BrandMark + blue accent rule remain outside the Hero's Card.
</verification>

<success_criteria>
LDG-18 satisfied: the Hero and Contacto sections are recomposed with `Card`/`Badge`, replicating TrustSection's idiomatic Card composition, with zero heading-semantics regression and the two Cards visually consistent.
</success_criteria>

<output>
Create `.planning/phases/LEXCV-110-refinamento-da-landing-webpage/110-02-SUMMARY.md` when done.
</output>
