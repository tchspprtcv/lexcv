---
phase: 110-refinamento-da-landing-webpage
plan: 03
type: execute
wave: 2
depends_on: [110-01, 110-02]
files_modified: []
autonomous: false
requirements: [LDG-17, LDG-18]
user_setup: []

must_haves:
  truths:
    - "pnpm build and pnpm lint both pass in webpage/ with the full Phase 110 change set applied"
    - "A human confirms on a mobile viewport that the hamburger opens the Sheet, the 4 items navigate + close the drawer, and Entrar does not appear twice"
    - "A human confirms the desktop header is unchanged and the Hero/Contacto Cards + Badges render correctly in both light and dark mode"
  artifacts: []
  key_links: []
---

<objective>
Close Phase 110 with a holistic gate and a human visual checkpoint, matching the milestone convention where every UI phase (102–109) ends with a build/lint gate plus a live visual sign-off. This plan runs the full `webpage/` build + lint against the combined Wave-1 change set (Sheet mobile nav from 110-01 + Card/Badge recomposition from 110-02), then a human verifies the two things a build cannot: mobile Sheet interaction and light/dark visual rendering.

Purpose: catch any integration issue between the two parallel Wave-1 plans and confirm the actual rendered result (interaction + theming), which no automated check can fully assert for a marketing page.
Output: a green build/lint gate and a recorded human-verify result. No source files are modified by this plan.

Scope boundary: `webpage/` is a SEPARATE Next.js app. Run all commands in `webpage/`, never `web/` (different dev server, port 3001).
</objective>

<execution_context>
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/LEXCV-110-refinamento-da-landing-webpage/110-CONTEXT.md
@.planning/phases/LEXCV-110-refinamento-da-landing-webpage/110-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Holistic build + lint gate for webpage/</name>
  <files>webpage/ (no source changes — verification only)</files>
  <read_first>
    - webpage/src/components/site-header.tsx (final state after 110-01)
    - webpage/src/components/hero-section.tsx (final state after 110-02)
    - webpage/src/components/contact-section.tsx (final state after 110-02)
    - webpage/src/components/ui/sheet.tsx (new primitive from 110-01)
    - webpage/src/components/ui/badge.tsx (new primitive from 110-02)
  </read_first>
  <action>
    Run the full production build and lint for the `webpage/` app to confirm the combined Phase 110 change set (both Wave-1 plans applied) compiles and lints clean. Run `pnpm build` and `pnpm lint` from within the `webpage/` directory (NOT `web/`). If either fails, fix the specific error in the file it points to (or report it as a blocker) before proceeding to the human checkpoint — do NOT advance to Task 2 on a red build. Also run a quick regression grep to confirm no forbidden pattern slipped in: `usePathname`/`useEffect` must be absent from `site-header.tsx`, and `CardTitle` must be absent from `hero-section.tsx` and `contact-section.tsx`.
  </action>
  <verify>
    <automated>cd webpage && pnpm build && pnpm lint && ! grep -q 'usePathname' src/components/site-header.tsx && ! grep -q 'CardTitle' src/components/hero-section.tsx && ! grep -q 'CardTitle' src/components/contact-section.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm build` in `webpage/` exits 0 (typecheck + production build clean)
    - `pnpm lint` in `webpage/` exits 0 (no ESLint errors)
    - `! grep -q 'usePathname' webpage/src/components/site-header.tsx` — anti-pattern absent
    - `! grep -q 'CardTitle' webpage/src/components/hero-section.tsx` and `! grep -q 'CardTitle' webpage/src/components/contact-section.tsx` — no heading demotion
  </acceptance_criteria>
  <done>Both `pnpm build` and `pnpm lint` pass in webpage/ with the full Phase 110 change set, and the regression greps confirm no forbidden pattern.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human visual + functional checkpoint (mobile Sheet + Hero/Contacto Card/Badge, both themes)</name>
  <files>(no source changes — human verification only)</files>
  <read_first>
    - .planning/phases/LEXCV-110-refinamento-da-landing-webpage/110-UI-SPEC.md (Copywriting Contract, Header layout resolution, Typography)
    - .planning/phases/LEXCV-110-refinamento-da-landing-webpage/110-01-SUMMARY.md and 110-02-SUMMARY.md (what shipped, any deviations)
  </read_first>
  <action>
    Present this checkpoint to the user. Ensure the landing dev server is running (`pnpm dev` in `webpage/`, port 3001) and walk the human through the `<how-to-verify>` steps below in both light and dark themes and at both mobile (<768px) and desktop (≥768px) widths. Do NOT auto-approve — this is a blocking human checkpoint. If the user reports an issue, fix it minimally within the files owned by Plans 110-01/110-02 (do not expand scope), then re-present. On "approved", record the sign-off in the SUMMARY and mark LDG-17/LDG-18 Complete in REQUIREMENTS.md.
  </action>
  <what-built>
    Phase 110 shipped: (1) LDG-17 — mobile navigation added to `webpage/`'s SiteHeader via a reused `Sheet` drawer (hamburger trigger, 3 anchor links + Entrar, click-to-close); (2) LDG-18 — Hero and Contacto sections recomposed with `Card`/`Badge`, replicating TrustSection's idiomatic Card look, with headings kept as raw `<h1>`/`<h2>`.
  </what-built>
  <how-to-verify>
    Start the landing dev server from `webpage/` (`pnpm dev`, runs on http://localhost:3001) and open http://localhost:3001.

    Mobile nav (LDG-17) — resize the browser to a narrow/mobile width (<768px, e.g. DevTools device toolbar at 390px) or use a phone:
    1. Confirm a hamburger (☰) button appears in the header, and the always-visible "Entrar" button is NOT shown at this width (it should only appear on desktop).
    2. Tap the hamburger — a drawer slides in from the RIGHT and shows: Funcionalidades, Confiança, Contacto, and an Entrar item. The drawer has a visible X close button (top-right).
    3. Tap "Funcionalidades" — the drawer closes AND the page scrolls to the Funcionalidades section. Repeat for "Confiança" and "Contacto" — each closes the drawer and scrolls to its section.
    4. Reopen the drawer, tap "Entrar" — the drawer closes (it navigates toward /login; a 404 in isolated `pnpm dev` is expected here since /login lives in the other app — do NOT treat that 404 as a bug).
    5. Widen the browser back to desktop (≥768px) — confirm the hamburger disappears, the 3 nav links + ThemeToggle + Entrar button reappear in the header exactly as before, and "Entrar" appears exactly ONCE.

    Card/Badge recomposition (LDG-18) — at desktop width:
    6. Hero section: the "PLATAFORMA DE GESTÃO JURÍDICA" eyebrow is now a pill-shaped Badge (neutral/gray secondary tone), still uppercase, same ~14px semibold size (not visibly smaller). The Hero heading, paragraph, and two CTA buttons sit inside a Card container that matches the visual style of the Confiança (Trust) cards below. The BrandMark logo and the short blue accent line still sit ABOVE the card, unchanged.
    7. Contacto section: the "CONTACTO" eyebrow is the same Badge style as the Hero's; its heading, paragraph, and "Pedir Demonstração" button sit inside a Card that looks identical to the Hero's card.
    8. Toggle dark mode (ThemeToggle in the header) and re-check steps 6–7 in dark mode — Badges and Cards render with correct contrast; nothing is invisible or clipped.

    Confirm no console errors appear (especially no Radix "DialogTitle" a11y warning when opening the drawer — the sr-only SheetTitle should prevent it).
  </how-to-verify>
  <verify>
    <human-check>All 8 steps above pass: mobile hamburger→Sheet works with click-to-close on all 4 items, no duplicate Entrar, desktop header unchanged, Hero/Contacto Cards+Badges render correctly in light and dark, no console a11y warning.</human-check>
  </verify>
  <acceptance_criteria>
    - Mobile (<768px): hamburger visible, header Entrar button hidden; drawer opens from the right with the 3 anchors + Entrar; each item closes the drawer; anchors scroll to their section.
    - "Entrar" appears exactly once at every breakpoint (never duplicated on mobile).
    - Desktop (≥768px): hamburger hidden; 3 nav links + ThemeToggle + Entrar button present and unchanged from before the phase.
    - Hero eyebrow renders as a secondary Badge at the original ~14px/600 uppercase size (not Badge's smaller default); Hero content sits in a Card matching TrustSection's card style; BrandMark + blue accent rule remain above the Card.
    - Contacto eyebrow/Card match the Hero's; both render correctly in light AND dark themes with no clipping.
    - No console errors, in particular no Radix DialogTitle a11y warning when the drawer opens.
  </acceptance_criteria>
  <done>Human confirms mobile Sheet nav (with click-to-close, no duplicate Entrar), unchanged desktop header, and correct Hero/Contacto Card/Badge rendering in both themes — or returns a concrete fix list.</done>
  <resume-signal>Type "approved" to close Phase 110, or describe any issue (which step, what you saw) to route a fix.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| n/a (verification-only plan) | This plan modifies no source files; it runs build/lint and a human visual check. It introduces no new trust boundary beyond those already dispositioned in 110-01 and 110-02. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-110-05 | Tampering (regression) | Phase 110 combined change set | mitigate | `pnpm build` + `pnpm lint` must exit 0 before human sign-off, catching any type error or forbidden-pattern regression across the two parallel Wave-1 plans; regression greps assert no `usePathname`/`CardTitle` slipped in. |
| T-110-SC | Tampering (supply chain) | Phase 110 dependency surface | accept | Zero new npm dependencies across the whole phase; both new primitives (sheet/badge) are byte-identical in-repo copies of files already vetted through Phase 101's package-legitimacy gate. |
</threat_model>

<verification>
- `cd webpage && pnpm build` passes.
- `cd webpage && pnpm lint` passes.
- Regression greps confirm no `usePathname`/`useEffect` in site-header.tsx and no `CardTitle` in hero/contact sections.
- Human checkpoint approved (mobile Sheet interaction + light/dark Card/Badge rendering).
</verification>

<success_criteria>
Phase 110 closed: LDG-17 and LDG-18 both verified end-to-end against a running build, with a human confirming the mobile navigation interaction and the Card/Badge visual result in both themes.
</success_criteria>

<output>
Create `.planning/phases/LEXCV-110-refinamento-da-landing-webpage/110-03-SUMMARY.md` when done.
</output>
