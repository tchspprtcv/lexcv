---
phase: 33-processos-workflow-gates-e-prazos
plan: "02"
type: execute
wave: 2
depends_on: ["33-01"]
files_modified:
  - web/src/types/processos.ts
  - web/src/schemas/processos.ts
  - web/src/lib/prazos.ts
  - web/src/hooks/use-processos.ts
autonomous: true
requirements: [PRC-27, AGD-22]

must_haves:
  truths:
    - "Tipos WorkflowResponse, Prazo, TransicaoInfo e requests existem e espelham o contrato do backend"
    - "Schemas Zod validam justificativa (min 10) e criacao de prazo"
    - "Helper prazosRiscoToVariant/prazosRiscoToLabel mapeia risco do backend para badge sem reimplementar a regra"
    - "Hooks TanStack Query para workflow, transicao, prazos list, create e toggle existem e invalidam as caches certas"
  artifacts:
    - path: "web/src/lib/prazos.ts"
      provides: "Fonte unica de mapeamento risco->variant/label no frontend"
      contains: "prazosRiscoToVariant"
    - path: "web/src/hooks/use-processos.ts"
      provides: "Hooks workflow/transicao/prazos"
      contains: "useWorkflow"
    - path: "web/src/types/processos.ts"
      provides: "Tipos do workflow e prazo"
      contains: "WorkflowResponse"
    - path: "web/src/schemas/processos.ts"
      provides: "Schemas de justificativa e prazo"
      contains: "transicaoJustificativaFormSchema"
  key_links:
    - from: "use-processos.ts useExecutarTransicao"
      to: "/processos/{id}/transicao/{acao}"
      via: "apiFetch POST"
      pattern: "transicao/"
    - from: "use-processos.ts usePrazos"
      to: "/processos/{id}/prazos"
      via: "apiFetch GET"
      pattern: "prazos"
    - from: "lib/prazos.ts"
      to: "types/processos.ts PrazoRisco"
      via: "type import"
      pattern: "PrazoRisco"
---

<objective>
Construir a camada de dados do frontend para o workflow e prazos: tipos TypeScript que espelham o contrato do backend (Plan 01), schemas Zod para os formularios de justificativa e prazo, o helper lib/prazos.ts (fonte unica de mapeamento risco->badge, analogo a lib/conflict-check.ts), e os hooks TanStack Query que consomem os endpoints. O frontend nao reimplementa a regra de transicao nem a regra de risco — apenas consome o que o backend devolve.

Purpose: Dar a camada de UI (Plan 03) contratos prontos (tipos, hooks, helper) para que a pagina nao tenha de explorar a API. Cobre PRC-27 e AGD-22 no lado do cliente.
Output: Tipos novos em types/processos.ts, schemas em schemas/processos.ts, novo lib/prazos.ts, e hooks novos em use-processos.ts.
</objective>

<execution_context>
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/LEXCV-33-processos-workflow-gates-e-prazos/33-CONTEXT.md
@.planning/phases/LEXCV-33-processos-workflow-gates-e-prazos/33-PATTERNS.md
@.planning/phases/LEXCV-33-processos-workflow-gates-e-prazos/33-01-PLAN.md

<interfaces>
<!-- Backend contract produced by Plan 01 (Wave 1). Mirror exactly. -->

GET  /processos/{id}/workflow → WorkflowResponse { estadoAtual, responsavelId, responsavelNome, proximoPasso, transicoesDisponiveis: TransicaoInfo[] }
  TransicaoInfo { acao, label, permissaoNecessaria: "processos:edit"|"processos:manage", requerJustificativa: boolean }
POST /processos/{id}/transicao/{acao}  body { justificativa? } → updated Processo
GET  /processos/{id}/prazos → Prazo[] { id, descricao, dataLimite, prioridade, responsavelId, concluido, escalonado, risco: "ok"|"proximo"|"vencido", createdAt }
POST /processos/{id}/prazos  body { descricao, dataLimite, prioridade?, responsavelId? } → created Prazo
PATCH /processos/{id}/prazos/{prazoId}/concluido  body { concluido } → updated Prazo

Existing in use-processos.ts to use as analogs (do NOT modify their behavior):
- apiFetch wrapper (always credentials:include; toasts non-401/403 errors).
- ProcessoApi type + normalizeProcesso (lines 21-44) — extend to carry responsavelId/responsavel_id.
- useConflictCheckDecisao (lines 400-412) — query analog for useWorkflow.
- useFormalizarProcesso (lines 414-432) — mutation+cache-invalidation analog for useExecutarTransicao.
- useProcessoPartes (lines 216-225) — list query analog for usePrazos.
- useAddProcessoParte (lines 227-239) — create mutation analog for useCreatePrazo.
- useUpdateProcessoFaseStatus (lines 289-308) — optimistic cache update analog for useTogglePrazoConcluido.

Existing in schemas/processos.ts:
- optionalTrimmedString (lines 3-7) — reuse, do not redeclare.
- conflictCheckDecisaoFormSchema (lines 56-73) — required-field Zod analog.

Existing in lib/conflict-check.ts (lines 1-31) — structural analog for lib/prazos.ts.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Tipos + schemas + helper lib/prazos.ts</name>
  <files>
    web/src/types/processos.ts,
    web/src/schemas/processos.ts,
    web/src/lib/prazos.ts
  </files>
  <read_first>
    - web/src/types/processos.ts (target; existing interface style lines 1-142, Processo interface line 15)
    - web/src/schemas/processos.ts (target; optionalTrimmedString lines 3-7, conflictCheckDecisaoFormSchema lines 56-73, processoMovimentacaoFormSchema lines 41-47)
    - web/src/lib/conflict-check.ts (analog for lib/prazos.ts — Record-based map, two exported functions, JSDoc, union return type)
    - .planning/phases/LEXCV-33-processos-workflow-gates-e-prazos/33-PATTERNS.md (sections: types/processos.ts, schemas/processos.ts, lib/prazos.ts — exact field lists and full file pattern)
    - .planning/phases/LEXCV-33-processos-workflow-gates-e-prazos/33-UI-SPEC.md (Prazo Risk Badge Color Mapping: ok->green, proximo->amber, vencido->red; labels PRAZO PRÓXIMO / PRAZO VENCIDO)
  </read_first>
  <action>
    In types/processos.ts append (after existing content): union types PrazoRisco ("ok"|"proximo"|"vencido"), PrazoPrioridade ("ALTA"|"MEDIA"|"BAIXA"), TransicaoAcao ("ativar"|"suspender"|"encerrar"|"reabrir"); interface TransicaoInfo { acao: TransicaoAcao; label: string; permissaoNecessaria: "processos:edit"|"processos:manage"; requerJustificativa: boolean }; interface WorkflowResponse { estadoAtual: string; responsavelId: string|null; responsavelNome: string|null; proximoPasso: string|null; transicoesDisponiveis: TransicaoInfo[] }; interface Prazo { id, processo_id, tenant_id, descricao, dataLimite, prioridade: PrazoPrioridade, responsavelId: string|null, concluido: boolean, escalonado: boolean, risco: PrazoRisco, createdAt: string }; interface PrazoCreateRequest { descricao: string; dataLimite: string; prioridade?: PrazoPrioridade; responsavelId?: string }; interface TransicaoRequest { justificativa?: string }. Also add optional responsavel_id?: string to the existing Processo interface (after data_fim), and optional listing-signal fields responsavel_nome?: string, risco_mais_critico?: PrazoRisco, tem_prazo_escalonado?: boolean to the Processo interface (consumed by the listing page in Plan 03).

    In schemas/processos.ts add transicaoJustificativaFormSchema = z.object({ justificativa: z.string().trim().min(10, "Justificativa deve ter pelo menos 10 caracteres") }) with exported type TransicaoJustificativaFormValues; and prazoFormSchema = z.object({ descricao: z.string().trim().min(1, "A descrição é obrigatória"), dataLimite: z.string().trim().min(1, "A data limite é obrigatória"), prioridade: z.enum(["ALTA","MEDIA","BAIXA"]).default("MEDIA"), responsavelId: optionalTrimmedString }) with exported type PrazoFormValues. Reuse the existing optionalTrimmedString — do not redeclare it.

    Create web/src/lib/prazos.ts exporting prazosRiscoToVariant(risco: PrazoRisco): "green"|"amber"|"red" mapping ok->green, proximo->amber, vencido->red (fallback amber); and prazosRiscoToLabel(risco: PrazoRisco): string mapping ok->"PRAZO OK", proximo->"PRAZO PROXIMO", vencido->"PRAZO VENCIDO" (fallback "PRAZO PROXIMO"). Import PrazoRisco as a type from @/types/processos. This file is the single source of truth for risco->badge mapping — the UI must not inline the mapping.
  </action>
  <acceptance_criteria>
    - types/processos.ts exports WorkflowResponse, Prazo, TransicaoInfo, PrazoCreateRequest, TransicaoRequest, PrazoRisco. Assertion: `grep -c 'WorkflowResponse' web/src/types/processos.ts` >= 1 and `grep -c 'export type PrazoRisco' web/src/types/processos.ts` >= 1.
    - Processo interface carries responsavel_nome, risco_mais_critico, tem_prazo_escalonado. Assertion: `grep -c 'risco_mais_critico' web/src/types/processos.ts` >= 1.
    - schemas/processos.ts exports transicaoJustificativaFormSchema with a min(10) rule and prazoFormSchema. Assertion: `grep -c 'transicaoJustificativaFormSchema' web/src/schemas/processos.ts` >= 1 and `grep -c 'min(10' web/src/schemas/processos.ts` >= 1.
    - lib/prazos.ts exports prazosRiscoToVariant and prazosRiscoToLabel and imports PrazoRisco from @/types/processos. Assertion: `grep -c 'prazosRiscoToVariant' web/src/lib/prazos.ts` >= 1.
    - Typecheck assertion: `cd web && pnpm exec tsc --noEmit` reports no new errors introduced by these files.
  </acceptance_criteria>
  <verify>
    <automated>cd web && pnpm exec tsc --noEmit 2>&1 | grep -E 'prazos|processos\.ts|schemas/processos' | head -10 || echo "no type errors in target files"</automated>
  </verify>
  <done>Workflow/Prazo types, justificativa+prazo Zod schemas, and lib/prazos.ts exist and typecheck; risco->badge mapping lives only in lib/prazos.ts.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Hooks TanStack Query para workflow, transicao e prazos</name>
  <files>web/src/hooks/use-processos.ts</files>
  <read_first>
    - web/src/hooks/use-processos.ts (target; ProcessoApi+normalizeProcesso lines 21-44, useConflictCheckDecisao 400-412, useFormalizarProcesso 414-432, useProcessoPartes 216-225, useAddProcessoParte 227-239, useUpdateProcessoFaseStatus 289-308, type imports 4-19)
    - web/src/types/processos.ts (WorkflowResponse, Prazo, PrazoCreateRequest, TransicaoRequest — created in Task 1)
    - .planning/phases/LEXCV-33-processos-workflow-gates-e-prazos/33-PATTERNS.md (use-processos.ts section: useWorkflow, useExecutarTransicao, usePrazos, useCreatePrazo, useTogglePrazoConcluido — full hook patterns and queryKey/invalidation rules)
  </read_first>
  <action>
    Add WorkflowResponse, Prazo, PrazoCreateRequest, TransicaoRequest to the type imports. Extend ProcessoApi and normalizeProcesso so the Processo carries responsavel_id (map api.responsavel_id ?? api.responsavelId) and the listing-signal fields responsavel_nome, risco_mais_critico, tem_prazo_escalonado pass through.

    Add useWorkflow(processoId): useQuery keyed ["processos","workflow",processoId], queryFn apiFetch&lt;WorkflowResponse&gt;(`/processos/{id}/workflow`), enabled when window defined and processoId truthy, staleTime 15_000.

    Add useExecutarTransicao(processoId): useMutation with mutationFn taking { acao: string; payload?: TransicaoRequest }, POST to `/processos/{id}/transicao/{acao}` with JSON body of payload ?? {}, returning normalizeProcesso(response); onSuccess invalidate ["processos","list"], setQueryData ["processos","detail",processoId], and invalidate ["processos","workflow",processoId] and ["processos","movimentacoes",processoId].

    Add usePrazos(processoId): useQuery keyed ["processos","prazos",processoId], queryFn apiFetch&lt;Prazo[]&gt;(`/processos/{id}/prazos`), enabled+staleTime same as useWorkflow.

    Add useCreatePrazo(processoId): useMutation mutationFn (payload: PrazoCreateRequest) POST `/processos/{id}/prazos`; onSuccess invalidate ["processos","prazos",processoId].

    Add useTogglePrazoConcluido(processoId): useMutation mutationFn ({ prazoId, concluido }) PATCH `/processos/{id}/prazos/{prazoId}/concluido` with body { concluido }; onSuccess optimistically setQueryData ["processos","prazos",processoId] replacing the matching prazo by id with the updated one.

    Use encodeURIComponent on path segments as in existing hooks. Do not add redundant toasts — apiFetch already toasts non-401/403 errors.
  </action>
  <acceptance_criteria>
    - use-processos.ts exports useWorkflow, useExecutarTransicao, usePrazos, useCreatePrazo, useTogglePrazoConcluido. Assertion: for each name, `grep -c 'export function {name}' web/src/hooks/use-processos.ts` >= 1.
    - useExecutarTransicao POSTs to a path containing "/transicao/". Assertion: `grep -c 'transicao/' web/src/hooks/use-processos.ts` >= 1.
    - useExecutarTransicao onSuccess invalidates ["processos","workflow",processoId]. Assertion: `grep -c '"workflow"' web/src/hooks/use-processos.ts` >= 1.
    - usePrazos/useCreatePrazo/useTogglePrazoConcluido reference queryKey ["processos","prazos",processoId]. Assertion: `grep -c '"prazos"' web/src/hooks/use-processos.ts` >= 2.
    - normalizeProcesso maps responsavel_id. Assertion: `grep -c 'responsavel_id' web/src/hooks/use-processos.ts` >= 1.
    - Typecheck assertion: `cd web && pnpm exec tsc --noEmit` reports no new errors in use-processos.ts.
  </acceptance_criteria>
  <verify>
    <automated>cd web && pnpm exec tsc --noEmit 2>&1 | grep 'use-processos' | head -10 || echo "no type errors in use-processos.ts"</automated>
  </verify>
  <done>Five hooks (workflow query, transicao mutation, prazos query, prazo create, prazo toggle) exist, consume the Plan 01 endpoints, and invalidate/update the correct query caches; normalizeProcesso carries responsavel_id and listing signals.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| frontend UI → REST API (via apiFetch) | Client sends acao/justificativa/prazo payloads; backend is the enforcement authority |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33F-01 | Elevation of Privilege | client-side permission checks | accept | Frontend permission/transition rules are UX only; all enforcement is server-side (Plan 01). The data layer does not duplicate the transition map — it consumes transicoesDisponiveis from the backend. |
| T-33F-02 | Tampering | risco classification in UI | mitigate | risco is read from the backend response; lib/prazos.ts maps risco→badge only, never recomputes ok/proximo/vencido. No client-side date math determines risk. |
| T-33F-03 | Information Disclosure | query caching | accept | TanStack Query caches are per-session in-memory; no cross-tenant data because the API is tenant-scoped server-side. |
| T-33-SC | Tampering | npm/pnpm installs | mitigate | No new dependencies added in this plan (Dialog/Textarea install is in Plan 03). N/A here. |
</threat_model>

<verification>
- `cd web && pnpm exec tsc --noEmit` → no new type errors from the four modified/created files.
- `cd web && pnpm lint` → no new lint errors in changed files.
- grep assertions in each task's acceptance_criteria pass.
</verification>

<success_criteria>
- Types mirror the Plan 01 contract exactly (camelCase where backend serializes camelCase; snake_case processo_id/tenant_id as the backend emits).
- Schemas enforce justificativa min 10 and required prazo fields.
- lib/prazos.ts is the single frontend source of truth for risco→badge.
- Five hooks consume the endpoints and manage caches correctly; no transition/risk business logic reimplemented client-side.
</success_criteria>

<output>
Create `.planning/phases/LEXCV-33-processos-workflow-gates-e-prazos/33-02-SUMMARY.md` when done.
</output>
