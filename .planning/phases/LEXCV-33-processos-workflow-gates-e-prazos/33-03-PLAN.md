---
phase: 33-processos-workflow-gates-e-prazos
plan: "03"
type: execute
wave: 3
depends_on: ["33-02"]
files_modified:
  - web/src/components/ui/dialog.tsx
  - web/src/components/ui/textarea.tsx
  - web/src/app/(dashboard)/processos/[id]/page.tsx
  - web/src/app/(dashboard)/processos/page.tsx
autonomous: false
requirements: [PRC-27, AGD-22]

must_haves:
  truths:
    - "O detalhe do processo mostra um card de Workflow com estado, responsavel, proximo passo e botoes de transicao gated por permissao"
    - "Transicoes criticas abrem um Dialog com campo de justificativa (RHF+Zod min 10)"
    - "O detalhe mostra um card de Prazos com badges de risco e toggle de concluido"
    - "A listagem de processos sinaliza responsavel, risco de prazo e flag de escalonamento"
  artifacts:
    - path: "web/src/components/ui/dialog.tsx"
      provides: "Componente Dialog shadcn"
      contains: "DialogContent"
    - path: "web/src/components/ui/textarea.tsx"
      provides: "Componente Textarea shadcn"
      contains: "Textarea"
    - path: "web/src/app/(dashboard)/processos/[id]/page.tsx"
      provides: "Workflow card + Prazos card + Dialogs"
      contains: "useWorkflow"
    - path: "web/src/app/(dashboard)/processos/page.tsx"
      provides: "Sinais de responsavel + risco na listagem"
      contains: "prazosRiscoToVariant"
  key_links:
    - from: "processos/[id]/page.tsx workflow card"
      to: "useWorkflow / useExecutarTransicao"
      via: "hook consumption"
      pattern: "useExecutarTransicao"
    - from: "processos/[id]/page.tsx prazos card"
      to: "usePrazos / useCreatePrazo / useTogglePrazoConcluido"
      via: "hook consumption"
      pattern: "usePrazos"
    - from: "processos/page.tsx estado cell"
      to: "lib/prazos.ts"
      via: "prazosRiscoToVariant/Label"
      pattern: "prazosRiscoToVariant"
---

<objective>
Construir a superficie UI do workflow e prazos: instalar os componentes shadcn em falta (Dialog, Textarea), adicionar ao detalhe do processo um card de Workflow (estado, responsavel, proximo passo, botoes de transicao gated por permissao, Dialog de justificativa para transicoes criticas) e um card de Prazos (lista com badges de risco, criar prazo, toggle concluido), e adicionar sinais de responsavel/risco/escalonamento na listagem de processos. Todas as regras de transicao e risco vem do backend via os hooks do Plan 02.

Purpose: Tornar o workflow e os prazos visiveis e operaveis no UI, respeitando o gating de permissao (espelhando o backend) e a fonte unica de mapeamento de risco. Cobre PRC-27 e AGD-22 no nivel de apresentacao.
Output: dialog.tsx + textarea.tsx instalados; detalhe e listagem de processos atualizados.
</objective>

<execution_context>
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/LEXCV-33-processos-workflow-gates-e-prazos/33-CONTEXT.md
@.planning/phases/LEXCV-33-processos-workflow-gates-e-prazos/33-PATTERNS.md
@.planning/phases/LEXCV-33-processos-workflow-gates-e-prazos/33-UI-SPEC.md
@.planning/phases/LEXCV-33-processos-workflow-gates-e-prazos/33-02-PLAN.md

<interfaces>
<!-- Frontend data layer produced by Plan 02 (Wave 2). Consume directly. -->

From @/hooks/use-processos:
- useWorkflow(processoId) → { data: WorkflowResponse, isLoading, isError }
- useExecutarTransicao(processoId) → mutateAsync({ acao, payload?: { justificativa } }), isPending
- usePrazos(processoId) → { data: Prazo[], isLoading, isError }
- useCreatePrazo(processoId) → mutateAsync(PrazoCreateRequest)
- useTogglePrazoConcluido(processoId) → mutateAsync({ prazoId, concluido })

From @/lib/prazos: prazosRiscoToVariant(risco), prazosRiscoToLabel(risco)
From @/schemas/processos: transicaoJustificativaFormSchema, prazoFormSchema (+ inferred value types)
From @/types/processos: WorkflowResponse, TransicaoInfo, Prazo, PrazoRisco, TransicaoAcao

Existing in processos/[id]/page.tsx:
- usePermissions; canEditProcessos = permissions.can.edit("processos"); canManageProcessos = permissions.can.manage("processos") (lines 65-69)
- Conflict Check card block (lines 279-356) — Card shell + permission-gated action + error display analog.
- dl grid (lines 242-274) — metadata rows analog.
- estadoVariant + estadoLabel already computed for the header badge.
- onFormalizar try/catch error handling (lines 137-144) — analog for transition/prazo handlers.

Existing in processos/page.tsx:
- estadoVariant switch (lines 308-317), sub-line pattern (lines 330-332), Entrada: date line (line 331).
- Processo rows now carry responsavel_nome, risco_mais_critico, tem_prazo_escalonado (Plan 01 + Plan 02).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Instalar Dialog e Textarea shadcn</name>
  <files>web/src/components/ui/dialog.tsx, web/src/components/ui/textarea.tsx</files>
  <read_first>
    - web/src/components/ui/badge.tsx (confirm shadcn/tailwind v4 conventions used by the project)
    - .planning/phases/LEXCV-33-processos-workflow-gates-e-prazos/33-UI-SPEC.md (Component Inventory + Registry Safety: Dialog and Textarea are shadcn official, no vetting required)
    - web/AGENTS.md (Next.js 16 caveat before touching framework-adjacent files)
  </read_first>
  <action>
    From the web/ directory, install the two missing shadcn official components: run `pnpm dlx shadcn@latest add dialog textarea` (fallback `npx shadcn add dialog textarea` if the pnpm form is unavailable). This creates web/src/components/ui/dialog.tsx (exporting Dialog, DialogContent, DialogDescription, DialogFooter, DialogHeader, DialogTitle, etc.) and web/src/components/ui/textarea.tsx (exporting Textarea). Do not hand-author these files — use the CLI so they match the project's shadcn registry. Both are shadcn official components (no third-party vetting required per UI-SPEC Registry Safety). If the CLI prompts, accept defaults consistent with the existing components in web/src/components/ui/. Do not run any other package install.
  </action>
  <acceptance_criteria>
    - web/src/components/ui/dialog.tsx exists and exports DialogContent. Assertion: `grep -c 'DialogContent' web/src/components/ui/dialog.tsx` >= 1.
    - web/src/components/ui/textarea.tsx exists and exports Textarea. Assertion: `grep -c 'Textarea' web/src/components/ui/textarea.tsx` >= 1.
    - No unrelated dependency added: `git diff --stat web/package.json` shows only @radix-ui/react-dialog (or equivalent) added, no other runtime packages.
    - Typecheck assertion: `cd web && pnpm exec tsc --noEmit` reports no errors in the two new files.
  </acceptance_criteria>
  <verify>
    <automated>cd web && test -f src/components/ui/dialog.tsx && test -f src/components/ui/textarea.tsx && echo "OK: dialog+textarea present"</automated>
  </verify>
  <done>dialog.tsx and textarea.tsx exist via shadcn CLI and export the required members; no unrelated packages installed.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Workflow card + Prazos card + Dialogs no detalhe do processo</name>
  <files>web/src/app/(dashboard)/processos/[id]/page.tsx</files>
  <read_first>
    - web/src/app/(dashboard)/processos/[id]/page.tsx (target; Conflict Check card 279-356, dl grid 242-274, permissions 65-69, onFormalizar 137-144, existing estadoVariant/estadoLabel)
    - web/src/components/ui/dialog.tsx, web/src/components/ui/textarea.tsx (installed in Task 1)
    - web/src/hooks/use-processos.ts (useWorkflow, useExecutarTransicao, usePrazos, useCreatePrazo, useTogglePrazoConcluido — Plan 02)
    - web/src/lib/prazos.ts (prazosRiscoToVariant, prazosRiscoToLabel — Plan 02)
    - .planning/phases/LEXCV-33-processos-workflow-gates-e-prazos/33-UI-SPEC.md (Layout: Workflow Card, Prazos Card, Justification Dialog, Novo Prazo Dialog, Copywriting Contract, Interaction States, lucide icons list)
    - .planning/phases/LEXCV-33-processos-workflow-gates-e-prazos/33-PATTERNS.md (processos/[id]/page.tsx section: card structures, Dialog, state management additions, onTransicaoClick handlers)
  </read_first>
  <action>
    Add imports: Dialog/DialogContent/DialogDescription/DialogFooter/DialogHeader/DialogTitle, Textarea, Label; lucide icons Play, Pause, XCircle, RotateCcw, Plus, CheckCircle2, Circle, AlertCircle, User; prazosRiscoToVariant/prazosRiscoToLabel from @/lib/prazos; useWorkflow/useExecutarTransicao/usePrazos/useCreatePrazo/useTogglePrazoConcluido from @/hooks/use-processos; transicaoJustificativaFormSchema/prazoFormSchema from @/schemas/processos; TransicaoInfo/TransicaoAcao types.

    Wire state in the detail content component: dialogOpen, activeTransicao (TransicaoInfo|null), prazoDialogOpen, transicaoError, prazoError; instantiate workflow=useWorkflow(id), prazos=usePrazos(id), transicao=useExecutarTransicao(id), createPrazo=useCreatePrazo(id), toggleConcluido=useTogglePrazoConcluido(id); two RHF forms (justificativaForm with zodResolver(transicaoJustificativaFormSchema) default { justificativa: "" }; prazoForm with zodResolver(prazoFormSchema) default { descricao:"", dataLimite:"", prioridade:"MEDIA" }).

    Insert the Workflow card and Prazos card BETWEEN the existing "Dados" card and the tab panel (Partes/Fases/Movimentações), per UI-SPEC layout.

    Workflow card (CardTitle "Workflow" + estado badge using existing estadoVariant/estadoLabel; rounded-none font-bold tracking-wide on badges): loading text "A carregar workflow..."; error text "Erro ao carregar o workflow. Tente novamente." (text-red-600); on data a dl grid (grid-cols-3) with rows Estado (estado badge), Responsável (workflow.data.responsavelNome or italic "Não atribuído"), Próximo Passo (workflow.data.proximoPasso or "—"); then a transitions section under a pt-4 border-t with label "AÇÕES DE TRANSIÇÃO", rendering workflow.data.transicoesDisponiveis as Buttons. Normal (processos:edit) transitions: bg-blue-600 fill; critical (processos:manage): outline neutral. Each button disabled when the user lacks the required permission (canEditProcessos for edit, canManageProcessos for manage) with a title tooltip "Sem permissão: requer {permissaoNecessaria}", and disabled while transicao.isPending. Icon per acao: ativar->Play, suspender->Pause, encerrar->XCircle, reabrir->RotateCcw. Empty list → "Nenhuma transição disponível neste estado."

    onTransicaoClick(t): if t.requerJustificativa set activeTransicao, reset justificativaForm, open dialogOpen; else call the direct transition (transicao.mutateAsync({ acao: t.acao })) inside try/catch setting transicaoError on failure to "Não foi possível executar a transição. Verifique os requisitos e tente novamente." The Justification Dialog binds justificativaForm: DialogTitle = `${activeTransicao?.label} Processo`, DialogDescription "Esta ação requer justificativa e regista uma movimentação no processo.", a Label + Textarea (min-h-[100px] rounded-none) bound to register("justificativa") with the red-600 error message, footer Cancelar (ghost) + Confirmar (submits transicao.mutateAsync({ acao: activeTransicao.acao, payload: { justificativa } }), shows "A processar..." while isPending, closes dialog on success).

    Prazos card (CardTitle "Prazos" + "Novo Prazo" button visible only when canEditProcessos): loading "A carregar prazos...", error "Erro ao carregar os prazos. Tente novamente.", empty "Nenhum prazo registado para este processo."; otherwise a divide-y list ordered as returned (already data_limite asc from backend). Each row: left descricao + data_limite (pt-CV formatted); meta prioridade tag + responsável; right a risco Badge via prazosRiscoToVariant/prazosRiscoToLabel, an AlertCircle escalonado indicator when prazo.escalonado, and a concluido toggle (CheckCircle2 emerald when concluido else Circle slate) calling toggleConcluido.mutateAsync({ prazoId, concluido: !p.concluido }) — visible only when canEditProcessos. The Novo Prazo Dialog binds prazoForm: Descrição (Input, required), Data limite (input type=date styled per UI-SPEC), Prioridade (select ALTA/MEDIA/BAIXA default MEDIA), Responsável (optional select), footer Cancelar + "Guardar Prazo" calling createPrazo.mutateAsync and closing on success; set prazoError on failure to "Não foi possível guardar o prazo. Tente novamente."

    Apply Anti-Safe Harbor CSS throughout: rounded-none on all interactive elements, font-bold tracking-wide on badges/CTAs, focus-visible:ring-blue-500 on inputs/textarea. Use apiFetch's existing toast behavior — set local error state for inline display only, no redundant toasts.
  </action>
  <acceptance_criteria>
    - page.tsx imports and calls useWorkflow, usePrazos, useExecutarTransicao, useCreatePrazo, useTogglePrazoConcluido. Assertion: `grep -c 'useWorkflow' web/src/app/\(dashboard\)/processos/\[id\]/page.tsx` >= 1 and same for usePrazos.
    - The Justification Dialog renders only for transitions where requerJustificativa is true; non-critical transitions call mutateAsync directly. Assertion: `grep -c 'requerJustificativa' web/src/app/\(dashboard\)/processos/\[id\]/page.tsx` >= 1.
    - Transition buttons are disabled when the user lacks the matching permission. Assertion: `grep -c 'canManageProcessos' web/src/app/\(dashboard\)/processos/\[id\]/page.tsx` >= 1.
    - Risco badges use the helper, not an inline map. Assertion: `grep -c 'prazosRiscoToVariant' web/src/app/\(dashboard\)/processos/\[id\]/page.tsx` >= 1 and `grep -c 'risco ===' web/src/app/\(dashboard\)/processos/\[id\]/page.tsx` returns 0.
    - Copy strings match the UI-SPEC Copywriting Contract (e.g. "AÇÕES DE TRANSIÇÃO", "Esta ação requer justificativa e regista uma movimentação no processo.").
    - Typecheck + lint: `cd web && pnpm exec tsc --noEmit` and `pnpm lint` report no new errors for this file.
  </acceptance_criteria>
  <verify>
    <automated>cd web && pnpm exec tsc --noEmit 2>&1 | grep 'processos/\[id\]' | head -10 || echo "no type errors in detail page"</automated>
  </verify>
  <done>Detail page shows the Workflow card (estado/responsavel/proximo passo + permission-gated transition buttons + justification Dialog) and the Prazos card (risco badges + escalonado flag + concluido toggle + Novo Prazo Dialog); all copy/colors follow the UI-SPEC and risco mapping uses lib/prazos.ts only.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Sinais na listagem + verificacao visual do workflow e prazos</name>
  <files>web/src/app/(dashboard)/processos/page.tsx</files>
  <read_first>
    - web/src/app/(dashboard)/processos/page.tsx (target; estadoVariant 308-317, Entrada: sub-line 330-332, estado Badge ~344)
    - web/src/lib/prazos.ts (prazosRiscoToVariant, prazosRiscoToLabel)
    - .planning/phases/LEXCV-33-processos-workflow-gates-e-prazos/33-UI-SPEC.md (Processos Listing: New Columns / Signals — placement and rendering rules)
    - .planning/phases/LEXCV-33-processos-workflow-gates-e-prazos/33-PATTERNS.md (processos/page.tsx section: responsavel sub-line, risco badge signal in ESTADO cell)
  </read_first>
  <action>
    Implementation portion (automate fully before the human check): In processos/page.tsx import prazosRiscoToVariant and prazosRiscoToLabel from @/lib/prazos and AlertCircle from lucide-react. Below the "Entrada: …" sub-line in the NÚMERO column cell, render a responsável sub-line: text-[11px] font-medium text-slate-400 dark:text-slate-500 prefix "Resp.:" + p.responsavel_nome or "—". In the ESTADO cell, below the existing estado Badge, render the prazo risco signal ONLY when p.risco_mais_critico is "proximo" or "vencido" (hidden for "ok" or absent): a Badge with prazosRiscoToVariant(p.risco_mais_critico) and prazosRiscoToLabel(...) (rounded-none font-bold tracking-wide text-[11px]) plus an AlertCircle (h-3 w-3 text-amber-500) when p.tem_prazo_escalonado. Do not add new table columns — only sub-lines/inline content within existing cells. Apply Anti-Safe Harbor CSS.

    Then PAUSE for human verification of the full Phase 33 UI.
  </action>
  <what-built>
    Listing signals (responsável sub-line, prazo risco badge, escalado flag) plus the previously built Workflow card and Prazos card on the detail page.
  </what-built>
  <how-to-verify>
    1. Start backend (`cd backend && mvn spring-boot:run`, ensure SEED_ENABLED=true once if needed) and frontend (`cd web && pnpm dev`); log in as admin@lexcv.cv / admin123.
    2. Open a processo in ATIVO state at http://localhost:3000/processos/{id}. Confirm the Workflow card shows Estado=ATIVO badge (green), a Responsável value or "Não atribuído", a Próximo Passo string, and transition buttons "Suspender" and "Encerrar" rendered as outline (manage-level).
    3. Click "Suspender" → a Dialog opens requiring justificativa. Submit with fewer than 10 chars → inline red validation. Submit with a valid justificativa → estado changes to SUSPENSO (amber badge) and a movimentação is recorded.
    4. In the Prazos card click "Novo Prazo", create a prazo with a past data limite → it appears with a red "vencido" risco badge; create one due in 2 days with prioridade MEDIA → amber "proximo". Toggle concluído on a prazo → icon flips.
    5. As a user WITHOUT processos:manage, confirm Suspender/Encerrar/Reabrir are disabled with the tooltip "Sem permissão: requer processos:manage".
    6. Go to http://localhost:3000/processos — confirm the "Resp.:" sub-line appears and processos with proximo/vencido prazos show the PRAZO PRÓXIMO / PRAZO VENCIDO badge (with the escalado AlertCircle when applicable); processos with only ok/no prazos show no risco badge.
  </how-to-verify>
  <acceptance_criteria>
    - processos/page.tsx renders the responsável sub-line with prefix "Resp.:". Assertion: `grep -c 'Resp.:' web/src/app/\(dashboard\)/processos/page.tsx` >= 1.
    - Risco badge in listing uses the helper and is hidden for "ok". Assertion: `grep -c 'prazosRiscoToVariant' web/src/app/\(dashboard\)/processos/page.tsx` >= 1 and the render guards on risco_mais_critico !== "ok".
    - tem_prazo_escalonado drives the AlertCircle. Assertion: `grep -c 'tem_prazo_escalonado' web/src/app/\(dashboard\)/processos/page.tsx` >= 1.
    - Typecheck: `cd web && pnpm exec tsc --noEmit` reports no new errors for this file.
    - Human verifier confirms steps 2–6 behave as described.
  </acceptance_criteria>
  <resume-signal>Type "approved" or describe issues (e.g. "Suspender not disabled for non-manage user").</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser DOM → REST API (via apiFetch) | User-initiated transition/prazo actions cross to the backend, which is the enforcement authority |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33U-01 | Elevation of Privilege | transition/prazo buttons | mitigate | Buttons disabled via canEditProcessos/canManageProcessos (mirrors backend), but the backend re-enforces — disabling is UX, not the security control. Tooltip explains the missing scope. |
| T-33U-02 | Spoofing | UI-displayed risco/estado | mitigate | risco and estado are read from backend responses; lib/prazos.ts maps risco→badge only and the UI never recomputes ok/proximo/vencido from dates. |
| T-33U-03 | Tampering | npm/pnpm installs (Dialog/Textarea) | mitigate | Only shadcn official Dialog + Textarea installed via shadcn CLI (UI-SPEC Registry Safety: no vetting required). No third-party registry. git diff on package.json reviewed in Task 1 acceptance criteria. |
| T-33U-04 | Information Disclosure | inline error text | accept | Inline errors surface only generic messages or backend-provided messages already sanitized; no stack traces shown. |
</threat_model>

<verification>
- `cd web && pnpm exec tsc --noEmit` and `pnpm lint` → no new errors across the modified files.
- `cd web && pnpm build` → succeeds.
- Human checkpoint (Task 3) confirms workflow transitions, justification gate, prazo risco badges, permission gating, and listing signals behave per UI-SPEC.
</verification>

<success_criteria>
- Dialog + Textarea installed via shadcn CLI.
- Detail page presents Workflow and Prazos cards per UI-SPEC, with server-derived transitions and risco.
- Listing surfaces responsável, prazo risco (proximo/vencido only), and escalado flag.
- Permission gating mirrors the backend; risco mapping uses lib/prazos.ts exclusively.
</success_criteria>

<output>
Create `.planning/phases/LEXCV-33-processos-workflow-gates-e-prazos/33-03-SUMMARY.md` when done.
</output>
