---
phase: 70-backend-refactoring-seeder-alignment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/main/java/com/lexcv/models/DocumentoTipo.java
  - backend/src/main/java/com/lexcv/models/Cliente.java
  - backend/src/main/java/com/lexcv/models/DadosTipo.java
  - backend/src/main/java/com/lexcv/models/DadosTipoConverter.java
  - backend/src/main/java/com/lexcv/controllers/ResourceController.java
  - backend/src/main/java/com/lexcv/seed/DatabaseSeeder.java
autonomous: true
requirements: [CLI-06, CLI-09]

must_haves:
  truths:
    - "The Cliente entity has no dados_tipo field, no @Convert(converter = DadosTipoConverter.class), and no DadosTipo import"
    - "DocumentoTipo enum contains a REG_COMERCIAL constant"
    - "The DadosTipo and DadosTipoConverter source files no longer exist in the backend"
    - "ResourceController.updateCliente does not reference dadosTipo (no setDadosTipo/getDadosTipo call)"
    - "The Empresa seed client (cliente2) uses DocumentoTipo.REG_COMERCIAL, not DocumentoTipo.NIF"
    - "The backend compiles cleanly (mvn -DskipTests package) with no references to DadosTipo remaining anywhere"
  artifacts:
    - path: "backend/src/main/java/com/lexcv/models/DocumentoTipo.java"
      provides: "DocumentoTipo enum including REG_COMERCIAL"
      contains: "REG_COMERCIAL"
    - path: "backend/src/main/java/com/lexcv/models/Cliente.java"
      provides: "Flat-column Cliente entity with no JSON identification blob"
    - path: "backend/src/main/java/com/lexcv/seed/DatabaseSeeder.java"
      provides: "Seed data consistent with the flat model and REG_COMERCIAL for Empresa"
      contains: "DocumentoTipo.REG_COMERCIAL"
  key_links:
    - from: "backend/src/main/java/com/lexcv/models/Cliente.java"
      to: "backend/src/main/java/com/lexcv/models/DocumentoTipo.java"
      via: "@Enumerated(EnumType.STRING) documentoTipo field"
      pattern: "private DocumentoTipo documentoTipo"
---

<objective>
Remove the `dados_tipo` JSON storage from the client identification model, add `REG_COMERCIAL` to the `DocumentoTipo` enum, and align the `DatabaseSeeder` so the Empresa seed client uses the correct document type. This converges `Cliente` on the flat-column identification model (CLI-06) and enables company-registration identification (CLI-09).

Purpose: Eliminate the last JSON `@Convert` blob used for client identification data, replacing it with the flat columns already present on the entity, and make the seed data reflect the intended per-type document semantics.
Output: Modified `DocumentoTipo`, `Cliente`, `ResourceController`, `DatabaseSeeder`; deleted `DadosTipo` and `DadosTipoConverter`.
</objective>

<execution_context>
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/LEXCV-70-backend-refactoring-seeder-alignment/70-CONTEXT.md
@.planning/phases/LEXCV-70-backend-refactoring-seeder-alignment/70-PATTERNS.md

<interfaces>
<!-- Contracts the executor needs. Extracted from the codebase; use directly, no exploration required. -->

Current DocumentoTipo.java (full file):
```java
package com.lexcv.models;

public enum DocumentoTipo {
    NIF,
    CNI,
    PASSAPORTE
}
```

Cliente.java field to remove (lines 65-68):
```java
    @Column(name = "dados_tipo", columnDefinition = "TEXT")
    @Convert(converter = DadosTipoConverter.class)
    @JsonProperty("dados_tipo")
    private DadosTipo dadosTipo;
```

Cliente.java sibling @Convert fields that MUST stay untouched (lines 78-95): documentosEntregues (DocumentosEntreguesConverter), documentosATratar (DocumentosATratarConverter), deslocacoes (DeslocacoesConverter), honorariosPropostos (HonorariosPropostosConverter).

ResourceController.java line 277 (inside updateCliente) to remove:
```java
        cliente.setDadosTipo(payload.getDadosTipo());
```
Surrounding NIF-derivation logic (lines 285-289) MUST stay unchanged:
```java
        if (payload.getDocumentoTipo() == DocumentoTipo.NIF) {
            cliente.setNif(payload.getDocumentoNumero());
        } else if (payload.getNif() != null) {
            cliente.setNif(payload.getNif());
        }
```

DatabaseSeeder.java cliente2 (Empresa) builder currently uses `.documentoTipo(DocumentoTipo.NIF)` (line 120). cliente1 (Singular) also uses `.documentoTipo(DocumentoTipo.NIF)` (line 104) and MUST stay as NIF. DatabaseSeeder imports `com.lexcv.models.*` (wildcard) — no import edits needed.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add REG_COMERCIAL and remove the dados_tipo JSON model from Cliente</name>
  <files>
    backend/src/main/java/com/lexcv/models/DocumentoTipo.java,
    backend/src/main/java/com/lexcv/models/Cliente.java,
    backend/src/main/java/com/lexcv/controllers/ResourceController.java,
    backend/src/main/java/com/lexcv/models/DadosTipo.java,
    backend/src/main/java/com/lexcv/models/DadosTipoConverter.java
  </files>
  <read_first>
    backend/src/main/java/com/lexcv/models/DocumentoTipo.java,
    backend/src/main/java/com/lexcv/models/Cliente.java,
    backend/src/main/java/com/lexcv/models/DadosTipo.java,
    backend/src/main/java/com/lexcv/models/DadosTipoConverter.java,
    backend/src/main/java/com/lexcv/controllers/ResourceController.java (lines 257-293)
  </read_first>
  <action>
    Make four coordinated edits (CLI-06 + the enum part of CLI-09):

    1. In DocumentoTipo.java, add a fourth enum constant `REG_COMERCIAL` after `PASSAPORTE` (keep `NIF`, `CNI`, `PASSAPORTE` in place; comma-terminate `PASSAPORTE` and add `REG_COMERCIAL` as the final constant). No annotations — it is a plain `EnumType.STRING` value.

    2. In Cliente.java, delete exactly the four-line block (the `@Column(name = "dados_tipo", columnDefinition = "TEXT")` annotation, the `@Convert(converter = DadosTipoConverter.class)` annotation, the `@JsonProperty("dados_tipo")` annotation, and the `private DadosTipo dadosTipo;` field). Do NOT touch any other field. Leave the four sibling `@Convert` fields (documentosEntregues, documentosATratar, deslocacoes, honorariosPropostos) and the `import com.fasterxml.jackson.annotation.JsonProperty;` import exactly as-is — that import is still used by numeroCliente, procuracaoKey, descricaoCaso, documentosEntregues, documentosATratar, honorariosPropostos. There is no explicit `import ...DadosTipo` line to remove (same package), and no `import jakarta.persistence.Convert` line to remove because `@Convert` is still used by the sibling fields via the `jakarta.persistence.*` wildcard import.

    3. In ResourceController.java (inside updateCliente, currently line 277), delete only the line `cliente.setDadosTipo(payload.getDadosTipo());`. Leave every other setter and the `DocumentoTipo.NIF` derivation branch (lines 285-289) unchanged. Do NOT add a new REG_COMERCIAL branch — CLI-09 requires only the enum value and its storage in documentoNumero, which the existing `setDocumentoTipo`/`setDocumentoNumero` setters already cover; the nif-derivation stays NIF-only as-is.

    4. Delete the entire files backend/src/main/java/com/lexcv/models/DadosTipo.java and backend/src/main/java/com/lexcv/models/DadosTipoConverter.java (they become dead code once the field is gone). Use `rm` for both.

    Data-migration note: `ddl-auto=update` in dev never drops the existing `dados_tipo` column, so no Flyway/DDL step is required for this refactor; the orphaned column is simply no longer mapped. Do not add a migration.
  </action>
  <verify>
    <automated>cd backend && grep -rl "DadosTipo" src/main/java | grep -c . | grep -qx 0 && grep -q "REG_COMERCIAL" src/main/java/com/lexcv/models/DocumentoTipo.java && test ! -f src/main/java/com/lexcv/models/DadosTipo.java && test ! -f src/main/java/com/lexcv/models/DadosTipoConverter.java && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rl "DadosTipo" backend/src/main/java` returns no files (zero references remain).
    - `backend/src/main/java/com/lexcv/models/DocumentoTipo.java` contains the constant `REG_COMERCIAL`.
    - Files `DadosTipo.java` and `DadosTipoConverter.java` no longer exist.
    - `ResourceController.java` contains no `setDadosTipo` or `getDadosTipo` occurrence; its `DocumentoTipo.NIF` derivation branch is unchanged.
    - `Cliente.java` still contains the four sibling `@Convert` fields (documentosEntregues, documentosATratar, deslocacoes, honorariosPropostos) and still imports JsonProperty.
  </acceptance_criteria>
  <done>DocumentoTipo has REG_COMERCIAL; Cliente has no dados_tipo field/converter; DadosTipo and DadosTipoConverter deleted; ResourceController has no dadosTipo reference; no DadosTipo references remain in the backend source tree.</done>
</task>

<task type="auto">
  <name>Task 2: Align DatabaseSeeder Empresa client to REG_COMERCIAL and verify compile</name>
  <files>backend/src/main/java/com/lexcv/seed/DatabaseSeeder.java</files>
  <read_first>
    backend/src/main/java/com/lexcv/seed/DatabaseSeeder.java (lines 95-126),
    backend/src/main/java/com/lexcv/models/DocumentoTipo.java
  </read_first>
  <action>
    Implement CLI-09 in the seed data. In DatabaseSeeder.java, in the `cliente2` builder (the Empresa: `.tipo("COLETIVA")`, `.nome("Empresa Atlântico, SA")`, currently around line 120), change `.documentoTipo(DocumentoTipo.NIF)` to `.documentoTipo(DocumentoTipo.REG_COMERCIAL)`. Leave `.documentoNumero("512345678")` as the registration number in documento_numero (satisfies CLI-09's "número correspondente registado em documento_numero"). Leave the `.nif("512345678")` line as-is.

    Do NOT change `cliente1` (the Singular client `.tipo("SINGULAR")`) — it correctly keeps `.documentoTipo(DocumentoTipo.NIF)`. There is no `.dadosTipo(...)` builder call anywhere in the seeder, so nothing to remove there. Do NOT attempt to reconcile the `"SINGULAR"`/`"COLETIVA"` string values with the `TipoCliente` enum (`PARTICULAR`/`EMPRESA`) — that inconsistency is explicitly out of scope for this phase.

    After the edit, run the full backend package build to confirm the whole module still compiles with the removed class and new enum value.
  </action>
  <verify>
    <automated>cd backend && grep -A20 'nome("Empresa Atlântico, SA")' src/main/java/com/lexcv/seed/DatabaseSeeder.java | grep -q "DocumentoTipo.REG_COMERCIAL" && mvn -DskipTests -q package && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - The `cliente2` (Empresa "Empresa Atlântico, SA") builder uses `.documentoTipo(DocumentoTipo.REG_COMERCIAL)`.
    - The `cliente1` (Singular) builder still uses `.documentoTipo(DocumentoTipo.NIF)`.
    - No `.dadosTipo(` call exists in DatabaseSeeder.java.
    - `mvn -DskipTests package` completes successfully (BUILD SUCCESS), proving no dangling references to the deleted DadosTipo/DadosTipoConverter and that REG_COMERCIAL resolves.
  </acceptance_criteria>
  <done>Empresa seed client uses REG_COMERCIAL; Singular seed client unchanged (NIF); backend compiles and packages cleanly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → API (PUT /clientes/{id}) | Untrusted JSON payload deserialized into a `Cliente` before controller processing |
| application → database (JPA) | Entity persisted via `ddl-auto=update`; column mapping changes here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-70-01 | Tampering | Removal of `dadosTipo` field from `Cliente` | accept | No new input surface is added; the field is removed, reducing (not expanding) attack surface. `documentoTipo`/`documentoNumero` are already-mapped flat columns validated downstream in later phases (CLI-05/CLI-10). |
| T-70-02 | Information Disclosure | Orphaned `dados_tipo` DB column retains prior JSON after unmapping | accept | Under `ddl-auto=update` the column is no longer read/written by JPA (no code path exposes it). No PII is newly exposed; column stays dormant. A destructive DROP is deliberately not performed to avoid data loss on shared dev DBs. |
| T-70-03 | Tampering | New `REG_COMERCIAL` enum value in `DocumentoTipo` | accept | `EnumType.STRING` deserialization rejects unknown values with a 400; adding a valid constant only widens the accepted set to an intended value. No authorization or tenant-scoping logic changes. |
| T-70-SC | Tampering | npm/pip/cargo installs | accept | No package-manager installs in this phase — purely in-repo Java source edits/deletions. No dependency provenance risk introduced. |
</threat_model>

<verification>
- `grep -rl "DadosTipo" backend/src/main/java` returns nothing.
- `DocumentoTipo.java` contains `REG_COMERCIAL`.
- `DadosTipo.java` and `DadosTipoConverter.java` are deleted.
- `DatabaseSeeder.java` Empresa client (`cliente2`) uses `DocumentoTipo.REG_COMERCIAL`; Singular client (`cliente1`) uses `DocumentoTipo.NIF`.
- `cd backend && mvn -DskipTests package` → BUILD SUCCESS.
</verification>

<success_criteria>
- CLI-06 satisfied: `dados_tipo` JSON storage fully removed from the identification model (field, converter, POJO, controller reference).
- CLI-09 satisfied: `REG_COMERCIAL` exists in `DocumentoTipo` and the Empresa seed client uses it with the number in `documento_numero`.
- Backend compiles and packages cleanly; sibling JSON converters and the unrelated `tipo` String/`TipoCliente` inconsistency are untouched.
</success_criteria>

<output>
Create `.planning/phases/LEXCV-70-backend-refactoring-seeder-alignment/70-01-SUMMARY.md` when done.
</output>
