---
phase: 115-linguagem-visual-icones-em-todos-os-botoes-filtros-icone-only
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/app/(dashboard)/processos/page.tsx
  - web/src/app/(dashboard)/processos/novo/page.tsx
  - web/src/app/(dashboard)/processos/[id]/editar/page.tsx
  - web/src/app/(dashboard)/processos/[id]/documentos-columns.tsx
autonomous: true
requirements: [ICON-01, FICO-01]
must_haves:
  truths:
    - "Processos list filter bar: Aplicar and Limpar are icon-only (Check/X) with Tooltip+aria-label; the already-icon-only disabled Exportar gains aria-label + Tooltip and stays disabled"
    - "Processos list Dashboard link and 'Ver Agenda Completa' keep text and gain icons"
    - "Processo intake wizard, editar page, and the documentos-column Apagar action all carry icons"
  artifacts:
    - path: "web/src/app/(dashboard)/processos/page.tsx"
      provides: "FICO-01 Aplicar(Check)/Limpar(X) icon-only + Exportar a11y completion + ICON-01 nav buttons"
      contains: "aria-label=\"Aplicar filtros\""
    - path: "web/src/app/(dashboard)/processos/novo/page.tsx"
      provides: "Multi-step intake wizard button icons (Voltar/Seguinte/submit/Formalizar/Conflict Check)"
      contains: "lucide-react"
    - path: "web/src/app/(dashboard)/processos/[id]/editar/page.tsx"
      provides: "Cancelar/Guardar icons"
      contains: "lucide-react"
    - path: "web/src/app/(dashboard)/processos/[id]/documentos-columns.tsx"
      provides: "Apagar row-action icon (matches sibling Download icon-only pattern)"
      contains: "Trash2"
  key_links:
    - from: "processos/page.tsx Aplicar/Limpar/Exportar"
      to: "Tooltip primitive + aria-label"
      via: "TooltipTrigger asChild + aria-label locked copy"
      pattern: "aria-label=\"(Aplicar filtros|Limpar filtros|Exportar CSV)\""
---

<objective>
Deliver the Processos-module icon work outside the heavy `[id]/page.tsx` ficha (that file is Plan 05): FICO-01 conversion of the list-page filter buttons (Aplicar→`Check`, Limpar→`X`, both icon-only + Tooltip; plus the already-icon-only disabled Exportar gets its `aria-label`+`Tooltip` a11y completion and stays disabled), and ICON-01 icons on the list nav buttons, the multi-step intake wizard, the editar page, and the documentos-column Apagar action.

Purpose: Own the four lighter Processos files in one parallel plan with exclusive file ownership.
Output: 4 modified files; 17 ICON-01 gaps (incl. 3 FICO-01 conversions) closed.
</objective>

<execution_context>
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/LEXCV-115-linguagem-visual-icones-em-todos-os-botoes-filtros-icone-only/115-UI-SPEC.md
@.planning/phases/LEXCV-115-linguagem-visual-icones-em-todos-os-botoes-filtros-icone-only/115-CONTEXT.md

Exact citations: `115-UI-SPEC.md` → "FICO-01 — Filter Action Button Inventory" (Processos + Processos-page rows, incl. the Exportar special case) and "Processos module" audit table. EVIDENCE, verified 2026-07-21.

FICO-01 pattern + locked copy: identical to Plan 02 — see UI-SPEC "Icon-only + Tooltip code pattern" and "Copy unification". Aplicar→`Aplicar filtros`, Limpar→`Limpar filtros`, Exportar→`Exportar CSV` (tooltip == aria-label). Keep each button's variant; drop width-stretch classes; import Tooltip primitives from `@/components/ui/tooltip`.

Processos Exportar special case (UI-SPEC "Special case: Processos' Exportar button"): L251-253 is ALREADY icon-only (`Download`, `h-9 w-9 p-0`) and `disabled` (non-functional placeholder — CSV export was never wired). Add `aria-label="Exportar CSV"` and wrap in `Tooltip`/`TooltipContent>Exportar CSV`. Do NOT implement export and do NOT change `disabled` — out of scope. (Note: a disabled button won't fire the hover tooltip because `disabled:pointer-events-none`; that's an accepted pre-existing limitation — the a11y value here is the aria-label. Do not remove `disabled` to "make the tooltip work".)

Vocabulary for the ICON-01 gaps here: Voltar→`ArrowLeft`; Continuar/Seguinte→`ArrowRight`; submit-a-create→`Plus`; Formalizar Processo→`CheckCircle2`; Apagar→`Trash2`; "Ver Agenda Completa" (navigate to related full view)→`ArrowRight`. Dashboard link (L143) → `LayoutDashboard` (semantic, standard lucide-react export; new import — CONTEXT.md leaves exact mapping to planner discretion "olhando ao contexto de cada botão"). "Executar Conflict Check" (L483) → `Search` (reuse the established glyph; if the executor judges a scan/shield clearer, `ShieldCheck` is an acceptable substitute).

Do-not-touch in processos/page.tsx: L148 Plus, L240 Filter, L261 Plus — already compliant.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Processos list — FICO-01 (Aplicar/Limpar icon-only, Exportar a11y) + nav icons</name>
  <files>web/src/app/(dashboard)/processos/page.tsx</files>
  <read_first>
    - web/src/app/(dashboard)/processos/page.tsx (imports + L140-265)
    - 115-UI-SPEC.md "FICO-01 — Filter Action Button Inventory" (Processos row + Special case) and "Icon-only + Tooltip code pattern"
  </read_first>
  <behavior>
    - Aplicar (L254-256, `type="submit"`, default-ish variant) → icon-only `Check` + `size="icon"` + `aria-label="Aplicar filtros"` + Tooltip("Aplicar filtros"); still submits.
    - Limpar (L257-259, `variant="ghost"` `onClick={onClear}`) → icon-only `X` + aria-label "Limpar filtros" + Tooltip; onClear still fires.
    - Exportar (L251-253, already icon-only `Download`, `disabled`) → add `aria-label="Exportar CSV"` + wrap in Tooltip("Exportar CSV"); stays `disabled`; no export logic added.
    - Dashboard link (L143) keeps text, gains `LayoutDashboard`; "Ver Agenda Completa" (L195) keeps text, gains `ArrowRight`.
  </behavior>
  <action>
    Extend/add the lucide-react import with `Check, X, ArrowRight, LayoutDashboard` (keep existing Plus/Filter/Download). Add `Tooltip, TooltipTrigger, TooltipContent` from `@/components/ui/tooltip`.
    Convert Aplicar (L254) and Limpar (L257) to icon-only via the UI-SPEC pattern: drop visible text + width classes, add `size="icon"` + locked `aria-label`, wrap in Tooltip; preserve variant/type/onClick.
    Complete the Exportar special case (L251): add `aria-label="Exportar CSV"`, wrap the existing disabled icon-only Button in Tooltip/TooltipContent; keep `disabled` and the existing `Download`/`h-9 w-9 p-0` markup otherwise.
    ICON-01 (keep text): Dashboard link (L143, inside Link if asChild) → `LayoutDashboard`; "Ver Agenda Completa" (L195) → `ArrowRight`.
    Do not touch L148/L240/L261 (compliant).
  </action>
  <acceptance_criteria>
    - Source assertion: three `aria-label` values from the locked-copy set are present; Aplicar/Limpar are `size="icon"` with no visible text child and each wrapped by `TooltipTrigger asChild`.
    - Source assertion: the Exportar button still has `disabled` and now has `aria-label="Exportar CSV"` inside a Tooltip.
    - Source assertion: Dashboard link renders `<LayoutDashboard`; "Ver Agenda Completa" renders `<ArrowRight`; both keep text.
    - Untouched: L148/L240/L261 byte-identical.
  </acceptance_criteria>
  <verify>
    <automated>grep -nE "aria-label=\"(Aplicar filtros|Limpar filtros|Exportar CSV)\"" "web/src/app/(dashboard)/processos/page.tsx" (expect 3) && grep -nE "disabled" "web/src/app/(dashboard)/processos/page.tsx" | grep -i export (Exportar still disabled) && grep -nE "<LayoutDashboard|<ArrowRight" "web/src/app/(dashboard)/processos/page.tsx"</automated>
  </verify>
  <done>Processos filter bar: Aplicar/Limpar icon-only with Tooltip+aria-label, Exportar a11y-completed and still disabled; nav buttons iconed with text; compliant buttons untouched; lint clean.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Processo intake wizard (novo)</name>
  <files>web/src/app/(dashboard)/processos/novo/page.tsx</files>
  <read_first>
    - web/src/app/(dashboard)/processos/novo/page.tsx (imports + the 9 button sites: L225, L455, L462, L483, L624, L634, L658, L765, L773)
    - 115-UI-SPEC.md "Processos module" novo/page.tsx row + vocabulary table
  </read_first>
  <action>
    Add a `lucide-react` import (file has none today). Map the 9 wizard buttons by visible text/context: L225 Voltar→`ArrowLeft`; L455 submit→`Plus` (or `ArrowRight` if it advances a step rather than creating — read the text); L462 Voltar→`ArrowLeft`; L483 "Executar Conflict Check"→`Search`; L624 submit decisão→`Check` (affirmative) or `Plus` per text; L634 disabled placeholder→map to its label via vocabulary, else `ArrowRight` as a neutral forward (keep whatever `disabled` state it has); L658 Seguinte→`ArrowRight`; L765 "Formalizar Processo"→`CheckCircle2`; L773 Voltar→`ArrowLeft`. Icon before text; keep all text, handlers, disabled logic. Optional `Loader2` pending swap only where an `isSubmitting`/`isPending` boolean is already wired into `disabled`.
  </action>
  <acceptance_criteria>
    - Source assertion: one new `from "lucide-react"` import; each of the 9 cited buttons renders a vocabulary-mapped icon.
    - Behavior: all keep visible text; the disabled placeholder (L634) keeps its `disabled` state.
  </acceptance_criteria>
  <verify>
    <automated>grep -nE "from \"lucide-react\"" "web/src/app/(dashboard)/processos/novo/page.tsx" (expect 1) && grep -cE "<(ArrowLeft|ArrowRight|Plus|Check|CheckCircle2|Search)" "web/src/app/(dashboard)/processos/novo/page.tsx" (expect ~9)</automated>
  </verify>
  <done>All 9 intake-wizard buttons carry vocabulary-correct icons with text preserved; import resolves; lint clean.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Processo editar page + documentos-column Apagar</name>
  <files>web/src/app/(dashboard)/processos/[id]/editar/page.tsx, web/src/app/(dashboard)/processos/[id]/documentos-columns.tsx</files>
  <read_first>
    - web/src/app/(dashboard)/processos/[id]/editar/page.tsx (imports + L140, L300, L306)
    - web/src/app/(dashboard)/processos/[id]/documentos-columns.tsx (full — see the compliant Download sibling for the row-action pattern)
    - 115-UI-SPEC.md "Processos module" rows for editar/page.tsx and documentos-columns.tsx
  </read_first>
  <action>
    `processos/[id]/editar/page.tsx` (add lucide-react import): L140 Cancelar→`X`; L300 Guardar→`Save`; L306 Cancelar→`X`. Icon before text; keep text.
    `processos/[id]/documentos-columns.tsx`: L107 "Apagar" is a row-action whose sibling Download button is already icon-only + `Tooltip` + `aria-label` (compliant). Match that established row-action pattern: `Trash2`, icon-only (`size="icon"` or the sibling's existing footprint), `aria-label="Apagar"`, wrapped in Tooltip("Apagar"). Add `Trash2` to the existing lucide-react import (which already has `Download`). Preserve the existing delete handler/confirmation exactly (UI-SPEC Scope Boundaries §5 — additive-visual only).
  </action>
  <acceptance_criteria>
    - Source assertion: editar page imports lucide-react and renders `X`/`Save` on the 3 buttons.
    - Source assertion: documentos-columns imports `Trash2` and the Apagar action follows the sibling's icon-only + Tooltip + aria-label pattern; its handler is unchanged.
  </acceptance_criteria>
  <verify>
    <automated>grep -nE "<Save|<X " "web/src/app/(dashboard)/processos/[id]/editar/page.tsx" && grep -nE "Trash2" "web/src/app/(dashboard)/processos/[id]/documentos-columns.tsx" (import + usage)</automated>
  </verify>
  <done>Editar buttons iconed with text; documentos-column Apagar is icon-only+Tooltip matching its sibling; handlers unchanged; lint clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Adds icons/Tooltip wrappers to existing client-side buttons. No new input, data flow, network call, or auth/authz change. FICO-01 conversion removes visible text but preserves every handler and the Exportar `disabled` state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-115-01 | Tampering | icon/Tooltip markup in processos .tsx | accept | Static compile-time glyph imports; no untrusted input crosses a boundary; delete-action confirmations untouched. |
| T-115-A11Y | WCAG 4.1.2 (a11y) | icon-only Aplicar/Limpar + a11y-completed Exportar (FICO-01) | mitigate | Icon-only removes the visible label; mitigated by `aria-label` + `Tooltip` identical copy on Aplicar/Limpar (satisfies FICO-01 AC4). Exportar's `aria-label` restores its accessible name even though its hover tooltip is suppressed by `disabled` (accepted pre-existing limitation — placeholder button). |
| T-115-SC | Tampering | npm/pnpm install | accept | No package installed — additional named exports from already-installed `lucide-react`. No legitimacy gate applies. |
</threat_model>

<verification>
- `cd web && pnpm lint` clean on the 4 files.
- Grep proves the 3 locked aria-labels + Tooltip wrapping in processos/page.tsx, the preserved Exportar `disabled`, and vocabulary icons across novo/editar/documentos-columns.
- FICO-01 interactive (hover/keyboard) verification deferred to Plan 11.
</verification>

<success_criteria>
- Processos list filter-action buttons are icon-only with Tooltip+aria-label (Exportar a11y-completed, still disabled) — FICO-01 for this module complete.
- Intake wizard, editar page, and documentos-column Apagar carry vocabulary-correct icons (ICON-01).
- Compliant buttons untouched; lint clean.
</success_criteria>

<output>
Create `.planning/phases/LEXCV-115-linguagem-visual-icones-em-todos-os-botoes-filtros-icone-only/115-04-SUMMARY.md` when done.
</output>
