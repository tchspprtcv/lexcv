---
phase: 115-linguagem-visual-icones-em-todos-os-botoes-filtros-icone-only
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/app/(dashboard)/agenda/page.tsx
  - web/src/app/(dashboard)/agenda/[id]/page.tsx
  - web/src/app/(dashboard)/agenda/[id]/editar/page.tsx
  - web/src/app/(dashboard)/agenda/novo/page.tsx
autonomous: true
requirements: [ICON-01, FICO-01]
must_haves:
  truths:
    - "Agenda list 'Limpar Filtros' is icon-only (X) with Tooltip+aria-label; no Aplicar/Exportar button was invented (Agenda filters auto-apply)"
    - "Agenda 'Ver Lista Completa' keeps text and gains an ArrowRight icon"
    - "Agenda event detail/create/edit buttons (Voltar/Editar/Concluir-Reabrir toggle/Apagar/Criar/Cancelar) carry icons"
  artifacts:
    - path: "web/src/app/(dashboard)/agenda/page.tsx"
      provides: "FICO-01 icon-only Limpar Filtros + ICON-01 Ver Lista Completa; optional month-nav Chevron aria-labels"
      contains: "aria-label=\"Limpar filtros\""
    - path: "web/src/app/(dashboard)/agenda/[id]/page.tsx"
      provides: "Voltar/Editar/Concluir-Reabrir/Apagar icons"
      contains: "lucide-react"
    - path: "web/src/app/(dashboard)/agenda/[id]/editar/page.tsx"
      provides: "Voltar/Cancelar/Guardar icons"
      contains: "lucide-react"
    - path: "web/src/app/(dashboard)/agenda/novo/page.tsx"
      provides: "Voltar/Criar/Cancelar icons"
      contains: "lucide-react"
  key_links:
    - from: "agenda/page.tsx Limpar Filtros"
      to: "Tooltip primitive + aria-label"
      via: "icon-only conversion"
      pattern: "aria-label=\"Limpar filtros\""
---

<objective>
Deliver the Agenda-module icon work: FICO-01 conversion of the list "Limpar Filtros" button (→ icon-only `X` + Tooltip + aria-label — the module's only filter-action button, since Agenda's Processo/Categoria/Estado filters auto-apply via `Select onValueChange` with no submit/export step), plus ICON-01 icons across the list, event detail, create, and edit pages.

Purpose: Own the four Agenda files in one parallel plan. Critically, do NOT invent an Aplicar or Exportar button where none exists (UI-SPEC "Agenda" row + QA checklist).
Output: 4 modified files; 15 ICON-01 gaps (incl. 1 FICO-01 conversion) closed; optionally 2 month-nav Chevron aria-labels.
</objective>

<execution_context>
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/LEXCV-115-linguagem-visual-icones-em-todos-os-botoes-filtros-icone-only/115-UI-SPEC.md
@.planning/phases/LEXCV-115-linguagem-visual-icones-em-todos-os-botoes-filtros-icone-only/115-CONTEXT.md

Exact citations: `115-UI-SPEC.md` → "FICO-01 — Filter Action Button Inventory" (Agenda row) + "Agenda module" audit table. EVIDENCE, verified 2026-07-21.

FICO-01 pattern + locked copy: same as Plan 02. Limpar→`Limpar filtros` (tooltip == aria-label). Icon-only `X`, keep variant, drop width classes, import Tooltip primitives; TooltipProvider is app-global.

Agenda has NO Aplicar and NO Exportar filter button — the list's Processo/Categoria/Estado filters auto-apply. Convert only "Limpar Filtros" (L291-302). Do not create any button that does not already exist.

Vocabulary: "Ver Lista Completa" (navigate to related full view)→`ArrowRight`; Voltar→`ArrowLeft`; Editar→`Pencil`; Criar/submit-create→`Plus`; Cancelar→`X`; Apagar/"Apagar esta instância"→`Trash2`; Concluir/Reabrir toggle→`CheckCircle2` (concluir) / `RotateCcw` (reabrir) — mirror the analogous Prazo-conclusion toggle pattern.

Discretion/optional: L196/L217 month-nav `ChevronLeft`/`ChevronRight` are already icon-only but MISSING `aria-label` — add `aria-label="Mês anterior"` / `aria-label="Mês seguinte"` (nearly free a11y win, recommended, not blocking). L209 "Hoje" single-word state chip — leave as-is unless it looks inconsistent next to iconed siblings (low necessity; do not force an icon).

Do-not-touch: L233 Plus ("Novo Evento") — already compliant.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Agenda list — FICO-01 Limpar + Ver Lista Completa + optional Chevron a11y</name>
  <files>web/src/app/(dashboard)/agenda/page.tsx</files>
  <read_first>
    - web/src/app/(dashboard)/agenda/page.tsx (imports + L190-235, L285-305, L505-515)
    - 115-UI-SPEC.md "Agenda module" agenda/page.tsx row + FICO-01 Agenda row
  </read_first>
  <behavior>
    - "Limpar Filtros" (L291-302, `variant="ghost"`, `self-end h-9 px-3 font-bold`, `onClick`) → icon-only `X` + `size="icon"` + `aria-label="Limpar filtros"` + Tooltip("Limpar filtros"); onClick still clears filters.
    - "Ver Lista Completa" (L510) keeps text, gains `ArrowRight`.
    - month-nav Chevrons (L196/L217) keep their icon, gain `aria-label` ("Mês anterior"/"Mês seguinte").
  </behavior>
  <action>
    Extend/add the lucide-react import with `X, ArrowRight` (keep existing Plus/ChevronLeft/ChevronRight). Add `Tooltip, TooltipTrigger, TooltipContent` from `@/components/ui/tooltip`.
    Convert "Limpar Filtros" (L291) to icon-only per the UI-SPEC pattern: drop text + width classes, add `size="icon"` + `aria-label="Limpar filtros"`, wrap in Tooltip; keep `variant="ghost"` and onClick.
    "Ver Lista Completa" (L510) → `<ArrowRight className="h-4 w-4" />` (inside Link if asChild), keep text.
    Optional a11y: add `aria-label` to the two month-nav Chevron buttons (L196/L217). Leave L209 "Hoje" as-is. Do not touch L233 Plus. Do NOT add any Aplicar/Exportar button.
  </action>
  <acceptance_criteria>
    - Source assertion: exactly one `aria-label="Limpar filtros"` on a `size="icon"` Button wrapped by `TooltipTrigger asChild`; no visible "Limpar Filtros" text child remains.
    - Source assertion: "Ver Lista Completa" renders `<ArrowRight` and keeps its text.
    - Negative assertion: no new `type="submit"` filter button and no "Exportar"/"Aplicar" button exists in this file (grep for them returns only pre-existing/none).
    - Untouched: L233 Plus byte-identical.
  </acceptance_criteria>
  <verify>
    <automated>grep -nE "aria-label=\"Limpar filtros\"" "web/src/app/(dashboard)/agenda/page.tsx" (expect 1) && grep -nE "<ArrowRight" "web/src/app/(dashboard)/agenda/page.tsx" && grep -ciE ">Aplicar<|Exportar" "web/src/app/(dashboard)/agenda/page.tsx" (expect 0 — none invented)</automated>
  </verify>
  <done>Agenda "Limpar Filtros" is icon-only with Tooltip+aria-label; "Ver Lista Completa" iconed with text; no filter button invented; optional Chevron aria-labels added; lint clean.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Agenda event detail page</name>
  <files>web/src/app/(dashboard)/agenda/[id]/page.tsx</files>
  <read_first>
    - web/src/app/(dashboard)/agenda/[id]/page.tsx (imports + L55, L148, L152, L157, L169, L189)
    - 115-UI-SPEC.md "Agenda module" agenda/[id]/page.tsx row
  </read_first>
  <action>
    Add a `lucide-react` import. L55 Voltar→`ArrowLeft`; L148 Voltar→`ArrowLeft`; L152 Editar→`Pencil`; L157 Concluir/Reabrir toggle→`CheckCircle2` when the action is Concluir and `RotateCcw` when Reabrir (choose per the button's current conditional label/state, mirroring the Prazo-conclusion toggle pattern); L169 Apagar (AlertDialog trigger)→`Trash2`; L189 "Apagar esta instância"→`Trash2`. Icons before text; keep text, handlers, and the existing AlertDialog confirmation exactly (additive-visual only).
  </action>
  <acceptance_criteria>
    - Source assertion: one new lucide-react import; the 6 cited buttons render vocabulary-mapped icons; the Concluir/Reabrir toggle uses `CheckCircle2`/`RotateCcw` per its state.
    - Behavior: AlertDialog delete confirmation unchanged.
  </acceptance_criteria>
  <verify>
    <automated>grep -nE "from \"lucide-react\"" "web/src/app/(dashboard)/agenda/[id]/page.tsx" (expect 1) && grep -nE "<ArrowLeft|<Pencil|<Trash2|<CheckCircle2|<RotateCcw" "web/src/app/(dashboard)/agenda/[id]/page.tsx"</automated>
  </verify>
  <done>Event detail buttons iconed with text preserved and the toggle state-correct; confirmation untouched; lint clean.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Agenda event create + edit pages</name>
  <files>web/src/app/(dashboard)/agenda/[id]/editar/page.tsx, web/src/app/(dashboard)/agenda/novo/page.tsx</files>
  <read_first>
    - web/src/app/(dashboard)/agenda/[id]/editar/page.tsx (imports + L57, L158, L295, L301)
    - web/src/app/(dashboard)/agenda/novo/page.tsx (imports + L105, L274, L280)
    - 115-UI-SPEC.md "Agenda module" rows for editar/page.tsx and novo/page.tsx
  </read_first>
  <action>
    `agenda/[id]/editar/page.tsx` (add lucide-react import): L57 Voltar→`ArrowLeft`; L158 Cancelar→`X`; L295 Guardar→`Save`; L301 Cancelar→`X`.
    `agenda/novo/page.tsx` (add lucide-react import): L105 Voltar→`ArrowLeft`; L274 Criar→`Plus`; L280 Cancelar→`X`.
    Icons before text; keep text/handlers. Optional `Loader2` pending swap only where an `isSubmitting`/`isPending` boolean is already wired into `disabled`.
  </action>
  <acceptance_criteria>
    - Source assertion: both files gained one lucide-react import; the 7 cited buttons render vocabulary-mapped icons with text preserved.
  </acceptance_criteria>
  <verify>
    <automated>grep -nE "from \"lucide-react\"" "web/src/app/(dashboard)/agenda/[id]/editar/page.tsx" "web/src/app/(dashboard)/agenda/novo/page.tsx" (expect 1 each) && grep -nE "<Plus|<Save" "web/src/app/(dashboard)/agenda/novo/page.tsx" "web/src/app/(dashboard)/agenda/[id]/editar/page.tsx"</automated>
  </verify>
  <done>Create + edit buttons iconed with text preserved; imports resolve; lint clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Adds icons/one Tooltip wrapper to existing client-side buttons. No new input, data flow, network call, or auth/authz change. FICO-01 conversion preserves the clear-filters handler; delete confirmation untouched. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-115-01 | Tampering | icon/Tooltip markup in agenda .tsx | accept | Static compile-time glyph imports; no untrusted input crosses a boundary; delete confirmation unchanged. |
| T-115-A11Y | WCAG 4.1.2 (a11y) | icon-only Limpar Filtros (FICO-01) + month-nav Chevrons | mitigate | Icon-only Limpar carries `aria-label` + `Tooltip` identical copy (FICO-01 AC4). The optional month-nav Chevron `aria-label`s restore accessible names for two previously-unlabeled icon-only buttons. |
| T-115-SC | Tampering | npm/pnpm install | accept | No package installed — additional named exports from already-installed `lucide-react`. No legitimacy gate applies. |
</threat_model>

<verification>
- `cd web && pnpm lint` clean on the 4 files.
- Grep proves the single `aria-label="Limpar filtros"`, ArrowRight on Ver Lista Completa, and vocabulary icons across detail/create/edit; negative grep confirms no invented Aplicar/Exportar.
- FICO-01 interactive verification deferred to Plan 11.
</verification>

<success_criteria>
- Agenda "Limpar Filtros" icon-only with Tooltip+aria-label; no filter button invented — FICO-01 for this module complete.
- All Agenda list/detail/create/edit gap buttons carry icons with text preserved (ICON-01).
- Lint clean; L233 Plus untouched.
</success_criteria>

<output>
Create `.planning/phases/LEXCV-115-linguagem-visual-icones-em-todos-os-botoes-filtros-icone-only/115-06-SUMMARY.md` when done.
</output>
