---
phase: 115-linguagem-visual-icones-em-todos-os-botoes-filtros-icone-only
plan: 11
type: execute
wave: 2
depends_on: [115-01, 115-02, 115-03, 115-04, 115-05, 115-06, 115-07, 115-08, 115-09, 115-10]
files_modified: []
autonomous: false
requirements: [ICON-01, FICO-01]
must_haves:
  truths:
    - "The whole web app builds and lints cleanly with all 32 icon-touched files integrated (no undefined/unused icon import anywhere)"
    - "The fully-compliant do-not-touch files are byte-identical (zero git changes, except the two documentos columns files whose Apagar half is legitimately changed)"
    - "FICO-01: the 10 converted filter buttons across the 5 named modules are icon-only, show a Tooltip on hover, announce their aria-label under keyboard focus, and are legible in both light and dark themes"
    - "ICON-01 high-leverage fixes (pagination, access-denied) are visible across multiple pages; Pareceres/Notificações filter buttons kept their text"
  artifacts: []
  key_links:
    - from: "phase build gate"
      to: "all 32 modified files"
      via: "pnpm build + pnpm lint pass"
      pattern: "n/a (command gate)"
---

<objective>
Phase-level verification for 115. First run the definitive cross-cutting automated gate that no single parallel plan could run alone — a full `pnpm build` + `pnpm lint` over the whole web app with all 32 icon-touched files integrated, plus a git-diff guard proving the do-not-touch compliant files are untouched. Then a human-verify checkpoint for the FICO-01 interactive behaviors (tooltip-on-hover, keyboard aria-label announcement, both themes) and the high-leverage ICON-01 spot-checks — the acceptance criteria that grep/build cannot prove, operationalizing the UI-SPEC "Verification / QA Plan".

Purpose: Catch any cross-file breakage (undefined/unused icon, a `Merge`/`UserCog` name absent in the installed lucide-react version) and confirm FICO-01 AC2/AC3/AC4 live.
Output: build/lint green + git-diff clean; human sign-off on FICO-01 interactivity + a11y.
</objective>

<execution_context>
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/LEXCV-115-linguagem-visual-icones-em-todos-os-botoes-filtros-icone-only/115-UI-SPEC.md

Depends on all of 115-01 … 115-10 (Wave 1). Do not start until every implementation plan has produced its SUMMARY.

UI-SPEC "Verification / QA Plan" is the source of the human checklist below. Fully-compliant do-not-touch files (UI-SPEC QA "What does NOT need checking"): `clientes/columns.tsx`, `processos/columns.tsx`, `pareceres/columns.tsx`, `notification-bell.tsx`, `date-picker-field.tsx`, `combobox.tsx`, `theme-toggle.tsx`, `ui/pagination.tsx`, `data-table-view-options.tsx`, `data-table-column-header.tsx`.

Note: `documentos/columns.tsx` and `processos/[id]/documentos-columns.tsx` ARE modified by plans 07/04 (their Apagar half), so they will show in the diff — that is expected; only their pre-existing Download row-action must be unchanged.
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Cross-cutting build + lint gate + do-not-touch git guard</name>
  <files></files>
  <read_first>
    - 115-UI-SPEC.md "Verification / QA Plan" (the "spot-check by leverage" + "What does NOT need checking" lists)
    - The 10 SUMMARY files 115-01-SUMMARY.md … 115-10-SUMMARY.md (confirm all waves completed)
  </read_first>
  <action>
    Run the full web build and lint to prove all 32 icon-touched files integrate with no undefined or unused imports: `cd web && pnpm lint` then `cd web && pnpm build`. Both must pass. If either fails, capture the exact file:line and route it back to the owning plan (do not patch here beyond a trivial import fix that is unambiguously that plan's intent — e.g. a `Merge`→`GitMerge` fallback if the installed lucide-react lacks `Merge`).
    Then run the do-not-touch guard: list the phase's changed files (`git diff --name-only` against the phase base commit) and confirm none of the fully-compliant files listed in context appear — EXCEPT `documentos/columns.tsx` and `processos/[id]/documentos-columns.tsx`, which are legitimately changed (their Apagar half) by plans 07/04; for those two, confirm via `git diff` that only the Apagar row-action changed and the pre-existing Download row-action is untouched.
    Spot-check by leverage (source-level, cheap): confirm `data-table-pagination.tsx` chevrons and `access-denied-state.tsx` ArrowLeft are present; confirm all 3 Financeiro files gained a lucide-react import with no unused names; confirm pareceres/notificacoes filter buttons still contain their text and NO `TooltipTrigger`.
  </action>
  <acceptance_criteria>
    - CLI: `cd web && pnpm lint` exits 0 (no errors).
    - CLI: `cd web && pnpm build` exits 0 (production build succeeds → every icon import resolves and typechecks).
    - Source assertion: `git diff --name-only` contains none of the do-not-touch compliant files except the two documentos columns files, whose Download half is unchanged.
    - Source assertion: grep confirms zero `TooltipTrigger` in pareceres/page.tsx + notificacoes/page.tsx (FICO-01 exclusion held).
  </acceptance_criteria>
  <verify>
    <automated>cd web && pnpm lint && pnpm build (both exit 0) ; grep -cE "TooltipTrigger" "web/src/app/(dashboard)/pareceres/page.tsx" "web/src/app/(dashboard)/notificacoes/page.tsx" (expect 0 each)</automated>
  </verify>
  <done>Whole-app build + lint green with all 32 files integrated; do-not-touch guard passes; FICO-01 exclusion confirmed in source.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification — FICO-01 interactivity + a11y + ICON-01 leverage spot-check</name>
  <files></files>
  <read_first>
    - 115-UI-SPEC.md "Verification / QA Plan" (FICO-01 per-module checklist + ICON-01 spot-check-by-leverage)
  </read_first>
  <what-built>
    ICON-01 icons added to 158 previously text-only buttons across 32 files, and FICO-01 icon-only conversion of the 12 filter-action buttons (Aplicar/Limpar/Exportar) across Clientes, Processos, Agenda, Documentos, Financeiro — each with a `Tooltip` and matching `aria-label`. Build + lint already confirmed green (Task 1).
  </what-built>
  <action>
    This is a human-verify checkpoint — Task 1 already ran every automation possible (build, lint, git-diff guard, source greps). Start the app (`cd web && pnpm dev`, http://localhost:3000), then present the how-to-verify checklist to the operator and PAUSE for their verdict. Do not self-approve and do not proceed to phase close until the operator responds with the resume-signal. If the operator reports a wrong glyph / missing tooltip / unannounced aria-label / illegible theme / wrong text-presence, record it as a gap for the owning module plan (candidate for `/gsd:plan-phase 115 --gaps`).
  </action>
  <how-to-verify>
    FICO-01 (the interactive acceptance criteria grep/build cannot prove):
    1. Clientes list filter bar: Aplicar, Limpar, Exportar CSV each show ONLY an icon (no text). Hover each → tooltip "Aplicar filtros" / "Limpar filtros" / "Exportar CSV". Click Aplicar filters the list; Limpar clears; Exportar downloads a CSV.
    2. Processos list: Aplicar/Limpar icon-only with tooltips; the Exportar icon button is still disabled (expected — placeholder). Processo detail (/processos/[id]) Timeline "Limpar filtros" is icon-only with a tooltip.
    3. Agenda: "Limpar Filtros" is icon-only with a tooltip; confirm NO Aplicar/Exportar button appeared where none existed.
    4. Documentos: "Filtrar" and "Limpar" icon-only with tooltips (Filtrar's tooltip reads "Aplicar filtros"); confirm NO Exportar appeared.
    5. Financeiro: apply a filter so "Limpar filtros" renders, confirm it + "Exportar CSV" are icon-only with tooltips; confirm NO Aplicar appeared.
    6. Keyboard-only pass (no mouse): Tab to each of the 10 converted filter buttons and confirm a screen reader / the accessibility tree announces the aria-label (FICO-01 AC4) — do not rely on the visual tooltip.
    7. Both themes (toggle light/dark): icons legible in each converted button's variant.
    ICON-01 leverage spot-check:
    8. Pagination "Anterior/Seguinte" show chevrons on at least 2 different list pages (Clientes, Financeiro).
    9. An RBAC access-denied screen shows the ArrowLeft on its Voltar button.
    10. Open a Cliente and a Processo ficha: the repeated Adicionar/Cancelar/Confirmar dialog groups render icons consistently.
    11. Pareceres + Notificações filter buttons kept their TEXT and show an icon beside it (NOT icon-only, no tooltip) — confirms the FICO-01 exclusion.
  </how-to-verify>
  <acceptance_criteria>
    - Behavior: all 10 FICO-01 buttons are icon-only, reveal a Tooltip on hover, and announce their aria-label under keyboard focus (steps 1-7).
    - Behavior: no Aplicar/Exportar button was invented in Agenda/Documentos/Financeiro where none existed (steps 3-5).
    - Behavior: pagination + access-denied icons visible across pages; Pareceres/Notificações kept text (steps 8-11).
    - Human sign-off recorded (the operator typed the resume-signal).
  </acceptance_criteria>
  <verify>
    <human-check>Operator confirms all 11 checklist items pass (Task 1's build/lint/git-diff gate is the automated precondition to this human pass).</human-check>
  </verify>
  <resume-signal>Type "approved" if all pass, or describe any button that is wrong (missing icon, wrong glyph, missing tooltip, unannounced aria-label, illegible in a theme, or a text label that should/shouldn't be there).</resume-signal>
  <done>Operator approved the 11-point checklist; verdict (and any reported gaps) recorded in 115-11-SUMMARY.md.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Verification-only plan — runs build/lint and a human visual pass. No code produced. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-115-A11Y | WCAG 4.1.2 (a11y) | the 10 icon-only FICO-01 buttons | mitigate | This checkpoint is the live gate for the a11y concern named in the phase security note: step 6 (keyboard-only aria-label announcement) directly verifies that icon-only conversion did NOT regress the accessible name for screen-reader/keyboard users. |
| T-115-SC | Tampering | npm/pnpm install | accept | No package installed anywhere in this phase — `pnpm build` additionally proves every icon resolves from the already-installed `lucide-react` (a missing name would fail the build, surfacing any `Merge`/`UserCog` version gap). |
</threat_model>

<verification>
- Task 1 automated gate (`pnpm lint` + `pnpm build` exit 0, git-diff guard, FICO-01-exclusion grep) must pass BEFORE the human checkpoint.
- Human sign-off ("approved") on the 11-point checklist closes the phase's interactive/visual acceptance criteria (FICO-01 AC2/AC3/AC4 + ICON-01 leverage).
</verification>

<success_criteria>
- Whole-app `pnpm build` + `pnpm lint` green; do-not-touch guard clean.
- FICO-01 tooltips appear on hover, aria-labels announce under keyboard focus, icons legible in both themes.
- ICON-01 high-leverage fixes visible across pages; Pareceres/Notificações kept their text.
- Human approval recorded.
</success_criteria>

<output>
Create `.planning/phases/LEXCV-115-linguagem-visual-icones-em-todos-os-botoes-filtros-icone-only/115-11-SUMMARY.md` when done (record build/lint result + the human verdict).
</output>
