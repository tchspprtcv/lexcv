---
phase: 115-linguagem-visual-icones-em-todos-os-botoes-filtros-icone-only
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/app/(dashboard)/documentos/page.tsx
  - web/src/app/(dashboard)/documentos/columns.tsx
  - web/src/app/(dashboard)/documentos/novo/page.tsx
  - web/src/app/(dashboard)/documentos/[id]/page.tsx
autonomous: true
requirements: [ICON-01, FICO-01]
must_haves:
  truths:
    - "Documentos list filter bar: 'Filtrar' (Aplicar semantic) and 'Limpar' are icon-only (Check/X) with Tooltip+aria-label; no Exportar button was invented"
    - "Documentos Upload trigger and Apagar (mobile card + column + detail) carry icons"
    - "Documento create + detail buttons (Voltar/Enviar/Cancelar/Download/Apagar) carry icons"
  artifacts:
    - path: "web/src/app/(dashboard)/documentos/page.tsx"
      provides: "FICO-01 icon-only Filtrar(Check)/Limpar(X) + ICON-01 Upload/Apagar-mobile"
      contains: "aria-label=\"Aplicar filtros\""
    - path: "web/src/app/(dashboard)/documentos/columns.tsx"
      provides: "Apagar row-action icon (matches sibling Download icon-only pattern)"
      contains: "Trash2"
    - path: "web/src/app/(dashboard)/documentos/novo/page.tsx"
      provides: "Voltar/Enviar/Cancelar icons"
      contains: "lucide-react"
    - path: "web/src/app/(dashboard)/documentos/[id]/page.tsx"
      provides: "Voltar/Download/Apagar icons"
      contains: "lucide-react"
  key_links:
    - from: "documentos/page.tsx Filtrar/Limpar"
      to: "Tooltip primitive + aria-label"
      via: "icon-only conversion (Filtrar uses the Aplicar-filtros copy)"
      pattern: "aria-label=\"(Aplicar filtros|Limpar filtros)\""
---

<objective>
Deliver the Documentos-module icon work: FICO-01 conversion of the list filter buttons — "Filtrar" (Aplicar semantic → `Check`, uses the unified "Aplicar filtros" copy) and "Limpar" (→ `X`), both icon-only + Tooltip + aria-label — plus ICON-01 icons on the Upload trigger, the Apagar actions (mobile card, column, detail), and the create/detail pages.

Purpose: Own the four Documentos files in one parallel plan. Do NOT invent an Exportar button (Documentos has none — UI-SPEC "Documentos" row).
Output: 4 modified files; 11 ICON-01 gaps (incl. 2 FICO-01 conversions) closed.
</objective>

<execution_context>
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/LEXCV-115-linguagem-visual-icones-em-todos-os-botoes-filtros-icone-only/115-UI-SPEC.md
@.planning/phases/LEXCV-115-linguagem-visual-icones-em-todos-os-botoes-filtros-icone-only/115-CONTEXT.md

Exact citations: `115-UI-SPEC.md` → "FICO-01 — Filter Action Button Inventory" (Documentos row) + "Documentos module" audit table. EVIDENCE, verified 2026-07-21.

FICO-01 note (UI-SPEC): Documentos' "Filtrar" (L219-221) is the Aplicar SEMANTIC — same action, same `Check` icon, and it takes the unified `Aplicar filtros` tooltip/aria-label copy (not "Filtrar"). "Limpar" (L222-224) → `X` + `Limpar filtros` copy. Both icon-only + Tooltip; keep each button's variant (`secondary`/`outline`), drop `w-full`. Import Tooltip primitives; TooltipProvider is app-global. Documentos has NO Exportar button — do not invent one.

Vocabulary: Upload trigger (L151)→`Upload`; Apagar→`Trash2`; Voltar→`ArrowLeft`; Enviar (file submission)→`Upload`; "Download" (text-only detail instance, L91-97)→`Download`.

Row-action pattern (columns.tsx L95 Apagar): match the compliant sibling Download button — icon-only + `Tooltip` + `aria-label="Apagar"`, using `Trash2`; add `Trash2` to the existing import that already has `Download`. Preserve the existing delete handler/confirmation (some sites use `window.confirm()` — leave it; additive-visual only, UI-SPEC Scope Boundaries §5).
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Documentos list (FICO-01 + Upload + Apagar-mobile) + column Apagar</name>
  <files>web/src/app/(dashboard)/documentos/page.tsx, web/src/app/(dashboard)/documentos/columns.tsx</files>
  <read_first>
    - web/src/app/(dashboard)/documentos/page.tsx (imports + L145-160, L215-230, L335-350 for the mobile DocumentoMobileCard Apagar)
    - web/src/app/(dashboard)/documentos/columns.tsx (full — see compliant Download sibling)
    - 115-UI-SPEC.md "FICO-01" Documentos row + "Documentos module" table
  </read_first>
  <behavior>
    - "Filtrar" (L219-221, `type="submit"` `variant="secondary"` `w-full`) → icon-only `Check` + `size="icon"` + `aria-label="Aplicar filtros"` + Tooltip("Aplicar filtros"); still submits.
    - "Limpar" (L222-224, `type="button"` `variant="outline"` `w-full` `onClick={onClear}`) → icon-only `X` + aria-label "Limpar filtros" + Tooltip; onClear still fires.
    - Upload trigger (L151) keeps text, gains `Upload`.
    - Apagar in the mobile card (L343) keeps its behavior, gains `Trash2` (its existing `window.confirm()` stays).
    - columns.tsx Apagar (L95) → icon-only `Trash2` + Tooltip + aria-label matching the sibling Download.
  </behavior>
  <action>
    In `documentos/page.tsx`: add/extend the lucide-react import with `Check, X, Upload, Trash2`; add `Tooltip, TooltipTrigger, TooltipContent` from `@/components/ui/tooltip`. Convert Filtrar (L219) and Limpar (L222) to icon-only per the UI-SPEC pattern (drop text + `w-full`, add `size="icon"` + locked `aria-label` — Filtrar uses "Aplicar filtros" copy — wrap in Tooltip, keep variant/type/onClick). Add `Upload` to the L151 upload trigger (keep text) and `Trash2` to the L343 mobile-card Apagar (keep its confirm).
    In `documentos/columns.tsx`: add `Trash2` to the existing lucide-react import; convert the L95 Apagar row-action to match the sibling Download — icon-only + Tooltip("Apagar") + `aria-label="Apagar"`; preserve the delete handler.
  </action>
  <acceptance_criteria>
    - Source assertion: `documentos/page.tsx` has `aria-label="Aplicar filtros"` and `aria-label="Limpar filtros"`, each on a `size="icon"` Button wrapped by `TooltipTrigger asChild`, with no visible "Filtrar"/"Limpar" text child.
    - Source assertion: Upload trigger renders `<Upload` with text; mobile-card Apagar renders `<Trash2` and keeps its `confirm`.
    - Source assertion: `columns.tsx` imports `Trash2` and the Apagar action mirrors the sibling Download icon-only+Tooltip pattern.
    - Negative assertion: no "Exportar" button exists in documentos/page.tsx.
  </acceptance_criteria>
  <verify>
    <automated>grep -nE "aria-label=\"(Aplicar filtros|Limpar filtros)\"" "web/src/app/(dashboard)/documentos/page.tsx" (expect 2) && grep -nE "<Upload|<Trash2" "web/src/app/(dashboard)/documentos/page.tsx" && grep -nE "Trash2" "web/src/app/(dashboard)/documentos/columns.tsx" && grep -ciE "Exportar" "web/src/app/(dashboard)/documentos/page.tsx" (expect 0)</automated>
  </verify>
  <done>Documentos filter bar Filtrar/Limpar icon-only with Tooltip+aria-label; Upload/Apagar iconed; column Apagar matches sibling; no Exportar invented; confirmations untouched; lint clean.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Documento create + detail pages</name>
  <files>web/src/app/(dashboard)/documentos/novo/page.tsx, web/src/app/(dashboard)/documentos/[id]/page.tsx</files>
  <read_first>
    - web/src/app/(dashboard)/documentos/novo/page.tsx (imports + L141, L252, L258)
    - web/src/app/(dashboard)/documentos/[id]/page.tsx (imports + L88, L91-97, L100)
    - 115-UI-SPEC.md "Documentos module" rows for novo/page.tsx and [id]/page.tsx
  </read_first>
  <action>
    `documentos/novo/page.tsx` (add lucide-react import): L141 Voltar→`ArrowLeft`; L252 Enviar→`Upload`; L258 Cancelar→`X`.
    `documentos/[id]/page.tsx` (add lucide-react import): L88 Voltar→`ArrowLeft`; L91-97 "Download" (currently text, not the icon-only pattern used in columns) → add `<Download className="h-4 w-4" />` before the text (keep text — this is an ICON-01 icon+text add, not a row-action conversion); L100 Apagar→`Trash2` (keep its existing confirmation).
    Icons before text; keep text/handlers.
  </action>
  <acceptance_criteria>
    - Source assertion: both files gained a lucide-react import; the cited buttons render vocabulary-mapped icons with text preserved.
    - Behavior: detail-page delete confirmation unchanged.
  </acceptance_criteria>
  <verify>
    <automated>grep -nE "from \"lucide-react\"" "web/src/app/(dashboard)/documentos/novo/page.tsx" "web/src/app/(dashboard)/documentos/[id]/page.tsx" (expect 1 each) && grep -nE "<ArrowLeft|<Upload|<Download|<Trash2" "web/src/app/(dashboard)/documentos/novo/page.tsx" "web/src/app/(dashboard)/documentos/[id]/page.tsx"</automated>
  </verify>
  <done>Create + detail buttons iconed with text preserved; imports resolve; lint clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Adds icons/Tooltip wrappers to existing client-side buttons. No new input, data flow, network call, or auth/authz change. FICO-01 conversion preserves submit + onClear handlers; delete confirmations (incl. `window.confirm`) untouched. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-115-01 | Tampering | icon/Tooltip markup in documentos .tsx | accept | Static compile-time glyph imports; no untrusted input crosses a boundary; confirmations unchanged. |
| T-115-A11Y | WCAG 4.1.2 (a11y) | icon-only Filtrar/Limpar (FICO-01) | mitigate | Icon-only removes the visible label; mitigated by `aria-label` + `Tooltip` identical copy on both (FICO-01 AC4). Filtrar's accessible name is unified to "Aplicar filtros" so keyboard users get a consistent action name across modules. |
| T-115-SC | Tampering | npm/pnpm install | accept | No package installed — additional named exports from already-installed `lucide-react`. No legitimacy gate applies. |
</threat_model>

<verification>
- `cd web && pnpm lint` clean on the 4 files.
- Grep proves the 2 locked aria-labels + Tooltip wrapping, vocabulary icons on Upload/Apagar/create/detail, and no invented Exportar.
- FICO-01 interactive verification deferred to Plan 11.
</verification>

<success_criteria>
- Documentos filter-action buttons icon-only with Tooltip+aria-label; no Exportar invented — FICO-01 for this module complete.
- All Documentos gap buttons carry icons with text preserved (ICON-01); row-action Apagar matches sibling pattern.
- Confirmations untouched; lint clean.
</success_criteria>

<output>
Create `.planning/phases/LEXCV-115-linguagem-visual-icones-em-todos-os-botoes-filtros-icone-only/115-07-SUMMARY.md` when done.
</output>
