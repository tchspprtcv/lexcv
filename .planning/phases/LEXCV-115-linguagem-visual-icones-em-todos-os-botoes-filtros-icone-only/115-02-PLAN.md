---
phase: 115-linguagem-visual-icones-em-todos-os-botoes-filtros-icone-only
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/app/(dashboard)/clientes/page.tsx
  - web/src/app/(dashboard)/clientes/novo/page.tsx
  - web/src/app/(dashboard)/clientes/merge/page.tsx
autonomous: true
requirements: [ICON-01, FICO-01]
must_haves:
  truths:
    - "Clientes list filter bar: Aplicar, Limpar and Exportar CSV show ONLY an icon (Check/X/Download), each wrapped in a Tooltip and carrying an aria-label"
    - "Clientes list Merge and Importar CSV buttons keep their text and gain an icon"
    - "Cliente create + merge pages: every Voltar/submit/Cancelar/Continuar/Fazer-merge button carries an icon"
  artifacts:
    - path: "web/src/app/(dashboard)/clientes/page.tsx"
      provides: "FICO-01 icon-only Aplicar(Check)/Limpar(X)/Exportar(Download) + ICON-01 Merge/Importar"
      contains: "aria-label=\"Aplicar filtros\""
    - path: "web/src/app/(dashboard)/clientes/novo/page.tsx"
      provides: "Voltar/submit/Cancelar/Continuar icons"
      contains: "lucide-react"
    - path: "web/src/app/(dashboard)/clientes/merge/page.tsx"
      provides: "Voltar + Fazer merge icons"
      contains: "lucide-react"
  key_links:
    - from: "clientes/page.tsx Aplicar/Limpar/Exportar"
      to: "Tooltip primitive"
      via: "TooltipTrigger asChild wrapping the icon-only Button"
      pattern: "TooltipTrigger asChild"
    - from: "clientes/page.tsx icon-only filter buttons"
      to: "screen-reader/keyboard users"
      via: "aria-label with identical copy to TooltipContent"
      pattern: "aria-label=\"(Aplicar filtros|Limpar filtros|Exportar CSV)\""
---

<objective>
Deliver the Clientes-module icon work: FICO-01 icon-only conversion of the three filter-action buttons in the list page (Aplicar→`Check`, Limpar→`X`, Exportar CSV→`Download`, each with `Tooltip` + `aria-label`), plus ICON-01 icons on the remaining Clientes list/create/merge buttons (keeping their text).

Purpose: Close Clientes' filter-action FICO-01 conversion and its ICON-01 gaps in one module-owned plan (exclusive file ownership → runs parallel with all other module plans).
Output: 3 modified files; 12 gap buttons (3 FICO-01 conversions + 9 ICON-01 icon-adds).
</objective>

<execution_context>
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/LEXCV-115-linguagem-visual-icones-em-todos-os-botoes-filtros-icone-only/115-UI-SPEC.md
@.planning/phases/LEXCV-115-linguagem-visual-icones-em-todos-os-botoes-filtros-icone-only/115-CONTEXT.md

The icon vocabulary and exact per-file/line inventory live in `115-UI-SPEC.md`. It is EVIDENCE (grep/read-verified 2026-07-21), not proposal. Do not re-audit.

FICO-01 conversion pattern (UI-SPEC section "Icon-only + Tooltip code pattern"): wrap the button in `<Tooltip><TooltipTrigger asChild>…Button…</TooltipTrigger><TooltipContent>copy</TooltipContent></Tooltip>`; add `size="icon"` + `aria-label` (identical copy to TooltipContent); remove the visible text; KEEP the button's existing `variant` (Aplicar default, Limpar ghost, Exportar outline) and its `type`/`onClick`/`disabled`. Drop width-stretching classes like `w-full` — `size="icon"` is a fixed 36×36 footprint. Do NOT add a native `title` attribute (redundant with Tooltip). Import `Tooltip, TooltipTrigger, TooltipContent` from `@/components/ui/tooltip`. TooltipProvider is already app-global (providers.tsx:30) — no wiring needed.

Locked FICO-01 copy (UI-SPEC "Copy unification"): tooltip AND aria-label are identical per action — Aplicar→`Aplicar filtros`, Limpar→`Limpar filtros`, Exportar→`Exportar CSV`.

asChild rule: `<Button asChild><Link>` (Merge L269-271; Voltar buttons) → icon goes INSIDE the `<Link>`. Verified compliant example in this same file: clientes/page.tsx:285-289.

Do-not-touch in clientes/page.tsx: L285 Plus (Adicionar Novo Cliente), L353-361 Filter (Avançados) — already compliant, leave byte-identical.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Clientes list — FICO-01 conversions + Merge/Importar icons</name>
  <files>web/src/app/(dashboard)/clientes/page.tsx</files>
  <read_first>
    - web/src/app/(dashboard)/clientes/page.tsx (read L1-40 for imports, then L255-400 for the toolbar + filter form)
    - 115-UI-SPEC.md sections "FICO-01 — Filter Action Button Inventory" (Clientes row) and "Icon-only + Tooltip code pattern"
  </read_first>
  <behavior>
    - Aplicar filters button (L364, `type="submit"`, default variant) → icon-only `Check` + `size="icon"` + `aria-label="Aplicar filtros"` wrapped in Tooltip with TooltipContent "Aplicar filtros"; still submits the surrounding `<form onSubmit>`.
    - Limpar button (L365-367, `type="button"` `variant="ghost"` `onClick={onClear}`) → icon-only `X` + aria-label "Limpar filtros" + Tooltip; onClear still fires.
    - Exportar CSV button (L265-267, `type="button"` `variant="outline"` `onClick={onExportCsv}`) → icon-only `Download` + aria-label "Exportar CSV" + Tooltip; onExportCsv still fires.
    - Merge button (L269-271, asChild+Link) keeps text, gains `Merge` icon inside the Link.
    - Importar CSV button (L273-282) keeps text (including the "A importar..." pending label), gains `Upload` icon.
  </behavior>
  <action>
    Extend the lucide-react import in this file with `Check, X, Download, Merge, Upload` (fallback: `GitMerge` if `Merge` is unavailable in the installed lucide-react version — verify against node_modules if in doubt). Add `Tooltip, TooltipTrigger, TooltipContent` import from `@/components/ui/tooltip`.
    Convert the three FICO-01 buttons (Aplicar L364, Limpar L365-367, Exportar CSV L265-267) to icon-only using the UI-SPEC "Icon-only + Tooltip code pattern": drop the visible text and the `w-full`/text-color width classes, add `size="icon"` + `aria-label` (locked copy), wrap in Tooltip/TooltipTrigger asChild/TooltipContent. Preserve each button's existing `variant`, `type`, and `onClick`/submit behavior. The Aplicar/Limpar pair currently sits in a `lg:col-span-2 flex gap-2` cell — after conversion the two 36px icon buttons sit side-by-side in that `flex gap-2` (remove `w-full` from each).
    ICON-01 (keep text): Merge (L269-271) → `<Merge className="h-4 w-4" />` inside its `<Link>`; Importar CSV (L273-282) → `<Upload className="h-4 w-4" />` before the conditional label.
    Do not touch L285 Plus or L353-361 Filter (already compliant).
  </action>
  <acceptance_criteria>
    - Source assertion: the three FICO-01 buttons render `size="icon"`, an `aria-label` from the locked-copy set, and are each wrapped by `TooltipTrigger asChild` with a `TooltipContent` whose text equals the aria-label.
    - Source assertion: no visible Portuguese text node ("Aplicar"/"Limpar"/"Exportar CSV") remains as a child of the three converted buttons.
    - Source assertion: Merge renders `<Merge` (or `<GitMerge`) inside its Link; Importar CSV renders `<Upload` and still shows the `A importar...`/`Importar CSV` conditional text.
    - Behavior preserved: Aplicar is still `type="submit"`; Limpar still calls `onClear`; Exportar still calls `onExportCsv`; each keeps its prior `variant`.
    - Untouched: L285 Plus and L353-361 Filter blocks are byte-identical to before.
  </acceptance_criteria>
  <verify>
    <automated>grep -nE "aria-label=\"(Aplicar filtros|Limpar filtros|Exportar CSV)\"" "web/src/app/(dashboard)/clientes/page.tsx" (expect 3) && grep -nE "TooltipTrigger asChild" "web/src/app/(dashboard)/clientes/page.tsx" (expect >=3) && grep -nE "<Merge|<GitMerge|<Upload" "web/src/app/(dashboard)/clientes/page.tsx" (expect Merge+Upload)</automated>
  </verify>
  <done>Clientes filter bar Aplicar/Limpar/Exportar are icon-only with Tooltip+aria-label; Merge/Importar keep text with icons; form submit + onClear + onExportCsv behavior unchanged; lint clean.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Cliente create + merge pages — ICON-01</name>
  <files>web/src/app/(dashboard)/clientes/novo/page.tsx, web/src/app/(dashboard)/clientes/merge/page.tsx</files>
  <read_first>
    - web/src/app/(dashboard)/clientes/novo/page.tsx (imports + the button sites at L153, L344, L351, L368, L371)
    - web/src/app/(dashboard)/clientes/merge/page.tsx (imports + L96, L149)
    - 115-UI-SPEC.md "Clientes module" audit rows for novo/page.tsx and merge/page.tsx, plus the vocabulary table
  </read_first>
  <action>
    `clientes/novo/page.tsx` (add a `lucide-react` import — file has none today): L153 Voltar → `ArrowLeft` (if asChild+Link, icon inside the Link); L344 submit (create cliente) → `Plus`; L351 Cancelar → `X`; L368 Cancelar (dialog) → `X`; L371 Continuar (dialog) → `ArrowRight`. All keep visible text; icon before text.
    `clientes/merge/page.tsx` (add a `lucide-react` import): L96 Voltar → `ArrowLeft` (icon inside Link if asChild); L149 "Fazer merge" → `Merge` (fallback `GitMerge`). Keep text.
    Use the exact icons from the UI-SPEC vocabulary (Voltar→ArrowLeft, Criar/submit→Plus, Cancelar→X, Continuar/Seguinte→ArrowRight, Fazer merge→Merge). Where a submit button already threads `isSubmitting`/`isPending` into `disabled`, optionally swap to `<Loader2 className="h-4 w-4 animate-spin" />` while pending (only if the boolean is already present).
  </action>
  <acceptance_criteria>
    - Source assertion: each of the 7 cited buttons renders an icon element from lucide-react matching the vocabulary mapping above.
    - Source assertion: both files gained exactly one `from "lucide-react"` import line, with no unused names.
    - Behavior: all 7 buttons keep visible text; asChild+Link buttons place the icon inside the Link (not as a Button child).
  </acceptance_criteria>
  <verify>
    <automated>grep -nE "from \"lucide-react\"" "web/src/app/(dashboard)/clientes/novo/page.tsx" "web/src/app/(dashboard)/clientes/merge/page.tsx" (expect 1 each) && grep -nE "<ArrowLeft|<Plus|<X |<ArrowRight" "web/src/app/(dashboard)/clientes/novo/page.tsx" && grep -nE "<Merge|<GitMerge" "web/src/app/(dashboard)/clientes/merge/page.tsx"</automated>
  </verify>
  <done>Cliente create + merge buttons all carry icons with text preserved; new imports resolve; lint clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Adds icon elements + Tooltip wrappers to existing client-side buttons. No new input, data flow, network call, or auth/authz change. The FICO-01 conversion removes visible text but preserves every handler (`onClear`, `onExportCsv`, form submit). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-115-01 | Tampering | icon/Tooltip markup in clientes .tsx | accept | Static compile-time glyph imports; no untrusted input crosses a boundary. |
| T-115-A11Y | WCAG 4.1.2 (a11y, not a classic vuln) | icon-only Aplicar/Limpar/Exportar (FICO-01) | mitigate | Removing the visible label would strip the accessible name for keyboard/screen-reader users. Mitigated by the locked pattern: every converted button carries `aria-label` + a `Tooltip` with identical copy (UI-SPEC "Icon-only + Tooltip code pattern"), satisfying FICO-01 AC4. |
| T-115-SC | Tampering | npm/pnpm install | accept | No package installed — additional named exports from already-installed `lucide-react`. No legitimacy gate applies. |
</threat_model>

<verification>
- `cd web && pnpm lint` clean on the 3 touched files.
- Grep proves: 3 `aria-label` from the locked-copy set + `TooltipTrigger asChild` present in clientes/page.tsx; new lucide imports resolve in novo/merge pages.
- Manual/keyboard verification of the FICO-01 tooltip-on-hover + aria-label announcement is deferred to the phase human-verify checkpoint (Plan 11).
</verification>

<success_criteria>
- Clientes filter-action buttons are icon-only with Tooltip + aria-label (FICO-01 for this module complete).
- All remaining Clientes list/create/merge gap buttons carry icons with text preserved (ICON-01).
- No do-not-touch compliant button altered; lint clean.
</success_criteria>

<output>
Create `.planning/phases/LEXCV-115-linguagem-visual-icones-em-todos-os-botoes-filtros-icone-only/115-02-SUMMARY.md` when done.
</output>
