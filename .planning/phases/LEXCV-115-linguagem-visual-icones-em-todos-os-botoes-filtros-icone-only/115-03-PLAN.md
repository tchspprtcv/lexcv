---
phase: 115-linguagem-visual-icones-em-todos-os-botoes-filtros-icone-only
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/app/(dashboard)/clientes/[id]/page.tsx
autonomous: true
requirements: [ICON-01]
must_haves:
  truths:
    - "The Cliente ficha top toolbar (Voltar/Cancelar/Guardar/Editar) shows icons"
    - "Every repeated Adicionar-trigger / Cancelar / Confirmar-Guardar group across Doc-a-Tratar, Deslocação, tipo-change, Documento-upload, inline Contactos edit, and inline Notas edit carries an icon consistent with its action"
    - "All 31 gap buttons keep their visible text (this file has no filter buttons — no FICO-01, no icon-only conversion)"
  artifacts:
    - path: "web/src/app/(dashboard)/clientes/[id]/page.tsx"
      provides: "31 ICON-01 gap buttons closed across the top toolbar + 6 repeated dialog/inline-edit groups"
      contains: "lucide-react"
  key_links:
    - from: "clientes/[id]/page.tsx buttons"
      to: "lucide-react vocabulary"
      via: "Plus/X/Check/Save/Pencil/ArrowLeft usages"
      pattern: "<(Plus|Save|Pencil|ArrowLeft)"
---

<objective>
Close all 31 ICON-01 gaps in the single heaviest file of the phase — the Cliente ficha (`clientes/[id]/page.tsx`, ~2100 lines). Mechanical icon-add against the UI-SPEC vocabulary. This file has ZERO filter buttons, so there is no FICO-01 work and no icon-only conversion — every button keeps its visible text and gains an icon before the text.

Purpose: Isolate the largest single-file edit in its own plan so its read/edit load does not degrade any other module plan. Split into two line-range tasks to keep each pass within a healthy context budget.
Output: 1 modified file; 31 gap buttons closed.
</objective>

<execution_context>
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/LEXCV-115-linguagem-visual-icones-em-todos-os-botoes-filtros-icone-only/115-UI-SPEC.md
@.planning/phases/LEXCV-115-linguagem-visual-icones-em-todos-os-botoes-filtros-icone-only/115-CONTEXT.md

Exact line citations for all 31 gaps are in `115-UI-SPEC.md` → "Clientes module" table, `clientes/[id]/page.tsx` row. It is EVIDENCE (verified 2026-07-21). Some of the repeated inline-edit buttons were pattern-inferred from structurally-identical siblings (verified at their `DialogTitle` anchors) rather than each byte-read — so read the actual JSX at each cited line before editing and map its VISIBLE TEXT/context to the vocabulary.

Deterministic vocabulary (UI-SPEC "Icon Vocabulary Contract"): Voltar→`ArrowLeft`; Editar→`Pencil`; Guardar/"Guardar X"→`Save`; Cancelar (dialog/form dismiss)→`X`; Adicionar/Novo/open-a-create-trigger or submit-a-create→`Plus`; Confirmar (dialog's own affirmative submit)→`Check`; Enviar/Upload (file submission)→`Upload`. Where a submit button already threads `isSubmitting`/`isPending` into `disabled`, optionally swap the icon for `<Loader2 className="h-4 w-4 animate-spin" />` while pending (only if the boolean is already at hand).

asChild rule: `<Button asChild><Link>` → the icon goes INSIDE the `<Link>`. Compliant example in a sibling file: clientes/page.tsx:285-289.

Do-not-touch: L404 Printer ("Imprimir Ficha") is already compliant — leave byte-identical.

Import discipline: extend the file's existing lucide-react import once with the FULL set both tasks need (`ArrowLeft, Pencil, Save, X, Plus, Check, Upload`) in Task 1, so Task 2 does not need to touch the import line. Verify `Loader2` presence only if you use the pending swap.
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Top toolbar + Doc-a-Tratar/Deslocação/tipo-change/Documento-upload groups (L401-L1363, ~15 buttons)</name>
  <files>web/src/app/(dashboard)/clientes/[id]/page.tsx</files>
  <read_first>
    - web/src/app/(dashboard)/clientes/[id]/page.tsx — imports block (L1-60), then targeted ranges around L401-434, L900-930, L960-1010, L1050-1060, L1300-1370
    - 115-UI-SPEC.md "Clientes module" clientes/[id]/page.tsx row
  </read_first>
  <action>
    Extend the existing lucide-react import with the full set for BOTH tasks: `ArrowLeft, Pencil, Save, X, Plus, Check, Upload` (do not duplicate any name already imported).
    Top toolbar: L401 Voltar→`ArrowLeft` (inside Link if asChild); L416 Cancelar→`X`; L419 Guardar→`Save`; L434 Editar→`Pencil`.
    Doc-a-Tratar group: L908 Adicionar-trigger→`Plus`; L926 Cancelar→`X`; L927 Confirmar/Guardar→`Check` (or `Save` if the visible text is "Guardar").
    Deslocação group: L966 Adicionar-trigger→`Plus`; L1003 Cancelar→`X`; L1004 Confirmar/Guardar→`Check`/`Save` per visible text.
    tipo-change confirm: L1055 Cancelar→`X`; L1056 Confirmar→`Check`.
    Documento upload: L1307 Adicionar-trigger→`Plus`; L1352 Cancelar→`X`; L1363 submit (document upload)→`Upload`.
    Each icon goes before the visible text; keep all text, handlers, variants, and disabled logic. Read each line's actual text before mapping (Confirmar vs Guardar decides Check vs Save).
  </action>
  <acceptance_criteria>
    - Source assertion: the lucide-react import now includes ArrowLeft, Pencil, Save, X, Plus, Check, Upload.
    - Source assertion: each of the ~15 cited buttons in the L401-L1363 range renders an icon element matching the vocabulary mapping.
    - Behavior: no visible text removed; L404 Printer unchanged.
  </acceptance_criteria>
  <verify>
    <automated>grep -nE "ArrowLeft|Pencil|Save|Upload" "web/src/app/(dashboard)/clientes/[id]/page.tsx" | head -20 (expect the extended import + usages) && grep -c "className=\"h-4 w-4\"" "web/src/app/(dashboard)/clientes/[id]/page.tsx" (should increase by ~15 vs pre-edit)</automated>
  </verify>
  <done>Top toolbar + the four dialog/upload groups (L401-L1363) all carry vocabulary-correct icons with text preserved; the shared import covers both tasks; lint clean.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Inline Contactos edit + inline Notas edit groups (L1546-L2113, ~16 buttons)</name>
  <files>web/src/app/(dashboard)/clientes/[id]/page.tsx</files>
  <read_first>
    - web/src/app/(dashboard)/clientes/[id]/page.tsx — targeted ranges around L1540-1560, L1650-1730, L1840-1930, L2040-2115
    - 115-UI-SPEC.md "Clientes module" clientes/[id]/page.tsx row (inline Contactos/Notas cites)
  </read_first>
  <action>
    The import was already extended in Task 1 — do NOT touch the import line unless a name is genuinely missing.
    inline Contactos edit group (L1546, L1550, L1655, L1687, L1724, L1727, L1907, L1915, L1921, L1924): read each button's visible text and map — Editar→`Pencil`, Adicionar→`Plus`, Cancelar→`X`, Guardar→`Save`, Confirmar→`Check`.
    inline Notas edit group (L1849, L2051, L2096, L2104, L2110, L2113): same mapping by visible text.
    Icon before text; preserve all text/handlers/variants. These blocks are structurally repeated — apply the same mapping consistently across siblings.
  </action>
  <acceptance_criteria>
    - Source assertion: each of the ~16 cited buttons in the L1546-L2113 range renders an icon element matching its visible-text→vocabulary mapping.
    - Source assertion: no new import line added (Task 1's extended import already covers Pencil/Plus/X/Save/Check).
    - Behavior: no visible text removed; no button converted to icon-only.
  </acceptance_criteria>
  <verify>
    <automated>grep -c "className=\"h-4 w-4\"" "web/src/app/(dashboard)/clientes/[id]/page.tsx" (total should now be ~31 higher than the pre-plan baseline: prior compliant Printer excluded) && grep -nE "from \"lucide-react\"" "web/src/app/(dashboard)/clientes/[id]/page.tsx" (expect exactly 1 import line)</automated>
  </verify>
  <done>All inline Contactos + Notas edit buttons carry vocabulary-correct icons with text preserved; still exactly one lucide-react import; lint clean; all 31 gaps closed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Adds icon elements to existing client-side buttons in the Cliente ficha. No new input, data flow, network call, or auth/authz change. No confirmation dialog added/removed/altered (UI-SPEC Scope Boundaries §5). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-115-01 | Tampering | icon markup in clientes/[id]/page.tsx | accept | Static compile-time glyph imports; no untrusted input crosses a boundary; existing destructive-action confirmations untouched. |
| T-115-SC | Tampering | npm/pnpm install | accept | No package installed — additional named exports from already-installed `lucide-react`. No legitimacy gate applies. |
</threat_model>

<verification>
- `cd web && pnpm lint` clean on the file (catches any unused/undefined icon after 31 edits).
- Grep confirms exactly one lucide-react import line and ~31 new `h-4 w-4` icon usages.
- `git diff` shows the L404 Printer block unchanged.
</verification>

<success_criteria>
- All 31 ICON-01 gaps in `clientes/[id]/page.tsx` closed with vocabulary-correct icons, text preserved.
- Single consolidated lucide-react import; no unused names; lint clean.
</success_criteria>

<output>
Create `.planning/phases/LEXCV-115-linguagem-visual-icones-em-todos-os-botoes-filtros-icone-only/115-03-SUMMARY.md` when done.
</output>
