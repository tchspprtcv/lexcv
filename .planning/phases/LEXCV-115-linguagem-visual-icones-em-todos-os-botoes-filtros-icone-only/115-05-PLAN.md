---
phase: 115-linguagem-visual-icones-em-todos-os-botoes-filtros-icone-only
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/app/(dashboard)/processos/[id]/page.tsx
autonomous: true
requirements: [ICON-01, FICO-01]
must_haves:
  truths:
    - "The Processo ficha top toolbar (Voltar/Editar/Gerar Termo/Formalizar) and both transition/prazo dialogs (Cancelar/Confirmar) show icons"
    - "The Timeline 'Limpar filtros' button (L1425) is icon-only (X) with Tooltip+aria-label (the only FICO-01 button in this file)"
    - "The 5 repeated sub-section groups (Partes/Fases/Decisões/Factos/Testemunhas), Reatribuir, and Documento-upload each carry vocabulary-correct icons"
  artifacts:
    - path: "web/src/app/(dashboard)/processos/[id]/page.tsx"
      provides: "31 gaps closed: 30 ICON-01 icon-adds + 1 FICO-01 icon-only Limpar filtros"
      contains: "aria-label=\"Limpar filtros\""
  key_links:
    - from: "processos/[id]/page.tsx Limpar filtros (L1425)"
      to: "Tooltip primitive + aria-label"
      via: "icon-only conversion"
      pattern: "aria-label=\"Limpar filtros\""
    - from: "processos/[id]/page.tsx buttons"
      to: "lucide-react vocabulary (reusing already-imported FileText/CheckCircle2)"
      via: "Plus/X/Check/Save/Pencil/ArrowLeft/UserCog/Upload"
      pattern: "<(Plus|Save|Pencil|UserCog)"
---

<objective>
Close all 31 gaps in the second heaviest file of the phase — the Processo ficha (`processos/[id]/page.tsx`, ~2600 lines). 30 are ICON-01 icon-adds (keep text); exactly 1 is a FICO-01 conversion (the Timeline "Limpar filtros" at L1425 → icon-only `X` + Tooltip + aria-label). Two icons are already imported-but-usable here (`FileText` unused, `CheckCircle2` used for the prazo toggle) — reuse them, zero-cost.

Purpose: Isolate the largest ficha edit in its own plan; split into two line-range tasks to keep each pass within budget.
Output: 1 modified file; 31 gaps closed (30 ICON-01 + 1 FICO-01).
</objective>

<execution_context>
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/LEXCV-115-linguagem-visual-icones-em-todos-os-botoes-filtros-icone-only/115-UI-SPEC.md
@.planning/phases/LEXCV-115-linguagem-visual-icones-em-todos-os-botoes-filtros-icone-only/115-CONTEXT.md

Exact line citations for all 31 gaps: `115-UI-SPEC.md` → "Processos module" table, `processos/[id]/page.tsx` row, plus the FICO-01 table row for the L1425 Timeline "Limpar filtros". EVIDENCE, verified 2026-07-21. The 5 sub-section groups were structurally verified identical via `DialogTitle` anchors, not all individually byte-read — so read the actual JSX at each cited line and map by visible text.

Reuse-first (UI-SPEC notes): `FileText` is ALREADY imported but unused in this file → use it for L830 "Gerar Termo de Honorários" (no new import). `CheckCircle2` is ALREADY imported (prazo toggle) → reuse for L906 "Formalizar Processo". The `ACAO_ICONS` map (L998-1017, `Play`/`Pause`/`XCircle`/`RotateCcw`) is compliant — do not touch. The raw `<button>` prazo-conclusion toggle (L1154-1165) is outside the `<Button>` audit scope — do not touch.

Vocabulary: Voltar→`ArrowLeft`; Editar→`Pencil`; Cancelar→`X`; Confirmar→`Check`; Guardar (per-row)→`Save`; Adicionar-trigger/submit-create→`Plus`; Reatribuir→`UserCog` (fallback `User`, already imported at L22, if `UserCog` unavailable); Documento upload submit→`Upload`.

FICO-01 (the ONE button): L1425 "Limpar filtros" (`variant="ghost"`, `h-8 px-3 text-xs`) → icon-only `X` + `size="icon"` + `aria-label="Limpar filtros"` + Tooltip("Limpar filtros"); drop visible text; keep the onClick. Import Tooltip primitives from `@/components/ui/tooltip`; TooltipProvider is app-global.

Import discipline: extend the existing lucide-react import once in Task 1 with the full set both tasks need (`ArrowLeft, Pencil, X, Check, Plus, Save, UserCog, Upload` — plus Tooltip primitives), reusing FileText/CheckCircle2 already present. Task 2 must not touch the import line.
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Toolbar + Gerar Termo/Formalizar + both dialogs + FICO-01 Limpar + Partes/Fases (L740-L1747, ~16 buttons)</name>
  <files>web/src/app/(dashboard)/processos/[id]/page.tsx</files>
  <read_first>
    - web/src/app/(dashboard)/processos/[id]/page.tsx — imports (L1-40, confirm FileText/CheckCircle2/User already present), then ranges around L740-906, L1050-1065, L1245-1260, L1420-1440, L1545-1610, L1655-1750
    - 115-UI-SPEC.md "Processos module" processos/[id]/page.tsx row + FICO-01 table (L1425)
  </read_first>
  <action>
    Extend the existing lucide-react import with the full set for both tasks: `ArrowLeft, Pencil, X, Check, Plus, Save, UserCog, Upload` (do NOT re-import FileText/CheckCircle2/User — already present). Add `Tooltip, TooltipTrigger, TooltipContent` from `@/components/ui/tooltip`.
    Toolbar: L740 Voltar→`ArrowLeft`; L744 Editar→`Pencil`; L830 "Gerar Termo de Honorários"→`FileText` (already imported); L906 "Formalizar Processo"→`CheckCircle2` (already imported).
    transition dialog: L1055 Cancelar→`X`; L1063 Confirmar→`Check`.
    prazo dialog: L1251 Cancelar→`X`; L1259 Confirmar→`Check`.
    FICO-01: L1425 "Limpar filtros"→ icon-only `X` + `size="icon"` + `aria-label="Limpar filtros"` + Tooltip("Limpar filtros"); drop visible text; keep onClick and (optionally) its compact sizing.
    Partes group: L1551 Adicionar-trigger→`Plus`; L1599 Cancelar→`X`; L1607 Confirmar→`Check`.
    Fases group: L1658 Adicionar-trigger→`Plus`; L1689 Cancelar→`X`; L1697 Confirmar→`Check`; L1747 per-row "Guardar"→`Save`.
    Icons before text except the FICO-01 L1425 (icon-only). Keep all text/handlers/variants/disabled; do not touch ACAO_ICONS (L998-1017) or the raw prazo-toggle `<button>`.
  </action>
  <acceptance_criteria>
    - Source assertion: the lucide-react import now includes ArrowLeft, Pencil, X, Check, Plus, Save, UserCog, Upload and still references FileText/CheckCircle2 (not duplicated); Tooltip primitives imported.
    - Source assertion: L830 renders `<FileText`; L906 renders `<CheckCircle2`; L1425 is `size="icon"` with `aria-label="Limpar filtros"` inside a Tooltip and no visible text child.
    - Source assertion: the ~15 non-FICO buttons in this range render vocabulary-mapped icons with text preserved.
    - Untouched: ACAO_ICONS map + raw prazo-toggle button byte-identical.
  </acceptance_criteria>
  <verify>
    <automated>grep -nE "aria-label=\"Limpar filtros\"" "web/src/app/(dashboard)/processos/[id]/page.tsx" (expect 1) && grep -nE "<FileText|<CheckCircle2" "web/src/app/(dashboard)/processos/[id]/page.tsx" && grep -nE "from \"lucide-react\"" "web/src/app/(dashboard)/processos/[id]/page.tsx" (expect 1 import line)</automated>
  </verify>
  <done>Toolbar, Gerar Termo/Formalizar (reused imports), both dialogs, the single FICO-01 icon-only Limpar filtros, and Partes/Fases groups (L740-L1747) all done; shared import covers both tasks; lint clean.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Decisões/Factos/Testemunhas groups + Reatribuir + Documento-upload (L1775-L2631, ~15 buttons)</name>
  <files>web/src/app/(dashboard)/processos/[id]/page.tsx</files>
  <read_first>
    - web/src/app/(dashboard)/processos/[id]/page.tsx — ranges around L1770-1865, L1940-2020, L2095-2195, L2390-2455, L2570-2635
    - 115-UI-SPEC.md "Processos module" processos/[id]/page.tsx row (Decisões/Factos/Testemunhas/Reatribuir/Documento cites)
  </read_first>
  <action>
    Import already extended in Task 1 — do NOT touch the import line unless a name is genuinely missing.
    Decisões group: L1775 Adicionar-trigger→`Plus`; L1855 Cancelar→`X`; L1863 Confirmar→`Check`.
    Factos group: ~L1945 Adicionar-trigger→`Plus`; ~L2008 Cancelar→`X`; ~L2016 Confirmar→`Check`.
    Testemunhas group: ~L2098 Adicionar-trigger→`Plus`; ~L2184 Cancelar→`X`; ~L2192 Confirmar→`Check`.
    Reatribuir: L2396 trigger→`UserCog` (fallback `User`); L2442 Cancelar→`X`; L2450 Confirmar→`Check`.
    Documento upload: L2573 Adicionar-trigger→`Plus`; L2620 Cancelar→`X`; L2631 submit→`Upload`.
    Icons before text; keep all text/handlers/variants. Read each cited line's actual text (Confirmar vs Guardar → Check vs Save) before mapping — these blocks are pattern-inferred siblings.
  </action>
  <acceptance_criteria>
    - Source assertion: each of the ~15 cited buttons renders a vocabulary-mapped icon; Reatribuir trigger renders `<UserCog` (or `<User`); Documento upload submit renders `<Upload`.
    - Source assertion: still exactly one lucide-react import line (Task 1's extension covers everything here).
    - Behavior: no visible text removed in this range (all ICON-01); no icon-only conversion here (the only FICO-01 button, L1425, is in Task 1).
  </acceptance_criteria>
  <verify>
    <automated>grep -nE "<UserCog|<User " "web/src/app/(dashboard)/processos/[id]/page.tsx" && grep -c "className=\"h-4 w-4\"" "web/src/app/(dashboard)/processos/[id]/page.tsx" (total should be ~30 higher than pre-plan baseline, plus the 1 icon-only FICO button) && grep -cE "from \"lucide-react\"" "web/src/app/(dashboard)/processos/[id]/page.tsx" (expect 1)</automated>
  </verify>
  <done>Decisões/Factos/Testemunhas/Reatribuir/Documento-upload groups all carry vocabulary-correct icons with text preserved; single import; lint clean; all 31 gaps closed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Adds icons/one Tooltip wrapper to existing client-side buttons in the Processo ficha. No new input, data flow, network call, or auth/authz change. Reatribuir/formalizar/delete handlers and confirmations untouched (UI-SPEC Scope Boundaries §5). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-115-01 | Tampering | icon markup in processos/[id]/page.tsx | accept | Static compile-time glyph imports; no untrusted input crosses a boundary; existing confirmations/handlers unchanged. |
| T-115-A11Y | WCAG 4.1.2 (a11y) | icon-only Limpar filtros L1425 (FICO-01) | mitigate | The one icon-only conversion carries `aria-label="Limpar filtros"` + a `Tooltip` with identical copy, preserving the accessible name for keyboard/screen-reader users (FICO-01 AC4). |
| T-115-SC | Tampering | npm/pnpm install | accept | No package installed — additional named exports from already-installed `lucide-react`. No legitimacy gate applies. |
</threat_model>

<verification>
- `cd web && pnpm lint` clean (catches unused/undefined after 31 edits and the reused FileText).
- Grep confirms: exactly one lucide-react import line, the single `aria-label="Limpar filtros"`, reused `FileText`/`CheckCircle2`, and ~30 new icon usages.
- `git diff` shows ACAO_ICONS map + raw prazo-toggle button unchanged.
</verification>

<success_criteria>
- All 31 gaps in `processos/[id]/page.tsx` closed: 30 ICON-01 (text preserved) + 1 FICO-01 icon-only Limpar filtros with Tooltip+aria-label.
- FileText/CheckCircle2 reused (no duplicate imports); single consolidated import; lint clean.
</success_criteria>

<output>
Create `.planning/phases/LEXCV-115-linguagem-visual-icones-em-todos-os-botoes-filtros-icone-only/115-05-SUMMARY.md` when done.
</output>
