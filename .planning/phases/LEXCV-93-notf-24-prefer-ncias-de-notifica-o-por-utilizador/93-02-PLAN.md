---
phase: 93-notf-24-prefer-ncias-de-notifica-o-por-utilizador
plan: 02
type: execute
wave: 2
depends_on: [93-01]
files_modified:
  - backend/src/main/java/com/lexcv/services/NotificacaoService.java
  - backend/src/test/java/com/lexcv/services/NotificacaoServiceTest.java
autonomous: true
requirements: [NOTF-24]

must_haves:
  truths:
    - "criar(...) não persiste nem devolve linha quando a categoria é silenciável E existe uma preferência de silenciamento para (tenant, destinatário, categoria) — devolve Optional.empty()"
    - "PRAZO_VENCIDO é sempre entregue por criar(...) mesmo que exista uma linha de preferência para ele (defense-in-depth via isSilenciavelCategoria)"
    - "O job diário (AlertasDiariosJob), que chama criar() diretamente, respeita o silenciamento sem qualquer alteração própria — porque o guard vive dentro de criar()"
    - "silenciarCategoria rejeita categoria desconhecida E PRAZO_VENCIDO; reativarCategoria remove a preferência; listarCategoriasSilenciadas devolve as categorias silenciadas do user"
  artifacts:
    - path: "backend/src/main/java/com/lexcv/services/NotificacaoService.java"
      provides: "Mute guard dentro de criar() + 3 métodos de preferência + dependência NotificacaoPreferenciaRepository"
      contains: "existsByTenantIdAndUserIdAndCategoria"
    - path: "backend/src/test/java/com/lexcv/services/NotificacaoServiceTest.java"
      provides: "20 construtores atualizados p/ 3 args + testes de mute guard, PRAZO_VENCIDO, e métodos de preferência"
      contains: "NotificacaoPreferenciaRepository"
  key_links:
    - from: "NotificacaoService.criar"
      to: "NotificacaoPreferenciaRepository.existsByTenantIdAndUserIdAndCategoria"
      via: "guard de silenciamento após validação, antes do builder"
      pattern: "existsByTenantIdAndUserIdAndCategoria"
    - from: "NotificacaoService.criar"
      to: "CategoriaNotificacao.isSilenciavelCategoria"
      via: "PRAZO_VENCIDO nunca é silenciado (guard só corre se silenciável)"
      pattern: "isSilenciavelCategoria"
---

<objective>
Inserir o guard de silenciamento DENTRO de `NotificacaoService.criar(...)` — o único ponto de escrita de criação de notificações — e adicionar os 3 métodos de serviço de preferência (silenciar/reativar/listar) que os endpoints (Plan 93-03) delegarão. Porque `criar()` é o choke point que TANTO os 4 métodos `notificar*` COMO o `AlertasDiariosJob` chamam, um único guard aqui faz o job diário respeitar mutes automaticamente (Pitfall 1 — o erro-clássico é pôr o check nos triggers e o job escapar).

Purpose: entregar a fundação de correção da NOTF-24 (Critérios de Sucesso 1, 2 e 3) num único ponto testável, deixando `criar()` num estado coerente e bem-testado para as fases 94/95/96 construírem por cima (cadeia sequencial no mesmo ficheiro).
Output: `NotificacaoService.java` com guard + 3 métodos; `NotificacaoServiceTest.java` com os 20 construtores migrados e novos testes de mute.
</objective>

<execution_context>
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/LEXCV-93-notf-24-prefer-ncias-de-notifica-o-por-utilizador/93-CONTEXT.md
@.planning/research/ARCHITECTURE.md
@.planning/research/PITFALLS.md
@.planning/phases/LEXCV-93-notf-24-prefer-ncias-de-notifica-o-por-utilizador/93-01-SUMMARY.md

<interfaces>
<!-- Contratos criados no Plan 93-01 (usar diretamente, sem explorar) -->
```java
// NotificacaoPreferenciaRepository
boolean existsByTenantIdAndUserIdAndCategoria(UUID tenantId, UUID userId, String categoria);
List<NotificacaoPreferencia> findByTenantIdAndUserId(UUID tenantId, UUID userId);
void deleteByTenantIdAndUserIdAndCategoria(UUID tenantId, UUID userId, String categoria);

// CategoriaNotificacao (enum em com.lexcv.models)
static Optional<CategoriaNotificacao> fromString(String valor);      // nunca lança
static boolean isSilenciavelCategoria(String valor);                 // false p/ desconhecido E PRAZO_VENCIDO
boolean isSilenciavel();

// NotificacaoPreferencia builder: NotificacaoPreferencia.builder().tenantId(..).userId(..).categoria(..).build()
```

<!-- Estado atual de criar() — assinatura e ponto de inserção (NotificacaoService.java) -->
<!-- public Notificacao criar(UUID tenantId, UUID destinatarioId, String categoria, String titulo, -->
<!--        String mensagem, String entidadeTipo, String entidadeId, String linkUrl) { ...validações... builder...save } -->
<!-- destinatarioId É o user cuja preferência se verifica. Guard entra APÓS o bloco requireMaxLength (linha ~53) -->
<!-- e ANTES de `Notificacao n = Notificacao.builder()` (linha ~55). -->

<!-- FACTO VERIFICADO (grep de todo o backend): NENHUM caller usa o valor de retorno de criar(). -->
<!-- Trigger methods (notificarAdmins/notificarFaseEntrada/...), AlertasDiariosJob.notificar e todos os -->
<!-- call sites de teste chamam criar(...) como statement / assertThrows / Mockito.verify — nunca leem o retorno. -->
<!-- Logo mudar o retorno Notificacao -> Optional<Notificacao> é seguro e contido a este ficheiro. -->

<!-- NotificacaoServiceTest tem 20 call sites `new NotificacaoService(notificacaoRepository, userRepository)` -->
<!-- (linhas 48,71,84,96,117,145,179,192,214,237,257,279,298,325,350,369,385,404,430,448). -->
<!-- Mockito-safety: um @Mock NotificacaoPreferenciaRepository não-stubbed devolve `false` por default -->
<!-- em existsBy... -> "não silenciado" -> as 20 asserções existentes mantêm-se verdes só com o 3º arg mecânico. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Mute guard em criar() (Optional) + 3 métodos de preferência</name>
  <files>backend/src/main/java/com/lexcv/services/NotificacaoService.java</files>
  <read_first>
    - backend/src/main/java/com/lexcv/services/NotificacaoService.java (ficheiro inteiro — construtor @RequiredArgsConstructor, criar() linhas 33-66, marcarLida @Transactional linhas 243-252 como padrão dos métodos de mutação)
    - backend/src/main/java/com/lexcv/models/CategoriaNotificacao.java (enum criado no Plan 93-01 — fromString/isSilenciavelCategoria/isSilenciavel)
    - backend/src/main/java/com/lexcv/repositories/NotificacaoPreferenciaRepository.java (Plan 93-01 — os 3 métodos derivados)
    - backend/src/main/java/com/lexcv/jobs/AlertasDiariosJob.java linhas 303-331 (o método notificar() que chama criar() diretamente — a razão de o guard ter de viver em criar())
  </read_first>
  <behavior>
    - criar() com categoria silenciável + linha de preferência existente para (tenant, destinatarioId, categoria) → NÃO chama save(), devolve Optional.empty()
    - criar() com categoria silenciável + SEM linha de preferência → persiste e devolve Optional.of(linha) (comportamento default-on preservado)
    - criar() com categoria "PRAZO_VENCIDO" + linha de preferência existente → AINDA persiste (isSilenciavelCategoria("PRAZO_VENCIDO")==false; guard não corre) — Critério de Sucesso 2
    - criar() com destinatário de outro tenant → continua a lançar IllegalArgumentException ANTES de chegar ao guard (validação de tenant intocada)
    - silenciarCategoria(tenant, user, "PRAZO_VENCIDO") → lança IllegalArgumentException (não-silenciável)
    - silenciarCategoria(tenant, user, "categoria_inexistente") → lança IllegalArgumentException (desconhecida)
    - silenciarCategoria(tenant, user, "FASE_ENTRADA") idempotente → se já existe linha, não cria segunda (respeita constraint única)
    - reativarCategoria(tenant, user, categoria) → chama deleteByTenantIdAndUserIdAndCategoria (idempotente; sem erro se ausente)
    - listarCategoriasSilenciadas(tenant, user) → devolve a lista de strings categoria das linhas de (tenant, user)
  </behavior>
  <action>
    (NOTF-24, decisão travada) Editar `NotificacaoService.java`:

    1. Adicionar a dependência ao construtor: como a classe usa `@RequiredArgsConstructor`, acrescentar o campo `private final NotificacaoPreferenciaRepository notificacaoPreferenciaRepository;` junto dos dois campos `final` existentes (`notificacaoRepository`, `userRepository`). Lombok gera o construtor de 3 args automaticamente. Import: `com.lexcv.repositories.NotificacaoPreferenciaRepository` e `com.lexcv.models.CategoriaNotificacao`.

    2. Mudar a assinatura de `criar` de `public Notificacao criar(...)` para `public Optional<Notificacao> criar(...)` (mesmos 8 parâmetros). `Optional` já está importado. Justificação travada (pesquisa): "muted" é um terceiro resultado (validação passou, nada persistido) e deve ser um Optional.empty(), não um null nem uma exceção — nunca conflacionar com o path de IllegalArgumentException de destinatário órfão. Facto verificado: nenhum caller lê o retorno, logo a mudança é segura e contida a este ficheiro.

    3. Inserir o mute guard APÓS o bloco `requireMaxLength(...)` (linha ~53) e ANTES de `Notificacao n = Notificacao.builder()` (linha ~55): se `CategoriaNotificacao.isSilenciavelCategoria(categoria)` for true E `notificacaoPreferenciaRepository.existsByTenantIdAndUserIdAndCategoria(tenantId, destinatarioId, categoria)` for true → `log.debug(...)` (mensagem própria, distinta do warn de órfão) e `return Optional.empty();`. A ordem da condição importa: `isSilenciavelCategoria` primeiro garante que PRAZO_VENCIDO (não-silenciável) nunca sequer consulta a preferência e é sempre entregue (Critério 2, defense-in-depth mesmo que uma linha órfã de PRAZO_VENCIDO existisse).
    - `destinatarioId` é o user cuja preferência se consulta (o destinatário É o utilizador que silenciou).
    - Colocar o guard DEPOIS das validações garante que só corre sobre input válido; as validações de tenant/nonBlank/maxLength mantêm-se exatamente onde estão.

    4. Ajustar o `return` final de `criar` para `return Optional.of(notificacaoRepository.save(n));`.

    5. Adicionar 3 métodos de serviço (os endpoints do Plan 93-03 delegam-nos — mantém `NotificacaoService` como único ponto de escrita do subsistema, conforme o doc-comment do NotificacaoController):
    - `public List<String> listarCategoriasSilenciadas(UUID tenantId, UUID userId)`: `notificacaoPreferenciaRepository.findByTenantIdAndUserId(tenantId, userId)` mapeado para `.getCategoria()` numa `List<String>`.
    - `public void silenciarCategoria(UUID tenantId, UUID userId, String categoria)`: validar via `CategoriaNotificacao.fromString(categoria)` — se `Optional.empty()`, lançar `IllegalArgumentException("categoria desconhecida: " + categoria)`; se presente mas `!isSilenciavel()`, lançar `IllegalArgumentException("categoria não silenciável: " + categoria)`. Depois, idempotente: só se `!existsByTenantIdAndUserIdAndCategoria(...)`, fazer `notificacaoPreferenciaRepository.save(NotificacaoPreferencia.builder().tenantId(tenantId).userId(userId).categoria(categoria).build())`. Anotar `@Transactional`.
    - `@Transactional public void reativarCategoria(UUID tenantId, UUID userId, String categoria)`: `notificacaoPreferenciaRepository.deleteByTenantIdAndUserIdAndCategoria(tenantId, userId, categoria)` (idempotente; sem erro se ausente). `@Transactional` é obrigatório para o delete derivado.

    NÃO duplicar o mute check em `notificarAdmins`/`notificarFaseEntrada`/`notificarDocumentoNovo`/`notificarProcessoAtribuido`/`notificarParecerAtribuido` — isso reintroduz o risco de N call sites do Pitfall 1. NÃO alterar `AlertasDiariosJob.java` — ele herda o guard de graça por chamar `criar()`. Os `try/catch (IllegalArgumentException)` existentes nos triggers continuam corretos: um retorno Optional.empty() não é exceção, o caller simplesmente prossegue.
  </action>
  <verify>
    <automated>cd backend && node -e "const fs=require('fs'); const strip=s=>s.split('\n').filter(l=>!l.trim().startsWith('//')&&!l.trim().startsWith('*')&&!l.trim().startsWith('/*')).join('\n'); const s=strip(fs.readFileSync('src/main/java/com/lexcv/services/NotificacaoService.java','utf8')); const ok = /Optional<Notificacao>\s+criar\s*\(/.test(s) && /isSilenciavelCategoria\s*\(\s*categoria\s*\)/.test(s) && /existsByTenantIdAndUserIdAndCategoria\s*\(\s*tenantId\s*,\s*destinatarioId\s*,\s*categoria\s*\)/.test(s) && /return\s+Optional\.empty\(\)/.test(s) && /return\s+Optional\.of\(\s*notificacaoRepository\.save/.test(s) && /silenciarCategoria/.test(s) && /reativarCategoria/.test(s) && /listarCategoriasSilenciadas/.test(s) && /deleteByTenantIdAndUserIdAndCategoria/.test(s); process.exit(ok?0:1)"</automated>
    <automated>cd backend && mvn -q -DskipTests compile</automated>
  </verify>
  <acceptance_criteria>
    - `criar` devolve `Optional<Notificacao>`; o guard consulta `isSilenciavelCategoria(categoria)` E `existsByTenantIdAndUserIdAndCategoria(tenantId, destinatarioId, categoria)`, devolvendo `Optional.empty()` quando silenciado
    - O guard vive APENAS dentro de `criar()`; nenhum dos 5 métodos `notificar*` nem `AlertasDiariosJob.java` foi alterado
    - Os 3 métodos de preferência existem; `silenciarCategoria` valida via `CategoriaNotificacao` e é idempotente; `reativarCategoria` é `@Transactional`
    - `cd backend && mvn -q -DskipTests compile` compila
  </acceptance_criteria>
  <done>criar() aplica o mute no choke point único (job diário incluído por construção); métodos de preferência prontos para os endpoints.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Migrar os 20 construtores do teste + testes de mute/preferência</name>
  <files>backend/src/test/java/com/lexcv/services/NotificacaoServiceTest.java</files>
  <read_first>
    - backend/src/test/java/com/lexcv/services/NotificacaoServiceTest.java (ficheiro inteiro — os 20 `new NotificacaoService(notificacaoRepository, userRepository)` e o padrão de @Mock/ArgumentCaptor/when(...save...))
    - backend/src/main/java/com/lexcv/services/NotificacaoService.java (a versão editada na Task 1 — assinaturas finais de criar/silenciar/reativar/listar)
    - backend/src/main/java/com/lexcv/repositories/NotificacaoPreferenciaRepository.java (métodos a stubar)
  </read_first>
  <action>
    Atualizar `NotificacaoServiceTest.java` (é um teste Mockito puro, sem contexto Spring — corre sem MINIO_ENDPOINT):

    1. Adicionar um novo campo `@Mock private NotificacaoPreferenciaRepository notificacaoPreferenciaRepository;` junto dos mocks existentes. Import de `com.lexcv.repositories.NotificacaoPreferenciaRepository`.

    2. Migração mecânica: substituir TODAS as 20 ocorrências de `new NotificacaoService(notificacaoRepository, userRepository)` por `new NotificacaoService(notificacaoRepository, userRepository, notificacaoPreferenciaRepository)`. Não alterar mais nada nos testes existentes — pela nota Mockito-safety, o mock não-stubbed devolve `false` em `existsBy...` (=não silenciado), preservando todas as asserções atuais. Manter o comportamento STRICT_STUBS: NÃO adicionar stubs a este mock nos testes existentes (chamá-lo sem stub é permitido; stubar sem usar é que seria flagged).

    3. Adicionar novos testes (nomes descritivos em pt, no estilo do ficheiro), cada um construindo o serviço com os 3 mocks:
    - `criar_categoriaSilenciada_naoPersisteEDevolveOptionalVazio`: stubar `userRepository.findById(DESTINATARIO_A)` para user válido do tenant; stubar `notificacaoPreferenciaRepository.existsByTenantIdAndUserIdAndCategoria(TENANT_ID, DESTINATARIO_A, "FASE_ENTRADA")` → `true`; chamar `criar(...)` com "FASE_ENTRADA"; assert `resultado.isEmpty()` e `verify(notificacaoRepository, never()).save(any())`. Comentar que este é EXATAMENTE o path que `AlertasDiariosJob.notificar()` percorre (chama criar() diretamente) — logo prova o Critério de Sucesso 3 sem precisar de tocar no teste do job.
    - `criar_prazoVencidoComPreferenciaDeSilenciamento_aindaPersiste`: stubar user válido; NÃO é necessário stubar existsBy (isSilenciavelCategoria("PRAZO_VENCIDO")==false faz o guard nunca consultar a preferência — se quiseres, stuba-lo a true para provar que é ignorado, mas então usa `lenient()` para não violar strict-stubs, OU simplesmente não stubes e confia no short-circuit); chamar `criar(...)` com "PRAZO_VENCIDO"; assert `resultado.isPresent()` e `verify(notificacaoRepository, times(1)).save(any())` — Critério de Sucesso 2.
    - `silenciarCategoria_prazoVencido_lancaIllegalArgumentException` e `silenciarCategoria_categoriaDesconhecida_lancaIllegalArgumentException`: `assertThrows(IllegalArgumentException.class, ...)`, e `verify(notificacaoPreferenciaRepository, never()).save(any())`.
    - `silenciarCategoria_categoriaValidaAindaNaoSilenciada_persisteUmaLinha`: stubar `existsBy...` → `false`; chamar silenciar("FASE_ENTRADA"); `verify(...preferenciaRepository, times(1)).save(any())`.
    - `silenciarCategoria_jaSilenciada_naoPersisteSegundaLinha`: stubar `existsBy...` → `true`; chamar silenciar; `verify(...preferenciaRepository, never()).save(any())` (idempotência).
    - `reativarCategoria_chamaDeleteDerivado`: chamar reativar; `verify(...preferenciaRepository, times(1)).deleteByTenantIdAndUserIdAndCategoria(TENANT_ID, DESTINATARIO_A, "FASE_ENTRADA")`.
    - `listarCategoriasSilenciadas_devolveCategoriasDoUser`: stubar `findByTenantIdAndUserId(TENANT_ID, DESTINATARIO_A)` → lista com uma NotificacaoPreferencia de categoria "DOCUMENTO_NOVO"; assert que a lista devolvida contém "DOCUMENTO_NOVO".

    Todos os testes têm de correr sem BD real (Mockito puro). Este é o local onde o Critério de Sucesso 3 (job diário respeita mute) fica provado por construção — documentar isso no comentário do primeiro teste novo.
  </action>
  <verify>
    <automated>cd backend && node -e "const fs=require('fs'); const t=fs.readFileSync('src/test/java/com/lexcv/services/NotificacaoServiceTest.java','utf8'); const threeArg=(t.match(/new NotificacaoService\(\s*notificacaoRepository\s*,\s*userRepository\s*,\s*notificacaoPreferenciaRepository\s*\)/g)||[]).length; const twoArgLeft=(t.match(/new NotificacaoService\(\s*notificacaoRepository\s*,\s*userRepository\s*\)/g)||[]).length; const ok = threeArg>=20 && twoArgLeft===0 && /@Mock[\s\S]{0,80}NotificacaoPreferenciaRepository/.test(t) && /criar_categoriaSilenciada/.test(t) && /prazoVencido/i.test(t) && /silenciarCategoria_/.test(t); process.exit(ok?0:1)"</automated>
    <automated>cd backend && mvn -q test -Dtest=NotificacaoServiceTest</automated>
  </verify>
  <acceptance_criteria>
    - As 20 chamadas de construtor passam agora 3 args (mock de preferência incluído); zero chamadas de 2 args remanescentes
    - Existem testes que provam: mute não persiste (mesmo path do job), PRAZO_VENCIDO sempre persiste, silenciar rejeita desconhecida/PRAZO_VENCIDO, idempotência, reativar chama delete, listar devolve categorias
    - `cd backend && mvn -q test -Dtest=NotificacaoServiceTest` passa a 100% (todos os testes existentes + novos, sem MINIO)
  </acceptance_criteria>
  <done>Suite de teste do serviço migrada e ampliada; o mute e as preferências estão provados por Mockito, o job diário coberto por construção.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| choke point criar() | Todas as 5 origens de criação de notificação (4 triggers + job diário) atravessam criar(); o guard tem de estar aqui para não haver escape |
| tenant + user isolation | O guard consulta preferências por (tenant_id, destinatarioId, categoria) — nunca por user_id sozinho |

## STRIDE Threat Register (ASVS L1 — security_block_on: high)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-93-04 | Elevation of Privilege | mute guard bypass via job diário | mitigate | Guard colocado dentro de `criar()` (Pitfall 1). Teste `criar_categoriaSilenciada_...` exercita o path exato que `AlertasDiariosJob.notificar()` usa (chamada direta a criar), provando que o job respeita o mute sem alteração própria. Asserção de fonte confirma que nenhum trigger nem o job foram tocados. |
| T-93-05 | Tampering | silenciar PRAZO_VENCIDO (categoria crítica) | mitigate | `isSilenciavelCategoria` faz o guard curto-circuitar para PRAZO_VENCIDO (sempre entregue, defense-in-depth); `silenciarCategoria` lança IllegalArgumentException para PRAZO_VENCIDO e categorias desconhecidas. Ambos com teste. |
| T-93-06 | Information Disclosure | consulta de preferência | mitigate | `existsByTenantIdAndUserIdAndCategoria`/`findByTenantIdAndUserId`/`deleteBy...` incluem sempre tenant_id + user_id (Pitfall 10) — o guard usa `destinatarioId` como user, nunca um id ambiente/spoofável. |
| T-93-SC | Tampering | npm/pip/cargo installs | accept | Zero dependências novas (JUnit5/Mockito já presentes). Sem instalações; gate de legitimidade N/A. |
</threat_model>

<verification>
- `cd backend && mvn -q test -Dtest=NotificacaoServiceTest` verde (Mockito puro, sem MINIO_ENDPOINT — Pitfall 5)
- Asserção de fonte: guard só em criar(); retorno Optional; 3 métodos de preferência; 20 construtores migrados
- Critério de Sucesso 3 (job diário respeita mute) provado por o guard viver em criar() e o teste exercitar esse path direto
</verification>

<success_criteria>
1. Silenciar uma categoria impede criar() de persistir novas notificações dessa categoria para esse user, sem afetar outros (NOTF-24 SC-1)
2. PRAZO_VENCIDO é sempre entregue por criar() (NOTF-24 SC-2)
3. O job diário respeita o silenciamento por construção (guard no choke point) (NOTF-24 SC-3)
4. Métodos de serviço de preferência (silenciar/reativar/listar) prontos e testados para os endpoints do Plan 93-03
</success_criteria>

<output>
Create `.planning/phases/LEXCV-93-notf-24-prefer-ncias-de-notifica-o-por-utilizador/93-02-SUMMARY.md` when done
</output>
