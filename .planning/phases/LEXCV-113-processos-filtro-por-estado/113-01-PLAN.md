---
phase: 113-processos-filtro-por-estado
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/app/(dashboard)/processos/page.tsx
autonomous: true
requirements: [PEST-01]

must_haves:
  truths:
    - "The Estado filter control (NativeSelect, 6 options) is visible in the main Processos filter bar without opening the collapsed 'Filtros' (advancedOpen) panel"
    - "The Estado control sits between the Pesquisar field and the 'Filtros' button in the always-visible bar"
    - "Selecting an estado and clicking 'Aplicar' still filters the process list to that estado (same draft + Aplicar submission model as before)"
    - "'Limpar' still resets estado to Todos together with the other filters"
    - "The remaining advanced filters (Tribunal, Area juridica, Cliente) stay inside the advancedOpen panel and fill the 12-column grid with no dangling empty column"
  artifacts:
    - path: "web/src/app/(dashboard)/processos/page.tsx"
      provides: "Estado NativeSelect relocated to the always-visible main filter bar (w-40 max-w-full wrapper), removed from the advancedOpen panel; the 3 remaining advanced fields at lg:col-span-4"
      contains: "w-40 max-w-full"
  key_links:
    - from: "Estado NativeSelect (now in main bar)"
      to: "draftEstado state (unchanged)"
      via: "value={draftEstado} / onChange setDraftEstado — moved verbatim, not rewritten"
      pattern: "value=\\{draftEstado\\}"
    - from: "onApply (unchanged)"
      to: "filters.estado -> useProcessos(filters) -> GET /processos?estado="
      via: "estado: draftEstado.trim() commit on Aplicar"
      pattern: "estado: draftEstado.trim\\(\\)"
---

<objective>
Promote the already-existing Estado filter from inside the collapsed `advancedOpen` panel to the always-visible main filter bar on the Processos list page, so users discover and use it without first clicking "Filtros". This is a pure JSX relocation: the control, its state (`draftEstado`), its handlers (`onApply`/`onClear`), and its 6 options move byte-for-byte — no logic, no data flow, and no backend changes.

Purpose: Close PEST-01. Per `113-CONTEXT.md`, the 3 ROADMAP success criteria are already technically satisfied by the existing code; the real gap is the control's *invisibility* (buried in a panel closed by default). Making it always-visible is the fix.

Output: modified `web/src/app/(dashboard)/processos/page.tsx` (single file).
</objective>

<execution_context>
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/LEXCV-113-processos-filtro-por-estado/113-CONTEXT.md
@.planning/phases/LEXCV-113-processos-filtro-por-estado/113-UI-SPEC.md

The 113-UI-SPEC.md "Relocation Detail" section is the authoritative, line-level contract for this task — it contains the exact source lines to remove, the exact destination, and the exact new wrapper markup (the "New wrapper markup" fenced block). Follow it verbatim.

<interfaces>
<!-- Current shape of processos/page.tsx (verified by direct read). These are anchors to move BETWEEN, -->
<!-- not code to rewrite. Do NOT create new state, handlers, hooks, or components. -->
// MAIN BAR (always visible): <div className="flex flex-wrap items-end gap-2"> at line 205.
//   Its two current children: (1) the Pesquisar wrapper <div className="w-[320px] max-w-full"> (lines 206-219,
//   closing </div> at 219); (2) the "Filtros" <Button ...>Filtros</Button> (lines 220-228). The new Estado
//   wrapper is inserted as a sibling BETWEEN child (1) and child (2).
// ADVANCED PANEL (collapsed by default): rendered by {advancedOpen ? ( ... ) : null} (line 249); its grid
//   <div className="w-full grid gap-3 lg:grid-cols-12 ..."> is at line 250 and currently holds 4 children, each
//   lg:col-span-3 (4x3=12): Estado (251-270, the FIRST child, to be removed), Tribunal (271-283),
//   Area juridica (284-296), Cliente (297-316).
// ESTADO CONTROL to move: <NativeSelect value={draftEstado} onChange={(e) => setDraftEstado(e.target.value)}
//   size="default" className="w-full"> with 6 <option>s: "" Todos / TRIAGEM Em triagem / ATIVO Ativo /
//   SUSPENSO Suspenso / ENCERRADO Encerrado / CONCLUIDO Concluido.
// UNCHANGED WIRING (do not touch): draftEstado state (line 42); onApply commits estado: draftEstado.trim()
//   (line 111); onClear does setDraftEstado("") (line 120) and resets filters.estado; useProcessos(filters)
//   (line 55); the empty-state ternary filters.estado === "TRIAGEM" (line 334) stays reachable identically.
// NativeSelect import already present (line 13); no new import needed. Cliente also uses NativeSelect (302-314)
//   and must remain in the advanced panel unchanged.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Relocate the Estado filter to the always-visible main bar (+ rebalance the advanced grid)</name>
  <files>web/src/app/(dashboard)/processos/page.tsx</files>
  <read_first>
    - web/src/app/(dashboard)/processos/page.tsx (full file — the main-bar flex row at line 205, the Pesquisar wrapper 206-219, the "Filtros" Button 220-228, the advancedOpen grid at 249-250, the Estado block 251-270, Tribunal 271-283, Area juridica 284-296, Cliente 297-316; and the unchanged wiring: draftEstado line 42, onApply line 111, onClear line 120, useProcessos line 55, empty-state ternary line 334)
    - .planning/phases/LEXCV-113-processos-filtro-por-estado/113-UI-SPEC.md (the "Relocation Detail" section — the exact "New wrapper markup" block, the "Required consequential edit", the "Apply-behavior" resolution, and the "Non-goals for this phase" list)
    - web/src/components/ui/native-select.tsx (confirm the NativeSelect API — size/className props — so the moved control is reused unchanged, not rewritten)
  </read_first>
  <action>
    Make exactly three edits to `web/src/app/(dashboard)/processos/page.tsx`, and nothing else:

    1. REMOVE the Estado block (currently lines 251-270): the entire `<div className="lg:col-span-3">` that is the FIRST child of the advancedOpen grid (the one wrapping the "Estado" label div plus the `NativeSelect` bound to `draftEstado`/`setDraftEstado` and its 6 options). After removal the advancedOpen grid has 3 children left (Tribunal, Area juridica, Cliente).

    2. INSERT that same Estado control into the always-visible main bar as a new child of `<div className="flex flex-wrap items-end gap-2">` (line 205), positioned BETWEEN the Pesquisar wrapper's closing `</div>` (line 219) and the "Filtros" `<Button>` (line 220) — per 113-CONTEXT.md's locked position "logo a seguir ao campo de pesquisa, antes do botao 'Filtros'". Use the exact "New wrapper markup" from 113-UI-SPEC.md's Relocation Detail: the outer wrapper is `<div className="w-40 max-w-full">` (this is the ONLY class that differs from the old block — do NOT reuse `lg:col-span-3`, and do NOT use `w-full`/`flex-1`, which would fight the search field and Filtros button for space in the flex-wrap row). Inside it, the label div (`text-[11px] font-bold tracking-wider uppercase text-slate-500 dark:text-slate-400` reading "Estado"), a `mt-2` inner div, and the `NativeSelect` (`value={draftEstado}`, `onChange={(e) => setDraftEstado(e.target.value)}`, `size="default"`, `className="w-full"`) with all 6 options unchanged. The NativeSelect and everything inside it is byte-for-byte identical to the removed block.

    3. REBALANCE the advanced grid (required, not optional): change each of the 3 remaining advanced children (Tribunal, Area juridica, Cliente) from `lg:col-span-3` to `lg:col-span-4` so the `lg:grid-cols-12` row fills exactly 12 columns (3x4=12) with no dangling empty 3-column gap on large screens.

    Constraints (from 113-CONTEXT.md "O que NAO promover" + 113-UI-SPEC.md Non-goals): Do NOT change any state or handler — `draftEstado`, `onApply` (keep `estado: draftEstado.trim()`), `onClear`, `filters.estado`, `useProcessos(filters)` all stay identical. Do NOT add an `onChange`-triggered immediate-apply for Estado; keep the existing draft + "Aplicar" submission model (UI-SPEC Apply-behavior resolution). Do NOT swap `NativeSelect` for a Radix `Select`. Do NOT promote Tribunal/Area/Cliente out of the panel. Do NOT add a count badge, KPI count, or icon, and do NOT touch `--radius` (those are Phase 114/115). Net result: exactly one `<NativeSelect value={draftEstado}` in the file, now in the main bar; the Cliente `<NativeSelect>` stays in the advanced panel.
  </action>
  <verify>
    <automated>cd web && pnpm exec tsc --noEmit && pnpm lint</automated>
  </verify>
  <acceptance_criteria>
    - `cd web && pnpm exec tsc --noEmit` exits 0 and `pnpm lint` reports no new errors for `processos/page.tsx`.
    - `grep -c 'w-40 max-w-full' 'web/src/app/(dashboard)/processos/page.tsx'` returns exactly 1 (the new Estado wrapper; distinct from the search field's `w-[320px] max-w-full`).
    - Relocation is real: the line number from `grep -n 'w-40 max-w-full' 'web/src/app/(dashboard)/processos/page.tsx'` is LESS than the line number from `grep -n 'advancedOpen ?' 'web/src/app/(dashboard)/processos/page.tsx'` — i.e. the Estado control now sits in the main bar, above the collapsed panel.
    - `grep -c 'lg:col-span-4' 'web/src/app/(dashboard)/processos/page.tsx'` returns exactly 3 (Tribunal, Area juridica, Cliente), and `grep -c 'lg:col-span-3' 'web/src/app/(dashboard)/processos/page.tsx'` returns 0 (these Tailwind tokens appear only in JSX className strings on this page — no comment collision).
    - No duplication or loss of the control: `grep -c '<NativeSelect' 'web/src/app/(dashboard)/processos/page.tsx'` returns exactly 2 (Estado + Cliente), and `grep -c 'value="TRIAGEM"' 'web/src/app/(dashboard)/processos/page.tsx'` returns exactly 1.
    - Wiring untouched (key links preserved): `grep -c 'value={draftEstado}' 'web/src/app/(dashboard)/processos/page.tsx'` returns exactly 1, and `grep -c 'estado: draftEstado.trim()' 'web/src/app/(dashboard)/processos/page.tsx'` returns exactly 1 — proving the moved control is still bound to `draftEstado` and Aplicar still commits it. No new `onChange`-immediate-apply / `setFilters` call was added for Estado.
  </acceptance_criteria>
  <done>The Estado NativeSelect renders in the always-visible main filter bar (between Pesquisar and "Filtros"), the advancedOpen panel holds only Tribunal/Area/Cliente filling the 12-col grid, all state/handlers are unchanged, and the page typechecks and lints clean.</done>
</task>

</tasks>

<threat_model>
This is a pure JSX-relocation phase: no new component, no new logic, no new state, no new data flow, no backend change. The Estado value's client->backend path (`draftEstado` -> `filters.estado` -> `useProcessos` -> `GET /processos?estado=`) already exists and is unchanged — only the DOM position of the control moves. The honest assessment is "no new threat surface — relocating existing, already-reviewed markup."

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser (React state) -> backend `GET /api/v1/processos?estado=` | The `estado` filter value crosses here as a query param. PRE-EXISTING and UNCHANGED by this phase — the same `draftEstado -> filters.estado` flow already ships; this phase only moves where the control renders. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-113-01 | Tampering / Injection | `estado` query param to `GET /processos` | accept | Pre-existing data flow, not introduced or widened here. Value is constrained client-side to the same 6 fixed `<option>`s; the backend scopes the processos query by tenant and treats `Processo.estado` as a free String (per 113-CONTEXT.md) — all already shipped and out of this phase's one-file diff. Relocating the control produces no new input path. |
| T-113-02 | Information Disclosure | Estado control now always-visible | accept | Making an existing filter always-visible changes zero authorization: the page is already RBAC-gated by `processos:view` (outer `AccessDeniedState`) and the list is already tenant-scoped server-side. No data is newly exposed by the control's position. |
| T-113-SC | Tampering (supply chain) | npm/pnpm installs | accept (N/A) | Zero package installs this phase. `NativeSelect` is already installed (Phase 105); zero `npx/pnpm shadcn add`; zero new dependencies. The Package Legitimacy Gate does not apply (no install tasks). |

No high-severity threat exists; `security_block_on: high` is not triggered.
</threat_model>

<verification>
- `cd web && pnpm exec tsc --noEmit && pnpm lint` — the single edited file typechecks and lints clean.
- Structural grep assertions (see acceptance_criteria) prove: the new `w-40 max-w-full` Estado wrapper exists exactly once and sits above the `advancedOpen` conditional; the advanced grid is rebalanced to three `lg:col-span-4` children with no `lg:col-span-3` remaining; the control is neither duplicated nor lost (`<NativeSelect` == 2, `value="TRIAGEM"` == 1); and the `draftEstado` / `estado: draftEstado.trim()` wiring is untouched.
- Manual reachability (for the phase verifier / optional live check): with the "Filtros" panel closed (its default), the Estado dropdown is visible between the search field and the "Filtros" button; selecting e.g. "Ativo" then "Aplicar" filters the list; "Limpar" resets it; opening "Filtros" shows Tribunal/Area/Cliente filling the row with no empty gap on large screens.
- No high-severity threat: the phase relocates already-reviewed markup and reuses the existing tenant-scoped `estado` filter without widening it.
</verification>

<success_criteria>
- PEST-01: the user filters the Processos list by estado through a dedicated control that is now always visible in the main filter bar (no longer hidden inside the collapsed advanced panel), working alongside the other filters without replacing them, and clearing returns to all processos.
- Zero logic/state/handler/backend change; `NativeSelect` reused unchanged; the advanced grid stays visually intact (3x`lg:col-span-4` = 12 cols); the page builds/lints clean.
</success_criteria>

<output>
Create `.planning/phases/LEXCV-113-processos-filtro-por-estado/113-01-SUMMARY.md` when done.
</output>
