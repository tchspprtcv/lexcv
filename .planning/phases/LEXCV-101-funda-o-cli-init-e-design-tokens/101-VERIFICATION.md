---
phase: LEXCV-101-funda-o-cli-init-e-design-tokens
verified: 2026-07-15T23:32:10Z
status: passed
score: 8/8 must-haves verified
overrides_applied: 0
---

# Phase 101: Fundação — CLI Init e Design Tokens Verification Report

**Phase Goal:** `web/` e `webpage/` têm uma fundação de design system corretamente inicializada (scaffolded pela CLI oficial, base Radix, tokenizada, com todos os primitivos que as fases seguintes vão precisar) sem que nenhuma página visível mude ainda.
**Verified:** 2026-07-15T23:32:10Z
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | FND-01: `web/components.json` exists, generated by `shadcn init -b radix` (Radix base, not Base UI), and the 9 pre-existing `@radix-ui/react-*` packages still compose via `asChild` without breaking any file | ✓ VERIFIED | `web/components.json` present with `style: "radix-vega"` (resolved from `-b radix`/`new-york` per UI-SPEC's documented legacy-value note), `tailwind.config: ""`, `tailwind.css: "src/app/globals.css"`, `baseColor: "neutral"`, `cssVariables: true`, `iconLibrary: "lucide"`. `button.tsx` still imports `Slot as SlotPrimitive from "radix-ui"` and uses `asChild ? SlotPrimitive.Slot : "button"` — composition intact. `pnpm build` in `web/` passes clean (24 routes generated, exit 0, verified live in this session, not just SUMMARY claim) |
| 2 | FND-02: `webpage/components.json` exists with config copied manually from `web/` (never an independent `init` wizard run) | ✓ VERIFIED | `webpage/components.json` is byte-identical to `web/components.json` on every field (`style`, `rsc`, `tsx`, `tailwind.{config,css,baseColor,cssVariables,prefix}`, `iconLibrary`, `aliases`, `menuColor`, `menuAccent`, `registries`) — diffed directly in this session |
| 3 | FND-03: `globals.css` in both apps carries the full shadcn semantic token set merged additively, in exactly one `:root`/`.dark` block, with `--background`/`--foreground` restored and `--radius`/`--primary` set deliberately | ✓ VERIFIED | `web/src/app/globals.css`: exactly 1 `:root` (line 51) and 1 `.dark` (line 86), `:root` declared before `.dark` (fixes the cascade tie-break bug found during 101-02's human sign-off). Contains `--background: #f8fafc`/`--foreground: #020617`/`--radius: 0rem`/`--primary: #2563eb` in `:root`, `--background: #020617`/`--foreground: #f8fafc`/`--primary: #3b82f6` in `.dark`, plus the full net-new token set (`--secondary`, `--muted`, `--accent`, `--destructive`, `--border`, `--input`, `--ring`, `--card`, `--popover`). `webpage/src/app/globals.css` mirrors this exactly (1 `:root` at line 50, 1 `.dark` at line 85, identical deliberate values) |
| 4 | FND-04/FND-06: The ~15 missing primitives exist in `web/src/components/ui/`, import/compile without error, and `react-day-picker` is pinned to exactly `9.14.0` | ✓ VERIFIED | All 16 files present (select, native-select, tabs, dropdown-menu, command, tooltip, checkbox, avatar, separator, skeleton, progress, calendar, breadcrumb, accordion, navigation-menu, empty) — each carries `data-slot`. `react-day-picker` resolves to exactly `9.14.0` in `package.json` (verified via `node -e`, not `^9.14.0` and not `10.x`). No `rounded-none` hardcode in `select.tsx`; the `rounded-none` occurrences that do exist are legitimate variant-specific cases in `calendar.tsx`/`tabs.tsx`/`input-group.tsx` (documented) or pre-existing in the 14 hand-rolled files (untouched by this phase's primitive-add tasks). `tsc --noEmit` reports only the 3 pre-existing, unrelated `vitest` errors (logged in `deferred-items.md`, confirmed unattributable to any of the 16 new files); `pnpm build` in `web/` passes clean |
| 5 | FND-05: All ui components (14 hand-rolled + 16 new) import Radix from the unified `radix-ui` package; no dual-tree bridge state remains | ✓ VERIFIED | `grep -rn "@radix-ui/react-" web/src` returns **zero** matches. `web/package.json` contains `"radix-ui": "^1.6.2"` and zero scoped `@radix-ui/react-*` entries. Spot-checked `alert-dialog.tsx`, `dialog.tsx`, `sheet.tsx`, `popover.tsx`, `switch.tsx`, `radio-group.tsx`, `label.tsx`, `button.tsx` — all import from `"radix-ui"` |
| 6 | FND-07: `tailwindcss-animate` removed and replaced by `tw-animate-css` in both apps | ✓ VERIFIED | `web/package.json` and `webpage/package.json` both list `tw-animate-css` (not `tailwindcss-animate`); both `globals.css` files contain `@import "tw-animate-css"` and zero occurrences of `tailwindcss-animate` |
| 7 | FND-08: Sonner's `<Toaster/>` mounted at `web/` root; `toast.tsx`/`toaster.tsx`/`@radix-ui/react-toast` fully removed; `toast.success`/`toast.error` call sites unchanged and still render "Sucesso"/"Erro" | ✓ VERIFIED | `web/src/components/ui/sonner.tsx` exists; `toast.tsx` and `toaster.tsx` are deleted (confirmed absent on disk); `@radix-ui/react-toast` absent from `package.json`. `use-toast.ts` imports `sonner`, calls `sonnerToast.success("Sucesso", ...)` / `sonnerToast.error("Erro", ...)`. `layout.tsx` imports `Toaster` from `@/components/ui/sonner` and mounts it once. `webpage/`'s clause correctly resolved not-applicable (zero toast/Toaster/sonner occurrences in `webpage/src`, grep-confirmed in this session) |
| 8 | No visible page changes shipped this phase | ✓ VERIFIED | `git diff --stat` against the pre-phase commit (`525eac9`) touches only: `components.json` (both apps), `globals.css` (both apps), `package.json`/`pnpm-lock.yaml` (both apps), `web/src/app/layout.tsx` (1-line Toaster import swap only), and files under `web/src/components/ui/*`/`web/src/hooks/use-toast.ts`. Zero files under `app/**/page.tsx` or `components/shared/*` touched |

**Score:** 8/8 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `web/components.json` | shadcn CLI config, Radix base | ✓ VERIFIED | Present, correct fields |
| `webpage/components.json` | Mirror of web/'s config | ✓ VERIFIED | Field-for-field identical |
| `web/src/app/globals.css` | Full token set, single :root/.dark | ✓ VERIFIED | 1 :root + 1 .dark, correct order/values |
| `webpage/src/app/globals.css` | Mirrored token set | ✓ VERIFIED | 1 :root + 1 .dark, matches web/ |
| 16 primitive files in `web/src/components/ui/` | select..empty | ✓ VERIFIED | All 16 present, each has data-slot |
| `web/src/components/ui/sonner.tsx` | Sonner Toaster wrapper | ✓ VERIFIED | Present, richColors enabled |
| `web/src/hooks/use-toast.ts` | Contract-preserving wrapper | ✓ VERIFIED | Delegates to sonner, preserves Sucesso/Erro |
| `web/package.json` | tw-animate-css, radix-ui, react-day-picker@9.14.0, no scoped radix, no react-toast | ✓ VERIFIED | All confirmed via direct read |
| `webpage/package.json` | tw-animate-css, no tailwindcss-animate | ✓ VERIFIED | Confirmed |

### Key Link Verification

| From | To | Via | Status | Details |
|------|-----|-----|--------|---------|
| `web/components.json` | `web/src/app/globals.css` | `tailwind.css` field | ✓ WIRED | `"css": "src/app/globals.css"` |
| `web/src/app/globals.css` | `tw-animate-css` | `@import` | ✓ WIRED | `@import "tw-animate-css"` present, no `tailwindcss-animate` |
| `web/src/app/layout.tsx` | `web/src/components/ui/sonner.tsx` | import + mount | ✓ WIRED | `import { Toaster } from "@/components/ui/sonner"`, `<Toaster />` rendered once |
| `web/src/hooks/use-toast.ts` | `sonner` | `toast.success`/`toast.error` delegation | ✓ WIRED | Titles "Sucesso"/"Erro" preserved verbatim |
| `web/src/components/ui/calendar.tsx` | `react-day-picker@9.14.0` | import + pinned version | ✓ WIRED | `package.json` resolves to exactly `9.14.0` |
| 14 hand-rolled + 16 new primitives | `radix-ui` unified package | import rewrite via `migrate radix` | ✓ WIRED | Zero `@radix-ui/react-*` imports remain anywhere in `web/src` |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| `web/` typechecks clean (only pre-existing unrelated errors) | `pnpm exec tsc --noEmit` (run live in this session) | 3 pre-existing `vitest` errors in Phase 97-02 test files only; zero new errors | ✓ PASS |
| `web/` production build passes | `pnpm build` (run live in this session) | Exit 0, all 30 routes generated (24 shown + auth/setup routes) | ✓ PASS |
| `webpage/` production build passes | `pnpm build` (run live in this session) | Exit 0, both routes generated | ✓ PASS |
| No dead scoped `@radix-ui/react-*` imports | `grep -rn "@radix-ui/react-" web/src` | Zero matches | ✓ PASS |
| No debt markers (TBD/FIXME/XXX) in phase-modified files | grep over phase's changed file list | Zero (one false-positive `XXX` inside a lockfile integrity hash, not a marker) | ✓ PASS |

### Requirements Coverage

| Requirement | Source Plan(s) | Description | Status | Evidence |
|---|---|---|---|---|
| FND-01 | 101-02 | `web/components.json` via `shadcn init -b radix` | ✓ SATISFIED | Truth 1 |
| FND-02 | 101-04 | `webpage/components.json` copied from web/ | ✓ SATISFIED | Truth 2 |
| FND-03 | 101-02, 101-04 | Semantic tokens in globals.css, both apps | ✓ SATISFIED | Truth 3 |
| FND-04 | 101-03 | ~15 missing primitives added | ✓ SATISFIED | Truth 4 |
| FND-05 | 101-05 | `radix-ui` unified, no dual state | ✓ SATISFIED | Truth 5 |
| FND-06 | 101-01, 101-03 | `react-day-picker` pinned 9.14.0 | ✓ SATISFIED | Truth 4 |
| FND-07 | 101-01, 101-02, 101-04 | `tailwindcss-animate` → `tw-animate-css` | ✓ SATISFIED | Truth 6 |
| FND-08 | 101-01, 101-04, 101-05 | Sonner adoption, webpage n/a | ✓ SATISFIED | Truth 7 |

All 8 declared requirement IDs (FND-01 through FND-08) are covered across the 5 plans; the union of each plan's frontmatter `requirements:` field exactly matches the phase's declared requirement list. No orphaned requirements found in REQUIREMENTS.md for Phase 101 beyond FND-01..FND-08. REQUIREMENTS.md itself marks all 8 as `[x]` / `Complete` — consistent with the codebase evidence above (not taken on faith; independently re-derived).

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `web/src/components/ui/sonner.tsx` | 42 | Dead `cn-toast` classNames override (no matching CSS rule anywhere in repo) | ℹ️ Info | Cosmetic no-op; carried over from `101-REVIEW.md` IN-02, not selected for the fix pass, does not affect goal achievement |
| `web/src/hooks/use-toast.ts` | 19-21, 40-45 | Unused `useToast()` hook / bare `toast()` wrapper (zero call sites) | ℹ️ Info | Intentional forward-compat shim per in-file comment (`101-REVIEW.md` IN-03); harmless |
| `web/src/components/ui/breadcrumb.tsx` / `button.tsx` | various | Inconsistent `Slot` aliasing style (`Slot.Root` vs `SlotPrimitive.Slot`) | ℹ️ Info | Cosmetic inconsistency from the `migrate radix` pass (`101-REVIEW.md` IN-04); both resolve to the same component |
| `web/src/components/ui/calendar.tsx` / `button.tsx` | various | `buttonVariants` CVA duplicated between the two files | ℹ️ Info | Documented, bounded duplication; explicitly deferred to Phase 102 reconciliation (`101-REVIEW.md` IN-05) |
| `web/package.json` | 27 | `"shadcn"` CLI package listed under `dependencies` instead of `devDependencies` | ℹ️ Info | Cosmetic dependency-hygiene issue (`101-REVIEW.md` IN-06); does not affect build or runtime |
| 7 hand-rolled files (`alert-dialog.tsx`, `dialog.tsx`, `sheet.tsx`, `popover.tsx`, `switch.tsx`, `radio-group.tsx`, `button.tsx`) | various | Two disconnected color systems (hardcoded neutral/slate palette + a magic `dark:bg-[#020617]` hex vs. the new semantic tokens) | ⚠️ Info (deliberately deferred) | This is `101-REVIEW-FIX.md`'s WR-02, deliberately NOT fixed in Phase 101 — confirmed by direct read of `ROADMAP.md` that these exact 7 (+`card`) files are the explicit subject matter of the next phase, "Phase 102: Reconciliação do Design System" (`ROADMAP.md:310-317`). This is a scheduled, named follow-up, not an unresolved gap in Phase 101's own goal — Phase 101's goal is only that the foundation exists and no visible page changes ship, which holds regardless of whether the 14 hand-rolled files are re-skinned onto semantic tokens (that reconciliation is explicitly Phase 102's job) |

No Critical or Blocker findings. All 6 code-review Info items (from `101-REVIEW.md`, re-reviewed after the fix pass in `101-REVIEW-FIX.md`) remain open by design — none affect whether Phase 101's own goal (foundation exists, compiles, builds, no visible page change) is achieved, and the one substantive item (WR-02 color-system duplication) is explicitly assigned to the next roadmapped phase rather than left untracked.

### Human Verification Required

None. Both blocking human-verify checkpoints required by this phase's plans (101-02 Task 3 for `web/`, 101-04 Task 3 for `webpage/`) were already executed during the phase and recorded specific, non-rubric-stamp verdicts in their respective SUMMARY.md files:
- 101-02: found and required a real fix (CSS cascade tie-break bug — `.dark` declared before `:root`), re-verified via live `getComputedStyle` checks after the fix, then approved. Independently re-confirmed in this verification pass that `:root` is declared before `.dark` in the current file.
- 101-04: approved on the first attempt with a verbatim recorded verdict quoting exact computed background/foreground/primary values in both light and dark mode, matching `web/` with no drift.

Both verdicts are corroborated by the artifact-level evidence gathered independently in this verification pass (correct hex values, correct block order, single-block token merge, clean builds) — no further human action is needed for this phase's goal to be considered achieved.

### Gaps Summary

No gaps. All 8 FND requirements are satisfied with codebase evidence gathered independently in this verification session (not solely from SUMMARY.md claims): both apps' `components.json`/`globals.css` inspected directly, both production builds run live and confirmed passing, `tsc --noEmit` run live, all 16 primitives confirmed present with `data-slot`, zero remaining scoped `@radix-ui/react-*` imports confirmed via repo-wide grep, `react-day-picker` version confirmed pinned via direct `package.json` read, Sonner contract (`Sucesso`/`Erro`) confirmed in `use-toast.ts`, and zero visible-page-file changes confirmed via `git diff --stat` against the pre-phase commit. The 6 Info-level code-review findings and the deliberately-deferred WR-02 item do not block phase-goal achievement — WR-02 is explicitly assigned to the next roadmapped phase (102), and Phase 101's own goal statement ("sem que nenhuma página visível mude ainda") holds because zero page files were touched.

---

*Verified: 2026-07-15T23:32:10Z*
*Verifier: Claude (gsd-verifier)*
