---
phase: 101-funda-o-cli-init-e-design-tokens
plan: 03
type: execute
wave: 3
depends_on: [101-02]
files_modified:
  - web/src/components/ui/select.tsx
  - web/src/components/ui/native-select.tsx
  - web/src/components/ui/tabs.tsx
  - web/src/components/ui/dropdown-menu.tsx
  - web/src/components/ui/command.tsx
  - web/src/components/ui/tooltip.tsx
  - web/src/components/ui/checkbox.tsx
  - web/src/components/ui/avatar.tsx
  - web/src/components/ui/separator.tsx
  - web/src/components/ui/skeleton.tsx
  - web/src/components/ui/progress.tsx
  - web/src/components/ui/calendar.tsx
  - web/src/components/ui/breadcrumb.tsx
  - web/src/components/ui/accordion.tsx
  - web/src/components/ui/navigation-menu.tsx
  - web/src/components/ui/empty.tsx
  - web/package.json
  - web/pnpm-lock.yaml
autonomous: true
requirements: [FND-04, FND-06]
must_haves:
  truths:
    - "All 16 missing primitives exist in web/src/components/ui/, the whole web/ project typechecks clean (`tsc --noEmit`), AND `pnpm build` passes in web/"
    - "react-day-picker resolves to exactly 9.14.0 (not the broken v10 line) after `shadcn add calendar`"
    - "None of the 14 pre-existing hand-rolled ui primitives were overwritten by the add pass (button/dialog/input/etc. unchanged)"
    - "Each new primitive carries its data-slot attribute and drives corners from the --radius token (no hardcoded rounded-none)"
  artifacts:
    - path: "web/src/components/ui/select.tsx"
      provides: "Radix Select primitive"
      contains: "data-slot"
    - path: "web/src/components/ui/calendar.tsx"
      provides: "react-day-picker Calendar primitive"
    - path: "web/src/components/ui/command.tsx"
      provides: "cmdk Command palette primitive"
    - path: "web/package.json"
      provides: "react-day-picker pinned to 9.14.0"
      contains: "9.14.0"
  key_links:
    - from: "web/src/components/ui/calendar.tsx"
      to: "react-day-picker@9.14.0"
      via: "import DayPicker; version pinned in package.json"
      pattern: "react-day-picker"
    - from: "new primitives"
      to: "radix-ui / cmdk"
      via: "import from the base declared in components.json"
      pattern: "from \"(radix-ui|cmdk|@radix-ui)"
---

<objective>
Add every primitive the module phases (102-110) will consume — Select, NativeSelect, Tabs, DropdownMenu, Command, Tooltip, Checkbox, Avatar, Separator, Skeleton, Progress, Calendar, Breadcrumb, Accordion, NavigationMenu, Empty — via the official shadcn CLI, and immediately pin `react-day-picker` to `9.14.0` so Calendar does not resolve to the broken v10 line. No existing hand-rolled primitive is touched; no visible page changes ship.

Purpose: Covers FND-04 (~15 missing primitives added via CLI in `web/`) and FND-06 (`react-day-picker` pinned to `9.14.0` immediately after `add calendar`).
Output: 16 net-new files in `web/src/components/ui/` and an updated `web/package.json` with `react-day-picker@9.14.0`.
</objective>

<execution_context>
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/LEXCV-101-funda-o-cli-init-e-design-tokens/101-UI-SPEC.md
@.planning/phases/LEXCV-101-funda-o-cli-init-e-design-tokens/101-PATTERNS.md
@.planning/research/STACK.md
@.planning/research/PITFALLS.md

<interfaces>
The 14 existing hand-rolled files in web/src/components/ui/ that MUST NOT be overwritten this phase (reconciliation is Phase 102): alert-dialog.tsx, badge.tsx, button.tsx, card.tsx, dialog.tsx, input.tsx, label.tsx, popover.tsx, radio-group.tsx, sheet.tsx, switch.tsx, table.tsx, textarea.tsx, toast.tsx (toast.tsx/toaster.tsx are removed in 101-05, not here).

The 16 add targets (from 101-UI-SPEC.md Registry Safety + FND-04): select, native-select, tabs, dropdown-menu, command, tooltip, checkbox, avatar, separator, skeleton, progress, calendar, breadcrumb, accordion, navigation-menu, empty.

Structural conventions every new file must match (from 101-PATTERNS.md Shared Patterns): `data-slot="<name>"` on the root element; `cn()` from @/lib/utils; CVA for variant-bearing primitives; the Radix compound Portal+Overlay+Content idiom for overlay primitives (select/dropdown-menu/tooltip/accordion) reusing the `data-[state=open]:animate-in` vocabulary now provided by tw-animate-css; corners driven by semantic `rounded-*` (which the --radius:0rem token from 101-02 renders sharp) — never a hardcoded `rounded-none`.

OUT OF SCOPE this phase: `form` (shadcn Form wrapper) — it is NOT in the UI-SPEC 16-primitive list; do not add it (prevents scope creep; STACK.md flags a zod v4 compat check that belongs to whichever module phase actually needs it).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the 15 non-Calendar primitives via the shadcn CLI</name>
  <files>web/src/components/ui/select.tsx, web/src/components/ui/native-select.tsx, web/src/components/ui/tabs.tsx, web/src/components/ui/dropdown-menu.tsx, web/src/components/ui/command.tsx, web/src/components/ui/tooltip.tsx, web/src/components/ui/checkbox.tsx, web/src/components/ui/avatar.tsx, web/src/components/ui/separator.tsx, web/src/components/ui/skeleton.tsx, web/src/components/ui/progress.tsx, web/src/components/ui/breadcrumb.tsx, web/src/components/ui/accordion.tsx, web/src/components/ui/navigation-menu.tsx, web/src/components/ui/empty.tsx, web/package.json, web/pnpm-lock.yaml</files>
  <scope_note>
    This task's file list is large (15 primitives + package.json + lockfile = 17 paths), which nominally crosses the "10+ files in one task" red flag. That classification does NOT apply here: every one of these files is CLI-generated boilerplate produced by a SINGLE non-interactive `shadcn add` invocation — none is hand-authored, and the executor writes zero lines of component code. The context cost is one CLI run plus a spot-check of 2-3 generated files, well under the split threshold. Kept as one task deliberately because splitting a single atomic `shadcn add` call buys nothing.
  </scope_note>
  <read_first>
    - web/components.json (confirm Radix base is recorded so `add` scaffolds Radix, not Base UI)
    - web/src/components/ui/dialog.tsx and web/src/components/ui/popover.tsx (the overlay-idiom analogs new files should structurally match — Portal/Overlay/Content, data-[state] animation, sr-only "Fechar")
    - web/src/components/ui/switch.tsx (Radix Root+Indicator analog for checkbox/progress)
    - .planning/research/STACK.md (Supporting Libraries — exact package/version each primitive pulls; "If adding a genuinely new component" guidance)
    - .planning/research/PITFALLS.md (Pitfall 2 — never blind-overwrite an existing file; Pitfall 8 — Command/NavigationMenu are net-new and warrant a first-time keyboard/ARIA smoke test)
  </read_first>
  <action>
    Re-verify each component name is still in the official shadcn registry at execution (`native-select` and `empty` are newer additions — confirm via `pnpm dlx shadcn@latest add --help` or a `--dry-run`). From inside `web/`, run `pnpm dlx shadcn@latest add select native-select tabs dropdown-menu command tooltip checkbox avatar separator skeleton progress breadcrumb accordion navigation-menu empty` non-interactively. These 15 names do NOT collide with any of the 14 existing hand-rolled files, so they are pure adds. CRITICAL: if the CLI reports that a registry DEPENDENCY of one of these (e.g. `command` pulls `dialog`, or a component pulls `button`/`input`) would overwrite an EXISTING hand-rolled file, DECLINE that overwrite — answer "no"/skip, never pass `--overwrite` (PITFALLS Pitfall 2; those 14 files are reconciled in Phase 102). Only the 15 net-new files above may be written. After the add, open 2-3 of the generated overlay files (select.tsx, dropdown-menu.tsx) and confirm they (a) carry `data-slot`, (b) import from the base declared in components.json, (c) use semantic `rounded-*` (NOT a hardcoded `rounded-none`), and (d) reuse the `data-[state=open]:animate-in` class vocabulary. Do NOT add `form` (out of scope per `<interfaces>`).
  </action>
  <verify>
    <automated>cd web && for f in select native-select tabs dropdown-menu command tooltip checkbox avatar separator skeleton progress breadcrumb accordion navigation-menu empty; do test -f "src/components/ui/$f.tsx" || { echo "MISSING $f"; exit 1; }; done && grep -Lq 'data-slot' src/components/ui/select.tsx src/components/ui/dropdown-menu.tsx src/components/ui/tabs.tsx; test $(git status --porcelain src/components/ui/button.tsx src/components/ui/dialog.tsx src/components/ui/input.tsx src/components/ui/badge.tsx | wc -l) -eq 0 && echo "15 primitives added; existing hand-rolled files untouched"</automated>
  </verify>
  <acceptance_criteria>
    - All 15 files exist: select.tsx, native-select.tsx, tabs.tsx, dropdown-menu.tsx, command.tsx, tooltip.tsx, checkbox.tsx, avatar.tsx, separator.tsx, skeleton.tsx, progress.tsx, breadcrumb.tsx, accordion.tsx, navigation-menu.tsx, empty.tsx.
    - `git status --porcelain` shows ZERO modifications to any of the 14 pre-existing ui files (button, dialog, input, badge, card, table, sheet, alert-dialog, label, popover, radio-group, switch, textarea, toast/toaster).
    - `grep 'rounded-none' src/components/ui/select.tsx` returns nothing (corners come from the --radius token, not a hardcode).
    - select.tsx and dropdown-menu.tsx each contain `data-slot`.
    - `web/src/components/ui/form.tsx` was NOT created.
  </acceptance_criteria>
  <done>The 15 non-Calendar primitives are scaffolded on the Radix base with data-slot/CVA conventions intact, and none of the 14 existing hand-rolled files were overwritten.</done>
</task>

<task type="auto">
  <name>Task 2: Add Calendar and immediately pin react-day-picker to 9.14.0</name>
  <files>web/src/components/ui/calendar.tsx, web/package.json, web/pnpm-lock.yaml</files>
  <read_first>
    - web/src/components/ui/button.tsx (calendar day-cells reuse buttonVariants — confirm the existing signature the generated calendar imports)
    - .planning/research/STACK.md ("What NOT to Use" — react-day-picker@latest is broken v10, issue #10914; pin 9.14.0 immediately after add; date-fns pulled automatically)
    - .planning/research/PITFALLS.md (react-day-picker pin rationale)
  </read_first>
  <action>
    From inside `web/`, run `pnpm dlx shadcn@latest add calendar`. The calendar registry item lists `button` as a dependency — if the CLI offers to overwrite the existing hand-rolled `web/src/components/ui/button.tsx`, DECLINE it (skip/"no"); only `calendar.tsx` (and the auto-installed `react-day-picker` + `date-fns` deps) may be written. IMMEDIATELY after the add, run `pnpm add react-day-picker@9.14.0` to force the exact pin (the registry manifest resolves to `@latest` = broken v10 per STACK.md/#10914). Confirm `web/package.json` now lists `react-day-picker` at exactly `9.14.0` (not `^9.14.0` that could float, and not any `10.x`). Verify `date-fns` was installed (calendar depends on it) and that `calendar.tsx` imports `DayPicker` from `react-day-picker`.
  </action>
  <verify>
    <automated>cd web && test -f src/components/ui/calendar.tsx && node -e "const p=require('./package.json'); const v=(p.dependencies['react-day-picker']||'').replace(/[^0-9.]/g,''); if(v!=='9.14.0'){console.error('react-day-picker is '+p.dependencies['react-day-picker']+', expected 9.14.0');process.exit(1)}; if(!p.dependencies['date-fns']){console.error('date-fns missing');process.exit(1)}; console.log('calendar OK; react-day-picker pinned 9.14.0; date-fns present')" && pnpm ls react-day-picker 2>/dev/null | grep -q '9.14.0' && test $(git status --porcelain src/components/ui/button.tsx | wc -l) -eq 0 && echo "calendar pinned + button untouched"</automated>
  </verify>
  <acceptance_criteria>
    - `web/src/components/ui/calendar.tsx` exists and imports from `react-day-picker`.
    - `web/package.json` `dependencies["react-day-picker"]` resolves to exactly `9.14.0` (no `10.x`), and `date-fns` is present.
    - `pnpm ls react-day-picker` shows an installed `9.14.0` (the lockfile pin held).
    - `web/src/components/ui/button.tsx` is unchanged (`git status --porcelain` empty for it).
  </acceptance_criteria>
  <done>Calendar is scaffolded and react-day-picker is pinned to exactly 9.14.0 in both package.json and the installed tree, with the hand-rolled button.tsx left untouched.</done>
</task>

<task type="auto">
  <name>Task 3: Typecheck + production build gate — all new primitives compile and build against the project</name>
  <files>web/src/components/ui/*.tsx</files>
  <read_first>
    - web/tsconfig.json (confirm noEmit:true so `tsc --noEmit` is a pure typecheck)
    - web/src/lib/utils.ts (cn() the new files import)
  </read_first>
  <action>
    Run the whole-project typecheck to prove every one of the 16 net-new primitive files compiles and resolves its imports (Radix / cmdk / react-day-picker / date-fns / cn) with zero new errors. Then run `pnpm build` from inside `web/` to prove the 16 additions also survive a real production build (bundling, tree-shaking, the tw-animate-css `data-[state=open]:animate-in` utilities the overlay primitives depend on) — `tsc --noEmit` alone does NOT exercise the bundler, and the generic orchestrator post-merge gate silently skips (workflow.build_command is unset, no root workspace). If any new primitive fails to typecheck or build (e.g. an import path the CLI generated for the wrong base, or a missing dep), fix the import/dep for that primitive only — do NOT modify any of the 14 existing hand-rolled files to make an error disappear. Do not add `eslint-disable` blanket suppressions to hide real type errors.
  </action>
  <verify>
    <automated>cd web && pnpm exec tsc --noEmit && pnpm build && echo "web/ typechecks clean AND production-builds clean with all 16 new primitives"</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm exec tsc --noEmit` in `web/` exits 0 (no type errors).
    - `pnpm build` in `web/` exits 0 (the 16 new primitives survive a real production build, not just a typecheck).
    - The 16 primitive files (select, native-select, tabs, dropdown-menu, command, tooltip, checkbox, avatar, separator, skeleton, progress, calendar, breadcrumb, accordion, navigation-menu, empty) are all present and included in the successful typecheck/build.
    - No `web/src/components/ui/*` file among the pre-existing 14 was modified to achieve the passing typecheck/build.
  </acceptance_criteria>
  <done>The entire web/ project typechecks clean AND production-builds clean with all 16 new primitives added — proving FND-04 is import-complete, compile-safe, and bundle-safe.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev/CI ← npm registry | `shadcn add` installs new @radix-ui/react-*, cmdk, react-day-picker, date-fns |
| shadcn registry → web/src/components/ui/ | CLI writes 16 new component source files into the repo |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-101-SC | Tampering | packages installed by `shadcn add` (cmdk, react-day-picker, date-fns, scoped @radix-ui/react-*) | mitigate | Gated by the 101-01 blocking-human legitimacy checkpoint; official shadcn registry only |
| T-101-DP | Tampering / Availability | react-day-picker version drift to broken v10 | mitigate | Explicit `pnpm add react-day-picker@9.14.0` pin verified in package.json AND installed tree (issue #10914) |
| T-101-OW | Tampering | accidental overwrite of the 14 hand-rolled primitives | mitigate | Never pass `--overwrite`; decline any registry-dependency overwrite prompt; `git status` asserts the 14 files unchanged |
| — | Spoofing / Repudiation / Info-Disclosure / DoS / Elevation | n/a | accept | Adding unwired primitive files introduces no endpoints/auth/input/data flow — zero new runtime attack surface until a module phase consumes them |
</threat_model>

<verification>
- 16 new primitive files present in web/src/components/ui/.
- react-day-picker pinned to exactly 9.14.0 (package.json + `pnpm ls`).
- The 14 pre-existing hand-rolled files unchanged.
- `pnpm exec tsc --noEmit` passes.
- `pnpm build` passes in web/ (Task 3 automated gate).
</verification>

<success_criteria>
- FND-04 and FND-06 satisfied: all ~15 missing primitives compile AND production-build in `web/`, Calendar is pinned to the working react-day-picker 9.14.0, and no existing component was disturbed.
</success_criteria>

<output>
Create `.planning/phases/LEXCV-101-funda-o-cli-init-e-design-tokens/101-03-SUMMARY.md` when done. Note any primitive whose registry name differed at execution time and any dependency-overwrite prompt that was declined.
</output>
</output>
