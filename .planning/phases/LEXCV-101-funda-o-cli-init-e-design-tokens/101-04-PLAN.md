---
phase: 101-funda-o-cli-init-e-design-tokens
plan: 04
type: execute
wave: 3
depends_on: [101-02]
files_modified:
  - webpage/components.json
  - webpage/src/app/globals.css
  - webpage/package.json
  - webpage/pnpm-lock.yaml
autonomous: false
requirements: [FND-02, FND-03, FND-07, FND-08]
must_haves:
  truths:
    - "webpage/components.json exists and matches web/components.json field-for-field (style, baseColor, cssVariables, iconLibrary, aliases, tailwind.config, tailwind.css) — created by hand-copy, NOT an independent init wizard run"
    - "webpage/src/app/globals.css carries the identical semantic token block to web/'s, in exactly one :root and one .dark, with restored --background/--foreground and deliberate --radius:0rem / --primary blue"
    - "webpage/ animation utilities load via `@import \"tw-animate-css\"`; tailwindcss-animate removed from globals.css and package.json"
    - "webpage/ mounts NO Toaster: it has zero toast call sites (grep-verified), so FND-08's 'e webpage/ se aplicável' resolves to not-applicable"
    - "`pnpm build` passes in webpage/ after the token merge (the mirror did not break the production build — PITFALLS Pitfall 1 explicit mitigation), AND a human has visually confirmed webpage/ light+dark render matches the 101-UI-SPEC.md Color values / Figma reference before the plan is considered done"
  artifacts:
    - path: "webpage/components.json"
      provides: "shadcn CLI config for webpage/, mirrored from web/"
      contains: "\"$schema\""
    - path: "webpage/src/app/globals.css"
      provides: "Full semantic token layer + tw-animate-css import, in lockstep with web/"
      contains: "--primary"
    - path: "webpage/package.json"
      provides: "tw-animate-css dependency; tailwindcss-animate removed"
      contains: "tw-animate-css"
  key_links:
    - from: "webpage/components.json"
      to: "web/components.json"
      via: "field-for-field identical (except nothing app-specific differs today)"
      pattern: "\"baseColor\": \"neutral\""
    - from: "webpage/src/app/globals.css"
      to: "web/src/app/globals.css"
      via: "identical token block kept in manual lockstep"
      pattern: "--radius: 0rem"
---

<objective>
Give `webpage/` the same design-system foundation as `web/` — a `components.json` and a fully-tokenized `globals.css` — WITHOUT running a second independent `shadcn init` wizard (which would risk the two apps' answers drifting). The config and token block are hand-copied from `web/`'s now-settled result. Also swap `tailwindcss-animate` for `tw-animate-css`. No visible page changes ship. This plan touches only `webpage/` files and runs in parallel with 101-03.

Purpose: Covers FND-02 (`webpage/components.json` from web/'s copied answers), the `webpage/` half of FND-03 (semantic tokens in `webpage/globals.css`) and FND-07 (animate swap), and resolves the `webpage/` clause of FND-08 ("e webpage/ se aplicável") — which is NOT applicable because webpage has zero toast usage.
Output: `webpage/components.json`, an extended `webpage/src/app/globals.css`, updated `webpage/package.json`.
</objective>

<execution_context>
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/LEXCV-101-funda-o-cli-init-e-design-tokens/101-UI-SPEC.md
@.planning/phases/LEXCV-101-funda-o-cli-init-e-design-tokens/101-PATTERNS.md
@.planning/research/STACK.md
@.planning/research/PITFALLS.md

<interfaces>
Source of truth to copy from (produced by 101-02): web/components.json and the final merged token block in web/src/app/globals.css. Both webpage/ files are byte-identical to web/'s ORIGINAL 2-variable globals.css today, and webpage/tsconfig.json already uses "@/*": ["./src/*"] — so the same components.json aliases resolve with zero remapping.

Deliberate token values (must match web/ exactly — from 101-UI-SPEC.md / 101-PATTERNS.md):
  :root → --background:#f8fafc; --foreground:#020617; --radius:0rem; --primary:#2563eb; --primary-foreground:#ffffff
  .dark → --background:#020617; --foreground:#f8fafc; --primary:#3b82f6
Net-new CLI tokens (--secondary/-muted/-accent/-destructive/-border/-input/-ring/-card/-popover + their -foreground pairs): keep the same neutral-baseColor defaults web/ ended with.

FND-08 disposition for webpage/: webpage/src/ has ZERO occurrences of toast/Toaster/sonner (grep-verified: no files match). Therefore no `<Toaster />` is mounted in webpage/src/app/layout.tsx — this is the correct reading of "se aplicável" (if applicable), not a gap. Do NOT invent toast call sites or mount an unused Toaster.

webpage/ is NOT part of FND-04 (primitives are web/-only) and NOT part of FND-05 (migrate radix targets web/'s dual-package risk; webpage's only Radix usage is @radix-ui/react-slot in button.tsx, no new primitives added, so no dual-state exists). Do not run `shadcn add` or `shadcn migrate radix` in webpage/ this phase.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create webpage/components.json by mirroring web/'s resolved config</name>
  <files>webpage/components.json</files>
  <read_first>
    - web/components.json (the source of truth produced by 101-02 — the exact field values to copy)
    - webpage/tsconfig.json (confirm "@/*": ["./src/*"] so the copied aliases resolve unchanged)
    - webpage/src/lib/utils.ts (confirm canonical cn() exists so utils alias is valid)
    - .planning/research/PITFALLS.md (Pitfall 5 — two independent init runs drift; copy, do not re-wizard)
    - .planning/research/STACK.md ("If working in webpage/" — same -b radix choice, fewer components)
  </read_first>
  <action>
    Do NOT run `pnpm dlx shadcn@latest init` in webpage/. Instead, create `webpage/components.json` by copying `web/components.json` verbatim (it is app-agnostic: same $schema, style=new-york, rsc=true, tsx=true, tailwind.config="" , tailwind.css="src/app/globals.css", baseColor=neutral, cssVariables=true, aliases {components/utils/ui/lib/hooks}, iconLibrary=lucide). webpage has no hooks/ dir yet and that is fine — the aliases entry is harmless until a hook is added. After writing, diff the two files field-by-field to confirm zero divergence on style, baseColor, cssVariables, iconLibrary, aliases, tailwind.config, tailwind.css (Pitfall 5 / UI-SPEC FND-02).
  </action>
  <verify>
    <automated>test -f webpage/components.json && node -e "const a=require('./web/components.json'),b=require('./webpage/components.json'); const keys=['style','rsc','tsx','iconLibrary']; for(const k of keys){if(JSON.stringify(a[k])!==JSON.stringify(b[k])){console.error('DIVERGENCE '+k+': '+a[k]+' vs '+b[k]);process.exit(1)}} const tw=['config','css','baseColor','cssVariables']; for(const k of tw){if(JSON.stringify(a.tailwind[k])!==JSON.stringify(b.tailwind[k])){console.error('DIVERGENCE tailwind.'+k);process.exit(1)}} if(JSON.stringify(a.aliases)!==JSON.stringify(b.aliases)){console.error('DIVERGENCE aliases');process.exit(1)} console.log('webpage/components.json matches web/ field-for-field')"</automated>
  </verify>
  <acceptance_criteria>
    - `webpage/components.json` exists.
    - It matches `web/components.json` field-for-field on style, rsc, tsx, iconLibrary, aliases, and tailwind.{config,css,baseColor,cssVariables} (the automated diff exits 0).
    - `webpage/components.json` `tailwind.config` is the empty string `""` and `tailwind.css` is `src/app/globals.css`.
    - No `shadcn init` was run in webpage/ (git shows only components.json added, no CLI-side lib/utils or layout churn).
  </acceptance_criteria>
  <done>webpage/components.json exists as an exact mirror of web/'s resolved config, created by hand-copy (never a second wizard run), so the two apps cannot silently fork.</done>
</task>

<task type="auto">
  <name>Task 2: Mirror the token block into webpage/globals.css + animate swap; confirm no Toaster needed; prove the build</name>
  <files>webpage/src/app/globals.css, webpage/package.json, webpage/pnpm-lock.yaml</files>
  <read_first>
    - web/src/app/globals.css (the final merged token block from 101-02 — the exact source to mirror)
    - webpage/src/app/globals.css (current 2-variable file to extend)
    - webpage/src/app/layout.tsx (confirm it mounts no Toaster today; it uses bg-background/text-foreground already)
    - .planning/phases/LEXCV-101-funda-o-cli-init-e-design-tokens/101-UI-SPEC.md (Color section — identical hex values across both apps)
    - .planning/research/PITFALLS.md (Pitfall 1 — single :root/.dark AND `pnpm build` + light/dark visual diff before the merge is called done; Pitfall 5 — lockstep)
  </read_first>
  <action>
    Extend `webpage/src/app/globals.css` so its `@theme inline`, `:root`, and `.dark` blocks carry the SAME merged semantic token set web/ ended 101-02 with — copy the token block from `web/src/app/globals.css`. Ensure exactly ONE `:root` block and ONE `.dark` block (PITFALLS Pitfall 1). Set the deliberate values identically: `:root` gets `--background:#f8fafc; --foreground:#020617; --radius:0rem; --primary:#2563eb; --primary-foreground:#ffffff`; `.dark` gets `--background:#020617; --foreground:#f8fafc; --primary:#3b82f6`. Keep the CLI's neutral defaults for the net-new tokens, matching web/. Perform the animate swap (FND-07): replace `@plugin "tailwindcss-animate";` with `@import "tw-animate-css";`, then `pnpm remove tailwindcss-animate` and `pnpm add tw-animate-css@1.4.0` inside webpage/. Leave the `@custom-variant dark (&:is(.dark *));` line unchanged. Then confirm FND-08 is not applicable to webpage/: grep `webpage/src` for `toast`/`Toaster`/`sonner` — it must return nothing; record in the SUMMARY that no `<Toaster />` is mounted because webpage has zero toast call sites (do NOT add sonner or a Toaster to webpage/). FINALLY, run `pnpm build` from inside `webpage/` and confirm it exits 0 — this is PITFALLS Pitfall 1's explicit "did the token merge break the build" gate applied to webpage/ specifically, and the generic orchestrator post-merge gate will NOT catch it (workflow.build_command is unset and web/ and webpage/ are independent apps with no root workspace). If the build fails on a token/import the mirror introduced, fix it here before finishing the task.
  </action>
  <verify>
    <automated>cd webpage && test $(grep -c '^:root' src/app/globals.css) -eq 1 && test $(grep -c '^.dark' src/app/globals.css) -eq 1 && grep -q -- '--radius: 0rem' src/app/globals.css && grep -q -- '--primary: #2563eb' src/app/globals.css && grep -q -- '--background: #f8fafc' src/app/globals.css && grep -q '@import "tw-animate-css"' src/app/globals.css && ! grep -q 'tailwindcss-animate' src/app/globals.css && ! grep -q '"tailwindcss-animate"' package.json && grep -q '"tw-animate-css"' package.json && test -z "$(grep -rIl -e toast -e Toaster -e sonner src 2>/dev/null)" && pnpm build && echo "webpage tokens+animate OK; no toast usage (FND-08 n/a); production build passes"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^:root' webpage/src/app/globals.css` === 1 and `grep -c '^.dark' webpage/src/app/globals.css` === 1.
    - webpage/globals.css contains `--radius: 0rem`, `--primary: #2563eb`, `--background: #f8fafc` (and the .dark `--primary: #3b82f6`), identical to web/.
    - webpage/globals.css contains `@import "tw-animate-css"` and zero `tailwindcss-animate`; webpage/package.json lists `tw-animate-css`, not `tailwindcss-animate`.
    - `grep -rIl -e toast -e Toaster -e sonner webpage/src` returns nothing — confirming no Toaster is needed; SUMMARY records FND-08 as not-applicable for webpage/.
    - `@custom-variant dark (&:is(.dark *));` present and unchanged.
    - `pnpm build` in `webpage/` exits 0 — the token merge did not break the production build (PITFALLS Pitfall 1 mitigation; the generic post-merge gate silently skips because no build_command/root workspace exists).
  </acceptance_criteria>
  <done>webpage/globals.css carries the identical semantic token block to web/ in a single :root/.dark, animations load via tw-animate-css, the FND-08 webpage clause is correctly resolved as not-applicable (zero toast usage, no Toaster mounted), and `pnpm build` passes clean.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human visual sign-off — webpage/ light+dark theme matches the reference and web/</name>
  <files>.planning/phases/LEXCV-101-funda-o-cli-init-e-design-tokens/101-04-SUMMARY.md</files>
  <action>
    Serve webpage/ for visual inspection: from inside `webpage/` run `pnpm build` (already gated green by Task 2) then `pnpm start` to serve the production output (or `pnpm dev`). Open the landing page in a browser and exercise BOTH light and dark mode per `<how-to-verify>`, ideally side-by-side with web/ (the dashboard) in the same mode so drift is obvious. Then PAUSE for the human verdict — do NOT auto-advance. This is a blocking visual sign-off: PITFALLS Pitfall 1 explicitly requires the light+dark visual diff to be performed on web/ AND webpage/ SEPARATELY, and Pitfall 5 requires webpage/ to read identically to web/. Record the human verdict (approved / discrepancy) in the 101-04 SUMMARY.
  </action>
  <what-built>
    Task 1 mirrored web/'s components.json into webpage/ and Task 2 mirrored the merged token block into webpage/globals.css (single `:root`/`.dark`, restored `--background`/`--foreground`, `--radius: 0rem`, `--primary` blue), swapped in `tw-animate-css`, and proved `pnpm build` passes. This checkpoint is PITFALLS Pitfall 1's mandated final step, which explicitly requires the light+dark visual diff to be done "on both web/ and webpage/ separately" — `pnpm build` proves webpage/ COMPILES, but only a human eye confirms the landing page's theme still LOOKS right AND matches the dashboard (web/) identity (Pitfall 5's drift-between-apps risk). It exists because this repo has no automated visual-regression coverage (PROJECT.md).
  </what-built>
  <how-to-verify>
    1. From inside `webpage/`, start the app: `pnpm dev` (or `pnpm build && pnpm start` to verify production output).
    2. Open the landing page. Confirm the LIGHT theme renders: background `#f8fafc`, text `#020617`, SHARP corners (`--radius: 0rem`).
    3. Toggle to DARK mode. Confirm background `#020617`, foreground `#f8fafc`, and institutional `--primary` blue on accent surfaces only. See 101-UI-SPEC.md "Color" for the exact hex values.
    4. Compare webpage/ side-by-side with web/ (the dashboard) in the SAME mode — the two must read as the same design system (identical background, foreground, radius, primary blue). This is the Pitfall 5 "close but not quite" drift check.
    5. Compare both against the Figma reference. If any shade, corner radius, or primary tint differs from web/ or the reference, describe it so Task 2 can be corrected before this plan is marked done.
  </how-to-verify>
  <verify>
    <human-check>Human confirms webpage/ LIGHT and DARK themes both match the 101-UI-SPEC.md Color values / Figma reference (background #f8fafc/#020617, foreground #020617/#f8fafc, sharp corners, --primary blue on reserved accents) AND read identically to web/ side-by-side — no Pitfall-5 cross-app drift.</human-check>
  </verify>
  <resume-signal>Type "approved" once webpage/ light+dark visually match the reference and web/, or describe the visual discrepancy to fix.</resume-signal>
  <done>A human has visually confirmed webpage/'s light AND dark themes match the 101-UI-SPEC.md Color values / Figma reference AND read identically to web/ — closing PITFALLS Pitfall 1's per-app visual sign-off and Pitfall 5's no-drift check for webpage/.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev/CI ← npm registry | `pnpm add tw-animate-css` in webpage/ |
| hand-copy web/ → webpage/ | config/token drift risk between two independent apps |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-101-SC | Tampering | tw-animate-css install in webpage/ | mitigate | Same package already cleared by the 101-01 legitimacy gate (this plan depends transitively on 101-01) |
| T-101-TH | Tampering | webpage/globals.css theme integrity | mitigate | Single :root/.dark enforced via grep gate; token values copied from web/'s reviewed result, not re-derived; `pnpm build` gate + human light+dark visual sign-off (Task 3) |
| T-101-DR | Tampering | web/↔webpage/ config drift | mitigate | components.json copied (not re-wizarded) + field-for-field diff gate + human side-by-side visual comparison against web/ (Task 3) (PITFALLS Pitfall 5) |
| — | Spoofing / Repudiation / Info-Disclosure / DoS / Elevation | n/a | accept | No endpoints/auth/input/data; webpage is a static public landing app and no runtime surface changes |
</threat_model>

<verification>
- webpage/components.json matches web/'s field-for-field.
- webpage/globals.css: single :root/.dark, identical deliberate token values, `@import "tw-animate-css"`, no tailwindcss-animate.
- webpage has zero toast usage → no Toaster mounted (FND-08 n/a, documented).
- `pnpm build` passes in webpage/ (Task 2 automated gate).
- Human has signed off that webpage/ light+dark visually match the reference and web/ (Task 3 checkpoint).
</verification>

<success_criteria>
- FND-02, the webpage/ half of FND-03 and FND-07, and the webpage/ clause of FND-08 satisfied: webpage mirrors web/'s design foundation with `pnpm build` passing, a human visual sign-off recorded, no drift, and no visible page change.
</success_criteria>

<output>
Create `.planning/phases/LEXCV-101-funda-o-cli-init-e-design-tokens/101-04-SUMMARY.md` when done. Record the FND-08 not-applicable determination for webpage/ explicitly, and the human visual sign-off verdict.
</output>
</output>
