---
phase: 101-funda-o-cli-init-e-design-tokens
plan: 02
type: execute
wave: 2
depends_on: [101-01]
files_modified:
  - web/components.json
  - web/src/app/globals.css
  - web/package.json
  - web/pnpm-lock.yaml
autonomous: true
requirements: [FND-01, FND-03, FND-07]
must_haves:
  truths:
    - "web/components.json exists, generated by `shadcn init -b radix` (Radix base, NOT the July-2026 Base UI default), with style=new-york, baseColor=neutral, cssVariables=true, iconLibrary=lucide, and an EMPTY tailwind.config field"
    - "web/src/app/globals.css contains the full shadcn semantic token set in EXACTLY ONE :root block and ONE .dark block (no silently-appended duplicate)"
    - "web/ light/dark --background/--foreground are restored to the validated hex #f8fafc/#020617 and #020617/#f8fafc; --radius is 0rem; --primary is #2563eb (light) / #3b82f6 (dark)"
    - "web/ animation utilities load via `@import \"tw-animate-css\"`; tailwindcss-animate is removed from both globals.css and package.json"
    - "No tailwind.config.js/.ts file exists in web/ (Tailwind v4 CSS-first is preserved)"
  artifacts:
    - path: "web/components.json"
      provides: "shadcn CLI config for web/"
      contains: "\"$schema\""
    - path: "web/src/app/globals.css"
      provides: "Full semantic token layer + tw-animate-css import"
      contains: "--primary"
    - path: "web/package.json"
      provides: "tw-animate-css dependency; tailwindcss-animate removed"
      contains: "tw-animate-css"
  key_links:
    - from: "web/components.json"
      to: "web/src/app/globals.css"
      via: "tailwind.css field points at the real Tailwind v4 CSS file"
      pattern: "\"css\": \"src/app/globals.css\""
    - from: "web/src/app/globals.css"
      to: "tw-animate-css"
      via: "@import replaces the old @plugin"
      pattern: "@import \"tw-animate-css\""
---

<objective>
Formally initialize the shadcn CLI in `web/` on the Radix base, merge the full semantic design-token set into `globals.css` without corrupting the already-shipped institutional theme, and swap the deprecated `tailwindcss-animate` plugin for `tw-animate-css`. This is the foundation gate every other Phase 101 plan (and every module phase 102-110) builds on. No visible page changes ship from this plan.

Purpose: Covers FND-01 (`components.json` via `shadcn init -b radix`), FND-03 (semantic tokens in `web/globals.css`, additive, single block, deliberate `--radius`/`--primary`, restored `--background`/`--foreground`), and the `web/` half of FND-07 (`tailwindcss-animate` → `tw-animate-css`).
Output: `web/components.json`, an extended `web/src/app/globals.css`, and updated `web/package.json`.
</objective>

<execution_context>
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/LEXCV-101-funda-o-cli-init-e-design-tokens/101-CONTEXT.md
@.planning/phases/LEXCV-101-funda-o-cli-init-e-design-tokens/101-UI-SPEC.md
@.planning/phases/LEXCV-101-funda-o-cli-init-e-design-tokens/101-PATTERNS.md
@.planning/research/STACK.md
@.planning/research/ARCHITECTURE.md
@.planning/research/PITFALLS.md

<interfaces>
Current web/src/app/globals.css (byte-identical to webpage/'s today) defines ONLY --background/--foreground + font vars inside one @theme inline, one :root, one .dark, and a body rule. Full current content is in 101-PATTERNS.md "globals.css" section. The deliberate token values to land after init (from 101-UI-SPEC.md Color + 101-PATTERNS.md):
  :root  → --background:#f8fafc; --foreground:#020617; --radius:0rem; --primary:#2563eb; --primary-foreground:#ffffff
  .dark  → --background:#020617; --foreground:#f8fafc; --primary:#3b82f6
Net-new tokens the CLI adds (KEEP the neutral baseColor defaults, do NOT hand-tune): --secondary, --secondary-foreground, --muted, --muted-foreground, --accent, --accent-foreground, --destructive, --border, --input, --ring, --card, --card-foreground, --popover, --popover-foreground.

Target components.json shape (from STACK.md lines 122-143 / 101-PATTERNS.md): $schema, style="new-york", rsc=true, tsx=true, tailwind.config="" (EMPTY), tailwind.css="src/app/globals.css", tailwind.baseColor="neutral", tailwind.cssVariables=true, aliases {components:@/components, utils:@/lib/utils, ui:@/components/ui, lib:@/lib, hooks:@/hooks}, iconLibrary="lucide". Both web/tsconfig.json and lib/utils.ts already match these defaults — zero alias remapping needed.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize shadcn CLI in web/ on the Radix base (dry-run first)</name>
  <files>web/components.json, web/package.json, web/pnpm-lock.yaml</files>
  <read_first>
    - web/package.json (confirm the 8 @radix-ui/react-* deps + class-variance-authority/clsx/tailwind-merge/lucide-react already present, and tailwindcss-animate present)
    - web/tsconfig.json (confirm "@/*": ["./src/*"] alias so components.json defaults resolve with zero remapping)
    - web/src/lib/utils.ts (confirm canonical cn() already exists — init must not clobber it)
    - .planning/research/STACK.md (Critical timing note + "components.json — expected shape for this repo" + Installation)
    - .planning/research/ARCHITECTURE.md (Pattern 2 — explicit --base radix; Pattern 4 — non-interactive init is supported)
    - .planning/research/PITFALLS.md (Pitfall 4 — tailwind.config stays "" for Tailwind v4; no tailwind.config.js may be created)
  </read_first>
  <action>
    FIRST re-verify the CLI surface at execution time (flag names are under active churn per the ROADMAP planning note): run `pnpm dlx shadcn@latest init --help` and confirm the Radix-base opt-out flag is `-b radix` / `--base radix` (the July-2026 default is now Base UI — plain `init` would scaffold an incompatible second primitives library, per STACK.md Critical timing note). Then run `pnpm dlx shadcn@latest init -b radix --dry-run` from inside `web/` and inspect the previewed diff to confirm it would touch only `components.json` + `globals.css` and would NOT create a tailwind.config file (CONTEXT.md decision + PITFALLS Pitfall 1/4). Then run the real `pnpm dlx shadcn@latest init -b radix` non-interactively, answering (or passing flags for): style=new-york, baseColor=neutral, cssVariables=true, iconLibrary=lucide. After it completes, open `web/components.json` and confirm every field matches the target shape in `<interfaces>` — in particular that `tailwind.config` is the empty string `""` and `tailwind.css` is `src/app/globals.css`. Do NOT let init add or overwrite any file under `web/src/components/ui/` (the 14 hand-rolled primitives are reconciled in Phase 102, not here) and do NOT let it modify `web/src/lib/utils.ts`. If init installed `tw-animate-css` automatically, that is expected and handled in Task 2; if it left `tailwindcss-animate` in place, Task 2 removes it.
  </action>
  <verify>
    <automated>cd web && test -f components.json && node -e "const c=require('./components.json'); if(c.tailwind.config!=='')process.exit(1); if(c.tailwind.css!=='src/app/globals.css')process.exit(1); if(c.iconLibrary!=='lucide')process.exit(1); console.log('components.json OK: style='+c.style+' baseColor='+c.tailwind.baseColor+' cssVariables='+c.tailwind.cssVariables)" && test ! -f tailwind.config.js && test ! -f tailwind.config.ts && echo "no tailwind.config file present (v4 preserved)"</automated>
  </verify>
  <acceptance_criteria>
    - `web/components.json` exists with `tailwind.config` === "" (empty string), `tailwind.css` === "src/app/globals.css", `iconLibrary` === "lucide", `tailwind.cssVariables` === true, `tailwind.baseColor` === "neutral", `style` === "new-york".
    - No `web/tailwind.config.js` and no `web/tailwind.config.ts` exist after init.
    - `git status` shows no new/modified files under `web/src/components/ui/` and no change to `web/src/lib/utils.ts` from this task.
    - `git diff web/src/app/globals.css` shows only additive token content (reviewed in Task 2) — the file was not truncated or replaced wholesale.
  </acceptance_criteria>
  <done>`shadcn init -b radix` has produced a correct `web/components.json` (Radix base, empty tailwind.config, lucide, cssVariables) with the existing 14 ui primitives and utils.ts untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Consolidate globals.css tokens to a single block and swap tw-animate-css</name>
  <files>web/src/app/globals.css, web/package.json, web/pnpm-lock.yaml</files>
  <read_first>
    - web/src/app/globals.css (the just-init'd file — inspect for duplicate :root/.dark blocks the CLI may have appended)
    - .planning/phases/LEXCV-101-funda-o-cli-init-e-design-tokens/101-UI-SPEC.md (Color section — exact hex values and the reserved-accent rules)
    - .planning/phases/LEXCV-101-funda-o-cli-init-e-design-tokens/101-PATTERNS.md (globals.css "Required post-init restoration" + "@plugin → @import swap")
    - .planning/research/PITFALLS.md (Pitfall 1 — duplicate :root/.dark append is the primary corruption mode; Pitfall 4 — v4 config)
  </read_first>
  <action>
    Review `git diff web/src/app/globals.css`. If `shadcn init` appended a SECOND `:root { ... }` or `.dark { ... }` block (the known corruption mode, PITFALLS Pitfall 1 — CSS cascade makes the last block silently win), hand-merge so exactly ONE `:root` block and ONE `.dark` block remain, each containing the full merged token set. In the merged `:root`, set the deliberate institutional values explicitly: `--background: #f8fafc`, `--foreground: #020617`, `--radius: 0rem`, `--primary: #2563eb`, `--primary-foreground: #ffffff`. In the merged `.dark`, set `--background: #020617`, `--foreground: #f8fafc`, `--primary: #3b82f6`. Leave every OTHER net-new semantic token the CLI added (--secondary, --secondary-foreground, --muted, --muted-foreground, --accent, --accent-foreground, --destructive, --border, --input, --ring, --card, --card-foreground, --popover, --popover-foreground) at its neutral-baseColor default — do NOT hand-tune those (they are net-new, no existing value collides; per UI-SPEC/ARCHITECTURE). Confirm there is still exactly ONE `@theme inline` block and that it maps the new tokens (e.g. --color-primary, --color-border, --color-ring, radius vars) so `bg-primary`/`border-border`/`rounded-*` utilities resolve. Then perform the animate swap (FND-07): replace the `@plugin "tailwindcss-animate";` line with `@import "tw-animate-css";` (place the @import near the top with the tailwindcss import, per Tailwind v4 CSS-first ordering). Run `pnpm remove tailwindcss-animate` and `pnpm add tw-animate-css@1.4.0` so package.json/lockfile match the CSS. Do NOT touch the existing `@custom-variant dark (&:is(.dark *));` line (Pitfall 4 — it already provides dark mode; no tailwind.config darkMode array).
  </action>
  <verify>
    <automated>cd web && test $(grep -c '^:root' src/app/globals.css) -eq 1 && test $(grep -c '^.dark' src/app/globals.css) -eq 1 && grep -q -- '--radius: 0rem' src/app/globals.css && grep -q -- '--primary: #2563eb' src/app/globals.css && grep -q -- '--background: #f8fafc' src/app/globals.css && grep -q '@import "tw-animate-css"' src/app/globals.css && ! grep -q 'tailwindcss-animate' src/app/globals.css && ! grep -q '"tailwindcss-animate"' package.json && grep -q '"tw-animate-css"' package.json && echo "tokens + animate swap OK"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^:root' web/src/app/globals.css` returns exactly 1, and `grep -c '^.dark' web/src/app/globals.css` returns exactly 1 (no silently-appended duplicate block).
    - globals.css contains literally `--radius: 0rem`, `--primary: #2563eb`, `--primary-foreground: #ffffff`, `--background: #f8fafc`, `--foreground: #020617` in `:root`, and `--background: #020617`, `--foreground: #f8fafc`, `--primary: #3b82f6` in `.dark`.
    - globals.css contains `@import "tw-animate-css"` and contains ZERO occurrences of `tailwindcss-animate`.
    - `web/package.json` lists `tw-animate-css` and does NOT list `tailwindcss-animate`.
    - The `@custom-variant dark (&:is(.dark *));` line is present and unchanged.
  </acceptance_criteria>
  <done>web/globals.css has a single merged :root/.dark carrying the full semantic token set with restored --background/--foreground, deliberate --radius:0rem and --primary blue, and animations now load via tw-animate-css with tailwindcss-animate fully removed from CSS and package.json.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev/CI ← npm registry | `shadcn init` may install packages (tw-animate-css); untrusted code enters the build |
| shadcn CLI → web/globals.css | CLI rewrites a production-shipped theme file via a PostCSS updater |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-101-SC | Tampering | packages installed by `shadcn init` / `pnpm add tw-animate-css` | mitigate | Gated by the 101-01 blocking-human legitimacy checkpoint (this plan runs only after 101-01 approval); official shadcn registry only |
| T-101-TH | Tampering | web/src/app/globals.css theme integrity | mitigate | `init --dry-run` before the real run; `git diff` review to guarantee exactly one :root/.dark block and restored institutional hex values — prevents silent theme corruption (PITFALLS Pitfall 1) |
| T-101-CFG | Tampering | Tailwind v4 config integrity | accept-with-control | Verify no `tailwind.config.js/.ts` is created and `tailwind.config` stays "" — a stray config file is inert in v4 but misleading (PITFALLS Pitfall 4) |
| — | Spoofing / Repudiation / Info-Disclosure / DoS / Elevation | n/a | accept | This plan adds no endpoints, auth, user input, or data flow — zero new runtime attack surface |
</threat_model>

<verification>
- `web/components.json` valid (Radix base, empty tailwind.config, lucide, cssVariables true).
- `web/src/app/globals.css` has exactly one :root and one .dark block with the deliberate token values and `@import "tw-animate-css"`.
- `tailwindcss-animate` removed from globals.css and package.json; no tailwind.config file created.
</verification>

<success_criteria>
- FND-01, FND-03 (web/), and the web/ half of FND-07 satisfied: a correct `components.json`, a single-block fully-tokenized `globals.css` with restored/deliberate identity values, and the animate-plugin swap complete — with no visible page change.
</success_criteria>

<output>
Create `.planning/phases/LEXCV-101-funda-o-cli-init-e-design-tokens/101-02-SUMMARY.md` when done. Record the final resolved `components.json` field values so 101-04 can copy them into webpage/ field-for-field.
</output>
