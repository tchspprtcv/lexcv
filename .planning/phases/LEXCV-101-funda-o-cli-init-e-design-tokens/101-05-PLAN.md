---
phase: 101-funda-o-cli-init-e-design-tokens
plan: 05
type: execute
wave: 4
depends_on: [101-03]
files_modified:
  - web/src/components/ui/sonner.tsx
  - web/src/hooks/use-toast.ts
  - web/src/app/layout.tsx
  - web/src/components/ui/toast.tsx
  - web/src/components/ui/toaster.tsx
  - web/package.json
  - web/pnpm-lock.yaml
autonomous: true
requirements: [FND-05, FND-08]
must_haves:
  truths:
    - "sonner's <Toaster /> is mounted at the web/ root; toast.tsx, toaster.tsx and the @radix-ui/react-toast dependency are all removed"
    - "toast.success(msg) still renders title 'Sucesso' and toast.error(msg) still renders title 'Erro', with success=green / error=red visual distinction, and all 26 existing call files compile unchanged"
    - "Every ui component (the 14 hand-rolled + the 16 new) imports Radix from the unified `radix-ui` package after `shadcn migrate radix`; no lingering unused scoped @radix-ui/react-* entries remain in package.json"
    - "The whole web/ project typechecks clean AND `pnpm build` passes after both changes"
  artifacts:
    - path: "web/src/components/ui/sonner.tsx"
      provides: "Sonner Toaster themed via next-themes"
      contains: "sonner"
    - path: "web/src/hooks/use-toast.ts"
      provides: "Contract-preserving toast/toast.success/toast.error wrapper over sonner"
      contains: "Sucesso"
    - path: "web/src/app/layout.tsx"
      provides: "Root mount of the sonner Toaster"
      contains: "Toaster"
  key_links:
    - from: "web/src/app/layout.tsx"
      to: "web/src/components/ui/sonner.tsx"
      via: "import { Toaster } and render it once near root"
      pattern: "components/ui/sonner"
    - from: "web/src/hooks/use-toast.ts"
      to: "sonner"
      via: "toast.success/toast.error delegate to sonner's toast API preserving titles"
      pattern: "from \"sonner\""
---

<objective>
Replace the deprecated Radix Toast stack with Sonner while preserving the exact runtime toast contract (`toast.success` → "Sucesso", `toast.error` → "Erro") so none of the ~146 existing call sites change, then run `shadcn migrate radix` so the 14 hand-rolled primitives and the 16 new ones all import Radix from the single unified `radix-ui` package — eliminating the dual-package bridge state. No visible page changes ship (toasts render the same copy and colors).

Purpose: Covers FND-08 (Sonner adoption; `<Toaster />` at web/ root; `toast.tsx`/`toaster.tsx`/`@radix-ui/react-toast` removed; call sites preserved) and FND-05 (`shadcn migrate radix` — unified `radix-ui`, no dual state). Runs last so migrate unifies old + new components in a single pass after all primitives exist.
Output: `web/src/components/ui/sonner.tsx`, rewritten `use-toast.ts`, updated `layout.tsx`, deleted `toast.tsx`/`toaster.tsx`, pruned `package.json`.
</objective>

<execution_context>
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/LEXCV-101-funda-o-cli-init-e-design-tokens/101-UI-SPEC.md
@.planning/phases/LEXCV-101-funda-o-cli-init-e-design-tokens/101-PATTERNS.md
@.planning/research/STACK.md
@.planning/research/PITFALLS.md

<interfaces>
The load-bearing toast contract to preserve verbatim (from web/src/hooks/use-toast.ts lines 219-224):
  toast.success(message, options?) → renders title "Sucesso", description = message, success/green styling
  toast.error(message, options?)   → renders title "Erro",    description = message, destructive/red styling
Callers import `{ toast }` (and its `.success`/`.error`) from "@/hooks/use-toast". Grep-verified consumers of the toast API: 26 files across app/ (dashboard pages, auth login), components/shared, components/profile, and lib/api.ts. The ONLY consumer of the reactive `useToast()` hook is web/src/components/ui/toaster.tsx (which is being deleted); grep for `useToast()` confirms no other file destructures it — so the reactive hook may be dropped.

Sonner mapping: sonner's API is `toast.success(title, { description })` / `toast.error(title, { description })`. The wrapper delegates: toast.success = (message, options) => sonnerToast.success("Sucesso", { description: message, ...options }); toast.error = (message, options) => sonnerToast.error("Erro", { description: message, ...options }); base toast(...) delegates to sonnerToast(...). This keeps title copy + green/red distinction identical to today (toaster.tsx used CheckCircle2/emerald for default and AlertCircle/red for destructive).

Providers: web/src/app/providers.tsx already wraps children in next-themes NextThemesProvider (attribute="class"), so shadcn's sonner.tsx (which reads useTheme()) works with no provider change.

Files removed by FND-08: web/src/components/ui/toast.tsx (imports @radix-ui/react-toast), web/src/components/ui/toaster.tsx (imports useToast + toast.tsx). The @radix-ui/react-toast dependency is then pruned from package.json.

migrate radix (FND-05): the 14 hand-rolled files still import scoped `@radix-ui/react-*` (dialog, alert-dialog, label, popover, radio-group, slot, switch — react-toast leaves with toast.tsx). The 16 new files (101-03) already import from unified `radix-ui`. `pnpm dlx shadcn@latest migrate radix` rewrites the scoped imports to the unified package so both sets agree (PITFALLS Pitfall 3).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Adopt Sonner behind the preserved toast contract; delete the Radix Toast stack</name>
  <files>web/src/components/ui/sonner.tsx, web/src/hooks/use-toast.ts, web/src/app/layout.tsx, web/src/components/ui/toast.tsx, web/src/components/ui/toaster.tsx, web/package.json, web/pnpm-lock.yaml</files>
  <read_first>
    - web/src/hooks/use-toast.ts (the full current file — the toast/toast.success/toast.error contract at lines 219-224 is what must survive)
    - web/src/components/ui/toaster.tsx (the file being replaced — its variant→icon/color mapping is the visual contract: default=green CheckCircle2, destructive=red AlertCircle)
    - web/src/components/ui/toast.tsx (imports @radix-ui/react-toast; the ToastProps/ToastActionElement types use-toast.ts imports from here must stop being referenced)
    - web/src/app/layout.tsx (line 5 imports Toaster from @/components/ui/toaster; line 36 mounts <Toaster /> inside <Providers>)
    - web/src/app/providers.tsx (confirm NextThemesProvider is present so sonner.tsx theming works)
    - .planning/phases/LEXCV-101-funda-o-cli-init-e-design-tokens/101-PATTERNS.md (Toast → Sonner swap section — the contract-preserving approach)
  </read_first>
  <action>
    Install sonner (`pnpm add sonner`) and scaffold the shadcn sonner wrapper: `pnpm dlx shadcn@latest add sonner` creates `web/src/components/ui/sonner.tsx` exporting a `Toaster` themed via next-themes. Rewrite `web/src/hooks/use-toast.ts` so it no longer imports anything from the (to-be-deleted) `@/components/ui/toast` and instead delegates to sonner: keep the public exports `toast`, `toast.success`, `toast.error`. Map `toast.success(message, options)` to sonner's `toast.success("Sucesso", { description: message, ...options })` and `toast.error(message, options)` to sonner's `toast.error("Erro", { description: message, ...options })`; the bare `toast(props)` delegates to sonner's `toast(...)`. Define any small local types the signatures need (do not re-import from toast.tsx). Since the reactive `useToast()` hook's only consumer is toaster.tsx (being deleted — grep-confirm no other consumer), you may drop `useToast` or keep a thin no-op shim returning `{ toast, dismiss }`; do NOT keep the old reducer/bridge machinery. Update `web/src/app/layout.tsx` to import `{ Toaster } from "@/components/ui/sonner"` and mount it once inside `<Providers>` (replacing the old toaster import + mount). Delete `web/src/components/ui/toast.tsx` and `web/src/components/ui/toaster.tsx`. Run `pnpm remove @radix-ui/react-toast`. Do NOT modify any of the 26 toast call sites — the whole point is that `toast.success(...)`/`toast.error(...)` keep working unchanged. Configure the sonner `<Toaster />` (richColors or an equivalent success/error color treatment) so success renders green and error renders red, matching the prior emerald/red visual contract. FINALLY, run `pnpm build` from inside `web/` and confirm it exits 0 — `tsc --noEmit` proves the call sites type-check, but only a real build proves layout.tsx's new Toaster mount, the deleted files, and the sonner import all bundle (the generic post-merge gate silently skips: workflow.build_command is unset, no root workspace).
  </action>
  <verify>
    <automated>cd web && test -f src/components/ui/sonner.tsx && test ! -f src/components/ui/toast.tsx && test ! -f src/components/ui/toaster.tsx && grep -q 'Sucesso' src/hooks/use-toast.ts && grep -q 'Erro' src/hooks/use-toast.ts && grep -q 'from "sonner"' src/hooks/use-toast.ts && grep -q 'components/ui/sonner' src/app/layout.tsx && ! grep -q '"@radix-ui/react-toast"' package.json && ! grep -rq '@/components/ui/toaster' src && ! grep -rq '@/components/ui/toast"' src && pnpm exec tsc --noEmit && pnpm build && echo "sonner swap OK + typecheck + production build clean"</automated>
  </verify>
  <acceptance_criteria>
    - `web/src/components/ui/sonner.tsx` exists; `web/src/components/ui/toast.tsx` and `web/src/components/ui/toaster.tsx` are deleted.
    - `web/src/hooks/use-toast.ts` imports from `"sonner"`, contains the literal titles `"Sucesso"` and `"Erro"`, and no longer imports from `@/components/ui/toast`.
    - `web/src/app/layout.tsx` imports `Toaster` from `@/components/ui/sonner` and mounts it once.
    - `web/package.json` no longer contains `@radix-ui/react-toast`.
    - No file under `web/src` still imports `@/components/ui/toaster` or `@/components/ui/toast`.
    - `pnpm exec tsc --noEmit` exits 0 — proving all 26 call sites still compile with zero call-site edits.
    - `pnpm build` in `web/` exits 0 — the Toaster remount + file deletions + sonner import bundle in a real production build (the generic post-merge gate silently skips here).
  </acceptance_criteria>
  <done>Sonner is mounted at the web/ root behind an unchanged toast.success/"Sucesso"/toast.error/"Erro" contract, the Radix Toast stack (toast.tsx/toaster.tsx/@radix-ui/react-toast) is fully removed, every existing call site still compiles, and `pnpm build` passes clean.</done>
</task>

<task type="auto">
  <name>Task 2: Run `shadcn migrate radix` and prune unused scoped Radix packages</name>
  <files>web/src/components/ui/*.tsx, web/package.json, web/pnpm-lock.yaml</files>
  <read_first>
    - web/package.json (the scoped @radix-ui/react-* deps still listed after Task 1 removed react-toast)
    - web/src/components/ui/dialog.tsx and web/src/components/ui/switch.tsx (representative hand-rolled files that still use scoped imports — verify they migrate)
    - .planning/research/PITFALLS.md (Pitfall 3 — unified radix-ui vs scoped; run migrate as a tracked step, then prune)
    - .planning/research/STACK.md (migrate radix mechanics; `shadcn diff`/migrate is a flag/subcommand on the current CLI)
  </read_first>
  <action>
    From inside `web/`, run `pnpm dlx shadcn@latest migrate radix` — this rewrites the scoped `@radix-ui/react-*` imports in the 14 hand-rolled files to the unified `radix-ui` package, so they agree with the 16 new files that already use it. Review the diff: confirm each migrated file now imports from `radix-ui` (e.g. `import { Dialog as DialogPrimitive } from "radix-ui"`) and that no component's behavior/props changed (import-path-only rewrite). Then reconcile package.json: ensure `radix-ui` is present, and remove any scoped `@radix-ui/react-*` entries that are no longer imported anywhere in `web/src` (grep each scoped package name across src before removing it — keep any that a migrated file still legitimately imports, e.g. `@radix-ui/react-slot` if `asChild`/`Slot` is still imported scoped). Run `pnpm install` / `pnpm dedupe` to settle the lockfile. Do NOT leave BOTH `radix-ui` and now-dead scoped packages installed (that is the dual-tree state FND-05 exists to remove). If `migrate radix` is unavailable or errors at execution time, document the exact CLI output and fall back to a manual per-file import rewrite, recording it as a bridge state in the SUMMARY. FINALLY, run `pnpm build` from inside `web/` and confirm it exits 0 — the import rewrite across 14 shipped files plus the dependency prune must survive a real production build, not just a typecheck (the generic post-merge gate silently skips: workflow.build_command is unset, no root workspace).
  </action>
  <verify>
    <automated>cd web && grep -q '"radix-ui"' package.json && for p in react-dialog react-alert-dialog react-popover react-radio-group react-switch react-label react-select react-tabs react-dropdown-menu react-tooltip react-checkbox react-avatar react-separator react-progress react-accordion react-navigation-menu; do if grep -q "\"@radix-ui/$p\"" package.json; then if ! grep -rq "@radix-ui/$p" src; then echo "DEAD scoped dep still in package.json: @radix-ui/$p"; exit 1; fi; fi; done && pnpm exec tsc --noEmit && pnpm build && echo "migrate radix OK: unified package present, no dead scoped deps, typecheck + production build clean"</automated>
  </verify>
  <acceptance_criteria>
    - `web/package.json` contains the unified `radix-ui` package.
    - For every scoped `@radix-ui/react-*` still listed in package.json, at least one file under `web/src` still imports it (no dead scoped dependency shipped); packages no longer imported anywhere are removed.
    - The migrated hand-rolled files import Radix primitives from `radix-ui` (import-path-only change; props/variants unchanged — e.g. badge's `gray` variant still present, dialog's markup unchanged).
    - `pnpm exec tsc --noEmit` exits 0.
    - `pnpm build` in `web/` exits 0 — the whole-repo import migration survives a real production build.
  </acceptance_criteria>
  <done>All ui components (old + new) import Radix from the unified radix-ui package, dead scoped @radix-ui/react-* entries are pruned, and the project typechecks AND production-builds clean — closing FND-05's no-dual-state requirement.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev/CI ← npm registry | `pnpm add sonner` + `migrate radix` installs the unified `radix-ui` package |
| shadcn CLI → web/src/components/ui/* | migrate rewrites imports across 14 shipped files |
| toast API → 26 call sites | contract change here would break user-facing feedback across the app |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-101-SC | Tampering | sonner + radix-ui installs | mitigate | Gated by the 101-01 blocking-human legitimacy checkpoint (this plan depends transitively on 101-01); official shadcn registry only |
| T-101-TC | Tampering | toast contract integrity (Sucesso/Erro titles, green/red) | mitigate | Rewrite preserves `toast.success`/`toast.error` public API; `tsc --noEmit` + `pnpm build` prove all 26 call sites still compile and bundle with zero call-site edits |
| T-101-RX | Tampering | dual Radix dependency tree | mitigate | `migrate radix` unifies imports; prune-dead-scoped-deps gate + `pnpm build` assert no dual state remains and the migration bundles (PITFALLS Pitfall 3) |
| T-101-RBAC | Elevation | (forward-looking) picker components later built on new Command/Select | accept-with-note | This plan wires no data-fetching pickers; module phases replacing an existing permission-gated picker MUST keep the same RBAC-scoped hook (PITFALLS Security Mistakes) — noted for 105/107/108, not actionable here |
| — | Spoofing / Repudiation / Info-Disclosure / DoS | n/a | accept | No endpoints/auth/input/data flow added; toast rendering and import paths only |
</threat_model>

<verification>
- sonner.tsx present; toast.tsx/toaster.tsx deleted; @radix-ui/react-toast pruned.
- use-toast.ts delegates to sonner preserving "Sucesso"/"Erro"; layout mounts sonner Toaster.
- `radix-ui` unified package present; no dead scoped @radix-ui/react-* deps; migrated files import from radix-ui.
- `pnpm exec tsc --noEmit` passes after both tasks.
- `pnpm build` passes in web/ after both tasks (Task 1 + Task 2 automated gates).
</verification>

<success_criteria>
- FND-08 and FND-05 satisfied: Sonner replaces Radix Toast with the runtime copy/color contract intact and all call sites compiling AND building, and the whole ui/ folder is unified on the single `radix-ui` package with no dual-tree bridge state left.
</success_criteria>

<output>
Create `.planning/phases/LEXCV-101-funda-o-cli-init-e-design-tokens/101-05-SUMMARY.md` when done. Record which scoped @radix-ui/react-* packages were pruned vs. kept, and any migrate-radix fallback taken.
</output>
</output>
