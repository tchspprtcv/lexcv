---
phase: "53-shell-responsivo"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/components/ui/sheet.tsx
  - web/src/components/shared/dashboard-shell.tsx
autonomous: true
requirements:
  - NAV-01
  - NAV-02
  - NAV-03

must_haves:
  truths:
    - "Em mobile (< 768px) a sidebar não está visível por defeito"
    - "O utilizador toca no ícone hamburger na top bar e a sidebar abre como drawer overlay"
    - "Ao navegar para qualquer página via link no drawer, o drawer fecha automaticamente"
    - "A top bar mobile mostra hamburger (esq.), nome da instituição (centro), notificações+perfil (dir.)"
    - "A barra de pesquisa fica oculta em mobile"
  artifacts:
    - path: "web/src/components/ui/sheet.tsx"
      provides: "shadcn Sheet primitive para o drawer overlay"
    - path: "web/src/components/shared/dashboard-shell.tsx"
      provides: "Shell responsiva com drawer mobile, hamburger na top bar, sidebar oculta em mobile"
  key_links:
    - from: "DashboardShell (hamburger button)"
      to: "Sheet (drawerOpen state)"
      via: "useState + onOpenChange prop"
      pattern: "setDrawerOpen"
    - from: "Sheet drawer nav links"
      to: "drawerOpen=false"
      via: "useEffect on pathname"
      pattern: "React.useEffect.*pathname.*setDrawerOpen"
---

<objective>
Instalar o componente shadcn Sheet e transformar o DashboardShell numa shell responsiva: a sidebar fica oculta em mobile, um botão hamburger na top bar abre o drawer overlay com o mesmo conteúdo da sidebar, e o drawer fecha automaticamente ao navegar.

Purpose: Cumprir os requisitos NAV-01, NAV-02 e NAV-03 — mobile-first navigation com drawer e top bar simplificada.
Output: web/src/components/ui/sheet.tsx (novo) + web/src/components/shared/dashboard-shell.tsx (modificado).
</objective>

<execution_context>
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/53-shell-responsivo/53-CONTEXT.md
@.planning/phases/53-shell-responsivo/53-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Instalar o componente Sheet via shadcn CLI</name>
  <files>web/src/components/ui/sheet.tsx</files>
  <read_first>
    web/src/components/ui/dialog.tsx — padrão de estrutura shadcn que o Sheet vai seguir (Radix UI wrapping, data-slot attributes, named exports, cn() usage)
  </read_first>
  <action>
    A partir da directoria web/ executar: npx shadcn@latest add sheet

    O comando instala @radix-ui/react-dialog (já presente como dep do dialog.tsx) e gera web/src/components/ui/sheet.tsx com os exports: Sheet, SheetClose, SheetContent, SheetDescription, SheetFooter, SheetHeader, SheetOverlay, SheetPortal, SheetTitle, SheetTrigger.

    SheetContent aceita a prop side="left" que produz um drawer slide-from-left. O overlay usa as mesmas classes data-[state=open]:animate-in / data-[state=closed]:animate-out que o dialog.tsx existente.

    Não editar manualmente o ficheiro gerado — o CLI gera-o conforme a convenção do codebase (data-slot, cn, "use client").

    Verificar que o ficheiro foi gerado e exporta pelo menos SheetContent e SheetTrigger.
  </action>
  <verify>
    node -e "require('fs').existsSync('web/src/components/ui/sheet.tsx') && process.exit(0) || process.exit(1)"
    E confirmar que o ficheiro contém a string SheetContent.
  </verify>
  <done>web/src/components/ui/sheet.tsx existe e exporta SheetContent e SheetTrigger</done>
</task>

<task type="auto">
  <name>Task 2: Tornar DashboardShell responsiva (hamburger + drawer + top bar mobile)</name>
  <files>web/src/components/shared/dashboard-shell.tsx</files>
  <read_first>
    web/src/components/shared/dashboard-shell.tsx — ler o ficheiro completo (199 linhas) para conhecer a estrutura exacta antes de modificar.
    web/src/components/ui/sheet.tsx — confirmar os nomes dos exports após Task 1.
  </read_first>
  <action>
    Modificar web/src/components/shared/dashboard-shell.tsx com as seguintes alterações precisas:

    1. IMPORTS — adicionar Menu ao bloco de importação do lucide-react (junto a Building2, Calendar, etc.). Adicionar a seguinte import após as imports de lucide-react:
       import { Sheet, SheetContent } from "@/components/ui/sheet";

    2. ESTADO — dentro da função DashboardShell, após a linha `const me = useMe();` (linha 51), adicionar:
       const [drawerOpen, setDrawerOpen] = React.useState(false);

    3. FECHAR DRAWER AO NAVEGAR — adicionar um novo useEffect após o existente (que trata me.isError). O novo useEffect tem pathname como dependência e chama setDrawerOpen(false):
       React.useEffect(() => { setDrawerOpen(false); }, [pathname]);

    4. ASIDE (SIDEBAR DESKTOP) — na tag <aside> da linha 72, adicionar "hidden md:flex" à className. A aside atual usa flex flex-col — substituir por hidden md:flex flex-col. A classe completa resultante deve ser:
       "hidden md:flex w-[270px] flex-shrink-0 text-white bg-slate-950 dark:bg-[#04091a] border-r border-slate-900/50 dark:border-slate-800/50 flex-col z-20"
       (remover "flex" standalone porque md:flex já inclui display:flex)

    5. DRAWER MOBILE — após o </aside> de fecho (linha 149) e antes do <main>, inserir o componente Sheet:
       <Sheet open={drawerOpen} onOpenChange={setDrawerOpen}>
         <SheetContent side="left" className="w-[270px] p-0 bg-slate-950 dark:bg-[#04091a] border-r border-slate-900/50 dark:border-slate-800/50 text-white flex flex-col">
           [CONTEÚDO IDÊNTICO AO INTERIOR DA ASIDE EXISTENTE]
         </SheetContent>
       </Sheet>
       O conteúdo dentro do SheetContent deve ser uma cópia exacta do interior da <aside> (o bloco px-5 pt-6 pb-4 com LEXCV, a nav com NAV.filter, o bloco Sistema, e o user card mt-auto). Não usar SheetTrigger aqui — o drawer é controlado via state (controlled mode).

    6. HAMBURGER BUTTON NA TOP BAR — no interior do <header> (linha 152), antes do div da barra de pesquisa (linha 153), inserir:
       <button
         type="button"
         onClick={() => setDrawerOpen(true)}
         className="md:hidden flex items-center justify-center h-9 w-9 rounded-md text-slate-500 hover:text-slate-900 dark:hover:text-slate-100 hover:bg-slate-100 dark:hover:bg-slate-800 transition-colors"
         aria-label="Abrir menu"
       >
         <Menu className="h-5 w-5" />
       </button>

    7. BARRA DE PESQUISA — no div da barra de pesquisa (linha 153, className "flex-1 max-w-md relative group"), adicionar "hidden md:flex" à className:
       <div className="hidden md:flex flex-1 max-w-md relative group">

    8. NOME DA INSTITUIÇÃO NA TOP BAR (mobile) — a div do nome na linha 161 já tem "hidden md:flex". Para mobile, inserir imediatamente antes do div ml-auto dos ícones da direita (linha 170) uma versão mobile do nome da instituição visível apenas em mobile:
       <div className="flex-1 flex items-center justify-center gap-2 md:hidden text-[13px] font-medium text-slate-700 dark:text-slate-300 truncate">
         {me.data?.tenant_logo_data_url ? (
           <img src={me.data.tenant_logo_data_url} alt="" className="h-5 w-5 object-contain rounded-sm flex-shrink-0" />
         ) : (
           <Building2 className="h-4 w-4 text-slate-400 flex-shrink-0" />
         )}
         <span className="truncate">{me.data?.tenant_nome ?? "LexCV"}</span>
       </div>

    Manter todas as outras partes do ficheiro inalteradas. Não usar bloco de código fenced na action — implementar directamente.
  </action>
  <verify>
    cd web && pnpm build 2>&1 | tail -20
    Deve terminar sem erros TypeScript. Em alternativa: pnpm lint 2>&1 | tail -20
  </verify>
  <done>
    Build sem erros. A aside tem "hidden md:flex". O Sheet está presente no JSX com open={drawerOpen}. O useEffect fecha o drawer ao mudar de pathname. O hamburger button tem className="md:hidden". A barra de pesquisa tem "hidden md:flex". A versão mobile do nome da instituição tem "md:hidden".
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tenant_logo_data_url → img src | URL de logótipo proveniente da API, renderizado em img |
| tenant_nome → texto DOM | Nome da instituição proveniente da API |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-53-01 | XSS | img src={me.data.tenant_logo_data_url} | accept | React escapa atributos src por defeito; data_url vem de backend autenticado; sem dangerouslySetInnerHTML; risco residual negligível |
| T-53-02 | XSS | texto {me.data.tenant_nome} | accept | React escapa text nodes automaticamente; sem innerHTML |
| T-53-03 | Information Disclosure | Sheet drawer — conteúdo filtrado por hasPermission | mitigate | O drawer usa NAV.filter(hasPermission) identicamente à sidebar — links não aparecem se sem permissão |
| T-53-SC | Tampering | npx shadcn@latest add sheet | mitigate | shadcn é package bem estabelecido (npmjs.com/package/shadcn); verificar checksum antes de instalar em ambiente de produção |
</threat_model>

<verification>
Após execução das duas tasks:
1. `pnpm build` em web/ sem erros TypeScript ou de compilação
2. `grep -n "hidden md:flex" web/src/components/shared/dashboard-shell.tsx` retorna pelo menos 2 ocorrências (aside + search bar)
3. `grep -n "drawerOpen" web/src/components/shared/dashboard-shell.tsx` retorna pelo menos 3 ocorrências (useState, useEffect, Sheet open prop)
4. `grep -n "md:hidden" web/src/components/shared/dashboard-shell.tsx` retorna pelo menos 2 ocorrências (hamburger button + nome mobile)
5. `test -f web/src/components/ui/sheet.tsx` retorna 0
</verification>

<success_criteria>
- Em viewport < 768px: a aside não é renderizada (hidden)
- Em viewport < 768px: o botão hamburger é visível na top bar
- Clicar no hamburger abre o Sheet com o mesmo conteúdo da sidebar
- Navegar para qualquer página a partir do drawer fecha o drawer (useEffect em pathname)
- A barra de pesquisa não aparece em mobile
- O nome da instituição aparece centrado na top bar mobile
- pnpm build conclui sem erros
</success_criteria>

<output>
Criar .planning/phases/53-shell-responsivo/53-01-SUMMARY.md quando concluído
</output>
