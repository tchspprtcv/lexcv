---
phase: "53-shell-responsivo"
plan: "02"
type: execute
wave: 2
depends_on:
  - "53-01"
files_modified:
  - web/src/components/shared/bottom-nav.tsx
  - web/src/components/shared/dashboard-shell.tsx
autonomous: true
requirements:
  - NAV-04

must_haves:
  truths:
    - "Uma bottom navigation bar fixa com 5 módulos é visível em todas as páginas em mobile"
    - "O item activo na bottom nav tem destaque azul (text-blue-400)"
    - "A bottom nav respeita permissões — módulos sem permissão não aparecem"
    - "O conteúdo das páginas não fica tapado pela bottom nav (padding-bottom em mobile)"
    - "A bottom nav não é visível em desktop (md:hidden)"
  artifacts:
    - path: "web/src/components/shared/bottom-nav.tsx"
      provides: "Componente BottomNav com os 5 módulos principais filtrados por permissão"
      exports: ["BottomNav"]
    - path: "web/src/components/shared/dashboard-shell.tsx"
      provides: "Shell com BottomNav integrada e pb-16 md:pb-0 na área de conteúdo"
  key_links:
    - from: "web/src/components/shared/dashboard-shell.tsx"
      to: "web/src/components/shared/bottom-nav.tsx"
      via: "import e renderização dentro do <main>"
      pattern: "BottomNav"
    - from: "BottomNav"
      to: "NAV.slice(0,5)"
      via: "hasPermission filter"
      pattern: "hasPermission.*slice"
---

<objective>
Criar o componente BottomNav com os 5 módulos principais e integrá-lo no DashboardShell, garantindo que o conteúdo das páginas não fica ocultado pela barra fixa.

Purpose: Cumprir o requisito NAV-04 — navegação directa aos módulos em mobile através de bottom nav sempre visível.
Output: web/src/components/shared/bottom-nav.tsx (novo) + ajuste ao dashboard-shell.tsx (adicionar BottomNav e padding).
</objective>

<execution_context>
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/53-shell-responsivo/53-CONTEXT.md
@.planning/phases/53-shell-responsivo/53-PATTERNS.md
@.planning/phases/53-shell-responsivo/53-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar o componente BottomNav</name>
  <files>web/src/components/shared/bottom-nav.tsx</files>
  <read_first>
    web/src/components/shared/dashboard-shell.tsx — ler para extrair: (1) a definição do array NAV (linhas 37-44), (2) o padrão de active state com pathname (linhas 79-94), (3) os imports de lucide-react icons já disponíveis, (4) o import de hasPermission, cn, useMe, usePathname.
  </read_first>
  <action>
    Criar web/src/components/shared/bottom-nav.tsx como componente cliente ("use client" na primeira linha).

    O componente recebe permissions como prop (type: string[] | undefined) para evitar segunda chamada a useMe. Se permissions for undefined, trata como array vazio.

    Imports necessários:
    - Link from "next/link"
    - usePathname from "next/navigation"
    - Home, Users, Scale, Calendar, FileText from "lucide-react" (os 5 ícones dos primeiros 5 itens do NAV)
    - hasPermission from "@/lib/permissions"
    - cn from "@/lib/utils"

    Definir internamente um array BOTTOM_NAV com os 5 primeiros módulos (mesmas entradas que NAV):
    - { href: "/dashboard", label: "Dashboard", icon: Home, requiredPermission: undefined }
    - { href: "/clientes", label: "Clientes", icon: Users, requiredPermission: "clientes:view" }
    - { href: "/processos", label: "Processos", icon: Scale, requiredPermission: "processos:view" }
    - { href: "/agenda", label: "Agenda", icon: Calendar, requiredPermission: "agenda:view" }
    - { href: "/documentos", label: "Documentos", icon: FileText, requiredPermission: "documentos:view" }

    Lógica de active state idêntica à sidebar:
    - const active = pathname === item.href || (item.href === "/processos" && pathname.startsWith("/processos/dashboard"))

    Estrutura JSX do componente:
    - <nav> com className: "fixed bottom-0 left-0 right-0 z-50 flex md:hidden bg-slate-950 dark:bg-[#04091a] border-t border-slate-800/60"
    - Dentro: BOTTOM_NAV.filter(item => hasPermission(permissions, item.requiredPermission)).map(item => ...)
    - Cada item: <Link href={item.href}> com flex flex-col items-center gap-0.5 flex-1 py-2 px-1 text-xs font-medium transition-colors
    - Ícone: <Icon className="h-5 w-5"> com classes activo/inactivo
    - Label: <span className="text-[10px] leading-tight truncate"> com item.label
    - Cores activo: text-blue-400 no Link, text-blue-500 no Icon
    - Cores inactivo: text-slate-400 no Link, text-slate-500 no Icon
    - Usar cn() para classes condicionais

    Export: export function BottomNav({ permissions }: { permissions?: string[] })
  </action>
  <verify>
    node -e "require('fs').readFileSync('web/src/components/shared/bottom-nav.tsx', 'utf8').includes('BottomNav') && process.exit(0) || process.exit(1)"
  </verify>
  <done>
    Ficheiro web/src/components/shared/bottom-nav.tsx existe. Exporta função BottomNav. Contém "md:hidden" na nav. Contém hasPermission filter. Contém active state com pathname.
  </done>
</task>

<task type="auto">
  <name>Task 2: Integrar BottomNav no DashboardShell e corrigir padding do conteúdo</name>
  <files>web/src/components/shared/dashboard-shell.tsx</files>
  <read_first>
    web/src/components/shared/dashboard-shell.tsx — ler o ficheiro completo (será a versão modificada pelo plano 53-01) para identificar: (1) a linha do div flex-1 overflow-auto p-8 onde está o {children}, (2) o fim do componente para saber onde inserir BottomNav, (3) os imports existentes.
  </read_first>
  <action>
    Fazer duas alterações no ficheiro web/src/components/shared/dashboard-shell.tsx modificado pelo plano 53-01:

    1. IMPORT — adicionar no topo do ficheiro, junto aos outros imports de componentes shared:
       import { BottomNav } from "@/components/shared/bottom-nav";

    2. PADDING DO CONTEÚDO — localizar o div que contém {children}. Actualmente tem className "flex-1 overflow-auto p-8". Alterar para:
       className="flex-1 overflow-auto p-8 pb-24 md:pb-8"
       (pb-24 em mobile garante que o conteúdo no fundo da página não fica ocultado pela bottom nav de h-14 aprox. mais safe area)

    3. INSERIR BOTTOMNAV — imediatamente antes do </main> de fecho, inserir:
       <BottomNav permissions={me.data?.permissions} />

    Não modificar mais nada no ficheiro.
  </action>
  <verify>
    cd web && pnpm build 2>&1 | tail -20
    Deve terminar sem erros TypeScript. E também:
    grep -c "BottomNav" web/src/components/shared/dashboard-shell.tsx
    Deve retornar 2 (import + uso no JSX).
  </verify>
  <done>
    pnpm build sem erros. dashboard-shell.tsx importa BottomNav e renderiza-o antes de </main>. O div do children tem pb-24 md:pb-8.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| permissions prop → hasPermission filter | Array de permissões do utilizador controla visibilidade dos links |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-53-04 | Information Disclosure | BottomNav — links visíveis sem permissão | mitigate | Usar hasPermission(permissions, item.requiredPermission) identicamente ao NAV.filter da sidebar — links de módulos protegidos não aparecem se utilizador não tem permissão |
| T-53-05 | XSS | item.label → texto DOM | accept | Valores são strings literais definidas no código (não vêm da API); React escapa text nodes |
| T-53-SC | Tampering | Nenhum package novo nesta fase | accept | Sem instalações npm neste plano; risco não aplicável |
</threat_model>

<verification>
Após execução das duas tasks:
1. `test -f web/src/components/shared/bottom-nav.tsx` retorna 0
2. `grep -c "md:hidden" web/src/components/shared/bottom-nav.tsx` retorna >= 1
3. `grep -c "hasPermission" web/src/components/shared/bottom-nav.tsx` retorna >= 1
4. `grep -c "BottomNav" web/src/components/shared/dashboard-shell.tsx` retorna 2
5. `grep "pb-24" web/src/components/shared/dashboard-shell.tsx` retorna a linha com o div dos children
6. `cd web && pnpm build` conclui sem erros
</verification>

<success_criteria>
- BottomNav aparece em mobile em todas as páginas (fixed bottom, md:hidden em desktop)
- Os 5 módulos são mostrados; módulos sem permissão do utilizador não aparecem
- O item activo tem text-blue-400 correspondente ao pathname actual
- O conteúdo das páginas tem padding suficiente para não ficar tapado pela bottom nav
- pnpm build conclui sem erros TypeScript
</success_criteria>

<output>
Criar .planning/phases/53-shell-responsivo/53-02-SUMMARY.md quando concluído
</output>
