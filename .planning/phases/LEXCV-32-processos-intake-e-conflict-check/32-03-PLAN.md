---
phase: 32-processos-intake-e-conflict-check
plan: 03
type: execute
wave: 3
depends_on: ["32-02"]
files_modified:
  - web/src/app/(dashboard)/processos/novo/page.tsx
  - web/src/app/(dashboard)/processos/page.tsx
  - web/src/app/(dashboard)/processos/[id]/page.tsx
autonomous: true
requirements: [INT-01, CFL-01]

must_haves:
  truths:
    - "Utilizador percorre um fluxo de 3 passos (Intake -> Conflict Check -> Abertura) em /processos/novo"
    - "Step 1 cria o processo em TRIAGEM; Step 2 corre o conflict check e regista a decisao; Step 3 formaliza"
    - "O botao Formalizar fica desabilitado com motivo quando ha conflito impeditivo ou nenhuma decisao registada"
    - "A listagem de processos mostra um badge EM TRIAGEM (purple) e permite filtrar por TRIAGEM"
    - "O detalhe do processo mostra uma seccao de conflict check com nivel, decisor, data, justificativa e matches"
  artifacts:
    - path: "web/src/app/(dashboard)/processos/novo/page.tsx"
      provides: "Wizard de 3 passos do novo processo"
      contains: "Conflict Check"
    - path: "web/src/app/(dashboard)/processos/page.tsx"
      provides: "Badge e filtro TRIAGEM"
      contains: "TRIAGEM"
    - path: "web/src/app/(dashboard)/processos/[id]/page.tsx"
      provides: "Seccao de conflict check no detalhe"
      contains: "Conflict Check"
  key_links:
    - from: "web/src/app/(dashboard)/processos/novo/page.tsx"
      to: "useCreateIntake / useRunConflictCheck / useRegistarDecisaoConflito / useFormalizarProcesso"
      via: "hooks do Plan 02"
      pattern: "useFormalizarProcesso"
    - from: "web/src/app/(dashboard)/processos/[id]/page.tsx"
      to: "useConflictCheckDecisao"
      via: "query da decisao guardada"
      pattern: "useConflictCheckDecisao"
---

<objective>
Construir a superficie UI da fase: substituir /processos/novo por um wizard de 3 passos (Intake -> Conflict Check -> Abertura), adicionar o badge/filtro EM TRIAGEM a listagem, e adicionar a seccao de Conflict Check ao detalhe do processo. Toda a logica de bloqueio vive no backend (Plan 01); a UI apenas reflete o estado e desabilita acoes com motivo.

Purpose: Torna o intake e o conflict check usaveis e visiveis e consistentes entre listagem e detalhe (Success Criteria 3 do ROADMAP; CONTEXT.md superficie UI). Cobre a vertente de UI de INT-01 e CFL-01.
Output: 3 ficheiros de pagina modificados/substituidos, todos consumindo os hooks/tipos/utilitario do Plan 02.
</objective>

<execution_context>
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/LEXCV-32-processos-intake-e-conflict-check/32-PATTERNS.md
@.planning/phases/LEXCV-32-processos-intake-e-conflict-check/32-UI-SPEC.md

<interfaces>
<!-- Tudo abaixo foi produzido pelo Plan 32-02 (wave 2) e esta garantido. -->

Hooks (web/src/hooks/use-processos.ts):
  useCreateIntake()                    -> mutation, mutateAsync(payload: ProcessoCreateRequest) => Processo (estado TRIAGEM)
  useRunConflictCheck(processoId)      -> mutation, mutateAsync() => ConflictCheckResponse
  useRegistarDecisaoConflito(id)       -> mutation, mutateAsync(req: ConflictCheckDecisaoRequest) => ConflictCheckDecisao
  useConflictCheckDecisao(processoId)  -> useQuery => ConflictCheckDecisao | null
  useFormalizarProcesso(processoId)    -> mutation, mutateAsync() => Processo (estado ATIVO) ou lanca em 4xx

Utilitario (web/src/lib/conflict-check.ts):
  conflictNivelToVariant(nivel) -> "green"|"amber"|"blue"|"red"
  conflictNivelToLabel(nivel)   -> "SEM CONFLITO"|"POTENCIAL"|"SANÁVEL"|"IMPEDITIVO"

Tipos (web/src/types/processos.ts): ConflictNivel, ConflictMatch, ConflictCheckResponse, ConflictCheckDecisao, ConflictCheckDecisaoRequest
Schema (web/src/schemas/processos.ts): conflictCheckDecisaoFormSchema, ConflictCheckDecisaoFormValues, conflictNivelEnum, processoFormSchema, ProcessoFormValues

Padroes de pagina existentes:
  novo/page.tsx (217 linhas): permission gate usePermissions().can.create("processos"); AccessDeniedState; RHF + processoFormSchema; useClientes({}); router.push(`/processos/${id}`); serverError state com <p className="text-sm text-red-600">; selectClassName/textareaClassName constantes ja definidas no topo
  page.tsx (371 linhas): estadoVariant switch (~304-312); filtro estado <select> (~215-226); Badge render (~335)
  [id]/page.tsx (544 linhas): dl grid card (~211-218); Card/CardHeader/CardContent; guards canEditProcessos; useProcesso(id)

CSS Anti-Safe Harbor obrigatorio em elementos novos: rounded-none; font-bold tracking-wide (badges/CTAs); focus-visible:ring-blue-500 (inputs); shadow-none (CTAs).
permissions: usePermissions().can.create("processos") e .can.manage("processos")
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wizard de 3 passos em /processos/novo</name>
  <files>web/src/app/(dashboard)/processos/novo/page.tsx</files>
  <read_first>
    - web/src/app/(dashboard)/processos/novo/page.tsx (ficheiro completo — gate, RHF, useClientes, serverError, classes)
    - web/src/hooks/use-processos.ts (hooks do Plan 02)
    - web/src/lib/conflict-check.ts (helpers de nivel)
    - web/src/schemas/processos.ts (processoFormSchema, conflictCheckDecisaoFormSchema)
    - .planning/phases/LEXCV-32-processos-intake-e-conflict-check/32-UI-SPEC.md (Multi-Step Intake Flow linhas 88-151; Copywriting linhas 154-187; Interaction States linhas 244-254)
    - .planning/phases/LEXCV-32-processos-intake-e-conflict-check/32-PATTERNS.md (novo/page.tsx linhas 480-550; conflict match row + blocked button)
  </read_first>
  <action>
    Substituir o conteudo de novo/page.tsx por um wizard de 3 passos na mesma rota (sem navegacao de router entre passos — estado local `const [step, setStep] = React.useState<1|2|3>(1)`). Manter o permission gate no topo (`usePermissions().can.create("processos")` -> AccessDeniedState). Calcular tambem `canManageProcessos = permissions.can.manage("processos")`.

    Step indicator (topo): 3 passos "1 Intake" / "2 Conflict Check" / "3 Abertura". Passo ativo: circulo 32px `bg-blue-600 text-white rounded-none`; passo concluido: `bg-emerald-600 text-white` com icone lucide `Check` 14px; passo futuro: `border border-slate-300 dark:border-slate-700 text-slate-400`. Conector 1px slate-200/700, lucide `ChevronRight`. Sem componente novo — inline com divs (UI-SPEC "No Analog Found").

    Step 1 (Intake): Card "Dados de Intake". RHF com `processoFormSchema` e useClientes({}) para o select de Cliente. Campos conforme UI-SPEC linhas 102-111 (Cliente select, Tipo de Processo select, Número, Área Jurídica, Tribunal, Data Início, Data Fim, Descrição). CTA "Continuar para Conflict Check": no submit chamar `useCreateIntake().mutateAsync(values)` (NAO enviar estado — o backend forca TRIAGEM), guardar `processoId = res.id`, e `setStep(2)`. "Cancelar" liga a /processos. Em erro: `setServerError(e.message)` com `<p className="text-sm text-red-600">`. Reutilizar selectClassName/textareaClassName ja no topo do ficheiro.

    Step 2 (Conflict Check): Card "Verificação de Conflitos". Botao "Executar Conflict Check" (primary) so habilitado com `canCreateProcessos`; ao clicar chama `useRunConflictCheck(processoId).mutateAsync()` e guarda o resultado em estado local; durante o loading mostrar "A verificar conflitos...". Renderizar os matches como linhas com borda (`border border-slate-200 dark:border-slate-800 rounded-none p-4`): badge de nivel via `conflictNivelToVariant`/`conflictNivelToLabel`, nome+tipo da entidade, e link para o registo (cliente -> /clientes/{id}). Estado sem matches: linha verde "Sem conflitos detectados" + copy de UI-SPEC linha 177. Formulario de decisao (so com `canManageProcessos`): RHF com `conflictCheckDecisaoFormSchema` — Select Nível final (4 niveis), Textarea Justificativa, Decisor (read-only = utilizador atual), Data (read-only = hoje), Input Referência (opcional); CTA "Registar Decisão" chama `useRegistarDecisaoConflito(processoId).mutateAsync(values)`. Apos decisao registada com nivel != impeditivo, habilitar "Continuar para Abertura" (`setStep(3)`); se nivel impeditivo, manter desabilitado (`opacity-50 cursor-not-allowed`) e mostrar o callout amber (UI-SPEC linha 138). Se o utilizador nao tiver `processos:manage`, mostrar o botao "Registar Decisão" desabilitado com a mensagem de UI-SPEC linha 184.

    Step 3 (Abertura): Card "Revisão e Abertura" com `dl` grid read-only dos dados do intake + decisao (nivel badge, decisor, data, justificativa). CTA "Formalizar Processo" chama `useFormalizarProcesso(processoId).mutateAsync()`; em sucesso `router.push(`/processos/${processoId}`)`. O botao fica `disabled opacity-50 cursor-not-allowed` quando nao ha decisao ou nivel impeditivo, com `<p className="text-sm text-red-600">` mostrando a mensagem exata de UI-SPEC linha 148/181/182. Como o backend e a fonte de verdade, se o mutateAsync lancar (4xx), mostrar a `message` do erro inline (nao confiar so no disable).

    Aplicar as copy strings exatamente como em UI-SPEC "Copywriting Contract" (linhas 154-187). Todos os elementos interativos `rounded-none`; CTAs `font-bold shadow-none`; focus rings `ring-blue-500`.
  </action>
  <verify>
    <automated>cd web && pnpm exec tsc --noEmit 2>&1 | tail -15 && grep -c "useCreateIntake\|useRunConflictCheck\|useRegistarDecisaoConflito\|useFormalizarProcesso\|conflictNivelToLabel" "src/app/(dashboard)/processos/novo/page.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - `tsc --noEmit` sem erros.
    - novo/page.tsx usa os 4 hooks de acao (useCreateIntake, useRunConflictCheck, useRegistarDecisaoConflito, useFormalizarProcesso) e o helper conflictNivelToLabel — grep >= 5.
    - O Step 1 NAO envia `estado` no payload do intake (o backend forca TRIAGEM).
    - O botao "Formalizar Processo" tem estado disabled com `opacity-50 cursor-not-allowed` e uma `<p className="text-sm text-red-600">` com motivo quando nao ha decisao ou nivel impeditivo.
    - As copy strings "Continuar para Conflict Check", "Executar Conflict Check", "Registar Decisão", "Formalizar Processo" aparecem no ficheiro.
    - O formulario de decisao usa `conflictCheckDecisaoFormSchema`.
  </acceptance_criteria>
  <done>Wizard de 3 passos funcional consumindo os hooks; bloqueio de formalizar refletido visualmente com motivo; copy alinhada ao UI-SPEC.</done>
</task>

<task type="auto">
  <name>Task 2: Badge EM TRIAGEM e filtro na listagem de processos</name>
  <files>web/src/app/(dashboard)/processos/page.tsx</files>
  <read_first>
    - web/src/app/(dashboard)/processos/page.tsx (estadoVariant switch ~304-312; filtro estado <select> ~215-226; Badge render ~335)
    - web/src/components/ui/badge.tsx (confirmar variant "purple" existe)
    - .planning/phases/LEXCV-32-processos-intake-e-conflict-check/32-UI-SPEC.md (Processos Listing: Triagem Badge linhas 191-204)
    - .planning/phases/LEXCV-32-processos-intake-e-conflict-check/32-PATTERNS.md (page.tsx triagem badge linhas 554-595)
  </read_first>
  <action>
    No `estadoVariant` (switch/ternario por estado) adicionar o caso `estado === "TRIAGEM" -> "purple"`. Adicionar um `estadoLabel` que mapeia `TRIAGEM` -> "EM TRIAGEM" (restantes mantem o valor existente). No `<Badge>` renderizar `estadoLabel` em vez do estado cru, mantendo as classes `rounded-none font-bold tracking-wide` e adicionando "purple" ao type union do variant. No `<select>` do filtro de estado, acrescentar `<option value="TRIAGEM">Em triagem</option>` antes de "Ativo". Nao alterar a logica de fetch/filtro existente — apenas a nova opcao e a apresentacao.
  </action>
  <verify>
    <automated>cd web && pnpm exec tsc --noEmit 2>&1 | tail -10 && grep -c "TRIAGEM\|EM TRIAGEM\|purple" "src/app/(dashboard)/processos/page.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - `tsc --noEmit` sem erros.
    - page.tsx contem `"TRIAGEM"`, o label `"EM TRIAGEM"`, e o variant `"purple"`.
    - O `<select>` de filtro contem `<option value="TRIAGEM">`.
    - O type union do variant do Badge inclui "purple".
  </acceptance_criteria>
  <done>Listagem mostra badge EM TRIAGEM (purple) e oferece a opcao de filtro Em triagem.</done>
</task>

<task type="auto">
  <name>Task 3: Seccao de Conflict Check no detalhe do processo</name>
  <files>web/src/app/(dashboard)/processos/[id]/page.tsx</files>
  <read_first>
    - web/src/app/(dashboard)/processos/[id]/page.tsx (dl grid card ~211-218; Card/CardHeader/CardContent; guards canEditProcessos; useProcesso(id))
    - web/src/hooks/use-processos.ts (useConflictCheckDecisao, useFormalizarProcesso)
    - web/src/lib/conflict-check.ts (helpers de nivel)
    - .planning/phases/LEXCV-32-processos-intake-e-conflict-check/32-UI-SPEC.md (Processo Detail: Conflict Check Section linhas 208-220)
    - .planning/phases/LEXCV-32-processos-intake-e-conflict-check/32-PATTERNS.md ([id]/page.tsx conflict section linhas 599-644)
  </read_first>
  <action>
    Adicionar um Card de Conflict Check entre o Card "Dados" e o painel de tabs (partes/fases/movimentacoes). Calcular `canManageProcessos = permissions.can.manage("processos")` e usar `useConflictCheckDecisao(id)` para obter a decisao guardada. Visibilidade: renderizar o Card quando `processo.estado === "TRIAGEM"` OU existe decisao (`decisao.data`). CardHeader: titulo "Conflict Check" + badge de nivel (right-aligned) via `conflictNivelToVariant`/`conflictNivelToLabel` quando ha decisao. CardContent: `dl` grid (mesmo padrao do card Dados) com decisor (`decisao.decisorId` ou nome quando disponivel), data (`decisao.dataDecisao`), referencia se presente; bloco "Justificativa" (`<p>` label + valor). Estado vazio (TRIAGEM sem decisao): `text-sm text-slate-500 dark:text-slate-400` com "O conflict check ainda não foi executado para este processo." Se `estado === "TRIAGEM"` e `canManageProcessos`, mostrar botao inline "Formalizar Processo" (`rounded-none font-bold shadow-none`) que chama `useFormalizarProcesso(id).mutateAsync()`; desabilitado com `opacity-50 cursor-not-allowed` + `<p className="text-sm text-red-600">` motivo quando nivel impeditivo ou sem decisao (mesmas mensagens do UI-SPEC). Em erro do mutate (4xx), mostrar a `message` inline. Usar o mesmo `conflictNivelToVariant`/`conflictNivelToLabel` (fonte unica) para garantir consistencia listagem<->detalhe.
  </action>
  <verify>
    <automated>cd web && pnpm exec tsc --noEmit 2>&1 | tail -15 && grep -c "useConflictCheckDecisao\|Conflict Check\|conflictNivelToLabel\|useFormalizarProcesso" "src/app/(dashboard)/processos/[id]/page.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - `tsc --noEmit` sem erros.
    - [id]/page.tsx usa `useConflictCheckDecisao` e `useFormalizarProcesso` e o helper `conflictNivelToLabel` — grep >= 3.
    - O Card so e visivel quando estado === "TRIAGEM" OU existe decisao.
    - Estado vazio mostra "O conflict check ainda não foi executado para este processo."
    - O botao Formalizar inline tem disabled + motivo `text-red-600` quando impeditivo ou sem decisao.
    - As badges de nivel usam o mesmo helper que a listagem (lib/conflict-check.ts) — consistencia garantida.
  </acceptance_criteria>
  <done>Detalhe mostra a decisao/estado do conflict check de forma consistente com a listagem e permite formalizar (gated) a partir do detalhe.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser UI -> /api/v1 | A UI dispara acoes; nao impoe regras de negocio |
| visibilidade de acoes por permission | Botoes gated por hasScopedPermission devem coincidir com @PreAuthorize do backend |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-08 | Tampering | botao Formalizar (wizard e detalhe) | mitigate | O disable e cosmetico; a regra real e o backend (Plan 01). A UI mostra a message do 4xx quando o backend rejeita, nunca assumindo sucesso pelo estado local. |
| T-32-09 | Elevation of Privilege | acoes de decisao/formalizar visiveis | mitigate | Botoes Registar Decisão e Formalizar gated por usePermissions().can.manage("processos"), espelhando @PreAuthorize('processos:manage'); Executar Conflict Check gated por can.create. |
| T-32-10 | Information Disclosure | links de matches para registos | accept | Os matches vem do endpoint tenant-scoped (Plan 01); a UI apenas linka entidades ja devolvidas dentro do tenant. |
| T-32-SC | Tampering | pnpm installs | mitigate | Sem novos pacotes (apenas shadcn/lucide ja presentes) — sem superficie de supply-chain. |
</threat_model>

<verification>
- `pnpm exec tsc --noEmit` em web/ passa apos os 3 tasks.
- `pnpm build` em web/ compila sem erros.
- Smoke manual: /processos/novo percorre os 3 passos; com decisao impeditiva o botao Formalizar fica bloqueado com motivo; a listagem mostra EM TRIAGEM e filtra por triagem; o detalhe de um processo em TRIAGEM mostra a seccao de conflito.
</verification>

<success_criteria>
- Success Criteria 1 (ROADMAP): intake estruturado preenchivel no wizard, processo nasce em TRIAGEM.
- Success Criteria 2: abertura formal bloqueada na UI (refletindo o backend) ate decisao registada; impeditivo nunca formaliza.
- Success Criteria 3: estado de triagem/decisao visivel e consistente entre listagem (badge/filtro) e detalhe (seccao), usando a mesma fonte de verdade de nivel.
</success_criteria>

<output>
Create `.planning/phases/LEXCV-32-processos-intake-e-conflict-check/32-03-SUMMARY.md` when done
</output>
