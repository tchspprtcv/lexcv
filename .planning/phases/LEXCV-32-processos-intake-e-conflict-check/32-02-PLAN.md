---
phase: 32-processos-intake-e-conflict-check
plan: 02
type: execute
wave: 2
depends_on: ["32-01"]
files_modified:
  - web/src/types/processos.ts
  - web/src/schemas/processos.ts
  - web/src/hooks/use-processos.ts
  - web/src/lib/conflict-check.ts
autonomous: true
requirements: [INT-01, CFL-01]

must_haves:
  truths:
    - "O frontend tem tipos para ConflictNivel, ConflictMatch, ConflictCheckResponse e ConflictCheckDecisao"
    - "Existe um schema Zod que exige justificativa quando o nivel e potencial ou sanavel"
    - "Existem hooks para criar intake (TRIAGEM), correr conflict check, registar decisao, ler decisao e formalizar"
    - "Existe uma fonte unica de verdade para o mapa nivel->variant de badge e nivel->label"
  artifacts:
    - path: "web/src/types/processos.ts"
      provides: "Tipos de conflict check"
      contains: "ConflictNivel"
    - path: "web/src/schemas/processos.ts"
      provides: "Schema da decisao com justificativa condicional"
      contains: "conflictCheckDecisaoFormSchema"
    - path: "web/src/hooks/use-processos.ts"
      provides: "Hooks de intake/conflict-check/decisao/formalizar"
      contains: "useFormalizarProcesso"
    - path: "web/src/lib/conflict-check.ts"
      provides: "Helpers de variant/label de nivel (fonte unica)"
      contains: "conflictNivelToVariant"
  key_links:
    - from: "web/src/hooks/use-processos.ts"
      to: "/processos/{id}/formalizar"
      via: "apiFetch POST em useFormalizarProcesso"
      pattern: "formalizar"
    - from: "web/src/hooks/use-processos.ts"
      to: "/processos/{id}/conflict-check"
      via: "apiFetch POST em useRunConflictCheck"
      pattern: "conflict-check"
---

<objective>
Construir a camada de dados/contratos do frontend que a UI do wizard e do detalhe vao consumir: tipos TypeScript, schema Zod da decisao, hooks TanStack Query para os 5 endpoints backend, e um utilitario unico para o mapeamento de nivel de conflito -> variant/label de badge.

Purpose: Separar contratos da UI evita o anti-padrao "scavenger hunt" — o Plan 03 (wizard/listagem/detalhe) recebe hooks e tipos prontos. O utilitario conflict-check e a fonte unica de verdade partilhada entre Step 2, Step 3 e detalhe (CONTEXT.md specifics). Cobre a vertente frontend de INT-01 e CFL-01.
Output: 1 ficheiro novo (lib/conflict-check.ts) + 3 modificados (types, schemas, hooks).
</objective>

<execution_context>
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/LEXCV-32-processos-intake-e-conflict-check/32-PATTERNS.md
@.planning/phases/LEXCV-32-processos-intake-e-conflict-check/32-UI-SPEC.md

<interfaces>
<!-- Contratos backend produzidos pelo Plan 32-01, ja garantidos em wave 1. -->

Endpoints backend disponiveis (todos sob /api/v1, cookie auth via apiFetch):
  POST /processos/intake                         (processos:create)  body: Processo intake -> 201 Processo (estado=TRIAGEM)
  POST /processos/{id}/conflict-check            (processos:create)  -> 200 { matches: [...], nivelSugerido }
  POST /processos/{id}/conflict-check/decisao    (processos:manage)  body: { nivel, justificativa?, referenciaEvidencia? } -> 200 decisao
  GET  /processos/{id}/conflict-check/decisao    (processos:view)    -> 200 decisao | null
  POST /processos/{id}/formalizar                (processos:manage)  -> 200 Processo (estado=ATIVO) | 4xx { message }

IMPORTANTE — convencao de serializacao: a entidade JPA ConflictCheckDecisao e serializada pelo Jackson por defeito em camelCase. Os tipos TypeScript da decisao DEVEM usar camelCase (tenantId, processoId, createdAt) para coincidir com o JSON real; usar snake_case faria esses campos vir undefined em runtime.

Padroes frontend existentes a reutilizar (web/src/hooks/use-processos.ts):
  import { apiFetch } from "@/lib/api"  — wrapper unico, credentials include, mostra toast em erros nao-401/403
  useCreateProcesso (mutation), normalizeProcesso(response as ProcessoApi), queryKeys ["processos","detail",id] e ["processos","list"]
  import type { Processo, ProcessoCreateRequest, ... } from "@/types/processos"

web/src/schemas/processos.ts ja tem o helper:
  const optionalTrimmedString = z.string().trim().transform(v => v.length ? v : undefined).optional();

web/src/components/ui/badge.tsx variants existentes: green, amber, blue, red, purple, gray, secondary
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Tipos e schema Zod do conflict check</name>
  <files>web/src/types/processos.ts, web/src/schemas/processos.ts</files>
  <read_first>
    - web/src/types/processos.ts (estilo das interfaces existentes; interface Processo)
    - web/src/schemas/processos.ts (optionalTrimmedString linhas 3-7; processoFormSchema)
    - web/src/schemas/clientes.ts (padrao superRefine + ctx.addIssue com path)
    - .planning/phases/LEXCV-32-processos-intake-e-conflict-check/32-PATTERNS.md (seccoes types linhas 288-331, schema linhas 335-382)
  </read_first>
  <behavior>
    - conflictCheckDecisaoFormSchema.parse({ nivel: "sem_conflito" }) -> sucesso (justificativa nao exigida)
    - conflictCheckDecisaoFormSchema.parse({ nivel: "potencial" }) -> falha com issue em path ["justificativa"]
    - conflictCheckDecisaoFormSchema.parse({ nivel: "sanavel" }) -> falha (justificativa obrigatoria)
    - conflictCheckDecisaoFormSchema.parse({ nivel: "potencial", justificativa: "motivo X" }) -> sucesso
    - conflictNivelEnum aceita exatamente sem_conflito|potencial|sanavel|impeditivo e rejeita outros valores
  </behavior>
  <action>
    Em `web/src/types/processos.ts` acrescentar (no fim, mantendo o estilo das interfaces existentes): `export type ConflictNivel = "sem_conflito" | "potencial" | "sanavel" | "impeditivo"`; `interface ConflictMatch` com `entidadeId: string; entidadeTipo: "cliente" | "parte"; nome: string; nif?: string; nivelConflito: ConflictNivel; motivo?: string`; `interface ConflictCheckResponse` com `matches: ConflictMatch[]; nivelSugerido: ConflictNivel`; `interface ConflictCheckDecisao` com `id: string; tenantId: string; processoId: string; nivel: ConflictNivel; justificativa?: string; decisorId: string; dataDecisao: string; referenciaEvidencia?: string; createdAt: string`; `interface ConflictCheckDecisaoRequest` com `nivel: ConflictNivel; justificativa?: string; referenciaEvidencia?: string`.

    CRITICO — usar camelCase em TODOS os campos da decisao (`tenantId`, `processoId`, `createdAt`, alem dos ja camelCase `decisorId`, `dataDecisao`, `referenciaEvidencia`). A entidade JPA ConflictCheckDecisao do backend (Plan 32-01) e serializada pelo Jackson por defeito em camelCase; usar snake_case (`tenant_id`/`processo_id`/`created_at`) faria estes campos vir `undefined` em runtime. Nenhum tipo frontend desta fase deve usar snake_case para campos que o backend emite em camelCase.

    Em `web/src/schemas/processos.ts` acrescentar `export const conflictNivelEnum = z.enum(["sem_conflito","potencial","sanavel","impeditivo"])` e `export const conflictCheckDecisaoFormSchema = z.object({ nivel: conflictNivelEnum, justificativa: optionalTrimmedString, referenciaEvidencia: optionalTrimmedString }).superRefine((data, ctx) => { ... })`. No superRefine: quando `data.nivel === "potencial" || data.nivel === "sanavel"` e `!data.justificativa`, emitir `ctx.addIssue({ code: z.ZodIssueCode.custom, message: "Justificativa é obrigatória para este nível de conflito", path: ["justificativa"] })`. Exportar `export type ConflictCheckDecisaoFormValues = z.infer<typeof conflictCheckDecisaoFormSchema>`. Reutilizar o `optionalTrimmedString` ja existente (nao redeclarar).
  </action>
  <verify>
    <automated>cd web && pnpm exec tsc --noEmit 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `tsc --noEmit` nao reporta erros nos ficheiros types/processos.ts e schemas/processos.ts.
    - `types/processos.ts` contem `export type ConflictNivel` e `interface ConflictCheckDecisao`.
    - `interface ConflictCheckDecisao` usa camelCase (`tenantId`, `processoId`, `createdAt`) — NAO snake_case — para coincidir com a serializacao Jackson do backend.
    - `schemas/processos.ts` contem `conflictCheckDecisaoFormSchema` com um `superRefine` que referencia `path: ["justificativa"]`.
    - O comportamento do `<behavior>` e satisfeito (justificativa condicional a potencial/sanavel).
  </acceptance_criteria>
  <done>Tipos e schema Zod compilam; campos da decisao em camelCase alinhados com o backend; justificativa obrigatoria apenas para potencial/sanavel.</done>
</task>

<task type="auto">
  <name>Task 2: Helper de nivel (fonte unica) e hooks TanStack Query</name>
  <files>web/src/lib/conflict-check.ts, web/src/hooks/use-processos.ts</files>
  <read_first>
    - web/src/hooks/use-processos.ts (import block linhas 1-16; useCreateProcesso linhas ~156-171; normalizeProcesso; queryKeys; padrao useQuery de sub-recurso)
    - web/src/lib/api.ts (apiFetch — assinatura e tratamento de erro)
    - web/src/types/processos.ts (tipos criados no Task 1)
    - .planning/phases/LEXCV-32-processos-intake-e-conflict-check/32-PATTERNS.md (seccao hooks linhas 386-476; Badge helper linhas 701-726)
    - .planning/phases/LEXCV-32-processos-intake-e-conflict-check/32-UI-SPEC.md (mapa nivel->variant linhas 73-84; labels linhas 171-175)
  </read_first>
  <action>
    Criar `web/src/lib/conflict-check.ts` exportando a fonte unica de verdade do mapeamento de nivel: `conflictNivelToVariant(nivel: ConflictNivel)` devolvendo "green" (sem_conflito), "amber" (potencial), "blue" (sanavel), "red" (impeditivo); e `conflictNivelToLabel(nivel: ConflictNivel)` devolvendo "SEM CONFLITO" / "POTENCIAL" / "SANÁVEL" / "IMPEDITIVO". Importar `ConflictNivel` de `@/types/processos`. Estes valores tem de coincidir exatamente com UI-SPEC (linhas 73-84 e 171-175).

    Em `web/src/hooks/use-processos.ts`, acrescentar ao import de `@/types/processos` os novos tipos (ConflictCheckResponse, ConflictCheckDecisao, ConflictCheckDecisaoRequest). Adicionar os hooks:
    - `useCreateIntake()` — mutation que faz `apiFetch<Processo>("/processos/intake", { method: "POST", body: JSON.stringify(payload) })`, normaliza via normalizeProcesso, e em onSuccess invalida ["processos","list"]. O payload segue ProcessoCreateRequest (sem estado — o backend forca TRIAGEM).
    - `useRunConflictCheck(processoId)` — mutation POST `/processos/${encodeURIComponent(processoId)}/conflict-check` devolvendo ConflictCheckResponse; onSuccess invalida ["processos","conflict-check",processoId].
    - `useRegistarDecisaoConflito(processoId)` — mutation que recebe ConflictCheckDecisaoRequest, POST `/processos/${id}/conflict-check/decisao`, devolve ConflictCheckDecisao; onSuccess invalida ["processos","conflict-check",processoId] e ["processos","detail",processoId].
    - `useConflictCheckDecisao(processoId)` — useQuery key ["processos","conflict-check",processoId], queryFn GET `/processos/${id}/conflict-check/decisao` devolvendo ConflictCheckDecisao | null, enabled quando ha processoId e typeof window !== "undefined", staleTime 15000.
    - `useFormalizarProcesso(processoId)` — mutation POST `/processos/${id}/formalizar` devolvendo Processo (normalizeProcesso); onSuccess invalida ["processos","list"] e setQueryData(["processos","detail",processoId], updated).
    Seguir o padrao exato de useCreateProcesso para a forma de mutation/queryClient. NAO adicionar toast manual — apiFetch ja trata erros nao-401/403; a UI (Plan 03) faz display inline via try/catch.
  </action>
  <verify>
    <automated>cd web && pnpm exec tsc --noEmit 2>&1 | tail -15 && grep -c "useFormalizarProcesso\|useRunConflictCheck\|useCreateIntake\|useRegistarDecisaoConflito\|useConflictCheckDecisao" src/hooks/use-processos.ts</automated>
  </verify>
  <acceptance_criteria>
    - `tsc --noEmit` sem erros.
    - `lib/conflict-check.ts` exporta `conflictNivelToVariant` e `conflictNivelToLabel` com os 4 niveis mapeados para green/amber/blue/red e labels SEM CONFLITO/POTENCIAL/SANÁVEL/IMPEDITIVO.
    - grep dos 5 hooks em use-processos.ts retorna >= 5.
    - Os endpoints chamados pelos hooks correspondem exatamente a /processos/intake, /conflict-check, /conflict-check/decisao (POST e GET) e /formalizar.
    - Nenhum hook adiciona toast manual (apiFetch trata).
  </acceptance_criteria>
  <done>Helper de nivel (fonte unica) + 5 hooks compilam e apontam para os endpoints corretos com as queryKeys de invalidacao.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| frontend hooks -> /api/v1 (same-origin rewrite) | Os hooks nao impoem regras; o backend e a fonte de verdade |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-06 | Tampering | useFormalizarProcesso | accept | O hook so dispara o POST; o bloqueio TRIAGEM->ATIVO e imposto no backend (Plan 01). O frontend nao replica a regra. |
| T-32-07 | Spoofing | decisorId na decisao | mitigate | ConflictCheckDecisaoRequest NAO inclui decisorId; o backend deriva o decisor do SecurityContext. O frontend nunca envia identidade. |
| T-32-SC | Tampering | pnpm installs | mitigate | Sem novos pacotes (apenas TanStack Query/Zod ja presentes) — sem superficie de supply-chain. |
</threat_model>

<verification>
- `pnpm exec tsc --noEmit` em web/ passa.
- `pnpm lint` nao introduz novos erros nos 4 ficheiros tocados.
</verification>

<success_criteria>
- Os contratos frontend (tipos + schema) e os 5 hooks existem e compilam, prontos para o Plan 03 consumir sem explorar o codebase.
- Os tipos da decisao usam camelCase, coincidindo com a serializacao Jackson do backend (sem campos undefined em runtime).
- A fonte unica de verdade nivel->variant/label existe em lib/conflict-check.ts.
</success_criteria>

<output>
Create `.planning/phases/LEXCV-32-processos-intake-e-conflict-check/32-02-SUMMARY.md` when done
</output>
