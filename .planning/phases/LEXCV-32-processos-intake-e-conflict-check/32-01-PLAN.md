---
phase: 32-processos-intake-e-conflict-check
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/main/java/com/lexcv/models/ConflictCheckDecisao.java
  - backend/src/main/java/com/lexcv/repositories/ConflictCheckDecisaoRepository.java
  - backend/src/main/java/com/lexcv/dtos/ConflictCheckRequest.java
  - backend/src/main/java/com/lexcv/dtos/ConflictCheckResponse.java
  - backend/src/main/java/com/lexcv/controllers/ResourceController.java
  - backend/src/main/java/com/lexcv/seed/DatabaseSeeder.java
autonomous: true
requirements: [INT-01, CFL-01]

must_haves:
  truths:
    - "Um processo criado pelo fluxo de intake nasce em estado TRIAGEM (nunca ATIVO)"
    - "Correr o conflict check devolve uma lista de matches (clientes + partes) e um nivel sugerido, apenas dentro do tenant do utilizador"
    - "A decisao de conflito e persistida com decisor, data, nivel final, justificativa e referencia opcional"
    - "A formalizacao (TRIAGEM -> ATIVO) e rejeitada pelo backend enquanto nao existir decisao registada"
    - "A formalizacao e rejeitada pelo backend quando o nivel registado e impeditivo"
    - "Correr o conflict check exige processos:create; registar decisao e formalizar exigem processos:manage"
  artifacts:
    - path: "backend/src/main/java/com/lexcv/models/ConflictCheckDecisao.java"
      provides: "Entidade JPA da decisao de conflito (tenant-scoped, 1 por processo)"
      contains: "class ConflictCheckDecisao"
    - path: "backend/src/main/java/com/lexcv/repositories/ConflictCheckDecisaoRepository.java"
      provides: "Repositorio com finder tenant+processo"
      contains: "findByTenantIdAndProcessoId"
    - path: "backend/src/main/java/com/lexcv/controllers/ResourceController.java"
      provides: "Endpoints intake/conflict-check/decisao/formalizar"
      contains: "formalizar"
    - path: "backend/src/main/java/com/lexcv/seed/DatabaseSeeder.java"
      provides: "Permissoes processos:create e processos:manage seeded e atribuidas a roles"
      contains: "processos:create"
  key_links:
    - from: "ResourceController.formalizarProcesso"
      to: "ConflictCheckDecisaoRepository.findByTenantIdAndProcessoId"
      via: "consulta da decisao antes de permitir a transicao"
      pattern: "findByTenantIdAndProcessoId"
    - from: "ResourceController.runConflictCheck"
      to: "clienteRepository.findByTenantId / parteRepository"
      via: "pesquisa tenant-scoped para matches"
      pattern: "findByTenantId"
---

<objective>
Construir a camada backend (fonte de verdade) do intake estruturado e do conflict check bloqueante: a entidade de decisao, o repositorio, os DTOs, as permissoes RBAC novas, e os quatro endpoints (intake em TRIAGEM, conflict-check, registo de decisao, formalizar) com o bloqueio da transicao TRIAGEM -> ATIVO imposto no servidor.

Purpose: O backend e o unico ponto de enforcement da regra de bloqueio (CONTEXT.md "specifics": a transicao TRIAGEM -> ativo e o ponto unico de enforcement). O frontend e passivo. Cobre INT-01 (intake estruturado + estado pre-formal) e CFL-01 (conflict check + bloqueio ate decisao registada).
Output: 4 ficheiros novos (model, repository, 2 DTOs) + 2 ficheiros modificados (ResourceController, DatabaseSeeder), 3 endpoints novos e 1 endpoint de intake.
</objective>

<execution_context>
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/LEXCV-32-processos-intake-e-conflict-check/32-CONTEXT.md
@.planning/phases/LEXCV-32-processos-intake-e-conflict-check/32-PATTERNS.md

<interfaces>
<!-- Contratos existentes que o executor consome diretamente. Nao explorar o codebase. -->

Processo (backend/.../models/Processo.java) — campos relevantes:
  UUID id; UUID tenantId; UUID clienteId; String numeroProcesso; String tipoProcesso;
  String areaJuridica; String tribunal; String estado; LocalDate dataInicio; LocalDate dataFim;
  String descricao; LocalDateTime createdAt (via @PrePersist)
  -> @GeneratedValue(strategy = GenerationType.UUID); @Table(name = "t_processo")

Cliente (backend/.../models/Cliente.java) — campos relevantes para matches:
  UUID id; UUID tenantId; String tipo; String nome; String nif; Boolean ativo

Parte (backend/.../models/Parte.java) — campos relevantes para matches:
  Integer id; UUID processoId; String nome; String tipo   (NAO tem nif)

UserPrincipal (backend/.../config/UserPrincipal.java) — @Getter Lombok:
  UUID getUserId(); UUID getTenantId()

ResourceController helpers ja existentes:
  private UUID getTenantId()  (linhas 54-58) -> le UserPrincipal do SecurityContext
  Guard tenant (linhas 579-581): if (processo == null || !processo.getTenantId().equals(getTenantId())) -> 404 Map.of("message", "Processo não encontrado")
  createProcesso (linhas 568-573): @PostMapping("/processos"), @PreAuthorize("hasAuthority('processos:edit')"), faz processo.setTenantId(getTenantId()); save; 201
  mergeClientes (linhas 429-497): exemplo de retorno Map.of(...) e logica de match

Repositorios existentes:
  ProcessoRepository: findByTenantId(UUID), findById(UUID)
  ClienteRepository: findByTenantId(UUID), findByTenantIdAndNif(UUID, String)
  ParteRepository: findByProcessoId(UUID)

DatabaseSeeder.seedRbac() (linhas 294-340):
  permKeys (linha 294) ja contem "processos:view","processos:edit" mas NAO "processos:create"/"processos:manage"
  upsertRolePermissions("ADMIN", permissionMap.values())  -> ADMIN recebe tudo automaticamente
  upsertRolePermissions("ADVOGADO", Arrays.asList(... processos:view, processos:edit ...))  -> linha 329-340
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Entidade, repositorio e DTOs da decisao de conflito</name>
  <files>backend/src/main/java/com/lexcv/models/ConflictCheckDecisao.java, backend/src/main/java/com/lexcv/repositories/ConflictCheckDecisaoRepository.java, backend/src/main/java/com/lexcv/dtos/ConflictCheckResponse.java, backend/src/main/java/com/lexcv/dtos/ConflictCheckRequest.java</files>
  <read_first>
    - backend/src/main/java/com/lexcv/models/Processo.java (analogo de entidade: tenant_id, @GeneratedValue UUID, @PrePersist createdAt, Lombok)
    - backend/src/main/java/com/lexcv/repositories/ProcessoRepository.java (analogo de repositorio: findByTenantId)
    - backend/src/main/java/com/lexcv/repositories/ClienteRepository.java (analogo do finder findByTenantIdAndNif)
    - backend/src/main/java/com/lexcv/dtos/ClienteMergeRequest.java (analogo de record DTO de comando)
    - .planning/phases/LEXCV-32-processos-intake-e-conflict-check/32-PATTERNS.md (seccoes ConflictCheckDecisao.java, repository, DTOs, linhas 31-179)
  </read_first>
  <action>
    Criar a entidade `ConflictCheckDecisao` em package `com.lexcv.models`, anotada `@Entity @Table(name = "t_conflict_check_decisao")` com Lombok `@Getter @Setter @NoArgsConstructor @AllArgsConstructor @Builder`. Campos: `UUID id` (@Id, @GeneratedValue(strategy = GenerationType.UUID)); `UUID tenantId` (@Column name "tenant_id", nullable false); `UUID processoId` (@Column name "processo_id", nullable false); `String nivel` (@Column nullable false — valores: sem_conflito | potencial | sanavel | impeditivo); `String justificativa` (@Column length 2000); `UUID decisorId` (@Column name "decisor_id", nullable false); `LocalDate dataDecisao` (@Column name "data_decisao", nullable false); `String referenciaEvidencia` (@Column name "referencia_evidencia"); `LocalDateTime createdAt` (@Column name "created_at", updatable false) preenchido por `@PrePersist onCreate()` com `LocalDateTime.now()`. Nao usar migration — JPA ddl-auto=update cria a tabela.

    Criar `ConflictCheckDecisaoRepository extends JpaRepository<ConflictCheckDecisao, UUID>` com `List<ConflictCheckDecisao> findByTenantId(UUID tenantId)` e `Optional<ConflictCheckDecisao> findByTenantIdAndProcessoId(UUID tenantId, UUID processoId)`.

    Criar os DTOs como Java records em `com.lexcv.dtos`: `ConflictCheckDecisaoRequest(UUID processoId, String nivel, String justificativa, String referenciaEvidencia)` no ficheiro ConflictCheckRequest.java (pode incluir tambem `ConflictCheckRequest(UUID processoId)` se necessario). Em ConflictCheckResponse.java definir `ConflictMatchDto(String entidadeId, String entidadeTipo, String nome, String nif, String nivelConflito, String motivo)` e `ConflictCheckResponse(List<ConflictMatchDto> matches, String nivelSugerido)`. `entidadeTipo` assume "cliente" ou "parte". Nenhum annotation Jackson necessario.
  </action>
  <verify>
    <automated>cd backend && mvn -q -DskipTests compile 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `mvn -DskipTests compile` termina com BUILD SUCCESS.
    - `ConflictCheckDecisao.java` contem `@Table(name = "t_conflict_check_decisao")` e `@Column(name = "tenant_id"`.
    - `ConflictCheckDecisaoRepository.java` declara `findByTenantIdAndProcessoId`.
    - `ConflictCheckResponse.java` declara os records `ConflictMatchDto` e `ConflictCheckResponse`.
  </acceptance_criteria>
  <done>Entidade tenant-scoped + repositorio com finder por tenant/processo + DTOs de request/response compilam.</done>
</task>

<task type="auto">
  <name>Task 2: Seed das permissoes processos:create e processos:manage</name>
  <files>backend/src/main/java/com/lexcv/seed/DatabaseSeeder.java</files>
  <read_first>
    - backend/src/main/java/com/lexcv/seed/DatabaseSeeder.java (metodo seedRbac, linhas 294-340 — permKeys e upsertRolePermissions)
    - .planning/phases/LEXCV-32-processos-intake-e-conflict-check/32-PATTERNS.md (seccao DatabaseSeeder, linhas 259-284)
  </read_first>
  <action>
    Em `seedRbac()`, adicionar `"processos:create"` e `"processos:manage"` a lista `permKeys` (a seguir a "processos:view", "processos:edit"). ADMIN recebe-as automaticamente via `upsertRolePermissions("ADMIN", permissionMap.values())` — nao alterar essa linha. Na atribuicao do role `ADVOGADO` (Arrays.asList que ja inclui processos:view e processos:edit), acrescentar `permissionMap.get("processos:create")` e `permissionMap.get("processos:manage")` para que advogados possam correr o conflict check, decidir e formalizar. NAO conceder processos:manage a ASSISTENTE nem a TECNICO (mantem-se apenas processos:view conforme ja seeded). Estes scopes alinham com o frontend `usePermissions().can.create("processos")` e `.can.manage("processos")` (D: RBAC backend e frontend devem concordar).
  </action>
  <verify>
    <automated>cd backend && grep -v '^[[:space:]]*//' src/main/java/com/lexcv/seed/DatabaseSeeder.java | grep -c 'processos:manage'</automated>
  </verify>
  <acceptance_criteria>
    - grep (excluindo comentarios) de `processos:manage` retorna >= 2 (uma na permKeys, uma na atribuicao ADVOGADO).
    - grep de `processos:create` retorna >= 2.
    - A linha `upsertRolePermissions("ADMIN", permissionMap.values())` permanece inalterada.
    - ASSISTENTE e TECNICO nao recebem `processos:manage` (verificacao por leitura dos respetivos Arrays.asList).
  </acceptance_criteria>
  <done>As duas permissoes novas existem na permKeys e estao atribuidas a ADMIN (implicito) e ADVOGADO; roles sem gestao nao as recebem.</done>
</task>

<task type="auto">
  <name>Task 3: Endpoints intake (TRIAGEM), conflict-check, decisao e formalizar com bloqueio server-side</name>
  <files>backend/src/main/java/com/lexcv/controllers/ResourceController.java</files>
  <read_first>
    - backend/src/main/java/com/lexcv/controllers/ResourceController.java (createProcesso 568-573; getProcesso 575-583; tenant guard 579-581; mergeClientes match logic 429-497; getTenantId 54-58)
    - backend/src/main/java/com/lexcv/repositories/ClienteRepository.java (findByTenantId, findByTenantIdAndNif)
    - backend/src/main/java/com/lexcv/repositories/ParteRepository.java (findByProcessoId)
    - .planning/phases/LEXCV-32-processos-intake-e-conflict-check/32-PATTERNS.md (seccao ResourceController endpoints, linhas 183-255; shared multi-tenant + RBAC, linhas 650-673)
    - .planning/phases/LEXCV-32-processos-intake-e-conflict-check/32-CONTEXT.md (decisions: 4 niveis, politica de override por nivel, bloqueio impeditivo)
  </read_first>
  <action>
    Injetar `ConflictCheckDecisaoRepository`, `ClienteRepository`, `ParteRepository` no controller (campos final + constructor existente, seguir o padrao dos repositorios ja injetados). Adicionar 4 handlers a seguir a seccao de processos:

    1. Intake em TRIAGEM: novo `@PostMapping("/processos/intake")` com `@PreAuthorize("hasAuthority('processos:create')")` recebendo `@RequestBody Processo processo`. Forcar `processo.setTenantId(getTenantId())` e `processo.setEstado("TRIAGEM")` independentemente do payload; save; devolver 201 com o processo. (Nao alterar o `createProcesso` existente — este e o caminho formal/legado.)

    2. `@PostMapping("/processos/{id}/conflict-check")` com `@PreAuthorize("hasAuthority('processos:create')")`. Carregar o processo e aplicar o tenant guard (404 "Processo não encontrado"). Construir matches tenant-scoped: (a) match exato por NIF do cliente do processo via `clienteRepository.findByTenantIdAndNif`; (b) match aproximado por nome — `clienteRepository.findByTenantId(tenantId)` filtrando por um helper de similaridade case-insensitive de nome (contains/lowercase) contra o nome do cliente e contra os nomes das `Parte` do processo (`parteRepository.findByProcessoId`); excluir o proprio cliente do processo dos matches de cliente. Cada match e um `ConflictMatchDto(entidadeId, entidadeTipo, nome, nif, nivelConflito, motivo)`. Calcular `nivelSugerido`: se houver match exato de NIF -> "potencial"; se apenas nome aproximado -> "potencial"; se nenhum -> "sem_conflito". A decisao final e humana (nunca automatica). Devolver `ConflictCheckResponse(matches, nivelSugerido)` em 200. TODAS as queries scoped por `getTenantId()` — nunca pesquisar fora do tenant.

    3. `@PostMapping("/processos/{id}/conflict-check/decisao")` com `@PreAuthorize("hasAuthority('processos:manage')")` recebendo `@RequestBody ConflictCheckDecisaoRequest payload`. Tenant guard no processo. Validar que `nivel` pertence a {sem_conflito, potencial, sanavel, impeditivo} — caso contrario 400. Para `potencial` e `sanavel` exigir `justificativa` nao-vazia (override registado) — caso contrario 400 com mensagem clara. Persistir/atualizar a decisao (upsert por `findByTenantIdAndProcessoId`): definir `tenantId=getTenantId()`, `processoId=id`, `decisorId=getUserId-do-UserPrincipal`, `dataDecisao=LocalDate.now()`, mais nivel/justificativa/referenciaEvidencia do payload. Para obter o decisor: ler `UserPrincipal` do `SecurityContextHolder` (mesmo padrao de getTenantId) e usar `getUserId()`. Devolver a decisao gravada em 200.

    4. `@PostMapping("/processos/{id}/formalizar")` com `@PreAuthorize("hasAuthority('processos:manage')")`. Tenant guard. Carregar a decisao via `findByTenantIdAndProcessoId(getTenantId(), id)`. ENFORCEMENT: se nao existir decisao -> 409 (ou 400) com Map.of("message", "Não é possível formalizar: o conflict check ainda não tem uma decisão registada"). Se `decisao.nivel == "impeditivo"` -> 409 com Map.of("message", "Não é possível formalizar: existe um conflito impeditivo registado"). Caso contrario, transicao: `processo.setEstado("ATIVO"); processoRepository.save(processo);` e devolver o processo em 200. Este e o UNICO ponto de enforcement do bloqueio — nao duplicar a regra.

    Adicionar tambem um `@GetMapping("/processos/{id}/conflict-check/decisao")` com `@PreAuthorize("hasAuthority('processos:view')")` que devolve a decisao (`findByTenantIdAndProcessoId`) ou 200 com corpo `null` quando ainda nao existe — consumido pelo hook `useConflictCheckDecisao` no frontend.
  </action>
  <verify>
    <automated>cd backend && mvn -q -DskipTests compile 2>&1 | tail -15 && grep -c "conflict-check\|/processos/intake\|/formalizar" src/main/java/com/lexcv/controllers/ResourceController.java</automated>
  </verify>
  <acceptance_criteria>
    - `mvn -DskipTests compile` -> BUILD SUCCESS.
    - ResourceController contem `@PostMapping("/processos/intake")`, `@PostMapping("/processos/{id}/conflict-check")`, `@PostMapping("/processos/{id}/conflict-check/decisao")`, `@GetMapping("/processos/{id}/conflict-check/decisao")` e `@PostMapping("/processos/{id}/formalizar")`.
    - O handler de intake contem `setEstado("TRIAGEM")`.
    - O handler de formalizar contem `findByTenantIdAndProcessoId` e bloqueia quando decisao ausente OU nivel impeditivo (duas verificacoes que retornam 4xx antes de qualquer setEstado("ATIVO")).
    - conflict-check e decisao usam `@PreAuthorize("hasAuthority('processos:create')")` e `('processos:manage')` respetivamente; formalizar usa `('processos:manage')`.
    - Cada handler aplica o tenant guard (`!processo.getTenantId().equals(getTenantId())` -> 404) antes de qualquer leitura de dados.
  </acceptance_criteria>
  <done>Os 5 endpoints existem, compilam, scoped por tenant, com RBAC correto e com o bloqueio TRIAGEM->ATIVO imposto no servidor (decisao ausente ou impeditivo => 4xx).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser -> ResourceController | Input nao confiavel: processoId, payload de intake, nivel/justificativa da decisao |
| utilizador autenticado -> dados de outro tenant | Risco de leakage cross-tenant na pesquisa de conflito |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-01 | Information Disclosure | runConflictCheck (pesquisa clientes/partes) | mitigate | Todas as queries usam findByTenantId/findByTenantIdAndNif com getTenantId(); tenant guard 404 no processo antes de pesquisar; matches nunca incluem entidades de outro tenant |
| T-32-02 | Elevation of Privilege | conflict-check/decisao/formalizar | mitigate | @PreAuthorize('processos:create') no check e ('processos:manage') na decisao+formalizar; backend e frontend partilham os mesmos scopes |
| T-32-03 | Tampering | transicao TRIAGEM -> ATIVO | mitigate | Bloqueio imposto exclusivamente no backend em formalizarProcesso: 4xx se decisao ausente ou nivel impeditivo; frontend desabilitar o botao e cosmetico, nao a regra |
| T-32-04 | Repudiation | override de sanavel/potencial | mitigate | Decisao persiste decisorId (UserPrincipal.getUserId()), dataDecisao e justificativa obrigatoria para potencial/sanavel; 400 se justificativa ausente |
| T-32-05 | Spoofing | identidade do decisor | mitigate | decisorId derivado do SecurityContext (UserPrincipal), nunca do payload do cliente |
| T-32-SC | Tampering | npm/pip/cargo installs | mitigate | Sem novos pacotes nesta fase (apenas codigo Spring existente) — sem superficie de supply-chain |
</threat_model>

<verification>
- `mvn -DskipTests compile` em backend/ termina com BUILD SUCCESS apos os 3 tasks.
- Smoke manual (opcional, requer DB + SEED_ENABLED=true): login como admin@lexcv.cv; POST /api/v1/processos/intake cria processo com estado=TRIAGEM; POST .../formalizar sem decisao devolve 4xx; POST .../conflict-check/decisao com nivel=sem_conflito; POST .../formalizar passa a devolver 200 com estado=ATIVO.
</verification>

<success_criteria>
- INT-01: existe caminho backend para criar um processo pre-formal em TRIAGEM com os campos do intake persistidos no proprio Processo.
- CFL-01: conflict check tenant-scoped devolve matches+nivel; a abertura formal so passa apos decisao registada e nunca quando o nivel e impeditivo.
- RBAC: processos:create e processos:manage seeded e atribuidos; @PreAuthorize aplicado nos endpoints corretos.
</success_criteria>

<output>
Create `.planning/phases/LEXCV-32-processos-intake-e-conflict-check/32-01-SUMMARY.md` when done
</output>
