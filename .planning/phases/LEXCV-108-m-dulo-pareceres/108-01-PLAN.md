---
phase: 108-m-dulo-pareceres
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/app/(dashboard)/pareceres/page.tsx
autonomous: true
requirements: [PARC-18]
must_haves:
  truths:
    - "All 6 Pareceres list filters (3 quick + 3 advanced-search) render as Radix Select, each with a non-empty 'todos' sentinel and zero native <select> elements"
    - "onApply translates 'todos' to empty (no filter) for the 3 quick filters; onPesquisar's 3 select checks gate on '&& x !== todos'; onClear/onLimparPesquisa reset the select states back to 'todos'"
    - "The pareceres list RBAC gate uses permissions.isFetched (not !permissions.isLoading)"
  artifacts:
    - path: "web/src/app/(dashboard)/pareceres/page.tsx"
      provides: "6 Radix Select filters + todos-sentinel filter logic + isFetched gate"
      contains: "<Select value={draftStatus}"
  key_links:
    - from: "web/src/app/(dashboard)/pareceres/page.tsx"
      to: "@/components/ui/select"
      via: "import Select/SelectTrigger/SelectContent/SelectItem/SelectValue"
      pattern: "from \"@/components/ui/select\""
---

<objective>
Migrate all 6 native `<select>` filter fields in the Pareceres list page (3 quick filters + 3 advanced-search filters) to Radix `Select`, introducing the mandatory `"todos"` sentinel (Radix `SelectItem` cannot take `value=""`) and updating the four filter handlers so `"todos"` is treated as "no filter". Bundle the `isFetched` RBAC-race fix at the single view gate.

Purpose: PARC-18 for the Pareceres list surface (Select for list filters, matching the Financeiro/Agenda precedent from Phases 106/107). This is a larger blast radius than prior phases — all 6 filters need the sentinel/predicate change, not a subset.
Output: one migrated route file, no dependency on any other Phase 108 plan (owns `pareceres/page.tsx` exclusively).
</objective>

<execution_context>
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/LEXCV-108-m-dulo-pareceres/108-CONTEXT.md
@.planning/phases/LEXCV-108-m-dulo-pareceres/108-PATTERNS.md
@.planning/phases/LEXCV-108-m-dulo-pareceres/108-UI-SPEC.md
@web/src/components/ui/select.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Quick filters (Estado/Advogado/Cliente) to Radix Select + todos sentinel + onApply/onClear translation + isFetched gate</name>
  <files>web/src/app/(dashboard)/pareceres/page.tsx</files>
  <read_first>
    - web/src/app/(dashboard)/pareceres/page.tsx (the file being modified — RBAC gate line 29; quick-filter draft state lines 45-47; onApply lines 80-88; onClear lines 90-95; the 3 quick-filter `<select>` blocks Estado 178-188, Advogado 196-207, Cliente 215-226)
    - web/src/app/(dashboard)/financeiro/page.tsx (the exact Select-with-"todos"-sentinel precedent: import line 9, "todos" state defaults + `!== "todos"` predicates lines 143-152, `<Select>` composition lines 210-225)
    - web/src/app/(dashboard)/agenda/page.tsx (clear-filters reset to "todos" lines 295-299; `permissions.isFetched` gate line 27)
    - web/src/components/ui/select.tsx (Radix Select — SelectTrigger takes a `size` prop and ships `w-fit`; `SelectItem` throws at runtime on `value=""`, hence the sentinel)
    - .planning/phases/LEXCV-108-m-dulo-pareceres/108-UI-SPEC.md (Component Inventory → Select section + Verified-against-source findings #1/#2/#3 — all 6 filters need the sentinel/predicate change, h-9 shipped height is intended, rounded-none addendum required)
  </read_first>
  <action>
    Four coordinated changes in `pareceres/page.tsx`, quick-filter scope only (advanced search is Task 2):
    (1) RBAC gate line 29: change `if (!permissions.isLoading && !canView) {` to `if (permissions.isFetched && !canView) {`, matching `financeiro/page.tsx:104` / `agenda/page.tsx:27` verbatim.
    (2) Quick-filter draft state (lines 45-47): change `draftStatus`, `draftAdvogadoId`, `draftClienteId` from `React.useState("")` to `React.useState("todos")`.
    (3) Handler logic:
        - `onApply` (lines 80-88): the `setFilters({...})` call currently passes `status: draftStatus.trim()`, `advogadoId: draftAdvogadoId.trim()`, `clienteId: draftClienteId.trim()`. Change each to translate the sentinel to empty: `status: draftStatus === "todos" ? "" : draftStatus`, `advogadoId: draftAdvogadoId === "todos" ? "" : draftAdvogadoId`, `clienteId: draftClienteId === "todos" ? "" : draftClienteId`. (The hook's `buildParecerSearch` already drops empty strings server-side — UI-SPEC finding #1 — so `""` is the correct "no filter" value.)
        - `onClear` (lines 90-95): change the three `setDraft*("")` resets to `setDraftStatus("todos")`, `setDraftAdvogadoId("todos")`, `setDraftClienteId("todos")`; keep `setFilters({})` unchanged.
    (4) Replace the three quick-filter `<select>` blocks (Estado lines 178-188, Advogado 196-207, Cliente 215-226) with the `<Select value={...} onValueChange={...}><SelectTrigger size="default" className="w-full rounded-none"><SelectValue /></SelectTrigger><SelectContent>...</SelectContent></Select>` composition from `financeiro/page.tsx:210-225`. Keep each surrounding `<div className="lg:col-span-4">` + label-chip `<div className="text-[11px] font-bold ...">` wrapper and the `<div className="mt-2">` unchanged; only the control inside `mt-2` changes.
        - Estado (`value={draftStatus} onValueChange={setDraftStatus}`): first `<SelectItem value="todos">Todos</SelectItem>`, then `PENDENTE`/`EM_ELABORACAO`/`EM_REVISAO`/`CONCLUIDO` with the exact existing option copy ("Pendente"/"Em elaboração"/"Em revisão"/"Concluído").
        - Advogado (`value={draftAdvogadoId} onValueChange={setDraftAdvogadoId}`): `<SelectItem value="todos">Todos</SelectItem>` then one `<SelectItem key={u.id} value={u.id}>{u.nome}</SelectItem>` per `advogados`.
        - Cliente (`value={draftClienteId} onValueChange={setDraftClienteId}`): `<SelectItem value="todos">Todos</SelectItem>` then one `<SelectItem key={c.id} value={c.id}>{c.nome}</SelectItem>` per `(clientes.data ?? [])`.
        Use `size="default"` (shipped `h-9`, a deliberate 4px reduction from the legacy `h-10` per UI-SPEC finding #2) and `className="w-full rounded-none"` on every `SelectTrigger` (UI-SPEC finding #3). Add `import { Select, SelectContent, SelectItem, SelectTrigger, SelectValue } from "@/components/ui/select";` at the top.
  </action>
  <verify>
    <automated>grep -q 'permissions.isFetched && !canView' "web/src/app/(dashboard)/pareceres/page.tsx" && grep -q 'from "@/components/ui/select"' "web/src/app/(dashboard)/pareceres/page.tsx" && grep -q '<Select value={draftStatus}' "web/src/app/(dashboard)/pareceres/page.tsx" && grep -q '<Select value={draftAdvogadoId}' "web/src/app/(dashboard)/pareceres/page.tsx" && grep -q '<Select value={draftClienteId}' "web/src/app/(dashboard)/pareceres/page.tsx" && grep -q 'draftStatus === "todos"' "web/src/app/(dashboard)/pareceres/page.tsx" && grep -q 'setDraftStatus("todos")' "web/src/app/(dashboard)/pareceres/page.tsx" && ! grep -q 'onChange={(e) => setDraftStatus' "web/src/app/(dashboard)/pareceres/page.tsx"</automated>
  </verify>
  <done>
    The 3 quick filters render as Radix `Select` with a `"todos"` sentinel; `onApply` translates `"todos"` to `""`, `onClear` resets drafts to `"todos"`; the view gate uses `permissions.isFetched`; no native quick-filter `<select onChange={... setDraft...}>` remains.
  </done>
  <acceptance_criteria>
    - Source assertion: `permissions.isFetched && !canView` present; `!permissions.isLoading && !canView` absent.
    - Source assertion: the 3 quick-filter drafts default to `"todos"`; `onApply` uses the `x === "todos" ? "" : x` translation for all 3; `onClear` resets all 3 to `"todos"`.
    - Source assertion: `<Select value={draftStatus}`, `<Select value={draftAdvogadoId}`, `<Select value={draftClienteId}` present; the 3 former native-select `onChange` handlers are gone.
    - Behavior: default view (sentinel `"todos"`) shows the unfiltered list; picking a specific Estado/Advogado/Cliente and clicking "Aplicar" narrows it; "Limpar" restores the full list.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Advanced-search filters (Cliente/Advogado/Estado) to Radix Select + todos sentinel + onPesquisar/onLimparPesquisa translation</name>
  <files>web/src/app/(dashboard)/pareceres/page.tsx</files>
  <read_first>
    - web/src/app/(dashboard)/pareceres/page.tsx (the file being modified — advanced-search state lines 52-54 for the 3 select-backed fields; onPesquisar lines 97-114; onLimparPesquisa lines 116-125; the 3 advanced `<select>` blocks Cliente 259-270, Advogado 278-289, Estado 297-307. Note: `pesquisaTexto`/`pesquisaDataInicio`/`pesquisaDataFim` are text/date inputs, NOT selects — leave them `useState("")` and untouched)
    - web/src/app/(dashboard)/financeiro/page.tsx (same `<Select>` composition already read in Task 1 — reuse the identical shape)
    - .planning/phases/LEXCV-108-m-dulo-pareceres/108-PATTERNS.md (Analog 2 / "Required adaptation for Pareceres" — the exact onPesquisar `if (status && status !== "todos")` change list)
  </read_first>
  <action>
    Three coordinated changes in `pareceres/page.tsx`, advanced-search scope only (the `import` and Select components were already added in Task 1):
    (1) Advanced-search state (lines 52-54): change `pesquisaClienteId`, `pesquisaAdvogadoId`, `pesquisaStatus` from `React.useState("")` to `React.useState("todos")`. Do NOT change `pesquisaTexto` (line 51), `pesquisaDataInicio` (line 55), or `pesquisaDataFim` (line 56) — those back plain text/date inputs, not selects.
    (2) Handler logic:
        - `onPesquisar` (lines 97-114): the three select-backed checks currently read `if (clienteId) next.clienteId = clienteId;`, `if (advogadoId) next.advogadoId = advogadoId;`, `if (status) next.status = status;` (where `clienteId`/`advogadoId`/`status` are the `.trim()`ed local consts on lines 101-103). Change each to `if (clienteId && clienteId !== "todos") next.clienteId = clienteId;`, `if (advogadoId && advogadoId !== "todos") next.advogadoId = advogadoId;`, `if (status && status !== "todos") next.status = status;`. Leave the `texto`/`dataInicio`/`dataFim` checks (lines 106, 110, 111) unchanged.
        - `onLimparPesquisa` (lines 116-125): change `setPesquisaClienteId("")`, `setPesquisaAdvogadoId("")`, `setPesquisaStatus("")` to `("todos")` each; keep `setPesquisaTexto("")`, `setPesquisaDataInicio("")`, `setPesquisaDataFim("")` as `""`.
    (3) Replace the three advanced-search `<select>` blocks (Cliente lines 259-270, Advogado 278-289, Estado 297-307) with the same `<Select>` composition used in Task 1, keeping each surrounding `<div>` + `<label className="text-[11px] font-bold ...">` + `<div className="mt-2">` wrapper unchanged:
        - Cliente (`value={pesquisaClienteId} onValueChange={setPesquisaClienteId}`): `<SelectItem value="todos">Todos</SelectItem>` + one per `(clientes.data ?? [])` (`c.id`/`c.nome`).
        - Advogado (`value={pesquisaAdvogadoId} onValueChange={setPesquisaAdvogadoId}`): `<SelectItem value="todos">Todos</SelectItem>` + one per `advogados` (`u.id`/`u.nome`).
        - Estado (`value={pesquisaStatus} onValueChange={setPesquisaStatus}`): `<SelectItem value="todos">Todos</SelectItem>` + `PENDENTE`/`EM_ELABORACAO`/`EM_REVISAO`/`CONCLUIDO` (same copy as Task 1).
        Every `SelectTrigger`: `size="default" className="w-full rounded-none"`.
  </action>
  <verify>
    <automated>grep -q '<Select value={pesquisaClienteId}' "web/src/app/(dashboard)/pareceres/page.tsx" && grep -q '<Select value={pesquisaAdvogadoId}' "web/src/app/(dashboard)/pareceres/page.tsx" && grep -q '<Select value={pesquisaStatus}' "web/src/app/(dashboard)/pareceres/page.tsx" && grep -q 'status !== "todos"' "web/src/app/(dashboard)/pareceres/page.tsx" && grep -q 'setPesquisaStatus("todos")' "web/src/app/(dashboard)/pareceres/page.tsx" && ! grep -q '<select' "web/src/app/(dashboard)/pareceres/page.tsx" && ! grep -q 'onChange={(e) => setPesquisaStatus' "web/src/app/(dashboard)/pareceres/page.tsx"</automated>
  </verify>
  <done>
    The 3 advanced-search filters render as Radix `Select` with a `"todos"` sentinel; `onPesquisar` only adds each select value when it is set AND `!== "todos"`; `onLimparPesquisa` resets the 3 select states to `"todos"` while leaving the text/date resets as `""`; zero native `<select>` elements remain anywhere in the file.
  </done>
  <acceptance_criteria>
    - Source assertion: `<Select value={pesquisaClienteId}`, `<Select value={pesquisaAdvogadoId}`, `<Select value={pesquisaStatus}` present.
    - Source assertion: `onPesquisar`'s three select checks use `&& x !== "todos"`; `onLimparPesquisa` resets the 3 select states to `"todos"` and the 3 text/date states to `""`.
    - Source assertion: `! grep '<select'` over the whole file returns no matches (all 6 filters migrated).
    - Behavior: opening Pesquisa Avançada, leaving all 3 selects at "Todos", entering only free text, and clicking "Pesquisar" sends no cliente/advogado/status filter; selecting a specific value narrows the search; "Limpar Filtros" resets every field.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → Next rewrite → Spring API | Existing parecer list/search endpoints. Filter values are user-selected and sent as querystring params; the server applies tenant scoping + `@PreAuthorize`. The client RBAC gate is defense-in-depth only. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-108-01 | Elevation of Privilege | client RBAC gate on pareceres list | mitigate | Switch to `permissions.isFetched && !canView`, closing the pre-resolve render race (matches Phases 103/105/106/107). Server `@PreAuthorize("hasAuthority('pareceres:view')")` remains authoritative. |
| T-108-02 | Tampering | filter sentinel/predicate translation (6 filters, 4 handlers) | mitigate | The `"todos"` → omit/`""` translation is a semantically-identical replacement for the prior truthiness/`if (x)` checks; the literal string `"todos"` is never sent as a real filter value (asserted by the "no filter when all Todos" behavior check). No new server input shape. |
| T-108-SC | Tampering | npm/pnpm installs | accept | Zero new dependencies (UI-SPEC Registry Safety); `Select` installed in Phase 101 and already has prior consumers (Financeiro/Agenda). |
</threat_model>

<verification>
- All 6 filters (`draftStatus`/`draftAdvogadoId`/`draftClienteId` + `pesquisaClienteId`/`pesquisaAdvogadoId`/`pesquisaStatus`) are Radix `<Select>` with a `"todos"` sentinel; zero native `<select>` remain.
- `onApply`/`onClear`/`onPesquisar`/`onLimparPesquisa` all handle `"todos"` as "no filter".
- The view gate uses `permissions.isFetched`.
- Plan-level `pnpm build`/`pnpm lint` is covered by the Wave-2 holistic gate (Plan 04).
</verification>

<success_criteria>
- PARC-18 satisfied for the Pareceres list surface (6 Radix Select filters).
- 1 of the 7 bundled RBAC gates fixed here (pareceres/page.tsx:29).
</success_criteria>

<output>
Create `.planning/phases/LEXCV-108-m-dulo-pareceres/108-01-SUMMARY.md` when done.
</output>
