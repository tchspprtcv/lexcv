---
phase: 108-m-dulo-pareceres
plan: 04
type: execute
wave: 2
depends_on: [108-01, 108-02, 108-03]
files_modified: []
autonomous: false
requirements: [PARC-18, PARC-19, PARC-20]
must_haves:
  truths:
    - "pnpm build and pnpm lint both pass with zero new errors across the whole web app"
    - "Zero native <select> elements and zero permissions.isLoading references remain across the 3 Pareceres files; the selectClassName const is gone; isResponsavelOuAdmin is preserved"
    - "A human has visually confirmed the 6 Select filters, 5 NativeSelect fields, timeline Tooltips (hover + keyboard), and Accordion default-open behavior in light and dark themes and across RBAC roles"
  artifacts: []
  key_links: []
---

<objective>
Final holistic quality gate for Phase 108: run the full build/lint pipeline, assert via regression greps that every migration landed and no legacy pattern survives across the 3 modified source files, then a blocking human visual checkpoint confirms the visual/interaction contract in both themes — including the two first-of-kind patterns (Accordion default-open behavior, Tooltip on a non-focusable timeline marker) and the RBAC matrix.

Purpose: this phase's heaviest file (`pareceres/[id]/page.tsx`) introduces the project's first Accordion consumer and first Timeline+Tooltip pairing; a single end-of-phase gate catches integration regressions (import gaps, leftover native selects, sentinel-logic mistakes, a missed `permissions.isLoading`) that per-file greps in Wave 1 cannot, and the human check validates what the automated gate cannot (real Accordion expand/collapse, keyboard-reachable tooltips, dark-mode popover elevation, no RBAC flash).
Output: green build/lint + regression assertions + recorded human sign-off.
</objective>

<execution_context>
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/LEXCV-108-m-dulo-pareceres/108-CONTEXT.md
@.planning/phases/LEXCV-108-m-dulo-pareceres/108-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Holistic build/lint gate + regression greps</name>
  <files>web/src/app/(dashboard)/pareceres/page.tsx, web/src/app/(dashboard)/pareceres/nova/page.tsx, web/src/app/(dashboard)/pareceres/[id]/page.tsx</files>
  <read_first>
    - .planning/phases/LEXCV-108-m-dulo-pareceres/108-UI-SPEC.md (the binding contract being verified — Select/NativeSelect/Tooltip/Accordion inventories + Copywriting Contract)
    - .planning/phases/LEXCV-108-m-dulo-pareceres/108-PATTERNS.md (Shared Patterns section — the 7 RBAC sites, the do-not-touch isResponsavelOuAdmin, the 6-Select / 5-NativeSelect blast radius)
    - web/src/app/(dashboard)/pareceres/page.tsx (verify Wave-1 output)
    - web/src/app/(dashboard)/pareceres/nova/page.tsx (verify Wave-1 output)
    - web/src/app/(dashboard)/pareceres/[id]/page.tsx (verify Wave-1 output)
  </read_first>
  <action>
    Run `cd web && pnpm build` and `cd web && pnpm lint`; both must exit 0 with no new errors. Then confirm the regression assertions below (all encoded in the automated verify as an &&-chain that exits 0 only when every assertion holds). If build/lint fails or any assertion fails, fix the offending file (this gate plan may edit any of the 3 listed source files to close a regression) and re-run until green. Regression assertions:
    (a) Zero native `<select>` survive: `<select` appears ZERO times across all 3 files (all 6 Select filters + all 5 NativeSelect fields migrated).
    (b) Select adopted in the list page: `<Select value={draftStatus}` and `<Select value={pesquisaStatus}` both present in pareceres/page.tsx.
    (c) The `"todos"` sentinel logic landed: `!== "todos"` present in pareceres/page.tsx.
    (d) NativeSelect adopted: `from "@/components/ui/native-select"` present in nova/page.tsx and in [id]/page.tsx; `NativeSelect id="versaoFinalId"` present in [id]/page.tsx.
    (e) Dead const removed: `const selectClassName` appears ZERO times in nova/page.tsx (and `const textareaClassName` is still present).
    (f) RBAC fully migrated: `permissions.isLoading` appears ZERO times across all 3 files; `isResponsavelOuAdmin` is still present in [id]/page.tsx (not deleted).
    (g) Tooltip + Accordion timeline landed: `from "@/components/ui/accordion"`, `from "@/components/ui/tooltip"`, `AccordionItem value={versao.id}`, and `tabIndex={0}` all present in [id]/page.tsx.
  </action>
  <verify>
    <automated>cd web && pnpm build && pnpm lint && cd .. && ! grep -rq '<select' "web/src/app/(dashboard)/pareceres/page.tsx" "web/src/app/(dashboard)/pareceres/nova/page.tsx" "web/src/app/(dashboard)/pareceres/[id]/page.tsx" && grep -q '<Select value={draftStatus}' "web/src/app/(dashboard)/pareceres/page.tsx" && grep -q '<Select value={pesquisaStatus}' "web/src/app/(dashboard)/pareceres/page.tsx" && grep -q '!== "todos"' "web/src/app/(dashboard)/pareceres/page.tsx" && grep -q 'from "@/components/ui/native-select"' "web/src/app/(dashboard)/pareceres/nova/page.tsx" && grep -q 'from "@/components/ui/native-select"' "web/src/app/(dashboard)/pareceres/[id]/page.tsx" && grep -q 'NativeSelect id="versaoFinalId"' "web/src/app/(dashboard)/pareceres/[id]/page.tsx" && ! grep -q 'const selectClassName' "web/src/app/(dashboard)/pareceres/nova/page.tsx" && grep -q 'const textareaClassName' "web/src/app/(dashboard)/pareceres/nova/page.tsx" && ! grep -rq 'permissions.isLoading' "web/src/app/(dashboard)/pareceres/page.tsx" "web/src/app/(dashboard)/pareceres/nova/page.tsx" "web/src/app/(dashboard)/pareceres/[id]/page.tsx" && grep -q 'isResponsavelOuAdmin' "web/src/app/(dashboard)/pareceres/[id]/page.tsx" && grep -q 'from "@/components/ui/accordion"' "web/src/app/(dashboard)/pareceres/[id]/page.tsx" && grep -q 'from "@/components/ui/tooltip"' "web/src/app/(dashboard)/pareceres/[id]/page.tsx" && grep -q 'AccordionItem value={versao.id}' "web/src/app/(dashboard)/pareceres/[id]/page.tsx" && grep -q 'tabIndex={0}' "web/src/app/(dashboard)/pareceres/[id]/page.tsx"</automated>
  </verify>
  <done>
    `pnpm build` and `pnpm lint` pass; assertions (a)-(g) all hold: no native `<select>` / no `permissions.isLoading` / no `selectClassName` survives, all Select/NativeSelect/Tooltip/Accordion migrations are present, `textareaClassName` and `isResponsavelOuAdmin` are preserved.
  </done>
  <acceptance_criteria>
    - CLI: `cd web && pnpm build` exits 0; `cd web && pnpm lint` exits 0.
    - CLI: the full &&-chain in the automated verify exits 0 (every presence/absence assertion holds).
    - CLI: `grep -rq '<select'` over the 3 files returns no matches; `grep -rq 'permissions.isLoading'` over the 3 files returns no matches.
    - CLI: `const selectClassName` absent in nova; `isResponsavelOuAdmin` + `const textareaClassName` still present.
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human visual checkpoint — Select filters, NativeSelect fields, timeline Tooltip + Accordion, RBAC (light + dark)</name>
  <read_first>
    - .planning/phases/LEXCV-108-m-dulo-pareceres/108-UI-SPEC.md (Component Inventory — Select/NativeSelect/Tooltip/Accordion contracts + Copywriting Contract tooltip strings)
    - .planning/phases/LEXCV-108-m-dulo-pareceres/108-CONTEXT.md (locked scope — do not touch isResponsavelOuAdmin; aprovado/aprovadoEm approval UI out of scope; marker color stays slate)
  </read_first>
  <action>
    After Task 1's gate is green, start the dev server (`cd web && pnpm dev`) with the backend reachable so RBAC/permission, cliente/advogado, and parecer/versoes data load. Present the checklist in `<how-to-verify>` and PAUSE for the human verdict. This is a blocking human-verify checkpoint — never auto-approve (`workflow.auto_advance` is ignored; this plan runs solo in Wave 2). Record the human's concrete evidence (themes exercised, the Accordion default-open item observed on a non-CONCLUIDO vs a CONCLUIDO parecer, the keyboard-reachable tooltip, the dark-mode Select/Accordion surfaces) and the approved/issues verdict in 108-04-SUMMARY.md; if issues are reported, enumerate them for a `/gsd:plan-phase 108 --gaps` closure plan rather than silently closing the phase.
  </action>
  <what-built>
    Phase 108 migrated the Pareceres module: the 6 list filters (3 quick + 3 advanced-search) to Radix `Select` with a `"todos"` sentinel; the 4 create-form fields (clienteId/processoId/prioridade/advogadoId) and the EntregarParecerDialog's version picker to `NativeSelect`; and rebuilt the "Histórico de Versões" timeline with a `Tooltip` on every version marker (labeled "Versão atual"/"Versão entregue"/"Versão anterior") plus an `Accordion` that collapses old versions, keeping only the most-recent (or the delivered version when CONCLUIDO) expanded by default. The project's FIRST Accordion consumer and FIRST Timeline+Tooltip pairing. The 7 RBAC gates were switched to `permissions.isFetched`; the instance-level `isResponsavelOuAdmin` check was left unchanged.
  </what-built>
  <how-to-verify>
    Run `cd web && pnpm dev` and, in BOTH light and dark themes:
    1. `/pareceres` — click "Filtros" to open the quick filters: Estado/Advogado/Cliente are now Radix `Select` buttons defaulting to "Todos". Pick a specific value in each, click "Aplicar" — the list narrows; "Limpar" resets all three back to "Todos" and shows the full list. Confirm no runtime error (the `""`→`"todos"` sentinel is what prevents the Radix "empty value" crash).
    2. `/pareceres` — click "Pesquisa Avançada": Cliente/Advogado/Estado are Radix `Select` (default "Todos"); the Pesquisar/Período text+date inputs are unchanged. Leave the 3 selects at "Todos", type some free text, click "Pesquisar" — confirm results come back (no cliente/advogado/status filter sent). Select a specific value and confirm it narrows; "Limpar Filtros" resets everything.
    3. `/pareceres/nova` — Cliente/Processo/Prioridade/Advogado are all `NativeSelect` (chevron affordance, w-full). Confirm Cliente shows "A carregar..." then "Selecionar cliente" while loading and is disabled during load; selecting a cliente repopulates the Processo list; submitting creates a parecer.
    4. Open a NON-CONCLUIDO parecer `/pareceres/{id}` with 2+ versions → right-column "Histórico de Versões": confirm it is now an `Accordion` with ONLY the top (most-recent) version expanded on load; clicking other version headers expands/collapses them; the dot+connecting-line column stays visually paired with each row. Hover the top version's dot marker — tooltip reads "Versão atual"; hover an older marker — "Versão anterior". Then TAB to a marker with the keyboard (not the mouse) and confirm the same tooltip appears (keyboard reachability — the marker is not a button, so this is the a11y-critical check).
    5. Open a CONCLUIDO parecer `/pareceres/{id}` (one that has been delivered) → confirm the delivered version (matching versaoFinalId) is the one expanded by default in the Accordion, and its marker tooltip reads "Versão entregue" (others "Versão anterior"). Confirm the left-column "Parecer Entregue" block is unchanged.
    6. As the responsible advogado (or ADMIN) on a non-CONCLUIDO parecer, open "Entregar Parecer" — the version picker is a `NativeSelect` defaulting to the most-recent version; the dialog's destructive "Confirmar Entrega" styling is unchanged.
    7. In dark mode specifically, confirm the `Select` dropdown panels and the `Tooltip` bubble have proper elevation/contrast (no flat/invisible surface), and the Accordion chevron + expanded content read correctly.
    8. RBAC: (a) log in as a role lacking `pareceres:view` and load `/pareceres`, `/pareceres/{id}`, `/pareceres/nova` — confirm AccessDeniedState renders immediately with NO flash of denied-then-content (or content-then-denied) during permission load (the `isFetched` fix). (b) log in as an ADVOGADO who is NOT the responsible advogado on some parecer and lacks ADMIN — confirm the "Nova Versão" form and "Entregar Parecer" trigger do NOT appear (the untouched `isResponsavelOuAdmin` instance check still works).
  </how-to-verify>
  <acceptance_criteria>
    - All 6 Pareceres filters are Radix `Select` with a working "Todos" default; applying/clearing narrows/restores the list with no Radix empty-value crash.
    - All 5 NativeSelect fields (4 create-form + 1 dialog) render with the chevron affordance and preserve their loading/disabled and default-value behavior.
    - The "Histórico de Versões" timeline is an `Accordion` with exactly one item open by default — most-recent on a non-CONCLUIDO parecer, delivered version on a CONCLUIDO one — and every marker shows a `Tooltip` on BOTH hover and keyboard focus with the correct "Versão atual"/"Versão entregue"/"Versão anterior" copy.
    - No access-denied flash for an unauthorized role across the 3 view gates; the instance-level `isResponsavelOuAdmin` gate still hides the Nova Versão / Entregar controls from a non-responsible advogado — both confirmed by an actual role switch, not inferred from code.
    - Dark mode: Select/Tooltip/Accordion surfaces render with correct elevation and contrast.
  </acceptance_criteria>
  <resume-signal>Type "approved" if all 8 checks pass in both themes, or describe the issues found.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| verification only | This plan runs build/lint/grep and a human visual check; it introduces no new runtime code path or server input. Any fix it applies is bounded to the 3 already-in-scope Pareceres files. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-108-08 | Elevation of Privilege | final RBAC-gate regression sweep | mitigate | Assertion (f) proves zero `permissions.isLoading` gates survive across all 3 files AND that `isResponsavelOuAdmin` is preserved — closing the risk that a wave-parallel edit reverted, missed, or over-reached a gate. Server `@PreAuthorize` + the instance-level responsável/ADMIN check remain authoritative. |
| T-108-09 | Tampering | leftover-legacy-markup sweep | mitigate | Assertions (a)/(e) prove no native `<select>` and no dead `selectClassName` survive; the human check confirms the sentinel logic and Accordion/Tooltip composition behave, not just compile. |
| T-108-SC | Tampering | npm/pnpm installs | accept | Zero new dependencies this phase; `pnpm build` confirms the lockfile is unchanged. |
</threat_model>

<verification>
- `pnpm build` + `pnpm lint` green.
- Regression greps (a)-(g) all hold.
- Human visual checkpoint APPROVED for all 8 checks in light and dark themes.
</verification>

<success_criteria>
- PARC-18, PARC-19, PARC-20 verified end-to-end across the 3 modified files; no legacy pattern survives; human sign-off recorded.
- Phase 108 ready to mark complete (PARC-18, PARC-19, PARC-20 → Complete in REQUIREMENTS.md).
</success_criteria>

<output>
Create `.planning/phases/LEXCV-108-m-dulo-pareceres/108-04-SUMMARY.md` when done.
</output>
