---
phase: 108-m-dulo-pareceres
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/app/(dashboard)/pareceres/[id]/page.tsx
autonomous: true
requirements: [PARC-18, PARC-19, PARC-20]
must_haves:
  truths:
    - "EntregarParecerDialog's versaoFinalId control renders as NativeSelect (className w-full, controlled value/onChange, no rounded-none, no height change)"
    - "The 4 permissions.isLoading RBAC sites (view gate, showNovaVersaoForm, showEntregarTrigger, loading-skeleton guard) use permissions.isFetched; isResponsavelOuAdmin logic is unchanged"
    - "Every timeline version marker carries a Tooltip whose trigger <span> has tabIndex=0 + an aria-label, labeled 'Versão atual'/'Versão entregue'/'Versão anterior'"
    - "The Histórico de Versões timeline uses Accordion with exactly one item open by default — the most-recent version, or the delivered version (versaoFinalId) when the parecer is CONCLUIDO"
  artifacts:
    - path: "web/src/app/(dashboard)/pareceres/[id]/page.tsx"
      provides: "dialog NativeSelect + Tooltip/Accordion timeline composition + isFetched RBAC"
      contains: "AccordionItem value={versao.id}"
  key_links:
    - from: "web/src/app/(dashboard)/pareceres/[id]/page.tsx"
      to: "@/components/ui/accordion"
      via: "import Accordion/AccordionItem/AccordionTrigger/AccordionContent"
      pattern: "from \"@/components/ui/accordion\""
    - from: "web/src/app/(dashboard)/pareceres/[id]/page.tsx"
      to: "@/components/ui/tooltip"
      via: "import Tooltip/TooltipTrigger/TooltipContent"
      pattern: "from \"@/components/ui/tooltip\""
---

<objective>
Migrate the Pareceres detail page: (1) swap `EntregarParecerDialog`'s native `<select>` to `NativeSelect` and bundle the `isFetched` fix at the 4 `permissions.isLoading` RBAC sites (Task 1), then (2) rebuild the "Histórico de Versões" timeline as an interdependent `Tooltip` + `Accordion` composition — the project's first `Accordion` consumer and first Timeline+Tooltip pairing (Task 2).

Purpose: PARC-18 (dialog NativeSelect), PARC-19 (Tooltip on timeline markers), PARC-20 (Accordion collapsing old versions). PARC-19 and PARC-20 land in the same block and share one "is this the current/delivered version" computation — the UI-SPEC binds them as a single composition, so they are one task, not two.
Output: one migrated route file, no dependency on any other Phase 108 plan (owns `pareceres/[id]/page.tsx` exclusively).
</objective>

<execution_context>
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/LEXCV-108-m-dulo-pareceres/108-CONTEXT.md
@.planning/phases/LEXCV-108-m-dulo-pareceres/108-PATTERNS.md
@.planning/phases/LEXCV-108-m-dulo-pareceres/108-UI-SPEC.md
@web/src/components/ui/native-select.tsx
@web/src/components/ui/accordion.tsx
@web/src/components/ui/tooltip.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: EntregarParecerDialog versaoFinalId to NativeSelect + isFetched RBAC (4 sites, isResponsavelOuAdmin untouched)</name>
  <files>web/src/app/(dashboard)/pareceres/[id]/page.tsx</files>
  <read_first>
    - web/src/app/(dashboard)/pareceres/[id]/page.tsx (the file being modified — view gate line 83; isResponsavelOuAdmin lines 153-155 [DO NOT TOUCH]; isConcluido line 156; showNovaVersaoForm lines 157-158 with `!permissions.isLoading` on 158; showEntregarTrigger lines 159-164 with `!permissions.isLoading` on 160; the loading-skeleton guard `{permissions.isLoading ? (` on line 217; the dialog `<select id="versaoFinalId">` lines 492-504)
    - web/src/components/ui/native-select.tsx (the primitive — controlled `value`/`onChange` pass through the `{...props}` spread; `size="default"` = `h-9`, matching the legacy control's existing height)
    - .planning/phases/LEXCV-108-m-dulo-pareceres/108-PATTERNS.md (Analog 1 for the dialog swap + the "RBAC fix" paragraph — line 217's polarity is inverted from the other 3 sites)
    - .planning/phases/LEXCV-108-m-dulo-pareceres/108-UI-SPEC.md (Verified-against-source finding #4 — versaoFinalId needs NO rounded-none addendum and NO height change, only the component swap)
  </read_first>
  <action>
    Five changes in `pareceres/[id]/page.tsx`:
    (1) View gate line 83: change `if (!permissions.isLoading && !canView) {` to `if (permissions.isFetched && !canView) {`.
    (2) `showNovaVersaoForm` (lines 157-158): change the leading `!permissions.isLoading &&` to `permissions.isFetched &&`, keeping the rest of the compound expression (`canEditPareceres && isResponsavelOuAdmin && !isConcluido`) unchanged.
    (3) `showEntregarTrigger` (lines 159-164): change the leading `!permissions.isLoading &&` to `permissions.isFetched &&`, keeping the rest (`!versoes.isLoading && canEditPareceres && isResponsavelOuAdmin && !isConcluido`) unchanged.
    (4) Loading-skeleton guard line 217: change `{permissions.isLoading ? (` to `{!permissions.isFetched ? (` — note the inverted polarity: this guard shows a skeleton WHILE permissions load, so the negation flips versus the 3 access gates above (which show denied AFTER load).
    (5) Dialog `versaoFinalId` (lines 492-504): replace the `<select id="versaoFinalId" className="flex h-9 w-full rounded-md border border-neutral-200 bg-transparent px-3 py-1 text-sm shadow-sm dark:border-neutral-800" value={selectedVersaoId ?? ""} onChange={(e) => setSelectedVersaoId(e.target.value)} disabled={entregar.isPending}>` opening tag with `<NativeSelect id="versaoFinalId" size="default" className="w-full" value={selectedVersaoId ?? ""} onChange={(e) => setSelectedVersaoId(e.target.value)} disabled={entregar.isPending}>` — drop the long inline className entirely, use `className="w-full"` only (NO `rounded-none` addendum, NO height change — finding #4). Keep the `.map` of `versoes` options (`Versão {versao.numeroVersao} — {formatDateTime(versao.createdAt)}`) unchanged; close with `</NativeSelect>`.
    Add `import { NativeSelect } from "@/components/ui/native-select";` at the top.
    DO NOT touch `isResponsavelOuAdmin` (lines 153-155) — pre-existing v2.6 instance-level RBAC logic (advogado responsável OR ADMIN), explicitly out of scope per 108-CONTEXT.md. DO NOT change any other `isLoading` reference (`adminUsers.isLoading` line 144, `cliente.isLoading` line 146, `parecer.isLoading` via `isLoading` line 148/174, `versoes.isLoading` lines 161/228/247) — only the 4 `permissions.isLoading` occurrences change.
  </action>
  <verify>
    <automated>grep -q 'from "@/components/ui/native-select"' "web/src/app/(dashboard)/pareceres/[id]/page.tsx" && grep -q 'NativeSelect id="versaoFinalId"' "web/src/app/(dashboard)/pareceres/[id]/page.tsx" && grep -q 'permissions.isFetched && !canView' "web/src/app/(dashboard)/pareceres/[id]/page.tsx" && grep -q '{!permissions.isFetched ? (' "web/src/app/(dashboard)/pareceres/[id]/page.tsx" && ! grep -q 'permissions.isLoading' "web/src/app/(dashboard)/pareceres/[id]/page.tsx" && grep -q 'isResponsavelOuAdmin' "web/src/app/(dashboard)/pareceres/[id]/page.tsx" && ! grep -q '<select' "web/src/app/(dashboard)/pareceres/[id]/page.tsx"</automated>
  </verify>
  <done>
    `versaoFinalId` is a `NativeSelect className="w-full"` (controlled, no rounded-none, no height change) keeping its option map; all 4 `permissions.isLoading` sites use `permissions.isFetched` (with the correct inverted polarity on the skeleton guard); `isResponsavelOuAdmin` is unchanged; zero native `<select>` remain.
  </done>
  <acceptance_criteria>
    - Source assertion: `NativeSelect id="versaoFinalId"` with `className="w-full"` present; the former long inline `<select>` className is gone.
    - Source assertion: `permissions.isFetched && !canView` present; `{!permissions.isFetched ? (` present; `permissions.isLoading` absent from the whole file (all 4 sites fixed).
    - Source assertion: `isResponsavelOuAdmin` still present (lines ~153-155 unchanged); `<select` absent.
    - Behavior: as the responsible advogado (or ADMIN) on a non-CONCLUIDO parecer, opening "Entregar Parecer" shows the version dropdown as a NativeSelect defaulting to the most-recent version; the RBAC gates render without a load flash.
  </acceptance_criteria>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Histórico de Versões timeline — Tooltip on every marker + Accordion collapsing old versions (one composition)</name>
  <files>web/src/app/(dashboard)/pareceres/[id]/page.tsx</files>
  <behavior>
    - On a non-CONCLUIDO parecer with N versions: exactly one Accordion item is expanded on load — the most-recent (highest numeroVersao) — and its marker tooltip reads "Versão atual"; every other item is collapsed and its marker tooltip reads "Versão anterior".
    - On a CONCLUIDO parecer: the expanded-by-default item is the delivered version (`parecer.data.versaoFinalId`), whose marker tooltip reads "Versão entregue"; all others collapsed with "Versão anterior".
    - Every marker <span> is keyboard-focusable (tabIndex=0) and carries an aria-label equal to its tooltip text; hovering or focusing any marker shows the tooltip.
    - The empty-versions state ("Nenhuma versão ainda") still short-circuits before the Accordion renders.
  </behavior>
  <read_first>
    - web/src/app/(dashboard)/pareceres/[id]/page.tsx (the file being modified — isConcluido line 156; the "Histórico de Versões" Card lines 242-307; empty-state branch lines 256-262; the current `<div className="relative">` + inline-sorted `.map` version loop lines 264-303; the marker `<span>` line 271; the per-version wrapper `<div key={versao.id} className="relative flex gap-3 py-4">` line 269; the connecting-line `<div className="absolute top-3 bottom-0 left-[5px] ...">` line 273; the header row lines 277-284; author line 285-287; conteudo paragraph 288-292; AnexoLink block 293-299)
    - web/src/components/ui/accordion.tsx (the primitive, read in full — Accordion root is `flex w-full flex-col`; AccordionTrigger ships `py-4`, `items-start`, auto-renders its own chevron via `data-[slot=accordion-trigger-icon]:ml-auto`; AccordionContent ships `pt-0 pb-4`; AccordionItem's `not-last:border-b` will not fire in this nesting)
    - web/src/components/ui/tooltip.tsx (the primitive — TooltipTrigger is a bare Radix Trigger, so `asChild` forwards to the marker <span>; TooltipProvider is already mounted globally in providers.tsx:30, delayDuration=700 — no new provider needed)
    - web/src/app/(dashboard)/pareceres/columns.tsx (lines 9, 88-103 — the established Tooltip import + `TooltipTrigger asChild` shape already used in this same module; note it wraps a focusable Button, whereas this task's <span> needs the manual tabIndex)
    - .planning/phases/LEXCV-108-m-dulo-pareceres/108-UI-SPEC.md (Component Inventory → Tooltip + Accordion section — the binding "Shared data derivation", default-open behavior, item structure, marker-column pt-4 adjustment, and the mandatory tabIndex/aria-label accessibility requirement)
    - .planning/phases/LEXCV-108-m-dulo-pareceres/108-PATTERNS.md (Analog 2 for the Tooltip-on-non-focusable-marker code + Analog 3 for the Accordion composition + the "Shared derivation needed" block)
  </read_first>
  <action>
    Rebuild the "Histórico de Versões" Card body (the non-empty `else` branch, current lines 263-304) as a single Tooltip+Accordion composition. Add `import { Accordion, AccordionItem, AccordionTrigger, AccordionContent } from "@/components/ui/accordion";` and `import { Tooltip, TooltipContent, TooltipTrigger } from "@/components/ui/tooltip";` at the top.

    Shared derivation (lift to component scope, right after `isConcluido` on line 156, so both the Tooltip copy and the Accordion default-open share one source of truth):
    - `sortedVersoes`: a `React.useMemo` returning `[...(versoes.data ?? [])]` sorted descending by `numeroVersao` (deps `[versoes.data]`). This replaces the inline `[...versoes.data].sort(...)` currently inside the `.map`.
    - `defaultOpenVersaoId`: `isConcluido ? (parecer.data?.versaoFinalId ?? sortedVersoes[0]?.id) : sortedVersoes[0]?.id`.
    - `versaoTooltipLabel(versao, index)`: a plain function returning, when `isConcluido`, `"Versão entregue"` if `versao.id === defaultOpenVersaoId` else `"Versão anterior"`; otherwise `"Versão atual"` if `index === 0` else `"Versão anterior"`. (`ParecerVersao` is already imported; `index === 0` is the most-recent since `sortedVersoes` is descending.)

    Timeline JSX (replace the `<div className="relative">{[...versoes.data].sort(...).map((versao, index, sorted) => {...})}</div>` block):
    - Root: `<Accordion type="single" collapsible defaultValue={defaultOpenVersaoId} className="relative">` wrapping `sortedVersoes.map((versao, index) => {...})`. (Executor's discretion per 108-CONTEXT.md: `type="multiple"` with `defaultValue={[defaultOpenVersaoId]}` is equally acceptable — the binding requirement is only that exactly the most-recent/delivered item is open by default. When Accordion renders, `sortedVersoes` has ≥1 element because the empty-state branch short-circuits first, so `defaultOpenVersaoId` is always defined.)
    - Per version, keep the outer wrapper `<div key={versao.id} className="relative flex gap-3">` but DROP its `py-4` (the AccordionTrigger's own shipped `py-4` provides the vertical rhythm now).
    - Marker column: keep `<div className="relative flex flex-col items-center">` but ADD `pt-4` (so the dot still aligns with the trigger's first line). Inside it, wrap the marker `<span>` in a Tooltip: `<Tooltip><TooltipTrigger asChild><span tabIndex={0} aria-label={versaoTooltipLabel(versao, index)} className="h-2.5 w-2.5 rounded-full shrink-0 bg-slate-400 dark:bg-slate-500" /></TooltipTrigger><TooltipContent>{versaoTooltipLabel(versao, index)}</TooltipContent></Tooltip>`. Keep the marker color unchanged (`bg-slate-400 dark:bg-slate-500` for every marker — do NOT add a `bg-primary` distinction; the Accordion's default-open state is the primary visual signal, per UI-SPEC Color section). Keep the connecting-line `<div className="absolute top-3 bottom-0 left-[5px] w-0.5 bg-slate-200 dark:bg-slate-700" />` and its `{!isLast ? ... : null}` guard (compute `const isLast = index === sortedVersoes.length - 1;` at the top of the map body).
    - Content column: replace the old `<div className="min-w-0 flex-1 pb-4">` with `<AccordionItem value={versao.id} className="min-w-0 flex-1">`. Inside it:
        - `<AccordionTrigger>` wrapping the existing header row unchanged: `<div className="flex items-center justify-between gap-2 flex-wrap w-full pr-2"><p className="text-sm font-medium text-slate-900 dark:text-white">Versão {versao.numeroVersao}</p><p className="text-xs text-slate-500 dark:text-slate-400">{formatDateTime(versao.createdAt)}</p></div>` (the primitive auto-appends its own chevron — do not add one). Add `w-full pr-2` to that inner div so it fills the trigger and clears the chevron.
        - `<AccordionContent>` wrapping everything that used to render below the header: the author line `<p className="text-xs text-slate-400 dark:text-slate-500 italic mt-0.5">{autorNome}</p>` (compute `const autorNome = versao.criadoPorId ? resolveUserNome(versao.criadoPorId) : "—";` at the top of the map body), the `{versao.conteudo ? (<p ...>{versao.conteudo}</p>) : null}` paragraph, and the `<div className="mt-2"><AnexoLink solicitacaoId={id} versaoId={versao.id} caminhoAnexo={versao.caminhoAnexo} /></div>` block — all copy/styling unchanged, only newly collapsible.
    - Do NOT add a `border-b-0` or any divider override to AccordionItem — its `not-last:border-b` does not fire here because it is the last child of its wrapper div, not a direct sibling of the next AccordionItem (UI-SPEC finding #7). Do NOT build any "expand all"/"collapse all" control. Do NOT touch the loading branch (lines 247-251), the error branch (252-255), or the empty-state branch (256-262). Do NOT touch `ParecerEntregueBlock` (lines 527-576) — it is a separate left-column block, not the timeline.
  </action>
  <verify>
    <automated>grep -q 'from "@/components/ui/accordion"' "web/src/app/(dashboard)/pareceres/[id]/page.tsx" && grep -q 'from "@/components/ui/tooltip"' "web/src/app/(dashboard)/pareceres/[id]/page.tsx" && grep -q 'AccordionItem value={versao.id}' "web/src/app/(dashboard)/pareceres/[id]/page.tsx" && grep -q 'defaultValue={defaultOpenVersaoId}' "web/src/app/(dashboard)/pareceres/[id]/page.tsx" && grep -q 'versaoTooltipLabel' "web/src/app/(dashboard)/pareceres/[id]/page.tsx" && grep -q 'tabIndex={0}' "web/src/app/(dashboard)/pareceres/[id]/page.tsx" && grep -q 'aria-label={versaoTooltipLabel' "web/src/app/(dashboard)/pareceres/[id]/page.tsx" && grep -q 'sortedVersoes' "web/src/app/(dashboard)/pareceres/[id]/page.tsx"</automated>
  </verify>
  <done>
    The "Histórico de Versões" timeline renders as an `Accordion` (one item open by default: most-recent, or delivered when CONCLUIDO) with a `Tooltip` on every marker `<span>` (tabIndex=0 + aria-label), sharing one `defaultOpenVersaoId`/`versaoTooltipLabel` derivation; marker color unchanged; empty/loading/error branches untouched.
  </done>
  <acceptance_criteria>
    - Source assertion: `Accordion`/`AccordionItem value={versao.id}`/`AccordionTrigger`/`AccordionContent` present; `defaultValue={defaultOpenVersaoId}` (or the `type="multiple"` array form) present.
    - Source assertion: every marker `<span>` is wrapped in `<TooltipTrigger asChild>` and carries `tabIndex={0}` + `aria-label={versaoTooltipLabel(...)}`; `versaoTooltipLabel` returns the 3 exact strings "Versão atual"/"Versão entregue"/"Versão anterior".
    - Source assertion: `sortedVersoes` (React.useMemo) replaces the inline `[...versoes.data].sort(...)`; marker className still `bg-slate-400 dark:bg-slate-500` (no `bg-primary`).
    - Behavior: on a multi-version non-CONCLUIDO parecer, only the top (most-recent) version is expanded on load and its marker tooltip reads "Versão atual"; on a CONCLUIDO parecer the delivered version is the expanded one and reads "Versão entregue"; tabbing to a marker and hovering both surface the tooltip.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → Next rewrite → Spring API | Existing parecer detail / versoes / entregar endpoints. Read-only for the timeline; the dialog's `versaoFinalId` selection feeds the existing entregar mutation (tenant-scoped + `@PreAuthorize` server-side, plus the instance-level advogado-responsável/ADMIN check that stays). Client RBAC gates are defense-in-depth. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-108-05 | Elevation of Privilege | 4 client RBAC sites on the detail page | mitigate | Switch to `permissions.isFetched` (with the correct inverted polarity on the skeleton guard), closing the pre-resolve render race. The instance-level `isResponsavelOuAdmin` gate is deliberately unchanged, and the server `@PreAuthorize` + responsável/ADMIN check remain authoritative for the entregar action. |
| T-108-06 | Information Disclosure | Accordion collapsing version history | accept | Collapsing is purely presentational; every version is still rendered in the DOM (just visually collapsed) exactly as before, to the same already-authorized viewer. No data is newly exposed or hidden at the API layer. |
| T-108-07 | Denial of Service (a11y) | non-focusable Tooltip marker | mitigate | The marker `<span>` gets `tabIndex={0}` + `aria-label`, so the new tooltip is keyboard-reachable and screen-reader-labeled — avoiding a mouse-only accessibility regression (UI-SPEC finding #6). |
| T-108-SC | Tampering | npm/pnpm installs | accept | Zero new dependencies; `NativeSelect`/`Tooltip`/`Accordion` all installed in Phase 101 (Accordion's Radix primitive vetted by Phase 101's legitimacy gate). |
</threat_model>

<verification>
- `versaoFinalId` is a `NativeSelect`; the 4 `permissions.isLoading` sites use `isFetched`; `isResponsavelOuAdmin` unchanged; zero native `<select>`.
- The timeline is an `Accordion` with one default-open item and a `Tooltip` (tabIndex/aria-label) on every marker.
- Plan-level `pnpm build`/`pnpm lint` is covered by the Wave-2 holistic gate (Plan 04).
</verification>

<success_criteria>
- PARC-18 (dialog NativeSelect), PARC-19 (timeline Tooltip), PARC-20 (Accordion) all satisfied for the Pareceres detail page.
- 4 of the 7 bundled RBAC sites fixed here (pareceres/[id]/page.tsx:83, 158, 160, 217).
</success_criteria>

<output>
Create `.planning/phases/LEXCV-108-m-dulo-pareceres/108-03-SUMMARY.md` when done.
</output>
