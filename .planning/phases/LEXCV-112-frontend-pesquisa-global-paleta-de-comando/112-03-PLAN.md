---
phase: 112-frontend-pesquisa-global-paleta-de-comando
plan: "03"
type: execute
wave: 3
depends_on: [112-02]
files_modified:
  - web/src/components/shared/dashboard-shell.tsx
autonomous: true
requirements: [SRCH-05]

must_haves:
  truths:
    - "The decorative topbar search input is replaced by a button that opens the search palette; it looks near-identical to today's input and shows a ⌘K / Ctrl K hint"
    - "On mobile (below md) a Search icon button in the topbar action group opens the same palette"
    - "GlobalSearchDialog is mounted exactly once so a single shared open-state backs both triggers and the keyboard shortcut"
  artifacts:
    - path: "web/src/components/shared/dashboard-shell.tsx"
      provides: "topbar desktop+mobile search triggers + single GlobalSearchDialog mount"
      contains: "GlobalSearchDialog"
  key_links:
    - from: "topbar desktop + mobile triggers"
      to: "GlobalSearchDialog open-state"
      via: "window.dispatchEvent(new Event(SEARCH_OPEN_EVENT))"
      pattern: "SEARCH_OPEN_EVENT"
    - from: "dashboard-shell"
      to: "GlobalSearchDialog"
      via: "mounted once in the topbar action group, zero props"
      pattern: "<GlobalSearchDialog"
---

<objective>
Wire the search palette into the topbar: replace the decorative `<Input>` (dashboard-shell.tsx:121-127) with a fake-input `<button>` that dispatches `SEARCH_OPEN_EVENT` and shows a platform-aware ⌘K / Ctrl K hint; add a mobile-only `Search` icon trigger in the existing action group; and mount `<GlobalSearchDialog />` exactly once. Both triggers plus the keyboard shortcut drive the single shared open-state the dialog owns.

Purpose: Realizes SRCH-05 (open from the topbar field or via Ctrl+K/⌘K, from any authenticated page). Depends on Plan 112-02 (imports `GlobalSearchDialog` + `SEARCH_OPEN_EVENT`). This is the integration point deliberately kept as its own small plan so the topbar edit is isolated from the component build.

Output: modified `web/src/components/shared/dashboard-shell.tsx`.
</objective>

<execution_context>
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/LEXCV-112-frontend-pesquisa-global-paleta-de-comando/112-CONTEXT.md
@.planning/phases/LEXCV-112-frontend-pesquisa-global-paleta-de-comando/112-UI-SPEC.md
@.planning/phases/LEXCV-112-frontend-pesquisa-global-paleta-de-comando/112-PATTERNS.md

<interfaces>
<!-- Contracts this plan CONSUMES from Plan 112-02. -->
import { GlobalSearchDialog, SEARCH_OPEN_EVENT } from "@/components/shared/global-search-dialog";
  // GlobalSearchDialog: zero-prop, self-contained, listens for a window Event named SEARCH_OPEN_EVENT and for Ctrl/⌘+K.
  // SEARCH_OPEN_EVENT: string — dispatch `new Event(SEARCH_OPEN_EVENT)` on window to open the palette.

<!-- Existing structure this plan EDITS (verified by direct read of dashboard-shell.tsx). -->
// Lines 121-127: the decorative <Input placeholder="Pesquisar processos, entidades..."> inside `hidden md:flex flex-1 max-w-md relative group` — to be REPLACED by the desktop trigger button (same wrapper + classes).
// Lines 112-119: the hamburger <button md:hidden h-9 w-9 ... aria-label="Abrir menu"> — the mobile icon-button precedent to copy for the new Search trigger.
// Lines 147-152: `<div className="ml-auto flex items-center gap-3">` holding ThemeToggle / NotificationBell / divider / UserMenu — the mount point + where the mobile Search trigger goes (immediately before <ThemeToggle/>).
// `Search` is already imported from lucide-react (line 17); `Input` is imported (line 22) and used ONLY in the block being replaced.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace decorative input with desktop trigger + kbd hint, add mobile trigger, mount GlobalSearchDialog once</name>
  <files>web/src/components/shared/dashboard-shell.tsx</files>
  <read_first>
    - web/src/components/shared/dashboard-shell.tsx (full file — the topbar structure at 110-153; the decorative input 121-127; the hamburger icon-button 112-119; the action group 147-152; existing imports 1-27)
    - web/src/components/shared/global-search-dialog.tsx (Plan 112-02 — confirm the exported names GlobalSearchDialog + SEARCH_OPEN_EVENT and that it is zero-prop)
    - web/src/components/shared/notification-bell.tsx (the sibling self-contained component already mounted once in this action group — the pattern GlobalSearchDialog mirrors)
    - .planning/phases/LEXCV-112-frontend-pesquisa-global-paleta-de-comando/112-UI-SPEC.md ("Trigger placement" — exact desktop class string to reuse, mobile 36px h-9 w-9 aria-label="Pesquisar", kbd hint copy "⌘K"/"Ctrl K")
    - .planning/phases/LEXCV-112-frontend-pesquisa-global-paleta-de-comando/112-PATTERNS.md (section "dashboard-shell.tsx" — the exact three snippets: decorative input to replace, hamburger precedent, action-group mount)
  </read_first>
  <action>
    Edit `web/src/components/shared/dashboard-shell.tsx`:

    1. Import at top: `import { GlobalSearchDialog, SEARCH_OPEN_EVENT } from "@/components/shared/global-search-dialog";`. Define a tiny module-level (or inline) helper `const openSearch = () => window.dispatchEvent(new Event(SEARCH_OPEN_EVENT));` usable by both triggers (or call it inline in each onClick).

    2. Desktop trigger — replace the `<Input>` block (lines 121-127) with a `<button type="button" onClick={openSearch}>` INSIDE the SAME `<div className="hidden md:flex flex-1 max-w-md relative group">` wrapper, carrying the SAME visual classes the input has today (`pl-9 bg-slate-100/50 dark:bg-slate-900/50 border border-slate-200 dark:border-slate-800 focus-visible:ring-1 focus-visible:ring-blue-500/50 rounded-full text-sm h-9 shadow-sm transition-all` + full-width + left text alignment) so it looks near-identical. Keep the left-aligned `Search` icon with `group-focus-within:text-blue-500`. Show placeholder-style text "Pesquisar..." in `text-slate-400`/muted. Add a trailing `<kbd>` hint (right-aligned, `text-xs` muted, Regular weight) showing the platform label (see step 4). `aria-label="Pesquisar"`. Do NOT keep any `<input>` element here. `rounded-full` is a fixed utility (pill shape), unaffected by Phase 114 — do not hardcode any other radius.

    3. Mobile trigger — in the `<div className="ml-auto flex items-center gap-3">` action group (lines 147-152), add a `<button type="button" onClick={openSearch} aria-label="Pesquisar" className="md:hidden flex items-center justify-center h-9 w-9 rounded-md text-slate-500 hover:text-slate-900 dark:hover:text-slate-100 hover:bg-slate-100 dark:hover:bg-slate-800 transition-colors">` with a `<Search className="h-5 w-5" />`, placed IMMEDIATELY BEFORE `<ThemeToggle />` (copy the hamburger button's shape verbatim; `md:hidden` guarantees exactly one trigger is visible at any width). Do NOT wrap it in a Tooltip.

    4. Kbd hint platform detection — the label must be "⌘K" on macOS and "Ctrl K" elsewhere. To avoid an SSR/hydration mismatch (navigator is undefined on the server, and dashboard-shell is SSR'd even as a client component), initialize a state to the stable non-mac default (`const [shortcutLabel, setShortcutLabel] = React.useState("Ctrl K")`) and set the mac label in a mount-only `React.useEffect` (`if (typeof navigator !== "undefined" && /Mac|iPhone|iPad/i.test(navigator.platform || navigator.userAgent)) setShortcutLabel("⌘K")`). Render `shortcutLabel` inside the desktop trigger's `<kbd>`. First paint is "Ctrl K" on both server and client, then upgrades post-mount — no mismatch.

    5. Mount `<GlobalSearchDialog />` once (zero props) inside the action group (e.g. right after `<NotificationBell />`), same relationship the shell has with `<NotificationBell />`.

    6. Remove the now-unused `Input` import (line 22) — it is used only in the replaced block. Keep the `Search` lucide import (still used by both triggers). Do not touch the sidebar/drawer/NAV or any other topbar element.
  </action>
  <verify>
    <automated>cd web && pnpm exec tsc --noEmit && pnpm lint && pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - `cd web && pnpm build` succeeds and `pnpm lint` reports no new errors (an unused `Input` import would fail lint — proves the import was removed).
    - `grep -c "GlobalSearchDialog" web/src/components/shared/dashboard-shell.tsx` returns >= 2 (import + single `<GlobalSearchDialog />` mount).
    - `grep -c "SEARCH_OPEN_EVENT" web/src/components/shared/dashboard-shell.tsx` returns >= 1 and `grep -c "dispatchEvent" ...` returns >= 1 (triggers dispatch the open event).
    - `grep -c "aria-label=\"Pesquisar\"" web/src/components/shared/dashboard-shell.tsx` returns >= 1 (accessible mobile trigger).
    - `grep -c "<input" web/src/components/shared/dashboard-shell.tsx` returns 0 in the topbar region — the decorative input is gone (the desktop trigger is a `<button>`).
    - Source review: exactly one `<GlobalSearchDialog />`; desktop trigger keeps the `rounded-full ... h-9` visual classes + `Search` icon + `<kbd>` hint; mobile trigger is `md:hidden h-9 w-9` before `<ThemeToggle />`; shortcut label defaults to "Ctrl K" and upgrades to "⌘K" only in a mount effect (no navigator access during initial render).
  </acceptance_criteria>
  <done>The topbar shows a desktop fake-input trigger (with a ⌘K/Ctrl K hint) and a mobile Search icon trigger, both opening the single mounted GlobalSearchDialog via SEARCH_OPEN_EVENT; the decorative input and its now-unused import are gone; the app builds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| topbar trigger → GlobalSearchDialog open-state | Same-origin, in-page `window` Event with no payload — no cross-origin or untrusted data crosses |
| global Ctrl/⌘+K keydown | A new app-wide keyboard shortcut is registered (inside the dialog, Plan 112-02) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-112-08 | Denial of Service / UX-hijack | global Ctrl/⌘+K keydown listener | accept | The keydown handler (owned by the dialog, Plan 112-02) matches only the ⌘K/Ctrl+K combo and calls preventDefault for exactly that combo; toggling a search dialog is the deliberate, industry-standard use of this shortcut. No untrusted input; no interference with normal typing (only the modifier+K combo is intercepted). Low risk, accepted |
| T-112-09 | Tampering | SEARCH_OPEN_EVENT window event | accept | The event carries no payload and is dispatched/consumed entirely within the same authenticated same-origin page; any script already running in-page has full DOM access regardless, so the event adds no new attack surface |
| T-112-SC | Tampering (supply chain) | package installs | accept (N/A) | Zero new dependencies — GlobalSearchDialog and lucide `Search` already exist. No npm install; package-legitimacy gate does not apply |
</threat_model>

<verification>
- `cd web && pnpm build` — the topbar edit compiles and the whole app builds.
- `cd web && pnpm lint` — passes, which also proves the unused `Input` import was removed (Next/eslint flags unused imports).
- Grep assertions prove: single GlobalSearchDialog mount, dispatch of SEARCH_OPEN_EVENT, accessible mobile trigger, and removal of the decorative `<input>`.
- No high-severity threat introduced (frontend-only in-page wiring; no new backend surface, no new dependency).
</verification>

<success_criteria>
- SRCH-05: the user opens the palette from the topbar field (desktop fake-input button) or the mobile Search icon, or via Ctrl+K/⌘K, from any authenticated dashboard page.
- The desktop trigger is visually near-identical to today's decorative input and shows a platform-aware shortcut hint with no hydration mismatch.
- Exactly one GlobalSearchDialog instance backs both triggers and the keyboard shortcut.
- The decorative input and its now-unused import are removed; the app builds clean.
</success_criteria>

<output>
Create `.planning/phases/LEXCV-112-frontend-pesquisa-global-paleta-de-comando/112-03-SUMMARY.md` when done.
</output>
