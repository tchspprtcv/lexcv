---
phase: 112-frontend-pesquisa-global-paleta-de-comando
plan: "05"
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/app/(dashboard)/documentos/page.tsx
  - web/src/app/(dashboard)/pareceres/page.tsx
autonomous: true
requirements: [SRCH-09]

must_haves:
  truths:
    - "Landing on /documentos?q=<termo> pre-fills a visible name filter and narrows the already-fetched document list to names containing <termo>, client-side (documentos has no server-side free-text filter)"
    - "Landing on /pareceres?q=<termo> opens the advanced-search panel, pre-fills its text box with <termo>, and shows the matching pareceres"
    - "Navigating to a new ?q= while already on either page re-seeds it"
  artifacts:
    - path: "web/src/app/(dashboard)/documentos/page.tsx"
      provides: "new client-side name filter seeded from ?q="
      contains: "useSearchParams"
    - path: "web/src/app/(dashboard)/pareceres/page.tsx"
      provides: "?q= seeds the advanced-search texto path (pesquisaTexto/pesquisaFilters/pesquisaSubmitted/pesquisaOpen)"
      contains: "useSearchParams"
  key_links:
    - from: "documentos page"
      to: "client-side .filter() over list.data by nome"
      via: "new q state seeded from searchParams.get('q'); filtered array feeds DataTable + mobile cards"
      pattern: "useSearchParams"
    - from: "pareceres page"
      to: "usePesquisarPareceres advanced-search path"
      via: "seed sets pesquisaTexto + pesquisaFilters({texto}) + pesquisaSubmitted(true) + pesquisaOpen(true)"
      pattern: "setPesquisaSubmitted"
---

<objective>
Make the palette's "Ver todos os Documentos" / "Ver todos os Pareceres" links land correctly on the two list pages whose filter shape is NOT the simple `filters.q` pattern:
- Documentos has NO free-text filter today (client or server) — add a small client-side name filter, seeded from `?q=`, that narrows the already-fetched list.
- Pareceres has a dual-mode filter — the free-text carrier is the advanced-search `pesquisaTexto`/`pesquisaFilters` path (only active when `pesquisaSubmitted`), so seeding must target THAT path and open its panel, never the unrelated simple `filters` state.

Purpose: Consumer side of SRCH-09 for the two "gap" list pages (per 112-PATTERNS.md's explicit gap notes). Independent of the palette component (decoupled by the `?q=` URL contract) → Wave 1.

Output: modified `documentos/page.tsx` (new client-side filter) and `pareceres/page.tsx` (advanced-search seeding).
</objective>

<execution_context>
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/LEXCV-112-frontend-pesquisa-global-paleta-de-comando/112-CONTEXT.md
@.planning/phases/LEXCV-112-frontend-pesquisa-global-paleta-de-comando/112-PATTERNS.md

<interfaces>
<!-- Existing machinery this plan SEEDS (verified by direct read). Read the gap notes carefully — these two pages
     do NOT share the clientes/processos `filters.q` shape. -->
// documentos/page.tsx: inner DocumentosContent. DocumentosListFilters has ONLY processo_id/cliente_id (NO q).
//   useDocumentos has no free-text param. Results block renders `list.data` directly with NO .filter() — DataTable
//   (data={list.data}, ~line 205) + mobile cards (list.data.map, ~line 208). => a NEW client-side filter is required.
// pareceres/page.tsx: inner ParecerPageContent. `filters`/setFilters is the SIMPLE list (no q/texto). The advanced
//   search uses pesquisaTexto (line 52), pesquisaFilters (58, ParecerPesquisaFilters with `texto`), pesquisaSubmitted
//   (59 — gates `searchActive`, line 76), pesquisaOpen (51 — renders the panel, line 239). Submit handler onPesquisar
//   (98-115) is the shape to mirror programmatically. usePesquisarPareceres runs only when pesquisaSubmitted===true.
// dashboard-shell.tsx is the precedent that useSearchParams needs no extra Suspense wrapper here (112-PATTERNS.md).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add a client-side name filter to Documentos, seeded from ?q=</name>
  <files>web/src/app/(dashboard)/documentos/page.tsx</files>
  <read_first>
    - web/src/app/(dashboard)/documentos/page.tsx (full file — DocumentosContent 53-232; the Filtros card 123-185; the results block 191-227 that renders list.data into DataTable at 205 and mobile cards at 208-224; the outer permission gate 35-42)
    - web/src/components/shared/notification-bell.tsx (lines 66-68 — the display-only `.filter()` over an already-fetched array precedent, the closest analog for filtering without touching the query hook)
    - web/src/components/ui/input.tsx (the Input primitive to reuse for the visible name filter)
    - .planning/phases/LEXCV-112-frontend-pesquisa-global-paleta-de-comando/112-PATTERNS.md (section "documentos/page.tsx" — the gap note: no q exists client- or server-side; add a display-only filter over d.nome)
  </read_first>
  <action>
    In `documentos/page.tsx`, add `useSearchParams` from `"next/navigation"` and `Input` from `@/components/ui/input` (if not already imported). Inside `DocumentosContent`:
    - Add `const searchParams = useSearchParams();`, `const seededQ = searchParams.get("q");`, and a new state `const [nomeFiltro, setNomeFiltro] = React.useState("");`.
    - Seed effect: `React.useEffect(() => { if (seededQ) setNomeFiltro(seededQ); }, [seededQ]);` (depend on the extracted `q` value — first load AND same-page re-navigation both re-seed).
    - Add a VISIBLE controlled input so the user sees what is being filtered: place a labeled `<Input value={nomeFiltro} onChange={(e) => setNomeFiltro(e.target.value)} placeholder="Pesquisar por nome..." />` inside the Filtros `CardContent` (as a sibling of the existing react-hook-form `<form>`, NOT a field inside it — do not add it to the zodResolver form). A small label "Pesquisar" consistent with the other filter labels.
    - Derive the filtered list once: `const documentosVisiveis = React.useMemo(() => { const termo = nomeFiltro.trim().toLowerCase(); const base = list.data ?? []; return termo ? base.filter((d) => (d.nome ?? "").toLowerCase().includes(termo)) : base; }, [list.data, nomeFiltro]);`
    - Replace `list.data` in the RESULTS RENDER block with `documentosVisiveis`: the empty check becomes `!documentosVisiveis.length`, `<DataTable ... data={documentosVisiveis} ...>` (line ~205), and the mobile map `documentosVisiveis.map(...)` (line ~208). Keep `list.isPending`/`list.isError` exactly as-is (they gate on the fetch, not the client filter). Do NOT modify `useDocumentos`, `DocumentosListFilters`, or the backend — this is purely a display-side filter (per the gap note). Do not alter the outer permission gate or the existing processo_id/cliente_id filters.
  </action>
  <verify>
    <automated>cd web && pnpm exec tsc --noEmit && pnpm lint</automated>
  </verify>
  <acceptance_criteria>
    - `cd web && pnpm exec tsc --noEmit` exits 0 and `pnpm lint` reports no new errors for the file.
    - `grep -c "useSearchParams" web/src/app/(dashboard)/documentos/page.tsx` returns >= 1 (from `"next/navigation"`).
    - `grep -c "nomeFiltro" web/src/app/(dashboard)/documentos/page.tsx` returns >= 4 (state, seed effect, input binding, filter memo).
    - A `.filter(` over `list.data` (or a derived base) matching `d.nome` case-insensitively exists; the DataTable and mobile map consume the derived `documentosVisiveis`, not `list.data` directly.
    - `useDocumentos` / `DocumentosListFilters` are unchanged (grep shows no new field added to the filters type); no backend file touched.
    - A visible `<Input>` bound to `nomeFiltro` exists in the Filtros card, outside the react-hook-form `<form>`.
  </acceptance_criteria>
  <done>/documentos?q=<termo> pre-fills a visible name filter and client-side narrows the fetched document list by nome; the fetch hook and backend are untouched; other filters and the permission gate are preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Seed ?q= into the Pareceres advanced-search path</name>
  <files>web/src/app/(dashboard)/pareceres/page.tsx</files>
  <read_first>
    - web/src/app/(dashboard)/pareceres/page.tsx (full file — ParecerPageContent 42-443; the dual-mode state 45-59; searchActive 76; onPesquisar 98-115; the panel rendered when pesquisaOpen at 239; outer permission gate 26-40)
    - web/src/hooks/use-pareceres.ts (usePesquisarPareceres 60-81 — runs only when `enabled: pesquisaSubmitted`; ParecerPesquisaFilters.texto is the free-text carrier)
    - .planning/phases/LEXCV-112-frontend-pesquisa-global-paleta-de-comando/112-PATTERNS.md (section "pareceres/page.tsx" — the gap note: seed the pesquisa* path, NOT filters/setFilters)
  </read_first>
  <action>
    In `pareceres/page.tsx`, add `useSearchParams` from `"next/navigation"`. Inside `ParecerPageContent` (NOT the outer gate), add `const searchParams = useSearchParams();`, `const seededQ = searchParams.get("q");`, and a seed effect:
    `React.useEffect(() => { if (seededQ) { setPesquisaTexto(seededQ); setPesquisaFilters({ texto: seededQ }); setPesquisaSubmitted(true); setPesquisaOpen(true); } }, [seededQ]);`
    This mirrors the `onPesquisar` submit handler programmatically: it populates the advanced-search text box (`pesquisaTexto`), sets the active filter object (`pesquisaFilters` with just `texto`), activates the search path (`pesquisaSubmitted = true`, which makes `searchActive`/`usePesquisarPareceres` take over from the simple list), AND opens the panel (`pesquisaOpen = true`) so the populated search box is actually visible rather than hidden behind the collapsed toggle. Do NOT write to `filters`/`setFilters` — that state has no `texto`/`q` field and drives the unrelated simple-list mode. Do not touch the other pesquisa* fields (cliente/advogado/status/datas) or the outer permission gate. Depend on the extracted `seededQ` value so first load and same-page re-navigation both re-seed.
  </action>
  <verify>
    <automated>cd web && pnpm exec tsc --noEmit && pnpm lint && pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - `cd web && pnpm build` succeeds (this is the last Wave-1 page — a full build confirms all four list-page edits compile together) and `pnpm lint` reports no new errors.
    - `grep -c "useSearchParams" web/src/app/(dashboard)/pareceres/page.tsx` returns >= 1 (from `"next/navigation"`).
    - The seed effect calls all four setters — `setPesquisaTexto`, `setPesquisaFilters`, `setPesquisaSubmitted(true)`, `setPesquisaOpen(true)` — and does NOT call `setFilters`.
    - `grep -c "setFilters" ` in the newly-added seed code is 0 (grep the seed effect region; the pre-existing onApply/onClear may still use setFilters — only the new effect must avoid it).
    - The effect's dependency array is the extracted `q` value (not `[]`, not the raw searchParams object).
  </acceptance_criteria>
  <done>/pareceres?q=<termo> opens the advanced-search panel, pre-fills its text box, activates the pesquisa path, and shows matching pareceres — without touching the unrelated simple-list `filters` state or the permission gate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL `?q=` → documentos client-side `.filter()` over already-fetched data | Reflected into a client-side string match and a visible input |
| URL `?q=` → pareceres `pesquisaTexto` → GET /pareceres/pesquisa?texto= | Reflected into an input and sent to the existing advanced-search endpoint |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-112-12 | Tampering / reflected XSS | `?q=` seeded into `nomeFiltro`/`pesquisaTexto` → `<Input value>` | mitigate | Both flow into controlled React `<Input value>` bindings (auto-escaped); the documentos filter is a plain `String.includes()` over already-tenant-scoped fetched data; no `dangerouslySetInnerHTML`. No script-execution path |
| T-112-13 | Information Disclosure | documentos client-side filter operates on already-fetched, already-tenant-scoped `list.data` | accept | The client filter only narrows data the user is already authorized to see and already received from the tenant-scoped GET /documentos; it never fetches more. Pareceres seeding reuses the existing tenant-scoped /pareceres/pesquisa endpoint unchanged |
| T-112-SC | Tampering (supply chain) | package installs | accept (N/A) | Zero new dependencies (`useSearchParams` is part of the already-installed `next`; `Input` already exists). No npm install; package-legitimacy gate does not apply |
</threat_model>

<verification>
- `cd web && pnpm build` — all four list-page edits (this plan + Plan 112-04) compile together.
- `cd web && pnpm lint` — both edited pages lint clean.
- Grep assertions prove: documentos gains a client-side nome filter seeded from `?q=` without touching useDocumentos/DocumentosListFilters; pareceres seeds the pesquisa* advanced path (all four setters) and never `setFilters`.
- No high-severity threat: reflected `q` is React-escaped; documentos filter operates only on already-authorized fetched data; pareceres reuses the existing tenant-scoped endpoint.
</verification>

<success_criteria>
- SRCH-09 (Documentos + Pareceres half): "Ver todos os Documentos" lands on /documentos?q=X with a visible pre-filled name filter narrowing the list client-side; "Ver todos os Pareceres" lands on /pareceres?q=X with the advanced-search panel open, text pre-filled, and results shown.
- Documentos gets a genuinely new (display-only) client-side filter — not a fake reuse of a non-existent `q`; useDocumentos/backend untouched.
- Pareceres seeds the correct dual-mode path (pesquisa*), never the unrelated simple `filters` state.
- Same-page re-navigation re-seeds; both pages build/lint clean.
</success_criteria>

<output>
Create `.planning/phases/LEXCV-112-frontend-pesquisa-global-paleta-de-comando/112-05-SUMMARY.md` when done.
</output>
