---
phase: 43-data-layer-backend-endpoints
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/main/java/com/lexcv/controllers/ResourceController.java
autonomous: true
requirements: [FIN-03, FIN-04, FIN-05, FIN-06]

must_haves:
  truths:
    - "GET /api/v1/honorarios/{id} returns the honorario JSON for the correct tenant or 404 if not found/wrong tenant"
    - "PUT /api/v1/honorarios/{id} updates valorTotal, descricao, dataAcordo and ignores processoId from body"
    - "DELETE /api/v1/honorarios/{id} returns 409 Conflict when the honorario has pagamentos; returns 204 when it has none"
    - "DELETE /api/v1/pagamentos/{id} deletes the pagamento and subtracts valorPago from ContaCorrente saldo, then returns 204"
    - "All four new endpoints enforce @PreAuthorize with the correct authority"
    - "All four new endpoints scope access via processo.getTenantId() (no direct tenant_id on Honorario)"
  artifacts:
    - path: "backend/src/main/java/com/lexcv/controllers/ResourceController.java"
      provides: "Four new endpoint methods after line 1819 (after existing createPagamento)"
      contains: "deleteHonorario"
  key_links:
    - from: "ResourceController.deleteHonorario"
      to: "pagamentoRepository.findByHonorarioId"
      via: "409 guard"
      pattern: "findByHonorarioId"
    - from: "ResourceController.deletePagamento"
      to: "contaCorrenteRepository.findByClienteId"
      via: "saldo subtraction"
      pattern: "subtract"
---

<objective>
Add the four missing CRUD endpoints for honorarios and pagamentos to ResourceController.java. These endpoints are currently absent — GET /honorarios/{id} returns 404, and PUT/DELETE variants do not exist. The useHonorario hook already calls GET /honorarios/{id} and has been getting 404 responses.

Purpose: Complete the backend CRUD surface for the financeiro module so that the frontend hooks added in Plan 01 have endpoints to call.
Output: Four new handler methods in ResourceController.java, positioned after the existing createPagamento method (line 1819).
</objective>

<execution_context>
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Honorario JPA model (backend/src/main/java/com/lexcv/models/Honorario.java): -->
```java
// Fields: id (Integer), processoId (UUID), valorTotal (BigDecimal), descricao (String), dataAcordo (LocalDate)
// No tenant_id, no createdAt
```

<!-- Pagamento JPA model (backend/src/main/java/com/lexcv/models/Pagamento.java): -->
```java
// Fields: id (Integer), honorarioId (Integer), valorPago (BigDecimal), dataPagamento (LocalDate), metodo (String)
// No tenant_id
```

<!-- Existing tenant scoping pattern (ResourceController.java lines 1780-1789): -->
```java
Honorario hon = honorarioRepository.findById(id).orElse(null);
if (hon == null) {
    return ResponseEntity.status(HttpStatus.NOT_FOUND).body(Map.of("message", "Honorário não encontrado"));
}
Processo processo = processoRepository.findById(hon.getProcessoId()).orElse(null);
if (processo == null || !processo.getTenantId().equals(getTenantId())) {
    return ResponseEntity.status(HttpStatus.NOT_FOUND).body(Map.of("message", "Honorário não encontrado"));
}
```

<!-- ContaCorrente saldo addition in createPagamento (lines 1806-1816 — invert subtract for delete): -->
```java
ContaCorrente cc = contaCorrenteRepository.findByClienteId(clienteId)
    .orElseGet(() -> contaCorrenteRepository.save(
        ContaCorrente.builder().clienteId(clienteId).saldo(BigDecimal.ZERO).build()
    ));
cc.setSaldo(cc.getSaldo().add(pag.getValorPago())); // for DELETE: .subtract(pag.getValorPago())
contaCorrenteRepository.save(cc);
```

<!-- Existing authorities in ResourceController (example pattern): -->
<!-- financeiro:view, financeiro:edit, financeiro:manage -->

<!-- ResourceController already imports: -->
<!-- Map, List, ArrayList, HttpStatus, BigDecimal, UUID, ResponseEntity -->
<!-- All repositories: honorarioRepository, pagamentoRepository, processoRepository, contaCorrenteRepository -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add GET /honorarios/{id} and PUT /honorarios/{id} to ResourceController</name>
  <files>backend/src/main/java/com/lexcv/controllers/ResourceController.java</files>
  <read_first>
    - backend/src/main/java/com/lexcv/controllers/ResourceController.java lines 1778-1820 (existing listHonorarioPagamentos and createPagamento — the pattern to follow and the insertion point)
    - backend/src/main/java/com/lexcv/models/Honorario.java (all fields and their Java types)
  </read_first>
  <action>
Insert two new methods into ResourceController.java immediately after the closing brace of createPagamento (after line 1819, before the DASHBOARD comment block at line 1821).

Method 1 — getHonorario:
- Annotation: @PreAuthorize("hasAuthority('financeiro:view')")
- Annotation: @GetMapping("/honorarios/{id}")
- Signature: public ResponseEntity<?> getHonorario(@PathVariable Integer id)
- Body: Apply the tenant scoping pattern (find honorario by id → find processo by hon.getProcessoId() → verify processo.getTenantId().equals(getTenantId())). Return 404 with Map.of("message", "Honorário não encontrado") if either lookup fails or tenant doesn't match. Return ResponseEntity.ok(hon) if tenant matches.

Method 2 — updateHonorario:
- Annotation: @PreAuthorize("hasAuthority('financeiro:edit')")
- Annotation: @PutMapping("/honorarios/{id}")
- Signature: public ResponseEntity<?> updateHonorario(@PathVariable Integer id, @RequestBody Map<String, Object> body)
- Body: Apply same tenant scoping pattern. After verification, update only the three editable fields from body — do NOT touch processoId under any circumstances:
  - If body contains "valorTotal": hon.setValorTotal(new BigDecimal(body.get("valorTotal").toString()))
  - If body contains "descricao": hon.setDescricao(body.get("descricao") == null ? null : body.get("descricao").toString())
  - If body contains "dataAcordo": hon.setDataAcordo(body.get("dataAcordo") == null ? null : java.time.LocalDate.parse(body.get("dataAcordo").toString()))
- Save and return ResponseEntity.ok(honorarioRepository.save(hon)).
- Wrap dataAcordo parsing in try/catch — return 400 with Map.of("message", "Data de acordo inválida") if LocalDate.parse throws.
  </action>
  <verify>
    <automated>cd C:/Users/francisco.horta/Documents/projects/personal/lexcv/backend && grep -n "getHonorario\|updateHonorario" src/main/java/com/lexcv/controllers/ResourceController.java</automated>
  </verify>
  <done>Both method names appear in the grep output. mvn -DskipTests package exits 0.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add DELETE /honorarios/{id} and DELETE /pagamentos/{id} to ResourceController</name>
  <files>backend/src/main/java/com/lexcv/controllers/ResourceController.java</files>
  <read_first>
    - backend/src/main/java/com/lexcv/controllers/ResourceController.java lines 1793-1820 (createPagamento with ContaCorrente logic — DELETE reverses this)
    - backend/src/main/java/com/lexcv/models/Pagamento.java (fields to understand honorarioId navigation chain)
  </read_first>
  <action>
Insert two more methods after the two methods added in Task 1 (still before the DASHBOARD block).

Method 3 — deleteHonorario:
- Annotation: @PreAuthorize("hasAuthority('financeiro:manage')")
- Annotation: @DeleteMapping("/honorarios/{id}")
- Signature: public ResponseEntity<?> deleteHonorario(@PathVariable Integer id)
- Body:
  1. Apply tenant scoping pattern (same as getHonorario).
  2. Check pagamentos: List<Pagamento> pagamentos = pagamentoRepository.findByHonorarioId(id);
  3. If !pagamentos.isEmpty(): return ResponseEntity.status(HttpStatus.CONFLICT).body(Map.of("message", "Não é possível eliminar um honorário com pagamentos registados"));
  4. honorarioRepository.deleteById(id);
  5. return ResponseEntity.noContent().build();

Method 4 — deletePagamento:
- Annotation: @PreAuthorize("hasAuthority('financeiro:manage')")
- Annotation: @DeleteMapping("/pagamentos/{id}")
- Signature: public ResponseEntity<?> deletePagamento(@PathVariable Integer id)
- Body:
  1. Pagamento pag = pagamentoRepository.findById(id).orElse(null); — return 404 if null.
  2. Navigate to honorario: Honorario hon = honorarioRepository.findById(pag.getHonorarioId()).orElse(null); — return 404 if null.
  3. Navigate to processo for tenant scoping: Processo processo = processoRepository.findById(hon.getProcessoId()).orElse(null); — return 404 if null or tenant mismatch.
  4. Saldo reversal (wrapped in try/catch Exception ignored): UUID clienteId = processo.getClienteId(); — ContaCorrente cc = contaCorrenteRepository.findByClienteId(clienteId).orElse(null); — if cc != null: cc.setSaldo(cc.getSaldo().subtract(pag.getValorPago())); contaCorrenteRepository.save(cc);
  5. pagamentoRepository.deleteById(id);
  6. return ResponseEntity.noContent().build();

Important: In deletePagamento, only subtract from ContaCorrente if cc != null (do not create a new ContaCorrente during deletion). This differs from createPagamento which uses orElseGet to create one.
  </action>
  <verify>
    <automated>cd C:/Users/francisco.horta/Documents/projects/personal/lexcv/backend && grep -n "deleteHonorario\|deletePagamento" src/main/java/com/lexcv/controllers/ResourceController.java</automated>
  </verify>
  <done>Both deleteHonorario and deletePagamento appear in grep output. mvn -DskipTests package exits 0 confirming no compile errors.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Authenticated client → ResourceController | JWT-authenticated requests; tenant derived from security context |
| ResourceController → Database | JPA queries scoped by tenant via processo chain |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-43-02-01 | Information Disclosure | GET /honorarios/{id} | mitigate | Tenant scoping via processo.getTenantId().equals(getTenantId()) — returns 404 (not 403) to avoid leaking existence |
| T-43-02-02 | Tampering | PUT /honorarios/{id} — processoId immutability | mitigate | Handler reads processoId from the fetched entity, never from request body — processoId cannot be changed |
| T-43-02-03 | Tampering | DELETE /honorarios/{id} — orphan pagamentos | mitigate | 409 Conflict guard: pagamentoRepository.findByHonorarioId(id).isEmpty() check before delete |
| T-43-02-04 | Tampering | DELETE /pagamentos/{id} — cross-tenant delete | mitigate | Tenant scoping: pag → hon → processo → getTenantId() chain before delete |
| T-43-02-05 | Tampering | DELETE /pagamentos/{id} — negative saldo | accept | Saldo can go negative (legitimate reversal of incorrectly recorded payment); no business rule against it |
| T-43-02-SC | Tampering | maven/pom.xml | accept | No new dependencies added in this plan |
</threat_model>

<verification>
After both tasks complete:
1. `mvn -DskipTests package` in backend/ exits 0 — all four new methods compile
2. `grep -n "getHonorario\|updateHonorario\|deleteHonorario\|deletePagamento" backend/src/main/java/com/lexcv/controllers/ResourceController.java` returns 4 method definitions
3. `grep -n "financeiro:view\|financeiro:edit\|financeiro:manage" backend/src/main/java/com/lexcv/controllers/ResourceController.java` shows the correct authority on each new endpoint
4. `grep -n "CONFLICT" backend/src/main/java/com/lexcv/controllers/ResourceController.java` confirms 409 guard in deleteHonorario
5. `grep -n "subtract" backend/src/main/java/com/lexcv/controllers/ResourceController.java` confirms saldo reversal in deletePagamento
</verification>

<success_criteria>
- GET /api/v1/honorarios/{id}: returns 200 + honorario JSON for valid tenant; 404 for unknown id or wrong tenant; requires financeiro:view
- PUT /api/v1/honorarios/{id}: updates valorTotal/descricao/dataAcordo; processoId unchanged; requires financeiro:edit
- DELETE /api/v1/honorarios/{id}: returns 409 if pagamentos exist; 204 if none; requires financeiro:manage
- DELETE /api/v1/pagamentos/{id}: returns 204 and subtracts valorPago from ContaCorrente; requires financeiro:manage
- mvn -DskipTests package exits 0
</success_criteria>

<output>
Create `.planning/phases/43-data-layer-backend-endpoints/43-02-SUMMARY.md` when done
</output>
