---
phase: 106-m-dulo-agenda
plan: 02
type: execute
wave: 2
depends_on: [106-01]
files_modified:
  - web/src/app/(dashboard)/agenda/novo/page.tsx
autonomous: true
requirements: [AGD-36]
must_haves:
  truths:
    - "agenda/novo access-denied gate (line 31) uses permissions.isFetched && !canCreateAgenda"
    - "The 4 RHF-bound <select> (processoId, tipo, prioridade, recurrenceRule) are NativeSelect size=default className='w-full'; the const selectClassName is deleted and const textareaClassName is kept (used by the descricao textarea)"
    - "dataInicio and dataFim use the shared DatePickerField withTime via Controller; recurrenceEndDate uses DatePickerField date-only via Controller, still gated inside the recurrenceRule !== 'NONE' conditional"
    - "No <Input type='datetime-local'> and no <Input type='date'> remain in the file; the form still submits YYYY-MM-DDTHH:mm / YYYY-MM-DD strings the eventoFormSchema accepts unchanged"
  artifacts:
    - path: "web/src/app/(dashboard)/agenda/novo/page.tsx"
      provides: "Create-Evento form on NativeSelect + DatePickerField (AGD-36)"
      contains: "DatePickerField"
  key_links:
    - from: "web/src/app/(dashboard)/agenda/novo/page.tsx"
      to: "@/components/shared/date-picker-field (DatePickerField)"
      via: "Controller control={form.control} wraps DatePickerField for each date field"
      pattern: "Controller"
---

<objective>
Migrate the create-Evento form (`agenda/novo/page.tsx`) to `NativeSelect` for its 4 RHF-bound selects and to the shared `DatePickerField` for its 3 date fields (`dataInicio`/`dataFim` with time, `recurrenceEndDate` date-only), and apply the bundled RBAC `isFetched` fix.

Purpose: Close the create-form share of AGD-36 (Calendar in the date inputs) and CLP-style NativeSelect consistency for Agenda forms.
Output: 1 file migrated; `pnpm build` green. Runs in Wave 2, parallel with 106-03 (different file).
</objective>

<execution_context>
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/LEXCV-106-m-dulo-agenda/106-CONTEXT.md
@.planning/phases/LEXCV-106-m-dulo-agenda/106-PATTERNS.md
@.planning/phases/LEXCV-106-m-dulo-agenda/106-UI-SPEC.md
@web/src/app/(dashboard)/agenda/novo/page.tsx
@web/src/components/ui/native-select.tsx
@web/src/schemas/eventos.ts

<interfaces>
<!-- Contracts the executor needs; created in 106-01, use directly. -->
- DatePickerField from "@/components/shared/date-picker-field" (created in 106-01): props { value: string | undefined; onChange: (v: string) => void; withTime?: boolean }. Emits YYYY-MM-DDTHH:mm when withTime, YYYY-MM-DD otherwise.
- NativeSelect from "@/components/ui/native-select": wrapper div defaults to w-fit (NOT w-full like Input) — per 106-CONTEXT.md Established Patterns, ALWAYS pass className="w-full" on RHF form fields so the control keeps the full width the old selectClassName gave it. register()-compatible: `<NativeSelect {...form.register("X")}>`.
- Controller from react-hook-form: DatePickerField renders a Button+Calendar (not a native input), so it is wired with Controller, not register — mirror clientes/novo/page.tsx's Controller idiom (106-PATTERNS.md Shared Pattern 4(c)).
- eventoFormSchema (web/src/schemas/eventos.ts): validates dataInicio/dataFim as non-empty Date-parseable strings with dataFim >= dataInicio, and requires recurrenceEndDate when recurrenceRule !== "NONE". No schema change needed — the new strings parse identically.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: RBAC fix + 4 NativeSelect + drop selectClassName</name>
  <files>web/src/app/(dashboard)/agenda/novo/page.tsx</files>
  <read_first>
    - 106-PATTERNS.md Shared Pattern 1 (RBAC, novo line 31 — and the warning NOT to touch line 256's submit-button `permissions.isLoading` guard) and Shared Pattern 2 (NativeSelect, exact before/after, the 4 occurrences table, selectClassName elimination, keep textareaClassName).
    - 106-UI-SPEC.md "Component Inventory > NativeSelect" (size=default, eliminate selectClassName).
    - agenda/novo/page.tsx: line 31 (RBAC gate), line 21 (const selectClassName), line 24 (const textareaClassName), the 4 selects at lines 119-133 (processoId), 146-157 (tipo), 174-178 (prioridade), 216-225 (recurrenceRule), and line 66 (`const recurrenceRule = form.watch("recurrenceRule")` — keep).
  </read_first>
  <action>
    Add `import { NativeSelect } from "@/components/ui/native-select";`. Change the access gate at line 31 from `if (!permissions.isLoading && !canCreateAgenda) {` to `if (permissions.isFetched && !canCreateAgenda) {` — do NOT touch line 256's `disabled={... || permissions.isLoading || ...}` submit-button guard. Replace each of the 4 `<select ... className={selectClassName} {...form.register("X")}>` with `<NativeSelect id="X" size="default" className="w-full" {...form.register("X")}>`, keeping every `<option>` child verbatim (including the dynamic processoId placeholder and the disabled={processos.isPending || processos.isError} prop on the processoId select). DELETE `const selectClassName = "..."` at line 21 once all 4 migrate; KEEP `const textareaClassName` at line 24 (still used by the descricao `<textarea>`). Leave `const recurrenceRule = form.watch("recurrenceRule")` and the conditional-recurrence wrapper intact (its inner date input is migrated in Task 2).
  </action>
  <verify>
    <automated>cd web && grep -c "<select" "src/app/(dashboard)/agenda/novo/page.tsx" | grep -qx 0 && grep -c "const selectClassName" "src/app/(dashboard)/agenda/novo/page.tsx" | grep -qx 0 && grep -q "const textareaClassName" "src/app/(dashboard)/agenda/novo/page.tsx" && grep -q "permissions.isFetched && !canCreateAgenda" "src/app/(dashboard)/agenda/novo/page.tsx" && grep -q "NativeSelect" "src/app/(dashboard)/agenda/novo/page.tsx" && echo GATE_PASS</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "<select"` returns 0; all 4 form selects are `<NativeSelect ... className="w-full">`.
    - `const selectClassName` deleted; `const textareaClassName` retained.
    - Access gate reads `permissions.isFetched && !canCreateAgenda`; the submit-button `permissions.isLoading` guard is untouched.
  </acceptance_criteria>
  <done>The 4 RHF selects are NativeSelect (w-full), selectClassName removed, RBAC race fixed.</done>
</task>

<task type="auto">
  <name>Task 2: 3 date fields → DatePickerField via Controller</name>
  <files>web/src/app/(dashboard)/agenda/novo/page.tsx</files>
  <read_first>
    - 106-PATTERNS.md Shared Pattern 4 (call-sites table: dataInicio/dataFim withTime=true, recurrenceEndDate withTime=false in novo only) and 4(c) (Controller wiring idiom).
    - 106-UI-SPEC.md "AGD-36" date+time variant + date-only variant specifics.
    - agenda/novo/page.tsx: line 7 (`import { useForm } from "react-hook-form"`), lines 185-190 (dataInicio Input), 192-198 (dataFim Input), 231-239 (conditional recurrenceEndDate Input), and the onSubmit (lines 68-96) that calls `new Date(values.dataInicio).toISOString()` — must keep working with the emitted strings.
  </read_first>
  <action>
    Upgrade the import at line 7 to `import { Controller, useForm } from "react-hook-form";` and add `import { DatePickerField } from "@/components/shared/date-picker-field";`. Replace the `dataInicio` `<Input id="dataInicio" type="datetime-local" {...form.register("dataInicio")} />` with a `<Controller control={form.control} name="dataInicio" render={({ field }) => (<DatePickerField value={field.value} onChange={field.onChange} withTime />)} />`, keeping the surrounding `<Label>`/error `<p>` block and grid slot unchanged. Do the same for `dataFim` (withTime). Replace the `recurrenceEndDate` `<Input id="recurrenceEndDate" type="date" {...form.register("recurrenceEndDate")} />` (inside the existing `recurrenceRule && recurrenceRule !== "NONE"` conditional) with a `<Controller control={form.control} name="recurrenceEndDate" render={({ field }) => (<DatePickerField value={field.value} onChange={field.onChange} />)} />` (no withTime — date-only). Keep the conditional wrapper, the labels, and the error paragraphs. Do NOT change the Input import (still used by the titulo field) and do NOT alter onSubmit — the emitted YYYY-MM-DDTHH:mm / YYYY-MM-DD strings feed `new Date(...)` exactly as the old datetime-local/date values did.
  </action>
  <verify>
    <automated>cd web && grep -c "datetime-local" "src/app/(dashboard)/agenda/novo/page.tsx" | grep -qx 0 && grep -c 'type="date"' "src/app/(dashboard)/agenda/novo/page.tsx" | grep -qx 0 && grep -q "DatePickerField" "src/app/(dashboard)/agenda/novo/page.tsx" && grep -q "Controller" "src/app/(dashboard)/agenda/novo/page.tsx" && pnpm build && echo GATE_PASS</automated>
  </verify>
  <acceptance_criteria>
    - No `datetime-local` and no `type="date"` Input remain; all 3 date fields render `DatePickerField` via `Controller`.
    - `dataInicio`/`dataFim` use `withTime`; `recurrenceEndDate` is date-only and still inside the recurrence conditional.
    - `pnpm build` completes with no new type errors (Controller generics over EventoFormValues resolve).
  </acceptance_criteria>
  <done>All 3 create-form date fields use the shared Calendar-backed DatePickerField; `pnpm build` green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → Next proxy → Spring API | Unchanged. The create-Evento POST payload shape and route are identical; only the input widgets change. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-106-02-T | Tampering | Date strings emitted by DatePickerField into RHF | mitigate | eventoFormSchema still validates dataInicio/dataFim as Date-parseable with dataFim >= dataInicio and requires recurrenceEndDate under a recurrence rule; the DatePickerField deselect guard (106-01) prevents a stray click from nulling a required field before validation. |
| T-106-02-E | Elevation of Privilege | Client RBAC gate (`permissions.isFetched && !canCreateAgenda`) | accept | Deny branch preserved (AccessDeniedState); server-side create is gated by backend `@PreAuthorize("hasAuthority('agenda:create')")` — unchanged. |
| T-106-SC | Tampering | npm/pip/cargo installs | accept | Zero installs — DatePickerField built from Phase-101 primitives; date-fns already installed. |
</threat_model>

<verification>
- `cd web && pnpm build` passes.
- `cd web && pnpm lint` reports no new errors.
- Task grep gates pass (no native `<select>`, no datetime-local/date Input, DatePickerField + Controller present, RBAC isFetched).
</verification>

<success_criteria>
- AGD-36 (create form): dataInicio/dataFim (with time) and recurrenceEndDate (date-only) use the Calendar-backed DatePickerField.
- 4 RHF selects on NativeSelect (w-full), selectClassName removed, textareaClassName kept.
- RBAC race fixed at line 31; `pnpm build` green.
</success_criteria>

<output>
Create `.planning/phases/LEXCV-106-m-dulo-agenda/106-02-SUMMARY.md` when done.
</output>
