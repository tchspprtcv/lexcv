---
phase: 106-m-dulo-agenda
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/components/shared/date-picker-field.tsx
  - web/src/app/(dashboard)/agenda/page.tsx
  - web/src/app/(dashboard)/agenda/[id]/page.tsx
autonomous: true
requirements: [AGD-36, AGD-37]
must_haves:
  truths:
    - "A shared DatePickerField (Popover + Calendar [+ optional Input type=time]) exists at web/src/components/shared/date-picker-field.tsx, renders month/weekday names in Portuguese (locale={pt}), starts weeks on Sunday (weekStartsOn={0}), and closes on day selection"
    - "The 3 Agenda list filters (Processo/Categoria/Estado) on agenda/page.tsx are Radix Select (SelectTrigger/SelectContent/SelectItem), size=default, widths w-48/w-40/w-36 — no native <select> remains"
    - "The Processo filter's sentinel is 'todos' (not the empty string '') everywhere it is read/written, and Limpar Filtros resets all 3 filters to 'todos'"
    - "The monthly calendar grid + drag-and-drop on agenda/page.tsx (buildMonthGrid/dayKey/cursorMonth) is byte-for-byte unchanged"
    - "agenda/page.tsx (line 26) and agenda/[id]/page.tsx (line 62) access-denied gates use permissions.isFetched && !canViewAgenda (no !permissions.isLoading race)"
  artifacts:
    - path: "web/src/components/shared/date-picker-field.tsx"
      provides: "First Popover+Calendar date-picker composition in the project; consumed by both form pages"
      contains: "weekStartsOn"
    - path: "web/src/app/(dashboard)/agenda/page.tsx"
      provides: "Radix Select list filters (AGD-37) + RBAC isFetched fix"
      contains: "SelectTrigger"
    - path: "web/src/app/(dashboard)/agenda/[id]/page.tsx"
      provides: "RBAC isFetched fix only (read-only detail page)"
      contains: "permissions.isFetched"
  key_links:
    - from: "web/src/components/shared/date-picker-field.tsx"
      to: "@/components/ui/calendar + @/components/ui/popover"
      via: "Popover wraps Calendar; onSelect commits RHF value and closes"
      pattern: "Calendar"
    - from: "web/src/app/(dashboard)/agenda/page.tsx"
      to: "@/components/ui/select"
      via: "controlled Select value/onValueChange replacing native <select>"
      pattern: "SelectTrigger"
---

<objective>
Build the shared `DatePickerField` component (the first `Popover`+`Calendar` composition in the project, consumed by both Agenda form pages in Wave 2), migrate the 3 Agenda list filters to Radix `Select` (AGD-37), and apply the RBAC `isFetched` fix to the two non-form Agenda files (list page + read-only detail page).

Purpose: Establish the date-picker contract before the form plans depend on it (interface-first), and close AGD-37 plus the list/detail share of the bundled RBAC bug fix.
Output: 1 new shared component + 2 modified files; `pnpm build` green.
</objective>

<execution_context>
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/LEXCV-106-m-dulo-agenda/106-CONTEXT.md
@.planning/phases/LEXCV-106-m-dulo-agenda/106-PATTERNS.md
@.planning/phases/LEXCV-106-m-dulo-agenda/106-UI-SPEC.md
@web/src/app/(dashboard)/agenda/page.tsx
@web/src/app/(dashboard)/agenda/[id]/page.tsx
@web/src/components/ui/calendar.tsx
@web/src/components/ui/popover.tsx
@web/src/components/ui/select.tsx
@web/src/components/shared/notificacao-snooze-control.tsx

<interfaces>
<!-- Extracted contracts the executor needs; use directly, no codebase exploration. -->
- Calendar from "@/components/ui/calendar": wraps react-day-picker@9.14.0; accepts mode/selected/onSelect/locale/weekStartsOn straight through via {...props}; supplies its own p-3 padding; root is w-fit.
- Popover set from "@/components/ui/popover": Popover / PopoverTrigger / PopoverContent. PopoverContent defaults to align="end" — MUST be overridden to align="start" and given className="w-auto p-0" (it has no p-0 default; Calendar brings its own padding).
- Select set from "@/components/ui/select": Select (controlled value/onValueChange root — NOT register()-compatible) / SelectTrigger (size="sm"|"default", className for width) / SelectContent / SelectItem (value cannot be "") / SelectValue.
- date-fns/locale export `pt` (Portugal-Portuguese; Cape Verde has no dedicated locale) — first direct import in the repo, but date-fns@^4.4.0 is already installed (Phase 101).
- Full reference implementation of DatePickerField is in 106-PATTERNS.md Shared Pattern 4(d) — copy its structure (parseDateOnly helper, commit() with the `if (!nextDate) return` deselect guard, withTime prop, flex gap-2 + Input type=time for the datetime variant).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create shared DatePickerField (Popover + Calendar composition)</name>
  <files>web/src/components/shared/date-picker-field.tsx</files>
  <read_first>
    - 106-PATTERNS.md Shared Pattern 4 in full — especially 4(d) (the reference implementation) and the note that react-day-picker@9.14.0's onSelect for mode="single" is (Date | undefined) => void, so the deselect must be guarded.
    - 106-UI-SPEC.md "Component Inventory > AGD-36" (trigger anatomy table, locale requirement, date+time variant specifics, close behavior).
    - web/src/components/ui/calendar.tsx and web/src/components/ui/popover.tsx (primitive defaults, read in full).
    - web/src/components/shared/notificacao-snooze-control.tsx (the existing controlled Popover open/onOpenChange idiom to mirror).
  </read_first>
  <action>
    Create the client component (`"use client"`) `DatePickerField` exporting a named function with props `{ value: string | undefined; onChange: (v: string) => void; withTime?: boolean }`. Import `Button` (@/components/ui/button), `Calendar` (@/components/ui/calendar), `Input` (@/components/ui/input), `Popover`/`PopoverContent`/`PopoverTrigger` (@/components/ui/popover), `cn` (@/lib/utils), `CalendarIcon` (lucide-react), and `pt` from `date-fns/locale`. Use a `parseDateOnly` helper that returns `undefined` for empty/NaN dates. Hold `open` in `React.useState(false)`. The trigger is `<PopoverTrigger asChild><Button type="button" variant="outline" className="justify-start text-left font-normal ...">` with a leading `<CalendarIcon className="mr-2 h-4 w-4 opacity-50" />`; when a value exists show `date.toLocaleDateString("pt-CV", { day: "2-digit", month: "2-digit", year: "numeric" })`, otherwise the placeholder `Selecionar data` in `text-muted-foreground`. `PopoverContent` MUST use `className="w-auto p-0"` and `align="start"` (override the component's align="end" default). `Calendar` MUST use `mode="single"`, `selected={parsedDate}`, `locale={pt}`, `weekStartsOn={0}` (Sunday, to agree with the page's Dom-first monthly grid), and an `onSelect` that calls a `commit()` helper then `setOpen(false)`. `commit(nextDate, nextTime)` MUST early-return on falsy `nextDate` (deselect guard), format the date as `YYYY-MM-DD` via zero-padded getFullYear/getMonth+1/getDate, and call `onChange(withTime ? \`${datePart}T${nextTime}\` : datePart)`. When `withTime`, wrap trigger + a `<Input type="time" className="w-24 shrink-0" value={timePart} onChange={...}>` in a `<div className="flex gap-2">` (trigger gets `flex-1 min-w-0`), where `timePart = withTime && value ? value.slice(11, 16) : "00:00"` and changing the time re-commits with the current parsed date; when not `withTime`, the trigger is `w-full` and there is no time input. Verify the calendar autofocus prop name against react-day-picker@9.14.0 in web/node_modules (v9 renamed initialFocus → autoFocus per web/AGENTS.md caveat) — pass the correct one, and if uncertain, omit it rather than block. Do NOT add any Radix Select, NativeSelect, or accent color to this component — it uses only the primitives' shipped rounded-md/tokens.
  </action>
  <verify>
    <automated>cd web && test -f "src/components/shared/date-picker-field.tsx" && grep -q "weekStartsOn={0}" "src/components/shared/date-picker-field.tsx" && grep -q 'locale={pt}' "src/components/shared/date-picker-field.tsx" && grep -q 'from "date-fns/locale"' "src/components/shared/date-picker-field.tsx" && grep -q 'align="start"' "src/components/shared/date-picker-field.tsx" && grep -q "if (!nextDate) return" "src/components/shared/date-picker-field.tsx" && grep -q "export function DatePickerField" "src/components/shared/date-picker-field.tsx" && pnpm lint && echo GATE_PASS</automated>
  </verify>
  <acceptance_criteria>
    - File exists and exports `DatePickerField` with a `withTime?: boolean` prop.
    - Uses `locale={pt}` (imported from `date-fns/locale`) and `weekStartsOn={0}`.
    - `PopoverContent` uses `align="start"` and `w-auto p-0`.
    - The `commit` helper guards `if (!nextDate) return` so a deselect never wipes a required field.
    - `pnpm lint` reports no new errors.
  </acceptance_criteria>
  <done>A reusable Popover+Calendar DatePickerField exists, Portuguese-localized, Sunday-first, with the datetime variant exposing an adjacent Input type=time.</done>
</task>

<task type="auto">
  <name>Task 2: agenda/page.tsx — 3 filters to Radix Select + 'todos' sentinel + RBAC fix</name>
  <files>web/src/app/(dashboard)/agenda/page.tsx</files>
  <read_first>
    - 106-PATTERNS.md Shared Pattern 3 (Select migration, before/after, the required "todos" sentinel logic change) and Shared Pattern 1 (RBAC isFetched fix table, agenda/page.tsx line 26).
    - 106-UI-SPEC.md "Component Inventory > AGD-37" (filter widths w-48/w-40/w-36, size=default, sentinel rule, verbatim option copy) and the "Do not touch" notes on the filter-bar container + label chips.
    - agenda/page.tsx: line 26 (RBAC), lines 43-45 (filter state), line 122 (Processo filter predicate), lines 242-298 (filter bar + Limpar Filtros), and lines 550-567 (buildMonthGrid — must stay untouched).
  </read_first>
  <action>
    Add `import { Select, SelectContent, SelectItem, SelectTrigger, SelectValue } from "@/components/ui/select";`. Change the access gate at line 26 from `if (!permissions.isLoading && !canViewAgenda) {` to `if (permissions.isFetched && !canViewAgenda) {`. Change `selectedProcessoId`'s initial state (line 43) from `React.useState<string>("")` to `React.useState<string>("todos")`. Change the Processo filter predicate (line 122) from `if (selectedProcessoId && e.processoId !== selectedProcessoId)` to `if (selectedProcessoId !== "todos" && e.processoId !== selectedProcessoId)`. In the Limpar Filtros onClick (line 292), change `setSelectedProcessoId("")` to `setSelectedProcessoId("todos")`. Replace all 3 native `<select value={...} onChange={(e)=>setX(e.target.value)} className="h-9 w-.. rounded-none ...">` with `<Select value={...} onValueChange={setX}><SelectTrigger size="default" className="w-48|w-40|w-36"><SelectValue /></SelectTrigger><SelectContent>...</SelectContent></Select>`. Processo: first item `<SelectItem value="todos">Todos os Processos</SelectItem>` then the mapped `(processos.data ?? []).map(...)` items (keep `p.numero || p.titulo || "Sem número"`), trigger `w-48`. Categoria: keep existing `value="todos"` "Todas" + `PRAZO` "Prazos Criticos" + `AUDIENCIA` "Audiências" + `DILIGENCIA` "Diligências" + `REUNIAO` "Reuniões" as SelectItems, trigger `w-40`. Estado: keep `value="todos"` "Todos" + `pendentes` "Pendentes" + `concluidos` "Concluídos", trigger `w-36`. Reuse each `<option>`'s exact copy verbatim as the SelectItem child. Do NOT touch the filter-bar container div (line 242, `bg-white dark:bg-[#020617] ... rounded-none`), the `text-[10px] font-bold ...` label chips above each filter, the Limpar Filtros Button styling, or anything in the monthly grid / drag-and-drop / sidebar cards.
  </action>
  <verify>
    <automated>cd web && grep -c "<select" "src/app/(dashboard)/agenda/page.tsx" | grep -qx 0 && grep -q "SelectTrigger" "src/app/(dashboard)/agenda/page.tsx" && grep -q "permissions.isFetched && !canViewAgenda" "src/app/(dashboard)/agenda/page.tsx" && grep -c 'setSelectedProcessoId("")' "src/app/(dashboard)/agenda/page.tsx" | grep -qx 0 && grep -q "buildMonthGrid" "src/app/(dashboard)/agenda/page.tsx" && echo GATE_PASS</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "<select"` returns 0; all 3 filters are Radix `Select`/`SelectTrigger`.
    - Processo sentinel is `"todos"`: no `setSelectedProcessoId("")` remains and the predicate compares against `"todos"`.
    - Access gate reads `permissions.isFetched && !canViewAgenda`.
    - `buildMonthGrid` (and the surrounding grid/drag-drop) still present and unchanged.
  </acceptance_criteria>
  <done>Agenda list filters are Radix Select with a non-empty sentinel and the RBAC race fixed; the monthly grid is untouched.</done>
</task>

<task type="auto">
  <name>Task 3: agenda/[id]/page.tsx — RBAC isFetched fix only</name>
  <files>web/src/app/(dashboard)/agenda/[id]/page.tsx</files>
  <read_first>
    - 106-PATTERNS.md Shared Pattern 1 (RBAC fix table, agenda/[id]/page.tsx line 62; and the explicit warning not to touch other permissions.isLoading usages).
    - agenda/[id]/page.tsx lines 43-72 (the access gate; this is a read-only detail page with no <select> and no date input).
  </read_first>
  <action>
    Change ONLY the access gate at line 62 from `if (!permissions.isLoading && !canViewAgenda) {` to `if (permissions.isFetched && !canViewAgenda) {`. Make no other change — this file has no `<select>`, no date input, no Calendar/Select scope this phase (read-only detail view). Do not touch any other `permissions.isLoading` reference if one exists elsewhere in the file.
  </action>
  <verify>
    <automated>cd web && grep -q "permissions.isFetched && !canViewAgenda" "src/app/(dashboard)/agenda/[id]/page.tsx" && grep -c "!permissions.isLoading && !canViewAgenda" "src/app/(dashboard)/agenda/[id]/page.tsx" | grep -qx 0 && pnpm build && echo GATE_PASS</automated>
  </verify>
  <acceptance_criteria>
    - Access gate reads `permissions.isFetched && !canViewAgenda`; the old `!permissions.isLoading && !canViewAgenda` no longer appears.
    - No other change to the file.
    - `pnpm build` completes with no new type errors across all files in this plan (including the new DatePickerField).
  </acceptance_criteria>
  <done>The Agenda detail page no longer flashes access-denied during permission load; `pnpm build` green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → Next proxy → Spring API | Unchanged. This plan adds no new fetch, endpoint, or navigation target. Client-side RBAC gates are UX affordances only; the authoritative gate remains backend `@PreAuthorize` per CLAUDE.md. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-106-01-E | Elevation of Privilege | Client RBAC gate (`permissions.isFetched && !canViewAgenda`) on agenda/page.tsx + agenda/[id]/page.tsx | accept | The change strengthens correctness (removes a false-negative flash) without removing the deny branch; unauthorized users still get AccessDeniedState, and the backend `@PreAuthorize` on the eventos/processos endpoints is the real enforcement — unchanged this phase. |
| T-106-01-T | Tampering | Radix Select filter values + "todos" sentinel | accept | Filters run purely client-side over already-fetched, tenant-scoped data; the sentinel is a display constant, no new input reaches the server. |
| T-106-SC | Tampering | npm/pip/cargo installs | accept | Zero installs this phase — Calendar/Popover/Select added in Phase 101; date-fns already installed (Phase 101). No Package Legitimacy Gate required. |
</threat_model>

<verification>
- `cd web && pnpm build` passes.
- `cd web && pnpm lint` reports no new errors on the 3 files.
- Task grep gates pass (no native `<select>` on agenda/page.tsx, DatePickerField exports/localized, RBAC isFetched on both files, monthly grid intact).
</verification>

<success_criteria>
- AGD-37: the 3 Agenda list filters are Radix `Select`.
- AGD-36 (foundation): a shared `DatePickerField` exists for Wave 2 to consume.
- Bundled RBAC fix landed on agenda/page.tsx (line 26) and agenda/[id]/page.tsx (line 62).
- Monthly calendar grid + drag-and-drop unchanged; `pnpm build` green.
</success_criteria>

<output>
Create `.planning/phases/LEXCV-106-m-dulo-agenda/106-01-SUMMARY.md` when done.
</output>
