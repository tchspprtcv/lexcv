---
phase: 106-m-dulo-agenda
plan: 04
type: execute
wave: 3
depends_on: [106-01, 106-02, 106-03]
files_modified: []
autonomous: false
requirements: [AGD-36, AGD-37]
must_haves:
  truths:
    - "cd web && pnpm build and pnpm lint both pass across all 4 modified Agenda files + the new DatePickerField"
    - "Regression greps confirm: zero native <select> across the 3 migrated files, zero datetime-local/type=date Inputs across the 2 forms, permissions.isFetched in all 4 Agenda files, and the monthly grid (buildMonthGrid) untouched"
    - "A human visually confirms (light + dark) the 3 Radix Select filters, the Portuguese Sunday-first Calendar date pickers with date+time sync and close-on-select, and the RBAC gate across the 4 roles"
  artifacts:
    - path: ".planning/phases/LEXCV-106-m-dulo-agenda/106-04-SUMMARY.md"
      provides: "Phase-close verification record"
      contains: "AGD-36"
  key_links:
    - from: "human visual checkpoint"
      to: "AGD-36 + AGD-37 success criteria"
      via: "manual walkthrough of both forms + the list filters in a real browser"
      pattern: "checkpoint"
---

<objective>
Close Phase 106 with a holistic automated gate (build + lint + regression greps over the whole phase surface) followed by a mandatory human visual checkpoint — because this phase ships the first `Popover`+`Calendar` composition in the project (no prior precedent to inherit visual correctness from) and touches an RBAC-gated module across 4 roles.

Purpose: Prove AGD-36 + AGD-37 are functionally and visually correct before marking the phase complete.
Output: Green build/lint + regression gate, then human sign-off. Runs alone in Wave 3 after 106-01/02/03.
</objective>

<execution_context>
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/LEXCV-106-m-dulo-agenda/106-CONTEXT.md
@.planning/phases/LEXCV-106-m-dulo-agenda/106-UI-SPEC.md
@web/src/app/(dashboard)/agenda/page.tsx
@web/src/app/(dashboard)/agenda/novo/page.tsx
@web/src/app/(dashboard)/agenda/[id]/editar/page.tsx
@web/src/app/(dashboard)/agenda/[id]/page.tsx
@web/src/components/shared/date-picker-field.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Holistic build + lint + regression gate</name>
  <files>web/src/app/(dashboard)/agenda/page.tsx, web/src/app/(dashboard)/agenda/novo/page.tsx, web/src/app/(dashboard)/agenda/[id]/editar/page.tsx, web/src/app/(dashboard)/agenda/[id]/page.tsx, web/src/components/shared/date-picker-field.tsx</files>
  <read_first>
    - 106-UI-SPEC.md "Checker Sign-Off" (the 6 dimensions) and "Scope note" (what must NOT have changed: monthly grid, filter-bar container, label chips, no recurrence UI in editar).
    - All 5 files listed above, to confirm the Wave 1/2 outputs are consistent before the human checkpoint.
  </read_first>
  <action>
    Run the full authoritative gate from `web/`: `pnpm build` then `pnpm lint` — both must be clean of new errors. Then run the regression greps across the phase surface and confirm each: (a) `grep -c "<select"` returns 0 in each of agenda/page.tsx, agenda/novo/page.tsx, agenda/[id]/editar/page.tsx; (b) `grep -c "datetime-local"` returns 0 in both form files and `grep -c 'type="date"'` returns 0 in agenda/novo/page.tsx; (c) `grep -q "permissions.isFetched"` in all 4 Agenda page files AND `grep -c "!permissions.isLoading && !canView\|!permissions.isLoading && !canCreate\|!permissions.isLoading && !canEdit"` returns 0 across the 4; (d) `grep -q "buildMonthGrid"` still present in agenda/page.tsx (grid intact); (e) `grep -c "recurrenceEndDate" agenda/[id]/editar/page.tsx` returns 0 (no recurrence UI leaked into editar); (f) `grep -c "const selectClassName"` returns 0 in both form files while `grep -q "const textareaClassName"` holds in both. If any check fails, fix the offending file (it is a Wave 1/2 regression) and re-run before proceeding to the checkpoint. Do not modify the monthly grid, the filter-bar container, or the label chips.
  </action>
  <verify>
    <automated>cd web && pnpm build && pnpm lint && for f in "src/app/(dashboard)/agenda/page.tsx" "src/app/(dashboard)/agenda/novo/page.tsx" "src/app/(dashboard)/agenda/[id]/editar/page.tsx"; do grep -c "<select" "$f" | grep -qx 0 || exit 1; done && grep -q "permissions.isFetched" "src/app/(dashboard)/agenda/[id]/page.tsx" && grep -q "buildMonthGrid" "src/app/(dashboard)/agenda/page.tsx" && grep -c "recurrenceEndDate" "src/app/(dashboard)/agenda/[id]/editar/page.tsx" | grep -qx 0 && echo GATE_PASS</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm build` and `pnpm lint` both pass with no new errors.
    - No native `<select>` in the 3 migrated files; no `datetime-local`/`type="date"` Input in the forms.
    - `permissions.isFetched` present in all 4 files; no `!permissions.isLoading && !canX` access gate remains.
    - `buildMonthGrid` still present (grid untouched); no `recurrenceEndDate` in editar; `selectClassName` gone / `textareaClassName` kept in both forms.
  </acceptance_criteria>
  <done>The whole-phase automated gate is green and every regression grep passes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human visual verification (date pickers, Select filters, RBAC matrix)</name>
  <read_first>
    - 106-UI-SPEC.md "Component Inventory > AGD-36" (trigger anatomy, locale, weekStartsOn, close-on-select, date+time layout) and "> AGD-37" (filter widths, sentinel, verbatim copy).
    - 106-CONTEXT.md "Correção de bug de RBAC (bundled)" (the isFetched fix intent — no access-denied flash).
  </read_first>
  <action>
    After Task 1's gate is green, start the dev server (`cd web && pnpm dev`) with the backend reachable so RBAC/permission data loads, present the checklist in `<how-to-verify>`, and PAUSE for the human verdict. This is a blocking human-verify checkpoint — never auto-approve (`workflow.auto_advance` is ignored, and `parallelization.skip_checkpoints` does not apply since this plan runs solo in Wave 3). Record the human's concrete evidence (themes exercised, roles walked, the date+time sync + close-on-select behavior observed) and the approved/issues verdict in 106-04-SUMMARY.md; if issues are reported, enumerate them for a `/gsd:plan-phase 106 --gaps` closure plan rather than silently closing the phase.
  </action>
  <what-built>
    Agenda list filters migrated to Radix `Select` (Processo/Categoria/Estado); the create form (`/agenda/novo`) and edit form (`/agenda/{id}/editar`) date fields migrated to the shared `DatePickerField` (Popover + Portuguese, Sunday-first `Calendar`, with an adjacent `Input type=time` for `dataInicio`/`dataFim`); all 4 Agenda pages' access-denied gate switched to `permissions.isFetched`.
  </what-built>
  <how-to-verify>
    Start the app (`cd web && pnpm dev`) and, logged in as ADMIN, in BOTH light and dark themes:
    1. `/agenda`: open each of the 3 filter dropdowns (Processo/Categoria/Estado) — confirm they are the styled Radix Select popover (not a native OS dropdown), the widths look like today (w-48/w-40/w-36), selecting a Processo filters the calendar/list, and "Limpar Filtros" resets all 3 to the "Todos…/Todas" option. Confirm the monthly grid + drag-and-drop still work unchanged.
    2. `/agenda/novo`: click the Início date trigger — the Calendar opens under the left edge, month/weekday names are Portuguese, weeks start on Sunday (Dom first, matching the grid), picking a day closes the popover and fills the trigger label (dd/mm/yyyy); the adjacent time input edits the hour independently; repeat for Fim; set Recorrência to a non-Nenhuma value and confirm the "Fim da recorrência" date-only picker appears (no time input). Submit and confirm the event saves with the chosen date+time.
    3. `/agenda/{id}/editar` on an existing event: confirm Início/Fim pre-populate the trigger label + time from the loaded event, edits save correctly, and there is NO recurrence field here.
    4. RBAC: with a role lacking agenda view/create/edit (e.g. a restricted test user among ADMIN/ADVOGADO/TECNICO/ASSISTENTE), load `/agenda`, `/agenda/novo`, `/agenda/{id}`, `/agenda/{id}/editar` and confirm the AccessDeniedState renders with NO brief flash of denied-then-content (or content-then-denied) during permission load.
  </how-to-verify>
  <acceptance_criteria>
    - Radix Select filters render and function in both themes; Limpar Filtros resets to the sentinel.
    - Date pickers are Portuguese, Sunday-first, close on selection, and the datetime variant syncs date+time; recurrence date picker appears only in novo and is date-only.
    - Edit form pre-populates and has no recurrence UI.
    - No access-denied flash for unauthorized roles on any of the 4 Agenda routes.
  </acceptance_criteria>
  <resume-signal>Type "approved" to close Phase 106, or describe the visual/functional issues to fix.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → Next proxy → Spring API | Unchanged — verification-only plan; introduces no code paths, inputs, or endpoints. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-106-04-E | Elevation of Privilege | RBAC gate behavior across 4 roles | mitigate | The human checkpoint explicitly walks the unauthorized-role path on all 4 Agenda routes, confirming the isFetched change still denies access (no flash-then-allow) — the one manual check that cannot be reduced to a grep. |
| T-106-SC | Tampering | npm/pip/cargo installs | accept | Zero installs this phase. |
</threat_model>

<verification>
- `cd web && pnpm build` and `pnpm lint` pass.
- All regression greps pass.
- Human checkpoint returns "approved".
</verification>

<success_criteria>
- AGD-36 + AGD-37 confirmed functional and visually correct in light + dark and across the RBAC matrix.
- No regression to the monthly grid, filter-bar chrome, or editar recurrence scope.
</success_criteria>

<output>
Create `.planning/phases/LEXCV-106-m-dulo-agenda/106-04-SUMMARY.md` when done.
</output>
