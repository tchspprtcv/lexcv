---
phase: 49-drag-drop-no-calendario
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/app/(dashboard)/agenda/page.tsx
autonomous: false
requirements: [AGE-07, AGE-08]

must_haves:
  truths:
    - "User can drag a non-prazo, non-recurring event pill to another cell in the same month"
    - "Hovered target cell shows a blue highlight ring while a drag is in progress"
    - "On drop, the event moves immediately (optimistic) and a PUT /eventos/{id} fires with new dataInicio/dataFim"
    - "If the API call fails, the event reverts to its original date and apiFetch shows an error toast"
    - "Prazo events and recurrence instances are not draggable"
    - "Dropping an event on its own current cell does not trigger an API call"
  artifacts:
    - path: "web/src/app/(dashboard)/agenda/page.tsx"
      provides: "HTML5 drag & drop on the month calendar with optimistic update + PUT mutation"
      contains: "moveDatePreservingTime"
  key_links:
    - from: "agenda/page.tsx onDrop handler"
      to: "PUT /eventos/{id}"
      via: "dragDropMutation.mutate via apiFetch"
      pattern: "apiFetch.*eventos/\\$\\{.*method:.*PUT"
    - from: "agenda/page.tsx allUnifiedEvents useMemo"
      to: "optimisticOverrides Map"
      via: "override lookup applied to dataInicio/dataFim before render"
      pattern: "optimisticOverrides\\.(get|has)"
---

<objective>
Add HTML5 drag & drop to the monthly calendar in `agenda/page.tsx`: the user can drag a movable event pill (not a prazo, not a recurrence instance) to another day cell of the current month. The event moves optimistically, persists via `PUT /eventos/{id}`, and reverts on API error.

Purpose: Satisfies AGE-07 (drag to move date) and AGE-08 (PUT update + immediate refresh).
Output: Modified `web/src/app/(dashboard)/agenda/page.tsx` (single file; no new files, no backend change).
</objective>

<execution_context>
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/LEXCV-49-drag-drop-no-calend-rio/49-CONTEXT.md

@web/src/app/(dashboard)/agenda/page.tsx
@web/src/hooks/use-eventos.ts

<interfaces>
From web/src/types/eventos.ts:
```typescript
export interface Evento {
  id: number;
  dataInicio: string;   // ISO "2026-07-10T14:00:00"
  dataFim: string;
  isRecurrenceInstance?: boolean;
  // ...
}
export interface EventoUpdateRequest { dataInicio?: string; dataFim?: string; /* ... */ }
```

From web/src/lib/api.ts:
```typescript
export function apiFetch<T>(path: string, init?: RequestInit): Promise<T>; // sends credentials, toasts non-401/403 errors
```

Existing helpers ALREADY in agenda/page.tsx (reuse, do not redefine):
- `dayKey(d: Date): string` → "YYYY-MM-DD" using LOCAL date parts (getFullYear/getMonth/getDate)
- `getCategoria(e)`, `buildMonthGrid(monthStart)`
- `allUnifiedEvents` useMemo (lines ~59-74): maps `eventos.data` (isPrazo:false) + `prazos.data` (isPrazo:true)
- `eventosByDay` useMemo (lines ~97-106): groups `filteredEvents` by `dayKey(new Date(e.dataInicio))`
- Calendar cell render (lines ~289-326): `days.map((day) => ...)` with `key = dayKey(day.date)`, pill `<Link>` at lines ~311-320
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add drag state, optimistic overrides, and the PUT mutation</name>
  <files>web/src/app/(dashboard)/agenda/page.tsx</files>
  <read_first>
    - web/src/app/(dashboard)/agenda/page.tsx lines 1-16 (imports) and 34-106 (component state + allUnifiedEvents + eventosByDay)
    - web/src/hooks/use-eventos.ts lines 74-90 (useUpdateEvento pattern) — for invalidation key shape ["eventos","list"]
  </read_first>
  <action>
    In `web/src/app/(dashboard)/agenda/page.tsx`:

    1. Update the React import to also bring in TanStack Query + apiFetch. Add at top with other imports: `import { useMutation, useQueryClient } from "@tanstack/react-query";` and `import { apiFetch } from "@/lib/api";`. (`cn` is already imported.)

    2. Add a module-scope pure helper near the other helpers (e.g. above `dayKey`):
       `function moveDatePreservingTime(originalISO: string, newDateKey: string): string { return newDateKey + originalISO.slice(10); }`
       This replaces the date portion (first 10 chars) and preserves everything from the `T` onward.

    3. Inside `AgendaPageContent`, after the existing `React.useState` filter declarations (around line 41), add three state hooks:
       - `const [dragState, setDragState] = React.useState<{ eventId: number; originalDataInicio: string; originalDataFim: string } | null>(null);`
       - `const [dragOverKey, setDragOverKey] = React.useState<string | null>(null);`
       - `const [optimisticOverrides, setOptimisticOverrides] = React.useState<Map<number, string>>(new Map());`

    4. Add the queryClient and mutation inside the component (after the state above):
       - `const queryClient = useQueryClient();`
       - `const dragDropMutation = useMutation({ mutationFn: ({ id, dataInicio, dataFim }: { id: number; dataInicio: string; dataFim: string }) => apiFetch(\`/eventos/${id}\`, { method: "PUT", body: JSON.stringify({ dataInicio, dataFim }) }), onSuccess: () => { setOptimisticOverrides(new Map()); queryClient.invalidateQueries({ queryKey: ["eventos", "list"] }); }, onError: () => { setOptimisticOverrides(new Map()); } });`
       Note: do NOT add an onError toast — `apiFetch` already toasts non-401/403 errors. Clearing the override on error reverts the optimistic move (AGE success criterion 3).

    5. Apply optimistic overrides in the `allUnifiedEvents` useMemo (lines ~59-74). For the `evs` mapping (real eventos only — prazos are never draggable), after `{ ...e, isPrazo: false }`, check the override: if `optimisticOverrides.has(e.id)`, compute the moved dates preserving duration:
       - `const newInicio = optimisticOverrides.get(e.id)!;`
       - duration offset: `const durMs = new Date(e.dataFim).getTime() - new Date(e.dataInicio).getTime();`
       - `const newFim = new Date(new Date(newInicio).getTime() + durMs).toISOString().slice(0, 19);`
       - return `{ ...e, isPrazo: false, dataInicio: newInicio, dataFim: newFim }`.
       Add `optimisticOverrides` to the useMemo dependency array.

    Do not touch prazo mapping, filters, or the JSX yet — that is Task 2.
  </action>
  <verify>
    <automated>cd web && pnpm exec tsc --noEmit -p tsconfig.json 2>&1 | grep -i "agenda/page" || echo "no agenda type errors"</automated>
  </verify>
  <acceptance_criteria>
    - `moveDatePreservingTime` exists at module scope and returns `newDateKey + originalISO.slice(10)`
    - `dragState`, `dragOverKey`, `optimisticOverrides` state hooks declared
    - `dragDropMutation` calls `apiFetch` with `method: "PUT"` to `/eventos/${id}`; onSuccess clears overrides + invalidates `["eventos","list"]`; onError clears overrides
    - `allUnifiedEvents` applies overrides to dataInicio AND adjusts dataFim by preserving the original duration
    - `pnpm exec tsc --noEmit` reports no new errors in agenda/page.tsx
  </acceptance_criteria>
  <done>State, helper, and mutation wired; overrides applied in allUnifiedEvents; type-checks clean.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Make pills draggable and cells drop zones</name>
  <files>web/src/app/(dashboard)/agenda/page.tsx</files>
  <read_first>
    - web/src/app/(dashboard)/agenda/page.tsx lines 288-327 (calendar cell + pill render) — modify in place
  </read_first>
  <action>
    In the `days.map((day) => ...)` block (lines ~289-326) of `web/src/app/(dashboard)/agenda/page.tsx`:

    1. Cell div (the outer `<div key={key}>` at ~293): add drop-zone handlers and highlight class.
       - Add to the `cn(...)` className, after the existing base/hover line and BEFORE the `day.isOutsideMonth` opacity line:
         `!day.isOutsideMonth && dragOverKey === key && "ring-2 ring-inset ring-blue-400 bg-blue-50 dark:bg-blue-900/20",`
       - Add handlers on the cell div:
         - `onDragOver={(ev) => { if (day.isOutsideMonth || !dragState) return; ev.preventDefault(); setDragOverKey(key); }}`
         - `onDragLeave={() => setDragOverKey((k) => (k === key ? null : k))}`
         - `onDrop={(ev) => { ev.preventDefault(); if (day.isOutsideMonth || !dragState) return; const originKey = dayKey(new Date(dragState.originalDataInicio)); if (key === originKey) { setDragState(null); setDragOverKey(null); return; } const newInicio = moveDatePreservingTime(dragState.originalDataInicio, key); const durMs = new Date(dragState.originalDataFim).getTime() - new Date(dragState.originalDataInicio).getTime(); const newFim = new Date(new Date(newInicio).getTime() + durMs).toISOString().slice(0, 19); setOptimisticOverrides((m) => new Map(m).set(dragState.eventId, newInicio)); const id = dragState.eventId; setDragState(null); setDragOverKey(null); dragDropMutation.mutate({ id, dataInicio: newInicio, dataFim: newFim }); }}`
       Use `key` (the cell's `dayKey(day.date)`) as the new date key — consistent with how `eventosByDay` groups by `dayKey(new Date(e.dataInicio))`.

    2. Event pill `<Link>` (lines ~311-320): make movable events draggable.
       - Compute per-event before the return: `const canDrag = !e.isPrazo && !(e as Evento).isRecurrenceInstance;`
       - On the `<Link>`: add `draggable={canDrag}`.
       - Add `onDragStart={(ev) => { if (!canDrag) { ev.preventDefault(); return; } ev.dataTransfer.setData("text/plain", String(e.id)); ev.dataTransfer.effectAllowed = "move"; setDragState({ eventId: e.id as number, originalDataInicio: e.dataInicio, originalDataFim: e.dataFim }); }}`
       - Add `onDragEnd={() => { setDragState(null); setDragOverKey(null); }}`
       - Add a conditional cursor class to the pill `cn(...)`: `canDrag && "cursor-grab active:cursor-grabbing"`.

    Note: cell `key` and event `e.id` are already in scope inside the map. `Evento` is already imported. Do not introduce dataTransfer-based id reading in onDrop — `dragState.eventId` is the source of truth (avoids cross-cell stale reads).
  </action>
  <verify>
    <automated>cd web && pnpm exec tsc --noEmit -p tsconfig.json 2>&1 | grep -i "agenda/page" || echo "no agenda type errors"; cd web && pnpm lint 2>&1 | grep -i "agenda/page" || echo "lint clean for agenda"</automated>
  </verify>
  <acceptance_criteria>
    - Movable pills (`!isPrazo && !isRecurrenceInstance`) have `draggable={true}`, onDragStart, onDragEnd, and `cursor-grab` class
    - Prazo pills and recurrence-instance pills are NOT draggable (canDrag false → draggable false, onDragStart preventDefaults)
    - Each in-month cell has onDragOver (preventDefault + setDragOverKey), onDragLeave, onDrop
    - Highlight class `ring-2 ring-inset ring-blue-400 bg-blue-50 dark:bg-blue-900/20` applies only when `!day.isOutsideMonth && dragOverKey === key`
    - onDrop guards same-cell drop (compares `key` to `dayKey(new Date(dragState.originalDataInicio))`) and skips mutation
    - onDrop sets optimistic override then calls `dragDropMutation.mutate`
    - `pnpm exec tsc --noEmit` and `pnpm lint` clean for agenda/page.tsx
  </acceptance_criteria>
  <done>Pills draggable per rules, cells accept drops with highlight, same-cell drop short-circuits, mutation fires.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify drag & drop end-to-end in the browser</name>
  <what-built>HTML5 drag & drop on the agenda month calendar: movable event pills, highlighted drop cells, optimistic move, PUT persistence, and revert-on-error.</what-built>
  <how-to-verify>
    1. Run backend (`mvn spring-boot:run` in `backend/`) and frontend (`pnpm dev` in `web/`). Log in and open http://localhost:3000/agenda.
    2. Ensure at least one normal (non-prazo, non-recurring) event exists in the current month; create one via "Novo Evento" if needed.
    3. Drag a normal event pill over another day cell of the SAME month — the destination cell should show a blue ring + light blue background while hovering.
    4. Drop it — the event should move to the new day immediately (no full reload), and a `PUT /eventos/{id}` should appear in the Network tab with the new `dataInicio`/`dataFim`. Confirm the time-of-day is preserved.
    5. Refresh the page — the event stays on the new day (persisted).
    6. Confirm a `[Prazo]` pill and a recurring-event pill (↻ icon) cannot be dragged (no grab cursor, drag does nothing).
    7. Drop an event back onto its own current cell — no network request should fire.
    8. (Error path) Stop the backend, then drag+drop an event: it should snap back to the original day and an error toast should appear.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues observed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → /api/v1/eventos/{id} | User-controlled dataInicio/dataFim cross into the backend on PUT |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-49-01 | Tampering | PUT /eventos/{id} payload (dataInicio/dataFim) | accept | Backend `PUT /eventos/{id}` is pre-existing, gated by `@PreAuthorize` + tenant scoping (getTenantId). This phase is frontend-only; no new endpoint or auth surface introduced. |
| T-49-02 | Elevation of Privilege | Moving another tenant's event | accept | Tenant isolation enforced server-side on the existing endpoint; client cannot reference events outside its tenant-scoped list. |
| T-49-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages installed — uses existing `@tanstack/react-query` and native HTML5 D&D API. No legitimacy gate needed. |
</threat_model>

<verification>
- `cd web && pnpm exec tsc --noEmit` clean
- `cd web && pnpm lint` clean for agenda/page.tsx
- Manual browser verification (Task 3) confirms drag highlight, optimistic move, PUT persistence, non-draggable prazos/recurring, same-cell no-op, and revert-on-error.
</verification>

<success_criteria>
- AGE-07: A movable event can be dragged to another day of the same month; destination cell highlights (ring-2 ring-blue-400).
- AGE-08: On drop, the calendar updates immediately (optimistic) and persists via `PUT /eventos/{id}` with new dataInicio/dataFim; success invalidates `["eventos","list"]`.
- Prazos and recurrence instances are not draggable; same-cell drop is a no-op; API failure reverts the move with an error toast.
- Only `web/src/app/(dashboard)/agenda/page.tsx` is modified.
</success_criteria>

<output>
Create `.planning/phases/LEXCV-49-drag-drop-no-calend-rio/49-01-SUMMARY.md` when done.
</output>
