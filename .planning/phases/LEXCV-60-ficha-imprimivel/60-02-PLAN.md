---
phase: 60-ficha-imprimivel
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/app/(dashboard)/clientes/[id]/page.tsx
  - web/src/app/(dashboard)/clientes/page.tsx
autonomous: true
requirements:
  - FICH-01
  - FICH-02

must_haves:
  truths:
    - "Na página de detalhe do cliente, existe botão 'Imprimir Ficha' no cabeçalho que abre /clientes/{id}/ficha em nova aba"
    - "Na listagem de clientes, cada linha tem ícone Printer que abre /clientes/{id}/ficha em nova aba"
    - "Os dois pontos de entrada funcionam independentemente sem recarregar a página actual"
  artifacts:
    - path: "web/src/app/(dashboard)/clientes/[id]/page.tsx"
      provides: "Botão 'Imprimir Ficha' no bloco de acções do cabeçalho, junto a 'Voltar' e 'Editar'"
    - path: "web/src/app/(dashboard)/clientes/page.tsx"
      provides: "Botão ícone Printer em ClienteRow, junto a Eye/Pencil/Trash2 (sempre visível com clientes:view)"
  key_links:
    - from: "web/src/app/(dashboard)/clientes/[id]/page.tsx"
      to: "web/src/app/(dashboard)/clientes/[id]/ficha/page.tsx"
      via: "Link href /clientes/{id}/ficha target=_blank"
      pattern: "ficha"
    - from: "web/src/app/(dashboard)/clientes/page.tsx"
      to: "web/src/app/(dashboard)/clientes/[id]/ficha/page.tsx"
      via: "Link href /clientes/{id}/ficha target=_blank em ClienteRow"
      pattern: "ficha"
---

<objective>
Adicionar os dois pontos de acesso à ficha imprimível: botão "Imprimir Ficha" na página de detalhe do cliente e ícone Printer directo na listagem de clientes (ClienteRow).

Purpose: Os utilizadores acedem à ficha a partir dos contextos onde trabalham com clientes — detalhe e listagem.
Output: Modificações em /clientes/[id]/page.tsx e /clientes/page.tsx.
</objective>

<execution_context>
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/LEXCV-60-ficha-imprimivel/60-CONTEXT.md
@.planning/phases/LEXCV-60-ficha-imprimivel/60-RESEARCH.md

<interfaces>
<!-- Padrões actuais extraídos dos ficheiros a modificar. -->

From web/src/app/(dashboard)/clientes/[id]/page.tsx (bloco de botões no cabeçalho, linhas 74-83):
```tsx
<div className="flex gap-2">
  <Button asChild variant="outline">
    <Link href="/clientes">Voltar</Link>
  </Button>
  {canEditClientes ? (
    <Button asChild>
      <Link href={`/clientes/${encodeURIComponent(id)}/editar`}>Editar</Link>
    </Button>
  ) : null}
</div>
```
Adicionar botão "Imprimir Ficha" ANTES do bloco condicional de Editar, após "Voltar". Visível para qualquer utilizador com clientes:view.

From web/src/app/(dashboard)/clientes/page.tsx (ClienteRow, bloco de acções, linhas 538-563):
```tsx
<div className="inline-flex items-center gap-1">
  <Button asChild size="sm" variant="ghost" className="h-9 w-9 p-0 text-slate-500 hover:text-blue-600 dark:hover:text-blue-400 transition-colors">
    <Link href={`/clientes/${encodeURIComponent(cliente.id)}`}>
      <Eye className="h-4 w-4" />
    </Link>
  </Button>
  {canEditClientes ? (
    <Button asChild size="sm" variant="ghost" className="h-9 w-9 p-0 text-slate-500 hover:text-emerald-600 ...">
      <Link href={`/clientes/${encodeURIComponent(cliente.id)}/editar`}>
        <Pencil className="h-4 w-4" />
      </Link>
    </Button>
  ) : null}
  {canEditClientes ? (
    <Button ... onClick={onDelete} ...>
      <Trash2 className="h-4 w-4" />
    </Button>
  ) : null}
</div>
```
Adicionar botão Printer APÓS o botão Eye, ANTES dos botões condicionais de edição. Sempre visível (não depende de canEditClientes — clientes:view é suficiente).

Import de Printer já disponível via lucide-react (pacote instalado no projecto).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Adicionar botão "Imprimir Ficha" na página de detalhe do cliente</name>
  <files>web/src/app/(dashboard)/clientes/[id]/page.tsx</files>
  <read_first>
    - web/src/app/(dashboard)/clientes/[id]/page.tsx — ler ficheiro completo para localizar o bloco "flex gap-2" dos botões do cabeçalho (em volta das linhas 74-83); ver os imports actuais para saber se Printer já está importado
  </read_first>
  <action>
    Modificar web/src/app/(dashboard)/clientes/[id]/page.tsx:

    1. Adicionar "Printer" ao import de lucide-react (se não estiver já importado). O import de lucide-react pode não existir ainda neste ficheiro — adicionar: import { Printer } from "lucide-react";

    2. No bloco div.flex.gap-2 do cabeçalho (componente ClienteDetailContent), adicionar botão "Imprimir Ficha" entre o botão "Voltar" e o bloco condicional {canEditClientes ? ... Editar ...}:

    Button com:
    - asChild variant="outline"
    - Link href={`/clientes/${encodeURIComponent(id)}/ficha`} target="_blank" rel="noopener noreferrer"
    - Conteúdo: ícone Printer (h-4 w-4 mr-2) + texto "Imprimir Ficha"

    Este botão é sempre visível para qualquer utilizador que chegue a esta página (a permissão guard já existe no início do componente pai FichaPage).

    Não modificar nenhum outro elemento do ficheiro — apenas adicionar o botão no sítio descrito.
  </action>
  <verify>
    <automated>cd web && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - web/src/app/(dashboard)/clientes/[id]/page.tsx contém a string "Imprimir Ficha"
    - web/src/app/(dashboard)/clientes/[id]/page.tsx contém "target=\"_blank\""
    - web/src/app/(dashboard)/clientes/[id]/page.tsx contém "/ficha"
    - web/src/app/(dashboard)/clientes/[id]/page.tsx contém "rel=\"noopener noreferrer\""
    - npx tsc --noEmit no directório web não reporta erros neste ficheiro
  </acceptance_criteria>
  <done>Botão "Imprimir Ficha" presente no cabeçalho da página de detalhe, abre /clientes/{id}/ficha em nova aba.</done>
</task>

<task type="auto">
  <name>Task 2: Adicionar ícone Printer na ClienteRow da listagem</name>
  <files>web/src/app/(dashboard)/clientes/page.tsx</files>
  <read_first>
    - web/src/app/(dashboard)/clientes/page.tsx — ler a secção ClienteRow (a partir da linha 476) para localizar o bloco inline-flex com Eye/Pencil/Trash2; ver a linha de import de lucide-react (linha 5) para confirmar que Printer pode ser adicionado ao mesmo import
  </read_first>
  <action>
    Modificar web/src/app/(dashboard)/clientes/page.tsx:

    1. Adicionar "Printer" ao import existente de lucide-react (linha 5 do ficheiro). Import actual: { Eye, Filter, Pencil, Plus, Search, Trash2 } — adicionar Printer à lista.

    2. Na função ClienteRow, no bloco div.inline-flex.items-center.gap-1, adicionar botão Printer IMEDIATAMENTE APÓS o botão Eye existente (antes dos blocos condicionais canEditClientes):

    Button com:
    - asChild size="sm" variant="ghost"
    - className="h-9 w-9 p-0 text-slate-500 hover:text-purple-600 dark:hover:text-purple-400 transition-colors"
    - Link href={`/clientes/${encodeURIComponent(cliente.id)}/ficha`} target="_blank" rel="noopener noreferrer"
    - Conteúdo: Printer com className="h-4 w-4"

    Este botão é sempre visível (não está dentro de {canEditClientes ? ...}) — qualquer utilizador com clientes:view que chegue à listagem pode imprimir a ficha. Isto é consistente com o modelo de permissões: clientes:view é suficiente para ver e imprimir.

    Nota de planeamento: D-08 original menciona "menu de contexto kebab" mas o padrão actual da listagem usa botões directos (Eye, Pencil, Trash2). Seguindo a recomendação da RESEARCH.md (Padrão 4 / Assunção A5), adicionamos botão directo Printer para manter consistência com o padrão existente. Criar DropdownMenu seria divergir do padrão estabelecido sem benefício claro.

    Não modificar nenhum outro elemento do ficheiro — apenas adicionar o botão e o import.
  </action>
  <verify>
    <automated>cd web && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - web/src/app/(dashboard)/clientes/page.tsx contém "Printer" no import de lucide-react
    - web/src/app/(dashboard)/clientes/page.tsx contém "/ficha" dentro da função ClienteRow
    - web/src/app/(dashboard)/clientes/page.tsx contém "target=\"_blank\"" dentro da função ClienteRow
    - O botão Printer está posicionado APÓS o botão Eye e NÃO está envolvido em {canEditClientes ? ...}
    - npx tsc --noEmit no directório web não reporta erros neste ficheiro
  </acceptance_criteria>
  <done>Ícone Printer presente em cada linha da listagem de clientes, abre /clientes/{id}/ficha em nova aba. Sempre visível para utilizadores com clientes:view.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Listagem → Link ficha | Link abre URL em nova aba; sem input do utilizador na construção da URL — apenas cliente.id proveniente da API |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-60-04 | Information Disclosure | Link target=_blank com cliente.id | accept | cliente.id é UUID opaque proveniente da API autenticada; rel="noopener noreferrer" previne window.opener attacks; a página de destino tem o seu próprio guard de permissão (plano 60-01) |
| T-60-05 | Spoofing | rel="noopener noreferrer" ausente | mitigate | Incluir rel="noopener noreferrer" em todos os Links target=_blank — previne que a nova aba aceda ao contexto da aba original via window.opener |
| T-60-SC | Tampering | npm/pip/cargo installs | accept | Esta fase não instala nenhum pacote novo; sem risco de supply chain |
</threat_model>

<verification>
1. Na página /clientes/{id}, botão "Imprimir Ficha" visível no cabeçalho junto a "Voltar" e "Editar"
2. Clicar "Imprimir Ficha" abre nova aba em /clientes/{id}/ficha (não navega na aba actual)
3. Na listagem /clientes, cada linha tem ícone Printer visível junto ao ícone Eye
4. Clicar Printer na listagem abre nova aba em /clientes/{id}/ficha para o cliente correcto
5. pnpm build em web/ passa sem erros de TypeScript ou lint
</verification>

<success_criteria>
- Botão "Imprimir Ficha" com target="_blank" e rel="noopener noreferrer" presente em /clientes/[id]/page.tsx — implementação D-07
- Ícone Printer com target="_blank" presente em ClienteRow de /clientes/page.tsx — implementação D-08 (botão directo, consistente com padrão actual da listagem)
- Nenhuma nova dependência instalada
- TypeScript compila sem erros
</success_criteria>

<output>
Create `.planning/phases/LEXCV-60-ficha-imprimivel/60-02-SUMMARY.md` when done
</output>
