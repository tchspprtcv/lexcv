---
phase: 128-auditoria-de-atribui-es-de-pap-is
plan: 09
type: execute
wave: 5
depends_on: ["128-08", "128-03", "128-05", "128-06"]
files_modified:
  - web/src/app/(dashboard)/settings/auditoria-tab.tsx
  - web/src/app/(dashboard)/settings/page.tsx
  - web/scripts/verify-auditoria-rbac.mjs
  - web/package.json
autonomous: false
requirements: [AUDT-01, AUDT-02, AUDT-03, AUDT-04]

must_haves:
  truths:
    - "An office administrator with rbac:manage sees an 'Auditoria' tab in Definições, right after 'Controlo de Acesso (RBAC)'"
    - "The tab lists the office's role and assignment events as PT-CV sentences, newest first, 20 per page"
    - "The administrator can filter by target user and by role; changing a filter returns to page 1"
    - "The tab has no edit, delete or clear-log affordance anywhere, not even disabled"
    - "The Phase 127 unsaved-matrix draft cache, beforeunload guard and dirty-count label in RbacTab keep working unchanged"
  artifacts:
    - path: "web/src/app/(dashboard)/settings/auditoria-tab.tsx"
      provides: "AuditoriaTab: Filtros card, Resultados card, rows, pager"
      exports: ["AuditoriaTab"]
      min_lines: 120
    - path: "web/scripts/verify-auditoria-rbac.mjs"
      provides: "Structural read-only and wiring gate"
  key_links:
    - from: "web/src/app/(dashboard)/settings/page.tsx"
      to: "AuditoriaTab"
      via: "activeTab === \"auditoria\" && hasRbacManage"
      pattern: "activeTab === \"auditoria\" && hasRbacManage"
    - from: "auditoria-tab.tsx"
      to: "useOfficeRbacAuditoria"
      via: "filters state → hook"
      pattern: "useOfficeRbacAuditoria\\("
    - from: "auditoria-tab.tsx"
      to: "auditoriaEventoToSentence"
      via: "row rendering"
      pattern: "auditoriaEventoToSentence\\("
---

<objective>
Ship the read-only "Auditoria" tab exactly as the approved UI-SPEC describes: new component file `auditoria-tab.tsx`, a purely additive wiring in `settings/page.tsx` (TabId, tab button, panel), a structural verify script enforcing the Read-Only Guarantee, and a human check of the full flow against a running backend.

Purpose: AUDT-03 becomes usable by the office administrator, and AUDT-04 holds on the UI surface.
Output: component, page wiring, verify script plus npm script, human verification.
</objective>

<execution_context>
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/LEXCV-128-auditoria-de-atribui-es-de-pap-is/128-UI-SPEC.md
@.planning/phases/LEXCV-128-auditoria-de-atribui-es-de-pap-is/128-08-PLAN.md
@web/AGENTS.md

<interfaces>
From Plan 08:
  useOfficeRbacAuditoria(filters: { utilizadorAlvoId?: string; papelId?: string; page?: number; size?: number }) → UseQueryResult<AuditoriaRbacPageResponse>
  auditoriaEventoToSentence(entry) → { texto: string; destaque: boolean }[]
  auditoriaEventoToTexto(entry) → string
  auditoriaCategoriaToLabel(categoria) → "Papel" | "Atribuição"
  auditoriaPermissoesDetalhe(entry, rotulos: Record<string,string>) → string | null
Existing hooks: useOfficeRbac() → data.papeis: OfficePapel[] ({ id, nome, ... }), data.permissoes: OfficePermissao[] ({ key, nome, ... });
  useAdminUsers(options?: { enabled?: boolean }) → AdminUser[] ({ id, nome, ... }), served by GET /admin/users, gated by users:manage on the backend
Existing shared: Combobox + ComboboxOption from web/src/components/shared/combobox.tsx
settings/page.tsx (re-read before editing; line numbers moved after commits a5de92cb/fe144ba1):
  type TabId = "profile" | "security" | "users" | "rbac" | "notificacoes";
  const hasUsersManage = can.manage("users"); const hasRbacManage = can.manage("rbac");
  tab strip: button blocks gated by hasUsersManage / hasRbacManage / can.view("notificacoes")
  panels: {activeTab === "rbac" && hasRbacManage && (<div className="animate-in fade-in duration-200"><RbacTab /></div>)}
  RbacTab (Phase 127 fix, DO NOT TOUCH): module-scope draft cache, beforeunload effect, dirty-count label via
  papeisComAlteracoesPorGravar / rotuloPapeisPorGravar imported from ./merge-local-papeis
Reference screens: web/src/app/(dashboard)/notificacoes/page.tsx (filter card, categoria <select>, "Limpar filtros", Pagination footer, page reset/clamp),
  web/src/app/(dashboard)/processos/[id]/page.tsx (formatDateTime, the toLocaleString("pt-CV") helper)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: AuditoriaTab component and additive page wiring</name>
  <files>web/src/app/(dashboard)/settings/auditoria-tab.tsx, web/src/app/(dashboard)/settings/page.tsx</files>
  <read_first>
    - .planning/phases/LEXCV-128-auditoria-de-atribui-es-de-pap-is/128-UI-SPEC.md (full; binding)
    - web/src/app/(dashboard)/settings/page.tsx (TabId, SettingsPage tab strip and panels; skim RbacTab only to confirm it is untouched)
    - web/src/app/(dashboard)/notificacoes/page.tsx (filter card, select, clear filters, empty states, Pagination block, page reset and clamp logic)
    - web/src/components/shared/combobox.tsx (props)
    - web/src/app/(dashboard)/processos/[id]/page.tsx (formatDateTime helper only)
    - web/src/lib/auditoria-rbac.ts and web/src/hooks/use-admin.ts (Plan 08)
    - web/node_modules/next/dist/docs/ (only if a Next-specific API is needed; this component is a plain client component)
  </read_first>
  <action>
Create `web/src/app/(dashboard)/settings/auditoria-tab.tsx`. It is a `"use client"` module exporting `AuditoriaTab`, following UI-SPEC §2-§4 literally. Structure:
- `space-y-6` wrapper; Card "Filtros" with `CardContent className="grid gap-4 sm:grid-cols-2"`.
- Label "Utilizador alvo" with the `Combobox`: options from `useAdminUsers({ enabled: hasUsersManage })` mapped to `{ value: id, label: nome }`, placeholder "Todos os utilizadores", searchPlaceholder "Pesquisar utilizador por nome...". `hasUsersManage` comes from `usePermissions().can.manage("users")`, because GET /admin/users is gated by users:manage. A caller with rbac:manage but without users:manage gets an empty option list instead of a 403 toast. Record this in a comment and in SUMMARY as a known limitation, next to "deleted users are not selectable".
- Label "Papel" with a plain styled `<select>` copied from the notificacoes categoria filter. The first option is "Todos os papéis" (value ""), followed by `useOfficeRbac().data?.papeis` with option value = papel.id and label = papel.nome. The filter sends `papelId` (Plan 08 reconciliation).
- The "Limpar filtros" ghost Button with the `X` icon appears only when a filter is active and resets the FILTER state only.
- Card "Resultados" covers the states in UI-SPEC order: loading "A carregar..."; error "Não foi possível carregar o registo de auditoria. Tente novamente."; empty with active filters "Nenhum evento encontrado para os filtros selecionados." plus "Limpar filtros"; true-zero `Empty` with "Sem eventos registados" and the body copy verbatim. When there are results, render a list of rows per UI-SPEC §3.
- Each row: `flex items-start gap-3 py-3 border-b ...`, the timestamp span `w-40 shrink-0` using a local `formatDateTime` (toLocaleString("pt-CV"), copied from processos/[id]), a `<p>` that maps `auditoriaEventoToSentence(entry)` segments to `<span>`s (`font-semibold` when destaque; text children only, never dangerouslySetInnerHTML), an optional `text-xs` sub-line from `auditoriaPermissoesDetalhe(entry, rotulos)` where `rotulos` is built from `useOfficeRbac().data?.permissoes` (key → nome), and `<Badge variant="outline" className="shrink-0">{auditoriaCategoriaToLabel(entry.categoria)}</Badge>`.
- The Pagination footer is rendered only when totalPages > 1, with "Página {n} de {total}", copied from notificacoes.
- State: `useState` for utilizadorAlvoId, papelId and page. Page size is the constant 20. Changing a filter resets page to 0; clamp the page when totalPages shrinks, as notificacoes does. No useEffect for data fetching.
- Read-Only Guarantee (UI-SPEC, binding): no row actions, no DropdownMenu, no pencil/trash icons, no useMutation, no DELETE/PUT/PATCH/POST call, not even disabled.

Edit `web/src/app/(dashboard)/settings/page.tsx` ADDITIVELY only:
(1) extend the TabId union with `"auditoria"`;
(2) add `History` to the existing lucide-react import list, and add `import { AuditoriaTab } from "./auditoria-tab";` next to the other `./` imports;
(3) insert a `{hasRbacManage && (<button ...>)}` block immediately after the RBAC tab button and before the notificacoes button, copying the RBAC button's className ternary with `activeTab === "auditoria"`, the `History` icon and the label "Auditoria";
(4) insert `{activeTab === "auditoria" && hasRbacManage && (<div className="animate-in fade-in duration-200"><AuditoriaTab /></div>)}` immediately after the rbac panel.
Do not modify RbacTab, the draft cache, the beforeunload effect, merge-local-papeis imports, or any other component in page.tsx.
  </action>
  <verify>
    <automated>pnpm -C web lint && pnpm -C web build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -F "export function AuditoriaTab" "web/src/app/(dashboard)/settings/auditoria-tab.tsx"` matches
    - `grep -F "useOfficeRbacAuditoria(" "web/src/app/(dashboard)/settings/auditoria-tab.tsx"` and `grep -F "auditoriaEventoToSentence(" "web/src/app/(dashboard)/settings/auditoria-tab.tsx"` match
    - `grep -F "Sem eventos registados" "web/src/app/(dashboard)/settings/auditoria-tab.tsx"` and `grep -F "Todos os papéis" "web/src/app/(dashboard)/settings/auditoria-tab.tsx"` match
    - `grep -F "useMutation" "web/src/app/(dashboard)/settings/auditoria-tab.tsx"` returns nothing; `grep -F "dangerouslySetInnerHTML" "web/src/app/(dashboard)/settings/auditoria-tab.tsx"` returns nothing; `grep -F "DropdownMenu" "web/src/app/(dashboard)/settings/auditoria-tab.tsx"` returns nothing
    - `grep -F "\"auditoria\"" "web/src/app/(dashboard)/settings/page.tsx"` matches; `grep -F "activeTab === \"auditoria\" && hasRbacManage" "web/src/app/(dashboard)/settings/page.tsx"` matches
    - Phase 127 work intact: `grep -F "papeisComAlteracoesPorGravar" "web/src/app/(dashboard)/settings/page.tsx"` and `grep -F "beforeunload" "web/src/app/(dashboard)/settings/page.tsx"` still match, and `git diff "web/src/app/(dashboard)/settings/page.tsx"` shows only added lines plus the edited TabId and lucide import lines (the executor pastes `git diff --stat` and the hunk headers into SUMMARY)
    - `pnpm -C web lint` and `pnpm -C web build` exit 0; `node web/scripts/verify-papeis-escritorio.mjs` still exits 0
  </acceptance_criteria>
  <done>Tab renders per UI-SPEC, is additive in page.tsx, and the Phase 127 gates still pass.</done>
</task>

<task type="auto">
  <name>Task 2: Structural verify script for the Auditoria tab</name>
  <files>web/scripts/verify-auditoria-rbac.mjs, web/package.json</files>
  <read_first>
    - web/scripts/verify-papeis-escritorio.mjs (structure: file reads, assertions, exit codes, output style)
    - web/package.json (verify:* scripts block)
  </read_first>
  <action>
Create `web/scripts/verify-auditoria-rbac.mjs` in the style of verify-papeis-escritorio.mjs (node built-ins only, resolved relative to the script's own directory, non-zero exit with a named failure on the first broken assertion, a summary line on success). Assertions, all literal substring checks:
(a) `auditoria-tab.tsx` contains none of: "useMutation", "dangerouslySetInnerHTML", "DropdownMenu", "Trash", "Pencil", "method: \"DELETE\"", "method: \"PUT\"", "method: \"POST\"", "method: \"PATCH\"";
(b) it contains "useOfficeRbacAuditoria(" and "auditoriaEventoToSentence(" and does not render `entry.acao` directly (it does not contain "{entry.acao}");
(c) `use-admin.ts` contains "export function useOfficeRbacAuditoria" and, within the text slice from that declaration to the next "export function" (or end of file), contains "useQuery(" and not "useMutation(";
(d) `page.tsx` contains "\"auditoria\"", "activeTab === \"auditoria\" && hasRbacManage", and still contains "papeisComAlteracoesPorGravar" and "beforeunload" (Phase 127 fix intact);
(e) `lib/auditoria-rbac.ts` contains the three binding fallback strings.
Add `"verify:auditoria-rbac": "node scripts/verify-auditoria-rbac.mjs"` to package.json scripts, after verify:consola-moldes.
  </action>
  <verify>
    <automated>node web/scripts/verify-auditoria-rbac.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `node web/scripts/verify-auditoria-rbac.mjs` exits 0
    - `grep -F "verify:auditoria-rbac" web/package.json` matches
    - Mutation check (run once, then revert; record in SUMMARY): adding the line `// useMutation` to auditoria-tab.tsx makes the script exit non-zero
  </acceptance_criteria>
  <done>The read-only guarantee and the additive wiring are enforced by a script that is proven to go red.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verification of the Auditoria tab end to end</name>
  <files>none (verification only)</files>
  <read_first>
    - .planning/phases/LEXCV-128-auditoria-de-atribui-es-de-pap-is/128-UI-SPEC.md (Copywriting Contract, Read-Only Guarantee)
    - web/src/app/(dashboard)/settings/auditoria-tab.tsx (Task 1)
  </read_first>
  <action>Before pausing, run every automated gate in this plan (lint, build, both verify scripts, vitest) and the backend suite `JAVA_HOME="/c/Program Files/Java/jdk-23" mvn -q -f backend/pom.xml test`. Then present the steps below to the user and wait. Make no code change during this task; any issue reported goes back into Task 1 or 2.</action>
  <verify>
    <automated>node web/scripts/verify-auditoria-rbac.mjs</automated>
  </verify>
  <acceptance_criteria>
    - The user replies "approved" after steps 1-8, or the reported issues are fixed and re-verified
    - The SUMMARY records which steps were exercised (step 8 only if a second office exists locally)
  </acceptance_criteria>
  <done>The user approved the end-to-end flow, the read-only surface and the intact Phase 127 behavior.</done>
  <what-built>Backend audit writes (plans 03-06), query endpoint (07), and the Auditoria tab (08-09). Migration 128 is only needed on validate installs; a local ddl-auto=update backend creates the column itself.</what-built>
  <how-to-verify>
    1. Start the backend (`JAVA_HOME="/c/Program Files/Java/jdk-23" mvn -f backend/pom.xml spring-boot:run`) and the web app (`pnpm -C web dev`); log in as an office administrator (e.g. admin@alcv.cv / Pa$$w0rd).
    2. Definições → "Controlo de Acesso (RBAC)": create a role "Teste Auditoria", rename it to "Teste Auditoria 2", tick one permission in the matrix and save.
    3. Definições → "Gestão de Utilizadores": assign "Teste Auditoria 2" to a user, then remove it.
    4. Open "Auditoria" (right after the RBAC tab). Expect newest first: "… retirou o papel Teste Auditoria 2 a …", "… atribuiu o papel …", "… alterou as permissões do papel Teste Auditoria 2." with an "Acrescentou: <label>" sub-line, "… renomeou o papel Teste Auditoria para Teste Auditoria 2.", and "… criou o papel Teste Auditoria." No raw code (papel_*) anywhere.
    5. Filter "Papel" = Teste Auditoria 2: all five events remain, including the create event written under the old name. Filter "Utilizador alvo" = that user: only the two assignment events. "Limpar filtros" restores the full list.
    6. Confirm that no row has any menu, pencil, trash or clear action.
    7. In the RBAC tab, tick a permission WITHOUT saving, switch to Auditoria and back: the unsaved edit and the dirty-count label are still there (Phase 127 fix intact).
    8. Log in as a user of a second office (if one exists): their Auditoria tab does not show the events above.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API → DOM | Names of users and roles (user-controlled strings) rendered in sentences |
| UI → audit endpoint | Any client code path that could mutate the log |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-128-42 | Tampering (XSS) | sentence rendering | mitigate | Segments rendered as React text children; the verify script forbids dangerouslySetInnerHTML |
| T-128-43 | Tampering | client-side log mutation | mitigate | Verify script forbids useMutation, mutating fetch methods, and row action menus in the tab, and forbids useMutation in the hook's body; the backend has no mutating endpoint anyway (Plan 01 gate) |
| T-128-44 | Information Disclosure | tab visible to non-admins | mitigate | Tab and panel gated by hasRbacManage (the same authority as the endpoint); the server gate (Plan 07) is authoritative |
| T-128-45 | Denial of Service (usability) | 403 toast from /admin/users for rbac:manage-only callers | mitigate | useAdminUsers enabled only when users:manage; documented limitation |
| T-128-46 | Tampering | regression of the Phase 127 unsaved-edits fix | mitigate | Additive-only edits; verify script asserts the draft-cache and beforeunload markers; the human check covers step 7 |
</threat_model>

<verification>
- `pnpm -C web lint && pnpm -C web build` exit 0
- `node web/scripts/verify-auditoria-rbac.mjs` and `node web/scripts/verify-papeis-escritorio.mjs` exit 0
- `pnpm -C web test` exits 0
- Human checkpoint approved
</verification>

<success_criteria>
- UI-SPEC implemented as approved: tab placement, filters, 20 per page, sentences, null-name fallback, Read-Only Guarantee
- Phase 127 RbacTab behavior intact
</success_criteria>

<output>
Create `.planning/phases/LEXCV-128-auditoria-de-atribui-es-de-pap-is/128-09-SUMMARY.md` when done
</output>
