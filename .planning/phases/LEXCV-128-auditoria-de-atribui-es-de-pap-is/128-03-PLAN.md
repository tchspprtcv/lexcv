---
phase: 128-auditoria-de-atribui-es-de-pap-is
plan: 03
type: execute
wave: 3
depends_on: ["128-02"]
files_modified:
  - backend/src/main/java/com/lexcv/controllers/OfficeRolesController.java
  - backend/src/test/java/com/lexcv/controllers/OfficeRolesControllerTest.java
  - backend/src/test/java/com/lexcv/controllers/OfficeRolesControllerAuditoriaTest.java
autonomous: true
requirements: [AUDT-01]

must_haves:
  truths:
    - "Creating a role records papel_criar with the actor, the moment, the role name and its initial permissions"
    - "Renaming a role records papel_renomear with the old and new names"
    - "Deleting a role records papel_apagar with the name and the permissions it held"
    - "The role write and its event commit in one transaction, or neither does"
    - "A refused create/rename/delete (400, 404, 409) writes no event and leaves the transaction rollback-only"
    - "The concurrent duplicate-name and concurrent-assignment races still return 409, not 500, under the new transaction"
  artifacts:
    - path: "backend/src/main/java/com/lexcv/controllers/OfficeRolesController.java"
      provides: "@Transactional + audited createRole/renameRole/deleteRole"
      contains: "auditoriaRbacService.registarPapelCriado"
    - path: "backend/src/test/java/com/lexcv/controllers/OfficeRolesControllerAuditoriaTest.java"
      provides: "Audit + transaction-boundary proof for the three handlers"
      min_lines: 120
  key_links:
    - from: "OfficeRolesController.createRole"
      to: "AuditoriaRbacService.registarPapelCriado"
      via: "call after saveAndFlush inside the same @Transactional method"
      pattern: "registarPapelCriado"
    - from: "OfficeRolesController refusals"
      to: "RecusaTransacional.recusar"
      via: "every non-2xx return"
      pattern: "recusar\\("
---

<objective>
Instrument the three `OfficeRolesController` write handlers (Decision 3): make each `@Transactional`, record its event through `AuditoriaRbacService` in the same transaction, route every refusal through `RecusaTransacional.recusar`, and keep the existing `DataIntegrityViolationException` → 409 handling working inside the new transaction.

Purpose: AUDT-01 for create/rename/delete.
Output: modified controller, updated existing test, new audit/transaction test.
</objective>

<execution_context>
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/LEXCV-128-auditoria-de-atribui-es-de-pap-is/128-CONTEXT.md
@.planning/phases/LEXCV-128-auditoria-de-atribui-es-de-pap-is/128-02-PLAN.md

<interfaces>
From Plan 02:
  AuditoriaRbacService (constructor-injected):
    void registarPapelCriado(UUID tenantId, UserPrincipal autor, TenantRole papel)
    void registarPapelRenomeado(UUID tenantId, UserPrincipal autor, UUID papelId, String nomeAntigo, String nomeNovo)
    void registarPapelApagado(UUID tenantId, UserPrincipal autor, TenantRole papel)
  RecusaTransacional.recusar(ResponseEntity<T>) -> same ResponseEntity, marks the current tx rollback-only (no-op without tx)

Current OfficeRolesController (read in full before editing):
  fields (constructor order): tenantRoleRepository, userRepository, roleRepository, permissionRepository
  createRole: tenantRoleRepository.save(novoPapel) inside try/catch(DataIntegrityViolationException) -> 409
  renameRole: tenantRole.setNome(nome); tenantRoleRepository.save(tenantRole) inside try/catch -> 409
  deleteRole: guards (404, protected 409, platform 409, count 409), then tenantRoleRepository.deleteById(id) inside try/catch (WR-01) -> 409
  No @Transactional anywhere in the file today.
TenantRoleRepository extends JpaRepository<TenantRole, UUID> (so saveAndFlush and flush exist).
OfficeRolesControllerTest line ~80: new OfficeRolesController(tenantRoleRepository, userRepository, roleRepository, permissionRepository)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Transactional, audited create/rename/delete with refusal rollback</name>
  <files>backend/src/main/java/com/lexcv/controllers/OfficeRolesController.java, backend/src/test/java/com/lexcv/controllers/OfficeRolesControllerTest.java</files>
  <read_first>
    - backend/src/main/java/com/lexcv/controllers/OfficeRolesController.java (full)
    - backend/src/main/java/com/lexcv/controllers/RecusaTransacional.java (Plan 02)
    - backend/src/main/java/com/lexcv/services/AuditoriaRbacService.java (Plan 02)
    - backend/src/test/java/com/lexcv/controllers/OfficeRolesControllerTest.java (full: every stub of tenantRoleRepository.save / deleteById that must follow the switch to saveAndFlush / flush)
  </read_first>
  <behavior>
    - Existing OfficeRolesControllerTest cases keep passing after the constructor gains AuditoriaRbacService and after save is replaced by saveAndFlush (stubs updated accordingly)
    - Existing refusal cases (404 cross-tenant, 409 protected, 409 assigned, 400 name) additionally verify auditoriaRbacService received no interaction
  </behavior>
  <action>
Constructor: append `private final AuditoriaRbacService auditoriaRbacService;` as the LAST field, so positional construction only gains a trailing argument. Update `OfficeRolesControllerTest.novoController()` (and any other `new OfficeRolesController(` in that file) to pass a `@Mock AuditoriaRbacService`.

Add a private `getPrincipal()` helper that returns the `UserPrincipal` from `SecurityContextHolder`. Keep `getTenantId()` delegating to it. The actor and tenant always come from the principal (Decision 3).

Per Decision 3, annotate `createRole`, `renameRole` and `deleteRole` with `@Transactional` (`org.springframework.transaction.annotation.Transactional`). Add one class-level doc paragraph (Portuguese) explaining:
- the event and the change commit together;
- why each write is now flushed inside its `try`: without a flush, a unique or FK violation would surface at commit, outside the catch, and become a 500;
- why every refusal goes through `recusar`.

createRole: replace `tenantRoleRepository.save(novoPapel)` with `tenantRoleRepository.saveAndFlush(novoPapel)` inside the existing try. On success, call `auditoriaRbacService.registarPapelCriado(tenantId, principal, papelGravado)` inside the try after the flush, before building the 201. Wrap every non-2xx return with `RecusaTransacional.recusar(...)`: the name validation, the duplicate pre-check 409, the unknown-permission 400, and the catch 409.

renameRole: capture `String nomeAntigo = tenantRole.getNome()` before `setNome`. Use `saveAndFlush`. Call `registarPapelRenomeado(tenantId, principal, papelGravado.getId(), nomeAntigo, papelGravado.getNome())` only when the name actually changed (`!nomeAntigo.equals(nome)`); a no-op rename is not a change and writes no event. Route all refusals (404, validation, 409 pre-check, catch 409) through `recusar`.

deleteRole: after all guards pass, and before deleting, keep a reference to the loaded `tenantRole`; its name and permissions are what the event records. Inside the existing try, call `tenantRoleRepository.deleteById(id)` followed by `tenantRoleRepository.flush()`, so the WR-01 FK race still lands in the catch. After the try, call `auditoriaRbacService.registarPapelApagado(tenantId, principal, tenantRole)` and return 204. Route all refusals (404, both 409 provenance guards, count 409, catch 409) through `recusar`. Update the WR-01 comment: the catch still exists; `@Transactional` at the default isolation does not close the count-then-delete window; the flush keeps the failure shape at 409; and the audit event cannot survive the rollback because it shares the transaction.

Update existing tests that stubbed `tenantRoleRepository.save(...)` for create/rename to stub `saveAndFlush(...)`. Update the DataIntegrityViolation cases to throw from `saveAndFlush` (create/rename) and from `flush()` (delete, previously `deleteById`). Add `verifyNoInteractions(auditoriaRbacService)` to each existing refusal test.
  </action>
  <verify>
    <automated>JAVA_HOME="/c/Program Files/Java/jdk-23" mvn -q -f backend/pom.xml test -Dtest=OfficeRolesControllerTest</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c -F "@Transactional" backend/src/main/java/com/lexcv/controllers/OfficeRolesController.java` is at least 3 (annotation lines; the executor confirms by reading that all three handlers carry it)
    - `grep -F "saveAndFlush" backend/src/main/java/com/lexcv/controllers/OfficeRolesController.java` matches
    - `grep -F "tenantRoleRepository.flush()" backend/src/main/java/com/lexcv/controllers/OfficeRolesController.java` matches
    - `grep -F "registarPapelCriado" backend/src/main/java/com/lexcv/controllers/OfficeRolesController.java`, `grep -F "registarPapelRenomeado" ...` and `grep -F "registarPapelApagado" ...` each match
    - `grep -c -F "recusar(" backend/src/main/java/com/lexcv/controllers/OfficeRolesController.java` is at least 11
    - `JAVA_HOME="/c/Program Files/Java/jdk-23" mvn -q -f backend/pom.xml test -Dtest=OfficeRolesControllerTest` exits 0 with no fewer tests than before this plan
  </acceptance_criteria>
  <done>Three handlers transactional and audited; all prior tests green after the constructor and flush adjustments; refusals proven to touch no audit.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Audit content and transaction-boundary proof</name>
  <files>backend/src/test/java/com/lexcv/controllers/OfficeRolesControllerAuditoriaTest.java</files>
  <read_first>
    - backend/src/test/java/com/lexcv/controllers/OfficeRolesControllerTest.java (principal fixture helper autenticarComoPrincipalDoTenant, OUTRO_TENANT_ID, novoProxyComMethodSecurity)
    - backend/src/test/java/com/lexcv/controllers/RecusaTransacionalTest.java (Plan 02: the TransactionInterceptor + mocked PlatformTransactionManager proxy technique)
    - backend/src/main/java/com/lexcv/controllers/OfficeRolesController.java (as modified by Task 1)
  </read_first>
  <behavior>
    - Reflection: createRole, renameRole, deleteRole each carry org.springframework.transaction.annotation.Transactional
    - createRole success through a TransactionInterceptor proxy: InOrder(txManager, tenantRoleRepository, auditoriaRbacService) shows getTransaction, then saveAndFlush, then registarPapelCriado, then commit with a status that is NOT rollback-only; registarPapelCriado receives the principal's tenantId and the saved role
    - createRole where registarPapelCriado throws RuntimeException: txManager.rollback is called and commit never is (the role write does not survive without its event)
    - createRole where saveAndFlush throws DataIntegrityViolationException: response 409, registarPapelCriado never called, committed status IS rollback-only
    - renameRole success: registarPapelRenomeado(tenant, principal, id, "Antigo", "Novo"); same-name rename: 200 and registarPapelRenomeado never called
    - renameRole on another tenant's id: 404, no audit interaction, status rollback-only
    - deleteRole success: registarPapelApagado called with the loaded role after deleteById and flush (InOrder); deleteRole of the protected admin role: 409, no audit, rollback-only
    - The tenantId passed to every registar* call equals the principal's tenant, even when the request targets an id belonging to OUTRO_TENANT_ID (which 404s)
  </behavior>
  <action>
Create `OfficeRolesControllerAuditoriaTest` (Mockito extension). Reuse the principal-fixture idiom from `OfficeRolesControllerTest`, copying its small helpers rather than coupling the two test classes. Build the transactional proxy exactly as in `RecusaTransacionalTest`: `ProxyFactory` over a real `OfficeRolesController` with mocked repositories and a mocked `AuditoriaRbacService`, `setProxyTargetClass(true)`, a `TransactionInterceptor` over a mocked `PlatformTransactionManager` returning `new SimpleTransactionStatus()`, and an `ArgumentCaptor<TransactionStatus>` on `commit`. Stub `permissionRepository.findAllByReservadaPlataformaFalse()` and `roleRepository.findByNome("ADMIN")` as the existing tests do. Cover every behavior bullet with one test method each, named in Portuguese in the existing style (for example `createRole_gravaEventoNaMesmaTransaccaoDepoisDoSaveAndFlush`).
  </action>
  <verify>
    <automated>JAVA_HOME="/c/Program Files/Java/jdk-23" mvn -q -f backend/pom.xml test -Dtest=OfficeRolesControllerAuditoriaTest</automated>
  </verify>
  <acceptance_criteria>
    - `grep -F "TransactionInterceptor" backend/src/test/java/com/lexcv/controllers/OfficeRolesControllerAuditoriaTest.java` matches
    - `grep -F "isRollbackOnly" backend/src/test/java/com/lexcv/controllers/OfficeRolesControllerAuditoriaTest.java` matches
    - `grep -F "inOrder" backend/src/test/java/com/lexcv/controllers/OfficeRolesControllerAuditoriaTest.java` matches
    - `JAVA_HOME="/c/Program Files/Java/jdk-23" mvn -q -f backend/pom.xml test -Dtest=OfficeRolesControllerAuditoriaTest` exits 0 with at least 10 tests
  </acceptance_criteria>
  <done>Same-transaction, event-failure rollback, refusal rollback-only and tenant-from-principal all proven for the three handlers.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| office admin (rbac:manage) → role CRUD | Untrusted body (name, permissions) and path id |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-128-14 | Repudiation | create/rename/delete | mitigate | @Transactional + event inside the same method; event failure rolls back the change (tested) |
| T-128-15 | Tampering | refused requests | mitigate | Every non-2xx return goes through recusar → rollback-only; renameRole's setNome before a later refusal cannot flush (tested for the 404 path; pattern applied to all) |
| T-128-16 | Spoofing | actor/tenant of the event | mitigate | Principal from SecurityContextHolder only; tested that the tenant passed equals the principal's even for foreign ids |
| T-128-17 | Denial of Service | unique-name / FK races inside the new tx | mitigate | saveAndFlush / flush inside the try keeps them at 409; a caught exception plus local rollback-only avoids UnexpectedRollbackException |
| T-128-18 | Tampering | deleteRole count-then-delete window | accept | Pre-existing WR-01 race, unchanged in kind; the FK still blocks the inconsistent state, and the audit event rolls back with it. Registered with the project's other accepted check-then-act races (DatabaseSeeder, limite de utilizadores T-117-07) |
</threat_model>

<verification>
`JAVA_HOME="/c/Program Files/Java/jdk-23" mvn -q -f backend/pom.xml test -Dtest=OfficeRolesControllerTest,OfficeRolesControllerAuditoriaTest,AuditLogImutabilidadeTest` exits 0.
</verification>

<success_criteria>
- AUDT-01 satisfied for create/rename/delete, with the event in the same transaction
- No regression in existing OfficeRolesController behavior
</success_criteria>

<output>
Create `.planning/phases/LEXCV-128-auditoria-de-atribui-es-de-pap-is/128-03-SUMMARY.md` when done
</output>
