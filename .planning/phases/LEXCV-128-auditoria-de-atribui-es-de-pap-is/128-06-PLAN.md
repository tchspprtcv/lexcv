---
phase: 128-auditoria-de-atribui-es-de-pap-is
plan: 06
type: execute
wave: 3
depends_on: ["128-02"]
files_modified:
  - backend/src/main/java/com/lexcv/services/SetupService.java
  - backend/src/test/java/com/lexcv/services/SetupServiceAtribuicaoAdminFundadorTest.java
  - backend/src/test/java/com/lexcv/services/SetupServiceInstanciacaoMoldesTest.java
  - backend/src/test/java/com/lexcv/services/SetupServiceProvisionTenantTest.java
autonomous: true
requirements: [AUDT-02]

must_haves:
  truths:
    - "Provisioning a new office from the platform console records the founding administrator's role assignment as the office's first audit event"
    - "The provisioning event is written in provisionTenant's existing transaction"
    - "The first-run setup wizard (initializeSystem) and the Phase 126 boot conversion record no events, and the code says why"
  artifacts:
    - path: "backend/src/main/java/com/lexcv/services/SetupService.java"
      provides: "provisionTenant records papel_atribuir with motivo provisionamento"
      contains: "MOTIVO_PROVISIONAMENTO"
  key_links:
    - from: "SetupService.provisionTenant"
      to: "AuditoriaRbacService.registarAtribuicoes"
      via: "call after instanciarMoldesEAtribuirAdminFundador, autor null"
      pattern: "registarAtribuicoes"
---

<objective>
Resolve Decision 4's discretionary item: provisioning IS audited. `SetupService.provisionTenant` is already `@Transactional` (PATTERNS.md), so recording the founding administrator's assignment costs one call and gives each new office the first line of its history. The first-run wizard (`initializeSystem`) and the Phase 126 conversion (`MigracaoPapeisEscritorioService`) stay unaudited, with the reasons recorded in code.

Purpose: AUDT-02 coverage for the one real assignment made outside the office surface.
Output: modified SetupService and its three test constructors, plus new test cases.
</objective>

<execution_context>
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/LEXCV-128-auditoria-de-atribui-es-de-pap-is/128-CONTEXT.md
@.planning/phases/LEXCV-128-auditoria-de-atribui-es-de-pap-is/128-02-PLAN.md

<interfaces>
SetupService (read in full): fields in order systemSettingRepository, tenantRepository, userRepository, roleRepository, passwordEncoder, tenantRoleRepository.
  @Transactional public void initializeSystem(SetupInitializeRequest)   // public first-run /setup wizard, no authenticated actor
  @Transactional public Tenant provisionTenant(SetupInitializeRequest)  // called only by PlatformAdminController (PLATAFORMA_ADMIN)
  shared private helper instanciarMoldesEAtribuirAdminFundador(...) sets adminUser.setTenantRoles(Set.of(tenantRoleAdmin)) when the ADMIN molde exists
Tests constructing it: SetupServiceAtribuicaoAdminFundadorTest (~line 65), SetupServiceInstanciacaoMoldesTest (~line 77), SetupServiceProvisionTenantTest (~line 71): `new SetupService(`

From Plan 02:
  void registarAtribuicoes(UUID tenantId, UserPrincipal autor, User alvo, Set<TenantRole> antes, Set<TenantRole> depois, String motivo)
  static final String MOTIVO_PROVISIONAMENTO = "provisionamento"
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Audit the founding-administrator assignment in provisionTenant</name>
  <files>backend/src/main/java/com/lexcv/services/SetupService.java, backend/src/test/java/com/lexcv/services/SetupServiceAtribuicaoAdminFundadorTest.java, backend/src/test/java/com/lexcv/services/SetupServiceInstanciacaoMoldesTest.java, backend/src/test/java/com/lexcv/services/SetupServiceProvisionTenantTest.java</files>
  <read_first>
    - backend/src/main/java/com/lexcv/services/SetupService.java (full)
    - backend/src/main/java/com/lexcv/controllers/PlatformAdminController.java (the provisionTenant caller and its gate)
    - backend/src/main/java/com/lexcv/services/AuditoriaRbacService.java (Plan 02)
    - the three SetupService test files (constructor helper and provisionTenant cases)
  </read_first>
  <behavior>
    - provisionTenant with the ADMIN molde present → registarAtribuicoes called once with the new tenant's id, autor null, the founding admin user, antes empty, depois = the user's tenantRoles (the instantiated admin role), motivo "provisionamento"
    - provisionTenant with no ADMIN molde (tenantRoles stays empty) → registarAtribuicoes either not called or called with empty depois; assert that no assignment is recorded (the service no-ops on an empty diff)
    - initializeSystem → auditoriaRbacService never touched
    - Pre-existing SetupService tests still pass
  </behavior>
  <action>
Append `private final AuditoriaRbacService auditoriaRbacService;` as the LAST field. Update the three tests' `new SetupService(` calls to pass a new `@Mock AuditoriaRbacService` as the last argument.

In `provisionTenant`, after `instanciarMoldesEAtribuirAdminFundador` has run (the founding admin's `tenantRoles` are final), call `auditoriaRbacService.registarAtribuicoes(tenant.getId(), null, adminUser, Set.of(), adminUser.getTenantRoles(), AuditoriaRbacService.MOTIVO_PROVISIONAMENTO)`. Use the actual local variable names found in the method. Doc-comment (Portuguese):
- this implements Decision 4's discretionary item, decided YES because provisionTenant is already @Transactional and the event joins it (MANDATORY propagation);
- the actor is recorded as null (a system event, rendered "A plataforma atribuiu..." by the UI-SPEC template), not the PLATAFORMA_ADMIN user. That user belongs to the platform tenant, and storing a cross-tenant user id or name in an office's log would leak platform identity into office data;
- initializeSystem is deliberately NOT audited: it is the public first-run wizard of the installation, with no authenticated actor, the same category as the Phase 126 boot conversion (Decision 4: a system bootstrap, not a decision by a person);
- MigracaoPapeisEscritorioService is untouched on purpose (Decision 4).

Do not add any call in `initializeSystem` or in the shared helper. The helper is shared with initializeSystem, which is exactly why the call lives in provisionTenant.

Add the new cases from the behavior list to `SetupServiceProvisionTenantTest`, and the initializeSystem `verifyNoInteractions` case to `SetupServiceAtribuicaoAdminFundadorTest` (it already exercises initializeSystem; otherwise put it in whichever of the three files already calls initializeSystem).
  </action>
  <verify>
    <automated>JAVA_HOME="/c/Program Files/Java/jdk-23" mvn -q -f backend/pom.xml test -Dtest=SetupService*Test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -F "MOTIVO_PROVISIONAMENTO" backend/src/main/java/com/lexcv/services/SetupService.java` matches
    - `grep -c -F "auditoriaRbacService.registarAtribuicoes" backend/src/main/java/com/lexcv/services/SetupService.java` equals 1
    - `grep -F "initializeSystem" backend/src/main/java/com/lexcv/services/SetupService.java` still matches and the executor confirms by reading that no audit call sits inside it
    - `git diff --stat -- backend/src/main/java/com/lexcv/services/MigracaoPapeisEscritorioService.java` is empty (Phase 126 conversion untouched, Decision 4)
    - `JAVA_HOME="/c/Program Files/Java/jdk-23" mvn -q -f backend/pom.xml test -Dtest=SetupService*Test` exits 0
  </acceptance_criteria>
  <done>Provisioning recorded in-transaction with a system actor; wizard and boot conversion provably silent; all SetupService tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| platform (PLATAFORMA_ADMIN) → new office | Platform action whose result becomes the office's first audit row |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-128-29 | Information Disclosure | provisioning event actor | mitigate | autor null: no platform user id or name is written into an office's log |
| T-128-30 | Repudiation | platform-side accountability for provisioning | accept | The platform console's own tenant creation is outside the office log by design (Phase Boundary: platform events have no tenant); recorded as a known limit |
| T-128-31 | Tampering | provisioning event without the tenant (partial provisioning) | mitigate | Written inside provisionTenant's existing @Transactional; MANDATORY propagation |
</threat_model>

<verification>
`JAVA_HOME="/c/Program Files/Java/jdk-23" mvn -q -f backend/pom.xml test -Dtest=SetupService*Test,AuditoriaRbacServiceTest` exits 0.
</verification>

<success_criteria>
- Decision 4's discretionary item decided (yes), implemented, and justified in code
- No event from initializeSystem or the Phase 126 conversion
</success_criteria>

<output>
Create `.planning/phases/LEXCV-128-auditoria-de-atribui-es-de-pap-is/128-06-SUMMARY.md` when done
</output>
