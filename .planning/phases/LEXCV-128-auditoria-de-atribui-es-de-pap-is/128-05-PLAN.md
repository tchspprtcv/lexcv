---
phase: 128-auditoria-de-atribui-es-de-pap-is
plan: 05
type: execute
wave: 4
depends_on: ["128-04"]
files_modified:
  - backend/src/main/java/com/lexcv/controllers/AdminController.java
  - backend/src/test/java/com/lexcv/controllers/AdminControllerAtribuicaoPapeisEscritorioTest.java
  - backend/src/test/java/com/lexcv/controllers/AdminControllerLimiteUtilizadoresTest.java
  - backend/src/test/java/com/lexcv/controllers/AdminControllerPlataformaAdminContencaoTest.java
  - backend/src/test/java/com/lexcv/controllers/AdminControllerRbacAutorizacaoTest.java
  - backend/src/test/java/com/lexcv/controllers/AdminControllerRbacCatalogoTest.java
  - backend/src/test/java/com/lexcv/controllers/AdminControllerRbacEscritorioTest.java
  - backend/src/test/java/com/lexcv/controllers/AdminControllerUltimoAdministradorTest.java
  - backend/src/test/java/com/lexcv/controllers/AdminControllerTransacaoTest.java
  - backend/src/test/java/com/lexcv/controllers/AdminControllerAuditoriaTest.java
autonomous: true
requirements: [AUDT-01, AUDT-02]

must_haves:
  truths:
    - "Changing a role's permissions records one papel_permissoes_alterar per role whose set actually changed, listing added and removed keys"
    - "Creating a user records one papel_atribuir per assigned role, naming the target user"
    - "Updating a user's roles records papel_atribuir for each added and papel_retirar for each removed role; unchanged roles and non-role edits record nothing"
    - "Deleting a user records papel_retirar for each role they held, marked utilizador_eliminado, with the target's name snapshotted before the hard delete"
    - "Refused requests (409 last administrator, 403 PLATAFORMA_ADMIN, 404 other tenant, 400 validation) record nothing"
    - "Every event is recorded inside the handler's transaction; an event failure rolls the change back"
  artifacts:
    - path: "backend/src/main/java/com/lexcv/controllers/AdminController.java"
      provides: "Audited updateRbac/createUser/updateUser/deleteUser"
      contains: "auditoriaRbacService.registarAtribuicoes"
    - path: "backend/src/test/java/com/lexcv/controllers/AdminControllerAuditoriaTest.java"
      provides: "AUDT-01/AUDT-02 proof for the four handlers"
      min_lines: 150
  key_links:
    - from: "AdminController.updateUser"
      to: "AuditoriaRbacService.registarAtribuicoes"
      via: "papeisAntes snapshot vs saved user.getTenantRoles()"
      pattern: "papeisAntes"
    - from: "AdminController.deleteUser"
      to: "AuditoriaRbacService.MOTIVO_UTILIZADOR_ELIMINADO"
      via: "registarAtribuicoes(..., papeisAntes, Set.of(), MOTIVO_UTILIZADOR_ELIMINADO)"
      pattern: "MOTIVO_UTILIZADOR_ELIMINADO"
---

<objective>
Record the RBAC audit events inside the four `AdminController` transactions that Plan 04 created: permission changes (AUDT-01), and assignment, removal and delete-driven removal (AUDT-02). Update every test that constructs `AdminController` for the new trailing constructor argument.

Purpose: completes Decision 3's write-path coverage. OfficeRolesController is Plan 03; provisioning is Plan 06.
Output: instrumented controller, eight updated test files, one new test class.
</objective>

<execution_context>
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/LEXCV-128-auditoria-de-atribui-es-de-pap-is/128-CONTEXT.md
@.planning/phases/LEXCV-128-auditoria-de-atribui-es-de-pap-is/128-02-PLAN.md
@.planning/phases/LEXCV-128-auditoria-de-atribui-es-de-pap-is/128-04-PLAN.md

<interfaces>
From Plan 02 (AuditoriaRbacService):
  void registarPermissoesAlteradas(UUID tenantId, UserPrincipal autor, TenantRole papel, Set<String> adicionadas, Set<String> removidas)  // no-op when both empty
  void registarAtribuicoes(UUID tenantId, UserPrincipal autor, User alvo, Set<TenantRole> antes, Set<TenantRole> depois, String motivo) // diff by TenantRole id
  static final String MOTIVO_UTILIZADOR_ELIMINADO = "utilizador_eliminado"

AdminController after Plan 04: the four handlers are @Transactional, every refusal goes through recusar(...), constructor is still
  (userRepository, roleRepository, permissionRepository, passwordEncoder, tenantRepository, resolucaoPapeisService, tenantRoleRepository)

Every test that constructs AdminController positionally (all must gain a trailing AuditoriaRbacService mock):
  AdminControllerAtribuicaoPapeisEscritorioTest, AdminControllerLimiteUtilizadoresTest, AdminControllerPlataformaAdminContencaoTest,
  AdminControllerRbacAutorizacaoTest, AdminControllerRbacCatalogoTest, AdminControllerRbacEscritorioTest,
  AdminControllerUltimoAdministradorTest, AdminControllerTransacaoTest (created by Plan 04)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire AuditoriaRbacService into AdminController and record events in the four handlers</name>
  <files>backend/src/main/java/com/lexcv/controllers/AdminController.java, backend/src/test/java/com/lexcv/controllers/AdminControllerAtribuicaoPapeisEscritorioTest.java, backend/src/test/java/com/lexcv/controllers/AdminControllerLimiteUtilizadoresTest.java, backend/src/test/java/com/lexcv/controllers/AdminControllerPlataformaAdminContencaoTest.java, backend/src/test/java/com/lexcv/controllers/AdminControllerRbacAutorizacaoTest.java, backend/src/test/java/com/lexcv/controllers/AdminControllerRbacCatalogoTest.java, backend/src/test/java/com/lexcv/controllers/AdminControllerRbacEscritorioTest.java, backend/src/test/java/com/lexcv/controllers/AdminControllerUltimoAdministradorTest.java, backend/src/test/java/com/lexcv/controllers/AdminControllerTransacaoTest.java</files>
  <read_first>
    - backend/src/main/java/com/lexcv/controllers/AdminController.java (full, as left by Plan 04)
    - backend/src/main/java/com/lexcv/services/AuditoriaRbacService.java (Plan 02)
    - each of the eight test files listed above, at least the constructor helper (grep `new AdminController(`)
  </read_first>
  <action>
Constructor: append `private final AuditoriaRbacService auditoriaRbacService;` as the LAST field, after `tenantRoleRepository`, with the same one-line rationale comment style used for `tenantRoleRepository` (appended so positional test constructors only gain a trailing argument). In each of the eight test files, add `@Mock private AuditoriaRbacService auditoriaRbacService;` and pass it as the eighth constructor argument. No other change to those tests in this task.

updateRbac (AUDT-01): inside the existing validation loop, after the floor checks pass and before `resolvido.put`, compute the added and removed permission keys for that role from its CURRENT `tenantRole.getPermissions()` versus `permissoesResolvidas`, before any `setPermissions`. Keep them in a parallel map keyed by TenantRole, or in a small private record. In the write loop, after `tenantRoleRepository.save(tenantRole)`, call `auditoriaRbacService.registarPermissoesAlteradas(principal.getTenantId(), principal, tenantRole, adicionadas, removidas)`. The service ignores empty diffs, so a full-matrix PUT that changes one role records exactly one event.

createUser (AUDT-02): after `user = userRepository.save(user)`, call `auditoriaRbacService.registarAtribuicoes(principal.getTenantId(), principal, user, Set.of(), user.getTenantRoles(), null)`.

updateUser (AUDT-02): immediately after the tenant check (the same place `detentorOriginal` is computed, before ANY mutation), snapshot `Set<TenantRole> papeisAntes = new HashSet<>(user.getTenantRoles())`. After `user = userRepository.save(user)`, and only when `body.containsKey("tenantRoleIds")`, call `auditoriaRbacService.registarAtribuicoes(principal.getTenantId(), principal, user, papeisAntes, user.getTenantRoles(), null)`. Deactivation, name, email, password and "permissions" edits are not role assignments and record nothing (Decision 3 scope). Say so in a comment.

deleteUser (AUDT-02, Decision 3): after the last-administrator guard passes and before `userRepository.deleteById(id)`, snapshot `Set<TenantRole> papeisAntes = new HashSet<>(user.getTenantRoles())`. After `deleteById`, call `auditoriaRbacService.registarAtribuicoes(principal.getTenantId(), principal, user, papeisAntes, Set.of(), AuditoriaRbacService.MOTIVO_UTILIZADOR_ELIMINADO)`. Comment: this is a hard delete, so the in-memory `user` still carries the id and nome that the service snapshots into detalhe (revised Decision 2), and without these events the history would keep an assignment with no end. A user holding no roles produces no event.

Every call uses `principal` from SecurityContextHolder for both the tenant and the actor, never a body value. No call is placed on a refusal path.
  </action>
  <verify>
    <automated>JAVA_HOME="/c/Program Files/Java/jdk-23" mvn -q -f backend/pom.xml test -Dtest=AdminController*Test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -F "private final AuditoriaRbacService auditoriaRbacService" backend/src/main/java/com/lexcv/controllers/AdminController.java` matches
    - `grep -c -F "auditoriaRbacService.registarAtribuicoes" backend/src/main/java/com/lexcv/controllers/AdminController.java` equals 3
    - `grep -c -F "auditoriaRbacService.registarPermissoesAlteradas" backend/src/main/java/com/lexcv/controllers/AdminController.java` equals 1
    - `grep -F "MOTIVO_UTILIZADOR_ELIMINADO" backend/src/main/java/com/lexcv/controllers/AdminController.java` matches
    - For each of the eight test files: `grep -F "AuditoriaRbacService" <file>` matches
    - `JAVA_HOME="/c/Program Files/Java/jdk-23" mvn -q -f backend/pom.xml test -Dtest=AdminController*Test` exits 0 (all pre-existing tests still pass)
  </acceptance_criteria>
  <done>All four handlers record events only on success paths; every test that constructs AdminController compiles and passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: AUDT-01/AUDT-02 behavior proof for AdminController</name>
  <files>backend/src/test/java/com/lexcv/controllers/AdminControllerAuditoriaTest.java</files>
  <read_first>
    - backend/src/test/java/com/lexcv/controllers/AdminControllerTransacaoTest.java (Plan 04: proxy + principal fixtures)
    - backend/src/test/java/com/lexcv/controllers/AdminControllerAtribuicaoPapeisEscritorioTest.java (fixtures for tenantRoleIds resolution)
    - backend/src/test/java/com/lexcv/controllers/AdminControllerRbacEscritorioTest.java (fixtures for updateRbac and the two-tenant idiom)
  </read_first>
  <behavior>
    - updateRbac with roles R1 (adds "financeiro:view") and R2 (unchanged) → registarPermissoesAlteradas called for R1 with adicionadas={"financeiro:view"}, removidas={}; for R2 it is called with both empty (the service no-ops); assert on the arguments, not the count of saved rows
    - updateRbac floor refusal (409) → auditoriaRbacService never touched
    - createUser with tenantRoleIds [A] → registarAtribuicoes(principalTenant, principal, savedUser, empty, {A}, null)
    - updateUser swapping roles {A,B} → {B,C} → registarAtribuicoes receives antes={A,B} (the pre-mutation snapshot, not the mutated collection) and depois={B,C}
    - updateUser changing only "nome" → registarAtribuicoes never called
    - updateUser tenantRoleIds containing a PLATAFORMA_ADMIN role → 403, no audit; containing a foreign-tenant id → 404, no audit
    - updateUser removing the protected role from the last administrator → 409, no audit
    - deleteUser of a user holding {A,B} → registarAtribuicoes(tenant, principal, user, {A,B}, empty, "utilizador_eliminado"), called after deleteById (InOrder)
    - deleteUser of a foreign-tenant id → 404, no audit
    - Through a TransactionInterceptor proxy: createUser where registarAtribuicoes throws → rollback called, commit never
  </behavior>
  <action>
Create `AdminControllerAuditoriaTest` (Mockito). Use an ArgumentCaptor on `Set<TenantRole>` to assert `antes`/`depois` by ids. Put the transactional-rollback case through the ProxyFactory + TransactionInterceptor technique from Plan 04's test. Test names in Portuguese, in the existing style.
  </action>
  <verify>
    <automated>JAVA_HOME="/c/Program Files/Java/jdk-23" mvn -q -f backend/pom.xml test -Dtest=AdminControllerAuditoriaTest</automated>
  </verify>
  <acceptance_criteria>
    - `grep -F "utilizador_eliminado" backend/src/test/java/com/lexcv/controllers/AdminControllerAuditoriaTest.java` matches
    - `grep -F "verifyNoInteractions(auditoriaRbacService)" backend/src/test/java/com/lexcv/controllers/AdminControllerAuditoriaTest.java` matches
    - `JAVA_HOME="/c/Program Files/Java/jdk-23" mvn -q -f backend/pom.xml test -Dtest=AdminControllerAuditoriaTest` exits 0 with at least 11 tests
  </acceptance_criteria>
  <done>Every bullet proven; refusal paths proven silent; event failure proven to roll back the change.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| users:manage / rbac:manage holder → assignment and matrix writes | Untrusted tenantRoleIds, permission keys, path ids |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-128-24 | Repudiation | assignment/removal/permission changes | mitigate | Event recorded inside the @Transactional handler after the domain save; rollback on event failure tested |
| T-128-25 | Repudiation | deleteUser hard delete erasing history | mitigate | papel_retirar per held role with motivo utilizador_eliminado and a snapshotted alvoNome, written in the same transaction as the delete |
| T-128-26 | Spoofing | forged actor/tenant | mitigate | tenant and actor from the principal only; foreign ids 404 before any audit (tested) |
| T-128-27 | Tampering | refused requests leaving events | mitigate | No registar* call on any refusal path; verifyNoInteractions on 403/404/409 cases |
| T-128-28 | Information Disclosure | detalhe content | mitigate | Controller passes entities; the service extracts only User.nome (Plan 02 privacy test) |
</threat_model>

<verification>
`JAVA_HOME="/c/Program Files/Java/jdk-23" mvn -q -f backend/pom.xml test -Dtest=AdminController*Test,AuditoriaRbacServiceTest,AuditLogImutabilidadeTest` exits 0.
</verification>

<success_criteria>
- AUDT-01 (permission changes) and AUDT-02 (assign/remove/delete-driven removal) recorded with actor, moment, target and change
- No pre-existing AdminController test broken
</success_criteria>

<output>
Create `.planning/phases/LEXCV-128-auditoria-de-atribui-es-de-pap-is/128-05-SUMMARY.md` when done
</output>
