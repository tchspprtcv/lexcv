---
phase: 105-m-dulos-clientes-processos-combinados
plan: 03
type: execute
wave: 2
depends_on: [105-02]
files_modified:
  - web/src/app/(dashboard)/processos/[id]/page.tsx
  - web/src/app/(dashboard)/processos/[id]/documentos-columns.tsx
autonomous: true
requirements: [CLP-02, CLP-04]
must_haves:
  truths:
    - "The Partes, Fases and Testemunhas tabs render through the reconciled Table/TableHeader/TableBody/TableRow/TableHead/TableCell primitives — no raw <table>/<thead>/<tbody>/<td> markup remains in the file"
    - "The Fases tab's inline NativeSelect (migrated in 105-02) and its Guardar Button, plus all existing 'Adicionar' Dialogs / RBAC / inline-edit / per-row delete logic on Partes/Fases/Testemunhas, are preserved 1:1 — only the table scaffolding changed"
    - "The Testemunhas 'Nome' cell shows an Avatar size=sm with derived initials before the name; Tipo/Contacto/Ações cells are unchanged. Partes are NOT given an Avatar (excluded by CLP-04)"
    - "The Documentos tab renders via the shared <DataTable> (Phase 104 pattern) fed by a new documentos-columns.tsx, replacing the <ul>/<li> ProcessoDocumentoRow list; the outer isLoading/isError/empty guards are preserved"
    - "documentos-columns.tsx models on documentos/columns.tsx but drops the redundant 'Processo' column (every row is already this processo); the ProcessoDocumentoRow tamanho/createdAt wire-shape workaround is carried into the new Ações/size cells"
  artifacts:
    - path: "web/src/app/(dashboard)/processos/[id]/documentos-columns.tsx"
      provides: "columns(canEditDocumentos: boolean): ColumnDef<Documento>[] factory for the processo-scoped Documentos DataTable"
      contains: "ColumnDef<Documento>"
    - path: "web/src/app/(dashboard)/processos/[id]/page.tsx"
      provides: "Partes/Fases/Testemunhas on Table primitives + Testemunhas Avatar + Documentos DataTable"
      contains: "<DataTable"
  key_links:
    - from: "web/src/app/(dashboard)/processos/[id]/page.tsx (ProcessoDocumentosTab)"
      to: "documentos-columns.tsx"
      via: "<DataTable columns={columns(canEditDocumentos)} data={documentos} getRowId={(d) => d.id} />"
      pattern: "<DataTable"
    - from: "web/src/app/(dashboard)/processos/[id]/documentos-columns.tsx"
      to: "existing useDocumentos wire shape"
      via: "confidencialidadeVariant() mapping reused verbatim + tamanho/createdAt workaround"
      pattern: "confidencialidadeVariant"
---

<objective>
Complete the Ficha de Processo migration (Wave 2, after 105-02 established the `TabsContent` wrappers): convert the Partes/Fases/Testemunhas tabs from raw `<table>` markup to the reconciled `Table` primitives, add `Avatar` to the Testemunhas "Nome" cell, and replace the Documentos tab's `<ul>` list with the shared `<DataTable>` fed by a new `documentos-columns.tsx`. Shares `processos/[id]/page.tsx` with 105-02, so it runs in the wave after it (same-file dependency, not parallelizable).

Purpose: Close the user-authorized scope expansion of CLP-02 (Partes/Fases -> Table primitives; Documentos -> DataTable) and the Testemunhas half of CLP-04 (Avatar). These are explicit CONTEXT.md expansions beyond the literal CLP text.
Output: `processos/[id]/page.tsx` inner content migrated + new `documentos-columns.tsx`; `pnpm build` green.
</objective>

<execution_context>
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/LEXCV-105-m-dulos-clientes-processos-combinados/105-CONTEXT.md
@.planning/phases/LEXCV-105-m-dulos-clientes-processos-combinados/105-PATTERNS.md
@.planning/phases/LEXCV-105-m-dulos-clientes-processos-combinados/105-UI-SPEC.md
@web/src/app/(dashboard)/processos/[id]/page.tsx
@web/src/app/(dashboard)/documentos/columns.tsx
@web/src/components/shared/data-table/data-table.tsx
@web/src/components/ui/table.tsx
@web/src/components/ui/avatar.tsx

<interfaces>
<!-- Full before/after excerpts + the confidencialidadeVariant()/DocumentoAcoesCell/imports patterns live in 105-PATTERNS.md Shared Patterns 3,4,6. Use them directly. -->
- Table: `Table, TableHeader, TableBody, TableRow, TableHead, TableCell` from "@/components/ui/table". Table self-wraps in `<div className="w-full overflow-auto">`. TableHead = `h-12 px-4 text-left font-semibold`; TableCell = `p-4 align-middle`. NOT yet imported in processos/[id]/page.tsx — add the import.
- DataTable: `DataTable` from "@/components/shared/data-table/data-table". Signature `<DataTable columns={...} data={...} getRowId={(d) => d.id} />`. Configures core/sorted/pagination row models only — NO getFilteredRowModel (filtering stays server-side). pageSize 10; zero-row fallback copy "Sem resultados para os filtros aplicados."
- New file factory signature: `columns(canEditDocumentos: boolean): ColumnDef<Documento>[]` — model on documentos/columns.tsx's `columns()` factory (lines 116-231) MINUS the `id: "processo"` block (lines 149-169). Reuse `confidencialidadeVariant()` (lines 28-43) and the DocumentoAcoesCell shape (lines 53-103) verbatim, but port ProcessoDocumentoRow's tamanho/createdAt wire-shape workaround (page lines 2693-2701) into the size/date accessors.
- Avatar: `Avatar, AvatarFallback` from "@/components/ui/avatar", size="sm". deriveInitials logic: `nome.split(" ").filter(Boolean).slice(0,2).map(p=>p[0]).join("").toUpperCase()`.
- RBAC: canEditDocumentos already computed at line 231 and passed to ProcessoDocumentosTab (line 2292); ProcessoDocumentoRow currently uses it (lines 2685-2744).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: New documentos-columns.tsx + swap ProcessoDocumentosTab ul -> DataTable</name>
  <files>web/src/app/(dashboard)/processos/[id]/documentos-columns.tsx, web/src/app/(dashboard)/processos/[id]/page.tsx</files>
  <read_first>
    - 105-PATTERNS.md Shared Pattern 6 (imports, confidencialidadeVariant, DocumentoAcoesCell, column set, page-level swap) — the complete recipe.
    - documentos/columns.tsx (231 lines, Phase 104 analog) — the exact same-role factory; copy its shape, drop the Processo column.
    - processos/[id]/page.tsx: ProcessoDocumentosTab (~lines 2664-2677) and ProcessoDocumentoRow (~lines 2683-2757) incl. its `useDocumentos({ processo_id: processoId })` usage (~line 2521) and the tamanho/createdAt wire-shape workaround (~lines 2693-2701).
    - data-table.tsx — confirm the call signature and that no filtering model is added.
  </read_first>
  <action>
    Create `web/src/app/(dashboard)/processos/[id]/documentos-columns.tsx` (name at discretion; this name is used by the verify gate). Model it directly on `documentos/columns.tsx`: `"use client"`, import `ColumnDef` from "@tanstack/react-table" (already a Phase-104 dependency, no install), `Badge`, `Button`, `Tooltip`/`TooltipContent`/`TooltipTrigger`, `DataTableColumnHeader`, `useDeleteDocumento` + `toast`, and the `Documento` type. Copy `confidencialidadeVariant()` VERBATIM (no new Badge color). Export a factory `columns(canEditDocumentos: boolean): ColumnDef<Documento>[]`. Column set: Nome, Tipo, Cliente, Confid., Versão, Tamanho, Criado, Ações — DROP the `id: "processo"` column (redundant: every row is already this processo). For the Ações + Tamanho + Criado cells, port ProcessoDocumentoRow's existing download/delete/window.confirm handlers and its `tamanho`/`createdAt` wire-shape workaround (read `tamanho ?? 0` and `createdAt` off the wire object) rather than re-deriving from documentos/columns.tsx, since the top-level list uses `size`/`created_at`. Keep the `DocumentoAcoesCell`-style per-row component (a small component so the delete hook is called per row, not inline in the column def). The "Cliente" column may stay (resolve like documentos/columns.tsx lines 170-184 if the Documento carries cliente_id) or be dropped — discretion; if unsure, keep it for parity with the top-level list.

    In `processos/[id]/page.tsx`: add imports `DataTable` from "@/components/shared/data-table/data-table" and `{ columns }` from "./documentos-columns". In `ProcessoDocumentosTab`, replace the `<ul className="space-y-1">{documentos.map(...ProcessoDocumentoRow...)}</ul>` branch with `<DataTable columns={columns(canEditDocumentos)} data={documentos} getRowId={(d) => d.id} />`. PRESERVE the surrounding guards exactly: the `list.isLoading` ("A carregar..."), `list.isError` (red error text), and `documentos.length === 0` ("Nenhum documento registado.") branches stay unchanged — only the final populated branch swaps `<ul>` for `<DataTable>`. Keep the existing `useDocumentos({ processo_id: processoId })` call unchanged (no new hook). Remove the now-unused `ProcessoDocumentoRow` component only if nothing else references it (grep first); if it is still referenced elsewhere, leave it.
  </action>
  <verify>
    <automated>cd web && test -f "src/app/(dashboard)/processos/[id]/documentos-columns.tsx" && grep -q "ColumnDef<Documento>" "src/app/(dashboard)/processos/[id]/documentos-columns.tsx" && grep -q "confidencialidadeVariant" "src/app/(dashboard)/processos/[id]/documentos-columns.tsx" && grep -c 'id: "processo"' "src/app/(dashboard)/processos/[id]/documentos-columns.tsx" | grep -qx 0 && grep -q "<DataTable" "src/app/(dashboard)/processos/[id]/page.tsx" && grep -q "Nenhum documento registado" "src/app/(dashboard)/processos/[id]/page.tsx" && echo GATE_PASS</automated>
  </verify>
  <acceptance_criteria>
    - `documentos-columns.tsx` exists, exports `columns(canEditDocumentos)` returning `ColumnDef<Documento>[]`, reuses `confidencialidadeVariant()`, and has NO `id: "processo"` column.
    - ProcessoDocumentosTab renders `<DataTable columns={columns(canEditDocumentos)} data={documentos} getRowId={(d) => d.id} />` in its populated branch.
    - The isLoading / isError / empty ("Nenhum documento registado.") guards are still present and unchanged.
    - The tamanho/createdAt wire-shape workaround is present in the new cells (size/date do not silently read the wrong wire keys).
  </acceptance_criteria>
  <done>Processo Documentos tab is a shared DataTable via a new columns factory (Processo column dropped, wire-shape workaround carried), guards preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Partes/Fases/Testemunhas raw table -> Table primitives + Testemunhas Avatar</name>
  <files>web/src/app/(dashboard)/processos/[id]/page.tsx</files>
  <read_first>
    - 105-PATTERNS.md Shared Pattern 4 (raw <table> -> Table, incl. the ClienteProcessosTab precedent and the "keep current styling, only swap markup" lock) and Pattern 3 (Avatar in Testemunhas Nome cell).
    - 105-UI-SPEC.md Component Inventory CLP-02 expanded (Partes/Fases 3-column, no sort/paginate — NOT a DataTable) and CLP-04 (Avatar entities; Partes excluded).
    - processos/[id]/page.tsx: Partes tab table (~lines 1653-1675), Fases tab table (~lines 1744-1792, incl. the inline NativeSelect at ~1762 and Guardar Button ~1777-1786), Testemunhas tab table (~lines 2244-2287, Nome cell ~line 2259). These now sit inside the `TabsContent` wrappers 105-02 created.
  </read_first>
  <action>
    Add import: `Table, TableHeader, TableBody, TableRow, TableHead, TableCell` from "@/components/ui/table" (NOT currently imported in this file). If a `deriveInitials` helper is not already present in the file, add the same one used elsewhere this phase (`nome.split(" ").filter(Boolean).slice(0,2).map(p=>p[0]).join("").toUpperCase()`).

    For EACH of the three tables (Partes, Fases, Testemunhas): swap `<table>`->`<Table>`, `<thead>`->`<TableHeader>`, `<tr>`->`<TableRow>`, `<th>`->`<TableHead>`, `<tbody>`->`<TableBody>`, `<td>`->`<TableCell>`. This is a markup-only swap (CONTEXT lock: "only the table markup changes") — preserve the existing outer `overflow-x-auto -mx-4 px-4 sm:mx-0 sm:px-0` wrapper div (harmless nesting with Table's own overflow), preserve `min-w-[400px]` on the Table, and preserve ALL existing logic verbatim: the Fases inline NativeSelect (from 105-02) and its Guardar Button inside their TableCell, every "Adicionar Parte"/"Adicionar Fase"/"Adicionar Testemunha" Dialog, all RBAC gates, inline-edit state, and per-row `window.confirm()` delete handlers. Do NOT convert any of these to a full DataTable (no sort/paginate/toolbar) and do NOT migrate the per-row delete confirm to AlertDialog (both out of scope, locked).

    Testemunhas Avatar: in the Testemunhas "Nome" `TableCell` only, replace the bare `{t.nome}` with `<div className="flex items-center gap-2"><Avatar size="sm"><AvatarFallback>{deriveInitials(t.nome)}</AvatarFallback></Avatar><span className="font-medium">{t.nome}</span></div>`. Import `Avatar, AvatarFallback` from "@/components/ui/avatar". Leave the Tipo/Contacto/Ações TableCells unchanged. Do NOT add an Avatar to Partes (CLP-04 excludes it).
  </action>
  <verify>
    <automated>cd web && grep -c "<table" "src/app/(dashboard)/processos/[id]/page.tsx" | grep -qx 0 && grep -c "<thead" "src/app/(dashboard)/processos/[id]/page.tsx" | grep -qx 0 && grep -c "<tbody" "src/app/(dashboard)/processos/[id]/page.tsx" | grep -qx 0 && grep -q "TableHeader" "src/app/(dashboard)/processos/[id]/page.tsx" && grep -q "AvatarFallback" "src/app/(dashboard)/processos/[id]/page.tsx" && grep -q "window.confirm" "src/app/(dashboard)/processos/[id]/page.tsx" && pnpm build && echo GATE_PASS</automated>
  </verify>
  <acceptance_criteria>
    - Zero raw `<table`, `<thead`, `<tbody` remain in the file (all 3 tables migrated to Table primitives).
    - `TableHeader` present; the Fases NativeSelect + Guardar Button and all Adicionar Dialogs / RBAC / inline-edit / `window.confirm` delete logic still present (gate asserts window.confirm survives).
    - Testemunhas Nome cell renders an Avatar + name; Partes has NO Avatar.
    - `pnpm build` completes with no new type errors.
  </acceptance_criteria>
  <done>Partes/Fases/Testemunhas use reconciled Table primitives with all interaction logic intact; Testemunhas Nome cell has an Avatar; `pnpm build` green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser -> Next proxy -> Spring API | Unchanged: the Documentos DataTable reuses the existing useDocumentos({ processo_id }) hook; delete/download reuse existing handlers. No new endpoint, input, or filter surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-105-03 | Elevation of Privilege | Documentos delete/edit actions in the new columns factory | mitigate | Carry the existing `canEditDocumentos` gate into the new Ações cell exactly as ProcessoDocumentoRow enforced it — no action button renders/fires when canEditDocumentos is false. |
| T-105-03b | Tampering | DataTable client-side sort/paginate | accept | Sorting/pagination are presentation-only over data already tenant+processo-scoped by the backend; no getFilteredRowModel added, so no client-side authorization bypass surface. |
| T-105-SC | Tampering | package installs | accept | Zero installs — @tanstack/react-table + shared DataTable were vetted/added in Phase 104; Table/Avatar in Phase 101. |
</threat_model>

<verification>
- `cd web && pnpm build` passes.
- `cd web && pnpm lint` reports no new errors.
- Task grep gates pass (no raw table markup; DataTable wired; delete guard survives).
</verification>

<success_criteria>
- CLP-02 (expanded): Partes/Fases/Testemunhas on Table primitives; Documentos on shared DataTable via new columns factory.
- CLP-04 (Testemunhas): Avatar in Nome cell (Partes excluded).
- All existing Dialog/RBAC/inline-edit/delete logic preserved 1:1.
- `pnpm build` green.
</success_criteria>

<output>
Create `.planning/phases/LEXCV-105-m-dulos-clientes-processos-combinados/105-03-SUMMARY.md` when done.
</output>
