---
phase: 105-m-dulos-clientes-processos-combinados
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/app/(dashboard)/processos/page.tsx
  - web/src/app/(dashboard)/processos/novo/page.tsx
  - web/src/app/(dashboard)/processos/[id]/editar/page.tsx
autonomous: true
requirements: [CLP-03, CLP-05]
must_haves:
  truths:
    - "The 2 list-filter <select> on processos/page.tsx (draftEstado, draftClienteId) are NativeSelect size=default; the inline stylings are gone"
    - "The 4 RHF-bound <select> on processos/novo/page.tsx are NativeSelect size=default and its const selectClassName is deleted (textareaClassName kept); a net-new Breadcrumb (Processos -> Novo Processo) is added — but the <h1> font-bold is intentionally LEFT unchanged (out of scope per UI-SPEC note #4)"
    - "The 1 RHF-bound <select> on processos/[id]/editar/page.tsx is NativeSelect and its const selectClassName is deleted (textareaClassName kept); a net-new 3-level Breadcrumb (Processos -> {processo.data?.numero} link -> Editar) replaces the single-level 'Voltar ao detalhe' link"
    - "No unrelated font-bold usages anywhere in these files are touched; Voltar affordances are preserved"
  artifacts:
    - path: "web/src/app/(dashboard)/processos/novo/page.tsx"
      provides: "NativeSelect x4 + net-new 2-level Breadcrumb"
      contains: "BreadcrumbList"
    - path: "web/src/app/(dashboard)/processos/[id]/editar/page.tsx"
      provides: "NativeSelect + net-new 3-level Breadcrumb"
      contains: "BreadcrumbList"
  key_links:
    - from: "web/src/app/(dashboard)/processos/[id]/editar/page.tsx"
      to: "useProcesso(id) already loaded (line ~50)"
      via: "3-level Breadcrumb middle crumb links back to /processos/{id} using processo.data?.numero — no new fetch"
      pattern: "processo.data\\?.numero"
---

<objective>
Migrate the three Processos secondary pages — the list page (`processos/page.tsx`), the create form (`processos/novo/page.tsx`), and the edit form (`processos/[id]/editar/page.tsx`) — to `NativeSelect`, add a net-new 2-level `Breadcrumb` to the create form and a net-new 3-level `Breadcrumb` to the edit form. Self-contained files with zero overlap with the ficha plans, so this runs in Wave 1 in parallel. Note: `processos/[id]/editar/page.tsx` is a DIFFERENT file from the ficha's `processos/[id]/page.tsx` (owned by 105-02/03) — no conflict.

Purpose: Close the Processos-secondary share of CLP-03 (NativeSelect) and CLP-05 (Breadcrumb).
Output: 3 files migrated; `pnpm build` green.
</objective>

<execution_context>
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/LEXCV-105-m-dulos-clientes-processos-combinados/105-CONTEXT.md
@.planning/phases/LEXCV-105-m-dulos-clientes-processos-combinados/105-PATTERNS.md
@.planning/phases/LEXCV-105-m-dulos-clientes-processos-combinados/105-UI-SPEC.md
@web/src/app/(dashboard)/processos/page.tsx
@web/src/app/(dashboard)/processos/novo/page.tsx
@web/src/app/(dashboard)/processos/[id]/editar/page.tsx
@web/src/components/ui/native-select.tsx
@web/src/components/ui/breadcrumb.tsx

<interfaces>
<!-- Before/after excerpts in 105-PATTERNS.md Shared Patterns 2 (Variants B/C) and 5 (Breadcrumb net-new + 3-level). -->
- NativeSelect / Breadcrumb signatures: same as 105-04-PLAN.md's interfaces block.
- Copy contract (105-UI-SPEC.md): novo -> `Processos` (Link /processos) -> `Novo Processo`; editar -> `Processos` (Link /processos) -> `{processo.data?.numero ?? "…"}` (Link /processos/{id}) -> `Editar`.
- LOCK (UI-SPEC Scope note #4): processos/novo's `<h1 className="text-2xl font-bold text-slate-900 dark:text-white">` STAYS untouched — the h1-weight fix is scoped ONLY to the 2 detail pages, not the novo form. processos/[id]/editar's h1 is already font-semibold (no change; it has no text-slate-900 override to drop).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: processos/page.tsx list filters -> NativeSelect</name>
  <files>web/src/app/(dashboard)/processos/page.tsx</files>
  <read_first>
    - 105-PATTERNS.md Shared Pattern 2 Variant C (list-filter controlled selects).
    - processos/page.tsx: the 2 filter selects (`draftEstado` ~lines 235-246, `draftClienteId` ~lines 280-289).
  </read_first>
  <action>
    Add import `NativeSelect` from "@/components/ui/native-select". Replace each of the 2 filter `<select value={...} onChange={...} className="...">...</select>` with `<NativeSelect value={...} onChange={...} size="default">...</NativeSelect>`, keeping the `<option>` children unchanged and dropping the inline className (including any stray blue focus ring). This is a list page — no Breadcrumb scope, no `const selectClassName` to delete.
  </action>
  <verify>
    <automated>cd web && grep -c "<select" "src/app/(dashboard)/processos/page.tsx" | grep -qx 0 && grep -q "NativeSelect" "src/app/(dashboard)/processos/page.tsx" && echo GATE_PASS</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "<select"` returns 0; both filters are `<NativeSelect`.
    - No Breadcrumb added (out of scope for the list page).
  </acceptance_criteria>
  <done>Both Processos list filters are NativeSelect.</done>
</task>

<task type="auto">
  <name>Task 2: processos/novo/page.tsx -> NativeSelect (x4) + Breadcrumb (h1 left as-is)</name>
  <files>web/src/app/(dashboard)/processos/novo/page.tsx</files>
  <read_first>
    - 105-PATTERNS.md Shared Pattern 2 Variant B (RHF selectClassName) and Pattern 5 net-new Breadcrumb + the explicit note that this page's h1 stays font-bold.
    - processos/novo/page.tsx: the 4 RHF selects at ~lines 285, 310, 333, 540 (`className={selectClassName}`), the `const selectClassName` (line 35) + `const textareaClassName` (line 38, used at ~lines 422 & 558), the header block (~lines 201-209) with the `<h1 className="text-2xl font-bold text-slate-900 dark:text-white">Novo Processo</h1>`.
  </read_first>
  <action>
    Add imports `NativeSelect` and the Breadcrumb set. Replace all 4 `<select className={selectClassName} {...form.register("X")}>` with `<NativeSelect id="X" size="default" {...form.register("X")}>` (options unchanged). DELETE `const selectClassName` (line 35); KEEP `const textareaClassName` (line 38). Insert a net-new Breadcrumb near the header: `Processos` as `BreadcrumbLink asChild` -> `<Link href="/processos">`, `BreadcrumbSeparator`, `BreadcrumbPage`>`Novo Processo`. CRITICAL: leave the `<h1 className="text-2xl font-bold text-slate-900 dark:text-white">Novo Processo</h1>` EXACTLY as-is — do NOT change font-bold to font-semibold and do NOT drop text-slate-900 here (UI-SPEC Scope note #4: the h1-weight fix is scoped only to the 2 detail pages, this novo form is explicitly excluded). Leave the Voltar Button untouched.
  </action>
  <verify>
    <automated>cd web && grep -c "<select" "src/app/(dashboard)/processos/novo/page.tsx" | grep -qx 0 && grep -c "const selectClassName" "src/app/(dashboard)/processos/novo/page.tsx" | grep -qx 0 && grep -q "const textareaClassName" "src/app/(dashboard)/processos/novo/page.tsx" && grep -q "BreadcrumbList" "src/app/(dashboard)/processos/novo/page.tsx" && grep -q 'font-bold text-slate-900' "src/app/(dashboard)/processos/novo/page.tsx" && echo GATE_PASS</automated>
  </verify>
  <acceptance_criteria>
    - All 4 selects are NativeSelect; `const selectClassName` deleted; `const textareaClassName` retained.
    - Breadcrumb (Processos link -> Novo Processo) rendered.
    - The `<h1>` STILL reads `font-bold text-slate-900` (gate asserts it was NOT swept) — intentional out-of-scope preservation.
  </acceptance_criteria>
  <done>processos/novo has 4 NativeSelect selects, selectClassName removed, a net-new Breadcrumb, and its h1 deliberately unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: processos/[id]/editar/page.tsx -> NativeSelect + 3-level Breadcrumb</name>
  <files>web/src/app/(dashboard)/processos/[id]/editar/page.tsx</files>
  <read_first>
    - 105-PATTERNS.md Shared Pattern 2 Variant B and Pattern 5 "3-level upgrade" (before/after with the middle crumb linking back to /processos/{id} via processo.data?.numero).
    - processos/[id]/editar/page.tsx: the 1 RHF select (`cliente_id` ~line 142, `className={selectClassName}`), the `const selectClassName` (line 25) + `const textareaClassName` (line 28, used at ~line 267), the header block (~lines 111-118) with the single-level "Voltar ao detalhe" `<Link>`, and `const processo = useProcesso(id)` (~line 50, already loaded — exposes `processo.data?.numero`).
  </read_first>
  <action>
    Add imports `NativeSelect` and the Breadcrumb set. Replace the 1 `<select className={selectClassName} {...form.register("cliente_id")}>` with `<NativeSelect id="cliente_id" size="default" {...form.register("cliente_id")}>` (options unchanged). DELETE `const selectClassName` (line 25); KEEP `const textareaClassName` (line 28). Replace the single-level `<div className="text-sm ..."><Link href={`/processos/${id}`}>Voltar ao detalhe</Link></div>` with a 3-level Breadcrumb: `Processos` -> `<Link href="/processos">`; `BreadcrumbSeparator`; `{processo.data?.numero ?? "…"}` as `BreadcrumbLink asChild` -> `<Link href={`/processos/${encodeURIComponent(id)}`}>`; `BreadcrumbSeparator`; `BreadcrumbPage`>`Editar`. Use the already-loaded `useProcesso(id)` (no new fetch). The h1 is already `font-semibold` — no weight change; per UI-SPEC it has no `text-slate-900 dark:text-white` override to drop (verified: not present) so this is a no-op there.
  </action>
  <verify>
    <automated>cd web && grep -c "<select" "src/app/(dashboard)/processos/[id]/editar/page.tsx" | grep -qx 0 && grep -c "const selectClassName" "src/app/(dashboard)/processos/[id]/editar/page.tsx" | grep -qx 0 && grep -q "const textareaClassName" "src/app/(dashboard)/processos/[id]/editar/page.tsx" && grep -q "BreadcrumbList" "src/app/(dashboard)/processos/[id]/editar/page.tsx" && grep -q "processo.data?.numero" "src/app/(dashboard)/processos/[id]/editar/page.tsx" && pnpm build && echo GATE_PASS</automated>
  </verify>
  <acceptance_criteria>
    - The select is NativeSelect; `const selectClassName` deleted; `const textareaClassName` retained.
    - A 3-level Breadcrumb is present with the middle crumb linking to /processos/{id} via `processo.data?.numero`; the old "Voltar ao detalhe" single link is gone.
    - `pnpm build` completes with no new type errors across all 3 files in this plan.
  </acceptance_criteria>
  <done>processos/[id]/editar has a NativeSelect and a 3-level Breadcrumb reusing the already-loaded processo; `pnpm build` green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser -> Next proxy -> Spring API | Unchanged: select/breadcrumb swaps add no new input, fetch, or navigation target beyond existing routes (/processos, /processos/{id}). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-105-05 | Tampering | NativeSelect form values (novo/editar) | accept | NativeSelect renders the same real `<select>` with identical register bindings; server-side validation downstream unchanged. |
| T-105-SC | Tampering | package installs | accept | Zero installs — NativeSelect/Breadcrumb installed in Phase 101. |
</threat_model>

<verification>
- `cd web && pnpm build` passes.
- `cd web && pnpm lint` reports no new errors on the 3 files.
- Task grep gates pass (incl. the deliberate assertion that processos/novo's h1 font-bold survives).
</verification>

<success_criteria>
- CLP-03 (Processos secondary): all 7 selects across the 3 files are NativeSelect; selectClassName deleted where present.
- CLP-05 (Processos form/edit pages): net-new 2-level Breadcrumb on novo, net-new 3-level Breadcrumb on editar.
- processos/novo h1 intentionally unchanged; `pnpm build` green.
</success_criteria>

<output>
Create `.planning/phases/LEXCV-105-m-dulos-clientes-processos-combinados/105-05-SUMMARY.md` when done.
</output>
