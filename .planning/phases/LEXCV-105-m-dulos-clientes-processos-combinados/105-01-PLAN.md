---
phase: 105-m-dulos-clientes-processos-combinados
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/app/(dashboard)/clientes/[id]/page.tsx
autonomous: true
requirements: [CLP-01, CLP-03, CLP-04, CLP-05]
must_haves:
  truths:
    - "Ficha de Cliente tab bar renders via Tabs/TabsList/TabsTrigger/TabsContent (role=tablist, aria-selected) — the manual variant={tab===...} Button-toggle array is gone; the existing overflow-x-auto responsive wrapper is preserved"
    - "The 2 RBAC-gated tabs (processos, pareceres) omit their TabsTrigger entirely when canViewProcessos/canViewPareceres is false, and their TabsContent keeps its inner permission check (AccessDeniedState fallback) — content is never shown from tab state alone"
    - "All 6 native <select> in the file are NativeSelect size=default; the const selectClassName declaration is deleted; textareaClassName is left intact"
    - "Advogados and Administrativos rows rendered by ResponsaveisCard show an Avatar size=sm with derived initials before the name block, and the row's outer justify-between (name block vs Remover button) is preserved"
    - "The header nav is a Breadcrumb (Clientes link -> current cliente identifier via the existing fallback chain) replacing the <div>+Link+'/' block"
  artifacts:
    - path: "web/src/app/(dashboard)/clientes/[id]/page.tsx"
      provides: "Ficha de Cliente migrated to Tabs + NativeSelect + Avatar + Breadcrumb"
      contains: "TabsContent"
  key_links:
    - from: "web/src/app/(dashboard)/clientes/[id]/page.tsx"
      to: "existing tab useState"
      via: "Tabs value={tab} onValueChange={(v) => setTab(v as TabKey)} — controlled root over the unchanged tab/setTab state"
      pattern: "Tabs value=\\{tab\\}"
    - from: "web/src/app/(dashboard)/clientes/[id]/page.tsx"
      to: "canViewProcessos / canViewPareceres"
      via: "conditional TabsTrigger render (omit, not disable) + inner content permission gate"
      pattern: "canView(Processos|Pareceres)"
---

<objective>
Migrate the Ficha de Cliente (`clientes/[id]/page.tsx`, 2127 lines) from its manual `Button`-toggle tab bar to real `Tabs`, replace all native `<select>` with `NativeSelect`, add `Avatar` to the Advogados/Administrativos rows in `ResponsaveisCard`, and replace the ad hoc `<div>+Link+"/"` header with `Breadcrumb`. Single-file plan — this file is owned exclusively by this plan so it runs in parallel with the Processos ficha and the secondary-page plans.

Purpose: Close CLP-01 (Cliente Tabs accessibility), and the Cliente half of CLP-03 (NativeSelect), CLP-04 (Avatar), CLP-05 (Breadcrumb). Delivered in the same phase as the Processo ficha so the two are never left visibly inconsistent.
Output: `clientes/[id]/page.tsx` migrated; `pnpm build` green.
</objective>

<execution_context>
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/LEXCV-105-m-dulos-clientes-processos-combinados/105-CONTEXT.md
@.planning/phases/LEXCV-105-m-dulos-clientes-processos-combinados/105-PATTERNS.md
@.planning/phases/LEXCV-105-m-dulos-clientes-processos-combinados/105-UI-SPEC.md
@web/src/app/(dashboard)/clientes/[id]/page.tsx
@web/src/components/ui/tabs.tsx
@web/src/components/ui/native-select.tsx
@web/src/components/ui/avatar.tsx
@web/src/components/ui/breadcrumb.tsx

<interfaces>
<!-- Primitive contracts. Full before/after code excerpts live in 105-PATTERNS.md Shared Patterns 1,2,3,5. Use those directly — no codebase exploration needed. -->
- Tabs: `Tabs, TabsList, TabsTrigger, TabsContent` from "@/components/ui/tabs". TabsList takes `variant="default"|"line"` — use "default" (locked). TabsContent has NO forceMount (unmounts inactive, matching today's conditional render).
- NativeSelect: `NativeSelect` from "@/components/ui/native-select". `size?: "sm"|"default"` — use "default". React.ComponentProps<"select">-compatible, so `{...form.register("field")}` spreads straight on; plain `<option>` children are fine. Built-in h-9/rounded-md/border-input/focus-visible:ring-ring/50 styling — no className needed.
- Avatar: `Avatar, AvatarFallback` from "@/components/ui/avatar". `size="sm"` => size-6 (24px), fallback text-xs. AvatarImage NOT used (no photo source).
- Breadcrumb: `Breadcrumb, BreadcrumbList, BreadcrumbItem, BreadcrumbLink, BreadcrumbPage, BreadcrumbSeparator` from "@/components/ui/breadcrumb". BreadcrumbLink supports `asChild` (wrap next/link's Link). BreadcrumbSeparator defaults to a ChevronRightIcon.
- Table primitives (`Table, TableBody, TableCell, TableHead, TableHeader, TableRow`) are ALREADY imported at line 25 and already consumed by ClienteProcessosTab/ClienteParecerTab — leave that import and those sub-components untouched (out of scope).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Header Breadcrumb + NativeSelect (6 selects) + Avatar (ResponsaveisCard)</name>
  <files>web/src/app/(dashboard)/clientes/[id]/page.tsx</files>
  <read_first>
    - 105-PATTERNS.md Shared Patterns 2 (NativeSelect, Variant A), 3 (Avatar in ResponsaveisCard), 5 (Breadcrumb 2-level) — before/after code excerpts.
    - 105-UI-SPEC.md Component Inventory CLP-03/CLP-04/CLP-05 rows + Color section (AvatarFallback keeps default bg-muted, never the blue chip tint).
    - clientes/[id]/page.tsx around: header block (~lines 355-364), selectClassName const (line 100) + textareaClassName const (line 103), the 6 <select> sites (grep `<select` in-file to enumerate all 6 exact locations), ResponsaveisCard function (~lines 1662-1674) and its two call sites (Advogados ~line 835, Administrativos ~line 844).
  </read_first>
  <action>
    Add imports at the top of the file: `NativeSelect` from "@/components/ui/native-select"; `Avatar, AvatarFallback` from "@/components/ui/avatar"; `Breadcrumb, BreadcrumbList, BreadcrumbItem, BreadcrumbLink, BreadcrumbPage, BreadcrumbSeparator` from "@/components/ui/breadcrumb". (Tabs import is added in Task 2.)

    NativeSelect: grep the file for every `<select` occurrence (there are exactly 6). Replace each `<select ... className={selectClassName} {...form.register("X")}>...</select>` with `<NativeSelect id="X" size="default" {...form.register("X")}>...</NativeSelect>`, keeping the exact same `<option>` children unchanged. For any controlled (non-RHF) select, use `<NativeSelect value={...} onChange={...} size="default">`. Delete `className={selectClassName}` on every migrated select. After all 6 are migrated, DELETE the `const selectClassName = "..."` declaration (line 100). Do NOT delete `const textareaClassName` (line 103) — it is used by `<textarea>` elements at lines 742 and 781, which are out of scope.

    Avatar: define a small local `deriveInitials(nome: string)` helper reusing the exact logic from `clientes/columns.tsx`: `nome.split(" ").filter(Boolean).slice(0, 2).map((p) => p[0]).join("").toUpperCase()`. In the ResponsaveisCard row (`<div className="flex items-center justify-between gap-3 rounded-md border ... p-2">`), wrap the existing `<div className="min-w-0">` name/details block together with a new `<Avatar size="sm"><AvatarFallback>{deriveInitials(u.nome)}</AvatarFallback></Avatar>` inside a new `<div className="flex items-center gap-2 min-w-0">` sub-container, so the outer `justify-between` (name block vs the Remover Button) is preserved. Do NOT reuse the blue chip styling — AvatarFallback's shipped `bg-muted text-muted-foreground` is correct (UI-SPEC Color, locked). ResponsaveisCard is shared by both the Advogados and Administrativos sections, so this one edit covers both entity types per CLP-04. Do NOT add Avatar to Partes (excluded by CLP-04).

    Breadcrumb: replace the header `<div className="text-sm text-neutral-500 ..."><Link href="/clientes">Clientes</Link> / <span>{identifier}</span></div>` with a `Breadcrumb` per 105-PATTERNS.md Pattern 5 "After (2-level)": `Clientes` as a `BreadcrumbLink asChild` wrapping `<Link href="/clientes">`, a `BreadcrumbSeparator`, then a `BreadcrumbPage` containing the exact existing fallback chain `{cliente.data?.numero_cliente ?? cliente.data?.nome ?? "…"}`. The `<h1 className="text-2xl font-semibold">Cliente</h1>` already uses font-semibold — leave it unchanged. Leave the separate "Voltar" Button (if present near the header) untouched.
  </action>
  <verify>
    <automated>cd web && grep -c "<select" "src/app/(dashboard)/clientes/[id]/page.tsx" | grep -qx 0 && grep -c "const selectClassName" "src/app/(dashboard)/clientes/[id]/page.tsx" | grep -qx 0 && grep -q "const textareaClassName" "src/app/(dashboard)/clientes/[id]/page.tsx" && grep -q "BreadcrumbList" "src/app/(dashboard)/clientes/[id]/page.tsx" && grep -q "AvatarFallback" "src/app/(dashboard)/clientes/[id]/page.tsx" && echo GATE_PASS</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "<select"` on the file returns 0 (all 6 raw selects migrated; `<NativeSelect` does not match this pattern).
    - `const selectClassName` no longer exists in the file; `const textareaClassName` still exists.
    - Exactly one `deriveInitials` helper defined; both ResponsaveisCard entity sections render Avatar via the shared function (no separate Avatar code duplicated per section).
    - Header renders BreadcrumbList/BreadcrumbLink(asChild Link)/BreadcrumbPage; the old `<span>/</span>` separator div is gone.
    - `<h1 ...>Cliente</h1>` still reads `font-semibold` (no weight change on this page).
  </acceptance_criteria>
  <done>All 6 selects are NativeSelect, selectClassName deleted, Avatar shown for Advogados+Administrativos, header is a Breadcrumb — with textareaClassName and the Table-primitive sub-components untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Tabs migration (7 triggers, 2 RBAC-gated, overflow-x-auto preserved)</name>
  <files>web/src/app/(dashboard)/clientes/[id]/page.tsx</files>
  <read_first>
    - 105-PATTERNS.md Shared Pattern 1 (Tabs migration) — the full before (lines 420-476) and after (7-trigger) excerpts.
    - 105-UI-SPEC.md Component Inventory CLP-01 table + the 7 trigger list (dados, contactosNotas, processos*, pareceres*, documentosEntregues, documentosATratar, deslocacoes).
    - clientes/[id]/page.tsx: the tab bar (~lines 420-476) and the full `tab === "x" ? (...) : tab === "y" ? (...) : ... null` content-dispatch chain that follows it (spans a large region below the bar). Read enough of the chain to identify each tab's content block boundaries.
  </read_first>
  <action>
    Add import: `Tabs, TabsList, TabsTrigger, TabsContent` from "@/components/ui/tabs".

    Wrap the whole tab region in `<Tabs value={tab} onValueChange={(v) => setTab(v as TabKey)}>`. The existing `tab`/`setTab` useState<TabKey> is unchanged — Tabs just becomes its controlled consumer.

    Tab bar: replace the `<div className="flex gap-2 w-max">` of `<Button variant={tab === "X" ? "secondary" : "outline"} onClick={() => setTab("X")}>` buttons with a `<TabsList variant="default">` of `<TabsTrigger value="X">Label</TabsTrigger>`. PRESERVE the existing `<div className="overflow-x-auto">` wrapper around the TabsList (Clientes-specific responsive behavior, locked — do NOT change it to flex-wrap). Reuse each Button's exact label text verbatim for its TabsTrigger (Dados, Contactos e Notas, Processos, Pareceres, Documentos Entregues, Documentos a Tratar, Deslocações).

    RBAC gating: the `processos` and `pareceres` triggers are conditionally rendered today via `canViewProcessos`/`canViewPareceres` — keep that exact behavior by wrapping only those two `<TabsTrigger>` in `{canViewProcessos ? <TabsTrigger value="processos">Processos</TabsTrigger> : null}` (and analogously for pareceres). OMIT the trigger entirely when false — never render it `disabled`.

    Content: convert each arm of the `tab === "x" ? (...)` dispatch chain into a `<TabsContent value="x">...</TabsContent>` holding that arm's existing JSX unchanged (no forceMount). For the two gated tabs, keep the inner permission check inside the TabsContent exactly as today — e.g. `<TabsContent value="processos">{canViewProcessos ? <ClienteProcessosTab clienteId={id} /> : <AccessDeniedState description="..." />}</TabsContent>` — so gated content is never rendered from tab state alone (Information Disclosure guard, T-105-01). Do NOT touch the internals of ClienteProcessosTab/ClienteParecerTab (they already use Table primitives; out of scope), and do NOT touch the `.roles.includes("ADMIN")` exception inside ClienteParecerTab (CONTEXT: out of scope).
  </action>
  <verify>
    <automated>cd web && grep -c 'variant={tab ===' "src/app/(dashboard)/clientes/[id]/page.tsx" | grep -qx 0 && grep -q '<TabsList variant="default"' "src/app/(dashboard)/clientes/[id]/page.tsx" && [ "$(grep -c '<TabsTrigger' "src/app/(dashboard)/clientes/[id]/page.tsx")" -eq 7 ] && [ "$(grep -c '<TabsContent' "src/app/(dashboard)/clientes/[id]/page.tsx")" -eq 7 ] && grep -q 'overflow-x-auto' "src/app/(dashboard)/clientes/[id]/page.tsx" && pnpm build && echo GATE_PASS</automated>
  </verify>
  <acceptance_criteria>
    - Zero `variant={tab ===` occurrences remain (no manual toggle buttons).
    - Exactly 7 `<TabsTrigger` and 7 `<TabsContent` (the 2 gated triggers still counted — they are rendered when permitted; the count-of-7 assumes the literal trigger elements exist in source regardless of runtime gating).
    - `overflow-x-auto` wrapper still present around the TabsList.
    - `pnpm build` completes with no new type errors.
    - `Tabs value={tab} onValueChange` present; the tab/setTab useState is unchanged.
  </acceptance_criteria>
  <done>The Cliente ficha tab bar is real Tabs with accessible semantics, RBAC-gated triggers omitted when unpermitted, overflow-x-auto preserved, and `pnpm build` green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser -> Next proxy -> Spring API | Unchanged this phase: no new fetch, no new input field, no new mutation. All reads/writes go through existing, unchanged hooks. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-105-01 | Information Disclosure | RBAC-gated tabs (processos, pareceres) on the Cliente ficha | mitigate | Keep `canViewProcessos`/`canViewPareceres` on BOTH the TabsTrigger (omit when false) AND inside the TabsContent (AccessDeniedState fallback). Tab content must never render from tab state alone. |
| T-105-01b | Elevation of Privilege | ClienteParecerTab `.roles.includes("ADMIN")` exception | accept | Out of scope (CONTEXT). Left byte-unchanged — no new exposure introduced. |
| T-105-SC | Tampering | package installs | accept | Zero package installs this phase — every primitive (Tabs/NativeSelect/Avatar/Breadcrumb) was installed in Phase 101. No new dependency surface. |
</threat_model>

<verification>
- `cd web && pnpm build` passes (Next build runs tsc — catches any type regression from the migration).
- `cd web && pnpm lint` reports no new errors on the file.
- Manual grep gates in each task pass.
</verification>

<success_criteria>
- CLP-01 satisfied for Cliente: 7 tabs via Tabs/TabsList/TabsTrigger/TabsContent, RBAC-conditional triggers preserved, overflow-x-auto preserved.
- CLP-03 (Cliente file): all 6 selects are NativeSelect, selectClassName deleted.
- CLP-04 (Cliente): Avatar on Advogados + Administrativos rows.
- CLP-05 (Cliente ficha): Breadcrumb header.
- `pnpm build` green.
</success_criteria>

<output>
Create `.planning/phases/LEXCV-105-m-dulos-clientes-processos-combinados/105-01-SUMMARY.md` when done.
</output>
