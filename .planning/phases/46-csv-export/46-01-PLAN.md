---
phase: 46-csv-export
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [web/src/app/(dashboard)/financeiro/page.tsx]
autonomous: true
requirements: [FIN-17]

must_haves:
  truths:
    - "O botão 'Exportar CSV' aparece no header da página financeiro quando há honorários na lista filtrada"
    - "Clicar no botão descarrega um ficheiro honorarios-YYYY-MM-DD.csv"
    - "O CSV começa com BOM UTF-8 (U+FEFF) para compatibilidade com Excel"
    - "O CSV contém os campos id, processo, cliente, valorTotal, totalPago, estado, dataAcordo"
    - "Quando filtros estão ativos, o CSV exporta apenas os honorários de filteredList"
    - "Quando filteredList está vazio, o botão não aparece"
  artifacts:
    - path: "web/src/app/(dashboard)/financeiro/page.tsx"
      provides: "função exportHonorariosCsv + botão Exportar CSV no header"
      contains: "exportHonorariosCsv"
  key_links:
    - from: "botão Exportar CSV"
      to: "exportHonorariosCsv"
      via: "onClick handler"
      pattern: "exportHonorariosCsv"
    - from: "exportHonorariosCsv"
      to: "filteredList"
      via: "argumento rows"
      pattern: "filteredList"
---

<objective>
Adicionar exportação CSV client-side na página financeiro, exportando filteredList com campos definidos em FIN-17.

Purpose: Permitir ao utilizador descarregar os honorários visíveis (com filtros aplicados) em formato CSV para análise em Excel ou outras ferramentas.
Output: web/src/app/(dashboard)/financeiro/page.tsx modificado com função helper e botão de exportação.
</objective>

<execution_context>
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/46-csv-export/46-CONTEXT.md

<interfaces>
<!-- Extraído de web/src/app/(dashboard)/financeiro/page.tsx -->

Variáveis disponíveis em FinanceiroContent no momento do render:
- filteredList: Honorario[]  — lista já filtrada pelos controlos da Phase 45
- processoById: Map<string, Processo>  — chave = processo.id
- clienteNomeById: Map<string, string>  — chave = cliente.id, valor = cliente.nome
- calcHonorarioStatus(totalPago: number, valorTotal: number): HonorarioStatus

Tipo Honorario (inferido dos hooks existentes):
- id: string | number
- processoId: string
- valorTotal: number
- totalPago: number
- dataAcordo?: string  — formato ISO date "YYYY-MM-DD"

Tipo Processo (inferido de processoById):
- id: string
- numero?: string
- titulo?: string
- cliente_id?: string

Header da página (linhas 93-104):
  <div className="flex items-start justify-between gap-4">
    <div> ... h1 "Financeiro" ... </div>
    {canCreateFinanceiro ? <Button asChild><Link ...>Novo honorário</Link></Button> : null}
  </div>
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Tarefa 1: Adicionar função exportHonorariosCsv e botão "Exportar CSV"</name>
  <files>web/src/app/(dashboard)/financeiro/page.tsx</files>
  <action>
Modificar web/src/app/(dashboard)/financeiro/page.tsx com dois acréscimos:

1. Adicionar a função helper exportHonorariosCsv como função pura ao nível do módulo (fora de qualquer componente, após calcHonorarioStatus), com a seguinte lógica (per D-01 do CONTEXT.md):
   - Parâmetros: rows (array de Honorario), processoById (Map), clienteNomeById (Map)
   - Cabeçalho CSV: ID, Processo, Cliente, Valor Total, Total Pago, Estado, Data do Acordo
   - Escape de campo: se o valor contiver vírgula, aspas ou newline, envolver em aspas duplas e duplicar as aspas internas
   - Para cada honorario em rows, construir linha com: String(h.id), proc.numero ?? proc.titulo ?? h.processoId (usando processoById.get(h.processoId)), clienteNomeById.get(proc?.cliente_id ?? "") ?? "", String(h.valorTotal), String(h.totalPago), calcHonorarioStatus(h.totalPago, h.valorTotal), h.dataAcordo ?? ""
   - Prefixar o conteúdo final com BOM UTF-8: "﻿" (per D-01 — compatibilidade Excel Windows)
   - Criar Blob com type "text/csv;charset=utf-8;", gerar URL com URL.createObjectURL, criar elemento <a> com href e download="honorarios-YYYY-MM-DD.csv" (data atual via new Date().toISOString().slice(0,10)), chamar .click(), depois URL.revokeObjectURL

2. No JSX de FinanceiroContent, modificar o header da página (div com "flex items-start justify-between gap-4") para mostrar dois botões quando aplicável:
   - Manter o botão "Novo honorário" existente (condicionado a canCreateFinanceiro)
   - Adicionar botão "Exportar CSV" visível apenas quando filteredList.length > 0, com onClick={() => exportHonorariosCsv(filteredList, processoById, clienteNomeById)}
   - Envolver ambos os botões num div com className="flex items-center gap-2" para alinhar horizontalmente
   - O botão "Exportar CSV" usa variant="outline" do componente Button existente (sem asChild, sem Link)
   - Não importar bibliotecas novas — usar apenas APIs nativas do browser (Blob, URL, document.createElement)
  </action>
  <verify>
    <automated>cd C:/Users/francisco.horta/Documents/projects/personal/lexcv/web && pnpm build 2>&1 | tail -20</automated>
  </verify>
  <done>
    - pnpm build conclui sem erros de TypeScript nem de compilação
    - A função exportHonorariosCsv existe no ficheiro com prefixo BOM "﻿"
    - O botão "Exportar CSV" aparece no JSX condicionado a filteredList.length > 0
    - O botão "Exportar CSV" invoca exportHonorariosCsv com os três argumentos corretos
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Descrição |
|----------|-----------|
| dados honorários → CSV | Dados já carregados do backend autenticado; exportação é client-side pura |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-46-01 | Information Disclosure | exportHonorariosCsv | accept | Dados já visíveis ao utilizador autenticado na UI; exportação não expõe nada novo |
| T-46-02 | Tampering | Blob/URL.createObjectURL | accept | Download é iniciado pelo próprio utilizador; sem risco de injeção server-side |
</threat_model>

<verification>
Após execução, verificar manualmente:
- Navegar para /financeiro com dados presentes — o botão "Exportar CSV" deve aparecer no header
- Aplicar um filtro (ex: filtro de status "Pendente") — o CSV descarregado deve conter apenas os honorários pendentes
- Abrir o CSV em Excel — encoding e separadores devem estar corretos (BOM garante UTF-8 reconhecido)
- Com lista vazia (filtro que retorna 0 resultados) — o botão não deve aparecer
</verification>

<success_criteria>
- pnpm build passa sem erros
- Botão "Exportar CSV" presente e condicionado a filteredList.length > 0
- CSV descarregado contém campos id, processo, cliente, valorTotal, totalPago, estado, dataAcordo
- CSV respeita filtros ativos (exporta filteredList, não honorarios.data)
- Ficheiro abre corretamente no Excel (UTF-8 BOM presente)
</success_criteria>

<output>
Criar `.planning/phases/46-csv-export/46-01-SUMMARY.md` ao concluir.
</output>
