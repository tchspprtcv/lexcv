---
phase: 84
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/app/(dashboard)/processos/novo/page.tsx
  - web/src/app/(dashboard)/processos/[id]/editar/page.tsx
autonomous: true
requirements: [PROC-03, PROC-04, PROC-02]

must_haves:
  truths:
    - "O utilizador não consegue avançar do passo 1 do intake sem escolher uma Origem (Petição Inicial | Notificações Avulsas)"
    - "Um erro de validação Zod aparece inline sob o campo Origem se o utilizador tentar submeter sem o preencher"
    - "O utilizador consegue editar o Juízo de um processo existente através da página /processos/{id}/editar, ao lado de Tribunal/Área Jurídica"
  artifacts:
    - path: "web/src/app/(dashboard)/processos/novo/page.tsx"
      provides: "Campo Origem no formulário de intake, vinculado a processoIntakeFormSchema"
    - path: "web/src/app/(dashboard)/processos/[id]/editar/page.tsx"
      provides: "Campo Juízo editável no formulário de edição do processo"
  key_links:
    - from: "web/src/app/(dashboard)/processos/novo/page.tsx"
      to: "processoIntakeFormSchema (web/src/schemas/processos.ts)"
      via: "useForm<ProcessoIntakeFormValues>({ resolver: zodResolver(processoIntakeFormSchema) })"
      pattern: "processoIntakeFormSchema"
    - from: "web/src/app/(dashboard)/processos/[id]/editar/page.tsx"
      to: "ProcessoUpdateRequest.juizo (web/src/types/processos.ts)"
      via: "form.register(\"juizo\") submitted via update.mutateAsync(values satisfies ProcessoUpdateRequest)"
      pattern: "register\\(\"juizo\"\\)"
---

<objective>
Wire the two remaining "small field" gaps flagged by the Pattern Map before any of the larger `processos/[id]/page.tsx` work starts: (1) the Origem field is added to the intake wizard's step-1 form AND the form is switched from `processoFormSchema` to `processoIntakeFormSchema` so the already-built Zod `.enum()` requiredness (Phase 83) is actually enforced at runtime for the first time (D-03/D-04 per CONTEXT.md; PROC-03/PROC-04 per REQUIREMENTS.md); (2) the Juízo field — present on `processoFormSchema` since Phase 83 but never rendered anywhere — is added to `processos/[id]/editar/page.tsx` next to Tribunal/Área Jurídica, which is the only editable surface for Processo fields in this codebase (the ficha page `processos/[id]/page.tsx` is read-only `dl`/`dd`). Without this task, ROADMAP success criterion 2 ("Juízo é ... editável") has no reachable UI path.

Purpose: close the schema-mismatch gap flagged in 84-PATTERNS.md (Flagged Risk 1) before any downstream plan reads/writes `origem`, and make Juízo actually editable per ROADMAP success criterion 2.
Output: `processos/novo/page.tsx` intake step 1 requires Origem; `processos/[id]/editar/page.tsx` exposes a Juízo input.
</objective>

<execution_context>
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/LEXCV-84-frontend-ui-intake-dados-sub-sec-es-documentos-termo-de-hono/84-CONTEXT.md
@.planning/phases/LEXCV-84-frontend-ui-intake-dados-sub-sec-es-documentos-termo-de-hono/84-UI-SPEC.md
@.planning/phases/LEXCV-84-frontend-ui-intake-dados-sub-sec-es-documentos-termo-de-hono/84-PATTERNS.md
@.planning/research/PITFALLS.md

<interfaces>
From web/src/schemas/processos.ts (already built, Phase 83 — do not modify):
processoFormSchema: { cliente_id (required), numero?, titulo?, tipo_processo?, area_juridica?, tribunal?, descricao?, estado?, data_inicio?, data_fim?, legal_hold?, data_retencao?, juizo? } — all except cliente_id are optionalTrimmedString/boolean.
origemProcessoSchema = z.enum(["PETICAO_INICIAL", "NOTIFICACOES_AVULSAS"])
processoIntakeFormSchema = processoFormSchema.extend({ origem: origemProcessoSchema }) — origem is REQUIRED, no default.
export type ProcessoIntakeFormValues = z.infer<typeof processoIntakeFormSchema>

From web/src/lib/origem-processo.ts:
export function origemProcessoToLabel(origem: OrigemProcesso): string — returns "Petição Inicial" | "Notificações Avulsas".

From web/src/types/processos.ts:
ProcessoCreateRequest includes optional `origem?: OrigemProcesso` and `juizo?: string`.
ProcessoUpdateRequest includes optional `juizo?: string` (no `origem` — immutable after intake, by design, never send it from the edit form).

From web/src/hooks/use-processos.ts (already built, Phase 81/83 — do not modify):
useCreateIntake(): mutationFn posts to POST /processos/intake via toProcessoApiPayload(payload), which already maps `origem`/`juizo` correctly (mapJuizoOrigemToPayload).
useUpdateProcesso(id): mutationFn posts to PUT /processos/{id}; toProcessoApiPayload strips `origem` implicitly for update payloads (mapJuizoOrigemToPayload checks `"origem" in payload`, and ProcessoUpdateRequest has no origem field, so TypeScript itself prevents accidentally sending it — do not add origem anywhere in editar/page.tsx).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add required Origem field to intake step 1, switch form to processoIntakeFormSchema</name>
  <files>web/src/app/(dashboard)/processos/novo/page.tsx</files>
  <read_first>
    web/src/app/(dashboard)/processos/novo/page.tsx
    web/src/schemas/processos.ts
    web/src/lib/origem-processo.ts
    .planning/phases/LEXCV-84-frontend-ui-intake-dados-sub-sec-es-documentos-termo-de-hono/84-PATTERNS.md (section "processos/novo/page.tsx — add Origem field to intake step 1")
  </read_first>
  <action>
    In `web/src/app/(dashboard)/processos/novo/page.tsx`: change the `intakeForm` useForm generic and resolver from `useForm&lt;ProcessoFormValues&gt;({ resolver: zodResolver(processoFormSchema), ... })` to `useForm&lt;ProcessoIntakeFormValues&gt;({ resolver: zodResolver(processoIntakeFormSchema), ... })`. Update the import from `@/schemas/processos` to bring in `processoIntakeFormSchema` and `type ProcessoIntakeFormValues` (keep or drop `processoFormSchema`/`ProcessoFormValues` only if still used elsewhere in the file — they are not used elsewhere in this file, so replace them outright). Add `origem: undefined` to the `defaultValues` object (alongside the existing `data_fim: undefined` entry). Update `onStep1Submit`'s parameter type to `ProcessoIntakeFormValues` (it is inferred from `intakeForm.handleSubmit`, so no explicit annotation needed if the generic change is done first) — the existing destructure `const { estado: _estado, ...intakeValues } = values;` continues to work unchanged since `origem` is just another key in `values` that survives the spread into `intakeValues`, and `useCreateIntake().mutateAsync(intakeValues)` accepts it because `ProcessoCreateRequest.origem?: OrigemProcesso` already exists.

    Insert a new form field block for "Origem" in the Step 1 card, positioned as its own `div.space-y-2` row directly after the "Row 1: Cliente + Tipo de Processo" grid (i.e., its own full-width row, not sharing a grid with another field — CONTEXT.md/UI-SPEC do not pin an exact position beyond "step 1", full-width after the two-column Cliente/Tipo row is the simplest slot that does not disturb the existing two-column rows). Use `Label htmlFor="origem"` with text `Origem`; a native `&lt;select id="origem" className={selectClassName} {...intakeForm.register("origem")}&gt;` with a first disabled-look placeholder `&lt;option value=""&gt;Selecionar origem&lt;/option&gt;` (matches the `tipo_processo` sibling's `Selecionar tipo` placeholder convention exactly) followed by two options: `value="PETICAO_INICIAL"` labelled `{origemProcessoToLabel("PETICAO_INICIAL")}` and `value="NOTIFICACOES_AVULSAS"` labelled `{origemProcessoToLabel("NOTIFICACOES_AVULSAS")}`. Import `origemProcessoToLabel` from `@/lib/origem-processo`. Below the select, render the Zod error exactly like every other field on this form: `{intakeForm.formState.errors.origem ? &lt;p className="text-sm text-red-600"&gt;{intakeForm.formState.errors.origem.message}&lt;/p&gt; : null}`.
  </action>
  <acceptance_criteria>
    - Source: `web/src/app/(dashboard)/processos/novo/page.tsx` imports `processoIntakeFormSchema` and `type ProcessoIntakeFormValues` from `@/schemas/processos`, and `intakeForm` is declared as `useForm&lt;ProcessoIntakeFormValues&gt;({ resolver: zodResolver(processoIntakeFormSchema), ... })`.
    - Source: the file no longer imports `processoFormSchema`/`ProcessoFormValues` (superseded by the intake variant in this file).
    - Source: a `&lt;select id="origem" ... {...intakeForm.register("origem")}&gt;` exists with exactly two non-empty `&lt;option&gt;` values, `PETICAO_INICIAL` and `NOTIFICACOES_AVULSAS`.
    - Source: `intakeForm.formState.errors.origem` is read and rendered as inline red text, matching the sibling `tipo_processo` error-rendering pattern.
    - Behavior: submitting step 1 with Origem left on the placeholder blocks submission client-side (Zod `.enum()` on an empty string fails validation) and shows the inline error, without calling `useCreateIntake`.
  </acceptance_criteria>
  <verify>
    <automated>cd "C:/Users/francisco.horta/Documents/projects/personal/lexcv/web" && pnpm exec tsc --noEmit</automated>
  </verify>
  <done>Step 1 of the intake wizard has a required Origem select bound to processoIntakeFormSchema; tsc passes with no new type errors.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add editable Juízo field to processos/[id]/editar/page.tsx</name>
  <files>web/src/app/(dashboard)/processos/[id]/editar/page.tsx</files>
  <read_first>
    web/src/app/(dashboard)/processos/[id]/editar/page.tsx
    web/src/schemas/processos.ts
    web/src/types/processos.ts
  </read_first>
  <action>
    In `web/src/app/(dashboard)/processos/[id]/editar/page.tsx`, this page already binds to `processoFormSchema`/`ProcessoFormValues`, which already declares `juizo` as `optionalTrimmedString` (Phase 83) — no schema change needed here, only wiring the three missing spots: (1) add `juizo: undefined` to the `useForm` `defaultValues` object, in the same position style as the other optional string fields; (2) add `juizo: processo.data.juizo` to the `React.useEffect` that calls `form.reset({...})` once `processo.data` loads, alongside the other `processo.data.*` mappings; (3) add a new `div.space-y-2` field block for "Juízo" — position it as its own row directly after the existing "Tribunal" field block (before the "Data de início / Data de fim" grid), using the exact same shape as the "Tribunal" block: `Label htmlFor="juizo"` text `Juízo`, `Input id="juizo" {...form.register("juizo")} placeholder="Ex.: 1º Juízo Cível"`, and the standard `{form.formState.errors.juizo ? ... : null}` error paragraph. Do not add an `origem` field anywhere on this page — origem is immutable after intake (Out of Scope, CONTEXT.md/REQUIREMENTS.md), and `ProcessoUpdateRequest` has no `origem` field, so `values satisfies ProcessoUpdateRequest` in the existing `onSubmit` would fail to compile if origem were added here.
  </action>
  <acceptance_criteria>
    - Source: `defaultValues` in the `useForm&lt;ProcessoFormValues&gt;` call includes `juizo: undefined`.
    - Source: the `form.reset({...})` call inside the `React.useEffect(() =&gt; { if (!processo.data) return; ... }, [processo.data, form])` block includes `juizo: processo.data.juizo`.
    - Source: a `&lt;Input id="juizo" {...form.register("juizo")}` element exists in the form JSX, positioned after the Tribunal field.
    - Source: no `origem` identifier is added anywhere in this file (grep for `origem` in this file returns zero matches after the edit).
    - Behavior: loading `/processos/{id}/editar` for a processo that already has a `juizo` value pre-fills the new Input with that value; submitting the form with an edited Juízo persists it (round-trip already wired by Phase 83's `toProcessoApiPayload`/`mapJuizoOrigemToPayload`, verified by that phase's own round-trip script — this task only needs to prove the UI reads/writes the field, not re-verify the mapping layer).
  </acceptance_criteria>
  <verify>
    <automated>cd "C:/Users/francisco.horta/Documents/projects/personal/lexcv/web" && pnpm exec tsc --noEmit && grep -c "origem" "src/app/(dashboard)/processos/[id]/editar/page.tsx" | grep -qx 0</automated>
  </verify>
  <done>Juízo is a visible, editable Input on processos/[id]/editar/page.tsx, pre-filled from and persisted via the existing juizo/origem mapping layer; the page still has zero references to origem.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser (authenticated user) → Next.js rewrite → Spring Boot `/api/v1` | Untrusted form input crosses here on every intake/update submit |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-84-01 | Elevation of Privilege | `processos/novo/page.tsx` step-1 submit | accept | The Origem field is added inside the existing intake form, which is already gated by the page-level `!canCreateProcessos` `AccessDeniedState` check and the submit button's `disabled={... || !canCreateProcessos}` — no new endpoint or permission surface is introduced by adding one more field to an already-gated form. |
| T-84-02 | Elevation of Privilege | `processos/[id]/editar/page.tsx` Juízo field | accept | The entire edit page is already gated by `!canEditProcessos → AccessDeniedState` before any form renders; adding one more registered field does not change that gate. |
| T-84-03 | Tampering | Origem enum value submitted client-side | accept | `origemProcessoSchema` (Zod `.enum`) rejects any value outside the two literals client-side, and the backend's `POST /processos/intake` validation plus `CAMPOS_MINIMOS_POR_TIPO` (Phase 81) independently re-validate server-side — defense in depth already shipped, this plan only activates the client-side half by switching to the correct schema. |
</threat_model>

<verification>
- `pnpm --dir web exec tsc --noEmit` passes with no new errors after both tasks.
- Manual/human-check (deferred to phase-level UAT, not a blocking checkpoint here): create a processo via the wizard without selecting Origem — submission is blocked with an inline error; select an Origem, complete intake, then edit the processo and set a Juízo value — reload the ficha and confirm Juízo persists (covered structurally by Phase 83's round-trip script, this plan only adds the UI surface).
</verification>

<success_criteria>
- `processos/novo/page.tsx` step 1 form is bound to `processoIntakeFormSchema` and cannot be submitted without a valid Origem value.
- `processos/[id]/editar/page.tsx` has a Juízo Input that reads from and writes to `processo.data.juizo` / `ProcessoUpdateRequest.juizo`.
</success_criteria>

<output>
Create `.planning/phases/LEXCV-84-frontend-ui-intake-dados-sub-sec-es-documentos-termo-de-hono/84-01-SUMMARY.md` when done
</output>
