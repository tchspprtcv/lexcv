---
phase: 84
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/app/(dashboard)/processos/[id]/page.tsx
autonomous: true
requirements: [PROC-01, PROC-02, PROC-05, PROC-15]

must_haves:
  truths:
    - "O card 'Dados' da ficha do processo mostra Juízo e Origem (Origem sempre não-editável, apenas leitura)"
    - "Quando o processo está ATIVO, um botão 'Gerar Termo de Honorários' aparece no card Dados e abre /processos/{id}/termo-honorarios numa nova aba"
    - "O grupo de botões-toggle da ficha ganha 4 novos valores possíveis (decisoes, factos, testemunhas, documentos) sem quebrar os existentes (timeline, partes, fases, auditoria)"
    - "As abas Partes e Fases mostram a mesma lista de sempre, mas o formulário 'Adicionar' passou de um Card lado-a-lado para um Dialog acionado por um botão 'Adicionar Parte'/'Adicionar Fase' junto ao título do Card"
    - "O gate canEditProcessos que já protegia os formulários Adicionar parte/Adicionar fase continua a proteger os mesmos Dialogs depois da refatoração"
  artifacts:
    - path: "web/src/app/(dashboard)/processos/[id]/page.tsx"
      provides: "TabKey estendido, Dados card com Juízo/Origem/Gerar Termo, Partes e Fases refatorados para Dialog"
  key_links:
    - from: "web/src/app/(dashboard)/processos/[id]/page.tsx"
      to: "origemProcessoToLabel (web/src/lib/origem-processo.ts)"
      via: "dd do card Dados"
      pattern: "origemProcessoToLabel\\(processo\\.data\\.origem\\)"
    - from: "web/src/app/(dashboard)/processos/[id]/page.tsx"
      to: "/processos/{id}/termo-honorarios"
      via: "Link asChild dentro de um Button, target=_blank, condicionado a estado === \"ATIVO\""
      pattern: "termo-honorarios"
---

<objective>
Lay the shared foundation inside `processos/[id]/page.tsx` that every later plan in this phase builds on: extend the `TabKey` union and tab-button toggle group to include the 4 new tab values (without yet building their bodies — that is Plans 84-04/84-05), add the Juízo/Origem read-only rows and the conditional "Gerar Termo de Honorários" button to the Dados card, and refactor the two EXISTING tabs (Partes, Fases) from their current grid-2-column fixed-form layout to the Dialog "Adicionar" pattern — an explicit, deliberate scope extension requested by the user during `/gsd:discuss-phase` (CONTEXT.md) for visual consistency with the 4 new tabs, not part of the original ROADMAP success criteria but treated as mandatory work for this phase.

Because every plan touching `processos/[id]/page.tsx` must be sequential (same file), this plan runs first in that sequence and does the interface-establishing work (imports, TabKey, Dados card) other same-file plans do not need to repeat.

Purpose: complete PROC-01/02/05 (Juízo/Origem visible on the ficha) and lay the entry point for PROC-15 (Termo de Honorários trigger); deliver the explicit Partes/Fases Dialog-pattern refactor requested in CONTEXT.md.
Output: `processos/[id]/page.tsx` with extended TabKey, updated Dados card, and Partes/Fases tabs refactored to the Dialog pattern (4 new tab bodies are stubbed as empty `null` branches in this plan — Plans 84-04/84-05 fill them in).
</objective>

<execution_context>
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/LEXCV-84-frontend-ui-intake-dados-sub-sec-es-documentos-termo-de-hono/84-CONTEXT.md
@.planning/phases/LEXCV-84-frontend-ui-intake-dados-sub-sec-es-documentos-termo-de-hono/84-UI-SPEC.md
@.planning/phases/LEXCV-84-frontend-ui-intake-dados-sub-sec-es-documentos-termo-de-hono/84-PATTERNS.md
@web/src/app/(dashboard)/clientes/[id]/page.tsx

<interfaces>
Current `web/src/app/(dashboard)/processos/[id]/page.tsx` state (full file already read — this IS the file being edited):
- `type TabKey = "timeline" | "partes" | "fases" | "auditoria";` (line 93)
- Dialog import (lines 27-34) currently: `Dialog, DialogContent, DialogDescription, DialogFooter, DialogHeader, DialogTitle` — missing `DialogTrigger`.
- Tab button group (lines 915-946): 4 `Button` elements (Timeline/Partes/Fases/Auditoria-gated) inside a `div.flex.flex-wrap.gap-2`.
- Dados card `dl` grid (lines 450-496): existing rows in order Número, Tipo do processo, Área jurídica, Tribunal, Cliente, Estado, Data de início, Data de fim, Legal Hold, Data Retenção, Criado, Atualizado.
- Partes tab body (lines 1206-1284): `div.grid.gap-4.lg:grid-cols-2` containing a conditional "Adicionar parte" `Card` (left, `canEditProcessos`-gated, inline `&lt;form onSubmit={parteForm.handleSubmit(onSubmitParte)}&gt;`) and a "Partes" list `Card` (right, raw `&lt;table&gt;`). `parteForm`, `onSubmitParte`, `parteServerError`, `addParte` are already declared/used — this task only moves the JSX, no new state/hooks.
- Fases tab body (lines 1285-1371): same shape — "Adicionar fase" `Card` (left) + "Fases" list `Card` (right, includes the per-row inline status `&lt;select&gt;` + "Guardar" `Button` in the "Ações" column, driven by `faseDraftStatus`/`onUpdateFaseStatus`/`updateFaseStatus` — this inline control is UNCHANGED by this refactor, only the "Adicionar fase" form moves).

From web/src/lib/origem-processo.ts: `origemProcessoToLabel(origem: OrigemProcesso): string`.

Dialog "Adicionar X" shell to replicate (source: `clientes/[id]/page.tsx` lines 898-923, and the "Shared Patterns" section of 84-PATTERNS.md): `Dialog open={x} onOpenChange={setX}` wrapping `DialogTrigger asChild` (`Button type="button" variant="outline" size="sm"`) + `DialogContent` (`DialogHeader` + `DialogTitle` + form body + `DialogFooter` with Cancelar/Confirmar).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Extend TabKey, add DialogTrigger import, add Juízo/Origem/Gerar-Termo to Dados card</name>
  <files>web/src/app/(dashboard)/processos/[id]/page.tsx</files>
  <read_first>
    web/src/app/(dashboard)/processos/[id]/page.tsx
    .planning/phases/LEXCV-84-frontend-ui-intake-dados-sub-sec-es-documentos-termo-de-hono/84-PATTERNS.md (sections "TabKey union", "Tab button toggle group", "Dados card — Juízo/Origem fields + Termo de Honorários button")
  </read_first>
  <action>
    Change `type TabKey = "timeline" | "partes" | "fases" | "auditoria";` to `type TabKey = "timeline" | "partes" | "fases" | "decisoes" | "factos" | "testemunhas" | "documentos" | "auditoria";`.

    In the `@/components/ui/dialog` import block, add `DialogTrigger` to the named imports (existing Dialogs in this file stay imperatively-opened via `setXDialogOpen(true)`; the new Dialog-triggered tabs use `DialogTrigger asChild` — both styles coexist, this is expected).

    Add `import { origemProcessoToLabel } from "@/lib/origem-processo";` to the lib imports block.

    In the tab-button toggle group (the `div.flex.flex-wrap.gap-2` after the "Novo Prazo" Dialog), insert 4 new `Button` elements between the existing "Fases" button and the `canManageProcessos`-gated "Auditoria" button, same shape as the surrounding buttons (`type="button" variant={tab === "X" ? "secondary" : "outline"} onClick={() =&gt; setTab("X")}`), with visible labels in this exact order: `Decisões` (tab value `decisoes`), `Factos` (tab value `factos`), `Testemunhas` (tab value `testemunhas`), `Documentos` (tab value `documentos`).

    In the Dados card `dl` grid, insert two new `dt`/`dd` pairs directly after the existing "Tribunal" row and before the "Cliente" row: a "Juízo" row (`dd` renders `processo.data.juizo ?? "—"`) and an "Origem" row (`dd` renders `processo.data.origem ? origemProcessoToLabel(processo.data.origem) : "—"`) — both read-only, matching the exact styling of the surrounding rows (`dt className="text-neutral-500 dark:text-neutral-400"`, `dd className="col-span-2"`). Origem has no edit affordance anywhere on this page by design (immutable after intake).

    Still inside the Dados `CardContent`, after the closing `&lt;/dl&gt;`, add a conditional block: `{processo.data?.estado === "ATIVO" ? (&lt;div className="pt-4"&gt;...&lt;/div&gt;) : null}` containing a `Button asChild className="rounded-none font-bold bg-blue-600 hover:bg-blue-700 text-white"` wrapping a `Link href={\`/processos/${encodeURIComponent(id)}/termo-honorarios\`} target="_blank" rel="noopener noreferrer"` with the plain-text label `Gerar Termo de Honorários` (no icon, per UI-SPEC's copy table for this element).

    Also extend the 4 new tab values into the render chain's ternary as inert placeholders for now (`) : tab === "decisoes" ? null : tab === "factos" ? null : tab === "testemunhas" ? null : tab === "documentos" ? null : tab === "auditoria" && canManageProcessos ? (` — these 4 `null` branches are placeholders that Plans 84-04/84-05 will replace with the real tab bodies; leaving them as explicit `null` (not omitted) keeps the ternary chain type-correct and prevents a TypeScript exhaustiveness gap since `TabKey` now has 4 more members than the chain currently handles).
  </action>
  <acceptance_criteria>
    - Source: `type TabKey` includes all 8 values: `timeline`, `partes`, `fases`, `decisoes`, `factos`, `testemunhas`, `documentos`, `auditoria`.
    - Source: `DialogTrigger` appears in the `@/components/ui/dialog` import list.
    - Source: `origemProcessoToLabel` is imported from `@/lib/origem-processo` and used exactly once in the Dados card `dd`.
    - Source: the Dados card `dl` contains `dt`/`dd` pairs whose visible label text is exactly `Juízo` and exactly `Origem`, both positioned after the `Tribunal` row.
    - Source: a `Link href={\`/processos/${encodeURIComponent(id)}/termo-honorarios\`}` exists, wrapped in a condition on `processo.data?.estado === "ATIVO"`.
    - Source: the tab-button group contains 4 new `Button` elements with visible text `Decisões`, `Factos`, `Testemunhas`, `Documentos`, each calling `setTab(...)` with the matching new `TabKey` value.
  </acceptance_criteria>
  <verify>
    <automated>cd "C:/Users/francisco.horta/Documents/projects/personal/lexcv/web" && pnpm exec tsc --noEmit</automated>
  </verify>
  <done>TabKey covers all 8 tabs, the tab-button row exposes all 4 new buttons, and the Dados card shows Juízo/Origem read-only plus a conditional Gerar Termo de Honorários button — all without touching Partes/Fases bodies yet.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Refactor Partes tab to the Dialog "Adicionar Parte" pattern</name>
  <files>web/src/app/(dashboard)/processos/[id]/page.tsx</files>
  <read_first>
    web/src/app/(dashboard)/processos/[id]/page.tsx
    .planning/phases/LEXCV-84-frontend-ui-intake-dados-sub-sec-es-documentos-termo-de-hono/84-PATTERNS.md (section "Partes tab refactor")
  </read_first>
  <action>
    Replace the current Partes tab body (the `div.grid.gap-4.lg:grid-cols-2` block containing the side-by-side "Adicionar parte" Card and "Partes" Card) with a single full-width `Card`. Add a new `React.useState&lt;boolean&gt;(false)` named `addParteModal`/`setAddParteModal`, declared alongside the other tab-local state near the top of `ProcessoDetailContent` (next to `parteForm`/`parteServerError`). The `Partes` `Card`'s `CardHeader` becomes `&lt;div className="flex items-center justify-between"&gt;&lt;CardTitle&gt;Partes&lt;/CardTitle&gt;{canEditProcessos ? (&lt;Dialog open={addParteModal} onOpenChange={setAddParteModal}&gt;&lt;DialogTrigger asChild&gt;&lt;Button type="button" variant="outline" size="sm"&gt;Adicionar Parte&lt;/Button&gt;&lt;/DialogTrigger&gt;&lt;DialogContent&gt;...&lt;/DialogContent&gt;&lt;/Dialog&gt;) : null}&lt;/div&gt;`. Move the existing "Adicionar parte" `&lt;form onSubmit={parteForm.handleSubmit(onSubmitParte)}&gt;` body (Nome input, Tipo/NIF two-column grid, `parteServerError`, submit button) verbatim into the `DialogContent`, with `DialogHeader`/`DialogTitle` reading "Adicionar Parte" and a `DialogFooter` replacing the old bare submit `Button` with `Button type="button" variant="outline" onClick={() =&gt; setAddParteModal(false)}` labelled "Cancelar" plus the existing submit `Button` (keep `type="submit"` inside the form, `disabled={parteForm.formState.isSubmitting || addParte.isPending}`, label "Adicionar" — do not rename this button, only its container changed). On successful submit (existing `onSubmitParte` success path already calls `parteForm.reset(...)` and `toast.success(...)`), additionally call `setAddParteModal(false)` to close the dialog — add this one line to the existing `try` block's success path in `onSubmitParte`. The `Partes` list table markup (lines ~1258-1280, unchanged raw `&lt;table&gt;`) stays exactly as-is inside the `CardContent`, just no longer sharing a `grid lg:grid-cols-2` with the old sibling Card.
  </action>
  <acceptance_criteria>
    - Source: the Partes tab body has no `grid lg:grid-cols-2` wrapper — grep for `lg:grid-cols-2` in the Partes tab region returns no match (Fases still has its own instance until Task 3 runs, so this check must be scoped to the Partes-only region, not the whole file).
    - Source: a `Dialog open={addParteModal}` exists, gated by `canEditProcessos ? (...) : null`, with `DialogTitle` text "Adicionar Parte".
    - Source: `onSubmitParte`'s success path calls `setAddParteModal(false)`.
    - Source: the Partes list `&lt;table&gt;` (Tipo/Nome/NIF columns) is still present, unchanged.
    - Behavior: a user without `processos:edit` sees the Partes list but no "Adicionar Parte" trigger (same gate as before the refactor, `canEditProcessos ? (...) : null` preserved).
  </acceptance_criteria>
  <verify>
    <automated>cd "C:/Users/francisco.horta/Documents/projects/personal/lexcv/web" && pnpm exec tsc --noEmit</automated>
  </verify>
  <done>Partes tab shows the list full-width with an "Adicionar Parte" Dialog trigger next to the CardTitle, gated by canEditProcessos exactly as before.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Refactor Fases tab to the Dialog "Adicionar Fase" pattern</name>
  <files>web/src/app/(dashboard)/processos/[id]/page.tsx</files>
  <read_first>
    web/src/app/(dashboard)/processos/[id]/page.tsx
    .planning/phases/LEXCV-84-frontend-ui-intake-dados-sub-sec-es-documentos-termo-de-hono/84-PATTERNS.md (section "Fases tab refactor")
  </read_first>
  <action>
    Apply the identical transformation as Task 2 to the Fases tab: replace the `div.grid.gap-4.lg:grid-cols-2` wrapper with a single full-width `Card`; add `addFaseModal`/`setAddFaseModal` state; move the "Adicionar fase" `&lt;form onSubmit={faseForm.handleSubmit(onSubmitFase)}&gt;` body (nome Input, `faseServerError`, submit button) into a `DialogContent` triggered by `Button type="button" variant="outline" size="sm"` labelled "Adicionar Fase" in the `Fases` `CardHeader`'s `flex items-center justify-between` row, `DialogTitle` text "Adicionar Fase", `DialogFooter` with Cancelar/submit. Add `setAddFaseModal(false)` to `onSubmitFase`'s success path. The Fases list table (Fase/Status/Ações columns) stays UNCHANGED inside the `CardContent` — this includes the per-row inline status `&lt;select&gt;` (`faseDraftStatus`) and "Guardar" `Button` (`onUpdateFaseStatus`, `updateFaseStatus.isPending`) in the "Ações" column; do not move, rename, or alter this inline control, only the top-of-tab "Adicionar fase" form moves into the Dialog.
  </action>
  <acceptance_criteria>
    - Source: the Fases tab body has no `grid lg:grid-cols-2` wrapper anywhere in the file (this check now covers the whole file safely, since Task 2 already removed Partes' instance).
    - Source: a `Dialog open={addFaseModal}` exists, gated by `canEditProcessos ? (...) : null`, with `DialogTitle` text "Adicionar Fase".
    - Source: `onSubmitFase`'s success path calls `setAddFaseModal(false)`.
    - Source: the Fases list `&lt;table&gt;` still contains a per-row `&lt;select&gt;` bound to `faseDraftStatus` and a "Guardar" `Button` calling `onUpdateFaseStatus(f.id)`.
  </acceptance_criteria>
  <verify>
    <automated>cd "C:/Users/francisco.horta/Documents/projects/personal/lexcv/web" && pnpm exec tsc --noEmit && grep -c "lg:grid-cols-2" "src/app/(dashboard)/processos/[id]/page.tsx" | grep -qx 0</automated>
  </verify>
  <done>Fases tab shows the list full-width with an "Adicionar Fase" Dialog trigger; the inline per-row status update control is untouched; no grid-2-column layout remains anywhere in the file.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser (authenticated user) → Next.js rewrite → Spring Boot `/api/v1` | New UI affordances (tab buttons, Dialog triggers, print-route link) surface existing already-gated backend operations; no new endpoint is introduced by this plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-84-07 | Elevation of Privilege | New Partes/Fases "Adicionar" Dialog triggers | mitigate | Both Dialog triggers are wrapped in `canEditProcessos ? (...) : null`, exactly preserving the gate that already existed on the pre-refactor inline forms — the JSX reshuffle in Tasks 2/3 must not drop this conditional, verified by acceptance criteria requiring the gate wrapper to be present around each new Dialog. |
| T-84-08 | Elevation of Privilege | "Gerar Termo de Honorários" button (Dados card) | accept | The button only navigates to `/processos/{id}/termo-honorarios`, which independently enforces its own `processos:view` gate (T-84-06, Plan 84-02) — the button itself grants no privilege, so gating it further here would be redundant. |
| T-84-16 | Tampering | 4 new tab-button values added to `TabKey` with `null` placeholder bodies | accept | Clicking a not-yet-implemented tab (Decisões/Factos/Testemunhas/Documentos) in this plan's intermediate state renders nothing (`null`) rather than throwing — this is a transient state only true until Plans 84-04/84-05 land within the same phase, not a shippable end state. |
</threat_model>

<verification>
- `pnpm --dir web exec tsc --noEmit` passes after each task.
- No `lg:grid-cols-2` remains in the file after Task 3.
- Manual/human-check (deferred to phase-level UAT): open a processo's ficha, confirm Juízo/Origem show in Dados, confirm Partes/Fases "Adicionar" now opens a Dialog instead of showing a side-by-side form, confirm the inline Fase status "Guardar" control still works.
</verification>

<success_criteria>
- Dados card shows Juízo/Origem read-only and a conditional Gerar Termo button.
- TabKey and the tab-button row are ready for 4 new tab bodies.
- Partes and Fases both use the Dialog "Adicionar" pattern with the original canEditProcessos gate preserved.
</success_criteria>

<output>
Create `.planning/phases/LEXCV-84-frontend-ui-intake-dados-sub-sec-es-documentos-termo-de-hono/84-03-SUMMARY.md` when done
</output>
