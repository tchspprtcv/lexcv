---
phase: 84
plan: 05
type: execute
wave: 3
depends_on: ["84-04"]
files_modified:
  - web/src/app/(dashboard)/processos/[id]/page.tsx
  - web/src/types/processos.ts
autonomous: true
requirements: [PROC-10, PROC-13]

must_haves:
  truths:
    - "A aba Factos lista os factos do processo ordenados por ordem e permite criar, editar (incluindo o campo ordem) e remover, tudo via Dialog"
    - "A aba Documentos lista os documentos do processo e permite upload (com barra de progresso), download e remoção, mirroring o padrão de Documentos Entregues do Cliente (v2.8)"
    - "Ambas as abas só mostram os botões de escrita a utilizadores com a permissão correta (processos:edit para Factos, documentos:edit para Documentos)"
  artifacts:
    - path: "web/src/app/(dashboard)/processos/[id]/page.tsx"
      provides: "Corpo das abas Factos e Documentos"
    - path: "web/src/types/processos.ts"
      provides: "Comentario desatualizado em FactoUpdateRequest.ordem corrigido para refletir o campo editavel introduzido nesta fase"
  key_links:
    - from: "web/src/app/(dashboard)/processos/[id]/page.tsx"
      to: "useUpdateFacto(id) (web/src/hooks/use-processos.ts)"
      via: "PUT JSON com ordem vinda de um input numerico local, nao do form Zod"
      pattern: "updateFacto\\.mutateAsync"
    - from: "web/src/app/(dashboard)/processos/[id]/page.tsx"
      to: "useUploadDocumentoComProgresso (web/src/hooks/use-documentos.ts)"
      via: "DocumentoUploadPayload com processo_id: id"
      pattern: "processo_id: id"
---

<objective>
Build the final two tab bodies inside `processos/[id]/page.tsx`, replacing the last two `null` placeholders from Plan 84-03: Factos (list/create/edit-with-reorder/delete, PROC-10) and Documentos (upload/list/download/delete, PROC-13, mirroring `ClienteDocumentosEntreguesTab` from v2.8 Phase 79 almost verbatim, scoped by `processo_id` instead of `cliente_id`). This is the last plan in the sequential chain that touches `processos/[id]/page.tsx` — after this plan the file's phase-84 scope is complete.

Factos' `ordem` field is a genuine gap between what Phase 83 built and what this phase's UI-SPEC requires: `factoFormSchema` (Phase 83) only covers `descricao`/`data` — it does not include `ordem`, because at the time Phase 83 was planned, no UI existed yet to decide the reordering mechanism. This phase's CONTEXT.md/UI-SPEC resolved that open question ("Claude's Discretion") by making `ordem` an editable numeric field inside the "Editar Facto" Dialog. This plan manages `ordem` via plain local component state (not the Zod-validated form) rather than modifying `factoFormSchema`, to avoid re-touching a Phase-83-owned schema file for a single edit-only field; it also fixes the now-stale docblock comment on `FactoUpdateRequest.ordem` in `types/processos.ts` (84-PATTERNS.md Flagged Risk 2), which currently claims "no form collects this field yet" — a claim this plan makes false.

Purpose: complete PROC-10 (list/edit/remove/reorder Factos) and PROC-13 (Documentos tab).
Output: `processos/[id]/page.tsx` with fully working Factos and Documentos tab bodies; `types/processos.ts` docblock correction.
</objective>

<execution_context>
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/francisco.horta/Documents/projects/personal/lexcv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/LEXCV-84-frontend-ui-intake-dados-sub-sec-es-documentos-termo-de-hono/84-CONTEXT.md
@.planning/phases/LEXCV-84-frontend-ui-intake-dados-sub-sec-es-documentos-termo-de-hono/84-UI-SPEC.md
@.planning/phases/LEXCV-84-frontend-ui-intake-dados-sub-sec-es-documentos-termo-de-hono/84-PATTERNS.md
@.planning/research/PITFALLS.md
@web/src/app/(dashboard)/clientes/[id]/page.tsx

<interfaces>
From web/src/hooks/use-processos.ts (already built, Phase 83):
useFactos(id) -> useQuery<Facto[]>, queryKey ["processos","factos",id]
useAddFacto(id) -> useMutation, payload FactoCreateRequest = { descricao: string; data?: string } — backend recomputes ordem server-side on create (Phase 81, synchronized max+1 per processo_id), so the create form never sends ordem.
useUpdateFacto(id) -> useMutation, args { factoId: number; payload: FactoUpdateRequest } where FactoUpdateRequest = { descricao: string; data?: string; ordem: number } — ordem IS required on update, this is the reordering entry point.
useDeleteFacto(id) -> useMutation, arg factoId: number.

From web/src/types/processos.ts:
Facto { id: number; processoId: string; descricao: string; data?: string; ordem: number }
FactoUpdateRequest currently has a stale docblock comment above `ordem` (lines ~196-198) claiming "Must be sourced from the current Facto.ordem value, never from user input — no form collects this field yet ... reusing a stale value would silently corrupt ordering (WR-02)." — this comment predates this phase's decision to add an editable ordem field; it must be corrected, not left contradicting the code this plan adds.

From web/src/schemas/processos.ts (already built, Phase 83):
factoFormSchema = { descricao: z.string().trim().min(1, ...), data: optionalTrimmedString } -> type FactoFormValues — deliberately has NO ordem field (Phase 83 predates this UI decision); this plan does not modify this schema, `ordem` is managed via local React state instead, only merged into the payload at submit time for the update branch.

From web/src/hooks/use-documentos.ts (already built, no changes needed):
useDocumentos(filters: { processo_id?: string; cliente_id?: string }) -> useQuery<Documento[]>
useUploadDocumentoComProgresso(options?: { onProgress?: (pct: number) => void }) -> useMutation<DocumentoUploadResponse, unknown, DocumentoUploadPayload>
useDeleteDocumento(id: string) / useDownloadDocumento(id: string) — both take the Documento's own `id`, no processo/cliente scoping needed at the mutation level (ownership already enforced server-side).

From web/src/types/documentos.ts:
Documento { id, tenant_id, processo_id?, cliente_id?, tipo?, confidencialidade?, versao, nome, filename, content_type, size, created_at }
DocumentoUploadPayload { file: File; processo_id?: string; cliente_id?: string; tipo?: string; confidencialidade?: string; replace_id?: string; nome?: string }

CONFIRMED (read directly from backend/src/main/java/com/lexcv/controllers/ResourceController.java lines 2466-2483 and backend/src/main/java/com/lexcv/models/Documento.java): `GET /processos/{id}/documentos` and `GET /clientes/{id}/documentos` both call `documentoRepository.findByTenantIdAndProcessoId(...)`/`findByTenantIdAndClienteId(...)` and serialize the SAME raw `Documento` JPA entity — the entity's actual field names are `tamanho` (Long) and `createdAt` (LocalDateTime), which do NOT match the frontend `Documento` type's declared `size`/`created_at` fields. This is the exact WR-01 workaround already applied in `ClienteDocumentoEntregueRow` (`clientes/[id]/page.tsx` lines 1404-1411) — it transfers as-is to the processo-scoped endpoint since both endpoints serialize the identical entity shape. Reuse the workaround verbatim, do not widen the shared `Documento` type (also consumed by unrelated `/documentos` pages outside this phase's scope).

Component to mirror near-verbatim: `ClienteDocumentosEntreguesTab` + `ClienteDocumentoEntregueRow` (`clientes/[id]/page.tsx` lines 1223-1468, already read in full) — including `FileDropZone` (`@/components/shared/file-drop-zone`, props `onFileChange`/`onClear`/`accept`/`disabled`/children), the `tipo` datalist built from existing documentos' distinct `tipo` values, the upload-progress bar, and `formatDocumentoSize`/`formatDocumentoDate` local helpers (not shared/exported anywhere — define local equivalents in this file, same implementation).

Copywriting (from 84-UI-SPEC.md, use verbatim):
Factos: tab label "Factos", CTA "Adicionar Facto", Dialog titles "Adicionar Facto"/"Editar Facto", empty "Nenhum facto registado.", list error "Não foi possível carregar os factos deste processo.", create/update error fallback "Erro ao adicionar facto"/"Erro ao atualizar facto", success toast "Facto adicionado com sucesso."/"Facto atualizado com sucesso.", delete confirm "Apagar este facto?", delete success "Facto apagado com sucesso.", delete aria-label "Apagar facto".
Documentos: tab label "Documentos", CTA "Adicionar Documento", Dialog title "Adicionar Documento", empty "Nenhum documento registado.", list error "Não foi possível carregar os documentos deste processo.", upload error fallback "Erro ao fazer upload", upload success "Documento enviado com sucesso.", delete confirm "Apagar este documento?", delete success "Documento apagado com sucesso.", delete aria-label "Apagar documento".
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Factos tab — list ordered by ordem, Adicionar, Editar (with ordem), Apagar; fix stale docblock</name>
  <files>
    web/src/app/(dashboard)/processos/[id]/page.tsx
    web/src/types/processos.ts
  </files>
  <read_first>
    web/src/app/(dashboard)/processos/[id]/page.tsx
    web/src/hooks/use-processos.ts
    web/src/schemas/processos.ts
    web/src/types/processos.ts
  </read_first>
  <action>
    In `web/src/types/processos.ts`, replace the stale comment above `FactoUpdateRequest.ordem` (currently claiming no form collects this field and that a stale value would corrupt ordering) with a comment stating that `ordem` is user-editable via the "Editar Facto" Dialog on `processos/[id]/page.tsx` (Phase 84) as the deliberate reordering mechanism for this milestone — sourced from a local numeric input pre-filled with the current `Facto.ordem` when the edit Dialog opens, submitted verbatim on save.

    In `web/src/app/(dashboard)/processos/[id]/page.tsx`: add to the `@/hooks/use-processos` import block: `useFactos, useAddFacto, useUpdateFacto, useDeleteFacto`. Add to the `@/schemas/processos` import block: `factoFormSchema, type FactoFormValues`. Add `type FactoUpdateRequest` (and `FactoCreateRequest` if not already imported) to the `@/types/processos` type-only import list.

    Declare `factos = useFactos(id)`, `addFacto = useAddFacto(id)`, `updateFacto = useUpdateFacto(id)`, `deleteFacto = useDeleteFacto(id)`; `addFactoModal`/`setAddFactoModal`, `editingFactoId`/`setEditingFactoId<number|null>`, `factoServerError`/`setFactoServerError`; `factoForm = useForm<FactoFormValues>({ resolver: zodResolver(factoFormSchema), defaultValues: { descricao: "", data: undefined } })`; and a plain (non-Zod) `const [factoOrdemDraft, setFactoOrdemDraft] = React.useState(1);` used only while `editingFactoId !== null`.

    `onOpenAddFacto` resets `factoForm` to blank defaults, sets `editingFactoId(null)`, opens the Dialog. `onOpenEditFacto(f: Facto)` resets `factoForm` with `{ descricao: f.descricao, data: f.data }`, sets `setFactoOrdemDraft(f.ordem)`, sets `editingFactoId(f.id)`, opens the Dialog. `onSubmitFacto(values: FactoFormValues)`: if `editingFactoId !== null`, call `updateFacto.mutateAsync({ factoId: editingFactoId, payload: { descricao: values.descricao, data: values.data, ordem: factoOrdemDraft } satisfies FactoUpdateRequest })` and toast "Facto atualizado com sucesso."; otherwise call `addFacto.mutateAsync(values satisfies FactoCreateRequest)` and toast "Facto adicionado com sucesso."; close the modal on success. Use the same try/catch + server-error-fallback shape as Decisões/Testemunhas from Plan 84-04 ("Erro ao atualizar facto"/"Erro ao adicionar facto"). `onDeleteFacto(factoId: number)` confirms with `window.confirm("Apagar este facto?")`, calls `deleteFacto.mutateAsync(factoId)`, toasts "Facto apagado com sucesso.", error fallback "Erro ao apagar facto".

    Replace the `tab === "factos" ? null` placeholder with a `Card` (`CardTitle` "Factos", `canEditProcessos`-gated Dialog trigger "Adicionar Facto", `DialogTitle` `editingFactoId === null ? "Adicionar Facto" : "Editar Facto"`). Dialog form: "Descrição" textarea (`{...factoForm.register("descricao")}`, required, inline error), "Data" date input (`{...factoForm.register("data")}`), and — only when `editingFactoId !== null` — an "Ordem" field: a numeric input bound to `factoOrdemDraft`/`setFactoOrdemDraft` (`type="number" min={1}`), entirely absent from the create form since ordem is server-computed on create. `factoServerError` inline; `DialogFooter` Cancelar/Confirmar (disabled while pending).

    `CardContent` renders `factos.data` sorted by `ordem` ascending client-side before mapping (the backend does not guarantee sort order in the list response — do not trust array order as-is) in a raw `<table>` with columns "Ordem" | "Descrição" | "Data" | "Ações" (Ordem cell shows `f.ordem`; Data cell via `formatDate(f.data)`), list-state ternary using "A carregar...", "Não foi possível carregar os factos deste processo.", "Nenhum facto registado.", Ações cell with the same Editar-text-link + "✕" `aria-label="Apagar facto"` pattern used for Decisões/Testemunhas, gated by `canEditProcessos`.
  </action>
  <acceptance_criteria>
    - Source: `web/src/types/processos.ts`'s `FactoUpdateRequest.ordem` docblock no longer contains the phrase "no form collects this field yet".
    - Source: `factoOrdemDraft` state exists and is only rendered as an input when `editingFactoId !== null` (create form has no ordem input).
    - Source: `onSubmitFacto`'s update branch includes `ordem: factoOrdemDraft` in the `FactoUpdateRequest` payload; the create branch's `FactoCreateRequest` payload has no `ordem` key.
    - Source: the Factos table renders from a client-side sort call keyed on `ordem` (e.g. `.sort((a, b) => a.ordem - b.ordem)`), not the raw unsorted `factos.data`.
    - Source: delete button has `aria-label="Apagar facto"`, guarded by `window.confirm("Apagar este facto?")`.
  </acceptance_criteria>
  <verify>
    <automated>cd "C:/Users/francisco.horta/Documents/projects/personal/lexcv/web" && pnpm exec tsc --noEmit && [ -z "$(grep "no form collects this field yet" "src/types/processos.ts")" ]</automated>
  </verify>
  <done>Factos tab lists (sorted by ordem), creates, edits (with an editable ordem field), and deletes factos, gated by canEditProcessos; the stale FactoUpdateRequest.ordem docblock is corrected.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Documentos tab — mirror ClienteDocumentosEntreguesTab, scoped by processo_id</name>
  <files>web/src/app/(dashboard)/processos/[id]/page.tsx</files>
  <read_first>
    web/src/app/(dashboard)/processos/[id]/page.tsx
    web/src/app/(dashboard)/clientes/[id]/page.tsx (ClienteDocumentosEntreguesTab / ClienteDocumentoEntregueRow, lines 1223-1468)
    web/src/hooks/use-documentos.ts
    web/src/types/documentos.ts
    web/src/components/shared/file-drop-zone.tsx
  </read_first>
  <action>
    Add to the imports: `FileDropZone` from `@/components/shared/file-drop-zone`; `useDocumentos, useDeleteDocumento, useDownloadDocumento, useUploadDocumentoComProgresso` from `@/hooks/use-documentos`; `type Documento` from `@/types/documentos`.

    Near the top of `ProcessoDetailContent`, call `const permissions = usePermissions();` (re-invoking the hook inside this component — cheap, TanStack-Query-cached under the same permissions key already used by the parent `ProcessoDetailPage`) and derive `const canEditDocumentos = permissions.can.edit("documentos");` — this is a DIFFERENT permission scope from `canEditProcessos`, matching the `clientes/[id]/page.tsx` precedent exactly (documentos:edit gates upload/delete, independent of processos:edit).

    Define two local helper functions at file scope (module-level, outside `ProcessoDetailContent`, same as `formatDate`/`formatDateTime`): `formatDocumentoSize(bytes: number): string` and `formatDocumentoDate(v: string | undefined): string`, copied with identical logic from `clientes/[id]/page.tsx`'s versions (bytes-to-B/KB/MB, `pt-CV` date formatting with `"—"` fallback).

    Replace the `tab === "documentos" ? null` placeholder with a new module-level function component `ProcessoDocumentosTab({ processoId, canEditDocumentos }: { processoId: string; canEditDocumentos: boolean })`, invoked as `<ProcessoDocumentosTab processoId={id} canEditDocumentos={canEditDocumentos} />` from the ternary branch. Inside this component: `const list = useDocumentos({ processo_id: processoId });`, local state `addDocumentoModal`, `novoTipo`, `novoFicheiro`, `progresso`, `uploadError` (identical shape to `ClienteDocumentosEntreguesTab`), `const upload = useUploadDocumentoComProgresso({ onProgress: setProgresso });`, a `tipoOptions` memo derived from `list.data`'s distinct `tipo` values, and `onConfirmarUpload` calling `upload.mutateAsync({ file: novoFicheiro, tipo: novoTipo.trim(), processo_id: processoId })` (note: `processo_id`, not `cliente_id`) with success toast "Documento enviado com sucesso." and error fallback "Erro ao fazer upload".

    Render a `Card` with `CardTitle` "Documentos" (or, matching the Cliente analog's `CardContent`+`h4` shell if that reads more consistently with this file's other Card-based tabs — use `CardHeader`/`CardTitle` to match this file's established Partes/Fases/Decisões/Factos/Testemunhas shell, not the Cliente page's `h4` variant, for intra-file consistency) with a Dialog "Adicionar Documento" trigger (gated `canEditDocumentos`) containing a `FileDropZone` (`onFileChange={setNovoFicheiro}`, `onClear={() => setNovoFicheiro(null)}`, same `accept` list as the Cliente analog, `disabled={upload.isPending}`), a `tipo` text input with a `<datalist>` sourced from `tipoOptions`, the upload progress bar (rendered when `progresso !== null`), `uploadError` inline, and `DialogFooter` Cancelar/Confirmar (`disabled={!novoFicheiro || upload.isPending}`).

    `CardContent` list-state ternary: loading "A carregar...", error "Não foi possível carregar os documentos deste processo.", empty "Nenhum documento registado.", else a `<ul className="space-y-1">` mapping each `Documento` to a `ProcessoDocumentoRow` component (new, module-level, mirrors `ClienteDocumentoEntregueRow`): reads `tamanho`/`createdAt` off the raw entity via the same `documento as unknown as { tamanho?: number; createdAt?: string }` workaround (confirmed transferable per the CONFIRMED note in this plan's interfaces section), renders nome/tipo/size/date, a "Download" text button (`useDownloadDocumento(documento.id)`, opens `res.url` in a new tab), and — gated by `canEditDocumentos` — a "✕" delete button (`useDeleteDocumento(documento.id)`, `window.confirm("Apagar este documento?")`, toast "Documento apagado com sucesso.", error fallback "Erro ao apagar documento", `aria-label="Apagar documento"`).
  </action>
  <acceptance_criteria>
    - Source: `useDocumentos({ processo_id: processoId })` (not `cliente_id`) is the list query in the new component.
    - Source: `upload.mutateAsync` call includes `processo_id: processoId`.
    - Source: `canEditDocumentos` is derived from `permissions.can.edit("documentos")`, distinct from `canEditProcessos`, and gates both the upload Dialog trigger and the delete button.
    - Source: the delete button has `aria-label="Apagar documento"` and is guarded by `window.confirm("Apagar este documento?")`.
    - Source: `formatDocumentoSize`/`formatDocumentoDate` are defined locally in this file (not imported from `clientes/[id]/page.tsx`, which does not export them).
    - Behavior: `pnpm --dir web run build` succeeds with all 4 new tabs (`decisoes`/`factos`/`testemunhas`/`documentos`) fully wired — no `null` placeholders remain in the tab-content ternary chain.
  </acceptance_criteria>
  <verify>
    <automated>cd "C:/Users/francisco.horta/Documents/projects/personal/lexcv/web" && pnpm run build</automated>
  </verify>
  <done>Documentos tab uploads (with progress), lists, downloads, and deletes documents scoped to this processo, gated by documentos:edit; production build passes with the full phase-84 tab set wired into processos/[id]/page.tsx.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → Next.js rewrite → Spring Boot `/api/v1/processos/{id}/factos`, `/api/v1/documentos/upload`, `/api/v1/documentos/{id}/download` | Factos CRUD reuses the Phase 81 double-checked endpoints; Documentos reuses the generic, already-hardened (Phase 79) upload/download/delete endpoints |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-84-12 | Elevation of Privilege | Facto Adicionar/Editar/Apagar affordances | mitigate | Dialog trigger and row Editar/Apagar actions wrapped in `canEditProcessos ? (...) : null`, identical to Decisões/Testemunhas/Partes/Fases. |
| T-84-13 | Elevation of Privilege | Documentos tab Adicionar/Apagar affordances | mitigate | Gated by `canEditDocumentos = permissions.can.edit("documentos")`, a distinct scope from `processos:edit`, matching `ClienteDocumentosEntreguesTab`'s precedent exactly — a user with `processos:edit` but not `documentos:edit` sees the Documentos list but cannot upload or delete. |
| T-84-14 | Information Disclosure | Document download link | accept | `useDownloadDocumento` calls the existing tenant/ownership-checked backend endpoint (`GET /documentos/{id}/download`, Phase 79 fix already closed the ownership-validation gap for this path) and opens a short-lived pre-signed URL — no new surface introduced. |
| T-84-17 | Tampering | Client-side `.sort()` on Factos before render, using server-returned `ordem` values that could in theory collide (Pitfall 9) | accept | Sort stability for tied `ordem` values is a backend-owned concern (already documented in PITFALLS.md Pitfall 9, resolution left to the Phase 81 backend which the current milestone does not require to compact/dedupe `ordem` on delete); the frontend sort is display-only and does not write back an implied order. |
</threat_model>

<verification>
- `pnpm --dir web exec tsc --noEmit` passes after Task 1.
- `pnpm --dir web run build` passes after Task 2 — this is the final gate for the whole `processos/[id]/page.tsx` sequential chain (Plans 84-03/84-04/84-05).
- Manual/human-check (deferred to phase-level UAT): reorder two Factos via the Editar Dialog's Ordem field and confirm the table re-sorts; upload a document via the new Documentos tab and confirm it also appears in the existing `/documentos` generic list page (shared backend list).
</verification>

<success_criteria>
- Factos tab supports list/create/edit(with reorder)/delete.
- Documentos tab supports upload/list/download/delete scoped to the processo.
- No `null` placeholders remain in the `processos/[id]/page.tsx` tab-content ternary; `pnpm build` passes.
</success_criteria>

<output>
Create `.planning/phases/LEXCV-84-frontend-ui-intake-dados-sub-sec-es-documentos-termo-de-hono/84-05-SUMMARY.md` when done
</output>
